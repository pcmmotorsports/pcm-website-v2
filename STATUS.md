# STATUS.md
> PCM Phase 1 SSoT. 衝突仲裁: STATUS.md > NORTHSTAR > 其他 md > 對話歷史.

## 當前狀態
**Phase:** Phase 1 / **Milestone:** **M-4a 後台第一期**(訂單+客戶+SSO;admin.pcmmotorsports.com LIVE)進行中。
**M-4a V 線 ✅ 全收工上 production**(V-1~V-3b:購物車/PDP 選車 §7 保守適用比對 + order_items.vehicle_snapshot 持久化硬閘〔create_order RPC 相鄰〕+ admin 訂單列表車款欄直出 + taxonomy 車款節點 NFKC 統一;三模型鏈 Codex 寫→Opus 審+prod 交易模擬→Fable 終審)。連帶 **P4+S4 目錄年份卡片 + priceMax=0 修**隨本次 FF 一併上線=/products preview gate 已解除(剩 Sean 正式站肉眼驗)。前一主線 **M-3 結帳 3DS 重設計整線 ✅ 全落地**(flag 全 false、🔴 prod checkout 仍不可開=真刷卡待 sandbox 3DS E2E + Sean 驗收)。V 線逐片流水+M-3 巨段已歸 PROGRESS.md。
**②Email 通知片實作中:E1a ✅ 全關(code `8b5bc74`+migration 已 apply+prod 驗證 PASS+已推 origin/dev)**。硬閘三步 07-17 走完:Sean db push → 值班台缺位、Sean 指示改由 fresh 驗證視窗開 Supabase MCP 跑 migration 檔尾清單(唯讀 catalog 包+寫入型合成資料包〔單一 DO block+結尾無條件 RAISE=單語句原子 rollback、零留痕物理保證〕;7/7c 依總則 B 免跑=Fable disposable PG 已證)→ **全 PASS、residual_rows=0**。E1a 經 **8 輪審查**(code-reviewer×2、codex 關卡2×5、Fable×3;Sean 07-17 明示 override codex 2 輪上限)。**E1b(port/adapter/組裝層+單測)✅ 已推 `7b30ced`**(Sean 拍 A+指定二審;雙審=code-reviewer R1 FAIL 1 Critical→修→R2 PASS + codex 關卡2 R1 FAIL 6 must-fix→全修→R2 PASS、2 nit 清畢)。**E1c(Resend 429 三分+退避合約)✅ 兩片全收工已推**(E1c-1 `e90cbd3` 程式 / E1c-2 `55365e6` 合約落 migration §⑨〔純註解零 DDL〕;Sean 07-17 拍 Q1-Q11;🔴 誠實揭示兩項=①429 body 有無 `name` 兩官方 SDK 不一致、無法實測 → 若無則所有 429 白等 24h〔Q11=A 明示接受、精準版 backlog #285〕②死信無人工重送工具=backlog #286)。**E2a 拆三片(Sean 07-17 Q12=A;原一片實估 95 分超鐵則 4)=E2a-a 零件層 → E2a-b 邏輯層 → E2a-c 接線層;Q13=A 三片皆不做告警**(全歸 E2a-2 獨立管道)。**E2a-a ✅ 已推(`6a8b155`;三審跑滿、Sean 拍 Q14=A 收案)** → **E2a-b(退避政策模組+sweeper use-case)✅ code 收工=本 commit、未推**(code-reviewer R1 FAIL 2 must-fix→修→R2 fresh PASS;codex 關卡2 R1 FAIL 4 must-fix+1 nit→全修→R2 PASS 五條銷案零新 findings)。~~下一=E2a-c~~ **E2a-c ✅ 已收工**。🔴 **2026-07-18 方向轉折:`E2a-2` 作廢、整線改走「通知線修正版 D′」**(Sean 拍 Q1-Q4 + 收案三拍板;真權威 plan = `docs/specs/2026-07-18-b0-order-notification-email-prd.md`、拍板 memory = `project_m4a-line-multichannel-pivot`)。🔴 **誠實現況**:sweeper 骨幹雖已在,但**寫 outbox 的觸發點從未接上(prod `email_outbox` 實查 0 行)、五訊號 dead-man 監控全 repo 查無實作** → 整線距「客人真的收得到信」仍有 §6 八項上線 gate。
**🔴 D′ 線進度(真權威 = `docs/specs/2026-07-18-b0-order-notification-email-prd.md`,上列 E1a-E2a-c 屬**作廢前**的 E 線歷史、僅供追溯)**:**B-1(`orders.notification_email` 加欄 + 六條件 CHECK)✅ 已 apply 上 production**;**B-2(`create_order` 8→9 參)✅ 已 apply 上 production 並通過路徑② 驗收**(四輪對抗審查、findings 21 條全修、Fable 判 `GO`;`db push` 輸出含斷言矩陣 NOTICE)。驗收:簽章總數 1 且為 9-param、完整指紋 `850e2e3c…` == apply 前預測值、六角色 ACL 矩陣 fail-closed、**`schema_migrations` 已記 `20260719120000`(M-5 裂縫未發生)**、PostgREST smoke 三案(9 參/8 參皆 `42501` 非 `PGRST202`、對照組 `PGRST202`)證明 schema cache 已重載且無 overload、資料面零留痕(orders 仍 30、產號序列未跳)。詳 plan §16。下一 = **B-3**。
**下一階段=M-4a 第一期收口(Sean 07-16 拍優先序:①客戶線→②Email→③最新商品、④小件穿插)**。**①客戶線五片 ✅ 全收案上 production**(列表/明細-a `30659b8`/明細-b `675b949`/儲值金編輯 `afea2b7`/tier 編輯 `13ce3a9`;migrations 至 `20260717010000` 全 apply、Sean 肉眼驗過)。**②Email 通知片=規劃 ✅ + 動工 gate 已解除 + E1a ✅ code 收工(見上)**(規劃史:v1 雙審 FAIL→Sean 四拍板→**架構改用 Supabase pg_cron**→v2→R2 雙審 FAIL→v3 銷案→Sean 拍 S1/S2/S3)。交接=`docs/handoff/2026-07-17-m4a-email-e1c-handoff.md`(當次快照;⚠️ 07-16 kickoff 的 §5 拆片表/§7 部分字面已被 E1a 超越);**plan 真權威=`docs/specs/2026-07-16-m4a-email-notify-plan.md` v3.2**(E2a/E4 開工前必讀 §3.4/§3.5-1/§3.5-3/§3.5b/§3.6/§4/§7/§9)。
**Branch:** dev

## 最後更新
2026-07-20(**本 commit;#288-a production build E2E 守門、非 M-4a 主線**)— Claude Code 視窗。**兩個月來第一次讓 e2e 進 CI**。🔴 **根因兩層**:①2026-07-19 三個目錄 bug 只在 `next build && next start` 重現、`next dev` 看不到,而既有 playwright 跑 dev(`playwright.config.ts:28`)②**更關鍵**:Playwright 2026-05-27 引進(`435baa4`)後 **config 建立至今零修改、e2e 停在 2 檔、從未進 CI**,當初註解自寫的「CI gate 留後續 slice(T-2+)」**兩個月沒發生** → 三個 bug 全由 Sean 逛正式站發現。落地:`playwright.prod.config.ts`(port 3200 / **`reuseExistingServer:false`** 硬性〔reuse 命中則 build 不跑 → 突變自驗會連到舊 server、該紅不紅〕/ `trace: retain-on-failure`〔`retries:0` 配 `on-first-retry` = 永不產生 trace〕)+ `scripts/e2e-prod-preflight.mjs` + `e2e-prod/runner-smoke.spec.ts` + `.github/workflows/e2e-prod.yml` + vitest `**/e2e-prod/**` exclude + `test:e2e:prod` script。🔴 **Sean 07-20 拍 A 推翻 plan 原「不接 CI」**(理由=沒人跑的守門等於沒做);**不打 Vercel 預覽網址**(實測 302 → `vercel.com/sso-api`,繞過需 Protection Bypass for Automation、**Hobby 是否可用查證未果**;而本機 build 冷啟實測僅 19s)。🔴 **code-reviewer R1 FAIL 6 must-fix,其中兩條合起來讓本片自己成為假綠**:①preflight 只驗鍵名,而 GitHub secret 未設定時 `${{ secrets.X }}` 內插成**空字串**、env var 仍建立 → 通過 ②smoke 只驗首頁,而首頁三個資料源全自帶 catch fallback(`lib/products.ts:249-256`/`:482-489`/`:535-542` + `page.tsx:85-88`)→ 零資料庫仍 200 + 骨架可見 → 通過。**⇒ CI 沒設 secrets 會全綠**,正是本片要防的東西。修法:preflight 改驗**非空值**(🔴 web Codex R1 又抓 MF1:自行解析 env 檔與 Next 優先序不符 → **重寫為直接呼叫 `@next/env` loadEnvConfig**,與 next build/start 同一套解析;另 MF2-MF4=plan 矛盾/SSoT 漂移/字面範圍過大,全修)、smoke 改打 `/products`(force-dynamic;🔴 **它一樣有 fallback**〔`products.ts:387-390` 回空 + error、HTTP 仍 200〕,防線是斷言本身)並斷言卡片數 >0 且 `.pp-count` >0。**反假綠實證**:`NEXT_PUBLIC_SUPABASE_URL` 指向不存在主機 → **轉紅 exit 1**(舊版同情境為綠);preflight 三負向(空字串 / `KEY=""` / 鍵名拼錯)皆擋;vitest exclude **反證**(拿掉 → 誤抓並炸 `Playwright Test did not expect test() to be called here`)。驗證:三綠 typecheck 8/8 + lint 10/10 + root test **231 檔 2540 測 +1 todo** + production E2E **1 passed(冷快取含完整 build,修後複驗 30.9s)**。🔴 **Sean 待辦**:GitHub Secrets 設 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`(兩者 `NEXT_PUBLIC_`、本即隨 client bundle 公開,非新增暴露面);**未設 → CI 硬紅**(刻意不 skip)。plan 真權威 = `docs/specs/2026-07-20-catalog-prod-build-e2e-plan.md` **v3.2**(關卡1 **三輪**雙審:R1/R2 皆 NO-GO、R3 Fable GO + codex NO-GO 4 條 → 全採納)。🔴 **流程自省**:Sean 拍板接 CI 後我**先寫 workflow 才回頭改 plan**,違反「拍板即落檔」、被 R1 判與 plan 打架。下一 = #288-b(資料合約 + mobile device project)。

2026-07-20(前一 commit `799a733`;前台小件、非 M-4a 主線)— Claude Code 視窗(**手機「選擇車款」死碼 chip 移除 + 抽屜車輛 tab 改名**;Sean 兩拍板,皆為**授權覆蓋 design**、已入 manifest `business_overrides`)。①**刪 `.cft-mobile-bar` 整組**:CascadeFilterTop markup + 無消費者的 `onOpenDrawer` prop + `vehShort` 衍生值、`filter-cascade.css` 四段樣式(`.cft-mobile-bar`/`-chip`/`-eyebrow`/`-name`)+ `:147` 隱藏規則、`filter-responsive.css` `:62`/`:109` 兩條隱藏規則;ProductsPage 同步移除傳入的 `onOpenDrawer`(`setDrawerOpen` 仍由 MobileFab 消費、非孤兒)。🔴 **根因=design 端本身即死碼**(`design-reference/styles/filter-top.css:322-323` base `display:none`、同檔 `:372`/`:476` 亦 none、**全檔無顯示規則**)→ storefront 照搬正確(鐵則 1),但成品是使用者永遠看不到的元件;production build 瀏覽器實測桌面 1440 與手機 390 皆 `0×0`。**留著的實害已實現**:#289 誤把它當唯一車款入口 → 把「`?page=3` 時選車」測試案例延期,**該延期理由 07-20 經實測證實不成立**(桌面 `.cft-bar` 可見可用、1440×64、三 combobox 展開 54 選項)→ 連帶更正 4 處錯誤字面(本檔下一條 07-19 `49afb07` 條目內文、backlog #288 與 #289 兩條目、三 bug 合併交接檔;🔴 依本檔「最近 3 commit」欄自立之法**不寫自指行號**)、新開並收案 backlog **#290**。手機車款入口不減=`.cft-bar` 三層 VehicleSelect + 「篩選」FAB → 抽屜 tab。②**FilterDrawer 車輛 tab「依車輛搜尋」→「選擇車款」**(Sean 直接指示;Header/FilterSide/FilterTop 三處同字面**刻意不動**=不同語境〔導覽列/側欄 Accordion/僅 dev-preview 掛載〕);`FilterDrawerVehicleTab` 檔頭註解同步。驗證:三綠 typecheck 8/8 + lint 10/10 + build 綠 + full vitest **231 檔 2540 測 +1 todo**;**突變自驗**(改回舊字面→轉紅、還原→綠,綁 `.fd-tab` 元素非僅驗字串);production build 實測 390px chip 已不在 DOM、抽屜首個 tab 顯「選擇車款」。審鏈=code-reviewer **R1 FAIL 3 must-fix**(manifest `last_modified_commit/date` 未更新〔`--validate` 只驗可達性擋不住〕+ STATUS 7 欄未同 commit〔**同款前科:前一片 R1 同一條**〕+ backlog #290 標完成卻留「待確認/估時/依賴」自我矛盾〔`feedback_claimed-sync-but-only-patched-touched-lines` 第 10+ 次復發〕)→ 本次全修 + 2 nit 順清。🔴 **鐵則 6**:`ProductsPage.tsx` 410 行仍 >400(非本片引入、本片 −1 行;拆檔另議)。🔴 **鐵則 12 判定=未觸發**(零 order/payment/schema/RLS/migration/tier/經銷價;code-reviewer 覆核同意)。

2026-07-19(前一 commit `49afb07`;插隊 bug 修復第三片、非 M-4a 主線)— Claude Code 視窗(**深連結頁碼被吃掉修復 = backlog #289 ✅**〔前一片 `630b7a6` 的 R2 抓出的殘餘缺陷,Sean 拍 Q10=B 當場接著修〕。症狀:帶 `?page=N` 的深連結進站 → `useDeepLinkRestore` 的 restore dispatch 讓篩選指紋由空變非空 → 被 `filtersChanged` 誤判為「使用者改篩選」→ 刪掉 page;**實測不會自癒**(`useBrowseUrlSync` deps `[currentPage,sort,perPage,router]` 此時全未變、effect 不重跑)→ 終態=內容第 1 頁 + 分頁 UI 停在舊頁碼,與最初回報的症狀同形。已對照 `61f45b6` 確認為既有 bug。修法=刪 page **之前**先用「尚未刪 page」的版本比對,若已等於當前 URL 代表 state 只是**剛追上 URL**(還原波)→ 直接收手;此判準不需 skip-once 旗標,故不與 `usePageResetOnFilterChange` 的 `skipOnceRef` 互搶(兩 hook effect 執行序 catalog sync 在前,共用旗標會被先吃掉、反使 `setPage(1)` 誤觸發——**推理,該方案未實作故未實測**)。🔴 安全前提=`params` 為 `window.location.search` 原樣拷貝、本 effect 只改寫 pbrand/category/price/pmin/pmax 五軸,外來鍵兩側恆等 → 該比對 ⟺「五軸已與 state 一致」;**不得**在該行之前再新增 `params.set/delete`。驗證:production build 實測 `/products?pbrand=akrapovic&page=2` 進站 → URL 保持完整、第一張卡 `akrapovic-sm-k10so1t`(**≠** 第 1 頁的 `akrapovic-s-d9so14-hifft`)= 真的停在第 2 頁;回歸=翻第 2 頁 ✅ / 第 2 頁勾品牌回第 1 頁 648 ✅ / 加勾 1103 ✅ / 取消非最後值 455 ✅ / 清空 12793 ✅;突變=拿掉 early return → 案例⑩紅;三綠 + 231 檔 **2539 測**全過。審鏈=code-reviewer R1 FAIL 2 must-fix(測試檔 docblock 過時宣稱「本檔不涵蓋還原波」未同步 + STATUS 未同 commit)→ 本次修畢;核心邏輯 reviewer 逐條擊破失敗、判定正確。🔴 **鐵則 6**:本檔 435 行、hook 檔(理由同前一片)。📄 **三片合併交接檔** = `docs/handoff/2026-07-19-catalog-url-state-three-bugs-handoff.md`(含四道判斷的依賴關係、安全前提、升級 Next 須重跑的實測清單;⚠️ CURRENT.md 未動、仍屬 M-4a 主線)。🔴 **未實測項(誠實揭示)**:#289 條目原要求一併實測的「`?page=3` 時**選車**」路徑**未驗證**——當時判定「`/products` 桌面版無車款選單」(「選擇車款」為 mobile-only chip、桌面實測寬高 0,須走 mobile drawer);🔴 **2026-07-20 production build 實測推翻此判定**:桌面車款選單 `.cft-bar` 可見可用(1440×64、三個 combobox、點擊展開 54 個選項),當時量到寬高 0 的是另一元件 `.cft-mobile-bar`(全尺寸皆 `display:none` 的死碼、承自 design-reference,見 backlog #290)→ 當時桌面即可實測、延期非必要;不硬湊,已明寫未驗證並把該案例**移交 backlog #288**(E2E 守門、桌面+mobile 各測一次)。讀碼評估此路徑不致造成內容與頁碼不一致(`usePageResetOnFilterChange` key 含整個 cascade、選車必 `setPage(1)`),最壞為多一次帶舊 page 的往返——**此評估同樣來自讀碼、未經實測**。)
2026-07-19(前一 commit `630b7a6`;插隊 bug 修復第二片、非 M-4a 主線)— Claude Code 視窗(**前台商品目錄「分頁失效」修復**〔驗證品牌片時發現;**已對照 `61f45b6` 實測確認為既有 bug、非品牌片引入**〕。症狀:點第 2 頁 → 分頁 UI 跳到第 2 頁,但網址與商品內容退回第 1 頁;目錄 512 頁形同翻不動。根因(單元測試坐實):`useCatalogFilterUrlSync` 的 effect deps 含 `restoreSources`(ProductsPage 的 `useMemo(...,[categories,brands])`)→ server 每回一次新 props 就換 identity → effect 重跑 → 舊版**無條件** `params.delete('page')` 把使用者剛翻到的 `?page=2` 洗掉。修法兩處:①`delete('page')` 改為只在**篩選值指紋**(category/brands/price/priceRange)真變動時執行 ②URL 比較改為**正規化**(排序後比對 entries)——重建 pbrand 必然把它排到尾端,字面比較會讓純順序差異也多送一次導覽 + 多查一次全型錄。驗證:production build 實測分頁 1/2/3 頁網址、頁碼 UI、第一張商品卡皆正確變動且每步僅 1 次查詢;第 3 頁時勾品牌 → 正確回第 1 頁 + 648;品牌六步無回歸;**六向突變測試**全數命中;三綠 + 231 檔 **2538 測**全過。審鏈=code-reviewer R1 FAIL 2 must-fix → 修 → **R2 FAIL 1 must-fix**(🔴 抓出我在註解與 backlog 寫了**未實測**的「最終由 useBrowseUrlSync 收尾落回正確頁」)→ **補實測推翻該斷言**、據實改寫並上修 #289 優先級 → Sean 拍 Q9=A 收案。🔴 **鐵則 6**:本檔 428 行,為 **hook 檔**(無 JSX,適用「>200 行評估拆分、不拆須寫理由」而非 400 行元件檔門檻);不拆理由=三個 hook 共用 parsers 與同一組「還原窗口守衛」橫切約束,拆檔會把該約束散到多檔更難追;下一片再長優先抽 parsers 獨立。🔴 當時殘餘:帶 `?page=N` 深連結進站仍會被還原波吃掉頁碼 → **已於下一 commit 修復**(見上一條、backlog #289 ✅)。)
2026-07-19(前一 commit;**商品卡去白邊 trim 線 ✅ 全線收工並上 production**)— Claude Code 視窗。五片 code(S1 `ff217b6` migration / S2 `1568ad2` 掃描腳本 / S4a `589fedb` 資料接線 / S4b `ef70443` 前端 / S3 `ef50482` CI job)+ 本 commit 熱修,已隨 Sean `push origin dev:main` **全數上 production**(`origin/main`=本 commit、Vercel production 部署 READY)。部署順序硬依賴**已按序走完**:Sean `supabase db push`(`20260719150000`)→ 路徑②驗收(16 項 DB 斷言 + 3 項 PostgREST smoke 全 PASS)→ OP-首灌 → 推 code。
🔴 **OP-首灌兩趟(第一趟失敗、根因已治本)**:第一趟 10,712 張全掃完,寫入**第 19 批撞 `CHECK bbox_complete`** → 舊碼 `process.exit(1)` **丟棄整趟結果**(僅前 3,800 列落地)。根因=`classifyTrim` 對**未捨入**值驗 `l+w<=1`,之後各欄獨立 `round5` 到 `numeric(6,5)`,每項最多 +5e-6 → 和可達 `1.00001` > 1。修法①**先 round5 再驗不變式**(不過則 `failed`、前端照舊 cover)+ 回歸測試(已對照未修復版本確認會紅、**非假綠**);修法②**單批 upsert 失敗降級逐列**,壞列單記其餘照寫,`upsert_failures>0` 才 exit 1 —— 一列違反 constraint 不得再賠掉整趟掃描。第二趟(增量續掃 6,912 張)`upsert_failures=0`。
**首灌結果**:DB 共 **10,762 列**(`ok` 8,825 / `no_trim` 1,254 / `failed` 683)= **82% 商品卡生效**。正式站**實抓 HTML 驗證生效**(目錄首頁 23 張、碳纖維部品分類頁 25 張帶 trim inline style;實際送出值如 `width:150.439%` / `93.296%` / `106.667%` = 確實逐圖各算)。Sean 肉眼驗 ✅ 通過。
🔴 **未結項**:①`failed` 683 列中 **665 列集中在 `www.eazi-grip.com`**(事後單張 `curl` 200 可取 = 首灌併發下被限流,**非圖片本身問題**);照 `FAILED_RETRY_MS`=7 天由 CI 增量 job 自動重試,**是否提前 targeted 重跑待 Sean 拍**(需加「只重跑 failed」旗標 = 新 code 改動)。②修法②**無自動化測試覆蓋**(該段在 `main()` 內未抽成可測函式),僅靠第二趟首灌人工驗證;code-reviewer 已列 Important、依一輪制修完 nit 即收工。③reviewer nit(降級逐列為序列 await、最壞 200 次往返)未處理,非正確性問題。
審鏈:plan 關卡1 Fable R1 NO-GO 3 must-fix→v1.1→R2 GO;codex 關卡2 S1 兩輪 / S4b 兩輪;code-reviewer 每片皆跑(S4a 抓「假向後相容」Critical=42703 目錄全斷);本熱修 code-reviewer **R1 PASS**(2 Important 已修:檔頭 exit 1 觸發條件未同步 / ②項無測試已於 commit body 誠實揭示)。三綠 typecheck 8/8 + lint 10/10 + build 綠 + trim 測試 13 綠。拍板落 memory `project_card-image-trim-decisions`。

2026-07-19(前一 commit `61f45b6`;插隊 bug 修復、非 M-4a 主線、**由平行 session 施工**)— Claude Code 視窗(**前台商品目錄「取消其中一個品牌、該品牌商品不消失」修復**〔Sean 回報〕。根因**實測 + 讀 node_modules 內 Next 16.2.6 原始碼坐實**:`route-params.js` `getCacheKeyForDynamicParam` 以 `Object.fromEntries(new URLSearchParams(...))` 產 page segment key → **重複 query key 只留最後值**;品牌走 `.append()` 重複 key + 字母序 → `?pbrand=a&pbrand=b` 與 `?pbrand=b` 的 key 相同 → `router.replace` 判定同一 segment、重用舊 CacheNode、**零 RSC 請求** → 畫面停在舊清單。⚠️ 僅在無 `page` 參數(第 1 頁)時顯現。修法=**條件式** `router.refresh()`(只在新舊 segment key 真碰撞時補一次;不可無條件——category/price 皆單值 key、天然不碰撞,無條件會讓每次切分類/拉價格都多查一次全型錄)。驗證:本地 production build 六步 648/1103/455/523/68/12793 全對 + 碰撞式新增(先 bonamici 再加 akrapovic)1103 正確 + 型錄查詢次數僅碰撞時為 2、其餘為 1;**三向突變測試**(拿掉 refresh→紅、改無條件→紅、改「只有變少才 refresh」→紅);三綠 + 231 檔 2530 測全過;code-reviewer R1 FAIL 2 must-fix→修→**R2 PASS**(R2 獨立讀 Next 原始碼確認 `segmentKey()` 與 Next 為字面同一運算式、等價成立),5 nit 全清。🔴 **殘餘成本**:碰撞情境仍需 2 次型錄查詢 → 治本解(品牌改單值 `?pbrands=a,b`)= **backlog #287**;能擋框架層回歸的 production build E2E = **backlog #288**。🔴 **同時發現既有 bug(已用基準版對照確認非本次引入)**:**分頁失效**——點第 2 頁時 `?page=2` 被第二個請求洗掉,分頁 UI 停在第 2 頁但內容退回第 1 頁,目錄 512 頁客人翻不動;Sean 拍 Q7=B **接著修、分開 commit**。)
2026-07-19(前一 commit)— Claude Code 視窗(**商品卡去白邊 trim 線 S1 = migration code 收工、未 apply**:`supabase/migrations/20260719150000_catalog_product_image_trim.sql`(新表 product_image_trim+RPC search_catalog_by_vehicle 同簽章加 card_image_trim 鍵〔paged CTE 分頁後才 JOIN〕+products_public 末欄+NOTIFY pgrst)。plan 真權威=`docs/specs/2026-07-19-product-image-trim-plan.md` v1.1(Sean 拍 Q1=B+指定 Fable 審;Fable 關卡1 R1 NO-GO 3 must-fix→修→R2 GO);codex 關卡2 R1 FAIL 6+2→修→R2 FAIL 2+1(**皆機械字面同步、依 E2a-a Q14=A 前例修完收案不開 R3、Sean 可否決**)→修畢+code-reviewer PASS(1 Important §9 漏記 R2 已補)。驗證=拋棄式 PG 17:整份原文執行零錯/15k 列 force_generic_plan 4-11ms/service_role 寫入正向/anon 三負向+CHECK 兩負向被擋/rollback(`docs/reviews/2026-07-19-trim-s1-rollback.md`)apply→rollback→強化三斷言回基線 true。🔴 **下一=Sean `supabase db push`**(僅此一支新 migration)→ 路徑②驗收(表+ACL 六面+PostgREST smoke)→ S2 掃描腳本。)
2026-07-19(前一 commit)— Claude Code 視窗(**akrapovic 品牌形象區 N°01+N°02 上架**:新 AkrapovicShowcase(N°01 三卡+N°02 冠軍工藝=官網首頁形象影片帶 720p/1.77MB 自 host、IO 進視窗才播、reduced-motion 停留海報+故事兩段+信任狀四格)+BrandShowcase case+manifest 同步;三綠+build+full vitest 227 檔 2493+next start 實跑 akrapovic 真商品頁截圖驗證;code-reviewer R1 FAIL(manifest 未同步)→修→R2 PASS。🔴 素材=官網公開資產、授權歸 Sean 既有品牌圖授權 gate;Sean 肉眼驗待跑。同日 B-2 記錄照留(下段)。
2026-07-19 — Claude Code 視窗(**B-2 處理四輪對抗審查的 NO-GO;code 已收工、未 apply 未 push** + 制度層補 deterministic hook)。
🔴 **四輪對抗審查、findings 共 21 條全數處置**:CLI 關卡2 round1(5 must-fix)→ round2(3+1 nit)→ **Sean 拍 A:不開 CLI round 3、改貼網頁版 Codex** → 網頁版 Codex(4+3 nit)→ **Fable(Sean 指定;2 must-fix + 2 nit)**。銷案表 = plan §15 / §15.2 / §15.3 / §15.4 / **§15.5**。
🟢 **首次出現正面訊號**:Fable 判定 **migration SQL 本體零 must-fix**,兩條 must-fix 皆在文件/SOP 層;且 Fable **自行重算**驗證了 prosrc delta / Q1=A 落地 / pg_temp 無 shadow / 狀態 A/B 二分 / ACL 六角色 / 金流鐵則等九面向。Fable 明示「F1+F2 修完即轉 GO」→ **修完後已請 Fable 複核,判定 `GO`(PASS-with-comments)**:兩條翻盤條件逐條銷案、2 nit 已清,並另自查三項(全樹 `SQL Editor` 字面掃描零對等並列殘留 / migration diff 僅動斷言 DO block 故兩個指紋常數不受影響 / packet 內嵌全文與檔案 EXACT MATCH)。新提 nit-3(章節順移後的舊 §5.1 指標 2 處)已順手清畢。<br>⚠️ **Fable 明示保留**:①依既有制度,**高風險金流 apply 前仍配 codex 正牌跨模型背書**(既定流程、非新增條件)②它**未重跑三綠**、不背書該數字字面(三綠由本視窗實跑:typecheck 8/8、lint 10/10、vitest 226 檔/2491 測、drift gate 3/3)。
①**B-2 主體**:新增 `supabase/migrations/20260719120000_m4a_b2_create_order_notification_email.sql`(同一 migration 內 `DROP IF EXISTS` 兩簽章 + `CREATE` 9-param〔第 9 參 `p_notification_email text DEFAULT NULL`〕+ ACL 鏡像重建 + `ALTER OWNER` + `COMMENT` + `NOTIFY pgrst` + 斷言矩陣;檔內自帶 `BEGIN/COMMIT` + `SET LOCAL lock_timeout='3s'` + `pg_advisory_xact_lock`)。
②**🔴 攔下的災難模式(實測非推論)**:PG 不允許 `CREATE OR REPLACE` 改參數數量 → 產生 **overload**;實測 8-param 與 9-param(第 9 參 `DEFAULT NULL`)**共存時,8 引數呼叫具名與位置兩種形式皆回 `42725 is not unique`** = 結帳全斷 → 必須 DROP 先 CREATE 後且原子。另實測 **Supabase 新建 function 的預設 ACL 含 PUBLIC + anon + authenticated + service_role**(anon 可執行)→ ACL 重建必須全部 REVOKE,比 PRD 原述更嚴重。
③**函式體零手動轉錄**:以程式從 prod 已驗證副本產生,**反向還原後 md5 == prod 基線**;`prosrc` 層 delta = **1 處**(orders INSERT 欄位 + VALUES;簽章與 COMMENT 不在 prosrc 內故檔面 = 3 處)。逐行 diff 為硬交付物、已附 Packet §6.2。
④**rollback 權威 = 凍結 snapshot `docs/reviews/2026-07-19-b2-preapply-snapshot.md` + 🔴新增補充檔 `docs/reviews/2026-07-19-b2-preapply-snapshot-supplement.md`**(關卡2 M-4:原 snapshot 只存 `prosrc` 本體、未明列 `proretset`、repo 內無可執行 rollback)。補充檔含**完整 `pg_get_functiondef`**(md5 與 prod **逐位元組相同**)+ E11 全欄 + **已 parse 且已實跑驗證的 rollback SQL**(刻意不放 `supabase/migrations/`,否則會被下次 db push 自動套用)。⚠️ 原 snapshot 的**基線指紋屬初代公式、已作廢**(該檔自訂不得再編輯)→ 現行值一律以補充檔為準。
⑤**審查五輪**:codex 關卡1 **R1/R2/R3 皆 FAIL**(Sean 明示破例加開 R3)累計 **18 條全數處置**,另 **1 條 codex 三輪皆未點名、由 grep 全檔自查補上**;code-reviewer **R1 FAIL**(3 must-fix + 3 nit)→ **R2 PASS**(0 Critical、鐵則 1-12 無違反;1 Important + 4 Minor 已順手清畢)。
⑥**驗證**:三綠 typecheck 8/8 + lint 10/10 + **root `pnpm test` 225 檔 / 2468 測全過** + 運費 drift gate 單獨 verbose 3/3(build N/A;🔴 過程抓到假綠 —— `pnpm --filter @pcm/domain test` 靜默 exit 0,該套件根本無 `test` script)。🔴 **新增本機拋棄式 PG 17.10 語法預檢(Sean 加拍)**:對 migration **檔案原文零剔除**整份執行,全數解析零語法錯;並完成**狀態 A/B 正向各一 + 負向各一**(竄改 ACL / 竄改 COMMENT 皆被守門擋下)與 **rollback 來回貫通**(指紋精確回到基線);**prod 交易模擬全過且零留痕已驗** —— SIM-A(資料庫**自行**由 prod 現行 `prosrc` 推導新函式體,md5 == 檔案內值)/ SIM-B 真建單三案(合法 email 存入正確、**省略第 9 參 → NULL**〔B-3 前現行 TS 不會斷〕、合成域 → **`23514 check_violation`**〔Q1=A 行為確認〕)/ SIM-C(**把檔內斷言 DO block 原文抽出實跑**)/ 守門原文實跑 + **mutation test 證明守門真的會觸發**。
⑦**Sean 拍板 Q1-Q7**(全落 memory `project_m4a-b2-create-order-9param-decisions`):Q1=A 不合規 email **裸傳由 CHECK 擋、RPC 內不加任何驗證**(不新增第三份規則;副作用=`nextval()` 不歸還會**跳號**,非幽靈訂單)/ Q2=A **`database.types.ts` 刻意未同步、留 B-4** / Q3=A 檔內自帶交易 + 可重跑、**不做** disposable DB 實驗 / Q4=B 破例加開 R3 / Q5=A 守門競態記為**有界假設**。
⑧**🔴 誠實揭示的未驗項**:(a)**原子性仍未被實證** —— codex 讀 CLI v2.98.1 原始碼確認「先送 migration 全部語句、之後才追加 history INSERT」→「schema 已改、紀錄未寫」**確定可能發生**;靠「可重跑 + 狀態 B 完整指紋守門 + 檔頭三查 SOP」收斂,**不是靠已驗證的原子性**(Sean Q3=A 明示不做 failure-injection);(b)~~整份 migration 從未被 PG parse~~ **已解除**(見⑥語法預檢);(c)守門與 DROP 間**競態窗口未消除**,靠「schema 變更單一管道」有界假設;(d)**PostgREST 快取重載**未驗(屬 apply 後路徑②);(e)**security label 偵測未做正向注入測試**(本機無 label provider);(f)本機 PG **17.10** vs prod **17.6** 版本差 —— byte-level 與指紋相符已大幅降低風險但非證明。
⑩**🔴 關卡2 兩輪抓到的自我誇大(字面 vs 事實)**:①advisory lock 用本 migration 時戳當 key = **假互斥**,卻宣稱「已緩解」→ 改為所有 create_order DDL 共用的固定常數 `1201033732` 並改寫能力邊界字面;②守門自稱「全屬性指紋」卻漏 `proretset`/`prosupport`/security-label → 補齊;③狀態 B 守門過弱會靜默洗掉未知變更 → 改驗完整預期產出指紋;④宣稱「斷言⑧能證明兩處公式一致」**不成立** → 改為 `pg_temp` helper **單一定義來源**(結構上不可能漂移);⑤`pg_seclabel` 只比 `objoid`(缺 `classoid`)且誤查函式不可能出現的 `pg_shseclabel` → 精確化。**指紋公式三代、常數三度重取,現行兩值皆於 production 實測**(8-param `77945871…` / 9-param `850e2e3c…`)。
⑪**🔴 網頁版 Codex 再抓四條 must-fix(皆成立、皆已修)**:①rollback SQL 的守門與斷言**各抄一份公式**、補充檔卻稱「自帶一份同義公式」= **round2 的假單一來源在 rollback 檔復發** → 改共用 helper + 補兩個 rollback 守門負向測;②**rollback 交付模式錯誤** —— 允許直接貼 SQL Editor 會讓 schema 退版但 history 仍記載已套用,下次 `db push` 跳過 B-2 = **自己防的裂縫自己踩** → 改新時戳 forward-only migration、SQL Editor 降為 break-glass;③**斷言⑧宣稱的保護路徑從未被測** → 補「產物改、常數不改」負向測(實測明確觸發並整批回滾);④plan 頂端狀態仍停在 round1 = 同款「只改被點名那一處」復發 → 已同步且不再寫死自指 hash。
⑬**🔴 Fable 再抓兩條(皆文件層、皆成立)**:①plan §9 頂部已把「貼 SQL Editor」降為 break-glass,但**同節的缺口對照表仍把它與 forward-only migration 並列**且不帶三義務 = w-2 銷案「全收」字面 vs 事實不符(**同款漏改第 6 次**);②「先退 app 再退 DB」只寫在 break-glass 分支,但 `42883`/`PGRST202` 是**交付模式無關**的 —— B-4 上線後走正常 rollback migration 一樣結帳全斷,把它掛在「不要貼 SQL Editor」段落 = **後果歸因錯位** → 已新增「步驟 0」到三處。nit 兩條(斷言編號亂序 + 未涵蓋清單漏 in-flight/cached plan)一併清畢。
⑫**🔴 自省**:上述①④與 nit「object-scoped 用詞殘留」是**同一個病的第三至五次復發**;③更是**在我剛把該教訓寫成 memory 的同一 session 內立刻復發**。治本已改為**在產生器內加 assert 讓漏改當場炸**,並把「負向測試」列為宣稱任何控制有效的硬前置。
⑨**制度層(Sean 拍 Q6=A/Q7=A)**:Fable 稽核查出「只改被點名那一處」**已復發 9+ 次**,且該規則的治本步驟 **07-17 就已寫在 memory**、07-18/19 照樣復發 → 根因 = **時機型規則放在只能靠回憶的載體**,而 `settings.json` 的 hooks 是唯一 deterministic 層、原 6 個 hook 零個觸發規則。→ 新增 `~/.claude/hooks/contract-sync-reminder.js`(PostToolUse `Write|Edit`;命中權威文件時提醒 grep 全樹建舊字面清單;每 session ≤3 次;錯誤一律靜默 exit 0;**六項測試全過**),掛進既有 `Write|Edit` 群組 = 零常載成本;MEMORY.md 該行改**觸發器句式**。MEMORY.md 精簡(156 行/26.8KB/140 檔,三項觸發值全破)= **Q7=A 順延至 M-4a 收尾**。
⑭**制度層第二支 deterministic hook(Sean 07-19 拍 A)**:新增 `~/.claude/hooks/push-state-watch.js`(**Stop** 事件,掛既有 Stop 群組=零常載成本)—— 每次停下自動比對未推數與 upstream SHA,**只在未推數下降或 upstream 位移時出聲**,不跑 `git fetch`,錯誤一律靜默 exit 0;**8 項測試全過**;`settings.json` 已備份 `.bak-20260719-b2`。動機:「收工前查未推數」當時**已散在 8 份文件內文**卻仍沒能在事發當下提醒任何人 → **prose 層對時機型規則無效已第二次被實證**(第一次=「只改被點名那一處」→ `contract-sync-reminder.js`)。

## 最近 3 commit
> dev。本 commit 見「最後更新」;下表列 3 個**已可達**前序 commit(`merge-base --is-ancestor` 驗)。
> 🚀 **`origin/dev` 指向何處、未推幾個 commit —— 本欄一律不寫死,以實跑為準**:
>   `git rev-parse --short origin/dev` / `git rev-list --count origin/dev..HEAD`。
> ⚠️ **為何不寫死**(2026-07-17,同款前科第 9-10 次):此處寫死的 hash/數字**每多一個 commit 或每次 push 就當場變假** —— 「本地零未推」在 `f39a5f8` 產出當下即假 → 改「未推 2 commit」在 `816f085` 產出當下又假 → **治本只治了「數字」、卻漏掉旁邊的 `origin/dev=55365e6`,push 完當場又假**(第 10 次;**證明「只補眼前那一處」的病連在治本時都會復發** → 同一欄的**所有**自指字面必須一起改)。**寫死的自指數字/行號是結構性假字面來源**(同 memory `feedback_claimed-sync-but-only-patched-touched-lines` 與「自指行號改引文錨」教訓)→ 一律改成**可執行的取得方式**。
> ⚠️ `dev`=pcm-admin 的 **production** 分支 → push 觸發 admin 重部署=預期(Email 線至 E2a-a 為止**零消費者**:`reclaimStaleLeases` 全 repo 零呼叫點、E2a-b/c 才接;migration 自 `55365e6` 起僅動頭註 = 純註解零 DDL、**無需 re-apply**)。origin/main:**同理不寫死**,以 `git rev-parse --short origin/main` 為準(2026-07-19 trim 線收工時 Sean 已 `push origin dev:main` → main 與 dev 對齊、trim 線全上 production;此前長期停在 `13ce3a9`)。

| Hash | 訊息 | 時間 |
|---|---|---|
| `799a733` | refactor(storefront): 移除手機選擇車款死碼 chip 並改抽屜 tab 文案 [#290] | 2026-07-20 |
| `1b84a2a` | docs(docs): 前台目錄 URL 狀態機三 bug 合併交接檔 [#287][#288][#289] | 2026-07-19 |
| `49afb07` | fix(storefront): 修帶 ?page=N 深連結進站被還原波吃掉頁碼 [#289] | 2026-07-19 |

## 下一步
🔴 **雙軌(2026-07-20 補記,codex #288-a MF3 抓出未寫)**:全域優先序仍 **M-4a B-3**(下方為主軌);**E2E 支線(非 M-4a 主線)下一片 = #288-b**(資料合約 globalSetup + mobile device project,見「最後更新」07-20 條目)。兩軌互不阻擋、各自獨立推進。
**M-4a 第一期收口**:②**通知線(修正版 D′)**——E1a/E1b/E1c/E2a-a/E2a-b/E2a-c ✅ 全關;**B-0 PRD ✅ 三輪雙審收案**;**B-1 ✅ 已 apply 上 prod**;**B-2 ✅ 已 apply 上 production 並通過路徑② 驗收(四輪審查 findings 全修)**。
⚠️ **2026-07-19 push 事故(Sean 拍 A:不撤、接受現況)**:B-2 視窗**從未執行 `git push`**,但四個 B-2 commit 已在 `origin/dev`(reflog `update by push`,同批夾帶另一線 akrapovic commit → 研判平行 session 推 `dev` 所致;**reflog 不記執行者,無法證明是誰**)。**損害為零**:production DB 完全未動(`create_order` 仍 8 參、水位 `20260718120000`、`20260719120000` 套用紀錄 **0 筆**)、`.github/workflows/` grep **查無 `db push`** = 無 CI 自動套。⚠️ 但「commit 不推 = Sean 手動推當 review checkpoint」這道關卡對這四個 commit **已失效**;`dev` 是線性共用分支,單線自律擋不住(詳 memory `project_parallel-sessions-shared-git-index-collision` 07-19 追加段)。
🔴 **下一步硬序(不可跳)**:①~~Sean `db push` B-2~~ ✅ **已完成** → ②~~路徑② apply 後驗收(含 PostgREST smoke)~~ ✅ **已完成,全過(詳 plan §16)** → ③**B-3**(結帳頁 email 欄 + zod 驗證 **必鏡像 §3.4 全部六條件**〔可列印 ASCII / 去尾點 / 擋子網域〕—— 漏做 = app 放行、DB 擋、**結帳 500**;單一 env flag 同時翻四層、預設 off)。<br>⚠️ **PostgREST smoke 與原設計的落差(誠實)**:原設計要以 `authenticated` 身分進入函式體看 RAISE;實際無可用使用者 JWT(簽發需 `.env` JWT secret、受規則封鎖)→ 改以 publishable key(anon)+ **對照組**達成同一判定目標。**證明了** 9 參簽章在 schema cache 可解析、8 參不撞 42725、anon 無權執行;**沒有證明**函式體內部行為(未進入函式體)。
🔴 **跨片唯一合法順序**(PRD codex R3 #7):`B-1/B-2 完成並驗證 → B-3/B-4 部署但 flag 保持 off → 開 flag 並記錄精確切換時戳(=cutoff)→ 觀察窗 → B-6`。**不得在 B-2 未 apply 前開 flag**(server 要求 email 但 RPC 尚無該參數 = 結帳中斷)。
⚠️ **B-2 apply 後 ≠ 必填生效**:第 9 參仍是 `DEFAULT NULL`、`authenticated` 直呼 RPC 可省略 → 必填收緊是 **B-6**(同片移除 DEFAULT,否則必填可被繞過)。
⚠️ **B-3/B-4 動 TS 時必銷的舊字面清單 11 項** = B-2 plan §8.2(含兩處**硬編碼 8 鍵字面斷言**:`mappers/order.test.ts:38-47` 的 sorted 陣列、`SupabaseOrderAdapter.test.ts:55-64` 的 mock —— 不同步即為假綠)。
**交接檔** = `docs/handoff/2026-07-19-m4a-b2-applied-handoff.md`(B-2 收案 + B-3 起手 + 未驗項 + rollback 路徑)。
拆片全圖 = PRD §4(B-1~B-6 + C-1~C-4)、**上線 gate 8 項** = PRD §6(全數達成前**禁用「通知功能上線/孤兒已消滅」字面**)。
③**最新商品**(storefront 例行 UI)。④小件穿插=Q3d 佔位圖/Q3a 佔位頁/Q3e 結帳內嵌地址;**akrapovic 品牌形象區 ✅ 已上 production**;**🆕 商品卡去白邊 trim 線(Sean 07-19 拍 Q1=B)✅ 全線收工並上 production**:S1 `ff217b6` → S2 `1568ad2` → S4a `589fedb` → S4b `ef70443` → S3 `ef50482` + 首灌熱修(見「最後更新」本 commit)(plan 真權威=`docs/specs/2026-07-19-product-image-trim-plan.md` v1.1)。硬序四步**已全數走完**:①Sean `db push`(`20260719150000`)✅ → ②路徑②驗收 16 項 DB 斷言 + 3 項 PostgREST smoke 全 PASS ✅ → ③OP-首灌 ✅(兩趟;DB 10,762 列 = ok 8,825 / no_trim 1,254 / failed 683)→ ④Sean 肉眼驗 ✅ 通過。正式站實抓 HTML 複驗生效。🔴 **未結**:`failed` 中 **665 列 = `www.eazi-grip.com` 首灌被限流**(圖本身正常、事後單張 curl 200),照 `FAILED_RETRY_MS`=7 天由 CI 自動重試;**提前 targeted 重跑需加「只重跑 failed」旗標(新 code 改動)= 待 Sean 拍**。🔴 **部署順序硬依賴(S4a Critical、已按序執行、後人回滾/重建環境仍適用)**:adapter select 指名 `card_image_trim`、未 apply 的 DB=**42703 目錄全斷**(非優雅降級)→ **先 db push、後推 dev/promote main**(admin 不用此 adapter、不受影響)。審查:Fable 關卡1 兩輪(R1 NO-GO 3 must-fix→R2 GO);codex 關卡2 S1 兩輪(6+2→2+1)、S4b 兩輪(2+1→3 字面);code-reviewer 每片(S1 PASS/S2 PASS/S4a R1 FAIL Critical→R2 PASS/S4b R1 FAIL→R2 PASS)。獨立線(未排):**LINE 補資料線**(動工前 Sean 須先拍「真實 email 存哪」);搜尋 S2 lightech #275→S3 MVP;M-3 真刷卡 gate;step-up 全套另片。**M-4a 收尾後**:①開 Sean 交代的「流程再優化」正式題 ②**MEMORY.md 精簡**(Q7=A 順延項)。

## Sean 待決策
⓪**(#288-a CI 生效前置、操作非決策)GitHub Secrets 設兩鍵**:repo Settings → Secrets and variables → Actions → 新增 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`(值=apps/storefront/.env.local 對應兩行;`NEXT_PUBLIC_` 本即隨 client bundle 公開、非新增暴露面)。**未設 → 下次 push 起 e2e-prod CI 硬紅(刻意不 skip)**。
①**(非阻擋、與 Email 片無因果)Vercel 升 Pro?** 官方明載 Hobby 禁商業用途、PCM 賣場已命中(「Advertising the sale of a product or service」+「processing payment」),$20/月=**既存合規風險**、Sean 已知悉未拍。③**(Email S4、非必拍)** ACL 採 GRANT service_role(Fable 裁)而非 RPC-only(codex 裁)——兩審分歧、Claude 已判、Sean 可否決(理由=RPC-only 不縮小 blast radius,持 key 者本就能讀 customers 全表;全紀錄 plan §4.3)。④**(LINE 補資料線動工前)** 真實 email 存哪、如何與 auth.users 同步、如何不破壞既有 LINE 防冒登入設計。⑥**✅ 已銷(Sean 07-17 拍 Q14=A 收案)**:E2a-a codex R2 3 must-fix 已修、未經 R3 覆核=接受(三條皆機械字面同步、無設計分歧;commit body 已誠實記;詳 memory `project_m4a-email-e2a-decisions`)。⑦**✅ 已銷(Sean 07-18 依建議)**:E2a-c(a)**無 `*_ENABLED` gate**〔codex 抓未批准+靜默失敗態→移除;自然閘=`ORDER_EMAIL_FROM` 未設即 503+E2b pg_cron+E3 前表零列〕(b)**codex R2 3 must-fix 收案**〔全機械字面/測試補強、已修+突變自驗 3/3、未經 R3 依硬上限;同 Q14=A 前例〕。
⑧**(非阻擋、trim 線)eazi-grip 665 張 `failed` 是否提前 targeted 重跑?** 現況=首灌併發下被對方站限流(圖本身正常),7 天後 CI 增量 job 會自動重試、期間該 665 張照舊 cover 顯示、零功能損害。提前重跑需加「只重跑 failed」旗標=新 code 改動 → 由 Sean 決定要不要現在做。
⑤(非阻擋)車款搜尋繼承件卡片是否併 inherited 年份=R3、日後另議。(Email Q1/Q2/Q4 + tier Q1/Q2——07-16 已拍、銷案,詳 memory。)

## Blocker
**🔴 0072 雙扣待 Sean W1 退款**(open):PCM-2026-0072 舊+0073 新各 17,300、W1 報表已偵測為 open anomaly→Sean 依 runbook(claim→TapPay Dashboard 退舊→resolve);非程式 blocker。
**🟡 M-3 prod checkout 仍不可開**:3DS flag 全 false、真刷卡待 sandbox 3DS E2E + Sean 驗收(非硬 blocker、獨立線)。
**🟡 M-3 階段⓪(經銷價同步/店家價 checkout)**:硬 gate 待報價單側三件就緒(合約 bump v2 + protected dealer view + least-privilege 憑證;非硬 blocker、訂單地基先行)。
**🟡 #215 tier server 認證**:defer,真死線=M-2-08 接真經銷價前(07-13 分析:現況零洩漏)。

## 緊急 backlog
無(不擋線 backlog:**#281 outbox 保留政策+清理 job〔PII `recipient_email` 無限期滯留、E3 上線後升 🔴〕/ #282 `cron.job_run_details` 清理〔E2b 後才有對象〕/ #283 Resend bounce webhook / #284 Email 文案 L2→L3 / #285 retry hint 精準退避〔=Q11=A 的第三選項;未知 429 一律 ≥24h 的已知代價〕/ #286 死信人工重送工具〔=Q9=A 的誠實缺口:死信零救援,告警後無事可做〕** 皆 07-17 新開;Fable F1 表級 CHECK / F2 哨兵 md5 / create_order p_invoice 自由欄型別 / admin 明細頁未顯 vehicle_snapshot / V-2g 雙擊縮放刻意未做,詳 kickoff §4)

---

## OD 商品頁改造線(並行 workstream、Claude-Code 自驅、不入主表)

> 與報價單資料線(S3a/S3b)並行的另一條線。**主表 7 欄由報價單線擁有**、OD 線狀態記此附屬區 + manifest `od_redesign`(Sean 2026-06-02 拍 A)。寫審分離 ROLE=A(施工 session 實作、審查 session 哨兵盯 dev 自動 fresh-context 複驗)、**不跑 busboy-end**(避 clobber 報價單線)、Sean 手動推。

- **視覺真權威:** OD 模板 `product-detail-rpm-template.html`(open-design "Website V2")+ `HANDOFF-rpm-template.md`。鎖定決策見 manifest `od_redesign.decisions`(決定一=A OD=視覺真權威 / D1=A 適用車款表全車種 3 欄無車系 / D3=A 12K 收進紋路無消光、由真資料 disable / Q1 override:N°03 留相關商品 + FAQ→N°04)。
- **當前 slice:** **Phase A 完成 ✅ + 已合併進 dev(merge cf630b2)**。OD-5 服務橫條 / OD-6 N°01 / OD-7 N°02 紋路牆+預覽卡+圖庫聚合 / OD-8 分頁碳纖維化 / OD-9 N°03 相關商品 / OD-10 N°04 FAQ+JSON-LD(保固共用 rpm-policies 單一真相)/ OD-11 buybar OD §12+響應式≤1079,共 11 片(OD-5~11)+ 先前 OD-1~4 全完。合併後三綠 + 完整 pnpm test 531 全綠,OD 元件 × S6 fitments 共存確認。
- **下一步:** ✅ **OD 線已收尾**(2026-06-16 查核更新):OD-12 適用車款表 + OD-12b/c/d 桌機重設計 + OD-13 FAQ + OD-V~V3b 手機/iPad 真機驗收修正**全部完成、併入 dev、已推 origin/dev**(`dev..od-redesign` 空、od-redesign tip `266f5f2` 在 origin/dev、審查 PASS `9cc5bbd` + 交接 `f3d6a42`)。此附屬區先前「OD-12 待做」字面為過時殘留、已更正。後續 OD 強化由 Sean 主導視覺(memory sean-owns-visual-design)。
- **Phase A 連續做(Sean 拍 B):** OD-3~OD-11 全速連續 commit、Sean Phase A 全完肉眼驗一次、哨兵每片自動審 FAIL 即通知。
- **分片清單:** Phase A(現做、不等 S3)OD-1→OD-11(地基/Hero/右欄/Picker+預覽/服務橫條/N°01/N°02紋路牆/分頁/N°03相關/N°04 FAQ/buybar+響應式);Phase B(等 S3b)OD-F1 適用車款表(接 fitments[])/ OD-F2 真資料收口。完整 OD區塊↔元件對照 + 計畫見對話 OD-0 偵察報告(待 Sean 點頭存 docs/specs/)。
- **⚠️ 跨線紀律:** OD 線不碰 `scripts/rpm-*.ts`(報價單線 WIP)、`docs/reviews/integration-phase1-review-log.md`(審查 session)、`docs/specs/*S3*`(報價單線)。

## 速查 / 歷史(已外移、降低本檔讀取成本)

- **速查**(Phase 1 範圍 / 技術棧 / 關鍵路徑)→ `docs/quick-reference.md`
- **變更紀錄**(slice 逐筆歷史)→ `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段

## 文件交叉引用

每次新對話依此順序對齊上下文:

1. **`STATUS.md`** ← 本檔(每次先讀)
2. `docs/PHASE-1-NORTHSTAR.md` v2 — Phase 1 真權威定義
3. `docs/lessons-learned.md` — 舊專案教訓彙整
4. `CLAUDE.md` — Claude Code 工作規則
5. `docs/PHASE-1-MILESTONES.md` — milestone 排程
6. `docs/decisions/` — 重大決策記錄
7. `docs/patterns/` — 通用 + PCM 專屬規矩
8. `docs/phase-1-backlog.md` — 未決事項
9. `docs/features/*.md` — PRD
10. `design-reference/` — 視覺真權威字面(submodule)
11. `PROGRESS.md` — 歷史紀錄
12. `docs/quick-reference.md` — 速查(Phase 1 範圍 / 技術棧 / 關鍵路徑)

衝突仲裁順序:
- STATUS.md 與其他 md 衝突 → STATUS.md 為準
- 其他 md 與對話歷史衝突 → md 為準
- 視覺 / 結構 / 路由 / 元件命名衝突 → design-reference 為準
- 業務邏輯(訂單流程、權限、價格、Medusa schema)衝突 → docs/decisions/ 為準

## Busboy 機制(沿用第一輪)

- **busboy-start.js:** Sean 在 Terminal 跑、輸出貼新 Claude Code session 第一則訊息
- **busboy-end.js:** Claude Code 在 session 最後跑、自動更新本檔 5 個欄位(最後更新 / Phase Milestone slice Branch / 最近 3 commit / 下一步 / Sean 待決策)、commit、不 push(Sean 手動推當 review checkpoint)
- ⚠️ WO-6 後「變更紀錄」已移 `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段、不再寫本檔;busboy-end.js 若仍寫 STATUS 變更紀錄表需同步改寫(pcm-tools 外部 repo、待 Sean 處理)
- repo 參數:`pcm`(本 repo)/ `tools`(pcm-tools)

第一次 busboy-end 跑之前、本檔欄位手動填(start template 用、由 Claude.ai 維護)。

busboy-end 跑完後 amend 進 slice 主 commit、不另開 commit。

— END —
