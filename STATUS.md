# STATUS.md
> PCM Phase 1 SSoT. 衝突仲裁: STATUS.md > NORTHSTAR > 其他 md > 對話歷史. 🧹 **2026-07-27 主表精簡(Sean 拍板 A)**:主表 357 → 30 行;移出的內容**逐字保存**於 `PROGRESS.md` §「2026-07-27 STATUS 主表精簡存檔」、**零刪除零改寫**,7 欄結構完整保留。

## 當前狀態
**Phase:** Phase 1 / **Milestone:** **M-4b 後台重建「員工上工」線**(2026-07-26 換軌)/ **Branch:** dev。北極星(Sean 逐字)=「可以完整上線給員工使用,操作,修改網站。而且他們不是工程師」;驗收=`docs/specs/2026-07-25-admin-backend-rebuild-spec.md` §1「員工的一天」27 項;🔴 **施工序 2026-07-27 深夜改為「E10 訂單閉環 → E8-B 認證(email+密碼)→ E8-C 2FA」**(Sean Q1=A、二次確認 Q6=A;E11 積木改為隨 E10 按需長出、不獨立成階段)。~~舊序 E11→E8→E10~~。
✅ **本線已完成**:全後台畫面預覽 artifact(46 畫面、Sean 驗收畢、U1-U7 全拍板)/ E11-1 `<AdminDataTable>` / E11-2 `<AdminForm>` / #296 儲值金按 Enter 誤扣款 / E8-A1 staff 名單進 DB(migration 已 apply production)/ E8-A2 `/settings/staff` 員工管理頁。📌 **暫緩非作廢**(細節在 PROGRESS 存檔區):M-3 TapPay 正式化線 S3 起 / 退刷線 RF2b 起 / M-4a 通知線 B-4。
🔴 **E8 未完成**:操作者身分仍是使用者**自己下拉挑的、系統未驗證**(`apps/admin/src/lib/session/actor.ts:6` 自陳非授權邊界);後台唯一入口是報價單站的**共用密碼**登入(`ADMIN_PASSWORD` 單一 env;🔴 **TOTP 實查為關閉狀態**——`auth_state.require_2fa=false`、`totp_devices` 0 列、`recovery_codes` 0 列,詳 memory `project_quote-2fa-deployed-but-dormant`)再 SSO 過來,SSO payload **不帶是誰**(`apps/admin/src/lib/session/session.ts:11` 逐字)⇒ **目前沒有「每個員工的帳號密碼」這個東西**。真認證=報價單端跨 repo 線、尚未開工。

## 最後更新
🎉 **2026-07-30 中午:訂單編號改制 N2 + N3a + N3b 三片 code 全部收工(`828cf5f` / `24f0b63` / `946b837`,未 apply、未 push)。** Sean 拍 Q1=A(只換產號器、舊單完全不動)+ 第二輪三題(Q1=A 產號用盡告警延後記 #300 / Q2=A 兩關都審 / **Q3=B domain 一起改對 ⇒ 新增 N2 片**)。**新單從此拿到 6 碼亂碼**(隔離庫實跑 `XM5GZT` 等)、**舊 30 張原號零異動**、兩格式永久並存。**N3c 收窗不做、CHECK 永久兩收、D1 全線退場**。
🔴 **Sean 手動兩件(缺一則下一片不該開工)**:①`supabase db push`(需先移開 `.env.local`,見 memory `reference_supabase-cli-reads-env-local-blocker`)②**1 元商品真刷 smoke** —— 規格 §5.4b 對 N3b 明文要求,**這是唯一能證明結帳沒壞的證據**,程式測試取代不了。③push。⚠️ 未 apply 的 DB 配上新 code 不會壞(N3b 只在 apply 後才改行為;未 apply = 照舊產 PCM- 格式)。
🔴 **審查:關卡1 Fable R1 NO-GO 4→R2 GO;關卡2-A code-reviewer(opus)FAIL 5 must-fix + 12 nit;關卡2-B codex 第三次同款逾時⇒換 Fable NO-GO 3 must-fix(含 1 BLOCKER)+ 6 nit。全部接受、駁回 0。** 詳錄 = plan §9/§10。**Fable 是 codex 的替代品、不是鐵則 12 字面要求的 codex 背書 —— 是否接受由 Sean 判斷(已提出、未答)。**
🔴 **最重的兩條 finding 都是「守門的字面大於它的實際能力」**:①**M2** —— 專為守「654 行手抄」而建的 §4.1d,把**最該守的 13 行(orders INSERT 的金額/tier/身分落點)留在自己的白名單裡**;我實測 `'general'::member_tier`→`'premiumStore'` **沒有任何 CHECK 擋、訂單真的建立、tier 寫成經銷價**,而守門印「✅ 通過」(審查者原舉的 subtotal↔total 對調其實被既有 CHECK 遮蔽、不算它守到)⇒ 已改 strip 後嚴格比對,兩個突變均證明抓得到。②**F1 BLOCKER** —— 我在**審查進行中**改檔(違反凍結紀律)時寫的斷言用了 `%%`,而 `%%` 的轉義只在 RAISE/format 格式字串成立、普通 SQL 字串裡是兩個百分號 ⇒ strpos 恆 0 ⇒ **N3a 每次 apply 必死,含 production 的 db push**;該條已整條刪除。**因果誠實記錄:違反凍結紀律直接造成這個 blocker。**
🔴 **折入過程另抓四個假綠(全寫進 plan §10)**:①「重試上限=5」的計次**被同一交易 rollback**⇒永遠讀到 0(接受 0 就是假綠;改用 sequence)②**突變連測試一起突變**(字母表字面在檔內 8 次、含斷言自己的期望值)③註解寫死隨機數字必過期(卡方剛記「範圍 101-166」、下一跑就 180.8 ⇒ 改記下界+樣本)④原探針 5 條有 2 條**數學上被遮蔽、無法單獨觸發** ⇒ 砍到 3 條。
✅ **驗證**:三綠 8/8·10/10·2/2 + 完整 `pnpm test` **275 檔 3336 passed + 1 todo、0 failed**;`scripts/n3-verify.sh all` 全綠 = **6 個 N3a 改檔突變各死在自己守門 + 消融**、**§4.1d 域外 255 行逐字元相同**(負測證明抓得到)、**行為段 B/C/D + B-m1/C-m1/D-m1 三個突變均證明有效 + 重試上限實測 5 次**、#216 drift gate 基準已移到 N3b(負測證明)。🔴 **誠實邊界:本機 PG17 非 Supabase、`auth.uid()` 是 shim、商品價格手塞 ⇒ B 段是煙霧測試,不是「結帳真的能用」。**
🆕 **backlog #300**(產號用盡無告警 = N3b-app 延後;延後依據明寫,不是「不需要」)/ **#301 🔴 高**(TapPay Record API 三處欄位假設實測為誤、`wire.ts:68` 宣稱不實 —— **退款線 RF2b 開工前的硬前置**)。
🛑 **2026-07-30 上午 Sean 拍板:D1c 的 26 張永久刪除「取消」;「換編號」目標保留、改走 29 張全部改號(可逆)。** 決定與完整理由 = memory `project_m4b-d1c-delete-cancelled`。**觸發點是 Sean 自己的質疑**(逐字大意:「這些都是我自己的測試單,要不要刪都可以」+「我以為是為了以後能在後台取消訂單退款才做的,如果不是那有點浪費時間」)—— 主對話核對後確認 **D1 對「員工能在後台取消訂單/退款」零貢獻**(那是 E10 別的片),其唯一用途是讓編號能換 6 碼亂碼;而**換編號不需要刪除**(全部改號、舊號進 `legacy_display_id`,改號可逆 ⇒ 取證/判定矩陣/對抗審查整層消失);且**留著反而有用** —— 5 張多品項單全在待刪 26 張裡(v2 §7「驗收盲區」自陳),刪完 production 沒有任何 `quantity > 1` 資料可驗新訂單後台。**實跑到哪**:`verify-ca` ✅ / **D1b1 首次對真 TapPay 正式商戶執行**(史上第一次)= `readback-aborted` / **D1b2、D1c 一步未跑,正式站全程零寫入**。⏰ 備份 07-30 18:06 過期 = 預期內,要用時重跑 D1a2。🔴 **筆數更正(2026-07-30 正式站唯讀實查)**:現存訂單為 **30 張**、不是 29 —— 新增 `PCM-2026-0105`(07-29 04:39 UTC 建立、`unpaid`)。**本檔其他地方的「29」是當時的歷史快照(D1a2 匯出筆數、dry-run 鎖列數),刻意不改**;凡指「現在還有幾張舊格式單」一律以 30 為準。連帶:N3b 換產號器前每過一天都可能再多一張永久舊格式單(Q1=A 已接受並存)。`order_display_seq` last_value=105 = orders 內最大序號 ⇒ 回滾舊產號器不會撞號(已實查)。
🔴 **同批實測查到三件事實,與規格假設不符(必須帶走)**:TapPay Record API 對**已全額退款**紀錄(0102/0104 實測)①`amount` 回 **0**、原額在 `original_amount` ②`refunded_amount` 回 **101 / 1180**(= `orders.total`),而矩陣要求它 `= amount`(0)③**`transaction_time_millis` 欄位不存在**(實有 `time` / `cap_millis` / `transaction_complete_millis` / `bank_transaction_*_millis`)⇒ `packages/adapters/src/tappay/wire.ts:68` 逐字宣稱欄名「以官方 Record API reference 核實」**與實回應不符**,未來任何用 Record API 判定退款的片先重查官方文件。另:5 把鍵呈乾淨世代分界 —— 6 月三把(0052/0064/0090)全**零命中**、7 月兩把(0102/0104)各命中 1 筆且 `rec_trade_id`+`order_number` 逐格相符(⇒ 商戶身分與查詢鍵綁定已正向證明);Sean 於 TapPay 後台實查 6/26、6/27 **兩日零交易**;🔴 `0052` 是 `paid` 卻零命中 ⇒ **零命中不蘊含「沒收到錢」**。原規格假設「rec_trade_id 來自 3DS 啟動 ⇒ TapPay 必有紀錄」(出處 `docs/reviews/2026-07-28-e10-fable-retrospective.md:82`)**被實測推翻**。
🧱 **同日:判定矩陣「窄化放寬」已寫但不足以解封 D1(保留備用、非施工中)。** 0064/0090 零筆 + `payment_status='unpaid'` ⇒ 新 verdict `not-charged-no-hit`(證據等級 `official-no-hit`、不升格 system-readback),非 unpaid 一律 abort;名單寫死兩張不沿用 `PENDING_ORDERS`。三綠 + `pnpm test` **3312 passed**、突變 7/7 紅。🔴 **關卡2 codex + code-reviewer 一致判 FAIL**:**主對話把未驗證的推論寫成事實** —— 檔頭與規格原寫「0102/0104 order_number/**金額**逐格相符」,實際 `judgeHit` 因 abort 在 0064 而**從未對 0102/0104 執行**,金額根本不符(已更正為只宣稱 `rec_trade_id`+`order_number`,並逐條記下三處欄位真相)。審查另抓 5 條漏測突變(最重:註解宣稱「名單寫死不沿用 `PENDING_ORDERS`」那道防護**零測試**;fixture 把 0064/0090 預設 `paid`、與正式站 `unpaid` 相反 ⇒ 全部 `record_status` 掃描跑的是錯路徑)—— **因刪除取消,這些未修,連同三處欄位錯誤一併記為待辦**。
🧱 **2026-07-30 凌晨 D1b1 取證動作**程式碼就緒**(🔴 從未打過任何 TapPay,一次都沒有)。** CLI 新增第五個動作 `readback`:兩句 SELECT(身分閘 + 事實查詢)→ 五筆有鍵者打 TapPay Record API → §8.7 判定 → 證據 JSON;**不進 orchestrator、不開交易、不鎖列、不動 cron、不寫 state**(不借用 `--dry-run` 的理由:它會對 29 張正式站 orders `FOR UPDATE`、對 attempts `FOR UPDATE NOWAIT`,取證不該付演練刪除的代價)。**審查 code-reviewer(opus)R1 FAIL 4 must-fix + 10 nit,全數接受、駁回 0** —— **M1 是審查者實跑證偽我的宣稱**:兩個呼叫端各自 `filter(recTradeId !== null)` 擋的是「沒有鍵」不是「不准查」,而 0101 是 unpaid + 卡片 pending、D1b1 又刻意不停 sweeper ⇒ 查詢前被回填鍵就會被送去打正式商戶 = §8.7 逐字禁止的那次查詢 ⇒ 新增共用入口閘 `selectFactsForQuery`(以 displayId 排除 + 送出 HTTP 前先跑整組前提斷言),**D1b1 與 D1c 步驟 8b 兩處都改用它**;M2 證據包補 `queryContext`(merchant id + endpoint —— 0052 的「正式商戶查無」效力全靠它;Partner Key 絕不入檔);M3 `rawResults` 其實是窄化後的物件 ⇒ 改名 `parsedResults` + 新增 `responseShapes`(只存鍵名與計數,承接 `d1-tappay-client.ts` 自己註解要求的「D1b1 首跑須把實回應形狀落 audit」);M4 CLI 分支零覆蓋 ⇒ 補 `runD1Cli` 接線測試。**驗證**:三綠 + 完整 `pnpm test` **3307 passed**;突變 3/3 紅;隔離庫實跑正反兩向;動到 orchestrator 查詢層入口 ⇒ 另跑 D1t3 n1,n234,n5 回歸全綠。🔴 **仍在禁區、等 Sean**:①打真 TapPay 正式商戶的實跑本身 ②`--merchant-id` 的第二來源(D1t1 就留下的未決題)③證據包保存位置與期限。
🎉 **2026-07-30 凌晨 D1t3 完成(timeout / rollback 整合負測)—— D1 線的執行器與它的故障證明全部收工,下一片 = D1b1。** 產物:`scripts/d1t3-negative.sh`(六項負測 harness,`source` 重用 D1t2 的 provision、不複製貼上)+ `d1t3-idle-timeout-probe.ts`。**規格 §5.1 row 18 的 ①-⑥ 逐項對真 PG17 隔離庫實跑**:①idle 逾時 ⇒ 交易 rollback、零半掛(一手證據 `25P03 terminating connection due to idle-in-transaction timeout`;誘餌 `email_outbox` 那筆 DELETE 確實回滾、`pg_stat_activity` 與 `pg_prepared_xacts` 皆 0)②反向持鎖 ⇒ attempts 的 `FOR UPDATE NOWAIT` **立即** abort(`could not obtain lock`,非等滿 5 秒的 lock_timeout)③kill -9 後 self-heal ④fault-inject recovery 的**兩條真分支**(job 仍 active ⇒ 只清 state / job 已停 ⇒ 必須真的 `alter_job(true)`)⑤雙 orchestrator ⇒ 後者被 advisory lock 擋下且零副作用 ⑥CAS 接管(`takenOverRunId` = A 真正的 run id)。**兩段等長消融證明「抓得到」**:N1 拿掉持鎖者同一道 dry-run 轉綠 / N6 關掉 idle 逾時、**閒置一樣久 330 秒**連線存活。**突變 7/7 全紅**:拿掉 NOWAIT / recovery 不清 state / 拿掉 state 的 clusterId 比對 / advisory lock 改成每 process 一把 / idle 改 `'2min'`(新單元測)/ idle 改 `'0'`(N6)/ `recoverSweeper` 不呼叫 `alter_job(true)`(N3 專屬)。**審查**:code-reviewer(opus)R1 **FAIL 5 must-fix + 4 important + 6 nit,全數接受、駁回 0** —— 最重的三條都是**假綠路徑**:①原 N2/N3 只差 `jobStoppedAt`,而該欄全樹**沒有任何讀取點** ⇒ 是「一個邊界跑兩次」(已改成 recovery 分派的兩條真分支;M6 突變證明舊設計會整個漏掉)②N6 消融只等 20 秒 ⇒ 任何 300 秒級的別的連線殺手會讓兩段同時綠(已改等長)③探針只要收到任何非同步 error 就回 `terminated:true`,連線其實還活著(已改成「那句 `SELECT 1` 必須真的 throw」)。另修 `d1t2-rehearsal.sh` 的 `cd` 用 `$0`(被 source 時會多 cd 一層)、`STAGES` 前導空白會讓六段**靜默全跳過且 exit 0**。**驗證**:三綠 + 完整 `pnpm test` **3293 passed**。🔴 誠實邊界(沿用且不放寬):fake 排程器不會真的跑、TapPay 是 fixture、本機 PG 非 Supabase;**kill -9 打的是 orchestrator process、不是資料庫** —— DB 端 crash 不在本片涵蓋。🔴 施工中自己踩了兩個假綠(持鎖者用了 PG 非法的 `count(*) … FOR UPDATE` 一列都沒鎖到;`kill -9 $!` 殺到 bash 子 shell、orchestrator 變孤兒繼續跑向 COMMIT),後者由自己加的 `pgrep` 守門當場抓出 ⇒ 已補 `ps -p $A_PID` 回歸閘 + EXIT trap。
🎉 **2026-07-30 凌晨 D1t2 完成(CLI + dry-run + 隔離 DB 實跑)—— orchestrator 首次對真資料庫全鏈實跑通過。** 產物:`d1-orchestrator-cli`(action grammar 四動作+四同名旗標別名;模式×目標 fail-closed 矩陣 —— production 必 `--merchant-id` 雙輸入、apply 必 `--confirm DELETE-26-ORDERS`、state 固定 `~/.pcm-d1/`、audit 拒覆蓋+0700/0600;rehearsal 必 fixture+cluster-id、loopback 釘 postgres/postgres、零正式環境依賴)/ `d1-readback-runner`(真 TapPay 版+fixture 版,共用 strict wire+merchant 檢查)/ 核心加 `recover-only` mode+**活連線身分閘提前到任何 cron 寫入之前**(loopback tunnel 打不到正式站)+ recovery 對帳只收 0|26 並依結果分流恢復失敗態 / 隔離 harness(`d1t2-rehearsal.sh` provision|scenarios|teardown,D1t3 重用):**拋棄式 PG17 + shim + 85 支 migration 全綠(fitments 相容 stub 插序)+ fake cron/pg_net(T-Q1=A)+ cohort 逐格同筆數 seed + 2 張非 cohort 誘餌** → **三段全過**(A dry-run 零寫入 SQL 摘要比對+audit 六筆逐驗 / B 負向 0102 查無急停+全摘要一致+sweeper 復原 / C apply 26 刪 3 改號+**keep-original 出口實跑**+外部矩陣+誘餌逐列不變)+ 消融證明(竄改誘餌 consent ⇒ 摘要轉紅)。`verify-ca` 對真 pooler 兩方向實跑過(密碼無條件假值;**d1-guard 合約債①②③全結案**)。**審查**:關卡1 codex 兩輪 16+6 + Fable R3 12 = 33 條全折;關卡2 = code-reviewer 兩輪(C1:master 三處 `--dry-run` 舊字面會被自家 parser 堵死 ⇒ 四別名)+ codex 兩輪(R1 15 must-fix 14 修 1 裁決、R2 3 must-fix 全修、**別名裁決獲 codex 接受**)。**驗證**:三綠 + 完整 pnpm test 3289+ / D1t2 突變 10/10 紅。🔴 誠實邊界:fake 排程器不會真的跑(production 以 D1b2 dry-run 唯讀 preflight 為準)、TapPay 是 fixture、本機非 Supabase。**下一步 = D1t3**(kill -9 / 雙 orchestrator / fault-inject 負測,重用 provision;🟡 T-Q1=A 已拍 fake 介面路線)。⏰ 備份時效 07-30 18:06,D1c 前重跑 D1a2(預期內)。
🧱 **2026-07-29 晚 D1t1 完成(orchestrator 交易核心;runbook 型高風險片)** = 4 支新模組 + 2 支既有補強 + 5 支測試:`d1-tappay-client`(獨立 Record client,不碰 server-only adapter、merchant id 雙輸入、整批 3 分鐘 deadline)/ `d1-readback`(§8.7 判定矩陣純函式,**D1b1 與 D1c 步驟 8b 共用**;0101 例外釘死拍板前提 NT$2,400+unpaid、order_number=UUID 強識別閘、結果 key set 精確比對)/ `d1-sweeper-control`(R24-R27 state+cron;排空判定含 pg_net + 70s 沉降 + 停前二次回查)/ `d1-orchestrator`(§8.4 步驟 8 前置 + 8b-13 + 驗收矩陣;**六態結果** completed/not_committed/not_committed_recovery_required/commit_outcome_unknown/committed_recovery_required/recovery_only,audit prepared→終態先落盤才 COMMIT、unknown 對帳先驗連線身分、advisory lock 金絲雀擋 6543 transaction pooler)。**審查鏈**:關卡1 三輪兩模型 44 條全折入(codex xhigh R1 24 + R2 8、Fable R3 換角度 12 —— F1=6543 只差一個 port 字元讓 single-flight 歸零;F5=守門焊死 production 會讓 D1t3 物理跑不起來⇒target seam);關卡2 = code-reviewer 兩輪(R1 FAIL:**Critical = pending_invoices 矩陣格用全表口徑,任何非 cohort 新付款單會讓 D1c 在不可逆交易當下 abort**;修畢 R2 PASS)+ codex 對抗審查 R1 FAIL 15 must-fix(**14 接受**:BEGIN 在 rollback fence 外、COMMIT 後 audit 寫失敗降級可重跑、stopSweeper 半途失敗不恢復、TapPay order_number 實為 UUID 須強識別、reconcile 未驗身分會被 clone 騙…;**1 駁回**:鐵則 4 拆片 —— 片界由規格 R11 定死、交接檔明令不重新設計,列 Sean 裁決)+ R2 確認輪。**驗證**:三綠 + 完整 pnpm test **3264 passed**;**突變測試 25/25 轉紅**(清單在 commit body)。🔴 **誠實邊界:未對任何資料庫實跑** —— 隔離 DB 實跑負測 = D1t3(D1c 硬前置、不可省);CLI/`--dry-run` 旗標/`--recover-sweeper` = D1t2。🟡 三題待 Sean(不擋 D1t2):D1t3 隔離環境怎麼供 pg_cron+pg_net / merchant id 第二來源 / §8.7「migration 註解」承接者(D1c 非 migration)。
🎉 **2026-07-29 傍晚 D1a6 還原演練實跑通過 —— D1 線 D1a0-D1a6 全部完成。交接檔 = `docs/handoff/2026-07-29-d1-line-handoff.md`(新 session 讀它 + 本檔即可接手,現行下一片 = D1t2)。**
**演練做法**:本機拋棄式 PostgreSQL 17.10 → 套用 **84 支 migration** → 造一份與正式站 cohort **逐格同筆數**的資料 → 跑**真正的匯出腳本** → 模擬 D1c 刪除(含刪掉 9 個父商品)→ 跑**真正的還原腳本** → 新連線重數。
**✅ 驗到**:十五張表重數全符 / 🔴 **3 張誘餌單的運送快照沒被 trigger 覆寫**(該防護第一次被真正驗到、不再只是斷言)/ trigger 已復原 `tgenabled='O'` / 父表從備份補回 9 商品 9 規格 / 重複執行當場被擋 / 🔴 **負向測試:把 Fable 的 BLOCKER 放回去 → 第一張父表就中止、整批 rollback、零寫入**(證明這場演練**抓得到**它,不只是「跑過了」)。
🔴 **誠實邊界**:**沒用 Sean 那包真的加密備份**(需他的 age 密碼;要補 = 跑一次 `scripts/d1-rehearsal.sh`)/ wrapper 的 TLS 步驟本機驗不到 / 環境是本機 PG 非 Supabase / pg_cron 那支 migration 跳過(需正式站 vault 密鑰)。
🔴 **順帶開出 #299(高)**:`public.product_fitments_effective` 在正式站是完整資料表(**148,716 列**、PK、3 索引、5 CHECK、FK、RLS),但 **`supabase/migrations/` 裡沒有任何一支建立它**;4 支 migration 引用它,其中 `search_catalog_by_vehicle` 是 `LANGUAGE sql`、本體建立當下就解析 ⇒ **repo 無法從零重建資料庫**,新環境一律卡在 2026-07-12(建 Supabase 分支實測 `MIGRATIONS_FAILED`、85 支只跑 54 支)。補上它之後 84 支全綠 ⇒ **它是唯一真因**。🔴 **#298 的版本號守門抓不到這一類**(兩邊完全一致 85/0/0 會回報「乾淨」;版本號對齊 ≠ migrations 建得出這個 schema)⇒ **先補齊(#299)再上守門(#298)**。
🔴 **Supabase preview branch 路線實測走不通**(已刪除止血):分支只給 **IPv6 直連位址、沒有 pooler**,本機連不上;而 `\copy` 是 psql 客戶端指令、MCP 跑不了。⇒ 演練環境改本機。
🔴 **安全**:操作過程中資料庫密碼一度被貼進對話(該值實為錯誤密碼、認證失敗,非有效憑證),已請 Sean 於 Supabase 重設。紀律:貼終端機輸出前刪掉含 `export D1_DB_URL=` 或 `postgresql://` 的整行;診斷用 `echo "長度=${#D1_DB_URL} / 開頭=${D1_DB_URL%%:*} / 尾段=${D1_DB_URL##*@}"`。

✅ **2026-07-29 18:06 D1a2 v2 重跑完成(15 表版,Sean 親手跑、全程對帳綠)**:十六份 CSV **同一交易快照**(`BEGIN`/`COMMIT` 確認),逐檔筆數與預測**逐格相符** —— 新增五張父表 customers 2 / customer_addresses 3 / products 10 / product_variants 10 / legal_terms_versions 2,原十表 orders 29 / items 39 / consents 4 / attempts 27 / invoices 3 / 五張 0 列各留表頭空檔 / manifest 29。manifest 記來源庫 `postgres` + 叢集 `7632885393857617092`。已 `age -p` 加密 = `~/d1-backup-20260729-v2.tar.gz.age`,🔴 **解密後 `diff -r` 逐檔比對一致才刪明文**(先證明救得回來、才丟原件);密碼在 1Password。⏰ **24h 時效重新起算 = 2026-07-29 10:06:04 UTC(台灣 18:06)**,逾時未執行 D1c 須再重跑。🔴 **早上那包(11 表、缺父表)已作廢**,改名為 `作廢-缺父表-…`;`preflight` 會主動擋下它。
🔴 **另:操作過程中資料庫密碼一度被貼進對話**(該值實為錯誤密碼、認證失敗,非有效憑證),已請 Sean 於 Supabase 重設 database password。**紀律**:貼終端機輸出前,含 `export D1_DB_URL=` 或 `postgresql://` 的整行一律刪除;錯誤訊息本身不含密碼、可安全貼。

🔴 **2026-07-29 16:xx D1a4/D1a5 還原腳本完成(備份已於 18:06 重跑補齊)**。三輪對抗審查 = codex `gpt-5.6-sol` xhigh 兩輪(R1 5 條 / R2 4 條,全 P1 must-fix,**駁回 0**)+ **Fable R3 換角度**(假設審查 / 災難當天可用性 / 修法回歸 / 測試假綠)抓 **9 條含 1 BLOCKER**。
**🔴 F1 BLOCKER = codex 兩輪 + 32 條突變測試全部漏掉**:父表驗證的 live 側沿用子表模板去查 `public.orders`,但父表補齊排在 orders 寫回**之前**、而 cohort 早被 D1c 刪光 ⇒ 子查詢恆空 ⇒ `v_live=0` ⇒ **第一張父表就 RAISE、四種組合一次都跑不完**。教訓逐字(Fable):「測試只驗腳本文字、從未對任何 DB 執行過,而它恰好藏在文字層驗不到的參數選擇裡」⇒ **D1a6 隔離 DB 實跑是 D1c 的硬前置,不是可選項**。
**Sean 07-29 下午三拍板**(詳 memory `project_m4b-d1-restore-backup-completeness`):**Q1=A 補匯出 5 張父表**(customers 2 / customer_addresses 3 / products 10 / product_variants 10 / legal_terms_versions 2,29 張口徑)—— `orders→customers` 是 **ON DELETE RESTRICT**、**D1c 刪掉訂單那刻這層保護消失**,之後任一父列被刪(地址刪除是既有功能)還原就整批失敗;**Q3=A `auth.users` 不備份**(含密碼雜湊),殘餘風險 = 客人自刪帳號則該筆救不回、**Sean 明列後拍板接受**;**Q2=A D1a7 移出第 1 批**(需 N3a 的 `pcm_generate_display_id()`;post-n3c 還原本來就只在第 3 批 N3c 後才用得到)。
**已修 18 條 findings 的重點**:①相依表原本零 assert(只有 orders 有)⇒ 十張表各加「筆數絕對錨 + 逐列逐欄雙向 `EXCEPT ALL`」;②`EXCEPT ALL` 是**拿載入的暫存表跟由它插入的資料互比 = 自我比對恆真**,證不了備份是對的那份 ⇒ 新增 `preflight`(連線目標 / 15 份 CSV 在場 / manifest cohort 26+3 相符 / 來源叢集)+ wrapper `scripts/d1-restore.sh` 走 `shasum -c`;③交易內 `system_identifier` **擋不掉實體快照 clone**(原樣保留同一值)⇒ 連線層才是唯一證明,production/rehearsal **兩個方向都釘死**;④psql 走 libpq、`buildD1PgConfig` 的離散欄位保護**沒參與** ⇒ 拒收帶 query string 的 URL + `PGDATABASE`(不進 argv)+ `PGSSLMODE=verify-full` 配 CA;⑤`orders` 的 BEFORE INSERT trigger 會**無條件覆寫**運送快照(trigger 自己的註解早就逐字預告)⇒ 同交易停用→寫回→復原,並 assert 已復原;⑥checksums.txt 原本記絕對路徑 ⇒ 解壓到別處會驗到**另一包檔案**、校驗形同虛設,改在備份目錄內產。
**驗證**:43 + 15 + 11 條測試;**突變測試 41 條全紅**(含 F1 blocker 專屬守門、`\copy` 方向 FROM→TO、COMMIT 提前到 assert 之前、wrapper 步驟被刪);三綠全過(3166 passed)。🔴 **仍未對任何資料庫實跑**。
⏰ **舊備份 `~/d1-backup-20260729.tar.gz.age` 作廢**(只有 11 份 CSV、缺五張父表;preflight 會擋)。新包輸出 `~/d1-backup-20260729-v2`,**24h 時效以重跑那次重新起算**;操作包 = `docs/runbooks/2026-07-29-d1a2-cohort-export.md`(已改版),災難當天的還原程序 = 🆕 `docs/runbooks/2026-07-29-d1-restore.md`(含 `HEADER MATCH` 報錯的正解、叢集識別碼合法變更的人工覆核、還原後 sweeper 會掃到 pending attempt 屬預期)。

✅ **2026-07-29 15:14 D1a2 正式匯出完成(Sean 親手跑、全程對帳綠)**:十一份 CSV **同一交易快照**(`COMMIT` 確認)、逐檔筆數與規格 §8.4 逐格相符(orders 29 / items 39 / consents 4 / attempts 27 / invoices 3 / 五張 0 列各留表頭空檔 / cohort-manifest 29);manifest 記錄 26 刪 3 留 + **0101 證據等級逐字入檔**(「Sean 本人查 TapPay 確認、非系統 read-back」)+ 來源庫 `postgres` 與叢集 `7632885393857617092`。已 `age -p` 加密 = `~/d1-backup-20260729.tar.gz.age`(11K),🔴 **解密後 `cmp` 逐位元組比對一致才刪除未加密檔**(先驗後刪、驗不過一個都不刪);密碼在 1Password。⏰ **24h 時效起算 = 2026-07-29 06:51:33 UTC(台灣 14:51)**,逾時未執行 D1c 須重跑匯出。🔴 **首次實跑抓到一個測試看不到的 bug**:psql 的 `\copy` 是**不跨行的 meta-command**,manifest 那道跨行 ⇒ `parse error`;十張表因 UUID 串在同一行而倖免。已修並補「每道 `\copy` 必須在自己那行結束」測試(第一版測試數 `\copy` 出現次數,折行後仍只出現一次 ⇒ 抓不到,突變當場打回)。連帶實證三件:①pooler 連線下 `session_user` 就是 `postgres`(先前擔心的守門自相矛盾**不成立**)②直連 `db.<ref>.supabase.co` **IPv6-only、本機不可達**(與 adapter docstring 一致)③`&&` fail-closed 在真實故障中生效(manifest 失敗 ⇒ 無 checksums、不會交出半套備份)。
✅ **D1a3 完成**(`a6de800`)= 還原用 26 組新號,**直接長在 `D1_DELETE_COHORT` 每一列**(不另開第二份清單 ⇒ 對錯行在型式上不可能發生)。依 §5.4a 用 production 的 `extensions.gen_random_bytes` 產(禁 `random()`);rejection sampling **只保留 < 252 再取 `% 28`**(252 = 9×28)。產生當下已驗:regex / 26 組互不重複 / 不撞留存 3 張新號 / 不撞現網任何 `display_id`·`legacy_display_id`。🔴 **此映射會過期**:N3b 換產號器後現網會長出新 6 碼單號 ⇒ **restore 執行當下必重驗碰撞、撞到依 §5.4a 重產、重產仍撞則 abort 人工** = D1a5 驗收條件。
🧱 **2026-07-29 午後 2:D1a1 補強 + D1a2 操作包已備**(`30ca25f` 起)。**改用 codex 官方 `codex review --commit` 審查模式**(先前一直手寫 `codex exec` 審查稿)⇒ 抓到 D1a1 三條、全採:①🔴 十道 `\copy` 在 autocommit 下各取不同時間點快照 ⇒ 還原會得到**從未存在過的狀態**(已框進單一 `REPEATABLE READ READ ONLY` 交易)②`\!` 失敗不中止 psql ⇒ 磁碟滿照樣印「完成」交出無校驗碼備份(已拿掉所有 `\!`、改 `&&` 串接)③manifest 沒記刪留範圍(已加 `cohort-manifest.csv`,含 0101「證據 = Sean 本人查 TapPay、非系統 read-back」)。另**重用 D1a0 守門**(不自創第二套)當匯出交易第一步 + manifest 記 `current_database()` 與叢集識別碼 ⇒ 連錯庫當場中止、備份能自證來源。突變 **16/16 轉紅**;三綠 + `pnpm test` **3106 passed**。📋 **D1a2 操作包 = `docs/runbooks/2026-07-29-d1a2-cohort-export.md`**(Sean 親手跑;含逐檔筆數對照表、24h 時效、age 加密;🔴 `age` 本機未安裝)。🔴 **誠實邊界:本腳本從未真的對 DB 執行過** —— 只驗到「產出的指令對」(selector 唯讀 count 與規格逐格相符、manifest SQL 語法實跑),沒驗到「跑起來對」。
🔬 **同日流程重審(Sean 要求「依官方現行建議重新設計」)**:主對話提四項優化,**Fable 獨立驗證打掉 1 項、修正 2 項**。🔴 **R2「審查 effort 降 medium」駁回** —— ①**跨模型外推**(官方那句講 Claude Opus 5,被拿去套 codex)②**選擇性引用**(原句 "fast pass **and a more thorough pass later**" 只引前半)③**牴觸既有拍板**(`00-work-rules` 逐字「effort 預設 high……已評估過,不要再提降 medium」)。🔴 **R3 理由錯**(偵測數謬誤:「typecheck 今天 0 條」≠ 沒價值,回歸閘的價值是反事實;等同「防火牆零起火所以拆掉」)、**R4 指標錯**(存活者偏誤:有效嚇阻不留事故證據 ⇒ 改用**消融測試**「拿掉這條還會不會做對」)。🔴 **最重要:四項合起來是四個閥門同向轉 = 被審方權力擴大 + 獨立驗證面全線收縮**,正好侵蝕「Sean 不核 diff、信任 = 字面等於事實」的地基 ⇒ **總閘:任何縮減驗證面的改動,先建立「該面失效時誰會發現」的偵測,再縮**(hook 失效無告警 = green-by-absence)。落檔 memory `feedback_review-process-shrinking-verification-surface`(含官方逐字 + URL)。✅ 已做:codex wrapper(`scripts/codex-run.sh`;🔴 實測 **codex 被 SIGTERM 仍 exit 0** ⇒ 早上「三次逾時」的歸因不可靠,改以 `tokens used` 完成標記判定)。🟡 **未做**:R3 hook 化與 R4 規則瘦身皆動 Sean 的檔案,且依總閘須先答「失效誰會發現」⇒ **建議 E10 第 1 批推完再做**。
🧱 **2026-07-29 午後:D1a1 完成(匯出腳本;`scripts/d1-export.ts` + 測試,共 148 行)** —— 一個 TS 檔把 psql 指令印到 stdout、由 psql 執行,不連 DB 不寫檔;cohort 直接 import `d1-cohort.ts`(不重抄 UUID = 不會漂移)。十張相依表逐表寫死 selector;🔴 **`order_refund_items` 實查自己就有 `order_id`**(規格說要繞 `order_refunds` 是多繞的)⇒ 改成兩條路都撈(備份寧可多撈);`payment_double_charge_anomalies` 走 `old_order_id`、其 events 表只有 `anomaly_id` 必須繞。**已用唯讀 count 對 production 實跑十張 selector,與規格 §8.4 逐格相符**(orders 29 / items 39 / consents 4 / attempts 27 / invoices 3 / 其餘五張 0)。突變測試 **7/7 轉紅**;三綠 + `pnpm test` **3102 passed**。🔴 **未做**:規格要求的「隔離 DB 測跑一次」**沒跑** —— `\copy` 的實際寫檔、manifest、CSV NULL 表示法都只驗到「產出的指令對」,沒驗到「跑起來對」。需要 DB 連線(`.env.local` 擋住)⇒ 併入 D1a2 由 Sean 執行時一併驗。
🧱 **2026-07-29 午:D1a0 完成(D1 線環境守門片、runbook 型高風險;三檔未 commit→已 commit)** = `scripts/d1-cohort.ts`(29 筆 cohort 凍結 + 指紋)/ `d1-guard.ts`(連線守門 + 交易內 SQL 守門)/ `d1-guard.test.ts`(38 條)。**codex `gpt-5.6-sol` xhigh 寫**,主對話 1 審,**Fable + codex 三輪對抗審查**。🔴 **共 11 條 must-fix 全接受、駁回 0**:**Fable F1 = 指紋只雜湊 `id+displayId`,兩陣列合併排序後歸屬資訊消失** ⇒ 把 `PCM-2026-0052`(留存、已付款)與 `0001`(待刪)整組互換,長度仍 26/3、UUID 仍 29 不重複、**指紋逐字元相同**(主對話實跑複驗成立)⇒ D1c 會刪掉有真錢的留存單;**F2 = `pg-connection-string` 的 query param 覆蓋位置參數**(repo 內 2.13.0 實跑:`?host=` 讓 pg 連 evil.example.com 而守門看到正式站;`?user=` 同理)。**codex R2 八條**:C1 同專案 shadow clone / 路徑留白吃 `PGDATABASE`;C2 `SET ROLE` 讓 `current_user` 過關但 `session_user` 是別人;C3 `sslmode=disable`;C5 **指紋函式掏空成 `return D1_COHORT_MD5` 測試全綠**(改吃 `rows` 參數修掉);C6 **SQL 插一行 `v_cohort_count := 29` 所有 `toContain` 全綠**(改成剝註解後比對語句序列);C7 denylist 少負測;C8 註解誇大 D1b1 涵蓋面(read-back 只查六筆、待刪組僅 0064/0090、0101 依 §8.7 不查、其餘 23 張無逐筆查證)。C4(`Reflect.set` 繞過 `as const`)判定為防禦深度非真實攻擊路徑,仍加 `Object.freeze`。**codex R3 換角度審「做法本身」** ⇒ 🔴 **判定「沒有任何 DB 側事實能唯一證明就是 production」**(Supabase clone 複製 schema/資料/角色/權限),身分唯一證明在連線層;主對話原假設「驗描述而非現實」**被推翻**(SQL 那半邊本來就在問 DB)。⇒ **Sean 拍板 A:強制 `sslmode=verify-full`、未帶也拒**(`require` 只加密、`verify-ca` 不驗主機名);codex 標「未確認」的 `pg_control_system()` 主對話**當場實查可讀**,正式站 `system_identifier = 7632885393857617092` 已釘進 SQL 守門,並寫明**擋不掉實體快照、Supabase clone 用哪種方式未確認**。🔴 **突變測試 21/21 全部轉紅**(含「把 0052 搬進待刪組」「指紋掏空」「SQL 插入賦值」「sslmode 放寬回 require」);**拍板 A 後突變一度抓到三條回歸假綠** —— 舊負例都沒帶 sslmode ⇒ 全被新關卡先擋掉、測試名稱說在驗 A 實際驗到 B,已逐條補成「除受測項外全部合法」。29 筆 cohort 以 `display_id` 為鍵向 production 反查、歸屬由 `paid_at` 決定、庫內 md5 `ba1e416a…83af` 與檔案端相符。三綠 + 完整 `pnpm test` **3114 passed**。🟡 **交接檔 ⑦「codex 三次逾時」的歸因存疑**:本 session 實測一次是 **stdin 未導向**(`< /dev/null` 修掉)、一次是外部 SIGTERM、一次原因不明 ⇒ **未足以推翻原紀錄,先不改交接檔**。🔴 **D1t1/D1t2 新增驗收條件**:①wrapper 不符時 process exit 非零、且 D1t1/D1t2 真的呼叫這兩個守門(前科:`assertDisplayId` 生產零呼叫端)②守門 SQL 必須與 DELETE 同交易(`RAISE` 才連帶中止)③CA 憑證正確時連得上 / 錯誤或缺漏時連不上,兩個方向都要實跑。
☀️ **2026-07-29 晨:Sean 五題全拍板、D1 線解封**(權威落檔 = v2 規格 **§8.0 拍板表 + §8.7 D1b1 條件改寫**;白話版 memory `project_m4b-e10-0729-morning-decisions`)。**A0a-1** 逐字「都沒扣到錢,放心刪除」⇒ 🔴 **cohort 維持 26 張、`PCM-2026-0101` 不移出**,但放行範圍**只解除它的無 `rec_trade_id` 硬閘** —— 其餘五筆 read-back 判定矩陣**不降級**、`bank_transaction_id` 替代**仍禁止**;0101 的無金流證據等級 = **Sean 本人確認,非系統 read-back**,須寫進 migration 註解 / cohort manifest / audit 三處。**A0c-1=A** ⇒ 主規格 §1 表第 19/26 列與小計已改(**2/6/19 → 2/5/20**),同檔 §0 與 §5.6 兩處過期的 `staff.ts:3` hardcode 字面一併修。**A0b-1=B** ⇒ 列表 **13 欄:留「年份廠牌車種」、拿掉「來源 · 管道」**,「12 欄」全檔作廢(決策視覺 artifact `68a00571-7efc-49e1-9758-472a650b156e`,三案同批資料同寬度並排)。**A3-1=A** ⇒ A3 加 `corrects_note_id`:**複合 FK `(corrects_note_id, order_id)→(id, order_id)`** 擋跨單更正 + partial unique(一筆最多被更正一次)+ `corrects_not_self`;append-only 不破壞;🔴 **U6 稽核查詢必須帶 `NOT EXISTS (… corrects_note_id = n.id)`** 否則點錯的「已告知客人」永遠算已履行 ⇒ **列為 A9a 驗收條件**。**A2-1=A** ⇒ A2 加 `order_item_procurement_receipts` 逐批到貨明細(append-only、service_role only);🔴 **新合約債**:`received_quantity` = 明細 SUM 這條等式 **A2 片無 DB 層強制**(M 型零 trigger)⇒ **列為 A4a 驗收條件**,A4a 之前由 A4b writer 同交易自持。兩支 migration 改後**已重跑 production 交易模擬**:全數通過、零留痕(orders 29 / order_items 39 未變)、**六個突變測試證明新斷言非假綠**(拿掉 quantity CHECK / GRANT INSERT / service_role 多權限 / 單欄 FK 擋不住跨單更正 / 無 partial unique / 天真版稽核查詢會誤算)。**關卡2 對抗審查**(`docs/reviews/2026-07-29-e10-a2-a3-expansion-k2.md`):🔴 codex **第三次同款逾時**(10 分零輸出)⇒ 依 R4「相同錯法第 2 次換路」不再重試,改由 **Fable(`adversarial-reviewer`, model: fable)** 接手 = **FAIL,must-fix 4 + nit 3,全數接受、駁回 0 條**。最重要一條 **F1 = 我自己在 A3 註解裡寫的「更正鏈迴圈物理上形不成」是錯的** —— PG 非 deferrable FK 到**整條 statement 結束**才驗,單筆多列 `INSERT` 兩列互指就成環(**主對話與 Fable 各自獨立命中同一條、production 實測成環 2 列** ⇒ certain)。已改成「單列 INSERT 紀律下不可達」+ 把 **A6 只准單列 INSERT**、**A10a 走鏈帶 visited 與深度上限**寫成驗收條件 + **加一條固定探針把破口釘住**。其餘:F2 更正**不可撤回**、要恢復只能重登一筆新的原始紀錄(合約已釘、🟡 產品面若要改成可還原請說);F3 稽核探針原本只測排除方向 = **半條假綠**(已補「更正前必須算 1 筆」);F4 receipts **無冪等鍵**、A4b double-submit 會讓到貨數虛增(已記債 ④ + A4b 驗收條件);F5 兩支註解寫的「D1c 排在本片之前」與**實際 apply 序相反**(已改,並在 §5.1 D1c 列加前置:DELETE 前必先斷言兩張新表對 cohort 零引用);F6 FK 斷言裸撈;F7 空白字元宣稱過寬。折入後重跑 production 模擬:更正前 1 筆 / 更正後 0 筆 / 鏈可延長 / 成環 2 列 / FK 1 支 / 探針零殘留 / rollback 順序 `2BP01` 驗證,**全部通過、DB 零留痕**。🎉 **三支已全部 apply 到 production(2026-07-29 晨,Sean 指示「你直接處理」)**:`20260729010000` D0 → `20260729020000` A2 → `20260729030000` A3,`supabase db push` 依檔名序一次套完、**三支的 fail-closed 驗收 NOTICE 全部在 production 實跑通過**。🔴 **繞過 `.env.local` 的方法**:CLI 只讀當前專案根的 `.env.local`(該檔有壞字元 `¿` 會讓 CLI 整個拒跑,而 Claude 被 deny 硬擋不能碰 `.env*`)⇒ 把 `supabase/` 複製到 scratch 目錄用 **`--workdir`** 指過去即完全繞開;憑證在 macOS keychain、全程零密碼提示;**零碰 `.env*`、零改 repo**(詳 memory `reference_supabase-cli-reads-env-local-blocker`)。**apply 前後對帳**:orders 29 / order_items 39 / attempts 27 / pending_invoices 3 **一列未動**;orders 34→**35 欄**(+`legacy_display_id`,29 列全 NULL);三張新表建立且**全部 0 列**;`orders_display_id_format` 已放寬成新舊雙格式(29 張舊號仍全數合法、新格式 0 張);`pending_invoices_status_check` 已含 `voided`;6 支新索引到位;三張新表 **RLS 全開**、**anon/authenticated 對三張表的 SELECT 權限合計 = 0**(原則 3 守住、供應商資料沒洩漏給客人);A9b1 的 `.or()` 搜尋形狀實查命中 1 筆。**ledger 零漂移**:`migration list` 85 支、local-only 0、remote-only 0、版本號不一致 0;`array_length(statements,1)` = 17/26/18(**>1 = db push 來源、非 MCP `apply_migration`**)。
🎉 **A9b1 + A10c1 端到端驗收通過(Sean 2026-07-29 肉眼驗)** —— flag 開啟後:輸入 `PCM-2026-0104` **命中 1 筆**、輸入亂碼 **顯示 0 筆**(fail-closed 成立、沒有退化成「不篩選 = 列出全部 29 筆」)。⇒ 🔴 **v2 §5.1 A10c1 逐字要求的「D1 開工前置」現在才真的滿足**(先前只是程式碼就緒 + DB 就緒、端到端一次沒跑過)。過程踩到:改 Vercel 環境變數**不 redeploy 不生效**(repo 既有實證 commit `03e4c13`)⇒ Sean 重新部署後才出現。**pcm-admin 的正式分支 = `dev`**(Vercel 實查:最近 20 次 production 部署來源分支全是 `dev`;`main` 守的是顧客站)⇒ 後台改動不需推 main。
✅ **`database.types.ts` 已重 gen**(schema 已變 ⇒ A5a/A6 開工前置解除):新增 `orders.legacy_display_id` 與三張新表型別,並補回 07-13 之後累積未同步的 migration。🔴 **重 gen 沖掉的人工校正已重貼並擴充**:`create_order` 的 `p_client_ip` / `p_client_ua` / **新的第 9 參 `p_notification_email`** 三者在 DDL 都吃得下 NULL(production 實查函式簽章逐字 `p_notification_email text DEFAULT NULL::text`),但 PostgREST 產生器**表達不了「必填但可為 null」** ⇒ 不校正金流建單路徑會型別紅。⚠️ 本次重 gen 另移除三個 view(`products_public` / `products_list_public` / `product_variants_public`)的 `Insert`/`Update` 型別 —— 已實查消費端全是 `.select()` 讀路徑、寫入一律走 base `products` 表 ⇒ 無影響,由 typecheck 把關(全綠)。三綠 + 完整 `pnpm test` **3076 passed**。
🌙 **2026-07-29 過夜施工 session(已收工)**:Sean 最終批准後從 A0a 起跑、照 v2 §5.0 DAG。✅ **A0a 完成**(`docs/reviews/2026-07-29-e10-a0a-data-recheck.md`)—— 表列數 / cohort 分佈 / attempts 狀態分佈與 v2 §8.4 **逐格相符**(orders 29、items 39=36+3、attempts 27=24+3、`pending_invoices` 3 全屬留存、consents 2+2、待刪 attempts `failed` 12 / `released` 9 / `pending` 3);`payment_method` distinct = `{tappay}` ⇒ **未映射值 0、canonical mapping 驗收過**;`fulfillment_status` 全表單一值 `notOrdered` = Q1=B「這欄沒有 writer 在維護」的實證;D0 三個前置字面已順手實查(`orders_display_id_format` / `orders_display_id_key` / `pending_invoices_status_check` 只允許 `pending,issued`)。🔴🔴 **A0a 命中規格自己寫的硬停條件:`PCM-2026-0101` 的 `payment_charge_attempts.rec_trade_id` 是 NULL**(六筆中唯一;該筆**有** `bank_transaction_id`)⇒ 依 v2 §5.1 D1b1 逐字「任一 NULL = 停下 raise Sean,**不得用寬條件替代**」,**D1b1 / D1c 停工等 Sean 拍板**(決策題 **A0a-1** 三選項見該檔 §6,主對話推薦 **B 把 0101 移出 cohort**)。✅ **A0b 完成**(`docs/reviews/2026-07-29-e10-a0b-code-recheck.md`)—— v2 §2.2 的 repo 斷言**逐條成立、零更正**(7 route / 6 個 `'use server'` / 4 支 admin owner RPC / `order_refunds` 在 admin 零引用 / 取消入口確實不存在 `order-detail.tsx:233-239` / rowSpan 從手上 `lines` 算 `orders-table.tsx:65-67`);`<AdminDataTable>` 2 個消費端(customers·staff)、**orders 沒接** ⇒ §7.3 與現況一致;`<AdminForm>` 已有 4 個消費端(含 `order-edit-form`)⇒ 第 1 批表單類 UI 有現成積木。stale reader 七處**全中零漏網**,`packages/` 層另有 adapter/mapper/型別 readers 但**已被 A9r/A9s 涵蓋、不需新片**。🟡 **A0b 查出 §5.1a 版面規格內部不一致**:12 欄清單少了「年份廠牌車種」(V-3b 專做)與「來源·管道」、多了「發票/操作」,但動作表只解釋 4 個動作 ⇒ 決策題 **A0b-1**(擋 A11a、不擋現在,推薦 B 保留車種欄)。✅ **A0c 完成**(`docs/reviews/2026-07-29-e10-a0c-27item-reconcile.md`)—— 27 項中 **25 項與 07-26 read-back 一致**,查出 **2 項漂移**:🔴 **第 19 項(取消訂單)被高估** —— 記 ⚠️「原因是自由文字」但全樹**零 `cancelled_at` 寫入路徑**(`types.ts:290` 逐字「本片純顯示、取消功能留取消片」)⇒ 實為 ❌;**第 26 項證據行過期** —— `staff.ts:3` 在 E8-A1 後已非 hardcode 名單(改 `listStaffRows` 讀 DB、`:3-4` 自陳非授權邊界),狀態仍 ❌ 但理由變了。⇒ **分母更正 = ✅2 / ⚠️5 / ❌20**(原 2/6/19);**E10 §6.1 的 17 項組成與第 1 批綠燈宣稱皆不需改**,只有「相對於什麼變綠」的基準要改。決策題 **A0c-1**(要不要動主規格那張驗收表,推薦 A 改)。⇒ **v2 §2.4 列的三項誠實邊界已全部清掉**。✅ **D0 完成(migration 檔已寫、🔴 未 apply)** = `supabase/migrations/20260729010000_m4b_e10_d0_display_id_expand.sql`:`legacy_display_id`(nullable+unique+格式 CHECK)/ `display_id` CHECK 放寬收新舊兩格式 / `fulfillment_status` 停止維護 COMMENT / `pending_invoices` CHECK 加 `voided`。**關卡2 codex 兩輪**(`docs/reviews/2026-07-29-e10-d0-k2-codex.md`):**R1 FAIL 9 must-fix 全接受零駁回**(最重要=缺 `lock_timeout`、驗收用 `position` 找子字串是假綠)→ **R2 7 關閉/2 部分關閉 + 新 6 條再折入**(ACL 補欄級 grant 實查、RLS 改整組 policy 精確比對、sentinel 改自訂 SQLSTATE、負向探針核對 CONSTRAINT_NAME)→ **達 2 輪上限、停止折衝**。實跑證據:production 交易模擬通過、**7 條負向探針全被正確約束擋下**、**突變測試證明斷言非假綠**(`{6}`→`{1,6}` 確實 fire)、DB 與工作樹零留痕。🟡 兩條交 Sean:**D0-1**(全 repo migration 都自寫 BEGIN/COMMIT 與 CLI 記帳的窄縫,推薦 A 不動或 C 開 backlog)、精確定義比對對 PG 版本敏感(已判定為 fail-closed 取捨、保留)。✅ **A9b1 完成**(單號搜尋合約、A 型標準片)= 新增 `packages/domain/src/order/order-number-search.ts`(`normalizeOrderNumberSearch` 正規化 + 形狀守門)+ `AdminOrderFilter.orderNumber` + `SupabaseOrderAdapter` 用 `.or(display_id.eq.X,legacy_display_id.eq.X)` 同時比對兩欄。🔴 **fail-closed 設計**:輸入了但格式不符 ⇒ **回零筆且完全不查 DB**,不得退化成「不篩選 = 列出全部訂單」。**code-reviewer(opus)FAIL 3 must-fix 全接受**:①舊格式流水號 `\d{4,}` 無上界 ⇒ 超長輸入會撐爆 `.or` 的 GET query string、PostgREST 414 ⇒ 列表整頁錯誤態(已加 32 字上限)②「與 migration 同形狀」的測試只 pin 自己的 `.source`、根本沒讀 migration = 假綠(已改成 `readFileSync` 真的讀 D0 檔比對兩條 regex 字面)③對「尚未 apply 的 D0」有硬 runtime 依賴卻沒寫(已在 adapter docstring 寫死)。另清 5 條 nit(其中 `toUpperCase` 會把 `ß`→`SS`、`ﬀ`→`FF` **靜默改寫成別人的單號** ⇒ 已加非 ASCII 守門)。三綠 + 完整 `pnpm test` **3050 passed**;SQL 交易模擬證明「改號後用舊號查得到、解析回新 `display_id`、未知號零命中」。✅ **A10c1 完成(程式碼層;🔴 端到端驗收未跑)** = `order_no` URL 參數 + 篩選列搜尋輸入框,**掛 env flag `ADMIN_E10_ORDER_NUMBER_SEARCH`、預設 off**(§7.1 逐批啟用閘)。🔴🔴 **D0 apply 之前不得開這個 flag** —— 開了會 42703、**整個後台訂單列表**(不只搜尋)進錯誤態。**code-reviewer(opus)FAIL 3 must-fix 全接受**:①元件檔頭前提註解逐字仍寫「query 全集=**五**篩選軸」,我加了第六個鍵沒同步 ⇒ 下一片依它判斷會漏算(已改六軸並寫明漏列=fail-open)②A9b1 在 adapter 留的安全註解說「現況安全靠 param 還沒接」,本片正是接了 ⇒ 註解說謊(已改成「安全靠 flag 預設 off」)③**驗收口徑**:規格 §5.1 A10c1 逐字要求「輸入舊號或新 6 碼**皆命中**」且「本片與 A9b1 驗收通過 = **D1 的開工前置**」,但 D0 未 apply + flag off ⇒ **端到端一次都沒跑過** ⇒ 🔴 **A9b1+A10c1 目前不構成 D1 前置,只是程式碼就緒**。另清 6 條 nit,其中三條是真的會咬人的:`type=search` 的原生 × 只清草稿不送出(使用者以為取消了、其實還篩著)、form 的 input 有 `name` 但 form 無 `action`(hydration 前按 Enter 走原生 GET、把其他五軸篩選清掉)、無條件 `setDraftNo` 會在等其他軸回音時把打到一半的字清空。三綠 + 完整 `pnpm test` **3071 passed**。✅ **A15 完成**(TapPay `recordQuery` 可選中止訊號、**高風險金流片**)= port `ITapPayAdapter.recordQuery` 加 `options?: { signal }`、adapter 傳進 fetch。**第 3 批退款 worker 經 port 注入呼叫,port 不開這參數 worker 就傳不了 signal、逾時控制形同不存在。** 🔴 **雙審 PASS**:**codex 關卡2 PASS(3 nit)** —— 獨立確認 `settle-charge.ts:76` 是裸 `catch`、任何 throw 都落 `pending/record_unreachable`,**中止不可能被誤判成 failed**(誤判會把已扣款的單移走);**Fable 深審 PASS(3 consider + 2 nit)** —— 🔴 抓到一條真陷阱:`AbortSignal.timeout()` 丟的是 **`TimeoutError`** 不是 `AbortError`,port JSDoc 原本寫死 `AbortError`,worker 若照 name 分支會把自家逾時誤判成真失敗(**已改寫契約:丟的是 `signal.reason`、消費端一律當 unknown-state 保留重試、不得按 name 分支**);另落檔兩條給第 3 批:①TS 對「多帶一個參數」可賦值 ⇒ **忽略 options 的實作照樣 typecheck 全綠**,worker 測試必須斷言 signal 真的到達 fetch ②逾時只開了 `recordQuery`,真正動錢的 `refund()` 還傳不了 signal。**突變測試**:刪掉 `signal: options?.signal` 一行 ⇒ 36 條中 4 條紅 ⇒ 非假綠。三綠 + **3076 passed**。✅ **A2 完成(migration 檔已寫、🔴 未 apply)** = `order_item_procurement` 採購真相表(每 order_item 可多列 = 拆給多家供應商)。**關卡2 codex R1 FAIL 4 must-fix 全接受零駁回**:①ACL 驗收只點名三個 role,沒查 `relacl` grantee allowlist 與欄級 `attacl`(已改 allowlist)②`ON DELETE CASCADE` 與「採購事實是真相」衝突、刪單會讓供應商單號與到貨數無聲消失(**已改 `ON DELETE RESTRICT`**)③`supplier_order_no` 未正規化 ⇒ A9b2 等值搜尋漏單 ④rollback 註解漏列消費端。🔴 **codex R2 連兩次逾時 ⇒ 依 R4「相同錯法第 2 次換路」不試第三次,改由 Fable 做確認輪 = PASS**,並抓到 R1 第 3 條**其實沒真的關掉**:**`btrim` 只剝 U+0020、剝不掉全形空白 U+3000 與 tab**,而中文輸入法打全形空白極常見 ⇒ 已改用 `[[:space:]]` regex,並**獨立實跑驗證**(對照斷言證明 `btrim` 確實剝不掉 U+3000 = 修法必要)。實跑:交易模擬全過、8 個 CHECK、**9 條行為探針**(含 1:N 成立與全形空白被擋)、負向探針全部核對 `CONSTRAINT_NAME`、零留痕。🟡 兩條合約債寫進檔案註解:canonical key 尚未由 DB 強制等於 A5b 輸出(列為 A5a 驗收條件)、無逐批到貨明細(決策題 **A2-1** 交 Sean)。✅ **A3 完成(migration 檔已寫、🔴 未 apply)** = `order_notes`(append-only;三型:`internal` 內部備註 / `contact_log` 聯絡紀錄 / **`customer_notified` 已告知客人 = U6 告知義務的證據**,獨立成一類才查得到、稽核得了)。**關卡2 codex R1 FAIL 4 must-fix**:①`occurred_at` 沒擋 `infinity` 與荒謬年份 ⇒ 假的告知證據(已加有限值 CHECK;「不得晚於現在」擋不進 CHECK 因 `now()` 非 IMMUTABLE ⇒ 那條是 A6 RPC 的責任、已寫明)②🔴 **`[[:space:]]` 擋不住零寬字元 U+200B/FEFF**(實測確認)⇒ 一整串零寬空白的備註會被當成有內容(已改 `translate` 先剝再判;**並回頭修了 A2 的 `supplier_order_no` 同一個洞** —— 實測證明前置零寬單號原本會通過)③append-only 措辭說太滿(已收斂成「應用路徑改不到」、明寫 owner 與 superuser 仍可改、trigger 也擋不住 owner 停用)④ACL 沒精確證明 service_role 是 SELECT only(只檢四種寫權會漏 `TRIGGER`/`REFERENCES` 誤授,已改成列出它實際持有的權限集合比對)。實跑:交易模擬全過、7 個 CHECK、含「零寬字元夾在真內容裡不得被誤殺」的正向案例、零留痕。🟡 **codex 第 4 條(`customer_notified` 誤選後無更正/作廢路徑 ⇒ 稽核永遠算成已履行告知義務)不自行加欄** —— 依 §1 原則 2「欄位不得早於它的模型」,列決策題 **A3-1** 交 Sean。
2026-07-27(**Eazi-Grip 每日同步修復已正式上線**)=根因為同商品群 spec 換位在 bulk upsert 中途撞 `pv_spec_unique`、非爬蟲漏抓;已分流 transition hazard 到單群 atomic RPC(鎖定→重驗→sentinel→孤兒刪除→完整 upsert→終態 assert),保留唯一鍵並鎖 service_role。✅ migration `20260727084801` 已 apply production + ACL/signature 驗證全綠,修復 commit `246fedd` 已推 `origin/dev`,正式單家同步成功:1686 群/5283 變體、atomic HOSEYAM005 3/3、0 孤兒/0 下架/0 價格異常;DB 終態=base 黑/BL 藍/RED 紅、sentinel 0。獨立唯讀複查 PASS。**同日較早規劃/文件 session**=①STATUS 主表 **357→30 行**(`dc46f14`;全文存 `PROGRESS.md`)②E8-B plan v3③完整後台規劃交接。
🆕 **2026-07-27 深夜 E10 規劃 session**(零 code、僅新增總規劃與交接兩份 docs):施工序改「先 E10」、Sean **P1-P4+E1-E4 八題全拍板**(訂單編號改 6 碼亂碼 / 九個壓縮碼退場改三軸 / 分三批驗收)、修掉報價單 LINE 推送**預設外洩成本價**(`bbb8e32` 未推)、查出**報價單不能跑 `db push`**(本地 146 檔 vs ledger 160 筆版本號零重疊)。E8-B plan **R2 FAIL 34 must-fix、兩輪上限用盡需重寫**。
🔴 **同日後台規劃 session 最重要發現**:報價單站的**整套 2FA 是「全公司一組」不是「每人一份」**(`totp_devices` **13 欄**(原記 14 欄=錯、07-27 production 實查更正)與 `recovery_codes` 7 欄皆無 `user_id`;`auth_state.last_consumed_step:28` 逐字「全域 TOTP 防重放」⇒ **員工 A 用掉某 30 秒窗,員工 B 同窗的合法碼會被判重放、登不進去**;2FA 管理六路由全域;「記住裝置」零實作;`login/route.ts:127-131` legacy cookie fail-open)⇒ **「動 4 處」是低估,重拆為 B0-B10 十一片**。**codex 關卡1 R1 = FAIL 17 must-fix、逐條核對全數成立、駁回 0 條**(對帳表 plan §9);主對話 v1 自己寫錯三處已更正(詳 plan §1.4/D2/D3)。**Sean 六題全拍板**(Q6=A 一路做完才上線 / Q1=A 備援唯讀 / Q5=A 只有 sean 管 2FA / Q2=A 舊裝置歸 sean / **Q3=C 帳號用 email** / **Q4 先答 B 聽完風險後改 A 強制改密碼**)。**更早的逐片紀錄 → `PROGRESS.md` 存檔區。**

## 最近 3 commit
> dev。🏁 **2026-07-25 Sean 拍板 A:不寫死 hash 與未推數**(寫死的自指數字每多一個 commit 或每次 push 就當場變假、同款前科已 10 次)→ 一律當場執行取得:`git log --oneline -4`(第 1 行=本 commit,其後 3 行=前序)/ `git rev-parse --short origin/dev` / `git rev-list --count origin/dev..HEAD` / `git merge-base --is-ancestor <hash> HEAD`(驗可達、防 busboy off-by-one orphan)。⚠️ `dev` = pcm-admin 的 **production** 分支,push 即部署;`origin/main` 同理不寫死。

## 下一步
🔴🔴 **施工序已於 2026-07-27 深夜改為「先 E10 訂單閉環、E8-B 認證線押後」(Sean Q1=A、二次確認 Q6=A)。唯一入口 = `docs/handoff/2026-07-27-e10-order-closure-handoff.md`**;總規劃本體 = **`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md`**(Sean 拍板在 §8/§4;**第 1 批片數一律以該檔 §5.1 表格 `awk` 當場數為準、本檔不寫死**,第 2/3 批開批才拆片 —— v1 就是靠寫死數字寫出 24/22 與 13/12/15 三組互相矛盾)。~~v1 `2026-07-27-…-master-plan.md` 已作廢~~;~~舊入口 `2026-07-27-admin-full-planning-handoff.md` 的施工序已被推翻~~。
- ✅ **2026-07-28:findings 已全數處理、總規劃已重寫** → **`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md`**(v1 作廢)。逐條裁定 = `docs/reviews/2026-07-28-e10-k1-findings-triage.md`:去重後**唯一 findings 67 條(must-fix 62 + nit 5;原始檔把同一份清單印了兩次,「約 60」= 62 的約數)**,**駁回 0 條**,其中 3 條是「codex 對、v1 主對話技術斷言寫錯」(篩選/彙總徽章/排序)、1 條部分成立(STATUS 已同步、CURRENT 未同步→已修)。🔴 **另補 2 條 codex 沒抓到的**:①`order_items` 與 `orders` 一樣對客整表開放 SELECT ⇒ 供應商資料放品項層會把上游洩漏給客人 ②Sean 拍板「訂貨/出貨可隨時來回改」與既有 `FULFILLMENT_TRANSITIONS` 禁倒退狀態機衝突。✅ **Sean 07-28 四題全拍板、已折入 v2**:**Q1=B** 訂單層彙總狀態不維護(`orders.fulfillment_status` 凍結、不加 `backorder`、不做計數器→enum trigger ⇒ v1 的 O5 整片取消,4 條 findings 隨之消失;🔴 推翻主規格 §3.1「必須維持實體 enum 欄 + trigger 同步」)/ **07-28 Q2=A** 砍 26 張無金流單 + 3 張有金流的改號(⇒ 新增 **D1 片**、雙格式支援整片取消,編號線 3→2 片)/ **Q3=A** E10 吃下退款寫入線 RF2b-RF8(第 3 批因此變大=知情選擇)/ **Q4=A** 報價單文案已修(`36cf77c`;R12 nit 更正:**實已在報價單 origin/main**,另一 session 推的)。🔴🔴 **關卡1 已跑四輪、codex 判定「未收斂」**:R1 **67** → R2 **33** → R3 **31** → R4 **32**(findings 各存 `docs/reviews/2026-07-28-e10-k1-r{2,3,4}-codex.md`)。R4 逐字:「**未收斂**:R4 仍重複出現 R2/R3 的 D1 安全、取消閉環、計數器真相、片大小與文件同步問題」。Sean 拍 Q15=D 續折。**R5 = FAIL 26 條但性質轉變**(從文件自相矛盾 → 對 production 實查會失敗的規格)且**首次實質下降**;codex 診斷 = 主因規格深度、另 **4 項真未定需求集中在 v2 §8.6 待 Sean**(已到貨取消 / 取消原因映射 / P1 後綴下場 / HCT 允許字元)。# 🎉 **R20 = PASS(2026-07-28,20 輪全程 67→33→31→32→26→24→15→17→16→7→5→4→7→10→10→6→5→7→3→PASS;附 2 nit 已修)**。codex 逐字「規格已達可施工狀態」。**✅ Fable 四線復盤已完成**(`docs/reviews/2026-07-28-e10-fable-retrospective.md`,14 must-fix 已折入;R21 確認輪抓折入五處只折半邊、已修)。# 🏁 **確認輪 R28 PASS(零 must-fix,2026-07-29)** —— 全線收官:主線 20 輪 + 復盤 4 線 + 確認 8 輪。codex 逐字「規格已達可施工狀態,現在可送 Sean 最終批准」。# ✅ **Sean 已最終批准(2026-07-29 凌晨)→ 過夜施工中**。授權範圍 = **不需批准的部分**:寫 code/migration 檔/測試/模擬,從 A0a 起照 v2 §5.0 DAG;**過夜禁區 = D1c apply、production `db push`(Sean 操作)、push、deploy**。分工 = Opus 主對話 / Codex(`-m gpt-5.6-sol` 顯式)寫機械片 / Fable(adversarial-reviewer, model:fable)+ codex 關卡2 審高風險。每片一 commit + STATUS 當場更新。**早上交 Sean:進度表 + 待批清單(db push 清單、D1 證據包)+ blocker**。✅ Sean 07-28 三拍板已落:Q17=B / Q18 照草案 / Q19=A。R7-R8 深水區 = **金流取消語意**:①`confirm_payment` 實查零 cancelled 檢查 ⇒ A8c1/A8c2 金流守門(**先上**;R10 更正鎖點 = `begin_charge_attempt` + `confirm_order_payment`)+ 三支 RPC 四施工片統一 `FOR UPDATE` 鎖序 ②第 1 批取消 fail-closed 收斂到無退款需求的單、A8b 移第 3 批解鎖線 ⇒ 第 19 項部分綠(整單閉環/部分取消安全但未閉環)③A7b 補 `bank_refund_id` 外部冪等(TapPay 親驗)、reconcile 基準欄、`submitted` 狀態(退款隔日生效)、claim token CAS。🟡 **R8 升兩題為 §8.6 第 3 批開批閘**:混合收款退款分軌 / partiallyPaid 應退額語意。🆕 **Sean 07-28 晚間指示(換 Fable 後)**:關卡1 全數審過之後(假如)→ **Fable 全面復盤一次、ultracode 已開、派多 workflow 分工審查**(= 鐵則 7 例外之「Sean 親口要的審查多代理」;範圍互斥不重疊)。復盤四線:①拍板一致性(全部 Q/P/E/U/N 拍板 vs 全樹落檔)②規格 vs repo 事實逐條 ③D1 runbook 安全攻擊 ④六輪 findings 流程復盤與制度教訓;主對話 Fable 彙整裁定。✅ **全貌視覺已交付並經 Sean 批准方向**(artifact `d4229e23-1e68-4315-bd3f-5811372efcaa(2026-07-29 終版;舊 ed7a6276 已失效)`;Q8=B 的動工前置之一已滿足)。🔴 **R4 抓到兩個「第 1 批設計實際不成立」的硬傷**:①取消片在包裹模型不存在時無法證明未出貨 ⇒ 照現行寫法**一件都取消不了**,卻宣稱第 19 項變綠 ②`instock_quantity` 在採購表裡**沒有到貨數量欄可推導**(R3 已提、未修)。🔴 **第 1 批現為 69 片**(片號與片數**一律以 v2 §5.1 表格 `awk` 當場數為準**,本檔不再複製清單;14→…→67→69 = 十八輪審查逐步重拆;~~O0-O7~~ 作廢)。🔴 **D1c 是唯一不可逆片**(D1 線現拆十四片 D1a0-D1c;片數以 v2 §5.1 為準):前有 Sean 獨立批准閘;`payment_charge_attempts` 待刪 24 / 留存 3,**`pending_invoices` 零刪除、唯一動作 = D1c 將三筆 `status` 更新為 `voided`**(R6 抓字面統一;~~「一筆都不能動」與「必須先清才可 DELETE」兩種舊寫法皆作廢~~);待刪組含 **3 筆 `pending` 刷卡紀錄**(0064/0090/0101,合計 NT$51,100)—— **Sean 07-28 確認「都沒扣到錢」拍 Q7=C 照刪**,證據等級=Sean 查 TapPay、非系統 read-back。v1 原始抽驗三條備查:①`internal_note` 放 `orders` 會洩漏給客人(`20260604120000…:190` 逐字 `GRANT SELECT ON TABLE orders TO authenticated` 整表全開)②**P2 建議與已拍板的 U7「Phase 1 人工查+手標已送達」直接衝突**(主對話流程錯誤;**已重問→Sean 拍 B「送達完全不做」= override U7=A**,連動不加 `delivered` enum 與 `delivered_at` 欄,三份檔已同步)③6 碼碰撞機率報錯(實為 5 碼 23.0% / 6 碼 0.929%,非 12.6%/0.4%;結論用 6 碼仍成立)。依鐵則 8/12,**Sean 批准 + 關卡1 通過 + 視覺全貌過目(已完成)三閘全過才動第一片 code**;輪次上限已由 Q8=B 解除。
- **E10 第 1 批片數以 v2 §5.1 為準**(P4=B 分三批、每批 Sean 肉眼驗;~~舊 O0-O7 片號全數退場~~)。起點 = **A0a/A0b/A0c 現況重驗**(orders 筆數、E11 積木採用度、27 項逐項重驗),其後 **D1 前有 Sean 獨立批准閘**。🔴 **R3 = FAIL 31 must-fix + 2 nit**(`docs/reviews/2026-07-28-e10-k1-r3-codex.md`),Sean 07-28 再拍 **Q10=A 包裹獨立編號**(解 P1 與 U1 衝突:併箱時沒有唯一基底訂單號)/ **Q11=C 文件二分**(施工規格給 AI 深寫、全貌給 Sean 走視覺)/ **Q12=B 前台改顯示「處理中」**(Q1=B 後 `fulfillment_status` 停止維護,但**全樹 8 處仍在讀、其中 2 處在客人前台** `OrdersTab.tsx:45`·`OverviewTab.tsx:108`)。✅ **過夜完成 9 片**:A0a / A0b / A0c(docs)→ D0 / A2 / A3(M,**檔已寫、未 apply**)→ A9b1(A)→ A10c1(U,掛 flag 預設 off)→ A15(A,高風險金流、雙審 PASS)。🔴 **下一步(2026-07-29 上午;三支 migration 已 apply、端到端已驗、型別已重 gen)** —— **三個開工前置全部解除,可直接進 §5.0 DAG**:✅ **D1a0-D1a6 + D1t1 + D1t2 + D1t3 已完成**(2026-07-29/30,詳最後更新欄;執行器與它的故障證明全部收工)⇒ ~~下一步 = D1b1~~ 🛑 **2026-07-30 Sean 拍板:D1c 刪除取消、D1 線停工**(理由與實測發現見最後更新欄與 memory `project_m4b-d1c-delete-cancelled`)。✅ **Sean 2026-07-30 拍 Q1=A(提案 `docs/specs/2026-07-30-order-renumber-instead-of-delete.md`):只換產號器、舊 **30** 張完全不動(筆數見本欄「最後更新」的更正)。** ⇒ **編號線施工範圍 = N2(domain 驗證改兩收)+ N3a(產號函式)+ N3b(`create_order` 改用它)三片**(N2 由 07-30 第二輪 Q3=B 加入);N3c 收窗不做、CHECK 永久維持新舊兩收;D1 線(D1a0-D1c)全線退場、已寫資產不刪留備用。接受的代價(Sean 已知)= `display_id` 新舊格式永久並存。🟡 Q2(舊 30 張測試單要不要在後台列表隱藏)未答、不擋施工。✅ **N2/N3a/N3b 三片已 commit 完成(2026-07-30 中午)**。🔴 **現行下一步 = Sean 拍板 C:先修 backlog #301(TapPay Record API 三處欄位重查官方文件 + 更正 `wire.ts`)**,它是退款線 RF2b-RF8 的硬前置。其後 ②回到 E10 真正推進「員工能在後台取消訂單/退款」的片。🔴 提案的核心觀察:**改舊單號碼對「客人推測不出訂單量」幾乎沒幫助**(拿著舊號的人早就看過了)⇒ 目標 100% 由換產號器達成。實查:`display_id` 的 CHECK 早已放寬成新舊兩收且已 apply、`legacy_display_id` 已存在、搜尋已吃兩欄 ⇒ **系統此刻已能接受 6 碼新單號**,只差 `create_order` 還在產舊格式。以下 D1 相關敘述保留供改號路線重用(`RENUMBER_SQL`、碰撞檢查、`legacy_display_id` 那套)。施工前盤點見交接檔 §⑩:唯一權威 = v2 **§8.7**(不是 §5.1 row 19 舊字面);判定與查詢三支模組都在,缺口 = **CLI 沒有獨立 read-back 入口**,推薦新增唯讀 `readback` 動作(不開交易、不鎖列)而非借用 dry-run(它會 `FOR UPDATE` 鎖 29 張正式站訂單)。🔴 **開工前必問 Sean**:`--merchant-id` 第二來源(D1t1 就留下的未決題)、D1b1 實跑本身、證據包保存位置與期限。其後 D1b2 dry-run 對 production → Sean 批准閘 → D1c(⏰ 備份 24h 時效 2026-07-30 18:06,D1c 前預期重跑 D1a2)。~~D1a7~~ 已依 Q2=A 移到第 2 批。①**D1 線續跑**(A0a-1 已放行、條件見 §8.7;🔴 **D1c 仍是唯一不可逆片、前有 Sean 獨立批准閘**,且 §5.1 D1c 列新增前置:鎖列後 DELETE 前必先斷言 `order_item_procurement`(經 `order_items`)與 `order_notes` 對 cohort **零引用**,非零則 abort)②**A7 / A7b / A1** 等 M 片 ③A11a 依 §5.1a **13 欄**施工(「12 欄」全檔作廢)。🟡 **非阻擋待辦**:D0-1(migration 自寫 `BEGIN/COMMIT` 與 CLI 記帳窄縫)Sean 未答;A9a 驗收條件已預先寫死兩條(U6 稽核查詢必帶 `NOT EXISTS`、負測兩個方向都要);A4a 驗收條件三條、A4b 冪等鍵、A6 只准單列 INSERT、A10a 走鏈帶 visited 與深度上限 —— 全部寫在 A2/A3 migration 註解裡。
- **E8-B 押後**:Q5=A 第一期只做 email+密碼(2FA 另立 E8-C)⇒ 11 片縮為約 8 片;🔴 plan v3 經 codex 關卡1 **R2 FAIL 34 must-fix**、兩輪上限用盡,**判定需重寫**。🔴 新增硬前置:**報價單不能跑 `db push`**(本地 146 檔 vs ledger 160 筆版本號零重疊)。
- **E12 供應商主檔 07-27 Q2=B 這期不做**(報價單 `suppliers` 16 列實為改裝品牌、非下單對象)。

## Sean 待決策
🆕 **2026-07-30 N3 線三條**:①🔴 **鐵則 12 的 codex 背書是否可由 Fable 代替** —— codex 今天三次同款逾時(關卡1 兩次、關卡2 一次;三次皆驗零留痕、最小測試證實 CLI 本身正常),依 R4 換 Fable。**Fable 是不同模型、有跨模型對抗性,但不是鐵則字面要求的那個**;若不接受,N2/N3a/N3b 應卡在未 apply 等 codex 問題解決。②`supabase db push` 套用 N3a/N3b ③**1 元真刷 smoke**(規格 §5.4b 對 N3b 明文要求、唯一能證明結帳沒壞的證據)。🟡 提案檔 Q2(舊 30 張測試單是否在後台列表隱藏)仍未答、不擋任何片。🟡 STATUS 主表 76 行 vs 規則 ≤30(接手前即漂移)、MEMORY.md 109 行/24.8KB 超觸發值但**已量測「壓字無槓桿、唯一有效是撤下 project_*」需 Sean 拍板** ⇒ 兩者皆未自行處理、建議獨立成片。
🔴 **需 Sean 親自做**:①向新竹站所電腦負責人申請物流 API 帳號(**查貨與出貨是兩張不同的申請表**;`docs/reference/hct-logistics-api-reference.md` §1)②**1 元商品真刷 smoke**(驗 RF2a-0 新 trigger 沒把結帳弄壞 —— 模擬與 apply 後驗證用的**都是合成單**)。🆕 **需 Sean 批准才動 code**:E10 施工規格 **v2**(`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md`;~~v1 已作廢~~)—— 關卡1 通過後送批;~~v2 §8.6 四題~~ **已拍三題(Q17=B / Q18 照草案 / Q19=A,07-28)**,§8.6 現存 = HCT 允許字元(外部)+ 三個開批閘:通知矩陣(第 2 批)、建單六題(第 3 批)、**退款線兩題(第 3 批,R8 新增:混合收款退款分軌 / partiallyPaid 應退額語意)**。🔴 **申請 HCT API 時要勾「用新竹貨號查詢」、不要勾訂單編號**(官方文件:只能擇一、申請時即選定、要改須聯繫營業所)。~~07-27 三題(施工序 / N9 / E8-B 承載方式)~~ **已於當晚全數拍板**(Q1=A 先 E10 / Q2=B E12 不做 / Q3=C 混合),連同 P1-P4+E1-E4 八題,全表見交接檔 §3。~~**非阻擋舊題**:是否加 CI 擋 migration 版本漂移~~ ✅ **2026-07-29 Sean 拍 A=加** ⇒ 已開 backlog **#298**;🔴 **不插隊 E10 第 1 批**(動 CI = 鐵則 12 ④平台設定 = 高風險片 + 鐵則 8 重大改動 ⇒ 先提 plan 等批、commit 前過 codex 關卡2);真難點 = CI 要拿 remote 讀取憑證,不是比對邏輯。已答畢拍板 → memory `project_m4b-real-auth-line-decisions` / `project_m4b-*` / PROGRESS 存檔區。✅ **2026-07-29 晨:A0a-1 / A0b-1 / A0c-1 / A3-1 / A2-1 五題全數拍板並落檔**(A0a-1「都沒扣到錢放心刪除」/ A0c-1=A / A0b-1=B 13 欄 / A3-1=A 加更正鏈 / A2-1=A 加逐批到貨明細)—— 權威 = v2 §8.0 + §8.7,白話版 memory `project_m4b-e10-0729-morning-decisions`。✅ **D0-1 已答(2026-07-29,Sean 拍 A = 維持現狀不動)**:全 repo migration 自寫 `BEGIN/COMMIT` 與 Supabase CLI 記帳的窄縫**不處理** —— 40 餘支既有 migration 全是顯式 `BEGIN; … COMMIT;`、窄縫從未踩到,且拿掉 `BEGIN` 會讓 `SET LOCAL lock_timeout` 失去交易邊界。🔴 **連動紀律:後續 migration 一律照舊寫法,不得「順手改成更好的寫法」**(會變成 40 幾支裡的唯一異類)。詳 `docs/reviews/2026-07-29-e10-d0-k2-codex.md`。

## Blocker
🛑 **D1 線已不是 blocker —— D1c 刪除於 2026-07-30 取消(Sean 拍板)**,~~改走 29 張全部改號~~ **最終為 Q1=A:舊單完全不動、只換產號器**(29 是當時口徑,實為 30 張)。~~D1c 唯一不可逆片 / Sean 獨立批准閘 / apply 一律 Sean 親手跑~~ **這三條隨刪除取消一併失效**。🟡 **接手前必讀**:①改號提案尚未寫成施工片,「換編號」目標仍在、路線已換 ②TapPay Record API 三處欄位假設經實測為誤(見最後更新欄),`wire.ts:68` 待更正 ③判定矩陣的窄化放寬已 commit 但**不足以解封 D1**、且有 5 條漏測突變未修 —— 若日後仍要走刪除路線,這些是硬前置。權威 = memory `project_m4b-d1c-delete-cancelled`。
~~✅ **D1 線 blocker 已解除(2026-07-29 晨,Sean 拍 A0a-1)**~~ —— 條件改寫見 v2 §8.7。~~`PCM-2026-0101` 無 `rec_trade_id` 擋住 D1b1/D1b2/D1c~~。🔴 **仍在的閘不變**:D1c 是唯一不可逆片、前有 Sean 獨立批准閘,且 **apply 一律 Sean 親手跑**。🟡 殘餘風險(Sean 已知、未自宣接受):0101 的「未扣款」在系統內沒有可機械複驗的證據,真出事的唯一還原路徑 = D1a2 的加密匯出檔(180 天)⇒ **不得因放行而省略匯出**。
🟡 **M-3 prod checkout 仍不可開**(3DS flag 全 false、非硬 blocker、獨立線)/ 🟡 **M-3 階段⓪** 經銷價同步待報價單側三件就緒(合約 bump v2 + protected dealer view + least-privilege 憑證)/ 🟡 **#215** tier server 認證 defer、真死線=M-2-08 接真經銷價前。✅ **已解除**:#291 正式法律頁全鏈上線(剩 Sean 肉眼驗渲染後 payload)、0072 雙扣(經 Sean 核對為其本人開發測試單、已砍除)。

## 緊急 backlog
**#297**(破壞性金錢動作缺二次確認=#296 的完整解;🔴 建議與 E11 `<AdminModal>` 積木同片做、儲值金扣款當第一個真實消費端)/ **#293**(真機驗收缺 HTTPS 通道)/ **#291**(正式法律頁人工 release checkpoint、非開發阻擋)/ **#292**(SSoT 事實收斂單一來源)。不擋線:**#281-#286**(outbox 保留政策 / cron 清理 / bounce webhook / 文案分級 / retry hint / 死信重送)、**#295**(免運門檻與運費後台管理)、🆕 **#298**(CI 擋 migration 版本漂移;Sean 07-29 拍 A=加,不插隊第 1 批、動 CI = 高風險須先提 plan)。~~#296~~ ✅ 已收案。

---

## OD 商品頁改造線(並行 workstream、Claude-Code 自驅、不入主表)

> 與報價單資料線(S3a/S3b)並行的另一條線。**主表 7 欄由報價單線擁有**、OD 線狀態記此附屬區 + manifest `od_redesign`(Sean 2026-06-02 拍 A)。寫審分離 ROLE=A(施工 session 實作、審查 session 哨兵盯 dev 自動 fresh-context 複驗)、**不跑 busboy-end**(避 clobber 報價單線)、Sean 手動推。

- **視覺真權威:** OD 模板 `product-detail-rpm-template.html`(open-design "Website V2")+ `HANDOFF-rpm-template.md`。鎖定決策見 manifest `od_redesign.decisions`(決定一=A OD=視覺真權威 / D1=A 適用車款表全車種 3 欄無車系 / D3=A 12K 收進紋路無消光、由真資料 disable / Q1 override:N°03 留相關商品 + FAQ→N°04)。
- **當前 slice:** **Phase A 完成 ✅ + 已合併進 dev(merge cf630b2)**。OD-5 服務橫條 / OD-6 N°01 / OD-7 N°02 紋路牆+預覽卡+圖庫聚合 / OD-8 分頁碳纖維化 / OD-9 N°03 相關商品 / OD-10 N°04 FAQ+JSON-LD(保固共用 rpm-policies 單一真相)/ OD-11 buybar OD §12+響應式≤1079,共 11 片(OD-5~11)+ 先前 OD-1~4 全完。合併後三綠 + 完整 pnpm test 531 全綠,OD 元件 × S6 fitments 共存確認。
- **下一步:** ✅ **OD 線已收尾**(2026-06-16 查核更新):OD-12 適用車款表 + OD-12b/c/d 桌機重設計 + OD-13 FAQ + OD-V~V3b 手機/iPad 真機驗收修正**全部完成、併入 dev、已推 origin/dev**(`dev..od-redesign` 空、od-redesign tip `266f5f2` 在 origin/dev、審查 PASS `9cc5bbd` + 交接 `f3d6a42`)。此附屬區先前「OD-12 待做」字面為過時殘留、已更正。後續 OD 強化由 Sean 主導視覺(memory sean-owns-visual-design)。
- **Phase A 連續做(Sean 拍 B):** OD-3~OD-11 全速連續 commit、Sean Phase A 全完肉眼驗一次、哨兵每片自動審 FAIL 即通知。
- **分片清單:** Phase A(現做、不等 S3)OD-1→OD-11(地基/Hero/右欄/Picker+預覽/服務橫條/N°01/N°02紋路牆/分頁/N°03相關/N°04 FAQ/buybar+響應式);Phase B(等 S3b)OD-F1 適用車款表(接 fitments[])/ OD-F2 真資料收口。完整 OD區塊↔元件對照 + 計畫見對話 OD-0 偵察報告(待 Sean 點頭存 docs/specs/)。
- **⚠️ 跨線紀律:** OD 線不碰 `scripts/rpm-*.ts`(報價單線 WIP)、`docs/reviews/integration-phase1-review-log.md`(審查 session)、`docs/specs/*S3*`(報價單線)。

## 速查 / 歷史(已外移、降低本檔讀取成本)

- **速查**(Phase 1 範圍 / 技術棧 / 關鍵路徑)→ `docs/quick-reference.md`
- **變更紀錄**(slice 逐筆歷史)→ `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段

## 文件交叉引用

每次新對話依此順序對齊上下文:

1. **`STATUS.md`** ← 本檔(每次先讀)
2. `docs/PHASE-1-NORTHSTAR.md` v2 — Phase 1 真權威定義
3. `docs/lessons-learned.md` — 舊專案教訓彙整
4. `CLAUDE.md` — Claude Code 工作規則
5. `docs/PHASE-1-MILESTONES.md` — milestone 排程
6. `docs/decisions/` — 重大決策記錄
7. `docs/patterns/` — 通用 + PCM 專屬規矩
8. `docs/phase-1-backlog.md` — 未決事項
9. `docs/features/*.md` — PRD
10. `design-reference/` — 視覺真權威字面(submodule)
11. `PROGRESS.md` — 歷史紀錄
12. `docs/quick-reference.md` — 速查(Phase 1 範圍 / 技術棧 / 關鍵路徑)

衝突仲裁順序:
- STATUS.md 與其他 md 衝突 → STATUS.md 為準
- 其他 md 與對話歷史衝突 → md 為準
- 視覺 / 結構 / 路由 / 元件命名衝突 → design-reference 為準
- 業務邏輯(訂單流程、權限、價格、Medusa schema)衝突 → docs/decisions/ 為準

## Busboy 機制(沿用第一輪)

- **busboy-start.js:** Sean 在 Terminal 跑、輸出貼新 Claude Code session 第一則訊息
- **busboy-end.js:** Claude Code 在 session 最後跑、自動更新本檔 5 個欄位(最後更新 / Phase Milestone slice Branch / 最近 3 commit / 下一步 / Sean 待決策)、commit、不 push(Sean 手動推當 review checkpoint)
- ⚠️ WO-6 後「變更紀錄」已移 `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段、不再寫本檔;busboy-end.js 若仍寫 STATUS 變更紀錄表需同步改寫(pcm-tools 外部 repo、待 Sean 處理)
- repo 參數:`pcm`(本 repo)/ `tools`(pcm-tools)

第一次 busboy-end 跑之前、本檔欄位手動填(start template 用、由 Claude.ai 維護)。

busboy-end 跑完後 amend 進 slice 主 commit、不另開 commit。

— END —
