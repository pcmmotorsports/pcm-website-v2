# STATUS.md
> PCM Phase 1 SSoT. 衝突仲裁: STATUS.md > NORTHSTAR > 其他 md > 對話歷史. 🧹 **2026-07-27 主表精簡(Sean 拍板 A)**:主表 357 → 30 行;移出的內容**逐字保存**於 `PROGRESS.md` §「2026-07-27 STATUS 主表精簡存檔」、**零刪除零改寫**,7 欄結構完整保留。

> 🔴 **主表行數一律以【非空行】計**,量法 `awk '/^---$/{exit} NF{c++} END{print c}' STATUS.md`(完整原文見下方搬出區)。

## 當前狀態
**Phase:** Phase 1 / **Milestone:** **M-4b 後台重建「員工上工」線**(2026-07-26 換軌)/ **Branch:** dev。北極星(Sean 逐字)=「可以完整上線給員工使用,操作,修改網站。而且他們不是工程師」;驗收=`docs/specs/2026-07-25-admin-backend-rebuild-spec.md` §1「員工的一天」27 項;🔴 **施工序 2026-07-27 深夜改為「E10 訂單閉環 → E8-B 認證(email+密碼)→ E8-C 2FA」**(Sean Q1=A、二次確認 Q6=A;E11 積木改為隨 E10 按需長出、不獨立成階段)。~~舊序 E11→E8→E10~~。
✅ **本線已完成**:全後台畫面預覽 artifact(46 畫面、Sean 驗收畢、U1-U7 全拍板)/ E11-1 `<AdminDataTable>` / E11-2 `<AdminForm>` / #296 儲值金按 Enter 誤扣款 / E8-A1 staff 名單進 DB(migration 已 apply production)/ E8-A2 `/settings/staff` 員工管理頁。📌 **暫緩非作廢**(細節在 PROGRESS 存檔區):M-3 TapPay 正式化線 S3 起 / 退刷線 RF2b 起 / M-4a 通知線 B-4。
🔴 **E8 未完成**:操作者身分仍是使用者**自己下拉挑的、系統未驗證**(`apps/admin/src/lib/session/actor.ts:6` 自陳非授權邊界);後台唯一入口是報價單站的**共用密碼**登入(`ADMIN_PASSWORD` 單一 env;🔴 **TOTP 實查為關閉狀態**——`auth_state.require_2fa=false`、`totp_devices` 0 列、`recovery_codes` 0 列,詳 memory `project_quote-2fa-deployed-but-dormant`)再 SSO 過來,SSO payload **不帶是誰**(`apps/admin/src/lib/session/session.ts:11` 逐字)⇒ **目前沒有「每個員工的帳號密碼」這個東西**。真認證=報價單端跨 repo 線、尚未開工。(⚠️ **本行只講了【安全面】(身分不可信);而 `#534` 是【可用性面】——不選就什麼都不會發生、寫入靜默失敗。同一個機制兩個病,見下方 Blocker 與決策單 Q1。**)
🔴 **2026-08-16 施工序再改(Sean 拍【甲】真登入現在做,訂單線讓路)** ⇒ 上面那段「E10 訂單閉環 → E8-B → E8-C」的順序**已過期**;新線 plan v4 見「Sean 待決策」。

## 最後更新
🗂️ **2026-08-16 深夜 110🏁 五線全收(E 窗稽核總結檔)+ 兩次自我推翻 + 實查「`main` 沒有分支保護」**(**收割** `customers` 1 顆 merge `651f3dc9`:`docs/security/2026-08-16-external-exposure-audit.md` 228 行,**取代 `E-670`~`E-685` 十五封信**(那十五封互相修正過六次 ⇒ 只讀其中一封會拿到錯的);驗活 **9 條逐字全中**含負向(`0`)與正向對照(`1`);typecheck 8/8 / lint 10/10 皆 `0 cached`、`vitest 500 檔 8317 passed` 皆 exit 0;**build 明示跳過** —— 本次 merge 動到的檔中 `.ts/.tsx/.css/.json` 數實測 **0**(僅一支新增 `.md`)。🔴 **E 窗自陳「唯讀窗、tip 不會動」在我來收之前【剛失效】** —— 那份檔原是 **untracked**(**未追蹤檔不會進 merge**),**它為了給我收才 commit**,tip `64fe4684`→`09b4d36c` ⇒ **照舊預設走會 merge 到一顆沒有那份檔的 tip 然後以為收完了**;⇒ **對帳一律重跑 `rev-parse`,不信任何信裡(含我十分鐘前讀過)的 hash**。🔴 **E 窗自查涵蓋範圍當場抓到兩個 0**(`E679-1` finding 漏收、`E684` 斷言樣板只被提到)並補 §8「本檔【不】包含什麼」⇒ house `feedback_completeness-of-a-file-is-not-its-coverage` 的正面案例。🔴🔴 **本輪我兩次自我推翻**:①`#548` 成本結論的**基準**選錯(含 `08-09` 尖峰的本期平均 vs 近 7 天速率 ⇒ **相反結論,而兩個都沒算錯**);②報「`dev`/`main` 重跑同一顆 sha 的 21.6 趴是純浪費、砍掉不減覆蓋」**當場收回** —— 7/1 起按 sha 分組實測:只在 `dev` **422** 顆 / 兩邊都跑 **123** 顆 / 🔴 **只在 `main` 跑過 7 顆**⇒ **對那 7 顆而言 `main` 那次不是重複,是唯一的一次**,砍掉會讓一條**六週內被走過 7 次**的路變零檢查⇒ house `feedback_removing-the-thing-that-was-hiding-the-defect`(**判別句「這格現在是綠的,綠的原因是什麼」我答不出來就報了**)。🔴🔴 **而查它時撞到今天最乾淨的一個「字面 vs 事實」:`main` 實得 0 條分支保護**(`rules/branches/main` ⇒ `0`;正向對照 `dev` ⇒ `5` ⇒ 那個 0 是真的 0),**而設定頁上有一個【名字叫 `main`】的 active ruleset —— 它的 `~DEFAULT_BRANCH` 解析出來是 `dev`**(`default_branch=dev`;`dev` 那 5 條 = 兩個 ruleset 的聯集,按 `ruleset_id` 逐條對過)⇒ **名字對、狀態對,而它在保護另一個東西,且誤導方向是【讓人以為安全】**;`main` = storefront 正式站,當時可被直接推/強推/刪除。**主視窗裁【乙 現在什麼都不做】**(近 7 天速率 `750–850` 分/月 vs 額度 `2000` ⇒ 本來就不需要省),**「`main` 納入保護」與 private 一起掛進 `docs/PHASE-1-NORTHSTAR.md` §5 上線清單,並把那個陷阱與重跑量法一起寫進去**。📎 CI 時間去向實測(258s):`Test` **144s** 佔 56 趴 / 環境與安裝共 **24s** 佔 9 趴 ⇒ **「加快取就能省」不存在**。📎 觸發條件實查 `ci.yml:3-6` 僅 `dev`/`main` ⇒ **施工窗分支不觸發,「多窗並行=高用量」是假的**;今天五窗全開而 `ci` 只跑 **3** 次 ⇒ **變數是推送頻率,不是工作強度**。)
🗂️ **2026-08-16 夜 109🏁 I 窗四線全數收割完畢(`print-docs` / `void-readers` / `products`)+ 撞到 repo 是【公開】的**(**收割**:`products` 5 顆 merge `16c3a388`(tip `03e8e7bb`,經**三輪審查 18 條 must-fix** —— R1 code-reviewer 10 / R2 code-reviewer 3(**全是折 R1 時自己造出來的**)/ R3 **換模型 codex** 3+5,🔴 **codex 抓的是 Postgres 語意層,前兩輪一條都沒碰到**)⇒ 兩道 REVOKE 立成 `docs/patterns/revoking-function-execute-in-supabase.md` + 路由表兩檔各一行 + `#545`/`#546`/`#547`/`#549`。**四綠+build 全 `0 cached` 真跑**:typecheck 8/8 / lint 10/10 / build 2/2 / `vitest 500 檔 8317 passed` 皆 exit 0。🔴 **收割前置掃描先做了**(`git merge-tree --write-tree` 乾跑、不碰工作樹 ⇒ exit 0 零衝突),並**預先算好併後期望值**:`528 + 531 − 527 = 532` 條、`#54x` 應為 `540..549` 共 10 條 ⇒ **併後實得逐項命中**。**兩窗各查各的號**(B 查 `#54[5679]`=4、I 查自己的 `#548`=1)—— **對方查我的號等於我自己驗自己**。⚠️ **驗活 6 條中 1 條對不上**:`literal-sweep '鎖死本檔'` 預期 3 實得 **4**;**查明是 B 的期望值寫錯,不是併掉東西**(`products` 側同樣是 4 ⇒ 兩側相等、零損失)⇒ **接受不退**。🔴🔴 **而本輪最重的是收割之外**:做 `#548` 成本估算時實查 **這個 repo 是 public**(兩來源:`gh api` ⇒ `private=false / visibility=public`;**未帶認證 curl ⇒ HTTP 200**)。✅ **先分類再報數,證實不是外洩**:被追蹤的 `.env` 只有 `apps/storefront/.env.example`;金鑰字面 18 處命中而**每一處 token 長度都是 10 = 光禿禿的 `sb_secret_`**(真 JWT ≥180 / 真 `sb_secret_` ≥40,實測皆 **0**)⇒ **全是【在講金鑰】不是【金鑰】**。🔴 **真正的暴露面是我們自己寫的分析**:`supabase/migrations` **179** 支(**9** 支含 `CREATE POLICY`)+ 本檔主表逐字寫著多個**未修**缺陷的重現細節 ⇒ 命中 house `feedback_the-warning-can-reproduce-the-hazard`(**為了論證危險而寫下的證據,本身就複製了那個危險**),而 Sean 08-16 早上才逐字定調「**我唯一擔心就是資料被駭客攻擊**」。⚠️ **公開是否刻意未知、今天無人提過 ⇒ 已升 Sean,`R3` 不可逆且對外,零動作**;✅ **2026-08-16 Sean 已裁【丙｜先不改,上線後再改】,逐字「等到網站上線後再改,畢竟為了安全著想」** —— 🔴 **他不是選了不安全那半**:改 private ⇒ Actions 開始吃 2000 分/月額度,而**實測本期半個月已用約 2548 分**(校準:每 run 恰 1 job、job/wall=`0.97`、計費每 job 進位到整分;與主視窗從帳單金額換算的 `2479` 差 **2.8%**,兩法互相印證) ⇒ 額度用完 **GitHub 預設不是收費是【停跑】** ⇒ **而 CI 是唯一在守 SQL/probe 那一層的東西** ⇒ **為了把 repo 藏起來而讓驗證鏈斷掉,淨安全性是負的**。**Vercel 他親口確認「我用 github 連的」** ⇒ 我查不到那半由他補上。📌 **到期動作已寫進 `docs/PHASE-1-NORTHSTAR.md` §5 上線清單**(private 與 spending limit **綁成一件事**,附可重跑量法與「數字會過期、請重跑」的警告) —— **不是只留在 memory,因為上線那天沒有人會回來讀 memory**。🔴🔴 **而我稍晚自我更正了那個成本推論的【基準】**:按日分佈實查 `08-09` 一天 **146** 次(佔本期 426 次的 **34%**)、`08-10` 之後每天只剩 **3–10** 次 ⇒ **照近 7 天速率外推約 750–850 分/月,遠低於 2000** ⇒ **「改 private 會月中燒穿」建立在【含尖峰的本期平均】上,用近 7 天會得到相反結論**;**兩個都是事實,差別在基準,而我上一版沒把基準講清楚**(未查 `08-09` 那天為何 146 次 ⇒ **無法判斷尖峰會不會再來,那才是決定性的一題**)。📊 **CI 時間去向實測**(單次 258s):`Test` **144s(56%)** / `Typecheck` 37s / `Playwright 裝 chromium` 27s / `Lint` 14s / **環境與安裝全部只佔 24s(9%)** ⇒ 🔴 **「加快取就能省」那條路不存在,省法只剩砍覆蓋 ⇒ 要 Sean 拍,而照上面的速率現在不需要省。****主視窗明令:在他裁之前不得改本檔內容去「先遮起來」**(git 歷史照樣讀得到,遮了只換來自我安慰)。📊 **`#548` 成本估算**:CI 單次中位數 `228s`(n=18)、近 7 天 `1093` commit / `110` run(**約 10 顆共用 1 次**)、逐顆跑 ≈ **69 小時/週**(標**推算**,併行度與佇列上限未量);🔴 **金錢成本 `$0` —— 但那【前提是 repo 為 public】,而該前提正要被 Sean 裁** ⇒ **寫進結論必帶前件,條件句的前件被改時後件不會自己跟著翻**。)
🗂️ **2026-08-16 傍晚 108🏁 I 窗(整合窗)首次收割 —— `print-docs` 3 顆併入 dev,四綠全真跑**(merge `print-docs`(tip `d6ff2ece`,與 `C-210-STOP.md` 機器可讀段逐字相符)⇒ `86bd3cae` `#10` 出貨單資訊架構 plan / `692849c8` `#522` 續章分母收斂 / `d6ff2ece` plan 吸收 Sean 傍晚裁示(三區定名 本次出貨/尚未出貨/訂單取消)。**四綠 exit 分開收、皆 `0 cached` 真跑**:`turbo typecheck --force` 8/8 exit 0 / `tsc -p tsconfig.scripts.json` exit 0 / `turbo lint --force` 10/10 exit 0 / `eslint scripts/*.ts` exit 0 / `vitest` **500 檔 8311 passed + 2 expected fail + 1 todo** exit 0。🔴 **驗活清單照跑,含 C 窗設計的負向那條** —— 把 `lineNeed` 改回 `l.quantity`(= 整個撤銷 `#522`)⇒ **9 failed / 3 檔**,分佈 `goods-axis-cases` 5 / `order-status-axes` 3 / `refund-wiring` 1,**與 C 的預測逐項相符**(C 自己寫的判別條件:**只紅 5 ⇒ 它的四格沒被收進去;零紅 ⇒ 跑的不是這棵樹**)。還原用 `cp` 不用 `git checkout`、`shasum -c` OK、突變字串 0 命中、樹乾淨。⚠️ **`docs/phase-1-backlog.md` 是自動併的** —— 那正是今天「兩邊都併成功而結果比任何一邊都糟」那支檔的形狀,已逐項驗:`#548` 標題唯一、`#547` 只剩我刻意留的改號註記、**本次 merge 動到的 5 支檔衝突標記皆 0**(⚠️ 全樹掃到 4 支「命中」是**假陽性**:3 支是圖檔隨機位元組、1 支是在講衝突標記的文件 ⇒ **失敗訊號在正常情況下也會出現**,範圍要收到 `git diff --name-only HEAD~1 HEAD`)。🔴 **C 窗主動用後信更正一個我已經 commit 進 STATUS 的數字**(「repo 零守門」為假,真值=軸有 5 格、小字零守門),**它選擇後信取代而不是私下改掉**;我獨立複驗真值表 `14` 案 / `7` 案含取消才採信,不轉抄。📌 **本次撞號一則**:I 窗 `#547` 撞 B 窗 `#547`,主視窗裁**甲=I 窗改號 `#548`**,判準**不是「先到先得」是「誰照協定走」**(B 標了「佔位、待主視窗確認」,I 窗沒標)。🔴 **I 窗自曝發號量法是壞的**:`grep -rn 佔位 ~/pcm-mailbox/*.md | head -10`,真實命中 **76** ⇒ 只看 13% 就下全稱結論,**而截斷處看起來像自然結尾 ⇒ 它偽裝成「我看過了」**;⚠️ **但它不是這次撞號的原因**(I 窗 commit `15:32:29` / B 的信建於 `15:37`,掃描當下信還不存在)——兩者都成立、方向相反,**不合報**。**通則已落 `#548` 條目**:`grep` 的工作是**定位**不是**回答**,接了 `head`/`cut` 的輸出不准拿來下全稱結論,**要下結論先取分母**;🔴 **同日第三次同形狀**(`head -10` 佔位掃描 / `cut -c1-140` 讀成未完成片語 / `gh run list --limit 100` 拿到剛好 `100`)⇒ **機械修法:任何帶 `--limit`/`-n`/`head` 的計數,結果【等於上限】一律當未完成,加大再跑**(提到 400 得真值 `110`)。🔁 **同線增量再收一次**(`052c5148` `#10` plan 收掉 `Q-C3`,純 docs 1 支檔 `+15 −10`;四綠重跑全 `0 cached` exit 0、`vitest` 仍 500 檔 8311 passed;驗活 `grep -c 'Q-C3:三個區塊'` ⇒ **0** + 正向對照 `grep -c '本次出貨'` ⇒ **14**)—— 🔴 **而我第一次量這批範圍時用了 `git diff --stat HEAD..print-docs`,它回「4 支檔 / −150 行」= 假的**,因為它把「我有而對方沒有」的 commit 算成刪除;**正解 `git diff --stat $(git merge-base HEAD <branch>)..<branch>` ⇒ 1 支檔 `+15 −10`,與交件宣稱逐字相符**。**這正是 2026-08-16 收割模擬踩過的同一個坑(當時 products 53→24、customers 49→32),而它在同一天對同一個人再犯一次** ⇒ **收割量範圍一律用 `merge-base`,不用 `HEAD..`。**)

## 最近 3 commit
> dev。🏁 **2026-07-25 Sean 拍板 A:不寫死 hash 與未推數**(寫死的自指數字每多一個 commit 或每次 push 就當場變假、同款前科已 10 次)→ 一律當場執行取得:`git log --oneline -4`(第 1 行=本 commit,其後 3 行=前序)/ `git rev-parse --short origin/dev` / `git rev-list --count origin/dev..HEAD` / `git merge-base --is-ancestor <hash> HEAD`(驗可達、防 busboy off-by-one orphan)。⚠️ `dev` = pcm-admin 的 **production** 分支,push 即部署;`origin/main` 同理不寫死。

## 下一步
🔴 **五個施工窗(A `pcm-void-readers` / B `pcm-products` / C `pcm-customers` / D `pcm-print` / E 唯讀審查)全部【停著等派工】**(2026-08-16 各窗自陳)。⚠️ 四窗的「未推 N 顆」是分支 tip 相對 `origin/dev` 的距離,**收割後那個數字不代表有待推**;判斷法 `git rev-list --count origin/dev..dev`。E 窗無工作樹 ⇒ **判死活只能看 `~/pcm-mailbox/E-*.md` 的 mtime**。
📋 **待辦(交接檔 §8 優先序)**:①`SECURITY DEFINER` 全樹 **388 次**只查了 `#525` 一支 ⇒ 推廣盤查(最大未關缺口)②真登入線 plan v4 等 Sean 批(鐵則 8)③Sean 08-16 順口講的三條新需求(出貨畫面重複顯示 / 全站配色跟上直角化 / 頁面寬度統一)④`order-status-axes.ts` 兩份分母收斂(`D-074` §8 有貼好的測試碼)⑤`#544` 編譯產物 `border-radius` 斷言(要一個 CI 順序的決定)⑥`scripts/525-verify.sh` 那條量法「只列出命中、不證明每個命中都已校正」未確認。

## Sean 待決策
🆕🔴 **2026-08-16 下午:真登入線 plan v4 已寫、等批(鐵則 8)** —— `docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md`(7 片,取代 07-27 v3 的 §2-§8;v3 §0 拍板史與 §9 codex 對帳表仍有效)。**桌上一題 `Q-AUTH-1`**:員工帳密用 Supabase 內建 vs 自己刻(推薦甲=內建;🔴 **甲案前提「那個專案有開 Auth」未查證** ⇒ B0 第一件事,查出來是關的則甲案作廢)。⚠️ **plan §1.3 報價單現況全表量在落後 `origin/main` 16 顆的本機 `3fc905f` 上,不是現況** —— B0 未複量前不得引用。**未跑 codex 對抗審查**(鐵則 12②,等 Sean 定方向後跑)。
🔴 **`#462` Q3 附加條件待釐清**(主視窗已問、Sean 未回):他拍 Q3=A 時逐字加了「**到貨的商品要可以轉去庫存那邊**」,而那與 08-12 Q6=A「不做庫存」+ 08-11 Q11「庫存落點在報價單 repo」的關係要他自己定 —— 選項 A=現在先記著、等庫存功能做出來再接(推薦,`#452` 已照此做完並落誠實缺口 G7);B=現在就要能轉(那要等報價單 repo,整條線停下來等)。

## Blocker
🔴🔴 **`SECURITY DEFINER` 授權缺口只查了一支**:全樹出現 **388 次**(量法 `grep -ci 'SECURITY DEFINER' supabase/migrations/*.sql | awk -F: '{s+=$2} END{print s}'`),本次只查 `#525`。⚠️ **不是全部都有問題**(同日 `20260816030000` 沒中,它 `:101` 明文寫著這個平台行為、`:104` 有 `REVOKE ... FROM anon, authenticated`)⇒ **要逐支看**。**知識在 repo 裡,只是沒進到那一支檔。**
✅ **`order-status-axes.ts` 兩份分母打架【已解】**(2026-08-16 I 窗收割 `print-docs` 併入,C 窗 `692849c8` 修:軸與解釋小字收斂成同一個 `lineNeed()`、並夾住分子)。**驗活實跑**:`grep -c "sum + l.quantity"` ⇒ **0**、`grep -c "sum + lineNeed(l)"` ⇒ **1**;負向對照把 `lineNeed` 改回 `l.quantity`(= 整個撤銷 `#522`)⇒ **9 failed / 3 檔**(`goods-axis-cases` 5 / `order-status-axes` 3 / `refund-wiring` 1),**逐項命中 C 的預測值**。🔴 **同時更正一句本檔先前寫過的假話**:原文寫「`#522` 那個已 commit 的修**現在零測試分得出它**」——**那是錯的,而且是我(I 窗)從舊文壓縮時原樣背書的**。真值:**軸那半一直有 5 格守門**(`goods-axis-cases.test.ts`,真值表 14 案 / 7 案含取消,我用 `json.load` 親數)、**零守門的只有【小字】那半**。病根是 C 窗**只跑一支檔拿到「42 格零紅」就寫成整個 repo**,而**「本檔零紅」與「repo 零守門」的輸出長得一模一樣——都只是一個數字**;抓到它的是 codex 第二輪,靠的是**去看 `goods-axis-cases.json` 存不存在**,不是推理。
🔴 **客戶後台的可見性靠一個角色屬性,而 repo 裡零行宣告或檢查它**(E 窗 `E-662` 實測):`init_customers_and_subtables.sql:144-161` 四條政策 = `select_own`/`update_own` **TO authenticated** + `insert`/`delete` **TO service_role** ⇒ **`FOR SELECT TO service_role` 這條不存在**;今天沒爆是因為 `service_role` 帶 `BYPASSRLS`(平台角色、**repo 內無法驗證,標未確認**)。🔴 **現實觸發不是「Supabase 改掉 service_role」,是【我們自己做一次安全強化】** —— 那是一個看起來嚴格更安全的改動,而它會讓客戶後台整個空白,**而搜尋(`SECURITY DEFINER` 繞 RLS)照樣打得到人** ⇒ **系統看起來活著而資料看起來不見了**。**Sean 已拍 `Q15`=甲**(補上那條缺的政策、不去斷言平台屬性)⇒ **動 RLS = 鐵則 12②,未開工。**
🔴 **BMW M 兩片皆未經【真路由】computed style 驗收 —— `片1 色票` 2026-08-16 已由 I 窗收割(merge `c84597ad`)、四綠全綠,而【綠不等於員工看到的是對的】**:片1 把 admin 整站色票從灰階換成 M 藍(`--primary: #0066b1`、頁底 `#f7f8fa`),**repo 內守門是「對比實算」(28 組配對逐組重算,比釘字面強)**,但**沒有任何一顆量到瀏覽器的 computed value** ⇒ 08-14 那次「三綠+10 發突變+postcss 守門全綠而手機觸控目標被改小」的形狀**原封不動還在**。**直角+邊框片**(已隨 `578a616f` 推出)同此。關卡在**部署後**不是 commit 前(admin 需 SSO+2FA、登入態只在主視窗瀏覽器)⇒ **主視窗要在真路由量輸入框/按鈕/狀態膠囊的 `borderRadius` 與 `borderTopColor` 並回報**;R3 原話「`token 已改` 被當成 `員工畫面已驗收` 的框架錯誤」。📎 狀態膠囊仍 pill,**待 Sean 裁方/圓**(圖 `~/Desktop/膠囊-方圓對照.png`)。⚠️ B 窗自報:`Q5`=方之後,圓角定位器【刻意放行 `rounded-full`】的理由就消失了**而守門仍是綠的** ⇒ 落地與拿掉放行**綁同一片**、不留「綠著的違規」(products 樹 17 處、print-docs 樹只要 1 處,**不要把成本估互相搬**)。
🔴 **正式站現有缺陷:沒選具名身分 ⇒ 後台所有寫入動作靜默失效**(無訊息、畫面完全正常),而「尚未選擇」**是每個人的預設狀態** ⇒ 新員工第一天就撞上。`backlog #534`;**Sean 已拍【不補擋板】—— 真登入線上線後自動消失**(`#536` 同)。
⚠️ **push 當時輸出帶一句 `Bypassed rule violations: Required status check "check" is expected`** ⇒ GitHub 分支保護的 status check **被繞過**了,原因未查 ⇒ **要確認 CI 有沒有真的跑。**
其餘無硬 blocker。🟡 M-3 prod checkout 仍不可開(3DS flag 全 false、獨立線、非本線)。

## 緊急 backlog
無(#441① SHAPE-3 判別力=決策題非急)。

---

## 🗂️ 主表溢出存放(2026-08-16,B 窗 `B-578` F1)
> 🔴 **主表(分隔線上)≤30 行嚴守** —— 而它超標到 **48 行**,規定就寫在本檔 `:2`。
> **超標不讓任何一句變假,它讓「下一個 session 最先讀的三十行」變成四十八行。**
> ✅ **本區零刪除、一字未改** —— 只把【歷史】與【已被覆蓋的分派】從主表搬下來。
> ⚠️ **搬完仍 37 行、仍超標** —— 剩下的體積在 `Blocker`,那是 Sean 現在就要讀的東西,**不砍**。
### 最後更新(較舊 7 條;最新 3 條仍留主表)
🗂️ **2026-08-14 夜 103🏁 `#473b-1` 收割 —— 鐵則 12 換模型輪由主視窗跑(Sean 授權),FAIL 7MF 全折**(merge `#473b-1` 表+view+RPC+回退+守門+併發 probe;主視窗獨立三綠 exit 分開收、`TSC/LINT` 皆 `0 cached` 真跑、`vitest **459 檔 7746 過**`。**審查鏈**:codex 關卡2 R1 14MF(修 13)→ R 同模型 R2 驗證輪(**明文不是模型多樣性第二意見**,因 codex 無可用 model 清單 + R 的 session 禁 Agent tool)→ **Sean 拍 `Q-換模型審查`=A 授權主視窗跑 `adversarial-reviewer`(opus/fresh/唯讀)** ⇒ **FAIL 7MF+3consider+4nit**。🔴 **最重兩條都是「作者自己寫下的規則沒套回自己」**:①**apply 斷言區零真呼叫**(`SELECT public.`/`PERFORM public.` 全檔零命中)而 R 自己在 plan §2b-3 寫下該通則 ②**新表沒開 RLS**,而斷言只查 `relacl` 不查 `relrowsecurity` ⇒ **這層缺席零格會紅**。🔴🔴 **must-fix 5 反噬主視窗**:R 的「`pg_default_acl` 給 `service_role` 全套八項」是**拿自家 shim `scripts/d1-supabase-shim.sql:50` 量的**,正式庫唯一實查 `20260814140000:142` 只列 `anon/authenticated=arwdDxtm`、**service_role 未出現**;主視窗傍晚**原樣落成 memory 並背書**(犯 `feedback_new-rationale-written-while-folding-a-finding-is-unverified` 的「別人給的理由不得轉抄+背書」),**memory 已更正**,上位教訓=**shim/拋棄式環境量到的 ≠ 正式庫事實**。R 的修法**不只改字面**:改成**動態枚舉 `relacl` 非 owner grantee 逐一 REVOKE ALL** ⇒ 不再依賴任何 defacl 假設。R 補斷言後**用突變驗紅得起來**(種回 `pg_catalog.coalesce` ⇒ apply exit=3 整支回滾),⚠️ **但第一次突變是無效的**(sed anchor 沒對上真程式碼行、`grep -c` 回的 1 是自己寫的註解)⇒ 新判準「**`grep -c` 的數字要能指出第幾行、那行長什麼樣;只看數字不看內容 = 沒量**」。**審查者打不破六個面**(含把 `sql-scan.ts` 邏輯**原樣移植成獨立腳本**全量重數、**換角度**按符號名重掃 TS 側、確認 probe 現況版是**真併發**`pg_blocking_pids()` 綁定)——**證據已留,不要重做**。**consider 8 R 判「記錄不修」**(更正效力綁在 `order_refunds` 刻意可變的 status/failed_reason 兩欄,要擋得改既有契約=本片承諾不碰),與 `#497`、R12/R13 同一批送 Sean。)
🗂️ **2026-08-14 夜 102🏁 乙片(採購作廢 RPC)完工收割 + E 三題解鎖 + 對抗審查一輪進行中**(**V 窗乙片** `c8db415f` merge:migration(前置閘 P1-P5 + 冪等帳本表 + RPC V1-V8 + 後置斷言 A1-A11)+ `scripts/452b-down.sql` + `scripts/452b-harness.sh`;🔴 **V 沒有只寫 SQL —— 在拋棄式 PG 17.10 實跑**(`.sql` 對三綠恆綠零判別力,`check-syntax-nonts.ts` 檔頭自陳)apply→16 格行為→隔離閘 `P2B02`→**帶一筆真實作廢列跑回退**(前後皆 1 筆、零業務資料損失)→再前推→再回退,exit 0;**harness 自身判別力也量了**(V7 非零檢查改 `IF false` ⇒ exit 3、C4 紅);V 自曝假格 C17 第一版測資 `received=7/receipts=0` **分辨不出 V7 順序**。**codex 關卡2 14 must-fix NO-GO 全折**,最重一條是 V 自己種的 🔴 `452b-down.sql` 的 **A4 一行查詢都沒有卻 NOTICE「A1-A4 全過」**(今日母題);另 `pg_get_functiondef` **連註解一起回** ⇒ 守門刪光只留註解也放行、併發同 `request_id` 敗方吃裸 23505、冪等宣稱全欄比對實際漏 `actor`、harness 自己兩個假格。🔴 **apply 前置(DB 閘看不到)**:甲片自陳安全上線靠「零 voided 列」而**本片就是產生者**,那條路靠 `#476` 片2 應用層關掉 ⇒ **app 必須先上**(反向形狀);主視窗實查 `git merge-base --is-ancestor 8dfad824 origin/dev` = **已在 origin/dev**,前置滿足。**E 窗**:Sean 批 `#486` 乙案開工 / B-1 開工(**檔數 5 不是 4**,E 讀 OD 後量出 `order-filter-bar.tsx:49` 的 `goodsAxes?.[0]` 多值時顯示與實篩不一致)/「未到貨」=`['none','ordered']`(B 案今天篩出 0 筆=永遠空的按鈕)/ `#485`「待處理」=**C 兩者聯集**;🔴 E 讀 OD 把「45 分 CSS 片」量成決策題(四顆 chip 有三顆沒定義,「退貨中」**資料面不存在**且要第二次查詢)⇒ 拆 B-1/B-2(`#485`)/B-3(`#500` 新立);E 另補「**聯集 ≠ 兩個 filter 疊起來(那是交集)**;現行 adapter 全 `.eq`/`.in` = AND,要 OR 得用 `.or()` 而它已被 L6 用掉一次、**兩個疊起來的語意我沒查過**」。**R 窗**:`473b-1` migration 寫完(版本號**與 V 撞 `20260814180000`,主視窗裁 V 保留、R 改 `20260814190000`** —— 🔴 同日第三次全域唯一資源撞號,前兩次是 backlog `#489`/`#498`,**主視窗未集中發號**;R 給的判準已採用=**凡全域唯一資源都該由一處發**);R 拋棄式庫實跑抓到兩條**只有真跑才會發現**的:🔴 **`pg_default_acl` 對 public 新表預設給 `service_role` 全套八項** ⇒ 只寫 `GRANT SELECT` 是廢話、要先 `REVOKE ALL`(擋下它的不是 R 自己寫的註解、是它寫的 apply 斷言 5e)/ 🔴 **`pg_catalog.coalesce` 不存在但 plpgsql 本體 `CREATE` 時不求值** ⇒ **migration 套用成功、五段 apply 斷言全過、第一個按下去的員工才爆** ⇒ 通則「**動 plpgsql 的片,apply 斷言之外必須有一次真的呼叫**」已落 memory。**R 的鐵則 12 換模型輪**:codex 無可用 model 清單(主視窗查證 `~/.codex/config.toml` 單一 model、`--help` 無清單)、R 的 session 禁 Agent tool ⇒ **Sean 拍 `Q-換模型審查`=A 授權主視窗跑**,`adversarial-reviewer` 一輪**進行中**。merge 後主視窗獨立三綠 exit 分開收、`TSC/LINT` 皆 `0 cached` 真跑、`vitest 458 檔 7741 過`。)
🗂️ **2026-08-14 夜 101🏁 三窗全開 + `#494` 與 `#484a` 片A2 收割 + `#497`·`#498`·`#499` 立案 + 乙片開工批准**(**收割**:V 窗 `#494` 兩片(`73b383bd` 列表膠囊 +`f1ad5a3c` 取消閘拒因分流,merge `6b840c2e`)/ E 窗 `#484a` 片A2(`81f5b45c` 列表換讀 `admin_order_list_v`+ 出貨狀態改篩貨品軸,merge `7fd6f84b`,**backlog 尾端撞衝突=雙方各自 append,解法保留兩側只刪 3 行標記、`uniq -d` 空**);主視窗獨立**四綠 exit 分開收**、`TSC/LINT/BUILD` 皆 `0 cached` 真跑、`vitest 458 檔 7741 過`。**拍板**:`Q-乙片`=A **開工實作**(plan 早已存在 934 行、審過、`§8.2` 零待裁項 ⇒ 卡的是批准不是缺 plan)/ `Q-加速` 主視窗先拍 A「看得見的片優先」**當晚被 Sean 本人收回**(逐字「還是以先完整建置為主…系統還是要安全的做完」;主視窗把抱怨讀成需求變更=判讀錯誤,已落 memory)/ `473b-1` plan v2 **批准寫 migration**(Sean 逐字「懶得看,你處理」交回裁量權;**apply 停點、鐵則 12③、回退腳本三件未被涵蓋**)。🔴 **當日兩次「窗擋住主視窗」**:①V 接到「寫乙片 plan」的派工後**先查有沒有人寫過**,查到甲片檔頭 `20260814100000:5` 逐字指著 934 行的既有 plan ⇒ **主視窗親手 apply 那支 migration 卻沒讀它的檔頭**,差點產生第二份競爭規格;②E 依規矩標「`#498` 佔位、請主視窗確認」,而主視窗發號前**沒掃信箱** ⇒ 同日第二次撞號(E 已改 `#499`,四處逐處開檔核、不用全域 sed)。**新立三條**:`#497`(判錯的退款不入帳 ⇒ `payment_status` 停在 `paid` ⇒ **客人訂單頁永遠寫「處理中」**;R 反向盤點抓到,主視窗開檔複驗並更正 R 一處字面「已付款」→實為「處理中」)/ `#498`(`admin_cancel_order` 與 `admin_delete_item_receipt` 對 voided **`grep -c` 皆 0**,缺口潛伏、**正確性靠乙片 V7 承重**;另更正易誤解一處=甲片檔名雖為 `adjacent_writers_voided_split`,實際 `CREATE OR REPLACE` 只有兩支)/ `#499`(貨品軸三份字面、兩套判序、零守門)。🔴 **兩條新品質教訓**:E 自曝**假紅**(突變腳本用 `git checkout` 還原 ⇒ 把突變與實作一起收掉、後 4 發的紅是實作不見了;**比假綠更難察覺,因為紅看起來像成功**;修法=`cp` 備份 + 收尾驗改動還在 + **重跑比對失敗數字**)/ V 自曝**恆綠格變形**(測試只驗「四句 title 彼此不同」⇒ 兩碼文案整組互換仍全綠)。**正式庫測資已清**(`9a02ea0f…` 的 `submitted_at` 還原 NULL;保秒驗證已過)。)
🗂️ **2026-08-14 傍晚 100🏁 壓縮後復工 + Sean 六拍板 + `#476` 片5 收割 + `#494`-`#496` 立案**(**拍板**:`Q-473-1`=A 最新一筆說了算 / `Q-473-2`=A 新開小表 / `Q-486`=**乙** ⋯改連結(知道 OD 意圖不兌現才選的)/ `Q-491`=A 車種欄維持現況 / `Q-動畫`=**B 要做但排最後** / `Q-隱藏`=**A 然後 B 也要做**(非二選一,已寫死進 `#494`~`#496`)。🔴 **`Q-473-最終` 來回三次**:R 主動送上「更正不會讓帳變對」的誤解風險 → Sean **一度改要 B** 並逐字要求「請你反駁我 或者說服我」 → 主視窗讀 `20260725130100:473`/`:96` 後反駁(**帳本是流水帳不是餘額看板;改舊列不是比較正確、是比較不正確,它抹掉「我們曾經判錯」的證據**)→ **Sean 改回 A**,但 A 附硬條件=**plan 要用機制證明沒有繞路,文字條款不算**;Sean「整個邏輯說不定太複雜」的懷疑**掛著未結案**,主視窗答「看到的每條都對得上會賠錢的錯,但沒全部審過⇒不能說全都有意義」。🔴 **`#494` 是 Sean 肉眼撞出來的真缺陷**:他點「未收未定」的單要取消一律被擋、訊息卻說它「已付款」並叫他去退款——實查正式庫 13 張,**顯示「未收未定」且未取消的 4 張裡 3 張真實狀態是 `refunded`**;打架兩處=`order-status-axes.ts:117`(顯示層 `!=='paid'` 全收斂成未收)vs `cancel-view.ts:543`(取消閘用真值),而 `:117` 上方 `:112` 註解**早已逐字預告此後果**⇒病灶是「想到了只記在原地」。**`#495` 實查後降半**:`return_status`/`'returned'` 在 admin+domain+adapters+migrations **零命中**,7 處「退貨」全是指向未來的第 3 批線 ⇒ 隱藏條件第一段只能用「已取消∪已退款」。**`#496` 立案前六字集掃 backlog+specs,與後台相關者零命中**(唯一 `#318` 範圍是前台;`transition` 12 處皆狀態機字面)⇒ 後台動畫**從來沒進過任何待辦**;驗收記為「久看不累」非「好看」。**V 片5 收割**(`2c5bc379`,`blockedInactiveNew` 文案與測試字面對齊實際可達路徑;V 自曝第一發突變只跑兩個測試檔就要下「片3 filter 零覆蓋」,跑全套才看到被 `item-procurement-section.test.tsx:479` 蓋著=**掃描字集比宣稱窄**再一次);merge 後主視窗獨立三綠 **exit 分開收**、`TSC 0 / LINT 0` 皆 `0 cached` 真跑、`vitest 458 檔 7729 過`(與 V 回報逐字相同)。**正式庫測資已清**:`9a02ea0f…` 的 `submitted_at` 還原 NULL;保秒驗證**已過**(Sean 存檔後查得秒數仍 45)。)
🗂️ **2026-08-14 午 99🏁 E 窗 L3 片5+片6 收割 merge(`d8b60a30`)+ 假 BLOCKER 撤板**(**E**:五欄再拉寬(單號 132/廠牌 188/單價 104/金額 104/發票 56、品名 322→154)全部 `0/18` 不截;🔴 **Sean 說的「列高卡到狀態圖標」病因不是列高** —— 三檔(40/32/26)紅框溢出量幾乎相同(−2.0/−2.5/−2.5)⇒ 列高非自變數,真兇=`align-top`+`PAY_MARK` box-shadow 外長 3px+片3 的 `overflow:hidden`,修法 `td.col-status{vertical-align:middle}` 一行、**列高一格沒動**、三檔各 `clipped 0/15`;`Q-E1`=A 單號格重複「已取消」膠囊下架;`Q-E2`=A **維持固定寬不做自適應**(Sean 看 1920 對照圖選的,B/C 量測表留在 design-brief);主視窗獨立三綠 `TSC 0 / LINT 0 **0 cached** 真跑 / vitest 456 檔 7632 過`。🔴 **部署後探測靶**(今早 Vercel 建置快取事故的直接後果):`col-status{vertical-align:middle}`×1 且 `col-oid{width:132px}`×1 且 `322px`×0 才是新產物,量法在 `~/pcm-mailbox/E-505-STOP.md` §3、**禁用 `document.styleSheets`**。**R**:🔴 **自報的金流 BLOCKER 是假的、已由 Sean 撤板** —— 「已付款單取消品項 ⇒ 守門式多退」場景**經由 RPC 不可達**(取消端 `a8a1:199-203`/`a8a2:360-364` 皆要求 `payment_status='unpaid'` 且零非-failed charge attempt;付款端 `confirm_order_payment` 三代皆帶取消閘、live=`20260810170000:369-370`,OP3 還把該閘字面釘進結構斷言 `:569-585`);R 驗的三條**全是真的**(total 有 CHECK/零 `UPDATE SET total`/cancel 只寫三欄)**結論仍假** ⇒ **量機制沒問可達性**;`Q-445-R4-1`=A **Sean 本人撤**、plan v8 已批、`#479`/`#480` 改標 ⛔ 不可達不刪、445a-1 五檔 code 三綠待關卡 2;估時回 445b 180。**D**:`#452` plan 照 C 案瘦身 46→36 格(`d40fda8e`)+ void×A5a 探針**含正向對照**(發⓪ 舊形狀 `40P01=1` 證 harness 有效、發④⑤ `=0` 且有 session 實卡 `wait_event_type=Lock`)⇒ 假設 A7 從推論升為觀察;Sean 直接在窗內放行寫 migration;R1 codex FAIL 7MF+2nit **全真**,D 自曝「作廢不鎖 `order_items`」是**假的理由**(A4a 內確有該鎖)但**結論仍成立**(實測),真因=作廢使該列離開索引。**制度**:🔴 **今日上位教訓=「拿一個相鄰的東西當成要問的那個東西」**(三例:機制為真≠場景可達 / 資料夾名≠圖檔日期(主視窗) / `grep -c "⏸ 待"`≠全部字面),**第二人開檔複核只證引用正確、不證場景可達=兩道不同的檢查**;判停令=plan 對抗審查停、改 commit 前審 diff(依據:近三日 422 顆 commit 中 docs+merge 320=75.8%、feat+fix 65=15.4%);**兩次窗擋住主視窗越權**(R 拒把「加速」當批准、D 拒把主視窗信當鐵則 8 批准)**兩次都對**;新常設=**窗每完成一個可交付單位必落信,commit body 與對話都不是通報載體**;`#481` 狀態閘表(只認 migrations 逐字+行號、不吃 docs/specs)進行中 —— graphify 實測**答不了**「什麼條件下才允許」且來源是 PRD 非 migration。詳 memory `feedback_blocker-must-prove-scenario-reachable-first`。**R 窗二次收割**(`e7d45601` 445a-1 回傳碼管線 5 檔 + `48c953ed` plan v8/`#479`·`#480` 不可達/`#481` 狀態閘表):**兩道審查關都過**(關卡2 codex FAIL 2MF+3nit 全處理、SOP⑥ code-reviewer PASS must-fix=0;🔴 **R 原本只跑關卡2 就要 commit,是 `pre-commit` reviewer gate 擋下來的** —— Sean 08-04 Q3=A 那道 hook 當場兌現);主視窗獨立三綠 `TSC 0 / LINT 0 --force 真跑 / vitest 456 檔 **7647** 過`。🆕 **`scripts/state-gates.sh` → `docs/reference/order-state-gates.md`**(Sean `Q-地圖`=A):**只讀 `supabase/migrations/*.sql`、不採信 `docs/`**;列出每支改狀態 RPC 的允許集合 + `檔案:行號` + **`CREATE OR REPLACE` 幾代與哪代是 live**;強度自陳「經由 RPC 非絕對」;**主視窗獨立驗可重現 —— 重跑後 sha256 逐字相同 `31abcb44…`、樹 0 dirty**;兩道自我守門(編碼閘用 `file(1)` 不用 `iconv`——後者對合法檔也失敗=恆紅閘;自測閘查無三支關鍵函式即拒絕覆蓋,第一版真的漏了 `confirm_order_payment`)。🔴 **R 自曝三件**:註解裡「新碼會靜默掉出 switch、沒有東西會紅」**兩處皆假**(`refund-repository.ts:270` 白名單先 throw、`refund-actions.ts:461-463` 早已 fail-closed)⇒ **守門有價值、錯的是替它編的理由**;自己種了 1 個恆綠格 + 1 道 no-op 守門(都被突變測出並刪除)——**同日它在別人 plan 抓了 8 個恆綠格**。**D 線甲片 R2 FAIL 12MF**,D 自判「兩輪病因重疊=同一層打轉」⇒ 主視窗裁 `Q-D4`=A **但加一道前置機械自掃**(三類:抄樣板只抄一半 / 宣稱大於事實 / 一處改了一處沒改 —— 這三類 grep 抓得到,不該用貴模型抓),自掃完才 R3 且**不准再用 `gpt-5.6-sol`**;`Q-D5`=A 只改 DB 訊息不動應用層。**D 甲片已收割**(`2c672867`+`c89f28ab`,merge 後主視窗獨立三綠 `457 檔 / 7661 過`):**codex 三輪 + code-reviewer 一輪**(後者 FAIL 2MF+6nit —— 🔴 **codex 三輪一條都沒抓到那 8 條**,證明兩種引擎打不同層:codex 打邏輯與守門、code-reviewer 打字面 vs 事實與慣例落差);M1=檔頭「誠實邊界」與 P5 互相矛盾**方向會害值班的人主動刪掉真的閘**;M2=動 2 支 `.ts` 卻 commit body 無三綠紀錄 ⇒ 🔴 **`pre-commit` 只跑 reviewer-gate + lint-staged、typecheck/build 完全無機制守**(待立 backlog,機制優先律);**nit3「錨點須恰命中 1 次」是本輪 CP 值最高的一條** —— 第一次跑就抓到 `C8` 錨在 A5a 出現 2 次、`MUT-A5a` 錨的第 2 次**出現在 D 自己的更正註解裡**(舊版只驗存在 ⇒ 會把註解一起改掉、而後面的紅會被當成「守門有效」的證據);C7/C8/C9 判別力**從論證升級為量到**(3 發定向突變各恰紅一格)。🔴 **D 反駁主視窗一條指令並且是對的**:主視窗依「前身是 `CREATE FUNCTION` 非 `OR REPLACE` ⇒ 回退掉 ACL」要求回退腳本帶 GRANT,D 實查該次 DROP+CREATE 是**因為換簽章**(加第 12 參),**本回退不換簽章 ⇒ `CREATE OR REPLACE` ⇒ ACL 原封**,且**真的退了一次**驗 `proacl` 逐字相同 ⇒ 改用**後置斷言**接風險而非多發 GRANT(「多發 GRANT 會讓『ACL 掉了』永遠不被發現」)。`scripts/452a-down.sql`(822 行)就緒 ⇒ 主視窗釘的「回退腳本未完成不得排 apply」已解除。⚠️ 主視窗自曝:拿 `bash -n` 檢查 `.sql` 檔 = 量具與標的不符,錯的是量具不是檔。)
🗂️ **2026-08-13 深夜 98🏁 四窗全收工 merge 入 dev + #452 已上正式庫 + 跨窗衝突解 + backlog #457-470 落檔**(**apply**:`#452` 片 2a-1 採購作廢欄 `supabase db push` 恰一支(dry-run 先確認),Sean 當面按;apply 前主視窗當著他的面跑 preflight Q1(唯讀)= `who=postgres / table_visible=t / col_exists=f / can_read=t` ⇒ 情境 A(欄位尚未建立=預期),判準 `col_exists=f` ⇒ 到此為止不送 Q2;三條 NOTICE 全在預期內(前置閘 A4a 指紋 `4ac2989a…` / 新指紋 `a03e50f7…` / 收工驗收含 `voided_at` 非 NULL 列數=0);**apply 後主視窗獨立複驗不只信 NOTICE** = 兩欄在、`business_key_index_cnt=1`、`business_key_constraint_cnt=0`、`voided_rows=0`;`APPLIED.tsv` 補登 sha256 `6781c355…`、全樹未登錄掃描=0。**四窗 merge**:B `#376` 撞號重編號(`2f91a7b3`)/ S OD 詳情線四片(`9fec15ce`)/ D `#452`(含 apply)/ E 訂單列表 L2+L0(`6f371758`)。🔴 **跨窗衝突**:E 的 `shipped_quantity`(內嵌層)× S 的 `customer_user_id`(orders 層)撞同一行明細投影 —— **主視窗差點用錯錨**(`cancelled_quantity)` 在該行出現 **2 次**,第二次是 `order_cancellation_items` 的、不得加欄)⇒ 改用 `instock_quantity, cancelled_quantity)` 並**先驗錨唯一**;雙向驗證「合成結果 replace 掉 `customer_user_id` == E 版原文」;**三處同步**(E 窗提醒,主視窗原只列兩處)。merge 後 vitest **455 檔 / 7588 格** + typecheck 8/8 + lint 10/10 皆 **`0 cached` = 真跑**。**三輪審查制實錘**:E-L2 三輪各抓不同層(引入的真回歸 / **新加的守門假綠** / 三條同一根因 ⇒ **換路:手寫配對→postcss AST**,七靶全紅);D-2a1 三輪(閘強度 / 宣稱vs字面 / **災難日**)+ 五發回退演練含負向(錯順序實測炸 `column p.voided_at does not exist`,同型前例 a7-rollback v2 擴列③ = **同族第二次**);S-片2 三輪(codex 雙 alias 反例 / R2 三種構造全綠 ⇒ 判停理由=「**每輪修法讓下一種更難構造卻沒讓它不可能**」)。🔴 **「給 Sean 看的三句話」被三輪各抓一句(①未附前提 / ①漏 partial index+③「一個字都沒動」為假 / ②回退宣稱+漏鎖表)⇒ 三句全被抓過** ⇒ 檔頭已寫「用三句話概括五百多行,每次概括都會丟掉一個他需要的東西 ⇒ **先寫三句,然後假設它漏了東西**」;主視窗**逐字讀檔頭**時另抓到 `:19` 舊句抵銷「三件事」框架(=**框架過期**,比資訊過期毒)。**設定優化**:新增 `~/.claude/hooks/block-git-add-all.js`(擋 `git add -A/.`,16 格正負測全綠、自測抓到 `git -C <path> add -A` 漏擋並修;實測擋住)—— 起因 D 窗當日違反該鐵律夾帶 0-byte 檔 `預期`。**backlog #457-470 十四條落檔**(零撞號實測),含 `#466` 已完成(觸控熱區 **視覺 24×16 不變、熱區 48×44、卡片高零變化** ⇒ 「無障礙 vs 太鬆」的對立來自「把視覺尺寸當觸控尺寸」這個前提)。**四窗共通母題**:今天四次「fixture/前提沒構造對,格子照樣印通過」+ 五次「改了一處舊字面留著」+ 主視窗自身七次(含**替沒驗過的論證背書並美化**、**以為有一道我把關的閘而那閘不存在**)。)
🗂️ **2026-08-13 下午 97🪟 四窗並行日:兩份 plan 雙雙判 FAIL(擋在寫 code 之前)+ B 收工 + S 片1 收割**(Sean 令「能開多少窗就開多少」⇒ 主視窗開 **E/S/D/B 四窗**,片界互不撞(退貨面板撞 #445 故不開、2g 排 #445 後、#450 未拍板 ⇒ 四窗是上限)。**B 窗收工** `c89df25c`:#376 撞號重編號,`#308/#342/#343` 三組各改一條(→`#453/#454/#455`)、18 處行級精準改號(不用全域 sed:同一個 `#343` 在不同行指不同條目)、機械複掃 74 處逐處開檔核、結構複驗 `uniq -d` 空;號池解凍**下一號 #457**(#456=22 處釘死 backlog 行號立案,主視窗裁 A)。**S 窗片1 收割** `7720ce8b`:備註搬家+三類分色+收合(主視窗裁 Q3=**C** 收合但未更正的 `customer_notified` 預設展開——稽核證據不預設藏);S **突變測試抓到自己測試的缺陷**(「截斷必須展開」那格的 `open` 其實是被「有告知筆數」滿足的 ⇒ 拿掉 `|| view.truncated` 照樣綠)。**E 窗**列表五片(L2 單一 markup 收斂進行中;🔴 實查 OD `grep -ic rowspan`=0 vs repo=19 ⇒ #447 收斂必須拆 rowSpan,交件附前後 1440 截圖)。**#445 plan v2** `e3573f8b`:折 codex R1 的 33 條,三個設計方向修正(§0-A `payment_refunds` 是刷卡補償退款非訂單退款帳、父表無 status ⇒ Sean Q3=A 前提錯已撤;§0-C v1 的 445a **會製造超退**——上界放 parser 先於 G4 ⇒ 重播被判「金額不合法」⇒ 員工換表單再退一次)。**但 R2 換模型判 FAIL,BLOCKER 指向主視窗自己**:§0-B「RPC 拿不到 TapPay Record」是**假的全稱句** —— 我證明的是「RPC 自己打不了 HTTP」(真),寫下的結論是「RPC 拿不到 Record」(假);`refund-baseline.ts:3-5` 逐字「由 server action 從 TapPay Record API 查得後**餵給** RPC」、`refund-actions.ts:231`(🔴 2026-08-14 R 窗更正:原寫 `:230`,實查 `:230` 是 `recordAmount: null`、`:231` 才是本句所指;行號會漂,認字面 `recordRefundedBefore: baseline.refundedBefore`)**partial 此刻已在傳** `recordRefundedBefore` ⇒ **Sean 的 Q-445-1=A 是在假的二選一框架下拍的,要重問**(C 案=值當參數傳進 RPC、比對放 G4 之後,更安全更便宜)。R2 另有 13 MF,最重=v2 把 `failed` 一律算佔額度會讓**網路斷線這類最常見的暫時性失敗永久卡死**,而解鎖出口(補償帳)要等 2g、2g 又排在本片之後 ⇒ 結構無解。**D 窗採購撤銷 plan v2** `9d38d4cf`(worktree):自報一條過期字面(業務鍵早由 S1b 改成 `supplier_id`)、附錄 A 八列前置檢查**逐列反向突變證明零恆真格**;**codex 關卡1 判 FAIL 19MF+7imp**,四條框架級=案 A 打破 A5a 自己的並發證明(它明寫依賴「本表無 DELETE writer」)/「下游全部備好」漏了搜尋(供應商訂單號只 JOIN 現存採購列)/冪等建在已知不可靠的 audit 上且 audit 寫在 DELETE **前**(既有 receipt RPC 是成功後才寫)/grep 數字**第二次**與 HEAD 不符 ⇒ 三案成本要整份重算。🔴 **codex 會編造像樣但不存在的檔名**(時間戳真、檔名造,四支全中)⇒ 收 findings 要按時間戳對映並逐條開檔核。**需求檔兩處更正** `4f6b8849`:§0-B「出貨完成看 `fulfillmentStatus`」三層錯(該欄 07-28 已明文凍結「新程式不得讀」/引的證據句講的是 `AdminOrderFilter` 篩選器/照做會讓「未收出貨」顯示成「未收現貨」= **最會賠錢那格的名字是反的**)、§0-J 補回寫備註與客人明細四件(S 窗 grep 全 620 行零命中、定案只活在 OD 成品裡)。memory 三條更新:假「做不到」**第二形狀=論證了 A 結論寫成 B**、「查了現況沒查現況的修訂史」、量具坑集 +3(`test -s` 驗存在不驗作者/BSD `sort -t'#'` 壞/codex 編檔名)。四窗全未 push。)
### 下一步(已被 2026-08-15 午那段覆蓋的舊分派)
~~**2026-08-14 傍晚(十六代主視窗壓縮後)**~~:**E** = `#484a` 片 A2(adapter 換源 + admin + 測試;**apply 已完成、`supabase gen types` 實測 EXIT=0 不需 `.env.local`**;🔴 **重 gen 會沖掉檔頭與「十個函式、二十六處」手動校正**,見 `packages/adapters/src/supabase/database.types.ts:1-27`;E 自行拆出「舊檔獨有 303 行 = 註解 250 + 程式碼 25」且 25 行對上 26 處計數;**動 4 檔 > 3 ⇒ 鐵則 8,Sean 本人已批 A=直接開工**;E 自報誠實缺口:⑥⑦⑧⑨⑩ 有沒有呼叫端守住尚未驗)。**V** = `#494`(已退款單標籤矛盾,Sean 拍 B 連標籤一起修;🔴 `ORDER_STATUS_LABEL` 2×4 是拍過的八值 ⇒ 新值名稱與掛哪軸**要先回主視窗**、不得自行發明第九詞;預期踩鐵則 8 ⇒ 踩到就停)。**R** = `473b-1` plan v2(**只能寫 plan、零 migration**;新增硬驗收=**用機制證明沒有繞路拿到未更正的「還能退多少」**,文字條款不算,反向盤點方向=從 code 列出所有回答「已退/還能退」的地方)。**仍在 Sean 桌上**:`#485`「待處理」chip 定義 / `#462` Q3 庫存附加條件 / 徽章兩題 `#446`·`#437` / `#374`。**排隊未派**:`#486`(乙案 10 分鐘)/ `#495` 第一段 / `#496`(刻意排最後)。
🔴 ~~**2026-08-14 午(十六代主視窗)**~~(三線分派已被 **2026-08-15 午那條**覆蓋 —— ⚠️ **原本寫「上面那條」,而它正上方那條自己就是刪除線**;其餘背景仍有效):①**E 線** L3 片5+片6 已收割(`d8b60a30`)⇒ **推版後必用探測靶驗線上產物**(`E-505-STOP` §3);E 下一件 = **OD 頁面的殼**(左側導覽/上方分頁/彩色狀態標籤/「⋯」選單;Sean `Q2`=A **要盤範圍不寫 code**、`Q4`=B **排在 E 之後不並行**,兩題不衝突、不需再問他)。②**R 線** plan v8 已批 ⇒ `#481` 狀態閘表(60 分)→ **鐵則 12 關卡 2 審 445a-1 五檔 diff** → findings 修完才 commit、不 push。③~~**D 線**~~ **D 窗 2026-08-14 午後結案**(甲片 `2c672867`+`c89f28ab` 已收割;通報路徑三犯 ⇒ Sean 開 **V 窗** `pcm-void-readers` 接手)。**`#476` 已開工並非「不開工」**(該字面隨 D 結案作廢):V 窗片1-片3 已收割/commit(投影帶回作廢標示 → 單一挑列入口 `findActiveProcurement` → 作廢列標示+不給到貨入口+選單排除),**片4 為最後一片**;`#476` 嚴重度已上修為「**編輯表單會用作廢資料覆寫生效採購(靜默資料損壞)**」、範圍更正為 **14 個面 / 8 支要動的檔**。🔴 **`#476` 必須先於 migration B 部署** —— 沒有機器守得住,`Q3`=A **上線當天由 Sean 在正式站 Console 貼唯讀量法**確認線上產物含 `voided` 字面才准跑。④仍在 Sean 桌上:`#462` Q3 庫存附加條件 / 徽章兩題 `#446`·`#437` / `#374`。
~~舊序~~(以下為 08-13 版,保留備查):①Sean 照 `docs/specs/2026-08-12-order-admin-acceptance-package.md` 跑訂單後台總驗收(🔴 六項優先;已推版、`REFUND_UI_ENABLED=1` 已設)。②Sean 看 OD 第四輪成果並拍狀態配色 A/B/C(OD 推薦 B)+ 密度預設檔 + 「未收未定」第五值。③~~2f apply 停點~~ **2026-08-13 已完成**(講完四條 → Sean 拍 A → 已上正式庫 → 台帳補登)⇒ 下一步是 **#445 超退閘**(排 2g 前)。④~~D 片 2 採購撤銷 plan~~ **v2 已寫、codex 判 FAIL 19MF 折中**;片 3 欄位精簡等 OD 定案。⑤#409 08-13 import 後三句 SQL 驗 0 結案。⑥2g→超退閘 #445→④-a/U1-U3 解鎖。
🏁 **2026-08-13 四窗全收工 merge 入 dev**(B `2f91a7b3` / S `9fec15ce` / D `6bcba338` 台帳+已 apply / E `6f371758`)—— 🔴 **2026-08-14 Sean 已全部 push 上 `origin/dev`(admin 正式站隨之重新部署)**;顆數一律當場 `git rev-list --count origin/dev..dev`、不看本行。~~E 線 L1 未 merge~~ **2026-08-14 已 merge**(`1a65b46b`;三綠 exit 全 0、`456 檔 / 7622 passed`、`@pcm/admin:typecheck` = cache miss 真跑 + 繞開 turbo 直跑 tsc 複驗 exit 0)。


## OD 商品頁改造線(並行 workstream、Claude-Code 自驅、不入主表)

> 與報價單資料線(S3a/S3b)並行的另一條線。**主表 7 欄由報價單線擁有**、OD 線狀態記此附屬區 + manifest `od_redesign`(Sean 2026-06-02 拍 A)。寫審分離 ROLE=A(施工 session 實作、審查 session 哨兵盯 dev 自動 fresh-context 複驗)、**不跑 busboy-end**(避 clobber 報價單線)、Sean 手動推。

- **視覺真權威:** OD 模板 `product-detail-rpm-template.html`(open-design "Website V2")+ `HANDOFF-rpm-template.md`。鎖定決策見 manifest `od_redesign.decisions`(決定一=A OD=視覺真權威 / D1=A 適用車款表全車種 3 欄無車系 / D3=A 12K 收進紋路無消光、由真資料 disable / Q1 override:N°03 留相關商品 + FAQ→N°04)。
- **當前 slice:** **Phase A 完成 ✅ + 已合併進 dev(merge cf630b2)**。OD-5 服務橫條 / OD-6 N°01 / OD-7 N°02 紋路牆+預覽卡+圖庫聚合 / OD-8 分頁碳纖維化 / OD-9 N°03 相關商品 / OD-10 N°04 FAQ+JSON-LD(保固共用 rpm-policies 單一真相)/ OD-11 buybar OD §12+響應式≤1079,共 11 片(OD-5~11)+ 先前 OD-1~4 全完。合併後三綠 + 完整 pnpm test 531 全綠,OD 元件 × S6 fitments 共存確認。
- **下一步:** ✅ **OD 線已收尾**(2026-06-16 查核更新):OD-12 適用車款表 + OD-12b/c/d 桌機重設計 + OD-13 FAQ + OD-V~V3b 手機/iPad 真機驗收修正**全部完成、併入 dev、已推 origin/dev**(`dev..od-redesign` 空、od-redesign tip `266f5f2` 在 origin/dev、審查 PASS `9cc5bbd` + 交接 `f3d6a42`)。此附屬區先前「OD-12 待做」字面為過時殘留、已更正。後續 OD 強化由 Sean 主導視覺(memory sean-owns-visual-design)。
- **Phase A 連續做(Sean 拍 B):** OD-3~OD-11 全速連續 commit、Sean Phase A 全完肉眼驗一次、哨兵每片自動審 FAIL 即通知。
- **分片清單:** Phase A(現做、不等 S3)OD-1→OD-11(地基/Hero/右欄/Picker+預覽/服務橫條/N°01/N°02紋路牆/分頁/N°03相關/N°04 FAQ/buybar+響應式);Phase B(等 S3b)OD-F1 適用車款表(接 fitments[])/ OD-F2 真資料收口。完整 OD區塊↔元件對照 + 計畫見對話 OD-0 偵察報告(待 Sean 點頭存 docs/specs/)。
- **⚠️ 跨線紀律:** OD 線不碰 `scripts/rpm-*.ts`(報價單線 WIP)、`docs/reviews/integration-phase1-review-log.md`(審查 session)、`docs/specs/*S3*`(報價單線)。

## 速查 / 歷史(已外移、降低本檔讀取成本)

- **速查**(Phase 1 範圍 / 技術棧 / 關鍵路徑)→ `docs/quick-reference.md`
- **變更紀錄**(slice 逐筆歷史)→ `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段

## 文件交叉引用

每次新對話依此順序對齊上下文:

1. **`STATUS.md`** ← 本檔(每次先讀)
2. `docs/PHASE-1-NORTHSTAR.md` v2 — Phase 1 真權威定義
3. `docs/lessons-learned.md` — 舊專案教訓彙整
4. `CLAUDE.md` — Claude Code 工作規則
5. `docs/PHASE-1-MILESTONES.md` — milestone 排程
6. `docs/decisions/` — 重大決策記錄
7. `docs/patterns/` — 通用 + PCM 專屬規矩
8. `docs/phase-1-backlog.md` — 未決事項
9. `docs/features/*.md` — PRD
10. `design-reference/` — 視覺真權威字面(submodule)
11. `PROGRESS.md` — 歷史紀錄
12. `docs/quick-reference.md` — 速查(Phase 1 範圍 / 技術棧 / 關鍵路徑)

衝突仲裁順序:
- STATUS.md 與其他 md 衝突 → STATUS.md 為準
- 其他 md 與對話歷史衝突 → md 為準
- 視覺 / 結構 / 路由 / 元件命名衝突 → design-reference 為準
- 業務邏輯(訂單流程、權限、價格、Medusa schema)衝突 → docs/decisions/ 為準

## Busboy 機制(沿用第一輪)

- **busboy-start.js:** Sean 在 Terminal 跑、輸出貼新 Claude Code session 第一則訊息
- **busboy-end.js:** Claude Code 在 session 最後跑、自動更新本檔 5 個欄位(最後更新 / Phase Milestone slice Branch / 最近 3 commit / 下一步 / Sean 待決策)、commit、不 push(Sean 手動推當 review checkpoint)
- ⚠️ WO-6 後「變更紀錄」已移 `PROGRESS.md`「STATUS.md 變更紀錄歸檔」段、不再寫本檔;busboy-end.js 若仍寫 STATUS 變更紀錄表需同步改寫(pcm-tools 外部 repo、待 Sean 處理)
- repo 參數:`pcm`(本 repo)/ `tools`(pcm-tools)

第一次 busboy-end 跑之前、本檔欄位手動填(start template 用、由 Claude.ai 維護)。

busboy-end 跑完後 amend 進 slice 主 commit、不另開 commit。

— END —


## 🗂️ 2026-08-16 I 窗 STATUS 大掃除搬出區(零刪除、逐字保存)

### 最後更新 · 由主表輪替下來(2026-08-16 深夜,主表 ≤30 行約束;逐字保存)

🗂️ **2026-08-16 下午 107🏁 四線收割全數 merge + 四支 migration 全進正式庫 + 已推 + 真登入線開線 + I 窗(整合窗)新設**(**收割**:`4accacef` void-readers 32 / `578a616f` products 17 / `15a65864` print-docs 18 / `a35900f3` customers 17,**每併一支跑一次完整驗證**、最後一次 `vitest 500 檔 / 8307 passed / 2 expected fail / 1 todo`;🔴 **三綠一律 `--force`** —— 第一次跑拿到 `FULL TURBO 8/8 cached` 而 replay log 路徑指向**另一棵樹** ⇒ **快取命中不是執行,而它的輸出長得跟成功一模一樣**。**migration 四支全套**(`20260816010000` `#525` / `030000` 客戶頁三欄 / `040000` `#518` / `050000` `#522`);`010000` 版本號逆序被 `db push` 擋、Sean 拍【甲】用 `--include-all`;🔴 **它第一次 apply 是被【自己的守門】擋下的** —— Supabase 平台對 `public` 掛 `ALTER DEFAULT PRIVILEGES`、新函式**直接授權給 `anon`/`authenticated` 具名角色** ⇒ `REVOKE ... FROM PUBLIC` **收不到具名授權**,而該支是 `SECURITY DEFINER`、`anon` 是 storefront 印在訪客瀏覽器裡的公開角色;修法 `f90670b3` 加 `REVOKE ALL ... FROM anon, authenticated`,通過的四項裡有一項是新加的 `has_function_privilege(...)` ⇒ **這是正式庫自己回答的,不是推論的**。**已推**(Sean 明確指示):93 顆進 `origin/dev`,`7c10f1a8` 補登 `APPLIED.tsv`;🔴 **`pre-push` 閘看的是台帳不是資料庫** ⇒ **「套了」與「登錄了」是兩件事,apply 完的下一個動作就是追加台帳**。**真登入線開線**:Sean 對「沒選身分⇒靜默寫入失敗」的第一反應不是選項而是反問「不是要跟報價單登入做在一起嗎?怎麼又回到要先選身份?」—— **他是對的,而檔案站在他那邊** ⇒ 重問後拍【甲｜真登入現在做】,`#534`/`#536` **不補擋板**;🔴 **判別句(可複用)**:**我出的選項,是不是全都站在同一個【當初是拍板、現在可能過期】的前提上?選項全在同一前提底下 ⇒ 那個前提永遠不會被問到。** **威脅模型定調**(以後不要再問要不要對員工分權):Sean 逐字「所有員工都可以看到客戶資料**沒有分權限**,我唯一擔心就是**資料被駭客攻擊**」⇒ **內部不分權是刻意的,防的是外部**。🆕 **I 窗(整合窗)2026-08-16 新設**:收割 / merge / 三綠 / 產 STATUS / 機械對帳歸它,**衝突不自解、退回原作者窗**;理由=這些事需要的是輸出不是記憶。詳交接檔 `docs/handoff/2026-08-16-collection-and-migration-handover.md`。)


### Blocker · 分母打架原文(2026-08-16 已解,逐字保存;內含一句已於主表更正的假話「零測試分得出它」)

🔴🔴 **`order-status-axes.ts` 兩份分母打架(收割產物,兩邊都併成功而結果比任何一邊都糟)**:`goodsAxisOfLines` 分母已是 `quantity − cancelled`(`#522`),而 `goodsAxisProgressNote()` 還是舊的 ⇒ 一張「訂 5 取消 2 剩 3 已到貨」的單:**膠囊寫「已到貨」✅、小字寫「本單 5 件中已出貨 0 件」🔴** ⇒ **員工會去倉庫找 2 件不存在的東西**。**驗活**(主樹 2026-08-16 實跑 = **1 命中,缺陷仍在**)`grep -c "sum + l.quantity" apps/admin/src/lib/orders/order-status-axes.ts`。🔴 **修法不是改那行數字,是讓小字與 `goodsAxisOfLines` 共用同一個 `need()`**;而 `#522` 那個已 commit 的修**現在零測試分得出它**(把 `need()` 分母改回 `quantity` = 整個撤銷 `#522` ⇒ 零紅;改成 `l.quantity + 999` ⇒ 13 格紅 ⇒ **量具是活的、那 13 格分不出這件事**)⇒ **本片第一個動作是【造出那兩格】,不是重構**。⚠️ 同批要改 `:293` 那句會立刻變假的 docstring,並拿舊字面跑 `scripts/literal-sweep.sh`。📎 正式庫 `cancelled_quantity > 0` 目前 0 筆 ⇒ **降低急迫性、不降低正確性**。


> 🔴 **主表整份停在「收割前」** —— 寫滿模擬、彩排、順序敏感度那些**已完成**的東西,
> **下一個人讀會誤以為還沒收割**。收割已於 2026-08-16 完成、四線全 merge、四支 migration 全套、已推 `origin/dev`。
> ✅ **本區零刪除、一字未改**,只把【已完成】與【已過期】的段落從主表搬下來。
> ⚠️ **搬出時實查抓到三處過期宣稱**(不是搬,是事實變了):
> ①「兩支 0814 migration 待 apply」→ `APPLIED.tsv` 已於 `a6abed10` 補登兩支;
> ②「`#535` 已修、只差 push」→ 已在 `dev` 且 `origin/dev..dev` = 0;
> ③「BMW M 片未 push」→ 已隨 `578a616f` 推出(真路由驗收仍未做,已留短行在主表)。

### 行數數法的完整原文(2026-08-16 E 窗 `E-669`)

> 🔴 **主表行數的數法(2026-08-16,E 窗 `E-669` 續):同一份檔同時有三個都對的數字** ——
> **分隔線上全部行 `54` / 扣尾端空行 `54` / 只數非空行 `41`。**
> **⚠️ `CLAUDE.md` 那條「主表(分隔線上)≤30 行嚴守」沒定義用哪一種 ⇒ 超標感差一倍多,**
> **而下一個人會挑對自己方便的那個,且他不會覺得自己在挑。**
> **⇒ 本檔一律以【非空行】計,量法 `awk '/^---$/{exit} NF{c++} END{print c}' STATUS.md`。**
> **(改 `CLAUDE.md` 本體要 Sean 拍板,此處先在本檔寫死。)**

### 最後更新 · 08-14~08-15 三條(被 08-16 那條接續)

🗂️ **2026-08-15 下午 106🏁 Sean 拍九題 + 六個抬頭值到齊 + codex 從「審查器」改當「產出器」(他指名的)**(**Sean 拍板**:`Q-下架=乙`(逐字「一次完整做好,不要做一半」⇒ 排進商品編輯線)/ `Q-D-20=甲`(出貨明細單=請款類)/ `Q-D-22=甲`(資料不完整整張不印)/ `Q-折扣欄位=甲`(下單時存原價,新單有折扣舊單空白 ⇒ A 線片1 之後的下一片)/ `Q-chip名字=甲`(改名,他舉例「未完成」「待收款/待訂貨」——**括號是舉例不是拍板,名字待定**)/ **商品後台八題全拍**(見 memory `project_0815-product-admin-eight-rulings`)。🔴 **`Q1` 他不是照選是改了選項**:codex 的甲要「必須填原因」,他逐字「不用填原因,太累了,有變更紀錄即可」⇒ **變更紀錄從稽核輔助升級成【唯一能回答「這欄為什麼被鎖住」的東西】,實作時是承重需求**;`Q4=乙`(主+副分類)與 `Q8=丙`(共用檢視大家能改)**推翻 codex 推薦**,代價各已寫進 demo 需求檔;`Q6=乙`(庫存 0 可接單=預購)+ 早先「庫存前台不顯示」⇒ **客人不知道自己買的是預購品**,已記成有觸發條件的缺口(未問 Sean)。**六個抬頭值逐字到齊**:`派達有限公司` / `+886 930-531-867` / `sean@pcmmotorsports.com` / `90003020` / `新北市新莊區化成路736巷18號一樓` / `@pcmmoto` + QR 帶參數版 ⇒ **D 的 C1 解鎖**(仍未實作,等 Sean 本人過 plan)。🔴🔴 **Sean 指出「功能性設計思考略遜於 codex」⇒ 改用 codex【產出】不只【審查】**,拍 `codex 協作=A+resume`;產出 = `~/pcm-mailbox/主-ALL-007-codex商品後台功能設計原文.txt`(**獨立內容約 487 行 —— 1,050 行是同一份重複兩次**),八情境骨架 + 8 題拍板題 + 12 件「一定會撞到但沒定義」。⚠️ **codex 讀不到 repo(明文禁止翻)⇒ 它講的我方現況全是我餵的回音** —— 實錘:我餵「凌晨同步」而**實查 `rpm-sync.yml:48` 是台灣 12:30 表訂、實際 13:25-14:21**(07-22 從 03:00 搬走躲爬蟲)⇒ **同步在上班時間正中間跑,員工上午改完下午自己看到東西消失**,已向 Sean 更正。🔴 **codex 三道判準(不互相涵蓋)**:①`task_complete`(跑完沒;**背景 `( … ) &` 三次全被收掉而 exit code 皆 0** ⇒ 一律前景跑)②逐條對交辦(涵蓋沒)③**內容有沒有重複**。**四線交件**:C `4e02fc79` 片3 窄版 chip 排自己一行(甲案,我裁;**三個窄寬度整列高皆 62 ≤64、命中區 48×44/60×44/60×44 `elementFromPoint` 四向探邊**;🔴 **C 自報「動了驗證本身」並舉手** —— 收窄一格守門的掃描範圍,因該格隱含前提過期(`.fchip` 頂層 1→2 條),**拒絕兩條繞過路、覆蓋只增不減、M3 突變證明判別力未閹**,我核准並照實記錄不寫成「順手調整測試」)/ B `75eba029` demo 需求檔 + 欄位歸屬表折完 E 的 F1-F4 / D plan v7 折完 codex R2 三條(**輪次 2/2 用畢**)/ A `1a` 三支檔已寫**零實跑證據**(migration 號我發 `20260815040000`)。**主視窗本輪自己犯的**:🔴 叫 D「釘六個值的常數」——**該釘的是渲染輸出**(常數對而顯示層正規化 ⇒ 測試照綠)/ 🔴 `#466` 熱區做法引錯對象(`:731` 是註解區、真規則在 `:781` 且掛在卡片模式取消鈕,`.fchip` 從無熱區)—— **C 指出病根是「讀到論證就當成適用範圍,而邊界那句就在同一段」**/ 🔴 報 codex 產出「982 行」而獨立內容 487。**新落 memory 五條**:`feedback_verify-the-result-not-that-the-action-ran` / `warning-carrier-must-outlive-what-it-warns-about` / `rules-need-applicability-clauses-not-just-prohibitions` / `wrong-subject-narration-every-word-true` / `pasting-intermediate-values-hides-your-own-arithmetic`,另 `project_0815-product-admin-eight-rulings`。)
🗂️ **2026-08-15 午 105🏁 五窗日班:三題送 Sean + STATUS 收帳權從紀律做成機制 + 兩個「顯示的資料從來不是現實」**(**已收割**:B `168bba7b` 商品編輯調研 v2 + **商品欄位歸屬表** `docs/specs/2026-08-15-product-field-ownership.md` / C `6560cd4d` `#485` 片2 待處理 chip + **chip 欄位單一來源** / A `052809ed` `#514`+`#515` 立案 / D `#513` + `#516` / 主樹 `6394b661` 守門。🔴🔴 **三題已送 Sean(未答)**:①**後台現在沒有任何方法可以把商品下架** —— `delisted_at` **零寫入端**(B 欄位級證據;完整敘述照 B 的更正=「同步以前會自動下架、現在不會了(他要的),而他要的另一半『自己按』還不存在」,**不是功能沒做,是換手做到一半**)②`Q-P-4` **重寫版**=「新欄位一出生要決定歸誰」(三個「甲」實際是 **7 個新欄位**;現行同步 **無條件覆寫 products 15 欄含 `price_general`/`availability`、variants 十欄全無條件**)③**經銷折扣在紙上看不看得見**(`price_store` 是獨立價格欄非折扣、`tier_at_checkout` 用來凍結用哪個價 ⇒ 照 schema 最順的實作會讓紙上「折扣」恆 0)。🔴🔴 **兩個「顯示的資料從來就不是現實」**:①`orders.fulfillment_status` **正式庫 13/13 全 `notOrdered`**(主視窗親跑 `supabase db query --linked`),而 `order-detail.tsx:368` 有一格「出貨狀態」直讀它 ⇒ **那格從來沒對過一次**;該欄 COMMENT 07-28 就寫「不得讀取本欄做判斷」—— **對「把它 render 出來」零約束力,因為 render 不是判斷** ⇒ `#514`(嚴重度高)②`order_items.workflow_status` 同樣是死的(RPC 撤權 + 零呼叫端)⇒ **系統裡根本沒有「標記已出貨」這個動作** ⇒ 貨品軸就是動作軸,A 的 0a 矩陣**不用改軸**(A 自行推翻先前「軸選錯一半」那句)。🔴 **主視窗發了一封錯誤指令**:逐字要三個子窗「STATUS 7 欄同 commit」,成因=照抄 `CLAUDE.md` 單 session slice 收工清單而當下是多窗 ⇒ **兩條規則都有效、都沒過期,錯的是【哪一條適用】,過期掃描抓不到**;**A/B/C 三窗全部自己查證擋下來**(靠的是那條 memory 自己寫了判別句「開工令是別人發的就是子窗」)⇒ **有判別句的規則才擋得住誤引用**;C 當場指出「三次都靠子窗多查一次,那不是機制是紀律」⇒ 照機制優先律做成 **`.husky/status-owner-gate.sh`**(`6394b661`,四格實測含逃生門,判別走 `git-dir == git-common-dir` 不寫死路徑)。🔴🔴 **但 C 窗當場實查推翻「已生效」**:`.husky/` 是 **repo 追蹤檔**,而那顆只在 `dev` ⇒ **各子窗分支的 `.husky/pre-commit` 裡 `grep -c status-owner-gate` = 0** ⇒ **那道守門現在裝在唯一不需要它的地方(主樹),而它要保護的每個窗都還沒有它**;**我那四格實測是在一個「帶著那顆 commit 的拋棄式 worktree」裡量的 ⇒ 它證明「gate 會擋」,不證明「真正的子窗有 gate」——量到的東西與要保證的東西差一格**。**生效條件=各窗把 `dev` 併進自己分支**(那顆已在本機 `dev`、不需等 push);在那之前**擋住它的仍然是紀律不是機制**,C 逐字「我今天沒被 gate 擋住,不是因為 gate 有效,是因為我查了三處出處 —— 那兩件不能混記」。**其餘**:A 自曝 0b 誤報「金額零 DB 阻擋」——實有兩條列內 CHECK,而**真正的洞是跨列**(`orders.subtotal = Σ(line_total)` DB 表達不了,原 DDL `20260604120000:172-174` 明寫「由 `create_order` **單一寫入者**保證、不加 deferrable trigger(RPC 權威已足)」)⇒ 🔴 **片1(改金額)定義上就是加入第二個寫入者,那個「已足」的前提在片1 落地當刻消失而 DB 不會有任何反應**;E 判 D 的對外出貨單 plan v4 **PASS 0MF**,但 **codex 卡死沒跑完**(它去查公開網路而沙箱沒網路;E 自陳「是我沒把禁網路寫進 prompt,不是 codex 的」⇒ 新教訓 **主詞放錯的敘述不會被察覺成錯誤,因為每一個字都是事實**),已重跑;`#516`=對外印刷品的 `www.` 網址是 6:117 少數派、會被未來「一致性整理」的好人掃掉。**新落 memory 三條**:`feedback_pasting-intermediate-values-hides-your-own-arithmetic`(A 差點撞 `#513` —— **`head -6` 剛好切掉腳本的結論行,然後自己補了一個看起來一樣的**;主視窗同日同坑)/ `feedback_wrong-subject-narration-every-word-true` / `feedback_rules-need-applicability-clauses-not-just-prohibitions`。)
🗂️ **2026-08-14 夜 104🏁 `#486` 乙案收割 —— 差點把手機觸控目標改小,而所有 repo 內守門照樣綠**(merge `8e786889`;主視窗獨立**四綠 exit 分開收**、`TSC/LINT/BUILD` 皆 `0 cached` 真跑、`vitest 459 檔 **7749** 過`。E 判 **FAIL-B** 自行 commit(9MF+6nit),主視窗核三條件後**接受不退**。🔴🔴 **本輪最重的一條**:E 宣稱卡片模式 ⋯ 是 36×36、熱區 44×44,`code-reviewer` **用真 Chromium 量到 30×36 與 36×42**,而**改版前是 50×48** ⇒ **這一片本來會把手機觸控目標改小**,而 `#466` 那條拍板正是為了「Sean 常用手機、取消鈕只有 24×16」而做的。病因=**跨選擇器特異性**(`.orders-grid .col-ops{width:34px}` 壓過卡片區塊 `td{width:auto}`;`a[data-nav='page']{display:inline}` 同特異性後出現蓋掉 `inline-flex`)。⇒ **三綠、10 發突變、postcss 守門全部照樣綠 —— 因為它們釘的是「字面」,壞掉的是瀏覽器算出的「用值」**;已落 memory `feedback_css-guards-pin-the-literal-not-the-computed-value`,常設要求=**改尺寸/顯示/位置的 CSS 交件必附兩檔真瀏覽器量測(視覺 + 觸控命中區 + 另一槽 `display:none`)**。⚠️ E 修 F4 時**第一版又踩同一個坑**(對兩槽通用寫 `display` ⇒ 手機同時冒出兩顆 ⋯),**兩次都是量出來的、零次是推出來的**。其餘:F7 把「靠機制」誠實降級成「靠紀律不靠機制」/ F9 `orders-table.tsx` 538 行**判斷不拆**、理由已入 commit body(鐵則 6 允許)/ F10 觸控引用改正(**44×44 是 AAA SC 2.5.5,AA 是 SC 2.5.8 的 24×24**)/ F15「桌機 26×26 零守門」是本輪唯一真空白,補格後突變由綠轉紅。**E 下一片 = B-1**(已批、`['none','ordered']`、5 檔),第一個必修=`order-filter-bar.tsx:49` 的 `goodsAxes?.[0]` 多值時顯示與實篩不一致。)

### 下一步 · 收割前的五條分派(收割已完成,全數過期)

🔴🔴 **本段【五條分派全部過期】(2026-08-16 11:0x,五窗 reload 後【各自自陳】,非二手推論)** ——
**A/B/C/D 四窗逐字都是「手上沒有進行中的施工 / 停著等派工」,E 窗唯讀無施工。**
⚠️ **本段原本只標了 B 過期** —— 而 E 窗 `E-669` MF-1 指出:**那一條標記會替其餘四條背書,比五條都不標更糟。**
🔴 **現況一律以各窗當場量測為準,不以本段為準。**下面原文保留備查:

~~🔴 **2026-08-15 午(五窗日班,最新;覆蓋下面所有分派)**~~:**A**(`pcm-void-readers`)= `#13` **片1 改金額 plan**(鐵則 12① ⇒ 關卡1+2 都不降級;四條硬約束已下:§0 首段就寫「本片讓一個 DB 註解裡的前提失效」/ 明確回答「不加 deferrable trigger」要不要翻案 / **不照抄「避免複雜度」當理由**(前提也沒了)/ 「有東西會紅」要指名檔與格且能被負測打紅);**寫完先給主視窗,不動手**。**B**(`pcm-products`)= ⚠️ **本段已過期(08-16 B 窗自查 `B-578` F2)**:B 線在 08-15 午之後【整夜都在做事】並已 commit(設計參照 / token 片 / 列印單據 OD 兩輪 / 合規盤點 / 圓角定位器四輪審查 + R4 換路 / `#540`-`#544` 四條立案),**未推顆數當場量**。🔴 **「停等 Sean」與「做完了在等推」在調度上是相反的兩件事** —— 照本段判斷會以為 B 線閒著。(`Q-P-4` 本身**沒有作廢**,它仍等 Sean;錯的是「B 這條線只有那件事」這個含義。)~~停等 Sean 答重寫版 `Q-P-4`~~ —— 下一片會改變同步行為(要跳過某些列)⇒ 不在主視窗授權範圍,且**先開會變成「猜一個歸屬規則然後實作」**。**C**(`pcm-customers`)= 片3(肉眼驗 + 空狀態文案)**綁「送 Sean 肉眼驗」一起排** —— 🔴 片1/片2 的正確性**還沒被真人按過**,片3 是第一次有人真的看到。**D**(`pcm-print`)= 補 `#513`(加一段:`price_store` 存在 / `tier_at_checkout` 用途 / 照 schema 最順的實作會讓折扣行恆 0;**寫成「這裡有兩條路」不寫「Sean 錯了」**)→ 停等 E findings。**E**(專職審查)= D 的對外出貨單 plan v4 **codex 重跑中**(prompt 已加禁網路三行並做成固定樣板)→ 然後寫 `#516`。**🔴 主視窗待辦**:三題送出後追 Sean 回覆;**商品線片1a/1b/2c 三片人眼驗收零次**已累積,要跟三題一起送。
🪟 **2026-08-14 三線並行(Sean 拍:全開獨立視窗)**:**E** `pcm-od-list` = L2 手機小片(每品項寫滿車種/廠牌,Sean 08-14 拍)→ 之後 L3 / **D** `pcm-procure-void` = `#452` 片 2a-2 void·unvoid RPC **只提 plan** / **R** `pcm-refund-guard` = `#445` plan v3 跑 **R3 對抗審查**(換角度換模型;plan §12 逐字)。追蹤帳 `~/pcm-mailbox/.pending-replies.md`(十三代版整份過期、已重寫;含**號碼配發台帳** —— 01:12 兩窗各自「查最大號+1」同時挑中 `#471`,主視窗製造的撞號,改為集中發號:D=`#471`/E=`#472`/R=`#473`)。**已收割**:`#473` RW4 判定單向不可逆(硬前置=`#445b` apply 停點,**不是員工上工前**)/ **`#445` plan v4**(684 行,折堆 B 22 條;🔴 **C1 金流 fail-open** = v3 只有 partial 那行有 `guard IS NULL OR`、full 那行沒有 ⇒ 兩行已對稱;🔴 R 折的過程**新抓一條 v3 沒有的**:`SUM()` 對零列回 NULL ⇒ 每張單第一筆退款會被擋死,已定 `COALESCE(SUM(...),0)`;堆 A 5 條標 `⏸` 待 `Q-445-R3-1`,11 處標記實查)。**E L3 拆 4 片**(主視窗裁):**片1/片2/片3 已收割 merge**(狀態八值欄上場+訂貨欄與付款膠囊下架 / 欄序重排+單價欄+四欄名依 Sean Q3=B / 14 欄寬+container query 換掉 `md` 斷點),片4(密度 `?den=`)開工中;三綠每次 merge 後重跑皆 exit 0(最後一次 `456 檔 / 7623 過`)。🔴 **reviewer gate 擋:E 與主視窗 session 皆禁用 AgentTool ⇒ 裁由 R 窗 fresh-context 代審(對 SOP `code-reviewer` subagent 的明文偏離,標記逐字申報、不假裝)**。🔴 **~~未推 19 顆時的推版限制~~已解除**(⚠️ **那個 `19` 是寫死的未推數,違反本檔 `:21` 自己的拍板「不寫死 hash 與未推數」;顆數一律當場 `git rev-list --count origin/dev..<branch>`**)(片3 修掉桌機橫向溢位;片2 後曾有 1664 vs 1414 中間態)。**Sean 桌上剩一題**:單價/金額/發票三欄放不下(最大差 26px)⇒ 加寬 59px 從品名扣 vs 照 OD 接受截斷;🔴 **舊格式單號已絕種**(實查 orders:新格式 6 碼 10 筆最晚 08-12、舊格式 3 筆最晚 **07-25**,產號器固定 6 碼)⇒ 單號欄不加寬、不為 3 筆死資料付永久成本。**E 兩次自我推翻**:①L2 手機差異是 fixture 不是 code(Sean 拍板建立在假差異上)②「緊湊檔可能放得下」被自己的量測推翻(密度一欄都解決不了)。🔴 **`#445` plan v3 已存在**(`188e185d`)—— 交接檔與舊追蹤帳寫的「停在 v2 待折」是過期字面。
🧹 **2026-08-14 環境整理(新視窗照舊路徑會撲空,詳交接檔 §0-B)**:報價單 repo 已 clone 到本機 **`~/API大量上架/PCM報價單-V2`**(與 origin/main 同步、`.env` 三支就位;舊的 222 顆落後版改名 `.OLD-222落後` 封存,含 `data/` 461M 可當備料)/ `~/pcm-截圖` → `pcm-website-v2/.screenshots/`(gitignore)/ 報價單一次性腳本 4 組 → 該 repo `data/one-off-archive/` / worktree 18→4(留主樹 + `pcm-od-list` + `pcm-site-redesign` + detached JSDoc 那顆)/ `.turbo` 16G 快取已刪 ⇒ **下次 typecheck/lint 全量重跑會慢一輪,是預期不是故障**。
🔴 **夜跑新視窗第一件事**:讀 **`docs/runbooks/multi-window-command-workflow.md`**(2026-08-09 拍板【常設】,CLAUDE.md 路由表有指;§B 主視窗建置 / §C 施工窗啟動)。⚠️ **本行原本指向 `docs/handoff/2026-08-13-night-run-handover.md` —— 那份沒壞,但它是【那一夜的】一次性交接檔,不是常設流程**;該夜細節仍可讀 `docs/handoff/2026-08-13-night-run-handover.md` —— 四線交接、各窗未落檔判斷、`#452` 開工前必讀、**E 線 L3 兩題待裁 + `order-list-view.ts` 668 行鐵則 6 判斷**(細節在 `~/pcm-mailbox/E-381-HANDOFF.md`,85 行必讀)。

### Sean 待決策 · 08-16 決策單 12 題(Sean 已於 08-16 早上答畢,見 memory `project_0816-sean-morning-13-rulings`)

🔴🔴 **2026-08-16 晨:桌上 12 題已整理成單一入口 `~/pcm-mailbox/主-SEAN-決策單-20260816.md`**(A 線上缺陷 / B 每日操作 / C 設計外觀 / D 操作類 / E migration apply)。**答法=回 `Q1=A Q2=B …`。** 🔴 **檔頭另有一條「醒來先跑」的三十秒命令** —— C 窗(`~/pcm-customers`)的 reviewer 標記綁 HEAD、已過期,**在 Sean 跑它之前 C 窗不能 commit**;⚠️ **「跑過標記」與「標記還對得上」是兩件事,而過期的外觀與從沒跑過【完全相同】**。

### Sean 待決策 · 已拍板結案的劃線條目(`§0-STANDING` 第 5 條 / E 線 L2 兩件 / 2f apply 停點 / `Q-445-1` / `Q-E-2` / 徽章兩題 / 視覺統一改輪 / OD 三題)

~~`§0-STANDING` 第 5 條~~(**2026-08-14 已拍「要」並落檔**:條文在 `docs/specs/2026-08-12-admin-order-ui-design-brief.md:542`、commit `916106bf` ⇒ 本格為過期字面,十五代撤下。內容=改動過的可點區域要用 hit test 驗、不能用截圖驗;兩個假通過(rect 0×0、視窗外回 `null`)已一併寫進條文)。
~~E 線 L2 兩件~~(**2026-08-14 已拍**:①桌機 = **收斂後**(維持 `025a7e7e` 現況、不退)—— Sean 逐字「那算了,太麻煩了。就選收斂後吧」,他是在知道代價之後選的(選項 A=退顆回到收斂前但程式碼變回兩份、B=保留一份再壓行高;他選第三條=接受現況不再花工)⇒ **表格總高 +10.4%、多品項單第一列 38→62px 為已接受的既成事實,後續片不得再拿它當缺陷報**;rowSpan 拆除與 §0-B 字面的衝突同時結案(判定=舊條載體被新拍板取代)。②**手機採 `l2-after-430-v2.png` 那版**=每個品項都重複寫滿「車種/廠牌」,**不是**現行 code 的最終版(第二項省略重複值)⇒ **待改一小片**,無架構衝突、交件須附 430 截圖(§0-STANDING)。)
~~2f apply 停點四條~~(2026-08-13 已講、已 apply)。~~Q-445-1~~(**2026-08-13 拍 C**:Record 值當參數傳進 RPC、比對放 G4 之後;推翻同日稍早的 A —— A 是在主視窗的假前提下拍的。**Sean 同時定調「我要求的是使用上直覺、好用即可」** ⇒ 已轉成 #445 驗收條件:錯誤訊息要讓員工知道**下一步做什麼**,不是丟技術碼)。~~Q-E-2~~(拍 A:訂貨 n/m 欄與付款膠囊下架、狀態欄獨扛)。徽章兩題:#446 沖銷之沖銷語意(D 立案三選項)/ #437 訂單標頭殘題(要不要標頭再一顆「已收足」徽章;四點主體已完工 `76aff2e3`)/ 採購「改軟」三片做完後要不要接 #374 採購單層與庫存(落點已定=報價單 repo)/ memory 索引下輪盤整再議。~~視覺統一改輪~~(2026-08-13 已拍:OD 三塊方向選定,詳情甲/列表丙/客人卡甲)。~~OD 三題~~(2026-08-13 已拍:狀態配色 **A 溫和**(推翻 OD 推薦的 B)/ 密度預設 **寬鬆 40px** / 「未收未定」第五值 **保留**;第五輪只修兩件=物品名稱吃空白其餘六欄被截、手機版排列)。

### Blocker · 兩支 0814 migration「待 apply」(過期:`supabase/APPLIED.tsv` 已於 `a6abed10` 補登兩支,commit 訊息逐字「2026-08-14 夜正式庫已 apply」)

🔴 **兩支 migration 待 apply,各帶一條前置(都是 Sean 的停點)**:①`20260814180000` 乙片作廢 RPC —— **apply 前確認正式站 admin 已含 `#476` 片2**(`8dfad824`;主視窗實查 `git merge-base --is-ancestor 8dfad824 origin/dev` = **已在**,前提滿足,**但 push 後仍要確認線上產物**,今早 Vercel 建置快取事故的教訓)。②`20260814190000` `#473b-1` —— **apply 前對正式庫跑 `select defaclrole::regrole, defaclobjtype, defaclacl from pg_default_acl;` 並留檔**(R 的動態 REVOKE 已不依賴它,但那個結果要當事實留下;原本的假前提就是 shim 量的)。

### Blocker · BMW M 直角片「未 push」的完整原文(過期:products 已 merge `578a616f` 並隨 `origin/dev` 推出;真路由驗收仍未做,已留短行在主表)

🔴 **推之前必讀(08-16)· BMW M 直角+邊框片(B 窗 `~/pcm-products`,未 push)**:**未經【真路由】computed style 驗收** —— 關卡在**部署後**不是 commit 前(admin 需 SSO+2FA、登入態只在主視窗瀏覽器、施工窗進不去)⇒ **推完主視窗必須在真路由量輸入框/按鈕/狀態膠囊的 `borderRadius` 與 `borderTopColor` 並回報**;R3 原話「`token 已改` 被當成 `員工畫面已驗收` 的框架錯誤」。📎 狀態膠囊仍 pill,**待 Sean 裁方/圓**(圖 `~/Desktop/膠囊-方圓對照.png`)。

### Blocker · 正式站兩缺陷完整原文(② `#535` 已修並已推,`order-keyword-cookie.ts:52` `ORDER_KEYWORD_COOKIE_PATH` 常數在樹上)

🔴🔴 **正式站兩個【現在就有】的缺陷(2026-08-16 主視窗真瀏覽器實證,非推論)**:①**沒選具名身分 ⇒ 後台所有寫入動作靜默失效**(無訊息、畫面完全正常),而「尚未選擇」**是每個人的預設狀態** ⇒ 新員工第一天就撞上,**擋在北極星上**;`backlog #534`,**碰 auth=鐵則 12②,列了甲乙丙未選,等 Sean**。②**「清除搜尋」假裝成功** —— chip 消失、列表回全部,而 cookie 因 `delete` 未帶 `Path` 沒被刪(**實測 `admin_order_keyword path=/orders`**)⇒ 下次進來又被篩著;`#535` **已修、只差 push**。

### Blocker · migration 四支 apply 的全部過程與已作廢字面(四支皆已進正式庫)

✅✅ **migration 四支全部已進正式庫(2026-08-16 13:3x 主視窗實套;`supabase migration list` 四支遠端欄皆有值)**
🔴🔴 **而 `20260816010000`(`#525`)的過程是今天最重要的事**:第一次 apply 被**它自己的守門**擋下,逐字
`#525:acl 形狀是 [anon:EXECUTE,authenticated:EXECUTE,service_role:EXECUTE],期望 service_role:EXECUTE`。
**真因**:Supabase 平台對 `public` 掛 `ALTER DEFAULT PRIVILEGES`,新函式**直接授權給 `anon`/`authenticated` 具名角色**
⇒ **`REVOKE ... FROM PUBLIC` 收不到具名授權**。這支是 `SECURITY DEFINER`、`anon` 是 storefront 印在訪客瀏覽器裡的公開金鑰角色。
✅ **修法 `f90670b3` 已 apply 通過,而通過的四項裡有一項是新加的「有效權限」檢查**
(`has_function_privilege('anon'|'authenticated', ...)`)⇒ 🔴 **這是【正式庫自己回答的】:那兩個角色現在沒有可繼承的 EXECUTE。**
⚠️ **仍未關的**:`NOINHERIT` 的角色 `SET ROLE service_role` 這條路徑守門看不到(拋棄式 PG 17.10 實測),
**Supabase 的 `anon.rolinherit` 實際值未確認**。
🔴🔴 **未做的推廣**:`supabase/migrations` 全樹 `SECURITY DEFINER` 出現 **388 次**
(量法 `grep -ci 'SECURITY DEFINER' supabase/migrations/*.sql | awk -F: '{s+=$2} END{print s}'`)—— **本次只查了 `#525` 一支。**
⚠️ 逆序:`010000` 版本號低於已套的三支,`supabase db push` 預設拒絕,Sean 2026-08-16 拍板用 `--include-all`。
~~✅🔴 **migration 四支(不是三支)—— 2026-08-16 11:3x 主視窗實套,三成一敗**~~(量法 `supabase migration list --workdir <scratch>`,正式庫實查):
**✅ `20260816030000` 客戶頁三欄 / ✅ `20260816040000` `#518` 錯誤訊息 / ✅ `20260816050000` `#522` 貨品軸分母** —— 三支各自的 `DO $verify$` 驗收 NOTICE 都印出通過。
🔴🔴 **`20260816010000`(`#525` 客戶搜尋)被【它自己的 apply 守門】擋下、未套。逐字**:
`#525:acl 形狀是 [anon:EXECUTE,authenticated:EXECUTE,service_role:EXECUTE],期望 service_role:EXECUTE`
🔴 **真因**:Supabase 平台對 `public` 掛了 `ALTER DEFAULT PRIVILEGES`,新函式**直接授權給 `anon`/`authenticated` 兩個具名角色** ⇒ **`REVOKE ... FROM PUBLIC` 收不到具名授權**。而這支是 `SECURITY DEFINER`、`anon` 是 storefront 印在瀏覽器裡的公開金鑰角色 ⇒ **任何人都能搜全公司客戶**。
🔴 **`:162` 的註解從第一版就寫對了**(「anon / authenticated 一律不得執行」)—— **錯的是它下一行 SQL 做不到它。**⇒ house `feedback_comment-states-intent-assertion-states-capability` 的正式站實例。
✅ **同日 `20260816030000` 沒中**:它 `:101` 明文寫著這個坑、`:104` 有 `REVOKE ... FROM anon, authenticated`,驗收區塊連**欄位層 ACL** 都查(`:217-223`)⇒ **知識在 repo 裡,只是沒進到那一支檔。**
⚠️ **我沒能獨立確認正式庫現在有沒有那支函式** —— `db dump` 要 Docker(未開)、psql 要連線字串(在 `.env*`、禁碰)。**已知的是**:①`migration list` 沒記它 ②把它從本地清單移走後它在遠端那欄**完全不出現** ③一檔一交易 ⇒ 應已回滾。**三條都不是「親眼看到那支函式不在」。**
🔴 **修法已寫(加 `REVOKE ALL ... FROM anon, authenticated`,抄 `030000` 的證明有效寫法),碰權限=鐵則 12,codex 對抗審查中、未 commit。**
⚠️ **順序漂移**:`010000` 會晚於 `030000/040000/050000` 才套進去,**版本號逆序**。後果未驗。
~~🔴 **等 Sean apply 的 migration 三支(A 窗 `~/pcm-void-readers`,未 push)**:`20260816030000` 客戶頁三欄 view / `20260816040000` `#518` DETAIL / `20260816050000` `#522` 貨品軸分母。⚠️ **`040000` 必須早於「後台開放給員工」**(apply 前 `P2C13` 只顯示通用錯誤)。🔴🔴 **`030000` 另有【順序約束】:必須先 apply 才可以推那一片** —— adapter **無條件**讀那支 view,而 admin production 追 `dev` ⇒ **推了而沒 apply ⇒ `/customers` 整頁 `42P01` 退成錯誤態**,那正是 08-07 那次 8 小時事故的形狀。~~

### Blocker · 收割真實模擬 / 彩排 / 順序敏感度 / `#537` 更正 / RLS 語意分岔 / 分母打架 —— 收割已於 2026-08-16 完成並推出,以下為過程紀錄

🔴 **收割:已做真實模擬(2026-08-16 06:43:19,基準 `dev=41a5fef6`,拋棄式 worktree、已移除零留痕)**。序 `products → print-docs → void-readers → customers`:**前二乾淨 / void-readers 衝 1 支(backlog)/ customers 衝 4 支**(`customers-table.tsx`、`order-detail.tsx`、backlog、`database.types.ts`)⇒ **`D-066` 的「2 次會衝、需人判 4 支」逐字命中**。⚠️⚠️ **「完整重疊面 26 支 ⇒ 22 支自動併、沒有人看過」是【錯的,已作廢】**(B 窗 `B-574` 與 A 窗獨立同時抓到):量法用了 `git diff dev..<branch>`,**它把「分支只是落後 dev」的檔一起算進去**(products 53→24、customers 49→32);**正解** `git diff --name-only "$(git merge-base dev <b>)".."$b"` ⇒ 🔴 **真值 11 支 = 4 真衝突 + 7 自動併**。**而那 7 支 E 窗與 D 窗已【全部看過】**(🔴 **「看過」不等於「判定成立」**:E 窗那一輪用的是**「hunk 區間不重疊」當安全證據,而那正是這類缺陷的【定義】** ⇒ **A/D/C 三個窗各推翻一支**(`order-status-axes` 兩份分母 / 守門 vs 新增違規 / 搜尋查表列表查 view);**E 窗其後用語意判準重跑【另外四支】**(`globals.css`/`shipping-doc`/`SupabaseCustomerAdapter.【test】.ts`/`identity/types.ts`)**,結論相同而理由全換;⚠️ 而【被推翻的那三支,E 窗沒有自己複驗】—— 它接受三窗的判定,兩批交集 = 0**)(`shipping-doc`/`globals.css`/`SupabaseCustomerAdapter.ts`+`.test.ts`/`shipment-section`/`order-status-axes`/`identity/types.ts`)⇒ **待審 0 支,不是 22 支**。🔴 **而錯數字曾被據以派工給三個窗,已叫停。** ⚠️ **A 窗另標一個仍未解的口徑問題**:**四個分支的 merge-base 不是同一顆**(`8efe9ca7`/`f56f29c7`/`f56f29c7`/`5e63de69`)⇒ **「重疊」怎麼算本身有歧義,而本段未寫明用哪個基準** —— 引用前先定義基準。🔴🔴 **而本更正曾在 2026-08-16 07:07 的主樹誤 reset 事故中【被吃掉一次】**:主視窗事後宣告「零損失」時用 `grep -c 'merge-base'` 驗證,**而該字串在本檔別處本來就有 3 處** ⇒ **那是「【提到】≠【做了】」的又一次,而這次它讓一個【真的損失】被判成零損失**。🔴🔴 **免費正向對照**:我故意用 `--theirs` 粗解那 4 支,結果 A 窗驗活清單的 **3 條回 0**(`從未下單`=0 需 2 / `admin_update_order_item_amount`=0 需 ≥1 / backlog `#517 #523 #524 #526` 全 0),而那三者**只住在 void-readers** ⇒ **「取對面」會靜默丟掉整排欄位而 git 零告警、無衝突標記殘留、檔案看起來完全正常** ⇒ **A/C 兩窗的驗活清單從「聽起來合理」升格為【已驗證有判別力】,收割完必跑**(`~/pcm-mailbox/A-69-STOP.md`、`C-MERGE-INTENT-2026-08-16.md`、彙整 `主-收割-真實模擬結果與驗活對照-20260816.md`)。⚠️ **保鮮期是分鐘**:跑模擬那 90 秒內 `products` 已從 `e44737c8` 動到 `27f6f878` ⇒ **收割當下必須重跑,不得讀該表**。

🔴🔴 **收割已抓到一支「兩邊都併成功而結果比任何一邊都糟」的檔**(A 窗發現、主視窗逐字複驗;它在那 **7 支自動併**裡(⚠️ 先前寫「22 支」為作廢的錯數,見上段),**不在** 4 支衝突裡):`apps/admin/src/lib/orders/order-status-axes.ts` —— void-readers `#522` 把 `goodsAxisOfLines` 分母改成 `quantity − cancelled`,customers `#514` 新增的 `goodsAxisProgressNote()` **分母還是舊的**(`sum + l.quantity`;五棵逐字量,該函式與該字面**只住在 customers**)。兩段行距很遠 ⇒ **git 安靜自動併**。併後一張「訂 5 取消 2 剩 3 已到貨」的單:**膠囊顯示「已到貨」✅,小字顯示「本單 5 件中已出貨 0 件」🔴** ⇒ **員工會去倉庫找 2 件不存在的東西**。⚠️ **比 `#522` 原病更難發現**:修前兩者一起錯、讀起來一致;修後並排矛盾而只有小字錯。**兩邊都沒打算要這個**(C 用 `l.quantity` 是當時慣例、A 改分母時不知道那支函式在別棵樹上)。**驗活** `grep -n "sum + l.quantity" apps/admin/src/lib/orders/order-status-axes.ts`(有命中=還沒跟上)。🔴 **修法不是改那一行數字,是讓小字與 `goodsAxisOfLines` 共用同一個 `need()`** ——「兩份各自算一次」才是病根;**跨兩條線 ⇒ 收割【之後】由一個人一起改,任何一邊都不要先動**。🔴🔴 **而 D 窗寫 plan(`D-074`)時挖到更前面的一件:`#522` 那個【已 commit 的修】現在零測試分得出它** —— 負向對照把 `need()` 分母改回 `quantity`(= 整個撤銷 `#522`)⇒ **42 passed / 0 failed、零紅**(⚠️ `D-074:257` 逐字:**8.0 的「13 格紅」與 8.3 的兩個「零紅」都量在 `-X theirs` 草率解出來的樹上** —— 主視窗先前把這條 hedge 掉了,B 窗 `B-574` F3 抓到);**而量具是活的**(同一行改成 `l.quantity + 999` ⇒ **13 格紅**)⇒ **那一行有 13 格覆蓋,沒有任何一格分得出 `quantity` 與 `quantity − cancelled`;覆蓋率會說它被測了,它沒有**。⇒ **本片第一個動作是【造出那兩格】,不是重構**;拆兩顆 commit(提升 `need` 到模組層=零行為改變、既有 42 格全綠即證據 / 讓 `total` 吃它=真行為改變、要新格才驗得了)。⚠️ **併起來的當下 `order-status-axes.ts:293` 那句 docstring(「本函式與 `goodsAxisOfLines` 都不扣已取消數量」)後半立刻變假、零機械訊號** ⇒ 同批改掉並拿舊字面跑 `scripts/literal-sweep.sh`。🔴 **D 窗答不出的一格**:`refund-wiring.test.tsx`(唯一引用 `goodsAxisProgressNote` 的測試)在 clone 裡因 `@testing-library/react` 解析失敗**跑不起來** ⇒ **上面「零紅」不涵蓋它,執行本片第一件事是在真環境跑它,不得沿用該數字**。📎 正式庫 `cancelled_quantity > 0` 目前 0 筆 ⇒ **降低急迫性、不降低正確性,且表示上線後才會第一次踩到**。

🔴🔴 **客戶搜尋與客戶列表【走不同的 RLS 語意】,而遮住它的是一個角色屬性、不是任何一行 code**(E 窗 `E-659` 實測 + 主視窗複核座標):搜尋 `admin_search_customers` = **`SECURITY DEFINER`**(繞過 RLS)/ 列表 `admin_customer_list_v` = **`security_invoker = true`**(套用 RLS,`…admin_customer_list_view.sql:73` 逐字)/ 底表 `customers` **`ENABLE ROW LEVEL SECURITY`**(`20260523034911_init_customers_and_subtables.sql:144`)。⚠️⚠️ **主視窗先前在此寫的「對底表加 RLS 後 `customers=1 / view=3` ⇒ 行集分岔」是【壓縮錯誤】,已作廢** —— E 窗 `E-659:33` 原始數據量的是**三個物件**:`customers=1` / **`v_def_off=3`(definer view)** / **`v_inv_on=1`(invoker view)**;🔴 **我方的 `admin_customer_list_v` 是 `security_invoker=true` ⇒ 對應的是 `v_inv_on=1` ⇒ 它與底表【一起縮小】,不分岔**。⚠️ **~~C 窗獨立複量確認(`customers=2`…)~~ —— 撤下:C 窗 2026-08-16 掃遍 `~/pcm-mailbox/C-*.md` 找不到那組數字的任何載體,唯一來源是一則 SendMessage 訊息,而訊息不是載體。**✅ **改掛有信可查的來源**:`E-659:31-34`(加 RLS 後 `customers=1` / `v_def_off=3` / **`v_inv_on=1`** ⇒ invoker view 與底表同數)+ `E-662`(`service_role` 無 `BYPASSRLS` ⇒ 表 0 / view 0 / **搜尋 RPC 3**,含 `BYPASSRLS` 正向對照 ⇒ 全 3)。🔴 **結論不變:`2/2/3`、`1/1/3`、`0/0/3` 是同一結構的三個實例,差別只在政策命中幾列。懸空的只有那組特定數字與誰量的。**📌 **立法:寫「X 窗獨立確認」進 STATUS 必須同時寫【是哪一封信】;沒有信號 ⇒ 來源是訊息 ⇒ 不算數。**。⇒ **「行集分岔」只發生在 definer view 上,而那不是我們的設定。** 🔴 **而真正分岔的是另一對(C 窗與 E 窗 `E-662` 兩邊都量到、方向一致)**:在**一個自建的、受 RLS 限制的 reader 角色**下 **view=2 / customers=2 / 而搜尋 RPC=3**(⚠️ **與下方 E 窗那組 `表0/view0/RPC3` 角色不同** —— 那組是 `service_role` 拿掉 `BYPASSRLS`;**兩組都對,因為不同角色命中不同政策**)(`SECURITY DEFINER` 繞過 RLS)⇒ **搜尋找得到 3 個 id、列表只列得出 2 個**。**⇒ 由此三件更新**:①**行為層(雙向 `EXCEPT` 比 view vs customers)對這一類【有盲區】**(兩邊一起縮 ⇒ 相等 ⇒ 通過)—— C 窗已在 A.8.3 加範圍限定「只涵蓋 view 定義層面,不涵蓋權限層」;②🔴 **目錄層是【唯一「資料無關」】看得到這件事的一層**(⚠️ 主視窗先前在此寫「唯一」是過度宣稱,E 窗用**它自己 `E-662` 的數據**打掉:**行為層換對物件也看得到** —— `RPC=3 / view=0`;**病不是「行為層看不到權限」,是 ⑥【比對的那一對物件選錯了】**(比 view vs 底表 ⇒ 兩邊一起縮 ⇒ 相等;**比 RPC vs view ⇒ 看得到**)。**而換對物件的行為層仍需要資料 ⇒ 空表時兩邊都 0 ⇒ 又瞎了** ⇒ **目錄層的不可取代性在【資料無關】這四個字上,不在「唯一」**);③ **目錄層要釘的不只 view 的 `security_invoker`,還有底表的 `relrowsecurity`(兩者都在 `pg_class`,零資料需求);而若哪天 RLS 是刻意要開的 ⇒ 必須【同時】改搜尋那一側,不能只開一邊**。⚠️ **今天沒爆的推測原因是 `service_role` 帶 `BYPASSRLS`,而那條【repo 內無法驗證】**(平台角色。⚠️ **2026-08-16 校正**:原寫「**全樹**零 `CREATE ROLE service_role`」是假的全稱句 —— 量法 `git grep -nE '全樹.{0,8}CREATE ROLE'` 當時回 **5 命中**,反例是**拋棄式測試環境自己造角色**(`docs/runbooks/throwaway-postgres-for-migration-verification.md:72` 逐字就有一行 `CREATE ROLE service_role NOLOGIN;`)。**真值=`supabase/migrations/` 底下零。**)⇒ **標未確認,不得當成安全保證**。**症狀若發生**=搜尋得到、點進去列表「共 0 位」而 chip 還寫著搜尋詞 ⇒ 員工得到「查無此人」的錯誤結論。🔴🔴 **而 E 窗 `E-662` 換問法後追到根因,比上述嚴重一級**:實測三發(基準 表3/view3/RPC3 · **`service_role` 無 `BYPASSRLS` ⇒ 表0/view0/而搜尋 RPC 照樣 3** · 有 `BYPASSRLS` ⇒ 全 3 = 正向對照)。**根因是結構性的**:`init_customers_and_subtables.sql:144-161` 四條政策逐字 = `select_own`/`update_own` **TO authenticated** + `insert_service_role`/`delete_service_role` **TO service_role** ⇒ 🔴 **`FOR SELECT TO service_role` 這條【不存在】** ⇒ **整個客戶後台的可見性靠一個角色屬性,而 repo 裡零行宣告或檢查它**。🔴🔴 **現實觸發不是「Supabase 改掉 service_role」,是【我們自己做一次安全強化】(把後台連線換成受限角色)——那是一個看起來嚴格更安全的改動,而它會讓客戶後台整個空白**;**症狀最壞**:列表整個空白 → **搜尋卻打得到人**(DEFINER 繞 RLS)→ 點進去又是空的 ⇒ **系統看起來活著而資料看起來不見了**。⚠️ **而 E 窗自陳它 `E-661` 那格【抓不到這一類】**(分家時 `security_invoker`/`relrowsecurity` **兩個值都沒變** ⇒ 兩條斷言全部安靜通過)⇒ **目錄層擋【物件側】不擋【角色側】**;角色側可從 `pg_roles.rolbypassrls` 構造(已量:`service_role=false` / bypass 版 `=true`)。**E 窗傾向補上那條缺的 `FOR SELECT TO service_role` 政策而非斷言平台屬性**(可見性改為依賴一條寫在 repo、看得到審得到的政策)⇒ **動 RLS = 鐵則 12②,已升 Sean `Q15`**。

🔴 **`#537` 兩處要更正(C 窗掃出一處、主視窗掃出另一處,兩處都在 D 窗那段實測補充裡)**:①**型別名 `UIProduct` 確實不存在,真名 `MockProduct`(`mock-products.ts:118`)** —— ⚠️ **而三個窗的量法【全部是壞的】,結論剛好都對**:C 窗 `git grep -lE '\bUIProduct\b' -- apps/storefront` ⇒ 0(🔴 **`\b` 在 `git grep -E` 上完全無效、靜默讓任何 pattern 回 0**;正向對照 `\bUIVariant\b`=0 而 `UIVariant`=14 支,而 `UIVariant` 確定存在)/ 主視窗 `git grep -l UIProduct -- apps/storefront` ⇒ 28 支(🔴 **子字串污染**:`toUIProduct` 85 次、`catalogRowToUIProduct` 17 次、`getFeaturedUIProduct` 3 次)/ D 窗自陳「我掃的是【檔】不是型別,措辭讓它聽起來比實際精確」。**可攜寫法** `git grep -oE '(^|[^A-Za-z0-9_])UIProduct([^A-Za-z0-9_]|$)' -- apps/storefront` ⇒ **7 處 / 4 支檔,而 7 處全在註解與 `it()` 標題裡**⚠️ **三條命令都必須帶 `-- apps/storefront`**(B 窗 `B-574` F2:主視窗原本漏了範圍限定 ⇒ 照原樣跑會得到 `69 支 / 241 / 30 / 8`,與宣稱值差 2.4–2.9 倍;**加上範圍後五個數字全部精確命中**⇒ **數字是對的,錯的是命令 —— 而代價是下一個人核不出來**)🔴 **第二層**:`STATUS.md` 自己現在含 `UIProduct` ⇒ **全 repo 重數會被【做出這個宣稱的那一段】污染**,而範圍限定同時解掉這一層。。🔴 **⇒「三個人得到同一個答案」不是交叉驗證;三個量具壞法不同而答案相同,那是運氣。**②🔴🔴 **經銷價 strip【已經有守門】,而 C/D 兩窗都報成「零守門」** —— `apps/storefront/src/lib/recommendations/rule-based-engine.test.ts:366-382` 逐字:`it('經銷價 strip:輸出 UIProduct 不含 priceByTier / 經銷金額')`,斷言 `expect(Object.keys(item?.product ?? {})).not.toContain('priceByTier')` **且** 把物件 `JSON.stringify` 後斷言不含 `38000`(store)/`36000`(premiumStore)。**主視窗是用字面 `priceByTier` 掃到的,不是用型別名** ⇒ **今晚第 N 次同一形狀:按識別字/型別名搜尋,會系統性漏掉【斷言行為與序列化結果】的那種測試,而那正是最好的一種。**

🔴🔴 **收割彩排(C 窗照【已落檔的意圖】真解一次,2026-08-16)抓到落檔意圖【兩處錯、一處根本不是選邊題】—— 而最重的那條沒有人提過**:`apps/admin/src/components/orders/order-detail.tsx` 🔴 **不是「取哪一邊」,取 void-readers(它把整段抽成 `<OrderSummaryCards detail={detail} />`,`:209`)會【安靜地】丟掉【兩條】Sean 拍板**(主視窗逐字複驗:抽出來的 `order-detail-summary-cards.tsx` 對 `customerEmailDisplay` 與 `GoodsAxisValue` **命中皆 0**):①`customers:390` **Email** = 08-16 拍板乙(已知)②🔴 `customers:413` **出貨狀態 `<GoodsAxisValue detail={detail} />`** = **08-15 拍板乙「本單 6 件中已訂 3 件」那行小字,亦即 `#514`(出貨狀態 13/13 恆假)的修** —— **這條【今晚無人提過】,而後果更重:員工看到「未訂貨」而不知已訂 3/6 ⇒ 重複下單、同一批貨訂兩次**。⚠️ **而「兩邊都要」照字面做 = 卡片畫兩次**。🔴 **正解要四個動作,第四個是設計決定**:①`order-detail.tsx` 取 HEAD ②新元件 Email 那行改呼叫 `customerEmailDisplay` ③新元件出貨狀態改用 `GoodsAxisValue` 🔴④**而 `GoodsAxisValue` 是 `order-detail.tsx` 裡的【區域、未 export】函式(`customers:84`,dev 與 void-readers 皆零命中=customers 新加)⇒ 取 void-readers 那版之後它整個消失 ⇒ 必須搬進新元件或抽成共用 —— 那是設計決定,不是解衝突**。**另兩處**:`database.types.ts` 取 void-readers ⇒ `admin_search_customers` 型別歸零(**會大聲**,但收割的人撞到一個不知道為什麼的紅;🔴**而那段是手動加的、migration 未 apply ⇒ 想「重 gen 一次就好」會把它弄不見且以為做對了**);`customers-table.tsx` 取 void-readers ⇒ **安靜**丟掉 `customerEmailDisplay`(0 vs 2),正解是兩邊都要。✅ **backlog 取聯集正確**,四輪各有自我檢查算式(`505+502−497=510` … `517+504−497=524`,實得逐輪相符、殘留衝突標記全程 0)。⚠️ **彩排的三個誠實邊界**:**typecheck 跑不起來**(拋棄式樹無 `node_modules`)⇒ C 窗自陳「我的解法有一個洞(補了呼叫沒補 import)——**那正是 typecheck 會抓的**;我寫『會紅』不是『紅了』,因為我沒跑」/ **A 窗 `A-75` 那【14】條並未逐條跑**(⚠️ 主視窗先前寫 15,`C-HARVEST:111` 原文為 14;而 A 窗其後補到 15 條 ⇒ 兩個數各自對應不同時刻,引用要帶時刻)(C 窗跑的是自己意圖信裡那四條,兩者不是同一組)/ `GoodsAxisValue` 落點未決。📎 零留痕已驗(彩排樹 remove+prune、五棵真樹 tip 全未動);中途 `git commit --no-verify` **被 repo hook 擋下而未繞道** ⇒ **該 hook 在拋棄式樹上照樣有效**。

🔴 **收割【順序】已量(D 窗 `D-082`,四個順序各跑一次)——結論:順序不改變衝突集合,但【現行順序仍有實質優勢】**。①現行 ②完全反過來 ③衝的在前 ④衝的在後 ⇒ **衝突【事件】總數皆 7 次、涉及 4 支檔,而且結構相同**(⚠️ **這個 `7` 與上面「7 支自動併」的 `7` 【意思相反】** —— 上面那個是**不衝突**的那一批;這裡是**衝突事件次數**:`backlog` 每步各一次共 4 次 + 三支 code 檔各一次)(`backlog` 每一步都衝=常數;`customers-table`/`order-detail`/`database.types` **永遠只衝一次,且固定在 void-readers 與 customers【後上的那一個】那一步**)⇒ **不用為了少幾個衝突去糾結順序**。🔴🔴 **而真正的差別在【哪一側是哪一邊】**:`order-detail.tsx` 的衝突區塊在順序①與④之下**逐字相同 72 行,唯一差異是 HEAD 側與 theirs 側整個對調**,而兩側**行數極不對稱(1 行的 `<OrderSummaryCards …>` vs 68 行 inline 卡片)** ⇒ **順序①:正解「取 HEAD」= 對的;順序④:「取 HEAD」會取到 68 行 inline 卡片 = 錯的,而它同時是【看起來比較安全】的那一個**。⚠️ **口徑**:D 窗證的是位置對調與行數不對稱,**「人比較容易解錯哪一個」是推論不是實驗**。🔴 **而 `A-75` 有【一條】綁在順序上,且是承重的那條**:**`~/pcm-mailbox/C-A75-RUN-2026-08-16.md:22`** 逐字「① 取 HEAD(void-readers 的重構)」。🔴 **(不是 `A-75-STOP.md` —— 那支對 `HEAD|順序|先併|第一步` 零命中,別去那裡找;A 窗 2026-08-16 10:5x 實測。)**⚠️ **`A-75` 的檢查條數正確值 = 15**(`grep -c` 回 17,含 2 個格式說明裡的同字,逐行看才分得出)——**換順序不會讓它報錯,它會【靜默地指向另一邊】** ⇒ **若改順序必須同批改那句**。⚠️ **D 窗自標兩條承重限制**:只跑 4 種(共 24 種排列)未窮舉、每步用 `-X theirs` 草率解 ⇒ **量的是「衝突發生在哪一步/哪幾支/幾塊」,不是「解完對不對」**。📎 **而它把做法從 `git worktree add` 改成 `git clone --local`,理由值得沿用:`worktree add` 會在【五棵樹共用的 `.git`】裡註冊 = 對共用基礎設施的寫入**(今晚已誤入主樹一次)。
