# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
.pnpm-store/
.pnpm-debug.log*
pnpm-debug.log*
npm-debug.log*

# Next.js
.next/
out/
# Next 自動生成、檔頭明寫「should not be edited」、每次 build 重生 → 不入版控(backlog #169)
apps/storefront/next-env.d.ts
apps/admin/next-env.d.ts

# Build outputs
dist/
build/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
# .claude/ 採 white-list pattern:預設忽略、explicit 放行共享檔(Stage 3 v4 工作流升級;2026-05-23 加 skills/)
.claude/*
!.claude/agents/
!.claude/settings.json
!.claude/skills/
.claude/scratch/

# Turborepo
.turbo/

# Logs
logs/
*.log

# Test coverage
coverage/

# Playwright E2E 產物(test report / trace / 截圖、本機 + CI 重生、不入 git;測試基建 T-1)
**/test-results/
**/playwright-report/

# Playwright MCP 暫存產出(本機真機/瀏覽器驗證截圖、不入 git)
.playwright-mcp/

# Storybook
storybook-static/

# OS
Thumbs.db

# Design reference uploads(submodule 內、submodule 自己也已加 .gitignore)
design-reference/uploads/

# Medusa
apps/medusa/.medusa/
apps/medusa/uploads/

# Supabase CLI cache(對齊 M-1-03-main-a2-1 Q2-temp=A1 拍板)
supabase/.temp/

# Spikes(腳本字面自稱「丟棄式 spike script、不 commit、不入 git、留檔給未來人類重跑」、規矩固化、未來 spike 統一不入 git)
packages/adapters/scripts/spikes/

# graphify 知識地圖工具產物(.graphifyignore 本身 track、見 repo 根;產物本機重建、不入 git)
graphify-out/

# graphify 本機 git hook(graphify hook install 產;每人自裝、不強加團隊;.husky/pre-* 才是共用)
.husky/post-commit
.husky/post-checkout

# impeccable design-hook 本機快取(session 工具產物、不入 git)
.impeccable/
.vercel
# 廣義擋所有 .env 變體(補上方 Env 段漏掉的 .env.production / .env.backup / *.bak 等)
.env*
# 例外必須放在廣義規則之後、否則會被上面那行蓋掉(範例檔無真密碼、需入 git 供對照)
!.env.example
!**/.env.example
# Next 16.3.0 起 next dev 自動產 app 層 agent 檔(內容=@import,會被 Claude Code 自動載入=外來規則注入)。
# Sean 2026-08-12 拍 Q-D11=A:先擋;治本=next.config agentRules:false(#434,隨下一個動 next.config 的片)。
# 只擋 apps/ 層,repo 根目錄的 CLAUDE.md/AGENTS.md 是我們自己的制度檔、不受影響。
apps/*/AGENTS.md
apps/*/CLAUDE.md

# Sean 2026-08-14 家目錄整理:PCM 網站截圖存查(本機參考、不入 git)
.screenshots/

# Python 語法檢查(py_compile)的產物 —— 2026-08-18 曾經被 commit 進去一次
__pycache__/
*.pyc
