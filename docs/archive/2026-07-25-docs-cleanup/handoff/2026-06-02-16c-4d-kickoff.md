# M-1-16c-4d 交接 — 選變體換圖 + selectedVariant 狀態提升

> 產出:2026-06-02 / pcm-website-v2 **16c-4c 審查 session** 寫給新開的 16c-4d 執行 + 審查 session。
> 對象:① 16c-4d 執行 session(自驅實作)② 平行 fresh-context 審查 session。照 CLAUDE.md「Claude Code 自驅 slice SOP」+「寫審分離」。
> 接續 `docs/handoff/2026-06-01-16c-4c-kickoff.md`(§2 原列 16c-4d scope)。

---

## 0. 現況(16c-1~4c 全完成 + 已 push)

- `origin/dev = 0fa78cf`、本地 = origin、**未推 0**、工作樹乾淨。CI(`check`)已對 `0fa78cf` 跑。
- 已上線的 16c 系列:

| 片 | commit | 內容 |
|---|---|---|
| 16c-3 | `81b5480` | 詳情頁接真變體 + 選變體換價(ProductInfo 資料驅動選擇器) |
| 16c-4a | `2b7d270` | 料號 pd-sku 真 sku + 副標 pd-sub 真 subtitle |
| 16c-4b | `fe5b059` | 規格表 productCode + 產地泰國 |
| 16c-4c kickoff | `5797842` | 交接 docs |
| 16c-4c | `0fa78cf` | 詳情頁 JSON-LD + SEO metadata(已潔淨、審查 PASS) |

- 16c-4c 審查全 PASS(三綠 / 完整 test 506 / 經銷雙 grep 零洩漏 / curl 實證 / codex k2 PASS)。

---

## 1. 16c-4d scope(修 16c-3 兩個已知限制)

核心 = **selectedVariant 狀態提升 ProductPage**,讓 gallery / mobile buybar 共用使用者選的變體:

1. **ProductPage 持有 selectedVariant 狀態**(建議存 `selectedSku`、從 `product.variants` derive `currentVariant`、避免 stale object;codex 16c-3 k2 consider)。
2. **ProductInfo 改受控**:收 `selectedVariant` + `onSelectVariant` props、移除 local `useState`(現 L94-95)+ reset `useEffect`(現 L102);`selectSpec`(L110)snap 後改 call `onSelectVariant`。`pd-sku` JSX 不變(`{selectedVariant?.sku ?? slug}`、只改來源)。
3. **ProductGallery 吃 `currentVariant.images`**(現 L70-79 吃 `product.images`);變體 images 空 → fallback 商品 images(**Q3=C**;~68% 變體無專屬圖、fallback 常見)。已有 activeImg reset effect。
4. **mobile buybar**(ProductPage L67-77 `addToCart`)改用**使用者選中**的 `selectedVariant.sku`(現 L77 用 `product.variants?.[0]?.sku` 預設變體)。無變體 → 退 `product.color`。

---

## 2. ⚠️ OpenDesign 視覺 gating(動手前先跟 Sean 確認)

Sean 排序拍板 **A**:16c-4d 在 OpenDesign 商品頁視覺定案後做。動手前確認:

- OD「Website V2」商品頁視覺是否已定案 + `design-reference` submodule 是否已同步?
- **關鍵判斷:OD 重設計有沒有動到 gallery / 變體選擇器 / mobile buybar 的結構?**
  - 若 OD 只動「內文塊 / swatch / 文案」(generic 通用塊)→ 不碰 16c-4d 結構 → 可平行做。
  - 若 OD 重排 gallery / buybar → 先 submodule update 對齊新結構,再做 16c-4d(否則白工)。

→ 這是 16c-4d kickoff 的 Sean 拍板點(prose multi-select)。視覺 polish 一律 Sean 自理、執行/審查不主動動視覺(memory `feedback_sean-owns-visual-design`)。

---

## 3. Sean 拍板彙整(16c 全程、必遵)

- **D1=A**:換圖 + 狀態提升排成 16c-4d(本片)。
- **變體圖=C**:選變體 → 換該變體 images;空 → fallback 商品 images。
- **Q1=A** 文字鈕 / **Q2=A** 中文規格標籤 / **Q3=A** 資料驅動第三排 / **Q4=A** 不顯庫存(沿用)。
- 詳情頁釘 general(tier-aware 延 M-2-08)。

---

## 4. 🔴 經銷價(16c-4d 非 pricing、仍守)

- 16c-4d 動的是 **sku / image**(皆公開)、**不碰價格** → 估**不跑 codex**(三綠 + code-reviewer 即可)。
- 三層防護仍守:`UIVariant` 型別無 `priceByTier`(編譯期擋);commit 前順手 grep `.next/static` 確認 `price_store`/`price_by_tier`/`priceByTier`/`cost`/`shopee` = 0(換圖/狀態提升不該新增洩漏面)。
- `selectedVariant` 物件只帶 `{sku,spec,price:number,images}`(general)、提升到 ProductPage 仍是這結構、無經銷欄。

---

## 5. 關鍵檔 + 現況(已核對真實 code、2026-06-02)

- `apps/storefront/src/components/ProductInfo.tsx`(`'use client'`;L94-95 `selectedVariant` local state、L102 reset effect、L110 `selectSpec`、L133 `displayPrice`、L142/L151 用 `selectedVariant?.sku`)→ 改受控。
- `apps/storefront/src/components/ProductGallery.tsx`(L70-79 gallery `useMemo` 吃 `product.images`、deps `[product.images,product.image,product.id]`)→ 改吃 `currentVariant.images` + fallback。
- `apps/storefront/src/components/ProductPage.tsx`(L67-77 `addToCart` 用 `variants[0]` 預設、L312 `pd-mobile-buybar`)→ 持有 selectedVariant 狀態 + buybar 用選中 sku。
- 型別:`apps/storefront/src/data/mock-products.ts` `UIVariant {sku,spec,price,images}`。
- 範例 handle:`rpm-dcc01`(9 變體、部分變體有專屬圖)。

---

## 6. 流程(執行 session)

讀 STATUS + 本 handoff + design 真權威(鐵則 1 grep)→ 自規劃 → **OD gating 先問 Sean(§2)** → 實作(前後台同步、CSS+TSX 同 slice、檔案 <400、React 19 hooks 受控改造別違 `rules-of-hooks`/`exhaustive-deps`)→ 三綠 → 完整 `pnpm test` → code-reviewer → 精準 git add + commit(字面 vs 事實一致)+ STATUS 7 欄自更 + busboy-end → **不 push**。

- ⚠️ 本片動 **design-mirrored 元件**(ProductInfo/ProductGallery/ProductPage)→ **`design-storefront-manifest.yaml` 對應條目必同步**(last_modified PENDING→可達祖先 hash;非 16c-4c 純 lib 那種不動 manifest)。
- ⚠️ 順手更新 3 個 smoke test:`ProductInfo.test.tsx` / `ProductGallery.test.tsx` / `ProductPage.test.tsx`(變體換圖 + 狀態提升 + buybar 用選中 sku 的斷言)。

---

## 7. 審查 session baseline(fresh-context 重審每片 commit)

- 三綠重跑 + **完整 `pnpm test`(非子集**、memory `feedback_run-full-vitest…`:改共用元件後跨檔整合測試會 cross-effect 紅)。
- 字面 vs 事實(commit body vs diff)。
- 🔴 經銷防護:確認狀態提升 / 換圖沒把 `priceByTier` 帶進 client、`.next/static` grep 0(雖非 pricing 仍掃)。
- **manifest 同步驗**(本片有動 design 元件、必查)。
- smoke test 真的更新到。
- STATUS 7 欄 + 表頂 hash 可達(`git merge-base --is-ancestor`、避 orphan;memory `project_status-top-hash-off-by-one`)。
- 肉眼驗(Sean):桌機+手機,進 RPM 詳情頁 → 選不同紋路/表面 → 圖跟著換(該變體專屬圖、空則商品圖)、手機購買列加購用的是選中變體 sku。

---

## 8. 16c 整組收尾(16c-4d 後跑、最後一片 code)

- `/pcm-roadmap`(更新 `docs/progress-roadmap.html`)。
- `/graphify --update`(知識圖譜增量、16c 整組動程式碼後一次跑;前幾片延後未跑)。
- 兩者 STATUS「下一步」仍列、屬待辦。

---

## 9. backlog / 已知(非本片 scope)

- **#203** save-variant 寫路徑(前台只讀、import 腳本直寫)。
- **#205** featured 通用機制 / **#206** ISR vs JSON-LD price / **#207** rendered-HTML 自動化 e2e / **#208** schema-dts。
- proper variantSku cart key(現 sku 走 color 欄 discriminator、不改 CartContext 契約)。
- **16d** 每週重抓同步(留)。/ **#162** 產地+保固殘差。
- g-7 wallet HOLD **#202** 不變。

— END —
