# OP7 · 部分取消的待退款函式 —— plan(不寫 SQL)

> 錨 `⟦b4-PARTCANCEL1⟧` · 線 `-account` 2026-09-05 · 主視窗派
> 🔴 **本檔不含任何 SQL**,也不動任何一行碼。它要回答的是「該不該多一支函式、接在哪」。
> ⚠️ **每個事實都帶 `檔:行`。本檔所有座標讀的是 `origin/dev`(我的工作樹落後 60 顆時量的)。**

---

## §0 先答主視窗點名的那一題:**是不是就該接在 `pcm_pending_refund_amounts` 上?**

### ✅ 答案:**是。而且不只是「接在上面」—— 那支函式【本來就是 Q13,只是把一個項寫死成 0】。**

Sean 的 Q13(逐字原話抄在 `supabase/migrations/20260810100000_m4b_e10_op1_order_payments_m.sql:25-26`):

> 退款額 = `max(0, 已收未退總額 − 取消後訂單剩餘應收總額)`

而 `pcm_pending_refund_amounts(uuid)`(`supabase/migrations/20260902030000_m4b_crossrail_pending_refund_net.sql:64`,
`RETURNS TABLE (rail text, amount bigint)`)今天算的是(同檔 `:76-92`):

- 逐軌 `SUM(order_payments.amount) − SUM(order_manual_refunds.refund_amount WHERE voided_at IS NULL)`
- 只留 `net > 0` 的軌(`:89`),再用前綴和差分把 `total` 分配下去

⇒ 🎯 **`total` = Σ 逐軌淨額 = 「已收未退總額」。**
⇒ ⇒ 🔴 **它就是 Q13 在「剩餘應收 = 0」時的樣子。** 而**整單取消時剩餘應收確實是 0**,所以它一直是對的。

### 📌 ⇒ 結論:**不要寫第二支算金額的函式。**

寫第二支的代價不是多花時間,是**兩份金額算法會分岔而沒有東西會叫** ——
而那正是這一族已經記過的病:同一支檔 `20260902030000:87` 逐字承認
`('bank_transfer'),('cash')` 是 `order_payments.rail` 值域的**手抄副本**,且**已經有兩份**要同步。

⇒ **OP7 = 把 `pcm_pending_refund_amounts` 裡那個隱含的 0 換成一個算得出來的值**,不是新開一支。

---

## §1 而在寫簽名之前,我要**收回我自己昨天寫上板的一句話**

我在 `⟦b4-PARTCANCEL1⟧` 上寫了:

> 「Q13 那個公式讓三個攤提問題(按品項攤?按實收比例?運費算不算?)整個消失 —— 那三問是問錯了層。」

🔴 **那句話【只對了三分之二】。**

- ✅ **「按品項攤」「按實收比例」確實消失了** —— Q13 不問錢該算在哪一件商品上,它只問兩個總額。
- ⛔ **而「運費算不算」【沒有消失】,它換了位置** —— 它不再是「怎麼分退款」,
  它變成 **「什麼叫【取消後訂單剩餘應收總額】」** 的一部分。

⚠️ **⇒ 這一格要端 Sean,而我上一則說「不用問」是錯的。** 理由在 §2。

📌 **而這個錯的形狀值得記**:我拿一個**正確的公式**,推翻了一組**問錯層的問題**,
然後**連帶把那組問題裡唯一一個問對層的也一起丟掉了**。
⇒ 🛑 **推翻一個清單時,要逐條問「這一條也一起倒了嗎」,不是整包倒。**

---

## §2 `剩餘應收` 算不出來 —— 而**成因是量到的**

🔴 **部分取消【不會改 `orders.total`】。** 實查 `20260805100000_m4b_e10_a8a2_partial_cancel.sql`:
全檔只有一處 `UPDATE public.orders`(`:463`),而它只在**全部品項都被取消**時
(`v_closed`,判定式在 `:456-462`)寫 `cancelled_at` / `cancelled_reason` / `updated_at` —— **一個金額欄都沒碰**。

🟢 **正對照**:`20260905060000_m4b_stuck_bank_orders_health.sql:68` 逐字「有退款的單 `total` 不會變」⇒ 同一個事實,另一個作者。

⇒ **所以 `剩餘應收` 沒有欄位可讀,必須算。** 而算它會撞到 `orders` 的金額不變式
(`20260604120000_m3_s2a_orders_order_items.sql:26` 的 DDL CHECK 逐字):

```
total = subtotal + shipping_fee - discount_total
```

取消掉幾件之後:
- `subtotal` 那一項**算得出來** —— `order_items` 減掉 `order_cancellation_items.cancelled_quantity`
  (表在 `20260730130000_m4b_e10_a7_order_cancellations.sql:206-224`,欄位 `order_item_id` + `cancelled_quantity`)
- 🔴 **`shipping_fee` 與 `discount_total` 算不出來** —— 它們**不是品項的屬性**,沒有任何規則說取消之後它們變多少。

### 🛑 而這裡有一個**具體而且會發生**的情境,不是理論

運費規則(`20260604120000:16` 逐字):**滿 NT$5,000 免運,未滿 flat NT$100**。
⇒ 客人買 5,200 免運;取消掉 300 那件之後剩 4,900 ⇒ **本來就該收 100 運費了**。
⇒ 🔴 **「剩餘應收」到底是 4,900(照舊免運)還是 5,000(補收運費)?**
⇒ ⇒ **這兩個數會讓退款差 100 元,而沒有任何一份文件答得出來。**

### ✅ Sean 2026-09-05 拍了 —— **而他選了我推薦的【反面】**

```
Q-部分取消運費 ⇒ 他答:乙
乙 = 照規則補收運費(5,200 免運、取消 300 剩 4,900 ⇒ 退 200)
```
⛔ ~~**推薦 甲。** 代價一句:甲 我們少收 100 運費,而**乙 要跟一個正在被退錢的客人多要 100 元**
—— 那通電話的成本遠高於 100。~~

🔴 **舊推薦加刪除線留著,不改寫成「我本來就這樣想」** —— 兩個理由:
① 下一個讀這份 plan 的人要看得到**這一題被推翻過**,否則他會以為甲從來沒被考慮;
② 我那句代價**今天仍然成立** —— 只是 Sean 判定「照規則」比「省一通電話」重要。
📌 **那是他的生意判斷,不是我算錯了。** 而把它擦掉會讓下一個人以為這裡沒有取捨。

---

## §2b 拍乙之後,`剩餘應收` 這個式子長什麼樣

> 🔴🔴 **本節被 codex 關卡 1(2026-09-05)判 FAIL,12 條 must-fix。**
> **下面是折完的版本;而【被推翻的第一版逐字留在 §2c】** —— 那一版看起來完全合理,
> 而它會**少退客人 100 元**。⇒ 📌 **一個算式的錯不會在 diff 上看起來像錯。**

### 運費規則的權威 —— 而它**只涵蓋顧客站**

`20260604130000_m3_s2b1_create_order_rpc.sql:222-227` 逐字:
```
store → 0(自取免運)
home  → v_subtotal >= 5000 ? 0 : 100
```
🔴🔴 **而那【不是所有訂單的運費權威】**(codex must-fix,我實查證實):
**手動建單的運費是員工自己打的** —— `admin_create_manual_order` 收 `p_shipping_fee integer`,
只驗 `IS NULL OR < 0`(`20260904251500:250`)⇒ **`store` 的手動單也可能收費, 而金額是任意的。**
⇒ 🛑 **所以「拿剩下的品項重跑那條規則」對手動單會【算出一個與原單無關的數字】。**
⇒ ✅ **修法**:`pcm_shipping_fee_for` **只用在 `order_source = 'web'` 的單**;
   `manual_*` 的單**沿用原單的 `shipping_fee`, 不重算**。
   ⚠️ 而那本身是一個**口徑決定** —— 見下方要端 Sean 的第二題。

### 式子(折完版)

```
剩餘件數 = Σ(oi.quantity − 累計取消量)                    ← 🔴 不是「未取消品項的 line_total」
剩餘 subtotal = Σ(oi.unit_price × (oi.quantity − 累計取消量))
剩餘 shipping =
    剩餘件數 = 0                      → 0            ← 🔴🔴 全部取消要【特判】
    order_source <> 'web'             → 原單 shipping_fee(不重算)
    shipping_method = 'store'         → 0
    否則                               → 剩餘 subtotal >= 5000 ? 0 : 100
剩餘應收 = 剩餘 subtotal + 剩餘 shipping − discount_total
退款額   = max(0, 已收未退總額 − 剩餘應收)                 ← Q13
```

### 🔴 三個被 codex 抓到的洞,逐條寫下來(不寫進 §2c 就會再犯)

**① 全部取消時,式子會【少退 100】**
剩餘 subtotal = 0 ⇒ `0 >= 5000` 為假 ⇒ 重跑規則得 **運費 100** ⇒ 剩餘應收 = 100
⇒ 🛑 **整單取消卻少退客人 100 元。**
⇒ ✅ **特判「剩餘件數 = 0 ⇒ 剩餘應收 0」**,而**不是**靠 `max(0, …)` 蓋過去。
📌 **這個錯的形狀值得記**:規則本身沒錯、套用方式也沒錯,錯在**邊界那個世界不該套用它**。

**② 「Σ 未取消品項的 line_total」是錯的受詞**
同品項買 3 取消 2 ⇒ 那一列**還在**,而 `line_total` 是**整列 3 件的原價**
⇒ 用它會把已取消的 2 件也算進剩餘應收 ⇒ **少退。**
⇒ ✅ 必須是 `unit_price × (quantity − 累計取消量)`。

**③ 🔴🔴 不只會等於 0 —— 客人可能【反欠我們錢】**
原單 5,050(免運)、取消 51 ⇒ 剩餘 4,999 **+ 補收運費 100** = 應收 5,099
⇒ 已收 5,050 − 5,099 = **−49** ⇒ `max(0, …)` 讓它變 0
⇒ 🛑 **那不是「不用退」,那是【客人欠我們 49 元】而系統把它靜靜抹掉。**
⇒ 📮 **這一格要 Sean 拍** —— 見下方第一題。**本 plan 不替他決定。**

### ✅ 拍乙之後新長出來的兩題 —— **Sean 2026-09-05 都拍了**

**Q-取消後反而欠錢 ⇒ 甲**(逐字:「就算了,不跟他要」)
```
⛔ ~~A: 甲 | 乙 / 乙 = 擋下來, 不讓員工這樣取消~~
✅ 甲 = 系統記 0, 不開待退款也不開應收, **而且不擋這個取消動作**
```
⇒ 🔴 **所以 `max(0, …)` 那個行為是【拍板的】, 不是我為了省事留的。**
   ⚠️ **而它的代價要留在碼裡**:那筆錢**真的沒收到**, 而系統**不會留下任何一筆紀錄**說它曾經存在
   ⇒ 📌 **對帳的人事後看不出來這裡少了 49 元。** 這不是缺陷, 是拍板的代價 —— 但要寫在函式註解裡。
   🔵 **⇒ §6 測試表 3i 那格的期望值現在寫得出來了:退款額 = 0, 而且【取消要成功】。**

**Q-手動單的運費 ⇒ 甲**(逐字:沿用員工打的數字, 不重算)
```
✅ 甲 = `order_source <> 'web'` ⇒ 剩餘 shipping = **原單 `shipping_fee`**, 不跑那條規則
```
⇒ 🔵 這與 §2b 我原本寫的修法**一致**, 而現在它是**拍板**不是我的判斷 ⇒ 註解要改成引他的話。
⚠️ **而甲 的代價**:手動單的運費**不會隨部分取消縮水** —— 客人取消到剩一件, 運費照原本那個數字算
⇒ 那可能對客人不利。**Sean 知道, 他選了「員工打的數字有他的理由」。**

### 🔴 還有兩個【我答不出來】的,不猜

**④ 用「當下規則」重算 ⇒ 舊單會隨規則改版而變價**
今天門檻是 5,000 / 100。**若哪天改了**,一張半年前的舊單被部分取消時會用**新規則**算
⇒ 退款額與客人當初的認知不同。
⇒ 🛑 **正解是把規則版本快照進訂單**,而那是另一片、而且要先拍板。
⇒ ⚠️ **本 plan 不含它** —— 而**不寫下來的話,下一個人會以為這裡沒有這個問題。**

**⑤ `discount_total` 我先前寫「Phase 1 恆為 0」—— 那句今天【不成立】**
⛔ ~~今天 Phase 1 它恆為 0(`20260604130000:228` 逐字)~~
🔬 實查:**券系統已經有表了**(`20260829150000_m4b_coupon_p1_tables.sql` 等 3 支),
而 `20260901021000_m4b_coupon_p3b_create_order_redeem.sql` 逐字「帶券碼時本函式呼 `redeem_coupon`」
⇒ **那句是引一支【舊 migration 的註解】當現況** —— 而它已經被後面幾代推翻。
⇒ 📌 **我引的是一句當時為真的話, 而它的日期比我的結論早三個月。**
⇒ ✅ 式子裡照 `orders.discount_total` **實際那一欄**走,**不假設它是 0**;
   而「部分取消之後折扣要不要跟著縮」是**另一個沒人答過的口徑題**,**本 plan 明列為未解**。

🔵 **而 codex 有一條我實查【不成立】,照實寫**:它說「現行 schema 是
`total = subtotal + shipping − discount + tax_total`,公式漏掉稅額」。
🔬 實查 DDL CHECK 逐字仍是 `total = subtotal + shipping_fee - discount_total`
(`20260604120000:112`,`orders_total_balances`);`tax_total` 有**自己的**約束
(`20260828100000:175` `orders_tax_total_...`),**沒有進那條不變式**。
⇒ **那一條不採納, 而理由寫在這裡** —— 不是忽略。
⚠️ **但「部分取消之後 tax_total 要不要跟著縮」仍然是未解的**(§9)。

---

## §2c 【被推翻的第一版】—— 逐字留著,不刪

> 🔴 **為什麼留**:它看起來完全合理,而它會**少退客人 100 元**(§2b ①)。
> 下一個人若自己推導,很可能推出同一個版本 ⇒ **留著這一段比刪掉它有用。**

```
⛔ 剩餘 subtotal = Σ(未取消品項的 line_total)
⛔ 剩餘 shipping = shipping_method = 'store' ? 0
⛔                                : (剩餘 subtotal >= 5000 ? 0 : 100)
⛔ 剩餘應收      = 剩餘 subtotal + 剩餘 shipping − discount_total
```
**它錯在四個地方**:①全部取消時算出 100 運費 ②`line_total` 是整列原價
③沒有處理手動單 ④`discount_total` 我當時假設是 0。

---

## §3 簽名(等 §2 答完才落地)

```
public.pcm_shipping_fee_for(p_subtotal bigint, p_method text) RETURNS integer  -- 🔴 先抽這支
public.pcm_order_remaining_due(p_order_id uuid) RETURNS bigint                -- 新, §2b 的式子住這裡
public.pcm_pending_refund_amounts(p_order_id uuid)                    -- 既有, 改它不新開
    RETURNS TABLE (rail text, amount bigint)
```

- 🔴🔴 **`pcm_shipping_fee_for` 要【先落地並讓 `create_order` 改成呼叫它】, 再做 OP7。**
  ⇒ 那一步動的是**已上線的建單路徑** ⇒ **獨立一片、獨立審、獨立 rollback**。
  🛑 **順手一起做 = 一顆 commit 同時動建單與退款兩條錢的路** —— 出事時分不出是哪一半。
  ⚠️ 而**不抽它**的代價是全 repo 第二份運費規則, 而它們分岔時**沒有東西會叫**(§2b ①)。
- 🔴🔴 **兩支新函式的 ACL 與 `search_path` 要在 plan 裡就定死**(codex must-fix):
  · `SECURITY DEFINER` + **`SET search_path = ''`**(不是 `public, pg_temp`)+ 每個物件完整限定
  · **三道 REVOKE**(`PUBLIC` / `anon` / `authenticated`)+ 只 `GRANT EXECUTE TO service_role`
  · **制式收權斷言清單**(`DO $grant_assert$ … v_functions text[] := ARRAY[…]`)—— 它防的是「忘記列」
  🔴 **`pcm_order_remaining_due(uuid)` 會吐一張單的財務數字** ⇒ 它與寫入函式**同一級**, 不是唯讀就寬鬆。
  🔵 `pcm_shipping_fee_for(bigint, text)` **不碰任何表**(純算式)⇒ 可以是 `IMMUTABLE` + 不需要 DEFINER,
     而 **ACL 照樣要收** —— 它洩漏的是**運費規則**, 而那不是秘密, 但一致性比個案判斷值錢。
- 🔴 **`pcm_pending_refund_amounts` 用 `CREATE OR REPLACE` 改**(不新開一支):
  它今天有三個呼叫端要繼續動 —— `pcm_pending_refund_open_for` 兩處
  (`20260905070000_m4b_pending_refund_on_late_payment.sql:204-205` 的 WARNING 判定 與 `:211` 的 INSERT ... SELECT)、
  以及本線 2026-09-05 新建的 TS 讀端 `apps/admin/src/lib/payment/pending-refund-repository.ts:56`。
- 🛑 **而 `CREATE OR REPLACE` 會靜靜蓋掉撞名**,`20260902030000:60-63` 逐字警告過這一格 ⇒
  改它的那支 migration 要**自己帶一道「改之前的定義長這樣」的斷言**,不能只靠 REVOKE 後置檢查。
- ⚠️ **ACL**:`CREATE OR REPLACE` 保留既有 ACL(三道 REVOKE + 只 GRANT `service_role`,`20260902030000:197-200`)
  ⇒ **不要重打一份 GRANT**,重打會讓下一個人以為那是新授權。

---

## §4 讀哪些表(全部,不省)

| 表 | 用來算什麼 | 座標 |
|---|---|---|
| `order_payments` | 已收(逐軌) | `20260810100000:189` 的 `CHECK (rail IN ('card','bank_transfer','cash'))` |
| `order_manual_refunds` | 已退(逐軌,`voided_at IS NULL`) | `20260820010000_m4b_manual_refunds.sql:163` `rail` 欄 |
| `order_items` | 原始應收的品項面 | `20260604120000:146` `quantity > 0` |
| `order_cancellation_items` | 取消掉多少 | `20260730130000:206-224` |
| `orders` | `shipping_fee` / `discount_total` / `total` | `20260604120000:26` 不變式 |

🔴 **`order_pending_refunds` 不在這張表裡** —— 它是**產物**不是輸入。
把它讀進來算金額會造出一個自我餵食的迴圈(這一輪的輸出變成下一輪的輸入)。

---

## §5 接線:呼叫端在哪、以及**為什麼不能只加一個 trigger**

### 今天的接線
- 部分取消的 UI:`apps/admin/src/components/orders/cancel-order-forms.tsx:228` `PartialCancelForm`
  (勾選框值 = `<order_item_id>:<數量>`,`maxCancellable > 1` 時旁邊有數量覆寫欄)
- 走的 RPC:`admin_cancel_order(p_order_id, p_idempotency_key, p_actor, p_reason_code, p_reason_detail, p_items jsonb DEFAULT NULL)`
  —— `20260805100000:80-87`。**`p_items` 為 NULL = 整單取消,非 NULL = 部分取消。**
- 🔴 **實查:A8a2 全檔 `pending_refund` 命中 = 0**(`git show origin/dev:<該檔> | grep -c pending_refund`)
  ⇒ **部分取消今天完全沒有碰待退款,那是板列說的那個洞。**

### 🛑 而**不能只加一個 trigger** —— 理由是既有碼寫著的
既有那支 `pcm_pending_refund_on_cancel()`(`20260901080000:367`)掛的是
`OLD.cancelled_at IS NULL AND NEW.cancelled_at IS NOT NULL`(同檔 `:377-378`)。
⇒ **部分取消不寫 `cancelled_at`(§2 實查)⇒ 那個 trigger 永遠不會醒。**

而 `pcm_pending_refund_open_for`(`20260905070000:121`)第一件事就是
`IF v_cancelled_at IS NULL THEN RETURN`(同檔 `:174-175`)
⇒ 🔴 **直接叫它也沒有用,它會安靜地什麼都不做、回 void、沒有錯誤。**

📌 **⇒ 那正是最危險的形狀:接上了與沒接上印同一個東西。**
### 🔴🔴 而 `open_for` 找 cancellation 的方式,對部分取消【是壞的】(codex must-fix)

`pcm_pending_refund_open_for` 用 `c.created_at = v_cancelled_at` 找那筆取消單
(`20260905070000` 那段 `SELECT count(*), (array_agg(c.id))[1] … WHERE c.order_id = … AND c.created_at = v_cancelled_at`)。
🛑 **部分取消的 `orders.cancelled_at` 是 NULL** ⇒ 那個比對**無法成立**
⇒ 拿不到 `cancellation_id` ⇒ **待退款那一列的歸屬會留白**。
🔴 **而多次部分取消時更糟**:同一張單有多筆 `order_cancellations`,
而 `order_pending_refunds` 的 `ON CONFLICT (order_id, rail)` 是**每軌一列**
⇒ **第二次取消會覆寫第一次的快照**, 而「覆寫」的語意**沒有人定義過**。
⇒ 📮 **這一格本 plan 不猜** —— 它要嘛在 OP7 裡明定(例:歸屬到觸發這次的那筆取消單),
   要嘛獨立成一片。**寫下來, 不要讓下一個人以為 `open_for` 直接可用。**

⇒ ✅ **接法**:在 A8a2 寫完 `order_cancellation_items` 之後**明呼**一次,
   並把 `open_for` 的那道 `cancelled_at` 閘**改成「整單取消 或 有取消品項」**,
   而**不是拿掉它**(拿掉 = 任何一張沒事的單都會被開待退款)。

---

## §6 測試:雙向,而且要有一格「這個突變落在目標上」

| # | 世界 | 期望 | 🔴 它防的是什麼 |
|---|---|---|---|
| 1 | 整單取消、匯款收 1000 | 待退 1000 | **回歸**:改完之後既有行為一個字都不能變 |
| 2 | 部分取消、收 1000、剩餘應收 700 | 待退 300 | 缺陷本體 |
| 3 | 部分取消、收 1000、剩餘應收 1200 | **待退 0 列**(不是一列 0 元) | Q13 的 `max(0, …)`,「不足不用退」 |
| 3b | 🔴 **拍乙才有的**:5,200 免運、取消 300 ⇒ 剩餘 4,900 **+ 補收運費 100** | 待退 **200**, 不是 300 | 這一格就是 Sean 拍的那個 |
| 3c | 🔴 取消金額**剛好等於補收的運費** ⇒ 退款額 = 0 | **0 列 + 一句話給員工** | §2b ③:0 列與「沒收過錢」同形 |
| 3d | 🔵 `shipping_method = 'store'`(自取)⇒ **永遠不補運費** | 退整整那件的錢 | 少了它, 一個「一律補 100」的實作會讓 3b 綠 |
| 3e | 🔴🔴 **整單取消**(剩餘件數 = 0) | 退**全額**, 不是全額 − 100 | §2b ①:少了特判會少退 100, 而 `max(0,…)` 蓋不掉它 |
| 3f | 🔴 同品項買 3 **取消 2**(那一列還在) | 剩餘只算 1 件 | §2b ②:用 `line_total` 會把已取消的 2 件算進去 |
| 3g | 🔴 **手動單**(員工打運費 350) | 沿用 350, **不重算** | **Sean 2026-09-05 拍甲**:沿用員工打的數字 |
| 3h | 🔵 **邊界 4,999 / 5,000**(各一發) | 4,999→補 100 / 5,000→0 | 門檻是 `>=`, 差一個字就錯一邊 |
| 3i | 🔴 **反欠錢**:5,050 取消 51 ⇒ 應收 5,099 | 退款額 **0**, 而且**取消要成功** | **Sean 拍甲**「就算了」⇒ 不開待退款、**不擋** |
| 3j | 🔵 **多次部分取消**:先取消 A 再取消 B | 第二次**不覆寫**第一次的歸屬 | §5:`ON CONFLICT (order_id, rail)` 每軌一列 |
| 3k | 🔵 `discount_total > 0` 的單 | 用**實際那一欄**, 不假設 0 | §2b ⑤:券系統已存在 |
| 4 | 部分取消、**一件都沒取消** | 待退 0 列 | 🔴 **負對照**:閘放太寬時這一格會紅 |
| 5 | 匯款收 1000 + 現金退 500、部分取消 | 逐軌不分岔 | 跨軌那條路(`20260902030000` 修的那個) |
| 6 | 剩餘應收剛好等於已收 | 0 列 | 邊界 |

🔴 **期望值一律從【Q13 公式】推,不從我要寫的那行碼推。**
✅ **3i / 3g 的期望值 2026-09-05 由 Sean 拍下來**(都是甲)⇒ 現在寫得出來。
📌 而在那之前 3i 是空的 —— **先寫一個猜的期望值, 那一格會從出生起就在測我猜的東西。**
⚠️ **自報**:這七格(3e~3k)我在 12:xx 那一則回報說「已加」, **而它們當時沒有真的進檔**
   —— 我的 `python` 替換 assert 過了, 但寫在另一個區段。**現在才真的補上。**
   ⇒ 📌 **「腳本沒報錯」與「東西進到我以為的位置」是兩個宣稱。**
`3` 那一格特別寫成「0 列」而不是「一列 0 元」——
因為 `pcm_pending_refund_amounts` 今天就是**濾掉非正數**的(`20260902030000:90`),
而「一列 0 元」會讓 `order_pending_refunds` 長出一列**沒有人要付的待辦**。

🔬 **突變要先證明它落在目標上**:把 `剩餘應收` 那一項改成常數 0 ⇒ **第 2、3、6 格必須紅、第 1 格必須綠**。
🛑 **若第 1 格也紅了 ⇒ 那不是好消息,那代表我改壞了整單那條路。**

---

## §7 rollback

- 🔴 **`CREATE OR REPLACE` 沒有自動回頭路** —— 回滾 = **把改之前那份定義原樣再貼一次**。
  ⇒ 那份定義的取得方式寫進 migration 檔頭:`bash scripts/latest-definition-of.sh pcm_pending_refund_amounts`。
- ⚠️ **而回滾函式**不會**回滾它已經寫進 `order_pending_refunds` 的列**。
  ⇒ migration 要附一段「哪幾列是本次開的」的查法(用 `created_at` 區間 + `cancellation_id` 非 NULL),
  **不要**寫成一段 DELETE —— 刪錯的代價是一筆該退的錢消失。
- ✅ 新函式 `pcm_order_remaining_due` 是**裸 `CREATE`**(不是 OR REPLACE)⇒ 撞名當場紅
  (理由逐字在 `20260902030000:60-63`);回滾 = `DROP FUNCTION`。

---

## §8 誰審 · 要誰批

- **鐵則 12①(錢)+ ③(DB 結構)⇒ codex 對抗審查,不降級。** 先跑 `code-reviewer`,再叫 codex 審 diff。
- **鐵則 8**:動 schema ⇒ **Sean 批准才執行**。
- 📮 **而在那之前,§2 那個一字題要先有答案** —— 沒有它,函式本體算不出來。

---

## §9 我**沒有**做的(不要讀成已查)

1. **沒有**驗證 `剩餘應收` 在現行 schema 下逐筆算得準(那要真資料,而我只有唯讀查詢那條路)。
2. **沒有**去正式庫看今天有幾張單處於「部分取消 + 收過非卡的錢」——
   ⇒ 🔴 **所以我答不出這件事今天漏了多少錢。** 板列原本也沒查。
3. 🔴 **`discount_total` 在部分取消之後要不要跟著縮 —— 沒查, 沒問。**
   ⛔ ~~折扣那一半我連問題都還沒寫得出來~~ ⇒ **今天寫得出來了**:券多半綁在特定品項或門檻上
   ⇒ 取消掉那件之後那張券還算不算數。**券系統已存在 ⇒ 這一格不是理論。**
4. 🔴 **`tax_total` 在部分取消之後要不要跟著縮 —— 沒查, 沒問。**
   本片的式子**完全沒有碰 `tax_total`** ⇒ 那不是「判定它不受影響」, 是**沒查**。
5. 🔴 **沒有量「抽 `pcm_shipping_fee_for` 會動到 `create_order` 的哪幾行」** ——
   那是它自己那一片的偵察。**不要讀成「已經評估過影響面」。**
   ⚠️ 而 codex 指出**我引的不是最新那一代 `create_order`**(券片之後又改過)
   ⇒ **動它之前先跑 `bash scripts/latest-definition-of.sh create_order`**, 不要照本 plan 的行號抄。
6. 🔴 **多次部分取消時「待退款覆寫」的語意沒有人定義過**(§5)。
7. 🔴 **加購 / 改價之後再部分取消** —— 沒查那條路存不存在, 也沒查式子撐不撐得住。
8. 🔴 **「用當下規則或下單時規則」沒有拍板** —— 規則改版後舊單會變價(§2b ④)。
9. ⚠️ **本 plan 的座標寫的是 `origin/dev`**, 而那是一個**會移動的名字**(codex nit)。
   ⇒ 引用時對當下的 `git rev-parse origin/dev` 重量一次;**本檔不寫死 SHA,
   因為寫死的那個在下一個人讀到時同樣會過期** —— 差別只在**它不會自己說**。
