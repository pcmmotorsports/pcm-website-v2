# ⟦f3-SHIPPDF1⟧ 出貨文件 PDF 傳客人 —— 路標與決策狀態(2026-09-05 線【出貨】`-ship`)

> 🔴🔴 **這一份【不是】plan。正本在 `~/pcm-mailbox/線-出貨-plan-412-伺服器產PDF-20260830.md`(1176 行,2026-09-04 21:17 改過)。**
> 它已經含鐵則 8 要的全部欄位 —— 要改什麼 / 為什麼 / 影響面 / rollback / **以及「不做」那一欄**(`§13-①〜⑥`)。
>
> 🛑 **我被交辦「寫一份 plan」時, 差一步就要寫第三份。** 而板列 `docs/launch-todo.md` `⟦f3-SHIPPDF1⟧` 逐字記著這個病:
> 「2026-08-31 主視窗因此把這一件【重新派了一次】給已經寫過它的那條線 …… **完整度不會讓它被找到**。」
> ⇒ 📌 **我今天這一發是那個病的第 3 個實例。**⇒ 所以本檔的用途是【一條路】, 不是一份內容。

---

## 1. 鐵則 1 真權威(今天當場解的,不憑記憶)

| 面 | 真權威 | 怎麼量的 |
|---|---|---|
| 這張紙長什麼樣 | 🔴 **OD 專案 `pcm-print-docs`** —— `shipping-picking-doc-a4.html` + `contract.md` + `brief.md`(共 11 檔) | `ls .../Open Design/.../data/projects` |
| `design-reference` | ⛔ **查無出貨單稿**。分母 = `design-reference` 全樹;`grep -rliE '出貨單\|shipping-doc\|packing\|invoice\|明細單'` ⇒ **命中 4 檔**, 逐檔開過 = `HANDOFF-v2.0.md` / `checkout.css` / `CheckoutPage.jsx` / `AccountPages.jsx` ⇒ **一支都不是印刷稿** | 同上 |

⚠️ **`list_projects` 今天連不上 daemon**(`http://127.0.0.1:7456` 不可達)⇒ 照鐵則 1 訂正條「數字不合以磁碟為準」**以磁碟 12 個專案為準**。
🛑 **而「連不上」不是「回空」** —— 我沒有拿到 OD 那一側的讀數, 這一格是**磁碟單邊**。

---

## 2. 已經拍過板的 —— **不要再問 Sean**

Sean 2026-08-23(正本 memory `project_0823-sean-shipping-doc-server-render.md`,主視窗轉逐字):
1. **出圖 = 甲 伺服器渲染**;格式 **只做 PDF**(PNG 不做)。
2. **原揀貨鈕改「訂單明細」** —— 已落地在線上(`order-detail-header.tsx:251`,顆 `4b3ef949`)。
3. **兩張紙、兩個用途,不是合併掉一張** —— ⚠️ 他親口把 08-16 那個「合併」換掉了。四格對照 `-30` 08-30 逐格量過 **全中 ⇒ 零剩餘工作**。

🔴 **memory 索引裡那串「揀貨單廢掉用出貨單取代」是錯的** —— 信箱已有訂正:「原話是 **08-23**、而且是【合併】不是【廢掉】」,而 08-23 他又把「合併」換成「兩張紙」。
⇒ 🛑 **那四個索引關鍵字(明細改真PDF / 明細改給客人存查 / 揀貨單廢掉 / 印電話地址照稿)在 memory 裡指不到任何 topic 檔**(`grep` 分冊 ⇒ **0**)⇒ **不得從關鍵字反推本體。**

## 3. 已經裁過的工程題 —— **也不用問**

【碼放哪】✅ **裁定 = ① `packages/adapters` 開 `./pdf` 子路徑**(主視窗 `-f0` 2026-09-03 裁,判「工程判斷不是 Sean 的題」)。
我 09-03 原本裁 ②,**已被推翻並接受** —— 反對 ① 的唯一理由(Next 靜態追蹤穿不穿 workspace package)被量掉了:admin 24 支 `.nft.json` 有 **21 支各含 24 個 `pg` 檔** ⇒ 它今天就在正式站穿過去。
⚠️ **不可讀太寬**:`pg` 是純 JS、chromium 是二進位 ⇒ 那一發**沒有**證明二進位資產也會被帶過去。

---

## 4. 🔴 板列 `⟦f3-SHIPPDF1⟧`(`origin/dev:docs/launch-todo.md:554`)**今天有兩處過期**

| 板上寫 | 今天的事實 |
|---|---|
| 「兩筆方向相反的裁定 …… 交主視窗裁,不由任一施工窗定案」 | ✅ **已裁**(見 §3),而板列沒被回頭改 |
| 誰欄「待派」/ 備註「`-c8` 接手」 | `-c8` 早已不在艦隊。`-7d` 已點破過一次 |

📌 **這是那一列自己記載的病(「板子寫對的那一天過去了」)的下一個實例 —— 而它記在自己身上時一樣沒有訊號。**

---

## 5. 真正剩下的三件(照順序,前一件沒過不要動下一件)

1. **前置:丁先被驗過** —— `/account/orders/<id>/statement.pdf` **正式站讀數至今 = 0**(引自 plan `§13`,**非我今天的量測**)。
   🛑 沒先打那一發, 抽套件的失敗會與丁本來就有的失敗混在一起, **分不出是誰弄的**。
2. **抽 `htmlToPdf` 到 `packages/adapters/pdf`** + admin 新增一條 PDF route,吃既有的 `shipping-doc.tsx` HTML。
3. **鐵則 8 + 12④ 要 Sean 批** —— 動 `package.json`、跨 2 個 Vercel 專案、admin 最大 route 從 **3.70 MB → 約 166 MB**(45 倍;上限 250 MB,餘裕 84 MB,**冷啟動未量**)。

## 6. 拆片(每片 ≤45 分鐘;**批准前一個字都不寫**)

| 片 | 內容 | 驗收 |
|---|---|---|
| 丁-驗 | 正式站打一發 statement.pdf | 下載得到 + **中文是字不是方框** |
| 甲 | `packages/adapters` 開 `./pdf`,`htmlToPdf`/`buildStatementPdfHtml` 搬過去 | storefront 那條 route 行為**逐位元組不變**(現成 fixture 比 HTML,不比 PDF 位元組) |
| 乙 | admin 新增 `/print/orders/[id]/shipping/[shipmentId]/doc.pdf` | 三綠 + 部署後真的下載一發 |
| 丙 | 那顆鈕 | 肉眼驗 |

⚠️ **驗收有一半只在部署後成立** —— `htmlToPdf` 在本機跑不起來(它自己的測試檔逐字寫了)。

---

## 7. 端給 Sean 的題 —— **只有一題**

```
Q-出貨PDF:後台加一條產 PDF 的路,那條路的函式包會從 3.7 MB 變成約 166 MB
          (上限 250 MB,塞得下;冷啟動會變慢多少我沒量過)。
A: 甲 = 做(推薦)   |   乙 = 先不做,員工照舊用瀏覽器「另存為 PDF」
```
**推薦甲。** 乙的代價:那張紙長什麼樣**取決於員工自己的瀏覽器與印表機設定** ⇒ 客人收到的檔案會因人而異,**而沒有任何東西會叫**;且乙**推翻他 08-23 拍的「甲 伺服器渲染」**⇒ 那要他自己說。

## 8. 🛑 我今天沒做什麼

- **一行碼都沒改**,一份 plan 都沒寫(刻意)。
- **沒有跑 `list_projects`**(daemon 不可達)⇒ §1 那格是磁碟單邊。
- **沒有複驗「正式站讀數 = 0」** ⇒ 那是引自 plan `§13`,不是我量的。
- **沒有量冷啟動**(要真部署)。
