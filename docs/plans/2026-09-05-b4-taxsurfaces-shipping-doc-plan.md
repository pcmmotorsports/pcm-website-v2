# Plan · ⟦b4-TAXSURFACES⟧ 第 7 步 —— 五個面的稅額列與「小計(未稅)」

> 提出者:線【帳號】`agent/line-account-step7`, 2026-09-05 凌晨。
>
> ## 🔴🔴 §0 先講一件對我不利的事(codex R1 must-fix #5)
>
> **本檔涵蓋兩段東西, 而它們的狀態【不同】, 不可混讀:**
>
> | | 範圍 | 狀態 |
> |---|---|---|
> | **A** | ③顧客站列印/PDF · ④會員中心詳情 · 「小計(未稅)」四個面 | 🔴 **碼已經寫完並測過了 —— 而【寫在 plan 之前】** |
> | **B** | ⑤出貨單(紙) | ✅ **一行碼都沒動**, 等批准 |
>
> ⛔ **我原本的判定是「③④ 屬於 Sean 已批的九步施工順序第 7 步 ⇒ 不另觸發鐵則 8」。**
> 🔴 **codex R1 打穿它, 而它是對的**:鐵則 8 逐字是「跨 3+ 檔」即觸發, **沒有寫「已在某個
>    已批順序裡就豁免」**;而 A 段實際動了 **20 支 code/test + 2 支 docs**(⛔ ~~原本寫「22 支檔」~~ —— 那個數字把 docs 混進 code 一起數;codex R2 訂正), 其中包含
>    共用投影 `MEMBER_ORDER_DETAIL_SELECT`(byte-equal 守門當場攔過我一次)、
>    共用型別 `MemberOrderDetail`、與一支**新的共用函式** `subtotalLabelOf`
>    —— 而**後兩者不在他批的那份九步字面裡**(那份只寫「五份印稅額那一行」)。
> ⇒ 📌 **我用「⑤另有 plan」把 ③④ 抵掉了, 而那是兩件事。**
>
> ## ⇒ 所以 A 段要怎麼處理(不是我能拍的)
> · 碼在分支 `agent/line-account-step7` 上, **未推、未合**。
> · 🛑 **合進 dev 之前要 Sean 追認這份 plan**, 而不是「反正測試綠就合」。
> · 若他覺得範圍不對(例如不要那支共用函式、或四個面不要一起改),
>   **退版成本很低** —— 它沒有 migration、沒有資料寫入, revert 一顆 commit 就回去了。
> · ✅ 而我把它寫在這裡而不是只寫在 commit body 裡, 理由是:
>   **commit body 只有 review 那個人會讀, 而板子與 plan 是他明天早上會打開的東西。**

---

## 1. 要改什麼

`apps/admin/src/components/print/shipping-doc.tsx` —— **隨箱給客人的紙**。
它今天印 小計 / 運費 / [折扣] / 訂單金額(`:1207` 起的 `MoneyRow`),**沒有稅額那一列**。

改成與其餘四個面一致:
- `tax_total > 0` ⇒ 多印一列「稅額」, 位置在 **折扣之後、訂單金額之前**
- `tax_total > 0` ⇒ 「小計」印成「**小計(未稅)**」(共用 `subtotalLabelOf`, 已在 `@pcm/domain`)
- `tax_total = 0` ⇒ **兩者都不變**(今天每一張單都走這條)

## 2. 為什麼(而它不是完整性要求)

🔴 **第 7 步修完前四個面之後, 製造了一個【修之前不存在的形狀】**:
- 修之前:五個面**一致地都漏稅** —— 全錯, 而它們互相對得上
- 修之後:**信與網頁對了, 紙錯了**
⇒ 🎯 **客人第一次有機會同時握著同一張單的兩份互相矛盾的帳** ——
  信上寫「小計(未稅) + 稅額」, 而箱子裡那張紙寫「小計」而且沒有稅。

⇒ 📌 所以這一片是 `⟦b4-INVOICE5PCT⟧` **第 9 步(RPC 真的算稅)的前置條件**, 不是收尾工作。
  第 9 步一上線 `tax_total` 就會有值, 而那一刻這張紙必須已經改好。

🛑 **而同一支檔 `:1214` 自己記過【同一型缺陷】**:
   「稿只印 小計 / 運費 / 訂單金額 ⇒ 折扣不為 0 時, 紙上三個數字加不起來」
   ⇒ **換一個欄位又發生一次**, 而上一次的修法是「把折扣印出來」, **沒有變成一道會擋的閘**。
   ⇒ 🔴 **本片要不要順手裝那道閘, 是一個獨立的決定** —— 見下方「不做什麼」。

## 3. 預期影響面

### 3.1 一定要動的(五處)

| # | 檔案 | 改什麼 | 風險 |
|---|---|---|---|
| ① | `packages/domain/src/order/types.ts` | `AdminOrderDetail` 加 `taxTotal: Money` | 低;會讓所有建 `AdminOrderDetail` 的 fixture 紅 ⇒ **那是要的** |
| ② | `packages/adapters/src/supabase/mappers/order.ts` | `SupabaseAdminOrderDetailRow` 的 Pick 加 `'tax_total'` + 映射一行 | 低 |
| ③ | `packages/adapters/src/supabase/SupabaseOrderAdapter.ts` | `ADMIN_ORDER_DETAIL_SELECT` 加 `tax_total` | 🔴 **本片的核心風險, 見 3.2** |
| ④ | `packages/adapters/src/supabase/SupabaseOrderAdapter.test.ts` | byte-equal 期望字串 + 假 row + 期望物件 | 低(閘會叫) |
| ⑤ | `apps/admin/src/components/print/shipping-doc.tsx` | 稅額列 + 小計標籤 | 低;純顯示、零運算 |

### 3.2 🔴 `ADMIN_ORDER_DETAIL_SELECT` 那一格

- **消費端(實測, 不是記得的)**:`grep -rn 'ADMIN_ORDER_DETAIL_SELECT' apps packages`(排除 `.next`)
  ⇒ `SupabaseOrderAdapter.ts` 6 · `mappers/order.ts` 3 · `types.ts` 2 ·
  `apps/admin/src/lib/shipping/shipment-candidates.ts` 2 · `shipment-repository.ts` 1 ·
  `shipping-doc.tsx` 1 · `IOrderRepository.ts` 1 · 測試 21
  ⚠️ **這串數字是【今天】的** —— 開工當天重跑一次, 不要引用本行。
- **byte-equal 守門**在 `SupabaseOrderAdapter.test.ts`。它會擋下這次改動 ⇒ **那是它的用途**,
  要有人在那裡簽名。🔵 第 7 步改 `MEMBER_ORDER_DETAIL_SELECT` 時它**當場攔下來過一次**。
- **`tax_total` 為什麼安全**:它是**訂單本身的金額欄**, 與 `subtotal` / `discount_total` 同族。
  不是價格表欄(`price_store` / `price_by_tier` / `price_general` / `cost`)、
  不是 PII、不是採購 token ⇒ 該檔的 forbidden-token 那格不受影響。
  🛑 **而這是【字面層】的論證** —— 它證明「投影字面沒有新增禁止 token」,
     **不證明**「這一欄在 RLS 下對每個角色都讀得到」。後者是行為層, 本片不宣稱。

### 3.3 不動、而要說出來的

- **顧客站那三個面(信 ×2 · 詳情頁 · 列印)已經在第 7 步做完** ⇒ 本片只補紙這一面。
- **`AdminOrderSummary`(列表)不動** —— 列表不印金額明細。
- **後台畫面上的訂單詳情不動** —— 那不是對客可見的紙。⚠️ 若之後要, 那是另一片。

## 3.4 🔴🔴 鐵則 1:那一列【稿上沒有】—— 查無, 而分母與正對照都在

> **來源:R4 換模型審查(opus `adversarial-reviewer`)F2 must-fix。**
> 它抓到的是:我在兩個顧客可見的面**新增了一列 UI**, 而 plan / commit body / 碼註
> **零 design 真權威查證**。⇒ 鐵則 1 逐字:「寫前台元件前**必先 grep design-reference 字面**」。

**2026-09-05 當場量的(不是記得的):**

| 尺 | 分母 | 「稅額」 | 🟢 正對照「小計」 |
|---|---|---|---|
| `design-reference/`(`design-ref-check.sh` 報 176 個檔) | 176 檔 | **0 命中** | **5 檔** |
| OD 磁碟 12 個專案 | 12 專案 | **0 個專案** | **7 個專案** |

- OD 專案逐字:`5b31ed57…` `accd9e6d…` `pcm-524f` `pcm-admin-order-ui` `pcm-admin-shell-4dir`
  `pcm-home-redesign` `pcm-home-redesign-codex` `pcm-home-redesign-opus5` `pcm-print-docs`
  `pcm-product-admin-4dir` `pcm-product-edit-screen` `prx-design-template`
- ⚠️ **`list_projects` 這次【叫不到】**(daemon 沒開:`cannot reach … 127.0.0.1:7456`)
  ⇒ 照鐵則 1 的字面「數字不合 ⇒ **以磁碟為準**」, 這裡用磁碟的 12。
  🛑 而**「叫不到」與「回 12」不是同一件事** —— 前者代表我沒有第二把尺去交叉, 照實寫。

🔴 **⇒ 結論:「稅額」這一列在【任何一份設計權威】裡都不存在。**
   而 Sean 2026-09-04 拍的那一板**只講標籤文字**(「小計」⇒「小計(未稅)」),
   **沒有講「金額區多一列」**。
   ⇒ 📌 **那一列是我加的, 而它沒有被授權過。**

✅ **處置(本片不自己拍)**:那一列留在碼裡, 而**題 A 附註一句**「版面多一列未經 design 授權」
   ⇒ 他若說不要, 拿掉那一列**不影響**「小計(未稅)」那半(兩者是分開的改動)。

---

## 3.5 🔴 第六個面:`picking-doc.tsx` —— 而它與 ⑤ 是【同一片】

> **來源:R4 F1 must-fix。** 我的分母漏了它, 而**漏掉的原因是量法**:
> 我用 `grep orderAmountsBalance` 找「印金額的面」, 而那支檔**結構上不含那個字**
> ⇒ 📌 **用一個東西的【某個實作細節】去找它的同類, 會漏掉沒有那個細節的那些。**

- `apps/admin/src/components/print/picking-doc.tsx:542-566` 印 小計 / 運費 / [折扣] / 訂單金額,
  **無稅列、無未稅標籤**。而該檔 `:10-11` 已經是「訂單明細 / ORDER DETAIL」、
  `:20-22` Sean 2026-08-29 拍甲要它印金額 ⇒ **它是給客人的**。
- ✅ **它吃的是 `AdminOrderDetail`(`:2` `:113`), 與 `shipping-doc.tsx` 同一個型別、同一條 select**
  ⇒ 🎯 **加 `taxTotal` 到 `AdminOrderDetail` 一次, 兩面都通** ⇒ **與 ⑤ 同一片、同走鐵則 8。**
- ⇒ 📌 **題 B 的受詞因此是【兩張紙】不是一張** —— 已改寫在 §7。

---

## 3.6 後台那兩個金額面 —— 而它們【不是同一件事】

> **來源:R4 F4 nit ⇒ 板列 `⟦b4-TAXADMINSURFACES⟧`。2026-09-05 當場量的判定。**
>
> ```
> ADMIN_ORDER_DETAIL_SELECT  含 tax_total ⇒ 0     AdminOrderDetail  含 taxTotal ⇒ 0
> ADMIN_ORDER_LIST_SELECT    含 tax_total ⇒ 0     AdminOrderSummary 含 taxTotal ⇒ 0
> ```
> ⇒ 🔴 **兩個都不在查詢裡, 而且走【兩條不同的 select】** ⇒ 沒有一個是「純 UI 加一列」。

### ① 後台訂單詳情 `ItemsTotals` ⇒ **併進題 B**

`apps/admin/src/components/orders/order-detail-items-support.tsx:209` 吃 `AdminOrderDetail`
⇒ `ADMIN_ORDER_DETAIL_SELECT`(byte-equal 守門 `SupabaseOrderAdapter.test.ts:1083`)
⇒ 🎯 **與 ⑤⑥ 同一條 select、同一道鐵則 8 關卡** ⇒ 加一次三個面都通。
⇒ 📌 **題 B 的受詞因此是「兩張紙 + 後台訂單詳情那一區」。**

### ② CSV 匯出 ⇒ **第三條投影, 而且它有一個【真的決定】**

`apps/admin/src/lib/orders/order-export.ts` 吃 `AdminOrderSummary`
⇒ **`ADMIN_ORDER_LIST_SELECT`**(守門 `:512`)⇒ 🔴 **題 B 蓋不到它。**
消費端含 `apps/admin/src/components/orders/orders-table.tsx` —— **後台訂單列表本身**。

🛑 **而它不是「加一欄就好」**:那份 CSV 是**每商品一列**, 而稅是**訂單層**的數。

🔬 **既有慣例(讀出來的, 不是猜的)**:`orderTotalCellFor(order, i === 0)`(`:88-90`, 用在 `:196`)
—— **只在該單的第一列印, 其餘留空**;欄名逐字「`訂單總額(每單只出現一次,可直接加總)`」。
而 `小計` 那一欄的欄名逐字是「`小計(每列都有)`」⇒ 📌 **這份檔本來就混著兩種欄, 而【欄名是它們的分辨方式】。**

```
甲(推薦)每單只出現一次 —— 與 orderTotalCellFor 同慣例, 欄名寫「稅額(每單只出現一次,可直接加總)」
乙       每列都印       —— 看單獨一列時讀得到稅, 而【整欄加總會把稅乘以品項數】
```

🔵 **推薦甲**, 三個理由:
1. **這份檔存在的唯一目的是被拿去對帳**(該檔 `:92-99` 逐字)⇒ 一個不能直接加總的金額欄違反它的用途。
2. **與既有的 `訂單總額` 同型** ⇒ 讀的人不必學第二套規則。
3. 🔴 **乙的錯法沒有訊號** —— 試算表對一欄按加總, 出來的數字**看起來很正常**, 只是大了 N 倍。

⚠️ **而甲的代價寫出來**:只看某一列的人**看不到稅**。
   🔵 而那個代價**今天已經被接受了** —— `訂單總額` 就是這樣, 而它比稅更常被單獨看。

🔬 **順帶查證(擋掉一個看起來合理的疑慮)**:「第一列被篩掉的話稅就不見了」——
   **不成立**。`i === 0` 是**該單自己那組 lines 的第一列**(`:176-177`),
   而 `order.lines` 為空時有 `[null]` 佔位 ⇒ **每一單一定有第一列**。

---

## 4. 怎麼驗收(每條可 yes/no)

1. `TURBO_FORCE=1 pnpm typecheck` / `lint` / `build` 三者 rc=0
2. `shipping-doc` 的測試:**兩個世界各一格** —— 稅 0 ⇒ 不印「稅額」且標籤是「小計」;
   稅 > 0 ⇒ 印「稅額」與那個數, 且標籤是「小計(未稅)」
3. 🔴 **突變**:把 `subtotalLabelOf` 改成永遠回原字面 ⇒ **五個面的測試都要紅**
   (第 7 步實測過四個面全紅;本片要看到第五個)
4. 🔴 **突變**:把稅額列的 `> 0` 拿掉 ⇒ 稅 0 那一格要紅
5. byte-equal 那格更新後仍然 byte-equal, 且 forbidden-token 那格照舊綠
6. 測試分母**用工具算**(`vitest related` 逐支餵改動檔), 不自己挑;連跑兩發比四個數
7. 🔴 **`⟦b4-TAXADMINSURFACES⟧` 只關掉【一半】** —— 後台詳情 `ItemsTotals` 隨題 B 做掉了,
   ⛔ ~~而我原本在這裡寫「已關(後台詳情 + CSV 那兩個面)」~~ **那句話是假的**(codex must-fix):
   🔴 **CSV 那一面【一行碼都沒動】**, 而它走的是另一條 `ADMIN_ORDER_LIST_SELECT`、
   還有「每單一次 vs 每列」那個未拍的決定(§3.6)。
   ⇒ 📌 **收尾時不得宣稱那一列已完整關閉。**
   ⛔ **這一條是 R4 F6 的精神**:F6 原本講的是「`⟦b4-MONEYSPLIT⟧` 開了列而 §4 與第 9 步都沒有它
   ⇒ **沒有人會被擋**」。🔵 `MONEYSPLIT` 那一列已在同一夜**證偽**(見板列)⇒ F6 的原受詞消失,
   **而那個病沒有消失** —— 它換到我當晚新開的 `⟦b4-TAXADMINSURFACES⟧` 身上。
   ⇒ 📌 **所以把它掛在這裡, 而不是說「F6 不適用」。**
8. 🔴 **鐵則 12①(錢)** ⇒ codex 對抗審查, **不降級**
   ⛔ ~~我原本標成 12⑥「共用元件行為改動」~~ —— **標錯了**(codex R2 must-fix 5):
   本片直接動**訂單金額的投影、型別與對客金額列** ⇒ 首先命中的是 **①錢**;
   而 `shipping-doc.tsx` **不是** `packages/ui` 共用元件 ⇒ ⑥根本沒命中。
   📌 兩者都會叫 codex, **而風險分類決定審查者去看什麼** —— 標錯會讓他看錯的東西。

## 5. Rollback

- 純 revert 那一顆 commit。**沒有 migration、沒有資料寫入** ⇒ 可逆。
- ⚠️ 唯一不可逆的東西:**已經印出去的紙**。
- ⛔ ~~我原本寫「在第 9 步之前上線, rollback 成本是 0」~~ —— **那句話少了前提**(codex R1 must-fix #3):
  🔴 `orders.tax_total` 這一欄**schema 上允許任何 writer 寫非零值**, 而第 9 步不是唯一的路
     (service_role 直寫、手貼 SQL、之後別片的 RPC 都可以)。
  ⇒ 📌 **只要出現一張非零稅的單, 而紙已經印出去了, 那張紙就收不回來。**
- ✅ **正確的講法**:「**只要 `tax_total` 全庫仍是 0, 改前改後印出來的紙逐字相同**
  ⇒ 那段期間 rollback 對客人可見的差異是 0。」
  🔬 **而那個前提是可以量的, 不是假設的**:apply 前跑一發
  `SELECT count(*) FROM public.orders WHERE tax_total <> 0;` ⇒ 期望 0。
  🛑 **不要跳過那一發** —— 「我以為還沒有人寫」與「量到 0」是兩個宣稱。
- 📌 **而「現在做」的理由仍然成立**:等到稅有值那天再做, **中間每一張紙都是錯的**。

## 6. 不做什麼(而它們是真的缺口, 不是我懶)

- 🔴 **不裝「金額欄位加不起來就擋」的閘。** `:1214` 那次的教訓說它該有,
  而它牽涉「紙上要不要有 fail-closed」這個更大的決定 ⇒ **另開一列, 不夾帶。**
- 🔴 **不碰「運費那一列要不要也標(未稅)」**(R4 F5 nit)——
  🔬 稅基 = **折後小計 + 運費**(`⟦b4-INVOICE5PCT⟧` Sean Q2 拍甲)⇒ 📌 **運費那一列在有稅時也是未稅的。**
  而四個面**只有小計帶「(未稅)」** ⇒ 紙上與信上**說不出運費含不含稅**。
  🛑 而 `Q-B`(運費要不要一起 +5%)**Sean 還沒答** ⇒ 這一題的前提還沒定, **現在問會問錯**。
  ⇒ 已記, 等 `Q-B` 有答案再處理。

- 🔴 **不碰後台那兩個金額面**(`ItemsTotals` + CSV)—— 已開板列 `⟦b4-TAXADMINSURFACES⟧` 帶關閉條件。

- 🔴 **不碰「品項表欄頭那個『小計』(行小計)要不要也加(未稅)」** ——
  Sean 拍板那句講的是金額區那一個。**行小計在有稅時也是未稅的**(他選乙:單價原樣保留),
  ⇒ 那是一題**還沒有人問過他**的, 已記板, **本片不代他決定。**

---

## 7. 要 Sean 拍的是【兩題】, 而 A 先問

> 🔴🔴 **這一節被 codex R3 must-fix 1 整個改掉了。**
> ⛔ 原本只有一題, 而它只問 B(出貨單)⇒ 📌 **他答完 B, A 段就【默認通過】了。**
> codex 逐字:「這正是『流程違規被透明自陳包裝後, 比較容易過關』的實例 ——
> 自陳是真的, 而它同時製造【已完成、測過、退版可惜】的錨定效果。」
> ⇒ ✅ 拆成**兩道獨立 yes/no**, 而 **A 沒有答案就不得 merge**(§0 的 A 段)。

### 題 A(先問;不答 = 不得 merge)

```
Q: 顧客站三個面(詳情頁 / 列印 / 兩封信)的稅額列與「小計(未稅)」已經寫完並測過了, 而
   碼是寫在 plan 之前的 —— 這件事要追認嗎
A: 甲 追認, 照現在這樣合(含那支共用的「小計(未稅)」函式)
   乙 追認方向, 而那支共用函式拿掉, 五個面各寫一次
   丙 不追認, 退回重切
```

🔵 **推薦甲**, 而推薦的理由要連同它的弱點一起看:
- 甲的好處:`tax_total` 今天全是 0 ⇒ **客人可見的行為改變是零**, 而第 9 步一上線就已經備好。
- 🛑 甲的代價:**它讓「先寫碼後補 plan」這件事這一次沒有成本** —— 而那正是它下一次會再發生的原因。
- 乙的代價:那支函式只有一行判斷, 拆掉不難;但**五個面各寫一次之後, 漏掉第六個面時沒有東西會紅**
  (⚠️ 而反過來說, **有那支函式也擋不住漏掉第六個面** —— codex R3 打過我這個論證,
  它證明的是「現有五個都接到同一個節點」, 不是「未來不會漏」)。

### 題 B(A 答完再問)

```
Q: 【兩張紙(出貨單 + 訂單明細)+ 後台訂單詳情那一區】現在都沒有稅額那一列, 要不要現在補
A: 甲 現在補 —— 動手前先跑一發 SELECT count(*) FROM public.orders WHERE tax_total <> 0,
      是 0 就代表今天印出來的紙一個字都不會變
   乙 等稅真的有值那天再補
```

⛔ ~~B 原本那句寫「今天印出來的紙一個字都不會變」而【沒有帶前提】~~ —— codex R2 must-fix 4:
🔴 **限制只寫在前文, 對【只讀決策句】的人等於不存在** ⇒ 決策句自己要帶著它的前提走。

---

## 7.5 🔴🔴 Sean 2026-09-05 拍板:**後台建單的單價一律填【未稅】**

> **他的逐字**:`甲 = 單價一律填【未稅】, 系統算稅, 文案改`
> (`⟦b4-PRICECOPYTAX⟧`;主視窗 `-94` 轉述, 2026-09-05 夜跑期間)
> 🛑 **這一節【只落板, 不動碼】** —— 它與題 A / 題 B 還在等答案, 而動碼要等那兩題。

### 這一板改變了什麼

**建單畫面今天逐字教員工相反的事**(`apps/admin/src/components/orders/manual-order-lines.tsx:122`):
> 「**單價這一格**請填**含稅**金額 —— 未稅 1,000 要填 1,050。填成未稅就少收那 5% 的稅, 而系統看不出來、不會擋。」

⇒ 🔴 **那句話從這一板起是錯的, 而它會讓稅被課兩次**(員工填含稅價 + 系統再算 5%)。

### 三個落點(**必須同一次上線**)

| # | 落點 | 改什麼 |
|---|---|---|
| ① | 建單 RPC(`admin_create_manual_order` 下一代) | `tax_total` 從**顯式的 0** 改成 `ROUND((折後小計 + 運費) × 0.05)`;`total` 跟著含稅 |
| ② | `manual-order-lines.tsx:122` | 文案改成「請填**未稅**」, 並拿掉「填成未稅就少收 5%」那句(它反過來了) |
| ③ | `manual-order-catalog-lookup.tsx:142` | 售價那格今天標「(含稅)」—— **它是含稅保證唯一的持有人**, 要跟著這一板重新想 |

🛑 **①②③ 分開上就是【已知會出錯的世界】**:
- 只上① ⇒ 員工還在填含稅價而系統又加一次 ⇒ **重複課稅**
- 只上② ⇒ 員工填未稅而系統仍寫 `tax_total = 0` ⇒ **少收 5%**
⇒ 📌 **兩個方向都錯, 只是錯給誰不同。**

### 🔴 與 2026-08-29 那一板的分界(不要讓兩板打架)

Sean 2026-08-29 逐字:「**網站售價都含稅沒問題, 但是經銷價都是未稅。**」
⇒ 那一板講的是**目錄上的價格**;本板講的是**後台建單時員工手打的那一格**。
✅ **分界寫成一句**:**目錄價(`price_general`)含稅不變;後台建單輸入格改成未稅。**
⚠️ 而 `manual-order-catalog-lookup.tsx` 正好把**目錄價**顯示給員工看、要他抄進**輸入格**
⇒ 🎯 **那兩個東西的稅制從此不同, 而它們在畫面上緊鄰** ⇒ ③ 那一格不是順手, 是這一板的核心風險。

### ✅ 2026-09-05 Sean 又拍了兩題(逐字), 算式因此定了

> `甲 = 稅基 = 小計 + 運費` · `甲 = 不回頭重算, 舊帳不動(推薦)`

**算式(寫死, 不留解釋空間)**
```
tax_total = ROUND((折後小計 + 運費) × 0.05)
total     = 折後小計 + 運費 + tax_total
```
- **「小計」= 折後小計** —— ✅ **2026-09-05 Sean 本人確認, 逐字**:
  `甲 = 折後小計 + 運費(有折扣的單稅算得少)`
  ⛔ ~~本行原本標「待複核 —— 這是我的解讀不是他的字面」~~ **已解除**:
  他不只答了折後, 還**自己講出了那個後果**(「有折扣的單稅算得少」)
  ⇒ 📌 **那句話證明他知道自己在選什麼** —— 而那比「他選了甲」值錢:
  一個只回「甲」的答案分不出「他懂」與「他挑了第一個」。
- **四捨五入**:`ROUND()`(Postgres 對 numeric 是**四捨五入、逢五進位**), 結果轉 `integer`。
  🔴 **金額是整數(元)** —— 本 repo 全線用整數存錢(CLAUDE.md Server 端鐵則)⇒ **不引入小數**。
  ⚠️ 而 `ROUND` 的**銀行家捨入**只發生在 `double precision`, 不在 `numeric` ⇒ 算式要確保運算元是
  `numeric` 不是 `float`。**這一格是實作時要驗的, 不是現在能宣稱的。**

**分界 = apply 那一刻**
- apply **之前**建的單:`tax_total = 0` 且單價含稅 ⇒ 🔴 **不回頭重算、舊帳不動**(他拍的)。
- apply **之後**建的單:單價未稅、系統算稅。
- 🛑 **⇒ 同一張報表上會同時有兩種稅制的單, 而它們的 `total` 都是對的。**
  📌 **這件事沒有欄位說得出來** —— 分辨的唯一依據是**建單時間 vs apply 時刻**。
  ⇒ 🔵 **建議(未拍)**:apply 那一刻把時間戳記進 `APPLIED.tsv` 那一列, 讓日後對帳的人查得到。

### 仍未答

- **既有那些單的【顯示】怎麼辦**:它們 `tax_total = 0` ⇒ 五個面都不會印稅額列、小計也不會標未稅。
  ✅ 那**正好是對的**(它們的小計本來就含稅)⇒ 📌 **本片的「稅 0 不印」設計讓舊單自動正確,
  而那不是運氣, 是那個判準選對了受詞。** —— 這一格**不需要他拍**, 寫在這裡是為了讓下一個人不去「修」它。

---

## 8. 還沒關的缺口(寫出來, 不當作做完)

1. 🔴 **紙那一面(⑤)還沒做** ⇒ 修完前四面之後, **信與網頁對了而紙錯了** ——
   而那個形狀在修之前不存在(修之前五個面一致地都漏稅)。⇒ **第 9 步之前必須到齊。**
2. 🔴 **真媒體只量到「一頁 A4 裝不裝得下」與「那一列在不在 DOM」** ——
   **不答**「多品項時會不會換頁把金額表切成兩半」。那需要一份多品項 + 有稅的樣本, 本片沒有造。
3. 🔴 **品項表欄頭那個「小計」(行小計)在有稅時也是未稅的, 而沒有人問過 Sean。**
   本片的測試**鎖的是現況、不是答案** —— 他拍甲的話那幾格要跟著翻面。
4. ⚠️ **R3 那一輪只換了角度, 沒換模型**(`~/.codex/config.toml` 只設一支 `gpt-5.6-sol`,
   而本 session 不叫 subagent)⇒ **不算完整符合第三輪規矩**。主視窗 `-94` 接手跑換模型那一輪。
