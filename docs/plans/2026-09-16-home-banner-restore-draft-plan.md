# 首頁大圖「封存之後拿得回來」(重新開成草稿)

> 依據:Sean 2026-09-16 批「要做」(那批 16 題裡的一題,逐字「全部依照建議」)。
> 這一份是**做法**的 plan —— 他批的是「要做」,還沒批「怎麼做」。動資料庫 ⇒ 照鐵則 8 先批再寫 migration。
> 寫的人:施工窗(worktree `pcm-admin-ui`),2026-09-16。
> **🔴 2026-09-18 窗 B 覆核(worktree `pcm-ops`,對著 `origin/dev` @ `200a6f571`)⇒ 見下面 §0。**

---

## 🔴 §0 覆核:**這份 plan 的「病」已經過期了**(2026-09-18)

**舊字面全部留著不刪**(下面每一段都還在)。而**它們描述的那個世界,今天不存在了**。

### 中間發生了什麼

`20260916250000_m4b_home_banner_duplicate.sql`(**已貼,帳本記 2026-09-17**)加了**第四支**函式
`admin_home_banner_duplicate`,而後台**每一張**大圖(含封存的)現在都畫得出一顆鈕。

🔬 逐字證據(`origin/dev` @ `200a6f571`,不是印象):

| 舊 plan 說 | 今天實際 | 在哪裡 |
|---|---|---|
| 「封存後**一顆鈕都沒有**」 | ❌ **假的**。有一顆「**複製一張來改**」 | `home-banner-editor.tsx:312-315` 逐字 `{banner !== null ? (<button … data-testid='hb-duplicate'>複製一張來改` ⇒ **沒有任何狀態條件** |
| 「按錯的人得**重新建一張、重新填字、重新等圖**」 | ❌ **假的**。字與圖都跟著複製走 | `20260916250000` 的 `INSERT` 逐字帶走 `eyebrow, title_line1, title_line2, subtitle, cta_label, link_path, image_desktop_url, image_mobile_url, image_origin, image_kind, rights_note, source_email_id, matched_variant_ids` |
| 「資料庫只有**三個動作**」 | ❌ **四個**了 | 多一支 `admin_home_banner_duplicate(uuid,text,text)` |
| 「`save_draft` 那道 `status <> 'draft'` 的擋」 | ✅ **還在,一個字沒動** | `20260916180000:151` 逐字 `RAISE EXCEPTION '只有草稿可以修改(這張是 %)'` |
| 「畫面上沒告訴員工怎麼辦」 | ❌ **有**,而且直接指路 | `home-banner-editor.tsx:171-172` 逐字「**要改請按下面的「複製一張來改」**,複製出來的是一張新草稿,這一張不會動」 |

⇒ 📌 **「拿得下來、放不回去」那個單向門,已經被【繞過去】了 —— 不是被【打開】。**

### 🎯 所以今天真正剩下的差距,只有這四格

複製只重設三樣東西,其餘全帶走。**⇒ 按錯封存的人今天救得回來,只是要多做兩步。**

| 剩下的差距 | 嚴重度 |
|---|---|
| ① `rights_confirmed` 被重設成 `false` ⇒ **要再勾一次授權** | 小(一個勾) |
| ② `starts_at` / `ends_at` 被清成 `NULL` ⇒ **檔期要重設** | 小(發布時本來就要給) |
| ③ 舊那張**封存的列留在清單裡**,永遠不會消失 ⇒ 清單會越積越多 | 中,而且**會隨時間變糟** |
| ④ **id 變了** ⇒ 任何指向舊 id 的東西都指不到新那張 | **量完了 ⇒ 幾乎沒有影響**(見下) |

🔵 而復原能省掉的就是 ①②③;④ 本來以為是復原獨有的好處,**量完之後它不是**。

#### ④ 我把它量了(原本寫「未量」,不留著當未知數)

🔬 三發,每發都附對照:

1. **別的表有沒有外鍵指到 `home_banners`** ⇒ **0**
   `grep -rn 'REFERENCES public.home_banners' supabase/migrations/` ⇒ 一行都沒有(連建表那支自己也沒有)。
2. **碼裡有沒有人把 banner id 存起來** ⇒ **0 個持久化的**
   `banner_id` / `bannerId` 剝測試後 9 命中,**每一個都是過路的**:4 個是 `home-banner-actions.ts` 的
   attempt log 欄位、4 個是 `home-banner-repository.ts` 送給 RPC 的 `p_banner_id`、1 個是表單欄位名常數。
   **沒有任何一處寫進資料庫或設定檔。**
   🟢 正對照:同一把尺數 `home_banners` ⇒ **14** 命中 ⇒ 尺會動,不是 grep 打錯字。
3. **稽核會不會斷鏈** ⇒ **不會,只是要多看一層**
   `20260916250000:170-172` 逐字:`target` 放的是**新**那張的 id,而 `before` 放的是
   `to_jsonb(v_src)` = **被複製的那一列整列** ⇒ 📌 **從哪一張來的,答案在那筆 JSON 裡面,不在 `target` 上。**
   ⇒ 鏈接得回去,而**用 `target` 一個欄位查是查不到的** —— 這一格值得寫進 runbook,不值得為它開一支函式。

⇒ 🎯 **④ 這格從「未知、可能翻盤」變成「幾乎沒有影響」** ⇒ 它**不再是**做復原的理由。

### 🛑 ⇒ 這是一題,不是一個做法

**判別句(`CLAUDE.md`〈做完就停〉逐字):「這件做完,客人的體驗或 Sean 的操作會不一樣嗎?」**

· 客人:**完全不會**(草稿與封存都不見客)。
· Sean / 員工:**會,但只差兩步**(少勾一次授權、少設一次檔期),外加清單少一列垃圾。

```
Q:首頁大圖「復原成草稿」還要不要做?(複製功能上線後,原本那個死路已經不是死路)
  甲 還是做   —— 多兩步是小事, 但清單裡每按錯一次就永遠多一列, 那個會越積越糟;
                 而且復原保住同一個 id。成本:一支 migration + 後台 6 檔 + 一輪對抗審查。
  乙 先不做   —— 今天救得回來(按複製), 差的兩步都只要幾秒;
                 把「封存的列清單看得很煩」留成獨立一題, 那題也許用【清單預設不顯示封存】就解了,
                 零 migration。
  丙 改做別的 —— 只做「清單預設收起封存的列」(純前端, 零 DB), 把積垃圾那格解掉, 復原不做。
  我的建議:乙。理由是【差距已經從「救不回來」縮成「多兩步」】, 而剩下最會變糟的那一格(③)
           有一個不用動資料庫的解法(丙)。碰 schema 的成本沒有變, 而它買到的東西小了很多。
  🔬 原本我寫「④ 沒量到, 量了可能翻盤」⇒ **我把它量了, 而它沒翻盤**:
     沒有任何外鍵指到 home_banners、碼裡沒有一處持久化 banner id、稽核鏈靠 before 那欄接得回去。
     ⇒ 量完之後【甲】又少一個理由, 建議仍是乙。
```

**🛑 在這題答完之前不寫 migration。** 板號 `20260918020000` 主視窗已指定並核過無撞號(我 2026-09-18 複核:全分支 `20260918*` 只有窗 A 的 `010000`)⇒ **號留著,不佔用。**

### 🔵 順手抓到一件,跟本片無關而且**現在就已經錯了**

`home_banners` 的表說明(最新那版在 `20260916180000:76`)逐字寫著:

> 「draft ⇒ published ⇒ archived。**寫入只走 `admin_home_banner_save_draft` / `_publish` / `_archive`**」

而 `20260916250000` 加的 `admin_home_banner_duplicate` **也寫這張表**(它 `INSERT INTO public.home_banners`),
**卻沒有回頭更新那句話。**

⇒ 📌 **那句話今天就是假的,不是等這一片做完才變假的。**
⇒ 這正是本 plan 下面〈一併改表的說明〉那節在講的病,**而它已經發生了一次而沒有人叫**
(靜態閘不看 COMMENT 與函式清單一不一致)。
⇒ **不管上面那題答甲乙丙,這一格都該修** —— 它是獨立的一行 `COMMENT ON TABLE`。

---

## 一句話

⛔ ~~首頁大圖按了「封存」之後,**現在一顆鈕都沒有、永遠拿不回來**。~~ ← **§0 已推翻**
加一個「重新開成草稿」,把它放回草稿區,要重新發布得再走一次原本的發布檢查。

## 現在是什麼情況(逐字查過,不是印象)

> ⛔ **以下整張表是 2026-09-16 的讀數,第 1 / 3 / 4 列在 2026-09-18 已被推翻(見 §0)。舊字面不刪。**

| 事實 | 在哪裡 |
|---|---|
| 資料庫只有**三個動作**:存草稿 / 發布 / 封存,**沒有回頭的路** | `20260916180000` 裡只有 `admin_home_banner_save_draft` / `_publish` / `_archive` 三支 |
| 想用「存草稿」把它改回去 ⇒ **資料庫當場擋掉** | `save_draft` 逐字 `IF v_before.status <> 'draft' THEN RAISE EXCEPTION '只有草稿可以修改(這張是 %)'` |
| 表上自己寫著狀態是**單向**的 | 表的說明逐字:「draft ⇒ published ⇒ archived」 |
| 畫面上封存後**沒有任何鈕** | `home-banner-editor.tsx:240` 逐字 `banner !== null && state !== 'archived'` ⇒ 封存的列連「下架」鈕都不畫;`:247` 的「存草稿」只給草稿 |

⇒ ⛔ ~~今天**真的是死路**,不是「藏起來了」。~~ ← **§0 已推翻:今天不是死路,是繞路。**

## 為什麼值得做

「封存」是一按就好、不問第二次的動作,而且**所有在職員工都能按**(Sean 09-16 Q5 乙 + 主視窗「發得出去就要收得回來」)。
⇒ ⛔ ~~**按錯的人自己救不回來** —— 他得重新建一張、重新填字、重新等圖。~~ ← **§0 已推翻**
⇒ 而「掛得上去、拿不下來」我們已經避開了;它的反面(**拿得下來、放不回去**)同樣是個單向門。
⇒ 🔵 **2026-09-18 的說法**:那個門今天被「複製」繞過去了,剩下的是〈§0 那四格差距〉。

## 要動哪三處

### ① 資料庫:新增**一支**動作 `admin_home_banner_restore_draft`

只做一件事:把 `archived` 的那一列改回 `draft`。**不收任何內容參數、不碰任何文字或圖片欄位。**

骨架逐字照抄現有的 `admin_home_banner_archive`(同樣三個參數 `p_banner_id / p_actor / p_request_id`):
- 權限:`staff.is_active`(在職就可以,理由見下面〈權限〉)
- **冪等**:本來就是草稿 ⇒ 回 `changed:false`,不報錯、不寫第二筆稽核(照 `_archive` 對已封存的那招)
- 寫一筆 `admin_audit_log`,action 叫 **`home_banner.restore`**
- `SECURITY DEFINER` + `SET search_path = ''`,兩道 `REVOKE`(`FROM PUBLIC, anon, authenticated`)+ `GRANT EXECUTE ... TO service_role`(房規 `docs/patterns/revoking-function-execute-in-supabase.md`:少一道都是開的)
- 後置閘照 `20260916180000` 那一段,把新函式加進 `v_functions` 陣列一起驗(anon/authenticated 不可執行、service_role 要能執行、`search_path` 是空字串、是 SECURITY DEFINER)

### 🔴 為什麼是**新增一支**,不是把「存草稿」放寬

| | 新增一支(**建議**) | 放寬 `save_draft` |
|---|---|---|
| 那道 `status <> 'draft'` 的擋 | **原封不動** | 要拆開 —— 而它正在保證「已發布 / 已封存的內容不會被人偷偷改掉」 |
| 能碰到什麼 | **只有一個狀態欄位**,收不到任何內容參數 | 同一支要同時管「改內容」與「換狀態」,多一個分支;那個分支寫錯,**已發布的列也可能被改到** |
| 稽核查得到嗎 | `home_banner.restore` 自己一筆 ⇒ 查「這張被復原過幾次」一個動作名就夠 | 混在一堆「改了字」的 `save_draft` 裡,分不出來 |

⇒ **建議新增一支。** 那道擋是承重的,不該為了省一支函式去動它。

### 🔴 `archived_by` / `archived_at` 復原時怎麼辦 ⇒ **留著**

**決定:留著。理由是【歷史事實不該因為復原而消失】** —— 「誰在什麼時候把這張圖下架過」,是之後要查「這張圖為什麼消失過」的唯一線索。

🛑 **而以下這件事【不可以】拿來當理由**:我查過 `archived_by` 在程式碼裡**零命中**、`archived_at` 有被撈出來但沒有任何一處真的在用。
📌 **「今天沒人讀」與「不該留」是兩句話,而它們在 grep 的輸出上長得一模一樣。** 那個零命中只證明今天沒人讀,證明不了可以丟。**下一個人不要拿它當依據。**

🟢 技術上也不卡:`home_banners_archived_shape_check` 逐字是 `status <> 'archived' OR (archived_by IS NOT NULL AND archived_at IS NOT NULL)` ⇒ **它只管 archived 那一態** ⇒ 改回 draft 之後那兩欄留著**不違反任何約束**。

### 🟢 復原之後仍然要過原本的發布檢查 —— **自動成立,不用多做東西**

而且要寫清楚**為什麼**自動成立,不是只寫結論:

那三條把關的 CHECK —— `home_banners_published_shape_check`、`home_banners_mail_published_needs_match`(信件來的要配到商品)、`home_banners_published_link_scope`(連結要指到站內商品頁)——
**每一條的條件都寫成「`status <> 'published'` 或 (…)」** ⇒ 它們**只在那一列是 published 的時候才有意見**。
⇒ ① 復原成 draft ⇒ 三條都自動成立 ⇒ **復原本身不會被擋**
⇒ ② 之後要再發布 ⇒ 那一列又變成 published ⇒ **三條原封不動地再擋一次**
⇒ 📌 **「復原」這條路沒有繞過任何一道發布閘,而我們一行程式都不用寫。**

### 🔴 同一支 migration 要**一併改掉表的說明** —— 不然它會開始說謊

表 `home_banners` 的 COMMENT 逐字寫著狀態是 **「draft ⇒ published ⇒ archived」**。
⇒ 這一片做完之後,**那句話就是假的**(它會有一條回頭的路),而它是下一個人查狀態機時第一個讀到的東西。
📌 這不是龜毛:`20260916180000-rollback.sql` 自己 `:13-14` 就記著同一件事 —— 逐字「不然目錄註解會繼續寫著新世界的規則,而行為是舊世界(**讀註解的人會被騙**)」。
⇒ 新 migration 一併把表的 COMMENT 改成 `draft ⇒ published ⇒ archived ⇒(可復原回 draft)`,並把新函式寫進那句「寫入只走 …」的清單。

### ② 網站程式:兩層,各一小段

- `home-banner-repository.ts`:加 `restoreHomeBannerDraft()`,逐字照 `archiveHomeBanner()`(打 RPC、錯就 throw、回傳形狀不對也 throw)
- `home-banner-actions.ts`:加 `restoreHomeBannerDraftAction`,逐字照 `archiveHomeBannerAction`(`authorizeAdminMutation` → 解析 id → 記 attempt → try/catch 分類錯誤 → `revalidatePath` → 導回)
- `home-banner-constants.ts`:結果代碼加 `restored`(文案:「已重新開成草稿。」)與 `nochange` 的第二種說法
  ⚠️ 現有 `nochange` 的文案逐字是「**這張本來就已經封存了。**」—— 復原若也用 `nochange`,員工會看到一句**意思相反**的話。⇒ 復原要有自己的 `restore_nochange`(「這張本來就是草稿了。」)
- `home-banner-editor.tsx`:封存狀態時畫一顆「重新開成草稿」
  ⛔ ~~(今天 `:240` 那一行只在 `state !== 'archived'` 畫鈕 ⇒ 加一條 else)~~
  🔴 **2026-09-18 更正**:那個落點今天不對了。鈕區已經重排過,而**封存的列現在已經有一顆鈕**
  (`:312-315` 的「複製一張來改」,無狀態條件)⇒ 要加的是**第二顆並排**,不是「從沒鈕變有鈕」。
  ⚠️ 而兩顆並排會撞出一個新問題,**舊 plan 沒有處理**:員工看到「複製一張來改」與「重新開成草稿」
  兩顆長得很像的鈕,**分不出差別**(一顆保留同一張、一顆開新的)。
  ⇒ 📌 真要做甲,**文案與擺位是這一片最難的部分,不是 SQL。** 那一格要端 Sean 看實體版本,不能用文字讓他想像。
  🔵 而 `:171-172` 那句「要改請按下面的『複製一張來改』」也要一起改 —— 不然它會漏講第二條路。

### ③ 部署順序:**板先貼、碼後推**

新函式 ⇒ 部署時序閘會擋(它認 `.rpc(`)。而且反過來會壞:碼先上 ⇒ 按鈕打一支不存在的函式 ⇒ 員工拿到 PGRST202。
⚠️ **這次沒有改既有函式的簽章** ⇒ 沒有 2026-09-16 板 199 那種「兩個方向都有空窗」的問題;舊碼不受影響(它根本不知道有這支)。

## 不會改變任何現有行為

- 沒有動 `save_draft` / `publish` / `archive` 任何一支
- 沒有動任何 CHECK、沒有加減欄位
- 沒有動前台顧客站(草稿本來就不見客)
- 封存的列在復原之前,畫面與今天**逐字相同**

## 出錯了怎麼退(rollback)

`supabase/rollbacks/<版本>-rollback.sql`:`DROP FUNCTION IF EXISTS public.admin_home_banner_restore_draft(uuid, text, text);`
- **順序**:先 revert 後台那顆(把鈕拿掉),再跑本檔。反過來 ⇒ 員工按那顆鈕會拿到「函式不存在」。
- 🔵 **退得乾淨**:這支不改任何既有資料。已經被復原過的列會停在 `draft`(那是它們現在真正的狀態,不是壞掉),`archived_by` / `archived_at` 仍在 ⇒ 要人工改回 `archived` 的話那兩欄都還在。
- 退完同批跑 `pcm_acl_approve_latest`(`p_note` 帶版本號與「rollback」)。

## 驗收(怎麼知道做對了)

1. 封存一張 ⇒ 畫面出現「重新開成草稿」鈕 ⇒ 按下去 ⇒ 它回到「草稿」分頁、文字圖片一個字沒變
2. **負對照**:同一張復原後**直接按發布** ⇒ 信件來的若沒配到商品,**照樣被擋**(訊息逐字「這張還沒配到商品,配到商品才能發布」)
3. **負對照**:對一張**本來就是草稿**的按復原 ⇒ 回「這張本來就是草稿了。」,**稽核不多一筆**
4. 復原後去查 `admin_audit_log` ⇒ 有一筆 `home_banner.restore`,而**原本那筆 `home_banner.archive` 還在**
5. 復原後那一列的 `archived_by` / `archived_at` **仍然是原本的值**(不是 NULL)
6. 一般員工(非管理者)按得動;**離職的員工**按不動(拿到「無權執行此操作」)

## 權限:✅ 已定案 —— **所有在職員工都能按**

**Sean 2026-09-16 逐字答:「誰都可以,員工跟我都可」⇒ 甲。**

原本的題目與理由留在這裡當依據(不刪:下一個人要知道這是**選過**的,不是預設值):

```
Q:大圖「重新開成草稿」誰可以按?
  甲 所有在職員工(跟「封存」同一條規則)   ← Sean 選這個
  乙 只有管理者
  理由:封存是所有員工都能按的; 若只有管理者能復原, 按錯的人自己救不回來,
       而他要等的那個人可能今天不在。復原本身不會讓任何東西上首頁(還是草稿)。
```

⇒ 函式裡的權限判準逐字照 `admin_home_banner_archive`:`PERFORM 1 FROM public.staff s WHERE s.id = v_actor AND s.is_active FOR SHARE;` —— **不另立一套**。

⚠️ **這一題答了,不代表整份 plan 批了。** 本檔其餘做法(新增一支 RPC、兩欄留著、一併改表 COMMENT)仍在等 Sean 批,**批之前不寫 migration**。

## 版本號

⛔ ~~預計 `20260916200000`,取號前跑一次 `ls supabase/migrations/ | tail -3` 核對。~~ ← 那一版沒貼,號已作廢。

🔴 **2026-09-18 定案:板號由主視窗指定 = `20260918020000`。窗自己不取號。**

```
supabase/migrations/20260918020000_m4b_home_banner_restore_draft.sql
supabase/rollbacks/20260918020000-rollback.sql
```

🔬 窗 B 2026-09-18 複核無撞號(**兩個路徑分開核**,不是只核 migration):
`git log --all --diff-filter=A --name-only -- 'supabase/migrations/20260918*' 'supabase/rollbacks/20260918*'`
⇒ 全分支只有窗 A 的 `20260918010000`(migration + rollback 各一)⇒ `020000` **兩個路徑都空**。

🛑 **為什麼要分開核那兩個路徑**(2026-09-17 實際踩過,不是假設):
兩支 migration 撞號時**檔名多半不同**(後綴不一樣)⇒ git 不衝突、三道閘都不叫;
而它們的 **rollback 檔名是 `<版本>-rollback.sql`,撞號就是【逐字同一個路徑】**
⇒ `git stash pop` 會靜靜地沒還原(它只說「The stash entry is kept」)。
📌 **「兩支 migration 撞號」與「兩支 migration 的還原檔撞號」是兩件事,只有後者會安靜地蓋掉東西。**

⚠️ **號先留著、不佔用** —— §0 那題答完(甲 / 乙 / 丙)才建檔。
