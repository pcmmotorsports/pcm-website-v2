# `check-anon-grants-prod.sh` 的行為守門 —— plan(**只寫 plan, 本片不動手**)

> 線【退款】`-refund` · 2026-09-08 · 主視窗 `-1a` 派(P1-1 的 ③)。
> 🛑 **`-1a` 交代:碰 anon 權限, 它要先看** ⇒ 本檔**不含任何實作**, 一行碼都沒動。

## §0 🔴 **先讀這段** —— 寫本 plan 時當場量到的, 而它決定了整片的驗收形狀

**三發實測, 全部 `rc=2`, 而它們是三個不同的世界**:
```
bash …check-anon-grants-prod.sh …那支檔          ⇒ 「🔴 用法錯:本腳本不收參數」  rc=2
bash …check-anon-grants-prod.sh                  ⇒ 「用法: read -rs PGURL …」    rc=2
bash -c 'bash …check-anon-grants-prod.sh' _ 那支檔 ⇒ 訊息同上(這就是「吃掉了」的證據) rc=2
```
🛑 📌 **只看 rc 的 selftest, 在【收到參數】與【沒有 PGURL】之間零判別力** ——
一格「餵參數 ⇒ 期望 rc=2」會在**接線壞掉**與**接線修好**兩個世界**同時綠**。

🔴 **⇒ 所以「兩個方向各留一句預期字面」在本片是【必要條件】, 不是建議。**
(它是今天同族第四例;前三例在 `guard-and-instrument-traps.md`
 「兩個方向都餵了, 而兩邊都沒有判別力」那節。)

---

## §1 它現在有什麼(先講這個, 因為它比大多數腳本成熟)

`scripts/check-anon-grants-prod.sh`(197 行)對**正式庫**查 `anon` / `authenticated` 的權限現值。
它**已經有**下面這些, 而這些正是我們平常要求別人補的:

```
✅ rc 是【三態以上】不是布林:0 / 1(工具問題)/ 2(用法錯)/ 6(沒校準表)/ 7(校準不符=發現)/ 8(net 兩表不存在)
✅ 檔頭逐字寫「rc=1 / 6 都不是【查無】—— 不要當成權限已經收乾淨了」
✅ 正向 + 負向【兩個】對照, 而且檔頭寫了**為什麼負向不能單獨用**:
   「admin_audit_log × anon ⇒ 0」在【表被鎖好了】與【表根本不存在】兩個世界印同一個東西
✅ 連線字串走 env 不走 argv(不進 ps / 不進 shell history)
✅ information_schema 會依連線角色過濾而 pg_catalog 不會 —— 第 ④ 段因此改走 relacl/attacl
```

## §2 它缺什麼:**沒有 `--selftest`**

🔬 量到的(P1-1 盤點):
```
內文 --selftest / --self-check ⇒ 0 次
lint-staged 指名它的 glob      ⇒ 0 條(只被 scripts/*.{sh,py} 那四道【形狀閘】掃到)
.husky / CI 接線               ⇒ 查無
⚪ 負對照 現造 scripts/zq7fh3k2m9x-nope.sh ⇒ 同樣 0(⇒ 上面那些 0 不是尺壞掉)
```
⇒ 📌 **它的【行為】沒有任何自動化在守** —— 四道通用閘守的是形狀(shell 方言 / 隱藏 helper / selftest 隔離),
不是「它判得對不對」。

## §3 難處:**它的本體要連正式庫, 而 selftest 絕不能連**

🛑🛑 **紅線(不可協商)**:`--selftest` **在任何情況下都不得連正式庫**。
理由不只是「唯讀所以安全」—— 而是:
```
· 它會被【lint-staged 每次 commit】叫 ⇒ 那等於每次 commit 都對正式庫發查詢
· 而它讀的是【權限現值】⇒ 那是最不該被自動化高頻拉取的東西
· 🔴 且本 repo 今天剛量到:lint-staged 會把 staged 檔名附加到命令後面
  ⇒ 一支「參數解析不嚴」的腳本可能把那個路徑吃成別的意思
```
🛑🛑 **而危險不只是慢(主視窗 `-1a` 2026-09-08 補的理由, 逐字收)**:
> 那條連線會住在一個**每個人每天跑幾十次**的位置, 而**沒有人會再去看它**。
> **一條沒有人在看的正式庫連線, 是最容易被下一次改動悄悄擴權的東西。**

📌 ⇒ 所以紅線的判準**不是「它現在唯讀所以安全」** —— 是**「它會待在一個沒有人看的位置」**。

### 它的七個離場點分三層, 而**只有前兩層不用 DB**

| 層 | 離場點 | 要造什麼世界 | 要不要 DB |
|---|---|---|---|
| A 參數/env | `rc=2` ×2(收到參數 / 沒有 `PGURL`) | 直接呼叫 | ❌ 不用 |
| B 工具 | `rc=1` ×3(`psql` 不在 / `python3` 不在 / `PGURL` 解析失敗) | `PATH` 遮蔽 + 餵壞字串 | ❌ 不用 |
| C 判準 | `rc=0 / 6 / 7 / 8` | 要一個**有表、有 GRANT** 的庫 | ✅ 要 |

## §4 兩個方案 —— **而我判不出高低, 要 `-1a` 拍**

### 甲:只做 A + B 兩層(**不起 PG**)
```
✅ 快(毫秒級)· 零 DB 依賴 · 掛 lint-staged 沒有任何速度風險
🛑 而它【證不到那支腳本判得對不對】—— 它只證「參數與工具那幾條路會照預期離場」
🔴 誠實的標籤要寫死在 selftest 的**輸出**裡:
   「本 selftest 沒有驗任何一條【判準】—— rc 0/6/7/8 四條路一格都沒有跑到」
   🛑🛑 **寫進【輸出】不是【註解】** —— 註解只有讀碼的人看得到,
      而**盤點的尺看的是輸出**(`-1a` 2026-09-08 明點)。
   🎯 **⇒ 而這正是本片 ① 那件的鏡像**:`rollback-drill.sh` 有 4 個 selftest 旗標,
      **而它從來沒真的跑過** —— 盤點看到旗標就把它算成「有守門」。
      ⇒ 📌 **一個只驗參數的 selftest, 會讓下次盤點以為它的判準也被守著。**
```

### 乙:A + B + C(**起一台拋棄式 PG**, 照 `scripts/rollback-drill.sh` 的形狀)
```
✅ 真的驗得到判準:造一張 anon 有權限的表 + 一張沒有的 ⇒ 四個 rc 各跑一次
✅ repo 裡已經有現成的形狀可抄(rollback-drill 的第二層就是這樣做的)
🛑 代價 = 時間, 而那個時間【變異很大】:
   🔬 rollback-drill --selftest 今天四發實測 **2 / 3 / 12 / >120 秒**(load 7.5~10.4)
   ⇒ 📌 變異 60 倍, 而我沒查清楚差在哪
🔴 而 `.husky/pre-push` 檔頭記過那個代價:
   「閘死於誤報與死於太慢一樣常見, 而被關掉的閘等於沒有。」
```

### 🎯 我的建議(而它不是拍板)
**先做甲、把誠實標籤寫死, 同時把乙開成一列**。理由:
```
· 甲今天就能上, 而它至少讓「有人改壞參數解析」會紅
· 而乙的價值全在 C 層, 那一層要的是【一台 PG】而不是【更多格子】
  ⇒ 它可以獨立於甲之後做, 不會白工
🛑 而甲【單獨存在】有一個真風險:它會讓這支腳本在 P1-1 那類盤點裡從「乙1」變成「甲」
  ⇒ 📌 **一個只驗參數的 selftest, 會讓盤點以為它的判準也被守著。**
  ⇒ 所以那個誠實標籤不是禮貌, 是**防止下一次盤點誤判**。
```

## §5 驗收條件(不論甲乙, 這幾條都要)

```
① 雙向驗, 而【兩個方向各留一句預期字面】——
   今天 rollback-drill 那一片踩到:紅了不等於守到、綠了不等於跑過
   🔴 世界一該看到:那一格自己的錯誤訊息(不是別的閘的、不是「不認得的參數」)
   🔴 世界二該看到:[STARTED] … — 1 file + [COMPLETED](不是 SKIPPED / 0 files)
② 接線形狀用 `bash -c '…'` —— 而**這一條我當場驗過了, 不是推論**:
```
bash …check-anon-grants-prod.sh …check-anon-grants-prod.sh   ⇒ 🔴 用法錯:本腳本不收參數  rc=2
bash …check-anon-grants-prod.sh                              ⇒ 用法: read -rs PGURL …     rc=2
bash -c 'bash …check-anon-grants-prod.sh' _ …那支檔            ⇒ 用法: read -rs PGURL …     rc=2
```
⇒ 🔴 直接照 100+ 支的同形寫法掛上去 ⇒ **它必然撞「用法錯」**(第 44 行就擋)。

### 🛑🛑 而這一發撞出一個【比接線更重要】的東西:**`rc=2` 有兩條路, 而它們離場碼相同**

上面三發**全部 rc=2**, 而它們是**三個不同的世界**:
```
① 收到不該有的參數      ⇒ 訊息「🔴 用法錯:本腳本不收參數」
② 沒有 PGURL           ⇒ 訊息「用法: read -rs PGURL …」
③ 附加參數被 bash -c 吃掉 ⇒ 訊息同 ②(⇒ 這就是「吃掉了」的證據)
```
📌 **⇒ 只看 rc 的 selftest, 在①與②之間【零判別力】** ——
而那正是本片最容易寫壞的地方:一格「餵參數 ⇒ 期望 rc=2」會在**接線壞掉**與**接線修好**兩個世界同時綠。
🔴 ⇒ **所以 §5① 那條「兩個方向各留一句預期字面」在本片不是建議, 是必要條件。**
🎯 而它是今天同一族的第四個實例(前三個在 `guard-and-instrument-traps.md`
   「兩個方向都餵了, 而兩邊都沒有判別力」那節)。
③ selftest 自己要有【期望格數釘子】(同 rollback-drill 的 EXPECT_TOTAL)
④ 每一格都要問「哪個世界會紅」—— 沒有答案的格子不要加(它只會讓 PASS 計數變飽)
```

## §6 要 `-1a` 拍的兩題

```
~~Q1 甲 / 乙 / 甲+開乙那一列 ?~~
🟢 **[2026-09-08 主視窗 `-1a` 已裁:甲 + 把乙開成一列]**
Q2 若走乙:那台拋棄式 PG 要不要與 rollback-drill 共用同一套 bootstrap?
   (共用 ⇒ 少一份會漂的複本;不共用 ⇒ 兩支互不影響)
```

## §7 本 plan 證不到什麼

```
· 沒有讀完那 197 行的每一條 SQL —— 我讀的是【七個離場點與它們的條件】
· 沒有實跑過它(它要 PGURL, 而我今晚沒有那條路的授權, 也不該為了寫 plan 去連)
· 沒有查它在別的 repo(報價單庫)那條路的現況 —— 檔頭說那需要另一組校準表
```

## §8 ✅ **甲案做完了(2026-09-08 · `-refund`)—— 而實測推翻本 plan 兩處**

### ① 🔴 **`§5②` 那句「直接照同形寫法掛上去 ⇒ 必然撞用法錯」——【做完之後不成立】**
```
⛔ ~~所以接線要用 bash -c '…' 包起來~~
✅ 實測:掛同族形狀 `bash scripts/check-anon-grants-prod.sh --selftest`,
   再模擬 lint-staged 把 staged 檔名附加在後面 ⇒ **兩份輸出 diff 完全相同, 5/5 全綠**
🔵 為什麼變了:那句寫於【還沒有 --selftest】的時候 —— 那時 `$1` 就是檔名 ⇒ 撞第 44 行的閘。
   現在 `$1` 是 `--selftest` ⇒ 進 selftest 分支, 多餘的 `$2` 被忽略。
📌 **⇒ 一個正確的觀察, 在它描述的那個東西被改掉之後就過期了。**
```

### ② 🔴🔴 **寫 selftest 的過程中撞到一個 plan 沒預見的【真 finding】, 已修**
```
🔬 舊碼 `eval "$(python3 - <<'PY' … )" || { echo "PGURL 解析失敗"; exit 1; }`
   python 拋錯 ⇒ stdout 空 ⇒ `eval ""` 的 **rc 是 0** ⇒ **那個 `||` 從來不會觸發**
🔬 實測 PGURL='postgres://h:notaport/db' ⇒ 印的是
   「psql 沒跑起來或連不上」**加上整段 Python traceback**(而 traceback 可能帶 URL 片段)
🎯 而它**碰巧仍然 rc=1** —— 後面 psql 沒有 PG* env 就連不上, 掉進另一個離場點
   ⇒ 📌 **對的 rc 配一個完全錯的理由** ⇒ 這正是 §0 那個病的實例, 而 §0 是我自己寫的
✅ 修法:先把輸出收進變數(`$(…)` 的 rc **就是** python 的 rc)⇒ `||` 真的會走。
🔴 ⛔ ~~而 `2>&1` 順便讓 traceback 進變數而不是噴給使用者~~ —— **那句是錯的, 照它做會把 bug 裝回去**
   (R2 must-fix):`_PGENV` **下一行會被 `eval`** ⇒ `2>&1` 讓 **stderr 變成 shell code 被執行**。
   ✅ 正解 = `exec 3>&2` 開在命令替換**之前**, 內部寫 `2>&3` ⇒ stderr **到終端、不進變數**。
⇒ 📌 **B3 那一格在今天之前【寫不出有判別力的版本】** —— 那條路名存實亡。
```

### 交付與驗收(逐條)
```
✅ §5① 兩個方向各留一句預期字面 —— 每格比【rc + 一句只有那個世界才會印的字面】兩個數
✅ §5③ EXPECT_TOTAL=5 釘子(實跑格數 != 5 ⇒ rc=2「量具失效」, 不是 PASS)
✅ §5④ 五格都答得出「哪個世界會紅」—— **六發突變逐格證(見下)**
   ⛔ ~~R1 交付時這裡寫「突變逐格證」而實際只有 2/5 格~~ —— code-reviewer 抓到,
      鐵則 11 字面 vs 事實。**舊字面留刪除線**:它是我自己寫的驗收條件, 而我自己寫成了做到。
✅ §4 甲的誠實標籤**寫進輸出**(不是註解):「本 selftest 沒有驗任何一條判準」
✅ 接線 package.json lint-staged(164 ⇒ 165 條;json.loads 回核 —— 三綠不 parse 任意 JSON)
```

### 🛑 紅線「selftest 絕不連正式庫」—— **量到的, 不是宣稱的**
```
量具:一個會把每次呼叫寫進 log 的假 psql, PATH 指過去
  世界一  --selftest              ⇒ psql 被叫 **0** 次
  🟢 世界二 本體 + 合法 PGURL      ⇒ psql 被叫 ⛔ ~~**2**~~ ⇒ **1** 次 ⇒ **這把尺量得到**
     🔴 **那個 2 是【行數】不是【呼叫次數】**(R3 抓到, 我複量確認):我的 shim 寫
        `echo "CALLED $*"` ⇒ `$*` 把 SQL 的換行一起寫進去 ⇒ `wc -l` 得 2 而 `grep -c CALLED` 得 1。
     📌 結論(0 vs ≥1 量得到)**仍然成立**, 而**數字是錯的** —— 結論對的時候沒有人查數字。
```
⚠️ **我第一版量的是「輸出裡有沒有連線字樣」⇒ 那只證明【沒印出來】, 不證明【沒發生】。**
   📌 換成 shim 才是「兩個世界印不同的東西」。

### 🔴 而 R1 之後那條紅線從【紀律】變成【機制】(code-reviewer nit ⇒ 機制優先律)
```
問題:紅線原本只靠「每一格自己記得覆蓋 PGURL / PATH」, 而 EXPECT_TOTAL 只抓格數變動
     ⇒ **抓不到「未來新增的第 6 格忘了覆蓋 PGURL」**。
修法:每個 probe 的 PATH 前置一支 guard psql —— 被執行到就印「SELFTEST 越線」+ rc=97。
⛔ ~~🟢 正對照:我造了一個故意越線的第 6 格 ⇒ 越線警報 1 次、該格紅~~
🔴 **那個對照不算數**(R2 nit 4):那一格**本來就會為別的理由紅** ⇒ 它證的是
   「紅的時候看得到那句話」, **不是「越線一定被抓」**。真正的對照見 §8-R2 ②。
⚪ 而正常五格 ⇒ **越線警報 0 次**
⇒ 📌 兩個世界印不同的東西 ⇒ 那個 0 是量到的, 不是「沒印出來」。
```

### 突變(每發只退一處 · 在**副本**上做, 工作樹那支一個字沒動 ⇒ 零還原風險)
```
M0  不改                            ⇒ 🟢 綠 5/5  (基線)
M1  只改 A1 的【訊息文字】, rc 不動    ⇒ 🔴 紅 A1   ⇒ **字面那一半有咬合力**
M2b 只改 A1 的【離場碼】, 文字不動     ⇒ 🔴 紅 A1   ⇒ **rc 那一半有咬合力**
M3  把今天那個 || 退回舊寫法, rc 仍 1   ⇒ 🔴 紅 B3   ⇒ **今天那個修有咬合力**
A2  把 PGURL 空值閘改成 `if false`    ⇒ 🔴 紅 A2
B1  拿掉 `command -v psql` 那道檢查    ⇒ 🔴 紅 B1
B2  拿掉 `command -v python3` 那道檢查 ⇒ 🔴 紅 B2
⇒ ✅ **五格各自都有一發專屬的紅** —— 沒有恆綠的格子。
🔵 **後三發的來源要說清楚**:是 code-reviewer R1 先替我補跑的(我那時只跑了打 A1/B3 的三發),
   而**我自己複量了一次**(每發斷言錨命中 == 1)⇒ 三發結論一致。
   📌 **我不是引用他的讀數, 是自己又量了一次** —— 別人交來的東西沒有人複量, 就沒有人量過。
⛔ M2(作廢)—— 我用 `s.index('本腳本不收參數')` 定位, 而那個字串在檔裡有 **2 次**
   (`:95` 是 selftest 自己的期望字面)⇒ 改到 `:124` 一條**沒有人走的路** ⇒ **全綠。**
   📌 M1/M3 我斷言了「錨命中 == 1」, 偷懶的那一發就出事 ——
      **一發沒套用到的突變, 印的東西與「守門抓不到」完全一樣。**
```

## §8-R2 🔴🔴 R2 覆審 FAIL —— 而它揭露的東西比 R1 大

> R1 三個 must-fix 修完之後跑的第二輪。**R2 又 FAIL, 三個新的 must-fix, 而它們我一個都沒想到。**

### ① UTF-8 locale 下 bash 把「」的首位元組吃進變數名 ⇒ `set -u` 當場離場
```
🔬 `$want_str」` ⇒ 變數名被讀成 `want_str\xe3` ⇒ shell **死在那一行**
⇒ 後面的格子不跑 · 結算那行不印 · **EXPECT_TOTAL 那道量具失效閘也不跑**
🎯 而我的六發突變**全部在 `LANG` 未設(= C)跑** ⇒ **整族看不到。**
✅ 修 `${want_str}` · 🟢 雙向實測(同一份檔、同一格紅、只差這個寫法):
     修後 UTF-8 ⇒ **22 行**, 結算行在, 5/5 跑完
     退回   UTF-8 ⇒ **5 行**, 結算行**不見**, 後面的格子沒跑
📌 **⇒ 一個只在某個 locale 成立的死法, 而我整晚都在另一個 locale 裡量。**
```

### ② 那道 guard 原本【完全沒有咬合力】—— 而修它花了三次才對
```
R2 的 finding:guard 的警報寫 stderr ⇒ 被 _probe 的 2>&1 收進 $out ⇒ **綠的那格整段丟掉**
   ⇒ 一格「真的越線而 rc 與字面【仍然相符】」= 完全靜音
🔴 我照它建議的「檢查 $out 有沒有那句話」做了 ⇒ **當時沒生效**。
⛔ ~~而我把成因寫成「本體的 run() 是 psql … 2>&1 包在命令替換裡 ⇒ 那句話連 $out 都到不了」,
   並從它推出一條通則:「一個訊號能不能被讀到, 取決於它走哪一條管路 —— stdout/stderr
   會被沿路重導, 檔案不會。」~~
🛑🛑 **那個因果是錯的, 而那條通則是從錯的觀察推出來的**(R3 抓到, 我自己複驗):
```
🔬 R3 實測:stderr **到得了** $out —— run() 吞進 EXIST 之後, 下面那行
   `echo "$EXIST" | head -3 >&2` 又把它原封吐回 stderr, 再被 2>&1 收進 $out。
🔬 我複驗真因:我第一版測的時候**腳本正壞著**(`3>&2` 那個 Bad file descriptor)
   ⇒ 解析永遠失敗 ⇒ **那一格根本走不到 psql** ⇒ guard 沒被叫 ⇒ **沒有訊號可讀**。
   把第一版的 $out 版 veto 放回【修好的】腳本上跑同一格 ⇒ **它抓到了**(rc=1 · 失敗 1)。
```
⇒ 🎯 **我把「那格根本沒越線」誤診成「訊號路徑被吞掉」, 再從誤診推出一條聽起來很對的通則。**
   📌 **那條通則不准搬進 memory / traps** —— R3 逐字點名。它可能在別的情境成立,
      而**這一次它零讀數**。
✅ 檔案版 veto **仍然保留**, 而理由換成真的:`$out` 版**依賴** `echo "$EXIST" | head -3 >&2`
   那一行剛好把 guard 的話吐回來 —— **那是巧合不是機制**, 那行改了它就失效。檔案不依賴任何人的重導。
🟢 真正的兩個世界(造一格越線而 rc 與字面都相符):
     無否決 ⇒ **rc=0 · 6/6 全綠**   ← 越線完全靜音
     有否決 ⇒ **rc=1 · 失敗 1**     ← 抓到
⚠️ 我前兩次的「對照」都不算數:一次那格為別的理由紅(R2 nit 4 點名的錯)、
   一次續行被我多逸出一層 ⇒ `rc=127` 那格根本沒跑。
```

### ③ 🔴🔴 **我為了修 R2 的 nit6, 把整支腳本弄壞了** —— 而 selftest 五格全綠
```
我寫 `_PGENV=$(python3 … 2>&3) 3>&2` ⇒ 那個 `3>&2` **不會套進命令替換內部**
⇒ `line 225: 3: Bad file descriptor` ⇒ **解析永遠失敗** ⇒ 整支腳本再也連不上任何庫。
🟢 對照 HEAD 版同一個輸入 ⇒ 正常走到 psql。
🎯 而 `--selftest` **五格全綠** —— 因為那五格**一格都沒走成功路**。
📌 **⇒ 這是同一夜第二次「我修一個東西的同時弄壞它, 而守門全綠」**(第一次是 `2>&1` 那個)。
   而兩次的形狀一模一樣:**壞掉的那條路, 沒有任何一格在走。**
✅ 正解 `exec 3>&2` 開在命令替換之前;雙向實測:
     合法 PGURL ⇒ 走到 psql(`Bad file descriptor` **0** 次)
     壞 PGURL   ⇒ rc=1 · traceback 在終端(診斷保住)· `command not found` **0** 次(eval 沒被汙染)
```

### 🎯 而那道 guard 當場證明了【另一個 bug 比我說的更嚴重】
```
M3 突變(把 `||` 退回舊的 `eval "$(…)"` 寫法)⇒ **B3 那格【越線】**, 一票否決抓到:
   🔴🔴 B3 … — **越線**:這一格真的叫到了 psql。
🔬 為什麼:舊寫法下 python 拋錯 ⇒ stdout 空 ⇒ `eval ""` rc=0 ⇒ **不離場** ⇒ 往下走到 `run()`
⇒ 📌 **一個壞掉的 PGURL 不會停在「解析失敗」, 它會【繼續往下去連庫】。**
⇒ 而 R1 我只說它「訊息說錯原因」—— **那個描述比事實輕。**
🔵 揭露它的是 R2 建議的那道 guard ⇒ **一道守門的價值不只在它擋的那件事。**
```

### 最終狀態(每一項都當場量過)
```
正常五格   C locale 5/5 綠 · UTF-8 locale 5/5 綠(兩個都跑了, 因為 ① 教我的)
六發突變   M1→A1 · M2b→A1 · A2→A2 · B1→B1 · B2→B2 · M3→B3(越線否決)
           ⇒ 五格各自都有專屬的紅
紅線       造一格真越線 ⇒ 無否決 6/6 全綠 · 有否決 rc=1 失敗 1
三綠       typecheck / lint / build 全 rc=0 且 `0 cached`
```

## §8-R3 🔵 R3 換模型換角度(adversarial-reviewer / fable)⇒ **PASS-WITH-NITS · 0 must-fix**

> 照 PCM 紀律「第 3 輪起必須換角度、換模型 —— 同模型再審傾向在同一框架內找更細的問題」。
> 我在 prompt 裡**明講前兩輪驗過什麼、要它不要重做**, 並要它質疑框架。
> 🎯 **結果:它抓到的三條裡有兩條是【我寫的紀錄失真】, 一條是框架缺口。**

### 🔴 F3(它標 nit, 而**我認為它是本輪最重要的一條**)—— 見上面 §8-R2 ② 的訂正
我寫的因果錯了, 而**結論碰巧對**(檔案版 veto 確實該留)⇒ 沒有人會去查理由。
📌 這正是 memory `feedback_a-right-conclusion-shields-a-wrong-reason` 的實例。

### 🔴 F2 —— 「psql 被叫 2 次」實際是 **1 次**(見上面訂正)
我數的是 `wc -l` 的**行數**, 而 `$*` 把 SQL 的換行一起寫進去了。

### 🛑 F1(consider · **要 `-1a` 拍一句話, 我不自己決定**)
```
守門對【到達 psql 前那一段】(:215-234 fd3 + 解析 + eval)**全盲**。
🔬 R3 實測:把 R2-③ 那個壞法放回去 ⇒ 真跑 Bad file descriptor · rc=1 · psql 到達 0 次,
   而同一支檔 `--selftest` 印 **格數 5/5 · 通過 5 · 失敗 0**。
🎯 **今晚兩次自傷都在那一段, 而 lint-staged 兩次都會綠。**
✅ 而它**不需要 DB 就守得住**:一支【錄音 shim psql】(印 $PGHOST/$PGDATABASE 後 exit 1)
   配合法 dummy URL ⇒ 正常版得 rc=1 + `REACHED_PSQL host=… db=…`;壞版得 0。
🛑 **而這撞到紅線的【字面】**:碼裡把紅線實作成「叫到任何名為 psql 的檔 = 越線」,
   而 `-1a` 給的**理由**是「一條沒有人在看的正式庫連線」—— 錄音 shim **不開任何 socket**。
⇒ 📌 **要 `-1a` 答一句**:「錄音 shim 算不算越線?」
   · 不算 ⇒ 補第 6 格(veto guard 與錄音 shim 分目錄、依格宣告)
   · 算   ⇒ 那就**不能**再說「要驗那段得起一台 PG」, 標籤要改
🔵 R3 的 would-change-verdict 逐字:若裁「算越線」**且**標籤不改 ⇒ **它改判 FAIL**。
```

### 🔵 F4 已修 —— 誠實標籤原本在【盤點的尺看不到的地方】
P1-1 那類盤點是 `grep` 檔案字面與 `package.json`, **它不執行 selftest**
⇒ 只寫在輸出裡的標籤它看不到 ⇒ ✅ **已補進檔頭**(`grep` 得到的位置)。

### ⚪ F5 已修, 而**它的必要性我構造不出來**(誠實標:未證實)
```
R3 說 guard 裡 `>> $_CROSSED` 沒加引號 ⇒ TMPDIR 含空白時 append 失敗、guard 靜默失效(它標 likely)。
✅ 我加了引號(那本來就是對的 shell 寫法, 零代價)。
⛔ 而我**構造不出那個世界**:macOS 的 `mktemp -d` **不吃 `TMPDIR`**
   (實測 `TMPDIR="/tmp/有 空白/" mktemp -d` ⇒ 仍然回 `/var/folders/…`)
   ⇒ 我那一發「兩個版本都抓到」**不是對照組成立, 是對照組沒造出那個世界**(我今天第三次)。
⇒ 📌 **修保留, 而我不宣稱證明了它的必要性。**
```

### ⚪ F6(寫下來當乙那一列的邊界, 本片不做)
`_probe` 的字面比對、`EXPECT_TOTAL` 閘、越線 veto **三者都沒有【常駐】正對照**
(veto 的正對照是我手動造第 6 格, 一次性)。`_probe` 若退化成恆真(例:`want_str` 傳空)
⇒ **五格恆綠而沒有人會發現**。與 `rollback-drill` 同構, 不是新病。

### 🔵 R3 對「這片該不該存在」的答覆(它的原話重點)
> 方向沒錯、碼是對的, 而**這一片守到的是本來就會大聲失敗的路,
> 它漏掉的正是今晚兩次真的壞掉、而且【不需要 DB 就能守】的那條路。**

## §9 ⚪ 甲案**做完之後**仍然證不到什麼(**乙案那一列的理由**)

> ⚠️ 與 §7 不同層:§7 說的是**寫 plan 當下**沒查的;本節說的是**碼交付之後**仍然沒守的。
```
· rc 0 / 6 / 7 / 8 四條【判準】路一格都沒跑到 ⇒ 這支腳本【判得對不對】仍然沒有守門
· 沒有連過任何真的庫(紅線, 刻意)⇒ 校準表在不在、relacl 讀得對不對, 都沒驗
· 沒有驗 CALIB_YES / CALIB_NO 那條報價單庫的路
```
