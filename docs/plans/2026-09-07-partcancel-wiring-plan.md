# ⟦b4-PARTCANCEL1⟧ 接線那半 —— plan(**不寫 SQL**)

> 線【資料】`-db` 2026-09-07 · 主視窗 `-f1` 派(原話:「trigger 那半 … 出 plan 給我(鐵則 12③), 不先寫 migration」)
> 🔴 **本檔【不重複】金額那半** —— 那一半在 `docs/plans/2026-09-05-op7-partial-cancel-refund-fn-plan.md`(207 行, 線 `-account` 寫)。
> 本檔只答一件事:**那支函式算出來的數字, 要靠什麼東西被叫起來。**

---

## §0 先講三件「已經有人做掉了」的事 —— 免得我重寫一遍別人的功課

主視窗給我的三個切入點, **有兩個既有 plan 已經答完了**。照實寫, 不假裝是我查到的:

| 我被交辦時以為要查的 | 實際狀態 |
|---|---|
| 運費口徑要不要問 Sean | ✅ **已拍且已落 plan** —— 他選 **乙**(照規則補收), 而 09-05 plan `§83` 逐字寫著「他選了我推薦的【反面】」。🔴 **過期的是【板列】不是 plan**:板列 1003 現在仍寫「已寫成一字題交主視窗進佇列(推薦甲)」⇒ **本片要順手更正那一段。** |
| `discount_total` 怎麼攤 | ✅ **已列為未解** —— 09-05 plan `§173` 第 ⑤ 條逐字「部分取消之後折扣要不要跟著縮是**另一個沒人答過的口徑題**, 本 plan 明列為未解」。⇒ 我**沒有**新發現, 只是複驗它仍然未解。 |
| 接線點在哪 | 🟡 **半答** —— 09-05 plan `§5` 論證了「**不能只加一個 trigger**」而它的射程**只涵蓋既有那一支**(見 §1)。 |

📌 **⇒ 本檔真正新增的只有 §1-§4。** 其餘請直接讀 09-05 那份。

---

## §1 🔴 09-05 plan 的「不能只加 trigger」**是對的, 而它沒有涵蓋我要提的那一種**

它的原話與依據(逐字複驗過, 成立):
- 既有 `pcm_pending_refund_on_cancel()` 掛的是 `OLD.cancelled_at IS NULL AND NEW.cancelled_at IS NOT NULL`
  (`20260901080000:379`, trigger 宣告 `:495-497`)⇒ **部分取消不寫 `cancelled_at` ⇒ 它永遠不會醒。**
- `pcm_pending_refund_open_for` 第一件事是 `IF v_cancelled_at IS NULL THEN RETURN`
  ⇒ **直接叫它也沒有用 —— 它會安靜地什麼都不做、回 void、沒有錯誤。**

🛑 **而那兩條講的都是【既有那一支 trigger / 那一支函式】** ——
它們**沒有**論證「在**另一張表**上新加一支 trigger」行不行。⇒ 那正是本檔要補的那一格。

---

## §2 我提的第三條路:**在 `order_cancellation_items` 上加 statement-level AFTER INSERT trigger**

### 🔬 它成立的三個事實(全部是開檔量到的, 附行號)
1. **部分取消【一定】會寫那張表**:`admin_cancel_order` 最新代
   (`20260830020000:115` 起)`:502` 逐字
   `INSERT INTO public.order_cancellation_items (cancellation_id, order_id, order_item_id, cancelled_quantity) SELECT …`
2. **一次取消 = 一發 INSERT**(不是逐列):部分那支 `:502-505` 與整單那支 `:507-513` **都是單一 `INSERT … SELECT`**
   ⇒ 🔵 **statement-level trigger 每次取消【恰好】醒一次** —— 不必自己去重, 也不會 N 列跑 N 遍。
3. **寫入順序是量到的**:`:497` 先寫 `order_cancellations` ⇒ `:502/:507` 寫品項
   ⇒ `:525` 才算 `v_closed` ⇒ `:532-533` 才 `UPDATE public.orders SET cancelled_at = pg_catalog.now()`。
   ⚠️ **行號的一個自踩, 記在這裡**:我第一版寫的是 `:383 / :388 / :411 / :418`——
   那是我用 `awk 'NR>=115 && NR<=560'` 切出來之後**視窗內的相對行號**, 不是檔案行號(差 114)。
   ⇒ 📌 **一個相對行號長得跟絕對行號一模一樣, 而它會把下一個人送到錯的地方。**
   ⇒ 已用 `grep -n <整支檔>` 重量一次訂正。

### ✅ 它比 09-05 §5 提的「在 A8a2 裡明呼一次」好在哪
09-05 §5 的接法要 **`CREATE OR REPLACE` 那支 445 行的 `admin_cancel_order`**。
🔴 **而【不要那樣做】是 `20260901080000:50-56` 自己寫下的理由**,逐字:
「`CREATE OR REPLACE` 整支 = 回捲風險(把後幾代的行為整個倒回去,**而三綠不會紅**)」。
⇒ 📌 **兩份 plan 在這一點上是牴觸的, 而牴觸點沒有被任何一份指出來。** 本檔指出來。
✅ 另外:掛 trigger 就自帶 `20260901080000:57-58` 想要的那個開關 ——
`ALTER TABLE public.order_cancellation_items DISABLE TRIGGER …`,**不必重新部署**。

### 🛑 它的代價(先講, 不要等審查抓)
1. **整單取消會走到兩條路** —— 新 trigger(在 `:502`)先醒, 既有 trigger(在 `:533`)後醒。
   ⇒ **必須先答:第二次進來時 `ON CONFLICT (order_id, rail) WHERE voided_at IS NULL AND settled_at IS NULL`
   (`20260901080000:449`)是 `DO NOTHING` 還是 `DO UPDATE`?** 兩者的行為完全不同, 而**我還沒讀那一段**。
   ⇒ 這是 §3 第一題。
2. **新 trigger 醒的那一刻 `orders.cancelled_at` 還是 NULL**(順序見上, `:533` 在 `:502` 之後)
   ⇒ **金額函式若讀 `cancelled_at` 判「是不是整單」就會判錯** ⇒ 它必須改用
   「剩餘未取消數量 = 0」當判準。⚠️ 這一條要與 09-05 plan `§2b` 的式子對一次, **我還沒對**。
3. **整單取消時「剩餘應收」不可以套運費規則** —— 全部取消掉之後剩餘小計 0,
   而「未滿 5,000 收 100」會算出**運費 100** ⇒ 退款額少 100。
   ⇒ 式子要有「剩餘數量 = 0 ⇒ 剩餘應收 = 0」那一支。⚠️ **我沒有查 09-05 §2b 有沒有這一支**, 列為 §3 第三題。

---

## §3 🔴 落地之前**必須先答**的三題(我不猜, 而它們不全是 Sean 的題)

| # | 題 | 誰答 | 為什麼不能猜 |
|---|---|---|---|
| ① | 同一張單**第二次**部分取消時, `order_pending_refunds` 那一列要**覆寫**還是**保留第一次**? | **人**(讀 `20260901080000:44x` 那段就答得出來一半;語意那一半要 Sean 或主視窗) | 09-05 plan `§282` 逐字「**覆寫的語意沒有人定義過**」。猜錯的形狀 = 客人的第一筆待退款**靜靜消失**。 |
| ② | 折扣 `discount_total` 在部分取消後怎麼攤 | **Sean** | 09-05 plan `§173⑤` 已列未解。⚠️ 券系統**已經上了**(`20260829150000` 等 3 支)⇒ 這一欄**不再恆為 0**。 |
| ③ | 整單取消(剩餘數量 0)時剩餘應收是不是**恆 0**(含運費) | **人**(對 09-05 §2b 的式子核一次即可) | 若式子照跑運費規則 ⇒ **每一張整單取消的單都少退 100**, 而它不會叫。 |

🛑 **①③ 兩題【不必等 Sean】** —— 它們是「去讀那段碼」而不是「要一個口徑」。
📌 **而把它們排進【等拍板】那一堆的代價, 板列 1003 自己記過**:
「分類錯的代價是它會被排進【等拍板】那一堆, 而那一堆沒有人會去做。」

---

## §4 落地順序(**本片不寫任何一行 SQL**)

```
0  更正板列 1003 那段過期字面(運費已拍乙 ⇒ 不是「已交佇列等 Sean」)   ← 純 .md, 可立刻做
1  答 §3 ①③(讀碼, 不必等人)                                          ← 下一個 slice
2  §3 ② 端給 Sean(一個字回得完:折扣要不要跟著縮)                     ← 走佇列
3  才寫 migration:①金額函式補「剩餘應收」②新 trigger ③ open_for 的閘
```

## §5 誰審 · 要誰批
- **鐵則 12③(DB 結構)** ⇒ 寫 migration 那一片要 codex 對抗審查, **不降級**。
- **鐵則 8**:跨 3+ 檔 + 動函式與 trigger ⇒ **plan 要 Sean 或主視窗批准才實作**。本檔就是那個 plan 的前半。

## §6 我**沒有**做的(不要讀成已查)
- 沒有讀 `20260901080000:44x` 的 `ON CONFLICT` 動作是 `DO NOTHING` 還是 `DO UPDATE`(§3①)。
- 沒有把 09-05 plan `§2b` 的式子逐項核過(§2 代價 2、3 都掛在它上面)。
- 沒有查 `tax_total` 在部分取消後怎麼縮(09-05 plan `§9` 已列未解, 我只是複驗它還在)。
- **沒有動任何一行碼, 也沒有建任何 migration。**
