# plan · #906 ⟦b4-REFUNDID1⟧ 退款 ID 在傳遞途中被丟掉 —— **要 Sean 打的那一發,今天打不了**

> 2026-09-12 · 窗 B(`~/pcm-ops`)· **只寫 plan,零改動。**
> 板列:`bash scripts/board-row-by-anchor.sh b4-REFUNDID1`

---

## 0. 🧑 給 Sean 的一頁

**碼那一半早就修好了**(2026-08-29 `7ae77a72`,已在 dev):退款編號現在會被記進 log、錯誤訊息與稽核。

**剩下的一半是一個問題**:TapPay 在「延後」與「被拒」這兩種回應裡,**到底有沒有給那個編號** ——
沒有人量過。要知道答案,得**真的打一發 TapPay 測試環境的退款**。

🔴 **而那一發今天打不了,理由不是沒人批准,是碼裡有一道閘擋著**:
系統不允許「測試卡 × 正式資料庫」這個組合(`composition.ts:71`),
而後台 `dev` **就是正式後台** ⇒ 你在那裡退款用的是**真金鑰、真錢**。
⇒ 📌 **「用測試環境刷、在正式後台退」這條路,設計上就不通。**

**你 08-31 已經為這件事拍過一次「甲 = 只刷不退」**;09-06 又答過 **q14 甲 = 找 10 分鐘打一發退款**。
🛑 **這兩次互相矛盾** —— 而較新那次是在「以為打得了」的前提下答的。§4 那一題就是請你重新選一次。

**今天的曝險**:這件事影響的**不是錢會不會錯**,是**對帳成本與稽核完整性**
(客服要用訂單與時間去翻 TapPay 後台,而不是貼一個編號)。

---

## 1. 現況(檔:行 + 今天的讀數)

```
碼那一半(已修, 在 dev)
  packages/adapters/src/tappay/TapPayChargeAdapter.ts   logRefund 六處帶 refundId;
  `refundId: null` 只剩一處(attempt_started, 落在 fetch 之前 ⇒ 那個 null 是對的)
  新增 TapPayRefundUnknownStateError 帶 refundId(7ae77a72 同一顆進來, apps+packages 共 6 檔)
沒做的那一半
  🔴 `deferred` / `rejected` 兩態 TapPay 帶不帶 refund_id ⇒ **沒有人量過**
擋路的那道閘(今天仍在, 我開檔確認)
  apps/admin/src/lib/payment/composition.ts:71 `tapPayEnvPairingViolation(env, supabaseUrl)`
  · sandbox 金鑰 × 正式庫 ⇒ 回字串 ⇒ getAdapter 之前就 throw ⇒ **adapter 根本建不出來**
  · 而 `dev` 分支 = pcm-admin 的 production ⇒ Sean 在後台退款 = production 金鑰 = 真錢
正式庫唯讀(2026-09-12):order_refunds 只有 confirmed 2 列 ⇒ **沒有 deferred / rejected 的真樣本**
  ⇒ 📌 那兩態的形狀今天**在我們自己的資料裡也查不到**, 不是「忘了查」。
```

---

## 2. 要 Sean 打哪一發 · 怎麼打(如果決定要打)

🔴 **前提:要先有一個【三者同一側】的環境** —— 拋棄式資料庫 + sandbox 帳本 + sandbox 金鑰。
```
今天最接近的兩塊已經存在:
  · bash scripts/storefront-probe/up.sh   —— 它會讀 ~/pcm-secrets/tappay-sandbox.env(sandbox 金鑰)
  · bash scripts/admin-probe/up.sh        —— 後台鑽機, 連的是本機拋棄式 PG(不是正式庫)
  ⇒ 兩支都走 127.0.0.1 的本機庫 ⇒ `tapPayEnvPairingViolation` **不會擋**(它擋的是正式庫那一側)
🛑 而缺的一塊我【沒有驗】:刷的那一筆與退的那一筆**必須在同一個庫裡**,
   而兩支鑽機今天各起各的庫 —— 能不能共用一個, **我沒試過**(見 §6)。
```
**Sean 那 10 分鐘要做的(在上面那個環境準備好之後,由窗陪著跑)**
```
① 顧客站鑽機下一張單, 用 TapPay 測試卡刷成功(拿到一個 sandbox 的 rec_trade_id)
② 後台鑽機打開那張單 ⇒ 退款區 ⇒ 全額退款
③ 造【延後】:用 TapPay 測試碼 10024(未請款 ⇒ 退款會被延後)
④ 造【被拒】:用測試碼 10051(額度不足那一類 ⇒ 退款被拒)
⑤ 每一發把畫面與 log 的原始回應抄下來 —— 我們要的只有一件事:
   **那兩種回應裡到底有沒有 refund_id 這個欄位**
🛑 全程在鑽機上, 零正式庫寫入、零真錢。
```

---

## 3. 改什麼(量到之後)

```
情況一 兩態都有 refund_id ⇒ 碼已經記得下來 ⇒ 本列直接 done, 零改動。
情況二 其中一態沒有       ⇒ 在 logRefund / 稽核那一層明寫「這一態 TapPay 不給編號」,
                            並把 refund-judgment 那條「用 Record 現值算 delta」寫成**唯一**對帳依據。
⇒ 兩種情況都**零 migration、零 schema**。本列從頭到尾沒有 DB 物件要動。
```
🔵 **而在量到之前不要動碼** —— 板列自己逐字寫著:「在那之前拿到 `null` 不代表壞掉,**不要去修一個不存在的東西**」。

---

## 4. ❓ 要 Sean 回的(一題,而它是**重新選一次**)

```
Q:那一發 TapPay 測試退款, 還要不要打?
   (你 08-31 拍過「只刷不退」, 09-06 又答「q14 甲 = 找 10 分鐘打一發退款」—— 兩次相反,
    而較新那次是在「以為打得了」的前提下答的。今天打不了, 除非先做出一個測試環境。)
A:甲 = 不打(推薦)。維持 08-31 那一拍。代價:那兩種回應的形狀**永遠是未知**,
       而稽核裡少一個外部編號 ⇒ 客服對帳要用訂單與時間去翻 TapPay 後台。
       🛑 錢不會因此算錯(對帳判定用的是 Record 現值算 delta, 不靠這顆編號)。
   乙 = 打。先由窗做出「刷與退在同一個拋棄式庫」的環境(我估要一段獨立的工, 而我還沒驗過做不做得出來),
       做出來之後你花 10 分鐘照 §2 走四步。
```

---

## 5. rollback / migration

```
migration  **不需要**(兩種情況都只動 TS 與註解)。
rollback   甲 = 沒有東西要退。乙 = 量測本身零寫入;若之後改碼, revert 那一顆 commit 即可。
```

---

## 6. 🛑 這份 plan 證不到什麼

1. **我沒有試過讓兩支鑽機共用同一個庫** —— §2 那個「環境做得出來」是讀 `up.sh` 推的,不是跑出來的。⇒ 選乙之前要先有人真的試一次,**而那一步本身可能就是失敗點**。
2. **我沒有打過任何一發 TapPay**(sandbox 或 production 都沒有)。
3. **`deferred` / `rejected` 的測試碼 10024 / 10051** 我引的是板列寫的,**沒有去 TapPay 文件複核**。
4. **兩次拍板互相矛盾**這件事,我讀的是板列轉述;08-31 那一拍的證據等級板列自己標成【轉錄】(單一來源是一顆 memory,沒有人看到他本人打字)⇒ **§4 那一題請他重新選,不要拿舊的去推新的**。
