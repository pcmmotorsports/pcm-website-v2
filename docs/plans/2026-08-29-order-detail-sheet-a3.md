# Plan · 訂單明細單(A3)—— 從 `ShippingDoc` 衍生 variant、`PickingDoc` 退役

> **狀態:等 Sean 批(鐵則 8)。批准之前不動任何一支 `.tsx`。**
> 提案人 `-c8` · 2026-08-29 · 板子 `docs/launch-todo.md` ⟦b4 的「出貨文件 PDF」那一列⟧

## 0. 一句話

Sean 2026-08-23 拍板「原揀貨單鈕改成**訂單明細**」。**表面那半已經落地**
(鈕文字、紙的抬頭都已是「訂單明細」);**剩下的是紙的內容**,而那是一次真的重構。

## 1. 要改什麼

| | 現況 | 目標(依 OD `pcm-524f/BRIEF-出貨列印-差異單-20260823.md`) |
|---|---|---|
| 涵蓋範圍 | 一個包裹 | **整張訂單**(全部品項, 不分包裹) |
| 出貨狀態字眼 | 有 | 🔴 **沒有** |
| 勾選框 | 有 | **不需要** |
| 版面 | 自己一套 | 🔴 **跟出貨單一樣**(Sean 逐字) |
| 金額 | **明文禁止** | ⚠️ **要畫**(見 §4 —— 這是唯一要 Sean 答的一格) |

## 2. 為什麼是重構而不是改幾行

```
shipping-doc.tsx   1,244 行   ← 共用列印元件，variant 從它衍生
picking-doc.tsx      498 行   ← 退役
引用 PickingDoc 的檔  4 支    （page.tsx / 兩支測試 / 自身）
```
「版面跟出貨單一樣」是 Sean **逐字**要求 ⇒ 不能各畫一套 ⇒ 必須共用骨架。

## 3. 影響面 / rollback

- **影響面**:只在 `apps/admin` 的列印路由。客人端零影響;訂單資料零寫入(兩張紙都唯讀)。
- **rollback**:單顆 `git revert`。退役的 `PickingDoc` **保留檔案**到下一個 milestone 再刪
  (刪檔與換用途分開兩顆, 這樣 revert 只需要退一顆)。

## 4. 🔴 唯一要 Sean 答的一格 —— **而它不是我發明的問題, 是稿自己標的**

BRIEF 逐字自陳(`:37-45` 一帶):
> 「**兩張紙都會有金額。**…⇒ 而**這是我推的, 不是他說的。**
>  若你設計時覺得哪一張不該有金額, **回一句, 不要自己扛。**」

而 repo 裡有**兩道明文守門**擋著金額, 其中一道**用突變驗過**:
```
(a) picking-doc.tsx:9  「本檔不得出現任何金額」
    驗收條件在 page.test.tsx:151 格② —— 檔頭逐字寫著「那格用突變驗過
    （故意塞金額進去 ⇒ 該格必紅）」
(b) shipping/[shipmentId]/page.test.tsx:461 「金額區塊還沒做 ⇒ 紙上不得出現任何金額」
    ⚠️ 該檔自己寫著「金額真的落地時要【改寫這格, 不是刪掉】」
```

🛑 **所以這一片會【反轉一道突變驗過的守門】。**
一般情況下「想動驗證本身」是立即停止訊號(R4)。**這一次不是** ——
那道守門對**揀貨單**是正確的, 而**那顆按鈕已經不是揀貨單了**(Sean 08-23 拍板)。
🔴 **但這個區別只有 Sean 分得出來, 所以它必須由他說。**

```
Q-DETAIL-MONEY:訂單明細那張紙, 要不要印金額？
A: 甲 要（它是「完整的一張訂單」⇒ 沒有金額不成立）
   乙 不要（那張紙也會被拿到倉庫 / 被客人瞄到）
```
**推薦甲** —— 依據:①「完整的一張訂單」是他自己的逐字 ②8/16 brief 已把「倉庫桌上那張有金額」
列為合併的**固有代價**而他選了甲 ⇒ 他已經買過這個代價一次。
⚠️ **而外洩顧慮沒有消失、是搬家到出貨單那張** —— 兩張都印, 就是接受它兩次。

## 5. 片界(批准後照這個順序, 每片 15-45 分鐘、可中斷)

```
~~A3-1  抽出共用版面骨架（不改任何畫面）⇒ 零行為改動~~ 🔴 **2026-08-29 開工當下作廢, 見 §8**
A3-2  訂單明細 variant：整張訂單範圍 + 拿掉出貨狀態字眼 + 拿掉勾選框
A3-3  金額區（等 Q-DETAIL-MONEY 答案；答乙就【不做這片】，A3-2 收工即完）
A3-4  PickingDoc 退役（改引用點）；刪檔留到下一個 milestone
```

## 6. 我沒查的(射程, 不要讀得比它大)

```
🔴 **~~稿的另一支 `預覽-出貨明細單.html`…以那支為準~~ ⇒ 2026-08-29 開工當下作廢, 原句留痕:**
  **那份檔不是稿。**`pcm-524f/預覽-出貨明細單.html:252` 逐字:
  > 「這個檔是 `patch-orders-ui.py` **自動產生**的, 每次重跑都會覆蓋。
  >   **來源頁**:`/print/orders/<id>/shipping/<sid>`。要改請改 `patch-orders-ui.py`, 不要改這裡。」
  ⇒ **它是把我們自己的頁面 render 出來再套補丁** ⇒ **拿它對稿 = 拿我們的碼比我們的碼 ⇒ 必然「完全吻合」。**
  ✅ **真權威 = OD 另一個專案 `pcm-print-docs`**:`contract.md`(171 行)+ `brief.md`(133)
     + `shipping-picking-doc-a4.html`(570 行, **無自動產生字樣**, 標題「出貨明細單(兼揀貨核對)· A4 版面」)。
  ⚠️ `contract.md` 自陳「座標實測自 worktree `/Users/sean_1/pcm-print` @ `8d3cb5b2`」⇒ **另一棵樹, 行號會漂, 引用前重跑。**
  📎 而 `mcp list_projects` 當場回 **1 個**(只有 `prx-design-template`)、磁碟 **12 個**
     ⇒ **照它做事會得到「我們沒有列印單據的設計稿」, 而 `pcm-print-docs` 就在磁碟上**(鐵則 1 那個坑當場複現)。
· PDF 那半（application/pdf 全 repo ⇒ 0）不在本 plan 裡，它是另一片
· 我沒有量「現在有多少人在用揀貨單那顆鈕」—— 那要線上 log
```

## 7. 🛑 而有一格會讓本片的【驗收條件在本機恆綠】(2026-08-29 線A `-11` 量到, 逐字帶)

> 「列印頁的中文, 是靠**開發者 Mac 上有字型**撐著的。repo 內字型檔 **0** 個;
>  列印頁自己的測試逐字寫著『字型是系統堆疊(…PingFang TC…)⇒ 本量測對 **macOS + Chrome** 成立』。
>  🛑 而 Vercel 是 **Linux 容器** ⇒ 預設映像通常一個 CJK 字型都沒有。
>  📌 **最壞的失敗形狀:本機看到正確中文, 只有正式站是豆腐字。**」

```
🔴 而板子 :194 那一列的驗收條件逐字是「驗收 = 【中文字真的在】」
⇒ 在字型那件落地之前，那個驗收條件【在本機恆綠】
⇒ 它會在 A3 全部收工、三綠全綠、我回報「做完了」之後，
   在【只有 Sean 打開正式站】的那一刻才紅
```
📌 **⇒ 那不是「我驗得不夠」, 是【我的驗收條件本身在本機是恆真的】。**
✅ **前置由線A 承接**(零 code:量現況 / 選字型 / 逐字讀授權 / 端方案給主視窗拍)。
🛑 **⇒ A3 任何一片都【不得】把「中文字真的在」寫成已驗收 —— 在字型落地之前, 那一格只能寫「本機成立, 正式站未量」。**

## 8. 🔴 片界訂正(2026-08-29 `-c8` 開工當下量到;**Sean 批的目標不變, 錯的是我的切法**)

### 量到的
```
apps/admin/src/components/print/shipping-doc.tsx  用 pd-* class ⇒ 46 個（設計稿的版面系統）
apps/admin/src/components/print/picking-doc.tsx   用 pd-* class ⇒ 【0 個】（純 Tailwind）
兩張紙的外框只差一個 class：
  picking  print-sheet mx-auto max-w-3xl space-y-4 p-6 print:max-w-none
  shipping 同上 + `pd-sheet`
```

### ⇒ 所以原 `A3-1` 不成立
> **兩張紙今天【沒有共用骨架可以抽】——**
> **而「讓它們共用」這件事本身, 就是 Sean 要的那個改動(「版面跟出貨單一樣」)。**
📌 **⇒ 我把一個【目標】切成了一個【前置步驟】, 而那個前置步驟的內容是空的。**
🔴 **⇒ 若照原 A3-1 做, 我會產出一顆「抽出一個只有一行 class 字串的共用元件」的 commit ——**
**三綠會全綠、既有測試會全綠、而它對 Sean 要的東西【零貢獻】。**

### ✅ 訂正後的片界(目標一格未改)
```
A3-1' 訂單明細改用 pd-* 版面系統（= Sean 逐字「格式跟出貨單一樣」）
      ⚠️ 這【是】視覺改動 ⇒ 真權威 = OD `pcm-print-docs/shipping-picking-doc-a4.html`
      驗收：pd-* class 從 0 變成非 0；既有測試中【與版面無關的那些】仍全綠
A3-2' 拿掉出貨狀態字眼 + 拿掉勾選框 + 範圍改為整張訂單
A3-3' 金額區（Sean 08-29 拍甲 ⇒ 要做）
A3-4' PickingDoc 退役（改引用點；刪檔留到下一個 milestone）
```

### 🛑 而這一格要留給下一個提 plan 的人
> **我在還沒開檔量之前, 就寫下了「先抽共用骨架, 零行為改動」——**
> **而那句話讀起來像【穩健】, 所以它通過了我自己的審查, 也通過了 Sean 的批准。**
> 🔴 **一個【看起來穩健】的第一片, 不會有人去問它裡面有沒有東西。**

