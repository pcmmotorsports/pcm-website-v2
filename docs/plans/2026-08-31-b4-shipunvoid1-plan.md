# ⟦b4-SHIPUNVOID1⟧ plan · 箱子作廢後再復原 ⇒ 出貨信永遠不寄

線【DB 金流】2026-08-31 · **本檔是提案,尚未動任何碼。**

## 0. 前提驗證(開工前跑,四格全中)

| # | 宣稱(板 `:731`) | 我量到的 | 判 |
|---|---|---|---|
| ① | 掃描 view 的 anti-join **不分 status** | `20260822010000` 該處**自己的註解逐字**:「這個 anti-join 只看『那一列存不存在』,**不分 status**」;SQL 本體 `NOT EXISTS (… WHERE e.event_type='order_shipped' AND e.dedup_key = public.pcm_shipped_email_dedup_key(s.id, o.id))` ⇒ **零 status 條件** | ✅ |
| ② | 那一列**永久佔住**去重鍵 | `20260717020000:377` `CREATE UNIQUE INDEX email_outbox_event_uniq ON public.email_outbox (event_type, dedup_key)` ⇒ **同鍵插不進第二列** | ✅ |
| ③ | `admin_unvoid_shipment` 不清那一列 | 該函式所在檔 `email_outbox` 命中 **0** | ✅ |
| ④ | **沒有任何東西會叫** | 🔴 **比板上說的更強**:今天才建的 `20260831020000` 缺口告警,`:120` 的 `COMMENT` **自己逐字寫著**:「已知盲區:那支 view 的 anti-join 不分 status ⇒ 一列 `skipped_shipment_voided` 會讓那一格永久離開分子(板上 `b4-SHIPUNVOID1`)⇒ **本量具在那個漏信面上恆印 0**」 | ✅ |

🔴 **④ 值得單獨講**:**為這個面蓋的那支量具,對這個面恆印 0,而它自己知道並寫下來了。**
⇒ 📌 那不是盲點,是**具名的盲區** —— 而它的作用是:**下一個看那支告警的人會看到全綠,而全綠是真的、只是不涵蓋這一格。**

## 1. 症狀(對客人)
員工把箱子作廢 → 出貨信落 `status='skipped_shipment_voided'`(**這是對的**,那是正常業務動作)
→ 員工**復原**同一個箱(`admin_unvoid_shipment`,同一個 shipment id)
→ 去重鍵 `(shipment_id, order_id)` 沒變 ⇒ view 的 anti-join 仍命中那一列
⇒ **那位客人的出貨通知永遠不會排進去,而狀態是「跳過」不是「失敗」** ⇒ 不進 due、不被任何 dead-man 命中。

## 2. 三個修法(**我不自己拍,列代價**)

### 甲 · `admin_unvoid_shipment` 把那一列清掉
- 動作:unvoid 時 `DELETE FROM email_outbox WHERE event_type='order_shipped' AND dedup_key=… AND status='skipped_shipment_voided'`
- ✅ **時點最準**:unvoid 就是「那個 skip 從對變錯」的那一刻
- 🔴 **代價**:那一列是**證據**(我們曾經因為作廢而跳過)⇒ 刪掉就沒了
  ⇒ 要補:同一交易寫一筆 `admin_audit_log`,否則我們把一個**沉默的漏信**換成一個**沉默的刪除**

### 乙 · 改 view 的 anti-join 忽略該 status
- 🔴 **單獨做會壞**:UNIQUE 擋住新 INSERT ⇒ view 每輪都回同一列而排不進去 ⇒ **從「不寄」變成「每輪空轉」**
- ⇒ 必須同時把入列路徑改成 `UPDATE` 既有列回 `pending` ⇒ **動 view + 入列邏輯兩處**

### 丙 · 寫入 skip 時就把去重鍵綁在「這一次作廢」上
- 動作:`skipped_shipment_voided` 那一列的 `dedup_key` 加上一個作廢世代(例如 `voided_at` 或 void 事件 id)
- ✅ 語意最正:**skip 是針對那一次作廢,不是針對這箱這單的永遠**
- 🔴 **代價**:改的是 `dedup_key` 的語意 ⇒ 影響面最大,而去重鍵是這張表的**核心不變式**

## 3. 我的推薦 = **甲 + 稽核**
理由:③ 已量到 unvoid **完全沒碰** outbox ⇒ 甲是**加一段**不是改語意;乙 會製造空轉;丙 動核心不變式。
🛑 而甲的那個代價要一起做,不能之後補:**把沉默的漏信換成沉默的刪除,不算修好。**

## 4. 命中的鐵則(自評,不降級)
- **12⑤ 對外不可回收(寄信)** —— 本片的失敗方向是**寄出不該寄的信**(例如箱又被作廢了卻寄了)
- **12③ 動 RPC** · **鐵則 8**(動 RPC ⇒ 先提 plan 等批)
⇒ **codex 對抗審查不降級。**

## 5. 驗收(每條可 yes/no)
1. 拋棄式 PG:作廢 → 出貨信落 skip → **unvoid** → 掃描 view **回得到那一列**(現況:回不到)
2. **反向**:作廢 → skip → **不 unvoid** ⇒ view **仍然回不到**(否則我們把靜默漏信換成錯誤寄信)
3. 🔴 **必死正對照**:把修法拿掉重跑,第 1 條**必須紅** —— 否則整發零判別力
4. `admin_audit_log` 有那一筆(甲的代價那一格真的落地)
   🔴 **而它要有自己的突變**:把稽核那一行拿掉重跑 ⇒ **必須紅**。
   (否則「有寫稽核」與「我沒有在驗稽核」印同一個綠。)
5. **作廢 → 復原 → 再作廢** ⇒ 掃描 view **必須回不到那一列**(否則我們寄出一封不該寄的信)
6. 連跑兩發:**檔數 / 測項 / 紅格 / 我餵幾條 vs 它跑幾支** 四個數皆相同
7. 三綠 + codex(12⑤③ 不降級)

## 6. 🛑 我還答不出來的(寫下來,不假裝已知)
- ~~同一箱被作廢/復原兩次以上,我沒演過~~ ⇒ 🔴 **改成驗收條(主視窗指定),不留在射程聲明裡**:
  **作廢 → 復原 → 再作廢** ⇒ 那一列已經被清掉了,而箱子是作廢的
  ⇒ 若入列邏輯照跑 ⇒ **寄出一封不該寄的信** ⇒ 那正是驗收第 2 條的反向,而它就住在這個世界裡。
- ~~正式庫現在有沒有這種列 ⇒ 我查不到~~ ⇒ ✅ **主視窗 2026-08-31 唯讀查了,而那個 0 要照它的分母寫**:
  ```
  正式庫上 status = 'skipped_shipment_voided' 的列 ⇒ 0。
  🔴 而那個 0 的分母是 **email_outbox 全表只有 2 列**,兩列都是 'sent'。
  ⇒ 那不是「這個病沒發生」,是【這張表幾乎沒有資料】—— 因為那條寄信線本身還很新(今天才接的)。
  ⇒ ⚠️ 所以那個 0 對「未來會不會發生」**零預測力**。
  ```
  📌 **⇒ 急迫度是「未來會漏」不是「現在就在漏」—— 而理由不是「查過了乾淨」,是「那條路還沒被走過幾次」。**
  🔴 **兩者結論相同而理由不同,而只有第二個理由答得出「它什麼時候會變成現在就在漏」:那條線開始正常出貨的時候。**
  🔵 **而主視窗自陳它第一發的尺打錯地方**(去 `pg_enum` 找那個值 ⇒ 0,而正對照 `'sent'` 也是 0
     ⇒ **那個欄位是 `text` 不是 enum**)⇒ **那一發的 0 是「尺沒接上」,不是「值不存在」。**
- 🔴 **⇒ 所以現在就做,而理由是時機**:今天 outbox 只有 2 列 ⇒ 改它的代價接近零;
  那條線一旦開始正常出貨,那張表會長起來。
  📌 **現在做 = 改一張幾乎空的表的行為;之後做 = 改一條客人正在等信的路。**

---

# 🔴 2026-08-31 21:5x · 這份 plan 的前半段【已被推翻】,而原文留著

## 發生什麼
甲2(在 `admin_unvoid_shipment` 那一側清掉那一列)**已實作、已被 codex 打穿、已全部丟棄**。
**打穿它的是一個我沒問的問題:這裡有幾個交錯順序?**

```
順序A 先 skip 後 unvoid ⇒ 甲2 有效
順序B 先 unvoid 後 skip ⇒ 🔴 **甲2 撲空,洞原封不動**
      sweeper 兩發【分開的】DB round-trip:讀到 voided → (unvoid 在這中間) → 才寫下 skip
      而寫那一發只有 `.eq('attempts', …)` 世代柵欄,**不重新檢查箱還作不作廢**
```
✅ **可達性是量到的**:probe `W4`(復現)+ `W4b`(同一順序用甲2 的形狀 ⇒ 仍然漏)。
🛑 **而「真的有人會落在那幾毫秒裡嗎」我沒量,也不假裝量過** —— 那一格是判斷,不是量測。
📌 但它的性質是:**發生時零訊號、事後零證據、受害者是一位收不到信的客人。**

## ✅ 落地的是丙,而它比原本估的便宜一個量級
```
原估  丙 = 動 dedup_key 核心不變式 ⇒ 動 schema ⇒ 鐵則 8 ⇒ 等 Sean
實際  丙 = **一個欄位,跟著 status 在同一發 UPDATE 裡寫出去**
      packages/ports/src/IEmailOutbox.ts          簽名多一個 currentDedupKey
      packages/adapters/.../SupabaseEmailOutboxAdapter.ts  dedup_key: `${key}:voided:${id}`
      packages/use-cases/src/sweep-email-outbox.ts 把 job.dedupKey 傳進去
⇒ **零 migration、零 RPC 改動、零 schema 改動** ⇒ 不命中鐵則 8
⇒ 仍命中 **12⑤(寄信)+ 12⑥(packages/ 共用行為)** ⇒ codex 不降級
```
🔵 **為什麼它兩個順序都關得掉**:退休與 status **在同一發 UPDATE**
⇒ 不可能一個成功一個失敗 ⇒ **不是多加一道檢查,是把「需要檢查」這件事拿掉。**
⛔ ~~而箱還作廢時不會誤寄:掃描 view 自己有 `s.deleted_at IS NULL`~~
🔴 **這句作廢(codex R1【4】/R2 再抓一次:我只改了碼裡那句,【沒有改這裡】)**:
   `s.deleted_at IS NULL` 只守**排信那個時點**,守不到**寄送那個時點** ——
   sweeper 讀到 live 之後、`sender.send` 之前箱仍可被作廢,而它不重查 ⇒ **仍會寄出不該寄的信**。
   ⚠️ 那條路**本來就在、不是本片造成的、本片也沒關掉它**。
   📌 **⇒ 同一句假話存在兩個地方,而我只修了一個** —— 今晚第二次(上次是 spec1 改號)。

## 三格開工前的檢查(主視窗指定,全部量過)
```
① dedup_key 有沒有長度上限 ⇒ **沒有**:`20260717020000:301` 是 `text NOT NULL`,
   只有 `email_outbox_dedup_key_nonempty CHECK (dedup_key <> '')`。varchar 命中 0、長度 CHECK 命中 0。
   🔴 為什麼要問:被截斷的話**兩列會撞成同一把鍵** —— 那是把一個病換成一個更難查的。
② 誰對 dedup_key 做等值比對 ⇒ **一處**:`SupabaseEmailOutboxAdapter.ts:331` `resolveUniqueViolation`。
   而它**只在 23505 撞鍵之後**才跑 ⇒ 退休過的列不再持有正規鍵 ⇒ 它本來就不該找到那一列 ⇒ 不受影響。
③ 同一列被 skip 兩次 ⇒ 後綴是那一列自己的 `id` ⇒ **算出來的鍵相同**(冪等,測試釘住)。
```

## 驗收(全部實跑過)
```
probe `scripts/shipunvoid1-apply-probe.sh` ⇒ ****PASS=14 FAIL=0**(⚠️ 原文寫 13 —— 那是加 W4b 前提斷言【之前】的數字;codex R2 抓到)**,連跑穩定
  W1 丙 ⇒ view 回得到      · W2 不 unvoid ⇒ 回不到(不誤寄)
  W3 **必死正對照**:舊行為 ⇒ 回不到 ⇒ W1 的 1 是修法帶來的
  W4 競態順序 ⇒ 丙 **仍有效** · W4b 同順序用甲2 形狀 ⇒ **撲空** ⇒ W4 的 1 是丙 特有的
  W5 兩輪作廢/復原 ⇒ 鍵不互撞、證據兩列都在、**一列都沒被刪**
單元(**真的呼叫那支 adapter,不是手抄**):值逐字 / 負對照換 id / 冪等 —— 三發突變全殺
  M1 拿掉那一行 ✅殺 · M2 後綴寫死 ✅殺 · M3 **帶了欄位但值沒變** ✅殺
  📌 M3 那格釘的是:**「有帶這個欄位」與「帶的值是對的」是兩個宣稱。**
```

## 📎 從丟掉的那支 migration 搶救下來的(下一支 `CREATE OR REPLACE` 的片可以直接抄)
🔴 **`CREATE OR REPLACE` 整支函式最危險的是它【靜靜少了一段】** —— diff 上看不出來。
⇒ 前置閘要逐一驗**承重件還在不在**,每一條對應一個具體後果,不是形式檢查:
```
idem_claim(冪等:重放會變成真的再做一次) · idem_record(回填:重放拿不到原結果)
deadlock_detected(有界重試) · 重試耗盡碼 · 隔離級別前緣 · lock_timeout(卡住時會等到天亮)
找不到目標的人話 · C9 方向覆寫(錯誤訊息會變成相反方向)
```
🔴 **而唯一索引不要用 `indexdef LIKE` 判**(codex 抓到):帶 `WHERE false` 的 partial unique index
也會命中,而它**證不到全表唯一**。改問 `pg_index`:`indisunique` + **`indpred IS NULL`** + 欄位集合相等。
🔴 **而 md5 指紋閘我刻意不做**:`pg_get_functiondef` 與檔案原文不同源 ⇒ 常數要從「已裝那一版的庫」
算出來才對得上,而那個常數**沒有第二來源可核** ⇒ 它會變成下一個免責貼紙。

## 📌 而這一片真正的教訓(收工條)
**修一個競態之前,先把所有交錯順序列出來,再問我的修法蓋住幾個。**
⇒ 「有幾個順序」不是事後才想得到的問題,**它是每一個並發修法的第 0 題**。
🔴 而**修一半在這個病上特別貴**:它會讓下一個人看到「這條修過了」而停止懷疑
⇒ **一個半修的洞,比一個沒修的洞更不容易被第二次發現。**

---

# 🔴 Sean 拍甲之後的第一件事:**如果那條矛盾路真的被走到,我們會從哪裡知道?**

**答案:我們不會知道。而理由是結構性的,不是「還沒做監控」。**

```
序列:信已被 Resend 收下 → `markSent` 寫 DB 失敗 → 該列被回收重試 → 箱被作廢
     → 丙 退休舊鍵 → 箱被復原 → 新的一列(新 uuid ⇒ 新冪等鍵)⇒ 第二封寄出
```
🔴 **而【啟動這條序列的那一步,正好就是抹掉第一封證據的那一步】**:
`markSent`(`sweep-email-outbox.ts:751`)是**唯一**把「這封寄出去了」寫回我們資料庫的地方。
它失敗 ⇒ 我們這邊**沒有任何紀錄**說第一封寄過了。
而 sweeper 的設計是 **counts-only、零 PII、不逐封 log**(檔頭 `:47` 逐字)⇒ 也沒有第二份痕跡。

⇒ 📌 **⇒ 兩封信同時存在的地方【只有 Resend 自己的後台】,而我們沒有在讀它。**

## 那我們資料庫裡看得到什麼(誠實版)
```
· 那張單會有兩列:一列 dedup_key 帶 `:voided:`(status = skipped_shipment_voided)
                 + 一列正規鍵(status = sent)
· 🔴 **而那個形狀與【正常修好的情況】一模一樣** ——
  正常情況也是:一列退休的 skip + 一列成功寄出的新信。
⇒ **差別只在「那一列退休的 skip,它的信到底有沒有真的送到客人手上」**
  而那正是 markSent 沒寫成功、我們答不出來的那一格。
```
📌 **⇒ 所以「查資料庫」查不出來 —— 不是查法不對,是那兩個世界在我們的資料裡長得一樣。**

## ⇒ 這就是拍甲之後的【第一個已知缺口】,寫下來不假裝有解
```
🛑 若要**降低**它,最短的路是【把 Resend 的冪等鍵改成 per-(shipment, order)】——
   那樣第二次送出去會被 Resend 自己擋掉。
   🔴 **而它【不是根治】(codex R2 抓到我寫過頭)**:`ResendEmailSenderAdapter.ts:7` 逐字
   「官方保留 **24h**、只是第一道網」⇒ **兩次寄送相隔超過 24 小時,仍然會重複。**
   ⇒ 📌 而本片這條路的兩次寄送之間隔著「作廢 → 復原」**一個人的操作**,那很容易超過一天。
   ⇒ 而那是另一片(`ResendEmailSenderAdapter.ts:207`), 不在本片。
⚠️ 次好的路(只是發現、不是防止):讀 Resend 後台比對 —— **我們今天沒有那條管線, 也沒有人在看。**
```
🔴 **而這一節存在的理由**(主視窗 2026-08-31 指定):
**「已知、接受」四個字要配一個【發現路徑】,否則它等於「已知、然後忘記」。**
⇒ 🔵 **更正(主視窗 2026-08-31 指出,而它是對的)**:發現路徑**不是「沒有」,是「不在我們的系統裡」**。
```
漏信     ⇒ 客人不知道有信要來 ⇒ 他不會打電話 ⇒ **沒有人會知道, 包括客人自己**
矛盾寄信 ⇒ 客人拿到兩個追蹤號 ⇒ 他會問「哪一封才對」⇒ **客服接得到**
```
⇒ **發現路徑 = 客服接到客人的電話,而不是任何一支監控。**
🔴 **而上面那一整節仍然成立,它答的是另一個問句**:**我們**從自己的資料裡看不出它發生過 ——
**就算客人打了電話,我們也回頭查不出當時發生了什麼**(那兩列的形狀與正常修好的情況一模一樣)。
📌 **⇒ 所以這一格要寫在【客服看得到的地方】,不是只寫在碼裡。**(明天處理,已記。)
⇒ 而我原本寫「發現路徑:沒有」—— **那句比證據大了一點點,正是今晚那族的又一次。**
