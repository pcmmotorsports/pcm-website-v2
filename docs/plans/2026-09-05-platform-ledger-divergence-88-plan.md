# Plan(一頁):**平台帳本與人帳本差 88 支** —— 評估完了,而決定不是我的

> 主視窗 `-f8` 2026-09-05 深夜派(Sean 推 main 被 `migration-ledger-divergence.sh --if-pushing-main` 擋)。
> 交付:`~/pcm-mailbox/給Sean-平台帳本補記-20260905.md`。**本 plan 不動任何碼。**

## 1. 病灶(不是 bug,是兩條路)

| 貼的方式 | 平台帳本 `supabase_migrations.schema_migrations` | 人帳本 `supabase/APPLIED.tsv` |
|---|---|---|
| `supabase db push` | ✅ 自動寫 | 要人記 |
| **Dashboard SQL Editor**(Sean 的慣用路) | 🔴 **完全不寫** | 要人記 |
| MCP `apply_migration` | ✅ 寫,而**自派版本號** ⇒ 對不起來 |要人記 |

⇒ 📌 **那 88 支不是「漏貼」,是「用了一條不寫平台帳本的路」。**

## 2. 量到的(全程唯讀)

- 人帳本 **309**(⛔ `-f8` 轉述 308 —— 我今晚剛記一列)· 平台 **221** · 差集 **88**(⛔ 轉述 87)
- 甲堆 **37** 支可用存在性證明 ⇒ **實測 37/37 全在正式庫**
- 乙堆 **51** 支存在性零判別力(純授權 22 / `CREATE OR REPLACE` 25 / 純資料 2 / 其他 3)
  ⇒ 🛑 **不是「沒貼」,是「我沒有辦法用這把尺證明它」**
- 🔴 甲堆初查有 1 支 `f`,而那是**我的尺假陰性**(問了片中的暫時約束名 `_v3`,
  而它片尾改回原名)⇒ 改問語意特徵 ⇒ `t`。
  📌 **「證不到」與「沒貼」在第一發查詢裡印同一個 `f`。**

## 3. 三個決定,都不是我的

| # | 決定 | 誰 | 我的輸入 |
|---|---|---|---|
| ① | 要不要對那 37 支跑 `migration repair` | **Sean**(那是 apply 側授權) | 指令稿 + 風險三條已寫進交付檔 |
| ② | 那 51 支怎麼辦 | `-f8` 派 / Sean 拍 | 要 51 次人工判斷,不是跑一支腳本 |
| ③ | 那道閘要不要改「只警不擋」 | **`-f8` 裁** | 利弊表已寫;而我建議**第三個選項**:讓它分辨 ⑤ 與 ⑦ |

## 4. 🛑 這份評估證不到什麼

- **乙堆 51 支一支都沒證**。交付檔第 3 節那個「同一批人同一種方式」是**旁證不是證明** ——
  📌 **「同一批裡有 37 支是對的」推不出「另外 51 支也是」。**
- **反方向沒查**:「平台有而人帳沒有」(⑦ 孤兒版本號)本份沒碰。
- **我沒有印出專案 ref** ⇒ 「主樹 link 到的是不是正式庫」這一格**沒有獨立驗證**
  (只驗了 `project-ref` 檔存在)。
- 🔵 全程唯讀:未跑 repair、未 apply、未動正式庫一個字。
