# plan · ⟦5b-MANUALPCM07NOEXIT⟧ —— 那句「請重新整理後確認。」沒有終點,而它今天就在正式庫裡

> 開立:2026-09-20 · 窗 shop-6 · 🛑 **本檔只提案,零落筆、零貼板。** 貼板的人是 Sean。
> 碰 schema(改函式本體)+ 碰錢(退款上限 guard)⇒ **鐵則 8 + 鐵則 12** ⇒ 要 Sean 批、落筆後要對抗審查。

## 1. 員工現在看到什麼,改完看到什麼(**這一節是給 Sean 看的,其餘他不用讀**)

**現在**(手動退款登記時,若那張訂單找不到):
> 找不到這筆退款要掛的訂單,退款沒有發起、錢沒有動。**請重新整理後確認。**

🔴 **問題**:訂單如果真的不在了,**重整一百次也不會有** —— 而畫面只叫他重整,沒告訴他「重整幾次還是這樣該怎麼辦」。
⇒ 員工會一直按。

**改完**(建議字面,照隔壁卡片那條路的講法,不自創):
> 找不到這筆退款要掛的訂單,退款沒有發起、錢沒有動。請重新整理一次確認;**若仍然找不到,請勿反覆重試,並通知系統維護。**

🔵 **為什麼是這個字面**:卡片那條路的同款訊息(`refund-action-state.ts:190-192`)**已經是這樣寫的**,
Sean 看過那一版。⇒ **兩條路對同一件事講同一句話**,而不是多發明一種說法。

## 2. 🔴 而這件事為什麼今天才被抓到 —— 一段【凍結】的註解

```
🔬 apps/admin/src/lib/payment/manual-refund-repository.ts:106-107 逐字:
   「20260901200000 在 APPLIED.tsv 命中 0(未 apply)⇒ 正式庫今天不會吐 PCM07
    ⇒ 這一格是【接好而沒有流量】。」
🔴 而 2026-09-20 實查:
   · git ls-files | grep 20260901200000  ⇒ 【0 支】—— 那個檔 repo 裡根本不存在
     🟢 正對照:同一把尺數一支已知在帳本的 20260912040000 ⇒ 1 ⇒ 尺會動
   · PCM07 真正住在 20260902010000 與 20260907110000, **兩支都在 APPLIED.tsv 裡**
   · 正式庫實查:pg_get_functiondef(pcm_order_refund_cap_guard()) 含 'PCM07' ⇒ **t**
     同一個 body 含「請重新整理後確認」 ⇒ **t**
     ⚪ 負對照(同一 body 找現造字)⇒ f · 🟢 對照組 admin_record_hct_submit ⇒ f
```
⇒ 🎯 **那句註解寫的當下是對的** —— `20260901200000` 後來被 `20260902010000` 取代,而**註解停在取代之前**。
⇒ 📌 **它不是寫錯,是【凍結】。而凍結比寫錯危險:寫錯會被人看出來,凍結的註解長得像剛查過的。**

## 3. 改什麼

```
① 新一支 migration:CREATE OR REPLACE pcm_order_refund_cap_guard(),
   本體【逐字照抄】20260902010000 那一代, 只換 :102-104 那一句 RAISE 的訊息字串。
   🛑 簽章不動(它是 trigger 函式, 零參數)⇒ 不會長出第二支多載。
   🛑 SET 子句整組照抄(CREATE OR REPLACE 會把 SET 換掉;memory reference_create-or-replace-resets-set-clause)。
② 前置閘:貼之前先確認本體與 20260902010000 那一代相符(md5 或關鍵字面),
   不相符 ⇒ 停手(代表中間有人又改過一代)。
③ 事後斷言:沿用 20260902010000 自己那一段 —— 它逐字要求
   「PCM07 在函式本體出現 1 次、PCM05 出現 1 次」⇒ **換訊息不得動到那個次數**。
④ app 那一側【零改動】:PCM07 已經映到 rejected(manual-refund-repository.ts:112),
   而 rejected 就是把 DB 原句原樣顯示 ⇒ 改了 DB 的字, 員工就看到新的字。
⑤ 那段【凍結的註解】一併處理 —— 🛑 **不刪**:
   改成「原本指 20260901200000, 該檔不存在;今天真正的所在是
   20260902010000 + 20260907110000(兩支都已貼)」
   📌 留著錯的座標 + 今天的座標, 下一個人才知道這裡踩過。
```

## 4. 驗證(落筆時要留的)

```
🔬 落筆前先餵一發該紅的:對【新的】RAISE 字面斷言「有重整指示 ⇒ 必須有出口」
   ⇒ 舊字面必須紅, 而且紅在 AssertionError 上(不是 import 錯 / 找不到函式)
⚪ 負對照:現造一句沒有出口的重試指示 ⇒ 同一把尺要判它違規
🔵 出口字彙沿用既有那道欠債守門的同一張表(refund-action-state.test.ts:198 的 EXIT regex),
   不自創第三套 —— 建議字面裡的「若仍然」「請勿反覆」「通知系統維護」三個都在那張表裡。
🛑 而【DB 的字串沒有任何 TS 測試掃得到】—— 這是本片最弱的一格, 照實寫:
   驗它只能靠 ①migration 自己的事後斷言 ②貼完之後拿 pg_get_functiondef 實查一次。
```

## 5. Rollback

`supabase/rollbacks/<版本>-rollback.sql` = **貼回 20260902010000 那一代的本體逐字**。
零資料改動(只換一個訊息字串)。

## 6. 我沒查的

1. `20260907110000` 有沒有把 PCM07 那句**再改過一次** —— 我只確認它含 PCM07,**沒有逐字比對兩代的訊息**。
   🔴 **落筆前必須先做這一格**:要抄的是「今天正式庫那一版」,不是 `20260902010000` 那一版。
2. 這條路今天**有沒有真的被觸發過** —— 沒查(要查 app log 或稽核,我沒做)。
   ⇒ 📌 所以**不知道有沒有員工真的對著那句話一直按**。
3. 手動退款那條路的**其他** RAISE 訊息有沒有同樣的病 —— **沒掃**,本片只處理 PCM07。
