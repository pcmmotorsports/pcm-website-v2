# plan · 刷卡單「先退款才准取消」(Sean 2026-09-05 拍乙)

> 鐵則 **8** + **12①**。片型 = 高風險片。板列 `⟦0a-CARDCANCELNOREFUND⟧`。
> 🔴 **第 3 版。** R1 FAIL(9)⇒ R2 FAIL(7+1)⇒ **R3(opus, 換角度)FAIL(12 must-fix / 7 consider / 3 nit)**。
> R3 全文:`~/pcm-mailbox/R3-opus-cardcancel-625a32d06-20260905.md`
> 🛑 **本檔【沒有】經過一輪乾淨的審查** —— 每一輪都抓到不同層的東西,見 §8。

---

## §0 🔴🔴 R3 抓到:**我要做的那兩件, 有一件已經在線上了**

🔬 **我開檔核過(R3 F10/F12 對)**:
`apps/admin/src/components/orders/cancel-review-section.tsx:104-106` **逐字**:
> `payment_card_rail`
> title:「**這張單是刷卡付款的,要先把錢退回原卡片才能取消**」
> hint:「…請到**本頁最下方的『退款』**裡處理;錢退完之後這張單才能取消。」

⇒ 📌 **「擋住 + 告訴他去哪退」——【已經做好了】。**
⇒ 🛑 **我前兩版 §1 的 ①② 是在重做一件已經上線的事。**
⇒ 而 R3 F10 另一半也對:**被擋的時候 `cancel-order-forms` 根本不渲染** ⇒ 我要改的那支檔**不會出現在畫面上**。
⇒ F11 也對:`canCancel` 的**唯一來源**是 `cancel-view.ts:774` ⇒ 我開新 gate 檔 **= 第二份規格**。

---

## §1 那真正缺的是什麼 —— **三件, 而第一件是一句話**

### ① 🔴 `payment_refunded` 那句話**是錯的**, 而它是**拍板**

同檔 `:120` 逐字:
> title:「這張單的錢已經退回去了」
> hint:「款項已經退還,不用再退一次。這張單的錢已經結清 —— **現在不需要為它做任何事。**」

🛑 **而「不需要為它做任何事」不成立**:那張單**還沒取消**,而它**取消不了**(§0-a 的述詞 + `admin_mark_order_cancelled` 沒人接)。
⇒ 📌 **員工照這句話走, 會讓一張退完錢的單【永遠留在未取消狀態】, 而畫面告訴他這是對的。**

🔴 **而它是 Sean 2026-08-14 拍板 B 的定稿**(同檔 `:115` 逐字記著)⇒ **改它要他點頭**(§3 第一題)。

### ② `admin_mark_order_cancelled` 在線上而**零呼叫端**
(`20260903093000:604`,帳本已記;`grep` 全 repo admin 側零呼叫端,命中 5 處全是註解)
它的閘:**只開放刷卡單** · **只認 `refunded`(全額)** · 已取消過會擋。
⚠️ **R2 #5**:它只回 `{marked, idempotent}`,而現行 parser 要**四鍵 + cancellation UUID**(`cancel-repository.ts:99-126`)
⇒ **要寫第二個 parser, 不可共用。**
⚠️ **R2 #6**:它**沒有 `p_items`** ⇒ **只能接整單取消**,接不了部分取消那張表單。

### ③ 🔴 取消信(R3 F6/F7)
`admin_mark_order_cancelled` **不寫 `order_cancellations`** ⇒ **取消通知信的 enqueue 缺一塊**。
⇒ 走這條路取消的單,**客人收不到取消信**。(§3 第二題)

---

## §2 🔬 線上有一張**活的**實例(不是假設)

唯讀量到(2026-09-05,`pcm_readonly`,有 `BYPASSRLS` ⇒ 看得到全部):
```
display_id | payment_channel | payment_status | 已取消
W9486K     | bank_transfer   | unpaid         | f
C8MYDB     | tappay          | refunded       | f      ← 就是它
```
⇒ 🔴 **`C8MYDB` 刷卡、已全額退、而【還沒取消】** ——
它今天在畫面上看到的是「**現在不需要為它做任何事**」,而**它取消不了**。

### ⚠️ 分母訂正(R3 F13 對, 而歸因要查)
`STATUS.md:37` 記著 2026-08-30 唯讀量到 **`orders` 總列數 23**,今天我量到 **2**。
🔬 兩種數法交叉:`count(*)` = 2、`pg_stat_user_tables.n_live_tup` = 2 ⇒ 一致。
🔴 **而 STATUS 記的那張真實刷卡單 `VXTQV2`,今天 `count` = 0 ⇒ 它不在了。**
🛑 **成因我沒查** —— Sean 2026-08-30 拍過「線上資料可砍可改」+「我知道哪些是測試的」,
   **而「被砍了」與「別的原因」我分不出來。⇒ 這一格是【未查】, 不是【已知】。**
⇒ 📌 **分母 2 ⇒ 任何「掃過沒有反例」都零判別力**;而**這一片不需要那種掃描** —— 它有一張活實例。

---

## §3 📮 要 Sean 拍的兩題

```
Q-那句話改不改:一張刷卡的單, 錢已經全退給客人了。
   畫面現在寫「這張單的錢已經結清 —— 現在不需要為它做任何事」。
   而那張單【還沒取消, 也取消不了】。線上現在就有一張(C8MYDB)。
   這句話要改嗎?
A: 甲 | 乙
甲 = 改成「錢已經退完了。這張單還沒取消 —— 按下面的取消把它結掉」(並把取消鈕接上去)
乙 = 不改(那句是 2026-08-14 你自己定的稿)
```
**推薦 甲。** 代價一句:甲 = **推翻你 08-14 定的那句話**(而它當時是對的 —— 那時取消鈕還沒有路);
乙 = **退完錢的單會一直掛在未取消**,而畫面告訴員工這是對的。

```
Q-取消信一起做不做:走這條新路取消的單, 客人【收不到取消通知信】
   (它不寫 order_cancellations, 而信是從那裡發的)。
A: 甲 | 乙
甲 = 這一片一起補上(讓它也寫那張表)
乙 = 先不補, 板上開一列記著, 這種單先由人工通知客人
```
**推薦 甲。** 代價一句:甲 = 要動到寫入路徑, **要另一輪 codex**(碰錢碰信);
乙 = **客人退了錢而沒收到任何通知**, 而沒有東西會提醒我們去補寄。

---

## §4 🔴🔴 R3 的 F1/F2 —— **我核完了, 它成立**, 而**這一片會放大它**

🔬 **開檔量到的**(`20260905010000_m4b_manual_refund_syncs_payment_status.sql:293-315`):
```sql
v_moved = SUM(order_refunds  WHERE status='confirmed')        -- 卡片軌
        + SUM(order_manual_refunds WHERE voided_at IS NULL)   -- 人工軌
v_target = CASE WHEN v_moved >= v_total THEN 'refunded' ELSE 'partiallyRefunded' END
IF v_ps <> 'refunded' AND v_ps <> v_target THEN UPDATE orders SET payment_status = v_target
```
⇒ 🔴 **加總【不分軌】** —— 一張**刷卡**單,只要在 `order_manual_refunds` 登記一筆
   `rail='bank_transfer'`、金額 ≥ 總額的退款 ⇒ `payment_status` **變成 `refunded`**,
   **而錢還在 TapPay 那裡沒退。**
⇒ 🔴 **而 `payment_status` 只升不降**(`IF v_ps <> 'refunded'`)⇒ **變了就回不去。**

**那道閘擋在哪、沒擋在哪**:
- ✅ DB 端 `admin_record_manual_refund:330` **有擋** `p_rail='card'`(「card 軌不得由人工登記」)
- 🔴 **而它【不擋】「這張單是刷卡收的」** ⇒ 對刷卡單登記一筆 `bank_transfer` **是允許的**
- 🔴 UI 那道 gate(`manual-refund-entry-gate.ts:185`)只在「有 bank/cash 收款列」時才開放登記,
  **而同檔 `:42` 逐字**:「UI 這道的 rail 條件 **server 端沒有重驗**」
  ⇒ 📌 **UI 擋、server 不擋 ⇒ 繞過 UI 就進得去。**

### ⚠️ 而這裡有一句話要說清楚
🔵 **這個洞【今天就在】, 不是這一片造成的。**
🔴 **而這一片會把它從「顯示錯」升級成「動得了單」** ——
   今天 `refunded` 只讓畫面說「不需要為它做任何事」;
   **接上取消之後, 它變成「把一張沒退錢的刷卡單標成已取消」。**
⇒ 🛑 **所以這一片動手前, 那道 server 端重驗要先補**, 否則我是在一個已知的洞上面加一條路。
⇒ 📌 **這一格不是本片的 nice-to-have, 是它的【前置】。**

---

## §5 影響面 / Rollback

- 真正會動的:`cancel-review-section.tsx` 那**一句話** · `cancel-repository.ts` **多一條分支 + 第二個 parser** · (若 §3 第二題拍甲)寫 `order_cancellations`。
- **零 migration**(`admin_mark_order_cancelled` 已在線上)⇒ Rollback = `git revert`。
- 🔵 回滾之後回到今天:**擋著、退完了還是取消不了、而畫面說不用做任何事。**

---

## §6 🛑 我**沒有**做的

1. **沒有**核 §4 那兩個洞(R3 F1/F2)。
2. **沒有**真的呼叫過任一支 RPC —— 正式庫不可以。
3. ✅ **查完了:零呼叫端是【刻意的】, 不是漏掉。**
   那顆 commit 的 body 逐字:「**本片不寄信、不碰 outbox(那是片④)**」
   ⇒ 📌 **它是一組片的其中一片, 只做 DB 那半** —— 而**片④與 UI 那半, 沒有人做**。
   ⇒ 🔵 **所以「接上去」是在完成一個原本就規劃好的形狀, 不是發明新東西。**
   ⚠️ 同一顆 body 另一句:「既有的 `admin_cancel_order` 有 `p_items jsonb`, 而本支沒有
   ⇒ 呼叫端**連『想傳數量』都做不到**, **不是『我們決定不傳』**」—— 與 R2 #6 對得上。
4. **沒有**查 `VXTQV2` 去哪了(§2)。
5. **沒有**讀 R3 全文的 7 條 consider 與 3 條 nit —— 只折了主視窗轉的骨與我自己核到的。
   ⚠️ **那 10 條裡可能有真的東西。**

---

## §7 測試(只列與新落點有關的)

| # | 世界 | 期望 |
|---|---|---|
| 1 | `refunded` + tappay + 未取消 | 畫面出現**新那句**(不是「不需要做任何事」)+ 取消鈕可按 |
| 2 | 按下去 | 🔴 走 `admin_mark_order_cancelled` **且第二個 parser 讀得懂 `{marked, idempotent}`** |
| 3 | `refunded` 而**不是** tappay | 停用(那支 RPC 只開放刷卡) |
| 4 | `refunded` 而**曾部分取消** | 停用 |
| 5 | **部分取消**表單 + `refunded` | **不可以**走 mark RPC(它沒有 `p_items`) |
| 6 | 刷卡未退 | 維持**現行**那句(`payment_card_rail`)⇒ 🔬 **負對照:它紅了代表我動到不該動的** |
| 7 | 突變:把新分支改回只呼叫 `admin_cancel_order` | 第 2 格必須紅 |
| 8 | (§3 第二題拍甲時)取消成功後 | `order_cancellations` 有列 ⇒ 信 enqueue 得到 |

---

## §8 🔴🔴 可信度(給要拍板的人)

**R1 FAIL(9)⇒ R2 FAIL(7+1)⇒ R3 FAIL(12/7/3)。本版是折入後的第 3 版, R4 未跑。**

每一輪抓的**不是同一層**:
R1「引註解當量測 / 抓錯帳本」· R2「回傳形狀 / 兩種表單 / 我的訂正自己過期」· **R3「我要做的事已經在線上了」**。
⇒ 📌 **三輪的共同形狀是:我在紙上推,而每一輪都少讀一支檔。**
⇒ 🛑 **所以這一片的下一步不該是「再審一輪」, 而是【先把 §6 那五件查完】** ——
   **審查抓得到我寫錯的, 抓不到我沒去過的地方。**
