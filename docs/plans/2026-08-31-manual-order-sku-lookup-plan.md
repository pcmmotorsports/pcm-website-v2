# Plan · 手動建單「打料號 ⇒ 自動帶入」(Sean 拍甲, 先做這件)

> 量測 **2026-08-31 14:35:22**(`date` 當場印)· 樹 `/Users/sean_1/pcm-website-v2` · dev @ `facff99c`
> 提案人 = 線 DB `[6cdb1c]` · 🛑 **未經批准, 我不動任何一行。**

---

## 0. 🔴 鐵則 1:真權威解析 ⇒ **查無**, 而分母寫在這裡

```
mcp list_projects  ⇒ **回空**(0 個)
磁碟實數          ⇒ **12 個**   ← 🔴 數字不合 ⇒ 照 CLAUDE.md 鐵則 1 以磁碟為準
   ls ~/Library/Application Support/Open Design/namespaces/release-stable/data/projects | wc -l
```
📌 **⇒ 那正是鐵則 1 記著的那一格, 今天又發生一次(這次是「回空」那一種形狀)。**

12 個專案全掃「手動建單 / 建立訂單 / 新增訂單」:
```
命中 1 支 —— pcm-home-redesign/content-pages-data.js:136
🔴 而開檔看 ⇒ 那是【顧客站的隱私政策內文】裡的「建立訂單」四個字, **不是設計稿** ⇒ 偽陽性
🔵 正對照 同尺掃「訂單」 ⇒ **380 支檔**命中(尺是活的)
🔵 負對照 現造字面        ⇒ **0 支**
```
路由表點名的那兩支(Sean 本人在改的)我開了, 而它們是現值:
```
HANDOFF-orders-ui.md   4,309 行
orders-admin-v2.html   6,795 行 · sha256 **fc4a24a584e9e7d2** ← 與路由表記的**逐字相符**
稿內:手動建單 0 · 帶入 0 · 商品編號 0 · 建立訂單 0 · 料號 5(🔵 正對照「訂單」21)
```
🛑 **⇒ 結論:這個畫面【沒有設計稿】。**
⇒ 而依鐵則 1,「查無」要寫進 plan 並附掃過的分母 —— 以上就是。
🔴 **⇒ 所以這一片會【產生新的視覺】, 而視覺不是我能拍的**
   ⇒ 要嘛 Sean 直接說「照現在的表單長相加就好」, 要嘛請 Design session 出稿。**§5 Q0。**

---

## 1. 🔵 兩件既有的東西可以直接用, 不要重寫

```
searchManualOrderCatalog(manual-order-catalog.ts:139)—— **已經寫好了, 只是沒有人用**
  .from('product_variants').select(MANUAL_ORDER_CATALOG_COLUMNS)
  .ilike('sku', %needle%)  ← 只有這一道
  .order('sku').limit(20)
MANUAL_ORDER_CATALOG_COLUMNS(:100)= 'id, sku, price_general, products(title)'
  ⇒ **品名與售價現成** · 🔴 **經銷價 price_store 要加**
```
✅ `price_store` 我核過**它真的在 `product_variants` 上**(`20260531142533:45`)——
🔴 而它逐字標著「**經銷敏感(view 永不投射)**」⇒ 見 §4 風險。
（⚠️ 這一格我特地核, 因為今天我才拿 `delisted_at` 踩過一次「檔頭說有、而它在別張表上」。）

---

## 2. 要改什麼(而片界我先切出來)

```
片1 後端:MANUAL_ORDER_CATALOG_COLUMNS 加 price_store + 一支 server action 包 searchManualOrderCatalog
片2 前端:那格「商品編號」換成【料號輸入 + 查詢 + 帶入】, 而 variant_id 由系統填
片3 (可選)帶入時的篩選 —— 見 Q4
```
🛑 **片2 卡在 Q0(沒有設計稿)** ⇒ 片1 可以先做, 片2 要等。

---

## 3. 片型:🔴 **高風險片**(開檔判的, 不是從題目那行字)

| 類 | 命中 | 依據 |
|---|---|---|
| ①錢 order | ✅ | 動的是**建單**那條路, 而它決定訂單金額與退款分母 |
| ②權限 | ✅ | `price_store` **經銷敏感** ⇒ 它會從 server 流向一個新的地方 |
| ③schema | ❌ | 不動 migration |
| ④平台 / ⑤對外 / ⑥ui 套件 | ❌ | |
⇒ **兩類命中 ⇒ 高風險片, codex 不降級。**

---

## 4. 🔴 爆炸半徑與風險(而最重的那個是經銷價)

```
要動:manual-order-catalog.ts(欄位表)· 一支新 server action · manual-order-lines.tsx(那一格)
     · manual-order-form.ts(若「商品編號」不再由人填, 解析器那六格要重看)
```
🔴🔴 **最重的風險:`price_store` 是經銷價。**
```
Server 端鐵則逐字:「經銷價絕不傳到一般會員瀏覽器」
· 後台是 server component + service_role ⇒ **今天不違反**
· 🔴 而本片要把它【送到瀏覽器】給員工看(Sean 要「單價下面顯示 售價/經銷價」)
  ⇒ 那是後台頁面, 員工看得到是對的
  🛑 **而它會進 HTML ⇒ 任何拿得到那個頁面的人都看得到它**
  ⇒ ⇒ 而「誰拿得到後台頁面」今天由 ADMIN_REQUIRE_REAL_IDENTITY 那道閘決定, **不是我這一片**
⚠️ **⇒ 這一格要在 plan 裡標, 不要在實作時才想起來。**
```
**壞掉會怎樣**:帶入錯的價 ⇒ 訂單金額錯 ⇒ 退款分母錯(而那正是 `⟦b4-EXCEEDSDEAD⟧` 那一族)。
**Rollback**:純 app 層 ⇒ `git revert`;不動 DB ⇒ 無資料殘留。

---

## 5. 決策題(Q3 主視窗已裁 · Q0/Q1/Q2/Q4 主視窗端 Sean)

```
Q0 【新增】沒有設計稿, 這個畫面長什麼樣?
   甲 = 照現在那張表單的長相加(一個輸入框 + 一顆查詢 + 一列候選)⇒ 快, 而不保證與 OD 稿一致
   乙 = 先請 Design 出稿 ⇒ 一致, 而要等
   (我掃過 12 個 OD 專案 ⇒ 手動建單那一塊【查無】, 分母見 §0)

Q1 查哪一份目錄?(他自己標的未知:「報價單或者顧客站, 我不知道你看哪一個數據」)
   甲 = 顧客站 product_variants(現有 searchManualOrderCatalog 查的就是這個)
        ⇒ 零跨庫成本;而它是【上架到顧客站的】那一份
   乙 = 報價單那個庫 ⇒ 另一個 repo / 另一個 Supabase ⇒ 要跨庫 ⇒ 成本高、要新的憑證
   🔵 我偏甲, 而理由要他知道:**兩邊的商品集合不一定一樣** ——
      顧客站沒上架的東西, 甲查不到 ⇒ 那些會落到「純手動」那條路。
   ⚠️ 而「兩邊差幾件」我**沒有量**(跨庫, 不在我射程)。

Q2 「單價下面顯示 售價/經銷價」是哪一種?
   🔴 **而主視窗要把這題端成另一個問法, 因為它不只是顯示** ——
      **「經銷價要不要進後台頁面的 HTML」**:不論甲乙, `price_store` 都會被送到瀏覽器,
      而**任何拿得到那個後台頁面的人都看得到它**。
   甲 = **帶入售價**, 經銷價當灰字提示(讀他的原話比較像這個)
   乙 = 兩個都列, 員工點一個
   🛑 **⇒ 片1 先不動 `price_store`** —— 等這題定案(主視窗 15:2x 指示)。

✅ Q3 **已裁 = 甲(hidden, 由系統填)** —— 主視窗 2026-08-31 15:2x。
   理由用 Sean 的原話:「**商品編號(我不知道這個是啥)**」⇒ **他不需要看到它**;
   唯讀顯示只會多一個他看不懂的東西在畫面上。
   🛑 而它**不能直接拿掉**(解析器要求「這一列在席就六格全部在席」,
      `manual-order-form.ts:93/269`)⇒ **hidden 正好滿足** —— 那也是現有 spec 欄的做法
      (`manual-order-lines.tsx:161` 那個 hidden 欄的註解逐字寫著同一條理由)。

Q4 帶入的候選要不要篩?
   🔴 現有那支**沒有任何篩選** ⇒ 下架的、缺貨的、沒規格的, 只要 SKU 對得上全撈得到。
   甲 = 不篩(照現況)· 乙 = 只帶「沒下架且有貨」的
   ⚠️ 而 `product_variants` **沒有 `delisted_at`**(那個欄在 `products` 上)——
      要篩「下架」得 join `products`。⇒ 乙比看起來貴。
```

## 6. Q 之外我自己會做的
```
· 「查不到 ⇒ 純手動」⇒ 他逐字要的就是現況 ⇒ **零改動**, 我不碰
· 帶入之後員工可以改嗎 ⇒ 現況那幾格都是可編輯的 input ⇒ 保持, 不加鎖
· 突變計畫寫在做之前(照片D 的規格), 而它要等 Q0-Q4 定案才寫得出來
```

🛑 **本 plan 未經批准, 我不動任何一行。**
