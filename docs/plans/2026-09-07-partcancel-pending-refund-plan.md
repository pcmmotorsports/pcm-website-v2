# ⟦b4-PARTCANCEL1⟧ 部分取消要開待退款 —— plan(不寫碼)

> `-ship` 2026-09-07 出;主視窗 `-f8` 批「出 plan 不寫碼」。**本檔零改碼、零 migration。**
> 🛑 **鐵則 12①(錢)** ⇒ 批完才寫碼,寫完 commit 前跑 codex 對抗審查(`-s read-only`、帶 `-m`)。
> ⚠️ **本檔不估工期**(主視窗指定)。

## 1. 要改什麼

🔴🔴 **[2026-09-07 · 受詞訂正(`-ship` 查、主視窗 `-f8` 裁)—— 這一節先前問窄了]**
> **本列的標題是「部分取消」, 而那個病的受詞是【任何讓 `effective_total` 變小的動作】。**

**實錘(不是推的)**:`supabase/migrations/20260815040000_m4b_e10_13_slice1_admin_update_order_item_amount.sql:427-430`
```sql
UPDATE public.order_items
   SET unit_price = p_unit_price, line_total = v_line_total
 WHERE id = p_order_item_id;
```
⇒ 🔴 **它不經 `admin_cancel_order`, 而它會改變 `effective_total`**(`= Σ(未取消數量 × unit_price)`)
⇒ 🛑 **改單價調低而客人已付 ⇒ 已收 > 應收 ⇒ 那也該開待退款, 而今天同樣【沒有任何觸發】。**

🔴 **⇒ 因此 §4 的甲/乙【要重寫, 不要沿用】**:乙(掛在 `admin_cancel_order` 裡)**只涵蓋一條路**,
而真正要問的是「**誰讓 `effective_total` 變小**」。**§4b/4c 的爆炸半徑分析在新受詞下要重做。**

⚠️ **這一發的分母與它的誠實邊界(主視窗指定要標【讀來的還是量到的】)**:
同時掃 `order_items` 與 `order_cancellation_items` 的寫入 ⇒ **命中 28 支**;🟢 **正對照撈到三支** `admin_cancel_order` 那族(`a8a1` / `a8a2_partial_cancel` / `cancelkind`)· ⚪ 負對照 **0**。
🔴 **而「28 支裡多數是建單 `INSERT`」那句是【判準歸類】不是【逐支驗證】** ——
　 判準 = 檔名含 `create_order` / `manual_order`;**我實際開檔驗過的只有 1 支**(`20260815040000`)。
　 📌 **⇒ 不要把它讀成「其餘 27 支都查過了」。**
✅ **那 2 支開完了(2026-09-07)**:
　· `20260716130000_..._admin_update_order_item_workflow_rpc.sql:138-141` ⇒ `SET workflow_status, version, updated_at` ⇒ 🟢 **不動金額**, 排除。
　· `20260816040000_..._p2c13_detail.sql:152-155` ⇒ `SET unit_price, line_total` ⇒ 🔴 **動金額**。
🎯 **而它與 `20260815040000` 【是同一支函式的不同代, 不是兩條路】** —— `latest-definition-of.sh admin_update_order_item_amount` ⇒ **共 3 代**(`20260815040000` create · `20260816040000` cor · **`20260905360000` cor = newest, 帳本 live 同版**)。
📌 **⇒ 「改單價」是【一條路】, 而它的最新一代住在 `20260905360000`** —— **寫碼時要對的是那一代, 不是我最先撈到的那一支。**

🔴 **而我第一發的尺是壞的, 留著當警告**:我原本只問「誰 `UPDATE`/`DELETE` `order_items`」⇒ 命中 **5 支**,
　 **而正對照撈不到 `admin_cancel_order` 本身** ⇒ 成因:**它根本不改 `order_items`**, 它寫 `order_cancellation_items`
　 (`20260903093000:499`/`:504`)。⇒ 🎯 **那 5 個命中看起來完全正常 —— 有數字、有檔案、可以拿去做事,**
　 **而它們答的是另一個問題。**📌 **一把撈不到自己標準答案的尺, 它的每一個命中都不算數。**

---

**一句(原本的範圍, 保留)**:`pcm_pending_refund_on_cancel` 今天只在**整單取消**那一刻開待退款;**部分取消不開** ⇒ 那些單收了的錢**沒有任何東西提醒要退**,而**它不會叫**。

## 2. 為什麼(而它為什麼不是「漏做」)

板列逐字:部分取消不開待退款**是刻意的** —— 因為「部分取消該退多少」曾被當成沒有人答過的口徑題。
🟢 **而那個前提已經不成立**:Sean 在 `Q13` 自己給了公式(原話抄在
`supabase/migrations/20260810100000_m4b_e10_op1_order_payments_m.sql:25-26`):

> 「保留,看剩下訂單總金額多少,退掉多餘部分,不足的話也不用退款,就當作是先收多餘部分」

⇒ **退款額 = `max(0, 已收未退總額 − 取消後訂單剩餘應收總額)`**

## 3. 🔴 算式的每個名詞指到哪一欄(主視窗指定;**這一節是本 plan 的重點**)

| 名詞 | 來源 | 狀態 |
|---|---|---|
| **取消後訂單剩餘應收總額** | `public.pcm_order_effective_amounts_v.effective_total`<br>(`supabase/migrations/20260907210000_m4b_order_effective_amounts_v.sql:64-67`)<br>定義 = `Σ(未取消數量 × unit_price)` + 運費(`>=5000 ⇒ 0 else 100`) | ✅ **存在**(2026-09-07 才建) |
| **已收未退總額** | 🔴🔴 **答出來了(2026-09-07 `-ship`, 主視窗 `-f8` 派):候選那支【兩者都不是】** —— `public.order_paid_totals_v.paid_total` 的定義逐字(`20260823030000:180-191`, **讀的是 VIEW 定義不是檔頭**):`COALESCE(SUM(p.amount), 0) AS paid_total FROM public.order_payments p GROUP BY p.order_id;`,COMMENT 逐字「**每張訂單的帳本已收淨額(直接加總 amount, 沖銷列為負)**」⇒ 🎯 **它只加總 `order_payments`;人工退款 `order_manual_refunds` 【不在裡面】。** **實查**:三支會 `INSERT INTO public.order_manual_refunds` 的 migration(`20260820021000` / `20260823020000` / `20260905280000`)**同時 `INSERT INTO public.order_payments` 的次數 = 0 / 0 / 0**。🔴🔴 **給寫測試的人(主視窗 `-f8` 要求寫進本節)**:**一半扣了一半沒扣, 比「全扣」或「全不扣」都更容易寫錯** —— 全扣或全不扣的話, **任何一筆退款都會讓數字明顯不對**;而「一半扣」的話, **只有走人工退款那條路的單會錯** ⇒ 📌 **測試多半用沖銷那一種造資料(它在同一張表、比較好造)⇒ 綠;而出錯的是另一條路。** ⇒ 🛑 **所以它是「已收(含沖銷)」, 不是「已收未退」** —— **沖銷那一種扣了, 人工退款那一種沒扣。**⇒ ✅ **Q13 的「已收未退總額」= `paid_total − 人工退款總額`**, **不是** `paid_total` 本身。 | ✅ **已答**(而見下面那格) |

🛑 **⇒ 這一格答不出來之前不要寫碼。**理由是主視窗那句:
**一個看起來對的金流算式,錯的地方通常在「那個欄位到底裝什麼」,不在算式本身。**
⚠️ 而 `20260823030000` 的檔頭自己就寫著它**一天之內被推翻三次**、以及「人工那條路永遠不動這欄」**已經是假的** ⇒ **那支的語意要逐行讀完再引用。**

## 4. 🔴 觸發集合的前後差(2026-09-08 重寫 —— **舊版的甲/乙是在錯的受詞下寫的**)

> 🔴🔴 **[2026-09-08 · `-ship` 重寫;舊版整段作廢, 不要沿用]**
> **舊版問的是「取消要不要開待退款」** ⇒ 它把觸發面定義在**動作**上(取消)。
> ✅ **本版改成定義在【那個數字的輸入】上** —— 因為病的受詞是
> **「任何讓 `effective_total` 變小的動作」**, 而**動作是會長出新的, 輸入不會。**

### 4-0. 先把那個數字拆開(**這一節是本節的地基, 不是背景**)

`pcm_order_effective_amounts_v`(`20260907210000:91-102`)逐字:
```
effective_subtotal = Σ( (oi.quantity − Σ ci.cancelled_quantity) × oi.unit_price )
effective_shipping = CASE WHEN o.shipping_method = 'store' THEN 0
                          WHEN effective_subtotal >= 5000  THEN 0 ELSE 100 END
effective_total    = effective_subtotal + effective_shipping
```
🎯 **⇒ 它只有四個輸入**:`order_cancellation_items.cancelled_quantity` ·
`order_items.unit_price` · `order_items.quantity` · `orders.shipping_method`。
📌 **⇒ 「哪些動作會讓它變小」不是用想的, 是【誰寫得到這四欄】。**

### 4-1. 誰寫得到那四欄(全 repo 實查, 2026-09-08)

| 輸入欄 | 會寫它的路 | 今天會不會開待退款 |
|---|---|---|
| `order_cancellation_items.cancelled_quantity` | `admin_cancel_order` 的**部分取消**(`p_items` 非 NULL;`20260903093000:177` 逐字 `v_partial := p_items IS NOT NULL;`) | 🔴 **不會** —— 部分取消時 `orders.cancelled_at` 不變 |
| `order_items.unit_price` | `admin_update_order_item_amount` **改單價**<br>🔴 最新一代 `20260905360000:972`(**共 3 代**, `latest-definition-of.sh` 查;`:1080-1083` 逐字 `SET unit_price = p_unit_price, line_total = …`) | 🔴 **不會** |
| `order_items.quantity` | ⚪ **實查:零路** —— 全 repo `UPDATE … order_items` 共 6 處,`SET` 的欄只有 `unit_price` / `line_total` / `workflow_status` / `version` / `updated_at`(`20260716130000:138-141` · `20260716120000:67-72` 只動 `workflow_status`) | —(**今天不存在, 而它不是永遠**) |
| `orders.shipping_method` | 🔴🔴 **`admin_update_order_workflow`(`20260714130000:209` 逐字 `shipping_method = v_shipping_method,`)** ⇒ **`home` 改成 `store` ⇒ 運費 100 ⇒ 0** | 🔴 **不會** |

🔴🔴 **⇒ 第四條是這一次重寫【新撿到的】** —— 前一版(含主視窗那句「受詞是兩條」)**沒有它**。
📌 **而它撿到的方式正是本節改法的證據**:**我不是想到「還有改運費」, 是把那個 view 的輸入列出來之後, 它自己掉出來的。**
⚠️ **證不到什麼**:①我**沒有**驗那條路今天在後台**按不按得到**(那要開 UI, 見 `⟦…⟧` probe)②`shipping_method` 由 `home ⇒ store` 只掉 **100**, **金額小而它仍然是「已收 > 應收」** ⇒ **要不要為 100 元開一筆待退款, 那是口徑題, 不是工程題** ⇒ 🛑 **本 plan 不替它拍板。**
　🔴 **而端這題出去的時候必須附這一句(主視窗 A 指定)**:
　> **「不開的話, 那 100 元停在客人那邊而沒有任何紀錄。」**
　📌 **口徑題要讓答的人看見【兩邊的殘留物】, 不只看見金額大小** ——
　**「才 100 元」與「一筆沒有紀錄的錢」是同一件事的兩種問法, 而它們會得到不同的答案。**

### 4-2. 現在的觸發面(逐字)

`20260901080000_m4b_autorefund_pending_refunds.sql:495-496`:
```
CREATE TRIGGER order_pending_refund_open_au
  AFTER UPDATE OF cancelled_at ON public.orders
```
`:377` 逐字「只在【由無變有】那一刻發火」⇒ **涵蓋:整單取消 · 一連串部分取消把最後一件也取消掉**(那時 `cancelled_at` 才變)。
🎯 **⇒ 四個輸入裡, 它一個都沒有盯** —— 它盯的是**取消這個動作的副作用**。

### 4-3. 三條掛法(**plan 階段不選, 而這一版把選項換掉了**)

| | 甲 掛在四個輸入的**資料層** | 乙 在每支會寫那四欄的函式裡**顯式呼叫** | 🔴 丙 **重算式**:一支 `recompute_pending_refund(order_id)`,四條路各自呼叫 |
|---|---|---|---|
| **壞掉時壞的是誰** | 🔴 所有動到那四欄的路(含未來新增的) | 🟢 只有被改的那一支 | 🟡 只有那一支重算函式 |
| **會不會漏觸發** | 🟢 不會(資料層繞不過) | 🔴 **會** —— 任何不經那些函式的路都漏(而 `order_items.quantity` **今天零路** ⇒ 📌 **明天多一條路的時候, 乙【安靜地漏掉它】**) | 🔴 會(同乙),而**漏的位置只有一處要補** |
| **重複開立** | 🔴 一次部分取消可能觸發多欄 ⇒ **要自己去重** | 🟢 一次呼叫一次 | 🟢 **重算式天生冪等**(算出來覆寫, 不累加) |
| **本列列尾那三題的①(覆寫還是累加)** | 需另外答 | 需另外答 | 🎯 **不必答 —— 重算就是覆寫** |

🔴 **`-ship` 的建議(未拍)= 丙**,理由**不是**「比較好寫」:
📌 **甲乙都要回答「這一次要不要覆寫上一次」, 而丙讓那個問題不存在。** 而那正是本列列尾三題的第一題。
⚠️ **代價要一起寫**:丙**仍然要有人去呼叫它** ⇒ **它把「漏觸發」從四處縮成一處, 沒有消滅它。**
🟢🟢 **[2026-09-08 主視窗 A 批准丙, 而把下面那句升成【必要條件】]**
🛑🛑 **丙 = 一支 `recompute_pending_refund(order_id)` ＋ 一道【資料層的對帳】—— 兩件一起, 不可以分兩片做。**
(對帳 = 定期或收工時比 `effective_total` 與待退款列。)
📌 **理由(主視窗逐字)**:**丙把漏觸發從四處縮成一處 —— 而一處也是一處;對帳是那一處漏掉時唯一會叫的東西。**
🔴 **⇒ 那不是加分項, 是丙成立的前提** —— **否則丙 = 乙 + 一層包裝。**

### 4b. 🔴 爆炸半徑(舊版兩條路的比較保留在 git 歷史;本版見 §4-3 表)

🛑 **舊版 4b 的甲/乙是在「只有部分取消」那個前提下寫的** ⇒ **整段作廢。**
📌 **而它作廢的方式值得記**:舊版兩條路**都是對的**, 錯的是**它們回答的問題** ——
**「掛在取消上, 還是掛在 `order_items` 上」在受詞是四個輸入之後, 兩個答案都不完整。**

### 4c. 🔴 「原本那條路失敗的話,那個動作本身會不會跟著失敗?」(主視窗指定必答)

> 🔵 **[2026-09-08 受詞跟著 §4 放寬]** 舊版問的是「**取消**會不會跟著失敗」;
> 而受詞是四個輸入 ⇒ **「那個動作」= 取消 / 改單價 / 改運送方式,三條路各問一次。**

**先講事實**:甲乙丙**都在同一個交易裡** ⇒ **預設全部「跟著失敗」** —— 除非顯式包 `EXCEPTION`。
⇒ 📌 **所以這不是「會不會」,是【要不要】** —— 而**兩個答案都可以接受,必須是選的**:

- **選「跟著失敗」**(不包 EXCEPTION):整筆回滾 ⇒ 🟢 **員工當場看得到、可以重按**;
  🔴 代價 = **開待退款這件事會擋住那條路**,而**取消與改單價都是天天在按的**。
- **選「不跟著失敗」**(包 EXCEPTION 吞掉):動作成功而退款列沒開 ⇒ 🛑 **沒有人會知道**
  ⇒ 📌 **那正是這一列本來要修的病**,等於原地繞一圈。

🔴 **`-ship` 的建議(未拍)= 跟著失敗**,理由:**吞掉例外會複製這一列自己的病**。
⚠️ **而代價在新受詞下【變大了】**:舊版只擋取消一條路,**新版會擋三條**。
🛑 **若選「跟著失敗」,`EXCEPTION WHEN OTHERS` 那一族的坑要一起看** ——
見 `⟦b4-NCPCANCELROLLBACK⟧`(同 repo、同款結構、`-ship` 已出 plan)。**兩片會撞在同一個地方。**

🔵 **而 §4-3 選丙的話這一題【不會消失】** —— 重算式一樣在同一個交易裡。

🔴🔴 **[2026-09-08 · 本裁定要重判, 不要沿用]**
> **上面那個「跟著失敗」的建議, 是在【只擋取消一條路】的前提下做的 —— 而那個前提變了。**
📌 **新前提**:受詞是三條路(取消 · 改單價 · 改運送方式), 而**三條都是天天在按的**
⇒ 🛑 **「開待退款失敗就整筆回滾」的代價從一條路變成三條路。**
✅ **主視窗 A 2026-09-08 說它會重判** ⇒ **寫碼的人不要照上面那句走, 先看它的新裁定。**
⚠️ 而我**沒有量**那三條路各自今天被按幾次 ⇒ **「天天在按」是板列與函式用途的字面, 不是我的讀數。**

## 5. 影響面

- **金流**:會**新增** `order_pending_refunds` 列 ⇒ 進「上限」的分母。🔴 板列逐字:**猜一個金額比不記還糟 —— 一個錯的上限比沒有上限難查。**
- **既有整單取消那條路**:不可回歸 ⇒ 需要「整單取消仍然只開一筆、金額不變」的斷言。
- **重複開立**:一張單被部分取消**多次**時,要決定覆寫還是累加 ⇒ 🔴 **那是本列列尾那三題的①,而它「讀碼就答得出來」**(板列逐字),**不必等 Sean**。

## 6. rollback / 回頭路(🔴 Sean `Q76`:回頭路寫在寫 migration【之前】)

- **`.down.sql` 先寫**:`DROP TRIGGER`(新增的那支)+ `DROP FUNCTION`(新增的那支),**不動 `order_pending_refunds` 的資料**。
- 🛑 **已經開出去的待退款列【不刪】** —— 它們是「有人該拿回錢」的紀錄;回滾的是**機制**,不是**帳**。
- ⚠️ **而回頭路擋不住的**:回滾之後那些列**留著**,而**沒有機制再開新的** ⇒ 回到今天的狀態,**且多了一批已開的**。**那個代價要在批准時知道。**

## 6b. 🔴 那一格的【真資料那一半驗不動】—— 明說, 不用「應該是」補

主視窗 `-f8` 指定第 3 步:**去問正式庫, 而兩個世界要能分開**(造一個「有收款也有退款」的情境)。
**唯讀量到(零寫入)**:`order_payments` **1 列** · `order_manual_refunds` **0 列** · `order_payments where amount < 0`(沖銷負列)**0 列** · 負對照 **0**。

🛑 **⇒ 今天的正式庫【造不出那兩個世界】** —— 沒有任何一張單同時有收款與退款,也沒有任何一列沖銷 ⇒ **`paid_total` 在「有扣」與「沒扣」兩個世界會印同一個數(都是那 1 筆)。**
📌 **⇒ 這一格的結論【只有碼那一半的證據】**:三支 migration 的 `×0` 是**字面實查**,而**行為上沒有被真資料驗過**。⚠️ **寫碼時要自己造那兩個世界**(拋棄式 PG), 不能等正式庫長出資料。

## 7. 本 plan 證不到什麼

1. ⛔ ~~我沒有讀完那支 view 的定義(只讀了檔頭)⇒ 第 3 節那一格是候選不是答案~~ ⇒ ✅ **已讀完並答出**(見第 3 節與 6b);而**新的證不到**是:**那個結論沒有被真資料驗過**(見 6b)。
2. **我沒有量部分取消今天發生過幾次** ⇒ 「會發生的路徑」是板列的字面,不是我的讀數。
3. **我沒有碰正式庫** ⇒ 全部是讀 repo。
4. 🔴 **[2026-09-08 加]** §4-1 那張表答的是「**誰在 SQL 層寫得到那四欄**」——
   它**答不出「今天在後台按不按得到」**(那要開 UI probe)。⇒ 📌 **一條 SQL 層存在的路,
   可能在畫面上根本沒有入口;而反過來不會發生。** 所以那張表是**上界**。
5. 🔴 **[2026-09-08 加]** `order_items.quantity` 的「**零路**」是**字面實查**
   (6 處 `UPDATE … order_items` 的 `SET` 欄)⇒ **動態 SQL 寫的、或 app 層直接寫的,我沒掃。**
   🛑 **而這個 0 最危險的用法是拿它去簡化設計** —— 「反正沒人改數量」會讓掛法選乙。
6. 🔴 **[2026-09-08 加]** §4-3 建議選丙(重算式)是**設計判斷, 沒有讀數支持** ——
   我**沒有量**四條路各自今天被呼叫幾次, 也**沒有**寫過一版丙來證明它真的冪等。

---

## 8. 🔴🔴 [2026-09-08 · 線【退款】`-refund` 接手實作時開檔核到的兩個洞 —— **本 plan 從頭到尾沒問過它們**]

> **本節不是補充,是【擋寫碼】。**兩個洞都會改變 migration 的形狀,不是實作細節。
> ⚠️ **證據等級**:以下全部是**讀 SQL 檔的字面**(座標都在)。**我沒有碰正式庫。**

### 8-1. 🔴 算式是【訂單級】,而那張表是【逐軌】—— 中間缺一個分攤規則

`§2` 的 Q13 算式:`退款額 = max(0, 已收未退總額 − effective_total)` ⇒ **一個訂單級的數字**。
而落點 `public.order_pending_refunds`(`20260901080000`)是**逐軌**的:
```sql
rail             text   NOT NULL CHECK (rail IN ('bank_transfer', 'cash'))
amount_at_cancel bigint NOT NULL CHECK (amount_at_cancel > 0)
-- 唯一索引 order_pending_refunds_live_order_rail_key ⇒ 每軌 live 一列
```
🛑 **⇒ 怎麼把一個訂單級的數字拆到各軌,沒有任何一節答過。**

**具體形狀(構造的例子,不是真資料)**:
```
一張單 匯款收 3000 + 現金收 2000 = 5000;部分取消後 effective_total = 4000 ⇒ 多收 1000
❓ 那 1000 記在【匯款】那一軌還是【現金】那一軌?
```
📌 **⇒ 而 §4-3 選丙(重算式)【沒有讓這一題消失】** —— 重算式一樣要決定它算出來的數字往哪一軌寫。
🔴 **⇒ 「丙天生冪等」只解掉了列尾三題的①(覆寫還是累加),沒有解掉「拆到哪一軌」。**

### 8-2. 🔴🔴 `paid_total` 含【卡】,而那張表【裝不下卡】—— 同一個 F3 洞從另一邊被打開

```
既有 trigger 的軌值域是【手抄副本】:20260901080000:441 逐字
    FROM (VALUES ('bank_transfer'), ('cash')) AS r(rail)
而值域的主人 order_payments.rail 有【三值含 card】(20260810100000:189)
```
⇒ **`order_paid_totals_v.paid_total` 是 `SUM(p.amount)` 不分軌**(`20260823030000:188`)⇒ **刷卡的錢在裡面。**
🛑 **⇒ 用訂單級 `paid_total` 去減,那筆【卡的多收】會被算出來,而它無處可寫** ——
　 要嘛撞 `CHECK (rail IN (...))`,要嘛**被靜默塞進錯的軌**。

🎯 **⇒ 而這正是本表 `COMMENT` 裡 F3 那條警告的形狀**(逐字:「**加 rail 的人要回來改這裡**」)——
📌 **而這一次不是「加 rail」,是【換一個層級去讀它】。⇒ 同一個洞從另一邊被打開,**
**而那句警告的字面【擋不住這種進來的方式】** —— 它只叫住了「加 rail 的人」。
🔴 **⇒ 一句寫對的警告,擋得住它想像得到的那條路;而它想像不到的那條路上,它一個字都不會說。**

### 8-3. ⚠️ 而這兩個洞今天【驗不出來】—— 這一格要跟著決定一起帶走

`§6b` 已經量過:正式庫 `order_payments` **1 列** · `order_manual_refunds` **0 列** · 沖銷負列 **0 列**。
🛑 **⇒ 今天造不出「一張單有兩條軌」或「有卡也有匯款」的情境** ⇒ **不論選哪一條,它都會全綠。**
📌 **⇒ 所以這兩題【不能靠測試選出來】,只能靠拍板。**而測試要造的是**合成的兩軌情境**(拋棄式 PG),
**並且在測試註解裡寫明它是合成的** —— 否則下一個人會把那個綠讀成「真資料驗過了」。

### 8-4. 三條掛法(**已端主視窗 A,未拍**)

| | 甲 逐軌各自算 | 乙 訂單級算完按已收比例拆 | 丙 訂單級算完,只開非卡軌 |
|---|---|---|---|
| **分攤規則** | 🔴 仍然要一個(該軌分攤多少 `effective_total`) | 🟢 有(按已收比例) | 🟢 有(卡的部分不處理) |
| **卡的那一份** | 🟡 不會被算出來 | 🟢 **算得出來 ⇒ 看得見** | 🟡 明文不處理 |
| **卡的那一份寫得進去嗎** | — | 🔴 **寫不進** ⇒ 要另開一條路 | 🟢 不寫(而要寫死一句) |
| **射程** | 全軌 | 全軌 | 🔴 **明文縮小** |

🔴🔴 **`-refund` 傾向丙,而【它不敢自己拍】,理由要寫下來**:
> **丙 = 明文縮小這個機制的射程。而縮小射程這件事在 diff 上看不出來 —— 它長得跟「我做完了」一模一樣。**
⇒ 📌 **那是要拍板的形狀,不是實作者可以自己判的。**
🔵 而丙的理由是真的:**卡的退款本來就是另一條線**(`⟦f3-AUTOREFUND2⟧` 自動退刷)。
🛑 **而若選丙,`COMMENT` 裡必須寫死一句「本機制不涵蓋 card 軌」** —— 否則下一個人會以為它涵蓋,
**而「以為它涵蓋」與「它真的涵蓋」在任何一發綠上都長得一樣。**

### 8-5. 🟢🟢 [2026-09-08 · 主視窗 A 裁定 —— **兩題都拍了,而 Q2 兩條都不是**]

**Q1 = C:跟著失敗,而【對帳獨立於交易之外】。**
🔴 A 的限定(逐字要點):**「對帳獨立」不是加一句話,是【對帳必須是另一支定期跑的東西,不是同一個交易裡的一段碼】。**
`§4-3` 那句定義本來就寫著「**定期或收工時**比 `effective_total` 與待退款列」⇒ 它本來就在交易外。
⇒ **C 實際上 = A（跟著失敗）＋ 把「對帳在交易外」寫成【驗收條件】**,而不是靠註解。
✅ **驗收要看得到的形狀**:對帳那支**不在** trigger / function 的呼叫鏈上(**附 grep 座標**),
　 而它**有自己的排程或呼叫點**。

**Q2 = D(A 加的第四條;`§8-4` 的甲/乙/丙都不採納)。**
```
D) 算式用【訂單級、含卡】(= 乙的算法, 數字才是對的)
   而【該訂單有任何 card 收款時 ⇒ 不寫列, 改由對帳報出來給人看】
```
🔴 **為什麼不是丙(我推的那條)—— A 的理由,而它推翻我的地方很具體**:
> 丙只把卡從【**寫入**】那半排除,而**沒有把卡從【算式】那半排除**。
> 一張單刷卡 1000 ＋ 匯款 3000、`effective_total` 掉到 3500 ⇒ 多收 500,
> 而丙會把那 500 **整筆記在匯款那一軌**。
📌 **⇒ 那不是「射程縮小」,是【算出一個對的總數、寫到錯的軌上】** —— **而它會全綠。**

🟢 **D 的三個好處(A 逐字要點)**:
```
① 數字含卡 ⇒ 不會少算
② 不猜怎麼拆 ⇒ 不用發明分攤規則
③ 🔴 射程限制由【行為】執行, 不由【註解】執行
```
🎯 **③ 正是我那句「縮小射程在 diff 上看不出來」的直接反用**:
**一句寫死的註解擋不住下一個人,一個 `IF 有卡 ⇒ 不寫` 擋得住。**

🛑 **D 要付的代價(A 指定寫明)**:
混軌單今天**造不出來**(`§6b`:正式庫 `order_payments` **1 列**)⇒ **「有卡就不寫」這條路今天無法用真資料驗**
⇒ 🔴 **它必須有【負對照測試】**:合成一張含卡的單 ⇒ 斷言 **零列** ＋ **對帳報得出來**。
📌 **⇒ 沒有那一發負對照,「有卡就不寫」與「這段碼根本沒跑」印同一個結果(都是零列)。**

### 8-6. 🔴 D 讓「有卡的單」變成【對帳的輸入】⇒ 對帳那支的規格要跟著長一格

**新增的規格格子**:對帳除了比 `effective_total` 與待退款列,還要**報出「因為含卡而沒有開列」的那些單**。
🛑 **⇒ 否則 D 的第 ② 半(「改由對帳報出來給人看」)在碼裡不存在** ——
**而它不存在的時候,D 讀起來仍然完整**:`IF 有卡 ⇒ 不寫` 那一半自己就跑得很好,
📌 **一個只做了前半的 D,在行為上等於「有卡的多收錢靜靜地消失」** —— 而那正是本列本來要修的病。
✅ **⇒ 驗收條件**:對帳的輸出裡要看得到那一族(而不只是「待退款列與 `effective_total` 對不上」那一族)。
