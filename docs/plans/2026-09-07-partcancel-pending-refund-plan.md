# ⟦b4-PARTCANCEL1⟧ 部分取消要開待退款 —— plan(不寫碼)

> `-ship` 2026-09-07 出;主視窗 `-f8` 批「出 plan 不寫碼」。**本檔零改碼、零 migration。**
> 🛑 **鐵則 12①(錢)** ⇒ 批完才寫碼,寫完 commit 前跑 codex 對抗審查(`-s read-only`、帶 `-m`)。
> ⚠️ **本檔不估工期**(主視窗指定)。

## 1. 要改什麼

**一句**:`pcm_pending_refund_on_cancel` 今天只在**整單取消**那一刻開待退款;**部分取消不開** ⇒ 那些單收了的錢**沒有任何東西提醒要退**,而**它不會叫**。

## 2. 為什麼(而它為什麼不是「漏做」)

板列逐字:部分取消不開待退款**是刻意的** —— 因為「部分取消該退多少」曾被當成沒有人答過的口徑題。
🟢 **而那個前提已經不成立**:Sean 在 `Q13` 自己給了公式(原話抄在
`supabase/migrations/20260810100000_m4b_e10_op1_order_payments_m.sql:25-26`):

> 「保留,看剩下訂單總金額多少,退掉多餘部分,不足的話也不用退款,就當作是先收多餘部分」

⇒ **退款額 = `max(0, 已收未退總額 − 取消後訂單剩餘應收總額)`**

## 3. 🔴 算式的每個名詞指到哪一欄(主視窗指定;**這一節是本 plan 的重點**)

| 名詞 | 來源 | 狀態 |
|---|---|---|
| **取消後訂單剩餘應收總額** | `public.pcm_order_effective_amounts_v.effective_total`<br>(`supabase/migrations/20260907210000_m4b_order_effective_amounts_v.sql:64-67`)<br>定義 = `Σ(未取消數量 × unit_price)` + 運費(`>=5000 ⇒ 0 else 100`) | ✅ **存在**(2026-09-07 才建) |
| **已收未退總額** | 🔴 **未確認** —— 候選 `20260823030000_m4b_841_order_paid_total_view.sql`,而**我沒有確認它的定義是「已收未退」還是「已收」**;扣除側另有 `public.order_manual_refunds`(`20260901080000:145-146` 逐字「扣除側讀它」) | 🔴 **本 plan 的第一件待辦** |

🛑 **⇒ 這一格答不出來之前不要寫碼。**理由是主視窗那句:
**一個看起來對的金流算式,錯的地方通常在「那個欄位到底裝什麼」,不在算式本身。**
⚠️ 而 `20260823030000` 的檔頭自己就寫著它**一天之內被推翻三次**、以及「人工那條路永遠不動這欄」**已經是假的** ⇒ **那支的語意要逐行讀完再引用。**

## 4. 🔴 觸發集合的前後差(兩張清單並列)

**現在**(`20260901080000_m4b_autorefund_pending_refunds.sql:495-496`):
```
CREATE TRIGGER order_pending_refund_open_au
  AFTER UPDATE OF cancelled_at ON public.orders
```
⇒ 只在 `orders.cancelled_at` **由 NULL 變非 NULL** 那一刻(`:377` 逐字「只在【由無變有】那一刻發火」)。
⇒ 涵蓋:整單取消 · 一連串部分取消**把最後一件也取消掉**(那時 `cancelled_at` 才變)。

**加完之後**(待定,**本 plan 不預設形狀**):要涵蓋「**訂單仍有未取消品項,而某些品項被取消**」那一刻。
🔴 **而【掛在哪】有兩條路,plan 階段不選**:
- 甲:掛 `order_items`(或取消明細表)的變動
- 乙:留在 `orders`,但改由 `admin_cancel_order` 的 `p_items` 路徑顯式呼叫
⚠️ **兩條路的爆炸半徑不同**,而選它要先答第 3 節那一格。

## 5. 影響面

- **金流**:會**新增** `order_pending_refunds` 列 ⇒ 進「上限」的分母。🔴 板列逐字:**猜一個金額比不記還糟 —— 一個錯的上限比沒有上限難查。**
- **既有整單取消那條路**:不可回歸 ⇒ 需要「整單取消仍然只開一筆、金額不變」的斷言。
- **重複開立**:一張單被部分取消**多次**時,要決定覆寫還是累加 ⇒ 🔴 **那是本列列尾那三題的①,而它「讀碼就答得出來」**(板列逐字),**不必等 Sean**。

## 6. rollback / 回頭路(🔴 Sean `Q76`:回頭路寫在寫 migration【之前】)

- **`.down.sql` 先寫**:`DROP TRIGGER`(新增的那支)+ `DROP FUNCTION`(新增的那支),**不動 `order_pending_refunds` 的資料**。
- 🛑 **已經開出去的待退款列【不刪】** —— 它們是「有人該拿回錢」的紀錄;回滾的是**機制**,不是**帳**。
- ⚠️ **而回頭路擋不住的**:回滾之後那些列**留著**,而**沒有機制再開新的** ⇒ 回到今天的狀態,**且多了一批已開的**。**那個代價要在批准時知道。**

## 7. 本 plan 證不到什麼

1. **我沒有讀完 `20260823030000` 那支 view 的定義**(只讀了檔頭)⇒ 第 3 節那一格是**候選不是答案**。
2. **我沒有量部分取消今天發生過幾次** ⇒ 「會發生的路徑」是板列的字面,不是我的讀數。
3. **我沒有碰正式庫** ⇒ 全部是讀 repo。
