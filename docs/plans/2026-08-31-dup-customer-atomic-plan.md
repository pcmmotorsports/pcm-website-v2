# Plan:後台建客人的重複偵測不是原子的 —— 而「同一個人」這件事,系統判不出來

> 線【出貨】`-1e` · 板上落點 `⟦b4-DUPACC1⟧` · Sean 批 🔟「甲 現在修」
> 量測 @ **2026-08-31 16:0x** · HEAD `26597f13` · 工作樹 **0**
> 🛑 **本檔是提案,一行碼都沒動。標 L1 / 高風險片(鐵則 12③ DB + 交易)。**

## 0. 那一列還成不成立 ⇒ **成立,而我是用碼證的不是讀板子**

```
bash scripts/is-this-still-true.sh "b4-DUPACC1"
① 三顆相關 commit **全是 docs 或 UI**(5f00d26b 訂正關掉條件 / 6d4f4323 建單面板找客人 / e1c8eafe 開列)
   ⇒ **沒有一顆動到原子性**
```
✅ **兩個錨我逐一開檔核過(用字面不用行號)**:
```
`manual-customer-actions.ts` 含「建之前先看一眼「這個人是不是已經在裡面了」」 ⇒ **1 處命中**
`manual-customer.ts:443` 逐字「⚠️ **誠實揭示(codex R3):這一發【不是】與 createUser 同一個原子操作。**」
   ⇒ 📌 **那支檔【自己】說它不是原子的 —— 這不是我推的。**
`supabase/migrations` 底下對 `customers` 的唯一約束 ⇒ **零命中**(沒有人偷偷加過)
```

## 1. 🔴 而這一片最重要的一段是【不能做什麼】

板上那條**原始關掉條件已作廢**(`5f00d26b`,codex R7 抓、主視窗裁),而理由要抄下來:
```
⛔ ~~加一條「manual + 正規化電話 + 姓名」的唯一約束~~
🔴 它與一個**合法的世界**矛盾:**同名、共用市話的第二個人(一家人)** ⇒ 那個人**永遠建不進去**。
📌 ⇒ 原病是「同一個人被建成兩個帳號」,而那個修法會變成
   「**兩個不同的人只能有一個帳號**」—— **它做出了自己要修的那個病的反面。**
```
🛑 **而板上那句「為什麼它會被照著做」我要原樣帶進本 plan**:
> 那條關掉條件**讀起來完全合理**,而且它是一個**機械的、可驗收的、寫得出 `grep` 的**條件。
> 📌 **一個可以 `grep` 的關掉條件,比一個模糊的更容易被照著做 —— 而那正是它錯的時候傷害最大的原因。**

⇒ 🔴 **本 plan 因此不提任何「用欄位組合當身分鍵」的方案。**
**系統判不出「是不是同一個人」;判得出來的是【人】。**

## 2. 現行關掉條件(板上訂正後那一版)= 三件事

```
① DB 鎖(advisory lock 或等價)
② 拿到鎖之後【重新檢查】一次
③ 撞到疑似重複 ⇒ **要求員工明確選擇**(而不是替他決定)
```
🔵 **而 ① 有現成成例可抄,不必自己發明**:
```
supabase/migrations/20260613130000_m3_3ds_0b_cart_session_dedup.sql
與 20260612150000 / 20260613140000 ——
形狀 `PERFORM pg_catalog.pg_advisory_xact_lock(pg_catalog.hashtextextended(<key>::text, 0));`
(實例見 …0c…sql:389,對 `customer_user_id` 上交易級鎖)
```
📌 **⇒ 交易級(`_xact_`)⇒ 交易結束自動放,不需要人記得解鎖。**

## 3. 分片

### 片 1 · migration:一支受控的「建客人」RPC(**本片的全部**)
```
· 交易內:先 `pg_advisory_xact_lock(hashtextextended(<正規化電話>, 0))`
  🛑 **鎖的是【電話】而不是【姓名+電話】** —— 鎖只需要讓「同一支電話的兩個請求排隊」,
    它**不決定他們是不是同一個人**。⇒ 📌 **鎖是排隊機制, 不是身分判準。**
· 拿到鎖後【重新查一次】現有 manual 客人
· 命中 ⇒ **回一個「疑似重複 + 那一筆的識別」給呼叫端, 而【不建】**
  ⇒ 由 UI 去問員工(② 與 ③)
· 沒命中 ⇒ 建
```
🔴 **而它答不出的那一格要寫在碼裡**:`auth` 帳號的建立在另一個系統(Supabase Auth),
**本 RPC 的交易涵蓋不到它** —— 那正是 `manual-customer.ts:443` 自陳的那一句。
⇒ **⇒ 所以本片把窗口【縮小】到鎖住的那一段,而不是宣稱它消失了。**

### 片 2 · UI:把「疑似重複」變成一個員工看得懂的選擇
⚠️ **不在本 plan 的驗收範圍** —— 它要文案與版面,而那是 Sean 的面。先做片 1。

## 4. Rollback

| 片 | 能不能單獨 revert | 備註 |
|---|---|---|
| 1 | ✅ 能 | `DROP FUNCTION`;而**呼叫端要一起回**,否則 UI 打一支不存在的 RPC |
| 2 | ✅ 能 | 未開始 |

🔴 **revert 不回來的**:期間**已經被建出來的重複帳號** —— 它們不會因為 revert 而合併。
⇒ 而「怎麼合併兩個已經存在的帳號」**不在本片**,也還沒有人做過。

## 4.5 🔵 Q1 我自己查了(唯讀正式庫,2026-08-31 16:0x)—— 兩格答案都改變了設計

### ① 正式庫**已經有一組**同姓名同電話的客人 —— **而它不是這個 bug 的證據**

```
數法 group by btrim(name), regexp_replace(phone,'[^0-9]','','g') having count(*)>1
⇒ **1 組**(2 筆)· 姓名指紋 13a88d06… · 電話指紋 0c936e85…(未撈明文)
   建立日 **2026-08-18** 與 **2026-08-19** —— **相隔一天**
🔵 正對照:同一把尺只看姓名 ⇒ 同樣 1 組(尺會動)
```
🔴🔴 **而那一組【不能】拿來當本片的理由,理由是時間**:
```
那道預檢是 `6d4f4323` **2026-08-28** 才進來的(git log -S'建之前先看一眼')
⇒ 而那兩筆是 **08-18 / 08-19** ⇒ **它們比預檢早了十天。**
⇒ 📌 **它既不證明預檢壞掉, 也不證明並發缺口存在** —— 它證明的是【預檢之前的世界】。
⇒ 而「相隔一天」本身就說明它**不是並發** —— 本片那道鎖擋不住它, 也不該擋。
```
🛑 **⇒ 我差一點把它寫成「這個 bug 已經發生過」。那會是一句有數字、有指紋、而結論錯的話。**
⚠️ **未確認**:那兩筆是不是 `manual` 客人 —— 要讀 `auth.users.raw_app_meta_data`,
而唯讀帳號 `permission denied for schema auth`。**查不到, 不是沒有。**

### ② 🔴 **11 / 14 個客人【沒有電話】** —— 而我片 1 的鎖是鎖電話

```
數法 count(*) filter (where regexp_replace(coalesce(phone,''),'[^0-9]','','g')='')
⇒ **11**(全部 14 · 不同姓名 13)
```
🛑 **⇒ 照我 §3 那個設計,14 個裡有 11 個【根本不會被鎖到】。**
📌 **⇒ 一把只在 3/14 的資料上會動的鎖, 它的綠沒有意義。**
⇒ **⇒ 這一格必須先解, 而它是 Q2** —— 我**不猜**:
```
· 若後台建客人【強制要電話】⇒ 那 11 筆是別的路徑進來的(自助註冊?)⇒ 本片本來就不管它們
· 若【不強制】⇒ 鎖的鍵要換, 而換成什麼是設計題
🔴 而我【沒有查】那個表單強不強制 —— 那是下一步, 不是猜得出來的。
```

## 5. 🛑 這份 plan 答不出什麼

```
· ✅ ~~正式庫現在有沒有重複帳號 ⇒ 我沒查~~ **已查, 見 §4.5** —— 有 1 組, 而它早於預檢十天
  ⇒ **不能當本片的理由**。而「要不要清掉那一組」是另一題(**未問**)。
· 🔴 `auth.users` 那一側的建立**不在同一個交易裡** —— 本片縮小窗口, **不消滅它**。
  ⇒ 真正消滅要嘛把 auth 建立搬進同一個交易(做不到, 它是外部系統),
    要嘛接受「先建 auth 再回頭補償」那條路(**未評估**)。
· 我沒有量過「兩位員工同時建同一人」在正式庫上發生過幾次 —— 那需要看歷史, 而
  重複帳號**在兩邊都查得到**(板上那句)⇒ **它沒有留下「這是重複」的痕跡。未數, 而且事後難查。**
· 🔴 鎖的粒度用【正規化電話】—— 而**14 個客人裡 11 個沒有電話**(§4.5 ②)
  ⇒ **那把鎖在 11/14 的資料上不會動。** 這是本 plan 最大的未解格, 而它擋著片 1。
```

## 6. 要拍板的

```
Q1: ✅ **已自答**(§4.5)—— 有 1 組而它早於預檢十天 ⇒ **不改變片1 的必要性, 也不支持它**
Q2: 🔴 **11/14 沒電話 ⇒ 鎖的鍵要重想。** 而先決條件是:**後台建客人的表單強不強制電話?**
    ⇒ **我沒查, 而那一查就知道** ⇒ 我建議**先查那一格再定片1 的鍵**, 不要現在拍
Q3: 那一組已存在的重複要不要處理 ⇒ 另開一列 | 本片一起 | 不動
Q3: 片1 的 RPC 授給誰 ⇒ 照 admin_* 慣例 service_role,而我會【查了才寫】
```
