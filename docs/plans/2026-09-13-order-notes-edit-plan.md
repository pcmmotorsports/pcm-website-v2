# Plan · 訂單備註「可以修改」(交辦 ④ 的另一半)

> **2026-09-13 A 窗寫。鐵則 8 的 plan,零實作** —— 沒有 migration、沒碰 `.sql`、沒改一行碼。
> 主視窗 `pcm-website-v2-4f` 2026-09-13 派工,明文「零實作、寫完停下」。
> 基準 `4abf005a8`(分支 `agent/shop-6`)。姊妹片(刪除)= `2026-09-13-order-notes-edit-delete-plan.md`,已實作、貼板 138 已貼。

---

## 0. 🔴 最重要的一句:**這一片要修的那件事,今天已經能做了**

**「備註可以連續修改」不需要任何 migration、不需要改一行碼 —— 它現在就能用。**

我在拋棄式 PG + 真瀏覽器上實跑過(§1.2 / §1.3),而且**負對照也跑了**。
⇒ 本 plan 的大部分是**取消一項不需要的工程**;真正剩下的是
**兩句誤導的文案**(§2)與 **四題要 Sean 答**(§4)。

### 🛑 而這件事推翻了三份文件寫過的同一句話

| 誰 | 寫了什麼 | 事實 |
|---|---|---|
| 交辦檔 `~/pcm-mailbox/0912-後台UX/交辦-會員等級字面統一.md` ④ | 「UI 有『更正』入口但**一筆最多被更正一次**」⇒ 建議「放寬成可以連續更正」 | 字面對,**推論錯**:那條限制擋的是「同一版被更正兩次」,不是「不能再改」 |
| 姊妹 plan §3.1(我寫的) | 「修改 = 放寬那道閘 ⇒ 移除 `ALREADY_CORRECTED`」 | **不需要**。移掉它反而會讓「兩個人同時改同一版」變成沉默覆蓋 |
| Fable C3 + 我親查 + 主視窗派工單 | 「還有一個 partial unique index 也擋著 ⇒ 規模比 plan 大」 | 那個 index 擋的是同一件事,而**那件事本來就該擋** ⇒ **不要 DROP 它** |

📌 **三個人(含我)連續讀同一句字面、連續推出同一個錯結論,而沒有人去按一次那顆鈕。**
今天一整天抓的是「寫下來的字過期了」;**這一次的字面沒有過期,是【推論】從第一手就錯了,然後被三份文件互相引用成事實。**

🔴 **而本 plan 第一版自己又犯了同一族的另一種**:我跑了負對照、看到 `ALREADY_CORRECTED` 這個**碼**,
就寫下「真正剩下的只有一句文案」—— 而**我沒有去看那個碼會讓員工讀到哪句話**(§2.2)。
📌 **看到碼、沒看到話。** 那句話叫他做的事,會把 U6 告知義務那本帳翻回「已告知」。

---

## 1. 現況(我親自跑過,不是讀碼推的)

### 1.1 A3 建表檔自己就寫著鏈是合法的

`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:158-159` **逐字**:

> 🔴 一筆紀錄最多只能被更正一次(兩筆都說「前面那個錯了」= 誰才算數答不出來)。
> **要再更正 = 去更正那筆更正(`A ← B ← C` 的鏈是合法的、也是預期用法)。**

⇒ 那條 partial unique index 擋的是 **`A ← B` 且 `A ← B'`**(兩筆指同一版),
**不是** `A ← B ← C`(一版接一版)。**兩件事被讀成同一件。**

### 1.2 實跑:`A ← B ← C` 今天就成立(拋棄式 PG 17,埠 55544)

```
第 1 版「客人要黑色」                      ⇒ APPENDED
第 2 版「更正 —— 客人要紅色」(指向第 1 版)⇒ APPENDED
第 3 版「再更正 —— 客人要藍色」(指向第 2 版)⇒ APPENDED   ← 🔴 這就是「連續修改」
🔴 負對照:再去更正【第 1 版】             ⇒ ALREADY_CORRECTED   ← 尺是活的
```

### 1.3 真瀏覽器:畫面也已經是對的(localhost:3021)

| 列 | 「已更正」badge | 可按的更正連結 | 原文看得到 |
|---|---|---|---|
| #6 第 3 版(最新) | 無 | ✅ **有** | ✅ |
| #5 第 2 版 | 「已更正(由 #6)」 | 停用鈕 | ✅ |
| #4 第 1 版 | 「已更正(由 #5)」 | 停用鈕 | ✅ |

⇒ **員工要改備註:在最新那一版按「更正」,打新內容,送出。想再改就再按一次最新那版。**
每一版的原文都留在時間軸上,稽核天然完整 —— 這正是姊妹 plan §4.1 想要的結果,**而它已經是現況**。

---

## 2. 那麼真正還缺什麼?**兩句誤導的文案,而第二句會弄壞一本帳**

⛔ ~~本節第一版寫「真正剩下的只有**一句**文案」~~ —— Fable 2026-09-13 審 F1 抓到第二句,
而**第二句比第一句危險得多**。我的負對照(§1.2)跑到了 `ALREADY_CORRECTED` 這個**回傳碼**,
而我**沒有去看員工會讀到的那句話**。📌 看到碼、沒看到話。

### 2.1 第一句(誤導,但只是讓人卡住)

`apps/admin/src/components/orders/notes-timeline.tsx:120` 那顆停用鈕的 `title` **逐字**:

> 已被更正,**一筆只能更正一次**

🔴 字面為真、而讀者會得到假的結論:員工讀到「這則備註改過一次就不能再改了」,
於是他**不會**去按最新那一版 —— 而那正是唯一該按的地方。

### 2.2 🔴🔴 第二句(會讓 U6 告知義務那本帳翻回「已告知」)

`apps/admin/src/lib/orders/note-action-state.ts:116` **逐字**:

> 這筆備註已經被更正過了,一筆只能更正一次;**請改為重新登記一筆新的紀錄。**

**可重現的壞結局**(Fable 構造,我開檔核過每一跳):
1. 員工 X 與 Y 同時開著同一張單。X 更正了第 2 版 ⇒ 第 3 版寫入。
2. Y 在舊分頁上按第 2 版的「更正」⇒ RPC 回 `ALREADY_CORRECTED` ⇒ Y 讀到上面那句話。
3. Y **照做**:登記一筆**沒有 `corrects_note_id` 指標**的新 `customer_notified`。
4. ⇒ 時間軸出現**兩個現行版**(第 3 版與第 4 版互不指向),
   而 `mappers/order-notes.ts:138` 的 `some(customer_notified && !corrected)` 因第 4 版回 **true**
   ⇒ **U6 被一個「照系統指示做事」的員工翻回「已告知」。**

📌 **那句話叫他做的,正好是這條資料設計最不希望他做的事。** 正確的處置是
「**重新整理,去更正最新那一版**」——而訊息明文叫他別走那條路。

### 2.2-bis 🔴 而就算訊息改對了,**他打的那段字還是會不見**(codex 審 must-fix 1)

Sean 2026-08-02 拍 Q1=A「**失敗保留輸入**」,而 action 那一端確實照做了
(`note-actions.ts` 的 `noteFailure` 把 `body` 與 `requestToken` 原樣帶回)。
**問題在那之後**:失敗路徑也會 `revalidate` ⇒ server component 重渲染 ⇒
`resolveCorrectTarget`(`order-detail.tsx:82-97`)發現目標**已經被更正**、回 `null`
⇒ 表單的 `key` 從「目標 id」變成 `'compose-new'`(`order-detail.tsx:512`)
⇒ **React 整個重建那個元件 ⇒ 失敗 state 與員工打的字一起消失**,
連那句 `ALREADY_CORRECTED` 的訊息都可能留不住。

📌 **「action 有把 body 帶回來」與「員工螢幕上那段字還在」是兩件事** ——
而我在姊妹 plan 與本 plan 第一版都把前者當成後者。
⚠️ 這**不是** DROP index 的理由(那只會換成沉默雙寫)。它要的是一個**草稿交接**的設計:
衝突之後讓他看到最新版、把他打的字留著、由他確認要不要接續更正。
⇒ **這一格是本片真正的工程量**,而它在 §4 Q1 甲的範圍裡 ⇒ **Q1 的選項要照實寫**(§4)。

### 2.3 🔴 而那句指引**不能放在 `title` 裡** —— repo 早就裁過

⛔ ~~本節第一版寫「把 `notes-timeline.tsx:120` 那個 `title` 改掉」~~
—— **那違反一條既有裁定**(codex 2026-09-13 審 must-fix 2;我開檔核過)。
`docs/phase-1-backlog.md:22301` **逐字**:

> 🔴 唯一不能變的是【**不能還是 title**】(主視窗 2026-08-18 對 `#639` 釘的約束,同族適用)。
> 最小可行:**停用控件旁邊印一行短字**(那正是 `#639` 甲在後台那處的做法,`611682e9` 有現成前例)。

📌 理由不是美感:**鍵盤使用者對不到停用鈕的焦點、觸控裝置根本叫不出原生提示**
⇒ 把唯一的操作指引放在 `title` 裡 = **對那些員工等於沒寫**。
⚠️ 而 `22358` 起那一段是**另一件事的撤回**(當時的停用狀態已經有可見 badge 說明原因)
⇒ **不是**「title 全面解禁」。本片新增的是**操作指引**,而它今天沒有任何可見載體。

### 2.4 三處要改的字(字面**待 Sean 定,不自己拍**)

| # | 落點 | 現況 | 方向 |
|---|---|---|---|
| 1 | `notes-timeline.tsx:120` 停用鈕旁 | 只有 `title='已被更正,一筆只能更正一次'` | **印一行可見、可選取的短字**:`要再修改請按最新那一版` · `title` 至多補充,不得是唯一載體 |
| 2 | 🔴 `note-action-state.ts:116` 錯誤訊息 | 「…請改為**重新登記一筆新的紀錄**。」 | `有人已經先更正這一版了。請重新整理,再去更正最新那一版。` ⚠️ **不得**再出現「重新登記一筆新的紀錄」 |
| 3 | `note-compose-form.tsx:181` 更正目標失效橫幅 | 「…(一筆只能更正一次)。下方表單送出的會是**新備註**,不是更正。」 | 後半句**是對的、要留**(它誠實說明會發生什麼);前半句的「一筆只能更正一次」要換成「這一版已經被更正過了」,並補一句指路 |

🔵 另有**三處碼註解**寫著同一句(`note-timeline.ts:77` / `notes-timeline.tsx:105` /
`order-detail.tsx:79`)。員工看不到,但**下一個讀碼的人會再推錯一次** —— 那正是 §0 那個病 ⇒ 一併訂正。

⇒ **仍然零 migration、零 RPC 改動**;動到的是**三個員工看得到的字面 + 一個新的可見元素 + 三處註解**(+ 測試)。

---

## 3. 主視窗點名的八格,逐格回答

### ① partial unique index 怎麼處理 ⇒ **不動,而且不該動**

`order_notes_corrects_note_id_key`(`20260729030000:160`)**留著**。

⛔ ~~第一版的理由:「DROP 之後兩筆都寫進去 ⇒ `Map.set` 靜靜蓋掉前一筆」~~
—— **推理不精確**(Fable 審 F2 證偽;結論仍然成立,但理由要換)。
走 RPC 的路徑**不會**雙寫:A6 `20260802150000:168` 先 `FOR UPDATE` 鎖 orders 單列,
`:177` 的 `EXISTS` 在鎖內跑 ⇒ **就算沒有那條 index,第二筆照樣回 `ALREADY_CORRECTED`。**

🔴 **正確的理由(兩條)**:
① 那條 index 是**繞過 RPC 的寫入者**(owner / superuser 直接 INSERT)**唯一的閘**
   —— A3 建表檔 `:22-25` 自己寫過「本片不宣稱物理不可改,只宣稱應用路徑改不到」,
   而 index 正是那個「應用路徑」以外還守得住的東西。
② 它是 **RPC 那個 IF 哪天被拿掉時的兜底**。兩個閘同時掉才會出事;而
   `correctorSeqByTargetId`(`note-timeline.ts:122-125`,註解逐字
   「partial unique ⇒ 一筆最多被指一次,**單值即可**」)是一個 `Map.set`
   ⇒ 到了那一天,**後寫的那筆會靜靜蓋掉前一筆**,畫面只印得出其中一個更正者。
📌 **一個「誰才算數答不出來」的世界,而畫面會假裝答得出來。**
🔵 其他依賴「單指向」這個不變式的消費端我與 Fable 各查過一次:`correctedBySeq`、
A3 事後閘、A6 自檢、`scripts/a6-verify.sh`、ACL 快照 —— **沒有漏的**。

### ② A3 那道事後閘(`20260729030000:294-300`)⇒ **不動,前提沒有變**

它釘的是「那個 index 仍是 UNIQUE 且帶 `WHERE corrects_note_id IS NOT NULL`」。
既然 index 不動,**閘的前提一個字都沒變** ⇒ 不需要改它,也不需要解釋它為什麼該改。
⛔ ~~主視窗派工單要求「寫清楚 A3 那道事後閘的前提為什麼變了」~~ —— **那個前提沒有變**。

### ③ A6 的窮盡合約(任一輸入必落 14 碼或步 1 RAISE)⇒ **完好,零新錯誤面**

不移除 `ALREADY_CORRECTED`、不 DROP index ⇒ **14 碼一碼不動,不會多出沒有碼的世界**。
📌 而這正是不動的最大理由:上一片(刪除)的驗收就建立在這個合約上,
**改它等於把自己的驗收基礎抽掉,換來一個本來就能做的功能。**

### ④ 更正鏈第 3 版以後的呈現 ⇒ **已經對了,不必動**(§1.3 實測)

`correctedBySeq` 記的是**直接**指向者 ⇒ 最新版天然沒有 badge ⇒ 規則不用改就成立。

🔴 **而我對 `walkCorrectionChain()` 的兩句話都是錯的,兩句一起訂正**(Fable 審 N3,我親查確認):
⛔ ~~「它有 `visited` 保證終止 ⇒ 顯示層不會掛死」~~ —— 函式本身確實有,
   但 **`grep -rn walkCorrectionChain apps packages` 只命中【定義一處 + 測試六處】,
   零非測試呼叫端** ⇒ **時間軸根本不走鏈**,那支函式今天對畫面**沒有任何影響**。
⛔ ~~「我沒有單獨跑過那支函式」~~ —— `note-timeline.test.ts:235-263` 有四格(end / missing / cycle / depth),
   **早就跑過了**,而我在姊妹 plan 與本 plan 第一版都寫「吻合而未實測」。
📌 **我兩次把一段【讀過的碼】寫成【它在保護畫面】,而它其實沒有被任何畫面呼叫。**
⇒ 本格結論不變(呈現已經對了),但**理由不是它** —— 是 `correctedBySeq` 只記直接指向者。
🔵 順帶一個給下一個人的事實:那支函式目前是**只有測試在用的碼**。要不要留是另一件事,不在本片。

### ⑤ `ORDER_NOTES_EMBED_LIMIT = 200` ⇒ 🔴 **風險是真的,而它不是本片製造的**

`mappers/order-notes.ts:35` = 200;`:131` 的 `rows.length >= 200` 是**截斷訊號**。
⛔ ~~「截斷後 `walkCorrectionChain` 回 `stop:'missing'`」~~ —— 那支函式沒有畫面呼叫端(見 ④)⇒ 這句話沒有現場。
🔵 **截斷真正的後果**在別處、而且是 fail-closed 的:`mappers/order-notes.ts:136-137` 把
`customerNotified` 回 `null`(= 無法判定),時間軸印「僅最新 N 筆」。
🟢 而 Fable 查到一件讓這一格**沒那麼可怕**的事實:抓取是
`SupabaseOrderAdapter.ts:1483-1485` 的 `created_at desc, id desc, limit 200`(newest-first)
⇒ **鏈尾(= 現行版)永遠在載入窗內**,被擠掉的是最舊那幾版。
🔴 **訂正一句我自己先前寫的**:我在姊妹 plan 寫「放寬連續更正會推高這個風險」——
**既然連續更正今天就能用,那個風險今天就存在,不是本片新增的。**
⇒ 而它仍然真實:一則備註改十次 = 十列。**列出來當待答,不自己決定**(§4 Q2)。

### ⑥ 與刪除(軟刪除)那片的交互 ⇒ **兩題,今天的行為都已確定,而其中一題要端 Sean**

| 情境 | 今天的行為 | 我的判斷 |
|---|---|---|
| 已被**收起**的備註,還能不能被更正? | **能** —— `canCorrect` 只看 `corrected`,不看 `deletedAt`(`note-timeline.ts:108`;貼板 138 刻意沒加第二道) | 合理:收起只影響顯示,不影響事實(Sean 2026-09-13 答甲的同一條線) |
| 已被**更正**的備註,還能不能收起? | **能**(⛔ ~~第一版這一格寫成「不能…⇒ 實際上是能」,自相矛盾~~,Fable 審 N2)。收起入口的條件是 `entry.deleted === null && canDeleteNotes === 'yes' && deleteToken`(`notes-timeline.tsx:147`),**不看 `corrected`** | 合理 |
| 🔴 **收起鏈中間那一版** ⇒ 畫面會長怎樣? | 那一列同時有「已更正(由 #n)」與「已收起」兩顆 badge,而**內容照樣印**;鏈不受影響 | ⚠️ **這一格要端 Sean**:他可能沒想過「一個已收起的東西還在更正鏈中間」長什麼樣 |

### ⑦ 權限:更正**維持任何員工** ⇒ 不動

理由:更正**不會讓任何事實消失**(舊版原文留在時間軸上)。
⛔ ~~而刪除會讓一則承諾紀錄「離開日常視野」~~ —— **那個理由與現在的 UI 不符**(codex 審 nit 6):
貼板 138 做出來的樣子是**淡化 + 一顆「已收起」badge,而全文照樣印**(`notes-timeline.tsx:140`)
⇒ 它並沒有「離開視野」。
✅ **重寫那條理由**:刪除的門檻比較高,是因為它**新增一個誰都看得到的狀態標記**
(「這一則被收起了」+ 誰收的),那是一個**對外可讀的判斷**;而更正只是再寫一筆新紀錄。
⚠️ 而這一題 **Sean 沒有被單獨問過** —— 兩片都是沿用交辦檔的建議。

### ⑧ 「備註已更新」那句字面 ⇒ **Sean 2026-09-13 逐字定案,而本片【用不到它】**

他三句裡的第三句,原本留給這一片當**結果訊息**。
🔴 **而「更正」今天走的是 `appendOrderNoteAction`,成功後的結果碼是 `NOTE_ADDED_RESULT_CODE`
⇒ 橫幅印的是「備註加好了。」**,不是「備註已更新」。
⇒ 要讓更正印他那句話,得讓 action 分辨「這是更正還是新增」並帶不同的結果碼
(`parsed.correctsNoteId !== null` 就分得出來,材料已經在手上)。
**那是一個小改動,但它是【改動】** ⇒ 列進 §4 Q3 問他要不要做。
⚠️ **不要**因為他定了字面就假設他要求那個改動 —— 他定字面時的前提是「這一片會做」。

---

## 4. 待 Sean 答的四題(**不自己決定**)

```
Q1: 「備註可以修改」你要的是哪一種?
A1: 甲｜現在這種:在最新那一版按「更正」,打新內容,舊版原文留在時間軸上。
      **改一次、改十次都已經能用**;要做的是把三句誤導的話改掉、把指引印成看得見的字,
      再補一個「兩個人同時改」時不會弄丟你打的字的處理 —— 推薦
    乙｜真的就地改掉那一則的文字(舊內容不再顯示,只留稽核)
      ⇒ 那要動 RPC 與資料表,而且會讓「對客人的承諾紀錄」在畫面上失去前一版
```

```
Q2: 一則備註改很多次會變很多列,單張訂單載入上限是 200 列。要不要處理?
A2: 甲｜先不動,等真的有單逼近 200 再說(今天正式庫 order_notes 0 列)—— 推薦
    乙｜現在就把上限拉高 / 或逼近時在畫面上警告
```

```
Q3: 更正成功之後,上面那句話要印你定的「備註已更新」嗎?
A3: 甲｜要 ⇒ 更正與新增用不同的結果碼 —— 推薦
    乙｜不用,兩者都印「備註加好了。」照舊
```
🔴 **若選甲,接線有四處,漏一處就會「成功卻沒有橫幅」**(codex 審 nit 2 補的,我原本只寫「小改動」):
① 新結果碼常數 ② `note-actions.ts` 依 `parsed.correctsNoteId !== null` 分流
③ 🔴 `result-banner.tsx:303` 的**查表**(漏了它 ⇒ 成功而畫面一片空白)
④ 測試三支:`result-banner.test.tsx:384` 的 `otherLines`(不加會紅)、
   `note-actions.test.ts` 的 `APPENDED` / `DUPLICATE_REQUEST` 兩路、`note-return-to.test.ts`
⚠️ **而「每顆鍵都要歸線」那道守門【抓不到】漏掉 ③ 的情形** —— 沒加鍵就沒有鍵要歸線。

```
Q4: 更正一則「客人聯繫 / 已告知客人」時,聯絡時間與管道要不要自動帶入原本的值?
A4: 甲｜帶入、而且可以改 —— 推薦
    乙｜照舊,每次重打
```
🔴 **這一題是 Fable 2026-09-13 審 F3 幫我補的,我原本漏了。** 現況:更正表單只帶入
`noteType`(`note-compose-form.tsx:125-126`),`channel` 與 `occurredAt` 起始是空的,
而 RPC `20260802150000:163` 對非 internal **強制那兩欄必填**
⇒ **員工只是要修一個錯字,卻必須重打「什麼時候、用什麼管道聯絡客人」。**
打錯一次,`order_notes_notified_idx`(A3 `:154-156`)與稽核上的「何時告知客人」就換成新值,
而舊值只剩在那列被標「已更正」的舊版裡。
📌 **那就是「修改」與「更正」在資料上的實質差別,而我把兩者當同義詞了。**

🔵 另外一件**只要告知、不用他決定**:一則已被「收起」的備註,**仍然可以被更正**;
而一則已被更正的舊版,**仍然可以被收起** ⇒ 那時那一列會同時有兩顆 badge。§3⑥ 那一格。

---

## 5. 影響 / rollback

### 5.1 若照 Q1 甲(推薦)

| 面 | 影響 |
|---|---|
| **migration** | **零**。不需要 139 |
| **RPC** | **零**。A6 一個字不動,14 碼完好 |
| **碼** | **三個員工看得到的字面**(§2.4)+ **一個新的可見元素**(指引不得只住在 `title`)+ **併發草稿交接**(§2.2-bis)+ 三處註解 + 測試 |
| **稽核** | **零新動作** —— 更正與新增走同一支 RPC、同一個 action `order_note.append`(A6 `20260802150000:198`,我與 Fable 各實查一次);`is_correction` 只進 `console.info`(`note-actions.ts:150`)⇒ **兩張字典都不用動** |
| **rollback** | ⚠️ **要限定條件**(codex 審 nit 3):**本片的碼若集中成一顆獨立 commit**,那 `git revert` 那一顆就撤得掉碼面差異。而 ①**部署另算**(revert 不會自動退掉已部署的版本)②**Sean 驗收時真的寫下去的更正列與稽核列不會被撤**(那些是真的發生過的事)③**貼板 138 不撤**。⛔ ~~第一版寫「rollback = 一顆 revert」~~ —— 那句話漏了這三件 |

### 5.2 若照 Q1 乙(就地改)

那是**另一份 plan**,規模與刪除那片相當或更大(改 RPC + 版本保存 + 稽核形狀 + 讀取端)。
本 plan **不展開**,因為在他答之前展開等於先做掉一個他可能不要的東西。

---

## 6. 驗收

### 6.1 Q1 甲(文案)

1. 已被更正的那一版,停用鈕的提示**說得出該去按哪裡**(不是只說「只能更正一次」)。
2. 🔴 **`ALREADY_CORRECTED` 的錯誤訊息不得再叫人「重新登記一筆新的紀錄」** ——
   釘一格斷言那句話**不含**那個字串,並在旁邊寫明 §2.2 那個結局。
   (那一格比「文案有沒有改」判別力高:它擋的是**語意退回**,不是字面。)
3. ⛔ ~~「Sean 自己連續改三次」~~ —— **那一格零判別力**(codex 審 nit 4,他說對了):
   **完全不改任何文案,今天也能連續改三次**(§1.2 就是這樣跑出來的)
   ⇒ 那個驗收在「改了」與「沒改」兩個世界印同一個結果。
   ✅ **換成有判別力的那一個**:準備一則已經是 `A ← B` 的備註,
   **不告訴 Sean 該按哪一顆**,請他照畫面上的字再改一次。
   · 他找得到 ⇒ 指引有效(而且要能用鍵盤走到、字要選得起來)
   · 他找不到 ⇒ 本片沒做到事,不管文案改了幾句
   📌 **差別在「不先告訴他答案」** —— 那才量得到指引本身。
4. 🔴 **雙人衝突那一格**(§2.2-bis):兩個分頁同時更正同一版,輸的那一邊
   **打的字要還在螢幕上**、而且訊息要叫他去更正最新那一版。
5. 三綠。

### 6.2 🔴 若任一 Q 的答案帶出新的稽核動作(例如 Q3 甲若改成獨立 action)

**新增的稽核動作要進【兩張】字典,而其中一張沒有守門**:
- `lib/audit/audit-field-label.ts`(欄名)⇒ **有**分母集合比對,漏了會紅
- `lib/audit/audit-list-view.ts`(動作)⇒ **沒有**守門,測試只斷言 `length > 0`,**漏了不會紅**

📌 該檔自己記過**兩次**同一個坑。主視窗 2026-09-13 裁:**第三次才加閘**。
🔵 **而本片(照 Q1 甲)零新稽核動作 ⇒ 這不是第三次。** 判準原樣留著等下一個人。
⚠️ Q3 若選甲、而且做成**獨立的 audit action**(不是只換 UI 結果碼),那就會是第三次 ⇒ 那時加閘。

---

## 7. 本 plan 的射程 —— 沒做什麼、不宣稱什麼

- 🛑 **零實作**:沒有 migration、沒碰 `.sql`、沒改一行 `.ts` / `.tsx`。
- 🛑 §1.2 的四格與 §1.3 的三列是**拋棄式 PG 與本機鑽機**上跑的,**不是正式庫、不是正式站**。
- ⛔ ~~`walkCorrectionChain` 的環那條路我沒有單獨跑過~~ —— **那句話是假的**:
  `note-timeline.test.ts:235-263` 有四格早就在跑。而更重要的是**它沒有任何非測試呼叫端**
  ⇒ 它對畫面沒有影響(§3④)。**兩句都訂正了,不是收窄。**
- 🛑 「更正維持任何員工」是沿用現況與姊妹 plan 的理由,**Sean 沒有被單獨問過這一題**。
- 🛑 §2 那兩句建議文案是**我寫的,未拍板** —— 依 `CORRECTION_IRREVOCABLE_NOTICE` 的先例,
  員工看得到的字面要 Sean 定。
- 🛑 我**沒有**去查「員工實際上有沒有因為那兩句話而卡住 / 而開了分叉」—— 那是推論,不是量到的。
  正式庫 `order_notes` **0 列**(2026-09-13 唯讀實查,`bash scripts/readonly-prod-sql.sh`,rc=0;
  同一發的結果寫在姊妹 plan §2)⇒ **那個分母不存在** ⇒ **這一片是預防性的,不是事故驅動的**。
  🔵 而 0 列同時意味著 §2.2 那個壞結局**今天還沒有發生過**,也還來得及在發生前把話改掉。
- 🛑 §1.2 / §1.3 是**拋棄式 PG 與本機鑽機**,不是正式庫、不是正式站。§3⑥ 那三列是**讀碼結論**
  (Fable 逐條複驗為真),不是我自己跑出來的。
- 🛑 §5.2「Q1 乙不展開」codex 複核過:**沒有必須現在展開的理由**。
  🔴 而它補了一條**現在不寫下來、之後會很貴**的前提,寫在這裡:
  **在建立完整的舊版保存之前,不可以開始覆寫原文** ——
  A6 的稽核 `after` 只有 `body_sha256` 與 `body_length`(`20260802150000:186`),
  **還原不出全文**。⇒ 誰要做 Q1 乙,第一件事是先有地方放舊版,不是先改 RPC。

## 8. 這份 plan 自己被審了兩輪(而兩輪都打中它)

| 輪 | 切入角 | 結果 |
|---|---|---|
| **Fable 5.1**(subagent) | 攻**中心主張** | 中心主張裁定 **SUFFICIENT**(A←B←C 今天就能用、index 不該 DROP);抓到 must-fix 1(§2.2 那句話)+ 兩條 consider + 五條 nit |
| **codex `gpt-6-astra`** | 攻**決策後果** | 抓到 must-fix 2(§2.2-bis 草稿會掉 / §2.3 不能用 title)+ 六條 nit |

🔴 **兩輪合起來打掉了我第一版的四個宣稱**,每一個都留了刪除線在原地:
① 「真正剩下的只有一句文案」⇒ **三句,而其中一句會弄壞 U6 那本帳**
② 「把那個 `title` 改掉」⇒ **repo 早就裁過不能是 title**
③ 「rollback = 一顆 revert」⇒ **漏了部署、資料、貼板三件**
④ 「請 Sean 連續改三次」⇒ **那一格零判別力,兩個世界印同一個結果**

📌 而值得記的是這一句:**我跑了實測、拿到了正確的中心主張,然後在它旁邊寫了四句沒跑過的話。**
一個被驗證過的結論,不會讓它周圍的句子跟著變成真的。



— END —
