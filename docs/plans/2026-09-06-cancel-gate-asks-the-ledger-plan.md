# 取消閘問錯了欄位 —— 而它該問的那本帳在另一張表

> **錨** `⟦0a-CANCELGATEASKSWRONG⟧`(板 `:925`)· 2026-09-06 · 線 `-account`(`-d8`)
> **鐵則 8**(動 DB 函式 · 跨檔)· **鐵則 12①(錢)③(DB)** ⇒ **關卡 1 codex 必跑, 不降級**
> **狀態:等批准。本 plan 不動任何一行碼。**

---

## ⚠️ 讀之前先看:**這份 plan 比派工單小, 而縮小的兩件事都是量到的**

派工是「①改閘 ②照 Sean 08-27 `Q1=甲` 讓已收款單取消時自動開待退款」。
🔬 **而 ② 已經在線上了** —— 見 §0-b。⇒ **本 plan 只剩 ①。**

---

## §0-a 先量那個數 —— **而它幾乎不帶資訊, 這一格要講清楚**

唯讀正式庫(`scripts/readonly-prod-sql.sh`, 零寫入)2026-09-06:
```
🔴 A  帳本有錢而 payment_status = 'unpaid'(本列的病)          0 張
   A1 其中還沒取消的(取消閘會放行的那些)                      0 張
🟢 B  帳本有錢而 status 已經是 paid / 部分(尺會動嗎)          1 張
🟢 C  order_payments 全表列數                                  1 列
🔵 D  負對照 payment_status = 現造值                           0
   E  全部訂單                                                 2 張
```
🛑🛑 **而這個 0 幾乎不帶資訊, 而理由是分母**:全站 **2 張單、1 筆收款**。
⇒ 📌 **「今天沒有」與「這條路走不出那個形狀」在這個分母下印同一個東西。**
⇒ ✅ 正確的說法是:**它不急**(沒有現存受害者), 而**它對不對與這個 0 無關**。
🔵 **而 B=1 讓我知道尺是活的** —— 那一筆收款的單, 狀態確實是對的。

## §0-b 🔴🔴 **「自動開待退款」那一半【已經在線上】** —— 派工單的 ② 不必做

```
trigger  order_pending_refund_open_au
表       public.orders
時機     AFTER UPDATE OF cancelled_at
函式     pcm_pending_refund_on_cancel
啟用     O(啟用中)          ← 唯讀正式庫 pg_get_triggerdef 逐字
```
⇒ **任何一發把 `orders.cancelled_at` 寫進去的動作**(含 `admin_cancel_order`)
**都會觸發它**, 而它照 `order_payments` **逐軌算淨額**開待退款
(`20260902030000` 那一版, 跨軌修法;而**那支 2026-09-02 就貼了**)。
📌 **⇒ 所以今天一張「匯款已實收而被當未付款取消」的單, 待退款【是會開的】。**

🎯 **⇒ 而這件事改變了本題的形狀**:
> ⛔ ~~問題是「錢沒有被記下來」~~
> ✅ **問題是「它走了一條【設計給未付款單】的路」** —— 而錢那一半, 別人已經接住了。

---

## §1 那麼還剩什麼問題?**這一節是本 plan 真正要答的**

`20260805100000:359-361` 逐字:
```sql
-- 步7 允許集合(unpaid + attempts 全終態 failed 或零筆;row 36)
IF v_order.payment_status <> 'unpaid'::public.payment_status
   OR EXISTS (SELECT 1 FROM public.payment_charge_attempts a
               WHERE a.order_id = p_order_id AND a.status <> 'failed') THEN
  RAISE EXCEPTION '%', v_generic_msg;
END IF;
```
而人工收款**不寫** `payment_status`(`20260823030000:32-42` 該檔自己逐字寫著)。
⇒ 🔴 **一張匯款已實收的單, `payment_status` 仍是 `'unpaid'` ⇒ 這道閘把它當未付款單放行。**

📌 **形狀(de-plan §0 的原話, 我複量過兩個座標都成立)**:
> **兩個地方都在記「付了沒」, 而它們不同意。閘問了會說「沒付」的那一個。**

### 🔴 而「放行了會怎樣」**我沒有量到**, 而那正是要答的

我證得到閘會放行, **證不到放行之後有沒有壞事**:
- ✅ 待退款會開(§0-b)
- ❓ **而那條路上還有什麼是【假設這張單沒收過錢】才成立的?** —— **未查**
  例:庫存釋放 / 稽核事由 / 通知信選擇 / 折價券回補 / 對帳分類
⇒ 🛑 **在答出這一格之前, 我提不出「該擋還是該放」** —— 而**那是這份 plan 的第一件事**, 不是實作。

---

## §2 三個方向, **而我不自己選**

| | 做法 | 代價 |
|---|---|---|
| **甲** | 閘改成**同時問帳本**:`unpaid` **且** `order_payments` 淨額 = 0 ⇒ 放行;有錢 ⇒ **擋** | 🔴 **會擋掉今天走得通的操作** —— 而員工要改走 `admin_cancel_order` 的哪一條路, **沒有人指定過** |
| **乙** | 閘照舊放行, 而**把「這張單其實收過錢」寫進稽核與取消理由** | 不改行為 ⇒ **零回歸風險**;而它只讓事後查得到, **不阻止任何事** |
| **丙** | 先不動閘, **只補 §1 那格未查**(那條路上還有什麼假設「沒收過錢」)⇒ 有真傷害再決定 | 最慢, 而**它是唯一一個不建立在猜測上的** |

🔵 **我傾向丙, 而理由是 §0-b**:原本以為「錢沒被記」⇒ 那是明確的傷害;
**而錢已經被記了** ⇒ **傷害變成假設性的**, 而**甲會為了一個假設性的傷害擋掉一條今天走得通的路**。
🛑 **而甲的代價是具體的** —— 員工按不下去, 而**替代路徑沒有人指定過**。

---

## §3 影響面 / Rollback(**若拍甲或乙**)

- 動 1 支 DB 函式(`admin_cancel_order`, `CREATE OR REPLACE`, 參數不動)⇒ **零 DDL**。
- 版本號 **`20260905340000`**(主視窗指定;⚠️ **動手前用 `git log --all` 重數一次** —— 我 09-05 撞過一次號)。
- Rollback = 把 `20260805100000:355-365` 那一段原樣貼回。
  🔴 **而回滾之後那條路就回來了** —— 與片③ 同型, **rollback 是不對稱的**。
- 🛑 **`admin_cancel_order` 是【今晚第二次】被指出「頭與 body 要分開比」的那種函式**
  ⇒ 動它之前**先唯讀量** `proconfig` / `prosecdef` / `proacl` / `COMMENT`,
  **不要只比 `md5(prosrc)`**(📎 plan `2026-09-05-refund-sync-p3-…` §8b)。

## §4 驗收(每條可 yes/no)

1. **兩個世界**:帳本零的 `unpaid` 單 ⇒ 放行;帳本有錢的 `unpaid` 單 ⇒ 照拍板的方向(擋 or 放行+留痕)。
2. 🔴 **而「有錢」那一格要造得出來** —— 造不出來就等於沒驗(09-05 我有一格就是這樣掉的)。
3. `order_pending_refunds` 該開的仍然開(**不要在修閘的時候把 §0-b 那半弄壞**)。
4. 上線前**重量 §0-a 那五格**(今天的 0 有時效, 而分母會長大)。
5. 三綠 + `scripts/migration-static-checks.sh` + 突變(把新判準改成恆真 ⇒ 必紅)。

## §5 這份 plan 【證不到】什麼

- §0-a 那個 0 **在分母 2 之下幾乎不帶資訊** —— **不要拿它當「這條路不會發生」。**
- §1 最後那一格(放行之後還有什麼壞)**完全未查** —— 而**方向的選擇整個掛在它上面**。
- 我**沒有**開 `admin_cancel_order` 的全文 —— 只讀了步 7 那一段。
- 📌 **本 plan 最大的貢獻可能是 §0-b**:它把一件「要做兩半」的事縮成一半,
  而那一半是**別人 2026-09-02 就做完的**。⇒ **開工前先問「這件是不是已經被誰關掉了」。**
