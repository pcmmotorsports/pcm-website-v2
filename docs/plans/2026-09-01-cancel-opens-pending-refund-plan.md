# 已收款單取消 ⇒ 自動開一筆待退款(plan,等批)

> 線【DB 金流】`-2d` 2026-09-01 16:0x · **鐵則 8 ⇒ 這一份要 Sean 批了才動手**
> Sean 2026-09-01 15:2x 逐字:**「乙 放行 + 自動開一筆待退款(你 8/27 拍過的方向)」**
> 🛑 **本檔零實作、零 apply。它要回答的是「這一片長什麼樣、會不會出事、退得掉嗎」。**

---

## 0. 為什麼要做(而這一段是量到的,不是背景)

```
客人匯款付了錢 ⇒ 人工收款【不寫】orders.payment_status(它走 order_payments 帳本)
⇒ 那張單的 payment_status 仍是 'unpaid'
⇒ 而「不准取消已付款的單」那道閘只問 payment_status ⇒ 🔴 放行取消
⇒ 而取消【不會開任何退款】
```
**2026-09-01 我實量的兩格(`⟦:660⟧` 那一列有全文)**:
- 最新一代 `admin_cancel_order`(`20260830020000:327`)提到 `order_refunds` / `order_manual_refunds` ⇒ **0 行**
  · 🟢 正對照 同檔提到 `order_payments` ⇒ **5 行** ⇒ 尺會動,那個 0 不是尺壞了
- 真值表(把那一代的述詞原封抄成純函式跑):`unpaid + 有收款紀錄 + 無刷卡` ⇒ **放行**

⇒ 📌 **完整後果:錢收了 · 單取消了 · 而沒有任何一張表記著要退這筆錢。**

---

## 1. ④ 先驗「他說的 8/27 那個方向,是不是同一件事」

🔴 **他這次逐字寫「(你 8/27 拍過的方向)」—— 而我不因為他這樣說就當它是。**

`memory/project_0827-sean-rules-b-wait-for-real-login.md` 逐字:
```
Q1(已收款單取消要自動開待退款,他 2026-08-27 拍甲)
```
✅ **⇒ 同一件事,確認。** 而那支 memory 還給了一格**這次的題目沒有的**:

```
Q1 卡在 ⇒ 退款登記 UI 入口封著(manual-refund-entry-gate.ts = true)
           卡在 ⇒ #885(admin_record_manual_payment 零上界 + 接受 unpaid 單)
                    部分卡在 ⇒ E8-B 真登入線
```
⚠️ **而那支 memory 寫的行號是 `:69`、板子 `⟦b4-CAPRACE1⟧` 寫 `:85`,而今天實際是 `:137`**
⇒ 📌 **同一個常數的座標在兩份文件裡各漂過一次。⇒ 引用它請用常數名,不要用行號。**

---

## 2. ② 狀態機 —— 🔴 **而這一格是本 plan 最重要的發現**

**問題**:那筆待退款產生之後,誰把它變成「已退」?

**實量(2026-09-01)**:
```
apps/admin/src/components/orders/manual-refund-entry-gate.ts:137
   export const MANUAL_REFUND_ENTRY_BLOCKED_BY_787: boolean = true;
:144  if (MANUAL_REFUND_ENTRY_BLOCKED_BY_787) return false;
🟢 而 RPC 本身是接好的:admin_record_manual_refund 在 app 碼有 13 處呼叫端(排除測試與型別檔)
```
🛑🛑 **⇒ 消化那一端【碼寫好了,而入口被一個常數關著】。**

⇒ 🔴 **所以現在做完整版 = 做一張【只會累積、不會消化】的表。**

### ⇒ 而那給出兩個選項,我要 Sean 選(我不自己決定)

```
甲 現在只做【記錄】:取消時開一筆待退款, 而消化那一端等 #787 解封
   代價:那張表會累積, 而沒有人按得到「已退」⇒ 需要一個地方讓人看得到它在累積
   好處:🔴 **錢收了而沒有紀錄** 這件事今天就停止 —— 那是本片的主要目的

乙 等 #787 解封再一起做
   代價:🔴 在那之前, 每一張被取消的匯款單都繼續【零紀錄】
   好處:一次到位, 不會有中間狀態
```
🔵 **我的推薦:甲。** 理由:**「沒有紀錄」與「有紀錄而還沒處理」差很多** ——
前者事後查不到(今天已經有一批這樣的單,而我們連數量都答不出來),後者至少留得下來。

---

## 3. ① 那筆待退款寫進哪裡 —— **用既有的,不新開表**

**實量兩張候選表的形狀(唯讀正式庫,2026-09-01)**:
```
order_refunds        17 欄 · 有 status(text)· 有 tappay_refund_id / rec_trade_id ⇒ 卡片那條路的形狀
order_manual_refunds 12 欄 · 有 rail / refund_amount / voided_at / void_reason ⇒ 人工那條路的形狀
```
✅ **建議寫進 `order_manual_refunds`**,理由:
- 匯款收款走的就是人工那條軌(`rail`),與這張表的語意一致
- 它已經有 `voided_at` / `void_reason` ⇒ **開錯了退得掉**(見 §5)
- 🔴 **而它已經有那道上限 trigger**(`pcm_manual_refund_rail_cap_guard`)⇒ 不會憑空多退

⚠️ **而它今天沒有「待退 / 已退」的狀態欄** ⇒ 這一片要加一欄或用既有欄位表達。
🛑 **那是本片唯一的 schema 改動,而它要 Sean 貼。**

---

## 4. ③ 舊資料 —— 🔵 **今天限定的便宜,而它會過期**

```
訂單今早隨 ⟦b4-PURGE1⟧ 清空 ⇒ orders = 0
⇒ ⇒ **零筆歷史要回填。**
```
🔴 **而那個便宜是【今天限定】的** —— 下週做就要面對「已經被取消而沒有紀錄的那些單」,
而那批單**事後補不回來**(取消那一刻沒有留下金額與收款來源)。
📌 **⇒ 這一格本身就是「現在做比較好」的理由,而它不是效率問題,是【資料還原性】問題。**

---

## 5. ⑤ 回滾

```
① migration 本身:單一交易(BEGIN … COMMIT), 斷言不過 ⇒ 整支回捲, 零殘留
② apply 之後發現不對 ⇒ 那一欄可以留著不用(不刪欄), 而取消那條路的新行為
   由一個【開關】控制 ⇒ 關掉就回到今天的行為
   🔴 而那個開關要走 env 或常數, 不要走「改碼再部署」—— 那不是回滾, 那是再上一次線
③ 已經寫進去的那幾筆待退款:用 voided_at / void_reason 作廢, 不刪列
   ⇒ 📌 刪列會讓「它曾經存在」消失, 而那正是本片要防的東西
```

---

## 6. 🛑 我不做的三件(照交辦)

```
· 不 apply(我做不到, 也不該試)
· 不動【取消】那條路的其他行為 —— Sean 拍的是「放行 + 開待退款」, 不是重新設計取消
· 不順手把 ⟦b4-CAPRACE1⟧(退款上限並行)一起做 —— 兩件的風險不同, 不混一顆
```

## 7. 三格追查結果(2026-09-01 16:2x,原本標「還沒回答」)

### ✅ ② 寫總額還是實收 ⇒ **已經決定了,在碼裡,不必問他**

```
`pcm_manual_refund_rail_cap`(#866 片1)算上限用的是:
   SUM(order_payments.amount)  ⇒ **實收**
   而它提到 `public.orders` ⇒ **0 行**、提到 `total` ⇒ **0 行**
```
⇒ 📌 **既有的上限就是拿【實收】當分母 ⇒ 新開的那筆待退款用同一個口徑,否則兩個數字會打架。**
🔵 **⇒ 這一格不必端他。**(而那正是「先查有沒有決定過」要防的:一題問出去,而答案已經在碼裡。)

### 🔴 ③ actor 是誰 ⇒ **答案是「一個在職的 staff id」,而【不是】操作的那個人**

```
DB 那一側【有驗】:20260830020000:411
   IF NOT EXISTS (SELECT 1 FROM public.staff s WHERE s.id = p_actor AND s.is_active)
⇒ ✅ 所以它擋得住「亂填一個不存在的 id」
🔴 而 app 那一側:cancel-actions.ts:108 逐字
   「actor 是**使用者自己下拉選的**(`../session/actor.ts` 自陳非授權邊界)」
```
🛑 **⇒ 所以精確的答案是:那筆待退款的 actor =【一個存在且在職的 staff id】,**
**而【不是】一個被驗證過的「就是這個人做的」。**
📌 **⇒ 而它比板子上那句「零驗證」精確一格 —— 不是零驗證,是【驗了存在性,沒驗身分】。**
⇒ 🔵 **本片不解那個** —— 它是 `⟦:490⟧` 與 `E8-B 真登入線` 的範圍。
**本片只要不假裝它是可信的:那一欄的意義是「誰按的鈕(自稱)」,不是「誰為這筆錢負責」。**

### 🔴 ① 那一欄叫什麼、值域是什麼 ⇒ **這一格【要等 §2】,而它是唯一真的要問的**

⇒ 見下方「要端給他的題」。

## 8. 要端給 Sean 的題(只剩一題,而選項帶內容)

```
Q: 匯款收了錢的單被取消時, 系統要不要自動記一筆「待退款」?
A: 甲 現在就記(消化那一端等入口解封)—— 錢收了而沒有紀錄, 今天就停止
   乙 等入口解封再一起做 —— 在那之前, 每一張被取消的匯款單繼續零紀錄
```
🔵 **推薦甲。**「沒有紀錄」與「有紀錄而還沒處理」差很多 —— **前者事後查不到。**
⚠️ **而他答甲之後,①那一欄的值域就定得下來**(只需要「待退 / 已退 / 已作廢」三態),
**不必再問他第二次。**
