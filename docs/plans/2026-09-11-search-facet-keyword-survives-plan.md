# Plan · 點篩選之後,客人打的關鍵字要留著(`⟦db-SEARCHFACETMUTEX⟧` 客人那一半)

> **一句話**:客人打「可調角度」得到 1 件,點一下分類 ⇒ **變成 10 件,而搜尋框空了**。
> 他打的那個字**沒有進那一發查詢**(實測 `"p_terms":null`)。
>
> · 🟢 **DB 那一半 2026-09-09 已經好了** —— 本片**不動 RPC、不動 schema**。
> · 🔴 **而本片要翻的是【主視窗 2026-09-03 的一個拍板】** ⇒ §3 那一題**必須先答**,不是實作細節。
> · **Sean 本人沒有看過本 plan。**
> · 🛑 **本 plan 一個字都還沒動到碼。**

---

## 1. 現在怎麼壞的(2026-09-11 真瀏覽器實測,不是讀碼推的)

🔬 拋棄式鑽機 `scripts/storefront-probe/up.sh`(起自 `5f02a8023`,**前端碼與正式站同一份**),
web 層照 `docs/runbooks/local-admin-with-real-data-probe.md` §8-g 搬到一棵無 `.env*` 的 worktree 跑。
**打開 `log_statement='all'`(只在鑽機上,該鑽機已整個刪除)**,逐發看 DB 真的收到什麼:

```
只搜尋那一發      "p_terms":["可調角度"]                          🟢 關鍵字有進 RPC
點一下分類那一發  "p_category":"外觀與後視鏡"   "p_terms":null    🔴 同一發, 關鍵字沒了
⚪ 負對照 現造參數名 p_zzz20260911 ⇒ 0
```

🟢 **判別格是挑過的,不是隨手點**:先找到唯一那件命中商品(`RIZOMA 車牌架(可調角度)`),
再點**它自己所屬**的分類 ⇒ 關鍵字還在應回 **1 件**、被丟應回 **10 件**(整個分類)。
**畫面實際:10 件。**

🖼 **客人看到什麼**:標題「外觀與後視鏡」· 膠囊**只有分類、沒有關鍵字** · **搜尋框空了** ·
頂端一行「查看全部 1 筆搜尋結果 →」。
🛑 **那一行是唯一線索,而它不是錯誤訊息** —— 沒有東西告訴他那個字被丟掉了。

### 🛑 動手前寫死的驗收(跑完一個字沒改)
```
🟢「修好了」只有在:那一發 RPC 的 p_terms 非 null 且等於我打的詞(切詞後)
🔴「還沒好」只有在:p_terms 是 null / 缺席 / 空陣列
⚪ 判不出來:攔不到那一發 ⇒ 寫判不出來, 不用畫面筆數代替
🛑 「畫面上還有結果」不算修好;「筆數變少」也不算 —— 只點 facet 本來就會這樣。
```

---

## 2. 成因在哪 —— **一行,而它不在 DB**

```
apps/storefront/src/components/use-catalog-filter-url-sync.tsx:538
    params.delete('search');
⇒ 點 facet 的那一刻 search 從 URL 消失
⇒ 而 products.ts 是從 query.search 切詞的 ⇒ 下一發 p_terms 就是 null
```

> ## 🎯 **⇒ 這件事從「要 Sean 批 schema」變成「改一行前端」。**
> 🔬 而那個轉變是 2026-09-11 量出來的:`20260909010000`(公開)/ `20260909040000`(經銷)
> 都已進正式庫,`page.tsx:430` 逐字「**關鍵字不再是另一條資料路**…兩條路合成一條」。

| | 舊的以為 | 今天實測 |
|---|---|---|
| 要動什麼 | 給 RPC 加關鍵字參數 | 🟢 **不用** —— `p_terms` 已在線上 |
| 片型 | 動 RPC ⇒ 鐵則 8 + 鐵則 12③ | 🔵 **純前端** ⇒ 不動 schema、不動共用元件 |
| 卡在誰 | Sean 批 schema plan | 🔴 **主視窗的一個拍板**(見 §3) |

---

## 3. 🔴🔴 **第一題(必答,不是實作細節):那句「不能從前端拿掉」的前提還在嗎?**

板列 `:1096` 寫死:
> 🛑 **不得用「直接砍 `use-catalog-filter-url-sync.tsx` 那行 `params.delete('search')`」當修法**
> —— 那會壞回 `7bfefe4af4` 修掉的病。

### 🔬 我去讀了那顆 commit,而**它的前提已經不在了**

```
7bfefe4af   2026-09-03 22:07   ← 🔴 比關鍵字修好(2026-09-09)【早六天】
```
`7bfefe4af` 當時的世界(commit body 逐字):
> 🛑 **而它不是接線 —— 有一道 DB 牆**:`/products` 走 `search_catalog_by_vehicle`,而**那支沒有關鍵字參數**。
> **修法:同一頁,兩條資料路** —— 有 `search` ⇒ 走 `lib/search.ts` 的 ILIKE;沒有 ⇒ 走 `fetchCatalogPage`。
> 🛑 **代價明寫:關鍵字這條路【吃不到 facet】。**

而 `delete('search')` 的理由(`use-catalog-filter-url-sync.tsx:484-489` 逐字):
> 主視窗 2026-09-03 拍板逐字:「提示句;facet 仍可點,**點了就清掉關鍵字**」
> ⛔ 我第一版只做了前半(提示句)⇒ code-reviewer 抓到:關鍵字還在 URL 上,而 `ActiveChips` 已經畫出一顆「已選」的膠囊
> ⇒ 📌 **畫面聲稱清單被那個 facet 縮過,而商品其實是關鍵字撈的。**

> ## 🎯 **⇒ 那個「安靜的錯」【需要兩條資料路才成立】—— 畫面說 facet、資料走關鍵字。**
> ## 🟢 **而 2026-09-09 兩條路合成一條之後,關鍵字與 facet 同時進同一發 RPC ⇒ 那個錯【構造不出來】。**
> ## 📌 **⇒ 板上那句「砍了會壞回舊病」,與 §1 那個 token 是同一族:前提變了而話沒變。**

### ⇒ 要 Sean(或主視窗)答的一題
```
Q 點了篩選之後, 客人打的關鍵字要留著還是清掉?
  🔬 背景:2026-09-03 主視窗拍「點了就清掉」, 而【那是為了避免一個只在兩條資料路下才會發生的錯】。
     2026-09-09 兩條路併成一條 ⇒ 那個理由消失了。
A: 甲 留著(推薦)—— 關鍵字 + 篩選同時生效, 那正是 ⟦db-SEARCHFACETMUTEX⟧ 這一列的受詞;
     而今天客人打了字再點篩選, 那個字就不算數, 他只拿到一行小字
 | 乙 維持清掉 —— 那要重寫一個【今天仍然成立】的理由, 因為原本那個已經不在了
```
🛑 **甲乙都不是「工程選擇」** —— 它翻的是一個拍板。**沒有這個答案之前不要動那一行。**

---

## 4. 要動的檔(等 §3 答甲才開工)

| # | 檔 | 改什麼 | 影響 |
|---|---|---|---|
| 1 | `apps/storefront/src/components/use-catalog-filter-url-sync.tsx:530-538` | 那段 `q0` 寫入 + `params.delete('search')` ⇒ 不再刪 `search` | 🔵 純前端;而 `q0` 那一段要一起判(留著會多一行「查看全部」指向自己) |
| 2 | `apps/storefront/src/components/products-url-state.tsx:168-186` | **同形的第二處**(改排序時也刪 `search`)—— 🔴 **站上刪 `search` 的點共有四個**,碼裡逐字寫著 | 不一起改 = 改了排序關鍵字照樣不見 |
| 3 | `apps/storefront/src/components/SearchKeywordChip.tsx:94` | ✕ 那個 —— 🟢 **不要動**(客人**明示**要丟掉那個字) | — |
| 4 | `apps/storefront/src/app/products/page.tsx:242` | 轉址那條(解析得到 facet 時)—— 判要不要一起改 | 它是「搜尋轉分類」那條路,受詞不同 |
| 5 | 各自的 `*.test.tsx` | `products-url-state.hooks.test.tsx` 有 ㉜㉝㉞㉟㊱㊲㊳㊵㊴ 九格在守現行行為 ⇒ **改行為就要改它們** | 🔴 這是本片最大的一塊 |

🔵 **切片**:①+②+⑤ 一片(同一個行為,分開改會出現「分類留著而排序不留」的不對稱)。

---

## 5. 🔴 壞法會不會叫 ⇒ **不會。而這一格要寫死,不要附贈修它**

```
壞法        改了之後關鍵字仍然不進 p_terms(或只有分類那條改到, 排序那條沒改到)
症狀        搜尋少回幾筆 / 多回幾筆 —— 而畫面完全正常, 沒有紅字、沒有 0 筆
會叫嗎      🔴 不會。今天沒有任何東西在看「p_terms 該有值而它是 null」
量得到嗎    🟡 測試量得到(那九格);而【線上】量不到
```
🛑 **要它會叫是另一件事,不併進本片** —— 那需要一個「關鍵字在 URL 而 p_terms 是 null」的告警,
而那是新機制(⇒ 減法版明文不加量具)。**本片只寫下它不會叫。**

✅ **而本片有一個【不用新機制】的驗收**:照 §1 那把尺再走一次 —— 打一個非 facet 的詞、
點它自己所屬的分類 ⇒ **筆數要維持 1,不是 10**;而判定看 `p_terms`,不看筆數。

---

## 6. rollback

```
純前端 ⇒ git revert 那一顆, 零 DB 動作、零貼板。
🟢 而【退回去是安全的】:退回 = 今天的行為(點 facet 清掉關鍵字 + 一行小字),
  那是一個【看得見的缺】不是【安靜的錯】—— 照 lib/search.ts 檔頭那個拍板的判準。
```

---

## 7. 那三筆 stash 怎麼處理 —— 🛑 **不要 pop**

```
agent/shop 上三筆, 三筆都是 ⟦db-SEARCHFACETMUTEX⟧:
  b47ca03f 2026-09-09 21:55  R1 三條 must-fix 未收      201 加 / 77 刪
  c446eebb 2026-09-10 02:56  R2 四條 must-fix           327 加 / 77 刪
  9fcc7a47 2026-09-10 10:49  FAIL 3 風險理由要重做      283 加 / 77 刪
🔬 三筆動的是【同樣的 4 支檔】、刪除行數三次都是 77 ⇒ 不是三種做法, 是同一條路走三次。
```
🎯 **而它們撞的那面牆,在它們擱下之後被別人拆了** —— `20260909010000` 合路是 2026-09-09,
而三筆的第一筆是同日 21:55。**它們在繞一個當時已經不需要繞的東西。**
🛑 **⇒ 新做法從本 plan 出發,不從 stash 出發。** 那三筆留著當「三次撞牆」的紀錄,不 pop、不 apply。
🛑 **那七條 must-fix(R1 三 + R2 四)不去撈** —— 它們是**舊做法**的 must-fix,而本片只改一行前端。

---

## 8. 🛑 我證不到什麼(照實列)

- **§1 是鑽機不是正式站** —— 商品是種子(108 件);而**前端碼與正式站同一份**(worktree 起自 `5f02a8023`)。
- **只走了「分類」這一個 facet** —— 品牌 / 價格 / 車款那幾條**沒走**。⚠️ §4 表格裡「排序那條也會刪 `search`」是**讀碼得到的**,不是實測。
- **我看的是 PostgREST 送進 DB 的 body**,不是前端送出去的那一發。
- **沒量客人多常這樣做** —— 正式站真客人 0 ⇒ 今天的曝險是「沒有人走過那條路」,不是「安全」。
- **§3 那個結論(舊病構造不出來)是【讀碼 + 讀 commit body】推的** —— 我**沒有**去把 `delete('search')` 拿掉再跑一次看會不會真的壞。🔴 **那一發要在開工時當第一個驗收做,不要當結論用。**
- **那九格測試我只數了編號,沒有逐格讀它們在守什麼。**

---

## 9. 落點

本 plan 交主視窗 → **§3 那一題先答**(它翻的是 2026-09-03 的拍板)→ 批准後照 §4 開工。
🛑 **§3 沒答之前,一行都不要動。**
