# Plan:客人下載自己的訂單明細(甲案 —— 他自己的瀏覽器畫)

- 線A `-e9` · 2026-08-30 00:4x · **只寫 plan,零碼、零 apply、不 push**
- 前置:`-b9` 的選型檔 `docs/plans/2026-08-29-pdf-render-selection.md`(`6a0a86b0`,120 行)
  **已經把「誰來畫」那半做完** ⇒ **本檔只做 delta,不重講選型。**
- 接件前先量(自己那支工具):板 `:208` **態 = `open`** ⇒ 這一列還開著,沒有被別人做掉。

---

## ① 真權威(鐵則 1)—— **已解析,而它不在 `pcm-524f`**

```
OD 磁碟目錄數 ⇒ 12（ls …/data/projects | wc -l）
而這一片的稿在 pcm-home-redesign，不是 pcm-524f（後者是後台訂單那一份）
```
**`pcm-home-redesign/order-detail-page.html` 逐字**:
```
:193  「下載訂單 PDF」= 訂單明細 / 對帳單，**不是發票**（2026-08-07 Sean 拍板）：
      公司行號的對帳需求走這裡；發票走財政部電子發票平台與載具，是另一條路。
      稿上刻意不出現「下載發票」四個字，免得網站端誤接成開立發票。
:197  <button type="button" class="acc-btn-ghost" id="od-pdf">下載訂單 PDF</button>
:286  原型不產檔，所以不假裝下載成功、也不給一個會 404 的 href
```
🛑 **⇒ 接的人千萬不要接成開發票。這是稿上唯一被特別叮嚀的一格。**

---

## ③ 🔴 授權 —— **好消息:那條路【今天已經存在】,不用碰 RLS**

```
apps/storefront/src/app/account/orders/[displayId]/page.tsx
  :53  supabase.auth.getUser()          ⇒ 未登入 → redirect /login?next=…
  :61  findOrderDetailForCustomer(displayId, user.id)
而 SupabaseOrderAdapter.ts:815 那支的查詢是
  .eq('display_id').eq('customer_user_id').maybeSingle()
```
✅ **⇒ 「客人打得開自己的、打不開別人的」已經由既有那條路保證。**
🔴 **⇒ 所以本片【不動 auth、不動 RLS】⇒ 不命中鐵則 12②。**
⚠️ **而新頁必須走同一條** —— 不得自己重寫一個查詢。**那是本片最容易出事的一格。**

---

## ② ⚠️ 而【要印什麼】有一格我不決定 —— **寫成問題給 Sean**

`-0b` 判後台那張「訂單明細」與客人要的**同族同源**,而**容差沒拍**。
**我開檔量到的差異面**(後台那張 `picking-doc.tsx` 現況):
```
它現在印：金額三欄 + 收件姓名/電話/地址（Sean 2026-08-29 拍 Q-DETAIL-MONEY = 甲）
而客人版：那些【本來就是他自己的資料】⇒ 沒有外洩問題
🔴 而真正的差別是【那張紙是給誰看的】：
   後台版有「揀貨/出貨」語境的欄位（而 Sean 08-29 已拍掉勾選框）
```
**⇒ 一個字回得完的題**:
```
Q-客人明細容差:客人下載的那一張，跟後台印的「訂單明細」要不要一模一樣？
   甲  一模一樣（同一份版面，客人版只是換個入口）
   乙  客人版要拿掉倉庫用的欄位（我先列給你看再做）
A: 甲|乙
```

---

## ④ 體積:**三片,每片 15-45 分鐘(鐵則 4)**

| 片 | 做什麼 | 為什麼可獨立 |
|---|---|---|
| **A** | 顧客站 `/account/orders/[displayId]/statement` 路由 + 走既有 `findOrderDetailForCustomer` | 只加一條路由,不改任何既有頁 |
| **B** | 版面:重用 `print-a4.css` 與那張明細的版面 | 🔴 **而它卡在 ② 那個容差題** ⇒ Sean 答了才動 |
| **C** | `OrderDetailView.tsx` 那顆 ghost 鈕接上去 + smoke test | 🔴 **必須最後做** —— 前兩片沒好就接,就是稿註解警告的那顆死鈕 |

🛑 **順序是硬的:C 不得早於 A/B。** 稿 `:286` 逐字「**不假裝下載成功、也不給一個會 404 的 href**」。

---

## ⚠️ 射程與未決

- **未量**:客人端 OS 分佈 —— 甲案在 Linux 桌機客人上可能落到 `sans-serif`(`-b9` 選型檔已標,我沒有新數字)。
- 🔴 **`print-a4.css` 在 `apps/admin`** ⇒ 顧客站要用它,**是複製還是抽共用套件**,我沒有決定 ⇒ **那是片 B 開工前要判的第一件事**(而抽共用 = 動 `packages/ui` ⇒ 鐵則 12⑥)。
- **本片不含**:發票(稿明文另一條路)、Email 夾寄明細(Sean 未提 ⇒ 視為不做)。
