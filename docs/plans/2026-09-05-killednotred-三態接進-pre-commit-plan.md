# plan · `⟦b9-KILLEDNOTRED⟧` 把 `greenlight` 的三態接進 pre-commit

> 線【身分】`-auth` · 2026-09-05 · 主視窗 `-f8` 派(**先驗還開不開,開著就出 plan**)
> 🛑 **本份零行碼、零 apply。** 動 `.husky/` = **鐵則 12④ 平台設定** ⇒ 要 codex 一輪,而**那不在本份裡**。

---

## 判型:**② 還開著** —— 而那是量到的,不是讀板列

```
.husky/pre-commit  grep 'greenlight|ENV-FAIL|SIGKILL|137|143'  ⇒ 🔴 零命中
scripts/greenlight.sh                                          ⇒ 三態仍在
                                                                  :18  ENV-FAIL(2) 工具自己跑不起來
                                                                  :123 rc=2 且印 ENV-FAIL(不是 RED)
```
⇒ 📌 **分類還在、而那條線【仍然沒有接上】** —— 與板列 2026-09-01 寫的一模一樣,一個字都沒變。

---

## 🔴🔴 而動手之前先讀這一格:**這台機器現在正在燒**

本列誰欄逐字:**「⚠️ 機器在燒的時候不要做這一片」**。而 2026-09-05 晚當場量:

```
sysctl vm.swapusage ⇒ total 18,432M · used 17,133M · free 1,298M   ⇒ 93% 滿
🟢 正對照 sysctl -n hw.memsize ⇒ 17,179,869,184(證明 sysctl 在回真值)
```

🎯 **⇒ 那個條件【現在成立】。** 而本列講的病(`SIGKILL` 被讀成「你的碼有問題」)**正是這種壓力下發生的**。
⇒ 🛑 **所以這一份只到 plan,不動碼** —— 在 swap 93% 的機器上跑一輪 codex 去審一支 husky hook,
   本身就是在製造那一列描述的那個事故。

⚠️ **而這個數字是【這一刻】的** —— 它不是本片的判準,它是「今天不要動手」的理由。
  真的要動手那天,**重跑那兩行**,不要引用這裡的數字。

---

## 要改什麼(形狀,不是碼)

### ① 病在哪(板列已記,這裡只留座標)
`SIGKILL` 讓 lint-staged 的 task 死掉 ⇒ husky 印 `pre-commit script failed (code 1)`
⇒ 🔴 **那個 `code 1` 與「你的碼有問題」在畫面上逐字相同** ⇒ 撞到的人會去查自己的碼。

### ② 修法 = **把既有的接上去,不是發明一個新分類**
`scripts/greenlight.sh` 已經是三態(`GREEN(0)` / `RED(1)` / `ENV-FAIL(2)`),
而它自己的檔頭逐字寫著「**看到 `ENV-FAIL` 不要查你的碼**」。
⇒ 📌 這是**機制優先律的教科書案例**:缺的不是分類,是**那條線**。

### ③ 最小形狀(三選一,我推薦甲)
```
甲(裁定版, 2026-09-05 主視窗 -f8 依三世界 + 真樣本重寫)
        ⇒ **判準 = 掃 log 找 `Task killed` / `[SIGKILL]` / `heap out of memory`**
        ⇒ 命中就印一行講人話的:「🔴 這不是你的碼 —— 工具被機器砍掉了(記憶體不夠)」
           + 建議先看 `uptime` 的 load
        ⇒ 仍然 exit 1(**不放行**), 只是把【訊息】換掉。
        🔵 為什麼仍然 exit 1:放行等於讓一次沒跑完的檢查變成綠 —— 那比訊息不好更糟。
        🔴🔴 **rc 只當【附加】不當判準** —— 而那是真樣本逼出來的:
           2026-09-01 那次 husky 看到的是 `code 1`(lint-staged 把子程序的死法吃掉了)
           ⇒ **137 / 134 在真實路徑上根本到不了 hook** ⇒ 只認 rc 的版本會對真實情況失明。
        ⏭️ **同一顆一起做**:`.husky/pre-commit:253` 的 `pnpm exec lint-staged --no-stash`
           接上 `|| exit $?` —— **同一行, 同一片**(主視窗裁)。
乙      改成 exit 2 走 greenlight 的三態 ⇒ 🛑 而 husky 對非 0 一律當失敗,
        exit 2 與 exit 1 對 git 是同一件事 ⇒ **它只換數字不換行為**, 收益等於甲而多動一層。
丙      不改 hook, 改 lint-staged 的 task 包一層 ⇒ 🛑 要動 62 個 task 的每一個, 爆炸半徑最大。
```

### ④ 驗收(兩個世界,而第二個是這一片的難處)
```
🟢 正對照  真的有 lint 錯 ⇒ 訊息【不得】變成「不是你的碼」
🔴 負對照  構造一個被 SIGKILL 的 task ⇒ 訊息要換掉
          ⚠️ 而【怎麼構造】是這一片最貴的一格:
             `kill -9` 自己的子程序做得到, 而它是不是與「核心因為 swap 滿而砍」走同一條路
             —— **我沒有量過**, 不要寫成「等價」。
          🔬🔬 **[2026-09-05 晚 · 三個世界量完了, 而結論推翻了甲案的判準]**
          ```
          世界                                              rc     log
          ① kill -9 一個 node 子程序                        137    (無)
          ② --max-old-space-size=64 + Buffer.alloc          137    (無)
          ③ --max-old-space-size=64 + "x".repeat(100000)    134    heap out of memory ✅
          ```
          🔴 **②為什麼也是 137(這一格值得記)**:`Buffer.alloc` 配的是 **V8 堆【外】**的記憶體
             ⇒ `--max-old-space-size` **管不到它** ⇒ 它撞的是 OS 不是 V8 ⇒ 被系統砍 ⇒ 137。
             ⇒ 📌 **同一個旗標, 配置的位置不同就給不同的 rc** —— 我第一發用 `Buffer.alloc`,
                量到 137 而以為「OOM 也是 137」, **那是錯的**。
             ⚠️ 主視窗預期 134, 而它**只對③成立** —— 兩邊都對一半, 而分辨它們的是【配在哪】。
          ⇒ ✅ **所以甲案要同時認 137 與 134**, 再加 log 字面 `SIGKILL` / `heap out of memory`。

          🔴🔴 **而【真樣本】把上面整段判準推翻了一半 —— 這才是最重要的一格**:
             板列引的 2026-09-01 真實那次, 畫面逐字是
             `✖ Task killed: eslint … [SIGKILL]` … `husky - pre-commit script failed (code 1)`
             ⇒ 🛑 **husky 看到的是 `code 1`, 不是 137 也不是 134。**
             ⇒ 📌 **lint-staged 把子程序的死法【吃掉了】, 它自己回 1。**
             ⇒ ⇒ 🔴 **「看 rc 是不是 137/134」這個判準, 對【真實情況】結構上失明。**
                **唯一還活著的訊號是【log 的字面】**(`Task killed` / `[SIGKILL]`)。

          🔴🔴 **順帶量到一格獨立的缺口(不是本片, 而它就在同一支檔上)**:
             `.husky/pre-commit:253` 是 `pnpm exec lint-staged --no-stash` —— **沒有接 `|| exit $?`**,
             而全檔 `set -e` 命中 **0**。
             ⇒ 而該檔檔頭 `:15-16` 逐字宣稱「本檔十道閘每一行都接 `|| exit $?` ——
                裸呼叫的行數 ⇒ **0**」, 並附了數法。
             🔬 **當場跑它自己那把尺**:`grep -cE '^\s*(sh|bash|node|npx|python3)\s+\S+\s*$'` ⇒ **0**
                —— 🎯 **而那把尺的字集裡【沒有 pnpm】** ⇒ 加上 pnpm 之後那一行就出現了。
             ⇒ 📌 **「裸呼叫 0」對它的尺為真, 對這支檔為假** —— 那正是 `⟦b9-CLAIMEDPROTECTION⟧` 那一族。
             ⇒ 🛑 **而漏掉的那一行, 正好是【跑全部 90 個 lint-staged task】的那一行**
                —— 也就是本列講的 SIGKILL 真正發生的地方。**兩件事在同一行相遇。**

          🔬 **兩發跑了(2026-09-05 晚), 而【只有一半量得到】**:
             · `kill -9` 一個 node 子程序 ⇒ **rc = 137** ✅ 量到
             · `ulimit -v 51200` ⇒ 🔴 **這台機器不支援**(macOS 常見:`ulimit -v` 被忽略)
               ⇒ **那條「讓 node 自己 OOM」的路在這台機器上【構造不出來】。**
          ⇒ 🛑 **所以那句「我沒有量過」【仍然成立, 只是換了理由】**:
             不再是「還沒跑」, 而是 **「跑了, 而這台機器給不出那個世界」**。
             ⇒ 📌 **我量到的是 `kill -9` 回 137, 而本片要擋的是【核心因記憶體壓力而砍】**
                —— 兩者是不是同一條路, **到今天仍然沒有證據**。
          ⚠️ **而那不是小事**:甲案的判準就是「看 rc 是不是 137」。
             若核心 OOM 那條路回的不是 137(例如只留 log 而 rc 是別的值),
             **甲案會對它失明, 而它正是本列最想擋的那一種。**
          ✅ **下一個能走的路(未做)**:去翻**真的發生過那次**的 log ——
             板列引的 2026-09-01 那次逐字有 `[SIGKILL]`,
             ⇒ 那是**真實世界的樣本**, 比我在這裡構造一個更接近。要看它的 rc, 得找到當時的完整輸出。

          🔴🔴 **而我做這一發時犯了一個錯, 寫在這裡因為它正是本列的主題**:
             我在一台**剛量到 swap 93%** 的機器上, 跑了一個**無上限的記憶體配置迴圈**
             (`for(;;) a.push(Buffer.alloc(1e7))`)⇒ 它跑了 120 秒被移到背景, 我才發現。
             ⇒ 📌 **我為了驗證「記憶體壓力會讓工具被砍」, 自己去製造記憶體壓力** ——
                而那台機器上還有別的窗在跑。已 `pkill -9` 收掉並複驗無殘留。
             ⇒ 🛑 **教訓寫成可執行的**:要造 OOM, **先設上限再跑**
                (`node --max-old-space-size=64`), 不要用無上限迴圈去撞天花板。
```

---

## 🛑 這份 plan 答不出什麼
* **它沒有驗過 husky 對 `exit 2` 的處置** —— 乙案那句「與 exit 1 對 git 是同一件事」是**讀來的常識,不是量到的**。要走乙必須先跑一發。
* **它沒有量過 62 個 lint-staged task 裡有幾個會在壓力下被砍** —— 丙案的爆炸半徑是**估的**。
* **它不解「為什麼機器會燒」** —— 那是別的事(今晚同時跑 codex / 鑽機 / 多窗)。本片只讓**撞到的人不要去查錯地方**。

## ⏭️ 下一步(要 Sean 或主視窗)
1. 主視窗裁甲/乙/丙。
2. ✅ **主視窗 `-f8` 2026-09-05 裁【甲】。**
3. 動 `.husky/` ⇒ **鐵則 12④** ⇒ codex 一輪 —— 🛑 **而那一輪要等機器不燒的時候跑**(見上面那格)。
   🔵 主視窗當場自陳:那 93% 主要是它的 chain 在跑 vitest 全套兩發(約 8 分鐘)
   ⇒ **成因已知、會退, 不是機器壞了** ⇒ 它推完會廣播, 那之後再跑 codex。
