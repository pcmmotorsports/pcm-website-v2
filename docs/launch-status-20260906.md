# 離上線還剩什麼(2026-09-06 · 白話版)

> **摘要,不是全部。** 完整清單 `docs/launch-todo.md`(761 列)。現值當場跑:`bash scripts/launch-blocking-count.sh`
> 產出:盤整窗 `tidy` · 基準 = **本檔所在的那顆 commit** · 2026-09-06 19:2x CST

## 1 · 一句話
**全部判完了 —— 102 件擋上線。**

```
open   387 ⇒ 擋 76 · 不擋 278 · 未判 33 · 未填 0
doing   22 ⇒ 擋  7 · 不擋  12 · 未判  3 · 未填 0
parked  91 ⇒ 擋 19 · 不擋  70 · 未判  2 · 未填 0   ← Sean「我怕有漏」之後補判的
standing 3 ⇒ 擋  1 · 不擋   2                    ← 同上
    合計 ⇒ 擋 102 · 不擋 364 · 未判 38
```
🔴 **Sean 那句「我怕有漏」救回 20 個擋** —— `parked`(刻意先不做)那 94 列原本不判,而 **19% 的擋藏在裡面**。
⇒ 📌 **「刻意先不做」與「不擋上線」是兩件事,而 `parked` 這個態只記錄前者。**
🔴 **83 不是「下界」,它是【open + doing 這 410 列】的完整答案。**
⚠️ 而 `parked` 91 + `standing` 3 **正在補判中**(Sean 2026-09-06 親口「我怕有漏」⇒ 主視窗裁甲);
`done` 259 不判 —— 它問的是「做完了沒」,不是「擋不擋」。

### 🔴🔴 數字要帶【判準版本】才能引用
|  | 2026-09-01 那把尺 | 本輪(tidy)這把尺 |
|---|---|---|
| 拿不準時 | **一律算「擋」** | **標「未判」** |
| 報出來的數 | 擋 37(分母 open 160) | **擋 102** + 未判 38(分母 503) |

📌 **同樣那 38 件「拿不準」,在舊尺下全部算擋 ⇒ 舊尺會報 140,不是 102。**
兩個都誠實,差別只在判準版本 —— **版本不寫在數字旁邊時它們分不出來,都是一個整數。**
**誰要動它們**:83 個擋按【誰欄】分好在 `docs/launch-blocking-by-owner-20260906.md`。
🔴 **那份的頭條是**(它產生時是 83 個擋,現在 102 ⇒ **分堆要重跑**):53 列不在任何一條線手上(48 列沒有人被指名 + 5 列在等 Sean),
只有 **30** 列有窗(mail 9 · account 5 · ship 5 · db 4 · tidy 3 · front 2 · auth 1 · 報價單 1)。
## 2 · 客人最直接會痛的幾件(**不是全部** —— 全部 83 件在 `docs/launch-blocking-by-owner-20260906.md`)

> 🛑 **這一節是【挑出來的】,不是清單。** 下面 17 件是最早判出、客人最直接感受得到的;
> 之後又判出 66 件(稅、權限、監測、出貨治理…)⇒ **不要把這 17 件讀成「就這些」。**

### 💰 錢(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKDUPPERCART⟧` | 同一個購物車能建出**兩張匯款單**,今天沒東西擋 |
| `⟦b4-BANKCARDRACE⟧` | 兩個分頁:刷卡單與匯款單**同時活著** |
| `⟦b4-CARDPENDINGWINDOW⟧` | 刷卡「進行中」窗口,客人另開分頁還能建匯款單 |
| `⟦b4-BANKCHARGESCARD⟧` | 翻開匯款開關那一刻,系統拿客人的卡去扣**一張匯款單**的錢 |
| `⟦b4-NCPCRONRACE⟧` | 逾期程式先鎖單、收款才落帳 ⇒ **「已取消而錢在裡面」** |
| `⟦b4-NCPCANCELROLLBACK⟧` | 重算超時,客人**已入帳的收款跟著被回滾** |
| `⟦0a-CARDCANCELNOREFUND⟧` | 刷卡單取消**零自動退款、零提醒** ⇒ 錢卡在公司而客人不知情 |

### 📦 出貨(3)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦ship-HCTLABEL⟧` | 要貼在箱子上那張紙,**今天沒有人印得出來** |
| `⟦ship-HCTUNKNOWNREAD⟧` | 卡在「送出結果未知」的箱子,按鈕會對新竹**造出重複託運單** |
| `⟦ship-HCTACTIVATION⟧` | 新竹上線缺具名 activation,正式站 endpoint 未確認 |

### ✉️ 客人收到的東西(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKNOEMAIL⟧` | 選匯款的客人結帳後**一封信都收不到** |
| `⟦b4-MAILRENDER1⟧` | 付款成功信**從沒有人在真信箱看過**(⚠️ 是「沒驗過」不是「已知會錯」) |
| `⟦5b-SHIPPEDNUMNOTRECORDED1⟧` | 沒存實寄追蹤號 ⇒ 客人可能**永久拿著錯號碼**,而沒東西會叫 |
| `⟦b4-EMAILTRIAGE⟧` | 換過 email 的人可能**收到別人訂單的交易資訊** |
| `⟦b4-RECIPLIVEVALUE⟧` | 客人改了 email,信還是寄到**舊信箱**(裡面有交易資料) |
| `⟦b4-CANCELMAILMIXEDRAIL⟧` | 卡+現金混合退款的取消單**系統不寄**,而沒有提醒叫人工寄 |
| `⟦front-PDPTAXONOMYEMPTY⟧` | 商品頁「適用車款」整區空掉,**頁面回 200、畫面不說** |

🔴 **另外 66 件**在那份 by-owner 檔裡,大宗是:**稅**(六個客人看得到的金額面缺稅列)、
**權限**(管理員閘繫於看不到值的開關 · 系統帳號可刪客戶表 · 災難還原零稽核)、
**監測**(車款同步失敗零告警 · 部署順序閘誤判放行)。

## 3 · Sean 側欠的動作
> ✅ **已完成(不用再做)**:Vercel 防火牆規則 `search-log-flood-cap` **已由 Sean 2026-09-06 19:1x 親手加入**
> (他逐字「規則名稱 search-log-flood-cap 加好ㄌㄜˊ」);主視窗在主樹跑 `vercel-firewall-cron-order-check.py` live 檢查過:
> 6 條規則,`search-log-flood-cap` #6、`bypass-machine-traffic` #5 ⇒ **bypass 之上沒有規則擋排程** ✅。
> ⚠️ 板列錨 `⟦auth-SEARCHWAFRATE⟧` 現在 grep 會回 **0** —— 它活在 auth 未推的 `10a53569d`,**39d 收割後才查得到**。
1. **貼板** —— 幾支 migration 等你說「貼 N」才進正式庫(一次一支)。
2. **合 main** —— `dev` 的東西要你手動合進 `main` 才上正式站。
3. **`Q-新竹傳輸方式`** —— 要新竹回應格式,`⟦ship-HCTUNKNOWNREAD⟧` 卡在這。
4. **`Q-出貨PDF`** —— `⟦f3-SHIPPDF1⟧` 在等。

## 4 · 🔴 這張板子沒涵蓋什麼(不要把它讀得比它大)

1. 🔴🔴 **那 410 列【不是我一列一列讀的】** —— 事實抽取與初判由 **7 個 subagent** 做,我逐條複核它們的報告(翻案 3 條、另 2 條加註記),**而我讀的是 7 份報告,不是 410 列原文**。⇒ 正確的說法是:**每一列都被讀過,而讀的人不全是我。**
2. 🔴🔴 **有一發 SQL【不要跑】** —— `docs/launch-todo.md` 那列「未確認 停產品會不會自動從店裡下架」(`done`,錨欄 `—`)提議「數上游已停產而站上仍 `delisted_at IS NULL` 的筆數」。**那一列自己逐字預言**:「若真的跑,會回一個**很大而且正確**的數字 ⇒ **一個看起來像事故的正常狀態**;而跑它的人會把那個數字端上來,而它每一位都是對的。」
   ✅ **因為那不是缺口,是規格** —— Sean 原話寫在 `supabase/migrations/20260815030000_…:74` 的 `COMMENT ON COLUMN`:**「如果原廠停產, 但是我有現貨庫存, 那我需要維持上架狀態」**;同段 `:75`「本欄**不得**被接到任何自動下架邏輯上」。
3. **`done` 259 列不判** —— 它問的是「**做完了沒**」,不是「擋不擋」,那是另一個問題。⚠️ 而查它的工具(`board-state-consistency.py` 規則④)檔頭逐字寫著**刻意不做**這一族 ⇒ **已知盲區,不是本輪漏的。**
4. **36 列標「未判」** = 誠實的答不出來,不是漏掉。其中兩列的理由是**政策上不准做那道檢查**(要對 GitHub 實測 force-push;要正式庫金鑰)。
5. **2026-09-01 那份「37 件」本輪沒逐列複驗** —— 那張表是**副本**,板子自己記著「37 列裡有 20 列的錨欄不是真錨」+「一律以主表為準」⇒ 拿副本對照會數錯,要做必須從主表走。
6. **知識地圖曾落後 4959 顆 / 15 天** ⇒ 本輪已刷新(**58,619 節點 / 82,239 邊 / 3,278 社群**,09-06 19:00)。⚠️ 它的分母是**跑它的那棵樹**,未推的 commit 不在圖上;`graph.html` 因節點超上限被跳過(已知限制,只能用 `query`)。
7. **`docs/phase-1-backlog.md` 狀態欄覆蓋率退步**:89.1%(08-18)⇒ **82.2%**(09-06)。歷史三格一格沒變,掉的**全是 08-18 之後新開的條目** ⇒ 病在「開條目那一刻」,回頭補會再掉。
8. **`docs/plans/`(88 支)與 `docs/specs/`(250 支)兩個平行計畫目錄未整併**;根層 `PROGRESS.md`(1736 行)是隱性待辦墳場,本輪不碰。
9. **194 列的錨欄沒有錨**(是 `#編號` 或 `—`)⇒ 板頭「引用一列給錨」那條規矩對它們**結構上做不到**。本輪不補錨。
10. 🔴🔴 **今晚有列被證明【板上比實況嚴重】—— 未派工前先跑 `what-happened-to`,那是規矩不是建議**
   · ✅ **我親自驗過的兩列**:`#868`(已取消單還會寄付款成功信)⇒ **寄信那一半早就接上了**(`sweep-email-outbox.ts:1325` 真的呼叫;`:1310` 是註解)⇒ 已轉 `done`。`⟦b4-RESTORE2⟧`(災難還原蓋回客戶表 + 零稽核)⇒ **兩半都不對**:根本不是「蓋回」(`ON CONFLICT DO NOTHING`,**開列當天就已經是**),稽核 8 天前補上了。
   · ⚠️ **主視窗另外點名 3 列**(`⟦b4-BANKCHARGESCARD⟧` / `⟦search-TAXONOMYTIMEOUT⟧` / `⟦front-PDPTAXONOMYEMPTY⟧`)—— 🛑 **那 3 列在本檔基準的樹上仍然是「擋」,我沒有複驗** ⇒ 標**轉述,未確認**,不併進上面兩列。
   📌 **形狀:一個宣稱可以【當初就描述錯】,也可以【後來被修掉】—— 而在板上它們長得一樣,都只是一句沒有人回來劃掉的話。**
11. 🔴 **本輪我自己撤回了三個結論** —— 寫在這裡,因為它們**都通過了當下的檢查**:
   ① 我說某列是 `done` 裡唯一的真殘餘 ⇒ **只讀了那列前 620 字元**,訂正住在尾巴(而那一列預言了「跑那發 SQL 會回一個很大而且正確、看起來像事故的數字」)
   ② 我叫助手「不要猜行號偏移量」⇒ **我自己一量,偏移量是常數**(13/13 命中 vs 對照組 5/13)⇒ **保守的訂正也會製造錯誤,而那種錯不會有人抗議**
   ③ 我說某道守門「沒人存檔的日子一聲不出」⇒ **每次 push 都會叫,而且現在就在叫** —— 我沒看到是因為**我從來不 push**
12. **本檔沒有估時。** 估時要動手的人給。
