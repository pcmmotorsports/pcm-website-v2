# 離上線還剩什麼(2026-09-06 · 白話版)

> **摘要,不是全部。** 完整清單 `docs/launch-todo.md`(761 列)。現值當場跑:`bash scripts/launch-blocking-count.sh`
> 產出:盤整窗 `tidy` · 基準 = **本檔所在的那顆 commit** · 2026-09-06 19:2x CST

## 1 · 一句話
**判過的 82 件裡,20 件擋上線。** 而板上 763 列 ⇒ 🔴 **另外 681 列還沒判,所以 20 是【下界】不是總數。**

```
擋 20 · 不擋 51 · 未判 11  ⇒ 相加 82(這一輪判過的)· 還沒填 681
分母 763 = open 389 / done 258 / parked 91 / doing 22 / standing 3
```

### 🔴🔴 這個數字要帶【判準版本】才能引用
|  | 2026-09-01 那把尺 | 本輪(tidy)這把尺 |
|---|---|---|
| 拿不準時 | **一律算「擋」** | **標「未判」** |
| 報出來的數 | 擋 37(分母 open 160) | **擋 20** + 未判 11(分母 82) |

📌 **同樣那 11 件「拿不準」,在舊尺下全部算擋 ⇒ 舊尺會報 31,不是 20。**
兩個都誠實,差別只在判準版本 —— 而**版本不寫在數字旁邊時,它們分不出來,都是一個整數。**
**為什麼先判這 82 件**:它們是「今天有人動過、而態還掛著 open/doing」的一族,命中率最高。
同一輪也證明方法可用:各窗自己已經關掉 11 列。
## 2 · 那 20 件,分三塊
### 💰 錢(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKDUPPERCART⟧` | 同一個購物車能建出**兩張匯款單**,今天沒東西擋 |
| `⟦b4-BANKCARDRACE⟧` | 兩個分頁:刷卡單與匯款單**同時活著** |
| `⟦b4-CARDPENDINGWINDOW⟧` | 刷卡「進行中」窗口,客人另開分頁還能建匯款單 |
| `⟦b4-BANKCHARGESCARD⟧` | 翻開匯款開關那一刻,系統拿客人的卡去扣**一張匯款單**的錢 |
| `⟦b4-NCPCRONRACE⟧` | 逾期程式先鎖單、收款才落帳 ⇒ **「已取消而錢在裡面」** |
| `⟦b4-NCPCANCELROLLBACK⟧` | 重算超時,客人**已入帳的收款跟著被回滾** |
| `⟦b4-PAIDTHENOVERPAID⟧` | 已付款的單被多匯一次 ⇒ **今天零觀眾** |

### 📦 出貨(3)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦ship-HCTLABEL⟧` | 要貼在箱子上那張紙,**今天沒有人印得出來** |
| `⟦ship-HCTLABELCAPTURE⟧` | 標籤圖存得下來了,而**一箱都還沒真的送過** |
| `⟦ship-HCTUNKNOWNREAD⟧` | 卡在「送出結果未知」的箱子,按鈕會對新竹**造出重複託運單** |

### ✉️ 客人收到的東西(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKNOEMAIL⟧` | 選匯款的客人結帳後**一封信都收不到** |
| `⟦b4-MAILRENDER1⟧` | 付款成功信**從沒有人在真信箱看過**(⚠️ 20 件裡最不確定的一件:是「沒驗過」不是「已知會錯」) |
| `⟦5b-SHIPPEDNUMNOTRECORDED1⟧` | 沒存實寄追蹤號 ⇒ 客人可能**永久拿著錯號碼**,而沒東西會叫 |
| `⟦b4-EMAILTRIAGE⟧` | 換過 email 的人可能**收到別人訂單的交易資訊** |
| `⟦b4-CANCELMAILMIXEDRAIL⟧` | 卡+現金混合退款的取消單**系統不寄**,而沒有提醒叫人工寄 |
| `⟦front-PDPTAXONOMYEMPTY⟧` | 商品頁「適用車款」整區空掉,**頁面回 200、畫面不說** |
| `⟦search-TAXONOMYTIMEOUT⟧` | 同一個靜默失敗存在 **9 個客人看得到的入口** |

### 🔧 守錢的監視器沒裝(3)
`⟦f3-REDNEEDSEXIT⟧` · `⟦db-MERGEBLINDGATE⟧`(`git merge` 繞過 19 道守門,今天真的漏擋一次)· `⟦db-DOGBLINDBRANCH⟧`(部署順序閘誤判放行,曾造成約 8 小時當機)
> 這一類算「擋」是 **Sean 2026-08-29 自己拍的**:「監測/守門沒裝,而被守的是錢或產線 = 擋」。

## 3 · Sean 側欠的動作
> ✅ **已完成(不用再做)**:Vercel 防火牆規則 `search-log-flood-cap` **已由 Sean 2026-09-06 19:1x 親手加入**
> (他逐字「規則名稱 search-log-flood-cap 加好ㄌㄜˊ」);主視窗在主樹跑 `vercel-firewall-cron-order-check.py` live 檢查過:
> 6 條規則,`search-log-flood-cap` #6、`bypass-machine-traffic` #5 ⇒ **bypass 之上沒有規則擋排程** ✅。
> ⚠️ 板列錨 `⟦auth-SEARCHWAFRATE⟧` 現在 grep 會回 **0** —— 它活在 auth 未推的 `10a53569d`,**39d 收割後才查得到**。
1. **貼板** —— 幾支 migration 等你說「貼 N」才進正式庫(一次一支)。
2. **合 main** —— `dev` 的東西要你手動合進 `main` 才上正式站。
3. **`Q-新竹傳輸方式`** —— 要新竹回應格式,`⟦ship-HCTUNKNOWNREAD⟧` 卡在這。
4. **`Q-出貨PDF`** —— `⟦f3-SHIPPDF1⟧` 在等。

## 4 · 🔴 這張板子沒涵蓋什麼(不要把它讀得比它大)
1. **681 列還沒填「擋不擋」**,含 `parked` 91 與 `done` 258 **全部未觸及** ⇒ **20 是下界。**
2. **2026-09-01 那份「37 件」本輪沒逐列複驗** —— 那張表是**副本**,板子自己記著它
   「37 列裡有 20 列的錨欄不是真錨,是清單原本的位置記號」+「一律以主表為準」
   ⇒ 🛑 **拿那張副本去對照會數錯 ⇒ 要做甲族必須從主表走。**
3. **知識地圖曾落後 4959 顆 / 15 天**(`graphify-out/graph.json` mtime `2026-08-22 06:13`,量於 09-06 17:57)
   ⇒ 本輪已刷新(**58,619 節點 / 82,239 邊 / 3,278 社群**,09-06 19:00);
   ⚠️ 它的分母是**跑它的那棵樹**,未推的 commit 不在圖上;`graph.html` 因節點超上限被跳過(已知限制,只能用 `query`)。
4. **`docs/plans/`(88 支)與 `docs/specs/`(250 支)是兩個平行計畫目錄**,未整併。
5. **根層 `PROGRESS.md`(1736 行)是隱性待辦墳場** —— `STATUS.md` 把「移出主表」的丟進它,而沒有載體指它當待辦。本輪不碰。
6. **`docs/phase-1-backlog.md` 狀態欄覆蓋率退步**:89.1%(08-18)⇒ **82.2%**(09-06)。歷史三格一格沒變,
   掉的**全是 08-18 之後新開的條目** ⇒ 病在「開條目那一刻」,回頭補會再掉。
7. **本檔沒有估時。** 估時要動手的人給。
