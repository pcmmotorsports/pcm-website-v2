# 離上線還剩什麼(2026-09-06 · 白話版)

> **摘要,不是全部。** 完整清單 `docs/launch-todo.md`(⛔ ~~761 列~~ ⇒ **780 列**, 2026-09-06 23:0x 合完 39f 當場數)。現值當場跑:`bash scripts/launch-blocking-count.sh`
> 產出:盤整窗 `tidy` · 🔴 **基準 `origin/dev = aba5c8052`(39k)· 重刷時點 2026-09-07 12:1x CST**
> ⚠️ **這些數字綁在那顆 sha 上** —— 七個窗整夜在寫, **要現值當場跑**:`bash scripts/launch-blocking-count.sh`

## 1 · 一句話
**全部判完了 —— 還在擋的有 90 件。**(⛔ ~~97~~ ⛔ ~~96~~ ⛔ ~~90~~ ⛔ ~~91~~ ⛔ ~~90~~ ⛔ ~~91~~ ⇒ **90**)
> ⚠️ **而「全部判完了」這句在 12:5x 之前【有 20 分鐘是不成立的】** —— `⟦f3-PDPSKUSTATIC⟧`
> (Sean 22:0x 親眼看到那個料號 bug)進板時**沒有 token**, 而**沒有任何東西會出聲**。已補判 **擋②**。

```
⟨擋⟩ 標記 92 · 其中態已是 done 的 2  ⇒  🔴 還在擋 90(2026-09-07 **07:29 CST** 重量;基準 `origin/dev = 8257a3a6a`, 39n 收割後)
🔵 **兩個數都動了而差維持 90** —— 標記 91⇒92、done 1⇒2。📌 **只看「還在擋」那個數會以為什麼都沒發生。**
⛔ ~~標記 92 · 還在擋 91~~ ⇒ 動的是 `⟦5b-REPORTEDNOTLANDED1⟧`:它的擋掛在「wallet ledger 修法未 applied」上, 而 `APPLIED.tsv` 記著 Sean 2026-09-06 已親自貼 ⇒ 改 ⟨不擋⟩。
🔴 **而這一發我先報「非 done 未填 = 2」, 那是【我的尺錯】不是板子錯** ——我的尺假設每列 7 格、取倒數第二格, 而 `:898` 行尾少一根管線只有 6 格 ⇒ 指到誰欄。真正沒 token 的只有 1 列(`⟦db-EXACTHITCJK⟧`), 已補(它誰欄自己就寫著「不擋 front, 也不擋上線」)。📌 **一把假設固定欄數的尺, 遇到欄數不對的那一列會安靜地指向錯的格。**
不擋 405 · 未判 41 · 不適用 3 · 全板 807 列 · 非 done **509** 列(⛔ ~~508~~;2026-09-07 08:1x 當場重量, 全板 807 未變)
                              ← 2026-09-07 06:3x(合完 39l `f84f58447` + tidy 未推)
```
🔴🔴 **這一組數字我今天【改了三次】, 而每一次都是有人發現它過期** ——
**七個窗整夜在寫板子, 它的半衰期以小時計。**
⇒ 📌 **不要引用上面那組。要現值就跑一行**:`bash scripts/launch-blocking-count.sh`
   (它印的是「⟨擋⟩ 標記數」;**還在擋 = 標記數 − 態已 done 的那幾列**, 兩個數字不一樣。)
```
🔴 **「⟨擋⟩ 標記數」與「還在擋」是兩個數字, 不是一個。**
⚠️ ⛔ ~~這一句原本寫死「標記 92 / 還在擋 91」~~ ⇒ **那兩個數字自己過期了兩次**
⇒ 📌 **講【關係】不講【值】** —— 關係不會過期, 值每小時都在動。
⚠️ **而【表格那一格】我已經漏改兩次** —— 09-07 09:0x 一次、12:1x 一次(都是 grep 掃過才發現)。
📌 **⇒ 同一個數字寫在兩個地方, 第二個地方會過期, 而它不會出聲。**
✅ 已把那一格改成**不寫死數字**, 只留「當場跑這行」+ 舊值刪除線。**少一處要同步的地方。**
差的那 1 列是**做完了而標記還停在「擋」** ⇒ 引用時要講清楚你數的是哪一個。
🔵 **96 ⇒ 90 ⇒ 91:數字會【往回走】, 那是正常的** —— 全板從 780 一路長到 **802**(整夜還在新開列)。
**兩股力同時在動**(新開 vs 被處理掉), 所以看單一時點的差值**答不出「今天做掉幾件」**。
🔬 **09-07 12:1x 這一發:總數【沒變】(91⇒91), 而【裡面換了一列】** ——
掉 `⟦b4-SHIPFEETAXBASIS⟧`(被處理掉)· 進 `⟦account-SETTLEVOIDEDPRED⟧`(它的 token 原本位置不對, 補正後才被算進來)。
🛑 **⇒ 只看總數會以為「今天沒有進度」, 而實際上有一件關掉、一件現形。**
🔬 **09-07 09:0x 那一發我也逐列比對過, +2 都指得出名字**:新開 `⟦mail-SKIPKEYNORETIRE⟧` ·
以及 `#64`「客人拿不到發票」**又變成兩列**(merge 帶回來的重複)⇒ 我已把舊的那份標掉, 淨 **+1**。
🔵 **97 ⇒ 96 是【ship 自己改的】,不是我判的** —— `⟦ship-HCTACTIVATION⟧` 在 39f 裡被 ship
從 `open` + 我判的 `⟨擋⟩` 改成 `done` + `⟨未判(ship:啟用文件有了, 能不能送是另一題)⟩`。
🔴 **⇒ 那一列的擋不是消失了, 是被拆成另一題** —— ship 逐字說「能不能送是 `⟦ship-HCTAPI⟧`/§二① 另一題」。
   **要看那一題有沒有人在管, 不要因為這裡 −1 就當它輕了。**
⛔ ~~我第一次回報時寫「是新列 `⟦mail-PHONEONLYNOTIFY⟧` 判不擋造成的」—— 那是錯的~~:
   **一列新的「不擋」不會讓擋數下降。** 我沒有比對前後的擋集合就寫了因果, 差集一跑就現形。
   📌 **判別句:一個數字動了 1, 我有沒有【指名】是哪一列動的?** 指不出來 ⇒ 那是猜的。
🔵 **98 ⇒ 97 那一件不是做完了** —— 是板上有**兩列 `#64`**(同編號、同一件事「客人拿不到發票」),**重複列讓它多算了 1**。已合併。
🔵 **從 103 降到 98,是 Sean 2026-09-06 21:58 + 22:03 一次答 16 題的直接結果** ——
5 列轉不擋、10 列拿到主人,而**「等 Sean」從 12 列降到 1 列**。
🛑 **而轉不擋的那幾列,殘餘我逐列寫在板上**(退款仍只要登入 · 67 支釘子仍沒釘 ·
OWASP 那層仍沒有 · 搜尋紀錄單一 IP 一天仍可寫 14,400 筆)——
📌 **它們是【你知情之後接受的取捨】,不是漏的。而兩者在板上長得一樣,所以要寫。**
🔴 **Sean 那句「我怕有漏」救回 20 個擋** —— `parked`(刻意先不做)那 94 列原本不判,而 **19% 的擋藏在裡面**。
⇒ 📌 **「刻意先不做」與「不擋上線」是兩件事,而 `parked` 這個態只記錄前者。**
🔴 **83 不是「下界」,它是【open + doing 這 410 列】的完整答案。**
⛔ ~~83~~ ⇒ **78**(2026-09-06 23:0x:open 70 + doing 8)。**那一句的【意思】沒變, 變的是分母**:
   當時 410 列, 現在 409 列 open+doing, 而 `parked`/`standing` 後來也判了 ⇒ **全板的擋是 96, 不是 78。**
⚠️ 而 `parked` 91 + `standing` 3 **正在補判中**(Sean 2026-09-06 親口「我怕有漏」⇒ 主視窗裁甲);
`done` 259 不判 —— 它問的是「做完了沒」,不是「擋不擋」。

### 🔴🔴 數字要帶【判準版本】才能引用
|  | 2026-09-01 那把尺 | 本輪(tidy)這把尺 |
|---|---|---|
| 拿不準時 | **一律算「擋」** | **標「未判」** |
| 報出來的數 | 擋 37(分母 open 160) | 🔴 **這一格【不寫死數字】** —— 現值當場跑 `bash scripts/launch-blocking-count.sh`。⛔ ~~還在擋 96 + 未判 40(507 列)~~ ⛔ ~~91 + 41(507)~~ |

📌 **同樣那 40 件「拿不準」,在舊尺下全部算擋 ⇒ 舊尺會報 136,不是 96。**
兩個都誠實,差別只在判準版本 —— **版本不寫在數字旁邊時它們分不出來,都是一個整數。**
**誰要動它們**:⛔ ~~83~~ ⛔ ~~96~~ ⇒ **當場印, 不看名單**:
```bash
python3 scripts/launch-dispatch-table.py
```
🔴 **`docs/launch-blocking-by-owner-20260906.md` 已停止維護**(它自己檔頭寫的)——
理由逐字:**「一份手寫名單的過期速度 = 板子的變動速度」**(七個窗整夜在寫)⇒ 2026-09-07 量到它與板子 **34 列對不上**。
✅ **那份仍然有用的是【分堆的理由】與「碰哪些檔」那一層 —— 那些不會過期, 名單會。**
🔵 當場印的結果(2026-09-07 08:29):擋 **90** · `mail` 15 · `f3` 15 · `ship` 9 · `account` 8 · `auth` 8 · `db` 5 · `front` 4 · `tidy` 2 · **無主 24**。
⚠️ **那份檔身還是 97 那一版的分堆** —— 差 1 列(`⟦mail-PHONEONLYNOTIFY⟧`, 判不擋 ⇒ 本來就不該在裡面)。
🔴🔴 **[2026-09-07 08:1x · tidy 當場重量 —— 下面那組數字的【分母】已經不是今天的]**
下面「54 無主 + 44 有窗」是 ⛔ ~~97~~ 那一版算的, 而**今天還在擋的是 90**。當場重數:
· 還在擋 **90** 列 · 誰欄像沒人接的 **33** 列
· 🔴 **而那 33 列要分成兩種, 它們在板上長得一樣**:**在等外部條件 12 列**(不該派工)· **真的沒人接 21 列**(該派)
📌 **「在等」與「沒人管」混在一起數, 會讓 21 件真的沒人做的事, 藏在 33 這個數字裡。**
⚠️ 而 by-owner 那份檔**還沒照 90 重產** —— 引用它之前先看它的產出時刻。

🔴 **那份的頭條是**(已按 ⛔ ~~97~~ **96** 重產、第三版):**54 列不在任何一條線手上**(**53** 列沒有人被指名 + **1** 列在等 Sean ⇐ 上一版是 12),
**44** 列有窗(mail 10 · account 9 · db 7 · ship 6 · auth 5 · front 3 · tidy 3 · 報價單 1)。
🆕 **那 53 列這一版多分了一層【碰哪些檔】**:後台 19 · DB/migration 11 · 顧客站 10 · 信件排程 7 · 判不出面 5 · 工具 1 ⇒ **可以整族派,不用一列一列看。**
## 2 · 客人最直接會痛的幾件(**不是全部** —— 全部 ⛔ ~~83~~ ⛔ ~~96~~ **90** 件當場印:`python3 scripts/launch-dispatch-table.py`)

> 🛑 **這一節是【挑出來的】,不是清單。** 下面 17 件是最早判出、客人最直接感受得到的;
> 之後又判出 66 件(稅、權限、監測、出貨治理…)⇒ **不要把這 17 件讀成「就這些」。**

### 💰 錢(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKDUPPERCART⟧` | 同一個購物車能建出**兩張匯款單**,今天沒東西擋 |
| `⟦b4-BANKCARDRACE⟧` | 兩個分頁:刷卡單與匯款單**同時活著** |
| `⟦b4-CARDPENDINGWINDOW⟧` | 刷卡「進行中」窗口,客人另開分頁還能建匯款單 |
| `⟦b4-BANKCHARGESCARD⟧` | 翻開匯款開關那一刻,系統拿客人的卡去扣**一張匯款單**的錢 |
| `⟦b4-NCPCRONRACE⟧` | 逾期程式先鎖單、收款才落帳 ⇒ **「已取消而錢在裡面」** |
| `⟦b4-NCPCANCELROLLBACK⟧` | 重算超時,客人**已入帳的收款跟著被回滾** |
| `⟦0a-CARDCANCELNOREFUND⟧` | 刷卡單取消**零自動退款、零提醒** ⇒ 錢卡在公司而客人不知情 |

### 📦 出貨(3)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦ship-HCTLABEL⟧` | 要貼在箱子上那張紙,**今天沒有人印得出來** |
| `⟦ship-HCTUNKNOWNREAD⟧` | 卡在「送出結果未知」的箱子,按鈕會對新竹**造出重複託運單** |
| `⟦ship-HCTACTIVATION⟧` | 新竹上線缺具名 activation,正式站 endpoint 未確認 |

### ✉️ 客人收到的東西(7)
| 錨 | 客人會遇到什麼 |
|---|---|
| `⟦b4-BANKNOEMAIL⟧` | 選匯款的客人結帳後**一封信都收不到** |
| `⟦b4-MAILRENDER1⟧` | 付款成功信**從沒有人在真信箱看過**(⚠️ 是「沒驗過」不是「已知會錯」) |
| `⟦5b-SHIPPEDNUMNOTRECORDED1⟧` | 沒存實寄追蹤號 ⇒ 客人可能**永久拿著錯號碼**,而沒東西會叫 |
| `⟦b4-EMAILTRIAGE⟧` | 換過 email 的人可能**收到別人訂單的交易資訊** |
| `⟦b4-RECIPLIVEVALUE⟧` | 客人改了 email,信還是寄到**舊信箱**(裡面有交易資料) |
| `⟦b4-CANCELMAILMIXEDRAIL⟧` | 卡+現金混合退款的取消單**系統不寄**,而沒有提醒叫人工寄 |
| `⟦front-PDPTAXONOMYEMPTY⟧` | 商品頁「適用車款」整區空掉,**頁面回 200、畫面不說** |

🔴 **另外那幾十件**⛔ ~~在那份 by-owner 檔裡~~ ⇒ **當場印:`python3 scripts/launch-dispatch-table.py`**(那份檔已停止維護, 見上一節)。大宗是:**稅**(六個客人看得到的金額面缺稅列)、
**權限**(管理員閘繫於看不到值的開關 · 系統帳號可刪客戶表 · 災難還原零稽核)、
**監測**(車款同步失敗零告警 · 部署順序閘誤判放行)。

## 3 · Sean 側欠的動作

### 🔴🔴 **先答這五個就好 —— 每一個都當場解鎖一條線**

> 🔴🔴 **前三個字解凍的不是三條線, 是【四分之三的待收工作量】。**
> 2026-09-07 08:3x 當場量:各線做完而還沒進主線的共 **73 顆**, 其中
> **56 顆(76%)在 `auth` 25 + `mail` 17 + `front` 14 這三條** —— **而那三條全部凍在「71/78/82 算不算」後面。**
> ⚠️ **這個數會動**(七個窗還在寫;20 分鐘前是 70 顆 / 80%)⇒ 要現值就跑:
> ```bash
> cd ~/pcm-wt-tidy && git fetch origin >/dev/null 2>&1
> git for-each-ref --format='%(refname:short)' refs/heads | grep 'agent/line-' | while IFS= read -r b; do
>   echo "$(git rev-list --count origin/dev..refs/heads/$b) $b"
> done | sort -rn
> ```
>
> 十五題全部在下面。**而這五題是【有人正停在那裡等】的那幾題。**
> 每題回**一個字**。不確定就回粗體那個。

| 回什麼 | 題 | 答了會怎樣 |
|---|---|---|
| **算** / 不算 | 貼板 **82**(搜尋料號索引) | 🔓 解開 **front 搜尋線** —— 佇列逐字「**82 最急 —— 它擋住 front 搜尋改動那條線**」 |
| **算** / 不算 | 貼板 **71**(建單寫未稅) | 🔓 解開 **auth 線**;另有一道守門的升級**排在它後面**(艦隊表逐字「升擋等 auth 解凍」「升擋順序 71」) |
| **算** / 不算 | 貼板 **78**(寄信批次計數) | 🔓 解開 **mail 線** |
| **甲** / 乙 | `Q39` 折價券部分取消怎麼算 | 🔓 解開 `PARTCANCEL1` 那支接線 migration(佇列逐字「**卡著 PARTCANCEL1 接線 migration**」) |
| **甲** / 乙 | `Q37` 留痕寫不進去時刪除要不要跟著失敗 | 🔓 解開貼板 **72**(刪單留痕) |

⚠️ **那三個貼板題本來是【同一題】** —— 佇列裡逐字是「`71/73/74/75/76/78/79/80/82` 算不算預授權」。
拆開讓你先答最急的三支;**其餘 6 支(73/74/75/76/79/80)仍在下面那一題等**, 一起回「算」也可以。

---

### 🔴 早上第一件:**十五題**只差你一個字(整夜都停在這裡)

> ⛔ ~~三題~~ ⛔ ~~九題~~ ⇒ **十五題**(2026-09-07 08:4x 重數 · 逐題 grep 佇列驗過存在)。
> 你**最後一次回答是 01:33**, 之後各窗又端了 **12 題** —— 📌 **它們不會催你, 而每一題後面都卡著一片工。**
> 🔴 **每一題都可以用一個字回完。**不確定就回推薦那個(粗體)。

| 題 | 白話 | 選 |
|---|---|---|
| `Q37` | 刪單要留紀錄, 而**紀錄寫不進去**時, 刪除要不要跟著失敗? | **甲**=一起失敗 / 乙=略過照刪 |
| **貼板** | 昨晚「預授權到天亮」, **清單外**又冒出 9 支(71/73/74/75/76/78/79/80/82)算不算? | **算** / 不算 ⚠️ **82 最急**(擋住搜尋那條線) |
| `QB-4` | 要不要讓唯讀帳號看得到兩張內部表(每天巡檢就不用打擾你)? | **甲**=開 / 乙=不開 |
| `QB-5` | 登入系統抖一下時, 經銷商那一刻拿不到身分 ⇒ 擋他還是放行走一般價? | **甲**=擋 / 乙=放行 ⚠️ **乙 = 畫面 1,100 而實扣 1,155, 客人被靜默多收** |
| `Q38` | 各窗的工**整晚只活在這台機器上**(分支從不 push)⇒ 要不要每晚備份到 origin? | **甲**=備份 / 乙=維持現狀 |
| `QB-6` | 客人改了信箱, 之前排好還沒寄的信怎麼辦? | 甲=自動重排 / **乙**=不寄, 後台列清單給客服 |
| `QB-7` | 客人改 Email 時, **舊信箱**要不要也收一封「你的 Email 被改成 X」? | **甲**=要(帳號被盜才有人發現, 零成本)/ 乙=只新信箱收 |
| `QB-8` | 用 LINE 登入、沒有 Email 的客人, 要不要讓他設定 Email? | 甲=要 / **乙**=先不做 ⚠️ **乙 = LINE 客人收不到任何通知信** |
| `Q39` | 用了「滿 5000 折 500」的券, 後來取消一件變 4000 ⇒ 折扣還算他嗎? | **甲**=照算(退 1,000)/ 乙=券不成立(退 500) |
| `Q40` | 商品「沒照片就放佔位圖」的判斷散在 8 個畫面, 今天都對, 下一個人忘了不會紅 | 甲=現在改 / **乙**=上線後再改 / 丙=只做前三片 |
| `Q41` | 驗 migration 的測試庫從零蓋不起來(370 支裡 68 支失敗)⇒ 改成抄正式庫的結構? | **甲**=抄結構(唯讀連線多一種用法)/ 乙=手抄缺件清單(會製造假綠) |
| `QB-10` | 寄信死信的鈴**一響就不會停**, 人會學會忽略它 | **甲**=改成巡檢信一行數字 / 丙=只報 24 小時新死的 / 現狀=留著 |
| `QB-11` | 後台取消一張**刷卡已付**的單, 錢留在 TapPay, **沒有任何提醒** | 甲=自動退刷 / **乙**=不退但畫面+巡檢信提醒 / 丙=什麼都不做 |
| **取消後入款** | 逾期被取消的匯款單, 客人的錢**之後才到**, 怎麼辦? | **甲**=開「待退款」/ 乙=標「多付待人工」/ 丙=自動恢復訂單 |
| **錢路 5 支** | 重算撞逾時會把客人收款一起回滾;全 repo 18 支沒接, **5 支在錢路** | 做 / **上線後**(沒量過觸發頻率) |

> 📌 逐字題目在 `~/pcm-mailbox/端Sean-0905早上佇列.md`(正本);索引在 `docs/decisions/2026-09-06-night-rulings.md`。**粗體是我們的推薦。**
> 🔴 **`Q40` 上面有七行互相作廢的訂正**(A 窗與 B 窗互讓了一輪)—— **不用理它們**, 上表那一行就是定案, 答一個字即可。

**另有一件等你的**:🔴 `Q-權限偷改偵測`(甲/乙, 在佇列 §B)——
板列 `⟦b9-ACLDRIFT5⟧` **卡在這一題**(有人在 dashboard 或 SQL Editor 手改權限, 要不要偵測)。
⚠️ 它**不在上面那十五題裡**, 因為它是更早端的、而**沒有被收進今晚那批**。⇒ **實際上是十六個字要回。**

**還有 6 件【在等一個外部條件】—— 不用你做, 而你要知道它們在等什麼**:
· `⟦b9-ACLDRIFT5⟧` **等你答 `Q-權限偷改偵測`**(見上一段)
· `⟦b4-ANOMCLAIM1⟧` 等時機:同時有兩個人在處理雙扣異常佇列(**今天實務上不會發生**)
· 🔴 `⟦db-DEADMANSIGNAL4⟧` **等第一張真的付款成功的訂單出現** —— **在那之前量不到, 所以不做**
  🛑 **而那天不會有人被提醒**(2026-09-07 08:2x tidy 補):觸發條件住在**正式庫**(第一張 `payment_status='paid'` 的訂單), 而板子、白話版、告警都不會自己去看它。
  ⇒ **誰先看到第一張真的付款成功的訂單, 誰喊一聲** —— 多半是你。
  ⇒ 它要驗的東西:**訂單付了款而通知信【根本沒被建出來】**。而所有既有的計數器都只數「已存在的信」⇒ 對「信沒被建」永遠印 0, **而那個 0 看起來像正常**。
· 其餘 3 件是**開列的人沒有接手**(誰欄寫的是量測者的代號, 不是負責人)⇒ 已交主視窗分派
📌 **⇒ 「在等」與「沒人管」在板子上長得一樣, 而只有前者不該被派工。**

### 📋 另外 **7 件不是決策題, 是待辦**(不用選, 而有幾件只有你做得到)

**只有你做得到的(2 件)**
1. 🔴 **打開 Vercel 面板看一眼**:`pcm-storefront` 專案 → Settings → Environments,**有沒有 Custom Environments**?
   回「有」或「沒有」就好。(`⟦auth-VERCELTARGETENV⟧` 兩格未確認卡在這)
2. 📄 **把 `~/pcm-mailbox/樣張-新竹標籤-20260907.pdf` 轉給新竹**, 問「這樣印可以貼嗎」。
   —— 那是 `⟦ship-HCTLABEL⟧` **唯一剩下的關閉條件**。

**要你點個頭的(2 件)**
3. 🗑️ **刪掉 `~/pcm-wt-ship-5b`** 那棵空的工作目錄(ship 量到 0 顆未推、0 行改動)⇒ 回「刪 ship-5b」我就刪。
4. 🔀 **`dev` 合進 `main` 一次** —— `main` 落後 `dev` **282+ 顆**;⚠️ **合了顧客站正式站才會有**今晚的搜尋 / 匯款區 / 取消件數。

**要你決定要不要加的(1 件)**
5. 📊 車款資料同步(`FITSYNC1`)停掉時要有人知道。告警信那半 auth 在做;
   **LINE 每日摘要那半住在【老闆腦】那個 repo**(`daily_digest.py`)⇒ 要不要在那邊加一行「fitment sync 幾天沒動」?

**你只要知道, 不用動的(2 件)**
6. ⏳ **在等外部條件的那幾件** —— 詳下一段。📌 **「在等」與「沒人管」在板子上長得一樣, 而只有前者不該被派工。**
   ⚠️ **件數對不上, 照實寫**:下一段自己列 **6 件**, 而主視窗報 **7 件** ⇒ 差 1。
   🛑 **我沒有尺可以數它** —— 「在等外部條件」要開檔判斷、沒有 token 可掃 ⇒ **兩個數我都沒驗**。
   ⇒ 這一格標**未確認**;要現值只能逐列開檔。
7. 🔴 **第一張真的付款成功的訂單出現時, 要有人喊一聲**(`⟦db-DEADMANSIGNAL4⟧` 在等它)。
   🛑 **而沒有任何東西會在那天提醒任何人** —— 觸發條件住在正式庫, 板子與告警都不會去看它。
   ⇒ **多半是你先看到。看到就喊。**

### ⚠️ 兩件會自己出事的(不做也不會有人提醒你)
1. ⛔ ~~那封「每日告警」明天還會叫 —— 它叫的是一件後台做不到的事, 已經叫了 15 天~~
   ⇒ ✅ **那句是錯的, 2026-09-07 08:2x 撤回(`-db` 量、`-ship` 收、主視窗訂正)**:
   **它明天【不會】叫** —— `-db` 01:0x 照 `get_payment_anomaly_alert_summary` 的述詞對正式庫量
   `attempt_manual_review_count` = **0**(正對照:現造一筆 ⇒ 回 1 ⇒ 尺是活的), 那兩筆舊單**不復現**;
   板列 `⟦db-DAILYALERTNOHAND⟧` 已由 `-ship` `cb5c3fe52` 收 **done**。
   🔴 **而我第二句錯得更重**:我寫「按鈕根本沒做」像在報一個缺陷 ——
   **而那是你自己的裁決**(該列逐字:「**不為它做按鈕** —— 這 2 筆是歷史殘留不是持續場景;
   **結掉之後看一個月, 再有新的就做按鈕**」)。
   📌 **⇒ 我把一個【你已經決定的事】寫成了【等你處理的事】。**
   ⚠️ **剩下的真殘餘只有一句**:`cancel-view.ts:131` 逐字仍寫「`#808` 面一(後台沒有手可以收尾這些單)**未修**」
   ⇒ **後台確實還是沒有那隻手**;而**照你的裁決, 那要等「一個月內再出現同款」才做**。
2. 🔴 **正式庫刪訂單【零留痕】** —— 你 09-04 請人刪測試單那次就是零紀錄。
   你已拍甲(上線前補), **而它要貼板才會生效。**

> ✅ **已完成(不用再做)**:Vercel 防火牆規則 `search-log-flood-cap` **已由 Sean 2026-09-06 19:1x 親手加入**
> (他逐字「規則名稱 search-log-flood-cap 加好ㄌㄜˊ」);主視窗在主樹跑 `vercel-firewall-cron-order-check.py` live 檢查過:
> 6 條規則,`search-log-flood-cap` #6、`bypass-machine-traffic` #5 ⇒ **bypass 之上沒有規則擋排程** ✅。
> ⚠️ 板列錨 `⟦auth-SEARCHWAFRATE⟧` 現在 grep 會回 **0** —— 它活在 auth 未推的 `10a53569d`,**39d 收割後才查得到**。
1. **貼板** —— 幾支 migration 等你說「貼 N」才進正式庫(一次一支)。
2. **合 main** —— `dev` 的東西要你手動合進 `main` 才上正式站。
3. **`Q-新竹傳輸方式`** —— 要新竹回應格式,`⟦ship-HCTUNKNOWNREAD⟧` 卡在這。
4. **`Q-出貨PDF`** —— `⟦f3-SHIPPDF1⟧` 在等。

## 4 · 🔴 這張板子沒涵蓋什麼(不要把它讀得比它大)

1. 🔴🔴 **那 410 列【不是我一列一列讀的】** —— 事實抽取與初判由 **7 個 subagent** 做,我逐條複核它們的報告(翻案 3 條、另 2 條加註記),**而我讀的是 7 份報告,不是 410 列原文**。⇒ 正確的說法是:**每一列都被讀過,而讀的人不全是我。**
2. 🔴🔴 **有一發 SQL【不要跑】** —— `docs/launch-todo.md` 那列「未確認 停產品會不會自動從店裡下架」(`done`,錨欄 `—`)提議「數上游已停產而站上仍 `delisted_at IS NULL` 的筆數」。**那一列自己逐字預言**:「若真的跑,會回一個**很大而且正確**的數字 ⇒ **一個看起來像事故的正常狀態**;而跑它的人會把那個數字端上來,而它每一位都是對的。」
   ✅ **因為那不是缺口,是規格** —— Sean 原話寫在 `supabase/migrations/20260815030000_…:74` 的 `COMMENT ON COLUMN`:**「如果原廠停產, 但是我有現貨庫存, 那我需要維持上架狀態」**;同段 `:75`「本欄**不得**被接到任何自動下架邏輯上」。
3. **`done` 那些列不判**(⛔ ~~259 列~~ —— **那個值已經漂過一次;講關係不講值**) —— 它問的是「**做完了沒**」,不是「擋不擋」,那是另一個問題。⚠️ 而查它的工具(`board-state-consistency.py` 規則④)檔頭逐字寫著**刻意不做**這一族 ⇒ **已知盲區,不是本輪漏的。**
4. **標「未判」的那些列**(⛔ ~~36 列~~ ⇒ **現值跑 `launch-blocking-count.sh`**) = 誠實的答不出來,不是漏掉。其中兩列的理由是**政策上不准做那道檢查**(要對 GitHub 實測 force-push;要正式庫金鑰)。
5. **2026-09-01 那份「37 件」本輪沒逐列複驗** —— 那張表是**副本**,板子自己記著「37 列裡有 20 列的錨欄不是真錨」+「一律以主表為準」⇒ 拿副本對照會數錯,要做必須從主表走。
6. **知識地圖曾落後 4959 顆 / 15 天** ⇒ 本輪已刷新(**58,619 節點 / 82,239 邊 / 3,278 社群**,09-06 19:00)。⚠️ 它的分母是**跑它的那棵樹**,未推的 commit 不在圖上;`graph.html` 因節點超上限被跳過(已知限制,只能用 `query`)。
7. **`docs/phase-1-backlog.md` 狀態欄覆蓋率退步**:89.1%(08-18)⇒ **82.2%**(09-06)。歷史三格一格沒變,掉的**全是 08-18 之後新開的條目** ⇒ 病在「開條目那一刻」,回頭補會再掉。
8. **`docs/plans/`(⛔ ~~88~~ ⇒ **94** 支)與 `docs/specs/`(⛔ ~~250~~ ⇒ **370** 支)兩個平行計畫目錄未整併**
   🔴 **舊那兩個數字是我 09-06 抄進來而沒有人重數過** —— `specs` 差 **120** 支。
   ✅ 2026-09-07 05:4x 當場數, 並盤了一次:`~/pcm-mailbox/盤點-plans與specs-20260907.md`
   ⇒ 合計 **464** 支, 而**只有 15.1% 被板列或 STATUS 指到**。
   ⚠️ 而「沒人指到」**不等於**過期(三種情況那把尺分不出來)⇒ **那張表是入口不是判決。**;根層 `PROGRESS.md`(1736 行)是隱性待辦墳場,本輪不碰。
9. ⛔ ~~194 列的錨欄沒有錨~~ ⇒ ✅ **2026-09-07 12:2x 當場重數(全板 801 列):那不是一個數, 是【兩種】** ——
   **只帶 `#編號` 26 列**(有識別字而舊守門的樣式不認)· **兩者皆無 159 列**(連編號都沒有)。
   帶 `⟦錨⟧` 的有 ⛔ ~~616~~ **624** 列, 無錨 ⛔ ~~159+26~~ **183** 列(2026-09-07 08:1x 重量)。⇒ 板頭「引用一列給錨」那條規矩對後面那 159 列**結構上做不到**。本輪仍不補錨。
10. 🔴🔴 **今晚有列被證明【板上比實況嚴重】—— 未派工前先跑 `what-happened-to`,那是規矩不是建議**
   · ✅ **我親自驗過的兩列**:`#868`(已取消單還會寄付款成功信)⇒ **寄信那一半早就接上了**(`sweep-email-outbox.ts:1325` 真的呼叫;`:1310` 是註解)⇒ 已轉 `done`。`⟦b4-RESTORE2⟧`(災難還原蓋回客戶表 + 零稽核)⇒ **兩半都不對**:根本不是「蓋回」(`ON CONFLICT DO NOTHING`,**開列當天就已經是**),稽核 8 天前補上了。
   · ⚠️ **主視窗另外點名 3 列**(`⟦b4-BANKCHARGESCARD⟧` / `⟦search-TAXONOMYTIMEOUT⟧` / `⟦front-PDPTAXONOMYEMPTY⟧`)—— **2026-09-06 `e3f721955`(39e)已合進來,它們的字面現在在我樹上了。**
   　🔴 **而合完之後它們【仍然是「擋」】** —— `⟦search-TAXONOMYTIMEOUT⟧` 與 `⟦front-PDPTAXONOMYEMPTY⟧` 態仍 `open`、標記仍 `⟨擋 ⑤⟩`;`⟦b4-BANKCHARGESCARD⟧` 態從 `open` 變 `doing`(auth 接手了)、標記仍 `⟨擋 ①⟩`。
   　⇒ 🛑 **所以我沒有把標籤改成「已訂正」** —— 合進來的字面**沒有推翻那三個擋**。上面那兩列(已驗過的)才是真的比實況嚴重。
   📌 **形狀:一個宣稱可以【當初就描述錯】,也可以【後來被修掉】—— 而在板上它們長得一樣,都只是一句沒有人回來劃掉的話。**
11. 🔴 **本輪我自己撤回了三個結論** —— 寫在這裡,因為它們**都通過了當下的檢查**:
   ① 我說某列是 `done` 裡唯一的真殘餘 ⇒ **只讀了那列前 620 字元**,訂正住在尾巴(而那一列預言了「跑那發 SQL 會回一個很大而且正確、看起來像事故的數字」)
   ② 我叫助手「不要猜行號偏移量」⇒ **我自己一量,偏移量是常數**(13/13 命中 vs 對照組 5/13)⇒ **保守的訂正也會製造錯誤,而那種錯不會有人抗議**
   ③ 我說某道守門「沒人存檔的日子一聲不出」⇒ **每次 push 都會叫,而且現在就在叫** —— 我沒看到是因為**我從來不 push**
12. 🆕 **那 39 件「未判」現在【每一件都寫了怎麼查】** —— 三格:**誰能跑 / 跑什麼 / 兩個世界長什麼樣**。
   ⇒ 規矩逐字:標「未確認」而附不出缺哪一道檢查 = **免責貼紙**。
   🔵 其中 **2 件的「誰能跑」是【只有你或主視窗】**(那是決定不是量測,派人過去沒用);
   **1 件寫「不是我們這邊」**(它問的是報價單那個 repo)。
13. 🆕 **派工清單裡的「按面分堆」是【入口】不是【歸屬答案】** —— 53 件無主的裡,
   **42 件(79%)同時命中一個以上的面**;而換一下我清單的順序,**顧客站從 10 件變成 0 件**。
   🛑 **不要用那四個數字回答「顧客站還剩幾件擋」** —— 它答不了那個問題。
14. ⛔ ~~板上的查重守門對 194 列(25.5%)失明~~ ⇒ ✅ **2026-09-07 已經不失明了**:
    我把守門擴成三層(`⟦錨⟧` / `#編號` / **無錨列退而用【事欄前 40 字】當弱身分**),
    第一發就抓到 **5 組真重複**(`#64` `#35` + 三組無錨), **全部合掉了**。
    ⚠️ 而**弱身分那一層是【疑似】不是判定** —— 兩列開頭一樣不代表是同一件事, 要開檔比對。
    🔴 **舊句留著加刪除線** —— 因為它描述的那個洞真的存在過, 而**下面那段講的仍是它的成因**:它只認 `⟦…⟧` 形狀的錨,
   而 `#編號` 與 `—` 那些不在分母裡(`⟦tidy-ANCHORUNIQBLIND⟧`)。
   ⇒ 🔴 **重複的列會讓這份報告的「還在擋」多算** —— 今天就多算了 1。
15. **本檔沒有估時。** 估時要動手的人給。
