# 要 Sean 回答的(**答案填在這裡,不要留在對話裡**)

- **建立**:2026-08-17 E 窗　**狀態**:待答
- 🔴 **這份檔的存在理由**:這些問題原本散在對話裡,而**對話會消失**。
  「拍板即落檔」現在是反過來運作的(答完才落檔)⇒ **問題先落檔,答案就有地方去。**
- **怎麼用**:在每題的 `答:` 那一行後面打字就好。**不必回覆背景,我只需要那一行。**

> 🔴 **正本是哪一份(先講,因為這是一份【會被填答】的活檔,三份會漂)**
>
> ```
> 正本 = 進到 dev 之後的這個路徑:docs/security/2026-08-17-questions-for-sean.md
> Sean 若直接在對話裡回答 ⇒ 由主視窗或 I 窗【抄進本檔】並標「來源:對話,YYYY-MM-DD」
> 🔴 對話裡的答案【不算數】，抄進來才算 —— 對話會消失，這份檔不會
> ⚠️ 本檔目前在 E 窗工作樹（分支 customers）尚未收割 ⇒ 收割前它【不在 dev 上】
> ```

---

# 🟢 A 區 · 一句話就能答(不用動手查)

### A1. 報價單系統現在有在對外用嗎?車行真的在開那些連結嗎?

- **不答會怎樣**:我們有一批報價單的發現(連結外洩相關),**現在無法判定它是「今天的問題」還是「以後的問題」**,兩者的處理急迫度差很多。
- **答:報價單有在用，有傳給車行看報價

### A2. 報價單那個後台,**兩階段驗證(2FA)現在是開著的嗎?**

- **不答會怎樣**:2FA 開著的話,一條舊的登入路徑會被自動擋掉;沒開的話,**「強制所有人重新登入」對那條路徑是無效的**(舊 cookie 撤不掉)。
- **答:目前有登入，但是只是用密碼而已 2FA應該沒有

### A3. 報價單給客人的連結,**你們有在設到期日嗎?** 還是通常就一直有效?

- **不答會怎樣**:連結一旦流出去(轉貼、截圖),**沒設到期就是永久有效**,沒有自然失效點。
- **答:有，但是我忘記是30天還是60天之類，其實你們可以去讀報價單專案資料

### A4. 顧客站(shop.pcmmotorsports.com)**目前確實還沒對外開放、沒有真實客人下單**,對嗎?

- **不答會怎樣**:這句話是我們整份「上線前必關清單」的**嚴重度基準**;它若已經不成立,**那份清單上每一條的急迫度同時上升**。
- **答:**對，沒有真實客人下單，都是我自己測試

### A5. 報價單的**正式網址**是什麼?後台(pcm-admin)的**正式網址**又是什麼?

- **不答會怎樣**:全站滲透測試有兩個面**現在沒有入口**,做不了。
- **答(兩個網址):** <https://quote.pcmmotorsports.com/>  報價單後台, <https://admin.pcmmotorsports.com/api/sso/start> 訂單管理

---

# 🟠 B 區 · 要動手查(每題都附「去哪裡、貼什麼」)

## B-1 · Supabase 後台(點兩下就好)

### B1. 資料庫設定裡的 **max rows** 現在是多少?

- **去哪**:Supabase → 你的專案 → Settings → API → **Max rows**
- **不答會怎樣**:我們有一條「一次撈太多筆會被靜默截斷」的風險,**沒有這個數字就算不出它今天會不會發生**。
- **答:** 1000rows

### B2. 這兩句貼進 Supabase 的 SQL Editor,把結果貼回來

```sql
SELECT has_table_privilege('anon','public.brands','MAINTAIN')     AS brands_maintain,
       has_table_privilege('anon','public.categories','MAINTAIN') AS categories_maintain;
```

- **不答會怎樣**:有一個**該收而可能沒收乾淨的權限**(`#628`)。兩個都是 `false` ⇒ 沒事,那條就關掉;有 `true` ⇒ 補一行 SQL 就好。
- **答:**| brands_maintain | categories_maintain |
| --------------- | ------------------- |
| true            | true                |

### B3. 這句貼進 SQL Editor(**網站庫**)

```sql
SELECT count(*) AS orders_without_notify_email FROM orders WHERE notification_email IS NULL;
```

- **不答會怎樣**:出貨通知信要不要做「備援寄信規則」,**完全取決於這個數字是 3 還是 3000**。數字小 ⇒ 人工處理就好,不用寫程式。
- **答:**| orders_without_notify_email |
| --------------------------- |
| 13                          |

### B4. 這句貼進 SQL Editor(**報價單庫**)

```sql
SELECT count(*) AS total,
       count(*) FILTER (WHERE expires_at IS NULL)                     AS never_expire,
       count(*) FILTER (WHERE created_at > now() - interval '30 days') AS last_30_days,
       count(*) FILTER (WHERE dealer_opened_at IS NOT NULL)            AS dealer_opened
  FROM quote_snapshots;
```

- **不答會怎樣**:這一句同時回答 **A1(有沒有在用)** 與 **A3(有沒有到期日)**,而且是直接證據。**A1/A3 你若答得出來,這題可以跳過。**
- **答:**| total | never_expire | last_30_days | dealer_opened |
| ----- | ------------ | ------------ | ------------- |
| 307   | 0            | 126          | 0             |

## B-2 · Vercel 後台

### B5. **兩個專案**(顧客站 / 報價單)的環境變數裡,`CRON_SECRET` **是不是同一個值?**

- **去哪**:Vercel → 各專案 → Settings → Environment Variables → 找 `CRON_SECRET`
- 🔴 **只要答「一樣」或「不一樣」,不要把值貼出來。**
- **不答會怎樣**:如果是同一個值,**任一邊外洩,另一邊的排程端點會一起被打開**,而兩邊的程式碼都看不出這件事。
- **答:** 看不到誒..不知道怎辦。

### B6. 這四個開關現在的值(你之前口頭回報過三個,我要**你看著面板**確認一次)

```
TAPPAY_3DS_ENABLED true
CRON_SWEEPER_ENABLED true
ANOMALY_ALERT_ENABLED true
CHECKOUT_NOTIFICATION_EMAIL_ENABLED  沒有這個  ← 這個之前沒問過
```

- **不答會怎樣**:我們目前是拿**轉述**在判斷,而其中一個(最後那個)會決定「結帳時客人**必須**填通知信箱」這件事現在到底有沒有生效。
- **答(四個都要):**

### B7-b. 這句貼進 SQL Editor(**網站庫**)—— 補一題,我原本漏了

```sql
SELECT relname, relrowsecurity,
       has_table_privilege('anon', c.oid, 'INSERT')   AS anon_insert,
       has_table_privilege('anon', c.oid, 'TRUNCATE') AS anon_truncate
  FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace
 WHERE n.nspname = 'net' AND c.relkind = 'r';
```

- **不答會怎樣**:這兩張表是**排程與告警留下軌跡的地方**。如果外部身分能清空它們,
  **出事那天就查不出發生過什麼**。這一題我先前列了風險卻**沒有把問題寫進本檔**,是我的漏。
- **答:**| relname            | relrowsecurity | anon_insert | anon_truncate |
| ------------------ | -------------- | ----------- | ------------- |
| http_request_queue | false          | true        | true          |
| _http_response     | false          | true        | true          |

### B7. LINE 的 **channel access token** 放在哪個專案的環境變數?**有沒有換過的流程?**

- **不答會怎樣**:那個 token 外洩的話,**別人可以用我們官方帳號的名義發訊息給所有加過好友的客人**,而客人分不出真假。我們需要知道萬一要換,換得掉嗎。
- **答:**放在報價單

## B-3 · 要動手做一次(只有你做得到)

### B8. 🔴 **一發測試就能定案的**:拿一個**已經加過我們官方帳號好友**的 LINE 帳號

用它登入我們的網站,然後試著用官方帳號推一則訊息給它。

- **不答會怎樣**:**「用 LINE 通知客人」這整條路成立不成立,卡在這一發。**
  成功 ⇒ 不用改資料庫、不用加欄位,直接可以做;失敗 ⇒ **整條路要重做身分綁定**,成本完全不同。
- **怎麼看結果**:推得出去 = 成立;回 `Invalid to`(或類似的 400 錯誤)= 不成立。
- **答:** 用Line登入網站後，我們官方帳號是看不到這個人，除非客人主動點擊網站上的line連結加入好友後，再傳訊息。
登入網站跟加入line 好友是不同東西，而官方line 帳號是無法加好友主動聯繫客人，除非客人有先發訊息給官方帳號過，不然永遠不知道客人line id

---

# 🔴 C · 一個給你參考的數字

我們有一份「風險觸發清單」共 14 條,其中 **11 條沒辦法用程式自動檢查**。
🔴 **我先前跟主視窗說「6 條」,那個數字不精確,逐條對完之後是這樣**:

| 清單條目 | 這裡哪一題會答到它 | 程度 |
| --- | --- | --- |
| ⑧ 三個 cron 旗標 | **B6** | ✅ 完整 |
| ⑫ LINE token 爆炸半徑 | **B7** | ✅ 完整 |
| ⑬ 報價單 token 永久有效 | **B4** | ✅ 完整 |
| ⑭ 跨專案 `CRON_SECRET` | **B5** | ✅ 完整 |
| ④ `net` 兩表對外可寫 | **B7-b**(本次補) | ✅ 完整 |
| ① `TAPPAY_3DS` 前置未達 | B6 只答「開關開著沒」 | ⚠️ 部分(前置本身要改 WAF 設定) |
| ⑩ LINE cohort 零送達 | B8 只答「這條路可不可行」 | ⚠️ 部分 |
| ⑫ 報價單 token 無法區分 | A1 只答「急不急」 | ⚠️ 部分 |
| ② `#622` 灌 inbox | 綁在 ① 上 | ❌ 沒答到 |
| ⑤ sweeper 靜默死亡 | —— | ❌ 要等機制落地 |
| ⑥ `settled` 未行使 | —— | ❌ 要真的有一筆錢走完 |

⇒ **完整答到 5 條、部分 3 條、沒答到 3 條。**(先前說的「6」是我沒逐條對就講的數字。)
⇒ **你答完 B 區,我們的自動檢查覆蓋率不會變,但【我們知道自己站在哪裡】的程度會差很多。**

⚠️ **最後那 3 條是真的沒辦法**(要真的有錢流過、要等機制落地、要對外送測試流量)——
**我不會假裝有辦法**,它們會留在清單上標「做不到」。

---

## 給填答者的備註

- 🔴 **不確定就寫「不確定」** —— 比猜一個答案有用得多,我們會據此標「未確認」而不是當成事實。
- 🔴 **不要貼任何 token / 密碼 / 金鑰的值**。B5 只要「一樣/不一樣」,B7 只要「放哪、有沒有流程」。
- 答完後這份檔就是那些答案的**唯一出處**,後續所有文件都會引用這裡。
