# 資安稽核 · **誠實缺口清單**（E 窗，2026-08-16）

> **這份存在的理由**：全窗即將重開。**只活在對話或信裡的缺口，下一個 session 接不起來。**
> 🔴 **每一條都附「要發生什麼事它才會消失」** —— 沒有出口的缺口最後會被某個人一次清掉。

---

## 0. 怎麼讀這份

**這裡列的不是「發現的問題」，是「我宣稱過但沒驗到底的地方」。**
發現的問題在各自的報告裡；**這份是【我的證據強度不足】的清單。**

| 欄 | 意思 |
|---|---|
| **卡在什麼** | 🔴 **寫【檔案／動作】，不寫【哪個窗】** —— 窗會重開，檔案歸屬不會自己解決 |
| **出口** | 要發生什麼事，這條才會從清單消失 |

---

## 1. 🔴 最大的一塊：**報價單庫（`pcm-quote-v2`）完全沒稽核過**

| | |
|---|---|
| **現況** | **一次都沒查過。** 今晚所有結論都是網站庫的 |
| **卡在什麼** | **Sean 貼建帳號 SQL 這個動作**（不是任何一個窗） |
| **東西備好了嗎** | ✅ 全部備好：`E-674b`（本體）＋ **`E-674c`（`pgbouncer` 42501 的正解，必讀）**＋ **`docs/security/2026-08-16-quote-db-audit-account-decision.md` §3 的斷言取代 `E-674c` §3 的 ②** |
| **開跑清單** | 🔴 **`docs/security/2026-08-16-quote-db-audit-startup-checklist.md`** —— **帳號一到，照它直接跑，不用重新想** |
| **出口** | 帳號建起來 + 開跑清單第一批查詢跑完 |

---

## 2. 已知的證據強度不足（**逐條附出口**）

| # | 我宣稱過什麼 | 實際證據強度 | 出口 |
|---|---|---|---|
| **2.1** | `dev-preview/*/fixtures.ts` **會進 JS bundle** | 🔴 **讀作者的話**，我沒 build 過翻 bundle | build 一次、翻 bundle 確認。⚠️ **內容我掃過（零 PII／零經銷價／零憑證）⇒ 就算進了也不構成曝露** ⇒ 重要性低 |
| **2.2** | `payment/composition.ts` 持的 DB 憑證**進不了瀏覽器** | ⚠️ **三層靜態證據**（`server-only` build 期強制、消費端零 `use client`、變數名零命中）**但沒翻 bundle** | 同 2.1，**一次 build 可同時收掉兩條** |
| **2.3** | 報價單庫**外部不可達** | ⚠️ **讀 Sean 的 Dashboard 截圖**，不是機器輸出 | 實打 `/rest/v1/<表>` 一發 + **一個已知 200 與一個已知 404 的對照**（沒有對照，單一個 404 不算證據） |
| **2.4** | `payment_confirmer` 那 20 支 RPC **零 PII** | ✅ 已逐支審完（型別分流 + 6 支 jsonb 逐欄讀） | **已關閉** |
| **2.5** | 報價單庫 `net.*` 的**列數** | 🔴 **沒量**（Sean 沒貼） | 三個 `count(*)`。**決定 `CRON_SECRET` 那條鏈是【理論的】還是【活的】** ⇒ 只影響憑證檔頭的語氣，不影響任何決定 |
| **2.6** | `cron.job` 裡那 **2 個疑似含祕密的排程是哪兩個** | 🔴 **只有計數，沒有名字** | 跑 `docs/security/2026-08-16-secrets-in-cron-job-definitions.md` §4.1 那句（**只回 `jobname`，不回 `command`**） |
| **2.7** | `security-audit` skill 的 **Phase 4/5/6** | 🔴 **從未跑過**（Phase 2 單線跑完、Phase 3 由 V 窗以 codex 跑完） | 跑它們，或明確決定不跑 |
| **2.8** | 退款／取消的**狀態轉移不變量** | ⚠️ 只驗了「有沒有稽核列」「外部呼不呼得到」「跨次額度」；**轉移邏輯本體只讀了部分** | 逐條讀 `admin_initiate/finalize_order_refund` 的轉移表 |

---

## 3. 🔴 已經**撤回**的結論（**下一個人最容易誤引的東西**）

| 撤回的 | 正確版 | 在哪 |
|---|---|---|
| 「拿掉冪等樹 ⇒ 一張 100 元的單記三次 / **300 元**」 | **沒有重複入帳。** 擋住它的是**條件式 `UPDATE` + 鎖 + `UNIQUE`**，不是冪等樹。冪等樹改的是**回給呼叫端的值**（無樹 ⇒ 已成交的單被回報成失敗） | `concurrency-harnesses.md` §2、`phase2-hunt.md` §6.10-e |
| 「`payment_confirmer` 憑證外流，**一列客戶資料都讀不到**」 | **撈不到客戶 PII 成立；「一列都讀不到」不成立** —— 交易 metadata 經 SECDEF RPC 可達 | `phase2-hunt.md` §5.1 |
| 「退款不變量已驗證」 | **那驗在一張還沒有 writer 的表上**（`payment_refunds`）。活的是 `order_refunds`，已重驗 | `phase2-hunt.md` §6.5(c)、§6.7 |
| 「`8173` / `223` 筆 / `2.66%`」 | **作廢** —— 那句 SQL 只查了 view `UNION ALL` 的上半。**只有 `8396` 可引用** | `external-exposure-audit.md` §7c-6 |
| 「`sharp` web runtime 碰不到」 | **碰得到**（`/_next/image`）。**但可利用性仍低**（本地圖可信、無遠端來源） | `phase2-hunt.md` P2-2 |
| 「webhook 是**唯一**沒節流的入口」 | **兩條**（webhook + 3DS callback） | `phase2-hunt.md` §6.10-b |

🔴 **這張表本身就是接手時最該先讀的東西** ——
**因為每一條的舊版本都曾經是「有出處、有數字、看起來驗證過」的。**

---

## 4. 已完成、**不需要重做**的（免得下一個人重跑）

```
✅ DB 層外部曝險稽核（RLS / SECDEF / 欄級 / view）—— 對【網站庫】production 實測
✅ 經銷價防護鏈 —— 三層各自獨立驗過（DB 欄級、app 逐欄白名單、eslint）
✅ Phase 2 應用層獵洞（單線編制）：XSS / server action 授權 / open redirect / 依賴鏈
✅ 業務邏輯與併發：退款不變量、#497 兩套 SUM、出貨 CAS、冪等順序
✅ V-017 十條 finding 全折
✅ storefront 對外曝露面盤點（三輪）
✅ 報價單庫稽核帳號【定案】（等 Sean 貼）
```

---

## 5. 接手的人**第一件該做什麼**

```
1. 先讀 §3（已撤回的結論）—— 那是最容易誤引的
2. 看 §1 報價單庫的帳號建起來了沒
   建了 ⇒ 照 startup-checklist 直接跑（那是唯一一整塊沒查過的地方）
   沒建 ⇒ 提醒 Sean，然後做 §2 的任何一條
3. §2 最便宜的一件：build 一次翻 bundle ⇒ 一次收掉 2.1 與 2.2
```
