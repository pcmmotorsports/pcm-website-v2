> 🔴 **本檔是【正本】**(2026-09-05 從 `~/pcm-mailbox/端Sean-0905貼板包.md` 搬進 repo;那份已改成指標)。
> 🛑 **而搬進來的理由不只是「進 repo 才對得到 hash」** —— 原本那五條 `after.sql` 路徑指向
>    **session 的 scratchpad**, 那個目錄會隨 session 消失 ⇒ **早上要用的時候那五條路是死的。**
>    ⇒ 五支一起搬進 `supabase/after-checks/`(逐支重數過:各 33/59 行、**寫入敘述 0**),
>    本檔的 5 處路徑同步改。📌 **一份文件搬家而它引用的東西沒搬, 它會在到達目的地之後才壞。**

# 端 Sean · 2026-09-05 貼板包(線 `-db` 備;順序取自 `端Sean-0905早上佇列.md` §A)

> 🔴 **每一支貼完先叫 `-db` 跑對應的 after.sql, 再貼下一支。** 有依賴的那三支順序不可換。
> 🛑 **標「等收割」的四支【現在還不在 `origin/dev` 上】** —— 我逐支用
>    `git ls-tree -r origin/dev -- supabase/migrations/` 查過, 不是憑印象標。**它們不能今天貼。**
> ⚠️ 預期數字一律寫**算式**不寫結果 —— 寫死的結果會在世界動的時候變成一個看起來對的謊。

---

## 1. `20260904270000_m4b_rls_service_role_select_36.sql` ✅ 在 `origin/dev`
- **做什麼(白話)**:後台現在能看到所有資料, 靠的是一把「萬用鑰匙」。這支**先把 40 張表的正式門開好**, 之後才收得掉那把鑰匙。**今天貼它, 後台行為零改變。**
- **貼前前置**:無(它是佇列第一支)。
- **貼完 Sean 該看到**:`Success. No rows returned` **不會出現** —— 它最後一句是 `SELECT`
  ⇒ 🔵 **會回一張表(三欄:回滾_先跑這段 / 回滾_再跑這段 / 這份東西住在哪)。看到那張表 = 成功。**
  🔴 而它若印紅字, 逐字看第一行:`前置閘⑤:目標名單與…不符` ⇒ **停下叫 `-db`, 不要改數字重貼。**
- **貼完跑 `bash scripts/acl-snapshot.sh`** —— 🔴 **期望 diff = 8 格改變 + 40 條 policy 新列。**
```
① 8 格改變:那 8 張本來沒有表層 GRANT 的表, service_role 那一列
   admin_saved_order_views · coupon_redemptions · coupons · customer_favorites
   legal_terms_versions · order_payments · payment_double_charge_anomalies · payment_refunds
   每一張:  ----|RLS   ⇒   S---|RLS      (只有 S 那一位翻, RLS 那半不變)
   🔬 那 8 個「現在是 ----|RLS」是從【基線檔本身】逐張查出來的, 不是從 migration 推的。

② 40 條【新增】的 POL 列, 每一條長這樣:
   POL	public.<表>|<表>_select_service_role	service_role	r|PERM
   🔬 基線現在 POL 共 52 條 ⇒ 貼完該是 92 條(52 + 40)

⇒ 總計:8 格改變 + 40 列新增。**多一格少一格都停。**
```
  🔵 **快照 2026-09-05 補了第五族 `POL`** —— 在那之前它**只記表層 GRANT 與 RLS 開關,不記 policy**
  　 ⇒ 那時「diff 只有 8 格」看起來就像「policy 沒建成」。**現在兩半都看得到了。**
  ✅ **而 `POL` 現在也記【內容雜湊】了**(2026-09-05 再補):每一列尾巴多兩段 —
  　 `md5(USING)` 與 `md5(WITH CHECK)`(NULL 印 `-`)⇒ **`USING (true)` 被改成 `(false)` 會顯形。**
  　 🔬 那 40 條新列的 `md5(USING)` **應該全部是 `b326b5062b2f0e69046810717534cb09`**
  　 　（= `md5('true')`, 我當場算過;基線裡現有 22 條就是這個值)⇒ **多一個不同的值就停。**
  　 　 `md5(WITH CHECK)` 應該全部是 `-`(SELECT policy 沒有 WITH CHECK)。
  🛑 **而 diff 只會說「那條 policy 的內容變了」, 不會說【變成什麼】** ——
  　 要看變成什麼, 去查 `pg_get_expr` 或那支 migration。
  🔴 **它仍然看不到**:policy 的**順序**(PG 不保證)· 同名 policy 在別的 schema · 欄級授權。
- **after.sql**:`supabase/after-checks/270000-after.sql`
  預期:① 期望張數 = policy到位 = GRANT到位(三個數相同)· ② 全表列數 = ① 的期望張數、本來沒GRANT的 = 8
  · ③ 排除表被建了 policy 的數 = 0 · ④ 外溢 = 0 · 🟢 正對照 policy 總數 ≠ 0 · 🔵 負對照 = 0

## 2. `20260904020000_m4b_create_order_payment_channel.sql` ✅ 在 `origin/dev`
- **做什麼**:讓「建立訂單」這件事**收得下付款方式**(刷卡 / 匯款)。匯款線三支的第一支。
- **貼前前置**:無。🔴 **而 3、4 支依賴它** —— 順序不可換。
- **貼完該看到**:`Success. No rows returned`。
- **貼完 ACL diff 期望 = `FN` +4 列 · `FNCFG` +1 列, 其餘 0** —— ✅ **2026-09-05 貼完當場量的, 不是推的**
  ⛔ ~~原字面「ACL diff 期望 = 0 格(它只改 `create_order` 的參數列, 不動任何授權)」~~ **不成立**。
  🔴 **改參數列 = 建一支新函式** —— PG 用 (名稱 + 參數型別) 認身分, 所以它是**多載**不是「改」。
  ⇒ 新函式出生就佔格:`FN` 4 列(4 個角色各一)+ `FNCFG` 1 列(DEFINER / `search_path`)。
  📌 實測那 5 列 = `create_order(... p_coupon_code text)`,DEFINER · `search_path=""` ·
     `authenticated` 有 EXECUTE · `anon` / `payment_confirmer` / `service_role` 沒有。
  🛑 **那不是權限漂移** —— 而寫成「期望 0」會讓讀的人把它當成漂移、去查一個不存在的事故。
- **after.sql**:`supabase/after-checks/020000b-after.sql` ✅
  預期:① 幾個多載 ≥ 1 · ② 含判別字面 `p_payment_channel` 貼**前** f ⇒ 貼**後** t
  · 🟢 正對照 t · 🔵 負對照 f
  🔬 **2026-09-05 已拿它對正式庫跑過一發**:印 `1 · f · t · f` ⇒ **尺會動, 而那支還沒貼**。

## 3. `20260904040000_m4b_sibling_lookup_sees_bank_orders.sql` ✅ 在 `origin/dev`
- **做什麼**:客人已經有一張匯款單時, 系統**看得到它**(以前看不到 ⇒ 會讓同一台車開出第二張單)。
- **貼前前置**:🔴 **要先貼 2**。
- **貼完該看到**:`Success. No rows returned`。
- **貼完 ACL diff = 0 格** —— ✅ **2026-09-05 貼完當場量的**:快照 1234 列、差異 65 行,
  與貼**前**逐字相同 ⇒ 這一支確實一格沒動。
  ⚠️ **而【新建】函式會多 4 列(× 4 角色)** —— 那是 `220000` 那支會發生的事, 見第 7 段。
  ⇒ 這支若真的多出列 ⇒ 它建了新東西, **停下看**。
- **after.sql**:`supabase/after-checks/040000b-after.sql` ✅
  預期:② 含 `bank_pending` 貼**前** f ⇒ 貼**後** t · 🟢 正對照 t · 🔵 負對照 f
  🔬 已跑過一發:`1 · f · t · f`。

## 4. `20260904050000_m4b_supersede_bank_order_on_card.sql` ✅ 在 `origin/dev`
- **做什麼**:客人改用刷卡的那一刻, **把他那張匯款單取消掉**(Sean 09-04 拍甲:符合條件的**全部一起**取消)。
- **貼前前置**:🔴 **要先貼 2 與 3**。
- **貼完該看到**:`Success. No rows returned`。
- **貼完 ACL diff = 0 格** —— ✅ **2026-09-05 貼完當場量的**:快照 1234 列 / 差異 65 行,
  與貼**前**逐字相同;參數列沒動 ⇒ 多載仍是 1。
  🔴 **而下一個抄這句的人要知道判準**:「重定義」只有在**參數列一字不動**時才是 0 —— 參數列變了就是**新多載**,
     會多 `FN` 4 列 + `FNCFG` 1 列(第 2 支就是這樣打臉原字面的)。
     ⇒ 量到非 0 時**不一定是事故**:先問「是不是多了一支新多載」, 再問「是不是有人動了授權」。
- **after.sql**:`supabase/after-checks/050000b-after.sql` ✅
  預期:② 含 `superseded_by_card` 貼**前** f ⇒ 貼**後** t · 🟢 正對照 t · 🔵 負對照 f
  🔬 已跑過一發:`1 · f · t · f`。
  🔵 而它的**檔案層**契約另有守門(`supersede-bank-order-contract.test.ts` 九格)。

## 5. `20260905010000_m4b_manual_refund_syncs_payment_status.sql` ✅ 在 `origin/dev`
- **做什麼**:人工退款登記之後, 訂單的**付款狀態會跟著更新**(以前不會 ⇒ 帳面看起來還是「已付款」)。
- **貼前前置**:🔴 **它的前置閘要 `20260904230000` 已貼** —— 我 09-04 驗過 **已貼** ✅。
- **貼完該看到**:`Success. No rows returned`。
- **貼完 ACL diff 期望 = 0 格** —— ✅ **這次是量的不是推的**:
```
掃那支檔的 GRANT / REVOKE / CREATE ⇒ 只有一支 CREATE OR REPLACE FUNCTION(重定義既有的)
🔴 而掃到的兩個 "GRANT" 是【RAISE 訊息的字串常值裡的字】不是敘述:
   ':330  「零 GRANT、只有 owner 執行得到」' · ':333 「前一版曾 GRANT service_role,已撤」'
   ⇒ 我的尺剝了 `--` 行註解, 剝不掉【字串常值】—— 開檔才看得出來
⇒ 它零 GRANT、零新物件 ⇒ ACL 矩陣不動
```
- **after.sql**:`supabase/after-checks/0905010000b-after.sql` ✅
  預期:② `pcm_sync_order_refund_payment_status` 含 `order_manual_refunds` 貼**前** f ⇒ 貼**後** t · 🟢 t · 🔵 f
  🔬 已跑過一發:`1 · f · t · f`。
  ⚠️ 而 `-f9` 09-04 量過 `order_manual_refunds` 全表 **0 列**
  ⇒ **貼完不會有任何一張單被翻動** —— 那是預期, 不是沒生效。

## 6. `20260904251500`(-account 發票勾選 owner RPC 第④代) 🔴 **等收割**
- **現在在** `agent/line-account`(`cdca84c09`), **不在 `origin/dev`** ⇒ **今天不能貼**。
- 佇列註記:**貼了才能收 `53df69735` 那顆碼**(應用層已 `.rpc()` 未 apply 的物件 ⇒ 被部署時序閘擋)。

## 7. `20260904220000_m4b_outbox_shipment_tracking_corrected_event.sql` ✅ 在 `origin/dev`
- **做什麼**:出貨追蹤號被改過時, **補一封「更正通知」信**的事件。
- **貼前前置**:無。
- **貼完該看到**:`Success. No rows returned`。
- **貼完 ACL diff 期望 = 恰好 +8 列(新增), 0 格改變** —— ✅ **量出來的**:
```
它建【兩支新函式】:pcm_tracking_corrected_at_key(timestamptz)
                    pcm_tracking_corrected_dedup_key(uuid, uuid, timestamptz)
🔬 正式庫現在 ⇒ 那兩支【0 筆】⇒ 貼完各多 4 列(× 4 角色)= **+8 列**
   每一列該是:anon/authenticated/payment_confirmer ⇒ `-|DEF`(或 INV)· service_role ⇒ `X|…`
   (它們各自帶 REVOKE ALL FROM PUBLIC, anon, authenticated + GRANT EXECUTE TO service_role)
而 admin_update_shipment_tracking 是【重定義既有的】:
   🔬 現在的簽名逐字 = 那支 migration 寫的 5 參數 ⇒ 不會變成新函式
   🔬 現在的 ACL:anon f · authenticated f · payment_confirmer f · service_role t
      = 它 REVOKE/GRANT 之後的目標狀態 ⇒ **0 格改變**
```
  🛑 **而我先前在這一行寫「未知 / 我沒讀過」是錯的** —— 那支檔**就在我樹上**。
  📌 **「我沒有那個東西」與「我沒有去拿」是兩件事, 而它們在報告上長得一樣。**
- **after.sql**:🔴 **我沒有**(`-ship` 的片)。

## 8. `20260904280000`(-ship 第四條線計數) 🔴 **等收割**
- **現在在** `agent/line-ship`(`995d141c7`)⇒ **今天不能貼**。
- 佇列註記:adapter 已 `.rpc()` 未 apply 的 `get_tracking_corrected_gap_counts` ⇒ **貼了 7 與本支才收得了那顆碼**。

## 9. `20260905020000` / `20260905030000`(-ship) 🔴 **等收割**
- 都在 `agent/line-ship`(`15b9c1761` / `ae37f1be9`)⇒ **今天不能貼**。
- 🔵 而 `20260905020000` 我 09-05 唯讀量過一件事:它裡面那支 `pcm_js_trim_whitespace()`
  在正式庫現在是 **anon / authenticated / service_role 三個都不能執行** ——
  **與「它還沒貼」相符**。貼完那三個角色的 EXECUTE 要重量一次(板列 `⟦01-INVOKERVIEWEXEC⟧`)。

---

## 🛑 這份包**證不到什麼**
- 🔴🔴 **「在 `origin/dev` 上」不等於「Sean 貼得下去」—— 而【我試著用拋棄式 PG 乾跑來補這一格, 補不起來】。**
  2026-09-05 用 `scripts/migrations-replay-from-zero.sh`(`-ship` `e5477affc` 版, 它補了三缺件)
  從零 apply `origin/dev` 全部, 再照 §A 順序走那六支 ⇒ **六支全紅**。
  🛑 **而逐支開訊息看, 沒有一支是「順序錯了」**:
```
270000      前置閘⑤ 目標名單與 2026-09-04 盤點不符   ← 釘住【正式庫當下的 catalog】
020000      事後② 舊的 10 參數簽名不見了            ← 斷言比對【貼之前的舊版】, 合成世界沒有舊版
040000      前置閘 prosrc md5 與抽取時不符           ← 釘住【正式庫那支函式的內容】
050000      前置閘 prosrc md5 與抽取時不符           ← 同上
0905010000  前置閘① pcm_noncard_settle_recompute 不存在  ← 連鎖(230000 自己先紅)
220000      pcm_shipped_email_dedup_key does not exist  ← 連鎖
```
  🎯 **⇒ 這批 migration 的前置閘【刻意】釘住正式庫的當下狀態, 而那正是它們的價值** ——
     它們在「這個庫不是我對著做的那個」時會停下來。
     📌 **而同一個性質讓「在合成世界乾跑」對它們【零判別力】** —— 合成世界必然不是那個庫。
  🔴 **所以這一格【沒有補起來】, 而我不假裝補了。** 全跑的分母:整棵樹 **67 支失敗**,
     其中 **25 支**訊息含「前置閘 / prosrc / 不符」⇒ 那個 67 是**上界**不是「有幾支真的壞了」
     (那句是那支腳本自己印的, 我照抄)。
  🔵 **真正能補這一格的只有一件事:Sean 貼的那一刻站在旁邊, 而 after.sql 已經備好了。**
- ✅ **那四支 after.sql 2026-09-05 補完了**, 而且**每一支都拿去對正式庫跑過一發** ——
  四支全部印 `1 · f · t · f`(函式在 · 判別字面還沒有 · 🟢 正對照 t · 🔵 負對照 f)
  ⇒ 📌 **那一發同時證了兩件事:那四支【真的還沒貼】, 而【那四把尺會動】。**
     少了正負對照那兩格, 貼後的 `t` 只是一個沒有分母的字。
- 🛑 而它們**只證「函式體裡有那串字」, 不證「它的行為對」** ——
  字面被改名 / 被搬到別支函式 ⇒ 印 f, 而那不一定是「沒貼」。每支檔尾都寫了這句。
- 「貼完該看到什麼字」我只對 **1** 那支是**讀過它最後一句**確認的;其餘幾支是**照慣例**寫的(它們都是
  `DO $$ … $$` 收尾 ⇒ 慣例上回 `Success. No rows returned`)⇒ **那是推論不是量測。**
