# 值班須知 — 訂單到貨登錄(#352)

> 立檔 2026-08-11(D 窗,批二 apply 當日)。來源 = `D-468-STOP` §D,Sean Q9=A 拍板。
> 真權威 = migration 本體 `supabase/migrations/20260811010000_m4b_e10_352c_item_level_room_guard.sql`;
> 本檔與它衝突時以 migration 為準,並回頭修本檔。

## 0. 🔴 現在按得到了嗎(先看這條再往下讀)

**2026-08-11 更新**:入口已經寫好並 commit(#352-b-1 採購區塊、#352-b-2 出貨彈窗),
呼叫端不再是 0(實 grep:`grep -rn admin_record_item_receipt apps/ packages/ --include=*.ts --include=*.tsx`
排除測試與生成型別後 **7 處**)。

⚠️ **但「寫好」不等於「站上看得到」**,兩個前置各自獨立:
1. **要部署**:程式碼還在 `site-redesign` 分支、**未 push**,正式站上還沒有這些按鈕。
2. **要 apply `20260811070000`**:那支把「店內現貨」供應商翻成啟用。
   **在它 apply 之前**,採購下拉裡**選不到「店內現貨」** ⇒ 沒有採購列的品項無法登錄到貨。
   這是**預期行為不是 bug**(a1 刻意先落 `is_active=false`,等入口做好再翻)。

⇒ 判斷「員工現在到底按不按得到」請看這兩件,不要只看本檔存在與否。

### 已知範圍限制(2026-08-11)

- 出貨彈窗那個入口,**只對 `未到貨` 的品項出現**。已取消 / 已全數配箱的品項沒有,那是刻意的
  (對它們說「貨到了」是假話)。
- 整張單**完全沒有東西可出**且**也沒有在等的貨**(例如全取消)時,彈窗**不會開** ——
  員工會看到「這些訂單目前沒有任何一件出得了(…)」。這也是刻意的。

## 1. 最常撞的一句:「這張單已取消,但供應商的貨到了」

登錄會回 **`EXCEEDS_ROOM_AFTER_CANCELLATION`**:這張單被取消掉的份額,**不能再登錄到貨**。

- ✅ **這不是壞掉,是設計**(Sean Q9=A)。貨變成自家庫存,**不掛回那張單**。
- 部分取消也會撞:原本 3 件、取消 2 件,第 1 件登錄得進去,第 2 件就回這個碼。

### 這批貨要記到哪裡去:**登記溢收**

**資料庫層的出口已經有了** —— 用「**到貨 0 件、溢收 N 件**」登記(`quantity=0 / surplus=N`)。
a1 migration `20260810230000:82` 逐字寫了這個用法:「訂單已收滿之後才到的貨 = `quantity=0 / surplus=N`」。

- 兩道額度守門(採購列層 `:193`、品項層 `:245`)**都只看「到貨件數」、不看溢收件數** ⇒
  到貨填 0 就不會再撞 `EXCEEDS_ROOM_AFTER_CANCELLATION`。
- 溢收的語意(Sean Q1=A):**有紀錄查得到,但不能自動拿去出別單** —— 它不加進可出量。
- 🔴 **不要**改小取消數量、改大訂購量,或自己新開一筆採購列來硬塞 ——
  那會讓客人的取消紀錄與退款對不上,而且額度是**品項層**的
  (`quantity − Σ已取消 − Σ已到貨`,`20260811010000_..._item_level_room_guard.sql:231-241`),
  換一筆採購列**在同一張單上一樣會被擋**。

✅ **畫面已經給得出這條路了**(2026-08-11 #352-b):小視窗的「到貨幾件」下界是 **0**(不是 1)、
旁邊就是「溢收幾件(供應商多送的,不掛回這張單)」;被擋時的訊息也直接寫**怎麼做**
(「請把『到貨幾件』改成 0、把實際到的件數填進『溢收』」)。
⇒ backlog **#386** 原本要補的「導向」在文案層已經補上;**它剩下的是更主動的引導**
(例如一顆「改記為溢收」的按鈕自動幫他歸零帶差額)。

溢收之後那批貨怎麼變成可以賣的庫存 = backlog **#374**(Phase 2、獨立庫存、要跟報價單整合);
在 #374 之前,溢收紀錄只是**查得到的帳**,不是能出貨的庫存。

## 2. 🔴 自診斷:看到 raw `23514` 就是環境沒上齊

若有人回報畫面吐出 **`23514`** 或 **`oiqs_instock_cancelled_le_quantity`** 這串字,
**代表甲片沒上到那個環境**(只上了 a1+a2)。那是資料庫的底層 CHECK 直接噴出來的,
員工看不懂、也不該看到 —— 甲片存在的唯一理由就是把它換成上面那句人話。

處置:查該環境的 `supabase_migrations.schema_migrations` 有沒有
`20260811010000`;沒有就是漏 apply,**三支要一起上**
(`20260810230000` → `20260810233000` → `20260811010000`,甲片會 `CREATE OR REPLACE` a2 的 record)。

## 3. 其餘回傳碼一覽(逐字取自 migration)

**登錄** `admin_record_item_receipt`:

| 碼 | 意思 | 值班怎麼做 |
| --- | --- | --- |
| `RECORDED` | 登錄成功 | 正常 |
| `DUPLICATE_REQUEST` | 同一張單同一個請求碼重送 | 正常,**不要再按一次**;第一次已經記進去了 |
| `EXCEEDS_ROOM_AFTER_CANCELLATION` | 這張單被取消掉的份額不能再登錄到貨 | 見 §1 —— 到貨填 0、多的登記**溢收** |
| `QUANTITY_EXCEEDS_ALLOCATED` | 超過這筆採購**訂了幾件** | 先對供應商出貨單;確實多送就到貨填 0、多的登記**溢收** |
| `PROCUREMENT_NOT_FOUND` | 找不到那筆採購 | 畫面重整;仍舊回報給工程 |
| `INVALID_QUANTITY` | 🔴 **不是「必須正整數」** —— 到貨與溢收各自要 ≥ 0,而**兩者加起來**要在 1～100000 之間 | 改輸入(到貨填 0 是合法的,見 §1) |
| `NOTE_TOO_LONG` | 備註超長 | 縮短 |
| `RECEIVED_AT_REQUIRED` / `RECEIVED_AT_OUT_OF_RANGE` / `RECEIVED_AT_IN_FUTURE` | 到貨日空白 / 超出合理範圍 / 填到未來 | 改日期 |

**刪除** `admin_delete_item_receipt`:`DELETED` = 成功 /
`ALREADY_DELETED` = 已經刪過(重按無害)/ `RECEIPT_NOT_FOUND` = 找不到該筆收據。
⚠️ **這支在畫面上還沒有入口**(2026-08-11):RPC 在庫,但 #352-b 刻意沒做刪除按鈕
(未執行過的 code 不進 commit)。要刪到貨紀錄目前只能找工程。
🔴 寫刪除入口的人注意:它有第三個 SQLSTATE **`P4A03`**,那是**業務拒絕不是系統錯誤**
(刪掉會讓可出量不夠),而且 DB 的訊息已經逐箱列出未出貨的包裹 ⇒ **原文帶給員工**。

## 4. 誠實邊界(不要對外說滿)

- **並發完全沒測過**:兩個人同時對同一張單按登錄與取消,靠的是雙方都鎖同一列 `order_items`
  —— 那是**程式碼寫著的字面**,沒有交錯時序的實測。
- 第 3 節那些碼的**畫面文案**已由 #352-b 定稿(暫定字面、待 Sean 肉眼定稿);本檔保證的是**碼**的意思。
- 🔴 **零真資料往返**:兩支 RPC 活在正式庫,但到 2026-08-11 為止**沒有任何一次真實登錄**跑過
  —— 所有驗證都是單元測試與突變靶。第一次真的按下去會是 Sean 的肉眼驗。
