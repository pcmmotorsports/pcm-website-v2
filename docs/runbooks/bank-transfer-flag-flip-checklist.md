# 匯款結帳:翻開 `BANK_TRANSFER_CHECKOUT_ENABLED` 之前的完整清單

> # 🔴🔴 **「翻 flag」不是一個動作, 是一個【要排序的組合】。**
> 🔬 依據(`4b88e7e36` commit body 逐字, codex R2 糾正原作者而它對):
> 　 `BANK_TRANSFER_CHECKOUT_ENABLED=false` **只擋 storefront action, 不是 DB 邊界**。
> ```
> 翻 env 而不貼那三支  ⇒ 客人走到一半撞牆
> 貼那三支而不翻 env   ⇒ 只擋得住 storefront;繞過 storefront action 的路(PostgREST 直呼)不受它管
> ```
> ⇒ 📌 **順序錯的兩種後果不一樣, 而它們都不是「沒生效」那麼溫和。**

> 🔴 **本檔的目的不是「做完了沒」, 是【翻那顆開關那一刻, 客人會撞到什麼】。**
> 建立:2026-09-05 線【信】`-mail`(主視窗 `-94` 派)。
> ⚠️ **每個數字都標了量測時刻。引用前當場重量, 不要抄。**

## 這張表怎麼讀

五節, 每節答 **ready / blocked / 誰給**。
🛑 **blocked 不是「還沒做」** —— 它是「**答案不在寫這張表的人手上**」, 而那一格寫了**誰給、缺哪一道檢查**。
📌 **一張三分之二 blocked 的表, 比一張看起來完整而有三格是編的表有用。**

---

## ① 還沒貼的 SQL — **已量到了**(而不是我原本以為的「全部沒貼」)

> 🔬 **量測者 = 線【資料】`-db`(唯讀正式庫, `pcm_readonly`, `rolbypassrls=t` ⇒ 不是被 RLS 擋住)**
> **時刻 = 2026-09-04 深夜。🔴 這張表會過期 —— Sean 隨時可能再貼, 用之前重跑。**
> **判準 = 正式庫的【函式體字面】, 不是 `APPLIED.tsv`。**

| migration | 它做什麼 | 正式庫 |
|---|---|---|
| `20260903080000` | 匯款/現金逾時 ⇒ **5 天** | ✅ **貼了** |
| `20260904020000` | `create_order` 加 `payment_channel` | ⛔ ~~🔴 **沒貼**~~ ⇒ ✅ **貼了**(09-06 複量) |
| `20260904040000` | dedup 看得到匯款單 | ⛔ ~~🔴 **沒貼**~~ ⇒ ✅ **貼了**(09-06 複量) |
| `20260904050000` | 刷卡時取消同車的匯款單 | ⛔ ~~🔴 **沒貼**~~ ⇒ ✅ **貼了**(09-06 複量) |
| `20260904230000` | 收款結清 + 逾期不殺已收錢的單 | ✅ **貼了** |
| `20260905010000` | 人工退款翻狀態 | ⛔ ~~⚠️ **未量**~~ ⇒ ✅ **貼了**(09-06 複量, 見下方尺的訂正) |

🔴🔴 **[2026-09-06 01:0x · 線【信】`-mail` 複量 —— 上表【整欄過期了】]**
> 量測者 = `-mail`, 唯讀正式庫(`scripts/readonly-prod-sql.sh`, 零寫入), 讀數時刻 DB `now()` = **2026-09-05 16:5x UTC**。
> 判準同樣是**正式庫的目錄事實**, 不是 `APPLIED.tsv`。逐格值:
>
> · `20260904020000` ⇒ `public.create_order` 多載 **2** 支, 其中帶 `p_payment_channel` 的 **1** 支
>   (🟢 正對照同尺問 `p_lines` ⇒ **2** · 🔵 負對照 `p_zzz_never` ⇒ **0**)
>   📌 **舊的那支 10 參版還在** —— 改 `create_order` 的人要知道有兩支。
> · `20260904040000` ⇒ `find_active_sibling_own` 函式體含 `bank_pending` **1** / `bank_transfer` **1**
>   (🟢 正對照 `active`/`charged`/`kind`/`none` ⇒ 各 **1** · 🔵 負對照現造字面 ⇒ **0**)
> · `20260904050000` ⇒ `begin_charge_attempt` 函式體含 `superseded_by_card` **1** / `not_card_order` **1**
>   (🟢 正對照「那支函式在不在」⇒ **1** · 🔵 負對照 `zzz_never_a_literal` ⇒ **0**)
> · `20260905010000` ⇒ 🛑 **我第一發用錯了尺** —— 我問的是 `admin_record_manual_refund` 存不存在(**1**),
>   而那支函式來自 `20260810200000` / `20260820022000`, **比它早** ⇒ 📌 **那個 1 對這一支【零判別力】**
>   (`is-migration-applied.sh` 檔頭逐字警告過這件事, 而我照樣踩了)。
>   ⛔ ~~✅ **改問它真正新建的物件**:`pcm_sync_order_refund_payment_status` 存在 **1**~~
>   🔴🔴 **第二把尺【也】沒有判別力 —— 線【船】`-68` 當場打掉它, 而它是對的。**
>   `pcm_sync_order_refund_payment_status` **不是** `20260905010000` 建的:
>   `grep -ln 'FUNCTION public.pcm_sync_order_refund_payment_status' supabase/migrations/*.sql` ⇒ 三支,
>   最早是 **`20260823010000`**(`…p1_extract_sync_fn.sql`), `20260905010000` 只是 `CREATE OR REPLACE`
>   ⇒ 📌 **它貼沒貼, 「那支函式在不在」都回 1。**
>   ⇒ 🎯 **同一族的坑, 兩把尺、兩個人、同一天 —— 而第二把只是【往上挪了一層】, 沒有離開那一族。**
>   ✅ **真正會動的那把尺 = 問【函式體裡面是哪一版】**(`-68` 挑的字, 我獨立複跑, 17:16 UTC):
>   　· `prosrc LIKE '%order_manual_refunds%'`(**只有新版 body 有**;本地分母:`20260905010000` 命中 **20**,
>   　  `20260823010000` 命中 **0**)⇒ **true**
>   　· 🟢 正對照 `'%order_refunds%'`(新舊皆有)⇒ **true**　· 🔵 負對照現造字面 ⇒ **false**
>   ⇒ ✅ **結論仍是「貼了」—— 而這一次它是量出來的, 前兩次是剛好答對。**
>   🛑 **通則(CLAUDE.md 路由表逐字寫過, 我們兩個今晚各撞一次)**:
>   　**`CREATE OR REPLACE` 這一族, 「物件在不在」永遠零判別力 —— 要問的是【裡面是哪一版】。**
>
> 🛑 **⇒ 下面那張【貼板佇列】的 1/2/3 全部已經在線上了 —— 不要再請 Sean 貼。**
> 🔴🔴 **而真正要緊的是它連帶殺掉的那個【擋路理由】**:板列 `⟦b4-BANKORDERINVISIBLE⟧` 寫著
>    「直接打 PostgREST 送 `bank_transfer` 這條繞路今天走不通, 而理由是【段 1-A 還沒貼 ⇒ `PGRST202`】」
>    —— ⛔ **那個理由今天不成立了**(A = `20260904020000`, 已貼)。
>    ⇒ 📌 **它自己就預告過這一刻**:「那是會過期的理由 —— Sean 貼下 A 的那一刻它失效, 而沒有東西會叫。」
>    ⇒ 🔴 **翻 flag 之前的 (c)「RPC 那側的 opt-in 守門」現在是【真的要查】, 不再有替代品擋著。**
>    ✅ **已查 —— 結果在本節正下方那一格「(c) RPC 那側今天有沒有守門」。**
> ⚠️ **本次證不到什麼**:讀的是 catalog(函式簽名 / 函式體字面), **不是行為** ——
>    我沒有真的打一發 RPC 去看它收不收 `bank_transfer`。

🟢 **正對照**:對同一批函式問一個一定在的字面(`RETURNS`)⇒ 三支全 `t` ⇒ **尺在這幾支上會動**
　 ⇒ 那三個「沒貼」是真的沒有那段碼, 不是讀不到。
🔬 **而 `20260904020000` 有第二個獨立證據, 比字面強**:正式庫 `create_order` 的參數列
　 (`pg_get_function_identity_arguments`)**沒有 `p_payment_channel`** ——
　 📌 **簽名少一個參數不是字面的問題。**

### 🔴🔴 (c) RPC 那側今天有沒有守門 ⇒ **量到了:【世界三 —— 沒守, 而且可達】**

> 量測者 = 線【信】`-mail`, 唯讀正式庫(`scripts/readonly-prod-sql.sh`, 零寫入),
> 讀數時刻 DB `now()` = **2026-09-05 17:06 UTC**(2026-09-06 台北凌晨)。

**① catalog —— 兩支 `create_order` 多載的本體讀不讀 flag / 拒不拒 `bank_transfer`**

| oid | 參數 | `bank_transfer` 字面 | 讀 `current_setting` | 讀 `BANK_TRANSFER_CHECKOUT_ENABLED` | prosrc 長度 |
|---|---|---|---|---|---|
| 66479 | 10 參(舊) | **f** | **f** | **f** | 16391 |
| 68345 | 11 參(帶 `p_payment_channel`) | **t** | **f** | **f** | 17499 |

🔵 負對照:同尺問現造字面 `zzz_never_a_literal` ⇒ 兩支皆 **f** ⇒ 這把尺會動。
🎯 **⇒ 新那支【收】`bank_transfer`(白名單, `20260904020000:168` 逐字), 而它【不讀任何旗標】** ——
　 沒有 GUC、沒有 env、沒有任何 opt-in 述詞。**DB 那一層零守門。**

**② PostgREST 那條 —— 誰叫得動**

| oid | `anon` EXECUTE | `authenticated` EXECUTE | `service_role` EXECUTE |
|---|---|---|---|
| 66479(10 參) | **f** | 🔴 **t** | f |
| 68345(11 參) | **f** | 🔴 **t** | f |

🟢 **正對照(證明這把尺會印 f)**:同兩個角色問 `begin_charge_attempt` ⇒ **f / f**;
　 `pcm_sync_order_refund_payment_status` ⇒ **f / f**;`find_active_sibling_own` ⇒ **f / t**。
　 ⇒ 📌 **有東西印 f, 所以上面那兩個 t 是讀出來的, 不是尺壞了。**
🟢 角色都在:`anon` / `authenticated` / `service_role` 三個 `pg_roles` 皆命中
　(`service_role` `rolbypassrls = t`, 另兩個 f)。

**③ apps 側 —— flag=false 時走哪一支**
`apps/storefront/src/app/checkout/charge-actions.ts:222-224` 逐字
`if (parsedCheckout.data.paymentChannel === 'bank_transfer' && !isBankTransferCheckoutEnabled())`
⇒ 回 `{ fieldErrors: { paymentChannel: '請選擇付款方式' } }`, **零建單零扣款**。
✅ 那道閘是真的, **而它的射程只到這支 action** —— 同檔 `:204` 的註解自己逐字寫著這件事。

**⇒ 結論(三個世界擇一)**:🔴 **世界三 —— 沒守, 而且可達。**
任何**登入中**的客人可直接打 PostgREST `/rpc/create_order` 送 `payment_channel: 'bank_transfer'`,
白名單收它、無旗標可攔、`authenticated` 有 EXECUTE ⇒ **完全不經過 `charge-actions.ts:222`。**

🛑🛑 **而【三件事要跟這個結論一起讀, 少一件就會做出過度或不足的反應】**:
1. **這條繞路【不是 `bank_transfer` 帶進來的】** —— 舊的 10 參多載對 `authenticated` 同樣是 **t**,
   而它比這整條線早。⇒ 📌 **「登入客人可以直接建單」本來就成立**;`bank_transfer` 改變的是
   **那張單的 `payment_channel` 欄會存什麼**, 不是「他能不能建單」。
2. **它原本的錢的傷害【已經被關掉了】** —— 這個洞會痛是因為「先建匯款單再刷卡 ⇒ dedup 看不見 ⇒ 兩邊都付」。
   而 `20260904050000`(刷卡時先取消同車匯款單)**已在線上**(複量:`begin_charge_attempt` 體含
   `superseded_by_card` 1 / `not_card_order` 1, 🟢 正對照 1 · 🔵 負對照 0)⇒ **那條路今天不再兩邊都付。**
3. **⇒ 所以這一格不是「立刻關路」, 是【要 Sean 拍一題】**:繞路建出來的匯款單今天的結局是
   零扣款、`/account/orders` 看得到、5 天後逾期自動取消。要不要為它另立 RPC 側 opt-in,
   是**成本 vs 殘餘風險**的取捨, 不是安全事故。**線【信】不自己拍。**

🔵 **[主視窗 `-f8` 2026-09-06 裁]**:照上面第 3 條 —— **當殘餘風險端 Sean**。
題目 **`Q-匯款繞路`**(甲 先不加 / 乙 加;**推薦甲**)已進佇列與明早清單。
🛑 **而端題時必須帶著這一句, 否則他會以為「可達」已經釘死了**:
**缺 PostgREST 暴露那一道 —— `create_order` 有沒有真的被開在 `/rpc/` 下
(schema 白名單 / `db-pre-request`)【本發未查】。**
📌 我量到的是「**DB 允許**」;「**外面打得進來**」還差那一道。兩句不一樣。

✅ **[Sean 2026-09-06 01:38 答【甲】—— 不加, 殘餘風險他接受]**
⚠️ **而主視窗 `-f8` 自己標了一格不確定, 逐字轉述、不改寫**:
> 「他是依我端題順序答的第三個裸『甲』, 我推的, 已在佇列標明;**他若之後說不是以他為準**」

📌 **⇒ 這一格的授權強度是【待 Sean 複核】, 不是【已確認】。**
🛑 **不要拿它去關掉任何一道現役的閘**, 也不要在別處把它引用成「Sean 已拍板不用管」。
🔴 **而缺的那一道仍缺**:PostgREST 有沒有把 `create_order` 暴露在 `/rpc/` 下
(schema 白名單 / `db-pre-request`)**未查**。

⚠️ **本次證不到什麼**(逐條, 不是免責貼紙):
· 讀的是 **catalog**(函式簽名 / 函式體字面 / ACL), **不是行為** —— 我**沒有**真的打一發 PostgREST 去看它收不收。
· `has_function_privilege` 答的是「這個角色有沒有 EXECUTE」, **答不出 PostgREST 有沒有把那支函式
  暴露在 `/rpc/` 下**(schema 是否在 `PGRST_DB_SCHEMAS` 裡、有沒有被 `db-pre-request` 擋)——**那兩層我沒查**。
· 我**沒有量**「今天有沒有人真的這樣繞過」—— 那要查訂單資料, 不是查目錄。

### 🔴 (d) 翻 flag 前還有一條 —— **匯款客人收不到任何一封信**

板列 **`⟦b4-BANKNOEMAIL⟧`**(2026-09-06 新開)。一句話:
`sweep-email-outbox.ts` 全檔 `payment_channel` **0 命中**(🟢 正對照 `order` **68** · 🔵 負對照 **0**),
而掃描面 `pcm_order_created_email_pending` 逐字 `WHERE o.payment_status = 'paid'`
⇒ **未付款的匯款單從來不進那張 view ⇒ 一封信都不寄。**
匯款資訊(帳號 / 戶名 / 備註 / 期限)只活在 `OrderDetailView.tsx:663-698` 那一頁上,
而**沒有任何一封信把他帶回那一頁**。
🛑 **⇒ 上面 (a)(b)(c) 三條【沒有一條在問信】** —— 這是第四條, 而它痛在**沒有寄**,
而「沒有寄出去的信」在每一把尺上都是綠的(零錯誤、零死信、心跳照常、outbox 零列)。

### 🛑 兩件不要把上面的結果讀太寬(量測者自己標的)
```
① `20260904230000` 那一列, 量測者第一發挑 `v_paid` ⇒ 得到 f, 而那是【他的字面壞了】不是沒貼。
   他今晚稍早另有一份獨立驗證(兩支函式存在 · definer · search_path="" · AFTER trigger 掛在
   order_payments, 五格全過)⇒ 該列標 ✅ 的依據是【那份】, 不是這張表。
   ⇒ 🔴 **一個 f 有兩種成因:真的沒貼 / 挑錯字面。而它們印同一個字。**
② `20260905010000` 他那棵樹【沒有那支檔】⇒ 取不到字面 ⇒ **那是「沒量」不是「沒貼」。**
   多樹 repo 裡「我這裡沒有」不等於「哪裡都沒有」。
```

### 🔬 三支未貼的 migration, 各自的審查狀態(2026-09-05 開 commit body 實查)

| migration | commit | codex 狀態(逐字) | 可以排進貼板嗎 |
|---|---|---|---|
| `20260904020000` | `4b6a16888` | 「codex 對抗審查 FAIL(11 must-fix + 6 nit), **全數處理**」 | 🟢 可排 |
| `20260904040000` | `2eecb6ba8` | 「codex 關卡2 抓到兩個真的 SQL bug…**我實測核過**」+「其餘 codex 條目」 | 🟢 可排 |
| `20260904050000` | `4b88e7e36` | 🔴 **「codex 關卡2 兩輪用盡, R2 仍 FAIL —— 不得視為全過」** | 🛑 **不可排, 見下** |

### 🛑 `20260904050000` —— **它自己的 commit body 就寫著「不進貼板佇列」**
```
🔬 逐字(commit 4b88e7e36 body):
   「那支 migration **不進 Sean 的貼板佇列**;板列寫明『已 commit 未 apply ⇒ 正式庫行為零改變』」
```
🔴 **那是主視窗 2026-09-04 拍的四個條件之一, 而條件已全數照做** ——
　 · 6 條 PARTIAL ⇒ 板列 `⟦b4-PIECEBGATEGAPS⟧`(**刻意不寫成「已接受的殘餘風險」** —— Sean 沒說過那句)
　 · 跨分頁競態 ⇒ 單獨一列 `⟦b4-BANKCARDRACE⟧`
⛔ ~~排貼板佇列之前要先問 Sean…不是我或主視窗能自己翻的~~ **作廢**(主視窗 2026-09-05 訂正)——
　 🔬 **那四個條件是【主視窗】拍的, 不是 Sean** ⇒ **它自己解得了, 不必上到 Sean。**
　 ⇒ 📌 **我把「拍板」預設成 Sean 拍的了** —— 而這條線上有兩層拍板者, 而它們的解除路徑不同。
　 ⇒ 🔵 **判別法:去看那句話是誰說的, 不要看它有多像一條紀律。**
✅ **解除條件(主視窗 2026-09-05 給)**:`codex R3 換角度 PASS` **+** `6 條 PARTIAL(⟦b4-PIECEBGATEGAPS⟧)折完`
　 ⇒ 兩件都做完才進佇列。那是線【信】`-mail` 明天的第二件。

### 📋 貼板佇列(主視窗 `-94` 2026-09-05 排;依賴順序不可換)
```
1. 20260904020000   create_order 加 payment_channel      ✅ 已在線上(09-06 複量)
   　↓ 後面兩支都要拿到那個參數的值, 少了它整條路第一步就斷
2. 20260904040000   dedup 看得到匯款單                    ✅ 已在線上(09-06 複量)
   　↓
3. 20260904050000   刷卡時取消同車匯款單                  ✅ 已在線上(09-06 複量;等 codex R3 那句已無對象)
   　↓
4. 20260905010000   人工退款翻狀態                        🔵 已 commit `0a7ee89c0`
                                                          ⚠️ 而它的前置閘①要求 20260904230000 先貼
                                                          🔬 而 -db 實測 230000【已貼】⇒ 這一格通得過
```
🛑 **`20260904050000` 的解除條件(主視窗 2026-09-05 訂正:那條是【它】拍的, 不是 Sean)**:
```
codex R3【換角度】PASS  +  6 條 PARTIAL(⟦b4-PIECEBGATEGAPS⟧)折完
⇒ 兩件都做完才進佇列
```
🔵 ⇒ 那是線【信】`-mail` 明天的第二件(第一件是本清單)。

### 🔵 而那顆 commit 還留了一個【射程訂正】, 它改變本清單的前提
```
🔬 逐字:「codex R2 糾正我而它對:BANK_TRANSFER_CHECKOUT_ENABLED=false
   **只擋 storefront action, 不是 DB 邊界**
   ⇒ 真正擋著這條路的是【段 1-A 還沒 apply 正式庫】。」
```
🎯 **⇒ 本清單的名字叫「翻開那顆 env 之前」, 而那顆 env 不是唯一擋著的東西** ——
　 **真正擋著的是 ① 那三支沒貼。**
⇒ 📌 **⇒ 翻 env 而不貼那三支 = 客人走到一半撞牆;貼了那三支而不翻 env = 只擋得住 storefront,
　 而【任何繞過 storefront action 的路徑】(PostgREST 直呼)不受那顆 env 管。**

### 🔴 ⇒ 對翻 flag 的意思(這一節真正的結論)
```
匯款線的碼有【三支】還沒進正式庫:020000 · 040000 · 050000
⇒ 而它們互相有依賴:create_order 收不到 payment_channel
  ⇒ 後面兩支拿不到那個值 ⇒ 整條路的第一步就斷了
⇒ 🎯 **任何「匯款單走得通」的宣稱, 現在在正式庫上【不成立】。**
```
⚠️ **順序有硬相依**:`20260905010000` 的前置閘①要求 `20260904230000` 先貼(含它那個 trigger)。
　 順序錯 ⇒ fail-closed 拒絕, 而**訊息會說出下一步**。

### ⛔ ~~原本這一節寫「六支帳本全 0」~~ **作廢, 而舊字面留著**
🔬 我 2026-09-05 收工前量的是 `APPLIED.tsv` ⇒ 六支全 **0**(帳本 374 行, 正對照 `20260712203000` ⇒ 1)。
🛑 **而那六個 0 有兩支是【貼了的】** ⇒ 📌 **帳本落後現實, 這一節是它的實錘。**
　 ⇒ 🔴 **不要再用帳本判這件事。**

## ② env — **blocked** · 誰給:Sean(或有 Vercel dashboard 的人)

🔬 2026-09-05 grep 實測到的名字(不憑記憶):
```
BANK_TRANSFER_CHECKOUT_ENABLED    顧客站匯款結帳入口(本清單要翻的就是它)
ADMIN_REQUIRE_REAL_IDENTITY       已上線 = 1;與本線相關而不是本線的閘
```
🔴 **我查不到它們在 Vercel 上的現值** ⇒ 缺的那道檢查 = 讀 production 的環境變數。
⚠️ 而 memory 記過一格:**刪 env 也要 redeploy** —— 翻開之後同理, **改了值不代表生效**。

---

## ③ 兩條已知缺口:接受還是修 — **blocked** · 誰給:**Sean**(這是他的決定)

| 錨 | 是什麼 | 現況 |
|---|---|---|
| `⟦b4-NCPCRONRACE⟧` | 逾期排程先鎖住那張單, 收款才落帳 ⇒ 最後變成「已取消而錢在裡面」 | 有既有安全網(`20260901080000` 待退款片, **帳本已 apply**)接住 ⇒ 錢落到待退款表, 不會消失。🛑 **而客人的單被取消了, 他要走人工退款 ⇒ 他會打電話來。** |
| `⟦b4-NCPCANCELROLLBACK⟧` | 重算撞上逾時被取消時, 客人那筆收款會跟著回滾 | 與 `20260903080000` 心跳段**同款結構**, 而該檔自陳是已知殘留風險 ⇒ 單獨為這一片解會變成只有這片特別 |

🔴 **兩條的「先不修」是【線信 `-mail` 2026-09-05 判的, 未經 Sean】** —— 引用時不要當成他的話。
⚠️ **而我沒有量過這兩條的觸發頻率** ⇒ 🛑 **「很少發生」這句話沒有來源, 不得寫進任何地方。**
⇒ 📌 **這一節真正的問題是:「要不要帶著兩個已知缺口翻開這顆開關」** —— 那不是技術題。

---

## ④ 真瀏覽器驗收 — **ready(而只能寫「翻開之後怎麼驗」)**

🔬 **今天走不到**:`BANK_TRANSFER_CHECKOUT_ENABLED` 關著 + ①那六支沒貼
⇒ 📌 **所以本節寫的是【翻開之後要做什麼】, 不是【已驗證】。任何人不得把它讀成後者。**

起本機顧客站:`bash scripts/storefront-probe/up.sh`(詳 `docs/runbooks/local-admin-with-real-data-probe.md` 同族)

### 🔴 翻開那一刻要一起做的事(不是驗收, 是【動作】)

```
把「銀行轉帳」加回顧客站 FAQ 的付款方式那句
  apps/storefront/src/components/ProductFAQ.tsx  付款那行
```
🔬 **來源**:線【前台】`-front` 2026-09-05 代發, 逐字「Sean 2026-09-04 拍甲時明說『匯款開了再加回』」。
🔴 **而那天 `ProductFAQ.test.tsx` 的守門格會紅 —— 那是【預期的】, 不是回歸。**
　 它釘著「不得出現銀行轉帳 / LINE Pay」⇒ 加回去那一刻它就該紅, 而**紅了要改它不是繞過它**。

⚠️ **而我把 `-front` 那句拿去核了, 兩件與它的描述不符 —— 寫出來免得下一個人以為表是錯的**:
```
🔬 2026-09-05 當場量:
  · origin/dev 上 ProductFAQ.tsx:36 逐字仍是「可用 銀行轉帳、線上刷卡、LINE Pay 支付」
    ⇒ 🔴 **那句話現在還沒被拿掉**
  · ProductFAQ.test.tsx 存在(92 行, 🔵 正對照證明尺會動), 而 grep「銀行轉帳 / LINE Pay」⇒ **0 命中**
    ⇒ 🔴 **那個守門格現在還不在**
```
⇒ 📌 **⇒ `-front` 講的是它【手上未推】的版本, 不是線上的現況。**
　 🔵 **兩者都對, 而它們講的是不同時刻** —— 而「翻開那天要加回去」這個動作**不受影響**:
　 　 它推上去之後那句話會被拿掉, 而翻 flag 那天要加回來。
🛑 **⇒ 執行這一步之前先當場 grep 一次那兩支檔的現況** —— 本節寫的是 2026-09-05 的樣子。

⚠️ 而 **LINE Pay 那半是另一件事**:實測 `line_pay` 在碼裡有值域(6 支檔命中)而
　 **沒有一支是結帳 UI**;結帳頁只有一顆「信用卡付款」且 `checked readOnly`。
　 ⇒ 🔴 **不論匯款開不開, LINE Pay 那半都是假的** ⇒ 已端主視窗, 那是獨立的一格。

### 走一遍(每一步都要**看到**那個東西, 不是「應該會有」)
```
1  商品頁 ⇒ 加入購物車 ⇒ 結帳
   🔴 Step 2「付款方式」要看到【兩個】選項(信用卡 / ATM 轉帳)
   ⚠️ 今天只有一個, 而且是 `checked readOnly`(CheckoutStep2.tsx 實測)
   ⇒ 這一步紅 = 前台那半沒上, 後面全部不用測
2  選 ATM 轉帳 ⇒ 送出 ⇒ 🔴 **不得跳到刷卡流程**
   (那正是 ⟦b4-BANKCHARGESCARD⟧ 在防的:翻開那一刻拿客人的卡去扣一張匯款單的錢)
3  成功頁 ⇒ 要看到收款資訊六樣:銀行 / 分行 / 戶名 / 帳號 / 金額 / 備註說明
4  訂單詳情頁(會員區)⇒ 同樣六樣要在
   🔴 而**它要顯示還剩幾天**, 不是只顯示已經過期(⟦b4-REMITNODEADLINE⟧)
5  後台登記一筆收款(全額)⇒ 🔴 回訂單詳情頁 ⇒ **匯款區塊要消失、狀態要變已付款**
   🛑 這一步是 ⟦b4-NONCARDPAID1⟧ 的驗收 —— 不消失 ⇒ **客人會再匯一次**
6  後台登記一筆人工退款 ⇒ 🔴 狀態要跟著變(部分退款 / 已退款)
   🛑 這一步是 `20260905010000` 的驗收
7  逾期:把一張零收款的匯款單建立時間改成 6 天前 ⇒ 跑逾期排程 ⇒ 要被取消
   而 2 天前的那張 ⇒ **不得**被取消(5 天規則)
8  逾期負對照:一張【已收到錢】的匯款單放 6 天 ⇒ 🔴 **不得**被取消
   🛑 這一步是 `20260904230000` 淨額腿的驗收
```
🔵 **第 8 步是整張表最容易被跳過的一步** —— 因為它要的是「**什麼都沒發生**」。
　 ⇒ 📌 **而「不得被取消」與「排程根本沒跑」印同一個畫面** ⇒ 要先確認排程真的跑了(看它的 log 筆數)。

---

## ⑤ 客服稿 — **ready(本節就是產物)**

🔬 **先查過有沒有人寫過**(三個載體):
```
~/pcm-mailbox/C-eb-C7-客服稿自測-20260821.md  ⇒ 有, 而是【刪資料】那條線的, 不是匯款
~/pcm-mailbox/58-841 / 58-867 / A-86          ⇒ 匯款的 **plan**, 不是客服稿
docs/runbooks/                                 ⇒ 匯款客服稿 **零**
```
⇒ 所以本節是新寫的。

### 🔴 自檢用的那把尺(取自 C7, 不是我發明的)
> **「這句話有沒有承諾一個我們做不到的動作?」**
> C7 當時五句抓到三句同一個病 —— **而那三句都是善意的。**

### 稿(每句後面標它為什麼安全)

**Q1 客人問:錢要匯去哪?**
> 「訂單成立後,您的**訂單頁**上會顯示我們的銀行、分行、戶名、帳號與應匯金額。
> 　 請以那一頁上的資訊為準。」
🔵 安全:**不在對話裡報帳號** ⇒ 客服不必背、不會抄錯;而那六樣的單一來源是
　 `packages/domain/src/order/remittance-info.ts` 的六個常數(`PCM_REMITTANCE_*`)。
🛑 **本檔刻意不抄那六個值** —— 抄下來就是第二份, 而它不會跟著改。

**Q2 客人問:我要在幾天內匯?**
> 「**5 天**內。超過我們會自動取消訂單,而您隨時可以重新下單。」
🔬 **5 這個數字有兩個獨立來源, 而它們今天一致**(2026-09-05 量):
　 · 文案端 `remittance-info.ts:65` ⇒ `PCM_REMITTANCE_EXPIRE_DAYS = 5`
　 · 排程端 `20260903080000` ⇒ `interval '5 days'`(bank_transfer 與 cash 各一)
🔴 **而沒有任何東西在守它們一致** ⇒ 📌 **改任一邊之前, 要記得另一邊也寫著 5。**

**Q3 客人問:我匯了,為什麼還顯示未付款?**
> 「我們收到款項後會由同事登記,登記完成您的訂單頁就會變成已付款。
> 　 如果您已經匯款超過一個工作天還沒變,請把**匯款後五碼**告訴我們,我們幫您查。」
🔵 安全:**沒有承諾「自動」** —— 實際上是人工登記, 而稿子照實說。
🛑 **而這一句有一個前提**:登記之後狀態**真的會變**(`20260904230000`)。
　 ⇒ 🔴 **那支沒貼之前, 這句話是假的** ⇒ 見 ①。

**Q4 客人問:我匯了,而單子被取消了怎麼辦?**
> 「您的款項不會消失。請告訴我們訂單編號,我們會確認並安排退款或重新為您建立訂單。」
🔵 安全:**沒有承諾「馬上退」也沒承諾「訂單會復活」** —— 兩者我們都做不到自動化。
🔬 依據:`20260901080000`(**帳本已 apply**)在整單取消那一刻逐軌開一筆待退款
　 ⇒ **錢會落在 `order_pending_refunds` 上, 不會沒有紀錄。**
🛑 而**退款本身是人工的** ⇒ 稿子說「安排」不說「處理完成」。

**Q5 客人問:可以用 LINE Pay 嗎?**
> 「目前**只有信用卡與 ATM 轉帳**。」
🔴🔴 **而商品頁現在寫的是三種(含 LINE Pay)** —— `ProductFAQ.tsx:36` 逐字
　 「可用 **銀行轉帳、線上刷卡、LINE Pay** 支付」。
🔬 而 `line_pay` 在碼裡有值域(6 支檔命中), **沒有一支是結帳 UI**;
　 結帳頁 `CheckoutStep2.tsx` 實測只有一顆 `信用卡付款`, 而且 `checked readOnly`。
⇒ 🎯 **⇒ 客人現在讀到的是假承諾, 而那一行同時餵 JSON-LD ⇒ 已經在被 Google 讀。**
⇒ 🛑 **這一格【不是客服稿能解的】** —— 它是文案要改, 而文案歸 Sean。已端主視窗。

**Q6 客人問:我匯錯金額了(多匯 / 少匯)怎麼辦?**

🔴 **這一格分兩個世界, 而客人自己分不出來 —— 客服要先問金額。**

**⑴ 少匯(收到的錢 < 應付)**
> 「我們收到了 NT$X,而這張訂單是 NT$Y。**請補匯差額 NT$(Y−X)**,補齊後我們會把訂單改成已付款。」
🔬 系統實際行為:`verdict = underpaid` 且已收 > 0 ⇒ 狀態變 **`partiallyPaid`(部分付款)**。
🔵 安全:**沒有承諾自動** —— 補匯之後仍要員工再登記一次。稿子照實說「我們會」不說「系統會」。

**⑵ 多匯(收到的錢 > 應付)**
> 「我們收到了 NT$X,超過這張訂單的 NT$Y。**多的 NT$(X−Y) 我們會退回給您**,
> 　 而**這張訂單目前還會顯示「未付款」** —— 那是**我們的系統對「多匯」這種情況沒有對應的狀態**,
> 　 不是您沒匯到。
> 　 請不要再匯一次。」
🔴🔴 **最後那句是這一整格存在的理由。** 三件全是量到的:
```
🔬 溢收 ⇒ verdict = overpaid ⇒ 20260904230000 【刻意不翻狀態】
   (理由寫在碼裡:payment_status 的五個值裡【沒有】對應「溢收」的值 ⇒ 不猜一個最接近的)
🔬 而那張單【不會被取消】—— 淨額腿涵蓋 overpaid / needs_human 兩種
🔬 而客人的訂單頁顯示條件逐字:
   !cancelled && paymentChannel === 'bank_transfer' && paymentStatus === 'unpaid'
   (OrderDetailView.tsx:598)
⇒ 🎯 **⇒ 一個【多匯了錢】的客人, 打開訂單頁看到的是:銀行帳號 + 全額 + 請於 5 天內匯款。**
⇒ ⇒ 🔴 **他會再匯一次。而這一次是第三次。**
```
🛑 **⇒ 所以「請不要再匯一次」不是客氣話, 它是這一格唯一擋得住第三次匯款的東西。**

**⑵b 已經顯示「付款完成」了, 而他又匯了一次**(⟦b4-PAIDTHENOVERPAID⟧, 2026-09-05 補)
> 「我們收到您兩次匯款,這張訂單的款項第一次就已經收齊了。
> 　 **多的 NT$Z 我們會退回給您**,請提供匯款帳戶(戶名／銀行／帳號)。
> 　 您的訂單狀態是正常的,**不需要再做任何事**。」

🔴 **這一格與 ⑵ 【不是同一句話】, 而客服很容易講成同一句。** 差別逐條:
```
⑵   第一次就匯多了      ⇒ 狀態停在 unpaid ⇒ 🔴 他的畫面【還在叫他匯款】
                          ⇒ 最重要的一句是「請不要再匯一次」
⑵b  第一次剛好, 第二次多匯 ⇒ 狀態已是 paid  ⇒ 🔵 他的畫面【完全正常】
                          ⇒ 「不要再匯」對他沒有意義, 他也沒有理由再匯
                          ⇒ 🔴 最重要的一句是【我們欠他錢】, 而他不會知道
```
🛑 **⇒ 對 ⑵b 的客人講「請不要再匯一次」會讓他以為自己做錯了什麼** ——
　 而他什麼都沒做錯, **是我們多收了。**

🔬 **系統實際行為(量到的, 不是推的)**:第二筆收款進來 ⇒ `verdict = overpaid`
　 ⇒ `20260904230000` 對 `overpaid` **刻意不翻狀態** ⇒ 停在 `paid`。
　 ⇒ 🔵 而 `partiallyPaid` 那條路同型(先短匯 ⇒ 補匯補過頭 ⇒ 停在 `partiallyPaid`)。

⚠️ **這一格今天靠什麼被發現**:`get_stuck_bank_orders_health` 的**第二個世界**
　 (那支 RPC 貼進正式庫之後, 每輪 cron 會把它算進告警信的【匯款單多收了錢】那一段)。
　 🛑 **而那個數字可能少報** —— 它用訂單總額當預篩, 而**有退款的單那個總額不會跟著變**
　 ⇒ 板列 ⟦b4-PAIDTHENOVERPAID⟧。**所以客人自己打來時, 不要因為告警信沒列到就說「系統顯示正常」。**

**⑶ 而有一種情況客服要直接轉給工程**
> (客人只會說「我匯了但狀態怪怪的」—— 這一格是給客服判的, 不是給客人的話術)
🔬 `verdict = needs_human`:OP6a 的**七條前提任一不成立**就會回它
　 (狀態值域 / 取消 / 品項快照 / 收款列形狀 / 帳本覆蓋 / 退款四面 / 金額範圍)。
⇒ 🔴 **它與「多匯」在客人那端【長得一模一樣】** —— 都是「錢進去了而狀態沒變」。
⇒ 📌 **客服的判別法不是問客人, 是看金額**:
```
收到 < 應付  ⇒ 少匯   ⇒ 走 ⑴
收到 = 應付  ⇒ 而狀態沒變 ⇒ 🔴 那不是金額問題 ⇒ 走 ⑶
收到 > 應付  ⇒ 多匯   ⇒ 走 ⑵
```

### ✅ C7 那把尺的自檢結果(逐句跑過, 而它改掉了一句)

> **判別句(取自 `~/pcm-mailbox/C-eb-C7-客服稿自測-20260821.md`)**:
> **「這句話有沒有承諾一個我們做不到的動作?」**

Q6 裡帶承諾動詞的有三句, 逐句查:
```
① 「補齊後我們會把訂單改成已付款」
   🔬 admin_record_manual_payment 存在(2 支 migration 建它)⇒ 動作存在 ⇒ ✅ 過
② 「多的我們會退回給您」
   🔬 admin_record_manual_refund 存在(最新代 20260823020000)⇒ 動作存在 ⇒ ✅ 過
   🔵 而稿子刻意**不給時間** —— 退款是人工的, 而沒有人量過實際要多久
③ ⛔ ~~「那是我們這邊還在處理」~~ **🔴 這句沒過, 已改。**
   🔬 因為我當場量到:後台**沒有任何一個面在顯示「這張需要人看」**
      (`grep -rl needs_human apps/admin/src` ⇒ 2 支, 而兩支都是物流 hct, 與本件無關)
   ⇒ 🎯 **「我們這邊還在處理」暗示【有人正在看它】—— 而沒有人知道它存在。**
      ⇒ 📌 **那正是 C7 抓到的那個病:承諾一個做不到的動作, 而它是善意的。**
   ✅ 改成陳述事實:「**我們的系統對『多匯』這種情況沒有對應的狀態**」——
      🔵 那句話為真、可查證(payment_status 的五個值裡確實沒有), 而**它不承諾任何人正在做事**。
```
🔵 **⇒ C7 那把尺在這份稿上抓到 1 句 / 3 句。** 而**那一句是我寫的時候覺得最體貼的一句。**

### 🛑 而這一格【證不到什麼】—— 寫在這裡, 因為它是這份稿最弱的一節
```
🔴 **後台沒有任何一個面在顯示「這張需要人看」** ——
   🔬 實測:`grep -rl needs_human apps/admin/src` ⇒ 2 支, 而**兩支都是物流(hct)**, 與這件事無關
   ⇒ 📌 **⇒ 一張 needs_human 的單, 現在【沒有人會知道它存在】** ——
     除非客人自己打電話來。而 ⑶ 那一格假設「客服會遇到他」。
   ⇒ 🔴 **⇒ 那是一個【真的缺口】, 不是稿子寫得不夠好。** 已端主視窗。
· ⑴⑵ 的金額算式(X / Y / 差額)客服要自己算 —— 稿子沒有給他一個查得到的地方
· 「多的我們會退回」⇒ 退款是人工的, 而**沒有量過實際要多久** ⇒ 稿子刻意不給時間
```

### ⚠️ 這份稿【證不到什麼】
```
· 五句都沒有真人講過 —— 沒有做過口語順不順的測試
· Q3 的「一個工作天」是我寫的, **不是量到的**(沒有人量過登記收款的實際延遲)
· 沒有涵蓋「客人匯錯金額」(多匯 / 少匯)——
  🔴 而那正是 20260904230000 裡 `overpaid` / `needs_human` 兩種 verdict 的世界,
    它們【刻意不翻狀態】⇒ 客服會遇到「錢收了而狀態沒變」而稿子沒有答案。
  ⇒ 📌 **那是下一份稿要補的第一格。**
```

---

## 這張表整體證不到什麼
```
· ①③ 的答案不在寫表的人手上(見各節「誰給」)
· ④ 從來沒有跑過 —— 它是【翻開之後】的步驟, 不是紀錄
· 本表沒有涵蓋「翻開之後要怎麼關回去」 —— 回滾路徑未寫
```
