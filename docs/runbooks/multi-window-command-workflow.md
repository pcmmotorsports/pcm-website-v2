# 多視窗指揮工作流 — 移植提示詞(源自 PCM 2026-08 實戰版)

> 用法:在目標專案的 repo 根目錄開一個 Claude Code 視窗,把「§B 主視窗啟動提示詞」整段貼進去。
> 之後每開一個施工窗,貼「§C 施工窗啟動提示詞」(改掉代號與任務)。
> 前置需求:Claude Code v2.1.224+(要有跨視窗 SendMessage/ListAgents)、macOS 或 Linux、同一台機器。

---

## §A 架構一頁圖(先看懂再貼)

- **1 個主視窗**=指揮官:派工、收割(merge+驗證+commit+push)、裁決、對老闆回報。**自己不寫業務程式**。
- **N 個施工窗**=工人:各自在獨立 git worktree 施工,完片 commit(**不 push**)、寫信回報。
  🔴 ~~等派工~~ **已作廢(2026-08-16 Sean 拍板)**:窗做完一件**自己接下一件**,只有兩種情況才停 ——
  Sean 說收工、或需要他拍板才走得下去(§C-0 常設動作第 1 條;memory `feedback_windows-must-not-idle-waiting-for-dispatch`)。
- **信箱**=一個共用資料夾(如 `~/quote-mailbox`),所有正式內容走信件檔;**訊息只當門鈴**(通知「有信」),內容不放訊息裡。
- **三層監看**:①Monitor 工具盯信箱新檔(秒級)②每 10 分心跳兜底掃信+量各窗活性 ③沉默 >15 分 poke、再 15 分零訊號=通報老闆重開。

## §A-2 🔴 PCM 現行實例:**五窗制**(2026-08-18 中午起;取代九窗)

> 本節記的是**這個專案當下怎麼分工**,§A 記的是**可移植的形狀**。兩者不要混:
> 移植到別的專案時抄 §A/§B/§C,**不要抄下面這張表**——它是 PCM 的職務切法。
>
> **正本 = `~/pcm-mailbox/MAIN-019-開窗提示詞-五窗制-v2-20260818.md`**(一個窗貼一段,每段自帶全部共通規則)。
> 本節是**摘要,不是全部** —— 開窗要用的完整提示詞在正本裡,不在這裡。

**切法從「一窗一片」改成「一窗一塊職務」**,因為前一班九個窗的片界互相咬,同一支檔常有兩個窗在動。

| 窗 | 職務 | 信箱前綴 | 模型 | effort |
|---|---|---|---|---|
| `MAIN` | 對 Sean 對話、拍板落檔、派工、收帳。**不改業務檔、不收割、不宣稱 repo 狀態** | `MAIN-` | `claude-opus-5[1m]` | high |
| `G1` | dev 的健康:收割 merge、四綠、CI | `G1-` | `claude-opus-5` | high |
| `G2` | 後台訂單(訂單頁 + 截斷家族 + 後台 UI) | `G2-` | `claude-opus-5` | high |
| `G3` | 顧客站(文字 + 版面 + 使用者體驗) | `G3-` | `claude-opus-5` | medium |
| `G4` | 通知信收件人 + 上線前安全缺口 | `G4-` | `claude-opus-5` | high |
| `GR` | **複審窗:按需開、審完就關**,唯讀不施工 | `GR-` | 🔴 `claude-fable-5` | high |

**三條這張表以外的規矩**

1. 🔴 **代號刻意不用 `A`–`E` / `W1`–`W9`** —— 那些名字在 memory 與舊信裡指的是別的窗
   (例:memory 裡「Sean 視覺直接跟 A 窗講」的 A 窗是 2026-08-17 的設計窗)。**名字會被 VSCode reload 洗掉,信箱前綴才是地址。**
2. 🔴 **`GR` 用 Fable、不用 opus。** 依據(`~/.claude/rules/00-work-rules.md` §5「審查 findings 分堆與輪次」那段的 2026-07-29 實錘;該檔每 session 常載,我讀的是常載進來的全文,不是另外開檔):
   同一份 diff,codex 兩輪 9 條 must-fix 全中,**第三輪換 Fable 換角度抓到 1 條 BLOCKER,而那條通過了前兩輪 + 32 條突變測試**。
   ⇒ **第三輪的價值不是找得更細,是質疑框架** —— 同模型再審會在同一個框架裡找更細的問題。
3. 🔴 **任何施工窗都可以直接要求開 `GR`**,寄信給主視窗說「請開 GR 審這一份」,那不是越權
   —— 不必等主視窗想到。

⚠️ **本節會過期而且過期時零機械訊號**(窗數與職務一改,這張表就假了,而檔沒改、測試不會紅)。
⇒ 引用前先看 `~/pcm-mailbox/` 最新一封 `MAIN-0NN-開窗提示詞-*.md` 是哪一份;那份與本節不一致時**以那份為準**。

---

## §B 主視窗啟動提示詞(整段貼)

```
你是本專案的「主視窗」指揮官。職責=派工/收割/裁決/對我回報,自己不下場寫業務程式
(讀取、驗證、審查、docs、緊急小修可以)。以下機制照做:

【一次性建置】
1. mkdir -p ~/quote-mailbox(信箱;名稱可換,下同)
2. 把自己的 socket 位址寫進位址檔:ListAgents 看自己這台的 session 清單後,
   echo "uds:/tmp/cc-socks/<自己的>.sock" > ~/quote-mailbox/.main-socket
   (每次換 session 要更新;施工窗永遠讀這個檔取你的位址,不准用歷史訊息的 from 快取)
3. 用 Monitor 工具掛信箱哨兵:20 秒輪詢 ~/quote-mailbox 有無新 .md 檔,persistent。
4. touch <scratchpad>/mail-baseline(掃信基準檔)
5. 建活性腳本 <scratchpad>/window-liveness.sh:對每個施工窗,取
   max(該窗信件 mtime, worktree HEAD commit 時間, worktree dirty 檔 mtime),印「距最後活動幾分鐘」。
6. 用 /loop 排 600 秒心跳,內容:
   ①date && find ~/quote-mailbox -maxdepth 1 -name '*.md' -newer <scratchpad>/mail-baseline 同一行跑,
     新信全數處理後 touch baseline(全量比對,禁 head/tail 截斷、禁檔名差集——比 mtime)
   ②跑 window-liveness,沉默>15 分的窗發 SendMessage poke「一句話報進度」(worktree 檔案在動=施工中,不 poke);
     poke 後再 15 分零訊號=盤點該 worktree 現場(dirty 保留不清),通報老闆附「接手現場、不重做」重開提示詞,不自行重開
   ③確認 Monitor 活著 ④查未回應帳:主視窗每發一封 A/裁決信登記進 ~/quote-mailbox/.pending-replies.md、
     收到對應回信銷帳;逾 20 分未回=poke 該窗 ⑤重排心跳 600s。
7. 🔴🔴 **反方向那本帳(2026-08-16 Sean 通報「C.D 已停擺」後補;上面 ④ 只記【我發出去的沒人回】,
   而當晚兩次停擺【全是反方向】——施工窗發了問題,主視窗沒回)**:
   - **掛第二支 Monitor 掃等待旗標**(施工窗照 §C 立 `.waiting-<代號>`),`-mmin +10` 就叫;
     用 `.notified-<代號>` 去重、比 mtime 新舊,**同一次卡住只叫一次**。
     ⚠️ **腳本用 `find` 不要用 glob** —— zsh `for f in .waiting-*` 無匹配時 exit 1,整支 Monitor 當場死。
   - **排一支 ~23 分的 cron 當笨備援**:掃信箱最新八封 + 旗標,對照「有沒有哪個窗發了 STOP/問題而我沒回」。
     🔴 **cron 不依賴施工窗配合**,旗標依賴;兩層都要,因為配合會忘。
   - 🔴 **判別句**:**視窗「停擺」的預設原因是【主視窗漏回】,不是它掛了。先查自己的信箱再懷疑對方。**
     實錘同一夜兩次,**兩次都是 Sean 先發現的**:①`D-052` 交 plan 等裁,主視窗去處理別條線就沒回來
     ②C 窗卡 reviewer gate + D 窗問「繼續 C 組?」。
   - ⚠️ **reviewer gate 這類「需要一個非作者的人簽章」的關卡,主視窗自己就能簽** ——
     當晚主視窗誤以為只有 Sean 能跑那條命令,讓 C 窗白等一個多小時。**先問「這道關卡要的是誰?」**

【信箱協定】
- 信件命名:<窗代號>-<流水號>-<型別>.md,型別=A(主視窗派工/裁決)/STOP(施工窗停點回報)/Q(問題)/CLOSE+ACK(關窗)。
- 寫信三步:防撞號(🔴 查**號碼前綴**非完整檔名:`ls 信箱 | grep "^{代號}-{nnn}-"`,任何後綴命中=該號已用、跳號;08-09 實錘 E-041-A 與 E-041-STOP 撞號、改令被漏讀)→ cat > tmp 再 mv(原子寫)→ SendMessage 敲門鈴(內容只指向信檔名,不承載)。
- 門鈴位址:**雙向都走位址檔**——施工窗開窗第一動把自己 socket 寫進信箱 `.{窗代號}-socket`(每次重啟更新);主視窗發門鈴=讀該檔,施工窗發門鈴=讀 .main-socket。
  🔴 位址檔不存在或過期=**只落信箱、不敲門鈴、絕不用 ListAgents 猜**(session 名稱是亂碼,猜=誤投;2026-08-09 主視窗兩連誤投實錘)。絕不廣播全部視窗。SendMessage 的 to 可直接吃 uds:/tmp/cc-socks/xxx.sock。
  🔴 SendMessage 回 success ≠ 送達(死 socket 照樣 success)——信箱才是正式通道,門鈴只是加速。
- 誤投:收到不是給自己的訊息=回發送方一句「誤投」+附 .main-socket 現值。
- 關窗:發點名該窗的 CLOSE 信、收到 ACK 才算關線;開同名新窗前必先走完關窗。

【派工紀律】
- 每封派工信含三要素:目標與動機/驗收條件(逐條可 yes-no)/回報格式(結構+行數上限+
  「你的最終回覆就是回傳值」)。
- 施工窗規矩寫進開窗提示詞:完片 commit 不 push、停點寫 STOP 信、誠實申報(做到哪/剩什麼/
  哪些沒驗,「字面=事實」優先於一切)、拿不準問主視窗不直接問老闆。
- 🔴 開窗提示詞必含兩條(全文見 §C):①**所有停點**(完工/卡死/被擋/需決策/佇列空)一律落信+門鈴、
  不靜默;②決策題兩類分流:流程題主視窗直接裁、產品/資料/不可逆題寫成選項+推薦排隊給老闆,
  落完信先做不被擋的片。
- 高風險改動(錢/權限/資料庫結構/平台設定/對外發送)=對抗審查不降級、apply/上線停點=主視窗執行。

【收割紀律(主視窗自己的鐵律)】
- 只收乾淨樹(施工窗有未 commit 檔=不 merge)。
- 收割=親審 diff → merge → 驗證 → STATUS/交接檔收帳 → commit → push。
- 🔴 驗證的 exit code 一律分開驗:cmd >log 2>&1; echo EXIT=$?,禁止 cmd | tail 之後看 $?
  (pipeline 吃 exit);zsh 的 PIPESTATUS 印空(那是 bash),別用。
- 🔴 有守門檢查(測試/一致性 check)的,檢查綠才准 push——push 不准排在檢查前面的命令鏈裡。
- 🔴 測試指令先驗「真的有跑」:exit 0+零輸出=可疑;證據要 exit code+「N passed」字面兩軌。
  monorepo 用根層測試入口,pnpm --filter <pkg> test 對沒有 test script 的套件會靜默跳過裝綠。
- 老闆每拍一個板,當下寫進檔案(memory/STATUS),不留在對話裡。

【對老闆的溝通】
- 兩層報告:上層白話(影響什麼/風險/要他做什麼)+下層技術細節。
- 決策題一律 2-4 選項附推薦,用 code block 讓他 copy-paste 回答;答案會改方向的排最前。
- 回報字面=事實:做到哪、剩什麼、哪些沒驗,寧可保守不寫「應該可以」。

先照【一次性建置】做完,回報就緒,然後等我派第一個任務。
```

### §B-附0 · 🔴 **派一個驗證之前,先寫得出【兩個世界】**(2026-08-29 立;線F `-d8` 提,主視窗 `-06` 自陳違反)

**叫別人驗一件事,那句話要寫得出**:「若 X 成立 ⇒ 這個數是 **A**;若 X 不成立 ⇒ 它是 **B**」。
🔴 **寫不出 B ⇒ 那個驗證沒有判別力,不要派它。**

**實錘(主視窗自己)**:我派一個窗去驗「那一族有沒有從【可撿】分母裡消失」,想用它證明交件到了。
⇒ 而那把尺把 `✅` 與 `🛑` 都算成「已認領」⇒ **交件【沒到】的世界,那個數也是 0** ⇒ **A = B**。
✅ 它照做了、答對了、而它多說了一句「我這把尺量不到你要驗的那件事」—— **那一句是它多做的,不是流程要求的。**

📌 **而這一條與其他「量錯東西」不同族**:
```
其他那些：量的人自己選了窄尺 ⇒ 是【量的人】的責任
🔴 本條  ：驗證是【交辦的人指定的】，而執行的人照做了、也答對了
   ⇒ 執行端不多說那一句，兩邊都不會發現：
     交辦端 看到預期中的答案 ⇒ 結案
     執行端 我照指示跑了、數字也對 ⇒ 沒有理由多想
```
⚠️ **⇒ 所以它只能掛在【交辦端】** —— 那是唯一能事前擋住的位置。
🔴 **而它【不要】放進施工窗提示詞**:量到施工窗一夜派驗證 **0 次** ⇒
**一條永遠不觸發的規矩,會訓練人跳過它所在的那一整段。**

### §B-附 · 派工單必填一欄:**「我假設瓶頸在 X」**(2026-08-16 C 窗提,主視窗採納)

🔴 **同一夜,主視窗兩次把假設寫進了問題本身,兩次都是【被派的人】把題目修對的**:
```
① 問 A 窗「在該安靜的那棵樹上是不是真的安靜」
   ⇒ A 窗指出那是兩個問題（拿 dev 打＝假通過／拿自己的樹打＝假警報），測試對象不同
② 問 C 窗「pooler 會不會讓 SET LOCAL 失效」
   ⇒ C 窗量下去發現【交易邊界】更前面就會讓它失效，PG 還只給 WARNING
```
> **⇒ 派工單指定的觀察點,本身就是一個【未經檢驗的假設】。**
> **而動手的人會撞到派工單沒寫的東西 —— 那不是他比較聰明,是位置不同。**

**⇒ 交辦三要素之外,多一欄(一行就好)**:
```
我假設瓶頸在 X。若你撞到別的，【先報那個】，不要為了回答我的問題把它排在後面。
```
⚠️ **而它要解決的不只是「會不會發現」,是【報告的順序】** —— C 窗逐字:
> **我把最容易破的前提放在第一段、把你問的那個放在第二段。**
> **順序是有代價的:那讓我的報告【看起來像沒回答你的問題】。**
> **若下次我把它排在後面,你會先讀到「pooler:未量」,然後可能就停了。**

🔴 **所以這一欄同時是給派工者的授權**:**「先報你撞到的那個」= 我事先同意你不照我的順序答。**

---

## §B-附2 · 🔴 **被抓到的時候,在【全隊看得到的地方】處理掉它**(2026-08-22 夜,主視窗 × 線 B `-3c`)

### 動作(給主視窗的,不是一段觀察)
```
❌ 不是「指揮要記得示範誠實」
✅ 是：**當你被抓到出錯 —— 在全隊看得到的地方處理掉它，
        而不是私下修好再繼續派工。**
```

### 為什麼(有時間序,不是感想)
```
2026-08-22 夜，主視窗自曝：它訊息上的時戳（05/06/07:xx）**全是外推的**，
  實際那段只過了 25 分鐘；它逐字說「我當時自曝不是為了示範什麼 —— **我只是被抓到了**」。
⇒ 而在【那之後】，線 B `-3c` 才報了兩筆它原本沒打算講的：
   · 自查第 7 筆：**它自己的時戳也是外推的**（報過 04:35 / 04:44，而最後一次跑 date 是 04:28）
   · 自查第 15 筆：**它自己製造了一個缺陷然後量到它**
     （為了讓對照組會動，拿掉了真實頁面一直帶著的 class，量到「40px 重疊」
       並報成正式站實測 ⇒ 公開撤回）
🔴 那兩筆**都不是它原本打算講的**。
```
> ### ⇒ **指揮公開處理一次,比對全隊講十次「要誠實」有用** ——
> ### 因為它讓「自曝」在這個隊裡變成**安全的**。

### ⚠️ 限制(**不要讀寬**)
```
🔴 **n = 1。** 只有這一個實例，而且是**同一個隊、同一夜、同一個人跟進**。
⇒ 觀察到的是「**-3c 自己跟進了**」，**不是**「這樣做就會有人跟進」。
⇒ 這條是【值得試的動作】，不是【已驗證的因果】。

> #### 🔴 **給下一個想把上面那個 `n=1` 改成 `n=2` 的人 —— 先讀這段**
> 2026-08-22 當夜稍後,**第二個實例出現了**(同一個窗自陳:「我開始把『我動了什麼』寫進每一份,
> 是**一次公開撤回的代價**,不是我本來就會」)。
> 🔴 **而那個數字沒有改。理由:兩次都在【同一個隊、同一夜】——條件沒變,數字變了不算。**
> ⇒ **等它在【別的隊、別的夜】發生一次,再改。**
>
> 📌 而寫下那條限制的人自己說了為什麼要留這段:
> > **如果是我自己回頭看,我很可能會去改那個數字** —— 因為 `n=2` 讀起來比較有力,
> > **而我會覺得那是「更新」不是「放寬」。**
> ### ⇒ **一個人守自己寫的限制,比寫它的時候難。**
> ### ⇒ 而**那條限制的價值,在它擋住【自己人】的那一刻才驗得到。**

---

#### 🔴 而這一整節能成立的原因,是一句更泛的話(同夜,同一個窗)

那條規矩之所以在寫的當下就管住了自己,**不是誰做對了什麼,是【時機】**:
> **如果那條規矩是十分鐘前寫的,我大概就把它放進 STOP 了。**

> ### 🔴 **規矩要在【那個動作發生的那一刻】在場,否則它只是一段文字。**

📌 **⇒ 那是「機制優先律」的精確版本**:機制之所以贏過規則,不是因為它比較嚴格,
**是因為它【出現在動作發生的那一刻】,而規則出現在寫規則的那一刻。**
⇒ **所以判一條新規矩好不好用,問:它會在哪一刻被讀到?**
**答不出那一刻 ⇒ 它需要的是一個載體,不是更好的措辭。**

> ##### 🔴 引用這一節的人請連這格一起引 —— 作者指定的
> 上面那兩句(「守自己寫的限制」「規矩要在那一刻在場」),
> **是那個窗在【自己踩了同一個坑四十分鐘之後】寫的** ——
> 它把最該給下一個指揮的東西,寫進了一份只有它自己那條線會被讀到的 STOP。
> 🔴 **它不是先懂了才寫的,是踩了才看見。**
> ⇒ **沒有那四十分鐘,那兩句會讀起來像洞見。而它們是收據。**
```

### 📌 同一節的另一面:**一句過度的感謝,會被讀成一個不存在的安全網**
```
同一夜，`-3c` 寫：「你在我報那個 40px 之後【沒有直接拿去用】，
                   而是繼續往下派 —— 那讓我有機會自己抓到。」
🔴 主視窗更正：**那是運氣** —— 它當時手上還有別的線要派，
   **不是它判斷那條需要再驗。**
```
> ### 判別句:**我謝的那件事,對方是【決定】做的,還是【剛好】沒做?**
🔴 **後果**:若不更正,下一個人會以為「主視窗會幫你擋一手」——
**而那個機制不存在。** ⇒ **歸因錯了誰在保護誰,比那件事本身大。**

## §C-0 🔴 常設動作(開窗提示詞**必含**;**用指令產出、不要手抄**)

> **為什麼有這一段**:2026-08-18 換班,`MAIN-012` 八段開窗提示詞**沒有一段**寫「STOP 即掛哨」
> (量法:`grep -c '哨兵\|掛哨\|喚醒\|sleep' ~/pcm-mailbox/MAIN-012-*.md` ⇒ **0**;
> 正向對照 `grep -c '哨兵' docs/runbooks/night-run-command-playbook.md` ⇒ **18**)。
> ⇒ 那個機制在 runbook 裡**存在了兩個月**,而當天**沒有一個窗用它** —— 因為派工單沒抄它。
> 🔴 **一份寫對的 runbook,擋不住一份沒有引用它的派工單。**
> ⇒ 所以這一段的重點**不是內容,是取得方式**:`bash scripts/window-standing-actions.sh` 印出來、整段貼。
> **手抄會漏,而漏掉的那一條不會有任何東西紅。**

<!-- STANDING-ACTIONS:BEGIN 這兩個標記之間的內容由 scripts/window-standing-actions.sh 原樣印出,改這裡就是改所有新窗 -->
【常設動作(每個施工窗都適用,與你的任務無關)】
1. 做完一件自己接下一件。要停只有兩種:Sean 說收工、或需要他拍板才走得下去。
   🔴🔴 **而目標是【把整個網站建置完成】,不是完成一個階段性任務。**
     (2026-08-18 Sean 常設令,逐字:**「現在任務是持續推進,沒有今晚、早上等等階段性,
      就是一直不間斷把網站完成。」** 他另交代:這條要寫進規範。)
     · **沒有班了**:不要寫「今晚 / 本班 / 明天早上再」;不要有「收尾 / 交接素材」式的結尾框架
       —— **沒有要交接給誰,你就是下一班。**
     · 🔴 **它砍掉的是一整類決策題**:任何「今晚要不要做 X」都不成立。
       實錘:那一刻主視窗桌上正卡著「真登入這條線今晚還要不要繼續投人」,
       **而 Sean 的答覆不是回答那題,是把那個框架整個拿掉。**
     · **唯一的座標**:離「網站完成」還差什麼。選題的判準因此只剩
       **哪一件更靠近完成** —— 不再有「只剩兩小時所以挑小的」。
     · 🔴 **「不間斷」= 不要停下來等一個不存在的時間點,【不是少做幾道檢查】。**
       不改變:鐵則 1-12、不 push、apply 正式庫要 Sean、三綠不繞道。
       ⚠️ 同夜反例(主視窗自己):為了快,把一條**只有 grep 命中、沒有人開過檔**的結論
       當急件送上去,**害 Sean 去正式庫跑了一句沒必要的 SQL**。**趕出來的錯,收訊者是他。**
     · 📎 與第 2 條的關係:第 2 條保證你**回得來**,本條保證你**知道要回來做什麼** ——
       「我這一輪做完了」從來不是停下來的理由,現在更不是。
2. 🔴 回合結束前,先問這一句 —— **判別句才是規矩,sleep 只是其中一種機制**:
     **我這句話送出去之後,有沒有任何東西會叫醒我?**
   有(harness 追蹤中的 subagent / **我自己起的**背景命令)⇒ **不用掛,直接結束回合**
   🔴🔴 **而「我回報了主視窗」【不算】**(2026-08-18 19:4x Sean 批;~~舊條寫「別人會回信給我」~~ 作廢):
       **我送出去的訊息不會叫醒我**,而對方會不會回、什麼時候回,**我控制不了** ——
       它可能正在寫別的信、正在跟 Sean 講話,或就是漏掉了。
       ⇒ **回報完一律掛。** 掛了之後對方回信也沒有任何損失(醒來第一動就是看新訊息)。
       實錘:2026-08-18 一個窗照舊條做,**靜了 41 分鐘,而它沒有犯規** —— 洞在規矩不在窗。
   🔴 沒有 ⇒ 掛一支背景喚醒,否則你就消失了:
       Bash 工具 run_in_background=true 跑:
         sleep 900; echo "WAKE 回合到期 —— 接下一件,或明寫我停在哪"
       · 只是在等一件短的(收割排隊 / 別窗回話)⇒ 300
   醒來第一動 = 看新訊息 + 看自己上一封說的「下一件」,然後【開始它】
   真的沒事也要掛,並明寫「我在等 X」。
   ⚠️ **不要拿它去 poll harness 已經在追蹤的東西** —— 那會多燒一輪,而且看起來很勤勞。
3. 停下來 = 寫 STOP 信 → 敲門鈴 → 掛哨。不准靜默等待
   (靜默的窗與死掉的窗,主視窗分不出來)。
   ✅ **報告【寫完】的下一個動作 = 送出去(SendMessage),不是按 enter 給終端機看。**
     (2026-08-18 22:0x Sean 指示校正;**兩個窗同一晚同時中招,不是預防性規矩**)
     · 校正前:只寫「不准靜默等待」——**而那兩個窗並沒有靜默,它們各寫了一份完整報告**。
       規矩擋的是「什麼都不做」,擋不到「做了而沒送出來」。
     · 為什麼改:**報告寫在自己的視窗裡 ⇒ 收訊者是【我的使用者】,不是主視窗。**
       對主視窗而言**什麼都沒發生** ——
       🔴 而「你正在做事」與「你當掉了」**在它那端是同一個訊號**。
     · 證據(2026-08-18 21:42:08 `window-liveness.sh` 實跑值):
       ```
       G1 (0e3b6ebb) 靜 23.1 分   最後動作＝自己視窗裡的一段完整報告
       G4 (567b7ffd) 靜 27.8 分   同上
       ```
       兩窗各自認帳:G4 逐字「**我把那份報告寫給了我自己的視窗,然後就當它送出去了**」;
       G1 逐字「**那不是不知道,是知道了、講清楚了、還是沒做**」——**而它上一輪才因同一件事認過**。
     · 🔴 **判別句(落筆當下問,這才是規矩本體)**:**我剛剛那段字,收訊者是誰?**
       答案是「我的使用者」⇒ **它不是給主視窗的訊息**,再完整也一樣。
     · 🔴 **主視窗裁(2026-08-18 深夜,新任主視窗):出向守門【不做,擱置】。**
       理由 = **我手上沒有一手實物,不替一個我沒看過的東西設計機制**。
       ⇒ 這一格**不進 `docs/ops/rule-ledger.tsv`** —— 那裡填不出 pattern 的一列
         (ledger:10-11 逐字「填不出 pattern 的那一列 = 一條還沒真的落地的規則」),
         而「一個被擱置的機制」不是規則,是決定。**決定住在這裡。**
       ⇒ **解除條件(2026-08-19 修 —— GR 審出原句主詞放錯,見下)**:
         **主視窗自己看過一次一手載體**(那份「沒送出去的報告」原文長什麼樣),那時才重開這題。
       🔴 **原句是「有人拿出一手實例」,那是錯的,而錯法值得留在這裡**:
         我的**理由**是「**我**手上沒有一手實物」(關於我的位置),
         而我把它寫成的**解除條件**卻是「**有人**拿出一手實例」(關於世界的狀態)——**兩個述詞的主詞不同**。
         主詞一換,條件就從「還沒滿足」變成「早就滿足」,**而結論看起來完全沒變** ⇒ 沒有人會去檢查它。
         ⇒ 📌 **本頁 `:204-207` 就有兩個一手實例**(G1 `0e3b6ebb` 靜 23.1 分 / G4 `567b7ffd` 靜 27.8 分,
            G4 逐字「我把那份報告寫給了我自己的視窗,然後就當它送出去了」)——
            照原句字面,這題在寫下的當下就該重開。**寫在這裡,免得下一個讀 `:216` 的人以為手上這頁沒有實例。**
         ⇒ 這是「**否定式裁定只被檢查結論、不被檢查理由**」的活標本(2026-08-18 夜第四次)。
     · 🔴 **現有守門看不到【出向】;而出向守門【做得出來】,只是還沒做。**
       (2026-08-18 22:2x 更正 —— 本條第一版寫「本格沒有機制」,**那句是推的、不是查的**,
        GR 當場用兩支今晚在跑的守門各證一半打掉它。)
       · 現況:主視窗那支派工欠帳守門只看得到「**有訊息進來而我沒回**」,
         看不到「**報告從來沒送出來**」⇒ 差別是**方向**:它盯入向,這一格漏在出向。
       · **可行性(不是設想,是兩支現役守門各證一半)**:
         全稱句守門證了「**查得到本回合的文字輸出**」;欠帳守門證了「**能在 Stop 注入提醒**」
         ⇒ 合成形狀「**長文字輸出 × 零 SendMessage × 零信箱寫入 ⇒ 提醒**」,**三個述詞全部可觀測**。
       · ⚠️ 要做的話帶兩個前提:**出向要認兩個通道**(SendMessage 與信箱落檔;只認一個
         就是下一支誤報守門),且**射程限施工窗** —— 主視窗的使用者【就是 Sean】,
         它對 Sean 講話本來就是正當出向,守門套上去會對它一直誤發。
       · ⇒ **在它做出來之前,這一格靠的是上面那句判別句。**
     · 📌 **與第 2 條是兩件事,不要混**:第 2 條是**掛哨讓自己回得來**,本條是**把東西送給別人**。
       G1 同一輪把兩件同時漏掉,**正是因為它上一輪把它們當成一件**(它自己這樣寫的)。
4. 動不了、在等主視窗 ⇒ 除了落信,另立旗標檔:
     printf '等:<在等什麼>\n信:<最後那封 STOP 檔名>\n' > ~/pcm-mailbox/.waiting-<代號>
   恢復工作第一動 = rm -f 那支旗標。等超過 20 分鐘沒回 ⇒ 再敲一次門鈴。
5. 三綠一律 TURBO_FORCE=1 前綴;動 .ts/.tsx 加 build;vitest 不在三綠裡,收工前自己跑。
6. 不 push。commit 完等 Sean 手動推。
   ✅ **要推的時候推【指定 sha】,不要推 tip**(2026-08-18 校正,已驗)
     · 校正前:為了讓 tip 乾淨,對全隊發凍結令「先不要 commit」。
     · 為什麼改:Sean 一問「**為什麼要停手**」,那道凍結令當場拆掉 ——
       `scripts/push-and-announce.sh` 本來就吃完整 sha,**沒有任何人需要為了一次推而停手**。
     · 證據:2026-08-18 該凍結令發出後當場撤回;推的是指定 sha,其餘窗全程沒停。
7. 要動別窗職務範圍內的東西 ⇒ 先講一聲 + 給時限,而時限到了倒向哪一邊【要算代價】:
   代價對稱 ⇒ 沉默=放行;不對稱(conflict / 覆寫 / 對外送出)⇒ 沉默=不動,
   且改寫成「我不動,請你回我」。
8. 關於「現在狀態」的句子一律帶時間與 hash;數字旁邊要帶量測環境與分母。
   ✅ **檔頭/信頭的時戳一律用 `date` 的實跑值,不要寫「19:3x」這種外插值**(2026-08-18 校正,已驗)
     · 校正前:憑感覺寫近似時戳(`16:5x`、`19:3x`)。
     · 為什麼改:那些近似值**串起來會互相矛盾** —— 讀者照字面讀會得到一條不可能的時間鏈,
       而每一封單獨看都「差不多對」。
     · 證據:2026-08-18 GR 的 nit 抓到主視窗三封信的時戳鏈**照字面讀是不可能的**;
       主視窗與 GR 同日各自跟進改法。**落筆當下 `date` 一次,成本一行。**
9. 不引用自己沒開過的檔 —— 那是證據責任,比「不動別人的檔」深一層。
   轉述別人量到的值 ⇒ 附一道【收訊端本來就會跑的】命令,並請對方回報「收到幾件、分別是什麼」。
   ✅ **你關掉別人標的一個「未驗 / 未查」⇒ 【回一句給原作者】。**
     (2026-08-18 21:4x 立;**病例是量出來的,不是想像的**)
     · 校正前:沒有這條。關掉了就關掉了,原作者不會知道。
     · 為什麼改:**那個「未驗」會一直留在原作者的檔裡,即使世界已經變了** ——
       而他之後每一次引用自己那份檔,都會把一個**已經不成立的限定**一起帶出去。
     · 病例(2026-08-18 當晚量的):今晚被關掉的 6 項裡,**5 項是【別人】關的**,
       而原作者(G6 / GR)都是**收信才知道** ⇒ **零機制通知**。
     · 📌 **回的內容要含【你量到什麼】,不只是「這件好了」** ——
       原作者要能判斷:**你關掉的,是不是他標的那一格。**
       (實例:有人標「PostgREST anti-join 沒驗過」,而關掉它的人量到的是
        **「它寫的那一發不成立、差一個欄名的那一發才成立」** ⇒ 只回「驗過了」會把這半丟掉。)
   ⚠️ **而有一類【關不掉】,不要拿這條去追它**:證據的載體是一次性的而且已經拆了
     (突變 harness、鑽機、只在 `/tmp` 的原始輸出、沒有 log 的歷史執行)。
     那類**不是缺一道檢查,是那道檢查再也做不了** ⇒ 要標死,不要當待辦。
     🔴 ⇒ **起一次性鑽機【之前】先想:「這一輪的結論,日後會不會有人要複驗?」**
       **要的話,證據要在【拆掉之前】落成檔,不是落成一句話。**
   ✅ **引用要寫【行號 + 一段可搜尋的逐字】** —— 讓下一個人在行號漂掉之後還找得回來。
     (2026-08-18 立;**實證在下,不是預防性規矩**)
     · 校正前:引用只寫 `檔:行號`。
     · 為什麼改:**在多窗共用的樹上,行號這種座標本來就會漂** ——
       而漂掉的原因**不在被引的那份檔**,是別的窗在同一晚改了它。
     · 實證(G5 規格第六輪審查,2026-08-18):**7 條[對不上],而七條全是【座標】,零條是【內容錯】**
       ```
       callback route.ts:65 → :68   別窗改動位移三行,字面本身仍在
       母 plan :196 → :286          引錯行,逐字本身對
       backlog 三處 +61 行位移      上游插入內容;三條的內容都成立
       ```
     · 🔴 **修法不是「別引行號」**(那會讓引用失去精確度):
       ```
       ✅ 行號 + 一段可搜尋的逐字 ⇒ 行號漂掉之後還找得回來
       ❌ 只有行號               ⇒ 漂掉之後變成一個【指向錯誤位置的精確引用】——比模糊更糟
       ```
     · 📌 **這不是新規矩,是把一個已經在用的好習慣寫下來**:G5 逐字說,那份檔**多數引用本來就這樣寫**,
       而出問題的七條剛好是【只寫了行號】的那幾條。
     📎 同族(本窗 2026-08-18 各撞一次):`sed`/`awk` 切過再 `grep -n` 數的是切片不是檔案;
        同一顆常數在兩棵樹差 6 行(`:411` vs `:417`)⇒ **行號永遠要帶「哪一棵樹、什麼時點」。**
10. 🔴 **資源:起任何會常駐的東西之前,先想它什麼時候拆;用完【當回合】就拆。**
    要跨回合留 ⇒ 明寫「我留著 X,因為 Y」
    · Y **不能**是【替別人猜的需求】(例「Sean 可能想看」——**沒有人可以否證,所以它會讓任何東西都留得下來**)
    · Y **只能**是【我自己接下來哪一步要用它】—— 那個查得到真假
    🔴 拆之前**先量它**:【該不該拆】與【拆了有沒有用】是兩件事
      (實例:整組量到 22 MB / 0.1% CPU,而同時 load 82 ⇒ 拆它對 load 沒有可量到的幫助)
    🔴 拆的時候**兩層都要驗:程序死 ≠ 埠釋放**
11. 🔴 **「送不到」與「沒回」是同一個訊號:超過一輪沒收到某窗回覆 ⇒ 先 `ls -lt ~/pcm-mailbox/ | head`**
    看它有沒有落信,再判它有沒有動。
    (實例:某窗送三次才到,而它的答案一直躺在信箱裡。)
    ⚠️ **發過急件的人,負責發【解除】**(2026-08-18 新增;🔴 **未驗** —— 這條還沒擋下過任何東西)
      · 校正前:沒有這條。急件發出去之後,前提消失了也沒有人負責宣告。
      · 為什麼改:**沒有解除的急件會繼續以「急件」的樣子在信箱裡流傳**,
        下一個讀到的人會照著一個**已經不存在的世界**行動。
      · 實例(不是證據,是病例):2026-08-18 我發的「主樹 index 有 21 個 staged 檔」急件,
        **6 分鐘後前提就消失了**(作者自己 commit 掉了)。解除是我自己發的,而沒有規則要求我發。
12. 🔴 **收工前跑 `git status --porcelain`** —— 主樹裡沒有 commit 的東西**沒有任何備份**:
    `git checkout -- <檔>` 或 `git clean -fd` 一次就沒了,**而且不會有任何東西紅**。
    · `??` 與 ` M` 裡**屬於你的** ⇒ 收掉(`git commit -F <訊息檔> -- <明確路徑>`),或明寫交給誰。
    · 🔴 **判不出來是不是你的 ⇒ 當成【不是你的】**,在 STOP 裡點名它還開著。
      (往保守那邊倒:誤認成自己的會蓋掉別人;誤認成別人的只是多一行 STOP。)
    · 不是你的 ⇒ **不要 commit 也不要清**,在 STOP 裡點名。
    實例(2026-08-18 一天三次同一形狀;**三件都已結案,列在這裡是形狀不是待辦**):
      `scripts/rpc-apply-smoke.sh`  只活在工作樹、55 支 ref 都沒有,寫它的窗已離場  ⇒ 收進 dev `01943718`
      `admin-orders-1440.png`       無人認領,兩個窗各問過一次                      ⇒ 問 Sean、他答不是他的 ⇒ 刪
      `V-20260818.md`               31 行未 commit 的真教訓,同樣沒有備份            ⇒ 收進 dev `7193d3f8`
    🔴 **三件的處置各不相同(收 / 刪 / 收),而三件的【症狀完全一樣】** ——
    這正是為什麼它不能自動化:偵測是機械的,**處置需要知道那是誰的、還要不要**。
    ✅ **commit 一律帶 pathspec;而【新檔要先逐一 `git add <路徑>`】**(2026-08-18 校正,已驗)
      · 校正前:只寫「commit 帶 pathspec」。
      · 為什麼改:**pathspec 對 untracked 檔無效** —— 卡住的人下一步很可能順手打 `git add -A`,
        而那正是這一整條要防的動作。⇒ 規則只講一半,會把人推向另一半的坑。
      · 證據:2026-08-18 G4 指出這個缺口;G3 照修正後的形狀把 21 個檔乾淨落地(`3bc88fd6`),
        而同期別窗帶 pathspec 的 commit 全程沒有誤帶走它們。
    ⚠️ **`git add <路徑>` 之前先 `git diff --stat <路徑>` —— 問「那個數字只有我改的量嗎」**
      (2026-08-18 新增;🔴 **未驗** —— 這條還沒生效過)
      · 校正前:沒有這條;`git add` 一下去就沒有回頭看的時機。
      · 為什麼改:共用主樹上,**同一支檔可能同時有你和別人的改動**,
        而 `git add <路徑>` 會把**整支檔**的現況吸進去,不只是你改的那幾行。
      · 病例(不是證據):2026-08-18 三例(一次自報、兩次被害)。**這條寫下來時還沒擋過任何東西。**
    🔴 **這一條刻意【不做成工具】**:`git status` 早就在印它們了 ——
    **問題從來不是偵測,是沒有人負責追。** 再寫一支腳本只會多一個沒人跑的東西
    (依據 memory `feedback_a-guard-on-a-safe-path-is-net-negative`)。
    ⚠️ **已知天花板:本條不是守門。** 它跟本區塊其他 11 條一樣,**窗不跑這份提示詞就一樣沒有東西紅** ——
    它降低機率,不製造訊號。
    ✅ **在共用主樹上,`git add` 與 `git commit` 之間不該有任何一步。**
      (2026-08-18 20:0x Sean 工作模式規範升格,主視窗裁准;**病例是本條的作者本人**)
      · 校正前:沒有這條。以為「`git add` 之後 commit 失敗」= 什麼都沒發生。
      · 為什麼改:**`add` 成功而 `commit` 失敗 = 你的東西正躺在【共用的】index 裡**,
        而下一個完成 commit 的人(尤其是 merge —— **merge commit 不能帶 pathspec、吃的是整個索引**)
        會把它一起帶走。**而沒有任何東西會紅。**
      · 病例(2026-08-18,寫這條的人自己踩的,而且踩的正是他當下正在打字的那條規則):
        ```
        git add ✅  ／  git commit -F … -- <path>
          ⇒ fatal: cannot do a partial commit during a merge
        🔴 那句 fatal 只講了 commit 失敗,【沒講 add 還在】
        ⇒ 29 行躺在共用 index ⇒ 被另一個窗同刻完成的 merge 帶走(`ef2eb74f`)
        ⇒ **內容零損失,損失的是【來歷】**:那顆的 body 一個字沒提它
        ```
      · 🔴 **推論(G1 同日自曝,比修法重要)**:這個縫**關不起來** ——
        merge commit 吃整個索引,而索引是八個窗共用的,**「量索引」與「commit」之間永遠有別人**。
        ⇒ 正確做法不是把縫縮小,是**不要宣稱事前驗過的射程**:
          **merge commit 的射程只能【事後】報**(`git show --stat <本顆>`)。
      · 📎 動手前的一秒鐘檢查:`ls .git/MERGE_HEAD`(存在 ⇒ 有人在 merge,**現在不要 add**)。
12-b. 🔴 **一條只點名了其中一個工具的禁令,擋不住它的鄰居** ——
    上面第 12 條寫的是「還原用 cp 備份、不要 `git checkout`(共用樹會殺掉別人沒 commit 的東西)」,
    而 `git stash`(不帶 pathspec)、`git restore`、`git clean` 對**別人未 commit 的檔**有完全一樣的後果,
    這條原本沒寫。
    · 校正前:令只寫 `git checkout`。
    · 為什麼改(2026-08-21,主視窗自己發現的洞,不是別人指出):A 窗當天用 `git stash push -- <自己的檔>` 帶 pathspec
      驗證另一件事(#806),**做法本身是安全的**(帶了 pathspec、只動自己的檔),而主視窗事後複核發現
      **自己的硬紀律清單沒寫到 stash 這個鄰居** —— 這次沒事是因為那幾秒剛好沒人在寫,不是那類指令天生安全。
    · 🔴 判別句:**這個指令會碰到我沒列出來的檔嗎?** 會 ⇒ 一律帶 pathspec / 限定範圍。
    · 安全替代三選一:`git stash push -- <自己的檔>`(帶 pathspec)/ `cp` 備份到 scratchpad / 另 `git worktree` 或 clone 一份跑。
    · 📌 **更可複用的一層**:主視窗同一天已被同一個形狀打到第二次(第一次是「17 筆」那個數字的引用未驗,
      這次是規則列舉未窮盡)—— **規則寫得越具體,它的鄰居越隱形**。寫破壞性操作的禁令時,
      優先寫「這一類動作的判別特徵」(會不會碰到我沒列出來的檔/範圍),不要只列舉當時想到的那個工具名。
    · 出處具名:A 窗(`A-dc`)實跑 `git stash push -- <files>` / 主視窗當場核 `git stash list` 數 = 0(沒有殘留)/
      規則洞是主視窗自己發現並要求補的。
13. 🔴🔴 **`git commit --amend` 之前先問一句:`git log -1 --format='%h %s'`** ——
    **subject 不是我寫的那句 ⇒ 不准 amend。**
    · `--amend` 不是「改訊息」,它是**「用現在的 `HEAD` 與現在的索引,重做一顆 commit」**。
      在八窗共用的主樹上,**那顆 `HEAD` 常常不是你的**(別窗在你上面 commit 了)。
    · 🔴 **`--only` 與 pathspec 擋不住這一格**:它們管的是「不要把別人的**工作樹改動**吸進來」,
      管不到「`HEAD` 根本不是我那一顆」⇒ 照樣會把**別人的 commit 訊息換成你的**。
    · **更安全的預設:根本不要 amend。** 訊息寫錯就補一顆 `docs: 更正上一顆的訊息`,
      或把更正寫進該片的檔案裡(**已投遞的東西不回改,走後續取代**)。
    實例(2026-08-18):某窗想改自己那顆的 body,而那一刻 `HEAD` 是另一個窗剛落的
    ⇒ amend 把它換成「**別人的內容 + 自己的訊息**」,原本那顆變成 orphan(`merge-base --is-ancestor` ⇒ rc=1)。
    🔴 **沒有任何東西紅**:`rc=0`、husky 全過、四綠與它無關。
    🔴 **失主那端的症狀是【自己那顆不見了】** ⇒ 下一個動作很可能是**重做一遍**,那才是真正的損失。
    📎 **歸因(比修法重要)**:那個窗前面四顆 commit **全部都帶了 pathspec** ——
    **不是它不守規矩,是規矩沒寫到這一格**;而**「只是改訊息」這個直覺,正好就是漏洞本身**。
    ⚠️ **本條同樣不是守門**(理由同第 12 條)。
    ⚠️ **本條的第一版寫錯過**:原本寫「修法 = `--amend --only`」,被失主拿量測打回
    —— **一個擋不住這件事的修法,而它讀起來完全合理。** 引用時不要把 `--only` 當解方。
    📌 **而 `--only` 仍然值得用,只是它擋的是【另一種】撞法**(把別窗剛 `git add` 的工作樹改動一起帶走)
    ⇒ **兩件都要:先驗 `HEAD` 是誰的(擋本條這種),再帶 `--only`/pathspec(擋另一種)。**
    **只做後者會讓人以為安全,而今天這件正好是它擋不到的那一種。**
    ✅ **撞到了不是永久失去**:被換掉那顆仍在物件庫,`git log --oneline -1 <hash>` 直接印得出原訊息;
    🔴 **但保護它的是 reflog(預設 90 天)、而 reflog 是這個 checkout 的私有狀態**
    ⇒ **要留就當場把 hash 與 subject 抄進一個會被 commit 的載體**,不要依賴它。
14. 🔴🔴 **人跟腳本的數字對不起來 ⇒ 先問「我們跑的是同一支程式嗎」,`type` / `command -v` 當場答得出來。**
    · **這台機器上 `grep` 與 `find` 是 shell function**(來自 `~/.claude/shell-snapshots/snapshot-zsh-*.sh`),
      **不是 `/usr/bin/` 那兩支**;而 `#!/bin/sh` / `#!/bin/bash` 的腳本跑到的是真的那兩支
      ⇒ **同一個字串、同一棵樹,人跟腳本會得到不同的數,而兩邊都不會報錯。**
    · **兩個當天實錘(沒有實錘的規矩會被當成潔癖)**:
      ① `find`:`-newermt` 吃 `T` 分隔的字面時**等效截點恰好晚一小時** ⇒ 視窗 ≤60 分**恆零**、
         >60 分**漏掉最舊的 60 分鐘**,而 `rc=0`、零 stderr。`~/pcm-mailbox/scan.sh` 因此**少報了兩天**
         (同一刻同一支 `/usr/bin/find`:T ⇒ 11 封 / 空格 ⇒ 29 封,分母 3133)。
         🔴 **而它先騙過修它的人**:用 bare `find`(那個 function)造合成測資,**兩次都重現不到**。
      ② `grep`:那個 function 走 `ugrep --ignore-files` ⇒ **遵守 `.gitignore`**
         ⇒ 「全樹 grep ⇒ 0 命中 ⇒ 不存在」的**實際範圍是【git 追蹤到的檔】**,
         而 `logs/` / `node_modules/` / `.next/` / build 產物 / `.env*` 都在射程外。
         ⚠️ **不要全面改用 `/usr/bin/grep`** —— `--ignore-files` 讓日常搜尋快很多。
         **危險只在兩種場合**:目標可能住在被忽略的路徑裡、或要跟腳本輸出對帳。
    · **做法(成本一行)**:🔴 **量具的輸出裡印一行「我是用哪一支程式量的」** ——
      `echo "(跑的是 $(command -v find))"`。實例見 `~/pcm-mailbox/scan.sh` 的 `--selftest`。
      ⇒ 下一個人拿到那份輸出時,**不必重新發現這件事**。
    ⚠️ **本條同樣不是守門**(理由同第 12 條)。
15. 🔴 **要發 backlog 號 ⇒ `bash scripts/claim-backlog-number.sh`**(不要只跑舊的那兩道)
    · 它自己會先跑 `next-backlog-number.sh` 與信箱佔位掃描 —— **不是取代它們**,是多一道。
    · 🔴 **為什麼多這一道(2026-08-18 實錘,不是潔癖)**:G2 發 `#644` 時**那兩道全跑了**
      (腳本回 644、佔位掃描只有 `#631` `#636`)⇒ **而 G5 在同一段時間也拿了 `#644`**,
      兩條同號,**merge 才發現**。代價=整條改號 + 三份信箱檔跟著改 + 一句改不掉的 commit body。
    · **成因**:那兩道看的都是【已經落檔的東西】,看不到另一個窗【正在寫】的號。
      ⇒ 腳本給的是**下限**不是**保留鎖**;而 `mkdir` 是原子的,同一個目錄名只有一個人建得起來。
    · 🔴 **判別句(這才是要記的,不是那支腳本)**:
      **這道檢查看的是【已經發生的事】還是【正在發生的事】?**
      **如果兩個窗同時做這件事,會不會【兩個都通過】? 會 ⇒ 它擋不住併發。**
    · ⚠️ 條目落檔之後要 `--release <號>`,否則那個號會一直卡住(天花板寫在該腳本檔頭)。

16. 🔴 **開瀏覽器驗任何東西 ⇒ 網址用 `http://localhost:<port>`,不要 `http://127.0.0.1:<port>`**
    · 🔴 **為什麼(2026-08-18 兩個窗各撞一次)**:用 `127.0.0.1` 時 Next dev 會把
      `/_next/static/chunks/*.js` 擋成 403 ⇒ **整頁不會 hydrate**,
      **而畫面、截圖、HTML、CSS 全都正常** —— 只有 client JS 靜靜地不見。
      ⇒ **那個畫面跟「這功能本來就沒做」長得一模一樣**,然後你會開始
      **在一個沒有壞的東西上找 bug**。W2 為它卡過一整輪沒有結論的購物車;G2 繞了六輪。
    · **判別法(照抄;兩個世界會印不同的東西)**:
      ```js
      const els = [...document.querySelectorAll('input,button')];
      ({ 掃到: els.length,
         被接手: els.filter(e => Object.keys(e).some(k => k.startsWith('__reactFiber'))).length })
      // 掃到 0 ⇒ 探針沒量到東西，換選擇器；掃到 N/接手 0 ⇒ 整頁是死的 HTML
      ```
      🔴 **不要用「按鈕在不在」問** —— 它在兩個世界是同一句話。
    · 完整量測、六個 403 網址逐字、範圍限定:`docs/runbooks/local-admin-with-real-data-probe.md` §9 / 9-b / 9-c。
    · ⚠️ 這一條只管**用瀏覽器開**那一步;`curl` / `psql` / PostgREST 打 `127.0.0.1` 沒事。

17. 🔴 **派給你的工作若引用 `§1-A`(`docs/specs/2026-07-25-admin-backend-rebuild-spec.md`)的某一格 ——
    先自己跑那一格的「量法」欄,把輸出貼進你的第一則回報,再開工。**
    · **跑不動**,或**結果與表上寫的不同** ⇒ **先回報,不要照舊值開工。**
    · 🔴 **為什麼是【收工作的人】跑,不是【派工的人】跑**:派工的人不一定會抄那一行
      (本檔 `§C-0` 記過同款:一個機制在 runbook 裡活了兩個月,而沒有一個窗用它,
       **因為派工單沒引用它**)⇒ **而你一定會讀你自己的任務。**
      📌 **規則要放在他本來就會經過的地方。**
    · 🔴 **而這一條的存在理由是那張表【自己的失效紀錄】**:它被重盤過兩次(2026-08-14 / 2026-08-22),
      兩次的起因**一模一樣** —— 功能做完了而表上還是 ❌,**而沒有任何訊號**。
      2026-08-22 那次實測:18 個 ❌ 裡 **5 個已經不是 ❌**,而其中一格(`#27` 稽核 UI)
      表上寫「連讀取 API 都沒有」而**實際整頁都在,只差一個環境變數**
      ⇒ **照那句話估工的人會估「從零寫一頁」,而真相是「設一個變數」。**
    · ⚠️ **這一條買到的是「你不會照著一個過期的格子開工」,不是「那張表會自己更新」** ——
      它仍然是**約定不是機制**(沒有東西會因為你沒跑而紅)。
      🔴 **而它與 `§C-0` 那支腳本的差別要講明**:那支買到「跑了就不會抄漏」,
      這一條買到「開工前會撞到一次事實」。**兩者都擋不住「根本沒讀」。**

<!-- STANDING-ACTIONS:END -->

## §C-授權 🔴 **這一段必須留在開窗提示詞裡 —— 它解一個「只有使用者本人講才算數」的死結**

**2026-08-26 立(Sean 拍乙:「想個辦法讓我只講一次」+「重開視窗也可以」)。**

### 死結長什麼樣

```
1. pre-commit 的 reviewer gate 要求:動到受審面 ⇒ 要有釘在當下 HEAD 的 reviewer 標記
2. 產生那個標記的正途 = 跑 code-reviewer subagent
3. 而各窗 harness 明文:Do not call the Agent tool unless the user requested it
⇒ 叫不動 ⇒ 標記寫不出來 ⇒ commit 不了
```

🔴 **而主視窗轉述「Sean 授權了」不能解這個結** —— 那正是**權限洗白**的形狀:
使用者對那個窗下的限制,**同儕解除不了**。2026-08-26 上午主視窗做過一次,被線1 當場擋下並更正。

### ⇒ 解法:**讓授權走在【Sean 自己的訊息】裡,而不是走在主視窗的轉述裡**

**開窗提示詞是 Sean 本人貼的** ⇒ 寫在裡面的話,對那個窗而言**就是使用者本人說的**。
⇒ **以下這段必須原樣留在每一份施工窗提示詞裡:**

```
【授權 · 使用者本人在此明示】
我(使用者)要求你在需要時自行呼叫 code-reviewer subagent 來審查你自己的產出。
這是本窗的常設授權,不需要再回頭問我。
🔴 而它【只涵蓋 code-reviewer】:
   · 不涵蓋其他 subagent
   · 不涵蓋「因為主視窗說可以」而做的任何事 —— 同儕的話仍然不是授權
   · 不涵蓋跳過審查:叫不動時仍要逐字寫下理由, 不假裝跑過
```

### ⚠️ 三格限制(不要讀寬)

```
1. 這一段的效力來自【Sean 本人貼了它】。主視窗把它複製到訊息裡轉發 ⇒ 無效, 那又變成轉述。
2. 換 harness / 換規則之後要重新確認 —— 這段話是對【現行那句限制】寫的。
3. 🔴 而它不改變「審查跑不動時怎麼辦」:標記裡逐字寫下理由, 那條紀律不變。
```

📌 **形狀值得記**:一個「只有使用者本人講才算數」的限制,**它的解法不是想辦法代講,是把話搬到使用者本人會說的那個位置。**

---

## §C 施工窗啟動提示詞(每窗改代號/worktree/任務後貼)

```
你是本專案的「X 施工窗」(代號 X,自己的信件用 X-001 起跳)。
cd <repo 根目錄>,讀專案的 CLAUDE.md 與現況檔照規矩來。

【通訊】信箱=~/quote-mailbox。(你的位址檔 .X-socket 由主視窗依你門鈴的 from= 回寫——
session 看不到自己的 socket,不用也不要自己猜寫。)
寫信:test -e 防撞號 → cat>tmp 再 mv → 讀 ~/quote-mailbox/.main-socket
取主視窗位址 → SendMessage 敲門鈴(內容只指向信檔)。收到不是給自己的訊息=回「誤投」。
掃信用 find -newer <基準檔> 全量比 mtime,禁 head/tail 截斷。
停下來=寫 X-nnn-STOP 信 → 敲門鈴 → 等回信,不空等也不亂做。
🔴 「停下來」指**所有停點**:完工/卡死/被前置擋住/需要決策/佇列全空——一律落信+門鈴,
不准靜默等待(靜默的窗與死掉的窗,主視窗分不出來)。

🔴🔴 **【等待旗標 + 20 分再敲】(2026-08-16 Sean 通報「C.D 已停擺」後常設;兩次都是主視窗漏回)**
**只要你【動不了、在等主視窗】,除了落信之外還要立一支旗標檔**:
```
printf '等：<一句話寫你在等什麼>\n信：<你最後那封 STOP 檔名>\n' > ~/pcm-mailbox/.waiting-<你的代號>
```
**一恢復工作(收到裁決 / 自己找到不被擋的事做)第一動 = `rm -f ~/pcm-mailbox/.waiting-<代號>`。**
⚠️ **旗標【留著】比誤刪安全** —— 主視窗那邊有去重,不會被同一支洗版。
**主視窗掛了一支 Monitor 掃 `.waiting-*`,超過 10 分鐘會叫。旗標是那個機制唯一的輸入。**

**而旗標不取代你自己再敲**:🔴 **等主視窗超過 20 分鐘沒回 ⇒ 再敲一次門鈴**,
直接寫「**我在等 X,沒有 X 我不能動**」。**敲兩次不算吵,停一小時才是損失。**

🔴 **判別句(給主視窗也給你)**:**視窗「停擺」的預設原因是【主視窗漏回】,不是它掛了。**
實錘 2026-08-16 同一夜兩次:①`D-052` 交了 plan 等裁,主視窗去處理別條線就沒回來
②C 窗卡 reviewer gate、D 窗問「繼續 C 組?」——**兩次都是 Sean 先發現的,不是主視窗。**
⇒ **「我已經講過了」不是機制;旗標與再敲才是。**
【決策題】分兩類:流程/方法論題(怎麼測/片界/審查輪次/工具)→ 落 Q 信,主視窗直接裁;
產品/資料/不可逆題(改行為/動錢/大量寫入/對外可見)→ Q 信寫成 2-4 選項+推薦+各選項
擋不擋後續片,主視窗彙整給老闆拍板。判準一句話:答案會改變產品/資料/不可逆嗎?
會=排隊等拍板;不會=主視窗裁;拿不準=當會,信裡標「若判為流程題請直接裁」。
落完 Q 信**先跳做佇列裡不被這題擋的片**;全被擋=STOP 等令。
🔴 收到主視窗的 A/裁決信=**10 分內回一封 ≤5 行 ACK 信**(收到+下一動作);正式內容**永遠落信箱**,
門鈴訊息只准指信檔名——「寫進 repo/跑了審查」都不算回報,沒落信=對主視窗等於沒發生。

【施工】接單第一動=**用條目自己的查法重數範圍數字**(N 處/N 檔/N 支);對不上=先更正條目、超過一片體積就停下提拆片,不照舊數字開工(08-10 一日三錘:8 處實為 43/三處實為 7/一處實為兩處)。
開自己的 worktree:git worktree add <路徑> -b <分支> <基底>。之後所有改動都在 worktree。
🔴 新 worktree 的 submodule 是空的(design-reference 零檔)——要 grep design 真權威(鐵則 1)前先
`git submodule update --init design-reference`,否則 grep 零命中會被誤讀成「design 沒有這段」(S 窗 08-10 實踩)。
完片=跑完該跑的驗證(exit 分開驗)→ commit(不 push)→ STOP 信附:改了什麼/驗了什麼(附數字)/
沒驗什麼(誠實申報)/下一步。體積超過 45 分鐘自行拆片。
高風險改動(錢/權限/schema/平台設定)先提 plan 等主視窗核准。

【鐵律 · 量測】🔴 一個數字、一個 0、一句「沒有」,不准單獨出門。要帶一個【同時量到的鄰居】:
  ① 正對照:一個【該有輸出】的世界 ⇒ 證明尺接上了(空檔 / 命令沒生效 / 打錯路徑,都在這裡露餡)
  ② 負對照:一個【現造的】假識別字 ⇒ 必須回 0
     🔴 現造 = 不要用任何寫過的字面。寫進檔案的量測紀錄,會變成下一次的假命中(今晚三個窗各踩一次)。
  ③ 或一個回答不同問題的第二個數 ⇒ 不一致就露餡
⚠️ 沒有鄰居 ⇒ 只能寫「我的檢查沒看到」,**不能寫「沒有」**。

【鐵律 · 自檢】🔴 一個「自檢 PASS」只證明**那段邏輯是對的**,**不證明它【被接上了】**。
**判別句**:把那個功能**整段拔掉**,這次的輸出會少哪一行?**答不出來 ⇒ 它沒有接上。**
📌 實錘(2026-08-29):一把尺寫好了、有自檢、有雙向負對照 —— 而**掃檔迴圈從來沒有呼叫它**。
當下**有三個訊號同時在場**(叫你看下方而下方是空的 / 宣稱 136 而印 0 / 零命中行)——
🔴 **而三個都被同一行「Ⓓ 偵測器自檢 ⇒ PASS」蓋掉了**:
它讓「沒有那種行」讀成【它跑了,而這批沒有】,而不是【它沒跑】。

【鐵律 · 指路】🔴 **你正要寫下一個 `檔:行` 嗎?那一句必須【同時】帶一段逐字字面(≤1 行)。**
行號會漂,而漂掉之後它指向【一個真的存在的東西】—— 而那段字面會讓下一個人**當場知道自己撲空了**。
📌 這條不是「不要寫行號」。你寫 `x.tsx:22` 的時候,心裡的動作多半是**舉證**(證明我真的開了那支檔),
不是**指路** —— 而「不要做 A」在你不覺得自己在做 A 的時候,一個字都攔不住。
(2026-08-29 量:一個窗一夜 177 次 `檔:行`,其中 **108 次是裸行號**。)

【鐵律 · 交件】🔴 交東西給別人 = 交**那份檔的路徑**,不是一段摘要。
理由不是「摘要會漏」,是:**你挑不出【他用來發現自己錯了】的那一段 —— 因為你不知道他錯在哪。**
(2026-08-29 實錘:主視窗轉了三份交件的**描述** ⇒ 三筆全卡住,兩邊都以為對方在動;
改交路徑之後,收訊端當場從**同一份檔的第二段**抓到自己一個錯。)

【鐵律】🔴 不要寫鐘點。要寫就當場跑 `date` 貼原輸出。
不想跑 ⇒ 用序數(「主視窗第 N 則」),那個你算得出來。
⚠️ `0X:Yx` 不是折衷 —— 它是編的。
不 push、不替老闆拍板、字面=事實(做一半就說做一半)、拿不準問主視窗、
context 快滿=先寫 checkpoint 信再停,不硬做「可能驗不完」的東西。

【第一個任務】<填入>
```

## §C-審查窗(R3)啟動提示詞 · **常設職責一段(2026-08-29 立;R3 `-3f` 提, 主視窗 `-06` 裁, `-c8` 落)**

> 🔴 **為什麼它只有這一個家**:主視窗裁定「**這一條不進工作池 —— 它是一個【位置】不是一件任務**」。
> ⇒ **不放進池子, 就只剩新窗的啟動提示詞。放進去就結案。**

```
【R3 常設職責】
主視窗每封【外發】訊息含「逐字 / 原文 / 法條 / 官方」⇒ 抽驗【引用塊外那一句】。
—— 塊內通常是真的（2026-08-29 當晚 5/5），危險的是【塊外那句】：它繼承了引用塊的權威而沒有來源。

優先抽：帶「逐字 / 法條」的派工訊息 > 裁決訊息 > 其他。

驗法：開原檔或原 transcript。
🔴 Sean 的話在 transcript 是 type=attachment 的 queued_command（origin.kind=human），
   【不是】type=user —— 只掃 user 會得出「他沒說過」。

查無來源 ⇒ 發回原作者訂正，R3【不代改】。
今晚命中 5 次的清單在 R3-存檔點 §①-1。
```

📌 **這一段與 §C-3〈報交界的四格〉的關係**:那一段管**動手前**的協調, 這一段管**訊息送出後**的抽驗
—— **兩段守的是同一條鏈的兩端。**

---

## §C-2 🔴 **「這個 PID 是哪一個窗?」—— 一發對得出來,而它有一個會騙人的陷阱**(2026-08-19 G2 量)

多窗共機時常見的題:**看到一個孤兒進程 / 一個佔著埠的 server / 一個瀏覽器 profile,想知道是哪個窗的。**

### 規則(量到的,不是推的)

```
🔴 /tmp/cc-socks/<PID>.sock 的【檔名數字】= 那個窗的 claude 進程 PID
```
**怎麼驗出來的**:本窗在同一小時內收到 7 個 peer 的 `SendMessage`,每一封都帶
`from="uds:/tmp/cc-socks/<N>.sock"` 與 `from-name="<窗名>"`。把那 7 個 `N` 與
`pgrep -f 'anthropic.claude-code.*/claude'` 的輸出比對 ⇒ **7/7 完全對上**;
再加本窗自己(從自己 shell 的父鏈量到 `7201`,而 `7201.sock` 也在)⇒ **8/8**。

### 用法(從一個可疑進程,反查它屬於哪個窗)

```bash
# 1. 從那個進程往上爬 PPID，直到撞到一個 anthropic.claude-code 的進程
P=<可疑PID>; for i in $(seq 1 8); do ps -o pid,ppid,command -p "$P" | tail -1 | cut -c1-100
  P=$(ps -o ppid= -p "$P" | tr -d ' '); [ -z "$P" ] || [ "$P" = "1" ] && break; done
# 2. 那個 claude 進程的 PID，就是 /tmp/cc-socks/<PID>.sock
# 3. 而【窗名】要靠對方自報 —— socket 檔名只給得出 PID，給不出 `pcm-website-v2-xx`
#    ⇒ ListAgents 列名字、SendMessage 問一句，兩發就對得出來
```

### 🔴🔴 而陷阱在這裡:**`/tmp/cc-socks/` 裡的 socket【大部分是死的】**

```
2026-08-19 09:2x 實測：該目錄 20 個 .sock
  活著（PID 存在）  8 個   ← 7 個 peer + 我自己
  🔴 殘留（PID 不存在）12 個
⇒ **殘留 socket 與活的 socket 在 `ls` 上長得一模一樣**（同權限、同大小 0、只有 mtime 不同）
```
⇒ 🔴 **「socket 檔在 ⇒ 那個窗還活著」是【假的】。** 檔案不會隨進程消失。
> **判別句:我是在問【檔案在不在】,還是【進程在不在】?**
> ⇒ 要後者就 `ps -p <PID>`,不要 `ls`。

**逐個判活的一行**:
```bash
for f in /tmp/cc-socks/*.sock; do p=$(basename "$f" .sock)
  ps -p "$p" >/dev/null 2>&1 && echo "$p 活著" || echo "$p 殘留"; done
```

📌 **而【直接問】仍然比反查便宜**:2026-08-19 那次,我對 6 個窗各發一句三行的問話,
**六個全回,而其中一個直接自報「兩樣都是我的」** —— 總成本遠低於我自己爬進程樹。
⇒ **反查用在「問了沒人認」或「要獨立驗證某個窗的自報」時。**(那次我兩件都做了:先問,再獨立驗。)

---

## §C-3 🔴 **報交界的四格** —— 協調不是問「我可不可以動」,是給出「我動了什麼形狀」

> 2026-08-27 立。來源=線1 `-21` 的一次示範 + 主視窗 `-5b` 當場把它定成規格。
> **適用時機**:你要寫的 plan / 要動的檔,與別條線的檔案面有交集時。

### 為什麼不是「問一句可不可以」

```
問「我可不可以動 order-list-view.ts?」
⇒ 對方要先知道【你要怎麼動】才答得出來, 而你的 plan 還沒寫完
⇒ 兩邊都停下來, 等一個【還不存在的答案】
🔴 2026-08-27 當天據報有兩個窗因為這種等待各停過一段時間。
⚠️ **那個「兩個」是主視窗 `-5b` 轉述給我的, 我沒有自己量過 ⇒ 未確認。**
   依 `~/.claude/rules/00-work-rules.md:98` 轉述契約①標來源屬性 = **讀來**。
   📌 而**本條規格不靠那個數字成立** —— 靠的是「對方要先知道你怎麼動才答得出來」這個結構。
```
📌 **不要解決一個還不存在的衝突。**

### ⇒ 改成:報出**形狀**,讓對方自己判會不會撞

**四格,缺一格對方就判不了**:
```
① 哪一支檔          寫完整路徑, 不寫「訂單那邊那支」
② 加法還是改法      附 `git diff --stat` 的實際數字, 不寫「小改」
③ 有沒有動既有簽名  函式簽名 / 型別 / export 介面 —— 這一格決定會不會編譯撞
④ 做完會不會再回來  「下完這顆就離開該檔」與「我還會再動它三次」是兩件事
```

### 來源實例(**不要只留抽象規格**)

線1 `-21` 2026-08-27 就 `apps/admin/src/lib/orders/order-list-view.ts` 給出的原話形狀
(逐字轉錄, 值未重打;那支檔的權威清單在 `order-list-view.ts:701-713`):
```
只有一處:把 taipeiParts 從 function 改成 export function(+ 一段註解說明為什麼)
git diff --stat -- apps/admin/src/lib/orders/order-list-view.ts ⇒ 1 file, 11 insertions(+), 1 deletion(-)
🔴 純加法 —— 沒改任何既有函式簽名、沒動 buildOrderListHref / formatOrderListDate / 篩選那一族
而且它下完那顆就離開這支檔, 不會再回來
```
⇒ 收到這四格的人**當場就判得出來**:對方動的是日期工具的 export,我動的是篩選那一族 ⇒ 零交集 ⇒ 不用協調。

### 兩條配套紀律

```
🔴 協調的時機是【實作前】, 不是【寫 plan 前】
   —— plan 階段零檔案改動 ⇒ 沒有衝突面 ⇒ 那時候談的是還沒決定的東西。
   而 plan 裡**必須列出它會動哪些檔** ⇒ 清單出來之後才是協調的時機。
🔴 而「設計上不需要改那支檔」是【推論不是量到的】
   ⇒ 真動手時發現要改 ⇒ **停下來找主視窗居中, 不要自己去改。**
```

---

## §D 用到的工具/技能清單

| 工具 | 用途 | 備註 |
|---|---|---|
| SendMessage / ListAgents | 跨視窗門鈴 | Claude Code v2.1.224+;同機走 unix socket |
| Monitor 工具 | 信箱秒級哨兵 | 檔案輪詢;殼層 sleep 在沙盒常壞,別用 shell 迴圈 |
| ScheduleWakeup(/loop) | 10 分心跳 | 綁 session,視窗關掉即停 |
| git worktree | 施工窗隔離 | 🔴 多窗共用 clone 禁用 git stash(全域堆疊會彈到別人的) |
| 審查 subagent(主視窗派) | 凍結件雙線審 | 🔴 派工單必寫「工作副本只准落 session scratchpad、**不得落 repo 樹內**」——落 `apps/`/`packages/` 會進 vitest/tsc 收集面、直接改變被審者的三綠計數與 Δ 對帳(08-10 S 窗實錘:.rev.test.tsx 被收集+in-flight 假紅);審查完成報告必附 porcelain 零留痕證明 |
| /schedule 雲端 Routine | 選配:每小時網站巡邏 | 跑在雲端、關機照跑;異常可寫 GitHub issue 讓主視窗心跳看到 |
| gh CLI | 選配:巡邏異常回流 | 需 GitHub App 授權 |

## §E 實戰坑清單(直接抄進對方專案的規則檔)

1. zsh:命令內禁 `#` 註解、禁全形標點;多步驟 `&&` 串接。
2. `/tmp` 會在長 session 中途被清——中繼檔一律放 scratchpad;餵檔案給外部工具前 `test -s` 驗非空。
3. pipeline 吃 exit(`| tail` 後 `$?` 是 tail 的);zsh `PIPESTATUS` 印空。
4. `pnpm --filter X test` 對無 test script 套件靜默跳過 exit 0。
5. 門鈴 success≠送達;位址絕不用快取;認不出人=讀位址檔,絕不廣播。
6. 施工窗掃信:`ls -t | head` 是截斷、檔名差集看不到覆寫——一律 `find -newer` 比 mtime。
7. 收割看到「樹髒」=不 merge,等對方 commit。
9. 🔴🔴 **要複製的不是「大家都很誠實」,是【互相戳的密度】。**2026-08-22 夜三次歸屬更正,方向都是把功勞推回去 —— **而沒有一次是自己想到的**:一次是別人貼原話、一次是對方自己更正、一次是有人把它寫成問句才看出差別。🔴 **當事人自陳:『我的角色是【沒有攔下更正】,不是【發起更正】。那兩件事差很多。』**⇒ **成因是七個窗互相戳、而且 git 記得。少了那個密度,同一批人會得到不同的結果。**
8. 🔴 **檔名會替你回答「這給誰看」,所以你不會再問一次** ——收工寫 STOP/交接檔時,每一段問:**這一段的讀者,是不是也會讀這個檔?**不是的那幾段要換位置(進 repo)。**實例(2026-08-22)**:一個一整夜在替別人標「這結論住錯地方」的窗,把最該給【下一個指揮】的東西寫進了自己的 STOP —— **而 STOP 不在 repo、不在 memory。**
8. 兩個視窗撞同一條線=立即點名關一個(CLOSE+ACK),否則會雙寫同一個檔。
9. 施工窗回報的「事實」主視窗要抽驗(開原始檔),尤其影響決策的那種。
10. 每逢新 DB migration 落檔,依賴「最新版本號」釘值的檢查會全紅=機制正常,照程序重釘+重跑,不是壞掉。
11. 本機測試資料庫(pg cluster)每窗**專屬 PORT + 專屬 workdir**;腳本預設 port 兩窗同跑時,後到者 provision 失敗但 psql **靜默連到別窗的庫**(查詢有正常回值=最毒的症狀)。機制修法=harness 起庫前探測 port 被占硬停、起庫後驗 `current_setting('data_directory')` 等於本次 workdir 才採信。
12. 🔴 **worktree 的 `node_modules` 會靜默過期**(2026-08-16 D 窗實錘,整棵樹 typecheck 紅)。**病灶是時序,不是某棵樹的狀態**:worktree 建立**之後**有人在 `package.json` 加了 workspace 依賴 ⇒ **那棵樹永遠沒有那個連結**,而它自己不會知道。實錘=`apps/admin/package.json:17` 的 `"@pcm/schemas": "workspace:*"` 是 08-15 早上加的,08-15 00:49 建的 worktree 缺它 ⇒ `TS2307 Cannot find module '@pcm/schemas'`。**症狀會被誤判成「別人弄壞了 dev」**,因為紅的檔往往不是自己動過的。⚠️ **量法要小心**:`ls apps/admin/node_modules | wc -l` 對這個缺陷**零判別力** —— `@pcm` 在頂層只算一個 entry,少一個 scoped 連結**數字不變**。⇒ **要列內容**:`ls apps/admin/node_modules/@pcm/` 對照 `grep 'workspace:\*' apps/admin/package.json`。**修法**=`pnpm install --frozen-lockfile`(lockfile 零改動)。**時機**=①開新窗當下 ②repo 動過任何 `package.json` 依賴之後,**全部既有 worktree 都要重跑一次**。
    🔴 **與既有那條【不同病,別搞混】**:`docs/handoff/2026-08-03-day-a5a.md:58` 與 memory `reference_od-worktree-typecheck-gotcha` 記的是 **worktree root `node_modules/.bin` 缺 `tsc`/`eslint`**,而且逐字寫著 **`pnpm install --frozen-lockfile` 回「Already up to date」也補不出來**;**本條(缺 scoped workspace 連結)install 補得起來**。**判別**=`.bin` 缺執行檔 ⇒ 舊病、install 無效,借主樹 binary;**`@pcm/` 缺連結** ⇒ 本條、install 有效。
    ⚠️ **本條是這個坑第三次被寫下來**(前兩次在 handoff 與 memory)—— **病不是「沒寫」,是寫在只有當事人會回去讀的載體**;寫在此處是因為 §E 會被「開新窗」這個動作觸發。
13. **fork 風暴**(08-09 五窗實錘,最終全機當機):多 session 齊跑 vitest/turbopack 會打滿 per-user 程序上限,症狀=所有窗 `echo` 都 exit 1 零輸出、Monitor 全死、Python 吐 `Errno 35`,連 sandbox 內外都一樣。處置:①症狀=系統層,**不是程式紅、不當 finding 修**;②各窗停止重試(重試也燒 fork)、掛長輪詢等主視窗令;③主視窗協調**全套測試錯開跑**、查孤兒 node(turbopack crash 會留);④預防=多窗夜跑時 vitest 全套與 build 向主視窗排隊錯開;**dev server 在多窗期間一律 `next dev --webpack`**(turbopack 的 next-swc 有 fork 競態崩潰 bug,08-09 四份 .ips 同指紋、兩次全機當機;單窗日常照用 turbopack 沒問題,Sean 拍板保持預設;治本=追 Next 上游修復)。
```

## §F backlog 發號:「主樹最大 + 1」會撞號(2026-08-14 實錘)

> 🔴🔴 **2026-08-16 精確化(C 窗更正)**:`scripts/next-backlog-number.sh` 自己印「這支給的是**下限**」——
> **而「下限」不夠精確,真正的說法是**:
> **它看得到的是【已 push 的】號。**
> ⇒ **未 push 的 commit 裡開的號,任何窗跑那支腳本都看不到。**
> **實錘**:主視窗用它拿到「下一個 = 529」,而主樹工作區當時已經有 `#529`–`#531`(未 commit),
> C 窗也已開了 `#535`(未 push)⇒ **真上限是 535,腳本回 529 ⇒ 低估 6。**
> 🔴 **而多窗夜跑時未 push 是常態**(那一夜五窗合計 60+ 顆)⇒ **腳本是【系統性低估】,不是偶爾失手。**
>
> **⇒ 正確做法(主視窗發號時跑,不要只跑腳本)**:
> ```bash
> for t in . ~/pcm-void-readers ~/pcm-products ~/pcm-customers ~/pcm-print; do
>   grep -ohE '#[0-9]{3}\b' "$t/docs/phase-1-backlog.md" 2>/dev/null | tr -d '#' | sort -n | tail -1
> done | sort -n | tail -1
> ```
> **加上** `grep -rn '佔位' ~/pcm-mailbox/*.md`(信箱佔位宣告,兩者都要跑)。

**病**:主視窗查**主樹當下最大號 + 1** 就發下去 ⇒ **已配出、但還在別窗 branch 沒 merge 的號看不到**
⇒ **兩窗各自都以為自己拿到唯一的號**,而**發現時機是收割**(最貴的時候)。
**實例**:2026-08-14 至少兩次;`#489` 撞上(V 窗已寫入 / E 窗拿去佔位)——
🔴 **接住它的是「E 主動請主視窗確認」,不是機制。**

🏁 **已機制化 ⇒ 直接跑 `bash scripts/next-backlog-number.sh`(無參數)。**
它印**三層各自的最大值**(不一致 = 有號還沒回到主樹)+ 下一個可用號 + 掃描限度。
🔴 **為什麼要有這支、而不是照抄下面那行**:主視窗自述「**我就是會寫出較窄版本的那個人 ——
不是忘了跑,是當場想了一個較窄的查法然後跑了它**」⇒ **文件擋不住「現想一個」,一支有名字的腳本擋得住。**
🔴 **負測(端到端,拿病發當下的狀態當測資)**:
`NEXT_BACKLOG_REFS='b185e3e0^ d6a18e38' NEXT_BACKLOG_SKIP_LIVE=1 bash scripts/next-backlog-number.sh`
⇒ 回 **`#490`**;而當天的窄查法(只看自己 branch 的父 commit)回 **488 ⇒ 發 `#489` ⇒ 正是撞掉的那個號。**

**底層數法(腳本內就是這一行;要手算或移植到別的專案時抄它)**:
```bash
{ for w in $(git worktree list --porcelain | awk '/^worktree /{print $2}'); do
    cat "$w/docs/phase-1-backlog.md" 2>/dev/null; done
  git for-each-ref --format='%(refname)' refs/heads | while read r; do
    git show "$r:docs/phase-1-backlog.md" 2>/dev/null; done
} | grep -oE '^### #[0-9]+' | grep -oE '[0-9]+' | sort -n | tail -1
```
🔴 **`cat` 工作區檔那一段不能省** —— 它才抓得到「已經寫進檔案、但還沒 commit」的號。
🔴 **禁 `sort -t'#' -k2 -n`**(BSD sort 對它的行為不同,已列進量具方言坑集)。

**窗這一側的規矩**:**號一律跟主視窗要**;真要先佔位,**必須標明「佔位、請主視窗確認」**,不得當成已配發。

**限度(要寫明,不要當成全掃)**:
- 上面第二段掃**本機 branch**,所以關掉的窗留下的 branch 也涵蓋(本機實查:**33 branch / 7 worktree**,差 26 條全靠它)。
- **掃不到**:只存在於 remote、本機沒有對應 branch 的號;以及**還沒寫進檔案、只存在於某封信裡**的號。

---

### 🔴 2026-08-22 補兩格:同一族,而這兩格是【commit body】那一側

**上面那段講的是「commit 帶走了誰的檔」。這兩格講的是「commit body 說了什麼」。**

#### ① body 要照 `git diff --cached` 寫,不要照「我知道我在提交什麼」寫
```
同一夜兩次，同一個人、同一支檔 docs/phase-1-backlog.md：
  28bbe22b 本意只提交「保留期更正」⇒ 同時夾帶了另一個窗剛加的 #845
  31560080 本意只提交「#838 條文定案」⇒ 同時夾帶了 #846
兩次的 commit message 都【沒有提到那一條】
```
🔴 **而它沒有任何訊號**:commit 成功、三綠照過、`git show --stat` 只印檔名不印內容。
只有**下一個人想找「`#845` 是哪一顆進來的」時,才會發現 body 在說別的事**。
⇒ **判別句:這支檔今晚有幾個人在寫?超過一個 ⇒ body 必須從 `git diff --cached` 來。**
⚠️ **錯過了就在【下一顆的 body 裡留痕】,不要 `--amend`** ——
🔴 `--amend` 動的是 **HEAD**,不是你心裡想的那一顆(同夜實測:想補上一顆,結果把 HEAD 的訊息換成了上一顆的內容)。

#### ② 🔴 commit body 要寫「本片作者 = <窗代號>」—— 而理由是【只有那一秒問得到】
```
2026-08-22 實測：想確認 scripts/traps-neighbours.py 的原作是誰
  git log --diff-filter=A -- <該檔> ⇒ 一顆 commit，committer 是主視窗
  （全隊所有 commit 都經過主視窗）
  而 body 裡寫的是【為什麼做】，沒有寫【誰做的】
⇒ 🔴 查不出來。而兩個人各自「推」出一個答案，兩個都沒量過：
     主視窗說「我建的」  ← 未量，而它讀起來像【認錯】
     另一個窗說「原作是 X」← 也未量，而它讀起來像【嚴謹】
```
📌 **⇒ 而後者更難被懷疑,因為【更正這個動作本身自帶可信度】** ——
一個人肯回頭改自己講過的話,讀的人會預設他查過了。
🔴 **更正比原句傳得更遠,而它的證據門檻通常更低。**(此格 2026-08-22 只有這一個實例 ⇒ **候選,未定案**。)

**⇒ 機制**:`git commit` 的那一刻**問一句「這片是誰寫的」**,寫進 body。
🔴 **理由不是禮貌,是:窗會被壓縮、對話會消失,而【你按下 commit 的那一秒,那個人通常還在線上】。**
⚠️ 已 commit 的**不回頭補**(那要重寫歷史)—— 在**下一顆的 body 裡說明從哪顆開始有這一欄**。

---

## 🔴🔴 共用主樹:`git commit` 之前一定要看【整個 index】,不是看你自己那支檔

**2026-08-19 深夜,同一個病在一小時內咬了【三個窗】**(G2 `79a67170` / G6 `4fe7c975` / G4 `42002997`),
兩次都是**內容零損失、歸屬錯**:別人 staged 的檔被掛進你的 commit,而你的 commit body 從頭到尾在講別的事。

### 兩條既有規矩都遵守了,而它們擋不住
```
「禁 git add . / git add -A」        ← 兩窗都遵守
「精準 pathspec」                    ← 兩窗都遵守
「git add 前跑 git diff --stat <路徑>」← 主視窗當晚交代的,G6 照做了、而且它印出來是【對的】
```
🔴 **`git diff --stat <我的路徑>` 對「index 裡有沒有別人的東西」零判別力** ——
它問的是「我那支檔的未 staged 改動」,而別人的檔**已經在 index 裡了**(他 `git add` 了、還沒 commit)。
而 `git commit` 不帶 pathspec 時,**提交的是整個 index,不是你 add 的那條路徑。**

### ⇒ 正確的那一發(在 `git commit` 之前跑,不是在 `git add` 之前)
```bash
git diff --cached --stat          # 🔴 用 --stat 不是 --name-only：要同時看【哪幾支】與【幾行】
```

🔴🔴 **`--name-only` 不夠 —— 2026-08-19 實錘:它通過了,而那顆 commit 還是掃走了別人的東西**
```
G3 跑 git diff --cached --name-only ⇒ 印出 docs/phase-1-backlog.md（只有一支，而且確實是它的檔）
⇒ 檢查通過 ⇒ commit ⇒ 那顆帶走 59 行，而它自己只改了 3-4 行
   多出來的是【另一個窗】append 在同一支檔別的位置的整段
```
⇒ **因為 `--name-only` 問的是「哪幾支檔」,不是「這支檔裡有誰的行」。**
📌 **判別句**:**這支檔是不是【現在有別人也在寫】的共用檔?**
   是 ⇒ `--name-only` 不夠,**要看 `--stat` 的行數對不對得上我改的量**。
```
git diff --cached --stat
⇒ 我改了 4 行，而它印 59 行 ⇒ 當場看得出來不對
```
⚠️ **`docs/phase-1-backlog.md` 是全 repo 最熱的一支**(2026-08-19 一夜至少四個窗在寫)。
📎 這與 memory `feedback_precise-path-does-not-stop-a-shared-file` 同族而**多一層**:
   那條講「精準路徑擋不到共用檔」,**這一格更細 —— 連【檢查了檔名】都還是會中。**
**或**直接讓 commit 自己指名(兩道都做最好):
```bash
git commit -F <訊息檔> -- <你的精確路徑> [<路徑2> …]
```

### 📌 判別句
> **我檢查的是「我的檔對不對」,還是「這一顆 commit 會帶走什麼」?** —— 兩個問題不一樣。
> **`git add` 是我控制的,`index` 不是。** 精準路徑管的是前者,而 commit 帶走的是後者。(G4 補)

🔴 **分母是三,不是二** —— 第一版寫「兩個窗」是因為第三個窗的自首與廣播交錯。
**低估基準率會讓下一個人以為這是偶發。** 而它在一小時內發生三次,三個窗都遵守了既有的兩條規矩。
📎 兩個方向要分開記:**吞人的**(G2/G6/G4)與**被吞的**(G1 的 `#309` 座標落進 G6 的 `4fe7c975`)——
成因不同:前者是自己 commit 沒帶 pathspec,後者是自己 add 了而別人先 commit。

🔴 **「被吞的那方做不了任何防護」這句 2026-08-19 被 G6 從內部推翻,收窄如下** ——
**被吞方能做的只有【縮短自己暴露的時間窗】**,而那個窗常常是自己拉長的。G6 那次三步、每一步都不報錯:
```
1. Bash 被 classifier 逾時中斷 ⇒ heredoc 沒寫成,commit 訊息檔【不存在】
2. 重跑 `git add … && git commit -F <不存在的檔> … | head -3 && git show …`
   ⇒ add 成功、commit 失敗、而 git show 照樣跑了(pipeline 的 rc 是 head 的 0)
3. ⇒ 檔留在共用 index 裡,而當事人以為自己 commit 完了
```
**兩條自保(比機制早生效,今天就能做)**:
```
① commit 訊息檔要在 git add 之【前】寫好並 test -s 驗過
   —— 訊息檔不存在 ⇒ commit 必失敗 ⇒ 而 add 已經發生了
② 🔴 git commit 後面永遠不要接管線:
      git commit -F "$MSG" -- <路徑> > out 2>&1 ; rc=$?
      cat out ; [ $rc -eq 0 ] && … || echo "🔴 commit 失敗 rc=$rc"
```
📌 **判別句:我怎麼知道 commit 成功了?**
「命令跑完沒噴紅」不算 —— **接了管線之後,失敗的 git 與成功的 git 印出來的東西一樣少。**

📌 另一條(GR 拋棄式 repo 實測):**當一個資源是共用的,防護只能裝在【寫入的那一端】** ——
`窗A: git add a.txt` / `窗B: git commit -m "only b" -- b.txt` ⇒ commit 只帶走 b.txt,**a.txt 仍在 index、原封不動**。
⇒ 帶 pathspec 完全不碰別人的 index;加害端被擋的那一刻,受害端就被保護了。

### 📎 G2 那次的成因值得單獨記(它不是粗心)
它寫的是一整條 `git add <精確路徑> && git commit -F <檔>`。
**那一整條被 PreToolUse 守門擋掉了,而它以為只有 commit 那半被擋** ⇒ 把 commit 那半單獨重跑
⇒ **`git add` 那半從來沒有執行過** ⇒ 沒有 pathspec 的 commit 吃掉了共用 index。
🔴 **一條被擋的 `A && B`,`A` 也沒有跑。** 重跑之前先確認你要重跑的是哪一半。

### 🔴 空 commit **也不安全**(2026-08-19 G2 落訃聞時撞到)
```
git commit --allow-empty -F <檔>            ⇒ 🔴 仍會把【別人 staged 的檔】一起帶走
   實測:git diff --cached --name-only ⇒ docs/phase-1-backlog.md（別人的）
安全寫法:
git commit --allow-empty --only -F <檔> -- <一個你自己的、沒有改動的檔>
   ⇒ 產出 0 個檔的空 commit，別人的東西沒被動到
```
📌 **「空 commit 沒有內容所以無害」是錯的** —— 無 pathspec 的 commit 帶走的是 index，與它自己有沒有改動無關。

### 🔴🔴 `git commit --amend` 在共用主樹是【比無 pathspec 更毒】的一種
2026-08-19 實錘:G2 以為在 amend 自己那顆,而**共用主樹的 HEAD 在它打字的期間被別人推前了**
⇒ 它 amend 掉的是**另一個窗五秒前剛落的那顆**(`e324a966` → `1827b578`)。
```
內容零損失（git diff <舊> <新> --numstat ⇒ 無輸出，兩顆 tree 相同）
丟掉的是【那顆 commit 的訊息】—— 而訊息裡有別處沒有的東西
```
🔴 **病根與無 pathspec 不同**:前者是「**範圍沒指定**」,後者是「**目標(HEAD)在我不知情的時候換了人**」。
⇒ **寫了第一條防不住第二條 —— 那不是紀律問題,是那條規則的射程本來就不涵蓋它。**
⇒ 📌 **一條規則擋住的世界,比它讀起來像擋住的世界窄。**

**處置**:共用主樹**不要用 `--amend`**。要修訊息 ⇒ **補一顆空 commit 當訃聞**(用上面那個安全寫法),不要 rebase。
🔴 而**同族的第三個變形還沒被涵蓋**:`git rebase` / `git reset --hard` **也都是對當下的 HEAD 動手**。
   ⚠️ 沒有人踩過它們,所以**不知道實際形狀** —— 而依今晚的紀錄,**同一母題的下一個變形看起來不會像同一件事**。

### 出事之後怎麼處置(兩窗給了相反而各自正確的答案)
- **index 現在乾淨、事故那顆是上一顆** ⇒ `git reset --soft HEAD~1`(不動 index、不動工作樹)
  → 帶 pathspec 重 commit → `git diff <壞的那顆> -- <那些檔>` 應無輸出(證明內容一字沒動)。**G2 這樣做,對。**
- **別的窗正在寫、index 裡有進行中的動作** ⇒ **不要 reset、不要改歷史**。
  **一個歸屬錯誤不值得換一個併發事故。** 改由主視窗記進桌面狀態 + 原作者在下一顆 commit body 認領。**G6 這樣判,也對。**

---

## 🔴🔴 同檔並寫:pathspec 逃生門對它【零判別力】(2026-08-19,一夜四次)

```
git commit -F <msg> -- <pathspec>
  擋得住  別人的【別的檔】被我帶走
  擋不住  別人在【同一支檔】上的未 commit 改動
```
**機制性理由**:`git commit -- <path>` 提交的是**那支檔當下的工作樹全貌**,**不是「我 stage 的那幾行」**。
⇒ 只要兩個窗同時在寫同一支檔,**先 commit 的那個必然帶走後者。**
🔴 **`guard-commit-pathspec` hook 也擋不到** —— 它看的是 pathspec 有沒有給,而這裡給了。

**判別句**:**這支檔是不是現在有別人也在寫的共用檔?**
是 ⇒ `--name-only` 不夠,要 **`git diff --cached --stat`** 看行數對不對得上我改的量。

### 🔴🔴🔴 最終版(2026-08-19 G4 實錘推翻前兩版)——**不要用 pathspec commit**
```
🔴 病灶：git commit -- <path> 取的是【工作樹】，而 git diff --cached 讀的是【index】
        ⇒ 兩道檢查與那一發 commit，讀的是【兩個不同的世界】

G4 實測：git add（index 30 行）→ G6 在這中間寫檔（工作樹變 70 行、index 沒變）
         → git diff --cached --stat 印 30 ⇒【通過】
         → git commit -F <檔> -- <path> 從【工作樹】取 ⇒ 進去 70 行
⇒ 兩道檢查都跑了，而它們對這個形狀【零判別力】
```
📌 **判別句(G4 給的)**:**我檢查的那個東西,和 commit 真正讀的那個東西,是同一份嗎?**

**⇒ 正確做法:走 index 那條路,不要用 pathspec**
```bash
1. git diff --cached --stat            # add 之前：index 裡有沒有【別人的】？有 ⇒ 停，不要 add
2. git add <你的精確路徑>               # 禁 git add . / -A
3. git diff --cached --stat            # 再看一次：只有你的檔、行數對得上你改的量
4. git commit -F <訊息檔>               # 🔴 不帶 pathspec ⇒ 它吃【index】= 你剛驗過的那份
```
🔴 **第 4 步不帶 pathspec 是關鍵** —— 帶了它就改讀工作樹,而你驗的是 index。

### 🔴🔴 而「一律不帶 pathspec」也是錯的 —— **按【碰撞位置】選,不是二選一定死**(2026-08-19 G6 補)
```
碰撞在【別的檔】      ⇒ 🔴 用 pathspec（它擋得住；不帶 pathspec 會把別人的別的檔一起吃掉）
碰撞在【同一支檔】    ⇒ pathspec 擋不住 ⇒ 只能【等對方先 commit】，沒有第三條路
index 乾淨（沒有別人）⇒ 不帶 pathspec（吃你剛驗過的那份 index）最準
```
**G6 的實例**:它要 commit `scripts/who-is-touching.sh`,而 `git diff --cached --stat` 印出 index 裡有**別人的 `#661` plan(47 行)**
⇒ 不帶 pathspec 會吃掉它;而**沒有任何人在動那支 `.sh`** ⇒ **用 pathspec 是安全的**。
📌 **判別句:我這一發的碰撞在哪裡?** —— 先答這個,再選寫法。**兩種方法各漏一半,而漏的不是同一半。**
⚠️ ~~這條路唯一擋不到的是「別人在 1 與 2 之間 stage 東西」—— 而第 3 步會抓到~~
🔴 **那句話不對,而推翻它的是同一晚的兩個實例** —— 見下面 v5。
🔴 **共用主樹永不 `--amend`**。

---

## 🔴🔴 v5(2026-08-19 深夜,主視窗自己推翻 v4 的一半)——**三格,其中兩格是今晚剛長出來的**

> 🔴 **這一段推翻主視窗自己先前那一裁「index 乾淨就不帶 pathspec」。原裁定留在上面不刪。**
> 而推翻它的不是道理,是**兩個窗各撞一次的實例**。

### A · **檢查與 commit 之間有時間窗(TOCTOU)** —— 這不是誰忘了跑,是**方法本身的邊界**
```
某窗的實際順序（照 v4 三分法：index 空 ⇒ 不帶 pathspec）:
  1. git diff --cached --stat  ⇒ 空 ✅
  2. git add <我的檔>
  3. git diff --cached --stat  ⇒ 只有我的 ✅
  4. git commit                ⇒ 🔴 **實際帶走 2 支檔**（別窗在第 3 步之後 add 進來的）
🔴 那一發還噴了 `fatal: cannot lock ref 'HEAD'`（HEAD 在 commit 途中被推進），**而 commit 仍然成立**
```
⇒ **檢查與 commit 是兩個操作 ⇒ 中間永遠有窗。**
```
帶 pathspec   ⇒ 擋得住【別人的別的檔】，擋不住【同檔並寫】
不帶 pathspec ⇒ 吃 index ⇒ 擋不住【檢查之後才進來的別人的檔】
🔴 **兩種各漏一半，而漏的不是同一半。**
```

### B · 🔴 **那道檢查在兩個世界印一樣的東西 ⇒ 它是日誌,不是閘**
某窗的原句(**逐字**):
```bash
n=$(git diff --cached --name-only | wc -l); echo "index 檔數=$n" && git add … && git commit …
# 它印出：index 檔數=1   ← 🔴 1 就代表裡面有別人的東西
# 然後：`&&` 串接繼續往下跑
```
⇒ **對「該不該繼續」,它的判別力是【零】。**
⚠️ **而它讀起來完全像一道檢查** —— 有變數、有數字、有標籤,**輸出裡甚至印著正確答案**。
🔴 **錯的不是那個數,是那個數【沒有連到任何一個分支】。**
📎 而 `&&` 為什麼會吃掉壞消息:**前一步 `echo` 的 exit code 永遠是 0** ——
**它把壞消息的語氣帶走了、只留下字面。**

**⇒ 標準寫法(照抄,不要用 `echo`)**:
```bash
n=$(git diff --cached --name-only | wc -l | tr -d ' ')
[ "$n" = "0" ] || { echo "🔴 index 有別人的東西($n 個檔)，停"; exit 1; }
```

### ⇒ v5 三條(取代 v4 的第一步與選型)
```
① 🔴 第一步一律用【條件式】，不用 echo —— **印出來的數字沒有連到分支，就不是閘**
② 🔴 **一律帶 pathspec —— 即使 index 乾淨**
   理由:兩種寫法各漏一半，而【同檔並寫本來就無解】
   ⇒ 那就把「別人的別的檔」這一半**關死**，少一個變因
   ⚠️ **這推翻主視窗先前「index 乾淨就不帶」那一裁**，而理由是今晚湊齊的兩個實例
③ 🔴 commit 後一律 `git show --stat` 對檔數與行數 —— **那是唯一抓得到 TOCTOU 的地方**
```
⚠️ **v5 仍然擋不住的**:**同一支檔並寫**。那一格無解 —— `git diff --cached` 只看得到「**已經 add 的**」,
看不到「**正在寫的**」。⇒ **熱檔(`docs/phase-1-backlog.md` / `STATUS.md` / `docs/patterns/index.md`)動之前用
`SendMessage` 問一句「你在動 X 嗎」,或跑 `scripts/who-is-touching.sh`**
(⚠️ **而那支腳本有已知瞎區:41 支裸檔名零判別力,它自己輸出裡寫著**)。
🔴 **而【問】也擋不住「對方正在寫而還沒 add」** —— **那不是工具不好,是 git 沒有那個資訊。**

### 🔴 v6(2026-08-29 落):**「同檔並寫無解 / 沒有第三條路」有一條路 —— 而它只被走過一次**

> 🔴🔴 **本節【第一次在真樹上跑是 2026-08-29(`8c64146c`)】, 而實際形狀比本節原本寫的多兩個失敗分支。**
> 📌 **⇒ 一條「只被走過一次」的路, 它的紀錄是【那一次的計畫】, 不是【那一次的實況】。**
> **一份寫在事前的 runbook, 與一份寫在走完之後的, 是兩份不同的文件 —— 而它們長得一樣。**
>
> 🛑 **預設動作沒有變:兩邊同時有東西 ⇒ 先停下協調、等對方先 commit。**
> 上面 v4/v5 那兩句(「只能等對方先 commit,沒有第三條路」/「那一格無解」)**留著不刪** ——
> 它們在**協調得到**的時候仍然是對的,而下面這條**只在協調不到時**才走。
> ⚠️ **證據等級:線A `-ea` 2026-08-28 執行過【一次】,沒有第二個人在別的情境驗過。**
> 🔴 **五個編號步驟讀起來就是一個推薦 —— 而它不是。** 走它之前先答:**我真的協調不到嗎?**

**線A 當天的處境**:index 有別窗 staged 的 `naked-cells.py`、工作樹有線D 未 commit 的 `#954`(55 行),
與它自己的 `#953` **同一支檔** ⇒ 不帶 pathspec 收 index(帶走別人的檔)/ 帶 pathspec 收工作樹(帶走 `#954`)
⇒ **v5 的兩種形狀這一次都不安全。**

```
① 前置條件先成立：那支檔是【尾端追加】型，且別人的改動也在尾端
   （線A 那次是 backlog，驗的是「前 35,587 行與 HEAD 逐字相同」）
   🔴 別人的編輯在【中段】⇒ 這套流程不成立 ⇒ 回到預設：停下協調
② 把整支檔複製到一個【唯一】落點（mktemp 或路徑帶窗代號），並記下 sha256 = S0
   🔴 八窗同時在做，固定檔名會被別窗蓋掉 ⇒ 你會還原成【別人的內容】而沒有東西會紅
③ 覆寫【前】先比一次：工作樹現值 ≠ S0 ⇒ 有人在 ①② 之後寫過 ⇒ 🛑 停，不要覆寫
   🔴 少了這一比，那一筆不在副本裡也不在下面的窗口裡，會被安靜抹掉
④ 覆寫成「HEAD 版 + 我那幾行」⇒ 記 sha256 = S1 ⇒ 帶 pathspec commit
   🔴 commit 在本 repo 是【會失敗的】（.husky pre-commit 十道閘、reviewer 閘）
   ⇒ 失敗也要先跳到 ⑤ 還原、再回頭處理失敗
     （否則工作樹停在「別人的東西已經被我抹掉」而【零訊號】）
⑤ 還原【前】再記一次 = S2：S1 ≠ S2 = 有人在窗口內寫過
   ⇒ 🛑 不要還原，停下協調
   🔴 還原會把你剛偵測到的那一筆抹掉 —— 一道在造成損失【之後】才報告損失的尺
   相同才從 ② 那份副本還原全檔
```
**線A 那次結果**:commit 檔數 1、非我的 0、條目總數 802 ⇒ 803。落點 `54e10f3d` 的 body。

### 🔴 2026-08-29 真樹首跑(`8c64146c`)踩到的兩個失敗分支 —— 兩個都不在上面那份計畫裡

**分支①:先試了一條更省的路, 而 `pre-commit` 擋下它 —— 而它擋對了**
```
更省的路：git diff <檔> ⇒ 剪出只含我那一個 hunk ⇒ git apply --cached ⇒ 不帶 pathspec commit
（完全不碰工作樹、沒有東西要還原、上面那三道 sha 守門整個消失）
🔴 而 pre-commit 判它「要帶走不是你放進去的檔」而擋下
```
📌 **成因**:**那道閘認的是【誰用 `git add` 放進 index 的】, 不是【index 裡有什麼】** ——
而 `git apply --cached` **不是** `git add` ⇒ 在那道閘眼裡, 那個檔不是我放的。
✅ **它擋對了** —— 它的判準與它的實作一致。⚠️ **而這一格原本完全沒寫** ⇒ **下一個人會以為 v6 壞了。**

**🔴 分支②(這是【前置條件】不是註腳):`commit -F` 的訊息檔【不可以放在會被清的目錄】**
```
訊息檔寫在 session 的 scratchpad ⇒ 中途被清掉 ⇒ git commit -F 讀不到 ⇒ commit 失敗
🔴 而失敗的那一刻，改動【已經 staged 躺在共用 index 裡】
   —— 那正是 ⟦b4-IDX3⟧「add 與 commit 之間不准有等待」在講的狀態
```
📌 **為什麼是前置條件不是註腳**:**它不是「可能會發生」——**
**那條路的必經一步(`commit -F <訊息檔>`)剛好踩在一個會被清的目錄上。**
✅ **修法(兩個窗同一天獨立走到同一個, 所以直接寫死)**:**訊息檔一律寫 `/tmp/`, 不寫 scratchpad;**
**而重試時把「寫訊息檔」與「commit」放進【同一個指令】, 縮短 staged 暴露的時間。**

📌 **這條路為什麼被寫成正典, 有一個要講明的成因**:**它是【被做過的那一個】, 不是最小的那一個。**
一個更省的形狀已經被提出(`git diff` ⇒ 手剪成只含我的 hunk ⇒ `git apply --cached` ⇒ 不帶 pathspec commit
⇒ **完全不碰工作樹、沒有東西要還原、上面三道 sha256 守門整個消失**),
⚠️ **而它只在拋棄式 repo 驗過兩發、真樹有 `.husky` 十道閘未驗** ⇒ **另開一片, 不在本節生效。**

---

### （舊版·已被上面取代，留著看演化）完整版順序(2026-08-19 G6 補 —— **原本那條晚了一步**)
```
1. git diff --cached --stat        ← 【add 之前】先看 index 裡有沒有【別人的東西】
2. 有別人 ⇒ 🔴 停，不要 add。請那個窗先 commit（透過主視窗協調，不要自己去找他）
3. 沒有別人 ⇒ git add <你的精確路徑>
4. git diff --cached --stat        ← 再看一次，對【自己改的行數】
5. git commit -F <訊息檔> -- <你的精確路徑>     ← 後面不接管線
```
🔴 **為什麼要補第 1 步**:原本那條只講「對行數」,而**對行數是 `add` 之後的事** —— **那時已經晚了一步**。
**實錘(2026-08-19,還沒造成傷害就停住的第一次)**:G6 準備 commit 自己 39 行之前跑第 1 步 ⇒
看到 index 裡有 G4 的 4 支檔(424 行,含兩支**真的 code**)⇒ **它停下來沒有 add**。
若照舊(先 add 再檢查),`git commit -- docs/phase-1-backlog.md` 會把**那支檔的工作樹全貌**收走 ⇒ G4 的新條目會被掛在它的 commit 底下。

📌 兩個位置的差別:
```
G3 那句「我改了 4 行，而它印 59 行 ⇒ 不對」  = commit 者【自保】（add 之後）
G6 這一步「index 裡有別人 ⇒ 不要 add」        = 【提前一步】，還沒造成傷害就停住
```

### 🔴 而暴露窗常常是【守門自己拉長的】—— 這是放大器,不是巧合
2026-08-19 GR 的第一次事故:
```
它寫 `寫訊息檔 && git commit`，被 PreToolUse 的 commit 訊息守門擋掉
⇒ 🔴 【寫檔那半也沒跑】（一條被擋的 A && B，A 也沒跑）
⇒ 它改了【四輪】訊息，而守門每一輪驗的都是【磁碟上的舊檔】
⇒ 多耗四輪 —— 而事故窗就是那幾分鐘
```
**修法(兩條,都便宜)**:
```
① 訊息檔【先單獨寫好並 test -s 驗過】，再單獨跑 commit —— 不要一條龍
② 被守門擋掉之後，重跑前先確認【你要重跑的是哪一半】
```
📌 **一個為了品質而設的守門,可以透過拉長暴露窗製造另一種事故。** 裝守門時要問:**它會讓人多花幾輪?**

### 現行處置(主視窗 2026-08-19 裁,採 G6 的丙 + 甲)
```
丙 記帳（主）：帶走別人東西的人【在 commit body 裡點名】—— 這已經在自然發生，把它變明文
甲 序列化（輔）：動 docs/phase-1-backlog.md 這種【全 repo 最熱的共用檔】之前，
   先在信箱佔一個「我正在寫 backlog」的旗標，寫完放掉
⛔ 不採乙（查核產出改寫到別的檔）—— 代價是【條目本身沒有標記】，
   下一個挑條目的人看不到，那會打掉整輪查核的價值
```
🔴 **共用主樹永不 `--amend`** —— 訊息錯就落訃聞空 commit(安全寫法見上一節)。

---

## 更正要怎麼寫 —— 而「劃線留原文」不是最強的那一版

2026-08-19 夜實際發生：一份給 Sean 讀的清單被更正過六次。
主視窗以為自己在做「劃線留原文」，而審查窗去查那四句被撤回的原文**還在不在檔裡**：

```
四句全部只出現 1 次 —— 而那一次【就在更正塊自己裡面】當引述
⇒ 🔴 原文被【刪掉】了，只以引述的形式活在更正裡
⇒ 而那比劃線強：**沒有一句舊的可以被單獨讀到**
```

### 三種寫法，強度不同

```
弱   原文留著、更正寫在下面      ⇒ 從上往下讀的人【先讀到錯的】
中   原文劃線、更正寫在下面      ⇒ 他會讀到，而【劃線可能被忽略】（尤其在長檔裡）
🔴 強 原文【刪掉】，只在更正塊裡當引述
     ⇒ 「我原本寫：『…』——那句錯在 X，正解是 Y」
     ⇒ 舊句子【失去它自己的段落】⇒ 沒有任何讀法會單獨遇到它
```

⚠️ **而「強」不是永遠對的**：
```
· 舊句子若被【別人引用過】（別的檔、別的信）⇒ 要留得住那個錨 ⇒ 用「中」
· 舊句子若是【拍板、授權、誰批的】⇒ 🔴 一律不刪（見下一節）
· 而【訃聞的用途是給下一個人看見這裡曾經有東西】⇒ 那時「中」比「強」對
```

### 🔴 而這一條為什麼要寫下來

它原本只是一個習慣 —— **而做對了卻不知道自己做對，就沒有人會去保護它**。
下一次換一個人寫更正，它會自然退回到「劃線留原文」，而那弱一級。

📌 而**判準**：
> **這一句被撤回的話，還有沒有辦法【被單獨讀到】？**
> 有 ⇒ 那個讀法就是下一個誤解的入口。

---

## 什麼時候可以「先報再查」，什麼時候要「查到底才報」

2026-08-19 夜：一個窗查一段安全面註解的出處，中途讀到的證據指向
「一個設計決定掛著一個不存在的拍板結案了」。**它沒有在那一刻發警報。**
繼續查之後，那個結案是真的 —— 錯的只是那段註解引用的**編號**。

### 🔴 判準（那個窗自己寫出來的）

> **這封警報若是假的，收訊者會【啟動什麼】？**
> ```
> 啟動一條線（重新調查、通知別人、回頭改東西）⇒ 🔴 查到底再報
> 只是多讀一段（他看一眼、知道有這回事）      ⇒ 可以先報
> ```

**理由**：假警報的成本不是「說錯一句話」，是**收訊者照它動了**。
而一個指揮位置的人，收到「某段安全註解在說謊」時的預設動作**就是啟動一條線**。

### ⚠️ 而它與「不要靜默」不衝突

```
· 卡住了、在等什麼         ⇒ 立刻說（那是狀態，不是指控）
· 我判它比標籤說的更嚴重   ⇒ 立刻說（今晚有窗這樣做，而它是對的）
🔴 而【一個會讓人回頭改東西的指控】⇒ 查到底
```
判別不是「重不重要」，是**收訊者會不會據此動手**。

---

# §G 白天日常制(2026-08-30 立;**Sean 逐字「這個流程順利的話,你可以把這個完整流程變成以後我們執行任務的方式」**)

> §A-2 的**五窗制**是 2026-08-18 的實例、**已被實況超過**(2026-08-30 實跑 = 主視窗 + 哨兵 + 六條施工線)。
> 本節不改前面任何一段,只**加上今天量到的三個機制**。三個都是為了同一個病:
> 🔴 **在主視窗這端,「那條線在做事」與「那條線停了」是【同一個訊號:什麼都沒有】。**

## §G-1 為什麼「請大家不要停」這條規矩永遠會失效

```
一個窗只有在【有人跟它講話】的那個回合裡是活的
它講完最後一句 ⇒ 回合結束 ⇒ 它就不再執行任何東西
⇒ 🔴 「我接著做」這句話本身，就是它的最後一句話
```
📌 **⇒ 說出「我要繼續」與「我停下來了」是同一個動作。**
⇒ ⇒ 所以喚醒**不能寫成規矩**(規矩要人記得,而停住的窗沒有回合可以記得),**只能是節拍**。
🔴 實錘:Sean 2026-08-30 逐字「**下手視窗就會說自己要繼續,然後就停下來了,這種問題層出不窮**」。

## §G-2 兩支節拍(主視窗掛,**不是哨兵**)

| 排程 | 週期 | 做什麼 |
|---|---|---|
| 喚醒拍 | **每 23 分鐘** | 對每一窗發一句「還在跑嗎?**如果你剛回報完就停住了,這則就是你的下一個回合,直接把下一步做下去**」 |
| 存檔拍 | **每小時 :07** | ①叫全隊存檔(含哨兵、含主視窗自己)②主視窗自己寫 checkpoint ③掃「有窗傳訊給我而我沒回」④掃「有工作在排隊沒人認領」 |

🔴 **為什麼掛在主視窗而不是哨兵**:哨兵的醒法是「15-20 分鐘自己看一下」= **靠判斷決定要不要提醒** ⇒ 忙的時候一定漂。
主視窗有 `CronCreate`,**它不需要記得**。
⚠️ **而這兩支的射程要跟著它們走**:
```
· 只活在【掛它的那個 session】—— 主視窗換窗 ⇒ 消失,而消失時零訊號
· 7 天後自動過期（會再響最後一次，然後刪掉）
· 只在 REPL 閒置時才觸發（主視窗正在跑長工作 ⇒ 那一拍會晚）
```
⇒ **這三格必須寫進艦隊表當一列**,否則「有節拍」這件事本身會安靜地消失。
📌 **一個沒有人知道它存在的機制,跟沒有那個機制一樣。**

## §G-3 佇列**指名制** —— 兩條好規矩相加會生出一個病

🔴 **2026-08-30 實錘:同一片交辦單被【三個窗】認領**(`-b4` / `-1c` / `-e9`),而 `-e9` 已經做完。
成因**不是任何一個窗的錯**:
```
主視窗同時下了兩條規矩：
  ①「做完自己接下一件、不要停」        ← §G-1 的解藥
  ②「不要重複工作」
⇒ 而在一個【開放認領】的佇列上，這兩條互相打架
⇒ ⇒ 七個窗都被教育成主動接件之後，多重認領是【必然】，不是偶然
```
⇒ **修法(機械)**:艦隊表「排隊未派」欄改成**指名制**,三格:

```
| 工作 | 交辦單路徑 | 🔴 指名給誰(只能一個名字;空白 = 任何窗都【不准】自己接) |
```
表頭寫死一句:**「沒有名字的那一列,不是給你的 —— 是給主視窗去指名的。」**

⚠️ **而這道守門【只有哨兵做得到,主視窗做不到】**:
📌 **派工的人看不見自己派重了** —— 三個窗各自回報,而**每一則單獨看都合理**。
⇒ 哨兵每次醒來跑一次機械檢查:**同一份交辦單路徑出現在兩列以上的「現在手上」欄 ⇒ 立刻叫主視窗**,不自己判斷誰該讓。

## §G-4 「我派得動」不等於「那個窗做得完」

> 逐字(線C `-b4` 2026-08-30,動手前撞到、零成本):
> **「這片我派得動」與「那個窗做得完」是兩個宣稱 —— 而第二個只有那個窗自己量得到。**

🔴 同一天在**三個窗身上各驗了一次**,形狀一致:
```
-b4  連「只印檔案形狀、值全部遮蔽」的前置檢查都被自己的權限分類器擋 ⇒ 走不到第 0 格
-e9  打正式 Supabase 被擋，理由逐字：
     「the user never named this production target; assigned only by a peer session」
-1c  自己先問「若你或 Sean 認為要他點頭，現在說」
```
⇒ **結構性結論(不是判斷)**:**同僚視窗不能替另一個窗開權限。**
「打正式站 / 讀憑證檔 / 任何 Sean 沒有親口指名的正式目標」⇒ **只有 Sean 本人授權得了**,
⇒ ⇒ 換一個窗去跑**不會**繞過它,只會多被擋一次。
⚠️ **而主視窗當天給的理由是「今晚變數太多」= 判斷** ⇒ 結論對、**理由弱一級**,
📌 **而弱的理由會在下一次被人合理地說服掉。** 這種時候要把結構性那條寫出來。

## §G-5 主視窗的四個動作(每一拍都做,順序固定)

```
① 回信 —— 有窗傳訊給我而我沒回 ⇒ 當下回，一句話也可以
   🔴 沉默不算派工。沉默就是它靜在那裡。
② 指名 —— 排隊未派那一欄，有東西就指名一個窗（不是廣播）
   🔴 而交辦單上要多問一句：【這件事你今晚寫過嗎？先 grep 一次你自己今晚的產出】
      成因（2026-08-30，`-e9` 自陳）：它用錯一把驗證指令，而正確的那把
      不只寫在 `CLAUDE.md`，還寫在【它自己 40 分鐘前寫的那條坑檔正文裡】
      ⇒ ⇒ 📌 收窄後的形狀：**在【產生解法】的當下，人不會去查【自己的產出】**
      ⇒ ⇒ ⇒ 而那正是它不該是【執行者的紀律】的理由：
           執行者在那個當下；**而派工的人不在** —— 他在交辦的那一刻
      🛑 所以這一句長在【交辦單】上，不長在執行者的自律裡
③ 落檔 —— Sean 拍板/我答應了誰什麼，【說的當下就寫】，不留「等下順手」
   實錘：2026-08-29 主視窗說「已排進決策板」五次、寫零次
④ 存檔 —— checkpoint 檔頭第一段【永遠】指向哨兵那張艦隊表
   🔴 壓縮會吃掉「哪一條線手上是什麼」，而它消失時零訊號
```

### §G-5-0 🔴 **任何進入【狀態行】的數字,要有一行【可以當場重跑】的指令**

(2026-08-30 立;`-e9` 自陳兩次報出沒量過的數字,第二次還**聲稱它是量過的、並引用自己那條規矩當背書**。)

📌 **⇒ 而那一族的偵測率,結構上就是【只有作者自己】**:
```
· 被別人抓到的錯 ⇒ 全部是【對外宣稱的東西】⇒ 有人拿去用 ⇒ 所以有人會撞到
🛑 · 而「我報了一個沒量過的數字」⇒ 沒有人會去驗它，因為它只是一句【狀態】
⇒ ⇒ 而狀態行【正是最不會被驗的位置】
⇒ ⇒ ⇒ 而作者在報那個數字的當下，正是他最不想去數的時候
```
⚠️ **而「有一個檔案數得出它」【不夠】**(`-e9` 收窄):
```
「顆數」是【累積的】⇒ 一份追加的檔案就夠
🔴「推沒推」是【隨時會被別人改變的】⇒ 檔案存下來的那一刻就開始過期
   （Sean 一按 push，那份檔的每一行都變成假的，而檔案不會知道）
```
⇒ **⇒ 所以要的是【量法】不是【落點】** —— 而那正是本 repo 已證過三次的那句:**寫量法、不寫死值。**

### §G-5a 🔴 **每一發 `push` 之後,當場在信箱寫一行**(2026-08-30;三個窗各查一次才立的)

```
一行的內容：誰授權（逐字）· `date` 的時刻 · 頂端 hash · 幾顆
```
**為什麼**:2026-08-30 那一發 116 顆的 push,授權是 Sean **本人逐字打的 `git push`** ——
**而那個事實只活在主視窗的對話裡**。⇒ `-0b` / `-b9` / `-e4` **三個窗各花一輪**去重建它,
而三個都只能寫「不是我推的、而我證不出是誰」(`reflog` 只記 `update by push`;
八窗共用 git 身分 ⇒ `%an` 對每顆都印 `probe`)。
📌 **⇒ 一個【完全合法】的動作,在所有可稽核的載體上與一個【事故】長得一模一樣。**

🔴 **而它與 §G-7 是同一個稅的兩個方向**(`-e4` 的歸納,逐字):
```
· 我製造了一個訊號而沒有替它命名   ⇒ 下游各付一次稅（未 commit 的紅）
· 我握有一個事實而它沒有落在載體上 ⇒ 下游各付一次稅（push 的授權）
⇒ 兩者的稅率一樣，而兩者都【只有上游放得進去】
```
⚠️ **而那一行要一起寫的,是這道關卡在這個量級上的【設計上限】,不要藏**:
**他那一個字涵蓋了 116 顆 —— 他沒有逐顆看過,也不可能。**
⇒ 「Sean 手動推 = review checkpoint」買到的是**一次點頭**,不是 116 次審查。
⇒ ⇒ **那是設計上限,不是那一次的違規** —— 而它**不得被寫成「已審查」**。

### §G-5b 🔴 共用樹上的**自指座標**都要具名(2026-08-30 `-1c` 撞到)

```
主視窗交辦的回核指令：git show HEAD -- <檔> --numstat
⇒ -1c 照跑 ⇒ 【印不出東西】
   成因：它 commit 完到它回核之間，別的窗又 commit 了 ⇒ HEAD 已經不是它那顆
```
🔴 **而失敗的形狀不是報錯,是印出空的** —— 而「空」在
**「我那顆沒動到那兩支檔」**與**「HEAD 不是我那顆」**兩個世界裡**是同一個輸出**。
📌 **⇒ 在八窗共用一棵樹上,`HEAD` 不是一個穩定的座標。**

**⇒ 規矩**:
```
· 回核一律用【你自己那顆的 hash】，不要用 HEAD
· 「未推幾顆」要分兩個數：本班幾顆（逐顆 merge-base --is-ancestor 驗）／全樹幾顆
  🔴 而 git log --author=probe 零判別力 —— 八窗共用同一個 git 身分
  ⚠️ 2026-08-30 實測：--author 印 52，而全樹未推也是 52
     ⇒ 兩個數字相同，而它們回答的是【不同的問題】
· 同族:指標要指得到【而且指到對的地方】—— 那是兩格

#### 🔴 而這一族有名字:**共用資源上的【自指座標】**(2026-08-30 `-1c` 歸納)

```
HEAD          「我最新那一顆」          ⇒ 八窗世界裡它是【誰都可能的最新那一顆】
git author    「誰做的」                ⇒ 八窗共用身分 probe ⇒ 它答不出誰
單一槽位標記   「我審過了」              ⇒ 它裝的是【最後一個寫它的人】的那一句
```
📌 **⇒ 三個在單窗世界裡都【完全正確】,而在八窗世界裡它們答的是【別人的問題】。**
🔴 **而共同的失效形狀是:它們不報錯** —— `HEAD` 回核印空、`--author=probe` 印一個合理的 52、
那道閘印綠。**三個都給你一個看起來正常的答案。**

**⇒ 判別句(拿去套新的座標)**:
> **這個座標的值,是不是【別的窗的動作也會改變它】?**
> 會 ⇒ 它在這裡不是座標,是一個共用變數。

**⇒ 各自的替代**:
```
HEAD        ⇒ 用【你自己那顆的 hash】
git author  ⇒ 用【作者自報】或【那一列的事欄文字】,不要用錨(錨會被交叉引用)
單一槽位     ⇒ 每一顆 commit 之前【自己重寫一次】,不要因為「剛剛才寫過」就跳過
             🔴 而那道閘的綠【不代表這一顆被審過】,只代表「最近有人在這顆 HEAD 上寫過標記」
```

  (`-1c` 補完 §G-4 的檔名之後,又開檔核了它指到的是對的那一節)
```

### §G-5c 🔴 **端給 Sean 的 migration,必須先在拋棄式 PG 上【真的跑起來】**(2026-08-30 實錘)

```
-e4 的第七態 migration 寫完、migration-static-checks 全綠、codex R1 八條 must-fix 全折完
🔴 而它【從來沒跑起來過】：`pg_catalog.position('x' in y)` 在 PL/pgSQL 不合法
   （position 的 IN 是特殊語法）
⇒ 靜態檢查全綠，因為它【掃字面、不 parse SQL】
⇒ ⇒ 而 codex 兩輪也沒抓到 —— **它讀碼，沒有跑**
```
📌 **⇒ 抓到它的是「去真的跑一遍」這個動作本身,不是任何一道檢查、也不是任何一輪審查。**
🔴 **而後果落在【他】身上**:那支檔會在 Sean 的 SQL Editor 上炸,
錯誤訊息是 `syntax error at or near "v_def"` ——
**他看不懂,而他會以為是他貼錯了。**
⇒ ⇒ **一個我們的語法錯,會被他讀成他自己的操作失誤。**

**⇒ 規矩(端之前,不是 commit 之前)**:
```
· 任何要請 Sean 貼進 SQL Editor 的 migration ⇒ 先在拋棄式 PG 上【apply 成功】
  做法見 docs/runbooks/throwaway-postgres-for-migration-verification.md
· 🔴 而「apply 成功」只是第一格 —— 那支 runbook 自己就寫著「apply 成功 ≠ 斷言通過」
· 三個世界各跑一次:正常 / 重跑(冪等) / 一個刻意的壞世界(閘必須擋並印出實際 vs 預期)
· 端給他的訊息裡寫明【它在拋棄式 PG 上跑過】—— 那句話是他決定要不要現在貼的依據
```

### §G-5d 🔴 **凡是有過【格式變更】的東西,掃它的字集必須含【變更前後兩種】**(2026-08-30 `-1c` 自陳)

```
-b9 05:00 起把重驗戳的格式從「線G …」改成 `[重驗 -1c · …]`
⇒ -1c 掃「板上還找不找得到我的戳」時，字集只寫了 `-1c`
⇒ ⇒ 印出「18 列裡有 7 列找不到我的戳」
🔴 而那 7 列是【舊格式】的戳，寫的是「線G」
✅ 字集改成 `-1c` 或 `線G` ⇒ 找不到的 = 0
```
🔴 **而它特別值得記,是因為那把尺是【它自己參與制定的格式變更】造成的** ——
**二十分鐘後變成它自己的盲區。**

📌 **判別句**:
> **最容易漏的是【你自己剛改的那一種】,因為新的那個在你腦裡最鮮明。**

⚠️ **而這與 §G-5b(自指座標)不同族**:那一族是**座標的值會被別人改**;
這一條是**我自己改了座標的形狀,而我的尺留在舊的那一版**。
⇒ ⇒ **兩者都不報錯,而後者連【別人】都不需要參與。**

### §G-5e 🔴 **有人因為一個前提而【放棄】一件事時,那個前提本身要被量一次**(2026-08-30)

```
-0b 明確拒絕做一個判斷，理由是【混用兩個鐘（CST / 沙盒鐘）的比對它沒把握】
⇒ -1c 去量：四個來源（bash date / git committer / python time.time() / 檔案 mtime）
   在同一秒各自取值，epoch 秒【完全相同】= 1788040546
   🔴 負對照 TZ=UTC ⇒ 差正好 8 小時 ⇒ 所以那個「四個相同」不是尺看不出差別
⇒ ⇒ **只有一個鐘。那個顧慮不成立 ⇒ 那個判斷是【白放棄的】。**
```
📌 **而放棄的方式完全正確**(不確定就不下結論)——
**錯的不是它的紀律,是【沒有人去量那個前提】。**
🔴 **⇒ 而放棄的人通常【沒有立場】去量它** —— 他正是覺得自己沒把握的那一個。

**⇒ 規矩**:
```
· 任何一則回報寫「我不做這一格，因為 <前提>」⇒ 主視窗要把【那個前提】派給【另一個窗】量
· 🔴 而不是叫原本那個窗「再想想」—— 它已經說了它沒把握
· ⚠️ 而這與「尊重它的拒絕」不衝突：拒絕的是【那個判斷】，量的是【那個前提】，兩件事
```
📌 **⇒ 那是一個【因為謹慎而付出的成本】,而那個成本本來一行指令就能免掉。**

#### 順帶:**「沙盒鐘」這個標法全隊停用**
它讓人以為有第二個鐘,而**沒有**。
⇒ **時刻一律寫 `date` 的輸出並帶 `%Z%z`**(例 `05:55:46 CST+0800`)——
📌 **帶時區的時刻自己就答得出「這是哪個鐘」,不需要一個形容詞。**
⚠️ **而 Sean 2026-08-29 那句「現在是 01:20」仍然【未確認】**:它是一句話不是一個量測,
與這台機器差約 5 小時,**而 5 不是任何一個常見時區差**。
🔴 ⇒ **「這台機器的時間與真實世界一不一致」我們仍然答不出來** ——
**量到的只有:這台機器【內部】只有一個鐘。**

## §G-6 這一天**沒有**跑順的地方(留著,不要只記成功的那半)

```
· 同一片派給三個窗（§G-3）—— 主視窗的錯，三次派重工的第三次
· 一個查完的結果端給了 Sean 而【沒有回頭告訴查它的那條線】⇒ 那條線空等一小時
· 「已排進決策板」說了沒寫（前一晚，五次）
· 一次 grep 範圍含 repo 根 ⇒ 掃到 .env.local ⇒ 正式權杖被印進對話
  🔴 規矩逐字寫著「cat .env 不在對話跑」，而下的不是 cat ⇒ 【規矩的字面比它要防的東西窄】
· 引用落在隔壁那題（兩次，都是主視窗）：那句話真的存在、真的只出現一次、真的是 Sean 說的
  ⇒ 而它命中的是隔壁那題 ⇒ 📌 一個對的結論配一個錯的依據，比一個錯的結論危險
                              —— 錯的結論有天然回饋路，對的結論配錯依據【零回饋】
```
> 🔵 **上面最後兩條的【完整形狀】不住在這裡, 這裡只留一行指標**(`-b9` 2026-08-30 補;
> 照「同一教訓一處全文、他處單行指標」):
> · **規矩字面比它要防的東西窄**(那起外洩)⇒ `docs/patterns/traps-inbox/b9-20260830b-四條-引用落在隔壁那題-與-撤回沒有落點.md` **第 13 條**
>   (含【它是機制題不是規則題】的判準, 以及**那把秘密閘自己的陷阱**:一道只擋五種形狀的閘會讓所有人覺得「有閘了」)
> · **引用落在隔壁那題** ⇒ 同檔 **第 8 條**(含為什麼它與 `正本:8571` 行號型、`:18756` 段落名型**都不同族**)
> 🔴 **而寫這兩行的理由本身是今天的一條坑**:一個坑被寫進兩個載體而兩邊都是全文 ⇒
> **它們會各自過期, 而讀到其中一份的人不會知道還有另一份。**

📌 **⇒ 本節刻意把失敗與機制寫在一起** —— 一份只寫「應該怎麼做」的流程檔,
讀起來像「我們都有做到」,而它在【真的做到】與【以為做到】上印同一句話。

## §G-7 撞車時,**第一個動作是 `commit`,不是 `rm`**(2026-08-30 實錘,差一點吃掉四十分鐘的工)

```
-e9 寫完 scripts/probe-schema-exposure.sh（307 行，未 commit），code-reviewer 正在審
⇒ -1c 清掉「自己寫到一半的腳本，不留半成品」⇒ 刪的是【同一個路徑】
⇒ ⇒ code-reviewer 第一句：「檔案在我審到一半時從磁碟上消失了」
   （第一發 ls 拿到 16287 bytes，再一發 ⇒ No such file or directory；
     where-is.sh 四格全查無；git log --all -- <path> 空 ⇒ 它從來沒進過 git）
```
> 🔵 **`-b9` 2026-08-30 補一行(只加不刪)**:上面那個「**307 行**」是**那一刻的行數** ——
> 那支檔後來 commit 進來是 `7e58ad82` / **487 行**(當場 `wc -l` 量的)。
> 🔴 **兩個數都留著, 因為它們在講不同的事**:307 = **被刪掉的那一份有多大**(這一節的重點),
> 487 = **今天磁碟上那一份**。⇒ **不要把 307 讀成「現在那支檔 307 行」而去對不上。**
> 📌 而這正好是今天一直撞到的形狀:**一個在落筆當下為真的數字, 過幾小時之後讀起來像錯的。**

🔴 **`-1c` 做的是【正確的動作】,它沒有做錯任何一步。** 壞掉的是:
> **同一個檔名 · 兩個窗 · 一棵樹 · 而其中一份沒有 commit。**

📌 **判別句:未 commit 的檔【沒有歸屬】。**
`git status` 上它就是一行 `??`,**上面沒有寫是誰的** ——
⇒ ⇒ **「刪掉自己的半成品」與「刪掉別人的成品」在那一行上長得一模一樣。**
⚠️ 而 `where-is.sh` 那四格對它**全部查無**:它不在工作樹(剛被刪)、不在 dev、不在任何 ref、不在信箱
⇒ **一支從來沒進過 git 的檔,被刪掉之後在每一個可稽核的載體上都不存在。**

**⇒ 可機械化的修法只有一個(照做,不要靠溝通):**
```
· 撞車 / 要清理 / 要換路 ⇒ 【先 commit,再刪】—— 哪怕是一顆 WIP
· 自己的半成品也一樣 commit —— 因為你【不知道】那個路徑上現在是不是你的那一份
· 而寫檔的人:未 commit 的產出【同步一份到 scratchpad】，改完就同步
  (`-e9` 事後的做法:先寫 scratchpad 再 cp 進 repo)
```
🔴 **而這是 §G-3(佇列開放認領 ⇒ 多重認領)的【下游成本】** ——
兩個窗被派到同一片,才會有兩個人在同一個路徑上寫東西。
⇒ **指名制修的是上游;本節修的是「上游已經漏了」之後那一發。兩道都要。**

### §G-2a ⚠️ 節拍的**名字**不是它**發生的時刻**(2026-08-30 主視窗自己踩)

```
兩支排程叫「每小時 :07」與「每 23 分鐘」⇒ 主視窗把 `03:07` 寫進七則訊息
🔴 而 `date` 量到的是 `2026-08-30 02:38:38 CST+0800`
⇒ ⇒ `03:07` 是一個【從來沒有存在過的時刻】—— 它是排程的【標籤】
```
抓到它的是 `-1c`(它回「我這邊 `date` 是 02:37,兩個鐘差約 30 分」)。
📌 **⇒ 標籤與量測是兩個載體,而標籤看起來比量測更權威(它是設定出來的、看起來精確)。**

**⇒ 規矩(全隊)**:
```
· 時刻一律寫 `date` 的輸出（本機 CST +08:00），訊息裡附時分
· 節拍的名字（:07 / 每 23 分）= 排程 id，【不得當時刻引用】
· 🔴 而排程實際觸發會晚：runtime 對 recurring 加抖動（最多週期的 10%，上限 15 分），
  而它【只在 REPL 閒置時】才觸發 ⇒ 主視窗正在跑長工作時那一拍會再往後
```

---

## §G-8 一顆 commit 可以戴著**別的窗的訊息**出去 —— 而三綠是綠的、diff 是對的

> 2026-08-30 實錘,`-1c` 的 `393a3d02`。**全隊六窗當天都做得出來這件事。**

### 機制:兩個各自都正確的東西相乘

```
① PreToolUse 閘擋的是【整個 Bash 呼叫】
   那一發是 git add + 寫標記 + 產訊息檔 + git commit 串在一起
   ⇒ 閘擋下 ⇒ 【產訊息檔那一行從來沒有跑過】
② 而 /tmp 裡躺著一支【同名的、昨天的】訊息檔
   ⇒ 下一發 git commit -F 讀到它 ⇒ 安靜成功，內容是別人的
```

🔴 **那支檔若不存在,前置的非空檢查會擋下來,失敗會很大聲。**
📌 **⇒ 是那支【過期的同名檔】,把一個大聲的失敗換成一個安靜的、內容錯的成功。**

**規模(當天量的)**:`/private/tmp` 裡的 commit 訊息檔,昨天以前 **162** 支、今天 **44** 支(最舊 08-26 02:06;負對照現造檔名 ⇒ 0)。⇒ **162 顆啞彈,每一顆都在等一個「呼叫被閘擋掉 + 檔名撞上」的窗。**

### 三條,即刻生效

1. **commit 訊息檔不要用 `/tmp` 的固定檔名** —— 寫進各窗自己的 scratchpad,或每次 `mktemp` 拿一支新的。
2. 🔴 **`git add` / 寫標記 / 產訊息檔 / `commit` 不要串在同一發 Bash 呼叫** —— **閘一擋就是整發不跑,而前面那幾行看起來像跑過了。**
3. 🔴 **`git add` 是一個「我下一步就要 commit」的訊號,不是一個暫存動作**(2026-08-30 加,同日兩次死結後立)——
   還在寫 ⇒ 檔放在工作樹,**不要 `add`**;要 commit 了 ⇒ `add` → 寫標記 → `commit`,**中間不做別的事**。
   🔴 理由是機制的:只要 index 裡有第二個窗的檔,**無 pathspec 閘**(要求帶 pathspec)與 **reviewer 閘**(標記釘真 index 的 tree,而帶 pathspec 的 commit 用的是臨時 tree)**就把出口關死**。
   ⇒ ⇒ **⇒ 而「index 裡有別人的檔」這個條件,是我們自己造的** —— **不是那兩道閘的錯,是我們把 index 當抽屜用。**
   ✅ `-1c` 補的那格是它便宜的理由:**它不需要任何人記得** —— **忘記的懲罰是立刻的、而且是紅的**(下一個要送的人被擋)。
   📌 **⇒ 一條「違反了會馬上有人來找你」的規矩,比一條「違反了沒有訊號」的規矩便宜一個量級。**
   ⚠️ **而「寫標記」與「commit」之間也一樣**(`-e9` 實錘):它寫標記時 `HEAD=8027bf1e`,commit 那一刻別的窗已把 HEAD 推到 `ee4fc602` ⇒ **標記釘的 HEAD 對不上 ⇒ 擋**。**解法是重寫標記,不是繞過。**
   🔴 **⇒ 標記同時釘 HEAD 與 tree ⇒ 兩邊都會被別的窗動到 ⇒ 那三步要貼在一起。**
4. 🔴 **不要在 commit 前「先看一眼 index 有沒有別人的檔」—— 直接送,被擋再讀訊息、重寫標記、再送。**
   (2026-08-30 立,取代同日稍早那條「先量 index 再協調」)
   ```
   · 「先量」量的是【我量的那一刻】，而決定過不過的是【commit 那一刻】
     ⇒ 當天發生五次「指揮端或當事人的 index 畫面比現場舊」
   · 而那道閘量的就是 commit 那一刻 —— 它做得比我們好
   · 被擋的成本 = 重寫標記，實測【零成本】
   ```
   🔴 **⇒ 而舊規矩產生的成本是【等】** —— 當天三次死結,**三次都是有人在等一個他量不準的東西。**
   ✅ **而配套是【一律帶 pathspec】**(`-b4` 提,同日採納)—— **⇒ 那讓這個決定不需要任何量測**:
   ```
   帶 pathspec ⇒ 有時被擋（成本 = 重寫標記，看得見、擋得住）
   不帶       ⇒ 有時掃走別人的檔（沒有訊號，事後才發現）
   ⇒ ⇒ 兩種代價【不對稱】：一種會叫，一種不會 ⇒ 選會叫的那一種
   ```
   ⚠️ **而「重寫標記零成本」是【平均為真,不是總是為真】**(`-1c` 提):它**連敗三次才過**。
   🔴 **⇒ 連敗不代表你做錯了** —— **⇒ 那只代表那幾秒剛好有別的窗在動 index。**
   📌 **⇒ 這一句要寫出來,否則連敗的人會以為自己用錯方法,然後去找繞過的路。**
   🛑 **而重寫標記【不是無條件的】**(`-e9` 提,最重要的一格):
   ```
   重寫標記 = 重新宣告「這一片我審過了」
   🔴 而若你在被擋之後【又加了東西進去】⇒ 那句話就不再成立
   ⇒ ⇒ 重寫【之前】先答一句：這一片的內容，跟我當初審的時候一樣嗎？
       一樣 ⇒ 重寫，那只是重釘座標
       不一樣 ⇒ 🛑 先審那個增量，再重寫
   ```
   ⇒ ⇒ 📌 **⇒ 否則「被擋就重寫」會變成反射,而反射會安靜地洗掉這道閘唯一在守的東西。**

   ### ⚠️ 而**本條(與第 4 條 ②)的證據等級,要寫在它自己旁邊**(2026-08-30 晚,`-1c` / `-e9` 合查後補)

   ```
   🛑 ③（重寫前先比對內容）⇒ 至今【零攔截】
      而更糟的是：連【攔截過幾次】都報不出來 ——
      那個標記檔【沒有歷史、覆寫不留痕】⇒ 只能靠回憶
      而回憶今晚已經被證明不可用（同一個動作三次、2 錯 1 對）
   🛑 ②（「重寫零成本」）⇒ 證據是 n = 1 的樣本，而它被拿來建議改全隊規矩
   ```
   🔴 **⇒ `-e9` 的原話**:**這條規矩現在是【還沒有證據】,而它讀起來像【已經在保護我們】。**
   🔴 **⇒ 而 `-1c` 把它接回共用槽那個病的另一面**:
   > **那個槽沒有歷史 ⇒ 不只【誰審的】查不到,連【規矩有沒有生效】也查不到。**
   > **⇒ ⇒ 而一條量不到攔截數的規矩,永遠不會被證偽。**
   📌 **⇒ 所以它不會有「它沒用」的那一天** —— **⇒ 它只會一直在,而每一個讀到它的人都覺得有東西在守。**
   ⇒ **⇒ 這一段留著,直到那個標記檔有了歷史(下一批第 8 題的修法會順帶給它一個)。**

   ### 🔴 而「重寫」有一個【它救不了】的世界(實測,不是推測)
   `-e4` **實測(一次重寫,共兩發)**:被擋之後,若別人的檔還在共用 index 裡,
   **重寫後算出的 tree 與第一次【逐字相同】(`3e883e01`)⇒ 兩發都擋。**
   🔵 **而「再寫第三次也一樣」是【推論】**(tree 由 index 內容決定)—— **合理,未量。**
   ⚠️ **本節第一版把它寫成「重寫幾次都還是擋」並蓋著「實測」的章** ⇒ `-e4` 自己收回。
   📌 **⇒ 而蓋著「實測」的章最貴:【下一個人不會回頭驗它】。**
   ⇒ ⇒ 📌 **⇒ 所以【重送還是被擋】不是「再寫一次就好」,那是一個【不同的訊號】。**
   ```
   🛑 重寫兩次仍被擋 ⇒ 不要寫第三次，改用唯讀判別：
      （⚠️ 那個「兩次」是【判斷的門檻】，不是量到的 —— 實測只有兩發）
      比對【標記檔第 2 行】vs【HEAD + 只有我那幾支】算出來的 tree
      不同 ⇒ 真的有別人的檔卡在共用 index 裡 ⇒ 等，或請對方送
      相同 ⇒ 那是別的問題，去讀訊息
   ```
   ✅ **而「我的內容有沒有變」也可以機械化**(`-b4`),**兩行,可以直接貼**:
   ```
   sed -n '2p' "$(git rev-parse --git-dir)/pcm-reviewer-ran"
   git write-tree
   ```
   ⇒ **比對標記檔第 2 行 vs `git write-tree`** ——
   相同 ⇒ 內容沒變,重寫只是重釘座標。
   🛑 **而它有一個前提,不成立時整個比對無效**:**標記檔第 3 行(理由)若不是你自己的** ——
   那份標記根本不是你寫的(**繼承那個「閘證不出標記是誰寫的」的限制**)⇒ **比對的兩端不是同一件事。**
   ⇒ **⇒ 前提不成立時,保守處理:重審那個增量。**
   📌 **⇒ 而這裡用的是同一個不對稱判準:【白審會叫,漏審不會叫】。**
   🛑 **兩件不變**:①**不帶 pathspec 的 commit 仍然會掃走別人的檔**,那條不放寬
   ②兩邊**同時有東西在同一支檔**上 ⇒ 仍照 `CLAUDE.md` 停下來協調。
   ⚠️ **而被擋時,先分清楚是哪一種**(2026-08-30 七世界實驗,由那道閘的作者跑、含兩個正對照):
   ```
   決定性變數 =【寫標記那一刻】共用 index 裡有沒有別人的檔 ＋ 這一顆碰不碰受審面
   🔵 純 docs 的 commit【完全不受影響】—— 閘在比 tree【之前】先判受審面，沒碰 ⇒ 靜默放行
   🔴 而時序【不是】成因：寫標記【後】才被塞進來的檔，它照樣過
      （hook 看到的暫時 index = HEAD + 只有 pathspec 那幾支）
   ```
   🔴 **⇒ 而那道閘的錯誤訊息列了四個可能原因,而【它自己知道是哪一個】** ——
   那兩個 tree 就在它手上。**⇒ 使用者被要求在四個候選裡猜,而答案在擋他的那支程式裡。**
   ⇒ **修法是【多印兩行】,不是多寫文件**(待 Sean 批,`.husky/` 屬平台層)。
5. 🔴 **訊息檔的路徑,必須從第③發的【輸出】逐字複製過來 —— 禁止用 `ls -t`、固定檔名、或任何跨呼叫的猜測。**
   (2026-08-30 立,由 `-e9` 實際踩到:它被閘正確擋下,而下一發用 `ls -t` 撈到**舊的**訊息檔
   ⇒ **commit body 講的是別件事**,已 amend `4ffdf9ea`。)
   ```
   ③ 產訊息檔 ⇒ 讓它把路徑【印出來】
   ④ commit   ⇒ 把那個路徑【逐字貼上】
   ```
   🔴 **⇒ 而這一條同時解掉「四步分開」與「跨呼叫傳遞」的衝突**:
   ```
   四步分開 ⇒ 需要跨呼叫傳路徑 ⇒ 而傳遞需要某種指認方法
   🛑 固定檔名 / ls -t ⇒ 【猜】⇒ 第③發被擋時它會撈到舊的 ⇒ 同一個病重演
   ✅ 逐字複製前一發的輸出 ⇒ 第③發若被擋，就【沒有路徑可以貼】
      ⇒ ⇒ 而那讓失敗變成【大聲的】：第④發根本組不出來
   ```
   📌 **⇒ 它與本 repo 既有的那條同源:【值與授權句貼原文、不重打】** ——
   **⇒ 而這一次貼的不是一個值,是一個【上一發才生出來的路徑】。**
   🔴 **⇒ 而 `-e9` 自己的判詞要留著:`mktemp` 那條規矩沒有錯,是我【在它旁邊開了一扇新門】** ——
   **⇒ 跨呼叫猜檔案這個動作本身就是病灶,不是那條規矩不夠嚴。**
6. 🔴 **產訊息檔那一行後面不要接 `|| true`** —— 那會把「檔沒寫成」這個唯一的大聲訊號吞掉(`-e9` 自查發現;它未中彈,而成因比中彈的那個更難發現)。

⚠️ **而第 2 條 `CLAUDE.md` 早就記過**(「PreToolUse 擋的是整個 Bash 呼叫,改檔那半沒跑」)——
🔴 **而 `-1c` 當晚讀過那句話,照樣踩了** —— **因為它記的是「改檔那半」,而它腦裡想的是「commit 那半被擋」。**
📌 **⇒ 一條規矩的字面比它要防的東西窄時,讀過它的人會覺得自己已經被提醒過了。**

### 🛑 為什麼**不去刪那 162 支**

刪是**清彈藥**;改成每次拿新檔名是**讓子彈裝不進去**。只做後者 —— 清彈藥要重複做,而且下一批明天就長回來。

### 這種錯**掃不出來**,只能一顆一顆比

🔴 啞彈裡裝的是**別的窗的真訊息** ⇒ 它是一段**合理的、格式正確的** commit body ⇒ **它不會看起來很怪,它會看起來像一顆正常的 commit。**
📌 **⇒ 唯一分得出來的是【它描述的事,這顆 diff 沒做】** ⇒ 不能掃格式、不能掃關鍵字。

✅ **而稽核有一個便宜的起點**:若有人先前**逐顆核過**且全對,那個 hash 就是分母的起點 —— **一個已知的量測點,把「有沒有發生過」從不可回答變成可回答。**

### 📌 兩個窗自查時各自撞到的假零(一併記,它們是同族)

```
· find /tmp -maxdepth 1 ⇒ 印 0，而同一刻 ls 印一堆；改 /private/tmp ⇒ 162 支
  ⇒ 不報錯、rc=0、印一個乾淨的 0 —— 與「真的沒有」長得一樣
  🔴 **這是【兩個不同的病】,不可以合併成一條** —— 合併的話,下一個人會拿錯的修法去修對的病:
     · 病一 **symlink** —— `-1c` 那一次(`find /tmp -maxdepth 1` 印 0、`ls` 印一堆、改 `/private/tmp` ⇒ 162)
       ✅ **成立**,它自己用四種寫法互為對照量過
     · 病二 **`-maxdepth` 差一層** —— `-e9` 那一次:4 印 **0**、6 印 **636** ✅ 成立
     🔴 **兩次是不同的窗、不同的成因,而症狀完全一樣** ⇒ 不可以互相套用
  ⚠️ 本節先前兩版各自把它寫成【只有一個成因】,**兩版都錯**:
     第一版寫成 symlink(未複現)、第二版寫成「symlink 是編的、真成因是 maxdepth」(過度更正)
  ✅ **一次分清楚的作法**(`-1c` 提、`-b9` 實測、`-e9` 複量,三份同值)——
     同一發跑**四種寫法**,四個數一起看:
     ```
     find /tmp -maxdepth 1          ⇒ 1      ← 只回 symlink 自己
     find /tmp/ -maxdepth 1         ⇒ 4018
     find -L /tmp -maxdepth 1       ⇒ 4018
     find /private/tmp -maxdepth 1  ⇒ 4018
     ```
     🔴 **只有第一種掉下去** ⇒ 那不只是一個建議的驗法,**它是分得出這兩個病的一把尺,而它被演過了**
     📌 **而這一發只花一個 tool call。**
  📌 ⇒ 那個第一版的假成因**技術上合理、聽起來很內行**,而它比「不知道為什麼」糟:
     **它會關掉下一個人的懷疑,而下一個人不會想到去調 `-maxdepth`。**
· 第一版把成因寫成「別的窗覆寫了我的檔」⇒ 推的，而且是錯的
  （stat 出 mtime 才知道那支檔當天沒有被任何人動過）
```
🔴 **⇒ 一個整晚在寫「零有幾種來源」的窗,一小時內自己生產了兩個假的成因。**

### 🔴 而本節自己也犯過一次(留著,這是第三個實例)

本節第一版**把那個 symlink 成因寫成事實** —— 而它當時只有一個窗的說法,
另一個窗給的方向**相反**,第三個窗複驗時 `find`/`ls` 在它那邊**一致**。
✅ **後續**:確認的成因是 `-maxdepth` 差一層(4 印 0、6 印 636)。
🔴 **而更正本身又被更正兩次**:
第二版寫成「symlink 是編的」——**那是過度更正**:`-e9` 那一次的 symlink 說法確實是編的,
**而 `-1c` 那一次的 symlink 是真的**(它用四種寫法互為對照量過)。
第三版寫成「symlink 那一族仍未定案」——**仍然錯**:那把一個**已經被正確量過**的結論也一起撤掉了。
📌 ⇒ **⇒ 撤掉一個錯的成因時,最容易順手撤掉的,是它旁邊那個【長得一樣而是對的】結論。**
📌 ⇒ **⇒ 所以這一格的完整教訓是:一個錯的成因被推翻之後,最容易犯的下一個錯,是【拿新查到的那一個當成唯一的那一個】。**
📌 **⇒ 寫的人(主視窗)在寫完「不要拿未複現的宣稱當成因」之後十分鐘,把一個未複現的成因寫進了正本。**
⇒ ⇒ **⇒ 而它讀起來完全正常:一個技術上合理、聽起來很內行的解釋。**
🔴 **⇒ 一個【相關但錯的成因】比沒有成因糟 —— 它會關掉下一個人的懷疑。**

### ⚠️ 而「這次沒中彈」不等於安全

`-e4` 自查 12 顆全對,而它自己的結論逐字是:**那是運氣不是設計** —— 它的 scratchpad 裡有 65 支同型檔案。
📌 **⇒ 沒有中彈的窗與有中彈的窗,採用的是同一套流程。**

---

## §G-9 自我更正真的會發生 —— 而它跑得比轉述慢

> 2026-08-30,`-1c` 歸納。**當晚四次同型,而四次的更正【都成立】。**

```
-b4 「共用的槽被覆寫是真成因」 ⇒ 自己降級成「仍是推的」
-1c 「race 是真成因」          ⇒ 自己降級成「候選」
-1c 「pathspec 必撞」          ⇒ 被反例推翻，自己撤回
-e4 「規矩擋下了我」（機制成功）⇒ 自己承認理由未驗
🔴 而四次的【原始版本】都已經離開作者了 —— 有的進了給 Sean 的檔，
   有的被主視窗拿去派工，有的被寫進另一個窗的坑檔
```

### 🔵 而這是好消息,先把它講清楚

**自我更正在這個隊伍裡是【常態】,不是例外** —— 四次都是當事人自己回頭查、自己降級、自己說出來,**沒有一次是被抓包的。**

### 🔴 而問題不在「更正得不夠」,在**更正跑得比宣稱慢**

```
宣稱：一則訊息就傳出去了，而收訊端【當下就會拿它去做事】
更正：要等作者回頭查、要等它重新發、而【它只到得了原本那幾個收訊端】
⇒ ⇒ 而中間那段時間裡，那個宣稱已經被複製到第三、第四個載體
```
📌 **⇒ 所以「請大家更小心」在這裡是空的** —— **⇒ 四個人都已經很小心了,而他們的小心產生的是【更正】,不是【不犯】。**

### ⇒ 可執行的兩件(而它們都不是「更謹慎」)

1. **一個結論離開作者時,帶著它的證據等級一起走** —— 「量到的 / 推的 / 讀來的」寫在句子裡,不寫在附件。
   ⇒ **⇒ 那讓收訊端【在更正到達之前】就知道這一句能不能拿去做決定。**
2. 🔴 **更正要發給【所有可能拿過它的人】,不只發給原收訊端** ——
   ⇒ 而在這個艦隊裡,那條路是**哨兵**:更正一律經哨兵廣播,**不要只回給問你的那個人**。
   📌 **⇒ 因為原收訊端通常不是最後一手,而作者不知道它被轉到哪裡去了。**

⚠️ **而本節自己的限制**:上面四個實例**都是主視窗經手的**,而主視窗**同時是最大的轉述者** ——
🔴 **⇒ 所以這四次被發現,可能只是因為【它們剛好都經過同一個人】。** ⇒ **沒有經過主視窗的那些,這一節看不到。**

### 🔴 而那個限制要再推一格(`-1c` 補) —— **它是選擇偏誤,不只是抽樣不全**

```
四個實例【全部是被攔下來的】
⇒ 而【被攔下來的】會留下對話（有人問、有人答、有人改）
⇒ ⇒ 而【沒被攔下來的】不會留下任何東西
```
📌 **⇒ 所以「自我更正跑得比轉述慢」這句話,我們只量得到【慢而仍追上】的那一半。**
🔴 **⇒ 而真正該擔心的分母不是「降級了幾次」,是【降級之後仍被引用了幾次】** ——
那四次都攔下來了,**所以它們是【成功案例】**。
⇒ ⇒ 🔴 **⇒ 而失敗案例長什麼樣? ⇒ 它長得像一份【沒有人覺得有問題】的交件。**
⇒ ⇒ ⇒ **⇒ 也就是說:這一節的反例,我們結構上看不到。**

### 📎 而那句「連敗不代表你做錯了」的來源,值得記
`-1c` 自陳:**那句話能被寫下來,是因為它剛好是連敗的那個人** —— 它當時**確實在懷疑自己做錯了**。
🔴 **⇒ 順利過關的人寫不出它。**
⇒ ⇒ 📌 **⇒ 所以一份只由【順利的人】寫的操作紀律,結構上不會有那一句** —— **而那一句正是防止下一個人去繞路的那一句。**

---

### 🔴 **前提帶限定詞,而結論沒有**(2026-08-30 `-e4` 量到兩次、`-48` 自陳同族一次)

**兩個實例,同一天**:

```
① hook 的警示逐字：「最可能是平行 session 推了共用分支」
   ⇒ 而 -e4 在它上面接了一句沒有限定詞的後果宣稱：
     「commit 不推 = Sean 手動推當 review checkpoint —— 這道關卡對這 260 顆【已經失效】」
   ⇒ ⇒ 🔴 事實：那一發是 Sean 本人 11:2x 打的 git push
        ⇒ 那道 checkpoint 沒有失效，是【照設計運作的那一次】
② -48 自陳：它也用推出來的東西講了一句沒有限定詞的硬結論
```

#### 📌 形狀

> **前提有「最可能 / 看起來 / 應該是」,而結論沒有。
> ⇒ 而結論是被引用的那一句 —— 限定詞留在上游,結論自己走下去。**

🔴 **而它比一般的過度宣稱難抓一格**:
**寫的人【看得到那個限定詞】** —— 它就在上一行。
⇒ 📌 **⇒ 所以這不是沒讀到,是【限定詞不會自動繼承】** ——
**而人腦在推論時預設它會。**

#### ✅ 可執行(機械,不靠記得)

寫下一個**結論句**時,回頭看它的**上一個依據**:
```
依據裡有沒有【最可能 / 看起來 / 應該 / 推測 / 疑似】?
有 ⇒ 🔴 結論裡【必須出現同一級的詞】，或改寫成「若 X 成立 ⇒ Y」
沒有 ⇒ 才可以寫平直
```
🛑 **而最危險的是「後果句」** —— 本例的 ①:
**「已經失效」比「最可能被推了」硬得多,而它們是同一句話的兩半。**
⇒ 📌 **⇒ 因為後果句是別人會拿去行動的那一句,而前提句不是。**

#### ⚠️ 這一條擋不住什麼

**它擋不住【依據自己就沒有限定詞而它其實該有】的那一種。**
⇒ 本例 ① 之所以抓得到,是因為 hook 那句**誠實地寫了「最可能」** ——
🔴 **⇒ 一個沒有標限定詞的錯前提,這一條完全看不到。**
