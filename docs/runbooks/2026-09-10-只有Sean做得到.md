# 只有 Sean 做得到的事 · 2026-09-10

> 🎯 **這張表上的每一件,都【不是工程做不完】,是【工程做不到】** —— 它要一個我們沒有的權限、
> 一雙我們沒有的眼睛,或一個只有他知道的答案。
> 🔵 **按【幾分鐘】由少到多排。** 📌 **他看到「五分鐘」會做,看到一列錨不會。**
>
> 🛑 **不含「等他批一份 plan」那一類** —— 那是流程,不是他獨有的能力。

---

| # | 錨 / 行 | 他要做的那一個動作 | 幾分鐘 | 做完會關掉什麼 |
|---|---|---|---|---|
| 1 | `⟦tidy-NETPUBLICALL⟧` `:2492` | **打開 Supabase → API 設定,唸一次「Exposed schemas」那一行,看裡面有沒有 `net`** | **1 分** | 🔴 那是今晚最重的一格。有 `net` ⇒ **拿到 anon key 的人可以從我們的庫發任意 HTTP + 清空回應表**(而 anon key 印在前端);沒有 ⇒ 嚴重度掉一級。**兩種答案的下一步完全不同。** |
| 2 | `⟦b4-ALERTROOTUNVERIFIED⟧` `:2497` | **打開 healthchecks.io,看那支 check 有沒有接上通知管道**(Email / LINE 都算) | **2 分** | 九支排程的偵測全部收斂到一支,而**它自己的偵測者沒有人驗過**。沒接 ⇒ 那條鏈最後一環是斷的。 |
| 3 | `⟦b4-TESTACCT1⟧` `:1076` | **打開 Supabase → Authentication → Users,看有沒有一個測試帳號還躺在那裡** | **2 分** | 🔵 **我今天查了,`auth` schema 對我的唯讀角色是 `permission denied`** ⇒ 這一格**這個窗結構上驗不到**。 |
| 4 | `⟦mail-PROBESNOSTOP⟧` `:2361` | **打開 Supabase → Storage,看標籤圖那個 bucket 裡是不是空的** | **2 分** | 同上 —— **`storage` schema 也是 `permission denied`。** |
| 5 | `⟦b4-MGRENV1⟧` `:984` | **打開後台任一頁,看最上面那條 manager 警示帶在不在** | **3 分** | 後台不能用 curl 驗(入口在 quote 站一律 429)⇒ **只有真的開瀏覽器看得到。** |
| 6 | `⟦f3-PDPSKUSTATIC⟧` `:495` | **開一個有規格的商品頁,選一個不同顏色,看上方那行「原廠料號」有沒有跟著變** | **3 分** | 選紅色仍印母料號 ⇒ 客人會照著錯的料號下單。 |
| 7 | `⟦front-PPSHELLXVSDESIGN⟧` `:2469` | **用手機開目錄頁,看左右留白**(稿是手機 12 / 桌機 32,而今天兩邊都是 40) | **3 分** | 一個純視覺的差,而**視覺是你的**。 |
| 8 | `⟦0e-PROBENOSCHED⟧` `:927` + `⟦b9-PROBESCHED⟧` `:1178` | **回答一句:那支曝露探針,這 17 天有沒有人手動跑過?** | **1 分** | 🔵 **我證得出「沒有排程」,證不出「沒有人跑」** —— 板上那句「17 天來那個人」講的是後者。⚠️ **這兩列是同一件事被算兩次。** |
| 9 | `⟦mail-FETCHTIMEOUT⟧` `:1612` | **問一句:那份「寄信路徑 52 條 findings 的逐條複核正本」在哪?** | **2 分** | 我掃 `docs/reviews/` 與「52 條」都撈不到。📌 **一個指不到正本的標籤列,與一個正本不存在的標籤列,在板上長得一樣。** |
| 10 | `⟦0a-CARDCANCELNOREFUND⟧` `:2343` | **拍一句:取消一張刷卡單,系統要不要【自動】去退那筆錢?** | **5 分** | 🔵 **那一列自己就寫著「本列不含這個決策,那是 Sean 的題」。** 今天是「零退刷零提醒」,而那是**事實**不是缺陷。 |
| 11 | `⟦b4-CRON6HB⟧` `:811` 一族 | **用手機開行動網路(不要 Wi-Fi)走一次顧客站** | **5 分** | 答「一個乾淨的客人會不會被 429 擋住」——**我們的 IP 全部進過黑名單,量不到乾淨的那一種。** |
| 12 | **B7 那一列** `:536` | **自己從頭走一遍後台登入 → 下單 → 出貨**(上線前本來就要走) | **20 分** | 🎯 B7 的限定②「真的用 `service_role` 讀一列」——**我今天量到那條路唯一的入口是走 API / 走後台**,而那正是你要走的那一遍。**不是為這一列多做的事。** |

---

## 🔵 而有兩件今天【已經被你關掉了】,記在這裡免得再問你一次

```
✅ ⟦mail-TXNMAILSPAM⟧ :934 · :1083   你 2026-09-10 逐字「email 沒有進垃圾」
   ⇒ :934 已修好(那一格要的就是「有人在真的信箱裡看過」)
   ⇒ :1083 條件已消失(一封實證)
   🛑 而措辭收窄:你證到的是【那一封】進了收件匣, 不是【每一種信】都不會進垃圾
```
🎯 **⇒ 那兩列擋了很久,而關掉它們的動作是【打開信箱看一眼】。**
📌 **而沒有任何一個窗做得到那件事 —— 不是能力問題,是授權問題。**

---

## 🛑 這張表怎麼來的(下一個人要能重做)

```
🔴 我第一版用關鍵字掃全板(面板 / 肉眼 / 他的題 / 問人)⇒ 命中 74 列 —— 太寬。
   成因:很多列【提到】Supabase 面板, 是因為那是【病因】(有人在面板上手動改權限),
        而不是【要 Sean 去開面板】。
   ⇒ 🎯 又是「提到」vs「是」—— 而判準是【那一步的動作】不是【那一列的主題】。
✅ 這一版是從【今晚實際判過的那些列】收的:每一列我都說得出「為什麼工程做不到」。
```

## 🛑 這張表證不到什麼

· **【證不到】它是完整的** —— 我沒有逐列開檔掃全板 485 列,**這是今晚撞到的那些**。
· **【證不到】那些「幾分鐘」準** —— 是估的,而**估的目的是讓他願意開始**,不是排程。
· **【證不到】第 11 項那個 429** —— 那一格是別條線量到的,**我照抄,沒有複驗**。
