# Sean 走一遍:2026-09-10 收盤版

> 🔵 **[2026-09-11 窗 C 更新]** 這份原稿寫在**稅務那三片落地之前** ⇒ 有兩格的預期值過期了。
> **我在原稿上改,不重寫** —— 改動處都標 `[09-11 C]`,而**原字面用刪除線留著**。
> 🛑 **而我憑什麼驗那幾格**:那三片是我改的,而下面每一格我都**在鑽機上實際按過一次**
> (`admin-probe`,零正式庫寫入),讀數逐字貼在那一格裡。
> ⚠️ **而 a9 原稿把它驗不到的兩格標成【轉述】而不照抄 —— 那個紀律我沒有破,我是去按了才寫。**

> 🎯 **這份照【你坐下來的操作順序】排,不是照我們發現的順序。**
> 每一格三件事:**要多久 · 看什麼 · 壞了會怎樣**(一句話,不用技術名詞)。
>
> ## 🔴 全篇只有【一格】是不可逆的 —— 第 7 格「叫車」。它用 🚨 標。
> 其餘每一格做錯了都救得回來,而那一格**按下去就有一台真的車開過來**。
>
> ## 🔵 另外兩份不重複寫,直接指過去
> · **只有你做得到的**(權限 / 面板 / 只有你知道的答案)⇒ `docs/runbooks/2026-09-10-只有Sean做得到.md`
> · **你自己下一單**(客人那一側:註冊 → 結帳 → 刷卡 → 收信)⇒ `Sean自己下一單-要看什麼-20260910.md`
> 📌 **這一份是【後台】那一側。**

---

## 一頁的路線(全部做完約 25 分鐘)

| # | 在哪 | 做什麼 · 看什麼 | 多久 | 壞了會怎樣 |
| --- | --- | --- | --- | --- |
| 1 | 後台 → 建單 | 統編那格打 `22099131`,按旁邊的**「查抬頭」** | **30 秒** | ⛔ ~~今天它一定會說「還沒接上來源 —— 請自己打抬頭(這不是你打錯)」~~ 🔵 **[09-11 C]那句話【不一定】是那一句 —— 它有四種,而哪一種出現看你的登入**(`invoice-title-lookup-button.tsx:124-131`):`統編要 8 碼數字`(你打錯)· `你的登入沒有這個權限 —— 請自己打抬頭` · `查抬頭還沒接上來源 —— 請自己打抬頭(這不是你打錯)` · `查不到 —— 請自己打抬頭`。🔬 **我在鑽機上按過**:它出現的是**「你的登入沒有這個權限」**(那台沒有登入票)。🎯 **你要看的是【它有沒有說人話、有沒有告訴你下一步】,不是它說哪一句** —— 四句都叫你「請自己打抬頭」,而那正是今天的正解。而位置順不順手要你看一眼 |
| 2 | 後台 → 建單 | ⛔ ~~單價 1048、稅基選「含稅」、勾開發票 ⇒ 總計 1,100~~ 🔴 **[09-11 C]那組數字是錯的 —— 它把兩種填法混在一起了。** `1048` 是**未稅**那條路的數字,而它叫你選**含稅** ⇒ 🔬 **我實按:畫面給 1,048,不是 1,100。** ✅ **改成打 `1100`、稅基選「含稅」、勾開發票** | **1 分** | 🔬 **我實按的讀數**:小計 1,048 · 營業稅(5%) 52 · **總計 1,100** ⇒ **正好湊回你打的那個數**。🔴 錯了(例如給 1,101 或 1,048)⇒ 你打的含稅數字回不來 |
| 3 | 後台 → 出貨清單頁 | 那一頁打得開,四箱都在,而「狀態」欄各寫什麼 | **1 分** | 打不開 ⇒ 出貨那條路整條看不到 |
| 4 | 同上,任一列最右邊 | 那一欄有**四個東西**:訂單明細 · 出貨明細單 · 託運標籤 · **叫車**<br>🔵 前三個是列印,第四個是動作 | **30 秒** | 少一個 ⇒ 那條路今天沒有入口 |
| 5 | 按「託運標籤」 | 印出來的**實體紙**:貨號 · 箱號 · 收貨人 · 條碼掃不掃得到 | **3 分** | 條碼掃不到 ⇒ 司機收不了件 |
| 6 | 出貨清單頁 | 看那一箱的「叫車」鈕**能不能按**,不能按的話它寫什麼理由 | **30 秒** | 寫得不清楚 ⇒ 員工不知道要做什麼 |
| 7 | 🚨 **叫車** | 🔴🔴 **按之前先叫我們。** 送出去的那一包已經逐欄印給你看了 | **5 分** | 🚨 **不可逆:一台真的車會開過來。** 而按下去之後**那顆鈕就永遠按不下去了** |
| 8 | 後台任一頁 | 最上面那條 manager 警示帶在不在 | **30 秒** | 不在 ⇒ 權限那道閘可能沒生效 |
| **2b** | 後台 → 建單(接著上一格) | 🔵 **[09-11 C 新增]** 把那顆「**這張單要開發票**」**取消勾**,看下面那個預覽 | **20 秒** | 🔬 **我實按**:總計掉回 **1,048**,而「營業稅」那一列**整列消失**。🎯 **這就是你 09-10 逐字要的那件事**(「勾選開發票自己幫我 +5% 上去計算」)—— 而它**在今天之前一次都沒有發生過**(那個預覽一律印「沒勾」的答案)。不會變 ⇒ 你又看不到稅了 |
| **2c** | 同上 | 🔵 **[09-11 C 新增]** 單價打 `1100`、稅基**留在預設「未稅」**、勾開發票 | **20 秒** | 🔬 **我實按**:總計 **1,155**。🔴 **而那正是你說的「會多收 5%」** —— 你心裡是含稅 1,100,而系統當它未稅。🎯 **要看的是:你【當場】就看得到 1,155,而不是建完單才發現。** 看不到 ⇒ 那道保護沒了 |
| **2d** | 後台 → 建單,品項那一區 | 🔵 **[09-11 C 新增]** 讀那句橘字**三行**,看它有沒有告訴你「手上只有含稅價怎麼辦」 | **20 秒** | 今天它逐字是:「請填**未稅**…稅由系統自己算 / 🔵 手上只有含稅價?把右邊那格切成**含稅**,系統會自己換算回未稅 / 🛑 而**不切**就直接填含稅數字 ⇒ 勾了開發票會多課一次稅」。🎯 **三句話三個動作:預設怎麼做 · 例外怎麼做 · 做錯會怎樣。** 少了中間那句 ⇒ 你會以為含稅價不能填 |

---

## 🚨 第 7 格:按之前要知道的三句

```
① 這一包只有三格:貨號 8947081964 · 箱號 S9FC6P · 派遣者「派達有限公司」
② 🔴 只有一格帶中文(派遣者), 而它【一次都沒有被真的送出去驗過】
   ⇒ 回錯誤的話, 最可能的嫌疑犯就是那六個字
③ 🛑 按下去不對勁 ⇒ 【不要再按第二次】, 打電話問新竹那張單有沒有排到車
   (單號 894-708-1964 · 客代 1307)
```
📎 逐欄那份:`叫車那一包-送出前逐欄-20260910.md`
📎 卡住怎麼救:`docs/runbooks/hct-dispatch-stuck-manual-reset.md`
🔴 **而那份 runbook 一次都沒有被走過** —— 它是照程式碼寫的,不是照經驗。
🛑 **而清掉那個記號【本身就有風險】**:「同一箱重複派遣會怎樣」新竹沒寫、我們沒問過。

---

## ⚪ 你【不用看】的(今晚已經被機器驗過)

📌 **這一段跟上面那張表一樣重要 —— 它讓你相信那份短清單是真的短。**

```
✅ 合併之後三綠 + 全套 931 檔測試(只有 1 個既有的紅, 與今天的改動無關)
✅ 含稅那支算法:九情境 + 十一顆反例在拋棄式資料庫上跑過, 而且已經貼上正式庫
✅ 叫車那支:36 格 + 四發破壞測試, 而閘預設關 ⇒ 沒有人不小心送得出去
✅ 貨運備註:18 格 + 五輪對抗審查(六顆問題全修)
✅ 十支排程全部在跑, 而心跳表 10 支都在寫(唯讀實測)
✅ healthchecks 五支全部 up, 累計 ping 次數與各自班次對得上

🔵 [09-11 C 追加] 而今晚多驗掉的:
✅ 含稅那條路的 app 半:對【真的 RPC】跑過七格(拋棄式 PG, 零正式庫寫入)——
   含「送一個算錯的未稅進去 ⇒ RPC 自己會拒」那一格
   ⇒ 📌 那一格證的不是「我算對了」, 是【算錯了會被抓到】
✅ 出貨明細單:缺電話 / 缺地址不再阻印, 改成紙上標注(45 檔 863 格)
✅ 建箱彈窗:缺電話會出聲(而不是靜靜放行), 兩格都缺時兩句話都在
✅ 出貨清單每一列點得到列印 —— 那三件其實【早就做完了】, 只是沒有人回頭看畫面
```

---

## 🔵 而三件【還不要排】

```
⛔ ~~· 勾發票數字當場變 —— 另一個窗正在修, 做完再排(而我沒有親自驗過, 這一句是轉述)~~
   ✅ **[09-11 C]做完了, 已排成第 2b / 2c 格** —— 我實按過, 讀數在那兩格裡。
⛔ ~~· 建箱彈窗那句「無電話」的警告 —— 我在 shipment-section.tsx 裡沒有找到那句字面~~
   🔵 **[09-11 C]你沒找錯地方, 是【兩棵樹】**:那句話寫在 `agent/mob`, 而你讀的是 `dev`。
   📌 **而「它不在這支檔」與「它還沒被寫出來」在 grep 的結果裡長得一模一樣。**
   ✅ 它在 `apps/admin/src/lib/shipping/recipient.ts` 的 `recipientWarning()`
      (**不是** `shipment-section.tsx` —— 那支是訂單頁那張卡, 另一件事)。
   🛑 **而它今天還沒進 dev ⇒ 這一格【仍然不要排】**, 等合併之後再排。
· 統編查抬頭【真的查得到】—— 要先決定去哪裡查(甲/甲′/乙), 那還在等你
   🔵 **[09-11 C]而那顆鈕本身今天按得動、四種訊息都說人話** ⇒ 見第 1 格。
```

---

## 🛑 這一份證不到什麼

1. **它只涵蓋後台那一側。** 客人那一側在另一份。
2. **第 5 格「條碼掃不掃得到」我證不到** —— 那要一台真的掃描器。
3. **第 1、6 格的「順不順手」是你的判斷**,不是對錯題。
4. **每一格的「多久」是估的**,不是量的。

### 🔵 [09-11 C] 而我這一輪的更新證不到什麼

5. **我按的是【鑽機】,不是正式後台。** 三個數字(1,100 / 1,048 / 1,155)是那台算的
   —— 而我**另外**拿同一組輸入餵過真的 RPC,兩邊一致;**但那也是鑽機上的 RPC。**
   ⇒ 📌 **「這支 SQL 的行為」與「貼到正式庫會怎樣」是兩件事。**
6. **第 1 格我按到的是「沒有權限」那一支** —— 因為鑽機沒有登入票。
   ⇒ 🛑 **你按下去看到哪一句我證不到**,我只證得到「四句都在碼裡、四句都叫你自己打抬頭」。
7. **2b / 2c / 2d 三格今天在 `agent/mob`,還沒進 `dev`** ⇒ **合併之前你看不到它們。**
   ⇒ 🔴 而那正是 a9 那一格「找不到那句字面」的成因 —— **兩棵樹,不是不存在。**
8. **我沒有真的建出一張單** —— 上面那些數字是【預覽】與【直接餵 RPC】,
   而**從畫面按下「建立訂單」那一步我沒走**(鑽機卡在操作員票)。
