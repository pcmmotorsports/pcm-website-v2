# A7 取消真相表回滾 runbook(order_cancellations / order_cancellation_items)v4

> 依 A7 plan §10(:309-339)撰寫;承接時點=A8a1 開工前置;A8a1 片落檔、關卡1 R1 折入升 v2;
> A8a1 關卡2 雙線折入升 v3(步 0 退款線 FK 前置/步 3 閘排除 A7-t/步 4 併拆 helper 函式/
> 2c 無條件化/2b ACL 斷言可執行化);Fable R3 折入升 v4(**步 8 A5a 回權**——a4a 步⑦ 前提在
> 本 runbook 終態永不可滿足、缺本步=採購寫入永久 silent 停擺;2c 補部分保留案逐單 DML)。
> 執行者=有 context 的 AI session+Sean 拍板。Supabase forward-only ⇒ 回滾=災難當天另立
> down migration,本檔=其執行程序。🔴 現況版改編(v2 擴列):①A1 落點=`order_item_quantity_summary`
> ②A8c 守門版本用 functiondef md5 機器判別 ③**A2b1/A4a 已 apply 且函式體引用取消表**(v1 漏,
> 關卡1 抓=DROP 後 42P01 寫入炸彈)。

## 0. 唯一有序程序(v2:消 v1「一般序 vs 資料分支」自撞;逐步走、每步有閘)

0. **退款線先行(v3 補;關卡2 抓)**:`order_refund_jobs` 的 `orj_cancellation_fk`(複合 FK →
   `order_cancellations`、ON DELETE RESTRICT;RW1a 2026-08-03 新增、晚於 A7 plan §10 成文)
   會擋步 2e 的 DELETE(有列時)與步 7 的 DROP(**無論列數**,2BP01)。⇒ 先走退款線自己的
   回滾(或 Sean 拍板後單獨 escape `order_refund_jobs` 資料 + DROP 該 FK)。
   閘:`SELECT count(*) FROM pg_constraint WHERE conname='orj_cancellation_fk'` = 0。
1. **拆 writer**:DROP `admin_cancel_order` 全部 overload。
   閘:`SELECT count(*) FROM pg_proc WHERE pronamespace='public'::regnamespace AND proname='admin_cancel_order'` = 0(枚舉式、防殘留 overload)。
2. **資料 escape**(a/b/d/e=兩表任一有列才需;**c=無條件跑**,v3 更正;writer 已拆 ⇒ 零新寫入、無 TOCTOU):
   a. 同一交易:CTAS 兩張 archive(`…_archive_YYYYMMDD`)+ ACL 三件套 + 計數比對:
   ```sql
   BEGIN;
   CREATE TABLE public.order_cancellations_archive_YYYYMMDD AS TABLE public.order_cancellations;
   CREATE TABLE public.order_cancellation_items_archive_YYYYMMDD AS TABLE public.order_cancellation_items;
   ALTER TABLE public.order_cancellations_archive_YYYYMMDD ENABLE ROW LEVEL SECURITY;
   ALTER TABLE public.order_cancellation_items_archive_YYYYMMDD ENABLE ROW LEVEL SECURITY;
   REVOKE ALL ON TABLE public.order_cancellations_archive_YYYYMMDD FROM PUBLIC, anon, authenticated, service_role;
   REVOKE ALL ON TABLE public.order_cancellation_items_archive_YYYYMMDD FROM PUBLIC, anon, authenticated, service_role;
   GRANT SELECT ON TABLE public.order_cancellations_archive_YYYYMMDD TO service_role;
   GRANT SELECT ON TABLE public.order_cancellation_items_archive_YYYYMMDD TO service_role;
   COMMIT;
   ```
   b. **窮舉斷言(v2 補全;兩張 archive 各跑;v3 改可執行 SQL,`YYYYMMDD` 代換後直接跑)**:

   ```sql
   SELECT coalesce(string_agg(g, ',' ORDER BY g), '<EMPTY>') AS non_owner_acl
     FROM (SELECT DISTINCT CASE WHEN a.grantee = 0 THEN 'PUBLIC' ELSE a.grantee::regrole::text END
                  || ':' || a.privilege_type || CASE WHEN a.is_grantable THEN '+GRANTOPT' ELSE '' END AS g
             FROM pg_class c, aclexplode(c.relacl) a
            WHERE c.oid = 'public.order_cancellations_archive_YYYYMMDD'::regclass
              AND a.grantee <> c.relowner) t;
   -- 期望恰 = 'service_role:SELECT';另斷言:
   SELECT relrowsecurity, relforcerowsecurity FROM pg_class
    WHERE oid = 'public.order_cancellations_archive_YYYYMMDD'::regclass;  -- 期望 t / f
   ```

   (⚠️ v3 補註:archive `ENABLE RLS` 且零 policy、owner=postgres ⇒ **service_role 實際讀不到**
   (非 owner、RLS 空集合;memory `reference_pg-has-table-privilege-not-rls-passthrough`)——
   fail-closed 是刻意方向,GRANT SELECT 只為 ACL 斷言形狀;災難當天要讀 archive=owner(postgres
   /dashboard SQL editor)直讀,別誤以為 service_role 端看得到。)計數=原表。
   c. 🔴 **orders 對客欄處置(v1 漏;v3 改無條件跑——ledger 兩表為空但 `cancelled_at` 非空的
   殘缺態也要處置,否則守門撤了、對客欄還留著)**:先跑
   `SELECT count(*) FROM orders WHERE cancelled_at IS NOT NULL`,>0 就走本步(與兩表列數無關);
   escape 名單=`SELECT id, display_id, cancelled_at,
   cancelled_reason FROM orders WHERE cancelled_at IS NOT NULL` 交 Sean 逐單拍板:
   **預設案=清 NULL(訂單復活、回 active)**;保留案=留註記但基底付款 RPC 不守門(風險自認)。
   拍板前不得進第 3 步。**預設案(全清)的可執行 DML+斷言**:
   `UPDATE public.orders SET cancelled_at=NULL, cancelled_reason=NULL WHERE cancelled_at IS NOT NULL;`
   → 斷言 `count(*) WHERE cancelled_at IS NOT NULL` = 0;保留案=零 DML、拍板記錄留檔。
   **部分保留案(v4 補;Fable R3 F4——Sean 逐單拍板後對「清」的單逐一跑)**:
   `UPDATE public.orders SET cancelled_at=NULL, cancelled_reason=NULL WHERE id = '<該單 uuid>';`
   → 斷言=清單上「清」的單 `cancelled_at IS NULL`、「保留」的單非空(逐單核對 escape 名單)。
   d. Sean 覆核 archive 筆數+內容+對客欄拍板。
   e. **清空原表(v2-R2 補;v1 漏了這步 ⇒ 第 5 步「資料已零」走不通)**:同一交易
   `DELETE FROM public.order_cancellation_items; DELETE FROM public.order_cancellations;`
   (先子後父;**用 DELETE 不用 TRUNCATE**——A7-t 的 TRUNCATE 攔截 trigger 不留逃生門,
   而 DELETE 至 header 全零在 DEFERRED 檢查=合法)→ 斷言兩表 0 列 ⇒ escape 完成。
3. **拆 A4a/A2b1 對取消側的依賴**(照各自 plan 回滾節;A4a 有 rehearsal 腳本)。
   閘:`SELECT count(*) FROM pg_proc WHERE pronamespace='public'::regnamespace AND prosrc LIKE '%order_cancellation%' AND proname NOT IN ('begin_charge_attempt','confirm_order_payment','pcm_assert_cancellation_has_items')` = 0
   (🔴 v2:A2b1/A4a 的**函式體**引用兩表,只查 trigger 不夠、殘留=DROP 後 42P01;
   🔴 排除 A8c 兩支——它們由第 5 步 md5 閘管,不排除則本閘被自己擋死;
   🔴 v3 再排除 `pcm_assert_cancellation_has_items`——A7-t 的 helper,第 4 步才拆,
   不排除則本閘在本步恆 ≥1、程序卡死(關卡2 兩線同抓、54331 實查證實))。
4. **拆 A7-t**(四支 constraint trigger + **v3 補:兩支 helper 函式
   `pcm_assert_cancellation_has_items`、`pcm_cancellation_ledger_block_truncate` 一併 DROP**,
   否則第 7 步 DROP 表後留下引用已刪關係的孤兒函式)。
   閘:兩表上非 internal trigger = 0,且
   `SELECT count(*) FROM pg_proc WHERE pronamespace='public'::regnamespace AND proname IN ('pcm_assert_cancellation_has_items','pcm_cancellation_ledger_block_truncate')` = 0。
5. **A8c1/A8c2 守門回基底**(各自 plan §7 決策樹;此刻資料已零+writer 已拆 ⇒ 其分支1 條件成立):
   閘:`md5(begin)`=`55e32431df7af3ab6eefa18555af9e6b` 且 `md5(confirm)`=`8eeb4274cbaede45a34818657ead7537`。
6. **A1 回滾**(summary 表;閘:`to_regclass('public.order_item_quantity_summary') IS NULL`)。
7. **DROP A7 兩表**:先 `order_cancellation_items` 再 `order_cancellations`(FK RESTRICT;
   v3:前提=步 0 已拆 `orj_cancellation_fk`,否則此步 2BP01)。
   down migration 自帶 BEGIN/COMMIT、版本號大於 ledger 尾、含上列全部閘的 SQL 斷言。
8. **A5a 回權(v4 補;Fable R3 F1——漏了本步=採購寫入永久 silent 停擺)**:步 3 走 a4a runbook
   時其步驟① 已 REVOKE `admin_upsert_item_procurement`;而 a4a runbook 步⑦ 的回權前提
   (A1+A4a 重放)在**本 runbook 終態永不可滿足**(A4a 依賴已 DROP 的取消表)⇒ 回權只能走
   本步、用 a7 專屬前提:
   ☐ 步 3-7 全部完成 ☐ 當日實查 `admin_upsert_item_procurement` prosrc 不含
   `order_item_quantity_summary`(2026-08-04 於 54331 副本實查=0 命中;prosrc 若已改需重評)
   ☐ Sean 確認採購功能要繼續營運。然後:

   ```sql
   -- 🔴 簽章 = 12 參(A9h-M 20260806200000 起;末參 p_preserve_optional_fields boolean)。
   --    型別清單少一個 boolean ⇒ 本句當場 undefined function、回權在唯一需要它的那天卡死。
   GRANT EXECUTE ON FUNCTION public.admin_upsert_item_procurement(
     uuid, uuid, integer, text, text, timestamptz, text, text, date, text, text, boolean) TO service_role;
   SELECT has_function_privilege('service_role',
     'public.admin_upsert_item_procurement(uuid,uuid,integer,text,text,timestamptz,text,text,date,text,text,boolean)', 'EXECUTE');
   ```

   閘:上句回 `t`。不做本步的後果=員工按儲存只收權限錯誤、零告警(a4a runbook MF2 同款)。

## 1. DB 擋不到的(人工核對)

- A8b enqueue RPC(第 3 批、無實名)=人工確認未上線。
- 應用層 consumer(A9g/A9d2/A13a-b/A11a-c/A9c/A9d1)=部署版本層面確認。
  (🔴 v2:A2b1/A2b2 已自此清單移除——它是 DB 物件,由第 3 步閘機器驗。)
- `admin_audit_log` 取消稽核列=稽核痕跡,不隨回滾刪(此語意限本 runbook/down migration;
  RPC 執行失敗時 audit 與取消同交易一起 rollback,屬正常交易性、非本條範圍)。
