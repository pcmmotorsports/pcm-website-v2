# 2026-09-10 `⟦5b-AUDITGRANTEXPIRY⟧` —— 六格重量,**而判準三是提出以來第一次真的被跑**

> 這一列的工作形態,板上自己寫得最清楚:
> 🔴🔴 **它是【週期性重量】,不是【一次做完就關】** —— 做完之後**沒有東西可以動手**(判準沒觸發)。
> 📌 **「做起來很快」與「做完會改變什麼」是兩件事,而在派工單上它們長得一樣。**
> 🛑 **這種列永遠不會自己變 `done`,而板上看不出來。**
>
> **這一份就是那個週期性重量的今日一發。**

來源標籤:**【量的】**(命令 + 讀數,尺先證明會咬)/ **【推的】** / **【證不到】**

---

## 〇、🔴 先更正我自己 —— 我開檔前那句推測又錯了

我標了「**開檔前不要引用**」的推測:
> **推的**:那個「事件」很可能就是 `⟦b9-RLSHARDEN⟧`(拿掉 `BYPASSRLS` 那天)。

🔴 **錯的。** 那個事件是 **員工上工** + `#26` 真認證整條落地 —— 與 `BYPASSRLS` 無關。

📊 **兩次錯、一次對**:
```
③ ⟦f3-ADPNARROWER1⟧    ❌ 受詞不同(收 vs 發)
④ ⟦b9-ACLDRIFT5⟧       ✅ 方向對(那個寫死的 WHERE 確實在那一列裡)
⑤ 本列                  ❌ 事件猜錯
```
🎯 **一次對兩次錯 —— 而那正是那個標註該有的命中率。** 如果三次都對,下一個人會開始把它讀成「差不多是真的」。

---

## 一、【量的】六格 + 判準三(2026-09-10 唯讀,零寫入,`ON_ERROR_STOP=1`,零 ERROR)

```
Ⓐ staff 逐列(6 列)
   op4_backfill       OP4 歷史回填(系統)                    mgr=f act=f
   payment_confirmer  TapPay 付款確認(系統)                 mgr=f act=f
   sean               Sean(老闆)                            mgr=t act=t
   staff_1            員工 1(占位)                          mgr=f act=t
   staff_2            Sean 測試帳號(一般員工權限,非真員工)  mgr=f act=t
   test_01            TEST                                  mgr=t act=f

Ⓑ 🟢 正對照 staff 總列數(板上 6)                        6      ✅
Ⓒ 判準一:標籤【不含】占位/測試的啟用非管理者(板上 0)    0      ✅ 未觸發
Ⓓ 判準二:admin_audit_log 相異 actor(板上 2)             2      ✅ 未觸發
Ⓔ 🟢 正對照 admin_audit_log 總列數(09-08 = 119)         130    +11
Ⓕ ⚪ 負對照 現造 actor(必須 0)                          0      ✅
🔴 判準三:占位/測試帳號在 2026-09-08 之後有沒有新稽核列  0      ✅ 未觸發

逐 actor:
   sean     113 筆   最後一筆 2026-09-10 02:59:22+00   ← 今天
   staff_1   17 筆   最後一筆 2026-08-14 09:09:30+00   ← 停在 08-14, 27 天沒動
```

⇒ ✅ **「後台目前只有 Sean 一人測試、員工未上工」今天仍然為真。**
⇒ ✅ **那個 `SELECT GRANT` 今天仍不需要收回。**
📌 **兩個數字動了(102 → 113 / 119 → 130)而【結論那一格沒動】** —— 動的是 Sean 自己的操作,而**這是板上第四次寫同一句話**。

---

## 二、🎯 而這一發的新東西:**判準三第一次真的被跑**

板上 2026-09-08 `-mail` 提出判準三,並且**自己標了它沒做**:
> 🔴 **判準三只寫了形狀、沒做成機制** —— **這一格與本列原病完全同型,它不假裝它被解決了。**

【量的】**今天是它被提出之後第一次真的被執行** ⇒ **讀數 0**。

🔵 **而它為什麼值得**:板列說明過那條看不見的路 ——
> 把既有的占位帳號**交給一個真員工**,**兩個判準都不會叫**:
> 判準一看【標籤】,而**標籤是我們自己寫的 ⇒ 交接不會改標籤 ⇒ 永遠 0**;
> 判準二看【第三個 actor】,而**他用既有的 `staff_1` ⇒ 永遠 2**。
> 🎯 **兩個判準都只守「新開一個帳號」,而「把既有帳號交出去」那條路上沒有任何東西。**

⇒ ✅ **判準三守的是【行為】不是【標籤】** —— 而今天它有了第一個讀數(`0`),**而那個 0 現在是量到的,不是假設的**。

### 🛑 而它**仍然不是機制** —— 這一格不能含糊

**是我用手跑的。** 沒有任何東西會在下一次自己跑它。
📌 **⇒ 板列那句「判準三只寫了形狀、沒做成機制」今天仍然成立 —— 我把「形狀」變成「跑過一次的形狀」,沒有變成機制。**
🔴 **而那正是本列的原病**:動作被寫下來了,而**觸發那個動作的訊號沒有人在收**。

---

## 三、⇒ 這一列今天的狀態

| 板上寫的 | 今天 |
|---|---|
| 前提「只有 Sean 一人、員工未上工」 | ✅ **仍為真**(三個判準全部未觸發) |
| 那個 `SELECT GRANT` 要不要收回 | ✅ **不需要** —— 收了會弄壞稽核檢視,而換到的安全是 **0** |
| 判準三「只寫了形狀、沒做成機制」 | 🟡 **跑過一次了(讀數 0),而仍然不是機制** |
| 「這種列永遠不會自己變 `done`」 | ✅ **今天再次成立** —— 我做完了,而**沒有東西可以動手** |
| 收訊號的機制在 `⟦auth-STAFFONBOARDSIGNAL⟧` | 🛑 **本份沒碰**(它等 Sean Q19,而 09-07 那一版整版退掉) |

---

## 四、🛑 我證不到什麼

· **【證不到】`#26` 真認證整條落地到哪** —— 板上三個窗連續標了這一格沒查,**我也沒查**(照抄不刪)。
· **【證不到】誰坐在 `staff_1` 後面** —— 稽核表答不出來,板上原話。
· **【證不到】`test_01` 是不是 `⟦b4-TESTACCT1⟧` 講的那個帳號** —— 那一列講 Supabase `auth` 層,不是 `staff` 表。**不宣稱是同一個。**
· **判準三只查了 `admin_audit_log`** —— 一個真員工若只做「不寫稽核」的操作(純讀),**三個判準都不會叫**。
· 🛑 **這些讀數有到期日** —— 它們說的是 **2026-09-10 這一刻**。**本列存在的理由正是「沒有人會回頭重估」,不要把這一段讀成永久結論。**

---

## 五、🔗 接手用(2026-09-10 存檔)

### 隊列做完了
主視窗第二輪 5 列 **全部做完**:
```
① ⟦auth-HALFREVOKEDTRIGGERS⟧  ✅ 9/9 收掉了(而沒有人驗過)
② ⟦auth-GRANTGATEBLIND⟧       ✅ 造了那次「真的擋」——【而它不擋, 它警告】
③ ⟦f3-ADPNARROWER1⟧           ✅ 三格還開著;6 支沒變成 11 支
④ ⟦b9-ACLDRIFT5⟧              ✅ 擋點不成立(我 09-09 就推翻過, 板上沒跟上)
⑤ 本列                         ✅ 三判準未觸發;判準三第一次被跑
```
🛑 **主視窗交代:隊列做完就停,不要自己挑第六列。**

### 下一件(排在最後)
**板子標記** —— 我查過而**結論變過**的列:②③④ 都變過,加上第一輪的 `NETPUBLICALL` / `ORDERDELETENOTRACE` / `UNTRACKEDGRANT` / `SAMETRIGGERNAME` / `SRVCONSUMERGAP` / `SRVMIN` / `ACLVALUEPROVENANCE`。
🔴 **標之前先 `git fetch origin dev` 看那一列有沒有人標過**(我跟窗 A/D 撞過一次)。**撞了回報主視窗,不要自己選一邊。**
🔵 憑據在 `docs/handoff/CURRENT.md`;**用錨欄不用行號**;驗法是「刪掉那幾行是加上去那幾行的嚴格前綴」。

### 🔴 我手上還沒落檔的判斷
· **④ 那一格(擋工理由過期)我判它是【第五次】同型,而它與前四次不同類** —— 前四次是「過期的狀態」,這一次是「過期的**擋工理由**」。**已落檔**(那一份 §2),而主視窗說要收進交接檔。
· **判準三的基線我沒有寫進任何機制** —— 今天的 `0` 只活在這一份檔裡。**下一個重估的人要自己重打那段 SQL**(六格 + 判準三,本檔 §1 全文可抄)。

**正式庫零寫入。沒改板、沒改態、沒動任何權限。**
