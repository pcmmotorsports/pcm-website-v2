# B7 端到端負向驗收 —— **還差多少**(2026-09-10 收盤)

> **這份檔只回答一個問題:還差多少。不重述過程。**
> 過程與每一個讀數在 `docs/evidence/2026-09-10-b7-報價單那半-有測不等於驗收過.md`(§1-§25)。
> 板列(無錨):`docs/launch-todo.md` 那列「**B7 端到端負向驗收 + 開關已開,只剩負向驗收**」。
> 九條的分母與 admin 側五條:`docs/specs/2026-08-27-m4b-e8b-b7-nine-condition-status.md`。

🔴 **一句先說**:**九條沒有任何一條今天可以標 `pass`。** 而每一條差什麼,下面寫得出來。

---

## `#1` `#4` `#6` · admin 側三條(不在本份範圍)

· **證到**:admin 側四支守門今天綠(4 檔 / 83 格),2026-09-04 做過兩發突變,真登入那道閘被關掉會紅。
· **沒證到**:83 格裡只有 3 格對那發突變有反應 ⇒ **「83 格都在守對的東西」沒有被驗**。缺的是**工**(要 83 發)。
· **要收掉**:照母 plan「九條都有著落 + 兩顆開關都開」——**這三條不是瓶頸,不要為它們開工。**

---

## 🔵 `#3` · 備援登入進得去而任何寫入被擋 ⇒ **Sean 2026-09-10 拍乙:不做**

· **證到**:報價單側**沒有**這道閘。`requireAdmin`(`lib/auth-server.ts:49`)零個 `sub.kind`、零個 `method`;
  `middleware.ts:64` 那道 `deny` 不分讀寫。唯一擋住的寫入是 `lib/identity.ts:399`(改密碼端點拒 fallback 帳號)。
· **沒證到 / 為什麼**:不適用 —— 🛑 **這一條缺的不是工也不是授權,是【已經有的一句話】。**
  Sean 逐字:「**乙 = 不要 —— 緊急的時候本來就是要進去修東西的**」。
· **要收掉**:🛑 **不要寫成「通過」。** 寫成:
  「**報價單側的備援唯讀要求:依 Sean 2026-09-10 決策不實作,不計通過;admin 側另列狀態。**」
  🔴 **而它仍然是一把繞過正常檢查的鑰匙** —— 這句留著,哪天改主意時下一個人才看得出當初換掉的是什麼。
  🔵 spec `2026-08-18-m4b-e8b-b6-b7-spec.md:77` 的 `#3` **含 admin 與報價單兩邊**,別讀成整條取消。

---

## 🟢 `#5` · SSO code 重放 ⇒ 擋

· **證到(2026-09-10,拋棄式 PG 17.10,真的跑)**:**DB 那一半**。
  序列:第二發同一顆碼 ⇒ **0 列**(🟢 正對照 另一顆沒用過的 ⇒ 1 列)。
  並行(spec §7 格 **9b**,spec 自己說第 9 格抓不到):A 搶到 / B 拿 0 / 加總恰好 1,
  而 **B 的 `UPDATE` 等了 2676.825 ms · 無人爭搶時 0.905 ms** ⇒ 它真的卡住。
  另外量到:**`used_at` 沒被寫進去 ⇒ 同一顆碼第二次照樣拿得到 1 列**(= 重放成功)。
· **沒證到 / 為什麼**:🔴 **route 那一半與 PostgREST 那一層。缺的是【工具】——一個真的 route + PostgREST 環境。**
  🔴🔴 **而已知的繞過①正在那一層**:PostgREST 的 `columns=` 參數會**忽略 body 裡沒列進去的欄**
  ⇒ 請求長得完全正確而 `used_at` 保持 NULL。**那一格在本 repo 從來沒有人做過。**
· **要收掉**:一個會跑 PostgREST 的環境,對 `exchange` 打兩發同碼,並且驗第一發真的把 `used_at` 寫進去了。

---

## 🟡 `#2` · legacy fail-open cookie 發不出來

· **證到**:`legacyCookieAllowed`(`lib/session.ts:246`)這支**判斷函式**擋得住兩種突變
  (白名單改黑名單 ⇒ 紅 1 格;整個閘 `return true` ⇒ 紅 3 格)。
· **沒證到 / 為什麼**:**登入端「不得設 `ADMIN_COOKIE`」那一層。** 現有三格**直接呼叫那支函式**,沒走 route
  ⇒ 🔴 **有人繞過那支函式直接設 cookie,三格照樣全綠。** 缺的是**工**(環境已經有,見下)。
· **要收掉**:用 `tests/node/alert-route-concurrency.test.mts:24-48` 那個 harness 真的打 `login/route.ts` 的 `POST`,
  驗具名登入成功時**沒有**發出有效的 legacy cookie。

---

## 🟡 `#8` · 查無帳號 vs 密碼錯:訊息 / 狀態碼 / 回應時間三者一致

· **證到**:**身分解析層**的失敗結果一致(`identity-named-login.test.mts:74`,比的是 `resolveNamedIdentity` 的回傳值)。
· **沒證到 / 為什麼**:🔴 **三軸的【路由】驗收全部沒有** —— 訊息與狀態碼那兩軸缺**工**(環境已經有);
  **回應時間那一軸缺【量法 + 環境】**(替身把真 Auth 的時間差拿掉了,量不到)。
  🔵 量法設計在 `~/pcm-mailbox/線3-8號怎麼量-20260827.md` —— **沒有人讀過它。**
  ⚠️ `failResponse` 在限速時會回 `429` ⇒ **`{401}` 不是實際狀態全集。**
· **要收掉**:同上那個 harness,在**相同限速條件**下打兩種失敗請求比 `status` 與 body;時間軸另外談。

---

## 🔴 `#7` · 混版:admin 新 / 報價單舊

· **證到**:報價單 session 自己的版本檢查擋得住一發突變(`v:1` 也收 ⇒ 紅 1 格,而**紅的是判別格,「一律拒」那格沒紅**)。
· **沒證到 / 為什麼**:🔴 **它與 `#7` 的情境對應【根本沒建立】。** `#7` 是 spec §7 的**五格**:
  `7`(舊 authorize × 新 exchange)· `7b`(admin 最終那顆 session 不得被轉成 `fallback`)·
  `7c`(部署交界的飛行碼,**每次部署都製造那個窗口**)· `8`(只有「不得 500/401」那半算數)· `8b`(舊 admin 的真實 callback)。
  **三格要跨到 admin 那一側**,而 spec `:311` 還要求把「舊版」**釘成可取回的 artifact**。
  🔵 spec 自己記著**四象限缺一格**:`quote 舊 × admin 舊` —— **而那是今天正在跑的那一格。**
· **要收掉**:**兩個 repo × 兩個版本。這一條不是補一發測試,是一件工,而它要有 plan。**

---

## 🛑 兩支寫了而**沒有交出去**的測試 —— 不要重寫第四支

```
~/quote-wt-c-b7            分支 e8b-c-b7-negverify(基底 main 863a0a25)· 三個 ?? 未 commit
  tests/node/b7-negative-acceptance.test.mts     字面掃描(#2 #8)
  tests/node/b7-sso-code-consumption.test.mts    行為測試 + 假 fetch(#5 route 半)
  WORKTREE-OWNER-C-B7.md
```
🔴 **為什麼不交**:兩支都被 codex 唯讀審**實際做出繞過**,綠燈之下安全性已經壞掉。
```
字面那支  被繞 5 種(含 void '/*' 讓正則刪掉真的會執行的碼)
行為那支  被繞 5 種, 而其中三種同一個成因:【我的假 client 不是 PostgREST】
          —— columns= 的語意 · 零列的回應形狀(真的是 200 + [], 不是 406) · select 的投影
```
🎯 **⇒ 判定(`00-work-rules §5` 換路不重試)**:替身那條路走不通,**再修一次替身只會產生第三種「替身跟真的不一樣」**。
✅ **十二個繞過逐條記在** `…有測不等於驗收過.md` §23 與 §24 —— **它們每一個都是一格現成的驗收案例,拿去用。**
🔵 **兩支檔留在那棵 worktree 裡**,要清之前先找 `pcm-website-v2` 的主視窗。

---

## 🔵 三格引用時會踩到的東西

1. **`~/API大量上架/PCM報價單-V2` 就是 `pcm-quote-v2`** —— 板上兩個名字,同一棵樹。
2. **baseline 不是 `sso_codes` 今天的全貌** —— `sub_kind` / `sub_staff_id` 兩欄是 **out-of-tree 貼的**
   (`supabase/APPLIED-OUT-OF-TREE.tsv:30`)。🔵 那兩欄與消耗無關,`#5` 的結論不受影響。
3. 🔴 **三支測試檔寫著「本 repo 無 route handler 執行環境測試」**
   (`sso-sub:63` · `password-change:139` · `identity-named-login:246`)—— **那句是過期的。**
   **8 支測試檔真的 import route 並執行它**,而 `alert-route-concurrency` 跑的正是 `login` 與 `password` 那兩支。
   📌 **`#2` `#8` 之所以看起來做不到,只是因為那三句話寫在它們自己的檔頭。**
