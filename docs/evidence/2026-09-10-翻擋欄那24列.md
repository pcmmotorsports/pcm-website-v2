# 2026-09-10 · 翻擋欄那 24 列 —— 20 列翻掉、4 列驗不過

> 🛑 **授權的來源要先寫清楚**:Sean 逐字「依照推薦」批准翻擋欄。
> **而那句話我是從主視窗 `pcm-website-v2-59` 轉述收到的,不是直接從 Sean 收到。**
> 📌 今晚所有轉述裁決都照同一條紀律標記,這一條也不例外。
>
> 🔵 **來源清單**:`~/pcm-mailbox/板子普查-全104列-20260910.md`
> 「二、應減 23 列」+ `:500`(6d 今晚查成已修好,而板上沒有章)= **24 列**。
>
> 🛑 **只動擋欄。態(`open`/`doing`)一個字都沒動** —— Sean 批的是擋欄。

---

## 一句話

```
24 列  ⇒  ✅ 翻掉 20 列  ·  🔴 驗不過 4 列(不翻)
尺C 重數:帶擋 114 ⇒ 94,差額 20,消失的正好是那 20 列,零列新冒出來
```

🔴🔴 **而這一片【第一版是 21 翻 3 擋,codex 唯讀審把它改成 20 翻 4 擋】** ——
兩顆 must-fix + 一顆讓我補翻。**三顆改動全部記在下面,不改寫成一開始就對。**

---

## 一、🔴 驗不過的 4 列(**這一節先讀**)

### 1. `:500` `⟦b4-SIG4ERRORS⟧` —— **我翻了,而 codex 把它推回來(must-fix)**

我抽驗的那一格:部署之後 `pcm-anomaly-alert` 跑過 **3 輪,全 `succeeded`**
(🟢 正對照 該 job 全部輪數 47 · ⚪ 負對照 現造 jobname 0)。

🛑 **而那個讀數在兩個世界印同一個東西。** 碼自己逐字寫著
(`apps/storefront/src/app/api/cron/anomaly-alert/route.ts:372`):

```
🛑 三態與 B4_DEPLOY_CUTOFF 同形, 而它們是【兩顆各自獨立的 env】——
   本顆沒設 ⇒ 那一格不查 ⇒ 行為與加這一片之前逐字相同(落地零風險)。
```
`:381` 那一支 `if (stuckRead.kind === 'unset')` 只印一行 `console.info`,
逐字 **「這一輪不查(不是失敗)」** ⇒ 📌 **那一輪照樣 `succeeded`。**

⇒ 🎯 **`succeeded` 證的是「那支 cron 有跑」,不是「訊號4 持續失敗那一格會叫」。**
⇒ 📌 **這正是今晚那個形狀的第 N 次:一個往綠掉的錯,你自己跑一百次都是綠的。**
🛑 **要收掉它,缺的是【量那顆 env 在跑著的碼裡生效了沒】** —— 而那要 Vercel 權限,不在我這條線。

### 2. `:1083` `⟦mail-TXNMAILSPAM⟧` —— **我翻了,而 codex 把它推回來(must-fix)**

本列的病逐字是 **「交易信寄成功了,而它進垃圾郵件匣」**。
我的章量的是 **6 列全 `sent`、錯誤碼 0**,加上 Sean 回報 DNS 那格改好了。

🛑 **而 `sent` 與錯誤碼在【進收件匣】與【進垃圾匣】兩個世界一樣。**
🔴 **我自己的查證檔就寫著這一句**,而我翻的時候沒有回頭讀它
(`docs/reviews/2026-09-10-mail-render-and-spam-verification.md`):

```
· 沒有讀 Resend 那一側的投遞事件 ⇒ 答不出「對方收了沒、進了哪個匣」
· 這一列真正要的東西只有寄一封真信才拿得到,而本線零寄信
  ⇒ 它等的是【觀察者】,不是工。
```
⇒ 📌 **「我知道我證不到」與「我用證不到的東西翻了它」可以同時發生** ——
而那句限制就寫在我自己八小時前寫的檔裡。

### 3. `:983` `⟦auth-DEALERTIERPRICING⟧` —— **章自己寫著「不要翻我」**

章的結論欄是「條件已消失」,而**同一個章的後半段逐字**:

```
🛑 而我【不動】這一列的 ⟨擋⟩ token 也不動態
📌 這一格要一個人去裁「09-09 那次是不是把本列一起帶走了」
⚠️ 未量:09-09 那次拍板有沒有明文點名本錨 ⇒ 吻合而未證實
```

⇒ 🎯 **寫章的人自己把它標成【推的】而不是【量的】,並且明文要求一個人來裁。**
⇒ 📌 **一個「條件已消失」的結論,與一個「我推測條件已消失、請人來裁」的結論,在應減清單裡長得一模一樣。**
🛑 **要 Sean 或主視窗答一句**:09-09「經銷價整包排上線後」那次拍板,有沒有把本列一起帶走?

### 4. `:2492` `⟦tidy-NETPUBLICALL⟧` —— **兩個章打架,而較新的那個是我今天寫的**

清單依據的章寫 **「不做」**;而本列還有一個**更新的**章,是我 2026-09-10 自己量完寫的,
結論逐字「**仍成立而【對外沒有出口】**」⇒ 命中翻列條件 ③ 的反面。

```
anon 對 net 兩張表:S/I/U/D/TRUNCATE(走 PUBLIC,grantee 空)
RLS 兩表皆 f · policies 0 · schema USAGE anon ⇒ t
🟢 正對照 anon 對 public.orders SELECT ⇒ f   ⇒ 尺會咬
```

🔵 **而 codex 提了一個我接受的反駁**:原 plan
(`docs/plans/2026-09-09-netpublicall-anon-can-reach-pg-net-plan.md:18`)
**早就把「DB 權限仍開、靠面板設定隔離」當成殘餘風險接受了**,`:238` 據此結案 ——
📌 **所以我的新章量到的是【同一個狀態】,不是一個新事實。**

⇒ 🛑 **我仍然不翻,而理由縮小成一句**:規則逐字是「那一列**沒有**另一個更新的章說它仍成立」,
而它有。**要翻它得由一個人裁掉我那個章,不是由我自己在同一夜裡繞過它。**
🔵 而那個裁決的內容很短:**出口是一個【面板設定】,不是一道閘 —— 這樣算不算不擋?**

---

## 二、✅ 翻掉的 20 列 · 每一列抽驗哪一格

🔴 **紀律**:每一列至少一格,帶 🟢 正對照;⚪ 負對照要**噴錯、回 0、或回 `f`**,
不可以「因為根本沒去查所以安靜地過」。
碼一律讀 `origin/dev`(`git show` / `git grep`),**不讀工作區**。
DB 一律 `bash scripts/readonly-prod-sql.sh`,零寫入。

### 2-a 我自己抽驗的(15 列)

| 行 | 錨 | 抽的那一格 | 讀數 | 對照 |
|---|---|---|---|---|
| `:447` | `b4-NCPCRONRACE` | 掃描器 cron 在正式庫跑著嗎 | `pcm-late-payment-sweep` · `*/10 * * * *` · active `t` | 🟢 cron.job 總數 10 · ⚪ 現造 jobname 0 |
| `:470` | `b4-BANKCHARGESCARD` | 「匯款單被當刷卡扣」那個形狀在不在 | `payment_channel='bank_transfer' AND payment_method IS NOT NULL` ⇒ **0** | 🟢 同尺放寬成 channel=匯款 ⇒ **7** ⇒ 那個 0 不是尺沒動 |
| `:672` | `db-ACLVALUEPROVENANCE` | 症狀那把尺今天讀到多少 | `pg_default_acl` 含 `anon` ⇒ **12**(與章逐字相同) | 🟢 ADP 總數 27 · ⚪ 現造角色名 ⇒ 0 |
| `:834` | `f3-FITSTALE1` | 車款選單是不是從零件自己長出來 | `vehicle_taxonomy_public` viewdef 含 `product_fitments` ⇒ `t` | ⚪ 同尺找現造字面 ⇒ `f` |
| `:1018` | `b4-INVOICE5PCT` | 含稅列走殘差貼進正式庫了嗎 | `admin_create_manual_order` prosrc 含 `* 20 / 21` ⇒ `t` | 🟢 同尺找 `BEGIN` ⇒ `t` · ⚪ 現造字面 ⇒ `f` |
| `:1019` | `b4-PRICECOPYTAX` | 三個落點的文案與方向 | 新文案 `請填<strong>未稅</strong>金額` ⇒ **1 支檔命中** · 比價偵測器方向字面 `Math.round(c.authority * 1.05) === c.typed` ⇒ **1 支檔**(該檔內 **2 處:1 註解 + 1 執行碼**) | ⚪ 舊文案 `請填<strong>含稅</strong>金額` ⇒ **0** |
| `:1145` | `b4-PENDINGRECONCARDRF` | 「還多收著多少」那支算式漏不漏卡片退款帳本 | `pcm_order_refundable_remaining` **兩本帳都讀**(manual `t` / card `t`) | 🟢 同尺找 `order_items` ⇒ 20 支 · ⚪ 現造表名 ⇒ 0 |
| `:1257` | `b4-PARTPAIDCOUPONNEVER` | 章的「不做」站不站得住 | `docs/reviews/2026-09-09-窗A-十二列查證.md:415` 逐字「**正確的結論:不做**」—— 卡的是【誰在什麼情況下把券退回去】這個設計決定 | 🔬 曝險:`coupon_redeem_on_paid` 在 pg_proc ⇒ **0**(🟢 正對照 `create_order` ⇒ 2 · ⚪ 現造名 ⇒ 0)⇒ 今天沒有任何券在扣 |
| `:1486` | `c7-LEDGERGATEREFUSES` | 關掉它的那三顆 commit 在 dev 嗎 | `87cf11092` · `aea57bdef` · `212399e2a` **三顆全在** | ⚪ 現造 sha ⇒ `fatal: Not a valid object name` ⇒ **噴錯,不是安靜回 false** |
| `:1488` | `f3-PAIDAMOUNTDRIFT1` | 表頭金額對不對得上品項合計 | 8 張單 · 不符 **0** | 🟢 同一把尺比成 `+1` ⇒ **8 張全抓到** ⇒ 那個 0 不是尺沒動 |
| `:2343` | `0a-CARDCANCELNOREFUND` | 擋在前面的那一段還在嗎 | `cancel-review-section.tsx` 含 `payment_card_rail` ⇒ **2 處** | ⚪ 同檔現造字面 ⇒ **0** |
| `:2361` | `ship-HCTLABELCAPTURE` | 標籤圖真的存下來了嗎 | `shipments.hct_raw_response->0->>'image'` 長 **19090**(與章逐字相同) | 🟢 shipments 總數 4 · ⚪ 現造 key ⇒ 0 |
| `:2465` | `search-TAXONOMYTIMEOUT` | 「說出來的那一半」接起來了嗎 | `tryVehicleTaxonomy` 在 **12 支碼檔**;`VehicleTaxonomyNotice` 真的畫字不是 `return null` | 🟢 全 storefront `TaxonomyFailed` 31 處 · 測試**兩個方向都有**(`failed` / `failed={false}`) |
| `:2479` | `b4-CANCELMAILMIXEDRAIL` | 「提醒人工去寄」那半在不在 | `manual-cancel-notice-read` / `-button` **兩支都在** · `pending_manual_send_count` **10 支檔** | ⚪ 現造欄名 ⇒ **0** |
| `:2488` | `db-ORDERDELETENOTRACE` | 刪除留痕三支 trigger + 留痕表 | 三支全在全啟用(`tgenabled='O'`)· `orders_deleted_log` 在 | 🟢 `admin_audit_log` 在 · ⚪ 現造表名 ⇒ 不在 |

⚪ **全域尺自證(不是一列)**:`SELECT * FROM public.zzq8_definitely_not_a_table` ⇒ **ERROR**
⇒ 🎯 **這一夜的唯讀尺會咬,不是無條件說 yes。**

### 2-b 引用不重驗的(5 列)

| 行 | 錨 | 為什麼不重驗 |
|---|---|---|
| `:1148` · `:1253` · `:1626` | `b4-ZEROREMAININGSHOWSFORM` / `b4-CAPRACE1` / `b4-PAYCONCUR1` | **6d 已抽驗過,三格全過**(`~/pcm-mailbox/b4族查證-20260910.md` 追加段);主視窗逐字「那三列你不用重驗」 |
| `:934` · `:1637` | `b4-MAILRENDER1` / `mail-SKIPKEYNORETIRE` | **章是我 2026-09-10 今天自己量的**,正負對照逐格記在 `docs/reviews/2026-09-10-mail-render-and-spam-verification.md` §1 與 `docs/reviews/2026-09-10-mail-three-rows-verification.md` §1 |

🛑 **而 codex 指出 2-b 有一格我原本沒寫出來的限制,補在這裡**:
`:1253` / `:1626` 那一輪抽驗**核的是版控與 `APPLIED.tsv`**,
而該檔 `:152` 明寫**沒有驗正式庫今天的定義**。
⇒ 📌 **帳本記錄不等於當日正式庫實測 —— 這兩列的強度比 2-a 那 15 列低一級。**

> 📌 **分母對帳**:2-a **15 列** + 2-b **5 列** = **20 列**。

---

## 三、六格自我檢查(翻完當場跑,全過)

```
① 行數不變                        ✅ assert 通過
② 原文完整保留(舊 ⟨擋⟩ 劃掉留著)   ✅ 前後綴逐字比對通過
③ 欄數不變(pipe-aware,\| 不算欄)  ✅ 逐列比對通過
④ 收尾直線後為空                  ✅ 全板殘留 0 列
⑤ 只動要動的那幾列                ✅ diff 行號集合 == 要翻的集合
⑥ 尺C 重數帶擋                    ✅ 114 ⇒ 94,差額 20,消失的正好是那 20 列,零列新冒出
```

### 🔴 而 ⑥ 的能力我第一版寫得太滿 —— codex 實際模擬過,寫下來

我原本寫的是「少得多 ⇒ 動到別的列;少得少 ⇒ 有列沒翻成功」,讀起來像 ⑥ 是一道總閘。
🔬 **codex 在記憶體裡模擬了五種壞法,而【五種都仍然差 20】**:

```
① 漏翻一列 + 錯翻另一列      ⇐ 只有這一種被【消失集合比對】抓到
② 順手改了態                 ⇒ 差額對、消失集合也對
③ 把舊 ⟨擋⟩ token 刪掉        ⇒ 差額對、消失集合也對
④ 多造出一個幽靈末欄          ⇒ 差額對、消失集合也對
⑤ 第一個「不擋」後面還有一個有效的擋 token ⇒ 差額對、消失集合也對
```

⇒ 🎯 **②③④ 是被 ①②③④(行數 / 原文保留 / 欄數 / 收尾)擋掉的,不是被 ⑥ 擋掉的。**
⇒ 📌 **⑥ 只答一個問題:「翻掉的數對不對、是不是那幾列」。**
🛑 **一個檢查在【它抓不到的那幾種壞法】面前,長得跟它抓得到的時候一模一樣。**

---

## 四、🛑 這一片證不到什麼

1. **我證的是「章指的那一格今天仍然對」,不是「那一列真的被修好了」。**
   一格過 ≠ 整列過 —— 一發抽驗只證得了它打到的那一格。
2. **`:2465` 我沒能重量症狀本身** —— 那要正式站的 log,不在我這條線的權限裡。
   我抽的是碼那一半;**章自己也標了「證不到客人真的看到那句話」**。
3. **`:1145` 我的尺是粗的** —— 我量的是「那支算式讀不讀第二本帳」,
   不是章說的「每一支都把收款限在非卡軌」。**那一格我沒量,是換了一格量。**
4. **`:1253` / `:1626` 是帳本記錄不是當日實測**(見 2-b 底下那段)。
5. **態一個字都沒動。** 翻擋欄不等於那些列做完了 —— 它只是說**它們不再擋上線**。
6. **🔴 而最重要的一條**:這一片的第一版有 **2 列翻錯**,兩列都是
   **「我拿一個在兩個世界印同一個東西的讀數去翻它」**。
   ⇒ 📌 **抓到它們的不是我的六格自我檢查,是 codex 唯讀審。**
   ⇒ 🎯 **六格檢查全部問【我有沒有照我說的方式動板子】,一格都沒問【我憑的那個讀數夠不夠】。**
