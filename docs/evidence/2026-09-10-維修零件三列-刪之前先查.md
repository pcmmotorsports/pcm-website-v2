# 2026-09-10 · 那三列「維修零件」—— 刪之前先查

> Sean 2026-09-10 拍甲(刪那兩列空的)。主視窗交辦:**只查不刪**。
> 🟢 **全程唯讀,一發 `DELETE` 都沒有跑。**
> 🔴 **結論先講:那兩列【不是同一種東西】,只有一列可以刪。**

---

## 1 三列各自被誰指到

```
                              products  products_public  子分類  這一列在不在
🟢 正對照 ce697b77(有貨)       1646        1646           0        1
🔴 待刪 A  d83ce275               0           0            0        1
🔴 待刪 B  69546b21               0           0          🔴 1        1
⚪ 負對照 現造 uuid                0           0            0        0
```

**而外鍵的分母不是我猜的 —— 去問 `pg_constraint`:**

```
指到 public.categories 的外鍵【只有兩條】, 而兩條都是 ON DELETE RESTRICT:
  categories.parent_category_id → categories(id)   ON DELETE RESTRICT
  products.category_id          → categories(id)   ON DELETE RESTRICT
```

## 2 🔴🔴 而樹長這樣 —— `69546b21` 是那 1,646 件的【父層】

```
id         name      sort_order  raw_path               parent
69546b21   維修零件      1001     維修零件               (無 —— 它是頂層)
  └ ce697b77 維修零件      10     維修零件 · 維修零件     69546b21     ← 1,646 件在這裡
d83ce275   維修零件        10     服務與其他 · 維修零件   e9f18025(服務與其他)
```

🎯 **⇒ `69546b21` 的「0 件」不是「沒有人用它」—— 它是一個【導覽節點】,
　　貨掛在它的子層。刪掉它 = 拆掉那 1,646 件的父層。**

🟢 **而 DB 會擋**:`ON DELETE RESTRICT` ⇒ 那一發 `DELETE` 會直接失敗,不會靜靜地成功。
🛑 **但那不是「所以可以放心試」** —— 📌 **一個會失敗的操作,與一個不該做的操作,不是同一件事。
　　前者靠資料庫救,後者要靠讀懂那棵樹。**

## 3 所以可以刪的是【一列】,不是兩列

```
✅ d83ce275(服務與其他 · 維修零件)
   products 0 · products_public 0 · 子分類 0 · 外鍵兩條都指不到它
   ⇒ 刪它不會斷任何東西

🛑 69546b21(頂層 維修零件)
   它是 ce697b77 的父層 ⇒ 【不能刪】
   ⇒ 而它「重複」的性質也不同:它與子層同名, 那是 raw_path「維修零件 · 維修零件」的來源
```

## 4 碼那一側:沒有寫死的 uuid,而有一個【按名字】的排除清單

```
🟢 三個 uuid 在 apps / packages 全文命中 ⇒ 0
🟢 sitemap.ts 不列舉分類(全檔提到 category 只有 1 行, 而那是註解)
🔵 而有一處按【名字】寫死:
   apps/storefront/src/components/CategoryGrid.tsx:247
     export const HOME_WALL_EXCLUDED = ['服務與其他', '維修零件'];
   ⇒ 首頁磚牆不顯示「維修零件」—— 那是 Sean 2026-08-20 的決定, 與刪哪一列無關
   ⇒ 🛑 而它按【名字】比對 ⇒ 刪掉任何一列都不會讓它失效, 也不會讓它自己更新
```

## 5 🔴 刪的 SQL 長什麼樣(**沒有跑**)

```sql
-- 🛑 只刪【一列】。第二列不能刪, 理由見 §2。
-- 🔵 先在同一個交易裡確認它仍然是空的 —— 從我量到現在到有人貼這一段之間, 世界會變。
BEGIN;

DO $gate$
DECLARE v_n bigint;
BEGIN
  SELECT count(*) INTO v_n FROM public.products
   WHERE category_id = 'd83ce275-1dd7-489d-a4c1-809a5c5c87fc'::uuid;
  IF v_n <> 0 THEN
    RAISE EXCEPTION '前置閘:那一列底下現在有 % 件商品 ⇒ 拒刪', v_n;
  END IF;

  SELECT count(*) INTO v_n FROM public.categories
   WHERE parent_category_id = 'd83ce275-1dd7-489d-a4c1-809a5c5c87fc'::uuid;
  IF v_n <> 0 THEN
    RAISE EXCEPTION '前置閘:那一列底下現在有 % 個子分類 ⇒ 拒刪', v_n;
  END IF;
END
$gate$;

DELETE FROM public.categories
 WHERE id = 'd83ce275-1dd7-489d-a4c1-809a5c5c87fc'::uuid;

DO $assert$
DECLARE v_total bigint; v_dup bigint;
BEGIN
  SELECT count(*) INTO v_total FROM public.categories;
  IF v_total <> 116 THEN
    RAISE EXCEPTION '事後斷言:categories 應為 116 列(刪前 117), 實際 % ⇒ 拒繼續', v_total;
  END IF;
  SELECT count(*) INTO v_dup FROM public.categories WHERE name = '維修零件';
  IF v_dup <> 2 THEN
    RAISE EXCEPTION '事後斷言:同名「維修零件」應剩 2 列, 實際 % ⇒ 拒繼續', v_dup;
  END IF;
  RAISE NOTICE '✅ 刪掉 1 列。而【仍有 2 列同名】(頂層 + 它的子層)⇒ 這一發沒有消滅同名, 只消滅了那個【會選錯】的第三者。';
END
$assert$;

COMMIT;
```

🛑 **而它不能被讀成「同名問題解決了」** —— 刪完仍然是 **2 列同名**(頂層 `69546b21` 與子層 `ce697b77`)。
🎯 **⇒ 它消滅的是【那個會被選錯而且是空的第三者】,不是同名本身。**
🔵 **而 `.find()` 選中的仍然由 `sort_order, name, id` 決定** —— 刪完之後 `sort_order 10` 只剩 `ce697b77` 一列
　⇒ ✅ **選中它從「巧合」變成「唯一解」** —— 而那正是這一發的實質收益。

## 6 本文答不出什麼

- **沒有查後台有沒有畫面在用 `d83ce275`** —— 我查的是 DB 外鍵與顧客站的碼,`apps/admin` 沒掃。
- **沒有查歷史訂單裡有沒有存過那個 category_id 的快照** —— `orders` / `order_items` 沒有指到 categories 的外鍵,而**「有沒有存過名字或 id 的 JSON 快照」我沒有查**。
- **沒有查 `raw_path` 那道 UNIQUE 在刪完之後會不會有別的影響** —— 刪一列不會製造重複,而那是推論不是量測。
- **沒有跑任何 `DELETE`**,上面那段 SQL 一次都沒有執行過。
