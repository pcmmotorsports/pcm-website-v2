# 2026-09-10 `mail-` 那一族 · 逐列查證 —— **給主視窗直接套用的清單**

> 🛑 **我沒有改 `docs/launch-todo.md`**(窗B 正在標它那 29 列)。本份只是清單。
> 🔴 **查證 = 去看那件事今天還在不在,不是讀板上那一行。** 每一列下面寫的是**我今天量到什麼**。

---

## 〇、🔴 先講分母 —— **「48 列」與「18 列」是兩把尺**

```
① 錨是 ⟦mail-…⟧                     全部 40 · open/doing 32 · 🔴 帶擋 18   ← 本份查的是這一批
② 錨 ⟦mail-⟧ 或出現 -mail(線名)      全部108 · open/doing 69 · 帶擋 41
③ 整列出現 mail 這四個字             全部426 · open/doing 232· 帶擋 89
④ 出現 寄信/信件/郵件/email          全部172 · open/doing 92 · 🔵 帶擋 47   ← 最接近主視窗報的 48
🟢 對照 全板帶擋的 open/doing = 125 —— 與主視窗逐字相同, 那把尺是同一把
```
🎯 **⇒ 兩個都是真的,而它們回答的不是同一題**:
· **④ 答「這一列跟寄信有關嗎」** —— 含很多**別條線的列順口提到信**
· **① 答「這一列是信件線的列嗎」**
🛑 **⇒ 對 Sean 的後果**:講「48 列」他會以為**有 48 件信件的事要做**,而**信件線自己的擋只有 18 件**。

### 🔵 而還有一格:**「還剩幾列」與「還剩幾件事」是兩個數**
```
40 列 · 而相異的錨只有 28 個 ⇒ 有錨重複出現在多列
   mail-TXNMAILSPAM 4 列 · PREPAREBUDGET 3 · FETCHTIMEOUT 3 · RECIPIENTNOTRECHECKED 3 · PROBESNOSTOP 3
```
🛑 **我不合併它們**(那要改板)。**標出來,要合再合。**

---

## 一、逐列(錨 · 今天量到什麼 · 結論 · 證據)

### ✅ `:1638` `⟦mail-DEADMAILREQUEUE⟧` ⇒ **本列描述有誤(受詞已變)**
· **板上逐字**:「一支 migration 把『死信怎麼救』交給這個錨 —— **而這個錨在板上【不存在】,直到現在**」
· **【量的】** `grep -c '⟦mail-DEADMAILREQUEUE⟧' docs/launch-todo.md` ⇒ **3 列**(⚪ 負對照 現造錨 ⇒ **0**)
· ⇒ 🎯 **那個錨【現在存在】,而且在三列上。** 板上那句話描述的是它自己出生前的狀態。
· **證據**:本檔;`docs/launch-todo.md` 的 `:1618` `:1638` 與另一列。

### 🔴 `:1620` `⟦mail-BACKOFFDIESSOONER⟧` ⇒ **仍成立 —— 而那個位置多了一個【看起來像做完了】的東西**
· **板上留的最後一格**:「**方向【還沒寫進碼裡】** —— 落點 `packages/use-cases/src/email-backoff.ts:52`」
  而它自己寫了**不做的理由**:「只加註解會位移別人引用的行號…**等推完再做**」。
· **【量的】① 那個「等推完」的條件今天【消失了】** —— `dev` 今天推了五發,`0be0aa88e` → `af6fd59e1` 已落地。
· **【量的】② 而那個位置【現在有一句「取捨方向」】,而它寫的不是那一列要的那個方向:**
```
板上要的(Sean 2026-09-08 拍甲)  方向 = 選擇【短故障恢復快】, 接受【長故障更早永久停寄】
碼裡 email-backoff.ts:52 現在寫  取捨方向 = 偏慢(寧可晚寄, 不要把一封毒信打成一串重試風暴)
🔍 email-backoff.ts 掃「短故障」⇒ 0 命中   ·   🟢 正對照 掃「長故障」⇒ 有(該檔命中)
🟢 正對照 同檔「退避」⇒ 13 命中 ⇒ 尺會咬
```
· ⇒ 🔴 **結論:仍成立。Sean 拍的那個方向【沒有】寫進碼裡。**
· ⇒ 🎯 **而多出來的那格是新的**:**下一個人 grep `email-backoff.ts:52` 會看到「取捨方向 = …」,然後以為做完了。**
  📌 **「有一句取捨方向」與「有【那一句】取捨方向」,在 grep 結果裡長得一樣。**
· 🔵 **而板上另一半(真實故障長度分布未知)仍成立,而它要上線後的真實資料 —— 沒有人可以催。**
· **今天的曝險**:🟡 **未量** —— 那正是「真實故障長度分布未知」那一格。**沒有真實故障資料之前量不到。**

### 🔴 `:1640` `⟦mail-PAYMENTNOCAP⟧` ⇒ **仍成立,而【曝險的形狀變了】**
· **板上逐字**:「收款登記可以填任意金額(**零上界**)⇒ 先灌一筆假的現金收款,就能把退款上限撐出來」
· **【量的】收款那一側今天仍然沒有上界**:
```
supabase/migrations/20260810100000_m4b_e10_op1_order_payments_m.sql:199
   amount  integer  NOT NULL  CHECK (amount <> 0)      ← 只有【非零】, 沒有上界
🔵 而該檔 :43 自己記著:第一版是 CHECK (amount > 0), 後來放寬成 <> 0 以容納沖銷列
```
· **【量的】而退款那一側 2026-09-07 之後【會留痕】了**:
```
20260907180000_m4b_caprace1_over_cap_mark.sql:257
   RAISE WARNING '人工退款登記超過這張單【現金/匯款】軌的可退上限:餘裕 % 元, 這一筆 % 元。'
   ⇒ 而它【照實登記】(Sean 2026-09-02 拍甲:帳要跟得上事實), 餘裕變負數
```
· ⇒ 🎯 **結論:仍成立(上界仍然沒有),而攻擊從【靜默】變成【事後看得見】。**
· **今天的曝險**:🟡 **未量** —— 要量的話是「今天有幾張單的 `pcm_manual_refund_rail_cap(order_id) < 0`」。
  🛑 **我沒跑那一發**(它要正式庫查詢,而我把時間花在別列了)⇒ **那是下一個人五分鐘的事。**

### 🟡 `:1619` `⟦mail-SENDINGSTUCKTIMEOUT⟧` ⇒ **機制在,而我沒驗它守的是不是那一句**
· **板上逐字**:「重試到第 4/5 次時資料庫短暫逾時 ⇒ 那封信【從來沒送出就永久死亡】」
· **【量的】** `releaseAfterPrepareFailure()` **今天在**:`packages/use-cases/src/sweep-email-outbox.ts:1176`(定義)· `:1481`(呼叫)· `:212`(註解引用)
  🟢 正對照 同族 `claimDue` 出現在 **5 支檔** ⇒ 尺會咬
· 🛑 **而我【沒有】驗它真的擋住那個情境** —— 我證的是「那支函式在」,不是「第 4/5 次逾時的那封信會被救回」。
· ⇒ **結論:🟡 需要一發突變或一個行為測試才判得了。**
  📌 **不要因為函式名字對就標「已修好」** —— 那是今天全隊踩最多次的那一格。

### 🟡 `:1637` `⟦mail-SKIPKEYNORETIRE⟧` ⇒ **機制在,而同上**
· **板上逐字**:「我們自己 skip 的那一列放回掃描面,不等於那封信排得進去 —— `dedup_key` 沒退休 ⇒ 下一輪 `enqueue()` 撞唯一」
· **【量的】**
```
sweep-email-outbox.ts:1955  「🔴 job.dedupKey 要傳進去 —— 實作會把它退休(⟦b4-SHIPUNVOID1⟧)」
20260907060000_m4b_pending_views_rearm_own_skips.sql:32 「鍵【一定】會退休(skip 的那一發 UP…)」
                                              :54 「修法是改 dedup_key 退休算式, 動六 writer + 五 scanner」
🟢 正對照 dedup_key 出現在 25 支 migration ⇒ 尺會咬
```
· ⇒ **結論:🟡 有一支 2026-09-07 的 migration 正面處理這件事,而我沒驗它涵蓋這一列講的那條路。**

### 🛑 `:1076` `⟦mail-PROBESNOSTOP⟧` ⇒ **證不到 —— 而缺的是【授權】不是工**
· **板上逐字**:「正式站 auth 裡還躺著一個測試帳號 —— 而【它有沒有被算進任何數字】沒有人知道」
· **【量的】** 我對正式庫跑唯讀查詢 ⇒ **`ERROR: permission denied for schema auth`**
· ⇒ 🛑 **我這條唯讀連線【讀不到 `auth`】。** 不是我沒查,是**查不了**。
· ⇒ 📌 **要收掉它需要**:一個讀得到 `auth.users` 的授權,或請有 Supabase 後台的人看一眼。

### 🛑 `:2361` `⟦mail-PROBESNOSTOP⟧` ⇒ **證不到 —— 同一個原因**
· **板上逐字**:「那張標籤圖【現在就會被存下來】—— 而我們一箱都還沒送過,所以庫裡是空的」
· **【量的】** ⇒ **`ERROR: permission denied for schema storage`**
· ⇒ 📌 **同上。而這兩列同錨,同一個授權就能一起收掉。**

---

## 二、🔴 一個結構性的限制 —— 它決定這一族還有多少列我查不了

【量的】**我這條唯讀連線讀不到 `auth` 與 `storage` 兩個 schema。**
⇒ 🎯 **⇒ 這一族裡凡是講【帳號】或【存下來的檔案】的列,我【結構上】驗不到。**
🔵 而那與我今天早上量到的另一格一致:`anon` 有 8 張會讀到零列的表,全在 `auth` / `storage` 那一族。
📌 **⇒ 這不是「我沒查」,是「這個窗查不了」。分派前先看那一列碰不碰得到那兩個 schema。**

---

## 三、⇒ 還沒查的 11 列(照實列出來)

```
:674  PREPAREBUDGET     :713  TXNMAILSPAM       :812  FAILEDMAXNORESCAN
:934  TXNMAILSPAM       :1083 TXNMAILSPAM       :1484 RECIPIENTNOTRECHECKED
:1612 FETCHTIMEOUT      :1615 CUTOFFENQUEUEONLY :1618 FAILEDMAXNORESCAN
:1624 BACKOFFDIESSOONER :2479 RECIPIENTNOTRECHECKED
```
🔴 **而其中兩列(`:934` `:1083`)我【判斷我不該查】** —— 它們講的是「**那封信落在垃圾郵件匣**」與
「**沒有任何一格是在真的信箱裡看過的**」⇒ 📌 **要驗它得【真的寄一封】,而我是零寄信授權。**
⇒ ✅ **那兩列的正確標記是「要 Sean 或有寄信授權的人開一次真的信箱」,不是「還沒查」。**

---

## 四、🛑 本份證不到什麼

· **【證不到】`:1619` `:1637` 那兩支機制真的守住板上講的那條路** —— 我只證了「它在」。
· **【證不到】`:1076` `:2361`** —— `auth` / `storage` 都 `permission denied`。
· **【證不到】曝險數字** —— 兩列標「未量」的地方我都寫了怎麼量,而**我沒跑**。
· **沒有改 `docs/launch-todo.md` 一個字。**

---
---

# 第二輪 —— 再查 7 列(累計 14 / 18)

> 🔵 **主視窗指定新增第五類結論**:**「機制在,而我沒驗它守的是不是那一句」**
> 📌 **它比前四類誠實** —— **不因為函式名字對就標「已修好」。**
> 🔵 **另外它點名一個分辨**:`:1640` 那種是**【同一個洞,而它現在會叫】** —— **不是修好,也不是單純仍成立。**

---

## ✅ `:1618` `⟦mail-FAILEDMAXNORESCAN⟧` ⇒ **已修好**

· **板上逐字**:「付款信燒完重試次數之後【再也不會被重排】—— **客人永遠收不到那封信**」
· **【量的】那顆重排鈕今天在,而且接對了受詞**:
```
apps/admin/src/app/settings/mail/page.tsx:4   import { requeueDeadEmailAction } from '@/lib/mail/dead-letter-actions'
                                        :11   「/settings/mail — ⟦b4-MAILDEAD⟧:寄不出去的信(死信)清單 + 重排」
                                        :27   「那支 RPC 的白名單只認 pending/failed 且【attempts 已燒完】」
apps/admin/src/lib/mail/dead-letter-actions.ts:44   export async function requeueDeadEmailAction(…)
```
· 🎯 **`:27` 那句是關鍵**:白名單**只認 attempts 已燒完的** ⇒ **它守的正是這一列講的那個受詞。**
· 🔵 而該頁 `:16` 自陳:「那顆重排的 RPC **2026-08-31 就上線了,而它 0 個呼叫端** —— **這一頁是它的第一個**。」
  📌 **⇒ 一支上線了三週而沒有呼叫端的 RPC,與一支不存在的 RPC,對客人是同一件事。** 而現在接上了。
· ⚠️ 而該頁 `:28` 自己標了代價:「**重排會讓下一輪 cron 真的寄一封信給真的客人**」。

## ✅ `:713` `⟦mail-TXNMAILSPAM⟧` ⇒ **心跳今天仍然活著(正式庫實查)**

· **板上逐字**:「結算程式死了沒人知道 —— **心跳表已在庫上,且今天第一次看見它活著**」
· **【量的】2026-09-10 唯讀查正式庫 `public.sweeper_heartbeat`** ⇒ **10 支 job,全部有【今天】的 `last_success_at`**:
```
pcm-acl-digest 00:00 · pcm-anomaly-alert 01:00 · pcm-capture-recheck 09:40 · pcm-email-sweep 09:45
pcm-expire-unpaid-orders 09:00 · pcm-late-payment-sweep 09:40 · pcm-net-exposure 00:00
pcm-order-ineligible-gate 09:48 · pcm-settle-retry 09:40 · pcm-settle-sweep 09:48
consecutive_failures 全部 = 0
🔵 而有兩支留著失敗紀錄:pcm-anomaly-alert(09-06 01:00)· pcm-settle-sweep(今天 05:16)
   ⇒ 兩支都在【之後】成功了 ⇒ 那張表分得出「曾經失敗」與「現在壞著」
```
· ⇒ ✅ **那一列說的「看見它活著」今天仍然成立,而且是十支不是一支。**
· 🛑 **而【證不到】「死了會不會有人知道」** —— 我證的是**心跳有在跳**,不是**心跳停了會不會有東西叫**。
  📌 **那要一發突變(把某支停掉看告警響不響),而那會動到正式庫的排程 ⇒ 我不做。**

## 🔴 `:812` `⟦mail-FAILEDMAXNORESCAN⟧` ⇒ **仍成立 —— 而上面那一發順便量到它**

· **板上逐字**:「監控與被監控【共命運】—— 那條路壞掉的時候,**會來告訴你的那個東西也壞了**」
· **【量的】** 上面那 10 支裡,**`pcm-anomaly-alert`(那個會來告訴你的)與其他九支【在同一張心跳表、走同一支 `pcm_cron.invoke_cron_route`】**
```
invoke_cron_route 在 migrations 出現 34 次 / 17 支檔;定義在 20260723120000:95(單一支)
⚪ 負對照 現造函式名 ⇒ 0
🔵 而 pcm-anomaly-alert 自己在 09-06 01:00 失敗過一次 —— 那正是這一列講的那個形狀
```
· ⇒ 🔴 **仍成立。** 而板上寫「9 支裡 5 支」,**今天是 10 支共用同一支 route**。
  📌 **⇒ 分母變大了,而那讓這一列【更成立】不是更不成立。**

## 🔴 `:674` `⟦mail-PREPAREBUDGET⟧` ⇒ **仍成立(我今天早上查過同一件事)**

· **板上逐字**:「ACL 漂移守門裝上了,而【路⑤ dashboard / SQL Editor 手動改權限】**仍然完全沒有人守**」
· **【量的】** 我 2026-09-10 早上在 `⟦b9-ACLDRIFT5⟧` 那一份查過同一格,逐字:
  「路⑤ dashboard 手動改仍沒人守 ⇒ ✅ **仍成立**(那是**結構性的**,pre-commit 文字閘看不到)」
· ⇒ 🔴 **仍成立,而它是結構性的** —— 📌 **不是「還沒做」,是「那條路上不可能有 pre-commit 閘」。**
  **證據**:`docs/evidence/2026-09-10-acldrift5-那個擋點我兩天前就推翻過了.md` §四

## 🟡 `:2479` `⟦mail-RECIPIENTNOTRECHECKED⟧` ⇒ **本列描述有誤(受詞已變)**

· **板上逐字**:「卡 + 現金混合退款的取消單【系統不寄】,要人工寄 —— **而今天沒有任何東西會提醒人工去寄**」
· **【量的】** `supabase/migrations/20260906960000_m4b_cancelled_mixed_rail_phone_notified.sql` 檔頭逐字:
```
「混合退款的取消單要**人工寄信**, 而有一種單根本沒有信箱…
 🛑 那種單【按不下】「登錄我已人工寄出取消通知」那顆鈕…
 ⇒ 結果:客服打了電話、事情處理完了, 而【那張單永遠留在提醒裡】」
```
· ⇒ 🎯 **那段話的前提是【提醒存在】** —— 它抱怨的是「**提醒關不掉**」,不是「**沒有提醒**」。
· ⇒ ✅ **⇒ 「沒有任何東西會提醒人工去寄」今天【不成立】。** 而 **Sean 2026-09-06 拍甲**:「已電話通知」鈕要做。
· 🛑 **而【證不到】那個提醒今天真的在畫面上跳** —— 我讀的是 migration 的檔頭,**不是後台畫面。**

## 🟡 `:1484` `⟦mail-RECIPIENTNOTRECHECKED⟧` ⇒ **第五類:機制在,而我沒驗它守的是不是那一句**

· **板上逐字**:「寄件信箱與信件內容沒有『同一張單』的不變式 —— 而換成 HTML 會把洩漏面放大一個量級」
· **【量的】** `20260907230000_m4b_pending_views_allow_recipient_stale.sql:4-7` 逐字:
  「`email_outbox.recipient_email` 是**排信**…**碼那一半(`e044a16c8`)已經會擋**:比出…**並把舊鍵退休成 `{舊鍵}:recipients…`**」
· ⇒ 🟡 **有一支碼在正面處理「收件人變了」這件事**,而 🛑 **我沒有驗它涵蓋的是不是這一列講的「信箱與內容同一張單」。**
  📌 **兩者相近而不相同**:一個是「**收件人變了要重排**」,一個是「**寄出當下這封信的內容與收件人是同一張單嗎**」。

## 🟡 `:1624` `⟦mail-BACKOFFDIESSOONER⟧` ⇒ **本列描述有誤,而【它自己已經標了】—— 剩的是 Sean 的一題**

· **板上標題已劃線**:~~同一把冪等 key 配不同 payload ⇒ 一路燒到死信~~ ⇒ **2026-09-07 已不成立**
· **【量的】** 板列自己記著三格同日做掉(分類 `ResendEmailSenderAdapter.ts:463-464` · 退避 `email-backoff.ts:88` 配 `idempotency_24h` · 落表 `SupabaseEmailOutboxAdapter.ts:117`)。
· 🔴 **而殘餘換了形狀**:從「**少一封而帳錯**」變成「**多一封而帳對**」—— 等 24h 之後會**再送一次**,若第一封真的寄出去了,**客人會收到第二封**。
· ⇒ **結論**:**本列描述有誤(標題已劃線)· 而它卡在 `Q62`(已端 Sean)**:等 24h 重送 vs 標記「可能已寄出、要人看」。
· ⚠️ **而那一列自己標了三句「我沒驗到的」**(沒打開 Resend 官方文件 · 沒實跑撞 409 · 沒盤組信那一側)—— **我這一輪也沒有補上任何一句。**

---

## 🛑 還沒查的 4 列

```
:934  TXNMAILSPAM        🔵 要【真的寄一封】—— 主視窗會接到 Sean 今晚那一單上
:1083 TXNMAILSPAM        🔵 同上(垃圾郵件匣)
:1612 FETCHTIMEOUT       標籤列(52 條 findings 的複核正本)—— 我找不到那份正本檔, 需要問開列的人
:1615 CUTOFFENQUEUEONLY  出貨信的送出層 —— shipped-email-cutoff.ts 在, 而我沒驗它是排信層還是送出層
```

## 🛑 本輪證不到什麼

· **【證不到】`:713` 的「死了會不會有人知道」** —— 要停掉一支排程看告警響不響,**那會動正式庫,我不做。**
· **【證不到】`:2479` 的提醒今天真的在畫面上** —— 我讀的是 migration 檔頭,不是畫面。
· **【證不到】`:1484` 那支碼守的是不是這一列那一句** —— 第五類。
· **沒有改 `docs/launch-todo.md` 一個字。正式庫零寫入。**

---
---

# 第三輪 —— 收尾 3 列(累計 17 / 18)+ 總結行

## 🔴 `:1615` `⟦mail-CUTOFFENQUEUEONLY⟧` ⇒ **仍成立,而那一列自己已經把尺寸寫對了**

· **板上標題(已訂正)**:「出貨信沒有以 `shipped_at` 為準的**送出層** cutoff」
  ⛔ 舊標題留著刪除線:~~停寄的閘裝在【排信】那一端~~ —— 📌 **它把「被擋著」讀成了「漏了」。**
· **【量的】2026-09-10 今天**:
```
apps/storefront/src/app/api/cron/email-sweep/route.ts:1015
   sendCutoffEventTypes: ['order_created', 'bank_order_created']    ← 🔴 仍然只有這兩種
送出層的讀取來源只有一支:IOrderPlacedAtReader(讀 orders.created_at)
   packages/ports/src/IOrderPlacedAtReader.ts · packages/adapters/src/email/SupabaseOrderPlacedAtReaderAdapter.ts
🔍 全 repo 找 shipped_at 的【送出層】讀取來源 ⇒ 0
🟢 正對照 sendCutoffIso 的接線在(route:1006 → sweep-email-outbox:1389)⇒ 那道閘本身是活的
⚪ 負對照 現造名 zzqNopeReader ⇒ 0
```
· ⇒ 🔴 **仍成立。** 而那一列自己寫的尺寸**今天逐字仍準**:**①新的讀取來源 ②新參數 ③route 接線 ⇒ 要 plan(鐵則 8),不是加一個字串進陣列。**
· 🎯 **而那一列最該被讀到的一句,我原封引用**(它是今晚同一族的又一種):
  > **一組正確的測試,對「這把尺該不該用在這裡」零判別力。**
  > 我原本設計的兩格突變問的是「**有沒有被擋到**」—— 它們會全綠,**而客人永遠收不到出貨通知。**

## ✅ `:934` `⟦mail-TXNMAILSPAM⟧` ⇒ **已修好 —— 而關掉它的不是工程**

· **板上逐字**:「那封付款成功信【**沒有任何一格是在真的信箱裡看過的**】—— 而它現在有版面了,所以看起來像做完了」
· **【Sean 2026-09-10 逐字】**:**「email 沒有進垃圾」**
· ⇒ ✅ **那一格要的就是「有人在真的信箱裡看過」,而他看了。那一格是【滿的】。**

## ✅ `:1083` `⟦mail-TXNMAILSPAM⟧` ⇒ **條件已消失(一封實證)**

· **板上逐字**:「交易信寄出去了,而它落在客人的垃圾郵件匣 —— 對客人而言那與【我們沒寄】完全一樣」
· **【Sean 2026-09-10 逐字】**:**「email 沒有進垃圾」**
· 🛑 **而措辭收窄**:
```
✅ 他證到的:【那一封】進了收件匣, 不在垃圾
🛑 他沒證到的:每一種信都不會進垃圾 —— 他看的是一封, 而交易信有好幾種
⇒ 📌 所以寫「條件已消失(一封實證)」, 不寫「垃圾郵件問題解決了」
```

## 🎯 而這兩列有一格值得單獨記

📌 **它們擋了很久,而關掉它們的動作是【一個人打開信箱看一眼】。**
🔴 **而沒有任何一個窗做得到那件事** —— **不是能力問題,是授權問題**(我們是零寄信、也沒有他的信箱)。
✅ **⇒ 那種列要及早分出來,不要排在工程隊列裡等。**

---

## 📊 總結行(`mail-` 錨那一族)

```
本族帶擋的 open/doing        = 18 列
其中【今天已可翻不擋】       = 3 列   :1618(已修好)· :934(已修好)· :1083(條件已消失)
🟡 而另有 2 列我【判不了要不要翻】,理由寫在下面 —— 我不替它決定
🛑 仍成立 / 未驗 / 證不到    = 12 列
⏳ 待問開列的人              = 1 列   :1612(標籤列, 指不到正本)
```

### 🟡 那 2 列我判不了,而理由不同
· **`:1638` `⟦mail-DEADMAILREQUEUE⟧`** —— 它的**陳述**(「這個錨在板上不存在」)已不成立,
  🛑 **而它指的那件事(死信怎麼救)是 `:1618`,而 `:1618` 我判已修好**
  ⇒ 📌 **兩列要不要一起翻,那是【合併判斷】,而合併要改板。我不做。**
· **`:2479` `⟦mail-RECIPIENTNOTRECHECKED⟧`** —— 「沒有任何東西會提醒人工去寄」已不成立,
  🛑 **而 Sean 2026-09-06 拍甲「『已電話通知』鈕要做」** ⇒ **那顆鈕做了沒,我沒查。**
  ⇒ 📌 **陳述錯了不等於那一列沒事做。**

🛑 **我沒有翻任何一列的擋欄** —— **翻態是改判定,而 Sean 只解凍了「加標記」。**

---

## 🛑 本輪證不到什麼

· **【證不到】`:934` `:1083` 的每一種交易信** —— Sean 看的是**一封**。
· **【證不到】`:2479` 那顆鈕做了沒。**
· **【證不到】`:1615` 的那道閘在正式站真的怎麼跑** —— 全部是靜態讀碼,**一發都沒實跑**(與 `-auth` 2026-09-07 同一個限制)。
· **沒有改 `docs/launch-todo.md` 一個字。正式庫零寫入。**
