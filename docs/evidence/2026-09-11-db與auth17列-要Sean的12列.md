# 2026-09-10 · `db-` + `auth-` 兩族 17 列:要 Sean 的 12 列 / 窗自己做得完的 5 列

> 🛑 **先更正我自己的一個對不起來**:我上一則報「要 Sean 12 · 窗做得完 5」,而同一則裡列的是「乙 3 · 乙* 12」。
> 📌 **那是【兩種切法】不是同一份**:甲乙丙丁切的是「什麼時候咬」,這一份切的是「誰解得開」。
> ⇒ 而我沒有把後者逐列列出來 ⇒ 本檔補上。**兩份都是 17 列,加總都對得上。**
>
> 🎯 **而每一列的【選項】那一格才是重點:一列寫「要 Sean 決定」而沒有選項,他答不了。**

---

## 🔴 A 要 Sean 的 12 列

| # | 錨 | 要他決定什麼 | 選項(甲 = 推薦) |
|---|---|---|---|
| 1 | `db-ACLVALUEPROVENANCE`(712) | 要不要造一把「線上這個權限值是不是那支 migration 給的」的尺 | **甲 不造**(減法版明文不加量具;代價是權限來源證不出來)· 乙 造 |
| 2 | `auth-GRANTGATEBLIND`(766) | 部署時序閘對純 `GRANT` migration 是瞎的 | **甲 不動閘**,改成「貼純 GRANT 前人工確認順序」寫進 runbook · 乙 改閘(**要他破一次「不加閘」那條**) |
| 3 | `db-ROLLBACKPROSE222`(856) | 222 段從沒執行過的散文回退 | **甲 上線前不動**,明標「回退要人現場寫」· 乙 挑最高風險的 N 支補可執行 down SQL(**而 N 要他給**) |
| 4 | `db-NOROLLBACKARTIFACT`(2463) | 351 支 migration 裡可執行反向 SQL = 0 支 | **甲 維持 sidecar 現況** · 乙 規定以後每支 migration 內含反向 SQL |
| 5 | `auth-PAIDAMOUNTNOTFROZEN`(2476) | 付款信金額是寄出當下重查的,比不了 | **甲 上線後再做**(今天真客人 0)· 乙 現在凍結金額快照(**碰錢+寄信**) |
| 6 | `auth-PARTIALREFUNDCANCELGAP`(2520) | 取消而只退一部分的單,兩條寄信線都不寄 | 🔴 **甲 上線前做最小版**(退了就寄一封通知)· 乙 上線後再做 <br>⇒ 📌 **這一列我推薦甲而不是乙,理由:「客人的錢動了而他零通知」的後果是客訴,而且事後補寄救不回那個當下** |
| 7 | `auth-MANUALORDERLIMITBURN`(2521) | 沒收件人的手動單每輪重撈、佔住單輪名額 | **甲 上線後再做** · 乙 現在補留痕 <br>🔵 條件今天已成立(手動單 3/3 無收件人),而今晚 `email_outbox` 7 列全 sent ⇒ **沒有信在等 ⇒ 還沒咬到人** |
| 8 | `db-TAXONOMYVIEW`(2524,態 doing) | 今天實測【選得到車】了,那個 view 還要不要繼續做 | **甲 讓它做完**(它治的是那 13 頁的路,我沒走過)· 乙 停掉,把人放到別的地方 |
| 9 | `db-ORDERDELETENOTRACE`(2528) | 訂單被直接 SQL 刪掉沒有留痕 | ⚠️ **判不出來 —— 要先確認一件事**:今晚有一份 `docs/evidence/2026-09-10-orders-delete-audit-真的會抄-驗證.md`,結論可能已經解掉這一列。**先讓寫那份的人答「這一列還成不成立」,再排選項。** |
| 10 | `db-ACLGATEPROSEMISFIRE`(2550) | 一道閘多報、另一道少報,而它們同一個受詞 | **甲 不修閘**,而把「那道閘掃過了」這句話從所有引用點拿掉 · 乙 修閘 |
| 11 | `db-SAMETRIGGERNAME`(2552) | 兩支還沒貼的 migration 建同一個 trigger 名 | **甲 貼之前裁順序並改其中一支** · 乙 兩支合併成一支 <br>🔵 **這一列他只要說「照建議」就解得開** —— 而它必須在【貼那兩支之前】答 |
| 12 | `db-RLSHARDENZEROROWS`(2554) | RLS 收緊那天,唯讀授權會安靜變成零列 | **甲 成本 0**:harden 之前把「要問客人視角就自己補 `delisted_at is null`」寫進 runbook · 乙 建一個 `NOBYPASSRLS` 的唯讀角色(**要新憑證 + 新 env + 動全隊在用的腳本**) <br>🎯 這與我今晚那份 RLS 射程文件 §3 是**同一題**,答案可以一起下 |

### 🔵 而 3 與 4 是同一族,可以一題答兩列
兩列講的都是「回退沒有可執行的東西」—— **問他一次「回退要做到什麼程度」就同時關掉兩列。**

---

## 🟢 B 一個窗自己做得完的 5 列

| 錨 | 做什麼 | 只是翻牌? |
|---|---|---|
| `auth-PROBENEXTRED`(767) | 態已 `done` ⇒ 翻擋欄那個 token | ✅ 翻牌 |
| `db-ROLLBACKLOCKWAIT`(858) | 態已 `done` ⇒ 翻擋欄那個 token | ✅ 翻牌 |
| `auth-HALFREVOKEDTRIGGERS`(1497) | 態已 `done`,而今晚有 evidence(`halfrevoked 那九對今天收掉了沒`)⇒ 翻牌 | ✅ 翻牌 |
| `db-VERSIONGATECROSSBRANCH`(2553) | 態已 `done` ⇒ 翻擋欄那個 token | ✅ 翻牌 |
| `db-REVIEWLEFTNOTRACE`(2549) | **寫一份文件**:一次核對的【過程】要有落點,不要只留結論 | 🔴 **不是翻牌 —— 這是這 17 列裡唯一「還沒做而且窗自己做得完」的** |

🎯 **⇒ 所以夜跑在這 17 列上能推進的是【1 列實作 + 4 列翻牌】。其餘 12 列不管跑多久都會停在同一個地方。**

---

## 🛑 這份答不出什麼

- **「要 Sean」是我依鐵則 8 與 12 判的,沒有問過任何人** —— 判錯的成本是他被問了一件其實不用問的事。
- **17 列的內容我沒有重查**(主視窗交辦:用上一輪的結果)。只有 `db-TAXONOMYVIEW` 我去正式站實測過。
- **第 9 列我明寫判不出來** —— 而那不是偷懶,是**它可能已經被別人解掉了,而我沒讀那份 evidence**。
- **甲/乙 的推薦是我的判斷,不是量測。** 尤其第 6 列(部分退款不通知)我推薦「上線前做」,而**今天真客人 0 ⇒ 它的曝險是 0**;我推薦它的理由是後果不可回收,不是它現在正在發生。
