# `b4-` 族查證 · 2026-09-10(唯讀;主 repo 樹上零寫入、零 git add、零 commit)

> 查的人:新 session(主樹 `/Users/sean_1/pcm-website-v2` · dev · `462bb70ea`),主視窗 `-59` 派工(第五輪)。
> **尺**:態 `open`/`doing` · 錨 `⟦b4-…⟧` · 擋欄第一個 `⟨…⟩` 判定是 `⟨擋…⟩`,**而首判被 `~~…~~` 劃掉時取下一個**(上一輪撿到的修正,這一輪已套用)。

---

## 🎯 總結行 —— 而它跟你預期的完全不同

```
本族 帶擋 41 列(你記 42 · 差 1, 我的數含「首判被劃掉就取下一個」那個修正)
🔴 其中【今天已可翻不擋】= 11 列   ⇒ 本族對那個 78 的【應減】= 11
🔴 而【34 列已經有人查過並寫下結論】—— 不是你說的 8 列
   ⇒ 📌 真正沒有人看過的只有 7 列, 而其中 4 列今晚被我或別人處理掉了
```

🛑 **⇒ 我這一輪最有價值的產出不是「又查了幾列」,是【證明這一族大部分已經被查完了,而結論全部卡在板上沒走回擋欄】。**

---

## 一、41 列的全普查(我逐列讀了最後一個 `⟨已查證…⟩` 章)

| 章的結論 | 列數 | 錨 |
|---|---|---|
| 🟢 **已修好** | **8** | `NCPCRONRACE`(:447)· `MAILRENDER1`(:934)· `INVOICE5PCT`(:1018)· `PRICECOPYTAX`(:1019)· `ZEROREMAININGSHOWSFORM`(:1148)· `CAPRACE1`(:1253)· `PAYCONCUR1`(:1626)· `CANCELMAILMIXEDRAIL`(:2479) |
| 🟢 **不做** | **1** | `PARTPAIDCOUPONNEVER`(:1257) |
| 🟢 **條件已消失** | **2** | `BANKCHARGESCARD`(:470)· `PENDINGRECONCARDRF`(:1145) |
| 🟡 本列描述有誤 | 3 | `REFUNDID1`(:1230)· `RECIPLIVEVALUE`(:1631)· `CUTOFFWRONGCOLUMN`(:2395) |
| 🟡 前提翻面 | 1 | `PARTPAIDNOCANCEL1`(:1255) |
| 🟡 關閉條件②已滿足 | 1 | `DEALERSIGNUPUNSEEN`(:1026) |
| 🟡 「下一步是寫 plan / 要外部通路」(不是還沒查) | 2 | `WITHHELD1`(:1239)· `REFUND10016`(:689) |
| 🔴 仍成立 | 16 | 略(逐列章都在板上) |
| ⚪ **完全沒有章** | **7** | `SIG4ERRORS`(:500)· `QUOTE2FA1`(:721)· `MGRENV1`(:984)· `TESTACCT1`(:1076)· `2FAOWNER1`(:1181)· `REPLAY1`(:1227)· `EMAILTRIAGE`(:1612) |

### 🔴 **應減 = 11 列**(`已修好` 8 + `不做` 1 + `條件已消失` 2)

**而其中 4 列的章上逐字自己寫著「板上還寫 open」:**
```
:1148 ZEROREMAININGSHOWSFORM  逐字「已做完且在 dev, 而板上還寫 open」
:1253 CAPRACE1               逐字「DB 已貼正式庫、畫面已在 dev, 而板上還寫 open」
:2479 CANCELMAILMIXEDRAIL    逐字「板上那一句今天不成立:提醒那半也做完了」
:1019 PRICECOPYTAX           逐字「三個落點全部落地且同一次上線」
```
📌 **⇒ 寫章的人【知道】板上沒跟上,而那句話也留在同一格裡沒有走出去。**

### ⚠️ 一個會讓你少算的細節

**章的日期不是只有 09-10** —— `已修好` 那 8 列裡 **4 列的章是 2026-09-09**(`:1019` · `:1148` · `:1253` · `:1626`)。
🛑 **只掃「已查證 2026-09-10」會漏掉它們。** 我這一輪掃的是**任何** `⟨已查證…⟩`,取**最後一個**。

---

## 二、沒有章的 7 列 —— 逐列處置

| 錨 | 今天量到什麼 | 結論 |
|---|---|---|
| `⟦b4-MGRENV1⟧` (`:984` · doing) | **我上一輪已經查完並回報過** —— 三格複現,洞原封在:`session.ts:175` 的 env 讀取 · `actor.ts` 三層 · 訊號元件掛在 `layout.tsx:152`。轉 `done` 的條件(放行真的被擋住)一格沒動 | 🔴 **仍成立**(詳見 `docs/evidence/2026-09-10-6d-db治理6列.md`) |
| `⟦b4-REPLAY1⟧` (`:1227`) | **我上一輪已經查完** —— 根因翻面(`product_fitments_effective` 版控裡建立它的 **0 ⇒ 3 支**),主張未複驗 | 🟡 **前提翻面(部分)**(同上檔) |
| `⟦b4-SIG4ERRORS⟧` (`:500`) | 🔴 **它卡的那一格今天【自己過期了】。** 本列的關閉條件逐字 = 「`main` 出現一次 production 部署**之後**,anomaly-alert 那一輪跑過一次」;而它卡在**部署時刻 `12:19:22` 沒寫時區**,兩種讀法差 8 小時。⇒ 🎯 **今天是 09-10,那次部署是 09-07** —— 排程逐字 `0 1 * * * active=t`(每天一輪)⇒ **不論時區怎麼讀,09-08 / 09-09 / 09-10 那三輪都在部署之後。** 📌 **時區歧義不再影響結論了。** | 🟡 **本列描述有誤(擋工理由已過期)** —— 而剩下的是**一發唯讀查詢**,不是一個判斷題 |
| `⟦b4-EMAILTRIAGE⟧` (`:1612`) | ✅ **複現你窗B 的發現**:事欄逐字 `🔵 **標籤列:寄信路徑 52 條 codex findings 的逐條複核正本**`;擋欄逐字指向 `docs/reviews/2026-09-01-email-path-codex-findings-triage.md`(137 行)+ 另一支(102 行),而那兩支**已進 `dev`**(該列自己用 `git cat-file -e origin/dev:<路徑>` 驗過) | 🟡 **本列描述有誤 —— 它是【索引列】不是一件事**(見 §四) |
| `⟦b4-QUOTE2FA1⟧` (`:721`) | 🛑 **類別:別的 repo** —— 事欄開頭逐字 `🔴 **[pcm-quote-v2]**`。**不在這棵樹,我沒有去碰任何別的 worktree** | ⚪ **標類別跳過**(不是「還沒查」) |
| `⟦b4-2FAOWNER1⟧` (`:1181`) | 🛑 **同上,`[pcm-quote-v2]`** | ⚪ **標類別跳過** |
| `⟦b4-TESTACCT1⟧` (`:1076`) | 🛑 **類別:要真庫** —— 「正式站 `auth` 裡還躺著一個測試帳號」,而 `auth` schema 要正式庫連線;**我全程沒連正式庫** | ⚪ **標類別跳過** |

---

## 三、兩列我做了額外的複核(章在,而今天有新事實)

### ① `⟦b4-CARDALREADYREFUNDED⟧`(`:1147`)—— **章說「仍成立」,而它是 09-09 的;09-10 那一貼把它做掉了**

```
章    ⟨已查證 2026-09-09 · 仍成立(逐字對得上, 而今天曝險是 0 張)⟩
🔴 而 09-10 發生了兩件事, 章都還不知道:
   · Sean 2026-09-09 拍甲, 逐字「那格的字改成【卡上那筆的狀態我確認過了】」
     (寫在 migration 檔頭 :5-6, 經主視窗 -b2 轉達)
   · 那支 migration 20260909090000 於 2026-09-10 貼上正式庫
     APPLIED.tsv 逐字「@20260910-022006-42536 貼板 …;主視窗依 Sean「貼 115」代貼」
🔬 而那句話真的換了:檔內 :273 逐字
   '在登記畫面把「卡上那筆的狀態我確認過了」那一格勾起來再送一次。'
   :139 / :412 兩道閘用 strpos(prosrc, '卡上那筆的狀態我確認過了') 守它
```
⇒ 🟢 **我判【已修好】** —— 板列那條「兩邊都錯的死路」的成因(勾下去等於說一句假話)已經被那句新文案解掉。
⚠️ **而我沒有驗**:那支 RPC 在正式庫上今天的 `prosrc` 是不是真的含那句話(要唯讀正式庫,我沒連)。**我證的是「檔案這樣寫、帳本記它貼了」。**

### ② `⟦b4-FITSYNC1⟧`(`:1240`)—— **章對,而【卡在哪】今天釘得更死**

章逐字 `⟨已查證 2026-09-10 · 仍成立, 而【卡在哪一格】今天釘住了 · 資料那半已不成立…⟩`。
🔬 **我補上碼那一側的讀數(章沒寫)**:
```
🔴 修法【三件全部寫好了, 而卡在最後一步】:
   · SQL   supabase/migrations/20260908090000_m4b_fitsync1_get_fitment_sync_freshness.sql
           檔頭逐字「🛑🛑 草稿。未 apply。」
   · 帳本  git show origin/dev:supabase/APPLIED.tsv ⇒ 20260908090000 【未貼】
   · TS    apps/storefront/src/app/api/cron/anomaly-alert/route.ts:420
           逐字 fitmentFreshnessRpcName: null
           而 :612 的 log 逐字「🔵 車款同步告警【還沒上膛】…本輪不查, 而這【不是】故障」
   ⇒ 📌 貼完之後 TS 那半只改【一行】(檔頭 :12-14 自己寫的)
```
⇒ 🔴 **仍成立,而它今天是「等一次貼板」,不是「等人做」。**

---

## 四、⚠️ 索引列 / 重複列(照你交代:撞到再記)

1. **`:1612` `⟦b4-EMAILTRIAGE⟧` 確認是索引列** —— 事欄第一個字就是「標籤列」,而它自陳「板上只開兩條,其餘 10 條的落點在正本裡」。
   🔴 **而它與 `:1227` 是同一族**(`:1227` 錨欄逐字「本列不重複記數字 —— 去讀 `#299`」)。
   📌 **⇒ 我這兩輪撞到 2 列索引列,而【沒有專門去掃】。**
2. `⟦b4-MGR0-RPC⟧`(`:831`)有兩支同名 migration,而 **`20260828070000_m4b_b4mgr0_is_manager_comment.sql` 帳本上【未貼】**、`20260829193000` 已貼 —— 只回報,**沒判它是不是遺留**。

---

## 五、🛑 我證不到什麼

· **全程沒連正式庫、沒對正式站發請求。** §一那張表是我**逐列讀板上的章**得到的 —— 📌 **我證的是「章這樣寫」,不是「那 11 列真的修好了」。** 要把它們翻掉之前,值得對 `已修好` 那 8 列各抽一格複驗。
· **`:1147` 我沒驗正式庫上那支函式今天的 `prosrc`。**
· **`:500` 我沒驗那三輪 anomaly-alert 真的跑過** —— 那要唯讀查 `cron.job_run_details`,而它是**一發十秒的查詢**,不是一個判斷題。
· **`:721` / `:1181` / `:1076` 三列我一個字都沒查** —— 它們分別要別的 repo 與正式庫 `auth`,**我標類別,不假裝查過**。
· **16 列「仍成立」我沒有複驗** —— 它們的章大多是今天寫的,我照抄分類,沒有重跑任何一發。

---

## 六、🔵 給你端 Sean 的一句

**這一族真正的狀況不是「還有 41 件擋著」,是:**
```
11 件已經做完或不做了, 而板上還寫著擋
16 件有人今天查過並寫下「仍成立 + 曝險」
 3 件要別的 repo 或正式庫才驗得到
 7 件的下一步是【貼一支 SQL】或【跑一發唯讀查詢】或【寫一份 plan】—— 不是「查」
```
📌 **⇒ 那個數字給他之前,先把這 11 件翻掉;否則他看到的擋工件數比真的多了四分之一。**
🛑 **而翻態是改判定,不是加標記 —— 我一列都沒動。**

---

**主樹狀態(交還前複查)**:`git status --short` = `?? h3check.png`(沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。

---

# 追加 · 2026-09-10 抽驗(主視窗指派:從 8 列「已修好」挑 3 列各驗一格)

> **挑法照指示:優先挑【章最舊的】** —— 那 8 列裡有 4 列的章是 2026-09-09,我從那 4 列挑 3 列。
> **三列全部通過。** 每一列驗兩格(碼/DB 各一),而每一格都帶對照。

| 錨 | 章說做完了什麼 | 我驗到的 | 判 |
|---|---|---|---|
| `⟦b4-ZEROREMAININGSHOWSFORM⟧` (`:1148`) | 「已做完且在 dev」·證人 commit `870c022b3` | **① 那顆 commit 今天在 `origin/dev`**(`git merge-base --is-ancestor` ⇒ 是;🟢 正對照 `af6fd59e1` ⇒ 也在;⚪ 負對照 全 0 的 sha ⇒ `fatal: Not a valid commit name`,**不是安靜的 false**)。commit 標題逐字「帳本未登記額 0/null 不再顯示登記表單」。<br>**② 而行為真的在 `origin/dev` 的碼裡**:`manual-refund-entry-section.tsx:115` 逐字 `if (ledgerSettled && !failed) return null;`;判準 `manual-refund-entry-gate.ts:274` 逐字 `return !(refundUnregisteredAmount !== null && refundUnregisteredAmount > 0);`(0 與 null 都算結清);呼叫端 `order-detail-money-tab.tsx:386` 逐字 `ledgerSettled={manualRefundLedgerSettled(refundUnregisteredAmount)}` | ✅ **過** |
| `⟦b4-CAPRACE1⟧` (`:1253`) | 「DB 已貼正式庫、畫面已在 dev」·證人 3 顆 commit | **① 三顆全部在 `origin/dev`**:`7166a68bf`(前端超額標記)· `212399e2a`(同時掛 `CAPRACE1` 與 `c7-LEDGERGATEREFUSES` 兩個錨)· `875e89334`(#445b 退款金額上限)。<br>**② DB 那半**:`20260907180000_m4b_caprace1_over_cap_mark.sql` 在 `git show origin/dev:supabase/APPLIED.tsv` 命中 **1** ⇒ 帳本記它已貼 | ✅ **過** |
| `⟦b4-PAYCONCUR1⟧` (`:1626`) | 「修法已在正式庫」·`pcm_noncard_settle_recompute` 用 `pg_advisory_xact_lock`、觸發器 `..._after_payment_ai` 掛在 `order_payments` | **① 兩個物件都在版控裡,而且同一支**:`20260904230000_m4b_noncardpaid_settle_and_expire_leg.sql` 同時建函式與觸發器。<br>**② 帳本命中 1 ⇒ 已貼。** 🟢 正對照 `pg_advisory_xact_lock` 全 migrations 命中 **24 支**(不是孤例)· ⚪ 負對照現造 `pg_advisory_zzz_lock` ⇒ **0** | ✅ **過** |

⇒ 🟢 **三列三格全過 ⇒ 那個【應減 11】有底了。**
🛑 **而我驗的仍然是【版控 + 帳本】,不是正式庫上那些物件今天的定義。** 要更硬,得對正式庫問 `pg_get_functiondef`;**這一輪我只對 `⟦b4-SIG4ERRORS⟧` 連了正式庫(唯讀),其餘沒有。**

---

## 🟢 `⟦b4-SIG4ERRORS⟧`(`:500`)—— 順手做掉了,而它是「時區歧義自己過期」的教科書例子

**本列自己寫死的關閉條件逐字**:「`main` 出現一次 production 部署(`target: "production"`)**之後**,anomaly-alert 那一輪跑過一次 ⇒ 那時才是 `done`」。
**而它卡住的原因**:板上記的部署時刻 `12:19:22` **沒有時區**,兩種讀法差 8 小時 ⇒ 沒有人算得出「那一輪在部署前還是後」。

🔬 **2026-09-10 唯讀正式庫實查**(`bash scripts/readonly-prod-sql.sh`,零寫入,SQL 裡寫入類語句 **0**):
```
A 排程本身        jobid=2 · pcm-anomaly-alert · schedule 0 1 * * * · active=true
B 部署之後跑過    3 輪   最早 2026-09-08 01:00:00.19253+00   最晚 2026-09-10 01:00:00.190255+00
C 逐輪狀態        2026-09-08 01:00 succeeded
                  2026-09-09 01:00 succeeded
                  2026-09-10 01:00 succeeded
🟢 正對照 全部輪數  47      ⚪ 負對照 不存在的 jobname  0
```
🎯 **而兩種時區讀法【結論相同】**:
· 讀成本機(`12:19:22+08` = **04:19 UTC**)⇒ 09-08 / 09-09 / 09-10 三輪都在它之後。
· 讀成 UTC(`12:19:22 UTC`)⇒ 第一輪候選 09-08 01:00 UTC 仍在它之後 ⇒ **一樣 3 輪。**
📌 **⇒ 時區歧義不再影響結論** —— 它不是被解決的,是**被時間走掉了**:等了三天,兩種讀法的答案合流了。

⇒ 🟢 **我判【已修好】** —— 關閉條件逐字滿足(部署之後跑過,而且不只一次,三輪全 `succeeded`)。
⚠️ **而我沒有驗的一格**:板上那次部署 `dpl_D1eFSbUizHnLTLSBLS6RDqJ6VfGe` 今天在 Vercel 上還是不是 `state=READY` —— **我沒有 Vercel 權限,板列自己也記著「MCP 逐字回 token expired」**。我信的是板上那一格的字面。

### 🔵 ⇒ 這一列讓 b4 的應減從 11 變 12

```
🔴 而「時刻沒寫時區」那個病沒有被修 —— 只是這一列不再被它擋住。
   📌 下一個寫「某某之後」的關閉條件的人, 還是會踩同一格。
```

---

**主樹狀態(追加段寫完再複查)**:`git status --short` = `?? h3check.png`(沒動)· 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
（⚠️ `git rev-list --count origin/dev..HEAD` = 12,而那 12 顆是主視窗在這棵共用樹上做的,不是我的。）
