# 2026-09-10 · ⟦search-DUPCATNAMES⟧ 查證:三組剩一組,而今天選對是巧合

> 板列 `docs/launch-todo.md:1011`。🟢 **全程唯讀**(`bash scripts/readonly-prod-sql.sh`),沒有改任何資料、沒有改任何碼。
> 🔴 **本文不動該列的 ⟨擋⟩** —— 那是 `-ship` 2026-09-08 的判定,不是我的。

---

## 0 量具自陳

```
db_now 2026-09-10 11:40:52+00
categories 117 列 · 相異 name 115
⚪ 負對照 name = 'ZZQQ不存在的分類' ⇒ 0
⚪ 負對照 name like '%ZZQQ水管%'   ⇒ 0
```

## 1 🔴 「三組同名」已過期 —— 今天是【一組】

板列逐字:「目錄裡有**三組**【同名分類】」,並記 `維修零件` 3 列 · `水管束環` 2 列 · `防爆水管組` 2 列,
守恆式 `(3-1)+(2-1)+(2-1) = 4 = 117 - 113`。

**今天實查:**

```
同名的組 ⇒ 只有 1 組
  維修零件 ×3   sort_order {10, 10, 1001}
✅ 守恆式仍然對得起來:(3-1) = 2 = 117 - 115
```

**另外兩組去哪了 —— 不是刪掉,是【改名】:**

```
水管束環            20  引擎與冷卻 · 水管束環        690 件
水管束環(四輪)      20  四輪 ATV/UTV · 水管束環       22 件
防爆水管組          10  引擎與冷卻 · 防爆水管組      769 件
防爆水管組(四輪)    10  四輪 ATV/UTV · 防爆水管組     22 件
```

📌 **⇒ 該列自己寫的關閉條件「那三組同名各自的去留有人拍板(合併 / 改名 / 刪掉)」,已經被滿足了三分之二 ——
而板上沒有任何一個字記著這件事。**

## 2 🔴 「由一個沒有保證的順序決定」也已過期 —— 今天那個順序【有保證】

板列指的是 `category-queries.ts:69` 只 `ORDER BY sort_order`。**碼今天不是那樣了:**

```
packages/adapters/src/supabase/helpers/category-queries.ts:71  .order('sort_order')
                                                        :106  .order('name')
                                                        :113  .order('id')      ← 唯一鍵 ⇒ 全序
```

🟢 而顧客站那一端**沒有再排一次**:`apps/storefront/src/lib/parse-search-facets.ts` 裡
`localeCompare` / `.sort(` 命中 **0**,解析仍是 `:462` 的 `allCats.find((c) => foldEquals(candidate.to, c.name))`。

⇒ 🎯 **⇒ 所以「選錯每次錯得一樣」那一半【已經落地】。板列自己預告過它會落地(「唯一鍵那一半另片處理」),而沒有人回來把它劃掉。**

## 3 🎯 而真正的那一句仍然成立 —— 只是理由換了

```
三列 維修零件 的今日曝險(顧客站看得到的商品數):
  ce697b77…  sort_order 10    raw_path 維修零件 · 維修零件        1646 件
  d83ce275…  sort_order 10    raw_path 服務與其他 · 維修零件         0 件
  69546b21…  sort_order 1001  raw_path 維修零件                      0 件

照今天的碼 ORDER BY sort_order, name, id 取第一列 ⇒ ce697b77…(1646 件)
```

✅ **⇒ 今天客人點「維修零件」拿到的是【有貨的那一列】⇒ 今日曝險 = 0 個客人看到空分類。**

🛑 **而那是巧合,不是保護:**

- 選中誰,是由 `id` 這個 `gen_random_uuid()` 的**字典序**決定的(`ce69…` < `d83c…`)。
- 沒有任何東西讓「有貨的那一列」排在前面。**兩列的 `sort_order` 都是 10,名字一模一樣。**
- ⇒ 🔴 **重建那一列 / 有人調 sort_order / 有人補資料到另一列 ⇒ 客人靜靜地翻到 0 件那一邊,而膠囊上的字一模一樣。**

📌 **判別句:一個由亂數決定、而今天剛好是對的答案,與一個被保證是對的答案,在畫面上印同一個東西。**

## 4 順手記一個過期的碼註解(**沒改**)

`category-queries.ts:108-110` 逐字:

```
🔬 正式站唯讀實查 2026-09-04:`(sort_order, name)` 這一對還有 3 組平手 ——
   `20 / 水管束環` ×2 · `10 / 防爆水管組` ×2 · `10 / 維修零件` ×2
```

⇒ 🔴 **今天只剩 `10 / 維修零件` ×2。** 那句話仍然在為 `.order('id')` 辯護 —— 而它舉的三個例子有兩個已經不存在。
🟢 **而那不影響結論**:`(sort_order, name)` 今天**仍然**不唯一 ⇒ `.order('id')` 仍然不能省。
🛑 **我沒有改那段註解** —— 動碼不在本次範圍。

## 5 本文答不出什麼

- **沒有量「客人實際點過幾次維修零件」** —— 板列 tidy 標的「客人實際曝險程度未查證」,我補的是**結構性曝險**(哪一列會被選中、各有幾件),**不是流量**。
- **沒有驗那三列 `維修零件` 應該怎麼處理** —— 那是目錄結構的決定,板列寫著要 Sean 或管目錄的人裁。
- **沒有碰 CI 接線那一半**(拍板丁,鐵則 12④)。
- **`raw_path` 那道 UNIQUE 我沒有重驗** —— 本次只驗名字與曝險。
