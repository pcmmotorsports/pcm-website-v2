# `coupon_revert_on_full_refund` 這條線 · 2026-09-11 · 窗 C

> 59 派的兩格:① 那兩個「提到它」的檔各是什麼 ② 要建它需要知道什麼。
> 🟢 **全程唯讀零寫入**,DB 讀數 `pcm_readonly` @ 正式庫 · rc=0 · 真錯誤 0 格 · `2026-09-10 16:58 UTC`。
> 🛑 **沒有寫那支函式的任何一行 SQL**(碰錢 ⇒ 鐵則 8)。

---

## 🔴 先講結論:**問題不是「沒人建它」,是【擋著它的那道牆已經倒了,而沒有人知道】**

板上與 plan 都寫著同一句前提:**扣券還沒上線,所以還不急。**
🔬 **而我今天量到:扣券的寫入路徑,在正式庫上【是通的】。**

```
create_order(… p_coupon_code text)     ⇒ 🔴 兩個 overload 都在線上, 都呼 redeem_coupon
redeem_coupon(p_code, p_user_id, …)    ⇒ 🔴 在線上, prosrc 含 INSERT INTO public.coupon_redemptions
coupon_redemptions 表(含 reverted_at)⇒ 在線上
後台券清單 view 那句 reverted_at IS NULL ⇒ 在線上, 而它排除的永遠是 0 筆
coupon_revert_on_full_refund(uuid)     ⇒ 🔴 **不存在**(repo 零支 CREATE · 正式庫 to_regprocedure NULL)
🟢 正對照 admin_compute_order_settlement(uuid) ⇒ 在  ⚪ 負對照 現造函式名 ⇒ 不在
```

> ## 🎯 **⇒ 今天沒出事的原因【不是】「寫入路徑還沒上線」,而是【一張券都還沒有人建】。**

```
public.coupons             ⇒ 0 列
public.coupon_redemptions  ⇒ 0 列(其中 reverted_at 非空 0 · 空 0)
⚪ 負對照 現造表名 ⇒ 0 列
```

🛑 **⇒ 而「還沒有人建券」不是一道閘,它是一個【當下的事實】。** 任何人手貼一句 SQL 建一張券,
　 那條路當天就從頭通到尾 —— 而**退回路徑不存在,券的名額就只減不增,而後台那張表會一路顯示得很正常。**

---

## 一、① 那兩個檔(以及其實不只兩個)

⚠️ **先訂正我自己**:我上一份寫「repo 命中 2 檔」——**那是我把範圍限在 `supabase/**.sql`**。
放開範圍後是 **9 個檔**,而其中兩個是板上的列。📌 **一個限定範圍的 grep,印出來跟一個全域 grep 一模一樣。**

| 檔:行 | 那是什麼 |
|---|---|
| `supabase/migrations/20260901021000…:311-315` | **前置閘**(`to_regprocedure` 查不到就 `RAISE`) |
| `supabase/migrations/20260901030000…:375-379` | 同一道閘,B 檔那一份 |
| `20260901021000…:291` | 逐字「建立 `coupon_revert_on_full_refund` 的那支 migration **必須排在本檔之前**」 |
| `20260901021000…:208` / `030000…:250` | 回捲說明:回捲後已扣的券名額不會自己回來 |
| `scripts/coupon-revert-migration-order.test.ts` | 守「任何定義它的 migration,版本號不得晚於前置閘」 |
| `docs/launch-todo.md:1340` **⟦b4-COUPONREVERT⟧** 態 **doing** | 🔴 **它有自己的一列,而且是 Sean 2026-09-02 拍 🅐 的前置條件** |
| `docs/launch-todo.md:2491` **⟦db-COUPONREVERTMISSING⟧** 態 **parked** | Sean 2026-09-07 `Q44` 答**乙**(券上線後再開) |
| `docs/evidence/2026-09-10-coupon-when-vs-body-四處逐字比對.md` | 6e 的比對 |
| `docs/handoff/CURRENT.md:383` | `⟦b4-PARTPAIDCOUPONNEVER⟧` 指過來 |

### ⇒ 回答 59 的三個選項

```
❌ 「有人打算建而沒建」—— 不精確
❌ 「從別處抄來的殘留」—— 不是
❌ 「它本來該在另一支 migration 裡」—— 不是
✅ 【它是一個被【刻意】留成前置條件的缺口, 有自己的板列、有 plan、有守門測試,
   而 Sean 明文裁過「晚一點再做」。】
```
🔬 佐證:`~/pcm-mailbox/plan-b4-COUPONREVERT-扣券上線前置條件-20260902.md`(222 行,10,879 bytes)
　 而 commit **`9b665fd7e`** 逐字改的是 —— `launch-todo.md` +1 · `order-state-gates.md` · **那支測試** ·
　 **`20260901021000` +72 行(= 那道閘)**。📌 **那一顆 commit 建的是【閘】,不是【函式】。**

---

## 二、🔴 而 Sean 那兩次拍板,是在一個【今天已經不成立的前提】上做的

```
2026-09-02 拍 🅐   「券的退回機制 = 扣券上線的前置條件」
2026-09-07 Q44 乙  「券上線後再開」(⟦db-COUPONREVERTMISSING⟧ 因此 parked)
plan 2026-09-02 §② 逐字:「今天的形狀是【表在、view 在、而沒有東西寫】」
                          並列 20260831160000 redeem_coupon RPC 為【未 apply】
```
🔬 **而今天 `redeem_coupon` 在正式庫上,`create_order` 也在呼它。**
⇒ 📌 **「扣券還沒上線」在 09-02 是真的;今天它是假的,而沒有一個地方寫著它變了。**
🛑 **⇒ 我不替他重裁。這一格要端回去問他,而問法要帶今天的讀數,不是帶那句舊前提。**

---

## 三、② 要建那支函式需要知道什麼

### 🟢 已經有答案的(不必再問)
```
① 什麼情況退回      Sean 逐字「只有整筆退才退回券」
                    (20260829150000_m4b_coupon_p1_tables.sql:13)⇒ 部分退 ⇒ 不退
② 怎麼表示「退回了」 不刪列, 寫 coupon_redemptions.reverted_at(timestamptz, 可空)
                    + reverted_by(text, 可空)   ← 兩欄都已經在正式庫上
③ 誰在算「用掉幾張」 後台券清單 view 已經寫死 WHERE r.reverted_at IS NULL
                    ⇒ 📌 只要把 reverted_at 填上, 名額就自己回來了, view 不用改
④ 簽章             前置閘釘死 public.coupon_revert_on_full_refund(pg_catalog.uuid)
                    而且【必須是 function 不是 procedure】
⑤ 版本號           守門測試釘死:定義它的 migration 版本號不得晚於 20260901021000
```

### 🔴 還沒有答案的(這才是要問的)
```
① 誰來呼它 —— 20260829150000:41-43 自己寫著逐字
     「reverted_at 現在沒有來源事件:誰在什麼情況下寫它?」
   而今天退款有幾條路(人工退款 / 卡片退款 / 取消未付款…), 哪幾條算「整筆退」⇒ 【沒有人裁過】
② 「整筆退」的判準寫在哪 —— 是看退款總額 = 訂單總額? 還是看訂單狀態?
   ⚠️ 而那與 ⟦b4-PARTCANCELTAX⟧ / ⟦auth-PARTIALREFUNDCANCELGAP⟧ 同一族, 那兩列在別的窗手上
③ 一張單退了又收、收了又退 ⇒ reverted_at 會不會被寫兩次 / 要不要能反悔
④ 誰有權限呼它(service_role? admin? trigger?)⇒ 碰錢碰權限 ⇒ 鐵則 8 + 12
```

---

## 四、🛑 我證不到什麼

1. **我沒有跑過任何一筆真的下單或退款** —— 上面「寫入路徑是通的」是**讀 `prosrc` 的字串位置**推的
   (`position('INSERT INTO public.coupon_redemptions' in prosrc) > 0`),**不是跑出來的行為**。
   🟢 那把尺帶了正負對照(`BEGIN` ⇒ t · 現造字串 ⇒ f),而它證的是**碼在**,不是**它會動**。
2. **顧客站今天沒有券碼輸入欄**(`apps/storefront/src` 只命中 1 個檔而那是註解),
   而 adapter `packages/adapters/src/supabase/mappers/order.ts:176` **已經接好了** ——
   `input.couponCode` 非空就送出去。⇒ 📌 **少的只是一個輸入框。**
3. **後台建不了券**:`apps/admin/src/lib/coupons/coupon-repository.ts` 全檔 33 行,
   `insert/update/delete/upsert` **零命中** ⇒ 今天只有手貼 SQL 建得出券。
   ⚠️ 而「後台沒有建券的功能」同樣**不是閘**,是一個當下的事實。
4. `20260831160000` 是**什麼時候**被 apply 的,我**沒有查** —— 我只量到「今天它在線上」。
