# `⟦b4-QUOTE2FA1⟧` 現值重量 · 2026-09-11 · 6d

> 板列(`docs/launch-todo.md:787`)逐字:**「報價單 2FA【裝好了而關著】—— 而那是後台前面唯一一道門」**(現值 34 天沒量)。
> 🔴 **而板列自己寫著「證據不在 repo…任何施工窗都重跑不了」** ⇒ **今天那句話不成立了 —— 我量到了。**
> 🛑 **全程唯讀**:`mcp__supabase-quote__execute_sql` 只跑 SELECT · **零寫入 · 沒有 `db push` · 沒改報價單 repo 一個字**。

---

# 🔴 §0 結論:**仍成立,而它從「一個過期的數字」變成「一個每天在用的入口」**

```
🔬 2026-09-11 唯讀實測(上一次讀數是 2026-07-27 ⇒ 【46 天】, 不是板上寫的 34 天)

① auth_state.require_2fa            = false
   而那一列的 updated_at            = 2026-06-11 12:21:19+00   ← 🔴 三個月沒有人動過
   updated_by                       = (null)                   ← 🔴 沒有人留下是誰設的
② totp_devices status='active'      = 0     ← 判準逐字(baseline_schema.sql:1019)
③ totp_devices 全部(不分狀態)      = 0
④ recovery_codes                    = 0
🟢 正對照 login_attempts 全表        = 144   ⇒ 尺會動
⚪ 負對照 現造狀態值 zzq0911         = 0     ⇒ 尺不是恆真
```
⇒ ✅ **memory `project_quote-2fa-deployed-but-dormant` 記的四格,今天【逐格一模一樣】。**
📌 **⇒ 這一列的價值不在「數字變了」,在【它沒變,而 46 天來沒有人知道】。**

---

# 🔴🔴 §1 而今天量到板上從來沒有的那一格:**那扇門【昨天還有人在走】**

```
🔬 2026-09-11 唯讀:
最近一次成功登入   = 2026-09-10 12:24:15+00   ← 🔴 昨天
近 7 天  成功 92 次 · 失敗 1 次
近 30 天 成功 143 次 · 失敗 1 次
⚠️ 而【分母的限定詞要跟著走】:login_attempts 最早一筆是 2026-08-27
   ⇒ 📌 這張表只涵蓋 15 天 ⇒ 那個「近 30 天 143」其實是「15 天 143」
🟢 正對照 全表 144 · ⚪ 負對照 現造 outcome ⇒ 0
```

🎯 **⇒ 而那 92 次全部是【只有密碼】那條路** —— 碼這一側逐字證得出來:
```
app/api/admin/login/route.ts:127 逐字
  「發 session cookie。amr=['pwd'] (密碼-only / require_2fa=false) 可退回 legacy」
⇒ 📌 require_2fa=false ⇒ 那一條【明文允許】只憑密碼發 session
```
🔴 **⇒ 所以這一列的曝險不是「將來可能」,是:**
```
過去 7 天, 有 92 次登入走過一扇【只有密碼】的門, 而它前面的第二道鎖【裝好了、關著、沒有人在看】。
```

---

# 🔵 §2 那道門的機制(三格,而它們設計得很好 —— 問題不在設計)

| 格 | 在哪 | 今天的值 | 誰讀它 |
|---|---|---|---|
| ① `auth_state.require_2fa` | **DB** | **false**(2026-06-11 起沒動) | `login/route.ts` —— 決定要不要走 TOTP |
| ② `REQUIRE_2FA_FLOOR` | **Vercel 環境變數** | 🛑 **我讀不到**(見 §4) | `2fa/toggle/route.ts:34` **硬閘**:逐字「缺 `REQUIRE_2FA_FLOOR=true` **不准開 2FA**」 |
| ③ Edge Config `auth_require_2fa` | **Vercel Edge Config** | 🛑 **我讀不到** | `middleware.ts` —— 2FA 開啟時**拒 legacy / 純密碼 session** |

✅ **而 ② 那道硬閘的註解逐字寫著它為什麼存在**:「把上線手冊的**口頭約束變程式防呆**」(哨兵 2026-06-11)。
🎯 **⇒ 所以要打開 2FA 是【兩步】不是一步**:先設 Vercel 的 `REQUIRE_2FA_FLOOR=true`,再開 DB 那一格。
📌 **⇒ 那一格擋住的不是攻擊者,是【我們自己順手開一半】** —— 而它做對了。

---

# 🎯 §3 而它與我今晚查的 `net` 那一列 **有共用前提**(你問的第 3 格)

**兩件事在不同的 repo、不同的專案、不同的機制 —— 而它們的形狀一樣:**
```
⟦tidy-NETPUBLICALL⟧   擋著它的是 Supabase 面板上一格【Exposed schemas】
⟦b4-QUOTE2FA1⟧        擋著它的是 Vercel 上兩格【環境變數 + Edge Config】

🔴 共用前提 ——【那道保護不在資料庫裡, 也不在版控裡】:
   · 它是一個【設定】, 而設定可以被一個人在面板上點掉
   · 而 repo 掃不到它、三綠掃不到它、codex 掃不到它
   · 🔴 而【翻了沒有任何一發會叫】
```
🎯 **⇒ 兩列真正共用的不是技術,是【我們今天沒有一份「面板上有哪些格在守我們」的清單】。**
📌 **而它們的差別要寫死**:`net` 那一格今天**是關的**(保護在);`QUOTE2FA1` 這一格今天**是開的**(保護**沒有**在,而它裝好了)。

---

# ⚪ §4 我證不到什麼

1. 🔴 **`REQUIRE_2FA_FLOOR` 今天是什麼值,我讀不到** —— 它是 Vercel 環境變數,而**我沒有 Vercel 權限**(而板列自己也記著同一件事)。
   ⇒ 📌 **而這一格【不影響結論】**:那道硬閘管的是「能不能**開**」;而今天 DB 那一格是 `false` ⇒ **門本來就沒開**。
2. 🔴 **Edge Config `pcm-auth` 今天的內容我讀不到** —— memory 記著 2026-07-27 它只有一筆 `greeting:"hello world"`(Vercel 預設範例),**而我沒有複量**。
3. **那 92 次登入是誰,我沒查** —— `login_attempts` 我只取了 `outcome` 與 `at` 的計數,**沒有取任何身分欄位**(而報價單那一側的 2FA 本來就沒有「這支手機是誰的」,見 `⟦b4-2FAOWNER1⟧`)。
4. ⚠️ **`login_attempts` 只涵蓋 15 天**(最早 2026-08-27)⇒ **「以前有多少次」我答不出來**,而那不是 0,是**沒有紀錄**。
5. **我沒有嘗試登入、沒有打那個站的任何一發 HTTP** —— 讀數全部來自 DB 的 SELECT。

---

# 🔵 §5 而 `⟦b4-MGRENV1⟧` 那一列:**已查,不重做**

**2026-09-10 我查過一輪,結論照抄**:🔴 **仍成立** —— 三格複現(`session.ts:175` 的 env 讀取在 · `actor.ts` 三層在 · 訊號元件掛在 `layout.tsx:152`)⇒ **會叫的還是只會叫**,轉 `done` 的條件(那個放行真的被擋住)一格沒動。
🛑 **而它的曝險【結構上量不到】**:那顆值 Vercel Type=`Secret`,**連 Sean 本人也讀不到**,`vercel env ls` 在 `=1` 與 `=0` 印同一個 `Encrypted`。
🎯 **⇒ 而它與本列、與 `net` 那一列是【同一族的第三個】** —— **保護住在面板上,而翻了不會叫。**

---

# 🎯 §6 一句給 Sean

```
報價單後台那道「第二把鎖」(手機驗證碼)——
🔴 它裝好了, 而它【是關著的】, 而那個狀態從 2026-06-11 起沒有人動過。
🔴 而過去 7 天, 有 92 次登入從那扇【只有密碼】的門進去 —— 最近一次是昨天中午。
✅ 而要打開它是兩步(先設 Vercel 一個變數, 再開資料庫那一格)——
   那個「兩步」是我們自己設計的防呆, 它做對了。
🛑 而今天沒有任何一發會告訴你這道門是開是關 —— 我是【去問資料庫】才知道的。
```
