# 2026-09-10 小族(`0a` `front` `0e` `c7` `d8` `15` `search` `tidy`)· 12 列裡查了 4 列

> 🛑 **沒有改板。** 📎 全板分佈:`~/pcm-mailbox/板子權威分佈-尺C-20260910.md`
> 🔵 **結論用六種**:已修好 / 條件已消失 / 本列描述有誤 / 仍成立(+今天曝險)/ 機制在而我沒驗它守的是不是那一句 / 前提翻面

---

## 🔴 `:2492` `⟦tidy-NETPUBLICALL⟧` ⇒ **仍成立 —— 而我今天複量逐格對上**

· **板上逐字**:「`anon` 今天對 `net` 兩張表有 SELECT/INSERT/UPDATE/DELETE/TRUNCATE —— **而 schema USAGE 也是 `t`,所以那些權限【到得了】**」
· **【量的】2026-09-10 唯讀查正式庫**(板上的讀數是 2026-09-07/08,**我複量的是今天**):
```
net._http_response      anon: S t · I t · U t · D t · TRUNCATE t
net.http_request_queue  anon: 同上
relacl 逐字 {supabase_admin=arwdDxtm/supabase_admin, =arwdDxtm/supabase_admin}
                                                      ^^^ grantee 空 = PUBLIC
RLS  兩表皆 f · policies 0
schema USAGE  anon ⇒ t · authenticated ⇒ t
🟢 正對照 anon 對 public.orders SELECT ⇒ f     ⇒ 尺會咬
⚪ 負對照 現造 schema / 現造角色 ⇒ 0
```
⇒ 🔴 **仍成立,而板上那三天前的讀數今天【一格都沒動】。**

### 🛑 而那一列有一格【我沒有,而它比現值重要】
> **授權來源是 `PUBLIC`,不是具名角色** ⇒ 📌 **修法要收的是 `PUBLIC`,逐個角色收會漏掉下一個新角色。**
> 🔴 而 `postgres` **不是 superuser、也不是 `supabase_admin` 的成員**,那些授權**沒有 GRANT OPTION**
> ⇒ **一支 REVOKE 會印 `REVOKE`、`rc=0`、交易 COMMIT,而 ACL 逐字原封不動**(WARNING 不進 rc,`ON_ERROR_STOP` 也擋不住)。

🎯 **⇒ 那是「一個成功的輸出,而什麼都沒發生」的最乾淨實例。而它已經被寫下來了 —— 我只是複量。**

### 🔵 而「外面叫不叫得到」那一格,板上標的是【量不到】不是【沒查】
它試過四種問法(`pgrst.db_schemas` / `db_schema` / `ALTER ROLE authenticator` 的 `setconfig` / DB 層 `setconfig`)全部拿不到,
並且明文:**不要用打一發 HTTP 去驗**(主視窗 2026-09-08 裁,三個理由)。
📌 **⇒ 我同意,而且我也沒打。**

**今天的曝險**:🟡 **仍是「兩個世界差很多」** —— 面板沒有 `net` ⇒ 深度防禦破口;有 `net` ⇒ 拿到 anon key 的人可以發任意 HTTP + `TRUNCATE` 掉回應表。
🔴 **而 anon key 是公開的(印在前端)。** ⇒ **那一格今天仍然只有 Sean 唸一次面板就答得出來。**

---

## 🔴 `:2505` `⟦front-ACLSNAPSHOTAGE⟧` ⇒ **仍成立**

· **板上逐字**:「`supabase/acl-snapshot.tsv` 可以無限期變舊,而【**沒有任何一發會紅**】」
· **【量的】**:
```
supabase/acl-snapshot.tsv 最後一次改動 2026-09-05  ⇒ 今天 2026-09-10, 距今 5 天
提到它的腳本 2 支:scripts/acl-snapshot.sh(產生它)· scripts/acl-digest-parity.sh(比 digest)
🔍 這兩支裡掃 mtime|age|過期|天|days|stat -f  ⇒ 沒有任何一處在判【年齡】
🟢 正對照 全 repo 掃「acl-snapshot 且含 age/過期/stale」⇒ 只命中 acl-digest-parity.sh
   而開檔看:它 :105 說的是「跑 acl-snapshot.sh 看基線是不是過期了」—— 那是【給人看的提示】不是閘
⚪ 負對照 現造檔名 ⇒ 0
```
· ⇒ 🔴 **仍成立。今天沒有任何東西在守它的新鮮度。**
· 🔵 而 `scripts/acl-snapshot.sh:10` 自己逐字寫著:「**今天手改之後,沒有任何一刻會有…**」⇒ **那一列與那支腳本說的是同一件事。**
· **今天的曝險**:🟡 **5 天** —— 而**它本身不是問題**,問題是「沒有東西會叫」。**5 天與 50 天在今天的機制下印同一個綠。**

---

## 🟡 `:525` `⟦tidy-ANONGATEJUDGE⟧` ⇒ **機制在,而我沒驗它守的是不是那一句**

· **板上逐字**:「`check-anon-grants-prod.sh` 的【判準】沒有任何東西在守 —— **而甲案只補得到參數那一層**」
· **【量的】** 那支腳本**有被掛在 lint-staged**:
```
package.json:170  "scripts/check-anon-grants-prod.sh": "bash scripts/check-anon-grants-prod.sh --selftest"
```
· 🛑 **而那一列講的是【判準】不是【參數】** —— **我沒有開那支 `--selftest` 看它守的是哪一層。**
  📌 **⇒ 「有一個 selftest」與「那個 selftest 守得到判準」是兩件事** —— 而那一列的整句話就是在講這個分別。
· ⇒ **🟡 第五類。要收它得開那支 selftest 逐格看。**

---

## ✅ `:927` `⟦0e-PROBENOSCHED⟧` ⇒ **仍成立(已於 `0e` 那份查過)**

證據:`docs/evidence/2026-09-10-0e族查證.md` §二。**沒有排程**(🟢 正對照 `rpm-sync` 有 schedule 而不跑它);
🛑 **而【證不到】「有沒有人手動跑過」** —— 板列那句「17 天來那個人」講的是後者,**要一個人回答,不是一個 grep**。

---

## 🛑 還沒查的 8 列

```
:1011 search-DUPCATNAMES         三組同名分類, 客人拿到哪一個由沒有保證的順序決定
:1027 front-CATALOGPRICEGENERALONLY  目錄頁篩選/排序用一般價算, 經銷客人畫面…
:1244 15-SHIPGATE-F1             停線期間的出貨信把死信告警訊號永久推成非零
:1486 c7-LEDGERGATEREFUSES       Sean 退了現金而系統不讓他記 —— 閘擋下的是【紀錄】, 錢早就離開了
:2065 0a-DUPKEYSHADOW1           一筆設定被後面同名的蓋掉, 而它讀起來完全正常
:2097 d8-CROSSRAILUNVERIFIED     線上證到的是【它在】, 不是【它算對】
:2343 0a-CARDCANCELNOREFUND      取消刷卡單今天沒有任何東西會自動退那筆錢
:2465 search-TAXONOMYTIMEOUT     車款查詢逾時而靜默降級, 客人拿到殘缺下拉
```
🔵 **而 `:1486` 值得先做** —— 它是**錢已經離開而系統不讓記**,那與我這條線(資料庫閘)同一族。

---

## 🛑 本份證不到什麼

· **【證不到】`net` 有沒有被 PostgREST 暴露** —— 板上標的是【量不到】,而**我也量不到,而且不打 HTTP**。
· **【證不到】`:525` 那支 selftest 守到哪一層** —— 沒開檔。
· **【證不到】其餘 8 列** —— 沒查。
· **正式庫零寫入。沒有改板。**

---
---

# 🎯 `:1486` `⟦c7-LEDGERGATEREFUSES⟧` ⇒ **前提翻面(第六類)—— 那一列在等的東西,今天到了**

## 一、那一列被推進到只剩兩格,而兩格今天都動了

板列 2026-09-08 線【資料】`-db` 逐字留下的兩個缺格:
> ① 🔴 **`order_manual_refunds` 今天 0 列** ⇒ **這條路在正式庫上一次都沒有被走過。**
>    我證的是**函式體不會擋**,**不是「一列真的進得去」**…
>    ⇒ 📌 **本列的性質因此從【等修】變成【等第一筆真的人工退款】。**
> ② ⚠️ **我沒有查 UI 那半有沒有 commit / 上線** —— 我只證了**碼裡有那個邏輯**。

## 二、【量的】缺格① —— **0 列 ⇒ 今天 2 列。那條路被走過了。**

2026-09-10 唯讀查正式庫:
```
public.order_manual_refunds 總列數      = 🔴 2   (板上 2026-09-07 是 0)
cap_state 分佈:  within 2 筆 · 超收合計 0
🟢 正對照 同一把尺問 public.orders     = 8      ← 與板上寫的 8 張逐字對上 ⇒ 尺沒歪
🟢 正對照 cap_state / over_cap_by 兩欄 = 2 欄在
⚪ 負對照 現造欄名                      = 0
```
⇒ ✅ **「一列真的進得去」現在是【量到的】,不是推的。** 那一列等的第一筆,來了(而且是兩筆)。
🎯 **⇒ 它從【等第一筆】變成【第一筆已經有了】** —— **前提翻面(第六類)。**

### 🛑 而【超過上限】那條路仍然沒有被走過
```
cap_state = 'over' 的列 = 0 筆
```
📌 **⇒ 所以「記得下來、而超出的那筆標得出來」那一半,今天仍然沒有在真實資料上發生過。**
🔵 而那**不是缺陷** —— 兩筆都在上限內是**正常的**。**它只是還沒有機會被驗。**
🎯 **⇒ 而那正是關閉條件③那一發負對照要的東西**(「沒超過上限的那一筆**不可以**被標紅」)——
✅ **今天那兩筆 `within` + `over_cap_by` 為 NULL,就是那個負對照的第一個真實讀數。**

## 三、【量的】缺格② —— **UI 那半在 repo,而且在 `origin/dev` 上**

```
apps/admin/src/components/orders/order-detail-money-tab.tsx:256-257
   manualRefundRed.overCap || manualRefundRed.capUnknown
最後一次改動  2026-09-08  97755e6b7
🟢 正對照 該檔在 origin/dev 上 ⇒ 在
```
🔵 **而 `CLAUDE.md` 逐字「推 dev = 後台上線」** ⇒ **推的**:**它上線了。**
🛑 **【證不到】那個紅真的在畫面上出現** —— **我沒有開後台看**(而後台不能用 curl 驗,`reference_admin-cannot-be-verified-by-curl`)。

## ⇒ `:1486` 的正確標記

```
前提翻面(第六類)· 那一列自陳「從【等修】變成【等第一筆真的人工退款】」—— 而第一筆今天有了(2 筆)
✅ 缺格① 收掉:order_manual_refunds 0 → 2 列, 兩筆皆 within, 兩個新欄在
✅ 缺格② 收掉:UI 那半在 origin/dev 上(2026-09-08 97755e6b7)
🛑 而【仍未驗】的是「超過上限那一筆會不會被標紅」—— cap_state='over' 今天 0 筆
   📌 那不是缺陷, 是【還沒有機會發生】⇒ 曝險 0(分母:訂單 8 張 · 人工退款 2 筆)
🔵 翻態歸 -c7 或 ship(Sean Q57 甲)—— 我沒有動態、沒有動 token
證據 docs/evidence/2026-09-10-小族那批.md §「:1486」
```

## 🛑 這一格證不到什麼

· **【證不到】那個紅在畫面上真的出現** —— 沒開後台。
· **【證不到】超過上限那條路會怎樣** —— **今天沒有任何一筆是 `over`。**
· **【證不到】那兩筆是誰在什麼情況下登記的** —— 我只數了列數與 `cap_state`,**沒有讀任何一筆的內容**(那是客人的錢的紀錄)。

---
---

# 第三輪 —— 再查 3 列(小族累計 8 / 12)

## 🟡 `:1011` `⟦search-DUPCATNAMES⟧` ⇒ **症狀少了三分之二,而【機制一格沒動】**

· **板上 2026-09-04 量到的**:`維修零件` **3** 列 · `水管束環` **2** · `防爆水管組` **2** ⇒ **三組**
  而它自己驗算過:`(3-1)+(2-1)+(2-1) = 4 = 117 個分類 vs 113 個相異名` 那個差。

### 【量的】2026-09-10 —— **症狀那一半**
```
維修零件 3 · 水管束環 🔴 1 · 防爆水管組 🔴 1      ⇒ 三組 → 【一組】
categories 總列數 117(不變)· 相異名稱 113 → 🔴 115
⚪ 負對照 現造名稱 ⇒ 0
```
🎯 **⇒ 總數沒變而相異名多了 2** ⇒ 📌 **那兩組是【被改名】,不是被刪除。**
✅ **而驗算仍然守恆**:`(3-1) = 2 = 117 − 115` ⇒ **兩個獨立的數對得上。**

### 🔴 【量的】而**機制那一半今天逐字沒動**
```
117 列 / 30 個相異 sort_order        ⇒ 與板上 09-04 【逐字相同】
號 10 有 19 列                        ⇒ 【逐字相同】
⚪ 負對照 現造 sort_order ⇒ 0
```
🛑 **⇒ 所以那一列的核心(`.find()` 拿第一個、而「第一個是誰」沒有保證)【完全沒有被碰】。**
🎯 **⇒ 正確結論是【本列描述有誤(數字)· 而問題仍成立(機制)】** —— 📌 **兩件事,不要合成一句。**

### ⚠️ 而我這一發自己撞到一個「兩個都對」
我問「與別人共號的列數」⇒ **100**,而板上寫 **87**。
【量的】拆開:`117 − 30 = 87`(**多出來的列數**)· 而**處在共號群裡的列數** = **100**
⇒ 驗算:17 個單獨的號 + 100 = 117 ✅ · 100 − 87 = 13 = **共號群的組數** ✅
📌 **⇒ 兩個都是對的,而它們回答的不是同一題。我沒有把 100 報成「板上那個 87 錯了」。**

**今天的曝險**:🟡 **一組同名(`維修零件` 3 列)** —— 而**客人點進去拿到哪一列仍然沒有保證**。

---

## 🎯 `:2097` `⟦d8-CROSSRAILUNVERIFIED⟧` ⇒ **那一列自陳「還開著的只剩一件」—— 今天量到了**

· **板上逐字**:「🛑 **⇒ 所以本列【還開著的】只剩一件:正式庫上那一支是不是同一份。**」
  (`-d8` 2026-09-05 已在拋棄式 PG 上證到**算得對**:匯款收 1000 / 現金退 500 ⇒ 開出 **500**,而壞的那一版會開 1000。)

### 【量的】2026-09-10 唯讀查正式庫
```
🟢 pcm_pending_refund_amounts 在正式庫上           ⇒ 1 支
🔴 而【舊字面 `x.amt > 0`】在它的定義裡            ⇒ 0 次   ← 那是壞版本的特徵
⚪ 負對照 現造函式名                                ⇒ 0
```
⇒ ✅ **正式庫上那一支【不含壞版本的特徵字面】。**
🛑 **而我要把話說到剛好**:**那證的是「它不是舊版」,不是「它與鑽機上那一份逐字相同」** —— **我沒有比對摘要。**
📌 **⇒ 那一列的措辭是「是不是同一份」,而我答得出來的是「不是那一份壞的」。兩句不一樣。**

### 🔵 而它的關閉條件是【事件式】的,而那個事件今天還沒發生
板上逐字:「**第一張真的跨軌取消單發生之後**,拿那張單對一次…相等 ⇒ 關。」
```
order_pending_refunds 今天 = 🔴 0 列
而 orders 已取消 3 張 / 全部 8 張 ⇒ 有取消, 而【沒有一張開出跨軌待退款】
```
⇒ 📌 **所以那一列不是「沒人做」,是【第一張跨軌取消單還沒出現】。**
🔵 **而它與 `:1486` 是同一個形狀** —— `:1486` 等的第一筆**今天到了**,`:2097` 等的**還沒**。

**今天的曝險**:**0(分母:已取消 3 張 · 跨軌待退款 0 列)**

---

## ✅ `:2343` `⟦0a-CARDCANCELNOREFUND⟧` ⇒ **今天沒查,而理由要寫清楚**

那一列自己已經把事實與決策分開了:
> ⚠️ **本列不含「該不該自動退刷」那個決策** —— **那是 Sean 的題**,這裡只記「今天零退刷零提醒」這個事實。

🛑 **⇒ 而它剩的那一半是【Sean 的一句話】,不是一個量測。**
🔵 **而它的成因板上也寫死了**:`pcm_pending_refund_amounts` 的值域逐字 `FROM (VALUES ('bank_transfer'), ('cash'))` ⇒ **裡面沒有卡**
⇒ **全刷卡的單回零列 ⇒ 不畫框。** 🎯 **那與我上面量到的「`order_pending_refunds` 今天 0 列」是同一個機制的兩面。**

---

## 📊 小族收盤

```
帶擋 12 列 · 今天查了 8 列 · 還沒查 4 列
   :1027 front-CATALOGPRICEGENERALONLY · :1244 15-SHIPGATE-F1
   :2065 0a-DUPKEYSHADOW1              · :2465 search-TAXONOMYTIMEOUT
✅ 今天已可翻不擋 = 0 列
   🔵 而 :2097 收掉了它自陳的最後一格(不是舊版), 只剩一個【事件】沒發生
   🔵 而 :1011 的數字要改(三組→一組), 而機制不變 ⇒ 不是翻不擋
```

## 🛑 本輪證不到什麼

· **【證不到】`:2097` 正式庫那支與鑽機那份逐字相同** —— 只證了**不含壞版本的特徵字面**。
· **【證不到】`:1011` 那兩組是誰改名的、什麼時候** —— 只量了今天的狀態。
· **【證不到】其餘 4 列** —— 沒查。
· **正式庫零寫入。沒有讀任何一筆訂單或退款的內容。沒有改板。**

---
---

# 第四輪 —— 最後 3 列(小族 11 / 12,`:1027` 主視窗另行處理)

## ✅ `:2465` `⟦search-TAXONOMYTIMEOUT⟧` ⇒ **已修好(端到端)**

· **板上逐字**:「車款查詢會逾時,而它【靜默降級】—— 客人拿到一份殘缺的車款下拉,**畫面不會說**」
  而它點名的成因:「`failed` 這個旗標**已經算出來了**,而 `fetchVehicleTaxonomy` **刻意把它丟掉** ⇒ 📌 **接住它的那一半在,說出來的那一半沒有。**」

### 【量的】而「說出來的那一半」今天【整條接起來了】
```
app/page.tsx:140   mark('tax', tryVehicleTaxonomy())        ← 改走【帶 failed】的那扇門
            :172   const vehicleTaxonomyFailed = vehicleTax.failed;
            :191   `taxFailed=${vehicleTaxonomyFailed}`     ← 進 log
            :240   vehicleTaxonomyFailed={vehicleTaxonomyFailed}   ← 往下傳
CartView.tsx:297   <VehicleTaxonomyNotice failed={vehicleTaxonomyFailed} />
products-message-state.tsx:107
   export function VehicleTaxonomyNotice({ failed }) {
     return <TaxonomyNotice failed={failed} message={VEHICLE_TAXONOMY_UNAVAILABLE} />;
   }                                        ← 🎯 它真的畫出一句話, 不是 return null
🟢 正對照 全 storefront(排除測試)掃 TaxonomyFailed ⇒ 31 處 ⇒ 接得很廣
🟢 正對照 測試【兩個方向都有】:CartView.test.tsx:594 failed / :604 failed={false}
```
· 🔵 而 `page.tsx:137` 的註解逐字寫著 **「2026-09-06(Sean 拍甲 · `⟦search-TAXONOMYTIMEOUT⟧`)」** ⇒ **它自己指回這一列。**
· ⇒ ✅ **已修好。** 🛑 **而【證不到】客人在真的逾時發生時看到那句話** —— 我證的是**碼接起來了 + 兩個方向都有測試**,**不是在正式站看過。**

---

## ✅ `:1244` `⟦15-SHIPGATE-F1⟧` ⇒ **條件已消失(而分母要寫在旁邊)**

· **板上逐字**:「那個數字現在天天印在日報上,**一直非零而不會降** ⇒ **新的死信仍然不改變任何可觀察的東西**」
  而它自陳「🛑 **而後果現在正在發生** —— 那片在 `origin/dev` 上」。

### 【量的】2026-09-10 唯讀查正式庫
```
public.email_outbox 總列數 = 7 · status 分佈:sent 7(只有這一種)
🔴 死信(attempts >= max_attempts)= 0 筆
   其中 event_type = order_shipped 的 ⇒ 0 筆(整個群組空)
```
⇒ ✅ **今天正式庫【沒有任何死信】** ⇒ 📌 **「那個數字一直非零而不會降」今天不成立 —— 它是 0。**
🛑 **而我要把分母寫在旁邊**:**整張表只有 7 列,而且全部 `sent`。**
🎯 **⇒ 這不是「那個機制被修好了」,是【還沒有任何一封信死掉】。**
📌 **⇒ 而那一列講的病(一旦非零就永遠非零)**,**今天沒有實例,而它的機制我沒有驗**。
· ⇒ **結論:條件已消失(分母:`email_outbox` 7 列全 `sent`)** —— 🔴 **而【前置那支 migration】仍然沒有人寫**,板上逐字「**而那支還沒有人寫,也沒有人認領**」。
  ⇒ 🛑 **所以它會在【第一封信死掉】那天回來。** 與 `:1486` / `:2097` 同一族:**等一個事件。**

---

## 🟡 `:2065` `⟦0a-DUPKEYSHADOW1⟧` ⇒ **本列描述有誤(一半)—— 偵測器今天在,而射程仍窄**

· **主視窗 A 2026-09-08 裁 ⟨擋⟩ 的理由逐字**:
  > **一個偵測不到的東西,「今天沒有實例」這句話本身沒有來源。**

### 【量的】而今天【有偵測器】,而且它掃全樹
```
scripts/package-json-dup-key-gate.py 在
package.json:119   "*.json": "python3 scripts/package-json-dup-key-gate.py"     ← 🎯 全部 .json
package.json:117   同一支的 --selftest 掛勾
而那支腳本的檔頭自己記著那個病:
   「窄的從來不是這支腳本 —— 它無參數時就是 git ls-files '*.json'(全樹)。
     窄的是它【什麼時候被叫】:原本逐字是 "package.json" ⇒ 只有動到它那一次才會跑
     ⇒ 改壞 vercel.json / turbo.json / … 一聲都不會叫」
```
⇒ ✅ **那個「窄的觸發」今天已經從 `package.json` 擴成 `*.json`** ⇒ 🎯 **「偵測不到」那一半【不成立】。**

### 🛑 而射程仍然窄兩格,而那兩格【那支腳本自己寫著】
```
① 它是 lint-staged ⇒ 只看【這一次 staged 的檔】⇒ 🔴「回頭也偵測不到」那一半【仍然成立】
   🔵 而它無參數時可以掃全樹 —— 那條路【存在而沒有被接上任何會自動跑的東西】
② JSONC(帶 // 註解的 json, 例 .vscode/*.json)它讀不動 —— 而那不是重複 key 的豁免, 是尺看不到
```
⇒ **結論:🟡 本列描述有誤(前向偵測今天在),而【回頭偵測】與 JSONC 兩格仍成立。**
📌 **⇒ 而主視窗那句判準沒有被推翻**:對 JSONC 與歷史檔,**「今天沒有實例」仍然沒有來源。**

---

## 📊 小族最終收盤

```
帶擋 12 列
✅ 已修好          2 列   :2465(端到端接起來)· 條件已消失 :1244
🟡 描述有誤(部分)2 列   :1011(三組→一組, 機制不變)· :2065(前向偵測在, 回頭仍缺)
🟡 只剩一個事件    2 列   :2097(不是舊版, 而第一張跨軌取消單沒出現)· :1486(第一筆到了)
🔴 仍成立          2 列   :927(沒有排程)· :2492 NETPUBLICALL(今天複量逐格對上)
🟡 第五類          1 列   :525(有 selftest 而我沒驗它守哪一層)
🔵 是 Sean 的一句話 1 列   :2343
⏭️ 主視窗另行處理  1 列   :1027(經銷價整包)
🛑 沒查            1 列   —— 已全部查完(11/12)
```
🔵 **而【今天已可翻不擋】= 2 列**(`:2465` · `:1244`)—— 🛑 **而 `:1244` 那一列我標「條件已消失」而不是「已修好」**,
📌 **因為它會在【第一封信死掉】那天回來,而前置那支 migration 仍然沒有人寫。**

## 🛑 本輪證不到什麼

· **【證不到】`:2465` 客人在真的逾時發生時看到那句話** —— 只證了碼接起來 + 兩個方向都有測試。
· **【證不到】`:1244` 那個機制** —— **今天沒有任何一封死信,所以它一次都沒有被觸發過。**
· **【證不到】`:2065` 的 JSONC 與歷史檔** —— 那支腳本自己說它讀不動 / 不會回頭掃。
· **正式庫零寫入。沒有讀任何一封信的內容。沒有改板。**
