# 2026-09-10 B7 端到端負向驗收 · 報價單那一半 —— **有測的比板上以為的多,而「有測」離「驗收過」還有一段**

> 🔴 **本檔的每一個數字,後面要跟著它的【母體】。跨 repo 的數字不與本 repo 的數字比對,除非兩邊的母體被逐字寫出來。**
> (主視窗 2026-09-10 交代。不是新規矩 —— 是把 09-10 撞了五次的那件事寫在最容易再撞的那扇門上。)

> 板列(`docs/launch-todo.md` 那列「**B7 端到端負向驗收 + 開關已開,只剩負向驗收**」,**無錨**)與狀態檔
> `docs/specs/2026-08-27-m4b-e8b-b7-nine-condition-status.md` 逐字寫著:
> 🛑 **「報價單側那半我【沒有下任何結論】—— 我看不到那個 repo,不寫『應該也有測』。」**
>
> **這一份就是去看那個 repo。**

來源標籤:**【量的】**(命令 + 讀數,尺先證明會咬)/ **【推的】** / **【證不到】**

🛑 **本份的操作邊界(措辭照實,不寫「零寫入」)**:
· **沒有留下任何持久的程式修改** —— 三發突變**暫時改過生產碼**,每一發都 `cp` 備份、跑完還原,**`shasum -a 256` 逐位元組相同 + `git status` 乾淨**。
· **沒有 commit / 沒有 push / 沒有 apply / 沒有跑 `db push` / 沒有碰另外兩棵 worktree 的任何檔。**

---

## 一、【量的】先把母體釘死 —— **板上那兩個名字是同一棵樹**

```
~/API大量上架/PCM報價單-V2  的 remote
  ⇒ git@github.com:pcmmotorsports/pcm-quote-v2.git
```
⇒ ✅ **板列 08-31 那一格同時用了 `PCM報價單-V2` 與 `pcm-quote-v2` 兩個名字,而【沒有人說過它們是同一個】。今天說了。**

| 量什麼 | 板上 | 今天 |
|---|---|---|
| 分支 | `main` = production | **`main`** ✅ |
| 工作樹 | — | **乾淨**(0 個未 commit 檔) |
| commit 總數 | 08-31 記 **2007** | 🔴 **3396** —— 那個數腐爛了 |

### 【量的】同 repo 另外兩棵 worktree
```
quote-wt-g5-e8b   302d0226  2026-08-21  chore(migration): B4 兩欄已套上正式庫 …    未 commit 檔 = 0
quote-wt-merge    8a4707e1  2026-08-21  merge(auth): 真登入線 B0-B4 併入 main …     未 commit 檔 = 1
🟢 正對照 主樹      863a0a25  2026-09-09                                          ⇒ 尺讀得出「最近」
```
⇒ ✅ **【量的】兩棵的最後 commit 都是 20 天前,而其中一棵有 1 個未 commit 檔。**
🛑 **而我第一版由此寫「沒有人正在做 B7」—— 收回。**(codex 2026-09-10 must-fix ⑦)
📌 **一個舊 HEAD 與一份還沒 commit 的活工作【可以同時存在】** ⇒ 🔴 **「有沒有人在做」本份【沒有確認】。**
🔵 **而日期仍然有用**:它把「**兩棵都是今天動的**」這個世界排除掉了 —— **那是它答得出的,不是全部。**
🛑 **兩棵都沒 checkout、沒讀檔、沒 merge、沒刪。那個未 commit 檔我沒看是什麼。**

---

## 二、🔴 而我第一把尺量到 **1**,正確答案是 **82**

```
我先用 *.test.ts / *.spec.ts 掃          ⇒ 1 支(而那支還在 docs/archive/ 底下)
正確的尺 tests/node/*.mts (node --test)  ⇒ 82 支
🟢 正對照 tests/ 底下的 python 測試        ⇒ 298 支 ⇒ 尺會咬
⚪ 負對照 現造檔名                        ⇒ 0
```
🎯 **⇒ 又是【命名的 0】。而那份 B7 狀態檔的第一段就是在警告這件事** ——
> 「給下一個 grep『B7』的人:你會拿到 **0**,而那個 0 是【命名】的 0,不是【覆蓋】的 0。」

🛑 **那份檔的作者已經替我踩過一次,而警告寫在第一段仍然沒擋住我。**
📌 **⇒ 這個 repo 的每一發之前先問一句:這把尺在【這個 repo 的命名習慣】下咬得到嗎。**

---

## 三、【量的】那 4+1 條,逐條對 —— **而每一格寫的是【我證到什麼】,不是【它過了沒】**

| 條 | 板上狀態 | 今天量到 |
|---|---|---|
| **#2** legacy fail-open cookie 發不出來 | ⬜ 分母不在這裡 | ✅ **那個判斷函式擋得住兩種突變**(§4);🔴 **登入端的 cookie 行為未驗** |
| **#7** 另一半 混版 admin新/報價單舊 | 🟡 另一半分母不在這裡 | 🟡 **session 相容性單測擋得住一發突變**(§5);🔴 **而它對不對得上 #7 的情境,我沒建立** |
| **#9** 首次登入沒改密碼不得經 SSO 繞過 | ⬜ 分母不在這裡 | 🟡 **首次登入那條路讀碼推得出擋得住**;🔴 **既有票那條路是已知缺口,算不算排除【沒有依據】**(§6) |
| 🔴 **#3** 另一半 備援進得去而寫入被擋 | 🟡 另一半分母不在這裡 | 🔴 **找到的是 2FA 存取閘,不是備援唯讀閘 —— 兩件事**(§7) |
| 🔴 **#5** SSO code 重放 ⇒ 擋 | ⬜ 分母不在這裡 | 🔴 **搜尋範圍內找不到**(§8) |
| 🟡 **#8** 訊息/狀態碼/**回應時間**三者一致 | 🟡 量法已設計、實作未做 | 🟡 **身分解析層的失敗結果一致有單測;HTTP 訊息/狀態碼與時間三軸的路由驗收都還沒對應**(§9) |

### 【量的】五支相關測試今天實跑(唯讀,不碰真庫)
```
主發(餵 5 支)  rc=0 · tests 56 · pass 56 · fail 0
第二發(同上)   rc=0 · tests 56 · pass 56 · fail 0        ⇒ 連跑兩發相同
🟢 正對照 只餵 1 支(session-sub)⇒ tests 13              ⇒ 尺隨輸入變, 非恆綠
⚪ 負對照 餵不存在的路徑        ⇒ rc=1                    ⇒ 不會靜靜通過
逐支獨立跑再加總:
   identity-named-login 26 · session-sub 13 · must-change-password-flow 2
   · sso-sub 7 · login-payload 8   ⇒ 合計 56 = 合跑那一發
```
⇒ ✅ **【逐支與合跑的測試總數一致】。**
🛑 **而那【只核對得了數量,核對不了身分】** —— 兩邊格數相同不保證跑的是同一組格。(codex nit ②)

🛑 **而我第一把「它跑幾支」的尺印 `0`** —— `node --test` 的輸出不印檔名,我 grep 錯形狀。
📌 **那是【尺壞了】不是【答案】** ⇒ 換成逐支加總才有讀數。**這一格不留白。**

🔵 **不碰真庫是量的**:那幾支檔頭自己寫著「驗密碼與查映射都用注入的假 deps」,
且 env 是 `https://example.supabase.co` 與 `'y'.repeat(40)` 這種假值。

---

## 四、`#2` —— **那個判斷函式擋得住兩種突變,而那不等於這一條驗收過了**

母 plan 逐字要的是:**「每一項都要有一格【會紅】的測試,不是『應該會擋』」**。
⇒ 📌 **一支測試存在 ≠ 它會為了這一條而紅。** 所以做突變。

**目標**:`lib/session.ts:246` 的 `legacyCookieAllowed`(生產碼,不是測試)。

```
🔴 M1  白名單 → 黑名單   return sub.kind !== 'user' && isPasswordOnlySession(amr);
   ← 正是那段註解自己警告的沉默失敗(W6-047 M1)
   前置斷言:突變真的落地(grep 到新字面)
   ⇒ rc=1 · tests 26 · pass 25 · fail 1
      not ok 14 —「🔴 白名單而非黑名單: 未知的 sub 變體【出生就不得】退回 legacy」
   ✅ 而【格 7 沒紅】—— 對的, 黑名單仍然擋得住 'user'

🔴 M2  整個閘 fail-open   return true;
   ⇒ rc=1 · tests 39 · pass 36 · fail 3
      「具名 session 永遠不得退回 legacy(格 7+7b)」
      「白名單而非黑名單…」
      「對照組: 備援/bootstrap 照舊可退回(格 8)」← 連對照組都紅

還原:sha256 逐位元組相同 · git status 乾淨
```
✅ **⇒ 證到的是:這兩個指定的錯誤,既有斷言分得出來。**
🛑 **而【證不到】這一條驗收過了。**(codex must-fix ①)
📌 **原因**:那三格**直接呼叫 `legacyCookieAllowed`**,**沒有走登入端真的發 cookie**。
🔴 **⇒ 若有一個呼叫端【繞過這支函式】直接設 `ADMIN_COOKIE`,那三格照樣全綠。**
🔵 而 B2 規格第 7/7b 格要的是「構造設定缺漏 / 簽章失敗 ⇒ 登入失敗**且不得設** `ADMIN_COOKIE`」——**那一層沒驗。**

🛑 **而「紅得有層次」這個說法要收窄**(codex nit ③):它描述的是**這兩發指定突變的差異**,
**不能延伸成整套測試的普遍判別力**;M1 跑 26 格、M2 跑 39 格,**兩個失敗比例也不該互相比較**。

---

## 五、`#7` 混版 —— **一發突變紅了,而紅的那一格證明了另一件事**

**目標**:`lib/session.ts:158` 的 `if (o.v !== 2) return false;`

```
🔴 M3  連 v:1 也收   if (o.v !== 2 && o.v !== 1) return false;
   前置斷言:突變落地(sed 印出新字面)
   ⇒ rc=1 · tests 20 · pass 19 · fail 1
      not ok 3 —「🔴 v 這道檢查【自己】要有判別力 —— 造一顆 v:1 但【帶合法 sub】的 cookie」
   🔴 而「舊 v:1 cookie 一律拒」那一格【沒有紅】
還原:sha256 逐位元組相同 · git status 乾淨
```
🎯 **為什麼「一律拒」那格沒紅**:一顆正常的 `v:1` 票**本來就沒有 `sub` 欄** ⇒ 它被**別的**檢查擋掉。
📌 **⇒ 那一格今天是綠的,而它綠的理由【不是它名字講的那件事】。**
✅ **而那支測試的作者早就知道** —— 所以他多寫了格 3,標題逐字就是「`v` 這道檢查**自己**要有判別力」。
🔵 **⇒ 這是「一支測試存在 ≠ 它會為了這一條而紅」最乾淨的一個實例:同一支檔裡,一格會另一格不會。**

🛑 **而這一發【不能算成 `#7` 驗收過了】。**(codex must-fix ③)
📌 `session-sub.test.mts` 驗的是**報價單 session 自己的版本 / 形狀 / 簽章**,
🔴 **它沒有構造「舊/新 `authorize` × `exchange` × admin」那些組合**,而 B4 混版規格要的是
「**缺少身分時不當機、不憑空補成 fallback,以及下游最後如何處理**」。
⇒ ✅ **能寫的是**:「找到 session 相容性單測,而**它與 `#7` 情境的覆蓋對應還沒建立**。」
⇒ 🔵 **下一個人的第一步不是突變,是先找對情境與測試。**

---

## 六、`#9` —— **首次登入那條路擋得住;既有票那條路是已知缺口,而【算不算排除沒有依據】**

【量的】`app/api/sso/authorize/route.ts` 掃 `must_change` ⇒ **0 命中**
(🟢 正對照 同檔 `sso_codes|NextResponse` ⇒ **9** ⇒ 尺會咬)。
【量的】而它的骨架是:`getSession()` ⇒ **沒有 session 就退**,然後才發 code。
【量的】而登入端有測(**以原始碼斷言檢查**,不是跑 HTTP):
> 「🔴 login route:`must_change_password` ⇒ 409,且在發 session 之前、不碰 cookie」
🔵 **而那個標題本身已經舊了**(codex nit ①):實際斷言要的是**不發新票 + 清掉瀏覽器兩種 cookie**。

⇒ 🔵 **【推的】,而射程要寫清楚**:**沒有既有有效票的首次登入** ⇒ 登入不發 session ⇒ `authorize` 找不到 session ⇒ SSO 繞不過去。
🛑 **而「這一次沒有發新 session」≠「這個請求手上沒有有效 session」** ——(codex must-fix ④)
🔴 **旗標在既有 session 發出【之後】才被設的那一條路,`authorize` 不重查 ⇒ 舊票照樣換得到 code。**

🎯 **那條路那支測試自己有一格記著**:
> 「🔴 **延遲引信:今天沒有『讓單一 session 失效』的伺服器端機制 —— 有了就要回來改 409**」

🛑 **而我第一版由此判「它是取捨不是漏洞,我不開新列」—— 收回。**
📌 **那一格記的是【撤銷能力的缺口】,它證不了【這個例外已經被批准排除在 `#9` 之外】。**
⇒ ✅ **正確的措辭**:「**既有票繞過仍是已知限制;它算不算排除於 `#9`,尚需明確依據。**」
⇒ 🔵 **要誰給那個依據:不是我。端不端是主視窗的事。**

---

## 七、🔴 `#3` 另一半 —— **我找到的是【2FA 存取閘】,不是【備援唯讀閘】。兩件事。**

我第一發掃 `isPasswordOnlySession` 於 `app/` `lib/` `middleware.ts` ⇒ 只撈到 `lib/session.ts` 自己。
換成掃**判準的形狀**而不是**函式的名字**,撈到兩處:

```
那個判準在生產碼裡共【三份實作】(母體 = app/ + lib/ + middleware.ts):
  lib/session.ts:230-231   export function isPasswordOnlySession(amr)   ← 有測
  lib/auth-server.ts:59    state.require2fa && payload.amr.length === 1 && payload.amr[0] === 'pwd'
  middleware.ts:64         state.require2fa && payload.amr.length === 1 && payload.amr[0] === 'pwd'
```
🎯 **⇒ 兩處真正在執法的閘,是【手抄】的,不是呼叫那支有測的函式。**
🔵 **而這是已經寫下來的** —— `lib/session.ts:225-228` 註解逐字:「本片只收編 `login/route.ts` 那一處…`middleware.ts` / `auth-server.ts` 兩處【不在本片體積內】,規格明文『不要順手改』」。
🛑 **而沒有人記下來的是**:那支測試(`session-sub.test.mts:145`)**守的是那支函式,不是那兩處手抄** ⇒ 有人改了 `isPasswordOnlySession`,那兩處不會跟著改,**而測試不會紅**。

### 🔴🔴 而我第一版的結論是錯的 —— 那道閘**不是** `#3` 要的那道閘(codex must-fix ②)

我寫「閘在,而它掛在一顆刻意關著的開關上 ⇒ 這一條是【做了而條件未到】」。**收回。**

【量的】自驗 —— **全 repo 掃 `sub.kind` 在守門上的用法**(母體 = `app/` + `lib/` + `middleware.ts`):
```
lib/session.ts:136 / 270 / 293 / 294   形狀驗證與序列化, 不是授權閘
lib/session.ts:246                     legacy cookie 那一支(= #2 的受詞)
lib/identity.ts:399                    改密碼端點拒絕 fallback 帳號   ← 唯一一個「備援不能做某個寫入」
🔴 lib/auth-server.ts 的 requireAdmin   零個 sub.kind · 零個 method/POST/GET
🔴 middleware.ts 的守門                 零個 sub.kind · 那道 deny 不分讀寫
🟢 正對照 sub.kind 字面在測試裡          session-sub 7 次 · identity-named-login 4 次  ⇒ 尺咬得到
```
⇒ 🛑 **`requireAdmin` 只看 `require2fa` 與 `amr`** ⇒ 📌 **一顆 `{kind:'fallback'}` 而 `amr` 帶 `totp` 的 session【照樣通過】。**
⇒ 🛑 **而 middleware 那道閘不分讀寫** ⇒ 📌 **它擋的是「純密碼 session 在 2FA 開啟後不得通行」,不是「備援可讀而寫入被擋」。**

⇒ 🎯 **正確的措辭**:「**目前找到的是 2FA 存取閘;報價單側符合 `#3` 的【備援唯讀限制】,今天沒有找到。**」
🔵 **唯一命中的一格是 `lib/identity.ts:399`**(改密碼端點拒 fallback)—— **一個寫入被擋,不是「任何寫入」。**

🛑 **而【證不到】`require2fa` 今天的值** —— 我引的是板列 08-31 那格,**量於 2026-07-27,距今 45 天**。
它從 Edge Config key `auth_require_2fa` **每次 fresh 讀**(`lib/edge-config.ts:35`),fallback 是 `(lastRequire2fa ?? false) || REQUIRE_2FA_FLOOR`。
🔴 **我讀不到 Edge Config,也沒有那個 repo 的庫授權 ⇒ 我沒有去要。**
📌 **⇒ 而【就算查到那個值,也推不出 `#3` 做好了】** —— 上面那一格說的正是這件事:**閘的受詞不對。**

---

## 八、🔴 `#5` SSO code 重放 —— **搜尋範圍內找不到**

**生產碼在**:`app/api/sso/exchange/route.ts:62-72` 逐字
```
// 原子消耗: 單一 UPDATE, PK=code_hash → 至多 1 列; 併發第二次 used_at 已非 NULL → 0 列。
.update({ used_at: nowIso }).eq('code_hash', …).eq('state_hash', …)
.is('used_at', null).gt('expires_at', nowIso).select('amr, auth_time, sub_kind, sub_staff_id').maybeSingle()
```

**測試那一側 —— 三把尺**:
```
① 關鍵字尺 重放|replay|原子|consume|只能用一次|single.use|once (tests/node/)
   ⇒ 唯一命中 session-sub.test.mts:133 的對照組, 而那是 cookie 重放 ≠ code 重放(不同受詞)
② 分母尺 碰得到 sso/exchange 或 sso_codes 的【node 測試】檔        ⇒ 1 支(sso-sub.test.mts)
   🟢 正對照 提到 sso_codes 的【生產碼】檔                          ⇒ 3 支 ⇒ 尺會咬
   而那 1 支的 7 格標題我逐條讀過, 測的是 payload 形狀不是消耗
③ python 那側(我第一版漏掉的一整套) sso_codes|sso/exchange|must_change_password|
   legacyCookie|auth_require_2fa                                   ⇒ 0 命中
   檔名帶 sso|login|auth|password|session 的 python 測試             ⇒ 0 支
   🟢 正對照 python 測試總數                                        ⇒ 298 支 ⇒ 尺會咬
```
⇒ ✅ **能寫的是**:「**目前搜尋範圍內找不到 code 重放的測試;唯一直接命中的 `sso-sub` 七格沒有測這個行為。**」
🛑 **而我第一版寫「沒有一格會紅」「唯一一條完全沒守門」—— 收窄。**(codex must-fix ⑤)
📌 **這三把尺都是【字面搜尋】,不是呼叫關係或執行覆蓋** ⇒ 間接匯入、共用測試工具、動態組路徑都可能不含那些字串。
⇒ 🔵 **接手動作因此改成:先核對間接覆蓋,缺了才補。**

---

## 九、🟡 `#8` —— **有測的那一格,不是那三軸**

板上的 `#8` 逐字:**「查無帳號 vs 密碼錯:訊息、狀態碼、【回應時間】三者一致」**。

【量的】`identity-named-login.test.mts` 有一格:「🔴 密碼錯 與 帳號不存在 ⇒ 回【逐字相同】的結果(驗收格 3 = 格 4)」。
🛑 **而它比的是 `resolveNamedIdentity` 的回傳值**(同一個 `{ok:false, reason:'deny'}`)—— **不是登入 HTTP 回應的訊息與狀態碼。**(codex must-fix ⑥)

⇒ ✅ **正確的措辭**:「**身分解析層的失敗結果一致,已有單測;而 HTTP 訊息 / 狀態碼 / 時間【三軸】的路由驗收都還沒對應。**」
📌 **⇒ 我第一版寫「兩軸有測、差時序一軸」會害下一個人只補時間那一軸。**
🔵 而狀態檔記的量法設計(尺A 白盒 code-path 等價 = 收工尺 / 尺B 黑盒時序 = prod-only backstop)在
`~/pcm-mailbox/線3-8號怎麼量-20260827.md` —— **我沒有讀那份設計。**

---

## 十、⇒ B7 這一列今天的大小

| 板上寫的 | 今天 |
|---|---|
| 「4+1 條的分母在報價單 repo,**本工作樹構造不出來**」 | ✅ **對 `pcm-website-v2` 成立** —— 而它**從來不是「那邊沒有東西」** |
| 狀態檔「報價單側那半我沒有下任何結論」 | ✅ **那是誠實的,而今天有【部分】結論了** |
| 4+1 條全部 ⬜ / 🟡 | 🎯 **沒有任何一條今天可以標 pass**(見 §3 那張表)—— 而**每一條卡在哪,現在寫得出來了** |

⇒ 🛑 **我沒有寫任何測試、沒有留下任何生產碼修改、沒有改板、沒有 push。**

---

## 十一、🛑 我證不到什麼

· **【證不到】任何一條驗收通過** —— 三發突變證的是「那三個指定的錯誤,既有斷言分得出來」,**不是那幾條的驗收範圍**。
· **【證不到】`#5` 真的沒有測試** —— 三把尺都是**字面搜尋**,答不了間接覆蓋。
· **【證不到】`require2fa` 今天的值** —— 引的是 45 天前的讀數;而**就算查到,也推不出 `#3` 做好了**(§7)。
· **【證不到】有沒有人正在做 B7** —— 只讀了 `git log -1` 與未 commit **檔數**,**沒有開任何檔**;而其中一棵**有** 1 個未 commit 檔。
· **【證不到】那 82 支 node 測試整體是綠的** —— **只跑了 5 支**(56 格)。其餘 77 支與 298 支 python 測試都沒跑。
· **【證不到】逐支與合跑跑的是同一組格** —— 只核對了**總數**。
· **沒有讀 `#8` 那份量法設計檔。**
· 🛑 **本檔讀數有到期日** —— 說的是 **2026-09-10 這一刻**。

---

## 十二、🔗 接手用(2026-09-10 存檔)

· **這一列無錨** ⇒ 用內容指:`docs/launch-todo.md` 那列「**B7 端到端負向驗收 + 開關已開,只剩負向驗收**」。
  🛑 **板子不要改** —— 主視窗 2026-09-10 交代;板子今天只解凍「查證過的列尾加一行標記」。
· **可重跑的形狀**(在 `~/API大量上架/PCM報價單-V2` 底下):
  ```
  node --test --experimental-strip-types --experimental-test-module-mocks \
    --import ./tests/node/_hooks/register-alias.mjs \
    tests/node/identity-named-login.test.mts tests/node/session-sub.test.mts \
    tests/node/must-change-password-flow.test.mts tests/node/sso-sub.test.mts \
    tests/node/login-payload.test.mts
  ```
  🔵 **突變的做法**:先 `cp` 備份 + 記 `shasum -a 256`,改生產碼,**先 grep 斷言突變真的落地**,跑,還原,**核對 shasum 逐位元組相同 + `git status` 乾淨**。
· 🔴 **下一個人最該做的四件(照代價排)**:
  ① **`#3` 端一個問題出去** —— 報價單側**沒有**「備援可讀而寫入被擋」這個東西。**那是要不要做的決定,不是量測。**
  ② **`#5` 先核對間接覆蓋** —— 缺了再補「同一個 code 用第二次 ⇒ 擋」。
  ③ **`#7` 先找對情境** —— 建立「舊/新 authorize × exchange × admin」與哪支測試對應,**再**決定突變位置。
  ④ **`#2` 補登入端那一層** —— 現有三格證的是判斷函式,不是「不得設 `ADMIN_COOKIE`」。
· 🎯 **本檔自己踩過的三格**:
  ① **命名的 0** —— `*.test.ts` 量到 1,`tests/node/*.mts` 量到 82。**那份狀態檔第一段就警告過,而警告沒擋住我。**
  ② **尺壞了不是答案** —— 「它跑幾支」那把尺印 `0`,換成逐支加總才有讀數。
  ③ 🔴 **閘的受詞** —— 我找到一道閘就寫成「`#3` 的閘在」,**而它守的是 `amr` 不是 `sub.kind`**。
     📌 **「有一道閘」與「有【那一道】閘」是兩件事,而它們在 grep 結果裡長得一樣。**

**沒有持久的程式修改 · 沒 commit · 沒 push · 沒 apply · 沒 `db push` · 兩棵 worktree 沒碰。**

---
---

# 第二輪(2026-09-10 稍後)—— 主視窗放行 ②③④,而**最大的一格是一句過期的註解**

> 主視窗 2026-09-10:「`#3` 我端 Sean。②③④ 繼續做,不用等我。」
> ② `#5` 先核對間接覆蓋 · ③ `#7` 先找對情境 · ④ `#2` 補登入端那一層
> 🔵 它同時交代:**「`#5` 的措辭保持『搜尋範圍內找不到』,不要在做的過程中悄悄升格回『沒有』。」**

---

## 十三、🔴🔴 **三支檔寫著「本 repo 做不到」,而九支檔正在做**

【量的】那句自我限制的註解逐字 —— **在三支檔裡**:
```
tests/node/sso-sub.test.mts:63              「本 repo 無 route handler 執行環境測試」
tests/node/password-change.test.mts:139     「本 repo 無 route handler 執行環境測試」
tests/node/identity-named-login.test.mts:246「本 repo 無 route handler 的執行環境測試」
```
🎯 **⇒ 那三支正是 `#2` / `#5` / `#8` / `#9` 的守門所在。**

【量的】而**真的 import route 模組並執行它**的測試檔(母體 = `tests/node/` 82 支):
```
9 支 —— 含 alert-route-concurrency.test.mts, 而它 import 的正是
        app/api/admin/login/route.ts  與  app/api/admin/password/route.ts
        ⇒ 【就是那三句註解在講的那兩支 route】
⚪ 負對照 現造路徑 app/api/zzq-nope-20260910 ⇒ 0 支
```

【量的】那個 harness 的形狀(`alert-route-concurrency.test.mts:24-48`)——**完全自足,不碰真庫**:
```
假 env(NEXT_PUBLIC_SUPABASE_URL = 'https://black-hole.invalid' …)
+ 換掉 globalThis.fetch(攔 LINE / 讓 supabase 一律 throw ⇒ 每道 guard degraded)
+ 真的 new NextRequest(...) 打真的 POST(...)
+ 8 發併發
```

⇒ 🛑 **那句註解是【過期的】,而它就寫在最需要那個能力的三支檔裡。**
📌 **後果不是「少了一種測試」,是【三格自我限制,而限制的理由今天不成立】** ——
🎯 **而正因為它寫著「做不到」,沒有人回頭看。**

### 🔵 而這一格改變了三件事的成本估算

| 條 | 原本以為 | 今天量到 |
|---|---|---|
| `#2` 「登入端不得設 `ADMIN_COOKIE`」那一層 | 「本 repo 做不到」 | 🟢 **做得到** —— `alert-route-concurrency` 就在跑 `login/route.ts` |
| `#5` 「同一顆碼換第二次 ⇒ 擋」 | 「本 repo 做不到」 | 🟡 **route 那一半做得到**(見下) |
| `#8` HTTP 訊息 / 狀態碼兩軸 | 「本 repo 做不到」 | 🟢 **做得到**(同一支 route 就在被跑) |

🛑 **而 `#5` 只有一半做得到,這一格不能含糊**:
route 的保證是「`UPDATE … WHERE used_at IS NULL` 第二次回 **0 列**」。
⇒ 在 stub 掉 `fetch` 的 harness 裡,**你 stub 掉的正是要被驗的那個東西**。
⇒ ✅ **做得到的**:route 那一半 —— 「拿到 0 列 ⇒ 回 401 且不洩因」。
⇒ 🔴 **做不到的**:DB 那一半 —— 「那個 `UPDATE` 在並行下真的只有一個成功」。
🎯 **而這個限制這個 repo 自己已經記過一次**,`login-guard-account-bucket.test.mts:70` 逐字:
> 「`login_rate_hit` 這支 RPC 真的是原子的嗎 —— **那是 DB 側, 要真庫才驗得到**(既有兩個桶同樣沒驗)」

---

## 十四、② `#5` 的間接覆蓋 —— **核對了,而答案比關鍵字尺硬**

codex must-fix ⑤ 要的是:**別把「搜尋不到」升成「確定沒有」,先核對間接覆蓋。**

【量的】`sso-sub.test.mts` **有一格直接讀 `exchange/route.ts` 的原始碼**(`readFileSync` + 剝註解),而它斷言的**五件事逐條列出**:
```
✅ .select('amr, auth_time, sub_kind, sub_staff_id')
✅ subFromColumns(data.sub_kind, data.sub_staff_id)
✅ 不得出現 sub: null
✅ ...(sub ? { sub } : {})
✅ export async function POST
🔴 而【沒有一條】碰 .update({ used_at / .is('used_at', null) / .gt('expires_at'
```
⇒ 🎯 **這比關鍵字尺硬得多**:那是**唯一一支讀這支檔原始碼的測試**,而**它斷言的五件事我逐條讀過,沒有一件是消耗。**

🔵 **而同一支檔的【姊妹格】用了正好那個技術**:
`authorize` 那一格有 `assert.doesNotMatch(code, /\.update\(\{/)` —— **它明文斷言 authorize 裡【沒有】DB UPDATE。**
📌 **⇒ 同一支測試檔,對 `authorize` 用了「掃 `.update(`」這一招,而 `exchange` 的整個安全性【就是】那個 `.update(`,卻沒被掃。**

⇒ ✅ **措辭照主視窗交代的保持不動**:**「目前搜尋範圍內找不到 code 重放的測試。」**
🛑 **仍然【證不到】不存在** —— 字面搜尋 + 一支檔的逐條斷言,答不了「有沒有人用完全不同的方式間接蓋到」。

---

## 十五、③ `#7` 的情境 —— **找對了,而 `session-sub` 不在裡面**

【量的】B4 spec `docs/specs/2026-08-18-m4b-e8b-b4-spec.md` §7 的混版格,**共五格**:
```
格 7   舊 authorize(不寫兩欄)× 新 exchange   ⇒ 不帶 sub, 不得 500 / 401
格 7b  接續 7:那顆回應送到 admin 之後【最終發出的 session 是什麼】
       🔴 必須是【沒有身分】, 不得被轉成 fallback
       📌 fallback 是一種身分, NULL 是沒有身分。把後者讀成前者 = 憑空給了一個身分
格 7c  🔴 部署交界的【飛行碼】—— 舊 authorize 發的碼(60 秒有效)在部署後被新 exchange 兌換
       📌 每一次部署都會製造這個窗口, 只是很短
格 8   新 authorize × 舊 exchange              ⇒ 只有「不得 500/401」那半算數
格 8b  新 authorize × 【舊 admin 的真實 callback】—— admin 最終那顆 cookie
```
🔴 **而 spec 自己記著四象限缺一格**:`quote 舊 × admin 舊` ⇒ **本表沒有它,而那是「今天正在跑的那一格」。**

⇒ 🛑 **`session-sub.test.mts` 驗的是【報價單 session 自己的版本 / 形狀 / 簽章】** ——
**它不在上面五格的任何一格裡。** ⇒ ✅ **codex must-fix ③ 成立,我的對應是錯的。**

⇒ 🔴 **而 `#7` 的真實成本比我以為的大**:五格裡有三格(7b / 7c / 8b)要**跨到 admin 那一側看最終那顆 cookie**,
而 spec `:311` 另外要求「**第 7/8 格的『舊』是哪一份 —— 釘成可取回的 artifact**」⇒ **要兩個 repo × 兩個版本。**
📌 **⇒ 這一條不是「補一發突變」,是一件工。**

### 🔴 而我在這一格差點第六次踩同一個坑 —— **這次在報出去之前擋住了**

spec 那張表最右欄有很多 **✅ 有**,而 **格 9「同一顆碼換第二次 ⇒ 401」也標著 ✅ 有**。
⇒ 🛑 **我當下的直覺是「spec 說有,而我量到零 ⇒ 矛盾,要報事故」。**

【量的】**去看欄名再說**:表頭逐字是 **「今天有判別力嗎」**,而表的標題是 **「§7 驗收(負向為主;每一格都要能紅)」**。
⇒ 📌 **那一欄答的是【這一格跑起來會不會恆綠】,不是【這一格做了沒】。**
⇒ ✅ **沒有矛盾。而我原本要報的那個「事故」不存在。**

🎯 **⇒ 今天第六次「一個 ✅ 回答的是另一個問題」,而這一次我在報出去之前先去讀了欄名。**
📌 **一張驗收清單與一張進度表,長得一模一樣。**

---

## 十六、④ `#2` 缺的那一層 —— **做得到,而它缺的是【誰去做】**

codex must-fix ① 指的是:現有三格**直接呼叫 `legacyCookieAllowed`**,沒走登入端發 cookie
⇒ 呼叫端若繞過那支函式直接設 `ADMIN_COOKIE`,三格照樣全綠。

【量的】而 `identity-named-login.test.mts` **本來就 import `app/api/admin/login/route.ts`**,
且 `alert-route-concurrency.test.mts` **已經在對同一支 route 發真的 `POST`**。
⇒ ✅ **「不得設 `ADMIN_COOKIE`」這一層,這個 repo 今天做得到。**
🛑 **而我沒有寫它** —— 那是在**別人的 repo、`main` = production 分支**上新增測試檔,**要授權,不是我自己決定。**

---

## 十七、⇒ 第二輪之後,那 4+1 條的形狀

| 條 | 卡在哪 | 大小 |
|---|---|---|
| `#2` | 缺登入端 cookie 那一層 | 🟢 **小** —— harness 在,同一支 route 已經在被跑 |
| `#5` | route 那半沒測;DB 原子性那半**要真庫** | 🟡 **一半小一半要授權** |
| `#8` | 三軸的路由驗收都沒對應 | 🟢 **小**(同上 harness) |
| `#7` | 五格中三格要跨 admin;還要釘「舊版」artifact | 🔴 **大 —— 兩個 repo × 兩個版本** |
| `#3` | **報價單側沒有備援唯讀閘這個東西** | 🔴 **不是工,是決定** ⇒ 主視窗端 Sean |

🛑 **而【證不到】的三件仍然成立**:`#5` 不能寫成「沒有」· `#7` 五格一格都沒跑過 · 那 82 支我只跑了 5 支。

---

## 十八、🔗 接手用 · 第二輪補充

· 🔵 **要寫 route 層測試的人,抄這一支**:`tests/node/alert-route-concurrency.test.mts:24-48`(假 env + 換 `globalThis.fetch` + 真 `NextRequest`)。
  🛑 **而先把那三句「本 repo 無 route handler 執行環境測試」的註解改掉** —— 否則下一個人跟我一樣被它擋住。
· 🔴 **`#5` 的 DB 那一半要真庫** ⇒ 這個 repo 已經記過同型限制(`login-guard-account-bucket.test.mts:70`)。**別假裝 stub 得掉。**
· 🎯 **本檔自己踩過的第四格(第二輪新增)**:
  ④ 🔴 **一張驗收清單與一張進度表,長得一模一樣。** spec 那欄的 ✅ 是「**今天有判別力嗎**」,不是「**做了沒**」。
     **我在報出去之前先讀了欄名 —— 那是今天六次裡唯一一次擋在前面的。**

**沒有持久的程式修改 · 沒 commit 到那個 repo · 沒 push · 沒 apply · 沒 `db push` · 兩棵 worktree 沒碰。**

---

## 十九、🔵 `#3` —— **Sean 2026-09-10 拍乙:不做**

逐字理由:**「乙 = 不要 —— 緊急的時候本來就是要進去修東西的」**

```
#3(備援登入進得去而【任何寫入被擋】)⇒ 🔵 【不做】
· 🛑 那道閘今天【不存在】—— 而那是【刻意的】, 不是缺口
· 🔴 而它仍然是一把繞過正常檢查的鑰匙 —— 這一句要留著,
     哪天 Sean 改變主意, 下一個人要看得出來當初換掉的是什麼
```
🎯 **⇒ 不要寫成「`#3` 通過」。那條驗收要的東西今天仍然不存在,變的是【我們不打算做它了】。**
📌 **「沒做」與「決定不做」在檔案裡長得一樣,而它們的下一步完全不同。**

🔵 **今天量到的相關事實留著**(給哪天要改主意的人):
· `requireAdmin`(`lib/auth-server.ts:49`)零個 `sub.kind`、零個 `method` ⇒ **不分讀寫、不分身分**。
· `middleware.ts:64` 那道 `deny` 同樣不分讀寫。
· 唯一一個「備援不能做某個寫入」是 `lib/identity.ts:399`(改密碼端點拒 fallback 帳號)。

---

## 二十、🔴 `#5` 的【反面】檢查 —— 主視窗要我先問一次,而答案有兩層

> 主視窗 2026-09-10 逐字:「`#3` 這一發你是**被 codex 打掉結論**才發現『找到的是 2FA 閘、要的是備援唯讀閘』。
> ⇒ 📌 **剩下四條裡,`#5` 最有可能是同一個形狀的反面** —— 你說『搜尋範圍內找不到守門』,
> 而**萬一它守在一個你沒想到的名字底下**,那就是『有【那一道】閘而 grep 看不到』。」

**⇒ 我去問了。而結果是:【沒有藏起來的測試】,但【那道閘比我原本寫的強】。**

### 【量的】① 那道閘其實是**兩層**,不是一層 —— 而第二層在 schema 裡

我原本只寫 route 那一行。實際上:
```
第一層 · schema      supabase/migrations/20260730000000_baseline_schema.sql:5728 逐字
                     ADD CONSTRAINT sso_codes_pkey PRIMARY KEY (code_hash)
                     ⇒ 同一顆 code 至多【一列】—— 這是「至多消耗一次」的地基
第二層 · route       app/api/sso/exchange/route.ts:66-70
                     .update({ used_at }).is('used_at', null).gt('expires_at', now)
                     ⇒ 併發第二發看到已 commit 的 used_at ⇒ 匹配 0 列 ⇒ unauthorized()
⚪ 負對照 現造表名在同一支 baseline ⇒ 0
```
🔵 **而設計意圖在 migration 註解裡逐字寫著**(`migrations_archive/20260712c_sso_codes.sql:10`):
> 「單次消耗靠 `used_at IS NULL`: exchange 用單一 `UPDATE … WHERE used_at IS NULL`」

🔵 **縱深也在**:`baseline:7031` `ENABLE ROW LEVEL SECURITY` + `:8689` 只 `GRANT ALL … TO service_role`(RLS 零 policy ⇒ default-deny)。

### 【量的】② 而【沒有】藏在別的名字底下的測試

```
migrations / supabase 那側  掃 sso_codes ⇒ 4 支檔, 逐支開檔核 ⇒ 全是【定義】不是【驗收】
tests/integration/          僅 test_reparse_concurrency.py(受詞無關)
全 repo 掃 used_at IS NULL|換第二次|第二次兌換|reuse|re-use
                            ⇒ 命中全在 fitment / translation / docs/archive ⇒ 假陽性, 逐條看過
⚪ 負對照 現造字串           ⇒ 0
```

⇒ ✅ **反面檢查的結論**:**沒有「有【那一道】閘而 grep 看不到」的情況。**
⇒ 🎯 **而我原本的描述【偏弱】,不是偏強** —— 我只寫了 route 那一層,漏了 PK 那一層。
📌 **⇒ 這次的偏差方向與 `#3` 相反**:`#3` 是**我把別的閘當成那道閘**;這次是**我把兩層寫成一層**。

⇒ ✅ **措辭仍然照主視窗交代不升格**:**「目前搜尋範圍內找不到 code 重放的測試。」**
🔵 **而可以加一句站得住的**:**那道閘的機制有兩層,兩層今天都在,而【兩層都沒有測試在守】。**

### 🔵 順帶一格(不是本列的,不展開)
【量的】baseline 的 `CREATE TABLE public.sso_codes` **不含** `sub_kind` / `sub_staff_id` 兩欄
—— 它們是 2026-08-21 **out-of-tree** 貼上去的(`supabase/APPLIED-OUT-OF-TREE.tsv:30`,含 `CHECK sso_codes_sub_shape`)。
✅ **那是有落檔的,不是漏記。** 🛑 **而它表示 baseline 不是那張表今天的全貌** —— 引用 baseline 的人要知道。**本份不追。**

---

## 二十一、⇒ 收工時那 4+1 條的狀態

```
#3  🔵 Sean 2026-09-10 拍乙【不做】——「緊急的時候本來就是要進去修東西的」
    🛑 不是「通過」。那道閘今天不存在, 而它是一把繞過正常檢查的鑰匙。
#2  🟡 斷言分得出兩個指定的錯(M1/M2)· 而登入端「不得設 ADMIN_COOKIE」那一層沒建立
    🟢 而 harness 在 ⇒ 小
#7  🟡 斷言分得出一個指定的錯(M3)· 而它與五格混版情境的對應【根本沒建立】
    🔴 五格中三格要跨到 admin + 要釘「舊版」artifact ⇒ 兩個 repo × 兩個版本 ⇒ 大
#5  🔴 機制兩層都在, 而兩層都沒有測試 · 反面檢查做過, 沒有藏起來的閘
    🟡 route 那半這個 repo 做得到;DB 原子性那半要真庫
#8  🟡 三軸的路由驗收都沒對應(我原本寫「兩軸有測」是錯的)
    🟢 harness 在 ⇒ 小
#9  🟡 首次登入那條路擋得住;既有票那條路是已知缺口, 算不算排除【尚需依據】
```
🛑 **沒有任何一條今天可以標 pass。而每一條卡在哪、多大,現在寫得出來了。**

**沒有持久的程式修改 · 沒 commit 到那個 repo · 沒 push · 沒 apply · 沒 `db push` · 兩棵 worktree 沒碰。**

---

## 二十二、🔴 codex 第二輪(無 must-fix · 5 nit)—— 而其中兩個是我的事實錯誤,一個是我明知故犯

### ① 🔴 **「9 支」是錯的,是 8 支;而 `identity-named-login` 【不 import】那支 route**

我在 §13 / §16 寫「9 支測試檔真的 import route.ts 並執行它」與「`identity-named-login` 本來就 import `login/route.ts`」。**兩句都錯。**

【量的】自驗:
```
真的 import 並執行 route handler ⇒ 8 支
  alert-route-concurrency · quote-cleanup-image-failure · pricing-preview-wiring
  audit-converge-supplier-scope · audit-accept-{entry-decisions-wiring,not-pending,idempotent}
  audit-ops-switch-wiring
🔴 我多算的那一支 dictionary-parse-aliases:
   :15 import { parseAliases } from '../../app/api/dictionary/_parse'   ← 輔助模組, 不是 route handler
只讀原始碼【不執行】的 ⇒ 7 支, 而 identity-named-login 與 sso-sub / password-change 都在這一堆
```
🎯 **⇒ 我的尺數的是「檔裡有沒有 `app/api` 這個字」,而不是「有沒有執行一支 route handler」。**
📌 **`app/api` 這個路徑出現在 import 裡,不代表 import 的是 route。** —— **今天這一族的第三種變形。**

✅ **而結論不變**:`alert-route-concurrency.test.mts:50-51` 逐字 `await import('../../app/api/admin/login/route.ts')` 並呼叫它
⇒ **那個能力確實存在,而那三句「本 repo 做不到」仍然是過期的。**
🔵 **改法**:「`identity-named-login` **讀 route 的原始碼**;而 `alert-route-concurrency` **才是真的執行 `login` 的 `POST`**。」

### ② 🔴 **我又把「搜尋範圍內找不到」升格成「沒有」—— 而主視窗才剛提醒過我這一格**

主視窗 2026-09-10 逐字:「**保持那個措辭,不要在做的過程中悄悄升格回『沒有』。**」
🛑 **而我在 §20 寫了「沒有藏起來的測試」與「兩層都沒有測試在守」。同一節裡還留著「搜尋範圍內找不到」⇒ 自相矛盾。**
📌 **⇒ 一個被明文提醒過的錯,在同一次工作裡照樣犯。這一格不辯解。**
✅ **統一改成**:「**所讀的 baseline 與 route 各有該機制;而在本次搜尋與逐檔核對的範圍內,沒有找到直接驗證它們的測試。**」
🔵 codex 另外指出:**baseline 有 PK、route 有條件,只證明【我讀到的那一版】,沒有驗證正式庫今天的狀態。** 收下。

### ③ 🔵 並行那一格要限定隔離級別(codex nit ④,技術補正)

我寫「併發第二發看到已 commit 的 `used_at` ⇒ 匹配 0 列」。
✅ **在 Read Committed 下成立**(第二筆等第一筆提交、再對新版重判 `WHERE`)。
🔵 **而較高隔離級別可能改成序列化失敗而不是零列** —— route 的 `if (error || !data)` 兩種都回 401,所以結論不變。
🔴 **另一格**:`expires_at` 比的是 `route:63` **應用端預先取得的時間**,等待期間**不會重取現在時間**
⇒ 📌 **它不保證「提交當下仍未過期」。**(而這不會造成同碼成功兩次。)
⇒ ✅ 正確的說法:**保證的是「同一列生命週期內、未被其他寫入重設之下,至多一次成功」。**

### ④ 🟡 `#8` 標「小」超出 harness 撐得住的範圍(codex nit ②)

改成:**「訊息 / 狀態碼兩軸可以沿用那個 harness;而【時間軸】另外需要量法與實際環境,成本尚未確認。」**
📌 理由:**替身把真 Auth 對「查無帳號 / 密碼錯」的時間差【拿掉了】** —— 那正是時間軸要量的東西。

### ⑤ 🔵 `#3` 的「不做」要限定在報價單側(codex nit ⑤)

B6+B7 spec `:77` 的 `#3` **含 admin 與報價單兩邊**,而我只寫「`#3` 不做」會被讀成整條取消。
✅ **改成**:「**`#3` 的【報價單側備援唯讀要求】:依 Sean 2026-09-10 決策不實作,不計通過;admin 側另列狀態。**」

### 🔵 而 codex 這一輪自己跑了東西,記下來(它的讀數,不是我的)
> 「已重跑既有 alert 測試:**1 個通過**;另以不落檔的記憶體探測確認 **login 的兩種拒絕皆為相同 401**、
> **簽章失敗為 500 且無 cookie**、以及 **exchange 的一列 / 零列分別為 200 / 401**。
> 未測真庫並行、正式 HTTP 環境或時間軸。兩個工作樹均維持乾淨,沒有修改任何檔案。」

🎯 **⇒ 那是【第三方的獨立讀數】,而它與我 §9 的判斷方向一致(訊息/狀態碼那兩軸的行為是對的)** ——
🛑 **而我仍然【沒有】把 `#8` 標成過** :它證的是那兩軸,而那一條要三軸。

---

## 二十三、🔴🔴 第四輪:**我寫了測試、突變過、它會紅 —— 而它擋不住真的壞法**

> 主視窗 2026-09-10 放行:在報價單 repo 開自己的分支,補 `#2` / `#5` / `#8`。
> 我照那個 repo 自己的 SOP 開了 `git worktree add ~/quote-wt-c-b7 -b e8b-c-b7-negverify main`,
> 寫了 `tests/node/b7-negative-acceptance.test.mts`(4 格),做了 4 發突變,**每一發只紅它自己那一格**。
> **⇒ 我據此判它有判別力,並且這樣回報了。**
>
> 🛑 **codex 唯讀審回來:4 個 must-fix。而它不是講理論 —— 它做出了繞過,而我的四格【全綠】。**

### 一、【量的】我自己逐發複現,三個全部成立

```
繞過 A  .update({ used_at: nowIso })  →  .update({ used_at: null })
        (= 碼永遠標不成已用 = 無限次重放)                      ⇒ 4 pass · 0 fail
繞過 B  把 .is('used_at', null) 包進 /* … */ 區塊註解
        (= 那個條件不再執行)                                    ⇒ 4 pass · 0 fail
        🔴 我的 stripComments 只剝 `//`, 不剝 `/* */`
           —— 而那支 helper 是我【逐字抄既有測試的】(sso-sub.test.mts:7)
        📌 ⇒ 那個洞不是我發明的, 是我【連同它的盲點一起抄過來的】
繞過 C  .gt('expires_at', nowIso)  →  .gt('expires_at', '1970-01-01T00:00:00Z')
        (= 過期的碼照樣換得到)                                  ⇒ 4 pass · 0 fail
每一發都先跑前置斷言(確認突變真的落地)· 跑完還原 · sha256 逐位元組相同
```

**codex 另外做出而我沒複現的**(它自己標明是記憶體內改寫、未落檔):
· 把 `.from('sso_codes')` 存成變數,再拆成 `select` 一發 + `update` 一發 —— **我的正則跨過分號,把兩發拼成「同一串」**
· helper 內改成 `''`,再到閘外設非空 cookie · `cookies.set({ name: ADMIN_COOKIE, value })` **物件式寫法完全不計入**
· 在原失敗出口**之前**插一個條件式 `404` / 「查無帳號」,保留原本的 `401` ⇒ **我的 `{401}` 集合照樣成立**

### 二、🎯 成因只有一句 —— **而我今天早上才寫過那句話**

📌 **我的四發突變,打的正是我自己寫斷言的那四個地方。**

而 `docs/evidence/2026-09-10-auditgrantexpiry-判準三第一次真的被跑.md` 不是這一句的出處 ——
出處是**同一天**的 B7 狀態檔 §③(我今天讀過並引用過)逐字:
> 🛑 **而它【不證】那句舊話**:83 格裡只有 **3 格**對突變②有反應 ⇒
> **「83 格都在守對的東西」仍然沒有被驗**…
> 📌 **⇒ 一發突變只證得了【它打到的那一格】** —— 要證 83 格各有判別力,得有 83 發。

🛑 **我讀了它、引用了它、然後在自己的產出上沒有套用它。**
🎯 **⇒ 「四發突變都紅」與「這四格有判別力」,在我這一端長得一模一樣。**
📌 **⇒ 這是今晚那一族的第五種變形,而這一次的兩件東西【是我自己造的】。**

### 三、🔴 而 codex 的第一條打掉的是【前提】,不是正則

我在檔頭寫:「那三種壞法用行為測試**抓不到**,所以用字面斷言。」
🛑 **錯的。**
· `#5` 拿掉消耗條件**就在 exchange 那條既有路徑上** ⇒ 行為測試走得到。
· `#8` **本來就該**構造「不存在的帳號 / 存在但密碼錯」兩種請求,比 HTTP 回應。
· `#2` 也構造得出具名登入的設定缺失、簽章失敗那些路徑。
· 而 **`alert-route-concurrency.test.mts` 已經示範過怎麼跑。**

📌 **⇒ 我用一個錯的前提,替一個比較弱的做法背書。**
🎯 **而那個前提【看起來像一個技術判斷】,實際上是我沒去試。**

### 四、🔴 順帶收回我上一則報給主視窗的一句

我報過:「兩格紅了,而**兩格都是先查出哪一種錯,再重寫斷言**,沒有改期望值過關。」
· `#2` 那格 ✅ **成立**(`\s*` 回溯繞過負向前瞻 —— codex 複現過,確認正確)。
· 🔴 `#8` 那格 **不成立**:我寫「`:213` 是備援登入那條路,`:276` 是具名」——
  **codex 查證:`:213` 也在 `namedLogin` 裡,處理空密碼 / 超長密碼。兩處都是具名登入。**
  ⇒ 🛑 **我診斷錯了,然後照著那個錯的診斷把測試改綠了。**
📌 **⇒ 「先查出哪一種錯再重寫」這個動作做了,而【查出來的那個原因是錯的】** —— 動作對,結論錯,而測試變綠了。

### 五、⇒ 決定:**沒有 commit**,而理由不是保守

📌 **一支綠的測試,宣稱它守著它其實守不住的東西 —— 那正是 B7 這一列存在要抓的東西。**
🛑 **送進去比沒有更糟。**

### 六、⇒ 而這是【換路】不是【第三輪重試】

```
第一輪  我自己紅兩格          ⇒ 正則太鬆
第二輪  codex 證明正則可繞
🎯 同一個錯法兩次:【想用靜態文字證明一個執行期的性質】
⇒ ~/.claude/rules/00-work-rules.md §5「相同錯法第 2 次 ⇒ 換路不重試」
```
✅ **換的路** = **攔 `globalThis.fetch`,斷言【真的送出去的那一發請求】**。
PostgREST 的 URL 會帶 `used_at=is.null` 與 `expires_at=gt.…`,body 會帶 `used_at` 的值。
🎯 **上面三個繞過【全部】會被這把尺抓到** —— 因為它看的是**送出去的東西**,不是**檔案裡的字**。
🔵 harness 現成(`alert-route-concurrency.test.mts:24-48`),不碰真庫。
🛑 **而交付物的形狀變了 ⇒ 我把選項端給主視窗,在回覆之前不寫碼。**

### 七、🔵 而這一輪【留得下來的東西】不是那支測試,是這份繞過清單

**那三個(加 codex 的四個)繞過,每一個都是一格現成的驗收案例。**
📌 **下一個人寫行為測試時,拿它們當輸入 —— 而它們是【已經被證明會讓靜態尺失效】的那一批。**

### 八、🛑 我證不到什麼(這一輪)

· **【證不到】codex 那四個我沒複現的繞過** —— 我只複現了三個。**其餘四個我照抄它的敘述,標明出處。**
· **【證不到】換成攔 `fetch` 之後就守得住** —— **那是我的預期,還沒寫、還沒被人繞過。**
· **【證不到】既有的 `sso-sub.test.mts` 有沒有同一個區塊註解盲點** —— 🔴 **它的 `stripComments` 與我抄的那支【是同一段】**
  ⇒ 📌 **推的:它應該有同一個洞。而我沒有去試,不寫成有。**
· 本輪**沒有** commit 任何東西到報價單 repo;三發複現全部還原,`sha256` 逐位元組相同,兩棵樹乾淨。

---

## 二十四、🔴🔴 第五輪:**換了路,還是被打穿 —— 而五個繞過是【同一個成因】**

> 第四輪之後我判「同一錯法第二次 ⇒ 換路」,把 `#5` 從字面斷言改成**行為測試**
> (`tests/node/b7-sso-code-consumption.test.mts`:攔 `globalThis.fetch`、把 URL 解析成 `searchParams` 判、
> body `JSON.parse` 後判鍵值、並驗「同一顆碼第二次兌換 ⇒ 401」)。
>
> ✅ **而它確實把上一輪那三發全部擋下了**(`used_at: null` / 區塊註解 / `expires_at` 改 1970),
> 無害重構 `.limit(1).maybeSingle()` 也不假紅。**我據此回報「換路成功」。**
>
> 🛑 **codex 第三輪:5 個 must-fix,又是實際做出繞過。**

### 一、【量的】五個繞過(codex 實測,我未複現 —— 標明出處)

```
① PostgREST 的 columns= 參數
   .update({ used_at: nowIso, amr: ['pwd'] }) + 請求帶 columns=amr
   ⇒ PostgREST 【忽略 body 裡沒列進 columns 的欄】⇒ used_at 保持 NULL
   ⇒ 同一顆碼下次照樣匹配得到, 而【我三格全綠】

② 我的假零列演錯了 —— 而這一格最嚴重
   我用 406 + {code:'PGRST116'} 演零列。而 supabase-js 2.112.3 之下拿到的是
        data: null  ·  error: { code: 'PGRST116' }        ← 【不是 error: null】
   ⇒ 📌 我以為在驗【正常零列】, 實際上在驗【DB 錯誤】那條路。
   ⇒ 把 route 的 `if (error || !data)` 改成 `if (error)`(= 拆掉一道真的防線)⇒ 我三格全綠
   🔴 而真正的正常零列是 200 + [] —— codex 換成那個形狀之後, 拆掉 !data 的版本【才】紅

③ .select('amr, auth_time')(漏選身分那兩欄)⇒ 我三格全綠
   成因:【我的 fixture 不理會 select, 永遠回完整那一列】
   ⇒ 我在檔裡寫「本支用行為再守一次那個 select」—— 那句是假的

④ .gt('expires_at', new Date(Date.now() - 30_000).toISOString()) ⇒ 我三格全綠
   我的容差寫 60 秒 ⇒ 它放過【剛過期 30 秒】的碼

⑤ (舊那支 b7-negative-acceptance)
   void '/*';  res.cookies.set(ADMIN_COOKIE, expected, { path: '/' });  void '*/';
   ⇒ 兩格全綠 —— 我補的區塊註解正則【把真的會執行的碼刪掉了】
   🔵 反過來也成立:在正常 helper 前後加那兩句無副作用的字串, 會讓正常程式【假紅】
```
🔵 **codex 自報**:全程未改檔,兩支受審檔 `sha256` 前後一致;上述突變都實際執行過。

### 二、🎯 而 ①②③ 是同一句話

📌 **【我的假 client 不是 PostgREST】。**
`columns=` 的語意 · 零列的回應形狀 · `select` 的投影 —— **三件都是 PostgREST 的行為,而我把它們 stub 掉了。**

🎯 **⇒ 一個我自己演的替身,證不了它替的那個東西。**
🛑 **而我三格綠燈,全部建立在那個替身身上。**

### 三、🛑 而我判:**停,不做第三次修補**

```
第一輪  靜態文字             ⇒ 被繞(codex 第二輪)
第二輪  行為 + 假 fetch      ⇒ 被繞(codex 第三輪)
🎯 而這兩輪【不是同一個錯法】——
   第二輪的每一個繞過, 成因都是「我的替身跟真的不一樣」
⇒ 📌 而【再修一次替身】只會產生第三種「替身跟真的不一樣」
```
📌 `~/.claude/rules/00-work-rules.md §5`:**相同錯法第 2 次 ⇒ 換路不重試。**
🔴 **⇒ 我不會再交一支「這次應該守住了」的測試。三次了。**

### 四、⇒ 真正的那條路,codex 兩輪都指同一個地方

> 「那部分需要**實際 PostgreSQL**,**可用拋棄式測試庫,無須正式庫**。」

🔵 **而我們有那份 runbook**:`docs/runbooks/throwaway-postgres-for-migration-verification.md` §1
(🔴 **必須整段貼,不要手打** —— 那份檔自己記著為什麼)。
🎯 **⇒ 拿真的 PG 去跑那一發 `UPDATE … WHERE used_at IS NULL`,①②③ 全部自動消失** —— 那時候沒有替身。
⚠️ **而它只驗得到 DB 那一半**;route 那一半仍要替身,**而替身仍會有第三種不一樣**。

### 五、🔵 而這一輪【留得下來的】

· ✅ **那三格是真的守得住的** —— codex 自己確認:「一般經 fetch 的先讀再寫,以及作者原先三種突變,**都會紅**」。
· ✅ **五個繞過本身,每一個都是一格現成的驗收案例**(加上第四輪那七個,共十二個)。
· 🔴 **而兩支測試都【沒有 commit】。**

### 六、🎯 今晚最該記的一句,我用這一輪換到

> 🔴 **一個替身跟真的不一樣的地方,永遠比你想到的多一個。**
> 🔴 **而每一次「這次應該守住了」,都是在賭你已經想完了。**

📌 **而它與今晚那一族是同一件事的最後一面**:
```
· 一句擋人的話, 在障礙消失之後仍然擋著                       (窗A)
· 「有一道閘」與「有【那一道】閘」在 grep 結果裡長得一樣      (我, #3)
· 一張驗收清單與一張進度表, 長得一模一樣                      (我, spec 那一欄)
· 一個標記, 標的是它自己所在的那一棵以外的東西                (WORKTREE-OWNER.md)
· 「四發突變都紅」與「這四格有判別力」長得一模一樣            (我, 第四輪)
· 🔴 一個替身的綠燈, 與那個東西真的是對的, 長得一模一樣        (我, 本輪)
```

### 七、🛑 我證不到什麼(本輪)

· **【證不到】那五個繞過** —— **我一個都沒複現**(前一輪我複現了三個;這一輪 codex 報完我就停了)。**逐條照抄它的敘述並標明出處。**
· **【證不到】改成拋棄式 PG 之後就守得住** —— 那是預期,**還沒做**。
· **【證不到】`pricing-apply-discipline.test.mts:57` 那份 `stripComments` 有沒有繞過 ⑤ 的問題**
  ——【推的】它用的是同一族正則 ⇒ **應該有**。而**我沒去試,不寫成有。**
  🔵 而我原本要把它抄過來取代我自己那份 —— 🛑 **繞過 ⑤ 之後,那個動作要重新想:抄一份【也有洞】的過來,只是換一個洞。**

**沒有 commit 任何東西到報價單 repo · 沒 push · 沒 apply · 沒 `db push` · 三棵樹都乾淨。**
