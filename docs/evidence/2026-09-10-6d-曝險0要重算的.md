# 「曝險 0」哪幾處要重算 · 2026-09-10(唯讀;板檔零改動)

> 🛑 **照交代:我【只指出來,不重算】。** 這是一份名單,不是一組新數字。
> **真實分母(來源 = 主視窗 `-59` 2026-09-10 唯讀正式庫補量,我沒有複量)**:
> `訂單 8 ⇒ 真實 5`(3 張是 `g3-sandbox-test` 的:`CH6D75` 新竹首箱連線測試 · `P7KZ55` / `MCGHHM` 稅驗證勿出貨)·
> `客戶 15 ⇒ 真實 12` · `箱子:唯一走過新竹那一箱是測試單的`。

---

## 🎯 先講一件比「重算」更嚴重的:**兩處把測試單當成【正面證據】**

**這兩處不是「0 被算髒了」,是【它們拿測試單去證明一件事是好的】。**

| 行 | 錨 | 板上逐字 | 🔴 問題 |
|---|---|---|---|
| `:1017` | `⟦b4-TAXSURFACES⟧` | 「①「稅會不會真的從 DB 走到畫面」在**後台手動單**那條路 2026-09-09 於正式庫**真單**上肉眼驗過(勾發票 **MCGHHM** 印小計 1,000/稅 50/總 1,050;不勾 **P7KZ55** 連「稅額」那一列都不印)」 | 🔴 **`MCGHHM` 與 `P7KZ55` 就是那兩張【稅驗證勿出貨】的測試單。** ⇒ 📌 **「真單」這兩個字撐不住** —— 它們是正式庫裡的真列,而**不是客人的單**。驗的是我們自己造的形狀,而客人的單長什麼樣沒有人看過(而同一格自己也寫著「**客人結帳那條路仍未驗**」)。 |
| `:2361` | `⟦ship-HCTLABELCAPTURE⟧` | 章逐字「本列的等待逐字是『等第一箱**真的**送出去』,而**那今天發生了**:唯讀正式庫 2026-09-10 量到 1 箱有回應,而那張標籤圖真的被存下來了(`hct_raw_response->0->>'image'` 長 19,090 字元)」 | 🔴 **那一箱是 `CH6D75`(新竹首箱【連線測試】)的箱子。** ⇒ 📌 **「標籤圖存得下來」這件事是真的、成立的;而「第一箱真的送出去」不是** —— 送出去的是我們自己的測試單。**兩件事被寫成同一句。** |

🎯 **⇒ 這兩處的共同形狀**:**一個用測試資料證出來的「會動」,讀起來跟「客人走得通」一模一樣。**
🔴 **而板上今天【沒有任何一列】記著那三張是測試單** —— 掃全板:`勿出貨` ⇒ **0 命中** · `連線測試` ⇒ **0 命中**;三個單號只有 `:1017` 提到兩個,**而它是拿它們當正面證據的那一列**(⚪ 負對照 現造單號 `ZZQ999` ⇒ 0)。

---

## 🔴 §一 · 分母含測試資料 ⇒ 那個 0 要重算(7 處)

| 行 | 錨 | 板上那個 0 | 為什麼要重算 |
|---|---|---|---|
| `:523` | `⟦b4-NETRECEIVEDLEFTOVERS⟧` | 「曝險 0 張 —— ①【實量】已取消且淨額為 0(付過款而全退)的單 **0 張**;**母數:全庫訂單 7 張 · 已取消 3 張 · 有效退款 1 筆**」 | 🔴 **母數逐字寫出來了,而它是髒的**:7 張裡有 3 張是測試單 ⇒ **真實母數 4 張**。而「已取消 3 張」是不是就是那 3 張測試單,**板上答不出來** ⇒ 📌 **若是,那個 0 的真實分母是【已取消的真實單 0 張】—— 一個分母 0 的 0。** |
| `:2359` | `⟦ship-HCTUNKNOWNREAD⟧` | 「曝險 0 箱(**0 箱走過這條路**;板上寫的兩件今天都還對)」 | 🔴 全庫只有 1 箱走過新竹,**而那 1 箱是測試單的** ⇒ **客人的箱子走過這條路 = 0** ⇒ 📌 **那個 0 不是「沒問題」,是【沒有資料】。** |
| `:2362` | `⟦ship-UNKNOWNREASONLOST⟧` | 「曝險 0 箱 —— **唯一走過新竹那條路的那一箱** `success=Y`,沒有進 unknown」 | 🔴 同上,**那「唯一一箱」是測試單的** ⇒ 真實分母 **0**。🔵 而這一列自己已經很小心(它記了自己第一次量錯 `hct_raw_response` 是陣列)—— **而它不知道那一箱是誰的。** |
| `:2478` | `⟦b4-CANCELMAILCORRECTED⟧` | 「曝險 **0 列**…🛑 而板上那句『今天的暴露是 0』寫的是『全站訂單 2 張』,**而今天是 8 張** ⇒ 分母變了四倍而結論沒變」 | 🟡 **這一列已經自己抓到「分母會變」了 ✅** —— 而它把新分母寫成 **8 張**。⇒ 🔴 **8 也是髒的,真實 5。** 📌 **它做對了一半:知道要追分母,而不知道分母裡有測試單。** |
| `:1143` | `⟦b4-PARTCANCEL1⟧` | 「曝險 **0 次(而那個 0 有兩種讀法,我分不出)**…`order.cancel`(**3 次**…)」 | 🔴 那 3 次 `order.cancel` 有幾次是測試單的,**沒有人分過** ⇒ 📌 **它自己標了「兩種讀法我分不出」——而【測試單】是它沒有想到的第三種。** |
| `:1144` | `⟦b4-PARTCANCELTAX⟧` | 「仍成立(結構成立,**而今天曝險 0**)」 | 🔴 那個 0 的分母是訂單數 ⇒ 髒。而本列講的是**未稅單(後台手動單)**,而 **`P7KZ55`/`MCGHHM` 正好就是後台手動單** ⇒ 📌 **這一列的受詞與那兩張測試單【重疊】,而它的 0 沒有把它們分出來。** |
| `:1624` | `⟦b4-RESEND409⟧` | 「🔵 背景數:**信件總數 7** · 死信 0」 | 🔴 那 7 封信裡有幾封是那 3 張測試單產生的,**沒有人扣過** ⇒ **真實信件數 < 7**。🔵 而這一列自己已經標了「曝險量不到:猜錯了欄名 ⇒ 不猜,標未量」✅ —— **只有那個背景數是髒的。** |

---

## 🟡 §二 · 已經自己標對了、但分母仍髒(1 處)

| 行 | 錨 | 板上逐字 | 判 |
|---|---|---|---|
| `:1255` | `⟦b4-PARTPAIDNOCANCEL1⟧` | 「**曝險 0 是【分母造成的】,不是修好了**」·「而它不塞進『仍成立』的理由:那一列的文字沒有跟著翻面 ⇒ 它在板上讀起來仍然像『還沒發生的事』」 | 🟢 **這一列的判斷方法是全板最好的一個** —— 它**自己說出那個 0 來自分母**。🔴 而它用的分母(匯款 + partiallyPaid + 未取消)仍然是從 8 張裡篩的 ⇒ **要重算的是數字,不是判斷。** |

---

## ⚪ §三 · 不受影響(11 處)—— 而理由要寫出來,否則下一個人會重查一遍

| 行 | 錨 | 那個 0 的分母是什麼 ⇒ 為什麼乾淨 |
|---|---|---|
| `:713` | `⟦b4-SWEEPDEAD1⟧` | **cron 班次**(近 24 小時 720 次零失敗)—— 與訂單無關 |
| `:811` | `⟦b4-CRON6HB⟧` | **cron 班次**(10 支全 active、近 24 小時零失敗)—— 同上 |
| `:1178` | `⟦b9-PROBESCHED⟧` | **天數與執行次數**(10 天 · 0 次)—— 同上 |
| `:2505` | `⟦front-ACLSNAPSHOTAGE⟧` | **天數**(落後 5 天 · 0 次提醒)—— 同上 |
| `:2098` | `⟦f3-REDNEEDSEXIT⟧` | **檔案數**(非測試檔 `Record<` = 0 / 12)—— 同上 |
| `:831` | `⟦b4-MGR0-RPC⟧` | **碼掃描**(`.rpc(` = 0,正對照 `await ` = 15)—— 同上 |
| `:2436` | `⟦auth-PAIDAMOUNTNOTFROZEN⟧` | **碼掃描**(金額比對 0 處,正對照收件人比對有命中)—— 同上 |
| `:1138` | `⟦f3-AUTOREFUND2⟧` | **可派的路 = 0 條**(卡前置)—— 不是資料量 |
| `:984` | `⟦b4-MGRENV1⟧` | **標「未量,而且結構上量不到」** —— 它沒有給出 0 |
| `:983` `:1026` `:1027` | 經銷三列 | **`tier=store` 0 人** —— 🔵 而客戶 15 ⇒ 真實 12,**那三個測試帳號都是 `general` 不是 `store`** ⇒ **這個 0 不受影響**。⚠️ 而**分母的字面要改**:「0 / 15」該寫成「0 / 12」。 |

---

## 🛑 §四 · 我證不到什麼

1. **真實分母那三個數字我沒有複量** —— 訂單 3 · 箱子 1 · 客戶 3 個測試帳號,**全部是主視窗 `-59` 2026-09-10 量的**,我照抄並標來源。**我自己那一發(訂單/金額)被本 session 的權限分類器擋下,我沒有繞過去。**
2. **§一那 7 處我【沒有重算】** —— 我指出「這個 0 的分母含測試資料」,**沒有算出扣掉之後是多少**。那要逐處回正式庫問,而那是下一個人的工。
3. **「已取消 3 張是不是就是那 3 張測試單」我答不出來** —— `:523` 那一格最需要這個答案,而**板上與我手上都沒有**。⇒ 📌 **那是一發查詢就能答的:那三張測試單的 `cancelled_at` 是不是非 null。**
4. **`:1624` 那 7 封信裡有幾封屬於測試單,我沒量。**
5. **我沒有掃 `docs/reviews/` 與 `docs/evidence/` 裡的「曝險 0」** —— 只掃了板子。📌 **而今晚大部分讀數的正本在那些檔裡,那裡很可能有同一批髒分母。**

---

## 🔵 §五 · 一句方法上的(給下一個寫「曝險 0」的人)

```
🔴 一個 0 要先問它的分母 —— 而今晚證明了那還不夠:
   還要問【那個分母裡有沒有我們自己造的資料】。
📌 一個用髒分母算出來的 0, 比一個沒算過的空格更危險 ——
   因為它看起來已經有人查過了。
```
⚠️ **而最省事的一道閘不是規則,是一個字面**:那三張單的單號寫進板頭,任何人量到它們就會看到。
🔴 **今天板上 `勿出貨` 與 `連線測試` 各 0 命中** —— **那三張單在板上沒有身分。**

---

**主樹狀態**:`git status --short docs/launch-todo.md` ⇒ 空(板檔我一個字都沒動)· 零 add / 零 commit / 零 stash / 零 checkout。
