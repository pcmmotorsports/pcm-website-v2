# ⟦b4-COUPONREVERT⟧ 接線 · 三道閘的輸出 · 2026-09-11 · 窗 C

> 🔴 **這份存在的理由**:重開之後要看得到「**它過了什麼**」,而不是「**有人說它過了**」。
> 對象:`supabase/migrations/20260910210000_m4b_coupon_revert_wiring.sql` + 對應 rollback。
> 🛑 **未貼正式庫** —— 代貼授權的單位是「那一個編號」,Sean 還沒給這一支。

---

## 0. body 是【抽出來的】不是重打的 —— 而它有讀數

```
🔬 從 20260907140000 / 20260905070000 逐位元組抽 body, 與正式庫 prosrc 的 md5 比對:
   pcm_sync_order_refund_payment_status  repo = prod = 38dc32ef2ad275588363db641f2019e3  (4,770 字元 · 6,479 UTF-8 bytes)
   pcm_pending_refund_on_cancel          repo = prod = 1dd4b13f79ae55be2e39e6fa7c4e9607  (  175 字元)
   ⇒ 🟢 **版控與正式庫在這兩支上逐位元組相同** ⇒ 抄 repo 是安全的
🔬 diff 驗:匯流點 +12 行 / −0 行 · 取消那個路口 +7 行 / −0 行
   ⇒ 🛑 **刪除 0 行 —— 其餘一個字都沒動。**
```

---

## 1. 🔴 零重播(`scripts/migrations-replay-from-zero.sh`)⇒ **我那支【沒有過】,而理由要看**

```
分母 417 ｜ 成功 350 ｜ 失敗 67
🔴 我那支在失敗清單裡, 錯誤逐字:
   接線 fail-closed:pcm_sync_order_refund_payment_status 的 body 不是我抽的那一版
   (實得 md5=62c17695621d002ec07789d1e1436f58)⇒ 有人改過它, 本支會蓋掉那個改動
```
> ## ✅ **那是我自己的前置斷言【正確地擋下來】,不是它壞了。**
> 🔬 成因:從零重放的世界裡 `20260907140000` **自己也失敗**(它自己也是 md5 前置閘),
> 　 所以那一刻庫上的匯流點停在**更早的一代** ⇒ 我的 md5 對不上 ⇒ 拒絕覆蓋。
> 📌 **⇒ 我這一支在重放中的行為,與【我要改的那一支】完全相同 —— 同一個原因、同一個世界。**
> 🛑 **而我不假裝它綠了。** 這棵樹今天從零重建不起來,已經是 67 支的事實,我這支是第 68 支。

**🔬 補量(同一個拋棄式庫的逐支 log):退款同步這條鏈【從哪一支開始斷】**
```
20260823010000  🟢
20260823020000  🔴 前置失敗 — 找不到那個精確簽章的 admin_record_manual_refund   ← 這條鏈第一個斷點
20260905010000  🔴 前置閘①c:目標函式的 body 指紋對不上
20260905440000  🔴 前置閘①:那三支函式現在共 2 支(期望恰 3)
20260906620000  🟢
20260907140000  🔴 前置閘 P3:庫上 body md5 = 62c17695… ⇒ 不是它抄的那一版
20260910210000  🔴 (本支)body 不是我抽的那一版
```
📌 **⇒ 這是一條【前置閘接力】:每一支都釘住前一支的產出,而第一個斷點在 `20260823020000`,
　 往後每一支都「正確地」拒絕。** 本支是這條鏈的最後一環。
🔵 整棵樹的**第一支失敗**是 `20260723120000_m3_s2_settle_sweep_pgcron.sql`(pg_net 未啟用 —— 環境缺件)。
🛑 **`20260823020000` 為什麼找不到那個簽章,我沒有往前追**(它的前一支 `20260820021000` 在同一個庫也失敗,
　 訊息是「需要至少一張既有訂單與一位啟用中的 staff」—— 看起來是種子資料缺件,**而那是推的**)。
🔵 codex R1 旁證:「`20260905440000` 產出的 `5ca77444…` 正是 `20260907140000` 要求的值;後者產出的
　 `38dc32ef…` 又符合本支要求」⇒ 📌 **在正式庫那個順序裡,這條鏈是接得起來的;斷的是重放那個世界。**

🔴 **而第一發它死在別的地方,那一格要留著**:
```
psql:…20260910210000…:94: ERROR: syntax error at or near "v_sync"
LINE 30: ...atalog.position('coupon_revert_on_full_refund' in v_sync) <>
```
⇒ `position(x IN y)` 是 **SQL 特殊語法,不是可加 schema 的函式** ⇒ `pg_catalog.` 前綴直接語法錯。
🎯 **而 `20260907140000` 的檔頭【就在警告這一族】**(它逐字實測過 `COALESCE`/`NULLIF`/`GREATEST`/`LEAST`)
　 —— **我抄了它的 body,而沒有讀它的警告。** 已改用 `pg_catalog.strpos(haystack, needle)`。

---

## 2. 🟢 拋棄式 PG · 兩個落點各走一次真的路徑 ⇒ **全過**

**先造世界**(零重放那個庫裡那兩支是更早的一代 ⇒ 先換成我抽 body 的那一版,md5 驗過):
```
🟢 世界造好:兩支都是我抽的那一版
```
**再跑我那支 migration** ⇒ `BEGIN / DO / CREATE FUNCTION ×2 / DO / COMMIT` **全綠**。

**行為驗收(不是讀碼,是真的動它)**
```
🟢 落點②(沒錢動的):UPDATE orders SET cancelled_at = now()
   ⇒ trigger 連鎖 ⇒ coupon_redemptions.reverted_at **有值**
🟢 落點①(有錢動的):種一筆 confirmed 1000 / total 1000, 呼匯流點
   ⇒ payment_status = refunded · 券 **真的退了**
```

**🔴 突變驗收(沒有它,「兩個落點都綠」與「那份驗收根本沒在測」印同一個綠)**
```
把落點② 那一行呼叫拿掉 ⇒ 取消之後券【沒有】退
⇒ 🟢 突變驗過:那一格真的在守, 不是恆綠
```

**🔴 而過程中我自己的斷言紅過一次,留著**:
```
接線 fail-closed:匯流點的 search_path 不是空字串
🔬 實測:`SET search_path = ''` 存進 proconfig 之後逐字是 `search_path=""`(帶雙引號)
   而我第一版寫 'search_path=' ⇒ 本斷言【正確地紅了】, 而紅的是【我的尺】不是那支函式
✅ 它紅得對:一道 fail-closed 的斷言寧可誤擋, 不可誤放
🔵 順手補上:第一版只問匯流點, 沒問取消那個路口 —— 兩支都是 SECURITY DEFINER, 現在兩支都問
```

---

## 3. 唯讀對抗審查(codex R1)⇒ **FAIL · 1 條 must-fix · 已修並實跑驗過**

**must-fix(逐字)**:「rollback 沒有檢查回捲前版本,會覆蓋其他修正 —— 接線已落地,之後任一函式又加入
退款修正 ⇒ 執行本 rollback ⇒ 那個修正被整支覆蓋,事後 md5 仍通過。」
📌 **事後斷言只證明【換成了舊版】,證不到【只撤銷了本支】。**

✅ **修法**:rollback 在任何替換之前,先問那兩支**現在**是不是接線那一版:
```
匯流點        md5 必須 = 5cd27b504015eb27ba3e8615a13bb149
取消那個路口  md5 必須 = 216fa25347796d94393f800348377f73
🔬 三方一致:migration 產生的 body · 拋棄式庫接線後實量 · codex R1 引用
```
**🔬 拋棄式 PG 實跑兩個方向**
```
① 正向 接線狀態 ⇒ 跑 rollback ⇒ COMMIT, 回到 38dc32ef… / 1dd4b13f…            🟢
② 突變 接線之後模擬「別人又改了匯流點」(md5 變 fb95c806…)⇒ 跑 rollback
      ⇒ ERROR「匯流點已經不是接線那一版 ⇒ 停下」· 別人的修正【仍在】            🟢
```

**nit(不改,照原樣記)**
1. 事後斷言是字串檢查 —— 包成 `/* PERFORM … */` 或把參數改成 `NULL::uuid` 仍會通過。
   今天兩行實際呼叫正確 ⇒ codex 不升級。📌 **那道斷言證的是「碼裡有那個名字」,不是「它被正確地呼叫」。**
2. body md5 不涵蓋函式設定 —— 若正式庫另經 `ALTER FUNCTION … SET` 改過,md5 看不出來。
   (codex 核過:原檔、新檔、rollback 的設定今天一致,沒有漏抄。)
3. 長度單位寫錯 —— 已訂正:4,770 是**字元**,UTF-8 是 6,479 bytes。

**codex 確認的**:抽取沒有漏帶或多帶 · 匯流點只有一個可執行的 RETURN · rollback body 與來源逐位元組相同 ·
兩檔無 COMMIT 後的 DDL/DML · 未改 trigger 狀態。

🛑 **R2 沒有跑** —— R1 有 must-fix,依鐵則 12 要 R2;而 Sean 要重開機。**重開之後第一件事:R2。**

---

## 4. 🛑 這三道閘【證不到什麼】

1. **我沒有在正式庫上跑過任何一句。** 上面每一格都是拋棄式 PG。
2. **零重播那一關我沒有過**,而我判它與「我要改的那一支」同因 —— 🔴 **那是我的判斷不是讀數**。
3. 行為驗收走的是**我自己種的假單**,不是真客人的路徑;`admin_finalize_order_refund` 那條
   完整的應用層路徑**沒有走過**。
4. 突變只做了**落點②**;**落點① 的突變沒做**(時間)。
   ⇒ 📌 **所以「落點① 有判別力」這句話今天說不出口** —— 它綠,而我沒有證明它會紅。
