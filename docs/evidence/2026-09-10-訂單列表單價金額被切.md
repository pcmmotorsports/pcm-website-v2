# 訂單列表的單價與金額被切(2026-09-10)—— ✅ 已修

> Sean 三張截圖 + 鑽機逐格重現。**零改碼。**
> 📷 `~/pcm-mailbox/單價欄-改前-每格都印NT-20260910.png` / `~/pcm-mailbox/單價欄-改後-只在表頭印一次-20260910.png`
> 兩張同一個視窗寬(1728)、同一批訂單、同一個面板狀態 —— **只有那一個變因不同。**

## 1. 重現:逐格對上稿,而【被切格數不是 0】

網址 `?panel=<uuid>`(面板是網址參數,不是另一頁)。

| | 稿說 | 量到 |
|---|---|---|
| 面板寬 | 520 | **520** ✅ |
| 列表容器 | 1072 | **1072** ✅ |
| 可見欄數 | 13 | **13** ✅ |
| **被切格數** | **0** | 🔴 **49** |

被切的:`客戶 7 · 料號 12 · 物品名稱 12 · 單價 12 · 金額 6`

🎯 **⇒ 稿的驗收條件,在它自己指定的那個世界裡就不成立。**

## 2. 🔴 算術缺口:目標寫 1072,而清單加起來是 1100

```
稿(FIX-44)說   表格 === 1072  ⇒ 剛好不用橫捲
而稿自己列的欄寬  38+120+54+152+90+128+130+42+62+70+72+84+58 = 1100
量到             表格內容 1100 · 容器 1072 ⇒ 溢出 28px
```

📌 **那 28px 就是差額本身。**
🎯 **⇒ 「剛好不用橫捲」從來沒有成立過,而它一直印在畫面上。**

🛑 **下一個人拿「被切格數 === 0」當目標之前要先看到這一段** —— 那是一個**算術上就達不到**的目標。

## 3. 修法與實測:18 格 → 0,而版面一個字都沒動

單價欄可用內容寬 **48px**:

```
「NT$ 3,670」需要  77px  ⇒ 切
「3,670」需要      42px  ⇒ ✅ 放得下
每一格省下         35px
```

把每格重複的 `NT$ ` 拿掉、幣別在表頭講一次(「單價 NT$」/「金額 NT$」):

| | 改前 | 改後 |
|---|---|---|
| 單價 + 金額 被切格數 | **18** | **0** |
| 列表容器 | 1072 | 1072 |
| 表格內容寬 | 1100 | 1100 |
| 可見欄數 | 13 | 13 |

🔵 理由不是省空間:**一欄裡每一格都寫同一個幣別,那個字沒有在分辨任何東西。**

## 4. ⚠️ 天花板:六位數仍會切

```
今天最大 5 位數(22,760)⇒ 42~48px ⇒ 放得下
而 6 位數「128,000」需要 59px > 48px ⇒ 🔴 仍會切
⇒ 要連六位數都吃, 單價欄 62 → 76(59 + 左右內距 14)
   代價:表格 1100 → 1120, 溢出 28 → 48px
```

📌 **⇒ 這是兩個決定,不是一個**:①拿不拿掉 `NT$` ②要不要現在就買六位數的頭部空間。


---

## 5. ✅ 落地(Sean 2026-09-10 兩題都拍甲)

```
① 幣別只在欄名印一次 ⇒ 表頭「單價 NT$」/「金額 NT$」, 每一格不再重複印
② 現在買六位數的頭部空間 ⇒ col-unit 62→76 · col-amount 70→76 · table min-width 1100→1120
```
🔴 **兩欄都改** —— 他買的是「六位數看得完整」,不是「單價欄變寬」。金額欄今天 70 也吃不下六位數。
🔵 **改的是【贏的那一條】**(檔尾 FIX-44 那一組,不在任何 `@container` 裡),**不是在後面加一條更大的去壓過它。**

### 🔴 而 `data-l` 也要帶幣別 —— 這一格差點漏掉

手機卡片**沒有表頭**,欄名是每一格的 `data-l`(`globals.css:1473` `content: attr(data-l)`)。
⇒ 只改桌機表頭的話,**手機上會變成一個沒有幣別的數字**。三處 `data-l` 一起改成「單價 NT$」/「金額 NT$」/「小計 NT$」。

### 驗收讀數(面板開著 · 六位數 128,000 已種進畫面)

| 視窗 | 面板 | 列表容器 | 表格 | 可見欄數 | 六位數格數 | **單價+金額被切** |
|---|---|---|---|---|---|---|
| 1728 | 520 | 1072 | 1120 | 14 | 2 | **0** |
| 1512 | 520 | 856 | 1120 | 14 | 2 | **0** |
| 1440 | 520 | 784 | 1120 | 14 | 2 | **0** |
| 1200 | 520 | 544 | 1120 | 13 | 2 | **0** |

🟢 **正對照**:六位數 `128,000`(單價)與 `261,080`(金額)兩格**都在畫面上、都完整**。
⚪ **負對照**:把欄寬改回 `62/70` ⇒ **立刻切 1 格,而那格就是六位數**。
⇒ 📌 **那四個 0 是真的守住了東西, 不是「它本來就不會切」。**

🔵 而容器從 1072 縮到 544 時**欄寬不縮、改用橫捲** —— 那正是稿 FIX-42 要的行為(「放不下就整表橫捲,欄位永不被壓縮」),現在在窄容器上真的成立了。

### ⚠️ 本片不處理的(而它們還在切)
```
客戶 7 格 · 料號 12 格 · 物品名稱 12 格
```
📌 **那三欄切的是【文字】不是【錢】** —— 而 Sean 這一題問的是金額。要不要處理是另一件事。

📷 `~/pcm-mailbox/單價欄-落地-六位數也完整-20260910.png`
