# 髒分母 · 正本那一側(`docs/reviews/` + `docs/evidence/`)· 2026-09-10

> 🛑 **只指出來,不重算。** 板檔零改動(這一輪我一個字都沒碰 `docs/launch-todo.md`)。
> **真實分母**(來源 = 主視窗 `-59` 2026-09-10 唯讀正式庫,我沒複量):訂單 8 ⇒ **真實 5** · 客戶 15 ⇒ **真實 12** · 唯一走過新竹那一箱是測試單的。
> 測試單:`CH6D75`(新竹首箱連線測試)· `P7KZ55` / `MCGHHM`(稅驗證勿出貨)。測試帳號:`g3-sandbox-test` · `g3-preview-sandbox` · `uitest`。

---

# 🎯 先答你那個「才是重點」的第 ③ 題

## **答案是【有】。而它比「被當成真客人算進去」更難看:那個知識【存在過,而且在一天之內退化掉了】。**

### 🔬 證據一 —— 窗B 在 **2026-09-09** 就把分母標成「測試單」,逐字

`docs/reviews/2026-09-09-窗B-十列查證結果.md`:
```
:98   ⚠️ **射程**:分母只有 4 張測試單, 這個 0 的效期只到有真單為止。
:208  1. 所有正式庫讀數的分母都是 **4 張測試單 / 6 封信**
      ⇒ 每一個 0 的效期只到有真資料為止。
```
🎯 **⇒ 窗B 知道。它寫下來了。而它寫的是【4 張測試單】,不是「4 張單」。**
🔴 **而 24 小時之後,同一批數字在別處變成了「訂單 8 張」「6 張單」—— 那個【測試單】的標籤掉了。**
📌 **⇒ 這不是「沒有人發現」,是【發現了、標對了,而標籤沒有跟著數字走】。**
🔵 **⇒ 那與今晚那個「結論沒走回擋欄」是【同一個病的另一個面】** —— 一個是結論不回板,一個是**限定詞不跟著數字走**。

### 🔬 證據二 —— 汙染至少一個月前就開始了

`docs/reviews/2026-08-08-full-site-ui-sweep.md:67` 逐字:
> 測試過程把 `uitest@pcmmotorsports.com` 的「個人資料 · 姓名」從原本的空值改成了「**UX 測試員**」

⇒ 🔴 **那就是為什麼它今天出現在客戶清單上、而且長得像一個人。** commit `cd5abdc2b`(2026-08-08)。
📌 **⇒ 這個病不是今天發生的,今天只是第一次有人把三個帳號並排數。**

### 🔬 證據三 —— 兩個【真的缺陷】,而它們唯一的證人是測試帳號

`docs/reviews/2026-09-09-窗B-十列查證結果.md`:

| 行 | 逐字 | 🔴 而 |
|---|---|---|
| `:362-364` | 「**後果具體**:客戶清單裡的 `UX 測試員`(`uitest@pcmmotorsports.com`)**電話欄是 `—`** ⇒ **在建單頁完全找不到這個人**,而畫面說得出他的 Email 應該找得到。」 | **那個缺陷是真的**(建單頁只吃電話),**而它唯一的受害者是我們自己的測試帳號。** ⇒ 📌 **今天沒有任何客人踩得到它 —— 而板上讀起來像有。** |
| `:369-373` | 「查 `0912345678` 回**兩列**,逐字都是 `G3 沙盒測試(0912345678)` —— 同名、同電話、**沒有任何一格分得出誰是誰**…一位是 `g3-preview-sandbox@`、一位是 `g3-sandbox-test@`」 | **缺陷是真的**(建單頁那一列少印 Email),**而「兩位客人」兩位都是我們的。** ⇒ 📌 **「員工會猜錯、單會掛到別人身上」今天是【零機率】,因為那兩個人都不是人。** |

🎯 **⇒ 這兩處與板上那兩處(`:1017` 拿測試單當「真單」· `:2361` 拿測試箱當「第一箱真的送出去」)是【同一個形狀】,而方向相反:**
```
板上那兩處:拿測試資料證明【東西是好的】⇒ 一個假的安心
這兩處    :拿測試資料證明【東西是壞的】⇒ 一個假的急迫
📌 兩種都會讓下一個人把力氣花錯地方, 而它們讀起來都像「有人查過了」。
```

---

# 🔴 §一 · 正本那一側要重算的處(逐處)

| 檔 · 行 | 逐字 | 為什麼髒 |
|---|---|---|
| `docs/reviews/2026-09-09-窗A-十二列查證.md:302,307` | 「訂單總數 **6** · `subtotal ≠ Σ line_total` 的張數 = **0**」·「⚠️ **而分母只有 6 張單** ⇒ 那個 0 的強度有限」 | 🔴 **這是 `⟦f3-PAIDAMOUNTDRIFT1⟧` 被判「已不成立 / 可翻不擋」的那一發。** 那 6 張裡有測試單(`P7KZ55`/`MCGHHM` 建於 2026-09-09,見同檔 `:241-242`)⇒ **真實分母 ≤ 4**。⚠️ 它已經自標「強度有限」✅ —— **而它不知道分母裡有我們自己的單。** |
| `docs/reviews/2026-09-09-窗A-十二列查證.md:362-368`(§14) | 「訂單 6 · **已取消 3** · 信件 6 · 已寄出 6 / 已取消的單身上有信 ⇒ **0** ← 分母是 0」 | 🔴 **你今天那一發把它釘死了**:已取消 3 張 = `W9486K`(真)+ `MCGHHM`(測)+ `P7KZ55`(測)⇒ **真實已取消 = 1 張**。⇒ 📌 **那個「分母是 0」其實是「分母是 1,而那 1 張沒有信」** —— 它的判斷方向對,而數字更小。 |
| `docs/reviews/2026-09-09-窗B-十列查證結果.md:91,98` | 「唯讀正式庫 **0 張**…⚠️ 射程:分母只有 **4 張測試單**」 | 🟢 **這一處【標對了】** —— 我列出來不是要它改,是要它**被引用時整句一起帶走**。🔴 **而今天有人引用了它的結論而沒有帶那句射程。** |
| `docs/reviews/2026-09-09-窗B-十列查證結果.md:208` | 「所有正式庫讀數的分母都是 **4 張測試單 / 6 封信**」 | 🟢 同上,**這是全隊最早、也最清楚的一句**。🔴 **它今天沒有被任何一列引用。** |
| `docs/reviews/2026-09-09-窗B-十列查證結果.md:458` | 「**分母極小**:全庫 `card` 收款只有 **1 筆 NT$4**、混軌單 0 張 ⇒「今天不成立」的效期…」 | 🟡 **標對了「極小」而沒標「是不是測試的」**。你今天的表顯示 `C8MYDB`(真 · refunded · 4 元)⇒ 📌 **那 1 筆 NT$4 是【真單】** ⇒ 這一處**不用重算**,而它值得補一句「那 1 筆是真的」。 |
| `docs/reviews/2026-09-09-窗B-十列查證結果.md:1113,1124` | 「『已取消且收款淨額為正』的分母 > 0 那一天才驗得動」·「那個分母今天是 **0**」 | 🔴 已取消 3 ⇒ 真實 1 ⇒ **那個 0 的分母又小了一層**。結論(等真資料)**不變**,而**強度更弱**。 |
| `docs/reviews/2026-09-09-窗B-十列查證結果.md:1234` | 「### 18-b 【今天實量】**縫的分母 = 0**」 | 🔴 同族,分母來自訂單集合 ⇒ 髒。 |

---

# 🔴 §二 · 我自己那六份也髒 —— 而其中一處是**我寫錯的**,不只是分母

**你叫我「掃它,不要跳過自己」。掃到了,而且撿到一個真的錯:**

| 檔 · 行 | 我當時寫的 | 🔴 訂正 |
|---|---|---|
| `docs/evidence/2026-09-10-6d-已量未查證-b4f3.md:60-66` | 「C 客戶總數 ⇒ **15** · D 訂單總數 ⇒ **8**…⇒ **客戶數 15 之中有 1 個是測試帳號 = 6.7%**」 | 🔴 **那個「1 個」是錯的 —— 是 3 個。** 我當時只查了 `g3-sandbox-test` 一個信箱,**而你今天並排量出三個**(`g3-sandbox-test` · `g3-preview-sandbox` · `uitest`)⇒ **15 人裡 3 個是測試帳號 = 20%,不是 6.7%**。<br>📌 **成因不是算錯,是【我只問了我知道名字的那一個】** —— 我的尺**只找得到我已經知道的東西**,而我沒有先問「總共有幾個測試帳號」。 |
| 同檔 `:60-61` 那組讀數 | 「客戶總數 15 · 訂單總數 8」 | 🔴 兩個都是髒分母 ⇒ 真實 12 / 5。 |
| `docs/evidence/2026-09-10-6d-front族查證.md`(`⟦front-CATALOGPRICEGENERALONLY⟧` 曝險) | 「今天曝險 = **0 人**(`tier=store` 0 人)」 | 🟢 **這個 0 乾淨** —— 三個測試帳號都是 `general` 不是 `store`。⚠️ **而它旁邊那個分母字面該改**:寫「0 / 15」的地方要變「0 / 12」。 |
| `docs/evidence/2026-09-10-6d-b4族查證.md`(`⟦b4-SIG4ERRORS⟧` 那一發) | cron 班次讀數 | ⚪ **乾淨** —— 分母是排程班次,與訂單無關。 |
| `docs/evidence/2026-09-10-6d-db治理6列.md` · `b9族查證` · `f3族查證` | 檔案數 / 天數 / migration 支數 / 白名單格數 | ⚪ **乾淨** —— 沒有一個分母來自訂單或客戶。 |

🛑 **⇒ 我那六份裡,髒的只有 `已量未查證-b4f3.md` 那一處,而它同時是【一個真的錯】。請優先改它。**

---

# ⚪ §三 · 掃到而不受影響的(說明理由,免得下一個人重查)

`docs/reviews/2026-09-09-上線前資安深掃.md` 的「8 張」全部是 **`storage` schema 的 8 張表**,與訂單無關(`:257` `:295` `:496` `:539` `:556`)。
`docs/evidence/2026-09-10-rlshardenzerorows-….md` 與 `docs/evidence/2026-09-10-mail族查證.md` 的「8 張」是 **`anon` 會讀到零列的 8 張表**,同族,乾淨。
2026-07 那批 e10 檔的「26 張 / 6 張」是**去年那批要刪的舊訂單 cohort**,不是今天的分母。
⚪ 負對照:現造字面 `zzq0910` ⇒ 全部 0 命中;🟢 正對照:同尺找「訂單」⇒ **65 支檔** ⇒ 尺是活的。

---

# 🛑 §四 · 我證不到什麼

1. **真實分母那三個數字我沒複量** —— 訂單 5 / 客戶 12 / 那一箱是測試的,**全部是你 2026-09-10 量的**,我照抄標來源。
2. **「窗A §13 那 6 張裡到底幾張是測試單」我沒量** —— 我只從同檔 `:241-242` 讀到那兩張建於 2026-09-09 ⇒ **推得出「≤4」,推不出確切數字。** 那要一發帶時間的查詢。
3. **我只掃了 `docs/reviews/` 與 `docs/evidence/`** —— **`docs/plans/` 與 `~/pcm-mailbox/` 沒掃**。📌 **而今晚有好幾個結論的正本在 plan 裡。**
4. **我沒有重算任何一處。**
5. **「有沒有第四個測試帳號」我沒問** —— 📌 **而那正是我 `:66` 那個錯的成因:我只找我知道名字的。** ⇒ **要答得先問「怎麼判一個帳號是測試的」,而那個判準今天不存在。**

---

# 🔵 §五 · 一句給下一個人的

```
🔴 今晚學到的不是「一個 0 要問分母」——
   是【一個帶限定詞的數字, 限定詞會在傳遞途中掉】。
📌 窗B 09-09 寫的是「4 張測試單」, 而 24 小時後同一批數字變成「8 張」。
✅ 而最省事的一道閘不是規則:把那三個單號與三個帳號寫進【數字旁邊】,
   不要寫在檔尾的射程段 —— 射程段不會跟著被複製走。
```
