# 2026-09-10 帶擋而【沒有錨】的那 9 列

> 🛑 **我沒有改 `docs/launch-todo.md`,也沒有幫任何一列補錨**(補錨是改板,而 Sean 只解凍了加標記)。
> 🔵 **它們今天唯一的座標是【行號 + 標題】** —— 本檔就用那個指。
> 📎 全板分佈:`~/pcm-mailbox/板子權威分佈-尺C-20260910.md`

---

## 〇、🔴 先訂正我自己的分佈檔 —— **那個「無錨 11」是 9,而成因是我的正則**

```
我的族碼正則 ⟦([a-zA-Z0-9]+)-([A-Z0-9]+)⟧   ← 🔴 假設錨【只有一個連字號】
而板上有 5 列不是:
   ⟦b4-MGR0-RPC⟧ · ⟦15-SHIPGATE-F1⟧ · ⟦c7-COR-IDENTITY-ASSERT⟧
   ⟦0e-DDLINTOVC-MARK⟧ · ⟦c7-NEIGHBOUR-FIRST⟧
⇒ 它們被歸成「無錨」
✅ 修正後:b4 41→42 · (無錨) 11→9 · 新增族 `15` 1 列 · 合計仍 114
🟢 正對照 全 open/doing 的無錨列 80 → 75
⚪ 負對照 現造族碼 ⇒ 0
```
🎯 **⇒ 那是尺C 的【第四個】沒說出來的前提:「錨只有一個連字號」。**
📌 **而前三個是**:「最後一欄是備註欄」「豎線是欄界」「第一個標記是判定」。
🔴 **四個前提各自都很合理,而它們一起錯了 28 列 + 5 列。**

---

## 一、🎯 而板上【已經有一列在講這件事】,而它自己也帶擋

`:684` `⟦ship-TWINROWNOGATE⟧` 逐字:
> **板上【無錨】的列可以重複任意多次,而沒有任何東西會叫 —— 而已經發生兩次,兩次都是「合併做完了、來源列沒刪」。**

⇒ 🔴 **而我手上這 9 列,正好就是它講的那個風險面。**
🔵 **⇒ 兩件事接起來了**:那一列是**病因**,這 9 列是**今天的存量**。

### 🛑 而我找它的時候又踩了一次「提到」vs「是」
我掃「重複列 / 不要派本列 / 正本」想找出**被重複計數的列** ⇒ 命中 2 列。
🔴 **而 `:684` 不是重複列,它是【講重複列】的那一列。**
⇒ ✅ **真正的重複列只有 1 列。**

---

## 二、✅ `:675` ⇒ **本列描述有誤(它是重複列,不該計進分母)**

· **板上逐字**:`⛔ **[重複列 · 正本 ⟦b9-ACLDRIFT5⟧]**` + 誰欄 `⛔ **不要派本列 —— 去正本**`
  而標題本身**帶著刪除線**:`~~ACL 漂移守門裝上了,而【路⑤ …】仍然完全沒有人守~~`
· 🎯 **⇒ 一列明文寫著「不要派」的重複列,今天被算進那 114。**
· ✅ **而它的正本我今天查過**:`⟦b9-ACLDRIFT5⟧` ⇒ **路⑤ 仍成立(結構性的)**,證據
  `docs/evidence/2026-09-10-acldrift5-那個擋點我兩天前就推翻過了.md` §四。
· ⇒ 🔵 **所以 114 裡至少有 1 列是重複計數** ⇒ **不重複的擋 ≤ 113。**
· 🛑 **而我不刪它、不改它的態** —— 那是改板。**給主視窗。**

---

## 三、🟡 `:859` ⇒ **兩把尺印不同的數,而我【不報任何一個】**

· **板上逐字**:「storefront 有 **5** 道 `service_role` 受控例外,而其中 **4** 道說不出是誰批的」
· **【量的】而它自己在誰欄給了數法**(`git grep -nE '^// esl…'`),我照著跑:
```
含 service_role 的 eslint-disable 行     ⇒ 3
另一把(含 eslint 的 service_role 行)    ⇒ 5
🟢 正對照 storefront 全部 eslint-disable ⇒ 41  ⇒ 尺會咬
```
· 🛑 **兩把尺印 3 與 5,而板上寫 5。** ⇒ 🔴 **我不宣稱哪一個對。**
  📌 **⇒ 這一列要收掉,得先把【它自己那個數法】跑對** —— 而它誰欄那行被截斷了(`^// esl…`),**我看不到完整的樣式。**
· ⇒ **結論:🟡 需要開它自己的數法原文。而那是一行,不是一件工。**

---

## 四、⇒ 其餘 7 列 —— **照實列出,今天沒查**

```
:536   B7 端到端負向驗收          ⟨擋·跨repo⟩  🔵 而【我今天做的就是它】——
                                   plan 已落 docs/plans/2026-09-10-b7-7號混版驗收-plan.md
                                   Sean 已答 Q3(要測)/ Q4(乙, 回退目標待定);Q1 Q2 未答
                                   ⇒ 📌 它不是「沒人動」, 是【等 Sean 答兩題】
:746   手動建訂單(匯款那條路)     ⟨擋·待辦⟩     誰欄有主:`-ship`
:777   33 項「員工的一天」6 格重量  ⟨擋·等真資料⟩  📌 它自己寫著等真資料 ⇒ 沒有人可以催
:780   心跳白名單那個零(doing)    ⟨擋(tidy 判 ⑥)⟩ 誰欄有主:線G `-1c`
       🔵 而我今天量過心跳表:10 支 job 全部有今天的 last_success_at
         ⇒ 🛑 而那【不是】這一列要的 —— 它要的是「那個零不能被讀成已經驗過了」
:1182  部件講錯/方向講反 15 筆     ⟨擋(tidy 判 ②)⟩ 🔴 跨 repo(報價單)
:1203  員工手動建單(電話/LINE)   ⟨擋⟩          誰欄有主:`96` 線 C
:1275  B5-a 的 rollback 是紙上約束 ⟨擋(tidy 判 ④)⟩ 待派
```
🔵 **而其中 4 列【誰欄有主】**(`:746` `:780` `:1203` 各有窗接,`:536` 等 Sean)
⇒ 📌 **⇒ 「無錨」不等於「無主」。那 9 列裡真正【沒有人也沒有座標】的,是 `:777` `:859` `:1182` `:1275` 四列。**

---

## 🛑 本份證不到什麼

· **【證不到】`:859` 那個 5** —— 兩把尺印 3 與 5,而它自己的數法原文被截斷。
· **【證不到】其餘 7 列今天成不成立** —— **只讀了標題與判定欄,沒有開檔查。**
· **【證不到】那 9 列有沒有【彼此重複】** —— `⟦ship-TWINROWNOGATE⟧` 說「無錨的列可以重複任意多次」,
  而**我沒有兩兩比對過它們**。
· **沒有改板、沒有補錨、沒有刪任何一列。**
