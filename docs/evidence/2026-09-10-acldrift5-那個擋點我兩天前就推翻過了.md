# 2026-09-10 `⟦b9-ACLDRIFT5⟧` —— **那個擋點,我 09-09 就推翻過了,而板上今天還寫著它**

> 板列今天寫著 **🛑 不要派 —— 卡 Sean**,而理由是 2026-09-09 那一格:
> 🔴🔴 **[這一題【題幹不存在】—— 它不是待辦,是缺口]** 誰欄寫「等 Sean(`Q-權限偷改偵測` 甲/乙)」,
> **而甲與乙的字面查無**…掃 `~/pcm-mailbox/*.md` + `docs/` ⇒ **命中 4 處,4 處全部只在講「這題還沒答」**,**零處寫著選項內容**。
> 🛑 **⇒ 要端 Sean 之前,得先有人把甲乙寫出來。在那之前這一列不可派。**
>
> 🔴 **而那一格【不成立】,而且我 2026-09-09 就寫下來了。**

來源標籤:**【量的】**(命令 + 讀數,尺先證明會咬)/ **【推的】** / **【證不到】**

---

## 一、【量的】今天複量:選項在,而且五樣都齊

同樣範圍(`~/pcm-mailbox/*.md` + `docs/`)找 `Q-權限偷改偵測`:
```
板上 2026-09-09 說:命中 4 處
🔴 今天同樣範圍:            10 支檔
```

而 **`~/pcm-mailbox/端Sean-0905早上佇列.md:424`** 逐字:

> **Q-權限偷改偵測**(`-db` 06:0x,plan `docs/plans/2026-09-05-acl-drift-runtime-detector-plan.md`):後台權限被人在 Supabase 網頁上手動改掉,今天沒有任何東西會發現。要不要做一支每天自動比對、有變就寄信給你的偵測?**A: 甲 = 現在做**(一支 migration + 告警信多一列;代價:每次貼板當天都會紅,貼完要順手多一步「更新基線」)**/ 乙 = 上線後再做**(在那之前手動改權限這條路仍沒人守)。**推薦甲。**

⇒ ✅ **題幹 · 甲 · 乙 · 代價 · 推薦 —— 五樣全在。**

### 🔴 而時間對不上「那時候還沒寫」這個解釋

```
端Sean-0905早上佇列.md   最後修改  Sep 8 01:40     ← 比板上那次掃描【早】
板上那一格               2026-09-09
```
⇒ 📌 **它在被掃的那一刻就在那裡。那不是「後來才寫的」。**

---

## 二、🔴 而最該記的是這一格:**我 09-09 就推翻過,而板上今天還寫著**

`docs/plans/2026-09-09-acl-drift-and-provenance-plan.md:52-58` 逐字(我自己寫的):
> 🔴 **我同法重掃,命中 8 個檔**,而其中一個逐字寫著完整題幹 —— `~/pcm-mailbox/端Sean-0905早上佇列.md:424`
> ⇒ ✅ **題幹、甲、乙、代價、推薦 —— 五樣都在。**
> ⇒ 📌 **而「佇列 §B 指不到一份現存的檔」也不成立** —— `端Sean-0905早上佇列.md` 就是那份佇列檔。

⇒ 🛑 **所以這不是「沒有人發現」,是【發現了、寫下來了,而它沒有走回板上】。**

🎯 **而板列自己數過這一族**:它在別處寫著「**這是本次對板第【四】次撞到『板列不知道自己被修好了』**」。
📌 **⇒ 這是第五次,而這一次被留在板上的不是一個過期的狀態,是一個【擋住派工的理由】。**
· 過期的狀態 ⇒ 下一個人多做一次白工。
· 過期的**擋工理由** ⇒ **下一個人根本不會來。**

---

## 三、【量的】而這一列的另一半(快照射程),今天原封不動

板列量到快照的射程是**寫死在 `WHERE` 裡**的(`20260905140000_m4b_acl_drift_digest_table.sql:149-155` 逐字
`WHERE n.nspname IN ('storage','public') OR n.nspname IS NULL`)。

2026-09-10 唯讀複量:

| 量什麼 | 板上 09-08 | 今天 | |
|---|---|---|---|
| `pg_default_acl` 總列數 | 27 | **27** | ✅ |
| 快照射程**內** | 9 | **9** | ✅ |
| 🔴 射程**外** | 18 | **18** | ✅ |
| 🔴 射程外**而且含 `anon=`** | 6 | **6** | ✅ |
| ⚪ 負對照 現造 schema 名 | 0 | **0** | ✅ |

**射程外是哪些 schema**:`auth` · `cron` · `extensions` · `graphql` · `graphql_public` · `realtime`

⇒ ✅ **四個數字逐格對上,一格沒動。**
🔵 **而板列刻意【不為 `graphql` 開新列】的理由我認同,不推翻**:那 6 列含 `anon` 很可能是平台設計,而**開一列本身就是一個宣稱**。
🎯 **而不論它是不是問題,「我們的偵測看不到它」這件事本身是真的** —— 那句是板列的,今天仍成立。

### 🔵 而我開檔前那句「開檔前不要引用」的推測,這次**方向對了**

我猜:「第④列講『dashboard 手動改權限沒人守』,而那個 `WHERE` 可能就是它的一半。」
✅ **開檔後:那個 `WHERE` 確實在這一列裡**,而它就是「路⑤ 事後看不看得見」那一半。
🛑 **而我上一次同樣形狀的推測(`⟦f3-ADPNARROWER1⟧`)是【錯的】** ⇒ 📌 **兩次都標了「開檔前不要引用」,而一次對一次錯 —— 那正是為什麼要標。**

---

## 四、⇒ 這一列今天的狀態

| 板上寫的 | 今天 |
|---|---|
| 「甲與乙的字面查無 ⇒ **不可派**」 | 🔴 **不成立。** 選項五樣全在(`端Sean-0905早上佇列.md:424`),而**我 09-09 就寫下來了** |
| 「佇列 §B 指不到一份現存的檔」 | 🔴 **不成立**(同上) |
| 快照射程外 18 列 / 含 anon 6 列 | ✅ **今天逐格對上,原封不動** |
| 路⑤ dashboard 手動改仍沒人守 | ✅ **仍成立**(那是結構性的,pre-commit 文字閘看不到) |

⇒ 🎯 **所以這一列真正卡的是【那一題沒有被端到 Sean 面前】,不是【那一題不存在】。**
⇒ 🛑 **而端不端是主視窗的事,不是我的。我沒有改板、沒有改態、沒有動任何東西。**

---

## 五、🛑 我證不到什麼

· **【證不到】板上那次掃描為什麼只命中 4 處** —— 可能是樣式不同、可能是掃的樹不同。**我只證了「今天同樣範圍是 10 支,而其中一支的修改時間早於那次掃描」。**
· **【證不到】Sean 有沒有看過那一題** —— 佇列檔在他的信箱裡,而**「檔在那裡」不等於「他讀過」**。
· **【證不到】那 6 列 `graphql` / `graphql_public` 是不是問題** —— 板列刻意不判,我也不判。
· **只複量了 `pg_default_acl` 那一族。** `acl-drift-gate` 本身的五條路現況、快照的實際 diff,**都沒做**。

---

## 六、🔗 接手用(2026-09-10 存檔)

### 我在哪一步
· **隊列**:主視窗第二輪 5 列,**第④列做完**。
  ① `⟦auth-HALFREVOKEDTRIGGERS⟧` ✅ · ② `⟦auth-GRANTGATEBLIND⟧` ✅ · ③ `⟦f3-ADPNARROWER1⟧` ✅ · ④ **本檔** ✅
  ⑤ `⟦5b-AUDITGRANTEXPIRY⟧` ⏭️ **下一列(最後一列)**
· **之後**:板子標記。🔴 **標之前先 `git fetch origin dev` 看那一列有沒有人標過**;撞了回報主視窗,**不要自己選一邊**。

### 🔴 我手上還沒落檔的判斷
· **⑤ `⟦5b-AUDITGRANTEXPIRY⟧` 我還沒開檔**,而它的標題是「稽核表 `SELECT` 權限的**到期日不是日期是事件**」。
  ⇒ **推的**:那個「事件」很可能就是 `⟦b9-RLSHARDEN⟧`(拿掉 `BYPASSRLS` 那天)——今晚多列都指到它。
  🔴 **開檔前不要引用這句。**(我今天兩次這種推測:一次錯 · 一次對。)
· **這一份的第②節**(我 09-09 推翻過而板上沒跟上)**要進板子標記**,而它是**第五次**同型。

**正式庫零寫入。沒改板、沒改態、沒動任何權限。**
