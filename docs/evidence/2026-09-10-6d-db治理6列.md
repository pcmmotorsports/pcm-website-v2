# 權限 + DB 治理 6 列查證 · 2026-09-10(唯讀;主 repo 樹上零寫入、零 git add、零 commit)

> 查的人:新 session(主樹 `/Users/sean_1/pcm-website-v2` · dev · `af6fd59e1`),主視窗 `-59` 派工(第四輪)。
> **紀律**:帳本一律問 `git show origin/dev:supabase/APPLIED.tsv`,不讀工作區那份(窗D 今天誤報的成因)。
> **6 列全部查完。**

---

## 🎯 總結行

```
本批 6 列 · 仍成立 5 · 前提翻面(部分)1 ⇒ 對那個 78 的【應減】= 0
🔴 而本輪撿到【三件會改變分母本身的東西】—— 見 §三,那比這 6 列值錢
🔴 主視窗對 2423 的假設【要撤回】:今天那四支的 rollback 是 sidecar, 不是內嵌 ⇒ 「= 0 支」原封成立
```

---

## 一、五欄表

| 錨 | 板上說什麼 | 今天量到什麼 | 結論 | 證據 |
|---|---|---|---|---|
| `⟦b4-MGRENV1⟧` (`:984` · **doing**) | manager 閘的效力綁一顆 env,那顆拿掉時拿**舊格式登入票**的人會被**放行**;2026-09-06 已做「乙:一道會叫的訊號」,**態是 `doing` 不是 `done` —— 它只出聲, 不擋**。轉 `done` 的條件 = **那個放行真的被擋住** | 🔴 **三格逐一複現,洞原封在**:①`session.ts:175` 逐字 `return process.env.ADMIN_REQUIRE_REAL_IDENTITY === '1';` **仍在** ②`actor.ts` 的三層 docstring **仍在**,第 3 層逐字「不是 `v:2` 而旗標【關】⇒ 維持改動前那一行」 ③訊號元件 `real-identity-warning.tsx`(5,713 bytes)+ 真瀏覽器測試(10,210 bytes)在,且 `app/layout.tsx:152` 逐字 `<RealIdentityWarning />` **掛著**。⇒ 📌 **會叫的還是只會叫。轉 `done` 的條件今天一格都沒動。** | 🔴 **仍成立** | `apps/admin/src/lib/session/session.ts:175` · `apps/admin/src/lib/session/actor.ts:64-72` · `apps/admin/src/app/layout.tsx:152` |
| `⟦db-ROLLBACKPROSE222⟧` (`:816`) | 回退方案是 **222 段從來沒有人執行過的註解**;檔名帶 `down`/`rollback`/`revert` ⇒ **0**;`Supabase forward-only` **103** 命中;帶 `DROP ` **146** 支 | **三把尺今天全部重跑,而三個數【全部漂了】**:分母 **222 ⇒ 413** · 檔名帶 down/rollback/revert **0 ⇒ 仍 0**(⚪ 負對照 `zzzbogus20260828` ⇒ 0)· `Supabase forward-only` **103 ⇒ 85**(⚪ 負對照 `Supabase backward-only` ⇒ 0)· 帶 `DROP ` **146 ⇒ 224**。🔴 **最值得看的是 forward-only 那一格:分母幾乎翻倍而命中【變少】** ⇒ 📌 **那個刻意的慣例正在被稀釋 —— 新檔沒有跟著寫那句話** | 🔴 **仍成立**(而板上三個數字全部過期) | `ls supabase/migrations/*.sql \| wc -l` ⇒ 413 · `grep -rl "Supabase forward-only" supabase/migrations/ \| wc -l` ⇒ 85 · `grep -l "DROP " … \| wc -l` ⇒ 224 |
| `⟦b4-REPLAY1⟧` (`:1227`) | `supabase/migrations/` 從零 apply 重現不了正式庫;根因 = `product_fitments_effective` **被引用 7 支、被建立 0 支** | 🔴 **根因那一半【翻面了】**:今天版控裡**建立**它的有 **3 支** —— `20260712180000_m4b_pfe_ddl_backfill_into_version_control.sql` · `20260901170000_m4b_pfe_ddl_into_version_control.sql` · `20260902210000_m4b_pfeddl2_staging_and_sync_log.sql`;被引用 **7 ⇒ 28** 支。🔵 而板列自己標過「**2026-09-07 已有答案, 見本列末段**」⇒ **有人早知道,而開頭句沒改**。🛑 **主張本身我沒複驗** —— 重放要拋棄式 PG,`scripts/migrations-replay-from-zero.sh`(27,397 bytes)在而**我沒跑** | 🟡 **前提翻面(未來式變現在式)** —— 根因已進版控;**而「從零重放對不對得起來」未複驗** | `grep -lE "CREATE .*product_fitments_effective" supabase/migrations/*.sql` ⇒ **3 支** · 引用 28 支 · 板列開頭句 vs 末段自相矛盾 |
| `⟦db-LEDGERSHADRIFT⟧` (`:2408`) | 帳本**有**那一列而它記的是**另一份內容** ⇒ 閘報一支**已貼過的** migration 是 `PENDING`。歷次讀數:09-06 分母 336 ⇒ MISMATCH **10**;09-07 分母 376 ⇒ **15**,而那 15 的成因 = **12 支帳本自述刻意 · 2 支只改註解 · 1 支判不出 · 零支偷改** | 🔴 **今天複量(一律 `origin/dev`)**:資料列 **404** ⇒ `MATCH 397 / MISMATCH 7 / NOFILE 0 / 壞行 0`,四個數相加 = 404 ✅。**逐支分類**(找出帳本 sha 對應的歷史 blob,兩份都剝掉 `--` 與空行再比):**純註解漂移 6 支** · **真的有碼變 1 支** = `20260905210000`,而**它的帳本註記自己寫著第二欄是 `@貼時` 的 sha**(算的是 `~/pcm-mailbox/貼板-0905/` 那一份,不是 repo 檔)⇒ 📌 **「零支偷改」今天仍然成立。**(🟢 正對照 `20260906400000` 帳本命中 1 · ⚪ 負對照現造 `20260801129999` ⇒ 0) | 🔴 **仍成立**(而數字往好的方向走:10 ⇒ 15 ⇒ **7**) | `git show origin/dev:supabase/APPLIED.tsv` · 逐支比對腳本讀數見上 · MISMATCH 七支版本號列在 §二 |
| `⟦db-NOROLLBACKARTIFACT⟧` (`:2423`) | **【寫在 migration 檔裡的】可執行反向 SQL = 0 支**;末格訂正:回退產物住三個位置,當發分母 388、檔案來源撈得到 41 | 🔴 **「內嵌 = 0 支」今天原封成立**:尺三 `^-- ══ ROLLBACK` ⇒ **5 支**(與板上相同,而板列已逐支開檔證過**那 5 個區段全是註解、區段內非註解行 0**);**今天貼的四支逐支查 ⇒ 內嵌 ROLLBACK 區段各 `0`**。sidecar 涵蓋率今天 = **48 / 413 = 11.6%**(板上末格 41/388 = 10.6%)。關閉條件①那份「哪些 migration 必須有 down 腳本」的判準 **今天仍不存在**(全 `docs/` 掃該字面 ⇒ 只命中板子自己) | 🔴 **仍成立** | 見 §三-① |
| `⟦db-ACLGATEPROSEMISFIRE⟧` (`:2510`) | 一道閘**多報**、另一道**少報**,而它們是同一個受詞 ⇒「那道閘掃過了」不成立。關閉條件②:在①滿足前,本列與 `⟦auth-GRANTGATEBLIND⟧` **兩列都在** | **兩件都複現**:①閘檔 `scripts/acl-drift-gate.py` **最後一次改動是 2026-09-06**(`d3bc0ec7a`),**2026-09-07 起零 commit** ⇒ 📌 **板列記的「2026-09-09 線 `sync` 試修…已全部撤回, 檔案零改動」是【真的】,git 歷史證得出來** ②另一半 `⟦auth-GRANTGATEBLIND⟧`(`:726`)**態仍 `open`** ⇒ 兩列都在 ⇒ 關閉條件②的前提照舊 | 🔴 **仍成立** | `git log -1 --date=short -- scripts/acl-drift-gate.py` ⇒ `2026-09-06 d3bc0ec7a` · `git log --since=2026-09-07 -- scripts/acl-drift-gate.py` ⇒ **空** · `docs/launch-todo.md:726` |

---

## 二、「仍成立」的今天曝險

| 錨 | 今天的曝險 | 量的 / 未量 |
|---|---|---|
| `⟦b4-MGRENV1⟧` | 🛑 **未量,而且【結構上量不到】** —— 洞只在「旗標關著 + 舊格式票」那一格,而那顆值 **Vercel Type = Secret,連 Sean 本人也讀不到**;`vercel env ls` 在 `=1` 與 `=0` 兩個世界印同一個 `Encrypted`(板列自己記的)。板上唯一的正面證據是 `authorize.ts:90` 逐字「正式站 2026-08-25 起 `=1`(Sean 親口 + 登出再登入的行為驗證)」⇒ 📌 **那是 08-25 的行為驗證,不是今天的讀數。** 🔵 而今天唯一可行的量法 = **有人打開後台看那條警示帶在不在** —— 後台不能用 curl 驗(入口在 quote 站一律 429),所以那要人。**射程**:所有用 `authorizeManagerMutation` 的地方 = `staff-actions.ts` 4 處 + 死信重排 1 處 | **未量**(附得出缺哪一道檢查:①旗標今天的值 ②後台畫面上那條帶在不在) |
| `⟦db-ROLLBACKPROSE222⟧` | **413 支裡,檔名層級的回退產物 0 支**(慣例是 forward-only,那是刻意的);而 `Supabase forward-only` 那句宣告只剩 **85 / 413 = 20.6%** 的檔帶著 ⇒ 📌 **曝險不是「不能退」,是【那個刻意的決定正在從新檔上消失】** —— 下一個人開一支新檔,不會從檔裡讀到這是 forward-only | **量的**(85 / 413) · 🛑 **「那 222/413 段註解跑不跑得起來」= 未量**(那正是本列在講的,而它要真的跑) |
| `⟦db-LEDGERSHADRIFT⟧` | **今天 `origin/dev` 上 7 支對不上** ⇒ 部署閘會把這 7 支算成 `PENDING`。🔴 **而危險的方向板列已寫死**:「**沒有閘的人會去查, 而看到 `PENDING` 的人會去貼**」。七支逐個:`20260801120000` · `20260823030000` · `20260825130000` · `20260905090000` · `20260906200000` · `20260902180000`(以上 **6 支純註解漂移**)· 🔴 `20260905210000`(**唯一碼真的有變**,而帳本註記自述第二欄是 `@貼時` 的 sha) | **量的**(7 / 404,逐支分類過) |
| `⟦db-NOROLLBACKARTIFACT⟧` | **413 支裡有 sidecar 的 48 支 = 11.6%** ⇒ 📌 **另外 365 支出事,回頭的路要當場現寫。** ⚠️ 而板列自己說**這不是「要補 365 支」** —— 它要的是一份判準(哪些屬於「出事要能退」),而**那份判準今天不存在** ⇒ 🛑 **所以 11.6% 這個數字沒有分母可比,它今天不能拿來說「夠不夠」** | **量的**(48 / 413)· 🛑 **「該有的是幾支」= 未量,因為判準不存在** |
| `⟦db-ACLGATEPROSEMISFIRE⟧` | **多報那一半今天仍會咬人**:閘讀的是 git index,而它把「描述授權的散文」讀成授權 ⇒ 任何一顆 commit 只要 staged 的 migration 裡有一段解釋授權的文字,`pre-commit` 就擋整顆。🛑 **而它今天【正好在你們的路上】** —— 今天有四支 migration 進來,而閘檔從 09-06 起沒動過。⚠️ **我沒有實測它今天會不會紅** —— 要測得 `git add` 進 index 再跑,而**那會動到主樹的 index**(你交代過不碰)⇒ **我沒做,這是我沒驗的一格** | **量的**(閘檔未動 · 兩列都在)· 🛑 **「今天餵它一支真的會不會紅」= 未量(而且是我刻意不做的,理由是不碰你的 index)** |

---

## 三、🔴 本輪撿到的三件會改變分母本身的東西

### ① 🛑 **主視窗對 `⟦db-NOROLLBACKARTIFACT⟧` 的假設要撤回**

你寫的:「今天貼了四支 migration,每一支都有 rollback 檔在 `supabase/rollbacks/` ⇒ **那讓 2423 的『= 0 支』可能已經不成立**」。

🔴 **不成立 —— 那是兩把不同的尺。**
· 板列那個 `0` 的受詞逐字是「**【寫在 migration 檔裡的】可執行反向 SQL**」= **內嵌**。
· `supabase/rollbacks/*` 是 **sidecar**,而板列早就把它算在**第四把尺**上(末格訂正逐字「回退產物住在三個位置」)。
🔬 **逐支查過,四支全部內嵌 0**:
```
20260910070000  內嵌 ^-- ══ ROLLBACK 區段 = 0   sidecar 20260910070000-rollback.sql
20260910080000  內嵌 = 0                        sidecar 20260910080000_down.sql
20260910090000  內嵌 = 0                        sidecar 20260910090000_down.sql
20260910130000  內嵌 = 0                        sidecar 20260910130000_down.sql
🟢 全板尺三 ^-- ══ ROLLBACK ⇒ 5 支(與板上相同)  ⚪ 負對照 ^-- ══ ZZQBACK ⇒ 0
```
✅ **而那四支確實把 sidecar 涵蓋率往上推了**:41/388(09-08)⇒ **48/413(今天)**,10.6% ⇒ 11.6%。
📌 **⇒ 正確的說法是「sidecar 那把尺進步了 7 支」,不是「內嵌那個 0 不成立了」。**

### ② 🔴 **你我這把尺有一個會誤判的形狀:首個判定【被自己劃掉】**

`⟦db-LEDGERSHADRIFT⟧`(`:2408`)的擋欄**第一個** `⟨…⟩` 逐字是 `⟨未判(tidy · b 今晚第一次真事件, 狀態仍在動)⟩`,
**而同一格的末尾逐字**:`⛔ ~~⟨未判(tidy · b 今晚第一次真事件, 狀態仍在動)⟩~~ ⇒ 上面是 2026-09-08 的判定。`
⇒ 🛑 **它被自己劃掉並改判了,而我們的尺讀的是【劃掉的那一個】。**

🔬 **我量了全板**:首個判定在同一格被 `~~…~~` 劃掉的列 = **2 列**(755 列有錨資料列裡)。
📌 **⇒ 影響很小(2 列),而它證明【首字判定法不是無敵的】** —— 要準,得先剔掉被劃掉的那一個再取下一個。

### ③ ⚠️ **117 列的列身含逃脫管線 `\|`,而它會把欄位切多**

例:`:1227` 列身有 `ls supabase/migrations/*.sql \| wc -l` ⇒ `split('|')` 把那一列切成 10 欄。
🔵 **對【數判定】沒有影響**(態欄與錨欄在逃脫點之前,而擋欄的第一片仍以 `⟨` 開頭)。
🔴 **對【讀列尾】有影響** —— 我讀 `:1227` 的「擋欄」只拿到 649 字,而整列有 **10,626 字**,末段被切到別的欄位去了。
📌 **⇒ 判定數得準,而「列尾最新結論」不能用同一個切法拿。** 我這一輪讀 `:1227` 末段是另外用整列字串搜的。

---

## 四、⚠️ 撞到的索引列 / 重複列(照你交代:撞到再記,不專門掃)

1. **`:1227` 的【錨欄】不是只有錨** —— 它逐字是
   `#907 ⟦b4-REPLAY1⟧ 🔵 **2026-08-30 -08:確認本列與 #299 是同一件事,已把新量測寫進 #299 那一列…本列不重複記數字 —— 去讀 #299。**`
   ⇒ 📌 **這一列自陳「不重複記數字,去讀另一列」** —— 它是**指標列**,不是一件事的完整紀錄。**與你說的 `:1612` 同一族。**
2. **`:2411` `⟦db-PENDINGLISTUNSEEN⟧`** 與 `:2510` / `:726` 同一個受詞(閘的多報少報),三列互相指 —— 我只讀到它的存在,**沒有判它是不是重複列**。
3. 這兩件**只是回報,我沒改板、沒改態、沒動任何一格**。

---

## 五、🛑 我證不到什麼

· **`⟦b4-MGRENV1⟧` 那顆 env 今天的值** —— 結構上讀不到(Vercel Secret),而後台不能用 curl 驗。**要人開後台看那條警示帶。**
· **`⟦b4-REPLAY1⟧` 的主張沒有複驗** —— 我只查了根因那一半(已進版控),**沒有跑重放**(要拋棄式 PG)。
· **`⟦db-ACLGATEPROSEMISFIRE⟧` 我沒有實測閘今天會不會紅** —— 要測得 `git add` 進 index,而你交代過不碰主樹的 index。**這一格是我刻意不做的,不是忘了。**
· **`⟦db-ROLLBACKPROSE222⟧` 那 222(今天 413)段註解跑不跑得起來,我沒跑。**
· **全程沒有連正式庫。** 帳本比對比的是 **repo 檔案的 sha**,不是「正式庫裡真的跑了什麼」—— 📌 **一支帳本 MATCH 的 migration,不代表正式庫上那個物件今天長那樣。**

---

**主樹狀態(交還前複查)**:`git status --short` = `?? h3check.png`(沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
