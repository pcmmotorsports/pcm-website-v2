# `b9-` 族查證 · 2026-09-10(唯讀;主 repo 樹上零寫入、零 git add、零 commit)

> 查的人:新 session(主樹 `/Users/sean_1/pcm-website-v2` · dev · `af6fd59e1`),由主視窗 `-59` 派工。
> **範圍**:`docs/launch-todo.md` 裡 態 = `open`/`doing`、錨是 `⟦b9-…⟧`、而【擋欄的判定字面是 `⟨擋…⟩`】的列。
> **紀律**:結論四選一;「仍成立」多帶一格【今天的曝險】。證不到的寫證不到。

---

## 〇、先訂正範圍:不是 64,是 **5 列**

| 量什麼 | 讀數 | 量法 |
|---|---|---|
| `b9-` 字面出現次數 | (主視窗量到 64,我沒複量出現次數) | — |
| 板上有錨的資料列(全族) | **755** | split `\|` · 態欄在五值封閉集 · 第 3 欄有 `⟦…⟧` |
| 其中錨 = `b9-` 的 | **32** | 同上 |
| 其中態 = `open`/`doing` | **18** | open 18 · doing 0(另 done 8 · parked 5 · standing 1) |
| 🔴 **其中擋欄【首個判定】= `⟨擋…⟩`** | **5** | 見下表 |
| ⚠️ 首個判定 = `⟨未判…⟩` | **1** | `⟦b9-PPGATE1⟧` —— 不算擋也不算不擋,單獨列在 §六 |
| ⚪ 首個判定 = `⟨不擋…⟩` | 12 | 不在射程 |

🛑 **我第一發的尺錯了,寫出來不遮**:我先用「整列含 `⟨擋`」撈,得到 **6** 列 —— 多出來的是 `⟦b9-RLSHARDEN⟧`,
它的擋欄**開頭逐字是 `⟨不擋(account 判 2026-09-08)⟩`**,而那 20,284 字的列身**後面某處**又出現 `⟨擋`。
📌 **一把「整列 grep」的尺,會把一列【已經被判不擋】的列算進待辦。** ⇒ 改成只讀擋欄的**第一個** `⟨…⟩` 判定。
⚪ 正對照:同一把尺撈得到 5 列真的帶擋 · 負對照:現造字面 0 命中。

---

## 一、五欄表(一列一行)

| 錨 | 板上說什麼 | 今天量到什麼 | 結論 | 證據 |
|---|---|---|---|---|
| `⟦b9-ACLDRIFT5⟧` (`:674`) | 誰欄:**🛑 不要派 —— 卡 Sean**(等 `Q-權限偷改偵測` 甲/乙);事欄:路⑤ dashboard/SQL Editor 手動改權限沒人守 | **擋工理由已被推翻兩次而板上沒跟上**:①`docs/launch-status-20260906.md:211` 逐字「**你已答甲**,偵測器建好在跑」②`docs/evidence/2026-09-10-acldrift5-…md`(**今天**)逐字複量:題幹/甲/乙/代價/推薦**五樣全在** `~/pcm-mailbox/端Sean-0905早上佇列.md:424`。而**事欄那一半仍成立**(路⑤ 是結構性的) | 🔴 **本列描述有誤(受詞已變)** —— 擋的是「沒人把題端上去」,不是「題不存在」 | `docs/launch-status-20260906.md:211` · `docs/evidence/2026-09-10-acldrift5-那個擋點我兩天前就推翻過了.md:44-46,86-92` · `docs/plans/2026-09-09-acl-drift-and-provenance-plan.md:52-58` |
| `⟦b9-SRVMIN⟧` (`:861`) | 三道憑證(payment/email/cron)射程量過了,而**沒有做任何 `REVOKE`**,沒有人動手 | **列尾自己已帶 2026-09-10 查證章**:`⟨已查證 2026-09-10 · 仍成立 · plan 在 docs/plans/2026-09-10-srvmin-first-revoke-plan.md · 等 Sean 批⟩`。我複核 plan 檔**存在**(14,343 bytes · Sep 10 11:28) | 🟡 **仍成立**(而卡點已從「沒人動手」變成「plan 已寫好、等 Sean 批」) | `docs/launch-todo.md:861` 列尾 · `docs/plans/2026-09-10-srvmin-first-revoke-plan.md`(`ls -l`) |
| `⟦b9-REFUNDNUM1⟧` (`:1136`) | 退款畫面數字要「顯示函式一起改」(B),Sean 未答;關閉條件=「把 `manual_failed` 判準收進單一處」,而列尾逐字「**後者沒有人在守**」 | 🔴 **「沒有人在守」這半條件已消失一半、而另一半仍成立**:`scripts/manual-failed-pairing-gate.py` **今天存在**(12,101 bytes),認的就是錨字面 `⟦b9-REFUNDNUM1⟧`(`:93 PAIRING_MARK`),並已被 `scripts/migration-static-checks.sh:1061` 呼叫。🛑 **而它【沒有掛在 `.husky/` 任何一道上】** ⇒ 沒有任何自動載體會跑它 | 🟡 **仍成立**(閘寫好了而沒接上;判準仍分散在兩支函式) | 閘檔 `scripts/manual-failed-pairing-gate.py:8,93` · 呼叫點 `scripts/migration-static-checks.sh:1061` · `grep -rn "manual-failed-pairing\|migration-static-checks" .husky/` ⇒ **0 命中**(⚪ 正對照同尺找 `acl-drift-gate.sh` ⇒ 2 命中) |
| `⟦b9-PROBESCHED⟧` (`:1178`) | 曝露探針 `scripts/probe-schema-exposure.sh` 沒有人定期跑,只在有人想到時跑一次;提案定稿方向乙(只排 12 個 HTTP 格) | **今天仍然沒有任何排程載體**:`vercel.json` **沒有 `crons` 這個 key**;`.github/workflows/` 只有 `ci.yml`/`e2e-prod.yml`/`rpm-sync.yml`,**三支都沒提到它**;`~/Library/LaunchAgents/` 裡 pcm 系 plist 沒有一支是它。全 repo 唯一非文件命中是 `package.json:163` 的 `--selftest` 登錄(那是清單,不是排程) | 🔴 **仍成立** | `python3 -c "json.load(open('vercel.json')).get('crons')"` ⇒ `NO crons key` · `ls .github/workflows/` · `grep -rn probe-schema-exposure`(非 docs 命中只有 `package.json:163`) |
| `⟦b9-UNTRACKEDGRANT⟧` (`:2506`) | `pcm_readonly` 在版控外被建、`cron` schema 的 `USAGE` 是 postgres 明確授的也不在版控 ⇒ **拿版控從零重放,建不出這個世界** | **兩半今天分開了**:①**角色那半已進版控** —— `20260905160000_…_pcm_readonly_role_and_grants_into_version_control.sql:131-149` 有 `CREATE ROLE pcm_readonly NOLOGIN` + `GRANT USAGE ON SCHEMA public`(⚠️ 而它建的是**空殼**,屬性不保證等於正式庫)②🔴 **`cron` 的 `USAGE` 那半查無** —— 全 `supabase/migrations/` 找 `SCHEMA cron` ⇒ **1 命中,而那一處是註解**,逐字說 `postgres` **發不出**這道 GRANT(schema 擁有者是 `supabase_admin`) | 🔴 **仍成立**(而板上的措辭要收窄:不是「角色建不出來」,是「**那道 cron USAGE 建不出來,而且沒有任何尺看得到它**」) | `20260905160000_…:131-149,186-201` · `grep -rn "SCHEMA cron" supabase/migrations/` ⇒ 唯一命中 `20260905140000_…:312`(註解) · 偵測射程見 §三 |

---

## 二、「仍成立」那三列 —— 今天的曝險

| 錨 | 今天的曝險 | 是量的還是未量 |
|---|---|---|
| `⟦b9-SRVMIN⟧` | **3 張非目錄表**(`customer_addresses` · `customer_vehicles` · `customer_wallet_ledger`)對 `service_role` 是 `SIUDTRGM` **八種全開** ⇒ 帳簿 `customer_wallet_ledger` **今天可以被 `TRUNCATE`**。🔵 **而應用端沒有人在走那條路**:`TRUNCATE <識別字>` 形狀全 repo 69 處,**`apps/` 0 處(掃 1443 檔)· `packages/` 0 處(掃 326 檔)**,69 處全在 `supabase/`(30)與 `scripts/`(43) | **量的**(讀數在列尾,2026-09-09 該列作者量;我**沒有複量**,照抄並標來源) |
| `⟦b9-REFUNDNUM1⟧` | **接觸面 = 未來每一支碰到那兩支函式的 migration**。今天的實際咬合次數:`20260909090000` 是**唯一**一支自己貼了配對句的檔,而它**已 apply**(`APPLIED.tsv:605` · 2026-09-10 貼板)。⇒ 📌 **曝險不是「今天算錯錢」,是「下一個改單邊的人不會紅」** —— 而現在連那個「不會紅」都不是閘,是靠人自己記得跑 `migration-static-checks.sh` | **量的**(閘的接線狀態) · 🛑 **「今天畫面有沒有算錯」= 未量**(要唯讀正式庫比兩支函式的回值,我沒做) |
| `⟦b9-PROBESCHED⟧` | **距離最後一次實跑 10 天**(板列逐字:2026-08-31 `both` 實跑 rc=0 · 30 PASS · 0 FAIL),期間執行次數 **0** ⇒ 那 10 天內任何 schema 曝露變更是**零訊號**。🔴 而方向乙就算接上也**只涵蓋 12 個 HTTP 格,另外 3 個 psql ACL 格不在射程** | **量的**(排程載體 0)· 🛑 **「這 10 天有沒有真的多曝露一個 schema」= 未量**(那正是這一列在講的:沒有人在量) |
| `⟦b9-UNTRACKEDGRANT⟧` | **未進版控的 GRANT 數 = 1**(`cron` schema USAGE → `pcm_readonly`)。🔴 **而它同時在偵測器的盲區裡**:`pcm_acl_digest()` 只有 8 個族(`ROLE`/`REL`/`FN`/`FNCFG`/`VIEWOPT`/`STORAGEACL`/`DEFACL`/`POL`),**沒有 schema 層 ACL(`nspacl`)這一族**;而 `DEFACL` 那族的射程逐字 `WHERE n.nspname IN ('storage','public')` ⇒ **`cron` 兩邊都碰不到** | **量的**(族名 8 個逐個列出 · 射程 `WHERE` 逐字) |

---

## 三、🔴 一個順手撿到的、跨兩列的東西

`⟦b9-ACLDRIFT5⟧` 09-09 貼上的那支 `20260909060000`(APPLIED.tsv:602 · 貼板 112)**把 `pcm_readonly` 加進偵測器的 `REL` 族**
(檔頭逐字:「一個 `GRANT … TO pcm_readonly` 即使【永久留著】也不會叫」)。
🛑 **而那只補了【表級】** —— `⟦b9-UNTRACKEDGRANT⟧` 講的那道 grant 是 **schema 級**,
而偵測器**根本沒有 schema 級這一族** ⇒ 📌 **兩列讀起來像同一件事被修好了,實際上補的是不同的層。**
⇒ 🔵 建議:這一句寫回 `⟦b9-UNTRACKEDGRANT⟧`,否則下一個人看到「ACLDRIFT5 補了 pcm_readonly」會以為它被涵蓋。

---

## 四、🛑 我證不到什麼

· **證不到 Sean 09-09 之後有沒有答過 `⟦b9-REFUNDNUM1⟧` 那一題。** 我的尺只掃 `~/pcm-mailbox/*.md`(最新一支 `Sep 9 11:11`,同尺找 `refundable_remaining`/`還能退多少`/`先扣掉` ⇒ 命中的全是 08 月舊檔;⚪ 正對照同組檔找 `拍甲`/`拍乙` ⇒ **246 支** ⇒ 尺是活的;⚪ 負對照現造字面 ⇒ 0)。**他在主視窗答的話,我這把尺咬不到。**
· **證不到那 5 列裡任何一列的正式庫現況。** 我全程只讀 repo 檔案,**沒有連正式庫**。列尾那些「唯讀量到」的讀數我**照抄並標來源**,沒有複量。
· **證不到 `⟦b9-SRVMIN⟧` 的 plan 內容對不對** —— 我只證了那支檔**存在**,**沒有讀它**。
· **`⟦b9-PROBESCHED⟧` 我沒有跑那支探針**(它會對正式站發 HTTP;而板上逐字說它「從未對正式站跑過」)。

---

## 五、⚠️ 順手看到的、我沒動的板子問題

1. **`⟦b9-PROBESCHED⟧` 這件事在板上有【三列】**:`:1178` 本列 · `:927` `⟦0e-PROBENOSCHED⟧`(態 open,標題同義) · `:781` **一列沒有錨**而事欄逐字也在講同一支探針。
2. **`⟦b9-ACLDRIFT5⟧` 有重複列** —— `:674` 的第 6 欄自己寫著「⛔ **[重複列 · 正本 …]**」,而該格逐字說來源列合併做完了、**來源沒刪**(`difflib` 比出共同片段 4201 字元 = 99.9%)。
3. 這兩件都**只是回報,我沒改板、沒改態、沒動任何一格**。

---

## 六、`⟨未判⟩` 那一列(不在射程,列出來讓你決定)

`⟦b9-PPGATE1⟧`(`:943`)擋欄逐字 `⟨未判(tidy · a deploy-order-gate 在 SSH force-push 下失不失效未驗, 政策明禁對 GitHub 實測)⟩`
⇒ 📌 **它不是「還沒查」,是【查它的唯一方法被政策禁掉】。** 那不是一列可以靠查證推進的列。**我沒有碰它。**

---

**主樹狀態(交還前複查)**:`git status --short` = `?? h3check.png`(那顆我沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
