# `front-` 族查證 · 2026-09-10(唯讀;主 repo 樹上零寫入、零 git add、零 commit)

> 查的人:新 session(主樹 `/Users/sean_1/pcm-website-v2` · dev · `af6fd59e1`),主視窗 `-59` 派工(第三輪)。
> **尺**:態 `open`/`doing` · 錨 `⟦front-…⟧` · **擋欄【第一個】`⟨…⟩` 判定是 `⟨擋…⟩`**。分母我自己數。

---

## 🎯 總結行(你要的那一行)

```
本族 帶擋 2 列 · 其中【今天已可翻不擋】0 列 ⇒ 本族對那個 78 的【應減】= 0
                 ⚠️ 另有 1 列是【待你端 Sean 確認】的應減候選(⟦front-CATALOGPRICEGENERALONLY⟧)
🔴 而本族真正的發現不在那 2 列 —— 見 §三:一個【第四種判定字】讓最靠近客人的那一列從你的尺上消失
```

---

## 〇、分母

| 量什麼 | 讀數 |
|---|---|
| 錨 = `front-` 的(全態) | **14** ⇒ `open` 7 · `done` 5 · `parked` 2 · `doing` 0 · `standing` 0 |
| `open`+`doing` | **7** |
| 🔴 擋欄首判 = `⟨擋…⟩` | **2** |
| 擋欄首判 = `⟨不擋…⟩` | **4** |
| 🔴 擋欄首判 = **`⟨記著⟩`** | **1** ← **第四種字,不在你我假設的三值裡** |
| `⟨未判…⟩` / 完全沒判定 | **0** / **0** |

🔵 **本族是全板最小的一族之一(14 列),而它是客人看得到的那一族。**

---

## 一、五欄表(帶擋的 2 列)

| 錨 | 板上說什麼 | 今天量到什麼 | 結論 | 證據 |
|---|---|---|---|---|
| `⟦front-CATALOGPRICEGENERALONLY⟧` (`:1027`) | 目錄頁的**篩選 · 排序**兩處都用**一般價**算,而經銷客人畫面上看到的是**經銷價**;根因是資料源 `products_list_public` 只有 `price_general`。⟨擋⟩ 的意思是「擋在 `⟦b4-DEALERSIGNUPUNSEEN⟧` 前面 —— 上游同步上線前本列必須先落地」 | 🔴 **技術主張今天【原封成立】,而且新的那支 RPC 也沒修它**:`20260908010000_m4b_q74_dealer_catalog_rpc.sql` 建的 `search_catalog_by_vehicle_dealer` —— 篩選 `:197-198` 逐字 `p.price_general >= p_price_min` / `<= p_price_max`;排序 `:247-249` 與 `:299-301` 三個 `p_sort` 分支**全部**是 `f.price_general` / `pg.price_general`;寫死價帶 `:226,232-234,238-245` 也是 `price_general`。⇒ 📌 **新 RPC 只換了【顯示】的來源(讀 `products_list_dealer`),【篩選與排序】仍然吃一般價。** 而該檔 `:26` 自己逐字寫著「正式庫今天 `tier='store'` **0 人**…**行為在正式庫上驗不到**」 | 🟡 **仍成立** —— ⚠️ **而它是應減候選,要你端 Sean**:見 §二 | `supabase/migrations/20260908010000_m4b_q74_dealer_catalog_rpc.sql:197-198,226-249,299-301,338-339` |
| `⟦front-ACLSNAPSHOTAGE⟧` (`:2505`) | `supabase/acl-snapshot.tsv` 可以無限期變舊而**沒有任何一發會紅**;不是缺一道閘,是**閘的觸發條件把使用者排除在外**。列上自標:判快照年齡**只能用 `git log`**,`mtime` 是那棵樹的屬性 | 🔴 **今天複量,而且數字比板上大**:`git log -1 -- supabase/acl-snapshot.tsv` ⇒ **2026-09-05**(`182193463`)⇒ **今天 2026-09-10 = 落後 5 天**(板上記的是「三天內沒有任何東西提醒過任何人」,而它自己標那是**下界**)。**觸發條件也複現了**:`acl-digest-parity.sh` / `acl-snapshot.sh` 在 `.husky/` **零命中**,只掛在 `package.json:111-112,125` 的**檔案觸發式登錄**(第三條逐字綁 `supabase/migrations/20260905140000_…sql`)⇒ 📌 **不碰那幾支檔的人,一輩子不會觸發它** | 🔴 **仍成立**(而 3 天 ⇒ 5 天) | `git log -1 --date=short -- supabase/acl-snapshot.tsv` ⇒ `2026-09-05 182193463` · `grep -rn "acl-digest-parity\|acl-snapshot.sh" .husky/` ⇒ **0** · `package.json:111,112,125` |

### 曝險

| 錨 | 今天的曝險 | 量的 / 未量 |
|---|---|---|
| `⟦front-CATALOGPRICEGENERALONLY⟧` | **今天 0 個客人會踩到** —— `tier='store'` 的會員 **0 人**(該 migration `:26` 逐字,而 `docs/reviews/…十二列查證.md` §9 2026-09-10 唯讀複量也是 `general 15 · store 0`)。🔴 **而「0 人」是今天的值不是性質** —— 同一份 §9 量到 `products.price_by_tier->'store'` **差價 9 筆**(全部 2026-09-09 更新,其中 **5 筆經銷價比一般價貴**,最大 `BMS1K2K-18` 4,600 ⇒ **12,800**)⇒ 📌 **資料已經開始有差價了,只是還沒有人登入來看** | **量的**(照抄 §9 讀數並標來源;我沒複量) |
| `⟦front-ACLSNAPSHOTAGE⟧` | **落後 5 天,而 5 天內 0 次提醒**。🛑 **而「以前落後幾天」沒有人量過**(列上自標是下界)⇒ 我也沒量 —— 要量得逐個 commit 走 `git log` 的間隔,那是另一件事 | **量的**(5 天)· 🛑 歷史最大落後 = **未量** |

---

## 二、⚠️ `⟦front-CATALOGPRICEGENERALONLY⟧` 是應減候選,而**判它的人不是我**

**Sean 2026-09-09 拍甲,逐字**(memory `project_0909-dealer-price-whole-package-after-launch`):
> `A: 甲 = 現在不動, 整包排上線後, 跟 B2B 子網域一起做`

**而板上今天的狀態是分裂的 —— 同一包三列,三種待遇:**

| 錨 | 態 | 判定 | 那個拍板進去了嗎 |
|---|---|---|---|
| `⟦front-FEATUREDRAILGENERALPRICE⟧` (`:1028`) | **`parked`** | — | ✅ **進去了**(全板唯一一處出現「B2B 子網域」四個字) |
| `⟦front-CATALOGPRICEGENERALONLY⟧` (`:1027`) | `open` | `⟨擋·記著⟩` | 🔴 **沒有** |
| `⟦b4-DEALERSIGNUPUNSEEN⟧` (`:1026`) | `open` | `⟨擋(tidy 判 ③⑤)·讀不出來⟩` | 🔴 **板上沒有,而 `docs/reviews/…十二列查證.md` §10 有** —— 該節逐字「**[2026-09-10 · 現在不修 —— 依 Sean 2026-09-09 拍甲]**…歸【上線後那一包】的規格,不在今天修」 |

🔬 **量法**:全板掃「整包排上線後」/「整包不動」⇒ **0 命中**(⚪ 正對照 同板掃「拍甲」⇒ **66**、「經銷價」⇒ **31**、「上線後」⇒ **41** ⇒ 尺是活的;⚪ 負對照現造字面 ⇒ 0)。

🛑 **⇒ 我不自己判它該不該減。** 理由兩個:
1. **「整包」的邊界是產品範圍,不是我能拍的** —— 這一列在不在那個包裡,要 Sean 說。
2. 這一列的 `⟨擋⟩` **不是擋上線,是擋另一列**(`⟦b4-DEALERSIGNUPUNSEEN⟧`)⇒ 若那一列跟著整包移到上線後,本列的擋自然休眠;而 `b4-` 是**窗 B 的域,我只讀不判**。

📌 **⇒ 這一題是你端 Sean 的:「經銷價整包排上線後」這句,涵不涵蓋 `:1026` 與 `:1027` 這兩列?**
· 涵蓋 ⇒ 兩列跟 `:1028` 一樣翻 `parked` ⇒ **那個 78 一次減 2**(其中 1 列是窗 B 的域)。
· 不涵蓋 ⇒ 兩列留著,而 `:1027` 的技術主張今天成立,我已複量在 §一。

---

## 三、🔴 本族真正的發現:**一個第四種判定字,讓最靠近客人的那一列從你的尺上消失**

`⟦front-VEHFACETSILENTDROP⟧`(`:835`)—— 事欄逐字:

> 🔴🔴 **顧客站「找我的車的零件」有【四種】壞法, 而其中三種畫面看起來完全正常** —— 認不得的車款不是回 0、不是報錯, 是**靜默降級**成別的篩選

**它的擋欄第一個判定逐字是 `⟨記著⟩`。**
⇒ 📌 **不是 `⟨擋⟩`、不是 `⟨不擋⟩`、也不是 `⟨未判⟩`** ⇒ 🛑 **你我的尺數 `⟨擋⟩`,它不在裡面;數 `⟨不擋⟩`,它也不在。它兩邊都不算。**

🎯 **而它是本族最靠近客人的一列,讀數是 2026-09-08 對【正式站】唯讀 GET 量的**(逐字附在列上):
```
① 整串認不出 ⇒ 落關鍵字, 有結果而【沒有按他的車過濾】, 零警告
   search=Trident 660   ⇒ 58 件 · 頁面 grep vehicle= 命中 0
② 只認出一個片段而那片段是錯的車款 ⇒ 0 件
   search=panigale v4 腳踏 ⇒ 落 vehicle=ducati:v4 ⇒ 0 件
   🟢 正對照 同分類不帶車款 ⇒ 1662 件 ⇒ 那個 0 是車款那一格造成的
③ 車款假 + 品牌真 ⇒ 靜默降級成只按品牌
   vehicle=ducati:nosuchmodel999 ⇒ 5433 件 = 只給品牌時逐字相同 ⇒ 車款那格被丟掉
④ 假車款是【客人選單裡的一個選項】
   vehicle=ducati:panigale-v4 ⇒ 1156 件 · panigale-v4-s ⇒ 1110 件 · 而 ducati:v4 ⇒ 2 件
```

**我今天能證的(唯讀本機)**:那支共用解析 `apps/storefront/src/lib/vehicle-url.ts` **最後改動是 2026-08-08**(`9d24c9667`)⇒ **比那次量測(09-08)還早一個月** ⇒ 📌 **成因沒有被動過。**
🛑 **我沒有重跑那四發 `curl`** —— 那是對正式站發請求,而 Sean 正要自己走一遍那個站;要不要複量請你決定(memory `project_0910-sean-authorizes-production-site-scanning` 記著他 09-10 拍丙授權掃正式站,所以**授權在,是我沒跑**)。

🔵 **而它的第④種今天有 Sean 的裁定了**:memory `project_0909-wrs-fake-models-deferred` 逐字「**Sean 拍 Q2 = 乙:WRS 那 46 列假車款【先擱著】,推完顧客站再說**」·「**『刪掉那 46 列』這個選項已經死了,不要再端一次**」。
⇒ 📌 **所以 ④ 有主、①②③ 沒有** —— 而整列掛在 `⟨記著⟩` 底下,**四種一起看不見**。

---

## 四、其餘 4 列(`⟨不擋⟩`,只列不查)

| 錨 | 判定 | 一句話 |
|---|---|---|
| `⟦front-PLACEHOLDERUNGUARDED⟧` `:2373` | `⟨不擋(tidy 判)⟩` | 2026-09-04 已改在資料出口解決,站內 6 處一次蓋掉 |
| `⟦front-DEADPLACEHOLDER⟧` `:2375` | `⟨不擋(tidy 判)⟩` | 只剩寫入側一格,而那格在等 Sean 拍上架方式 |
| `⟦front-RECOENGINESTOPPED⟧` `:2463` | `⟨不擋(shop 判:它現在不發生)⟩` | 推薦區塊停了而沒有人知道為什麼;列上自標兩個候選成因**不互斥** |
| `⟦front-PPSHELLXVSDESIGN⟧` `:2469` | `⟨不擋(tidy 判 · 視覺一致性)⟩` | 留白 40 vs 稿 12/32;**不派 · 等 Sean 肉眼** |

🔵 **`:2469` 與我上一輪報的 `⟦f3-PDPSKUSTATIC⟧` 是同一種**:等 Sean 的眼睛,不是等工。
⚠️ **而兩列的判定字不同**(這裡 `⟨不擋⟩` · 那裡 `⟨擋⟩`)⇒ 📌 **「等 Sean 肉眼」這件事在板上沒有一致的判定。** 只回報,我沒判。

---

## 五、🛑 我證不到什麼

· **沒有連正式庫,也沒有對正式站發任何請求。** `:1027` 的「0 人 / 差價 9 筆」與 `:835` 的四發 `curl` 讀數,我**照抄並標來源**,一發都沒複量。
· **`:835` 的四種形狀我只證了「成因那支檔沒被動過」**,沒證「今天畫面還是那樣」。
· **`⟦b4-DEALERSIGNUPUNSEEN⟧` 我只讀了它的態與判定字**,沒查它 —— 那是窗 B 的域。
· **`:2505` 的「以前落後幾天」我沒量。**
· **§四那 4 列我沒有查證**,只讀了判定字與標題。

---

## 六、⚠️ 順手看到、我沒動的

1. 🔴 **判定字不是三值** —— 本族出現 `⟨記著⟩`(`:835`)與 `⟨擋·記著⟩`(`:1027`)兩種複合/新字。⇒ 📌 **任何「數 `⟨擋⟩` 有幾列」的尺,都要先講清楚 `⟨記著⟩` 算哪一邊。** 我這一輪把 `⟨擋·記著⟩` 算進(首字是「擋」)、把 `⟨記著⟩` 排除(首字不是),**而那是我選的,不是板上定義的。**
2. `⟦front-DEADPLACEHOLDER⟧` 列尾自己記著另一列「態 `done` 而誰欄開頭仍是『等人:主視窗排一片』」⇒ 那是 `board-state-consistency.py` 檢查 ⑥ 在數的那一族(它不判紅,只列給人看)。
3. 都只是回報,**我沒改板、沒改態、沒動任何一格**。

---

**主樹狀態(交還前複查)**:`git status --short` = `?? h3check.png`(沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
