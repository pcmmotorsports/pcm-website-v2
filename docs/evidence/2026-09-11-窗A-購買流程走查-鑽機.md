# 窗 A 購買流程走查(鑽機)· 2026-09-11 · 走到「送出訂單」為止

> 目標:找商品 → 商品頁 → 購物車 → 結帳 → 付款 → 訂單信,列出客人會卡住 / 錯字 / 錯數字的地方。只列,不修。
> 前半段(到購物車)12:4x 走,後半段 13:0x 接回。**送出訂單之後三格在鑽機上走不到**,見文末〈結論〉。
>
> 環境:`~/pcm-shop` HEAD `d34847fa4`,`scripts/storefront-probe/up.sh`(108 件種子商品)。
> TapPay 刻意用假金鑰起(`TAPPAY_ENV_FILE=/nonexistent-pcm-tappay.env`),不對 TapPay sandbox 發任何請求。
> 截圖:`/private/tmp/claude-502/-Users-sean-1-pcm-website-v2/34bcbb3f-5808-4eb2-be08-637fd5aa4587/scratchpad/walk/`(session 暫存,會消失)。

## 走到的格子

| # | 哪一步 | 看到什麼 | 應該是什麼 | 嚴重度 |
|---|---|---|---|---|
| 1 | 首頁 N°04 服務宣言 | 「下單之後,」「全站皆為原廠正品,部分品牌正式代理、部分平行輸入,杜絕仿品風險。」「不只賣您部品,是陪您騎一輩子的夥伴。」—— **半形逗號**(`HomeStatement.tsx:31, :80, :108`) | 設計稿用全形「，」(`design-reference/components/HomePage.jsx:228, :235`) | 字不對 |
| 2 | 購物車 車款欄 | 「給哪台車用(套用全部商品)」「建議填寫車款,方便我們為您確認商品是否適用」—— 半形括號與逗號(`CartView.tsx:301-302`) | 設計稿沒有這一欄;我判斷應該跟 #1 同一個規矩用全形 | 字不對 |
| 3 | 首頁 N°03 部品分類 | 小字「依件數排序取前 11 名 · 全站共 17 類」(`CategoryGrid.tsx:272`) | 設計稿是一個連結「查看全部 11 類」(`HomePage.jsx:172`);我判斷「取前 11 名」讀起來像工程說明,不像給客人看的字。碼上的註解說刻意現算,不抄稿上的數字 | 字不對(低) |
| 4 | 選車後的商品列表 | 側欄「件數暫時無法顯示」 | 鑽機限定:`/api/catalog/facet-counts` 在鑽機回 503(16 路並發打爆鑽機 proxy,`fetch failed`)。**正式站要另外看** | 走不到(鑽機) |
| 5 | 商品頁 · 無規格商品 | 按「加入購物車」⇒「這件商品目前不能單獨購買,請聯繫客服 LINE 協助訂購。」 | 刻意的(`ProductInfo.tsx:243-262`,Sean 2026-08-31 拍「不賣」)。鑽機種子 108 件裡只有 k=3 那 24 件有規格,所以大多數商品在鑽機上買不了。**正式站有幾件沒規格:未查** | 不是 bug(記下) |
| 6 | 首頁 N°03 | console 警告:5 個分類上了首頁卻沒有 icon,會變純文字 chip(操控部品、車殼外觀、引擎部品、電子系統、車架) | 這 5 個是鑽機 migration 帶進來的分類名;**正式站的分類有沒有同樣情況:未查** | 會錯(待正式站確認) |

## 走過而沒問題的

- 搜尋「水箱護網」25 件;改排序、點分類都保留關鍵字(本日 `4b8c13a3a` / `d33a2dff6` 已驗)。
- 首頁選車 Yamaha → YZF-R7 → 2021 ⇒ `?vehicle=yamaha:yzf-r7:2021`,72 件,標籤三顆都在。
- 首頁分類「外觀與後視鏡16」⇒ 列表 16 件,數字對得上。
- 商品頁換顏色 ⇒ 料號跟著變(`AKRAPOVIC-3-BLK` → `-BLU`);價格、「含稅 · 滿 NT$ 5,000 免運」都在。
- 加入購物車 ⇒「已加入購物車 · 車上共 1 件」,右上角徽章 1。
- 購物車:小計 2,100、運費 100、「再買 NT$ 2,900 享免運」(5,000 − 2,100 = 2,900 ✓)、總計 2,200 ✓。

## 後半段:購物車之後(2026-09-11 13:00 接回)

> 環境同上,另加 `BANK_TRANSFER_CHECKOUT_ENABLED=true` 起鑽機(不加的話付款只有信用卡,見 #7)。
> 🔴 **鑽機另外補了一件**:`CREATE EXTENSION pgcrypto SCHEMA extensions`(鑽機只裝了 pg_trgm;
> Supabase 正式庫預設就有 pgcrypto)。補完之後又撞到下一個缺口(#8),**到那裡就停,不再補**。
> 鑽機已 down.sh,那個補丁跟著資料目錄一起刪了。

**擋路 / 會錯 排前面:**

| # | 哪一步 | 看到什麼 | 應該是什麼 | 嚴重度 |
|---|---|---|---|---|
| 7 | 付款方式 | 沒設 `BANK_TRANSFER_CHECKOUT_ENABLED=true` ⇒ **只有「信用卡付款」一個選項**(`lib/payment/bank-transfer-flag.ts`,`CheckoutStep2.tsx:341`) | Sean 拍過「匯款結帳:甲 = 要, 上線前做完」(`CheckoutStep2.tsx:326` 引用)。**正式站 Vercel 有沒有設這個變數:未查**(只能看名稱,不能看值) | **擋路(若正式站沒設)** |
| 8 | 送出匯款訂單 | 畫面「付款失敗,請稍後再試或聯繫客服 LINE」。server log `[checkout] create_order 簽章不符 code 42883`;pg.log 真因依序是 ① `extensions.gen_random_bytes(integer) does not exist`(補 pgcrypto 後過了)② `column "price_tax_mode" of relation "orders" does not exist` | 鑽機限定:136 支 migration 套不上,orders 表跟正式庫不一樣。**下單成功頁、outbox、訂單信在鑽機上都走不到** | 走不到(鑽機) |
| 9 | 送出失敗的字 | 客人選的是 **ATM 轉帳**(還沒付錢),失敗時卻寫「**付款**失敗」(`charge-actions.ts:88` 共用一句) | 我判斷:匯款那條失敗應該說「訂單沒有建立成功」,不然客人會以為被扣款 | 會錯(字) |
| 10 | 付款 · 信用卡 | 「付款模組暫時無法使用,請稍後再試或聯繫客服 LINE」 | 鑽機限定:沒有 TapPay 替身;這次刻意不帶 sandbox 金鑰,server 也缺 `TAPPAY_PARTNER_KEY`(`lib/payment/composition.ts:75` 直接 throw) | 走不到(鑽機) |
| 11 | 結帳區塊編號 | N°01 收件資料 → N°02 配送方式 → **N°04** 付款方式 → N°05 確認訂單,**沒有 N°03** | N°03 是發票,Sean 2026-08-08 拍板先藏(`CheckoutStep2.tsx:170-176`)⇒ 編號跳號是藏的副作用 | 字不對(低) |

**走過而沒問題的:**

- 購物車「前往結帳」沒登入 ⇒ 轉 `/login?next=/checkout`,「結帳前請先登入，購物車會幫您留著。」登入後回到結帳,購物車還在。
- 新增收件地址(姓名 / 手機 / 地址 / Email)⇒ 存好,標 DEFAULT(設計稿 `CheckoutPage.jsx:229` 也是 DEFAULT)。
- 金額:兩件各 2,100 ⇒ 小計 4,200、運費 100、應付 4,300 ✓;第二步摘要跟第一步一致。
- 確認訂單列出兩件、各自顏色(藍色 / 黑色)、數量、單價 ✓。
- 沒勾同意條款 ⇒「請先閱讀並同意服務條款與隱私政策」+「還有 1 個項目需要確認,已在上方標示」,擋得住。
- 選 ATM 轉帳時按鈕仍寫「確認付款 NT$ 4,300」—— 設計稿同字(`CheckoutPage.jsx:600`),不列。

**訂單信(只讀碼,沒寄、沒打 `/api/cron/*`):**

- 匯款單成立信:主旨「訂單 {編號} 已成立,請於期限內完成匯款」(`order-email-assembly.ts:315`),內文 `sweep-email-outbox.ts:135-156`。
  文案是 Sean 2026-09-06 核可的那一份(`docs/specs/2026-09-06-bank-order-created-email-copy.md`),測試逐字比對 ⇒ 不列。
- 真的產生 outbox 列、真的組出一封信:**走不到**(#8,訂單建不起來)。

## 結論:鑽機走得到哪裡

```
找商品 ✅ → 商品頁 ✅ → 購物車 ✅ → 結帳資料 ✅ → 選付款 ✅ → 送出訂單 ⛔(鑽機 schema)→ 成功頁 ⛔ → 訂單信 ⛔
```
⇒ 「送出訂單」之後三格,**只有正式站(或 schema 跟正式庫一致的環境)走得到**。
⇒ Sean 明天自己走的時候,這三格是第一次有人真的看到。

截圖:`walk-07` 到 `walk-14`(同一個 scratchpad 目錄)。
