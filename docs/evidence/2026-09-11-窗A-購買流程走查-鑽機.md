# 窗 A 購買流程走查(鑽機)· 2026-09-11 · 🟡 走到一半,存檔

> 目標:找商品 → 商品頁 → 購物車 → 結帳 → 付款 → 訂單信,列出客人會卡住 / 錯字 / 錯數字的地方。只列,不修。
> 🛑 **走到「購物車」就停了**:主視窗插單(dev CI 紅)。結帳、付款、訂單信**還沒走**,下面標「未走」。
>
> 環境:`~/pcm-shop` HEAD `d34847fa4`,`scripts/storefront-probe/up.sh`(108 件種子商品)。
> TapPay 刻意用假金鑰起(`TAPPAY_ENV_FILE=/nonexistent-pcm-tappay.env`),不對 TapPay sandbox 發任何請求。
> 截圖:`/private/tmp/claude-502/-Users-sean-1-pcm-website-v2/34bcbb3f-5808-4eb2-be08-637fd5aa4587/scratchpad/walk/`(session 暫存,會消失)。

## 走到的格子

| # | 哪一步 | 看到什麼 | 應該是什麼 | 嚴重度 |
|---|---|---|---|---|
| 1 | 首頁 N°04 服務宣言 | 「下單之後,」「全站皆為原廠正品,部分品牌正式代理、部分平行輸入,杜絕仿品風險。」「不只賣您部品,是陪您騎一輩子的夥伴。」—— **半形逗號**(`HomeStatement.tsx:31, :80, :108`) | 設計稿用全形「，」(`design-reference/components/HomePage.jsx:228, :235`) | 字不對 |
| 2 | 購物車 車款欄 | 「給哪台車用(套用全部商品)」「建議填寫車款,方便我們為您確認商品是否適用」—— 半形括號與逗號(`CartView.tsx:301-302`) | 設計稿沒有這一欄;我判斷應該跟 #1 同一個規矩用全形 | 字不對 |
| 3 | 首頁 N°03 部品分類 | 小字「依件數排序取前 11 名 · 全站共 17 類」(`CategoryGrid.tsx:272`) | 設計稿是一個連結「查看全部 11 類」(`HomePage.jsx:172`);我判斷「取前 11 名」讀起來像工程說明,不像給客人看的字。碼上的註解說刻意現算,不抄稿上的數字 | 字不對(低) |
| 4 | 選車後的商品列表 | 側欄「件數暫時無法顯示」 | 鑽機限定:`/api/catalog/facet-counts` 在鑽機回 503(16 路並發打爆鑽機 proxy,`fetch failed`)。**正式站要另外看** | 走不到(鑽機) |
| 5 | 商品頁 · 無規格商品 | 按「加入購物車」⇒「這件商品目前不能單獨購買,請聯繫客服 LINE 協助訂購。」 | 刻意的(`ProductInfo.tsx:243-262`,Sean 2026-08-31 拍「不賣」)。鑽機種子 108 件裡只有 k=3 那 24 件有規格,所以大多數商品在鑽機上買不了。**正式站有幾件沒規格:未查** | 不是 bug(記下) |
| 6 | 首頁 N°03 | console 警告:5 個分類上了首頁卻沒有 icon,會變純文字 chip(操控部品、車殼外觀、引擎部品、電子系統、車架) | 這 5 個是鑽機 migration 帶進來的分類名;**正式站的分類有沒有同樣情況:未查** | 會錯(待正式站確認) |

## 走過而沒問題的

- 搜尋「水箱護網」25 件;改排序、點分類都保留關鍵字(本日 `4b8c13a3a` / `d33a2dff6` 已驗)。
- 首頁選車 Yamaha → YZF-R7 → 2021 ⇒ `?vehicle=yamaha:yzf-r7:2021`,72 件,標籤三顆都在。
- 首頁分類「外觀與後視鏡16」⇒ 列表 16 件,數字對得上。
- 商品頁換顏色 ⇒ 料號跟著變(`AKRAPOVIC-3-BLK` → `-BLU`);價格、「含稅 · 滿 NT$ 5,000 免運」都在。
- 加入購物車 ⇒「已加入購物車 · 車上共 1 件」,右上角徽章 1。
- 購物車:小計 2,100、運費 100、「再買 NT$ 2,900 享免運」(5,000 − 2,100 = 2,900 ✓)、總計 2,200 ✓。

## 還沒走(下次從這裡接)

- 結帳:收件資料、配送方式、發票。
- 付款:卡片那條**在鑽機上一定走不到** —— 伺服器缺 `TAPPAY_PARTNER_KEY`(`lib/payment/composition.ts:75` 直接 throw),而且這次刻意不帶 sandbox 金鑰。改走「銀行匯款」。
- 訂單信:只查 outbox 列,不打 `/api/cron/*`。
- 下次接的時候,要買的是**有規格**的商品(`probe-*-3` 那 24 支)。
