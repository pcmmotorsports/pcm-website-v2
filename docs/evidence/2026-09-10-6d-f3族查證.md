# `f3-` 族查證 · 2026-09-10(唯讀;主 repo 樹上零寫入、零 git add、零 commit)

> 查的人:新 session(主樹 `/Users/sean_1/pcm-website-v2` · dev · `af6fd59e1`),由主視窗 `-59` 派工(第二輪)。
> **尺**:態 `open`/`doing` · 錨 `⟦f3-…⟧` · **擋欄【第一個】`⟨…⟩` 判定是 `⟨擋…⟩`**。
> 🔴 **分母我自己數,沒有用主視窗給的數字。**

---

## 〇、我自己數的 `f3-` 分母

| 量什麼 | 讀數 |
|---|---|
| 板上有錨的資料列(全板) | **755** |
| 錨 = `f3-` 的(全態) | **117** ⇒ `open` 76 · `doing` 2 · `done` 34 · `parked` 5 · `standing` 0 |
| `open`+`doing` | **78** |
| 🔴 其中擋欄首個判定 = `⟨擋…⟩` | **12** |
| 其中 = `⟨不擋…⟩` | **66** |
| 其中 = `⟨未判…⟩` | **0** |
| 其中**擋欄完全沒有判定** | **0** |

🔵 **一格對照你那 120**:你量到全板有 120 列擋欄**沒有任何判定**。
📌 **而 `f3-` 這一族是 0 —— 78 列每一列都被判過。** ⇒ 那 120 列**不在這一族**,而它是一個可以逐族縮小的分母。

⚠️ 12 列裡有 1 列態是 `doing`(`⟦f3-PDPSKUSTATIC⟧`),其餘 11 列 `open`。

---

## 一、五欄表(一列一行)

| 錨 | 板上說什麼 | 今天量到什麼 | 結論 | 證據 |
|---|---|---|---|---|
| `⟦f3-PDPSKUSTATIC⟧` (`:495` · doing) | 商品頁上方「原廠料號」**不會跟著選的規格變** —— 選紅色仍印母料號(Sean 09-06 親眼看到) | 🟢 **碼面已修而且已 commit**:`ProductInfo.tsx:362` 逐字 `{product.brand} · 原廠料號 {selectedVariant?.sku?.trim() \|\| (product.productCode ?? product.slug)}`;commit `9a2a0967d`「商品頁料號隨選中變體連動」;測試 `ProductInfo.test.tsx:85` 斷言的是 `variants![0]!.sku`。工作樹對該檔零 diff | 🟡 **條件已消失(碼面)** —— 而擋欄留著是對的:列尾逐字「**沒有肉眼驗**」,而肉眼驗是 Sean 專屬 | `apps/storefront/src/components/ProductInfo.tsx:362` · `git log --oneline -- ProductInfo.tsx` ⇒ `9a2a0967d` · `ProductInfo.test.tsx:85` |
| `⟦f3-ALERTSAMELINE1⟧` (`:812`) | 監控與被監控共命運:**9 支裡 5 支**走同一支 `pcm_cron.invoke_cron_route`,**含告警器 `pcm-anomaly-alert` 自己** | **仍是 5 支,而分母變了**:版控裡走 `invoke_cron_route` 的路徑逐個是 `/api/cron/anomaly-alert` · `capture-recheck` · `email-sweep` · `order-ineligible-gate` · `settle-sweep` ⇒ **告警器自己仍在裡面**。而白名單今天是 **10 支**(見下一列)⇒ 📌 **「9 支裡 5 支」今天是「10 支裡 5 支」** | 🔴 **仍成立**(而分母的數字過期) | `grep -rhoE "invoke_cron_route\('[^']+'\)" supabase/migrations/*.sql \| sort -u` ⇒ 5 條 · `packages/domain/src/ops/cron-jobs.ts` ⇒ 10 |
| `⟦f3-ALLOWLISTMANUAL1⟧` (`:813`) | 白名單是**手寫的** —— 有排程在跑而沒有人在看;誰欄已訂正「真正的定義在 `packages/domain/src/ops/cron-jobs.ts`」,舊數法今天回 `0` | **兩件都複現**:①舊座標 `apps/admin/src/lib/dashboard/cron-heartbeat-read.ts` 用板上那個數法 ⇒ **0**(訂正是對的)②真定義處 ⇒ **10**(板上記的是 **6**)。名單逐個:`pcm-anomaly-alert` `pcm-capture-recheck` `pcm-email-sweep` `pcm-expire-unpaid-orders` `pcm-order-ineligible-gate` `pcm-settle-sweep` `pcm-acl-digest` `pcm-settle-retry` `pcm-late-payment-sweep` `pcm-net-exposure` | 🔴 **仍成立**(白名單仍是手寫的;而**它從 6 長到 10** —— 那正是本列在講的病:名單會長而沒有人在看) | `grep -cE "^\s*\{ jobName:" packages/domain/src/ops/cron-jobs.ts` ⇒ **10** · 同尺對舊座標 ⇒ **0** |
| `⟦f3-FITSTALE1⟧` (`:834`) | 車款搜尋的資料停更了,沒有人會知道 | 列尾已帶今天的章 `⟨已查證 2026-09-10 · 不做⟩`,而我核了證據檔**存在且逐字對得上**:`docs/reviews/2026-09-09-窗A-十二列查證.md` §1 逐字「**客人那一側【今天答不出來】⇒ 結案不修**(Sean 端拍甲)」·「卡在誰:**無**」 | 🟢 **條件已消失** —— 🛑 **而它今天仍然是 `open` + `⟨擋⟩`** | `docs/reviews/2026-09-09-窗A-十二列查證.md` §1(檔 39,878 bytes · Sep 10 13:12) |
| `⟦f3-HALFWRITE1⟧` (`:901`) | 供應商同步失敗留下**半寫入中間態**,而三方都以為是整批回捲 | 🔴 **證據今天就在檔裡,而板列不知道** —— 同一支檔 §2 逐字:「**主張仍成立;⑤ 已被線 `sync` 打掉,① ② 仍開**」,座標當場複量是新的(`rpm-import.ts` `upsertBatched` 在 `:1087` · `runAtomicGroups` 在 `:1118`),探針 `scripts/halfwrite1-partial-write-probe.sh` 在。**而本列列尾最新只到 `⟨已量 2026-09-09⟩`,沒有 09-10 的章** | 🔴 **仍成立**(而板列漏了今天的查證) | `docs/reviews/2026-09-09-窗A-十二列查證.md` §2 · 對照 `docs/launch-todo.md:901` 列尾無 09-10 章 |
| `⟦f3-AUTOREFUND2⟧` (`:1138`) | 已收款單取消要自動開待退款(Sean 08-27 拍甲),已 apply 而**跑對了沒未驗**;誰欄「等人:**E8-B 真登入線落地後**」 | **前置今天仍未落地**:板上 `:536` 那一列態仍 `open`,事欄逐字「B7 端到端負向驗收 + ~~打開開關~~ **開關已開,只剩負向驗收**」 ⇒ 📌 **開關開了 ≠ 驗收做了** | 🔴 **仍成立 · 不可派**(卡前置,不是卡人) | `docs/launch-todo.md:536`(態 open) · `:1138` 誰欄 |
| `⟦f3-RPCOWNER1⟧` (`:1205`) | **[`pcm-quote-v2`]** 金流 RPC 的歸屬只到「某個有後台密碼的人」;前置綁 `E8-B` | 🛑 **主體我證不到** —— 它在 `pcm-quote-v2`,**不在這棵樹**,而我沒有去碰任何別的 worktree。**而它的前置我證得到**:`E8-B` 那一列(`:536`)今天仍 `open` | 🟡 **仍成立**(以「前置未解」這一半論定;**主體未查**) | `docs/launch-todo.md:536` · 🛑 主體證不到,見 §四 |
| `⟦f3-A4ADEBT1⟧` (`:1404`) | A4a 契約債 ①-⑥ 一整張清單;列尾「債⑦已清償」、「有人在管這一族,只是還沒管完」 | **runbook 逐字對得上而且更精確**:`docs/runbooks/a4a-summary-rollback.md:70-78` 有兩個 ✅ 落款(停寫動作已補 2026-08-06 · 真相側停寫已補 2026-08-08),**而同一段逐字寫著「🔴 仍未涵蓋的:債⑤」** ⇒ 債⑤ 在 ①-⑥ 的射程內 | 🔴 **仍成立** | `docs/runbooks/a4a-summary-rollback.md:70-78` |
| `⟦f3-ADPNARROWER1⟧` (`:1452`) | `ALTER DEFAULT PRIVILEGES` 射程比報的窄一格 | 列尾已帶今天的章:`⟨已查證 2026-09-10 · 仍成立 · plan 在 (無, 只複量) · 等 Sean 批⟩`。**我沒有複量**(該列 9,983 字,那一發是別人做的) | 🟡 **仍成立**(照抄今天的章,標來源) | `docs/launch-todo.md:1452` 列尾 |
| `⟦f3-PAIDCANCELRACE1⟧` (`:1485`) | 取消狀態只讀一次 ⇒ 讀完之後才被取消的單照樣收到「付款成功」;Sean 09-02 已拍**乙 事後偵測**而未落地 | 章 + 證據都在,而我核了 §14 逐字:「**仍然成立**…八天過去它仍然不存在」;座標**全漂了**已用真實行號重量(`load :1583` → `send :2192`,中間 609 行);偵測器那一發 `check-anomaly-alerts.ts` 找 `sent_at`/`sentAt` ⇒ **0**(🟢 正對照 `cancelledMixedRail` ⇒ 12 ⇒ 尺會動) | 🔴 **仍成立** | §14 · `docs/plans/2026-09-10-paid-cancel-race-after-the-fact-detection-plan.md`(7,504 bytes · Sep 10 02:21) |
| `⟦f3-PAIDAMOUNTDRIFT1⟧` (`:1488`) | 表頭金額與品項是兩次獨立查詢,沒有人驗它們相等 | 列尾章寫 `⟨已查證 2026-09-10 · 已不成立⟩`,而 §13 的措辭比它精確:「**這一列自己寫的關閉條件【滿足了】**」·「**卡在誰:🧑Sean / `-ship` —— 關閉條件滿足 ⇒ 可翻不擋,而翻不翻是上線判斷**」 | 🟢 **條件已消失** —— 🛑 **而它今天仍然是 `open` + `⟨擋⟩`** | §13(含正式庫兩道 CHECK 實查 ✅ 在 · 分母 6 張單的限定照抄) |
| `⟦f3-REDNEEDSEXIT⟧` (`:2098`) | 紅字要有出口與觀眾;關它要一個「會自己長大的定義」(`Record<Union,…>`),而 **admin 側今天沒有那個東西** | 🟢 **逐格複現**:`grep -rn "Record<" apps/admin/src/lib/dashboard/` ⇒ **12 命中,而 12 命中全部在 `.test.ts`**(`stuck-payment-read.test.ts` · `cron-heartbeat-read.test.ts` · `today-read.test.ts` · `freshness-read.test.ts`)⇒ **非測試檔命中 0** | 🔴 **仍成立** | `grep -rn "Record<" apps/admin/src/lib/dashboard/` ⇒ 12 · 同尺過濾 `test\|spec` ⇒ 12 ⇒ **差 0** |

---

## 二、「仍成立」那幾列 —— 今天的曝險

| 錨 | 今天的曝險 | 量的 / 未量 |
|---|---|---|
| `⟦f3-ALERTSAMELINE1⟧` | **共命運的排程 = 5 支**(含告警器自己)⇒ `invoke_cron_route` 一壞,**5 支同時啞,而會來通知你的那一支也在裡面**。另 5 支不走它(例:`pcm-net-exposure` 逐字 `SELECT public.pcm_net_exposure_record()`,純 SQL)⇒ 那 5 支不受這條路影響 | **量的**(5 / 10)。🛑 **「它今天壞了沒」= 未量**(要問正式庫 `cron.job` 與執行紀錄,我沒連) |
| `⟦f3-ALLOWLISTMANUAL1⟧` | **白名單 10 格,而它 18 天內從 6 長到 10** ⇒ 📌 **曝險不是「今天漏了幾支」,是【它每長一支就多一次漏登記的機會,而漏了沒有訊號】**。🛑 而本列自己標過一格**本來就量不到**:有人在 Supabase SQL Editor 手排、完全不進 repo ⇒ 兩個分母都撈不到它 | **量的**(10 · 6 是板上舊值)。🛑 **「今天有沒有漏登記的排程」= 未量,而且結構上量不到**(照抄列上自標) |
| `⟦f3-HALFWRITE1⟧` | 🛑 **未量** —— §2 逐字說 ①②(「那次幾筆進去」)**要正式庫的數,本機庫證不了**。我沒連正式庫 ⇒ **我沒有數字可填,不補一個聽起來合理的** | **未量** |
| `⟦f3-AUTOREFUND2⟧` | **曝險 = 0 條可派的路** —— 它等的是 `E8-B` 的負向驗收,而那一列態仍 `open` | **量的**(前置態) |
| `⟦f3-A4ADEBT1⟧` | **①-⑥ 裡至少債⑤ 未清**(runbook 逐字)。而 ②那條會咬人:「先超後補」交易起必須**同時** defer 兩名 trigger,只 defer 一名 ⇒ 先紅在 `oiqs_ordered_le_quantity`(`23514`) | **量的**(runbook 落款) · 🛑 **「①③④⑥ 今天各自清到哪」= 未量**(我只讀了 :70-78 那一段) |
| `⟦f3-PAIDCANCELRACE1⟧` | **今天的實際命中 = 0,而那個 0 沒有材料** —— §14 逐字:已取消的單身上有信 ⇒ **0**,而**分母就是 0**(訂單 6 · 已取消 3 · 信 6,已取消的單一封信都沒有)⇒ 🎯 **一個 0 要先問它的分母。那證不到它沒發生過。** 而 Sean 09-02 拍的偵測**八天後仍不存在** | **量的**(照抄 §14 的讀數與它自己標的分母限定) |
| `⟦f3-REDNEEDSEXIT⟧` | **非測試檔的「會自己長大的定義」= 0 個** ⇒ 每加一種紅字狀態就要有人記得同步清單,而**漏了沒有訊號**。⚠️ 而列上那句「清單今天是完整的」**綁 2026-09-08 01:0x 那個時點,不是一個性質**(照抄) | **量的**(0 / 12) |
| `⟦f3-RPCOWNER1⟧` | 🛑 **未量** —— 主體在別的 repo | **未量** |

---

## 三、🔴 這一輪對你那個 78 的直接影響

**12 列裡有 2 列,今天已經被判定不該再擋,而它們仍然坐在 `open` + `⟨擋⟩` 裡:**

| 錨 | 今天的判定 | 而板上 |
|---|---|---|
| `⟦f3-FITSTALE1⟧` | `⟨已查證 2026-09-10 · **不做**⟩` · §1 逐字「結案不修(Sean 端拍甲)」·「卡在誰:**無**」 | 態 `open` · 擋欄首判仍 `⟨擋⟩` |
| `⟦f3-PAIDAMOUNTDRIFT1⟧` | `⟨已查證 2026-09-10 · **已不成立**⟩` · §13 逐字「關閉條件滿足 ⇒ **可翻不擋**」 | 態 `open` · 擋欄首判仍 `⟨擋⟩` |

📌 **⇒ 這兩列是【已經有人查完並寫下結論、而結論沒有走回擋欄】。**
🛑 **而我不外推** —— 我只證了 `f3-` 這一族有這 2 列,**沒有量別族有幾列同型**。要知道 78 該減多少,得逐族照同一把尺跑。

🔴 **第三個同型、方向相反的**:`⟦f3-HALFWRITE1⟧` **今天被查過**(§2 有整整一段),**而板列上沒有那一章** ⇒ 下一個人會以為它沒人碰過,再查一次。
⇒ 🎯 **合起來是同一個病的兩個方向**:結論沒回板 ⇒ ①該減的沒減(FITSTALE1 / PAIDAMOUNTDRIFT1)②該記的沒記(HALFWRITE1)⇒ **一邊多報、一邊重工。**

---

## 四、🛑 我證不到什麼

· **`⟦f3-RPCOWNER1⟧` 的主體證不到** —— 它在 `pcm-quote-v2`,不在這棵樹。我**沒有**去開任何別的 worktree(你交代過 `~/quote-wt-c-b7` 原封別動,我一步都沒靠近)。我只證了它的前置 `E8-B` 態仍 `open`。
· **全程沒有連正式庫。** `⟦f3-ADPNARROWER1⟧` 與 `⟦f3-PAIDCANCELRACE1⟧` / `⟦f3-PAIDAMOUNTDRIFT1⟧` 那些「唯讀正式庫量到」的讀數,我**照抄並標來源**,一個都沒複量。
· **`⟦f3-PDPSKUSTATIC⟧` 我沒有開瀏覽器。** 我只證了碼與測試,而列尾逐字說「肉眼驗✅ 是 Sean 專屬用詞」—— **我不代寫。**
· **`⟦f3-ALERTSAMELINE1⟧` 的 5 支是從【版控 migration】數的**,不是從正式庫 `cron.job` 撈的 ⇒ **若有人在 SQL Editor 手排一支走同一條路的,我這把尺撈不到**(而那正好是 `⟦f3-ALLOWLISTMANUAL1⟧` 在講的洞)。
· **`⟦f3-A4ADEBT1⟧` 我只讀了 runbook `:70-78`**,沒有逐條核 ①③④⑥。

---

## 五、⚠️ 順手看到、我沒動的

1. **`⟦f3-ALLOWLISTMANUAL1⟧` 的誰欄自己已經訂正過一次舊座標**(舊數法今天回 0),✅ **而訂正是對的,我複現了**。而**新座標的那個數字(6)沒有跟著更新** ⇒ 同一格訂正了「在哪裡」,漏了「有幾個」。
2. **`⟦f3-ALERTSAMELINE1⟧` 列尾自陳「沒有關閉條件」** —— 逐字「一條沒有關閉條件的 `⟨擋⟩`,永遠不會變 `done` —— 它不是待辦,是一個永久的標記」,而它自己說「那一族該不該留在 `⟨擋⟩` 裡是明天的題」。📌 **今天就是那個明天。** 只回報,我沒判。
3. 這些**都只是回報,我沒改板、沒改態、沒動任何一格**。

---

**主樹狀態(交還前複查)**:`git status --short` = `?? h3check.png`(沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
