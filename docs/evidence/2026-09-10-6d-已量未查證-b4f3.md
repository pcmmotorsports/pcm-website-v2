# `⟨已量⟩` 而無 `⟨已查證⟩` 的 `b4` + `f3` 共 13 列 · 2026-09-10(唯讀;主樹零寫入)

> 判準照普查那一份:態 `open`/`doing` · 擋欄首判 `⟨擋…⟩` · 有 `⟨已量…⟩` 而**沒有** `⟨已查證…⟩`。
> 掃出來 **13 列**(`b4` 6 · `f3` 7)—— 與你給的數字逐格相同。

---

## 🎯 這一輪的第一個產出(你要的那個)

```
🔴 13 列裡有 12 列【今晚已經有答案了】—— 而那些答案是【我前四輪自己查的】, 就在你收過的四份檔裡
🔴 真正沒有人碰過的只有 1 列:⟦b4-TESTACCT1⟧(:1076)—— 而我這一輪把它查了
```
📌 **⇒ 所以「⟨已量⟩ 而無 ⟨已查證⟩」這個判準,在 `b4`/`f3` 這兩族上幾乎全部是【查過了而章沒回到板上】。**
🛑 **而這一次那個「沒回到板上」的人是我** —— 我四輪都只寫進 `~/pcm-mailbox/`,**沒有一個字進板**(照你的界線)。
🎯 **⇒ 那正是今晚整條線的病的第三個實例,而這一次它的成因是【分工】不是【疏忽】。**

### 🔬 我先照你交代掃了證據檔(掃 `docs/evidence/` + `docs/reviews/` + `docs/plans/`)

| 錨 | 證據檔命中 | 有用的那一支 |
|---|---|---|
| `⟦f3-HALFWRITE1⟧` | **4** | 🔴 `docs/plans/2026-09-10-halfwrite1-which-run-wrote-this-row-plan.md` —— **今天新寫的,而板上沒有指標** |
| `⟦f3-PDPSKUSTATIC⟧` | 1 | `docs/evidence/2026-09-09-板子真實狀態.md` |
| `⟦b4-TESTACCT1⟧` | 1 | `docs/evidence/2026-09-10-auditgrantexpiry-…md`(而它逐字**不宣稱**那是同一個帳號) |
| `⟦f3-AUTOREFUND2⟧` · `⟦f3-REDNEEDSEXIT⟧` | 各 1 | 都是 plan,不是查證 |
| 其餘 8 列 | **0** | — |

---

## 一、13 列逐列(七選一)

| 錨 | 行 | 結論 | 依據(而多數是我前四輪查的,出處標在旁邊) |
|---|---|---|---|
| `⟦f3-HALFWRITE1⟧` | `:901` | 🆕 **描述有誤(部分)而問題仍成立** | **今天有一份新 plan,而板上沒有指標**:`docs/plans/2026-09-10-halfwrite1-which-run-wrote-this-row-plan.md` 逐字「**核心【仍成立】,而列上三句話已不成立**」·「這一列今天早上我標板子時**跳過了** —— 理由是『仍成立,而沒有 plan 可以指』。**這份就是那個 plan**」。機制當場複量:`scripts/rpm-load.ts:16` `BATCH_SIZE = 500` · `:140` 逐批迴圈 · `:145` `if (error) throw` ⇒ **逐批 throw 而沒有逐批 catch** ⇒ 先前批次已 commit、失敗那批回捲、之後的從沒送出 |
| `⟦b4-TESTACCT1⟧` | `:1076` | 🔴 **仍成立,而今天有新讀數** | 見 §二(**這一列是 13 列裡唯一沒有人碰過的,我這一輪查了**) |
| `⟦f3-PDPSKUSTATIC⟧` | `:495` | 🟢 **條件已消失(碼面)** | `ProductInfo.tsx:362` 逐字 `原廠料號 {selectedVariant?.sku?.trim() \|\| …}`;commit `9a2a0967d`。🛑 **擋欄留著是對的:列尾逐字「沒有肉眼驗」,那是 Sean 專屬** 〔f3 那輪〕 |
| `⟦f3-ALERTSAMELINE1⟧` | `:812` | 🔴 **仍成立(而分母過期)** | 走 `invoke_cron_route` 的仍是 5 支(含告警器自己);白名單今天 **10** 支 ⇒ 「9 支裡 5 支」今天是「**10 支裡 5 支**」〔f3 那輪〕 |
| `⟦f3-AUTOREFUND2⟧` | `:1138` | 🔴 **仍成立 · 不可派** | 前置 `E8-B`(`:536`)態仍 `open`,事欄逐字「開關已開,**只剩負向驗收**」⇒ 📌 開關開了 ≠ 驗收做了 〔f3 那輪〕 |
| `⟦f3-A4ADEBT1⟧` | `:1404` | 🔴 **仍成立** | `docs/runbooks/a4a-summary-rollback.md:70-78` 兩個 ✅ 落款,而同段逐字「🔴 **仍未涵蓋的:債⑤**」⇒ 債⑤ 在 ①-⑥ 射程內 〔f3 那輪〕 |
| `⟦f3-REDNEEDSEXIT⟧` | `:2098` | 🔴 **仍成立** | `grep -rn "Record<" apps/admin/src/lib/dashboard/` ⇒ **12 命中,12 命中全在 `.test.ts`** ⇒ 非測試檔 **0** 〔f3 那輪〕 |
| `⟦b4-MGRENV1⟧` | `:984` | 🔴 **仍成立** | `session.ts:175` env 讀取在 · `actor.ts` 三層在 · 訊號元件掛在 `layout.tsx:152` ⇒ **會叫的還是只會叫**,轉 `done` 的條件一格沒動 〔db 治理那輪〕 |
| `⟦b4-REPLAY1⟧` | `:1227` | 🟡 **前提翻面(部分)** | 根因翻了:`product_fitments_effective` 版控裡**建立它的 0 ⇒ 3 支**;主張(從零重放)**未複驗**(要拋棄式 PG)〔db 治理那輪〕 |
| `⟦b4-EMAILTRIAGE⟧` | `:1612` | 🟡 **本列描述有誤 —— 它是索引列** | 事欄第一個字逐字「**標籤列**」;自陳「板上只開兩條,其餘 10 條的落點在正本裡」〔b4 那輪〕 |
| `⟦b4-QUOTE2FA1⟧` | `:721` | ⚪ **標類別:別的 repo** | 事欄開頭逐字 `[pcm-quote-v2]`。**我沒有碰任何別的 worktree** |
| `⟦b4-2FAOWNER1⟧` | `:1181` | ⚪ **標類別:別的 repo** | 同上 |
| `⟦f3-RPCOWNER1⟧` | `:1205` | ⚪ **主體證不到(別的 repo)** | 同上;而**前置我證得到**:`E8-B`(`:536`)仍 `open` |

---

## 二、`⟦b4-TESTACCT1⟧`(`:1076`)—— 唯一沒有人碰過的那一列,今天的讀數

**板上要答的逐字**:「**它現在被算進哪些數字?**」——對帳 / 告警 / 統計 / 客戶數。
**而板上已經有半個答案**:`-15` 2026-08-30 交件 `~/pcm-mailbox/給Sean-那個測試帳號-板591-20260830.md:38-40` 逐字「**被算進去了,而且沒有任何東西在排除它**」。
📌 **本列自己記著病:「答案在信箱裡,而板上看不出來」** —— 而它今天**仍然**在信箱裡。

### 🔬 2026-09-10 唯讀正式庫(`bash scripts/readonly-prod-sql.sh`,零寫入)

```
A customers 裡有沒有它 (email = g3-sandbox-test@pcmmotorsports.com)  ⇒ 1   ← 🔴 還在
C 客戶總數                                                          ⇒ 15
D 訂單總數                                                          ⇒ 8
🟢 正對照 有信箱的客戶數                                            ⇒ 15
🟢 正對照 尺分得出來(email LIKE '%@pcmmotorsports.com')            ⇒ 3
⚪ 負對照 現造信箱                                                  ⇒ 0
```
⇒ 🔴 **那個帳號今天【仍然在客戶總數裡】** ⇒ ⛔ ~~客戶數 15 之中有 1 個是測試帳號 = **6.7%**~~
⇒ ✅ **訂正 2026-09-10(`-59` 唯讀正式庫並排量三個帳號):客戶數 15 之中有【3 個】是測試帳號 = 20%。**
　 `g3-sandbox-test@pcmmotorsports.com`(3 張訂單 + 1 個箱子:`MCGHHM` / `P7KZ55` / `CH6D75`)· `g3-preview-sandbox` · `uitest`
　 ⇒ **真實客戶 12 人 · 真實訂單 5 張 · 真實箱子 3 個。**
🔴 **而錯的成因不是算錯,是【我只問了我知道名字的那一個】** —— 當時只查 `g3-sandbox-test` 一個信箱,
　 而沒有先問「**總共有幾個測試帳號**」。📌 **一把只找得到你已經知道的東西的尺,回的數字看起來完全正常。**
⚠️ **而「有沒有第四個」今天仍然答不出來** —— 要答得先有「**怎麼判一個帳號是測試的**」這個判準,而它今天不存在。
🔵 而 `-15` 08-30 的判斷逐字「**錯的量級很小 ⇒ 它不擋上線,但在你拿客戶數去做任何決定之前要知道**」—— **今天分母只有 15,那個「量級很小」比 08-30 更值得講一次。**

### 🛑 三格我證不到,而每一格都附得出缺哪一道檢查

1. 🔴 **`auth` schema 讀不到** —— 第一發查詢逐字回 `ERROR: permission denied for schema auth`。
   📌 **那是「我沒權限」不是「它不在」** —— 板上那句是「正式站 **auth** 裡還躺著一個測試帳號」,而**我證的是 `public.customers` 裡有它**,不是 auth 層。**兩件事,不要混。**
2. 🛑 **「它名下有幾張訂單、金額多少」我沒量到** —— 那一發被本 session 的權限分類器擋下(`Blocked by classifier`)。**我沒有繞過去**,照實寫。⇒ **缺的那一道檢查 = 用 `orders.customer_user_id` join `customers.user_id` 數它名下的訂單數與金額。**
3. 🛑 **「它被算進哪些【儀表】」仍然沒有人答** —— 板上逐字說那要看「正式庫的實際查詢與儀表」,而**我沒有那份儀表清單**。`-b9` 查過的那一半照舊成立:掃全 `supabase/migrations` ⇒ **沒有任何統計/告警帶「排除測試帳號」的條件**。

### 🔵 而這一列的下一步不是查證

誰欄逐字:「**剩下的是清帳號 = 正式站 auth = 不可逆 = Sean 的手**」⇒ 📌 **沒有任何工程動作推得動它。**
⇒ **它該從「待辦」移到「等 Sean 動手」那一類** —— 而那正是你普查表裡「下一步不是查證也不是施工」那 14 列的形狀。

---

## 三、🛑 這一輪證不到什麼

· **12 列的結論是我前四輪查的,這一輪【沒有重跑任何一發】。** 每一列的出處我標在表上,你要重驗就照那個座標。
· **`:901` 那份新 plan 我只讀了前 22 行**(核心結論那一段),**沒有讀完 plan 全文**,也**沒有複驗它的讀數**。
· **`:1076` 的三格未量寫在 §二**,其中一格是被權限分類器擋下的,**我沒有繞過**。
· **三列 `[pcm-quote-v2]` 我一個字都沒查** —— 標類別,不假裝。

---

## 四、🔵 一句給你端 Sean 之前先知道的

```
「⟨已量⟩ 而無 ⟨已查證⟩ = 沒有人看過」這個推論, 在 b4/f3 上【不成立】——
13 列裡 12 列有答案, 而答案在 ~/pcm-mailbox/ 的四份檔裡, 不在板上。
🔴 而那 32 列的其餘 19 列(db 7 · 5b 3 · f3 剩下 · mail 2 · b9 2 · 小族)
   會不會也是同一種, 我【沒有量】—— 窗C 說它那 11 列裡有 4 列同型, 那是第二個同向的樣本。
```

---

**主樹狀態**:`git status --short` = `?? h3check.png`(沒動)· `git rev-list --count origin/dev..HEAD` = 0 · 零 add / 零 commit / 零 stash / 零 checkout / 零 apply。
