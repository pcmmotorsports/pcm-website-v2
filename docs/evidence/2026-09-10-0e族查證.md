# 2026-09-10 `0e-` 那一族 · 逐列查證

> 🛑 **我沒有改 `docs/launch-todo.md`,也沒有翻任何一列的擋欄** —— **翻態是改判定,而 Sean 只解凍了「加標記」。**

---

## 〇、🔴 先講分母 —— **我第一把尺印 5,正確答案是 1**

### 我第一發做錯的兩件事,照實寫

**① 我用「整列 grep」當分母** ⇒ 命中 **30 列**。
🔴 **而那把尺把【這一列提到的錨】當成【這一列的錨】。**
🔬 **當場撞到**:`:1489` 被算進 `0e-RLSLIVE1`,**而那一列的錨是 `⟦f3-SVCROLEUNVERIFIED1⟧`** —— **我今天早上才查過它。**

**② 我改成「錨在第 3 欄」** ⇒ `0e-` 印 **0 列**,而我差點寫成「這一族沒有自己的列」。
🟢 **正對照救了它**:同一把尺對 `mail-` 跑 ⇒ 印 **0**,**而它應該印 40**。
```
錨實際出現在第幾欄:mail- ⇒ [2,5,6,7,8] · 0e- ⇒ [2,4,5,14] · f3- ⇒ [1..9,12,13]
⇒ 📌 這塊板【沒有固定的錨欄】。:1489 剛好在第 3 欄, 是那一列的結構, 不是規則。
```
🎯 **⇒ 兩把自製的尺,一把多算、一把全歸零,而兩次的輸出都【看起來正常】。**

### ✅ 正確的做法:**用這個 repo 自己的腳本,不要重造**
`scripts/board-row-by-anchor.sh` —— 它的檔頭逐字記著:
> 實量:`b4-SHIPGATE1` **整行 grep 10 列 / 錨欄 2 列** ⇒ **8 個假陽性,而第一個讀起來完全正常。**
> **判別句:錨欄裡那個錨【不帶反引號】才是它自己;帶反引號的是在講別人。**

📌 **⇒ 我踩的兩個坑,那支腳本的檔頭【都寫過】。** 🎯 **而我沒有先開它。**

---

## 一、【量的】用那支腳本定位 —— 11 個相異錨

```
0e-PARALLELTIMEOUT      :907   done
0e-PROBENOSCHED         :927   open    ⛔ 帶擋      ← 本族唯一帶擋的 open/doing
0e-TOOLCANTPRINTTHAT    :847   open    (不帶擋)
0e-HARNESSDEAD1         :949   done
0e-LEDGERVSLIVE1        :950   done
0e-RLSLIVE1             :951   done
0e-NEWFILEONLY1         :953   doing   ⟨不擋(tidy 判)⟩
0e-STORAGEACL           :958   parked
0e-MAILCANCELSEND       :1302  parked
🟡 0e-MIGGATE3RDWAY     :1140  open    —— 錨欄【帶反引號】⇒ 那一列在講別人, 不是它自己
🟡 0e-ANCHORNOTINCOMMIT :2045  open    —— 同上
```

---

## 二、🔴 `:927` `⟦0e-PROBENOSCHED⟧` ⇒ **仍成立**

· **板上逐字**:「`scripts/probe-schema-exposure.sh` **沒有排程** —— 規格說『由人或排程跑』,而 **17 天來那個人**…」
· **【量的】2026-09-10 今天**:
```
.github/workflows/  三支:ci · e2e-prod · rpm-sync
   ci        schedule 0 · probe-schema 0
   e2e-prod  schedule 0 · probe-schema 0
   🟢 rpm-sync schedule 1 · probe-schema 0   ← 正對照:我的尺【看得見排程】, 而它不跑這支
vercel.json / apps/*/vercel.json 的 cron ⇒ 找不到它
package.json:163 只把它註冊成 lint-staged 的 --selftest 掛勾, 不是排程
docs/probes/ 底下沒有它的輸出檔  ·  ⚪ 負對照 現造檔名 ⇒ ls 噴不存在
```
· ⇒ 🔴 **仍成立。那支探針今天仍然沒有任何排程在跑它。**
· 🛑 **而【證不到】「有沒有人手動跑過」** —— 我證的是**沒有排程**,不是**沒有人跑**。
  📌 **兩者不同**:板列那句「**17 天來那個人**」講的是後者,而**它要一個人回答,不是一個 grep。**

---

## 📊 總結行(`0e-` 錨那一族)

```
相異錨                        = 11 個
態分佈                        done 4 · doing 1 · open 4 · parked 2
🟡 其中錨欄帶反引號(在講別人)= 2 個(:1140 · :2045)⇒ 不算這一族自己的列
🔴 本族帶擋的 open/doing      = 1 列   (:927 0e-PROBENOSCHED)
✅ 其中【今天已可翻不擋】     = 0 列
⇒ 🔵 對那個 78 的應減 = 0
```

🎯 **而這一族最有價值的不是那個 1,是【我第一把尺印 5】。**
📌 **⇒ 若有人拿「整列 grep」去數任何一族,那個數會偏大,而偏大的方向是【看起來還有很多事要做】。**
🔵 **`mail-` 那一族我是用整列 grep 數的(18)** —— 🛑 **那個 18 我沒有用這支腳本複核過。**
⇒ **【推的】它可能也偏大。而要收窄它,就是把那 18 個錨逐個丟進 `board-row-by-anchor.sh`。**
📌 **我沒做 —— 寫出來,不留在心裡。**

---

## 🛑 本份證不到什麼

· **【證不到】`:927` 有沒有人手動跑過那支探針** —— 那要問人。
· **【證不到】那兩列(`:1140` `:2045`)的內容** —— 它們的錨欄帶反引號 ⇒ **它們自己的錨是別的**,而我沒去看那是什麼。
· **【證不到】`mail-` 那個 18 是不是也偏大** —— **沒複核**,見上面。
· **沒有改 `docs/launch-todo.md` 一個字。**
