# 供應商同步:那 10 件沒規格的商品、與 4 次失敗的根因(2026-09-12 · 窗 A · 唯讀)

> Sean 2026-09-12 推翻「先裝眼睛」,逐字「應該說查清楚為何會失敗才對,然後看是顧客站修正還是報價單那邊」。
> 本檔只有查,**沒有改任何碼、沒有寫正式庫**。

## 🔴 先訂正我自己昨天那句

⛔ ~~「正式庫有 10 件沒規格的商品,客人看得到」~~ ⇒ **錯的**。
- `products_list_public` **自己沒有 WHERE**(`20260811040000:198-224`),下架品是 **RLS** 擋的:`products_select_public USING (delisted_at IS NULL)`(`pg_policy` 實查)。
- 我用 `pcm_readonly` 查 ⇒ **不吃那條政策** ⇒ 數到的是下架品。
- ✅ 重量:沒有規格的商品 10 件,**下架 10 / 未下架 0** ⇒ 📌 **客人看得到而沒有規格的商品 = 0 件。**
- 📌 教訓:**用一個不吃 RLS 的帳號,去回答「客人看不看得到」** —— 那個答案從一開始就不可能對。

## ① 那 10 件是什麼

| 供應商 | 件數 | 例子 | 狀態 |
|---|---|---|---|
| rpm-carbon | 8 | 車台護蓋 ×7(ARSV421-08-*)、前腳踏翅膀 ×1 | 全部已下架,7/20~8/10 最後更新 |
| k-speed | 1 | Diabolus 分離式座椅 | 已下架 |
| eazi-grip | 1 | 儀表板保護貼(EG-75617) | 已下架 |

客人點進去會怎樣:**看不到**(PDP 走同一條 RLS)。⚠️ 我沒有實際打那幾個網址驗(正式站連打會被防機器人擋)⇒ 這一格是**讀政策推出來的,未逐頁實測**。

## ② 它們為什麼沒有規格 —— 三種假設怎麼分辨

| 假設 | 它會留下的指紋 | 實得 |
|---|---|---|
| A 同步寫到一半 | **未下架** + 0 規格(商品先寫、規格沒寫) | **0 件** |
| B 來源本來就沒有規格 | **未下架** + 0 規格 | **0 件** |
| C 設計行為:整群從來源消失 | **已下架** + 0 規格 | **10 件(10/10 全中)** |

⇒ **是 C,不是半寫入。** 兩條路徑合起來就長這樣:
- S4 來源消失對賬:整群從來源不見了 ⇒ 商品**軟下架**(`scripts/rpm-import.ts:14,166,582`)
- V1 變體對賬:來源沒有的變體 sku = 孤兒 ⇒ **硬刪**(`scripts/rpm-import.ts:740-764`)
⇒ 剩下「已下架的商品 + 0 筆規格」。**A 與 B 的指紋今天在正式庫是 0 件。**

## ③ 最近 4 次失敗,根因各在哪一側

| 時間 | 供應商 | 訊息(逐字) | 停在哪 | 根因側 |
|---|---|---|---|---|
| 09-10 | gilles | `分類異常、abort 不寫…群內衝突 9 群(修來源 v2)` | **寫入前** | **報價單那邊**(訊息自己就這樣寫) |
| 09-08 / 09-07 | rizoma ×2 | `fetchPage@0 重試 3 次仍失敗:57014 canceling statement due to statement timeout` | **讀來源時**,連寫都還沒開始 | **報價單那邊**(讀的是來源的 `storefront_catalog_v`,`scripts/rpm-fetch.ts:112`) |
| 09-05 | dbk | `pv_spec_unique preflight 撞鍵、停止(避免部分寫的髒中間態)` | **寫入前的預檢** | **報價單那邊**的資料(兩個變體撞同一個 spec 鍵);我方預檢**做對了** |

🟢 今天 08:44 那一次:12+ 個供應商**全部成功**,含 gilles / dbk / rizoma ⇒ 三種狀況都已自行恢復。

## ④ 建議(不動手,等 Sean 批)

1. **要修的是報價單那一側的資料品質**:分類 v2 群內衝突、變體 spec 撞鍵、來源查詢逾時。顧客站這邊的同步邏輯在這四次裡**表現正確**:三次都在寫入前停住(fail-closed),沒有留下半套。
2. **顧客站這邊今天不用改**:半寫入的實例 0 件、客人看得到的殘骸 0 件 ⇒ 改寫入路徑(整批一個交易)現在沒有分母。
3. **真正缺的那一格是「紅燈有沒有人看到」**:那三種失敗都是非零退出、GitHub Actions 會紅,而 **Sean 有沒有收到通知,我沒查**。⇒ 這是最小、最便宜、也最該先確認的一件。
4. 半寫入的主張本身仍然成立(拋棄式 PG 實跑過)⇒ 板列不必關,但可以從 ⟨擋⟩ 降級:**它今天沒有客人看得到的症狀**。
