# OD 稿(`orders-admin-v2.html`)裡「哪一條真的贏」

> 🔴 **為什麼要有這份檔**:`orders-admin-v2.html` 是一份會**層疊**的程式(39 個 `<style>` 區塊,
> 含多個 `data-od-fix="FIX-N"` 後補修正塊),不是一份宣告文件。同一個 CSS 變數常常被宣告
> 超過一次,**只看第一次出現的值會拿到錯的答案**——2026-08-24 夜這條線自己就踩過一次
> (誤判 `--primary` 有差異,實際是漏看了後面的 `FIX-10` 覆蓋)。
> 這份表把「稿裡宣告超過一次的變數,最後誰贏」算完一次,供之後每次對稿直接查,不用重算。
> 產生方式:唯讀解析(程式追蹤 `{ }` 巢狀與 selector/at-rule 堆疊,不是肉眼掃行號),
> 過程與腳本存於本次任務的 scratchpad,原始交件見 `~/pcm-mailbox/06-交件-稿的實際生效值表-20260824.md`。

---

## ① 區塊數(當場印,不用信裡的數字)

```
grep -c "<style" 那支稿  ⇒ 40
```
🔴 其中 1 個是假的:`:6378` 那個 `<style>` 字樣其實是 `<script>` 裡的 JS 字串字面
(`function styles(){ return "<style>"+s.textContent+"</style>" }`),不是真的 style 標籤
⇒ **真正的 `<style>` 區塊 = 39 個**。前 3 個起始行:`9-4561`(base)、`4562-4565`、`4566-4595`;
其餘 36 個各自帶 `data-od-fix="FIX-N"` 標籤(FIX-08 起跳到 FIX-80,非連號)。

## ② 全部 `--變數:值;` 宣告

```
39 個區塊逐一解析 ⇒ 共 427 筆宣告(第二次訂正後的數字,見下方兩節「🔴 訂正」)
出現超過一次的變數名 = 83 個(第二次訂正前的舊分母;訂正後會再變, 未重算, 不影響本表 28 列的結論)
只出現一次的變數名 = 99 個(同上,未重算)
```

## 🔴 訂正一(2026-08-24 稍後,回頭比我方時發現;第一個 bug)

**解析腳本原本沒有先剝掉 CSS 註解就在數 `{` `}` 巢狀層級。** 這份稿裡的中文說明註解常常**直接引用
CSS 片段當例子**(例如註解裡寫 `.rail-collapsed #nav-rail{width:56px}` 這種文字),
註解裡就藏了 **20 個**不成對的大括號(`grep` 數過:225 段註解裡合計 20 個 `{`/`}`),
足以讓沒剝註解的巢狀計數器在局部走歪,漏抓到夾在corrupted區段裡的宣告。

```
影響範圍:9 筆宣告在原始掃描中被漏掉(407 → 416)
實際改變贏家的:只有 2 個變數
  --border      原判贏家 :5046(FIX-27)#ccd5e2  →  訂正後 :5316(FIX-38)#e3e9f0
  --border-soft 原判贏家 :4858(FIX-10)#e8ecf2  →  訂正後 :5317(FIX-38)#f1f5f9
```
修法:重新解析前先把 `/\*.*?\*/`(含跨行)整段替換成等長空白再交給巢狀計數器,行號因此不漂。

## 🔴🔴 訂正二(2026-08-24 更後,1b 複驗時再抓到;第二個 bug——與訂正一不同族)

**解析腳本沒有處理「規則區塊裡最後一個宣告沒有結尾分號就直接接 `}`」這種合法 CSS 寫法。**
例:`:root{--pcm-pill-w:62px;--pcm-three-w:198px}` —— 第二個宣告 `--pcm-three-w:198px` 後面
直接是 `}`,原本的解析器只在遇到 `;` 時才收一筆宣告,遇到 `}` 就直接跳過,**這個沒有分號的
最後一筆會被靜默丟掉**(不報錯,表面上看起來一切正常)。

```
全檔用同一個樣式(--變數:值}, 缺分號直接接右括號)掃了一次, 共 11 處命中:
  :4829  --font-sans   = system-ui,-apple-system,"Segoe UI","Noto Sans TC","PingFang TC","Microsoft JhengHei",sans-serif
  :4836  --od-fs-sm    = 13px
  :4837  --od-fs-sm    = 13px
  :4838  --od-fs-sm    = 12px
  :5042  --od-fs-sm    = 13px
  :5045  --fg-2        = #333e4e     ← 影響下方第⑤節主表
  :5054  --od-fs-sm    = 14px
  :5055  --od-fs-sm    = 14px
  :5066  --pcm-three-w = 198px       ← 影響下方第⑤節主表(新增列)
  :5334  --od-row-h    = 48px
  :5404  --od-fs-sm    = 13px
```
```
實際改變贏家的:2 個變數(都在下方第⑤節主表裡)
  --fg-2         原判贏家 :4853(FIX-10)#3c4757  →  訂正後 :5045(FIX-27)#333e4e
                 🔴 訂正後的值與我方 globals.css:128 現值 #333e4e 相同 ⇒ 這格其實【沒有差異】,
                    不是需要照稿改的堆甲。1b 原本要我改成 #3c4757 反而是錯的方向,已擋下未執行。
  --pcm-three-w  原本整筆漏掉  →  新增 :5066(FIX-28)198px(我方 globals.css:1626 現值 128px)
其餘 9 處(--font-sans / --od-fs-sm ×6 / --od-row-h)只影響下方「密度條件式」與 font-mono/
  font-sans 那個「工具限制」附注, 不在核心 28 列表裡, 已一併更新於對應段落。
```
修法:解析器在遇到 `}` 收合區塊時,除了原本的 pop 堆疊,**也要把 `}` 之前尚未被 `;` 收掉的
殘留文字當成最後一筆宣告嘗試解析一次**,兩處收合入口(`;` 與 `}`)共用同一個 flush 函式。

⚠️ **這是這份表出現的第二個獨立解析器 bug,不是同一個坑犯兩次** ——訂正一是「沒剝註解」,
訂正二是「沒處理缺分號的最後宣告」,兩者互不相關,分開記錄。

## ③ 正對照 / 負對照(方法先驗過,再信結果)

```
正對照 --primary(已知答案:第一筆 #0066b1,真正生效 #1a5c96,FIX-10 贏)
  程式跑出來:第一筆 :3560『:root』#0066b1 → 贏家 :4858『:root』(FIX-10)#1a5c96
  ⇒ 與已知答案一致, 方法沒有壞

負對照 挑一個只宣告一次的變數(--tw-translate-z,:16,值 0)
  ⇒ 正確地【沒有】被列進「出現超過一次」那批
```

## ④ 特異性:同特異性才是「後面贏」,不同特異性另列

```
83 個裡, 排除掉「只在 .dark / prefers-color-scheme:dark 條件下重複」的(明暗兩套各自的值,
不互相競爭)之後,剩 78 個在 light 模式下有 ≥2 筆宣告。
其中 25 個是 Tailwind 內部工具變數(--tw-* 開頭)——一條全域 reset 給預設值, 再由各自的
  utility class 在該 class 存在時覆蓋, 兩者是分工不是衝突, 排除。
另外 21 個是 --color-* 開頭的 Tailwind 主題色階——差異是同一個顏色的 hex 版 + lab() 精確版
  (@supports 漸進增強), 不是值真的改了, 排除。
```
⇒ 排除完雜訊,剩 **32 個**是真正跟後台設計代幣有關的變數。

**再拆這 32 個**:
```
3 個(--od-fs / --od-fs-sm / --od-row-h)是【密度模式】的條件式變體, 不是衝突, 另列不進主表。
2 個(--font-mono / --font-sans)是特異性計算工具在【逗號分隔選擇器】(`:root, :host {...}`)
  上的已知限制:`:host` 在一般網頁不會命中, 真正比較基準應與純 `:root` 同級 ⇒ 不會是 `:87/:88`
  那個自我循環字串。🔴 訂正二之後:真正贏家也不是原本手動訂正猜的 `:3587` 那組(那組同樣被
  訂正二的「缺分號」bug 蓋過),而是更後面 `:4829`(缺分號、直接接 `}`)的
  `system-ui,-apple-system,"Segoe UI","Noto Sans TC","PingFang TC","Microsoft JhengHei",sans-serif`
  ⇒ 這格連續錯了兩次,兩次都是同一個「只看到某一個宣告,沒有查後面還有沒有」的病,已第三次訂正。
```

---

## ⑤ 核心設計代幣表(28 個,同特異性、light 模式、排除雜訊後的乾淨結果)

| 變數 | 第一次宣告(行:值) | 最後贏家(行,FIX:值) |
|---|---|---|
| `--primary`(正對照)| `:3560` `#0066b1` | `:4858`(FIX-10)`#1a5c96` |
| `--ring` | `:3579` `#0066b1` | `:4858`(FIX-10)`#1a5c96` |
| `--sidebar-primary` | `:3582` `#0066b1` | `:4858`(FIX-10)`#1a5c96` |
| `--sidebar-ring` | `:3587` `#0066b1` | `:4858`(FIX-10)`#1a5c96` |
| `--background` | `:3554` `#f7f8fa` | `:5315`(FIX-38)`#f8fafc` |
| `--foreground` | `:3555` `#111827` | `:4853`(FIX-10)`#1f2937` |
| `--card` | `:3556` `#fff` | `:4852`(FIX-10)`#fff`(值沒變,重複宣告同值) |
| `--card-foreground` | `:3557` `#111827` | `:4853`(FIX-10)`#1f2937` |
| `--popover` | `:3558` `#fff` | `:4852`(FIX-10)`#fff`(值沒變) |
| `--popover-foreground` | `:3559` `#111827` | `:4853`(FIX-10)`#1f2937` |
| `--secondary` | `:3562` `#eef3f8` | `:5318`(FIX-38)`#f1f5f9` |
| `--secondary-foreground` | `:3563` `#111827` | `:4856`(FIX-10)`#1f2937` |
| `--muted` | `:3564` `#eef3f8` | `:5318`(FIX-38)`#f1f5f9` |
| `--muted-foreground` | `:3565` `#606e85` | `:5045`(FIX-27)`#4d596d` |
| `--accent` | `:3566` `#eef3f8` | `:5318`(FIX-38)`#f1f5f9` |
| `--accent-foreground` | `:3567` `#111827` | `:4856`(FIX-10)`#1f2937` |
| `--destructive` | `:3568` `#e4002b` | `:4859`(FIX-10)`#b42318` |
| `--success` | `:3571` `#12883e` | `:4859`(FIX-10)`#0f7a3d` |
| `--warning` | `:3572` `#f59e0b` | `:4859`(FIX-10)`#a15c07` |
| `--border` 🔴已修正 | `:3573` `#d7dee8` | `:5316`(FIX-38)`#e3e9f0` |
| `--border-soft` 🔴已修正 | `:3574` `#edf1f6` | `:5317`(FIX-38)`#f1f5f9` |
| `--input` | `:3575` `#898989` | `:4857`(FIX-10)`#8b93a1` |
| `--fg-2` 🔴訂正二 | `:3570` `#334155` | `:5045`(FIX-27)`#333e4e`(與我方現值相同,**無差異**) |
| `--sidebar` | `:3580` `#fff` | `:4860`(FIX-10)`#fff`(值沒變) |
| `--sidebar-accent` | `:3584` `#eef3f8` | `:4860`(FIX-10)`#e9edf3` |
| `--sidebar-border` | `:3586` `#d7dee8` | `:4860`(FIX-10)`#d3dae4` |
| `--pcm-pill-w` | `:4192` `40px` | `:5066`(FIX-28)`62px` |
| `--pcm-three-w` 🔴訂正二新增 | `:4193` `128px` | `:5066`(FIX-28)`198px` |

## 🔴🔴 訂正三(2026-08-25,cf 線抓到;這一次錯的不是值,是**座標**)

> ~~⚠️ 行號:每一行都已個別用 `grep`/`sed` 對過原稿(不是套用單一固定偏移量——訂正二發現同一支~~
> ~~script 對不同 `<style>` 區塊的行號誤差並不一致,套用「一律 +1」會在某些區塊上又錯一次)。~~
>
> 🔴 **上面那句話是假的,而它是本檔可信度最高的一句。** 劃掉不刪,因為**它為什麼會被寫下來**
> 比它寫了什麼更重要:那句話**逐字保證了「我逐格對過」**,於是每一個讀它的人(包括 1b,
> 他拿它當權威開了綠燈)都**停止了查證**。⇒ **一句自稱「我驗過了」的話,關掉的是別人的量具。**

**2026-08-25 逐格重量的結果**:
```
「第一次宣告」欄   28 / 28 全部正確
「最後贏家」欄     🔴 16 / 28 錯,全部剛好 −1,零例外
```
⚠️ **cf 線回報的是 14 格,我當場重量是 16 格** —— 兩個數字都是量出來的,而我沒有去對齊他的。
**差異未解釋、掛著**;要用哪個數字 ⇒ **自己跑下面那段重量,不要抄我們任何一個。**

🔴 **不要拿「同行多宣告時第 2 個起 +1」那個機制假說去批次修**(cf 提出後自己證偽:
`--muted-foreground` 是第 1 個卻錯、`--fg-2` 是第 3 個卻對、`--pcm-three-w` 是第 2 個卻對)。
**本次訂正是逐格比對「該行實際含不含這個變數 + 含不含那個贏家值」,不是套偏移量。**

📏 **重量方法(可直接抄,repo 根執行)**:
```python
python3 -c "
import io, re
OD='<下面釘的那支稿的路徑>'
lines=io.open(OD,encoding='utf-8').read().splitlines()
md=io.open('docs/design/od-cascade-winning-values.md',encoding='utf-8').read()
row=re.compile(r'^\|\s*\`(--[\w-]+)\`[^|]*\|\s*\`:(\d+)\`\s*\`([^\`]*)\`[^|]*\|\s*\`:(\d+)\`',re.M)
b1=b2=0
for var,l1,v1,l2 in row.findall(md):
    occ=[i+1 for i,ln in enumerate(lines) if re.search(rf'{re.escape(var)}\s*:',ln)]
    if int(l1) not in occ: b1+=1
    if int(l2) not in occ: b2+=1
print('第一次宣告欄 對不上 =',b1,'/28')
print('最後贏家欄   對不上 =',b2,'/28')
"
```
**訂正後當場重跑 ⇒ `0 / 28` 與 `0 / 28`。**
🟢 **正對照(證明這把尺不是恆回 0)**:訂正**前**同一段程式回報 `0 / 28` 與 `16 / 28`
⇒ **它在兩個世界印不同的東西,所以那個 0 有判別力。**

## 🔴 釘住原稿的指紋 —— 對不上 ⇒ **本檔所有行號全部作廢**

本檔釘了約 60 個行號,而**行號只在某一份特定的檔案上成立**。
```
檔案   ~/Library/Application Support/Open Design/namespaces/release-stable/
         data/projects/pcm-524f/orders-admin-v2.html
sha256 fc4a24a584e9e7d24eeb6e7e0839e8cc0c25a754db5bf9fee537e7e97f933d67
bytes  577132
行數   6795
mtime  2026-08-24 20:43
```
📏 量法(2026-08-25 本窗當場跑,**不是轉抄**):
`shasum -a 256 <檔>` · `wc -c < <檔>` · `wc -l < <檔>` · `stat -f '%Sm' -t '%Y-%m-%d %H:%M' <檔>`

🔴 **sha256 對不上 ⇒ 稿被改過 ⇒ 本檔【所有行號全部作廢】,值也要重算。**
⚠️ **`mtime` 不能當版本判準**(它只記最後一次寫,而這支稿會被 OD 覆寫)⇒ **以 sha256 為準。**

🔴 **`--pcm-pill-w` 與 `--pcm-three-w` 是同一組版面配對,1b 已裁決這輪停著不動**:
稿在同一行(`:5066`)把兩者一起改(`40→62` 且 `128→198`),而我方 `globals.css:1705` 那條
grid 用到 `var(--pcm-three-w)`;只改其中一個會把版面改成一個稿上不存在的中間狀態
⇒ 這是版面幾何、不是換色,要單獨量過欄寬才動,本輪兩個都不改。

## 密度條件式(不是衝突,附上供對照,不進主表;訂正二後已更新)

```
--od-fs       :3779『.orders-grid』.875rem  →  :5404『.orders-grid』14px
--od-fs-sm    :3780『.orders-grid』.8125rem →  :5404『.orders-grid』13px
--od-row-h    :3778『.orders-grid』40px     →  :5404『.orders-grid』40px(繞了一圈回到起點值,
                                                中間經過 26/32/36/44/48 多種密度變體,見訂正二列表)
```

---

## 用法

對稿時,先查這張表有沒有你要比對的變數;有 ⇒ 直接用「最後贏家」那欄的值,不要自己重 grep
第一個出現的值。表裡沒有的變數 ⇒ 代表它在稿裡只宣告一次,你 grep 到的值就是生效值。

## 已知限制(未涵蓋的部分)

```
· 只涵蓋 CSS 自訂屬性(--變數), 不含一般屬性(color/background-color 等直接寫值、不走變數的宣告)
· 只做了「哪個 :root 級變數贏」, 沒有做「我們自己 globals.css 的同款表」——那份已有人數過,
  見 260e51fd 附的盤點檔(69 處宣告 / 38 個選擇器)
· 🔴🔴 **payload(`:4608` gzip+base64)本尺【照不到】—— 而 2026-08-25 量到它裡面有本尺沒有的東西。**
  本表所有「贏家值」的射程,精確地說是:**在那 39 個 `<style>` 區塊之內誰贏**。
  b4 線 2026-08-25 解開那段 payload 後量到:交接檔 `§12` 對組 28 只記了 **1 條**宣告,
  而真規格是 **7 條**,其中 **6 條從來沒有進過任何紀錄**。
  ⇒ **在 b4 重掃完之前,本表(以及任何引用 OD `§12`「查無 / 不適用」的結論)一律讀成
    「本尺照不到」,不要讀成「不存在」。**
  ⚠️ 這不是說本表的 28 列錯了 —— 那 28 列在 `<style>` 這個分母裡仍然是對的
    (雙尺各自重算過)。**是說分母可能比我以為的小。**
```

## 獨立第二把尺(交叉驗證用,已跑過;2026-08-24 深夜再訂正——舊盲區已被 cf 線修掉)

```
工具    scripts/tool-final-css.py(repo 內版本;與 ~/pcm-mailbox/tool-final-css.py 逐字元相同,
        cf 線已把兩份都修好、下面的盲區不再存在)
函式    styles_in_document_order(html) / final_values(html) / final_values_css(css)
自檢    python3 scripts/tool-final-css.py  ⇒ 印「全部自檢通過。」(2026-08-24 深夜再跑一次,PASS)
```
拿它獨立跑一次這份稿(`final_values`),對照上面第⑤節的 28 列 ⇒ **28 列全部逐字相同**,
包含訂正一那兩列(`--border` `#e3e9f0` / `--border-soft` `#f1f5f9`)、以及訂正二的
`--fg-2` `#333e4e` 與新增的 `--pcm-three-w` `198px` 也全部對得上
⇒ **兩把互相獨立的尺算出同一組贏家,交叉驗證成立**(這支工具天生不受訂正二那個 bug 影響——
它用 `body.split(';')` 切宣告,即使最後一筆沒有分號,`split` 仍會把它當最後一段留住)。

🔴 **舊版盲區已修好,兩側現在都能用同一把尺算**:舊版 `walk()` 會把「第一個 `{` 之前的全部
文字」當一整塊 prelude、判不是 `@media`/`@supports` 就整段丟棄,導致我方 `globals.css` 開頭
`@import ×2 + @custom-variant + :root {…}` 這種「零大括號陳述句黏在選擇器前面」的形狀,
連黏在最後面的 `:root` 都一起被吞掉。cf 線的修法:prelude 裡只取【最後一個分號之後】的片段
(scripts/tool-final-css.py:88-98,含世界F 正對照 `@import 'x'; :root{...}` 驗證過)。

**2026-08-24 深夜用同一把尺(`scripts/tool-final-css.py`)把 28 列兩側各自重算一次,逐格對答案**:

```text
ours final_values_css(globals.css) 的 :root 抓到 48 個變數(舊版盲區時只抓得到殘存小區塊,
  現在抓到主區塊,數字合理——不是 0、不是可疑的個位數)
od   final_values(稿) 的 :root 抓到 44 個變數
```

結果:**28 列裡 26 列相同、2 列不同**,不同的兩列正是已裁決暫停的
`--pcm-pill-w`(OD `62px` / ours `40px`)與 `--pcm-three-w`(OD `198px` / ours `128px`)
——與第⑤節主表、與 1b 的暫停裁決**完全一致,沒有出現任何新的落差**。
`--fg-2`(`#333e4e`)、`--border`(`#e3e9f0`)、`--border-soft`(`#f1f5f9`)三格用同一把尺
兩側直接對出「相同」,不再需要分開用兩支不同解析器各驗一半。

🔴 **那個 26 是【顏色正規化後】的數法 —— 純字串比對重跑會得到 23,而那不是表錯了**:
`--card` / `--popover` / `--sidebar` 三格 OD 稿寫 `#fff`、我方寫 `#ffffff`,**同一個顏色、不同寫法**。
數法(1b 2026-08-24 深夜獨立第三次重算,程式碼 `<scratchpad>/recheck28.py`):
```text
純字串比對  ⇒ 相同 23 / 不同 5 / 【有一邊抓不到 0】
顏色正規化  ⇒ 相同 26 / 不同 2   (差的 3 格 = #fff vs #ffffff)
負對照      ⇒ 餵一個不存在的變數名, 兩側都回 None(尺沒有在亂答)
```
🔴 **「有一邊抓不到 = 0」這格是這次複驗真正的收穫**,而它不在 06 的回報裡:
`od.get(n)` 與 `ours.get(n)` **兩邊都是 `None` 時,比對結果也是「相同」**。
⇒ 一個名字若在兩側都沒被抓到,它會安靜地被算進「26 列相同」裡面。這次量到 0,所以那 26 是真的。

⇒ **這是 §6「不知道自己漏了什麼」那個天花板的解法**:雙尺不一致的格只剩已知且已裁決的那兩格,
沒有第三個未預期的落差冒出來。回比我方的 27 列交件(`~/pcm-mailbox/06-交件-27列回比我方-20260824.md`)
是修盲區之前的舊交件,單尺 + `grep` 抽驗版本,本節這次才是雙尺各自獨立重算兩側的完整版本。
