# 兩張列印紙的【幾項會爆到第二頁】—— 量到的門檻與量法

> 🔴 **`CLAUDE.md` 路由表那一行(觸發情境「列印出來的紙多了一頁 / 紙上看不到頁碼 / 換一台印表機就不一樣」)引的就是本檔。**
> ⇒ 雙向指標:那一行給結論, 本檔給【它憑什麼】與【怎麼重跑一次】。
> (2026-08-29 線A 量;`a024b4c5` 加那一行時, 這些數字只在一台機器上 ⇒ 所以收進 repo。)

---

## 可印區 = **1024px**
`@page` 是 `margin: 12mm 12mm 14mm 12mm`(由 `print-a4-css.test.ts` 釘死)
⇒ A4 297mm − 12 − 14 = **271mm** ⇒ 96dpi 下 **1024px**。

---

## 四個世界的門檻

🔴 **每一個世界都用【兩把獨立的尺】各量八點, 而它們的答案全部相同** ——
數法:尺一預測(內容高 ≤1024 ⇒ 1 頁)vs 尺二實測(`sh scripts/pagecount.sh <html>`)逐格比對。
✅ 訂單明細 8/8 · 出貨單「無」8/8 · 出貨單全出 8/8 · 出貨單只出 1 項 8/8。


> 🔴🔴 **這整份檔的每一個頁數,都是【在 margin = 0 的世界】量到的。**
> 那是 `page.pdf({format:'A4'})` 的預設(該支測試檔 `:636` 自己的註解就寫著),
> 而 **margin = 0 正是列印對話框裡的「無邊界」** ——
> 🔴 **Sean 2026-08-30 逐字說,後台那兩張紙他【必須手動切成無邊界】才會是一頁**
>   (「前台的列印範圍、邊界、比例我用預設都剛好是一頁,後台的訂單明細跟出貨單卻要
>    設定成 無邊界才可以變成一頁」)。
> ⇒ ⇒ 📌 **所以這些數字答的是【他要手動切換才會看到】的那個世界,不是他預設看到的那個。**
>
> ⚠️ **而這【不是說數字錯了】** —— 2026-08-30 `-30` 掃過邊界 0/5/10.16/12/14/16/18/20/25mm,
>   三份 fixture × 九格**全部 1 頁、沒有懸崖**;頁首頁尾開/關也全 1 頁。
>   ⇒ **在我們的 fixture 上,那個差複現不出來。**
> 🔴 **⇒ 難的正是這一格:數字是對的、三綠會過、突變殺得掉、連跑兩發相同 ——**
>   **而它掛在一個【沒有寫出來的前提】上。⇒ 要補的是【世界的名字】,不是數字。**
> ⇒ ⇒ **引用這份表的人:先問「我要答的是哪一個世界」,再抄數字。**

> 🔴🔴 **這張紙的餘裕只有【6mm 級】—— 2026-08-30 第一次有數字。**
>
> 那不是算的,是**從 Sean 那台印表機的兩次真實列印回推**的(他兩次都用**預設邊界**):
> ```
> 修前（紙高 250mm + 後台外殼 12.7mm = 262.7mm）⇒ 他印出 2 頁
>   ⇒ 他那台的可印高 < 262.7mm ⇒ 預設邊界 > 17.15mm 每側
> 修後（732e8249 關掉外殼 ⇒ 250.0mm）        ⇒ 他印出 1 頁
>   ⇒ 可印高 ≥ 250mm ⇒ 預設邊界 ≤ 23.5mm 每側
> ⇒ ⇒ 他那台的預設邊界落在 17.15 ~ 23.5mm 之間
> ```
> 📌 **⇒ 所以在【他那台 + 預設邊界】上,這張紙的餘裕大約是 6mm。**
> ⇒ ⇒ **再加任何一個與內容無關的東西(外殼、頁首頁尾、一條分隔線)都可能讓它翻頁。**
>
> ⚠️ **而它證不到什麼(不要把 6mm 讀成一個通則)**:
> · 這是**他那一台 HP Color LaserJet Pro + 他那組設定**回推的,
>   其他機器的預設邊界不同 ⇒ 餘裕不同(而我們沒有其他機器的數字)
> · 🔴 **本檔上面那張表是【在 `margin = 0` 的世界】量的** ——
>   那與這一段的【預設邊界】**不是同一個世界**。兩個數字不要互相代入。
> · 只有 1 項那一格被驗過;長內容那一端沒有用他那台印過
>
> 🔵 **為什麼把它寫在表的正上方**:下一個要動這張紙的人會先看那張表,
> 而那張表答的是「幾項會跨頁」—— **它答不出「我還能不能再加一行東西」**,
> 而那才是動版面的人真正在問的。

| 世界 | 固定成本 | 每項 | 第一頁裝 | 幾項起兩頁 |
|---|---|---|---|---|
| 出貨單 · 有「尚未出貨」表 · **全部出貨** | 828px(80.8%) | 95.5px | 2 項 | **3 項** |
| 出貨單 · 有「尚未出貨」表 · **只出 1 項** | — | 47.8px(只有尚未出貨表在長) | 3 項 | **4 項** |
| 出貨單 · **「尚未出貨:無」**(~~整表換成一行 55px 的字~~ ⇒ 2026-08-30 起是一條「以下空白」分隔線) | ~~751px(73.4%)~~ | ~~47.6px~~ | ~~5 項~~ ⇒ **7 項** | ~~**6 項**~~ ⇒ **8 項** |
| 訂單明細(picking) | 651px(63.6%) | 47.1px | 7 項 | **8 項** |

### 🔵 2026-08-30 傍晚補量:**「全部出貨」那一列,四改之後的門檻從 3 移到 4**(線-出貨)

🔴 **這一段的第一版我把世界認錯了,現在寫的是複核後的版本**(`code-reviewer` 2026-08-30 抓,
我自己開檔複量確認)。錯在哪與怎麼分辨,寫在本節末尾 —— **留著,因為下一個人會用同一把錯的尺。**

**量到什麼**(fixture = `page-measure.test.tsx` 的 `emit(n)` ⇒ `shipping-1..7item` + 12):

| 品項數 | 1 | 2 | 3 | **4** | 5 | 6 | 7 | 12 |
|---|---|---|---|---|---|---|---|---|
| 真頁數(`pagecount.sh` 數 `/Type /Page`) | 1 | 1 | 1 | **2** | 2 | 2 | 2 | 2 |

**它是哪一個世界** —— 不靠檔名,靠內容數出來的:
```
shipping-3item.html 裡每一個 SKU 出現 2 次（SKU-0000/0001/0002 各 2）
⇒ 同一項同時在「本次出貨」與「尚未出貨」兩張表 ⇒ 【全部出貨】那一列（本檔 :63）
⇒ 不是「只出 1 項」那一列（那一列每項只佔一列）
旁證：本檔 :147 shipF 那行的原始高度 1 項=923 / 12 項=1973，與本 fixture 逐字相同
      （shipB 那行 12 項是 1449 ⇒ 對不上）
```

🔴🔴 **所以這是一次【門檻移動】,不是一次確認**:
```
本檔表上「全部出貨」那一列寫的是 ⇒ 3 項起兩頁   （四改【之前】，用高度推的：3 項=1114 > 1024）
今天實量 post-四改             ⇒ 4 項起兩頁
```
📌 **⇒ 它正好填掉本檔 `:124-127` 自己指出的那個洞** ——
那段逐字寫著「上表其餘三列**沒有重量**…它們的門檻很可能也移了,而那是【未量】不是【沒動】」。
✅ **現在其中一列量了,而它真的移了一格。**⚠️ 另外兩列(「只出 1 項」與訂單明細)**仍然未量**。

⛔ **本節第一版寫過「兩把獨立的尺在門檻上收斂」—— 那句話已作廢,理由留著**:
高度那把尺量的是**「只出 1 項」那一列的四改【之前】**(`1019→1066`),
真頁數這把尺量的是**「全部出貨」那一列的四改【之後】**。
⇒ **不同的列 + 不同的 CSS 狀態,兩個 4 相等是巧合。**
📌 **兩把尺要能互相背書,前提是它們站在同一個世界的同一個時點 —— 而那兩件事都不會自己出現在數字旁邊。**

**量法(可重跑)**:`page-measure.test.tsx` 產 fixture ⇒ `sh scripts/pagecount.sh <檔>`;
`--selftest` 先跑一發(四格含負對照:不存在的檔 ⇒ `rc=2`,不是「0 頁」)。
分母照實寫:**3~6 各連跑兩發相同,1 / 2 / 12 各只跑一發**;
世界活錨驗過 1~7 共七份(`還欠幾件`=1 / `以下空白`=0),**12 那一格沒驗活錨**。

⚠️ **這一列的 fixture 把每一項的 `quantitySummary` 餵成 `null`** ⇒ 每列都印「數量資料尚未就緒」,
且**必定**走「有未出貨表」那條。⇒ 它是這個世界裡**內容偏長**的一種,不是「一般的一張出貨單」。

⚠️ **與本檔 `:17-19` 的關係要講清楚,不要讀成矛盾**:那裡寫「每個世界都用兩把尺各量八點、8/8 相同」——
那是**四改之前**、在**線A 自己的 fixture** 上量的。本節不是推翻它,是**在四改之後重量了其中一列**。

---

### 🔴🔴 一個會讓人讀錯行的坑:**`shipB` 這個名字在兩個檔裡指【相反的世界】**

```
本檔的「原始數字」那一節：  出貨單 只出1  shipB-N     ⇒ 有「尚未出貨」表
page-measure.test.tsx：     shipB-${n}item             ⇒ emitNoOutstanding ＝【無】未出貨
                            shipping-${n}item          ⇒ emit ＝【全部出貨】（每項兩列）
```
⇒ **同一個字首,兩個檔,兩個相反的分支。**而且 `shipping-*` 也不是本檔任何一列的名字。
⇒ 拿本檔的 `shipB` 去對測試產出的 `shipB-*.html`,會**整列讀錯**,
   而**兩邊都印得出一個看起來合理的頁數** ⇒ 沒有任何訊號。
📌 **判別句:不要靠檔名認世界,靠【數同一個 SKU 出現幾次】** —— 2 次 = 全部出貨、1 次 = 只出 1 項;
   再配活錨 `還欠幾件`(有表)/ `以下空白`(無表)。
   (⚠️ **不可以用 `尚未出貨` 那四個字**:兩條分支都印它。)
🛑 **本輪【沒有】改名** —— 改名會動到已被引用的字面,不在這一片範圍。這一段的用途是**讓下一個人在讀錯之前先撞到它**。

🔴 **而我就是那個讀錯的人**:第一版我把量到的數字掛在「只出 1 項」那一列,
理由是我看到檔名 `shipping-*` 又看到活錨說「有表」⇒ 兩個訊號都對,而**結論錯**。
📌 **活錨只答得出「有沒有那張表」,答不出「每一項佔幾列」—— 而後者才是決定門檻的那件事。**


> 🔴🔴 **2026-08-30 傍晚 Sean 直接下的四改把上表的【出貨單 ·「尚未出貨:無」】那一列推走了。**
> 改動:拿掉兩句解釋小字(`.pd-multi` 與本次出貨區的 note)、拿掉續頁抬頭 `.pd-contbar`、
> 「尚未出貨:無 —— …」那句換成一條「以下空白」分隔線、`.pd-flow{min-height}` 240⇒250。
> **實測(真 PDF 逐頁抽字,同一發)**:
> ```
> 改前        5⇒1  6⇒1  7⇒2
> ①②③ 之後   5⇒1  6⇒1  7⇒1  8⇒2
> ④ 之後      5⇒1  6⇒1  7⇒1  8⇒2   ← 與上一列完全相同 ⇒ ④ 不花容量
> ```
> ⚠️ **上表其餘三列【沒有重量】** —— 分支A 那兩列與訂單明細那列,本次沒有量,
>    而 `.pd-contbar` 的移除**對它們也生效**(三區共用同一個 `Section`;訂單明細那張是另一支檔,
>    但也被拿掉了同型的一列)⇒ **它們的門檻很可能也移了,而那是【未量】不是【沒動】。**
> 📌 **⇒ 一張表裡只有一列被重量過, 而整張表看起來一樣新。**


🔴 **出貨單每一項要付【兩倍】高度** —— 同一個品項在「本次出貨」與「尚未出貨」兩張表各佔一列。
數法(2026-08-29 實跑):
```
grep -o 'SKU-0002' shipping-3item.html | wc -l   ⇒ 2   ← 第 3 個品項在紙上出現兩次
grep -o 'SKU-0002' pick-3.html        | wc -l   ⇒ 1   ← 訂單明細只出現一次
負對照 SKU-9999(兩檔)                            ⇒ 0 / 0
```
🛑 **不要用 `grep -c`** —— 它數的是【行】, 而整份 HTML 是**一行** ⇒ 它永遠回 1。
(本檔第一版就寫錯成 `grep -c`, 而寫的人當場重跑才發現。留著這一句免得下一個人再寫一次。)
✅ 而兩個命中都在 `<td class="pd-sku">` 裡 ⇒ 數的是品項格, 不是別的東西。
🔴 **而兩張表的表頭合計 297px = 一頁的 29%, 而它們裝的是零項資料。**

### 原始數字(內容高 px;可印區 1024)

```
訂單明細 pick-N      1:698  3:792  5:887  6:934  7:981  8:1028  10:1122  12:1216
出貨單「無」 shipN-N   1:799  2:846  3:894  4:942  5:989  6:1037   8:1132  12:1323
出貨單 全出 shipF-N    1:923  2:1018 3:1114 4:1209 5:1305 6:1400   8:1591  12:1973
出貨單 只出1 shipB-N   1:923  2:971  3:1019 4:1066 5:1114 6:1162   8:1258  12:1449
```
✅ **相鄰斜率線性**(訂單明細 47.0×6 + 47.5×1;出貨單「無」47~48)⇒ **沒有跨頁非線性**
⇒ 所以「之後每頁 N 項」可以外推。
🔴 **8 項的訂單明細只超出 4px**(1028 vs 1024)⇒ **那條線細如刀鋒。**

---

## 🔴 兩把獨立的尺, 8/8 完全一致

| | 尺一:內容高度 | 尺二:真頁數 |
|---|---|---|
| 工具 | playwright chromium + `media:'print'` | `sh scripts/pagecount.sh`(headless Chrome 印成 PDF、數 `/Type /Page`) |
| 判準 | 內容高 ≤ 1024 ⇒ 預測 1 頁 | PDF 裡真的有幾頁 |

訂單明細八點:`1/3/5/6/7/8/10/12` ⇒ 預測與真頁數 **8/8 相同**。

---

## ⚠️ 那兩行(`下單:…` / `本次出貨:N 項`)的代價 —— **而它有解析度限定**
```
在 1..12 的每一格, 拿掉它們【都沒有讓頁數改變】⇒ 門檻一格都沒有往後移
🛑 而正對照只證明這把尺看得到【650 字元(= 一個完整品項)】的差
   而那兩行只有約 60 字元 ⇒ 尺【沒有被證明】看得到那個量級
⇒ 正確字面:**小於一個品項列, 且不足以在 1..12 移動任何門檻**。不是「代價為 0」。
```
📌 而它們**不是**任何回歸的兇手:`git log -S'本次出貨:{lines.length} 項'` ⇒ `14f8a8e8`, 落地 2026-08-15。

---

## 🔴 而真正的成因不是「幾項」—— 是**餘裕是零**
```
`.pd-sheet` 的 `min-height`(⛔ ~~271mm~~ ⇒ **2026-08-29 起 250mm**, 見下)+ 頁尾 `margin-top: auto`
⇒ 內容【永遠恰好等於那個 min-height】, 不論 1 項還是 7 項
   (2026-08-29 改值前量:六份不同的紙 × 三種形狀, 全部剛好 **1024px**;
    改值後那個常數變成 **945px**, 而【永遠恰好等於它】這件事沒變)
⇒ 改值前實測:可印高 271mm ⇒ 1 頁;**269mm ⇒ 連只用 68% 的那張也變 2 頁**
⇒ **當時是 2mm 寬的懸崖;改值後懸崖在 250mm。而它與品項數、資料、字型都無關。**
```

### 🔴 2026-08-29 `min-height` 271mm ⇒ **250mm**(Sean 逐字「可以」)

🛑 **而這【不是把機制修掉】—— 是把懸崖搬家。**(2026-08-29 code-reviewer 抓到, 原句寫「已修」。)
```
`margin-top:auto` 把頁尾釘在框底 ⇒ 內容【仍然】永遠恰好填滿 min-height ⇒ **餘裕仍然是零**
⇒ 只是那個框從 271mm 變成 250mm
🔴 ⇒ 可印區低於 250mm 的裝置上, 同一件事會【一模一樣地】再發生
```
📌 **⇒ 所以正確的字面是「在 Sean 那台上不再發生」, 不是「這個問題修好了」。**
```
成因:Sean 那台 HP 的【那組設定下】可用高在 **[252, 265.6) mm** 之間
     (他實測:縮放 98% 仍兩張 ⇒ 上界 271×0.98=265.6;93% 才一張 ⇒ 下界 271×0.93=252)
     🔴 **252 是【下界】不是量到的點值** —— 而 94~97% 沒有問過。
     ⇒ 所以「≤252 保證安全」是對的, 而【不要】把 252 當成那台機器的真實能力。
⇒ 而 271mm 之下, 在他那台上【連一項的單也是兩頁】(實測 0 項 / 0 項)
實測(可印 252mm):271mm ⇒ 0 項/0 項 · 260mm ⇒ 0 項/0 項 · **250mm ⇒ 6 項/4 項**
✅ 而在我們宣告的世界(可印 271mm):271mm 與 250mm 都是 **7 項/5 項** ⇒ **容量零代價**
🔴 而【它什麼時候要重算】:換一台印表機 / 使用者改列印邊界設定 ⇒ 用同一個縮放回推法重量

```

---

## ⇒ 怎麼重跑一次(HTML 是產生得出來的, 沒有收進 repo)

```bash
# 0. 先 build —— 量測要吃【建置後的 CSS】,不是原始碼
TURBO_FORCE=1 pnpm build

# 1. 產 HTML(既有管線;它會寫進 /tmp/pcm-print-measure/)
npx vitest run --project admin \
  'apps/admin/src/app/print/orders/[id]/shipping/[shipmentId]/page-measure.test.tsx'
#   ⚠️ 它開頭會 rmSync 整個輸出目錄 ⇒ 產完【立刻把檔複製走】,否則下一次跑就沒了

# 2. 數頁數
sh scripts/pagecount.sh /tmp/pcm-print-measure/shipping-1item.html
#   exit code 三態分得開:0=成功(頁數在 stdout) · 3=Chrome 沒產出 PDF · 4=數不出頁數
#   ⚠️ 機器負載高時會 ENV-FAIL ⇒ `sh scripts/pagecount.sh --selftest` 會自己說是不是環境問題
```
🔴 **本檔那四個世界的 fixture, 現有管線只造得出其中一部分。** 要造另外三個:
```
出貨單「尚未出貨:無」⇒ 條件是 outstandingRows.length === 0(shipping-doc.tsx)
   而 outstandingQuantity = max(0, quantity − cancelled − shipped − 這箱)
   ⇒ fixture 給 quantitySummary: { quantity:1, orderedQuantity:1, instockQuantity:1,
      cancelledQuantity:0, shippedQuantity:0 } + 這箱出 1 + shippedAt: null ⇒ 得 0
🛑 而給 `quantitySummary: null` ⇒ outstandingQuantity 回 null ⇒ 而【null 一定會被列出來】
   ⇒ 那樣永遠走「有未出貨表」那條, 而你會以為自己在量正常情況
✅ 活錨(證明你在哪個世界, 而不是以為):
   ⛔ ~~走「無」那條   ⇒ expect(html).toContain('這張訂單沒有還欠客人的品項')~~
   🔴 **2026-08-30 傍晚 Sean 拿掉了那句話** ⇒ **那個字面已經不在碼裡**
      ⇒ 照這一行寫的人會拿到一個【永遠對不上的錨】, 而他不會懷疑一份被路由表背書的檔。
   ✅ 走「無」那條   ⇒ expect(html).toContain('以下空白')
   ✅ 走「有表」那條 ⇒ expect(html).toContain('這張訂單還欠客人的東西') + not.toContain('以下空白')
      ⚠️ **不可以用 `not.toContain('尚未出貨')` 當「有表」的反向錨** ——
         兩條分支都印那四個字(舊版是「尚未出貨:無 —— …」);而新版「無」那條
         連那四個字都沒有了 ⇒ **那個錨從「恆綠」變成「會過但講的是別的事」。**
```

---

## ⚠️ 這份東西量不到什麼(四行, 不可略)

1. 用的是 **fixture 資料**, 不是真訂單(品名 20 字 / 地址 17 字 / 單價六位數)。
   而真單通常更短 ⇒ 真的印會**比這裡更矮**, 不會更高。
2. 量的是 **playwright chromium / headless Chrome**, **不是任何一台印表機**。
3. 每一發都附**加總回核**(直接子元素高度含 margin 相加 vs 總高, 差 0~1px)⇒ 表上沒有漏掉的區塊。
4. 🔴 **「真印表機」那一格【至今未驗】** —— 所有量測都是 PDF 的墨水位置,
   而**不可印區是印表機的、不是 PDF 的**。要驗掉它, 要有人用實體印表機印一張、
   **照抄**紙上最下面正中央那一行字(不能問「有沒有」——那在兩個世界是同一句話)。
