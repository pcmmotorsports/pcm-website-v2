# 現在該做什麼 —— 唯一入口

> 🔴🔴 **這是唯一一份【不用維護】的待辦入口。** 它不列清單,它只教你**當場印**一份。
> 📌 **為什麼**:2026-09-08 量到,repo + 信箱裡檔名含「清單/上線/待辦/todo」的檔有 **168 支**。
> 而 `docs/launch-blocking-by-owner-20260906.md` 檔頭自己寫著為什麼不能再手寫一份 ——
> 逐字:「**一份手寫名單的過期速度 = 板子的變動速度** ⇒ 再刷一次只是把同一天往後推」。
> 🛑 **⇒ 所以本檔【永遠不列任何一件事】。列了就會過期,而過期的清單比沒有清單糟。**

---

## 壓縮之後、接手之後、不知道要做什麼的時候 —— 跑這三行

```bash
cd /Users/sean_1/pcm-website-v2
python3 scripts/launch-dispatch-table.py     # ① 現在擋上線的, 按「誰欄」分好
bash scripts/launch-blocking-count.sh        # ② 現在總共幾件(它自己會說這是快照)
```

**① 印出來的表裡找你那條線的那一節。** 目錄名 = 線名(`~/pcm-wt-<線名>`;`pcm-wt-mainB` = 退款線 `-refund`)。

---

## 🔴 挑到一列之後,動手【之前】必跑

```bash
python3 scripts/what-happened-to.py <錨>     # 那一列是不是已經被做掉了
```
📌 **理由是量到的**:板上寫 `open` **對「還要不要做」零判別力** ——
2026-09-08 一天之內,主視窗派錯三列已經做完的;而**跑了這支之後仍派錯 3/6**
(成因:`docs-only` 的 commit 也可能就是**裁掉它的那一顆**)。
⇒ 🛑 **所以還要再看一眼那一列的【末格】** —— 板列的規矩是**新的往尾巴加**,只讀開頭會拿到被推翻的句子。

---

## 唯一的真相在哪

| | |
|---|---|
| **正本** | `docs/launch-todo.md`(板)—— 所有狀態以它為準 |
| **視圖** | 上面那兩支腳本 —— **當場算,不存檔** |
| **落板** | 🛑 只有 `ship` 那條線動板檔。別條線寫「落板文字」寄它 |

🔴 **而 `~/pcm-mailbox/` 不是 repo,它不會跟著 push** ⇒ 那裡的每一份清單都是**一張會消失的快照**。

---

## ⛔ 這些是歷史快照,**不要拿它們當待辦**(留著是為了它們的【理由】不是【名單】)

```
docs/launch-blocking-by-owner-20260906.md   分堆的理由與「碰哪些檔」那一層不會過期, 名單會
docs/launch-status-20260906.md              白話版摘要, 綁在 origin/dev = 41443a6c2 那顆 sha 上
~/pcm-mailbox/上線清單-活的-20260902.md      11 列, 2026-09-02;其中三支 migration 今天實查【已貼】
~/pcm-mailbox/派工-74件擋上線-主視窗A-20260908.md
~/pcm-mailbox/工作佇列-主視窗A-20260908.md   ⇒ 它的 P1/P2 已全部做完;P3 要推完才動得了
```
📌 **判別句**:一份清單如果**存了一個數字或一串錨**,它從存下來那一刻就開始過期。
⇒ **要用它,先問「它是哪一顆 sha 上的」;答不出來 ⇒ 當歷史讀,不當待辦讀。**

---

## 🔴 而有一件事這兩支腳本答不出來

它們數的是**板上 token 有沒有寫**,**不是判得對不對**,也**不涵蓋沒有人盤過的面**。
⇒ `docs/launch-todo.md` 檔尾有一節「**這張板子沒涵蓋什麼**」—— **接手時先讀那一節。**
