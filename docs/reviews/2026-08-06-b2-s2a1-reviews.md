# B2-S2a-1(migration 片)審查總帳 —— code-reviewer R1 + codex 關卡2 R1/R2 + Fable R3

> 2026-08-06,a4a-chain 施工窗。受審 = staged diff 三個檔
> (`supabase/migrations/20260806100000_m4b_e10_b2_s2a_summary_shipped_quantity.sql` 新檔、
> `docs/runbooks/a4a-summary-rollback.md`、`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md`)。
> 依 Sean 2026-08-06 拍板 **Q1=A**:片級 plan 已凍結、只當意圖文件,
> **plan §0.6 的四處已知壞點與關卡1 R2 的 16 條一律在本 diff 修**,不回頭改 plan。
> 依 **Q2=A**:量到的數字寫在本檔與 commit body,**不回填 plan**。
> 依 **Q3=C**:plan §3.5 的 quarantine 機制整節作廢不實作(理由與代償寫在 migration 檔頭)。

---

## 0. 實跑證據(本片所有數字的唯一來源)

| 項 | 命令 / 環境 | 結果 |
|---|---|---|
| 從零 provision + 套全套 migrations(含本片) | PG17、`initdb` 拋棄庫 **port 54351**、workdir `/tmp/b2s2a` | ✅ 綠;本片輸出 `S2A 結構驗收全數通過(6 欄 / 10 CHECK / 7 註解物件)` |
| 重跑本片 | 同庫 | ✅ `ERROR 42701 duplicate column`(只證「拒絕重跑」,**不證原子回復**) |
| 突變 A:必含 COMMENT 清單刪一列(`cmp` 確認有差異) | 同庫、退回 pre-S2a(⚠️ 用 DROP 捷徑,見 §3c) | ✅ 紅在 `S2A-COMMENT-COUNT:必含清單應恰七個物件,實跑 6 圈` |
| 突變 B:C9 的 `<=` 改 `<`(`cmp` 確認有差異) | 同上 | ✅ 紅在 `S2A-C-DEF-MISMATCH`(印出期望與實際兩個定義字串) |
| 🔴 突變 C:刪掉 `COMMENT ON COLUMN shipment_items.shipped_quantity` 整句 | **乾淨 pre-S2a**(排除本檔的從零 provision、port **54353**、`/tmp/b2s2apre`) | ✅ 紅在 `S2A-COMMENT-ANCHOR`(同一份突變檔在 DROP 捷徑狀態下**全綠**,見 §3c) |
| 🔴 突變 D:保留必含錨、把舊字面塞回去 | 同上乾淨庫 | ✅ 紅在 `S2A-COMMENT-STALE` |
| 對照組:真檔 | 同上乾淨庫 | ✅ 綠 `S2A 結構驗收全數通過(6 欄 / 10 CHECK / 7 註解物件)` |
| 行為 smoke ①C9 負測 `q/o/i/c/s = 10/9/2/4/3` | seed 後 `BEGIN…ROLLBACK` | ✅ `23514 / oiqs_shipped_le_instock` |
| 行為 smoke ②C8 負測 `10/9/2/4/-1` | 同上 | ✅ `23514 / oiqs_shipped_nonneg` |
| 行為 smoke ③C9 邊界正測 `shipped = instock = 2` | 同上 | ✅ 落庫回查 = 2 |
| 行為 smoke ④C8 零值正測 `shipped = 0` | 同上 | ✅ 落庫回查 = 0 |
| 三綠 | worktree 根 | ✅ `pnpm typecheck` / `pnpm lint` / `pnpm build` **RC 全 0** |

🔴 **三綠的環境註記(重要,別當成繞道也別當成沒事)**:
`pnpm typecheck` 一開始 RC=1,turbo 那 8 格全綠、紅在尾段的 `tsc -p tsconfig.scripts.json --noEmit`
報 `command not found`。**根因不是本 diff**:root `package.json:15` 呼叫 `tsc`,但 root `package.json`
**沒有宣告 typescript 這個 devDependency**;主 repo 只是靠一個過期的 hoisted bin 才碰巧跑得動
(主 repo `node_modules/.bin` 有 15 個,本 worktree 只有 8 個、沒有 `tsc`)。
`pnpm install --frozen-lockfile` 實測無效(回 `Already up to date`)。
本輪處置 = 在本 worktree 補回 `node_modules/.bin/tsc → ../.pnpm/typescript@5.9.3/…` 的連結
(**不進 git**),讓官方命令真的跑完 ⇒ 現在 RC=0。
🔴 **repo 層缺陷本身未修**,另立一片(見 §4 交棒)。

---

## 1. code-reviewer(opus)R1 —— **FAIL / 5 must-fix + 7 nit**,全數處置

| # | 級 | finding | 處置 |
|---|---|---|---|
| 1 | must-fix | runbook `:182` 仍寫「連帶**七條** CHECK」,同檔 5 行外已改 6 欄/10 CHECK ⇒ 典型「只改碰到的行」 | ✅ 改「十條(A1 七 + S2a 三)」 |
| 2 | must-fix | runbook 叫人重放 S2a,但本片 §1 的閘在出貨線上線後**必然 abort**,無替代路徑 | ✅ 加路徑 A / B 兩條(A = 只跑 §2 的 ALTER + §3 COMMENT + §4 驗收) |
| 3 | must-fix | 「必須不含」迴圈 fail-open:CASE 無 `ELSE`,漏加 WHEN ⇒ `position(x IN NULL)` 為 NULL ⇒ IF 不觸發、靜默全綠 | ✅ 兩個 CASE 都補哨兵 `'((S2A-UNMAPPED))'` + 該迴圈補 `IS NULL` 擋 |
| 4 | must-fix | 檔內宣稱「契約債 ① 已清償」,但 A1 那條債逐字要求**同時寫進 master plan**,master plan 仍是舊字面 | ✅ master plan 四處同 commit 更新(施工歸屬 ×2、三軸計數 ×2) |
| 5 | must-fix | quarantine(plan 訂為硬前置)沒做,檔頭完全沒提 | ✅ 檔頭新增「刻意不做」整段:Q3=C、Fable F3 理由、代償 ①②③、殘餘風險 |
| 6 | nit | 檔內「六個物件」vs VALUES 七列 vs NOTICE 七 | ✅ 統一七 |
| 7 | nit | 「五條跟 quantity 比」把 C5 算間接卻不算 C9,分類不一致 | ✅ 改「**四條直接跟 quantity 比**(C4/C6/C7/C6′)」,間接的 C5/C9 明寫不計入 |
| 8 | nit | 排除錨 `零效力` 是 plan v1 措辭、**從未進過 DB** ⇒ 零判別力 | ✅ 整列刪除(新欄本來就沒有舊字面可排除) |
| 9 | nit | 回滾 preflight 的 `pg_get_functiondef` 對 aggregate 直接 ERROR ⇒ 長得像「查無」而放行 | ✅ 加 `p.prokind = 'f'` 並註明理由 |
| 10 | nit | 建議用 `pnpm install --frozen-lockfile` 讓官方 typecheck 自己綠 | ⚠️ **實測無效**(見 §0 註記);改走 bin 連結修復 + 另立一片 |
| 11 | nit | `scripts/b2s1-concurrency-probe.sh` 未追蹤 | ✅ 前一棒刻意留、不屬本片;精準 add 未帶到,STOP 內說明歸屬 |
| 12 | nit | apply 停點與 `database.types.ts` 重生債需要落點 | ✅ 寫進 migration 尾段 apply 說明 + STOP 交棒節 |

**R1 查過沒發現的面(原樣收錄,因為它是判別力的證據)**:七條必含錨與五條排除錨逐條對照 A1
`:140-162` 與 S1b `:92-94` 的舊字面,**沒有任何一條是舊字面本來就含有的**;C6′ 的代數冗餘宣稱獨立推過屬實;
檔頭四個宣稱(不用鎖 / C7 不動 / 零 trigger 零函式 / 「沒有 writer 會去觸發它」)與檔內 SQL 一致;
`§4g` 的零 trigger 不會被 A4a 四支 trigger 誤紅(那四支全掛在來源表)。

---

## 2. codex 關卡2 R1 —— **FAIL / 18 must-fix**,全數折入

| # | finding(壓成一行) | 處置 |
|---|---|---|
| 1 | 檔頭說「ALTER 取 ACCESS EXCLUSIVE 會等出貨交易」是**錯的** —— 那把鎖只鎖摘要表,與出貨兩表無關 | ✅ 「殘餘 race」整段重寫:閘只是快照、ALTER 不會等、閘後寄出的包裹不會被反映且零告警 |
| 2 | 閘用 `sum()` ⇒ 正負可互抵成 0 | ✅ 改**有效已寄出的品項列數**(`count(*)`),沒有互抵面 |
| 3 | 「欄加完恆為 DEFAULT 0」與誠實邊界自相矛盾 | ✅ 改「一律是欄預設 0…**這不是 DB 強制恆 0**」 |
| 4 | 把不存在的 `scripts/b2s2a-verify.sh` 寫成**現有**前置閘 | ✅ 兩處都改「由後續片 S2a-2 交付、**目前尚未存在**;在那之前執行者是 apply 當下的人」 |
| 5 | `S2A-APPLY-NOTE` 全檔出現兩次 ⇒ presence oracle 恆綠 | ✅ 檔頭那處不再寫出該字串,全檔只剩一次 |
| 6 | 欄集合只數 6 ⇒ 改名但仍六欄照樣綠 | ✅ 改逐字比對**欄名集合字串** |
| 7 | 「A1 七條原封不動」只數名字、只比 C7 定義 | ✅ 改**逐條定義字面 + `convalidated` + 圈數自斷言(7)** |
| 8 | COMMENT manifest 沒斷言恰七列,NOTICE 卻寫死七 | ✅ 兩份清單各自加圈數自斷言(7 / 4)—— 即 Fable **F2** 的落點 |
| 9 | 「零 trigger / 零函式」大於該斷言能證明的範圍 | ✅ 訊息改「**摘要表**零 trigger」,並註明 diff 層宣稱由 review 負責 |
| 10 | `shipment_items` 註解宣稱 C9 已是強制點,但 S2b 前 C9 只檢查恆 0 的預設值 | ✅ 加「**對真實出貨量尚未生效**;強制鏈要 S2b 上線才閉合」 |
| 11 | 回滾 preflight 只 SELECT、不 RAISE、未與 DROP 同交易 | ✅ 改成會 `RAISE` 的 DO block,並放進可貼的 `BEGIN…COMMIT` 內、排在 DROP 之前 |
| 12 | 還原成 A1 原文會把已修好的兩個錯誤放回 catalog | ✅ `quantity` 欄與 C7 兩處**明令不得**還原 A1 原文,替代字面逐字寫死 |
| 13 | runbook 停寫只 REVOKE 採購側,未涵蓋 S2b 的出貨 writer | ✅ 步驟① 加契約債(S2b 上線同批補);現況無該路徑 ⇒ 暫時完整 |
| 14 | runbook divergence 只對帳三軸 ⇒ S2b 後 shipped 漂移全綠 | ✅ 建表前加契約債(S2b 同批補 snap/truth 兩欄) |
| 15 | runbook 寫「三段 ALTER」,實際是一個 `ALTER TABLE` 內含四個 ADD | ✅ 改正 |
| 16 | master plan mapper 仍寫「三個 0」(**其中一處是我自己的修法引用了舊字面**) | ✅ 兩處都改四個 0;修法不再引述舊字面 |
| 17 | master plan 頭寫 C9「由 A4a row-level 重算維護」,S2b 前不成立 | ✅ 加「落地分兩片,S2b 接線才成立」 |
| 18 | `pnpm typecheck` RC=1 未滿足鐵則 11 | ✅ 根因查明 + 環境修復,三綠現在 RC 全 0(見 §0) |

**零留痕**:跑前後 `git status --porcelain` 逐字相同。

---

## 3. codex 關卡2 R2 —— 見下

**FAIL / 7 must-fix + 1 nit,全數折入。**

🔴 R2 第一次跑被我設的 10 分鐘 tool timeout 砍在輸出 findings 之前(**不是審查者跑不完**),
只留下一行「三個阻擋點」。我照那一行自行定位並修了三處,再用背景跑補完同一輪的完整 findings。
補跑確認我修對了三處,並另抓出四條。

| # | finding | 處置 |
|---|---|---|
| 1 | runbook 替代路徑 A 只說跑 §2–§4,**沒有交易邊界** ⇒ psql autocommit 下 §4 紅掉時 ALTER 與 COMMENT 已分段提交 | ✅ 明令自補 `BEGIN;…COMMIT;`,並寫出「半套狀態」的具體後果 |
| 2 | 回滾 preflight 與 DROP 同交易**仍擋不住並行的 S2b**(CREATE FUNCTION 不碰本表的鎖) | ✅ 認列為誠實邊界 + 立操作前提(回滾期間不得有 migration/部署在跑、完成後重跑一次 preflight) |
| 3 | 「可直接貼的完整回滾交易」裡 COMMENT 還原是 **placeholder** ⇒ 實跑什麼都不會還原 | ✅ 五句 `COMMENT ON …` 逐字寫出 |
| 4 | 🔴 **最尖的一條**:A4a 的 `ON CONFLICT DO UPDATE` **不碰** `shipped_quantity` ⇒「寫回的永遠是欄預設值」是錯的 | ✅ 親查 `20260803140000_…a4a….sql:183-187` 屬實。改成:新建列取預設 0、**既有非 0 值原樣保留、A4a 不會清掉**;並補一條新風險(見下) |
| 5 | `shipment_items` 註解寫 C9「檢查的是恆為 0 的預設值」,與「owner/SECDEF 寫得進去」矛盾 | ✅ 改「C9 對當下欄值永遠有效;S2b 前沒有 writer 把真實出貨量搬過去 ⇒ 它看不到那個數字」 |
| 6 | runbook divergence 契約債用「S2a 階段 shipped 恆 0 ⇒ 暫時無漏」 | ✅ 改「沒有 writer 會產生第四軸漂移,所以這個盲點目前不會被觸發」 |
| 7 | master plan 我新增那行又寫「第四軸恆為 0」 | ✅ 同上口徑改寫 |
| 8 | `git diff --staged --check` 因行尾空白失敗 | ✅ 剝除(1 行) |

🔴 **由第 4 條連帶查出、本片新記的營運風險**:C9 上線後,任何殘留的非 0 `shipped_quantity`
會讓**日後合法的 A4a 重算**在 instock 下降時紅在 `23514 / oiqs_shipped_le_instock` ——
紅的是無辜的到貨更正交易。已寫進欄註解與檔頭誠實邊界。

**零留痕**:跑前後 `git status --porcelain` 僅多出我自己在等待期間建立的本檔,codex 零異動。

---

## 3b. 第三輪:adversarial-reviewer(**model = fable**,換模型換角度)—— **PASS / 0 must-fix**

依 `00-work-rules` §5(Sean 2026-07-29 口頭推翻輪次上限):輪次不設限,但**第 3 輪起必須換角度、換模型**。
本輪四個角度前兩輪都沒用過:①假設審查 ②災難當天可用性 ③修法回歸 ④測試假綠。

| # | 級 | finding | 處置 |
|---|---|---|---|
| F1 | consider | **C9 的承重代價沒被寫出來**:它比的是同列**兩個衍生快取值**(instock 由 A4a 維護、shipped 將由 S2b 維護)⇒ 強度 = min(兩者正確性);壞法是「超額出貨當下零違規提交,等下一筆 A4a 把 instock 修正下來才 23514」⇒ **紅在無辜交易** | ✅ 寫進 C9 的原始碼註解,並**明文交棒**:S2b plan 必須把「stale-high instock 讓超出貨靜默提交、事後紅在誰身上」列為負測情境 |
| F2 | nit | 回滾 preflight 的 `LIKE '%shipped_quantity%'` **分不清 `shipment_items` 的同名欄** ⇒ 未來合法 RPC 會讓它假陽性,凌晨三點的人無從判斷 | ✅ 錯誤訊息與查詢各補一句提醒「同名欄會一起命中,逐支人工判讀」 |
| F3 | nit | `S2A-APPLY-NOTE` 的修法是「把恆綠改成弱」——仍只證區塊存在、不證內容正確 | ⚠️ 接受;判別力落在 S2a-2 harness。**交棒註記:S2a-2 驗收時不得又縮回 presence 檢查** |
| F4 | consider | 🔴 **兩個已跑突變沒有覆蓋 COMMENT 家族任何一條斷言** —— 突變 A 刪的是 harness 自己的清單列(只證自簿記)、突變 B 走 constraint 路徑 ⇒ `ANCHOR / MISSING / STALE` 三族**從未被證明會紅**。建議第三發突變 = 刪掉 §3 的 `COMMENT ON COLUMN public.shipment_items.shipped_quantity` | ✅ **當場實測**(見 §3c) |
| N3 | nit | 檔頭引用 A4a 的行號寫 `:181-188`、檔名尾綴不符 | ✅ 改成 `20260803140000_m4b_e10_a4a_quantity_summary_recompute.sql:183-187`(親 grep) |

Fable 明文**不推翻** Q1=A、Q3=C、「C9 唯一強制點」三個拍板;C6′ 的代數蘊含它獨立推導成立。

---

## 3c. 🔴 Fable 的翻案條件②當場實測 —— **先假綠、再證實是我的測試捷徑污染**

Fable 的 `WOULD-CHANGE-MY-VERDICT` ② 逐字:「實跑證明刪掉 §3 任一句 COMMENT 後 §4 仍印出全數通過
⇒ F4 從 consider 升 **BLOCKER**:七物件驗收整段是裝飾」。我當場跑了:

| 步 | 狀態 | 突變 | 結果 |
|---|---|---|---|
| ① | 我原本的「pre-S2a」= 在已套過本片的庫上 **DROP 欄 + 三條約束** | C:刪掉 forward-override 那句 COMMENT | 🔴 **全綠** —— 翻案條件成立 |
| ② | 診斷 | — | **DROP 欄不會還原註解** ⇒ 上一次成功套用留下的**正確註解還在 catalog** ⇒ 斷言照樣通過 |
| ③ | 真正的 pre-S2a = **排除本檔的從零 provision**(新拋棄庫 port 54353、`/tmp/b2s2apre`),親驗 `shipped_quantity` 欄不存在、`shipment_items` 註解仍含舊字面「強制點未定案」 | C(同一份突變檔) | ✅ 紅在 `S2A-COMMENT-ANCHOR:… 缺少錨「oiqs_shipped_le_instock」` |
| ④ | 同一乾淨庫 | D:保留必含錨、把舊字面塞回去(只讓 STALE 那條可紅) | ✅ 紅在 `S2A-COMMENT-STALE:… 仍含過期字面「強制點未定案」` |
| ⑤ | 同一乾淨庫 | 對照組(真檔) | ✅ 綠 `S2A 結構驗收全數通過(6 欄 / 10 CHECK / 7 註解物件)` |

⇒ **產品沒有缺陷,COMMENT 家族的三條斷言都有判別力**;假綠來自**我的測試捷徑**。

🔴 **這條必須寫進 S2a-2 的 harness 設計(交棒硬要求)**:
plan §7 的「pre-S2a 可重建狀態」**只能是「排除本檔的從零 provision」**,
**不得**用「DROP 欄 + 約束」當捷徑 —— 那個捷徑會讓**整個 COMMENT 驗收家族靜默失去判別力**,
而且症狀就是「突變全綠」,長得跟「斷言是裝飾」一模一樣。
(同型教訓:memory `feedback_negative-test-observation-supplied-by-another-mechanism`。)

---

## 4. 交棒(不屬本片、但已定位)

| # | 落在 | 內容 |
|---|---|---|
| 1 | **另立一片(建議指給主線,不在本分支)** | 🔴 root `package.json` 未宣告 `typescript`,卻在 `typecheck` script 呼叫 `tsc` ⇒ **任何全新 clone 或 worktree 的 `pnpm typecheck` 都會紅在最後一步**。主 repo 只是靠過期 hoisted bin 掩蓋。修法 = 把 typescript 加進 root devDependencies(會動 lockfile ⇒ 該進 dev、不該混進本分支)。 |
| 2 | S2a-2/3/4 | `scripts/b2s2a-verify.sh` 三段(結構 / 行為 / 回滾與故障注入)、完整突變靶、runbook rehearsal 驗六欄十 CHECK。**F1 判準「apply 當下任一出貨表非 0 → 停、問人」的落點就是 S2a-2 的第一格。** |
| 3 | 大線 S2b | helper 四軸化、`shipments` 重算 trigger、真值 backfill、oracle 四軸化、a1/a4a-verify 修復、port 54329 同埠地雷 |
| 4 | Sean apply 之後 | `database.types.ts` 重生 → nullable 校正 → `pnpm typecheck` |
| 5 | S2b 同批 | runbook 步驟① 的出貨側停寫、步驟② divergence 的 shipped 兩欄(兩條契約債已寫進 runbook) |

## 5. 誠實邊界(收割時原樣帶)

- 本片**只挖格子**:`shipped_quantity` 落地後實務上恆 0,因為**摘要表的唯一 writer A4a 還不算第四軸**。
  C9 這個「強制點」要等大線 S2b 接線才對真實出貨量生效 —— 現在它檢查的是預設值。
- 突變只跑了**兩靶**(COMMENT 圈數、C9 定義),不是 plan §5 的十一靶;其餘在 S2a-2/3/4。
- 行為 smoke 的 fixture 是**臨時把一筆 order_item 的 quantity 改成 10**(`line_total` 同步)後在
  `BEGIN…ROLLBACK` 內做的,**不是** harness 的正式 fixture 設計。
- 檔內 §4 只驗結構 / 定義字面 / 註解字面;**「本片零函式、零 RPC、出貨兩表零新 trigger」是 diff 層的宣稱**,
  由 review 背書,不是那段 SQL 能證明的。
- 三綠的 `typecheck` 是在**手動補回 bin 連結之後**才綠的(§0);repo 層缺陷仍在。
- 本片**未 apply 任何正式環境、未併 dev**。
