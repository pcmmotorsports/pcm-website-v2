# A5a 關卡1 對抗審查逐字紀錄(codex gpt-5.6-sol xhigh、read-only)

> 對象 = docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md v1;判定 = NO-GO(21 must-fix + 3 nit、駁回 0)。
> v2 折入對照:F1→ALLOCATED_BELOW_RECEIVED 碼、F2→SET CONSTRAINTS IMMEDIATE、F3→no-op 先於停用檢查、F4/F5→Q1 選項重整(C 撤)、F6→clock_timestamp、F7→B3b 逐欄矩陣、F8→C 區衝突矩陣、F9/F10→B19 barrier 加嚴、F11→v_row_id、F12→呼叫紀律合約、F13→B20、F14→結構 DO 補 order_items、F15→IS DISTINCT FROM、F16→lower() 錨+禁動態 SQL、F17→家族序補 a4b、F18/F19→runbook drain+口徑、F20→§1 措辭、F21→業務日入債、F22-24(nit)→措辭/版本序/七隱形字。
> 執行插曲(誠實記錄):R1 第一次執行在 web search 時程序死亡、無判定;第二次禁 web 後 17 分鐘收斂。

## R1 原文

1. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:66-70,116-123`；`supabase/migrations/20260729020000_m4b_e10_a2_order_item_procurement.sql:75-77` — 已到貨 4 件的列把 allocated 5→3，會先撞 `received_range` 的 raw 23514，未落入任何固定碼。— must-fix

2. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:68,117-123,158,220`；`supabase/migrations/20260803130000_m4b_e10_a2b1_procurement_allocation_guard.sql:171-179` — guard 被 defer 時 P2B01 不在 DML catch 內發火；只 defer guard 會先漏成 A4a 23514，兩支都 defer 則 COMMIT 才冒 raw P2B01。— must-fix

3. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:69,96-101,203` — 成功建立後供應商被停用，再重放完全相同 payload 會先回 `SUPPLIER_INACTIVE`，不會是宣稱的 `NO_CHANGE`；A9h 重送冪等被狀態轉換擊破。— must-fix

4. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:96-100,203` — 步驟 11 無條件在讀採購列前拒 inactive，物理上無法實作 Q1=A/C 的更新例外；Q1 尚未拍板，plan 不是可施工狀態。— must-fix

5. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:203`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:379` — Q1=C 允許停用後調升既有 allocated，等同繼續向停用供應商追加訂購；除非明確改 master，C 不是合規選項。— must-fix

6. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:116,137,166`；`supabase/migrations/20260802150000_m4b_e10_a6_admin_append_order_note.sql:35-36` — `now()` 是交易起始時間；同交易兩次真更新可得到相同 `status_changed_at`，與「每次更新／B3 必須動」矛盾。— must-fix

7. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:69,164-177,184-190` — 更新測試只釘 reply/allocated，未逐欄釘 contact、submitted、order no、reason、arrival；漏 SET 或漏比較任一欄仍可能全綠，也未釘非 NULL→NULL→重放。— must-fix

8. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:72-102,174-190` — §3.3 宣稱固定碼順序會「逐對突變」，但 §5 沒有衝突輸入矩陣；交換 allocated/reply 等檢查順序，所有單一可達性案例仍會綠。— must-fix

9. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:124-125,182-183` — 「23505 後第二圈必見列」沒有 repo 既有實測依據；B19 未要求第二連線先確認阻塞於 unique，若它在首筆 COMMIT 後才開始，根本沒走 catch 也會假綠。— must-fix

10. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:182-183` — B19 接受 `NO_CHANGE/UPDATED` 任一結果，沒有釘兩連線 payload 是否相同；錯誤更新或錯誤 no-op 都可被聯集斷言吞掉。— must-fix

11. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:112-121,130-132,164-166` — `v_row_id` 只在 INSERT RETURNING 被賦值；UPDATE 稽核若照草圖使用它，`target` 會合法落 NULL，而 B3 未要求核對 update target。— must-fix

12. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:136`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:250-254` — 「無環」未限定單次呼叫：T1 A5a 首建後同交易呼叫 S2、T2 同鍵 A5a 等 unique，可形成 supplier SHARE 升 FOR UPDATE 與 unique wait 的循環。— must-fix

13. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:98-99,158,170-190` — 沒有 A5a×S2 barrier 格；普通 SELECT 做 active 判斷、稍後在死枝補一個 `FOR SHARE`，文字計數、B7、M1 都能綠但 TOCTOU 仍在。— must-fix

14. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:92-95,158` — SECDEF 會讀 `order_items`，但結構 DO 的 owner/權限集合漏了它，也未釘相關表 `relforcerowsecurity=false`；migration 可綠、首呼叫卻 42501 或假 `ORDER_ITEM_NOT_FOUND`。— must-fix

15. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:68,117-123`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:219-224,277-281` — 若照 S2 用 `v_con <> expected`，`CONSTRAINT_NAME` 為 NULL 時 IF 判定也是 NULL，非目標 P2B01/23505 會被吞；須釘 `IS DISTINCT FROM`。— must-fix

16. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:158` — 禁止錨是大小寫敏感字串且未禁動態 SQL；小寫 `for no key update` 或 `EXECUTE` 可繞過零次斷言，正向錨亦可放死枝滿足。— must-fix

17. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:192-193,212` — 宣稱 A4b 不受污染，家族序卻漏跑既有 `a4b-concurrency-probe.sh`，也沒有 A5a×receipts 的交錯格；與 receipts sync「無環」未被驗證。— must-fix

18. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:197-199`；`docs/runbooks/a4a-summary-rollback.md:124` — REVOKE 沒有 repo-backed 的既有呼叫 drain 證明；已進函式的交易可在快照後提交，且既有 divergence 成員會被步驟⑤的成員式檢查放過。— must-fix

19. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:199`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:303-306` — runbook 只要求「零 owner 手動寫入」，漏掉 repo 已明列的 owner 憑證服務與 pg_cron；停寫前提小於實際權能面。— must-fix

20. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:16-17,145` — §1 宣稱不存在繞過稽核的寫入路徑，§3.6 又承認 owner 可直寫；只能宣稱「service_role 應用路徑唯一」。— must-fix

21. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:137,209-210,220`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:379` — Asia/Taipei 業務日只口頭丟給未建 A9a，未進驗收或 contract 債；「row 32 五合約逐條有綠格」大於事實。— must-fix

22. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:98-99`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:250-254` — FOR SHARE 確實會擋 S2；但 S2 已先取 FOR UPDATE，所以 KEY SHARE 也會擋這條特定路徑。FOR SHARE 額外保護的是裸非鍵 UPDATE，理由需分清。— nit

23. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:11` — 版本序寫成 A4a `140000` > RW1a `150000`，數字相反；正確為 A5a `160000` > RW1a `150000` > A4a `140000`。— nit

24. `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:82-87`；`supabase/migrations/20260802150000_m4b_e10_a6_admin_append_order_note.sql:106-110` — contact/reason 的空白判定漏 repo 已知的 U+2800、U+3164、U+00AD；肉眼全空值仍可作非 NULL 入庫。— nit

查無：預設 immediate 的 P2B01 被 EXCEPTION 捕捉時，子交易會回滾該 DML 與 trigger 效果，沒有半套持久化；破口是 deferred 情境逃出 catch。

NO-GO


## R2(codex 同模型確認輪,對象 = plan v2;判定 = NO-GO,9 must-fix;R1 18/24 已關)

> v3 折入對照:R2-1→received_quantity 錨改讀取形式、R2-2→C 區相鄰對列全、R2-3→Q1 待 Sean(唯一未關)、R2-4→拍板後同步 master plan :379、R2-5→11g 單一落點 + B20b、R2-6→B21、R2-7→drain 改 xact_start 錨、R2-8→時間界端點寫死、R2-9→業務日來源欄位寫死。

R1-1 已關（procurement `FOR UPDATE` 與 receipts sync `FOR NO KEY UPDATE` 互斥，預檢無競態）。

R1-2 已關（repo 既有 B13c 證實 `SET IMMEDIATE` 會回沖 pending events；B16b/M8 可釘後續 DML。先前事件提早發火的歸因成立）。

R1-3 已關（no-op 分支零 DML，不會漏進應擋寫入；但與 master 字面衝突，見 finding）。

R1-4 未關（Q1 仍未拍板，11b 與 B8 仍不可定稿）。

R1-5 已關（不合規的 C 案已撤）。

R1-6 已關（兩路均改用 `clock_timestamp()`）。

R1-7 已關（B3b 已覆蓋七欄、NULL 轉換與重放）。

R1-8 未關（C 區仍是未列完的占位，不是真正矩陣）。

R1-9 已關（B19 FIFO + blocker 指認可實作；該交錯中 A 其餘鎖皆與 B 相容，阻塞點可歸於 unique）。

R1-10 已關（同 payload 與唯一預期 `NO_CHANGE` 已釘死）。

R1-11 已關（update 明確賦 `v_row_id`，B3 核對 target）。

R1-12 已關（單交易單呼叫、不混 S2 已升為 migration／下游合約）。

R1-13 未關（B20 只驗首建路徑，既有列更新路徑仍可漏掉 supplier lock）。

R1-14 已關（結構 DO 已納入 `order_items` owner／RLS）。

R1-15 已關（兩個 catch 均要求 `IS DISTINCT FROM`）。

R1-16 已關（大小寫與動態 SQL 洞已補；但新文字錨自相矛盾，見 finding）。

R1-17 未關（家族序補了 a4b，但仍沒有 A5a×receipts 交錯格）。

R1-18 未關（drain SQL 不可正確證明停寫）。

R1-19 已關（owner 手動 SQL、pg_cron、持 owner 憑證服務均納入口徑）。

R1-20 已關（唯一性已收斂為 service_role 應用路徑）。

R1-21 未關（雖已入債，但未指定業務日應由哪個 server timestamp 推導）。

R1-22 已關（FOR SHARE 額外保護範圍已正確說明）。

R1-23 已關（migration 版本序已修正）。

R1-24 已關（三文字欄已納入七種隱形字）。

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:69,108,157` — 正確預檢必讀 `received_quantity`，但結構 DO 又要求該字串出現 0 次；正確函式必被自身 migration 驗收拒絕。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:75-86,185` — C 區漏 0/0b、0b/1、1/2、5/6、7/8 與 11b 子順序，卻列非相鄰 0b/2；調序 mutant 仍可全綠。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:172,207-212` — Q1 未拍板，停用既有列的實作條件與 B8 預期仍是變數；A/B 雖都能寫，但本版尚不可施工。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:17-20,103-116,209-212`; `docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:379` — no-op 不漏寫入，但 A 允許事實更新、兩案都允許 inactive no-op，與 master「拒收停用供應商」字面形成雙權威；須同步澄清例外。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:107,144,184` — B20 只跑無列首建；若 `FOR SHARE` 僅放 11c、11b 用普通 SELECT，anchor、B7/B8、B20 仍綠，S2 可在更新決策後停用供應商。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:69,108,136,170,196`; `supabase/migrations/20260803140000_m4b_e10_a4a_quantity_summary_recompute.sql:288-312` — 現有鎖語意確實無競態，但沒有 A5a×receipts barrier；錯把 update-path `FOR UPDATE` 放死枝時，receipt 可插入預檢與 UPDATE 之間，冒 raw 23514 而既有格仍綠。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:202-205` — drain 查詢會命中自己目前的 active SQL，count 永不為 0；即使排除自身，也會漏掉 RPC 後改跑其他 SQL、尚未 COMMIT 的舊交易。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:96-98,175` — `submitted_at` 只寫「界外」，未定 2020/2100 邊界及是否含端點；不同實作者可產生不同固定碼行為且都自稱符合。— must-fix

`docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:139,218,230-231`; `docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:379` — 業務日債只寫 `AT TIME ZONE`，未指定以 server 產生的 `first_ordered_at`、`status_changed_at` 或 caller-controlled `submitted_at` 為來源，無法判別是否符合「server 端算」。— must-fix

NO-GO（9 must-fix）

## R3(換模型換角度:adversarial-reviewer/Fable;對象 = plan v3;判定 = NO-GO,4 must-fix + 3 nit)

> 視角 = 假設審查 / 災難日可用性 / 修法回歸 / 測試假綠(00-work-rules §5 輪次紀律:第 3 輪起換角度換模型)。
> v4 折入對照:F1/F2→C 區兩不可構造格刪除重寫、F3→債② lost-update 防線、F4→B13/B14 三欄逐欄、F5→§3.5 殘文、F6→債⑥ SET CONSTRAINTS 交易域、F7→債④ qty=1 owner 處方。

F1|plan C 格 (11b①,11b②)「低於 received 且超量」不可構造:new<received≤old ⇒ 必為調降,A2b1 調降跳過 ⇒ 撞不到 P2B01;SKIP 0 下此格只能假綠或卡死。— must-fix
F2|plan C 格 (11g,11b①)「停用+調升至低於 received」自相矛盾(調升恆不低於 received);漏可構造的 (11g,11b②) 停用+調升超量 → SUPPLIER_INACTIVE 先。Q1=B 才存在調降版本。— must-fix
F3|全量 payload + 無 CAS:未帶滿七欄的呼叫端清 NULL;兩員工並發編輯後送者靜默蓋掉前者(月 100-300 單多人後台)。債② 需明文「先讀最新列再送全量 + A10b 全欄 hydrate」。— must-fix
F4|B13/B14 只以 supplier_order_no 舉例:channel/reason 首尾空白無 A2 CHECK 後盾,漏 btrim 全 harness 照綠、乾淨值重放回 UPDATED = A9h 冪等靜默破功。— must-fix
F5|§3.5「僅 11b 與 11c」是 v2 殘文,與 11g 單一落點矛盾。— nit
F6|SET CONSTRAINTS 交易域、RETURN 後不回復,migration 註解應載明。— nit
F7|qty=1 選錯供應商 → 正確家永遠 OVER_ALLOCATION,債④ 需 owner SQL 當日處方。— nit

R3 正面確認(假設面):SET CONSTRAINTS 兩目標名實存皆 deferrable;isolation GUC 與 SECDEF 無交互(A2b1 已上線實證);有列時 supplier 必存在(FK+不可刪);B21 鎖語意成立(sync UPDATE 的 NKU 列鎖與 FOR UPDATE 互斥、RC 鎖下重讀新值);PostgREST 單 statement 交易 ⇒ B16b 屬縱深防禦。

## R3 閉環確認(同審查者、對象 = plan v4)

F1-F7 逐條已關(F1/F2 的「無共同輸入」宣稱經 A2b1 `:125-129` 核實;F7 的 owner DELETE 處方經 A4a trigger AFTER I/U/D + receipts FK RESTRICT 抽驗成立);v4 改動段零新洞。**PASS**(Q1 拍板 = 已知實作前置,不在本輪範圍)。

## 關卡1 總結

R1 codex 21MF+3nit → R2 codex 9MF(R1 18/24 已關)→ R3 Fable 換角度 4MF+3nit → 閉環 PASS。累計 34 must-fix + 6 nit、全折、駁回 0。唯一開口 = §7 Q1(Sean)。
