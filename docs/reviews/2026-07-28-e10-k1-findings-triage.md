# E10 總規劃 codex 關卡1 findings 逐條裁定(2026-07-28)

> 受審對象 = `docs/specs/2026-07-27-e10-order-closure-master-plan.md` v1(commit `c1a59cf`)
> findings 原文 = `docs/reviews/2026-07-27-e10-master-plan-codex-k1.md`
> 處置產物 = `docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md`(重寫,非修補)

## 0. 數量校正(當場量,不用交接檔的約數)

原始檔 152 行內把同一份清單**印了兩次**(`awk` 切 11-77 與 84-150 兩塊 `diff` 逐位元組相同)。
去重後**唯一 findings = 67 條:must-fix 62、nit 5**。交接檔與 STATUS 寫的「約 60」= 62 的約數,非另一份清單。

## 1. 裁定總覽

| 裁定 | 數 |
|---|---|
| 成立、v2 已處理 | 63 |
| 成立、v1 已處理(本輪只確認未回歸) | 2(#19、#43) |
| **部分成立**(半條已失效) | 1(#65) |
| **codex 對、v1 主對話寫錯**(非片型問題,是我給的技術斷言錯) | 3(#48、#49、#54) |
| 駁回 | **0** |

🔴 **另有 2 條 codex 沒抓到、主對話本輪自查補上**(見 §3)。

## 2. 逐條裁定

編號 = 去重後清單由上而下第 N 條。「落點」= v2 檔內處理它的位置。

| # | 主旨 | 裁定 | 落點 |
|---|---|---|---|
| 1 | 第 1 批依賴未閉合(O0-O7 卻承諾 O7a/O7b/O8 的成果) | 成立 | v2 §5 第 1 批重組:出貨軸與新編號**移出**第 1 批 |
| 2 | 文件承認 O7b 依賴 O8 卻未選定前移或後移 | 成立 | v2 §5;`O8` 這片本身取消(單號不放 orders,見 #6) |
| 3 | `dev` 推即正式站,E10 無 flag / hidden route / 逐批閘 | 成立 | v2 §7.1 逐批啟用閘 |
| 4 | C1 同檔既寫「未定」又寫「已拍自動後綴」 | 成立 | v2 §3:C1 = 已拍(P1),移除「未定」字面 |
| 5 | `hct-logistics-api-reference.md:217` 仍記後綴未定案 | 成立 | **本 session 已改該檔**(§4) |
| 6 | O9 先把單號寫 orders、O10 才建包裹 = 兩個真相 | 成立 | v2 §1 原則 2「模型先於欄位」;`orders` 永不放 tracking |
| 7 | O11/O12 只依賴 O9、未依賴 O10 | 成立 | v2 §5 第 2 批依賴圖 |
| 8 | O19 改單未依賴包裹/帳本 | 成立 | v2 §5 第 3 批依賴圖 |
| 9 | O20 待辦缺前置(缺貨/出貨/退款/跟進日都不存在) | 成立 | v2 §5 待辦排第 3 批最末 |
| 10 | 🔴 `internal_note` 放 `orders` 會被客人整表 SELECT 讀到 | 成立(資安) | v2 §1 原則 3 + `order_notes` 新表 |
| 11 | O2 標準片不成立:service_role 對 orders **無 UPDATE** | 成立 | v2 §1 原則 1「一片一層」,寫入片一律 owner RPC+ACL+audit |
| 12 | O3 缺取消 RPC / 按鈕 / 部分取消 / 退款分流 | 成立 | v2 §5.3 取消線拆片 + 折入 UX §1-4 |
| 13 | O3 把 expand/回填/雙寫/讀切換/收緊塞成一片 | 成立 | 同 #11 |
| 14 | `cancelled_reason` 是**對客**文字,受控 code 直接取代會丟舊值或把內部 code 給客人 | 成立 | v2 §5.1:**不取代**,另加內部 `cancel_reason_code`,對客欄原封保留 |
| 15 | O4 nullable + CHECK 證不了不變式;NOT NULL 又缺回填規則 | 成立 | v2 §5.1 A2:加欄+回填+NOT NULL+CHECK 同片做完,附回填規則 |
| 16 | 計數器誰原子更新 / 防超量 / 如何同步訂單層 enum 未定 | 成立 | v2 §5.1 A4 owner RPC;enum 同步與否 = **Q1 待 Sean** |
| 17 | PG enum 加值不可逆,「一律 expand/contract」對 O5 字面不成立 | 成立 | v2 §1 原則 4 改寫該句;Q1=B 則整片消失 |
| 18 | O5 漏 domain 型別 / 轉移表 / storefront 顯示 | 成立 | v2 §2.3(已親查兩檔字面) |
| 19 | `delivered` 無可信 writer | 成立、**v1 已處理**(P2′=B 已移除該值) | v2 §2.3 確認未回歸 |
| 20 | 訂貨/出貨已拍 item 層,O5 卻擴 order 層 enum | 成立 | **Q1 待 Sean** |
| 21 | O6 標準片不成立(需 schema+RPC+CAS+audit+UI) | 成立 | 同 #11 |
| 22 | O6 漏管道 / 送出時間 / 回覆狀態 / 異常原因 / 正規化 | 成立 | v2 §5.1 `order_item_procurement` 表含 UX §2 #5 全欄 |
| 23 | `<AdminDataTable>` 批次選取未做,且互動 cell 雙渲染會產重複表單 | 成立 | v2 §5.1 A10:**不套積木**,批次直接做在 `orders-table`(積木等第三個消費端) |
| 24 | O8 單號放 orders、O10 才建包裹 → 永久雙寫欄位 | 成立 | 同 #6 |
| 25 | O10 只寫「一單多包裹」,漏 U1 已拍的「多單併一箱」 | 成立 | v2 §5.2 `shipments`/`shipment_items` 吃 U1 全形狀 |
| 26 | `-1/-2` 序號來源 / 刪包裹是否重排 / 重送是否沿用皆未定 | 成立 | v2 §3 C1 落地規則(序號 = 包裹建立序、**永不重排**、重送沿用原值) |
| 27 | O13 塞多筆匯款+差額+催款+溢款+稽核,且未建付款帳本 | 成立 | v2 §5.3 `order_payments` 帳本 |
| 28 | 匯款退款去向 / 受款帳戶複核 / 轉帳參考號 / 防重複匯款缺席 | 成立 | v2 §5.3 折入 UX §1 #3 |
| 29 | 🔴 O14 不是「接帳本做 UI」:帳本只給 service_role SELECT,RF2b-RF8 未施工 | 成立 | v2 §6.2 誠實邊界 + **Q3 待 Sean** |
| 30 | 因此第 17 項映射不誠實 | 成立 | 同 #29 |
| 31 | O15 漏 `order_refunds.return_id` 關聯 | 成立 | v2 §5.3 折入主規格 §3.2 |
| 32 | O17 未依賴產號器、可能複製第二套碰撞邏輯 | 成立 | v2 §5.4 產號器單一來源 |
| 33 | 手動單的價格/稅/折扣/地址/付款/庫存/經銷價權限全未定 | 成立 | v2 §5.3 標為**開批決策閘**,未拍板不開工 |
| 34 | O19 未折入 U2「已裝進包裹部分鎖定」 | 成立 | v2 §4 U 系列折入表 |
| 35 | O21 稽核 UI:service_role 對 audit log **無 SELECT** | 成立 | v2 §5.3 稽核線先補 GRANT SELECT 片 |
| 36 | 且缺安全讀取 RPC/ACL,現況連「不可信的 UI」都交付不了 | 成立 | 同 #35 |
| 37 | O7b 是顯示片,卻映射第 8「輸入單號」與第 19「取消」 | 成立 | v2 §5 每片映射逐條校正,顯示片只映射顯示 |
| 38 | 第 7 項「缺貨要等」缺 item 層數量/異常原因/已告知紀錄 | 成立 | v2 §5.1 procurement 表 + U6 告知義務欄 |
| 39 | 宣稱 17 項全綠,但 UX 必折入的 §1-4 / 通知矩陣 / #23#24#26 未進片 | 成立 | v2 §4 折入對照表(逐條) |
| 40 | O11 只有客戶出貨信,漏內部通知(新付款/待核匯款/物流異常/退款失敗) | 成立 | v2 §4 U4 + 通知矩陣 |
| 41 | 完成定義漏前台 #240 包裹追蹤與 RF6 部分退款顯示 | 成立 | v2 §6.1 完成定義含前台回路 |
| 42 | U5 負責人 + 下次跟進日完全沒有片 | 成立 | v2 §5.3 `order_internal` |
| 43 | P2 與已拍 U7 衝突 | 成立、**v1 已處理**(P2′=B override U7=A) | v2 §3 確認未回歸 |
| 44 | 二元膠囊表達不了「3 件已訂 2、已出 1、退 1」 | 成立 | v2 §5.1 A11:膠囊顯示 `n/m`,滿量才變純色 |
| 45 | 一品項只有一組供應商資料,表達不了分兩家採購 | 成立 | v2 §5.1 procurement 表 = 每 line **1:N** |
| 46 | 把「真實下單日」覆寫成最近切換日會破壞交期/SLA/供應商績效 | 成立 | v2 §5.1:存**兩個**時間(`first_ordered_at` 語意 / `status_changed_at` 顯示) |
| 47 | no-op 是否改日期、來回切換合法性、Asia/Taipei 業務日未定 | 成立 | v2 §5.1:no-op 不動日期;業務日一律 Asia/Taipei server 端算 |
| 48 | **「篩選必須在資料層」不成立** —— 真正必要的是篩完重新分組並重算 rowSpan | **成立、v1 主對話寫錯** | v2 §7.2 改寫該約束(已親讀 `orders-table.tsx:64-77` 確認 codex 對) |
| 49 | **「不做整單彙總徽章」過度概化** —— 註解只證明不完整投影不能算彙總 | **成立、v1 主對話寫錯** | v2 §7.2 改寫(已親讀 `:86-93` 註解逐字) |
| 50 | 分頁單位是 20 張訂單;改品項分頁會拆散 rowSpan 群組 | 成立 | v2 §7.2:分頁維持**訂單**為單位、不改 |
| 51 | 12.6%/0.4% 只對 32 字元成立;依字面剩 28 字元,實為 23.0%/0.929% | 成立 | v2 §5.4 更正 |
| 52 | 該百分比是「首抽碰撞」非建單失敗率;重試/亂數源/捕捉哪個 constraint 未定 | 成立 | v2 §5.4 產號規格 |
| 53 | RNG 壞掉會連撞;捕捉所有 unique violation 會誤判非單號衝突 | 成立 | 同 #52(只捕捉具名 constraint) |
| 54 | (nit)admin 列表**已**用 `created_at DESC, id DESC`,「排序改 created_at」不是待做項 | **成立、v1 主對話寫錯** | v2 §5.4 移除該項(已親讀 `SupabaseOrderAdapter.ts:270-272`) |
| 55 | O7a 漏 domain 驗證 / format / parse / 型別 / 測試 | 成立 | v2 §5.4 編號線拆 3 片 |
| 56 | 舊新並存不是只放寬 CHECK;現有 parser 假定三段格式 | 成立 | 同 #55(已親讀 `display-id.ts:18,81-85`) |
| 57 | `create_order` 是 654 行 SECURITY DEFINER 金流函式,O7a 不可能是單片 | 成立 | 同 #55(`wc -l` = 654) |
| 58 | O0/O1/O2/O3/O4/O5 都需再拆 | 成立 | v2 §1 原則 1 |
| 59 | O6/O7/O7a/O7b/O9/O10/O11/O12 都超 45 分鐘 | 成立 | 同上 |
| 60 | O13-O21 除極窄唯讀 UI 外皆過大 | 成立 | 同上 |
| 61 | 高風險片數同檔寫 13 與 12,依表實際 **15** | 成立(`grep -c` 實測 15) | v2 §6.3:**不預先宣稱總數**,逐片標、開批時當場數 |
| 62 | (nit)片數先寫 24、誠實邊界寫 22 | 成立 | v2 單一來源 |
| 63 | (nit)server action 實際 6 檔不是 7 | 成立(`'use server'` grep = 6;第 7 個是 `.test.ts`) | v2 §2.2 更正 |
| 64 | (nit)「申請單送出即不可改」不符引用文件 | 成立 | v2 §3 改為「要改須聯繫營業所」 |
| 65 | STATUS 與 CURRENT 仍寫舊施工序 | **部分成立**:`STATUS.md:5` 已於 `c1a59cf` 同步為 E10 優先;`CURRENT.md:10-11` 確實仍舊 | 本 session 已改 CURRENT(§4) |
| 66 | 視覺 artifact codex 讀不到、不能視為已核對 | 成立 | v2 §5.1:版面規格改用 **repo 內可驗字面**(欄位清單),artifact 降級為視覺參考 |
| 67 | (nit)以字數推論列高/欄寬不足 | 成立 | v2 §5.1 降級為「實作時實測」,不當已驗事實 |

## 2a. Sean 2026-07-28 拍板後的 findings 狀態變更

| findings | 變更 |
|---|---|
| #17、#19、#20 | **Q1=B 後不再適用** —— 不加 `backorder`、不加任何 enum 值、不做計數器→訂單層 enum 同步 ⇒ v1 的 O5 整片取消 |
| #16 | 只剩一半(「誰原子更新計數器」由 A4 owner RPC 回答);「如何同步訂單層 enum」隨 Q1=B 消失 |
| #29、#30 | **Q3=A 後解法確定** —— E10 吃下 RF2b-RF8,第 17 項才算綠;第 3 批體積因此變大(Sean 知情選擇) |
| #55、#56 | **若 Q2 走「26 張砍掉 + 3 張改號」** ⇒ 雙格式支援不用做,N1 整片取消、N2 的 CHECK 直接換新格式不必放寬。待 Sean 重新拍板 |
| **N-2** | **Q1=B 後自動消解**(不驅動 `fulfillment_status` 就碰不到禁倒退規則) |

## 3. 🔴 主對話本輪補抓(codex 沒抓到)

| # | 內容 | 證據 |
|---|---|---|
| **N-1** | **`order_items` 與 `orders` 一樣對客全開** ⇒ 供應商名稱/單號/預計到貨/異常原因放 `order_items` 會**把 PCM 的上游供應商洩漏給客人**(代購生意的核心風險:客人可直接找上游)。codex 只抓到 `orders.internal_note` 這一半 | `20260604120000_m3_s2a_orders_order_items.sql:191` 逐字 `GRANT SELECT ON TABLE order_items TO authenticated`;`20260716120000_m4a_d2_order_items_workflow_status.sql:21-23` 逐字「新欄會員查自己單品項時可直讀」 |
| **N-2** | **Sean 拍板的「訂貨/出貨狀態可隨時來回改」與既有出貨狀態機直接衝突** —— `FULFILLMENT_TRANSITIONS` 是逐級線性、**禁跳級/禁倒退/禁自我轉移**,`shipped` 為終態 | `packages/domain/src/order/state-machine.ts:50-55` 逐字;`packages/domain/src/order/types.ts:57` 4 值 |

⇒ N-1 落 v2 §1 原則 3(內部資料一律進 service_role-only 新表);N-2 落 **Q1 決策題**。

## 4. 本 session 已同步的 repo 真權威(#5、#65)

| 檔 | 改動 |
|---|---|
| `docs/reference/hct-logistics-api-reference.md` | §8 未確認清單第 4 項「分批出貨後綴未定案」→ 改記 **2026-07-27 Sean 拍板 P1 自動後綴**,附落地規則指標 |
| `docs/handoff/CURRENT.md` | 舊入口與舊施工序(E11→E8→E10)→ 改指 E10 優先與本輪唯一入口 |

— END —
