# 2026-09-19 · 窗 shop-6 唯讀查證:⟦b4-INVOICE5PCT⟧ 與「帳本第二欄就是檔案 sha」

> 起因:主樹 `docs/handoff/CURRENT.md` 第 3b 節(未 commit)把 ⟦b4-INVOICE5PCT⟧ 排成待辦,
> 並標「**未確認**:`manual-order-lines.tsx` grep `5%` / `1.05` / `0.05` 【0 命中】⇒ 看起來沒落地」。
> 主視窗 `pcm-website-v2-8d` 要我「自己把整條路走完再下判斷,不要接手那個 0 命中當結論」。
> 🟢 全程唯讀,零寫入正式庫。量的基準一律是 `origin/dev`,不是本窗 HEAD。

## §1 ⟦b4-INVOICE5PCT⟧ —— 整條路已落地,板列的 ⑩ 與 ⑪ 兩格都不再是待辦

**四支板全在 `supabase/APPLIED.tsv`、全已貼正式庫**(`git show origin/dev:supabase/APPLIED.tsv` 實查):

| 版本 | 做什麼 | 帳本記的貼板日 |
|---|---|---|
| `20260904251500` | 旗標落點(`invoice_requested`) | 2026-09-04 |
| `20260909030000` | 勾了才算稅 | 2026-09-09 |
| `20260910090000` | 第 8 代含稅列走殘差 | 2026-09-10 |
| `20260915170000` | 缺鍵改 `RAISE`(不再替員工猜 true) | 2026-09-15 |

- 板列 ⑩ 逐字「相容期裡缺鍵與明確勾選同為 true…要之後另一支 migration 把缺鍵改回 RAISE,**那支還沒有人排**」
  ⇒ 🎯 **那支就是 `20260915170000`**,檔名即 `_m4b_manual_order_invoice_requested_required`,已貼。**⑩ 已關。**
- 板列 ⑪ 逐字「那顆勾選現在預設打勾…**一題要 Sean 拍**」
  ⇒ 🎯 **Sean 2026-09-15 早上拍「建單發票預設不勾」**(memory `project_0915-morning-rulings`)。**⑪ 已答。**

### 🔴 而「grep 0 命中」那把尺對這一列零判別力 —— 0 是對的數字、錯的問題
板列 1099(⟦b4-MANUALORDERDEADEND⟧)逐字留著換規格的憑據:
> **Sean 2026-09-05 拍板逐字 `甲 = 單價一律填【未稅】, 系統算稅, 文案改`**

⇒ 乘法從此住在 **RPC 算稅**那一段,**TSX 本來就不會出現 `1.05`**。
⇒ 📌 **0 命中要先問分母:你 grep 的那支檔,是不是唯一放得下那個係數的地方。**

### ⚠️ 我沒查的
沒開瀏覽器走一遍畫面。**轉 `done` 之前要有人看一遍**,不是再派人寫碼。

## §2 🛑 `20260915170000` 檔頭寫「未貼」而帳本說已貼 —— 採帳本,而【不要去改那個檔頭】

- 🔬 該檔檔頭逐字仍寫著 `🛑 未貼(貼板 Sean 一張一點名)`。
- 🔬 `APPLIED.tsv` 同版本那一列:`2026-09-15` 已貼。
- ⇒ **採帳本**。帳本是真相;檔頭是寫檔那一刻的話,貼完沒有人回去劃掉。

主視窗原本派我「就地把檔頭那行改成已貼」。查下去那筆交易是虧的:

```
🔬 git show origin/dev:supabase/migrations/20260915170000_*.sql | shasum -a 256
   ⇒ e06c0612c72af827c775738f778c7d80c6dba91db0cc8feb8399b356fd52c030
🔬 APPLIED.tsv 那一列第 2 欄
   ⇒ e06c0612c72af827c775738f778c7d80c6dba91db0cc8feb8399b356fd52c030   一字不差
```

⇒ 📌 **第二欄記的就是那支檔的 `sha256` 本身** ⇒ 改一個字,那一列就對不上。

```
🔬 python3 scripts/ledger-col2-census.py(唯讀, rc=0, 2026-09-19)
   帳本資料列 495 · ① 等於 repo 檔 sha 485 · ② 等於貼板那份 4 · ③ 兩者都不是 6
   ③a 丙會降級 6 · ③b 真卡住 0
```

⇒ **改下去 ③b 會從 0 變 1,而那一列會是第一顆。**
🎯 **等於拿「帳本能證明當初貼的就是 repo 這份」去換一句註解的更新。**

### ⚠️ 而那支普查今天【沒有 hook 在叫它】
該檔檔頭逐字:「2026-09-09 起本檔**沒有任何 hook 會呼叫它**(Sean 拍板規則減法)。**grep 得到它 ≠ 那道保護還在。**」

⇒ 🛑 **所以改了也不會有東西紅。**
📌 **那是【更要停下來問】的理由,不是【可以改】的理由。**

✅ 主視窗 2026-09-19 裁 **甲**:標在 `.md`(本檔 + 板列末尾一行),**不動 migration,sha 不變**。
📌 主視窗自己的話留著:「**我拿帳本能證明當初貼的就是 repo 這份,去換一句註解 —— 這筆交易是虧的。**」

## §3 這一發之外、順手撞到而【沒有往下查】的
- `docs/handoff/CURRENT.md` 第 3b 節只住在主樹**未 commit 的工作區**,`origin/dev` 上沒有那一節。
- 第 3b 節標了 A、B 兩條「板上寫沒做而其實做完」的誤派源;本檔的 §1 是**第三條**,同一個形狀。
