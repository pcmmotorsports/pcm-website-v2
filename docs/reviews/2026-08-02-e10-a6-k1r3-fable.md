# 關卡1 R3 — A6 備註線 plan v3 對抗審查(Fable adversarial-reviewer,換模型換角度)

> 2026-08-02。審查對象 = plan **v3**(凍結於 `ee4ca1b`)。判定 **NO-GO**,6 must-fix + 4 nit。
> 指定四角度 = v3 修法自捅 / 員工可用性 / 61 格 manifest 假綠面 / 假設審查;與 R1/R2 58 條**零重疊**。
> 主對話親驗五處行號全成立、駁回 0;折入結果 = plan **v4** §0.6。

NO-GO

[F1] must-fix / plan:168,207 vs 20260729030000:111-112 / §4.2.1 對 author_nonempty 的「不可達」論證是假的:步 1 只驗「剝空白→非空→≤200→零控制字元」,不蘊含 DB CHECK `^[a-z0-9_]{1,64}$`。 / actor='Sean'(大寫)或 65-200 字元 → 通過步 1 → INSERT 撞 raw 23514,「任一輸入必落 14 碼或步 1 RAISE」被證偽([36] 同型)。緩解:staff.id 有同 regex(20260726120000:21)故 A9d2-1 實際打不到;但 RPC 契約層零格釘住此 mirror。

[F2] must-fix / plan:178,301,319 vs 20260712210000:85-89、樣板:432-439 / 步 11 讓 owner RPC 新增對 admin_audit_log 的 SELECT 依賴,但 F 區與 §7.3-5 照抄樣板只驗 INSERT。 / audit 表 owner 漂移、只補 INSERT grant → migration 全綠 commit,正式站每一次呼叫在步 11 炸 42501。

[F3] must-fix / plan:179,266-268 / DUPLICATE_REQUEST 是 14 碼中唯一「操作其實已成功」的碼,而 §5 沒有 14 碼→員工語意映射。 / double-submit 第二發被映射成紅色錯誤 → 員工以為沒寫進去、重填重送(新 request_id)→ 產生真重複備註 —— 冪等設計反而誘發它要防的事。

[F4] must-fix / plan:299 / D-⑧ 一格捆 13 條「失敗碼兩表恰 +0」,突變欄只寫一個 ⇒ 12 條斷言判別力從未被證明;或 13 突變則「每格一突變」記帳自破。 / 修法 = 突變逐 13 碼各做一次,格數與突變數分開記帳。

[F5] must-fix / plan:301 vs 樣板:384-392 / 「PUBLIC+anon+authenticated 零授權」格構造不出只紅它的突變:任何 GRANT 同時讓「攤平恰一列」格轉紅(樣板 5e 自承是縱深第二層)。 / 「61 格有限互斥、每格一指定紅點」在此格證偽 ⇒ 明文降標為縱深格。

[F6] must-fix / plan:269-271 / A10a 未列「更正」的 UI 入口與啟用規則 ⇒ R1 [7] 只修了 RPC 半邊:沒有更正鈕則能力仍不可達;有鈕但不對已被更正列 disable,員工日常撞 ALREADY_CORRECTED 死路(partial unique 20260729030000:156-158)。

[F7] nit / plan:174 / `> now()` 零時鐘容忍:裝置時鐘快 1-2 分,員工選「現在」被拒(contact/notified 型 occurred_at 必填、天天用)。 / 建議 now()+5min 寬限。

[F8] nit / plan:303 / H 區 3 格只給 2 個突變;「釘總案例數」格自己的突變(拿掉一條案例→數字閘紅)未指定。

[F9] nit / plan:255-260 / mapper 等價論證隱含前提「投影回該單全部 notes」未成驗收;PostgREST max-rows 是否截斷 embedded rows 未確認 ⇒ 截斷會讓 U6 notified 靜默算錯。

[F10] nit / plan:284 vs 20260624120006:15-16 / R7 鎖序論證的字面前提「全庫唯一 orders FOR UPDATE 持有者 = confirm_order_payment」早已過時(20260714130000:113、20260716130000:268 亦鎖 orders),A6 再 +1;plan 未要求更正該敘述,下一個讀者會拿假前提做鎖序推理。

## 四角度結論(審查者原文摘錄)

- **角度 1(v3 修法自捅)**:F1/F2。鎖序已嘗試擊破、未破 —— grep 全 migration 的 FOR UPDATE/SHARE 逐支比對,A6 鎖足跡 = orders 單列→note 列(同向);confirm(orders-only)、markFailed(attempt→orders)、workflow×2(orders first)、A7c trigger(orders FOR SHARE→refunds,20260801120000:223 同向)⇒ 無環。步 11 走 request_id 索引免鎖;§4.2.1 其餘各列逐條對 :87-131 驗過皆成立,唯 author 列假(F1)。
- **角度 2(員工可用性)**:F3/F6/F7。最大的洞 = DUPLICATE_REQUEST 顯示成錯誤會誘發重複備註。
- **角度 3(manifest 假綠面)**:F4/F5/F8。C 區 10 相鄰對逐對親構造過、全部構造得出來(含 (6,7)='2150-01-01' 同時界外且未來、(8,9)=4001×U+200B、子序格 = A 單已被更正的 note 被 B 單指向);相鄰對釘住即釘住全序。B 區紅點歸屬對 :99-100 逐碼位核過正確。61=14+7+11+8+6+9+3+3 覆算相符。
- **角度 4(假設審查)**:sha256(bytea) = PG11 起內建(官方文件、repo 無先例、未實測 → 本機 harness 首跑即驗);char_length 數碼位、零寬計入 = 正確;NOT VALID CHECK 對新 INSERT 照樣 enforce(官方文件語意、未實測);PostgREST embed 可行(orders←order_notes 恰一支 FK :50、自指複合 FK 不製造歧義、同投影已有 embed 先例 SupabaseOrderAdapter.ts:93);audit request_id 索引 20260712210000:78 存在且形狀吻合。
