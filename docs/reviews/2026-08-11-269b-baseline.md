# #269-b 段二閘 — **apply 前**基準線(2026-08-11 12:2x,真 PostgREST anon)

用 `scripts/269b-gate.sh` 對正式庫實打,**apply 之前**量的(#269-b 段一 = `6c988f04`,尚未 apply)。存在理由:
沒有 apply 前的數字,apply 後量到的秒數**無法判斷是不是我弄慢的**。

| 格 | 內容 | 首打(冷) | 穩態 ×5 | 判讀 |
|---|---|---|---|---|
| ① | 不帶 p_new_since + recommend, limit=50 | 1.337s | 0.449–0.685s | 現行主要型錄路徑 |
| ② | 不帶 p_new_since + recommend + p_brand=Ducati, limit=1 | 2.056s | 0.458–0.608s | facet 單發;一次請求最多 ×108 |
| ③ | sort=new, limit=50 | 0.464s | 0.455–0.606s | ⚠️ **不是新排序的基準**,見下 |
| ④⑤⑥ | 帶 p_new_since | HTTP 404 `PGRST202` | — | 預期:函式今天沒有這個參數 |

## 🔴 三條讀這張表必須知道的事

1. **首打 2.056s 是冷啟動,不是穩態。** 我第一次量到 2.056s 差點把它當成「離 3s 只剩 0.94s」
   回報出去 —— 重跑五次都是 0.5s 上下。**單一樣本不能當基準線。**
   但冷啟動本身仍是事實:**冷路徑真的會到 2s**,而 anon 的門是 3s ⇒ apply 後若冷啟動再變慢,
   餘裕比穩態數字看起來的小很多。
2. **格③今天的 0.46s 不是「sort=new 的基準」。** `p_sort='new'` today 不被函式認得,
   ORDER BY 的 CASE 全部落空 = 等同預設排序 ⇒ 它量到的是「沒有排序成本」的版本。
   apply 後格③會第一次真的做 19,017 列的 created_at 排序,**沒有可比較的前值**。
3. **④⑤⑥ 回 PGRST202 = 預期,不是失敗。** 它同時證明了閘裡那條
   「PGRST202 ≠ 57014、要重送 NOTIFY 而不是 rollback」的判讀**真的會觸發**
   (偵測器打到的是回應 body,不是腳本自己的字串)。

## 怎麼重跑

```bash
bash scripts/269b-gate.sh        # 六格全跑
bash scripts/269b-gate.sh 2      # 只跑第 2 格(T+20min 補跑用)
```
⚠️ 腳本自己會找 `apps/storefront/.env.local`(repo 內找不到就退回主樹);
**只 grep 取單顆值、不 source、不 echo 金鑰**。

## apply 後怎麼比

- 格①② 穩態若 **> 1.5s**(約基準線 2.5 倍)= 明顯劣化,即使還沒到 3s 也要停下來看。
- 格①② 任一到 3s / 回 57014 ⇒ **rollback**(分格表)。
- 格③④⑤⑥ 爆 ⇒ 開索引片、不接段二、**不 rollback**。
- **T+20min 再跑一次格②**(`CATALOG_REVALIDATE_SECONDS=900` ⇒ 最長 15 分鐘流量才全換過去)。
