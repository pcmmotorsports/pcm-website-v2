# 關卡1 R1 — A6 備註線 plan v1 對抗審查(codex `gpt-5.6-sol` xhigh, read-only)

> 2026-08-02。審查對象 = `docs/specs/2026-08-02-e10-notes-line-plan.md` **v1**。
> 判定 **NO-GO**,35 條(27 must-fix + 8 nit)。
> 主對話親驗 `[1][4][5][6][10][23][33]` 共 7 條 — **全部開原始檔逐字確認成立、駁回 0**。
> 折入結果見 plan **v2** §0 與各節 `[編號]` 標註。

NO-GO

[1] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:14,94-96`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:389` / 三片鏈漏掉負責 note server action 的 A9d2，卻把寫入算進 A10a。 / A10a 不是沒有寫入呼叫端，就是偷併權限邊界卻標成不觸發鐵則 12。

[2] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:90-92`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:385` / 母 plan 的 A9a 是 notes 加 procurement，本文卻只做 notes join。 / 本片完成後若宣稱 A9a 完成，採購投影會成為無名欠帳；若日後再做 A9a，片號與驗收會重複。

[3] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:14,19` / 「A9a 與 A10a 都是兩條線共用」與同表採購線終點 A10b 自相矛盾。 / 這會把只屬備註線的 A10a 誤算成採購線的必經工作。

[4] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:78-84`；`docs/specs/2026-07-25-admin-backend-rebuild-spec.md:373-376` / 不寫 `admin_audit_log` 直接違反現行「所有新後台寫入＝owner RPC 加同交易稽核」合約，且未標示該合約被新拍板推翻。 / 上線後統一稽核檢視器、request_id 追蹤與「今天員工做了什麼」會永久缺備註事件。

[5] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:45,77-80`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:21-25` / 「無 `updated_at` 所以 append-only 由形狀保證」是錯的；A3 明寫只靠應用角色 ACL，真正強制需要 trigger。 / table owner、superuser 或未來 owner RPC 可 UPDATE／DELETE，所謂自帶稽核帳本可被改寫。

[6] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:52-53`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:21-27` / `service_role` 只有 SELECT 只擋直接表寫入，擋不住 owner、superuser、pg_cron 或其他 SECURITY DEFINER 函式。 / 日後新增另一支 owner 函式即可繞過 A6，零 trigger 的表不會阻止它改寫或刪除備註。

[7] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:75-80` / plan 一面拿更正鏈證明 `order_notes` 適合作稽核帳本，一面讓 A6 不收 `corrects_note_id` 且本線不做更正入口。 / 員工誤寫的營運紀錄透過唯一核准路徑無法更正，論證依賴的是實際不可達的能力。

[8] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:64-68,78-83` / `p_request_id` 在不寫 `admin_audit_log`、表又無 request_id 欄時沒有任何持久化用途。 / 同一請求重送可新增兩筆無法互相辨識的備註，也無法由 request_id 串回 server log。

[9] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:35-46`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:70-85,154-158` / 正式站已唯讀驗證其 ACL 與主要 CHECK 相符，但清單漏了 `order_notes_corrects_not_self`、`UNIQUE(id,order_id)` 與 `corrects_note_id` partial unique。 / A9a／A10a 若把 §3 當完整 schema 合約，會漏掉更正鏈的三道實際限制。

[10] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:107`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:102-104` / plan 接受未來 `occurred_at`，但 A3 明確把「不得晚於現在」交給 A6。 / `contact_log` 帶明天時間會通過 DB CHECK 並回 `APPENDED`，直接違反上游已立合約。

[11] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:39,71-72`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:90-99` / plan 沒承接 A3 指定給 A6 的 body 正規化與已知空白邊界。 / 只含 U+2800、U+3164 或 U+00AD 的肉眼空白備註會通過表 CHECK，並被當成成功紀錄。

[12] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:71-72` / 六碼未涵蓋 body NULL／空白、actor NULL／非法、occurred_at 越界、order_id NULL、request_id 非法。 / RPC 會留下 raw 23502／23514 或 P0001；UI 是否原樣顯示未驗證，因 A9d2 未納入且目前沒有該呼叫端可查。

[13] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:71-72` / 回傳碼沒有定義重疊條件的優先序，例如 bogus type 加缺聯絡欄、internal 加非法 channel、無單加非法 type。 / 同一輸入同時符合兩個碼，實作者改變檢查順序就會改變呼叫端結果。

[14] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:71-72` / `ORDER_NOT_FOUND` 沒定義鎖序，也沒要求只捕捉具名 `order_notes_order_id_fkey`。 / 先查到訂單、並行 DELETE 隨後提交時，INSERT 可改以 raw 23503 失敗而不是固定碼。

[15] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:60-72`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:137-179` / 宣稱照抄樣板，卻未規格化 actor／request_id 的先剝空白、非空、長度及控制字元守門。 / 實作者照 §4 只鏡像表 CHECK，會讓 request_id 被忽略，或讓非法值以非固定例外漏出。

[16] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:64-68` / `p_body` 是無上限 text，plan 沒有任何 RPC 或表單長度上限。 / 持 service key 的呼叫端可塞入極大備註，使明細時間軸、網路回應與後台渲染承受不必要負載。

[17] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:60,115-123`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:84-87` / 樣板的 `BEGIN`／`COMMIT`、`lock_timeout`、`statement_timeout` 沒列入 A6 規格或驗收。 / 拿掉 apply timeout 仍能通過現有 §7 字面，遇 schema lock 時 db push 可長時間卡住。

[18] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:57-84`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:302-311` / 樣板要求的 `COMMENT ON FUNCTION` 合約沒有被列為產物或驗收。 / 正式站 introspection 將看不到固定碼、權限、輸入語意與誠實邊界。

[19] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:120` / 表 ACL 驗收只寫 `service_role=r` 與欄級 ACL 0，沒明定非 owner allowlist、PUBLIC／anon／authenticated、grant option、RLS on 與 zero policy。 / 額外給 authenticated SELECT 時，`service_role=r` 仍成立，這條驗收可能照樣綠。

[20] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:117`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:365-391` / 「service_role 恰 EXECUTE」未逐字要求 `is_grantable=false`，也未要求 `proacl IS NOT NULL` 的前置。 / service_role 若持 WITH GRANT OPTION，便能轉授 owner RPC，而粗略 EXECUTE 檢查仍可能通過。

[21] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:108-109,115-123` / R6 把 production apply 後才能做的 type re-gen 列為 A6 DoD，但 §7 沒有獨立 production 批准、apply、ledger 與 read-back 步驟。 / A6 在未 apply 時無法完成 DoD；若為完成 DoD直接 apply，又越過正式環境 checkpoint。

[22] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:64-69,109`；`packages/adapters/src/supabase/database.types.ts:1-10` / 單純重 gen 會把 nullable SQL 參數 `p_channel`、`p_occurred_at` 產成非 nullable，plan 沒列手動 `| null` 校正。 / internal note 必須傳兩個 NULL，typed `.rpc()` 會型別紅或被迫使用不安全 cast。

[23] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:57-84,118`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:186-195` / A3 的硬前置「A6 一次只准 INSERT 一列、禁多列 VALUES／INSERT SELECT」未進規格或結構驗收。 / 未來實作若由查詢產出多列，正式站已證實可建立更正環，A10a 可能無限走鏈。

[24] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:118` / 行為驗收只要求成功多一列，未要求其餘五碼零新增，也未逐欄驗 body／author／channel／occurred_at／created_at／corrects_note_id。 / 函式可先寫入經改造的列再回錯誤碼，或漏掉正規化，六碼測試仍可全綠。

[25] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:119` / 所謂配對規則「雙向負測」只測 internal 帶 channel 與 contact_log 缺 occurred_at。 / 拿掉 internal 僅帶 occurred_at 的守門，或放行非 internal 缺 channel 但有時間，兩條驗收仍全綠。

[26] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:116-121` / `scripts/a6-verify.sh` 只要求 0 FAIL，未釘預期案例數、0 SKIP 與 fixture order 必定存在。 / harness 若因 orders 為空跳過全部行為案例，仍可能以 0 FAIL 結束。

[27] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:121` / 「每一道新守門」是自我指涉，plan 沒先列有限 guard／mutant manifest 與每格指定紅點。 / 實作者漏做一個守門時，只要也不把它列入突變清單，整套突變驗收仍全綠。

[28] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:116` / 從零套用成功只證明 migration 可執行，不證明任何輸入、ACL 或回傳碼守門存在。 / 把函式縮成無條件 `RETURN 'APPENDED'` 仍可通過本條。

[29] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:122` / typecheck 與 lint 不讀 SQL 行為，對本片安全守門沒有消融判別力。 / 刪掉所有 PL/pgSQL 輸入驗證時，兩綠仍會保持綠色。

[30] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:123` / 「審查已跑且 findings 折完」是流程證據，不是任何特定守門的可執行驗收。 / 審查者若漏看一條風險，此項仍能被標成完成。

[31] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:139` / 「零資料影響、本片不寫資料」把 migration apply 與 RPC 營運效果混為一談；正式站目前雖是 0 列，A6 的用途正是持續 INSERT。 / 員工已寫備註後 DROP 函式，既有列與對 orders 的 RESTRICT 引用都會保留，狀態不會回到 A6 前。

[32] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:137-142`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:447-458` / rollback 未要求另立較新 down migration，也未寫明先撤 A10a／A9d2／A9a 再 DROP A6。 / 直接先 DROP 會讓仍在線上的 action 呼叫不存在的 RPC，且 migration ledger 與 schema 可能分裂。

[33] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:91-92`；`packages/adapters/src/supabase/SupabaseOrderAdapter.test.ts:636-640` / 引用的 249-260 是列表投影守門，明細投影 byte-equal 實際在 636-640。 / 實作者照 plan 行號修改會碰錯白名單，或誤判明細根本沒有守門。

[34] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:90-92,125-128`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:716` / A9a 驗收漏掉母 plan 已鎖死的「更正前算 1、更正後算 0」雙向負測與 `NOT EXISTS(corrects_note_id=n.id)`。 / 天真地只查 `note_type='customer_notified'` 會把已更正的誤選永久算成已履行告知義務。

[35] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:94-96,130-133`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:179-195` / A10a 描述漏掉「更正不可撤回」文案，以及走更正鏈必帶 visited 集合與深度上限的既有合約。 / owner 路徑若留下環狀資料，時間軸可無限迴圈；員工也可能誤以為「更正的更正」會恢復原紀錄。
