# A7b 關卡1 · codex `gpt-5.6-sol` xhigh · R1 findings(逐字保存)

> 受審 = `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md` v1(**已因本輪判定不可施工**)
> 執行 = 2026-07-31 清晨,`codex exec -s read-only -m gpt-5.6-sol -c model_reasoning_effort=xhigh`
> 跑前後 `git status --porcelain` 比對:**零留痕**。
> 🔴 本檔逐字保存,不摘要、不裁定 —— 裁定與折入紀錄寫在 plan v2。

---

FAIL

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:4,29,162-180`＋master plan `:54-61,352`：A7b 被標成 M 片，卻把 trigger／DB 函式列為核心交付，直接違反「M 零行為、trigger 歸 T」；修法：拆成 A7b-M（表、CHECK、索引、ACL）與 A7b-T（INSERT／UPDATE／DELETE／TRUNCATE 守門及行為測試），同步更新 master DAG，重新取得 Sean 對重大切片變更的批准。

- [must-fix] master plan `:352` 對照受審 plan 全文：R9 的 baseline 初始化與 job↔ledger 等值、R13 的成功歸零／失敗遞增 CAS、R15 的 durable LINE 重發、R16 的結案 RPC 鎖與稽核、R18/R19 的重開限制與三條負測都被省略或只剩文字；修法：加入 R9–R19 關閉矩陣，逐項指向本片 schema／T 片／第 3 批 RPC／指定驗收案例，不得只寫「worker 之後照做」。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:89,129-140`＋master plan `:352`＋`docs/reference/tappay-reference.md:101`：master 明訂 `bank_refund_id UNIQUE`，DDL 與「三道唯一性」卻完全漏掉，實際權威至少要求四道；修法：新增具名 `UNIQUE(bank_refund_id)`、結構斷言及重複值負測。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:97-126,169-177`：trigger 只有 `BEFORE UPDATE`，因此可直接 `INSERT status='completed'/'submitted'/'dead'`，完全繞過七態轉移；修法：A7b-T 加 `BEFORE INSERT` 守門，初代只能以乾淨 `queued` 建立，後代必須走具名的原子授權消耗流程。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:133-150,172-173`：`reviewed_at` 可單獨寫入，甚至可夾帶在 `processing→processing`／`reconciling→reconciling`，隨即讓 U2/U3 的 `reviewed_at IS NULL` 失效並放行第二個 active job；修法：強制所有 review 欄僅能出現在 `dead`，且 `reviewed_at/reviewed_by/resolution` 必須全 NULL 或同時非 NULL，trigger 只准 `OLD.status='dead'` 的結案更新。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:133-158`：`completed` 或任意已複核 job 之後，直接插入 `generation+1` 會通過 U1/U2/U3，三道唯一性只擋同時重複，擋不住序列式第二次退款；修法：新世代 INSERT 必鎖最大世代，要求直接前代為已結案 `dead + retry_authorized + retry_consumed_at IS NULL`，並在同交易消耗授權。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:153-158,220`＋master plan `:352`：R18/R19 目前只是未受強制的文字，沒有擋舊授權開 gen3、`external_refund_confirmed` 重開、`over_refund_writeoff` 重開；修法：在 T／R 邊界選定唯一強制機制，並補這三條負測及兩 session 併發重開測試。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:184-192`：表級 ACL 只擋一般角色，擋不住 table owner／SECURITY DEFINER RPC 刪除或 TRUNCATE；一旦清掉 submitted/completed 歷史，唯一索引就無法阻止重退；修法：A7b-T 加永久 `BEFORE DELETE`／`BEFORE TRUNCATE` 阻擋，並驗 owner 路徑也會失敗。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:80-126`＋master plan `:352,537-538`＋`supabase/migrations/20260725130100_m3_rf2a2_order_refunds_ledger.sql:80-131,143-169`：master 要求的 quote 快照整組消失，現有欄位不足以建立必填的 ledger header、品項明細、運費、actor、reason、request_id；修法：動工前定死快照的具名欄位或子表、來源與不可變規則，證明隔日可不重算現況就完整寫帳本。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:121-122,144`＋master plan `:352`：`refund_id` 只有裸 FK，未強制 job 與 ledger 的 `bank_refund_id`、金額及訂單相同，也未阻止兩個 job 共用同一 ledger；修法：completion trigger/RPC 同交易驗 cancellation.order_id、ledger.order_id、bank ID、退款額全部等值，並對非 NULL `refund_id` 加唯一約束。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:54-58,94-95,174-176`＋master plan `:352`：enqueue 時 baseline 必為 NULL、claim 後才呼叫 Record，但狀態機沒有「目前 token 持有人在送 Refund 前持久化 baseline」的合法 `processing→processing` 路徑；修法：增加 token-CAS 的 baseline 初始化自轉移或獨立 `prepared` 相位，並要求 baseline 已持久化且重新驗證安全後才准送 Refund。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:151`：`refunded_target IS NOT NULL ⇒ target = before + amount` 在 `before IS NULL` 時結果是 SQL `NULL`，PostgreSQL CHECK 會放行；修法：改成「兩欄同為 NULL，或兩欄同為非 NULL 且等式成立」的完整 CHECK。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:94-95,151`：修正 baseline 成對規則後，`refunded_target > 0` 會被 `before >= 0`、`refund_amount > 0`、等式三者嚴格蘊含，行為層無法單獨觸發；修法：刪除冗餘 CHECK，或像 A1 一樣明標「只做結構字面、沒有獨立行為證明」，不得宣稱每條 CHECK 都有獨立負測。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:16-17,51-64,164-180`：trigger 看不到 TapPay Refund 是否成功、Record 累計是 `< / = / > target`、baseline 是否來自真 API，卻宣稱能逐條擋所有條件；修法：把能力名稱收斂成「狀態圖與本地欄位守門」，外部結果、token CAS、baseline 真實性改列第 3 批 owner RPC／worker 的強制測試。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:54-64,101-110`＋master plan `:352`：只要求 lease 三欄非 NULL，沒要求五分鐘、到期後才可重領、新 token 必須不同、`next_check_at`／`next_retry_at` 已到期；修法：逐轉移定義並驗證時間條件，至少鎖住 `claim_expires_at > claimed_at`、重領只在舊 lease 過期、隔日與 backoff 到期後才可 claim。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:58-63,107-110,176`：兩個計數器被簡化成「任何 UPDATE 都可 +1 或歸零」，可在錯誤相位重設、讓第六次仍回 queued，或由舊 worker 覆蓋新值；修法：逐 edge 寫死 retry/check count 的舊值、新值與狀態，Record 成功但未達標才歸零、異常才 +1、第六次同交易進 dead，其餘 edge 不得改。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:102-104,147-150,178-180`＋master plan `:352`：master 要人工結案 RPC 做 token CAS，但 plan 又要求 dead 的 claim token 必為 NULL，兩份合約無法同時成立；修法：動工前選定獨立 `review_version/review_token`，或把 master 改成鎖列＋`reviewed_at IS NULL` CAS，不能留到施工時猜。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:113-120,228-235`＋master plan `:352`：R15 的「LINE 失敗後每輪重發直到結案」完全消失；修法：在 A7b 合約 COMMENT 與第 3 批 release gate 明訂 unresolved-dead 掃描、LINE 失敗重發及指定負測。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:144-150`：狀態一致性多為單向，允許非 failed 留 `failed_reason`、非 dead 標人工複核、`reviewed_by` 單獨存在；修法：改成逐狀態完整 CASE，明訂每個狀態允許／禁止的 lease、排程、失敗、review、refund 欄位集合。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:90,174-176`＋`supabase/migrations/20260730130000_m4b_e10_a7_order_cancellations.sql:168-176`：金流 job 的 `payload_hash` 只驗長度、且不在不可變欄位，64 個任意字元可通過並可事後改寫；修法：對齊 repo 既有做法，使用 `text`＋恰 64 碼 lowercase hex 的具名 CHECK，並列入不可變欄位與 golden-vector 測試。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:88`＋`docs/reference/tappay-reference.md:98`：官方 `rec_trade_id` 是 String(20)，plan 只驗 `btrim() <> ''`，沒有長度上限且 tab／部分不可見字元可通過；修法：定死 1–20 字元及可接受字元形狀，補 NULL、空白、21 字元負測。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:80-126`＋`docs/reference/tappay-reference.md:103`＋既有 ledger `:87-88`：Refund API 回傳的 `refund_id` 沒有 job 落點，送出後 crash 或隔日建 ledger 時會遺失外部退款識別；修法：新增不可變、submitted 後持久化的 `tappay_refund_id`，或在 plan 明確拍板只靠 `bank_refund_id` 並說明為何 ledger 欄可合法留 NULL。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:116`＋`supabase/migrations/20260730130000_m4b_e10_a7_order_cancellations.sql:95-112`：`reviewed_by` 是任意 nullable text，與 repo 已採用的 `staff(id) ON DELETE RESTRICT` 稽核形狀不一致；修法：改為 staff FK，並揭露 E8-B 上線前它仍只是操作者自陳，不能當已驗證身分。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:162-192`：未定 trigger 函式是否 SECURITY DEFINER、`search_path`、owner、EXECUTE ACL，也沒寫 `CREATE` 或 `CREATE OR REPLACE`；修法：能用 SECURITY INVOKER 就不用 DEFINER，函式全限定 schema、使用 `CREATE`、收回 PUBLIC/anon/authenticated/service_role EXECUTE，並納入結構驗收。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md` 全文：高風險 migration 沒有 BEGIN/COMMIT、`lock_timeout`、`statement_timeout`、rollback 與 dependency preflight；修法：補完整 apply／read-back／forward-only down migration，明訂第一個 writer 前可 DROP、已有資料後只能 forward repair。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:211-213`：U2 與 U3 測試若使用同 cancellation＋同 rec_trade_id，會先紅在任一索引，另一索引刪掉仍全綠；修法：U2 用「同 cancellation、不同 rec_trade_id」，U3 用「不同 cancellation、同 rec_trade_id」，未複核 dead 也拆成兩個指定索引案例。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:207-210,222-224`：非法轉移突變可能仍被目標狀態 CHECK 擋住，例如移除 `submitted→processing` 守門後因缺 lease 欄轉紅、移除 completed 終態守門後因未清 `refund_id` 轉紅；修法：每個 transition 探針先建出「除 edge 外全部合法」的新列形狀，再斷言指定 trigger ID。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:217-218`：`retry_count 0→6` 與不可變欄更新若在同狀態 UPDATE 執行，可能先被「該狀態不准自轉」擋下；修法：把突變夾在合法 edge 中測，例如 `processing→failed` 同時跳 count、`queued→processing` 同時改 refund_amount，確保只剩目標守門可轉紅。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:220`：直接 INSERT `generation+1` 的正向案例只證明 partial unique 已放行，完全沒證明鎖最大世代、直接前代、原子消耗，反而替目前的重退破口背書；修法：刪除此直接 INSERT 驗收，改由具名 enqueue RPC／insert guard 的正向與 R19 三條負測驗收。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:206-224`：宣稱「每條 CHECK 都有刪除突變」，但清單沒有覆蓋 generation、bank 長度、payload 形狀、baseline、failed/dead/review、兩個 count 上下界等多數 CHECK；修法：列出 constraint ID → 正向前提 → 負向資料 → 指定 SQLSTATE/CONSTRAINT_NAME → 對應 mutant 的完整一對一矩陣。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:198-204,222-224`：A7b 的核心是 trigger，驗收卻沒有承接 A7-t 已實證的 `tgenabled`、`tgqual`、`tgrelid`、`tgfoid`、事件位元、函式本體指紋、owner/ACL 等假綠路徑；修法：把這些逐項納入外層 oracle，並各配 DISABLE、REPLICA、WHEN false、同名異 schema、錯表、no-op 函式突變。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:202`：所有負測都要求 `SQLSTATE + CONSTRAINT_NAME`，但 trigger 若只是普通 `RAISE EXCEPTION`，`CONSTRAINT_NAME` 會是空值；修法：所有 CHECK 具名，trigger RAISE 明訂自訂 SQLSTATE 與 `USING CONSTRAINT`，runner 再精確驗兩者。

- [must-fix] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:239-242`：migration 順序不是待決題，版本號已固定成 A7-t `140000` → A1 `150000` → A7b `20260731120000`；修法：把 Q1 改成真正需 Sean 批准的「分開 apply＋逐片 read-back」或「聯合 apply＋聯合回滾」，且 apply 保持獨立正式環境 checkpoint。

- [nit] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:84`：`cancellation_id` FK 沒逐字指定 `ON DELETE RESTRICT`，雖預設 NO ACTION 目前同樣不會 cascade，但不符合 A7 對財務／取消事實的明確留存慣例；修法：明寫 RESTRICT 並驗 FK OID、欄序與刪除規則。

- [nit] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:124-125`：`updated_at` 只有 DEFAULT，沒有任何機制會隨狀態更新；修法：由狀態 trigger 統一設值，或移除這個會說謊的欄位。

- [nit] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:78-126`：沒有 queued、failed-due、submitted-due、unreviewed-dead 的 claim／掃描索引；修法：依第 3 批具名查詢形狀先定 partial indexes，至少讓排程不必全表掃描。

- [nit] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:21`：隔日生效實際在 `docs/reference/tappay-reference.md:107`，不是 `:100`；修法：更新行號或只引用 §2.3，避免後續漂移。

- [nit] `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:198`＋`STATUS.md:10`：A1 骨架目前只在本機 PG17.10 驗過，migration 尚未 apply／push，「已證明有效」容易被讀成正式站已證明；修法：改成「本機 harness 已證明，正式站未 apply」。