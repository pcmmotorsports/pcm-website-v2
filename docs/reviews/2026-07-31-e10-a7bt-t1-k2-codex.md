[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:484 — D9c 只在 E2 取樣，E2b 送錢前沒有最後重驗 — gen N+1 的 E2 讀到 0 通過後，gen N 舊退款才反映 1000；E2b 隨即用新 `bank_refund_id` 再退 1000，D7/D9 全綠卻雙退。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:811 — E14 的「無後繼」檢查有等待後快照過期競態 — 交易 B 鎖前代並插入 gen2；交易 A 先取得看不到 gen2 的快照後等待，B 提交後 A 仍以舊快照通過 E14，留下授權已撤銷但仍可退款的 gen2。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1084 — job↔ledger 等值只掛在 job，ledger 自身改動不會觸發 — completed 後 owner 將 `order_refunds.reason` 改成另一個合法非空值，ledger 內部檢查通過、job 不動，十一欄不等仍可提交。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:543 — E3b 白名單允許 `claim_token`，卻沒強制換新 — 過期 processing 列保留原 token，只重設 lease 時間與 `next_check_at` 後轉 reconciling，整筆通過，舊 worker 仍持有有效 token。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1108 — 子表 deferred trigger 漏掉 UPDATE，與保留 DELETE 作 break-glass 防線的理由矛盾 — 只停用 `a7bt_items_block_update` 後改 `quantity/line_amount`，C2-C4 與後代 item set 都不會排隊檢查，失衡可提交。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:194 — 四支 trigger 函式擅自改成 SECURITY DEFINER，違反 plan 的全數 INVOKER 邊界 — service_role 的 INSERT/UPDATE 會以 postgres 權限讀跨表資料，且檔內未指出 INVOKER 究竟缺哪個必要權限。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:43 — 前置閘完全沒查 migration ledger，無法區分矩陣情境 2 與 3 — M SQL 已提交但 M 版本未登記時，本段仍放行 T、移除 gate，日後 `db push` 重跑 M 會撞非冪等 `CREATE`。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:56 — 前置只驗 gate 同名存在，直到 9.7 才驗 `CHECK(false)` — 同名 gate 被改成 `CHECK(true)`且表初始為空時，T 建函式期間可有合法 queued INSERT 提交；T 後段雖回滾，無守門資料已永久留下。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:145 — 新增第七支 helper 函式，但 plan §10 rollback 只具名六支 — 照規格 rollback 後 `pcm_a7bt_allowed_delta(text)` 殘留，重建 M/T 會先撞「已有同名函式」而失敗。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1238 — 第 9 段不驗函式本體指紋或 owner — 把 `pcm_a7bt_jobs_before_update()` 改成直接 `RETURN NEW`，名稱、SECURITY DEFINER、search_path、ACL 全不變，9.4 全綠且所有 UPDATE 守門失效。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1124 — trigger manifest 漏驗 `tgenabled` 與 `tgqual` — 在 DO block 前 DISABLE `a7bt_jobs_before_update`，或替它加 `WHEN(false)`，9.1 仍全綠並移除 gate，但更新守門永不執行。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1127 — trigger 只比 `relname/proname`，沒有比 schema 與 `tgfoid::regprocedure` — 改綁 `evil.pcm_a7bt_jobs_before_update()` 同名 no-op，public 正版函式留著供 9.4 檢查，整段全綠。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1142 — trigger manifest 完全不比 `tgargs` — 把 items DELETE 的參數改成 `a7bt_items_update_blocked`，9.1 全綠，但後續負測得到錯的 `CONSTRAINT_NAME`，精確歸因契約失效。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1287 — allowed-delta 驗收只查非 NULL、含兩個自動欄與欄名存在，沒比精確集合 — 將 `next_retry_at` 加進 E6 白名單後 9.5 全綠，E6 可任意重算重試時間，註解宣稱的自動強制消失。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1113 — 第 9 段完全沒重驗 M 的 36 條 CHECK、五道唯一性與索引指紋 — M 後把 D9d 同名改成 `CHECK(true)`，T 全綠並移除 gate，`recorded_refunded != refunded_before` 仍可授權新世代。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1223 — FK 驗收只比全庫同名且 `confdeltype='r'`，未比表、來源欄與目標欄 — 將 `orji_item_fk` 改為單欄 `order_item_id→order_items.id RESTRICT` 仍全綠，跨訂單品項可掛進退款 job。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1170 — 42 欄 manifest 漏驗 DEFAULT — M 後把 `status` 預設改成 `dead`，9.2 仍全綠並移除 gate；一般省略 status 的 INSERT 隨後全被 `a7bt_insert_must_be_queued` 拒絕。

[must-fix] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:1267 — 函式 ACL 只排除四個具名角色，不是完整 allowlist — 額外 `GRANT EXECUTE ... TO custom_role WITH GRANT OPTION` 後 9.4 全綠，SECURITY DEFINER 函式多出未宣告入口。

[nit] supabase/migrations/20260731120100_m4b_e10_a7b_t_refund_job_guards.sql:927 — 聲稱雙向差集但 UNION/EXCEPT 未加括號，依左結合實際只剩「前代減後代」 — 後代保留全部前代再多一列時 `v_bad=0`，只會先紅在 C2，`a7bt_successor_item_set_differs` 沒證到新增方向。

VERDICT: FAIL