# A9h plan v2 關卡1(codex `gpt-5.6-sol`,唯讀)—— **NO-GO**,11 must-fix + 1 nit

- 審查對象:`docs/specs/2026-08-06-e10-a9h-batch-procurement-plan.md` @ **`5d83713`**(送審前已凍結)
- 跑法:`codex exec -s read-only -m gpt-5.6-sol --cd <worktree>`;**零留痕**(跑前後 `git status --porcelain` 皆空)
- 四個主視窗指定打擊面:**全部 FAIL**

---

## 主對話親驗(不採信審查者的外部事實)

| codex 宣稱 | 我驗了什麼 | 結果 |
|---|---|---|
| A5a 全量覆寫四個選填欄(`20260803160000:354`) | 直接開 migration 讀 UPDATE 的 SET 清單 `:354-360` | ✅ **成立** —— `submitted_at` / `supplier_order_no` / `exception_reason` / `expected_arrival_date` 四欄**無條件**由參數覆寫,無 COALESCE、無 preserve 分支 |
| `apps/admin/vercel.json` 未釘 `maxDuration` | `cat apps/admin/vercel.json` | ✅ **成立** —— 只有 `framework` / `regions`。且 repo 內**已有前例**:訂單明細頁顯式 `export const maxDuration`,並由 `composition-tappay-wiring.test.ts:151` 守門 ⇒ 這個 repo 認為「長工作要顯式釘」是慣例 |

⇒ 這兩條是整份 NO-GO 裡最貴的兩條,**都是真的**。

---

## 🔴 會改變片界的一條(不是我能自己決定的)

**must-fix #1 — 批次會把四個人工填寫欄清成 `null`。**

我自己在 plan §3.1.1 抓到並自標為打擊面;codex 的裁定是
**「警告不能把資料損失變可接受」** —— 也就是說 §3.1.1「寫進測試 + A12b 畫面警告」的處置**不夠**。

它給的第三條路 = **在 A5a RPC 內做明確的 `preserve` 模式**(同一列鎖內原子保留),
或**另建窄版 patch RPC**;並指出單純 `COALESCE` 會讓「合法清空」失效(員工真的想清掉某欄時做不到)。

🔴 **這代表 A9h 不再是純應用層的 `A` 型片** —— 它會需要一支 migration
⇒ 命中鐵則 12③(schema/migration)+ 需要 Sean 手動 apply
⇒ **片界、片型、驗收方式全變**,而這是 `E-121-A` 裁 Q1=A 時沒有預見的。

**我不自行擴張片界**(這是我明文不該做的),列為決策題送回。

---

## 其餘 10 條 must-fix(plan 層可修,不改片界)

| # | 摘要 | 我的初判 |
|---|---|---|
| 2 | **簽章掉了 `orderId`** ⇒ `findOrderIdForItem()` 沒有可比對的對象,MF4 那道「從 A 單寫進 B 單」的防護**被我靜默拿掉了** | ✅ 真的。單列路徑 `procurement-actions.ts:192` 是拿表單 orderId 比 owner;我的批次簽章只有 `orderItemId` ⇒ 無從比 |
| 3 | `{ok:false}` **表達不了「RPC 可能已 commit、結果未知」**(網路在 commit 後斷)⇒ A12b 畫成失敗、誘發重送 | ✅ 真的。需要第三態 `unknown` |
| 4 | §3.1 註解「同品項不同供應商所以用陣列」**與 Q1=A(整批一家供應商)自相矛盾** | ✅ 真的 —— 那段理由是 v1 寫的,折 Q1=A 時**我沒同步改**(正是「只修碰到的行」那一族) |
| 5 | 型別不是執行期驗證;plan **沒有批次上限** ⇒ 空陣列 / 重複列 / 數千列 | ✅ 真的 |
| 6 | **Δ=0 不是行為證明** —— 刪一補一、改斷言、skip 都能維持 Δ=0 | ✅ 真的,這是我把「對帳點」講得比它的能力大 |
| 7 | 「單列 byte 級不變」**未定義且整檔不可能成立** | ✅ 真的,要限定成「分類器本體逐位元組搬移 + action 只准 import 與刪除原區塊」 |
| 8 | 一般 DB / 網路例外(`procurement-repository.ts:149` 裸 throw)**沒有策略** | ✅ 真的,plan 只寫了 `ProcurementCallerBugError` |
| 9 | **授權只做一次**,session 可能在第 k 列前過期 | ✅ 真的。要嘛訂批次與總時長硬上限、要嘛分段重驗 |
| 10 | 「同一家供應商幾乎等於同一張訂單」**不成立** —— 跨訂單同供應商才是正常批次 | ✅ 真的,我 §0 第 3 點的推論站不住(**結論〔序列〕仍成立,但理由要換**) |
| 11 | 逾時 / 失聯時前列已 commit **但結果陣列送不到 A12b**;`NOT_ATTEMPTED` 無濟於事 ⇒ 需 read-back / reconcile | ✅ 真的 |
| nit | Q3 同時寫「已拍」與「確認中、可能翻案」= 活字 | 部分同意:那句活字來自 `E-123-A:7` 主視窗自己的保留,**我不自行清掉**,回報時問 |

---

## 結論與處置

- **NO-GO,不開工。** A9h-1 一行 code 都不寫。
- **must-fix #1 送決策題**(改片界,非我可拍)。
- 其餘 10 條**待 #1 定案後一起折**——因為 #1 的走向會改變簽章(§3.1),
  而 #2 / #3 / #4 / #5 全都掛在簽章上。**先折會白折。**

— E 窗,2026-08-06

---

# 關卡1 R2(同模型 codex `gpt-5.6-sol`,審 plan v3 @ `ff7a906`)—— 仍 **NO-GO**

**R1 十一條:CLOSED 3 / STILL-OPEN 8**(另 R1 nit Q3 活字 = CLOSED)。**新增 12 must-fix + 2 nit。**
零留痕(跑前後 `git status --porcelain` 皆空)。

## 我親驗過的兩條(其餘採信但未逐條驗)

| codex 宣稱 | 我驗了什麼 | 結果 |
|---|---|---|
| `INVALID_INPUT`/`ORDER_ITEM_NOT_FOUND`/`SUPPLIER_NOT_FOUND` 已被既有 code 歸為「呼叫端 bug 型」,而我的規則會讓它們繼續跑 | 開 `procurement-action-state.ts:83-106` | ✅ **成立** —— **`:96`**(⚠️ 原寫 `:95` 是錯的,A9h-1 code-reviewer 抓到並已回頭更正:`:95` 是 `EXPECTED_ARRIVAL_OUT_OF_RANGE`、在分隔線**上方**、屬可改輸入型)註解逐字「呼叫端 bug 型(RPC 回傳碼;表單流程下不該出現)」,底下 `:97-99` 正是那三碼 |
| 我自己開了「跨訂單」與「單一頂層 `orderId`」的矛盾 | 對讀 plan v3 §0-3(採信 codex R1「跨訂單同供應商才是正常批次」)vs §4.1(頂層單一 `orderId` 且要求所有列相符) | ✅ **成立,且是我 v3 自己開的新洞** —— 兩者不可能同時對,第二張訂單的列必被拒 |

## 🔴 判停:兩輪不收斂,且 findings 在同一層打轉

- **v1 → v2 → v3,累計 23 條 must-fix,0 行 code。**
- R2 的 12 條新 must-fix **幾乎全部是同一形狀**:「plan 對 X 規定得不夠精確」
  (批次上限沒有數字 / 業務失敗集合未定義 / byte 級驗收逼出不合法 diff / read-back 沒進硬契約…)。
- 依 `~/.claude/rules/00-work-rules.md` §5 判停條件:**「某輪的 finding 開始重複前輪、或都在同一層打轉 = 方向問題,整理決策題給 Sean 而非繼續折衝」** ⇒ **達成,停。**
- 對照 memory `feedback_decide-process-questions-yourself` 記載的 PCM 前科:
  **「5 份 plan、104 條 must-fix、0 行 code」** —— 我正走在同一條路上,而且該早一輪就喊停。

## 我的診斷:不是 plan 寫得爛,是片界太大

A9h 目前被要求在**同一份 plan** 裡同時定死三種東西:
① 一支 migration 的完整規格 ② 應用層編排 ③ **一個還不存在的片(A12b)的消費契約**。

而 R2 反覆要的那些精確數字 —— 批次上限、逾時預算、per-row timeout —— **只能從實測反推**,
plan 階段寫下的任何數字都是猜的,codex 每輪都會(正確地)打它。
⇒ **繼續寫 plan v4 不會收斂**,因為缺的不是文字,是量測與尚未存在的下游。

處置見 `E-223-STOP` 的決策題。
