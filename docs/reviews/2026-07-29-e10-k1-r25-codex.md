# codex 關卡1 R25(確認輪五)— E10 施工規格 v2(2026-07-29)

> `gpt-5.6-sol`、零留痕已驗。受審 commit `66da298`。**判定 FAIL —— 1 must-fix。**
> R24 的順序/邊界/字面全閉;新抓雙 orchestrator ABA(B 覆寫 A 的 state → A 清掉 B 的 →
> B 停 job 後 kill -9 = 重現不可恢復窗)。修法 = 全段 exclusive lock + run id CAS。

## codex 收斂判斷(逐字)

> R24 指定的寫入/停用順序、恢復後清檔、兩個 crash 邊界及頁首兩閘字面均已正確同步;
> 片表亦重算為 69 片,高風險 42 片未漂移。惟上述並行 ownership 競態會重新產生同類
> 不可恢復狀態,因此本輪仍不可 PASS。

---

FAIL
**Findings**
must-fix — [master plan:822](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:822)、[D1t3:321](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:321)
R24 原 finding 在「只有一個 orchestrator」的前提下已閉合；但新 state 流程沒有 single-flight／CAS，仍存在雙執行 ABA 競態：
1. A、B 同時通過 `active=true`。
2. A 寫 state、停 job；B 隨後用自己的 run id 覆寫 state。
3. A 完成後恢復 job，並把其實屬於 B 的 state 清掉。
4. B 再執行 `active=false` 後若 `kill -9`，會重新形成 R24 要消滅的 `active=false + 無 state`，無法安全恢復。
目前 state 雖存 `run id`，但規格沒有要求恢復／清除前核對它，也沒有禁止第二個執行器覆寫。須定死單一執行鎖或等價的 exclusive ownership，鎖涵蓋「啟動恢復 → 寫 state → 停 job → 執行 → 恢復 → 清 state」全段；恢復與清除須 compare run id，失配即 abort。D1t3 再補「兩個 orchestrator 交錯、後取得者不得覆寫或恢復前者 ownership」負測。
**收斂判斷**
R24 指定的寫入／停用順序、恢復後清檔、兩個 crash 邊界及頁首兩閘字面均已正確同步；片表亦重算為 69 片，分布 `U18/runbook14/A16/M7/R6/T5/docs3`，高風險 42 片未漂移。惟上述並行 ownership 競態會重新產生同類不可恢復狀態，因此本輪仍不可 PASS、不可送 Sean 最終批准。
審查結束仍為 `66da298`，工作樹乾淨，期間零 commit。
