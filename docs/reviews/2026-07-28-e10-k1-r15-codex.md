# codex 關卡1 R15 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `32875bf`,審查期間全 repo 零 commit。
> **判定 FAIL —— 10 must-fix + 1 nit。** 趨勢:67 → … → 7 → 5 → 4 → 7 → 10 → **10**。
> 真缺口:①canonical mapping 漏了現行 writer 寫的 `'tappay'`(`20260611120000:181` 親驗)⇒ 驗收
> 「非 canonical 必為零」照字面必 FAIL ②閉環矩陣漏 `order_payments` 域 ③cash 退還無合約
> ④dead 告警無 durable delivery ⑤**P3 九碼退場只拆了列表 —— 明細頁 `ItemWorkflowStatusCell`
> 與舊 workflow RPC 沒有片承接停寫** ⑥legacy COMMENT 無片承接。

## codex 收斂判斷(逐字)

> 相對 R14,空狀態、舊排序、九題/N6、自由品項及 UX 後綴五項已實質關閉;62片算術與高風險39片
> 正確,也未見既有片明顯必超過45分鐘。但 rail mapping、card切換、cash退款及 reconciler仍未真正
> 閉合,另有P3與legacy COMMENT未被片表承接,DAG/矩陣/62片因此尚非完整一致。R15仍不可施工。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:294:A0a 要求「非 canonical 值為零」，但現行 writer 寫入 `payment_method='tappay'`，且全檔沒有 legacy→canonical mapping。修法：改驗「未映射值為零」，並列明至少 `tappay → card` 的完整 mapping。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:260:閉環矩陣漏掉 `order_payments`，DAG 也未定 schema、live writer、歷史 backfill 的發布順序，切換窗口可能漏記新卡款。修法：補該域矩陣與 `schema → live writer（含 paid replay upsert）→ idempotent backfill → readers/UI → A8b` 子 DAG及負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:451:純現金退款被導向 UX §1 #3，但該路徑只定義銀行帳戶、匯款參考號，沒有現金退還的資料、狀態及防重複合約。修法：另定現金退還登記／複核合約並同步 UX 文件。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:455:A8b 可測矩陣仍只列卡、匯款及「卡+匯款」，漏掉純現金與所有含 cash 的多 rail 組合。修法：補純現金分支，並用 `COUNT(DISTINCT rail)>1` 統一 fail-closed，附各組合測試。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:318:`> target` 先標 `dead` 再送 LINE 沒有 durable delivery／重試狀態，LINE 失敗後該 job 不會再被 claim。修法：加入可認領重試的告警事件／outbox與通知失敗負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:456:worker 摘要仍寫「累計達 target 即完成」，會把 `>` 誤讀為完成，牴觸 A7b 的三路合約。修法：此處同步逐字寫成 `<` 順延、`=` 完成、`>` dead。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:614:已要求替 `orders.fulfillment_status` 寫 legacy-stale COMMENT，但 62 片表、DAG與矩陣沒有任何片承接。修法：明確併入 D0或新增 M 片，並同步片數及依賴。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:418:P3 要求九碼商品狀態退場，但片表只重建列表，現行明細頁仍有 `ItemWorkflowStatusCell`，舊 action／RPC亦未安排停寫。修法：新增具名片清除列表與明細 consumer、停用 writer並定義歷史欄保留策略，再同步 DAG／矩陣／片數。
- [must-fix] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:441:作用中條目仍把計數器指回已 superseded 的 §3.1，施工時仍可能一次補齊全部 counter。修法：改連 E10 v2 §1 原則2與§5.1c，明寫按軸增量。
- [must-fix] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:449:作用中規格仍以 `payment_channel='bank_transfer'` 當匯款收款依據，但該欄只是預期管道，不是實收真相。修法：改以 `order_payments` ledger及其窄 writer為唯一收款／稽核來源。
- [nit] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:503:仍寫「34 筆既有資料」，與本版29張基線及A0a重驗口徑不一致。修法：標成歷史快照或移除固定數字，統一引用A0a。
**收斂判斷**：相對 R14，空狀態、舊排序、九題／N6、自由品項及 UX 後綴五項已實質關閉；62片算術與高風險39片正確，也未見既有片明顯必超過45分鐘。但 rail mapping、card切換、cash退款及 reconciler仍未真正閉合，另有P3與legacy COMMENT未被片表承接，DAG／矩陣／62片因此尚非完整一致。R15仍不可施工。
