# 關卡1 R2 — A6 備註線 plan v2 對抗審查(codex `gpt-5.6-sol` xhigh, read-only)

> 2026-08-02。審查對象 = plan **v2** + R1 findings 逐字檔。判定 **NO-GO**。
> 第一件(折入稽核):35 條中 **26 真修 / 9 假修**(假修 = `[8][9][12][13][16][19][23][24][27]`)。
> 第二三件:**新增 23 條**(18 must-fix + 5 nit,`[36]`-`[58]`)。
> 🔴 `[49]` 是 `[2]` 的同型復發(把部分工作叫成整片,這次是 A9d2 = note+cancel 兩支)。
> 🔴 `[58]` 主對話寫「親驗 6 條」但列了 7 條 — 當日第 5 次數字不一致。
> ⇒ 下一輪依 00-work-rules §5「第 3 輪起必須換角度換模型」= **Fable**。

NO-GO

### 第一件：35 條折入稽核

| 編號 | 結果 | 一句話證據 |
|---:|---|---|
| 1 | 真修 | v2 `:27-28,43,165` 已把 A9d2 放回鏈上，寫入責任不再偷塞 A10a。 |
| 2 | 真修 | v2 `:15,161-164` 改稱 A9a-1，並明說 A9a-2 留給採購線、不宣稱整個 A9a 完成。 |
| 3 | 真修 | v2 `:32-33` 已明確否定 A10a 是採購線必經工作。 |
| 4 | 真修 | v2 `:143-149` 已要求 RPC 同交易寫 `admin_audit_log`；其失敗路徑仍有新洞，見 [40]。 |
| 5 | 真修 | v2 `:52-62` 已承認沒有 `updated_at` 不等於 DB append-only，也不再把單表當不可竄改帳本。 |
| 6 | 真修 | v2 `:54-62` 已明列 owner、superuser、SECURITY DEFINER 均可越過表 ACL。 |
| 7 | 真修 | v2 `:113,131` 已加入 `p_corrects_note_id` 與序列式 `ALREADY_CORRECTED` 路徑；併發仍破，見 [39]。 |
| 8 | 假修 | v2 `:145-149` 只把 `request_id` 寫進另一張表，沒有冪等約束或 note id 關聯，同請求仍可新增兩筆。 |
| 9 | 假修 | v2 `:72-87` 自稱「完整約束」，仍漏 migration `:41-68` 的 PK/default、`created_at NOT NULL DEFAULT now()` 等。 |
| 10 | 真修 | v2 `:129` 已在 INSERT 前固定攔截未來 `occurred_at`。 |
| 11 | 真修 | v2 `:141` 已把 U+2800、U+3164、U+00AD 與既有空白碼位納入 A6 判空。 |
| 12 | 假修 | v2 `:124-138` 仍讓 actor/request 走 RAISE，且過早時間等輸入會落到 DB raw exception，十二碼不是窮盡。 |
| 13 | 假修 | v2 `:122-132` 雖加總順序，但第 8 步內「跨單且已被更正」仍同時命中兩碼，沒有子優先序。 |
| 14 | 真修 | v2 `:132,140` 已先 `FOR SHARE` 鎖 order，並限定只捕捉具名 order FK；該 catch 本身是死路，見 [38]。 |
| 15 | 真修 | v2 `:124` 已承接樣板的先剝白、非空、長度與控制字元守門。 |
| 16 | 假修 | v2 `:130` 只有「上限」兩字，沒有任何數值或計量單位，`BODY_TOO_LONG` 無實作 oracle。 |
| 17 | 真修 | v2 `:117,213` 已要求 BEGIN/COMMIT、5s lock timeout 與 statement timeout。 |
| 18 | 真修 | v2 `:118,214` 已把 `COMMENT ON FUNCTION` 列為產物與驗收。 |
| 19 | 假修 | v2 `:212` 只寫「完整 allowlist」，沒有像函式 ACL `:211` 一樣逐字釘 `is_grantable=false`。 |
| 20 | 真修 | v2 `:210-211` 已要求 `proacl IS NOT NULL` 與 `service_role:EXECUTE:false`。 |
| 21 | 真修 | v2 `:223-225` 已拆 code-DoD／apply-DoD，production apply 另留 Sean checkpoint。 |
| 22 | 真修 | v2 `:180,224` 已列三個 nullable RPC 參數的手動 `\| null` 校正。 |
| 23 | 假修 | v2 `:192` 只禁多列 VALUES 與 INSERT SELECT，兩句單列 INSERT、loop 或動態 SQL 仍可一次寫多列。 |
| 24 | 假修 | v2 `:204` 驗欄值與失敗零增，卻沒逐字要求成功時兩表各恰好 `+1`，雙寫仍可能通過。 |
| 25 | 真修 | v2 `:205` 已補齊配對規則四向負測。 |
| 26 | 真修 | v2 `:206` 已要求固定案例數、0 SKIP 與 fixture fail-closed。 |
| 27 | 假修 | v2 `:190-197` 雖編成 G1–G24，但大量合併、重複、無逐格 mutation，並不是有限且互斥的 manifest。 |
| 28 | 真修 | v2 `:216-219` 已明說從零套用本身對守門判別力為零。 |
| 29 | 真修 | v2 `:216-219` 已明說 typecheck/lint 不證明 SQL 行為。 |
| 30 | 真修 | v2 `:216-219` 已明說審查流程不是守門證據。 |
| 31 | 真修 | v2 `:231-234` 已改成「migration 可逆、營運效果不可逆」。 |
| 32 | 真修 | v2 `:235-236` 已要求較新 down migration 與先撤消費端、最後 DROP A6。 |
| 33 | 真修 | v2 `:162` 已改到實際明細守門 `SupabaseOrderAdapter.test.ts:636-640`。 |
| 34 | 真修 | v2 `:91-96,163` 已補 `NOT EXISTS` 語意及更正前 1／更正後 0 雙向測試。 |
| 35 | 真修 | v2 `:95-98,167` 已補更正不可撤回文案、visited 集合與深度上限。 |

### 第二件、第三件 findings

[36] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:124-140`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:102-109` / 十二碼不是全集：例如 2019 年的 `occurred_at` 不命中未來碼，最後只會撞 raw `23514`。 / 合法 typed RPC 呼叫落在所有固定碼之外，A9d2 沒有穩定結果可映射。

[37] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:131-132` / 第 8 步沒有定義「目標跨單且已被更正」應先回 `CORRECTS_NOT_FOUND` 還是 `ALREADY_CORRECTED`。 / 同一輸入由兩種合理實作得到不同碼，明文檢查順序仍不完整。

[38] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:132,140,196` / 十二碼本身皆有可達向量，但 G18 的具名 order FK catch 靜態不可達：`FOR SHARE` 後 DELETE 只能先完成使查詢回無單，或等待並在 A6 commit 後自己撞 RESTRICT。 / 刪掉該 catch 不會讓「並行 DELETE」測試轉紅，G18 是死 guard。

[39] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:113,131`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:79-85,154-158` / `ALREADY_CORRECTED` 序列第二次可達，但兩筆併發更正會同時通過前查，第二筆直接撞 partial unique 的 raw `23505`。 / 兩名員工同時更正同一筆時，固定碼合約失效；`corrects_not_self` 在 A6 不收新 id 的簽章下只是 DB 縱深。

[40] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:143-149,193-204` / §4.3 沒規定 audit INSERT 失敗必須不捕捉並整筆 rollback，也沒有「note 已寫後故意讓 audit 失敗」負測。 / 實作者若在內層 EXCEPTION 吞掉 audit 錯誤，note 可落地、audit 缺筆，仍回 `APPENDED`。

[41] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:145-149` / audit 只記 body 長度，無法支撐 §3.1「竄改需要動兩表」的 body 完整性宣稱。 / 攻擊者把內容換成同長度文字，只改 `order_notes` 一表，audit 完全看不出來。

[42] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:145-149`；`supabase/migrations/20260712210000_m4a_admin_audit_log.sql:43-58` / audit 沒記新 note id，`request_id` 也沒有唯一性，僅能模糊關聯到 order。 / 同 request 重送兩次且 metadata／長度相同時會有兩筆 note、兩筆 audit，無法判定哪筆是重送。

[43] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:130,138` / `BODY_TOO_LONG` 沒有具體上限、字元或 byte 計量與邊界向量。 / 實作者可任選 1,000、10,000 或 octet length，測試仍無權威答案。

[44] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:188-199` / G1–G12 沒逐碼列 mutation，且其中 `INVALID_BODY`／未來時間與 G16／G17 重複承載。 / 拿掉同一 predicate 會由多格一起轉紅，無法證明各格具有獨立判別力。

[45] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:120-132,191` / G13 只測「order lookup 與 type」一組相對順序，沒有證明其餘檢查的完整 1→10 序。 / 交換 channel／配對、body／corrects 或 corrects／order 的順序仍可能全綠。

[46] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:192,204` / G14 與 APPENDED 驗收沒有釘函式內 `order_notes` INSERT 次數恰一、兩表成功增量各恰一。 / 改成兩句單列 INSERT 加兩筆 audit，不含多列 VALUES／INSERT SELECT，仍可能通過。

[47] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:145-149,193-204` / G15 只驗 audit 列數，沒有逐欄驗 `action/target/after/request_id/source_app`。 / 函式寫一筆錯 target、錯 request_id 或空洞 after 的 audit，仍被判為同交易稽核完成。

[48] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:141,194`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:90-100` / G16 只列三個新碼位，漏掉 A6 也必須攔截的 U+200B/U+200C/U+200D/FEFF。 / 拿掉 A6 對四種零寬字的處理後，輸入會落到 DB raw CHECK，而指定 G16 向量不會抓到。

[49] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:165`；`docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:389` / 母 plan 的 A9d2 是 note＋cancel 兩支 action，v2 卻直接把「只有 note」叫完整 A9d2，沒有 A9d2-1／A9d2-2。 / 本線收工若標 A9d2 完成，cancel action 會重演 finding [2] 的無名欠帳。

[50] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:161-164`；`packages/adapters/src/supabase/SupabaseOrderAdapter.ts:319-331` / v2 要在 PostgREST `ADMIN_ORDER_DETAIL_SELECT` 投影中驗 SQL `NOT EXISTS` 形狀，卻未增加 view/RPC，也未指定改由 mapper 集合運算。 / 實作者不是塞入 PostgREST 不支援的子查詢而 runtime 400，就是放棄更正排除語意。

[51] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:161-167`；`packages/adapters/src/supabase/SupabaseOrderAdapter.ts:319-331` / notes 時間軸沒有任何 `created_at` 排序與相同時間的 tie-break 合約。 / PostgREST 內嵌列可用任意順序回傳，員工會看到更正出現在原紀錄之前或時間軸跳動。

[52] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:148-149`；`supabase/migrations/20260712210000_m4a_admin_audit_log.sql:82-89` / 「audit ACL 較鬆，因 service_role 有 INSERT」把寫權當成讀取曝險；現況 audit 對 service_role 反而沒有 SELECT。 / 隱私取捨建立在錯的 ACL 比較上，雖然不複製 body 全文的決定本身仍合理。

[53] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:212` / `order_notes` 表 ACL 沒逐字要求 `service_role:SELECT:is_grantable=false`，與函式 ACL `:211` 的精確度不一致。 / service_role 持 SELECT WITH GRANT OPTION 時，模糊的「完整 allowlist」仍可能被實作者判綠。

[54] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:72-87`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:41-131` / 「完整約束清單」漏 id PK/default、created_at NOT NULL/default，並把 partial unique index混稱在約束清單內。 / 後續 A9a/A10a 若以此段當完整 schema 合約，會遺漏識別與排序欄的不變式。

[55] must-fix / `docs/specs/2026-08-02-e10-notes-line-plan.md:242`；`supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql:70-131` / §9 的 A3「契約」行號不完整：`:100-105` 沒含 occurred_at CHECK 本體 `:106-109`，也整段漏更正約束 `:70-85` 與 author／配對／channel `:111-131`。 / 實作者照偵察索引精準開檔，仍會錯過本 plan 自稱已讀過的承重契約。

[56] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:243`；`supabase/migrations/20260801160000_m4b_e10_s2_admin_upsert_supplier.sql:346-363,421-439` / §9 樣板行號沒列出 §7.3 實際依賴的 `proacl IS NOT NULL`、owner 對齊與 audit INSERT 能力檢查。 / 實作者只讀列出的 ranges，可能只複製 ACL 攤平而漏掉三個前置守門。

[57] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:180,224`；`packages/adapters/src/supabase/database.types.ts:1-8` / apply-DoD 只寫重補三個新 nullable，卻未明令重補重 gen 同時沖掉的既有七處。 / 只照 §7.5 操作會讓 `create_order` 與 `admin_upsert_supplier` 型別先轉紅，再靠 typecheck 才被動發現。

[58] nit / `docs/specs/2026-08-02-e10-notes-line-plan.md:264`；`docs/reviews/2026-08-02-e10-a6-k1-codex.md:5` / v2 寫「親驗 6 條」但括號列出七條，上一輪原文也明寫共 7。 / 這是本輪再次出現的數字與列舉不一致。

第三件核對正確、未另列 finding 的行號：A3 `:21-27`、`:150-195`；樣板 `:84-87`、`:137-179`、`:302-311`、`:365-391`、`:447-458` 的內容本身；母 plan `:385/:389/:716`；後台規格 `:373-376`；明細投影測試 `:636-640`。§9 的 memory keys 不是檔案行號，graphify 亦明載尚待執行，本輪未把兩者當成已驗證事實。
