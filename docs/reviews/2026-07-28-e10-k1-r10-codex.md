# codex 關卡1 R10 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `dd8299c`,審查期間全 repo 零 commit。
> **判定 FAIL —— 7 must-fix + 0 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → 15 → 17 → 16 → **7**。
> 🟢 大幅收斂:R9 的 16 條「約 12 條已實質關閉」;DAG/矩陣/60 片算術一致、handoff 與 HCT 無新衝突。
> 剩 4 個真缺口(A8c1 鎖錯付款起點 RPC —— 主對話親驗 `begin_charge_attempt` 實存於
> `20260613130000:350`、codex 對 / job 表 `rec_trade_id` 未 NOT NULL ⇒ partial unique 對 NULL 失效 /
> submitted 重領缺 `reconciling` 相位 ⇒ crash 後可能重呼 Refund / A15 漏 port 介面擴充)
> + D1t 片仍超鐵則 4 + 兩處文件同步(§8 Q5「需要時再跑」與硬閘矛盾、STATUS 舊字面)。

## codex 收斂判斷(逐字)

> 相對 R9,16 條中約 12 條已實質關閉,DAG/矩陣/60 片算術目前一致,handoff 與 HCT reference
> 也未見新衝突;但 submitted 重領 phase、NULL 序列化、錯鎖付款起點及 AbortSignal port 四項
> 仍未真正關死,D1t 亦仍違反鐵則 4。R10 尚不可 PASS、目前不可施工。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:324:A8c1 鎖錯 RPC；`record_charge_pending_rec` 是 charge 後寫入，真正起點是 `begin_charge_attempt`。改守 `begin_charge_attempt`，並補取消併發與全域鎖序負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:315:`rec_trade_id` 未定為 NOT NULL，Postgres partial unique index 允許多筆 NULL，仍可產生多個 active job。卡退款 job 強制非 NULL，並補 NULL／併發 enqueue 負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:315:首次送款與 reconciler 都進 `processing`，lease 過期重領後無法辨識應呼叫 Refund 還是 Record，可能重送退款。新增 `reconciling` 或持久化 phase，並測 submitted claim 後 crash/reclaim 不再呼 Refund。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:299:A15 只寫 adapter 的 `options.signal`，漏掉 R9 要求的 `ITapPayAdapter` port；以 port 注入的 worker 仍不能傳 AbortSignal。同步擴 port、typed callers/mocks 與 timeout 負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:308:D1t 同片含完整 orchestrator、交易流程、隔離 DB dry-run、雙層 timeout 與 rollback 負測，仍明顯超過 45 分鐘。拆成執行器核心與 timeout／rollback 整合驗證片，重算 DAG、片表與統計。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:599:拍板表仍說 TapPay read-back「需要時再跑」，與 §8.4 的 D1b1／D1c 強制硬閘衝突。改成正式 DELETE 前兩次 read-back 均不可跳過。
- [must-fix] STATUS.md:19:現行 SSoT 仍留「五支 RPC」及「D1 九片」，與 master plan 的三支／12 片矛盾。待 A8c1 正確鎖定後，同步精確 RPC 名稱與 D1 片數。
**收斂判斷：**相對 R9，16 條中約 12 條已實質關閉，DAG／矩陣／60 片算術目前一致，handoff 與 HCT reference 也未見新衝突；但 submitted 重領 phase、NULL 序列化、錯鎖付款起點及 AbortSignal port 四項仍未真正關死，D1t 亦仍違反鐵則 4。R10 尚不可 PASS、目前不可施工。
