# ⟦f3-RECIPIENTBIND1⟧ 複量 —— 查的是「當初判它不擋的理由今天還在嗎」

> 2026-09-10 · 線【SEO/GEO】窗 D · 全唯讀,零檔案改動,零寄信。
> 🛑 `docs/launch-todo.md` 凍結唯讀 ⇒ **本檔不是板子的更新**,是查證紀錄。板上那一列的字保持原樣。

## 為什麼是這個問法

那一列 2026-09-07 已被主視窗裁 ⟨不擋⟩,而裁定當時寫了一個**復活條件**(逐字):

> 「`PaidEmailContext` 的來源哪天不再是當場用 `job.orderId` 現撈(換成快取 / 批次 / 別的來源)⇒ 立刻升回 ⟨擋⟩。」

⇒ 📌 一列已被裁不擋的東西,**正確的複查不是重問「它壞了嗎」**(那會把已經量過的結論再寫一遍),
**是問「當初判它不擋的那個理由今天還成不成立」。**

## 量到的(2026-09-10)

✅ **復活條件仍閉著 —— 四發內容查詢全部還鍵在 `job.orderId`,現撈,沒有快取也沒有批次:**

| 位置 | 逐字 |
|---|---|
| `packages/use-cases/src/sweep-email-outbox.ts:1583` | `loadPaidContext({ orderId: job.orderId })` |
| 同檔 `:1699` / `:1931` | `loadShippedContext({ orderId: job.orderId, shipmentId })` |
| 同檔 `:1790` | `isBankOrderStillMailable({ orderId: job.orderId })` |

✅ **收件人仍是單一來源**:全檔 `recipientEmail` 只有 6 處,真正用到的兩處是
`:2105` 的守門(`cur.email !== job.recipientEmail` ⇒ 標 stale 不寄)與 `:2135` 的 `to: job.recipientEmail`。

✅ **缺口本體也還在**:`packages/ports/src/IPaidEmailContext.ts:96` 只有 `orderDisplayId`,沒有 `orderId`
⇒ 今天仍然斷言不出「這份內容屬於這張單」。

🟢 正對照:同一支 `/usr/bin/grep` 問 `order` ⇒ 111 命中 ⇒ 尺是活的,上面的讀數不是空手而回。

## 🔴 板上那組行號已經漂了 —— 而形狀沒變

```
板上寫(2026-09-07)  守門 :2013 · to: :2043 / :1747 · 匯款比對 :1504-1511
2026-09-10 真值      守門 :2105 · to: :2135         · 匯款比對 :1816
```

🔵 **只有行號舊了,結構完全沒變** —— 不要以為那幾段被改寫過。
📌 而**下一個照板上行號去開檔的人會開到別的地方,且他不會知道自己開錯了**。

## 處置(2026-09-10 主視窗定)

```
狀態    維持 open(不是「還沒做」, 是「等一個不會自己來的觸發」)
不做的理由
  · 原觸發路徑已死:讓客人自己改 email ⇒ Sean 2026-09-09 裁不做
  · 復活條件仍活:PaidEmailContext 哪天不再現撈(改快取 / 批次)⇒ 立刻升回擋
  · 而那個條件【與客人無關】—— 它是我們自己哪天改架構才會踩到
真要修的成本  4 檔 + 動寄信路徑 ⇒ 鐵則 8 + 12⑤ ⇒ 要批准 + codex
```

🔴🔴 **而那個復活條件今天沒有任何東西在守** —— 有人把 `loadPaidContext` 改成吃快取的那天,
**不會有任何一格紅**。這件事只靠人記得(主視窗記進 `CURRENT.md` 的「靠人記得的事」那節),
**刻意不加閘** —— Sean 2026-09-09 明文不做量具。

## 本次證不到什麼

① 只讀 repo,**沒有跑任何一條真實寄信路徑**(本線零寄信)。
② 繞過應用直接寫 DB 的情境不在射程。
③ `⟦f3-PAIDHTMLWIRING1⟧` 在等的那題(值不值得現在放大洩漏面)是 Sean 的題,本次不碰。
