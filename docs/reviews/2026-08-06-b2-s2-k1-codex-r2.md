# B2-S2 plan 關卡1 對抗審查 R2(codex,`-s read-only`)—— **兩輪用盡、停損**

> 2026-08-06 夜跑窗。受審 = plan **v2**(R1 的 25 must-fix + 2 nit 全折入後的版本)。
> **零留痕已驗**:跑前後 `git status --porcelain` 逐字相同。原始輸出 = `/tmp/codex-k1-r2-out.txt`。
> R2 的任務**不是重掃 R1 已修的面**,是查「折入本身有沒有製造新問題」。

## 判定

**FAIL / 22 must-fix。其中 14 條標「R1 #N 未真正修復」。**

⇒ 依 `B-130-A` ③ 與 `~/.claude/rules/00-work-rules.md` §5 的輪次紀律(codex 每片硬上限 2 輪),
**停,不開 R3**;整理決策題給 Sean。**plan 凍結在 v2,R2 findings 不折入**
(折入 = 變相第 3 輪,而且方向題沒拍板前折了會白折)。

## 22 條(原文)

| # | R1 對應 | 標題 | 位置 | 一句話 |
|---|---|---|---|---|
| 1 | **#1 未修** | restore 選檔現況即必誤選 | plan:482-497 | 規則會選入只在註解提到摘要表的 A2b1/S1a1 等非冪等 migration,重跑 A4a 裸 `CREATE FUNCTION` 撞 `42723` |
| 2 | **#1 未修** | down 世界與四軸活體斷言互斥 | plan:484-493 | 合法 down 收斂回三軸後,步驟 2 仍硬驗四欄 ⇒ 正確 restore 也必紅 |
| 3 | **#24 未修** | 五片封窗只有文字、沒有物理閘 | plan:121-137,409-412 | S2a/S2b 一進 `supabase/migrations/`,`db push` 完全看不到三個 S2c commit |
| 4 | **#23/#24 未修** | 型別重生與封窗形成循環 | plan:348-357,515-527 | S2c-3 要在 push 前 commit,但項 28 明定只能在 apply 後完成 ⇒ 字面永遠無法同時滿足 |
| 5 | **#24 未修** | 五片不是各自可綠的 commit | plan:121-137,343-356 | S2b 進目錄後 `a4a-verify` N8 會紅到 S2c-2、`a1-verify` 斷裂態留到 S2c-3 |
| 6 | 新 | 大量行為驗收沒有承接腳本 | plan:123-129,292-405 | 五片產物沒有 `b2s2-verify.sh`;項 10-20/26-27/故障注入/突變矩陣塞不進正式 migration |
| 7 | **#7 未修** | 函式 ACL 驗收保留已知假綠角 | plan:182-188,316-317 | 忘記 `REVOKE` 時 `proacl IS NULL` = PUBLIC 仍可 EXECUTE,但 `aclexplode(NULL)` 回零列 ⇒ 項 9b 全綠 |
| 8 | **#11 未修** | 行為靶⑥兩個環境都沒有紅點 | plan:318-321,389-401 | 漏 `shipped_at IS NOT NULL` 時項 10 仍正確;矩陣引用的「草稿箱變體」**不存在於驗收表** |
| 9 | **#11 未修** | 突變唯一對應仍斷裂 | plan:393-401 | 靶④同時指向項 26/27 多格;靶⑬一次改兩支函式且沒有具名 barrier oracle |
| 10 | **#8 未修** | `ORDER BY` barrier 仍只有名詞 | plan:258-262,399-401 | 沒定義兩 session 的 fixture／同步點／提交序／預期 SQLSTATE ⇒ 突變可完全沒跑仍宣稱有 barrier |
| 11 | **#10 未修** | X1 rollback 仍只重述結論 | plan:327 | 沒寫如何先觀察重算值、再製造 commit-time 零品項違規 |
| 12 | **#13 未修** | 三處真相式守門沒有判別力 | plan:108-116,335-339 | runbook 實際複製兩份(初始對帳 + 收尾重驗)⇒ 連 helper、shell 共**四個**文字實體;無邊界標記/正規化/逐處 mutant |
| 13 | **#17 未修** | md5 雙對照庫只存在處置表 | plan:312 | 同庫先跑 control 會把 helper 換成四軸,之後 mutant 即使沒改也被拒 ⇒ 重現原假證明 |
| 14 | **#21 未修** | 全樹契約債仍未結清 | plan:264-275 | master plan `:555-564,580-585` 仍稱未定案;上游 v5 `:679,683` 仍交棒 writer RPC |
| 15 | **#22 未修** | DB COMMENT 清單仍漏處 | plan:277-285 | A1 表 COMMENT `:140-144` 仍寫「三個 0／四條比較」;新欄沒有欄 COMMENT |
| 16 | **#25 未修** | 三支 S1 harness 仍無預期計數 | plan:350-356 | 項 25d 只要求事後「數字入帳」⇒ 少一格或多紅一格都能照抄成成功 |
| 17 | **#27 未修** | 行號漂移仍只修了一半 | plan:337 | runbook oracle 實為 `:60-73,146-149`、`a4a-verify.sh` 實為 `:146-156` |
| 18 | 新 | 四值互異宣稱與 fixture 相反 | plan:322,543 | 項 14 實值 `q=3/i=2/c=0/s=3` —— `quantity` 與 `shipped` **相同**,欄位誤接仍可能假綠 |
| 19 | **新(設計)** | `shipment_items` 防禦 trigger 不該在本片落地 | plan:197-206,258-262 | 今天零唯一契約,卻新增高風險函式 + 每次 INSERT 的鎖 + 反序多列 `40P01` 面 |
| 20 | 新 | rollback oracle 候選全集漏 shipment-only 品項 | plan:335-339 | 品項只存在 `shipment_items` 且摘要列遺失時,`procurement ∪ cancellation ∪ summary` 根本不產生該列 |
| 21 | **#18 未修** | S2b 故障注入仍未具體化 | plan:414-420 | 沒指定 COMMIT 前注入點與基準快照 |
| 22 | **#24 未修** | 五片估時仍失真 | plan:123-129 | S2b(migration + 兩函式 + 兩 trigger + backfill + ACL + 20 多格驗收 + 突變)不可能 45 分;S2c-3 橫跨四支約 1,990 行 |
| 23 | 新 | row-count gate 沒有可判定門檻 | plan:154-158 | 只要求「重跑列數」卻沒有通過／停止值 |

## 主對話親驗(全部抽驗,無一駁回)

| R2 # | 親驗方式 | 結果 |
|---|---|---|
| **1** | 實跑選檔規則:`版本 > 20260730150000 且檔內含 order_item_quantity_summary` | 🔴 **成立,且是 v2 引進的新 bug**。命中 **6 支**:`20260801150000` / `20260803130000`(A2b1)/ `20260803140000`(**A4a**)/ `20260803160000`(A5a)/ `20260805100000`(A8a2)/ `20260805170000`(S1a-1)。A4a 內有 **5 個裸 `CREATE FUNCTION`** ⇒ 重跑必 `42723`,**連 S2a 都到不了**。我為了修 R1 #1 寫的修法,自己是壞的 |
| **7** | PG 語意 | ✅ 成立。`proacl IS NULL` = 未曾 `GRANT/REVOKE` ⇒ PUBLIC 有預設 `EXECUTE`;`aclexplode(NULL)` 回零列 ⇒「零 grantee」斷言全綠。與 memory `reference_pg-table-acl-flatten-blind-to-column-grants` 同族 |
| **12/17** | 開 runbook 與 harness 比對 | ✅ 成立。runbook 的漂移式在 `:60-73` 與 `:146-149` **各一份**;`a4a-verify.sh` 的 `ORACLE_SQL` 是 `:146-156` 多行,非單行 `:152` |
| **14** | 開 master plan 與 v5 比對 | ✅ 成立。MP `:555-564` 仍含「尚未定案」;v5 `:679` 仍寫「強制點由出貨 writer RPC 承接」、`:683` 仍寫「shipped 該在哪一層被強制未定案」——**兩處都被 Sean 08-05 Q1=A 推翻,但字面還活著** |
| **20** | 讀 `a4a-verify.sh` 的 `ORACLE_SQL` 第三段 | ✅ 成立。候選全集逐字 = `order_item_procurement UNION order_cancellation_items`,**沒有 `shipment_items`** |
| **18** | 讀我自己 plan 項 14 | ✅ 成立。`quantity=3` 與 `shipped=3` 相同,違反我自己在 §9.1 #24 立的「四值互異」紀律 |

## 為什麼判「不收斂」而不是「再修一輪」

- 兩輪合計 **47 條 must-fix**,R2 有 **14 條是「R1 說了、v2 寫了字、但沒寫出機制」** ——
  這不是 codex 鬼打牆(它的事實全部查證屬實),是**我在 plan 層反覆用文字承諾機制**。
- R2 #19 更是把 R1 #5 的結論往前推了一步:R1 只要我「收回過度宣稱」,
  R2 直接問「那這支 trigger 為什麼還在本片」——**這是設計岔路,不是缺陷**,不該由審查輪決定。
- 依 memory 的輪次紀律:「某輪的 finding 開始重複前輪、或都在同一層打轉 = 方向問題,
  整理決策題給 Sean 而非繼續折衝」。⇒ **停在這裡是照規則,不是放棄。**

—— R2 完 ——
