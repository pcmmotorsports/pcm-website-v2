# 2026-09-09 窗 A 十二列查證

> 每列三行:**結論** / **證據逐字** / **卡在誰**。
> 🛑 本檔**不動任何一列的態或 token** —— 翻態是上線判斷,窗 A 只量與回報。
> ⚠️ 全檔讀數量於 2026-09-09,正式站當時跑 `9917fae21`(`dpl_9hYJMgwtkmALZYUPvyFacLTfom3Y`)。

---

## 0 · 三格「本機驗不到、要線上量」

### ①「?category= 的 h1 會不會印分類名」
- **結論**:會。
- **證據**:瀏覽器讀水合後的 DOM ⇒ `.pp-title` = `排氣系統` · 麵包屑 `首頁 › 商品目錄 › 排氣系統` · `940 件商品`。
  🛑 **curl 看到的是 `全部商品`** —— 那是水合前的殼,分類標題由 client cascade state 畫。**這一格不能用 curl 驗。**
- **卡在誰**:無,結案。

### ②「anon 能不能直讀 `product_variants_public`」= 變體料號搜尋有沒有生效
- **結論**:🎯 **生效**。commit `0dc6c79b4` 在正式站是活的。
- **證據**:`?search=MATERYA-9281-9401` ⇒ **1 件商品** · `?search=VF11SIL` ⇒ **1 件商品**。
  ⚪ **反對照(唯讀正式庫)**:這兩個字串在 `products.title` / `products.external_id` 命中 **0 / 0**,而在 `product_variants.sku` 各 **1**
  ⇒ 📌 **母表撈不到而頁面撈得到 ⇒ 只可能是變體 fallback。**
- **卡在誰**:無,結案。

### ③ 側欄 facet counts 的 fan-out 穩不穩
- **結論**:穩。8 輪全有數字,「件數暫時無法顯示」**0 次** ⇒ 這一列結案不修。
- **證據**:同一頁 8 次 ⇒ 每次 `碳纖維部品 2456` / `排氣系統 940` 都在;失敗字樣 `FACET_COUNTS_UNAVAILABLE`(`products-message-state.tsx:60`)命中 0。
  🛑 **我第一把尺是死的**:curl 抓 `.ft-brand-count` 兩邊都 0 —— class 名不對。
  🟢 換成「抓真數字 + 分類名當正對照」才算數。
- **卡在誰**:無,結案。

---

## 1 · `⟦f3-FITSTALE1⟧` 車款搜尋資料停更沒有人會知道
- **結論**:🔴 **客人那一側【今天答不出來】⇒ 結案不修**(Sean 端拍甲,經主視窗轉達)。
- **證據**:
  · 同步是活的:`product_fitments_effective_sync_log` 最後一筆 `2026-09-08 23:05:26+00 · success · new_count 280238`(距量測 13h34m)。
  · 🔑 **車款選單是從零件資料自己長出來的**:`pg_get_viewdef('vehicle_taxonomy_public')` 逐字 `SELECT DISTINCT … FROM product_fitments` UNION ALL `FROM product_fitments_effective`
    ⇒ 📌 **「還沒收錄的車」根本不在選單裡,客人選不到它。**
  · 選得到卻 0 件的車:**2 / 3818**(`Harley-Davidson BigTwin` · `Harley-Davidson Pan America`)。
  · 庫裡沒有獨立車款主檔(`information_schema.tables` 只有 `customer_vehicles` 與那支 view)⇒ 打字搜不到時,**分不出「沒收錄的真車」與「打錯字」**。
  ⚠️ 列上指的 `search/page.tsx:76` 已非客人落地頁(2026-09-03 起是 `/products?search=`)⇒ 座標過期。
- **卡在誰**:無。🛑 乙案(拿 `customer_vehicles` 當「存在但沒收錄」名單)是**新功能不是修 bug**,要另外排、要 Sean 批。

---

## 2 · `⟦f3-HALFWRITE1⟧` 供應商同步半寫入
- **結論**:主張仍成立;⑤ 已被線 `sync` 打掉,**① ② 仍開**,而修法那半不是施工窗能收的。
- **證據**:座標當場複量**是新的** —— `rpm-import.ts` 的 `upsertBatched(target, 'product_variants', …)` 在 `:1087` · `runAtomicGroups` 在 `:1118` · `grep -c catch` **10** · `grep -c 'throw new Error'` **17**;探針 `scripts/halfwrite1-partial-write-probe.sh` 在。
- **卡在誰**:① ②(「那次幾筆進去」要正式庫的數 / 本機庫證不了正式資料)= 🔧我們;**修法(跳過繼續 vs 整批停)列上逐字「要另外拍板」⇒ 🧑Sean**。

---

## 3 · `⟦db-SEARCHFACETMUTEX⟧` 打了字就不能點篩選
- **結論**:🔴 **DB 那半已落地,客人那半仍開**(前台還沒接上)。
- **證據**:
  · 正式庫:`search_catalog_by_vehicle` 與 `search_catalog_by_vehicle_dealer` **兩支都有 `p_terms`**(13 參數)。
    🔵 順帶:公開那支還留著一支 **11 參數舊 overload(無 `p_terms`)** ⇒ 前台接線時要確認打的是 13 參那支。
  · 前台沒接:`use-catalog-filter-url-sync.tsx:538` 仍 `params.delete('search')`;`app/products/page.tsx:387` 關鍵字仍走 `searchProducts`(ILIKE)。
  · 🔬 **線上實測(瀏覽器)**:`?search=MATERYA-9281-9401&category=煞車系統` ⇒ **1 件**;`?category=煞車系統` ⇒ **448 件**。
    那顆商品不在煞車系統裡 ⇒ 兩者若同時生效應為 **0** ⇒ 📌 **分類完全沒生效,互斥仍在。**
  · 🔵 止血那半在跑:打「排氣管」自動轉成 `?q0=排氣管&category=排氣系統 · 尾段排氣管(Slip-On)` ⇒ 376 件;「隔熱」⇒ `隔熱罩與防燙蓋` 38 件。
- **卡在誰**:🔧我們(接線)。🛑 而**「點篩選要不要清關鍵字」是另一題** —— 來源鏈斷在 `7bfefe4af4` body、自標【主視窗】拍的不是 Sean ⇒ 要推翻它得找拿得到原文的人。

---

## 4 · `⟦auth-PROBENEXTRED⟧` 鑽機收攤後 typecheck 紅在沒人改過的檔
- **結論**:已 `done`,修法實地核在檔裡,無事可做。
- **證據**:`scripts/storefront-probe/down.sh:86` `rm -rf …/apps/storefront/.next/dev` · `scripts/admin-probe/down.sh:122` `rm -rf "$REPO_DOWN/apps/admin/.next/dev"`;⚪ 負對照現造路徑 **0 / 0**。
- **卡在誰**:無。

---

## 5 · `⟦f3-FWBYPASSORDER⟧` 防火牆 bypass 排序
- **結論**:已 `done`,兩支閘都在;🔴 **而兩件對不上,只回報不動。**
- **證據**:
  · `scripts/vercel-firewall-cron-order-check.py` 與 `scripts/vercel-json-waf-cron-gate.py` 都在。
  · (a) 列上自己寫「為什麼還不能 `done`(三個條件一個都沒滿足)」—— 順序不是被強制的 · `vercel.json` 那條路放不了 `bypass` · CLI `rules add` 落點未測;**而翻 `done` 的理由是「兩個載體已同步」** ⇒ 📌 **那是兩件事,關門條件沒被滿足。**
  · (b) 列上引的 `CLAUDE.md:77` 逐字**已失效** —— 2026-09-09 減法版把那段砍了。當場量 `grep -c FWBYPASSORDER CLAUDE.md` ⇒ **0**(⚪ 負對照現造 `FWBYPASSZQ8V` ⇒ 0);兩支腳本現在只出現在 `:62`。
    ⇒ 🎯 **那正是本列自己記的病:一句描述落差的話,壽命比落差長 —— 而它發生在它自己身上。**
- **卡在誰**:🧑Sean / `-ship`(要不要因為關門條件沒滿足而退回 `open`)。

---

## 6 · `⟦search-TAXONOMY2MB⟧` 車輛對照表塞不進 Next data cache
- **結論**:🎯 **出場條件答完了,而答案是【裁定站得住】** —— 列頭那句的兩半都倒了,3/7 現在是 **7/7**。
- **證據**:
  · 列上逐字的出場條件:「有人讀到 `dpl_7hmz…` 那 4 筆,而其中有一筆的 key 是車輛對照表 ⇒ 本裁定作廢」。
  · **那 4 筆讀到了**(窗 `2026-09-05T13:00Z`–`19:00Z`,`get_runtime_logs` scope 到該部署;7d 全窗一律逾時,逐日切窗才拿得到):
    ```
    17:06:48 /products?_rsc=…&per=100&vehicle=ducati:scrambler-1100-club-italia  (3927137 bytes)
    17:06:18 /products?_rsc=…&per=100&vehicle=ducati:scrambler-sixty2            (2683747 bytes)
    17:06:10 /products?_rsc=…&per=100&vehicle=ducati:scrambler-800:2023          (2679379 bytes)
    17:05:47 /products?_rsc=…&per=100&vehicle=ducati:scrambler-800:2023          (2679379 bytes)
    ```
  · ⇒ 📌 **7 筆的 key 全部是 `/products?…&vehicle=`(商品清單頁),車輛對照表一筆都沒有。**
  · 🟢 同一批 log 裡車款樹自己那把 key(`vehicle_taxonomy_public` 那支 producer)是 `revalidating cache with key`,**沒有被拒絕**。
  · 🟢 正對照:同一把尺在其餘四天各回「no logs found」而不是逾時 ⇒ 尺會動、也分得出空與死。
- **卡在誰**:🧑Sean / `-ship` 重判 `⟨擋⟩` token。**我不動它。**

---

## 7 · `⟦search-TAXONOMYTIMEOUT⟧` 車款查詢逾時且靜默降級
- **結論**:🔴 **列上那個症狀今天量不到了** —— 機制沒了,**而現況也量到了**(那正是本列自己寫的判別式)。
  ⚠️ **而 `57014` 本身還在,只是換了一條路** —— 見下。
- **證據**:
  · 本列症狀逐字是 `[tryVehicleTaxonomy] cached fitments fetch failed: … 第 4 頁(offset 3000)失敗 … 57014`(分頁路)。
  · 今天正式站 ~30 筆 `[vehicleTaxonomy]` 逐字全是 `cold n=12315 ms=498…2210` ⇒ **零次逾時、零次殘缺**。
  · 🟢 **本列③「`n` 只證回傳自洽,不證等於 `count(*)`」今天關掉了**:唯讀正式庫 `select count(*) from vehicle_taxonomy_public` ⇒ **12315**,與 log 的 `n=12315` **逐位元組相同**(兩把獨立的尺)。
  · 🔴 **而 `57014` 出現在【另一條路】**:`13:21:09 GET /api/search 503` 逐字
    `[searchProducts] searchByKeyword failed: { code: '57014', message: 'canceling statement due to statement timeout' }`
    ⇒ 📌 **那是搜尋 RPC 逾時、客人拿到 503,不是車款樹。**
    ⚠️ **射程**:那一批流量是我自己連續打出來的(同時段被 Vercel 擋成 429)⇒ **那是負載下的讀數,不是自然尖峰**;n=1。
- **卡在誰**:車款那半 🔧我們(只剩翻態);🔴 **`/api/search` 那個 503 是新的一格,不在任何一列上。**

---

## 8 · `⟦db-TAXONOMYVIEW⟧` 顧客站切分類要 3 秒
- **結論**:🔴 **標題那句【仍然成立】,而它點名的真兇【已經洗清】。**
- **證據**:
  · **真兇洗清**:`products.ts` 現在是一發 `.rpc('get_vehicle_taxonomy')`,`tax=` 這一腿今天逐字落在 **40–717 ms**(10 筆),沒有一筆逾時;13 頁分頁在碼裡不存在。
  · **標題仍成立(真的點側欄,尺 = 第一張商品卡的 href 換掉)**:
    ```
    排氣系統 2489ms(940 件) · 進氣系統 3095ms(782 件) · 煞車系統 3467ms(448 件)
    ```
    ⇒ 比本列原記的 3371/3378/5487/6259 ms 好,**而仍在 2.5–3.5 秒**。
  · **時間現在花在別的腿**:同批 `[catalogRoute]` 逐字 `cats=` 出現 **1513 / 1893 / 3556 ms**,而 `tax=` 全在 717 ms 以下。
  🛑 **我第一把尺是死的**:用「`.pp-title` 變成新分類名」當訊號 ⇒ 量到 **17ms / 4ms** —— 標題是樂觀更新,瞬間就變。換成商品卡才是客人看到的那件事。
  ⚠️ **射程**:同上,那段時間我正在對正式站連續打;n=3、單一客戶端、非尖峰。
- **卡在誰**:🔧我們 —— 但**受詞要換**:不是車款樹,是 `cats=` 那一腿。

---

## 還沒碰的 3 列(碰錢,排最後)
`⟦auth-DEALERTIERPRICING⟧` · `⟦b4-DEALERSIGNUPUNSEEN⟧` · `⟦db-DEALERCATALOGRPC93⟧`

## 兩件量測紀律(今天各撞到一次,寫在這裡不另開檔)
- 🔴 **正式顧客站連續 curl 會被擋** —— HTTP **429 `Vercel Security Checkpoint`**。交接檔原本只寫「後台不能用 curl 驗」,**顧客站也會**。
  ⇒ 那之間的四筆讀數我**作廢**,改用瀏覽器重量。**線上量一律用瀏覽器。**
- 🔴 **`get_runtime_logs` 對寬窗會逾時而不是回空** —— 逾時與「沒有」長得不一樣但都拿不到東西。
  ⇒ scope 到單一 `deploymentId` **還不夠**,要再逐日切窗;而「其餘四天回 no logs」正是證明尺沒死的那一格。

---

# 追加 · 2026-09-10 經銷三列

## 9 · `⟦auth-DEALERTIERPRICING⟧` 經銷會員登入看不到經銷價
- **結論**:仍成立,而**列上撐著它的那個前提已經不成立** —— 那個「0」變成 **9**。
- **證據**(2026-09-10 唯讀正式庫):
  ```
  customers.tier                     general 15 · store 0        ← 經銷會員仍 0 人
  product_variants                   61,156 · price_store 有值 1 · 差價 0
  products.price_store 欄位          25,866 · 有值 1 · 差價 0
  products.price_by_tier->'store'    有鍵 25,866 · 【差價 9】     ← ⛔ 列上寫 0
  ```
  · 那 9 筆**全部是 2026-09-09 更新的**,其中 **5 筆的經銷價比一般價貴**
    (最大 `BMS1K2K-18` WSBK 搖臂護蓋 4,600 ⇒ **12,800**)。
  · 🔬 **三條路讀三個不同的來源**(逐字):
    `products_list_dealer` ⇒ `COALESCE(pr.price_store, v.price_general)` ← products 欄位
    `get_effective_prices` 變體層 ⇒ `coalesce(v.price_store, v.price_general)` ← variants 欄位
    `get_effective_prices` 商品層 ⇒ `p.price_by_tier -> v_tier ->> 'amount'` ← products JSON
    `create_order` ⇒ `v_variant.price_general`(`20260906500000:382,837`)
  🛑 **我一開始量錯受詞** —— 先量了 JSON,而目錄讀的是欄位。開檔核完才分得開。
- **卡在誰**:🧑Sean —— 「三個來源哪一個才算數」是產品決定。**今天 0 筆會踩到**(經銷會員 0 人)。

## 10 · `⟦b4-DEALERSIGNUPUNSEEN⟧` 經銷會員登錄那條路沒有人驗收過
- **結論**:🟢 **關閉條件②(有人實際走完並附證據)我做到了。** 而走出一個新缺口。
- **證據**(拋棄式鑽機真瀏覽器,`probe2@example.com` 升成 `tier='store'`,
  `G3-0001` 一般 3,670 / 經銷 3,000;**零正式庫寫入**):

  | 走到哪 | 經銷會員 | 一般會員(⚪ 反對照) |
  |---|---|---|
  | 帳戶頁徽章 | 「店家會員 · 已享店家經銷價」 | — |
  | **目錄卡片** | **NT$ 3,000** | **NT$ 3,670** |
  | **商品頁** | **NT$ 3,000** | **NT$ 3,670**(頁面上無 3,000) |
  | **關鍵字搜尋** | 🔴 **NT$ 3,670 = 一般價** | — |

  ⇒ 🟢 目錄與商品頁**都接對了**(列上 2026-09-04 記的「價格那一半全站沒接」已不成立)。
  ⇒ 🔴 **而關鍵字搜尋那條路,經銷會員拿到的是一般價** —— 那是 `⟦db-SEARCHFACETMUTEX⟧`
     合路那片會修的,而現在它是**走出來的**,不是推的。
- 🛑 **兩個坑我踩到並排除掉了,兩個都是【我的環境】不是 bug**:
  ① 第一發目錄頁印「載入失敗、請稍後再試」—— 查出 `products_list_dealer` **在鑽機上不存在**
     (`to_regclass` 回空);正式庫今天讀得到它的定義。⇒ 照正式庫定義逐字補建後就正常了。
  ② 商品頁第一發顯示 3,670 —— 鑽機的 `price_by_tier` 是**純整數** `{"store":3000}`,
     而正式庫是 `{"amount":3000,"currency":"TWD"}`;`get_effective_prices` 讀 `->>'amount'`
     ⇒ 在鑽機的形狀上永遠 NULL 而退回 general。改成正式庫形狀後 ⇒ 3,000。
  📌 **兩次都差一步就變成一條假 finding。**
- **卡在誰**:關閉條件①(Sean 本人看過)仍未滿足 ⇒ 🧑Sean。上表可當他看之前的參考。
- 🛑 **[2026-09-10 · 現在不修 —— 依 Sean 2026-09-09 拍甲]** 逐字「**現在不動,整包排上線後,
  跟 B2B 子網域一起做**」(經主視窗轉達)⇒ **上表那格「關鍵字搜尋經銷會員拿到一般價」
  歸【上線後那一包】的規格,不在今天修。**
  📌 寫這一行的理由:三個月後看這份檔的人會問「量到了為什麼沒修」—— **這一行就是答案。**

## 11 · `⟦db-DEALERCATALOGRPC93⟧` 經銷客目錄的篩選 / 排序 / 筆數
- **結論**:列上「**沒有任何東西保證有人叫新 RPC**」那一格**已經過期**;而同一發撈到一個新缺口。
- **證據**:
  · ⛔ ~~`search_catalog_by_vehicle_dealer` 在 `apps/storefront/src` 非測試檔 ⇒ 0 處~~
    ⇒ ✅ 今天 `lib/products.ts:435` 逐字 `const CATALOG_RPC_DEALER = 'search_catalog_by_vehicle_dealer'`
    (🟢 正對照 舊那支 `:434` 也在 · ⚪ 負對照 現造名 ⇒ 0)。上面第 10 列的走查也證了它真的在跑。
  · 🔴 **新缺口:側欄件數那條路仍走【公開】那支** ——
    `lib/vehicle-facet-counts.ts:83` 逐字 `fn: 'search_catalog_by_vehicle'`(`:203` 同),
    而它由**公開端點** `/api/catalog/facet-counts` 提供。
    ⇒ 經銷會員的**清單**用經銷價篩,**面板數字**用一般價篩。
    🎯 而那支檔自己的 docblock 逐字寫著它的保證是
    「**面板數字 = 點進去的件數**」來自「**兩邊走同一支 RPC 的同一個述詞**」
    ⇒ 📌 **那個保證對經銷會員已經破了。**
    🛑 而它**不能只是把 tier 傳進去** —— 那是公開端點,傳了等於把經銷價的件數給 anon 算得到。
- **卡在誰**:🧑Sean / 🔧我們 —— 修法要先決定公開端點怎麼處理經銷身分。
  轉 `done` 的條件②「第一個真經銷客登入時有人在旁邊看過」仍未滿足。
- 🛑 **[2026-09-10 · 現在不修 —— 同上,Sean 2026-09-09 拍甲]** 側欄件數那格歸【上線後那一包】。
  🔵 而我原本要端給 Sean 的那題(**公開端點怎麼處理經銷身分**)**不用問了** ——
  主視窗逐字:**B2B 子網域會讓那題消失**,整個站都是經銷的,就沒有「公開端點要不要算經銷件數」這回事。
  📌 **那正是他選子網域的理由。**
- 🛑 **同一包還要帶走一格(2026-09-10 燒突變時撞到的)**:
  `scripts/sql-ts-literal-binding.test.ts` 的 P6 **沒有任何一格在守
  `search_catalog_by_vehicle_dealer` 的年份述詞** —— 把經銷那支的 `year_start` 述詞
  換成 `(true)`,整支測試**不紅**(⚪ 反對照:同樣改公開那支 ⇒ 當場紅)。
  🔵 那個缺口**不是收窄造成的**:收窄之前那一格是紅的(4≠2),它本來就沒在守任何東西。
  🎯 判別句:**「公開那支綠」不等於「依車輛搜尋對兩種客人都對」。**

---

# 追加 · 2026-09-10 `⟦ship-CANCELQTYTOSTOREFRONT⟧`

## 12 · 顧客站算不算得出「這一件出完了沒」
- **結論**:列上「**還沒生效**」那句**已經不成立** —— 三個前提今天都滿足。
  而**修法對不對我證不到**,標【吻合而未證實】,不是「已修好」。
- **證據**(2026-09-10):
  ```
  ① RPC 在正式庫  get_member_order_cancelled_quantities
                   prosecdef=t · proconfig={"search_path=\"\""} · proacl={postgres,authenticated}
                   ⚪ 負對照 現造函式名 ⇒ 0            ⇒ 貼板 67 貼了
  ② 碼在 main     33ccb2fb8 · c6fd6de40 · aeda352d9 · 379eefd55
                   git merge-base --is-ancestor <c> origin/main ⇒ 四顆皆【是】
  ③ 算式正確      mappers/order.ts:1494-1502 逐字
                   shippedQty >= Math.max(0, it.quantity - (cancelledByItemId[it.id] ?? 0))
  ```
  · 正式庫 `order_item_quantity_summary` 共 **4 列**,**3 列是「訂 1 / 取消 1 / 已出 0」**
    ⇒ 真相是出完了,而**舊規則**會說沒出完 ⇒ 對一張永遠不會再出貨的單說「其餘商品出貨時會再通知您」。
- 🛑 **而那 3 張單【不是 Sean 的】** ⇒ 他看不到那句話:
  ```
  P7KZ55  g3-sandbox-test@pcmmotorsports.com          訂1 取消1 出0   2026-09-09
  MCGHHM  g3-sandbox-test@pcmmotorsports.com          訂1 取消1 出0   2026-09-09
  W9486K  manual_…@manual.pcmmotorsports.local        訂1 取消1 出0   2026-09-05
  C8MYDB  bsas0830@gmail.com(Sean)                    訂4 取消0 出0   2026-09-02  ← 沒有取消量
  ```
  客人看的網址形狀 = `/account/orders/<display_id>`(實測;吃**單號**不是 uuid,
  給 uuid 會印「查無此訂單」)。
  ⚠️ **而 `g3-sandbox-test@` 是公司網域** ⇒ 他若拿得到那個帳號就看得到 —— **那是他知道我不知道的事。**
- **卡在誰**:⏳ 一次真實事件 / 一台完整的鑽機 / 或 Sean 拿得到那個 sandbox 帳號。**不修**。

### 🛑 走查:走出來的是【舊規則長什麼樣】,不是【新規則對不對】
拋棄式鑽機造了最毒的世界(單三個品項各訂 1:A 取消 1 出 0 · B/C 各出 1 ⇒ 真相全部出完),
客人頁逐字印 **「其餘商品出貨時會再通知您。」**
🛑 **而我沒把它報成 finding** —— 先查了 `get_member_order_cancelled_quantities` **在鑽機上不存在**
⇒ `cancelledByItemId == null` ⇒ 走舊規則 ⇒ 那句謊是**舊行為**。
🔴 接著想套 `20260906990000` 補前置,**它自己的前置閘擋下來**,逐字:
「`order_item_quantity_summary` 的 policy 集合不是預期的那一條(總數 0)…
**不要在一個我沒讀過的世界上疊東西**」⇒ **閘是對的**,鑽機缺 `20260904270000`。

### 🔬 造這種 fixture 撞到的 9 道約束(給下一個人,省一整輪)
```
1  shipments.hct_status      白名單 draft/submitted/failed/unknown('none' 不行)
2  shipments_hct_submitted_evidence   submitted 要有證據 ⇒ 測試用 draft
3  recipient_snapshot        必須恰好 {name, phone, line} 三鍵且值全為字串
4  shipment_reference        ^[23456789BCDFGHJKMNPQRSTVWXYZ]{6}$
   🔵 **防混淆字母表 —— A E I L O U 與 0 1 都不在裡面。** `ZZTEST` 因為那個 `E` 被擋。
5  shipments_shipped_needs_tracking   有 shipped_at 就要有 tracking_number
6  pcm_b2_shipment_items_parent_guard 已寄出不可再加品項
   ⇒ 順序必須是【建包裹(不帶 shipped_at)→ 加品項 → 才 UPDATE shipped_at】
7  order_item_quantity_summary 是【觸發器維護的】,不要手寫 ——
   來源三張表:order_item_procurement_receipts(到貨)· order_cancellation_items(取消)
   · shipment_items(已出);寫錯會撞 oiqs_shipped_le_instock
8  order_cancellations.idempotency_key 是 uuid 不是 text
9  order_cancellations.payload_hash    64 位;reason_code 白名單;
   reason_code='other' 才准填 reason_detail,其餘必須 NULL;actor 要 FK 到 staff
```
📌 **同一族的紀錄**:`⟦db-DEALERCATALOGRPC93⟧` 記過「建 fixture 撞到六道約束」—— 今天是 9 道。
　🔵 那本身是「這棵拋棄式庫像不像正式庫」的正向讀數,而它證不到【缺的那些 migration 帶走了什麼】。

### 🎯 今晚第三次同型的坑,三次都在報出去之前攔下來
```
① 經銷目錄「載入失敗」 ⇒ products_list_dealer 在鑽機不存在
② 商品頁顯示一般價     ⇒ price_by_tier 形狀與正式庫不同({"store":3000} vs {"amount":…})
③ 那句謊              ⇒ get_member_order_cancelled_quantities 在鑽機不存在
```
📌 **共同形狀:症狀是真的,而受詞是「我的環境」不是「產品」。**
🎯 判別句:**畫面印出你預期的那個錯之前,先問「這台機器有沒有那個修法」。**

---

# 追加 · 2026-09-10 `⟦f3-PAIDAMOUNTDRIFT1⟧`

## 13 · 表頭金額與品項是兩次查詢,沒人驗它們相等
- **結論**:🟢 **這一列自己寫的關閉條件【滿足了】** —— 而它逐字寫著開列的人「**我沒有跑它 ——
  需要正式庫唯讀連線**」。我有那個連線,跑了。
- **① 那兩次查詢今天還是兩次**(列上的描述**仍然正確**):
  `packages/adapters/src/email/SupabasePaidEmailContextAdapter.ts`
  `:97 .from('orders')` 取 `subtotal / shipping_fee / discount_total / total / tax_total`
  `:148 .from('order_items')` 取 `line_total` ⇒ **中間沒有任何相等性檢查。**
- **② 關閉條件那一發**(2026-09-10 唯讀正式庫):
  ```
  訂單總數 6 · subtotal ≠ Σ line_total 的張數 = 0
  🟢 正對照(同一把尺對故意算錯的假單):
     ZZ-正對照-對的 1000 / 1000 ⇒ 尺說沒問題
     ZZ-正對照-錯的 1000 /  999 ⇒ 尺抓到了      ⇒ 那個 0 不是尺沒動
  ```
  ⚠️ **而分母只有 6 張單** ⇒ 📌 **那個 0 的強度有限**;列上原本的限定(讀數過期沒訊號)照舊。
- **③ 造得出來嗎 —— 我去找第二個寫入者,而它【維護】那個不變式而不是打破它**:
  `admin_update_order_item_amount`(後台改品項金額,正式庫現行定義)
  · `4i` 逐字 `SELECT COALESCE(SUM(i.line_total),0) INTO v_subtotal … UPDATE public.orders SET subtotal = v_subtotal`
    ⇒ **同一交易重算表頭**。
  · 🛑 **而我一度以為抓到一個缺陷,結果是我錯了**:它算 `v_total := v_subtotal + shipping_fee - discount_total`
    **沒帶稅**,而正式庫的 CHECK **有帶稅** ⇒ 我以為有稅的單會撞 CHECK。
    ✅ **實查有擋門**:`:128` 逐字 `IF v_ord.price_tax_mode = 'exclusive' OR COALESCE(v_ord.tax_total,0) <> 0 THEN`
    ⇒ 對有稅的單**直接 RAISE**,而 `:121` 的註解逐字寫著理由就是「**撞 `orders_total_balances`**」。
    📌 **⇒ 那不是缺陷,是被論證過並擋住的。而我在報出去之前查了。**
  ⚠️ **射程**:我查的是「SQL 層 `UPDATE orders` 的那批檔」+ 這一支,**沒有窮舉所有寫入路徑**。
- **④ 列上自己標的那個射程,補上了**:它逐字「**沒有問正式庫那兩道 CHECK 今天在不在**」⇒ 今天問了:
  ```
  order_items_line_balances  CHECK (line_total = unit_price * quantity)                    ✅ 在
  orders_total_balances      CHECK (total = subtotal + shipping_fee - discount_total + tax_total)  ✅ 在
  ```
  🔴 **而它與列上引的版控字面【不同】** —— 列上引的是 `total = subtotal + shipping_fee - discount_total`
  (**沒有 `+ tax_total`**);正式庫**有**。⇒ 📌 **列上那句「射程:我讀的是版控」是對的,而差異是真的存在。**
  🔴 **同一個過期字面還住在第二個地方**:上面那支 adapter 的 `:21` 註解引的也是**舊的等式**
  ⇒ **一句描述 DB 約束的註解已經過期,而沒有任何東西會叫。**(只回報,本次不動碼。)
- **卡在誰**:🧑Sean / `-ship` —— **關閉條件滿足 ⇒ 可翻不擋**,而翻不翻是上線判斷不是我能拍的。
  🔵 而列上 2026-09-08 那格的提醒照舊成立:**要重開這一列,要推翻的是「RPC 權威已足」那個理由,
  不是重新提出那個風險。** 今天的讀數**支持**那個理由(唯一的第二寫入者自己重算 + 對有稅的單拒絕)。

---

# 追加 · 2026-09-10 `⟦f3-PAIDCANCELRACE1⟧`

## 14 · 取消狀態只讀一次 ⇒ 讀完才被取消的單照樣收付款成功信
- **結論**:🔴 **仍然成立。而它缺的不是「再加一道檢查」,是【Sean 2026-09-02 拍的那個事後偵測】——
  八天過去它仍然不存在。**
- **① 那個「只讀一次」今天還是只讀一次**(座標**全部漂了**,用真實行號重量):
  ```
  逐封閘      sweep-email-outbox.ts:1523  listIneligibleAmong([job.orderId])
  adapter     SupabasePaidEmailContextAdapter.ts:101  .select(… cancelled_at)
  ⛔ 列上記 load(:710) → send(:916)
  ✅ 今天     load(:1583) → send(:2192)   ← 檔案長了, 中間 609 行
  ```
- **② 而「paid 那條路零 `await`」今天【仍然成立】** —— 🛑 **而我第一把尺是壞的**:
  我先數了 1583–2192 整段的 `await` ⇒ 20+ 個 ⇒ 差一點寫成「窗口變大了」。
  📌 **那些 `await` 多半在【別種信】(出貨 / 銀行單 / 追蹤號)與錯誤路徑上** ⇒ **分母比受詞寬。**
  ⚠️ 更糟的是我那一發用 `grep -n` 數的是**過濾後**的行號 ⇒ **對不回真實行號**。
  ✅ 用真實行號重量 paid 分支(`1578–1669`)⇒ 裡面的 `await` 只有 **4 個,全在失敗或 `cancelled` 路徑**
  (`:1585` `:1642` `:1649` 錯誤路 · `:1631` `markSkippedOrderCancelled`)
  ⇒ 🎯 **成功那條路仍然零 `await`** ⇒ 窗口仍是那幾行同步碼 ⇒ **TOCTOU 關不掉,結論不變。**
- **③ 🔴 Sean 拍的那個【乙 事後偵測】今天仍然不存在**:
  ```
  packages/use-cases/src/check-anomaly-alerts.ts
    sent_at / sentAt        ⇒ 0 命中
    🟢 正對照 cancelledMixedRail ⇒ 12   ⇒ 尺會動
    ⚪ 負對照 現造字串          ⇒ 0
  ```
  📌 **他 2026-09-02 逐字選了「看得見那一邊」,而八天後那個東西還沒有人接。**
- **④ 🛑 而關閉條件那個讀數【沒有材料】—— 我獨立重現了 `-mail` 記的那一格**:
  ```
  訂單 6 · 已取消 3 · 信件 6 · 已寄出 6
  已取消的單身上有信            ⇒ 0     ← 分母是 0
  先寄付款信才被取消            ⇒ 0     ← 所以這個 0 不是答案
  🟢 正對照 信寄出晚於建單       ⇒ 6     ⇒ join 是活的
  ⚪ 負對照 晚於建單 +100 年     ⇒ 0
  ```
  🎯 **判別句:一個 0 要先問它的分母。** 已取消的單身上一封信都沒有 ⇒
  「先寄了才被取消」當然是 0,而那**證不到它沒發生過**。
- **卡在誰**:🔧我們(接那段偵測 SQL 進 `check-anomaly-alerts.ts`)。
  🛑 碰寄信 ⇒ 要動碼**先寫 plan 等 Sean 批**;而列上逐字「動告警模組屬**標準片**,要走完整 SOP,不是順手」。
