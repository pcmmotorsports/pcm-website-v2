# 窗 B 2026-09-09 查證結果(十四列)

> **為什麼有這一支**:這十四列裡有十一列的結論是「已不成立 / 已裁不做 / 撞車 / 已完成待貼」——
> 那種結論**沒有 commit 可以掛**,只活在對話裡。對話被壓縮之後,下一個人(包括未來的我)會重做一遍。
> 🛑 **本檔只寫結論與證據,不寫教訓、不給建議。** 板子凍結,一個字都沒有動。

---

## 一、上半場六列(結論在對話裡,證據出處在此)

⚠️ **標【今天實量】的是 2026-09-09 我自己跑的;標【轉述】的是我先前那幾片的結論,本檔沒有重跑。**

### 1. `⟦b4-BANKCHARGESCARD⟧` —— **已不成立**
· **結論**:不成立。
· **證據**:【轉述】唯讀正式庫實查 —— `orders` 全表只有 4 列且全是測試單,
  其中 3 張 `bank_transfer` 的 `payment_method` 是 NULL,沒有「匯款單被當刷卡扣」的那個形狀。
· **卡在**:沒有卡,可關。

### 2. `⟦mail-PAYMENTNOCAP⟧` —— **成立,而修法已被 Sean 拍板排除**
· **結論**:洞是真的(`admin_record_manual_payment` 只擋 `<= 0`,沒有上界),**而不補**。
· **證據**:【今天實量】`docs/phase-1-backlog.md:30767` 逐字
  「🔴 而它不是『再加一道上限』:**Sean 2026-08-23 拍板不在收款端加上界(溢收是合法生意)**
  ⇒ 金額那條路沒有正確的上界可用 ⇒ 這一格只能靠**歸屬**解,不能靠**限額**解。」
· **卡在**:不卡。它是「成立但拍板排除」,不是待辦。

### 3. `⟦b4-MANREFUNDNOAUDIT⟧` —— **撞車,別的窗已有 plan**
· **結論**:洞是真的(`admin_record_manual_payment` 寫稽核、`admin_record_manual_refund` 不寫),
  而**已經有別的窗在做**,我停手沒動。
· **證據**:【轉述】防撞車兩把尺 —— `grep -rl <錨> docs/plans/` 命中、
  `git log --oneline --all --grep=<錨>` 命中。
· **卡在**:🔧那個窗 + Sean 批 plan。

### 4. `⟦0a-CARDCANCELNOREFUND⟧` —— **兩處裁不做,而現況比 Sean 要的更嚴**
· **結論**:不成立。系統**根本不讓**「錢沒退完就取消」,比 Sean 當初擔心的更嚴格。
· **證據**:【轉述】`cancel-review-section.tsx` 的 `payment_card_rail` 那一段是擋在前面的;
  我先前寫錯的那句「沒退完根本取消不了」已在 `packages/use-cases/src/check-anomaly-alerts.ts`
  ~:803 的註解裡當場訂正(commit `91d779b71`,**未推**)。
· **卡在**:不卡。

### 5. `⟦b4-PARTPAIDCOUPONNEVER⟧` —— **已完成,等 Sean 貼**
· **結論**:碼做完了,卡在一支還沒貼進正式庫的 SQL。
· **證據**:【轉述】。
· **卡在**:🧑 Sean 貼板。

### 6. `⟦b4-PAIDTHENOVERPAID⟧` —— **成立,而剩下的兩件都不歸這一片**
· **結論**:成立。**它與 Sean 08-23 那條拍板【不是同一件事】**——
  那條拍的是【擋不擋】(收款端不加上界),這一列問的是【多收了以後有沒有人看得到】。
  兩者不衝突,本列反而**依賴**那條拍板。⇒ 不是「拍板排除」。
· **證據**:【今天實量】列裡點名的 6 顆 commit 對 `origin/dev` 跑 `git merge-base --is-ancestor`
  ⇒ `08ed14148` `e25aa7e3a` `bd977c10b` `e581dbed2` `aaead962f` `ce1451e62` **全部在 dev**;
  ⚪ 負對照我自己沒推的 `904ad37ab` ⇒ **不在 dev**(尺是活的)。
  本樹 `TURBO_FORCE=1 pnpm typecheck` **rc=0 · 9/9**,log 用
  `/usr/bin/grep -cE "error TS|Cannot find module"` ⇒ **0**(🟢 同尺餵人造兩行 ⇒ **2**)。
· **卡在**:① 第二層帶金額的文案要先加一欄 view ⇒ 一支 migration ⇒ 🧑 Sean 貼;
  ② 「多付, 待人工」那一行**沒有人在正式後台看過** ⇒ 🧑 Sean 走一遍。

---

## 二、下半場四列(2026-09-09 今天實量,防撞車兩把尺都跑過:0 plan)

### 7. `⟦b4-PAYCONCUR1⟧` —— **已不成立(修法已在正式庫)**
· **結論**:不成立。那個「兩筆收款同時進來算錯」的競態,**第三條路已經上線了**。
· **證據**:唯讀正式庫 —— `pcm_noncard_settle_recompute(uuid)` **存在**,
  函式體含 `pg_advisory_xact_lock` = **t**;`order_payments` 上掛著
  `pcm_noncard_settle_after_payment_ai`(AFTER,啟用),而那支 wrapper 真的叫 recompute = **t**。
  🔬 `position('FOR UPDATE' ...)` 也是 t,**而逐行印出來看,7 個命中【全部在註解裡】**
  (`:19` `:24` `:25` `:174` `:175` `:176` 開頭都是 `--`,真正的碼是 `:27` 的 `PERFORM pg_advisory_xact_lock`)
  ⇒ 板列那句「不要把 `FOR UPDATE` 加回去」**沒有被違反**。
  ⚪ 負對照:對不存在的表問觸發器 ⇒ **0**;🟢 正對照:`orders` 上 **5** 支。
· **卡在**:不卡。板列首段那句「前置 `⟦b4-NONCARDPAID1⟧` 還沒上」今天已經作廢兩次
  (`tidy` 2026-09-08 判過一次,今天正式庫的讀數是第二次)。

### 8. `⟦f3-PAIDAMOUNTDRIFT1⟧` —— **已不成立(它自己寫的關閉條件今天達成)**
· **結論**:不成立。表頭金額與品項**沒有漂移**。
· **證據**:本列自訂的關閉條件逐字「有沒有任何一張單,其 `subtotal` ≠ 該單品項 `line_total` 合計」
  ⇒ 唯讀正式庫 **0 張**。🟢 正對照(同一把尺左邊 +1)⇒ **4**(尺會動);分母:有品項的訂單 **4** 張。
  🔬 順帶補上該列 `:25` 自陳「沒問過正式庫」那一格:兩道 CHECK **今天都在** ——
  `orders_total_balances` 與 `order_items_line_balances` 定義都撈得出來;⚪ 負對照假約束名 ⇒ **0**。
  🔬 而「RPC 權威已足」那個前提**比 migration 寫的更強**:`has_table_privilege` 實量
  `authenticated` 與 `anon` 對 `order_items` 的 **INSERT / UPDATE 全部 f**,`order_items` **有開 RLS**;
  🟢 正對照 `service_role` 對 `orders` SELECT ⇒ **t**。
· **卡在**:不卡。
· ⚠️ **射程**:分母只有 4 張測試單,這個 0 的效期只到有真單為止。

### 9. `⟦b4-CUTOFFWRONGCOLUMN⟧` —— **乙已落地在 dev;甲仍等 Sean;而本列的未判理由有一句是錯的**
· **結論**:
  · **乙**(加守門盯那個假設)—— **做完了而且在 dev**:`569a02784` ⇒
    `git merge-base --is-ancestor` 對 `origin/dev` = **在**。碼在
    `packages/use-cases/src/deploy-cutoff.ts` + `apps/storefront/src/app/api/cron/email-sweep/route.ts:543-560`
    (`unpaidCancelCutoffIsFresh`,cutoff 值落在 48 小時內就印一行帶錨的警告)。
  · **甲**(把契約與碼對齊)—— **仍等 Sean**,態不動。
· 🔴 **而本列的未判理由「它的無害性靠【兩個 cutoff 值】今天剛好相等,而沒有守門盯那個相等」
  —— 那句話的前半是錯的:根本只有一個值。**
  🔬 證據:`packages/adapters/src/email/SupabaseUnpaidCancelledOrderScannerAdapter.ts:179-180` 逐字
  `.gte('cancelled_at', input.cutoff)` 與 `.gte('created_at', input.cutoff)` ——
  **同一個 `input.cutoff` 參數**,不是兩顆 env。⇒ 兩者**恆等,不可能分歧**。
  ⇒ 所以那一列 `:8` 開的那道檢查(「比 `BANK_ORDER_CREATED_EMAIL_CUTOFF` 與 `CANCELLED_EMAIL_CUTOFF`
  兩個值」)**問錯了對象** —— 這支 scanner 用的是 `B4_DEPLOY_CUTOFF`(route `:531` 逐字「與 B-5 共用同一顆 cutoff」)。
  🎯 **真正的殘餘不是「兩個值會不會分歧」,是 `created_at >= cutoff` 結構上就漏掉
  「cutoff 之前建立、之後才被取消」的單** —— 那一格不受相等與否影響,而它就是要 Sean 拍的甲。
  🔬 而 `569a02784` 那道守門自己也把射程寫出來了(`route.ts:547-548` 逐字):
  「它問的是 cutoff 這個【值】在不在 48h 內,不是 cutoff 有沒有被換掉
  ⇒ **回填式換值(補設一顆過去的 cutoff)它一次都不會叫**」。
· **卡在**:🧑 Sean(甲)。乙不卡。

### 10. `⟦f3-PAIDCANCELRACE1⟧` —— **成立,Sean 早就拍了乙,而那個東西今天還是不存在**
· **結論**:成立。Sean 2026-09-02 拍**乙 = 事後偵測**,七天過去**零落地**。
· **證據**:`grep -c "sentAt\|sent_at" packages/use-cases/src/check-anomaly-alerts.ts` ⇒ **0**;
  🟢 正對照同檔既有訊號名 `cancelledMixedRail` ⇒ **12**(尺會動);⚪ 負對照現造名 ⇒ **0**;
  全樹 `grep -rln PAIDCANCELRACE apps packages` ⇒ **0 支檔**。
  唯讀正式庫:「取消後才寄出的信」⇒ **0 列**,🛑 **而那個 0 沒有材料** ——
  已取消的單身上總共 **0** 封信(分母是 0)。
  🟢 換到有材料的維度證明尺會動:`sent_at > created_at` ⇒ **6**;⚪ 負對照 `+100 年` ⇒ **0**。
· **卡在**:🧑 Sean 貼一支 migration。
  ⇒ plan 已寫:`docs/plans/2026-09-09-paidcancelrace-postsend-detection-plan.md`
  (codex R1 五條 must-fix 已折;其中第 ④ 條我開檔核過**方向相反,沒照做**,理由寫在 plan §3②)。

---

## 三、手動建單那四列(2026-09-09 今天實量;兩把尺都跑過)

> ⚠️ **這四列的共同結論:它們大部分【已經被做掉了】,而板上還寫 open / doing。**
> 🔴 而其中一件是**今天才上線的**,所以第 14 列的計時器**從今天開始跑**。

### 11. `⟦b4-MANUALORDERDEADEND⟧` —— **五格裡四格已修且在 dev,剩兩格要 Sean 或真環境**
· **結論**:板上的「畫面從來沒說出來」**今天不成立**。
· **證據**(全部對 `origin/dev` 的那一份查,不是我的工作樹):
  · 開場白不再承諾「可以出貨」⇒ `8365fcb82` **在 dev**;`grep -c "可以出貨、開發票。<"` on dev = **0**
    (🟢 正對照:該檔在 dev 有 **138** 行 ⇒ 檔案撈得到,那個 0 不是撈空)。
  · 那句指向不存在動作的死路訊息(「請重新從商品清單挑一次」)⇒ dev 上 = **0**;
    新訊息 `這一格留白就好` = **1**。
  · 目錄查詢結果**點得動了** —— dev 上 `emitManualOrderLineSeed` = **2**(原本純 `<li>` 不能挑)。
  · 乙1(擋下訊息只說不行不說怎麼才行)⇒ `62775f03` **在 dev**。
· **卡在**:
  · **乙2/乙3(建單體積:9 步 · 8 必填 · 3 個要用腦記的數)** ⇒ 🧑 Sean 拍「縮或不縮」。
  · **「未選具名身分 ⇒ 寫入靜默失效」** ⇒ 🔧 要走得到那條路的環境(鑽機的票是手貼的,走不到)。
· 📌 而 **乙4 已經不是這一列的事**:Sean 2026-09-05 拍「甲 = 單價一律填未稅」⇒ 併進第 13 列。

### 12. `⟦b4-INVOICE5PCT⟧` —— **已落地在正式庫,而板上那題 Sean 也已經答了**
· **結論**:做完了。板列 `doing`、⟨已量⟩ 寫「卡在 🧑Sean」—— **那一格今天過期**。
· **證據**:
  · 唯讀正式庫 `admin_create_manual_order` 本體 **md5 = `3804f3463058df0062ac5ca34efcb2c9`**,
    **逐字等於** `20260909030000` 檔頭宣告的「新本體 md5」⇒ 🎯 **第 7 代真的在線上跑**。
  · 逐行印出算稅那一段:`:399` `v_tax := round((v_subtotal + p_shipping_fee - 0) * 0.05)` /
    `:402` `v_tax := 0` ⇒ 它在 `IF v_invoice_requested THEN` **裡面** ⇒ 沒勾真的不加。
    (第 6 代那一行是**無條件**的 ⇒ 沒勾也加,而那是線上曾經在發生的事。)
  · ⚪ 負對照假字面 = f · 🟢 正對照 `BEGIN` = t。
  · **板列 ⑪ 那題(勾選預設打勾?)Sean 已拍** ——
    `manual-order-form-body.tsx:232-234` 逐字「⛔ ~~`defaultChecked` 這件事沒有被 Sean 拍過~~
    ⇒ **他拍了**…「預設不勾選,也就是預設不開發票」⇒ ✅ 所以 `defaultChecked` 拿掉」。
· **卡在**:只剩板列 ⑩ 那個**已知未關而沒有人排**的缺口 ——
  `20260909030000:339-340` 逐字 `缺鍵 ⇒ COALESCE(..., true)` ⇒
  **「舊呼叫端沒送」與「員工明確勾了要開」在資料上是同一個 `true`**,分不出來。
  要關掉需要**之後另一支 migration** 把缺鍵改回 RAISE。🛑 **那支今天仍然沒有人排。**

### 13. `⟦b4-PRICECOPYTAX⟧` —— **三個落點【全部落地且同一次上線】,而它要 Sean 複核的那題今天無效**
· **結論**:做完了,而且是照它自己寫的「三個落點必須同一次上線」做的。
· **證據**(三個落點逐一):
  · ① 建單 RPC 真的算稅 ⇒ **在正式庫**(見第 12 列的 md5)。
  · ② `manual-order-lines.tsx` 文案 ⇒ dev 上 `請填<strong>未稅</strong>金額` = **1**;
    ⚪ 負對照舊文案 `請填<strong>含稅</strong>金額` = **0**。新句逐字:
    「單價這一格請填**未稅**金額 —— 經銷價單上多少就填多少。稅由系統自己算,不要自己加上去;
    填成含稅會多課一次稅。」
  · ③ 比價偵測器方向翻過來 ⇒ dev 上 `Math.round(c.authority * 1.05) === c.typed` = **2**
    (`54924382d` 在 dev)。而目錄查詢那一行**兩個價都標稅制**(`售價 X(含稅)` / `經銷 Y(未稅)`),
    檔內逐字「兩邊都標 —— 只標一邊,讀的人會以為另一邊沒標所以沒問題」。
· 🔴 **而板列標的【待複核】(「折前小計」還是「折後小計」)今天在這條路上沒有差別**:
  `20260909030000:483` 逐字「稅基 = 小計 + 運費 − 折扣。而 `discount_total` 在本函式**寫死成 0**」,
  算式落在 `:514` 是 `(v_subtotal + p_shipping_fee - 0)`。
  ⇒ 📌 **手動建單今天沒有折扣可以「之前」或「之後」** ⇒ 那題要 Sean 答的**答案不會改變任何數字**
  (⚠️ 而**哪天手動單有了折扣,它就會有差別** ⇒ 這句不是「這題作廢」,是「今天不擋」)。
· **卡在**:不卡。

### 14. `⟦b4-PARTCANCELTAX⟧` —— **結構成立,而今天曝險是 0;而計時器【今天才開始跑】**
· **結論**:成立,但今天一張都碰不到。**兩道都還沒接上**。
· **證據**(唯讀正式庫;這一格正是該列自陳「我沒有碰正式庫」的那一格):
  · **`exclusive`(未稅)的單今天 0 張** —— `orders` 全部 4 張都是 `inclusive`,`tax_total` 合計 **0**。
  · 那支 view 存在,而它的定義 **`position('tax') > 0` = f**(定義裡真的一個 tax 都沒有)、
    `unit_price` = **t** ⇒ **該列「它一個字都沒提到稅」那句今天成立**;⚪ 負對照 = f。
  · **OP7 那支會寫出待退款列的函式今天【不在正式庫】** ——
    `proname LIKE '%partial_cancel%'` ⇒ **0 筆**(🟢 正對照 `admin_create_manual_order` = 1 ·
    ⚪ 負對照假函式名 = 0)⇒ **多退那條路今天走不到底。**
· 🔴🔴 **而這一列的時鐘今天才上發條**:第 12 列那支第 7 代 RPC **今天貼上線**,
  而它勾了開發票就寫 `price_tax_mode = 'exclusive'` ⇒ 📌 **第一張 `exclusive` 的單隨時會出現**,
  而在那之前這一列的 0 是「還沒開始」不是「不會發生」。
· **卡在**:🧑 Sean(建議處置未拍:算式不可信就不寫列、交給對帳報出來,不要自己把稅加回去算)
  + 🔧 OP7 那支函式還沒進正式庫。

---

## 四、這十四列我證不到什麼

1. 所有正式庫讀數的分母都是 **4 張測試單 / 6 封信** ⇒ 每一個 0 的效期只到有真資料為止。
2. 標【轉述】那五格(1 / 3 / 4 / 5,與 6 的部分)**本檔沒有重跑**,它們是我先前那幾片的結論。
3. 第 9 列的「乙已落地」只證到**碼在 dev**,**沒有證到它在正式站真的叫過**。
4. 第 10 列的 plan **一行碼都還沒寫**,它是 plan 不是實作。
5. 第 11-14 列**我沒有開瀏覽器走過** —— 我查的是 `origin/dev` 上那幾支檔的字面與正式庫的函式本體。
   「文案在 dev 上是新的」與「Sean 打開後台看到的是新的」是兩個宣稱,我只證得到前者。
6. 第 12 列的 md5 相等只證「本體逐字相同」,**不證那支函式被呼叫時的行為** —— 我沒有真的建一張單。
7. 第 14 列的「0 張 exclusive」是 2026-09-09 的讀數,而第 7 代**今天才上線** ⇒ 這個 0 最短命。

---

## 五、手動建單【在正式後台】真的走了一遍(2026-09-09 · 窗 B · Sean 本人登入)

> 🔴 **這一段與上面所有段落不同層級** —— 上面是讀碼與讀正式庫的函式本體,
> 這一段是**真的用瀏覽器打開 `admin.pcmmotorsports.com` 看到的畫面**。
> 🛑 **零寫入**:「建立訂單」那顆從頭到尾是 disabled(沒挑客人),一筆資料都沒進正式庫。
> 🔵 登入是 Sean 本人做的(具名身分 `Sean(老闆)`,不是共用密碼)。
> ⚠️ **本來要在鑽機走** —— Sean 當場改成正式後台並親自登入,所以這一段的證據比鑽機強一級。

### 5-1 那四格的答案

**① 開場白(逐字抄畫面,不是抄碼)** ✅ **已經是新的**
> 客人用電話或 LINE 下的單,在這裡幫他建。建好之後就跟網站上的單一樣,可以開發票。
> 要出貨得先在訂單頁按「貨到了」登記到貨 —— 貨還沒到,出貨那邊會顯示「可出 0」,那不是壞掉。

⇒ 📌 **`⟦b4-MANUALORDERDEADEND⟧` 的主症狀在正式站上已經不成立** ——
它不但拿掉了「可以出貨」那句假話,還把**前置條件**與**會看到什麼**都說了。

**③ 單價那一格的說明文字(逐字)** ✅ **已經是「未稅」**
> **單價這一格**請填**未稅**金額 —— 經銷價單上多少就填多少。
> 稅由系統自己算,不要自己加上去;填成含稅會多課一次稅。

⇒ 📌 `⟦b4-PRICECOPYTAX⟧` 的②在正式站上落地了。

**④ 目錄查詢的結果點得動嗎** ✅ **點得動,而且行為是對的**
· 查 `AP007` ⇒ 回 5 列,每一列是 `button` 不是純 `<li>`。
· 點第一列 ⇒ **長出第 2 列**,`料號 AP007F` · `品名 PMMA旅行版風鏡` · `數量 1` ·
  `商品編號 43d701ac-2be7-427e-bb5a-9793007467c6` 都自動填好,並印一行
  `已加成第 2 列:AP007F`。
· 🔴 **而【單價那一格留空】,那是對的** —— 這一列沒有經銷價,而畫面唯一有的是含稅售價;
  把含稅價種進一個要未稅的格子就是**多課一次稅**。⇒ 📌 **它選擇不填,不是漏填。**

**② 勾與不勾的稅額** ❌ **這一格答不出來,而原因是畫面本身**
🛑 **建單頁從頭到尾沒有任何地方顯示小計 / 稅額 / 總額。**
⇒ 要看到那 5%,**只能真的把單建出來** ⇒ 那是正式庫寫入 ⇒ 已停下問 Sean,未擅自建。
⇒ 📌 所以到此刻為止,那 5% 的**行為**只證到「線上函式本體逐位元組等於第 7 代」
(見第 12 列的 md5),**沒有任何人用眼睛看過那個數字**。

### 5-2 走的時候撞到兩件(只記錄,本片不修)

**🔴 甲:經銷價全是 `—`,而單價那一格要未稅 ⇒ 員工沒有東西可抄**
· 查詢結果每一列逐字:`AP007F · PMMA旅行版風鏡 · 售價 5,500(含稅) / 經銷 —(未稅)`,
  五列**全部**是 `經銷 —`。
· ⇒ 畫面唯一有數字的是**含稅 5,500**,而輸入格要**未稅** ⇒ 員工要自己算 `5500 ÷ 1.05 = 5238.09…`,
  **除不盡**。
· 🔵 **系統這一端沒有做錯**:它印了「沒有經銷價,單價要自己填」,而且**刻意不把 5,500 種進去**。
· 📌 **這是兩列在畫面上相遇**:`⟦account-DEALERPRICEEMPTY⟧`(經銷價全庫是空的,`:452`,等 Sean Q16
  店家價來源)撞上 `⟦b4-PRICECOPYTAX⟧`(輸入格改未稅)⇒ **各自都對,合起來讓員工卡住。**
· ⚠️ **射程**:我只查了 `AP007` 這一組 5 列,**沒有量全庫有幾成商品沒有經銷價**。

**🟡 乙:按下去之前看不到這張單多少錢**
· 整張表單沒有「小計 / 稅 / 總計」預覽 ⇒ 員工按「建立訂單」之前不知道結果。
· ⇒ 而那也正是 ② 那一格答不出來的原因 —— **同一件事的兩面**。

### 5-3 這一遍證不到什麼

1. **沒有建單** ⇒ 稅額、`price_tax_mode` 寫成什麼、發票欄位存成什麼,**全部沒看到**。
2. **只走了建單頁** ⇒ 收款 / 訂貨 / 到貨 / 出貨那幾步這一遍沒走(出貨那條先前走過)。
3. **只查了一組料號**(`AP007`)⇒ 「經銷價全是 —」的射程只到這 5 列。
4. **沒有測「未選具名身分 ⇒ 寫入靜默失效」**(`⟦b4-MANUALORDERDEADEND⟧` 那一格)——
   我是用 Sean 的具名身分登入的,**那條路走不到**;而要走到它得換身分,那會動到稽核紀錄。

---

## 六、那 5% 親眼看到了 —— 正式庫兩張測試單(2026-09-09 · Sean 拍甲授權)

> 🔴 **授權範圍**:Sean 逐字「甲,你建立」。主視窗同步劃了界線:**只建兩張、不出貨、不收款、
> 不寄信、不碰既有的單、建完立刻取消**。以下全部照做。
> 🛑 **代用**:客人清單裡**沒有**叫「Sean 測試帳號」的(那是**員工具名身分**不是客人)⇒
> 改用 `G3 沙盒測試`(`0912345678`),它是測試帳號。

### 6-1 ② 那一格的答案 —— 數字全對

| | 勾開發票 `MCGHHM` | 不勾 `P7KZ55` |
|---|---|---|
| 畫面小計 | **NT$ 1,000**,標籤逐字「小計(**未稅**)」 | **NT$ 1,000**,標籤逐字「小計」 |
| 運費 | NT$ 0 | NT$ 0 |
| **稅額** | **NT$ 50** | **那一列根本不印** |
| 總計 | **NT$ 1,050** | **NT$ 1,000** |

🎯 **兩種稅制的畫面【長得不一樣】,而那是對的** —— 沒開發票的單不但稅是 0,連「稅額」那一列
都不出現,小計也不標「未稅」。📌 **`⟦b4-TAXADMINSURFACES⟧` 記的「稅 0 不印」那個判準,
在真後台上驗到了。**

🔬 **唯讀正式庫複驗**(不是只信畫面):
```
display_id | price_tax_mode | subtotal | tax_total | total | invoice_requested
MCGHHM     | exclusive      |     1000 |        50 |  1050 | t
P7KZ55     | inclusive      |     1000 |         0 |  1000 | f
```
⚪ 負對照:問一張不存在的 `ZZQNOPE` ⇒ **0 列**。

🔴🔴 **全庫第一張 `exclusive` 單誕生了**(分布:`exclusive` **1** / `inclusive` **5**)
⇒ 📌 **`⟦b4-PARTCANCELTAX⟧` 的計時器,在按下那一顆的瞬間真的開始跑。**

### 6-2 稽核軌跡(`admin_audit_log`)—— 記得住是誰、記得住稅制

四筆,`actor` 全部是 **`sean`**,`source_app = admin`:
· `order.manual_create` × 2 —— `after` 裡逐字含 `price_tax_mode` 與
  `line_tax_bases:[{tax_basis:"untaxed", unit_price:1000, ...}]`
  ⇒ 🎯 **「員工填的是未稅」這件事【存下來了】**,不是只活在畫面文案裡。
· `order.cancel` × 2 —— `reason` 逐字 **`internal_error`**(= 我選的「內部作業疏失」)。

🔵 **而畫面上給客人看的 `cancelled_reason` 是「訂單已取消,詳情請洽客服」** ——
與稽核裡的 `internal_error` **是兩個欄位**。📌 **那是對的**:內部原因不該進客人的視野
(而這正是同日 `9aee2e6be`「內部註解不再進客人的信」修的那一族)。

### 6-3 收尾確認(唯讀)

· 兩張 `cancelled_at` 都有值、`payment_status` 都是 `unpaid`。
· **這兩張身上的信 = 0 封**(沒寄任何東西)。
· **收款列數 = 0**(沒收任何錢)。

### 6-4 「多收」那一格 —— **畫面上沒有這一格**

主視窗要我特別看未稅單取消時「多收」算出來的數字。
🛑 **走不到** —— 這兩張 `已收 = 0`,而「多收」只在 `已收 > 應收` 時才有意義。
畫面顯示的是「應收 1,050 元 / 已收 0 元 / **還差 1,050 元**」⇒ **方向相反,不是多收。**
⇒ 📌 要造出 `⟦b4-PARTCANCELTAX⟧` 的樣本得**先收款**,而收款不在這次授權裡。**沒做。**

### 6-5 🔴 走的時候撞到三件(只記錄,一個字都沒改)

**🔴🔴 甲:一張填得完整的單被拒絕,而它不說是哪一格,還把整張表單清空**

第一次送出 ⇒ 網址回 `?r=manual_order_invalid`,畫面逐字:
> 表單有欄位沒填好,這張單沒有建出來。請重新填一次,每一格都要填。

🛑 **而整張表單被清空了** —— 客人選取、品項、發票勾選、收件資料**全部歸零**,
員工要**從頭重打一次**(而板列 `⟦b4-MANUALORDERDEADEND⟧` 量到的體積是「9 步 · 8 必填 ·
3 個要用腦記住的數」⇒ **重打一次就是再走一遍那 9 步**)。
📌 **這正是 `⟦b4-DEADENDMSG1⟧` 的判別句要抓的形狀:一個擋住人的訊息,沒有給他一件他做得到的事。**
「每一格都要填」不是一件做得到的事 —— 他不知道**哪一格**沒填。

⚠️ **而我【不能斷定】是哪一格造成的** —— 第二次我同時改了**兩件事**:
① 補上「第 1 列料號」(第一次留白)② 收件地址從含全形括號逗號的長字串換成純文字。
⇒ 🛑 **兩個變因一起動,分不出是誰** —— 我沒有再拆開試(那要第三張單,不在授權裡)。
🔵 **而值得下一個人先查的是①**:面板提示逐字只說「網站上沒有的東西(代購)就把**商品編號**留白」,
**沒有說料號可不可以留白** ⇒ 若料號是必填,那句提示等於**漏講了一格必填欄**。

**🔴 乙:「找客人」只吃電話,而標籤說它吃姓名和 Email**

輸入格標籤逐字「找客人(**電話 / 姓名 / Email**)」,placeholder 也是。
而我輸入「測試」按下去 ⇒ 回一句 **「電話至少要打 3 個數字才找得動。」**
🛑 ⇒ **它只吃電話。**
📌 **後果具體**:客戶清單裡的 `UX 測試員`(`uitest@pcmmotorsports.com`)**電話欄是 `—`**
⇒ **在建單頁完全找不到這個人**,而畫面說得出他的 Email 應該找得到。
🔬 對照:`/customers` 那一頁的搜尋格 placeholder 也是「姓名 / Email / 電話」,**而那一頁是真的三種都吃**
(我用它找到了 `UX 測試員`)⇒ 🎯 **兩頁同一句話,一頁做得到一頁做不到。**

**🟡 丙:兩位客人在挑選清單上長得一模一樣**

查 `0912345678` 回**兩列**,逐字都是 `G3 沙盒測試(0912345678)` —— 同名、同電話、
**沒有任何一格分得出誰是誰**(`/customers` 那一頁看得出來:一位是
`g3-preview-sandbox@`、一位是 `g3-sandbox-test@`)。
⇒ 📌 **建單頁那一列少印 Email** ⇒ 員工只能**猜**,而猜錯了單會掛到別人身上。
🔵 我選了第一位,事後從 `customer_user_id` 反查是 `8d402365-…`(= `g3-sandbox-test@` 那位)。

### 6-6 這一段證不到什麼

1. **兩張都是【匯款 + 未付款】** ⇒ 收款、退款、多收、出貨、寄信,**一格都沒走到**。
2. **「多收」那條路沒有樣本** —— 要有樣本得先收款,不在授權裡。
3. **第一次被拒的真正原因我沒有隔離出來**(兩個變因一起動)。
4. **只試了一種商品(代購,無料號對應)** —— 有型錄料號、有經銷價的那條路沒走。
5. **稅率只驗了 1000 這一個數字** —— 除不盡的數(例如 999)會不會四捨五入到差一元,沒驗。

---

## 七、`⟦b4-PENDINGRECONCARDRF⟧` —— **不成立,而理由是這一列自己寫下來的那一句**

> 兩把尺:`docs/plans/` 命中 1 支(`op7-partial-cancel-refund-fn-plan.md`,是本列自己的落點文件)·
> `git log --grep` 的 `fix|feat` commit **0 顆**(🟢 正對照抓得到 `paidcancelrace` 那支 plan)。
> **修法被裁定排除過嗎**:沒有。列尾自陳「判斷不缺,缺的是工」。

### 7-1 結論

本列的主張是:「任何『這張單還多收著多少』的算式若只扣 `order_manual_refunds`,
就漏掉 `order_refunds`(卡片退款帳本)」⇒ **報出太大的多收 ⇒ 錢往外多走。**

🛑 **那句話當【一般命題】是對的,而【今天正式庫裡沒有一支這樣的算式】。**
🎯 **每一支都把收款限在非卡軌,所以卡片收款根本進不去** ⇒ 卡片退款也就不可能讓它算錯。

### 7-2 證據(唯讀正式庫,逐字)

**① 全庫掃過一遍**(不是挑一支看):找「扣了 `order_manual_refunds` 而完全沒提到 `order_refunds`」的
物件 ⇒ **函式 9 支 + view 1 張**。其中**真的讀 `order_payments`** 的只有 **3 支**:
`pcm_manual_refund_rail_cap` · `pcm_pending_refund_amounts` · `pcm_pending_refund_open_for`。
🟢 正對照:有提到 `order_refunds` 的函式共 **21** 支(尺會動)· ⚪ 負對照現造表名 ⇒ **0**。

**② 那 3 支的收款側逐字,全部限在非卡軌**
· `pcm_pending_refund_amounts` 本體 `:4-5` 逐字
  `SUM(p.amount) FROM public.order_payments p WHERE p.order_id = p_order_id AND p.rail = r.rail`,
  而 `:12` 逐字 `FROM (VALUES ('bank_transfer'), ('cash')) AS r(rail)`。
· `pcm_manual_refund_rail_cap` 本體 `:3-6` 逐字
  `SUM(p.amount) FROM public.order_payments p WHERE p.order_id = p_order_id
   AND p.rail IN ('bank_transfer', 'cash')`。
· `pcm_pending_refund_open_for` **自己不算**,它 `:101` / `:111` 呼叫 `pcm_pending_refund_amounts`
  (它本體裡那句 `order_manual_refunds` 在 `:105`,是**錯誤訊息字串**不是算式)。

**③ 而卡片退款只可能掛在卡單上** —— 本列自己 `:6` 引的 `20260801120000:256-257`:
`order_refunds` 的 INSERT 閘要求那張單有 `tappay_rec_trade_id`,否則 `RAISE`。

⇒ 🎯 **①②③ 合起來**:卡片收款進不了那三支的分子,卡片退款也就減不到任何東西。
📌 **在它們裡面加 `- SUM(order_refunds WHERE status='confirmed')`,就是本列 `:7` 自己警告的那段:**
> 「在【只處理非卡單】的那支函式裡減它, 是一段永遠碰不到的碼 —— 而它讀起來像
> 『我連卡片退款都考慮到了』, **一次都不會執行, 也永遠不會有測試紅**。」

**④ 今天的曝險面**
· 收款軌分布:`card` **1** 筆(NT$4)· `cash` **1** 筆(NT$10,500)。
· **混軌單(同一張單既有卡又有非卡收款)= 0 張** ⇒ 那個「卡的缺口被推給現金軌」的形狀今天不存在。
· `order_pending_refunds` **0 列** ⇒ **今天沒有任何東西在叫。**
· 🟢 正對照:有收款的單 **2** 張(不是對空表提問)· ⚪ 負對照假 status ⇒ **0**。

### 7-3 🔴 我自己在這一列上犯了一次,寫下來

**我第一次量的時候報了一個【假的真樣本】。**
我用「`SUM(order_payments.amount)` − 人工退款」當作「現行算法」,對 `C8MYDB` 算出
**現行 4 / 修正後 0**,並回報「在正式庫上抓到真樣本了」。

🛑 **那個算式是我自己寫的,不是那支函式的。** 函式 `:5` 有 `AND p.rail = r.rail`,而 `C8MYDB`
那筆收款的 `rail` 是 **`card`** ⇒ 照函式真正的算式,兩條軌的 `net` **都是 0** ⇒
**它根本不會吐出那張單。**

📌 **錯法的形狀**:我**重寫了**一份我以為等價的算式去量它,而不是**讀它本人**。
⇒ 兩者差一個 `WHERE`,而那個 `WHERE` 正是整列成不成立的關鍵。
✅ 我在往下寫 plan 之前開檔讀了本體才發現,已當場收回。

### 7-4 🔵 順手量到一格【不是本列受詞】的不對稱(只記錄,不修)

同樣是「這張單的錢」,兩支函式對**退款那一側**的分軌處理**不一樣**:
· `pcm_pending_refund_amounts` `:6-8` 扣人工退款時**有** `AND m.rail = r.rail`
· `pcm_manual_refund_rail_cap` `:8-11` 扣人工退款時**沒有** rail 條件

⇒ 一筆 `rail` 不是 `bank_transfer`/`cash` 的人工退款,會**壓低** `rail_cap` 的上限。
🔵 **方向是保守的**(上限變小 ⇒ 擋住而不是多退)⇒ **不是急件**,而且可能是刻意的。
⚠️ **我沒有查它是不是刻意的**,也沒有量今天有沒有這種人工退款列 ⇒ **標未確認。**

### 7-5 這一段證不到什麼

1. **我沒有呼叫那支函式** —— 唯讀角色對 `pcm_pending_refund_amounts` **沒有 EXECUTE**
   (實測回 `permission denied for function`)⇒ 我證的是**它的本體逐字**,不是它的回傳值。
2. **分母極小**:全庫 `card` 收款只有 1 筆 NT$4、混軌單 0 張 ⇒ 「今天不成立」的效期只到有真資料為止。
3. **我只掃了 DB 物件** —— app 端若另有一份自己算的「多收」,這一掃看不到
   (⚠️ 而 `pending-refund-repository.ts` 自稱是那支 RPC 的「唯一呼叫端(讀)」,我沒有再獨立驗這句)。
4. **7-4 那個不對稱是不是刻意的,我沒查。**

---

## 八、`⟦b4-CARDALREADYREFUNDED⟧` —— **成立,逐字對得上,而今天曝險是 0 張**

> 兩把尺:plans 命中 1 支(`2026-09-08-mixedrail-manual-refund-plan.md`)· 碼 commit 1 顆
> (`bfb92915d`,**在 dev**)—— 🔵 **而兩者都是【姊妹片 `⟦b4-MIXEDRAILMANUALREFUND⟧`】,不是撞車**:
> 本列 `:8` 自己寫著「**本片(MIXEDRAIL)沒有讓它變壞** —— 只是讓死法從【看不到表單】變成
> 【看得到而送不出去】」。
> **修法被裁定或拍板排除過嗎**:🔴 **甲被【資料證偽】過**(不是被拍板排除)—— 見下 8-2。

### 8-1 正式庫逐字複核(本列 2026-09-08 開列時是讀 migration,我讀的是【線上函式本體】)

**① 那道閘的述詞,逐字**(`admin_record_manual_refund` 本體 `:82-85`):
```
SELECT EXISTS (
  SELECT 1 FROM public.order_payments op
   WHERE op.order_id = p_order_id AND op.rail = 'card'
) INTO v_has_card;
```
⇒ 🎯 **它只問「有沒有一筆 card 收款」,完全不問那筆【退了沒】** —— 本列 `:6` 的根因描述**成立**。

**② 而那個形狀是刻意的**(同函式 `:78-81` 逐字):與 `admin_cancel_order` 的述詞
「**刻意讀同一欄** —— 兩道閘看同一件事就不會分岔」,且是「**鎖單之後才讀**」
(在鎖之前讀,並行插入一筆 card 收款會讓「不是刷卡單」在下一瞬間變成假的)。
⇒ 📌 **一個為了防並發而刻意收緊的述詞,在另一個維度上收得太寬** —— 本列 `:7` 成立。

**③ 那句錯的話,逐字**(`:93`):
> 🔴 若卡上其實已經退成功了, 不要在這裡登記 —— 那會變成退兩次。

🛑 **對混合單這句是錯的**:卡那半退成功了,**現金那半仍然要登記**。本列 `:26` 成立。

**④ 而 `order_refunds` 在這支函式裡出現過 3 次,全部不是述詞**
(`:27` `:29` 是**錯誤訊息字串**「card 軌不得由人工登記」· `:201` 是**註解**)⇒
🔵 我一開始的粗尺(`position('order_refunds' in prosrc) > 0` = `t`)差點讓我以為本列過期了 ——
**逐行印出來才看得出那三處都不在算式裡。**

### 8-2 甲那條路為什麼是【資料上做不到】,不是【沒人想做】

`order_manual_refunds_rail_check` 的定義,**正式庫實查逐字**:
```
CHECK ((rail = ANY (ARRAY['bank_transfer'::text, 'cash'::text])))
```
⇒ 📌 **人工退款帳本裝不下「卡片退款」這件事** ⇒ 系統自己不知道卡那筆退了沒
⇒ `v_has_card` 改成「有【未退成功的】card」時,**要查的那個事實不存在於系統裡**。
🟢 正對照:同表的 CHECK 共 **6** 條(尺會動)。

### 8-3 今天的曝險 = **0 張**(本列自陳沒量過,估上界 1)

· **混合單(既有 `card` 收款又有非卡收款)= 0 張**
· 有卡收款的單 **1** 張 · 有非卡收款的單 **1** 張 · 有收款的單 **2** 張
  ⇒ 🎯 **那兩張是不同的單** ⇒ 沒有任何一張踩得到這條死路。
⇒ 📌 **本列的估計(上界 1)偏高一格,而方向是安全的那一邊。**

### 8-4 剩下的工 = **Sean 回一個字**

本列已經把題目收成一格(`:23-25`),而我複核過兩個選項都不動並發那道述詞 ⇒
**不需要重新論證鎖的射程,也不需要 migration 改述詞** —— 只動文案與勾選格。
🛑 **而它動的是 Sean 親手選的字面**(`20260905280000:96-97` 逐字引他:
「不擋:保留『卡退失敗改匯回去』這條退路, 但登記時多勾一格**『我確認卡上沒退』**」)
⇒ 🔵 **而他那次被問的是「卡退【失敗】改匯回去」**,不是本列的「卡上那筆【已經退成功】」
⇒ 📌 **這不是推翻拍板,是他沒有被問到的那一格。**

### 8-5 這一段證不到什麼

1. **我沒有呼叫那支函式**(SECDEF,唯讀角色叫不動)⇒ 證的是**本體逐字**,不是回傳值。
2. **0 張是今天的讀數** —— 全庫只有 2 張有收款的單,這個 0 極短命。
3. **我沒有量前端那格勾選今天長什麼樣**(本列 `:94` 提到「若畫面上還沒有那一格」的情況)。
4. **甲′ / 乙′ 哪一個比較好,我沒有意見** —— 那是文案品味,是 Sean 的。

---

## 九、`⟦b4-ZEROREMAININGSHOWSFORM⟧` —— **已做完且在 dev,而板上還寫 `open`**

> 兩把尺:plans 命中 2 支(其中 `2026-09-08-zero-remaining-shows-form-plan.md` **就是本列自己的 plan**)·
> 碼 commit **1 顆同錨**:`870c022b3 fix(admin): ⟦b4-ZEROREMAININGSHOWSFORM⟧ 帳本未登記額 0/null 不再顯示登記表單`
> ⇒ ✅ **`git merge-base --is-ancestor` 對 `origin/dev` ⇒ 在 dev**(⚪ 負對照:我自己的 HEAD `e91a1d337` ⇒ 不在)。
> **修法被裁定或拍板排除過嗎**:沒有。

### 9-1 結論:做完了,而且**連本列要求的那半也做了**

本列 `:12-14` 逐字提醒:
> 修法看起來只是把 `> 0` 改成 `>= 0` —— 🛑 **而動手前要先答**:`remaining === null`(帳本讀不到)
> 現在是**放行**的,而 DB 那端對 `NULL` 是 **fail-closed 拒絕** ⇒ **那一格也是同一個病**,
> 要一起判、不要只改看得見的那半。

✅ **兩半都改了。** dev 上 `manual-refund-entry-gate.ts:268-274` 逐字:
```ts
export function manualRefundLedgerSettled(refundUnregisteredAmount: number | null): boolean {
  // 🔴 **只有正數才算「還有東西可登記」**。`0`(帳本已把全額佔走)與 `null`(讀不到)都算結清。
  return !(refundUnregisteredAmount !== null && refundUnregisteredAmount > 0);
}
```
⇒ `0` ⇒ 結清 ⇒ **不顯示表單** · `null` ⇒ 結清 ⇒ **不顯示表單** · 只有**正數**才顯示。
⇒ 📌 **與 DB 那端對齊了**:`:277` `IF p_refund_amount > v_remaining THEN RAISE`(0 時任何正金額都被拒)
與 `:273-276` 對 `NULL` 的 fail-closed 拒絕,畫面現在都不會再給一張填什麼都送不出去的表單。

🔵 而該函式自己記了一句值得留的:
> `null > 0` 在 JS 是 `false`,所以 `!== null` 那道**在行為上是冗餘的** ——
> 而拿掉它 `typecheck` 會紅(`TS18047`)⇒ **守它的是型別那把尺,不是測試。**

### 9-2 不是死碼(我查過接線)

`manualRefundLedgerSettled` 的呼叫端:`order-detail-money-tab.tsx:386` 逐字
`ledgerSettled={manualRefundLedgerSettled(refundUnregisteredAmount)}` ⇒ **真的傳進元件**。
🟢 正對照:同一把尺在 gate 檔本身命中 **2** 處(定義 + 檔頭引用)⇒ 尺會動。

### 9-3 今天的曝險

· 本列 2026-09-08 量到「有非卡軌的單 = **1** 張」⇒ 上界 1。
· 我今天複量:**有非卡收款的單 = 1 張**(與該列一致)。
· 🛑 **而「那 1 張的 `remaining` 是多少」我一樣量不到** —— `pcm_order_refundable_remaining` 是
  `SECURITY DEFINER`,唯讀角色叫不動(本列 `:21` 也撞到同一堵牆)。
  ⇒ 📌 **上界仍是 1,不是一個發生數。**

### 9-4 這一段證不到什麼

1. **我沒有開瀏覽器看那張表單今天在不在** —— 證的是 `origin/dev` 上的碼與接線。
2. **`remaining` 的實際值量不到**(SECDEF + 唯讀角色)⇒ 沒有人證得出今天有沒有單真的踩到。
3. **我沒有跑那支測試** —— `870c022b3` 帶了 150 行測試(`entry-gate.test.ts` +101 · `refund-wiring.test.tsx` +49),
   我讀的是碼不是測試結果。

---

## 十、`⟦b4-REFUNDID1⟧`(#906)—— **列內有兩段互相矛盾的拍板,而較新的那一段引的是較舊的事實**

> 兩把尺:plans 命中 1 支(`2026-09-03-refund-unknown-state-outcome-split.md`)·
> 碼 commit 1 顆 `7ae77a723`(`fix(adapters): log 裡看得到、錯誤訊息裡看得到, 而程式拿不到`)
> ⇒ ✅ **在 dev**(我今天複跑 `merge-base --is-ancestor`,與本列 `:1` 寫的一致)。
> **修法被裁定或拍板排除過嗎**:🔴 **有,而這正是本列最值錢的一格 —— 見 10-1。**

### 10-1 🔴 同一列裡兩段講相反的事,而它們都掛 2026-08-31

· **`:19`(線DB 2026-08-31 19:5x 寫)**逐字:
  > ✅ **Sean 2026-08-31 拍甲(只刷不退)** —— 而**他第一次答的是「做完整刷+退」,是在錯的前提下**;
  > 三格端回去之後他改答甲。⇒ **本列的退款那一半今天【決定不做】,不是還沒做。**

· **`:23`(`-a0` 2026-09-01 寫)**逐字:
  > Sean 2026-08-31 13:11 答【甲 = **他打一發 TapPay sandbox 退款**】
  > (**證據等級=轉錄**:單一來源 = memory,**本窗未見他本人打字**)

🎯 **兩段都叫「甲」,而一個說【不退】、一個說【打一發退款】。**
📌 **而時間軸解得開**:`:23` 引的是 **13:11**;`:19` 說「他第一次答的是刷+退…**三格端回去之後他改答甲**」,
而那三格是線DB **19:5x** 寫的 ⇒ **13:11 那個答案就是「第一次」,已被同日傍晚的更正取代。**
🛑 **而 `:23` 是 09-01 寫的 —— 比 `:19` 晚一天,卻引了被更正掉的那一版。**
⇒ 🔴 **本列 2026-09-09 的 ⟨已量⟩ 一行接著引 `:23`**,把「等 Sean 打一發」寫成現況
⇒ 📌 **一個更正被寫進了列裡,而下游那三格沒有一格回頭讀它。**

### 10-2 那個「等 Sean 按」的前提,今天仍然不成立(我複核了擋它的碼)

本列 `:14-21` 說退款那條路**沒有任何環境驗得了**,成因是一道 fail-closed 斷言。
🔬 **今天實查,逐字還在**(`apps/admin/src/lib/payment/composition.ts`):
```
:48   export const PROD_SUPABASE_HOST = 'bmpnplmnldofgaohnaok.supabase.co';
:100    return `TAPPAY_ENV=sandbox 但帳本 DB 是正式站 ${PROD_SUPABASE_HOST}`;
:142  const violation = tapPayEnvPairingViolation(env, requireEnv('NEXT_PUBLIC_SUPABASE_URL'));
:143  if (violation) {
:144    throw new TapPayConfigError(
:145      `TapPay 環境與帳本 DB 配對不成立、拒絕建立退款用 adapter:${violation}`,
```
⇒ 🎯 **`sandbox` × 正式帳本 ⇒ 在【建 adapter 之前】就 throw** ⇒ **退款 adapter 根本不存在。**
✅ **`origin/dev` 上同一份**(同尺命中 **2**)⇒ 這不是我這棵樹的狀態,是線上的。
🟢 正對照:同檔同尺 **2** 命中 · ⚪ 負對照現造名 **0**。

⇒ 📌 **所以「排進端給 Sean 的那一疊」這個動作要先停一下** —— 端過去他也按不下去,
而 `:18` 寫了按下去的代價:production TapPay 找不到那筆 sandbox 的 `rec_trade_id` ⇒ 退款失敗,
**而 `order_refunds` 那一列已經寫進正式庫、佔著 single-flight 與可退額度,而我們沒有清理路徑。**

### 10-3 結論

· **碼那一半:做完了,在 dev**(`7ae77a723`,四個掉點一起收)。
· **剩下那一半:不是「等 Sean」,是【已決定不做】+【今天做不到】** ——
  `:19` 的更正已經回答了它,而 `:21` 寫了要做得到需要什麼:
  「一個**配對正確**的環境 —— 拋棄式 DB + sandbox 帳本 + sandbox 金鑰,三者同一側。**那不存在,要先做出來。**」
· ⇒ 🎯 **本列今天該做的不是派工,也不是端給 Sean,是把 `:23` 與 ⟨已量⟩ 那兩格對齊 `:19`。**
  🛑 **而板子凍結,我一個字都沒動** —— 落點在這裡。

### 10-4 這一段證不到什麼

1. **我沒有讀 `TAPPAY_ENV` 今天的值**(env 值不進對話)⇒ 我證的是**那道閘的存在與行為**,
   不是「今天正式站正好踩在被擋的那一組」。
2. **`:19` 與 `:23` 誰對,我是用【時間軸 + 兩段自己寫的話】推的,不是問 Sean 本人。**
   ⚠️ 而 `:23` 自標「證據等級=轉錄,本窗未見他本人打字」⇒ 兩段的證據等級**都不是第一手**。
   ⇒ 📌 **要收死這一列,還是要他一句話。**
3. **我沒有查 `:22` 那格更廣的問題**(前台完全不擋 `TAPPAY_ENV` 配對)今天還成不成立。

---

## 十一、`⟦b4-CAPRACE1⟧` —— **兩半都做完了:DB 已貼正式庫、畫面已在 dev,而板上還寫 `open`**

> 兩把尺:plans 2 支 · 碼 commit 3 顆(`7166a68bf` `212399e2a` `875e89334`)⇒ **全部在 dev**。
> **修法被裁定或拍板排除過嗎**:🔴 **有一半被裁掉,而那一格是本列最值錢的**——
> 「加鎖」那個**解法**被 codex R3 判**不要 commit**;而「這道閘拒絕登記已經退掉的錢」那個**問題**還在。
> 📌 **R3 擋掉的是解法,不是問題。**

### 11-1 為什麼「加鎖修好它」方向是反的(本列 `:2` 的實測,我沒重跑,轉述)

`order_manual_refunds` 記的是**已經退出去的錢**,不是叫系統去退錢
(`manual-refund-entry-section.tsx:88` 逐字「記錄一筆**已經交還給客人**的現金或匯款退款,
系統不會實際動錢——**錢是人交回去的**」)⇒ 📌 **閘擋下的是【紀錄】,不是【錢】。**
· 今天(無鎖)兩人各退 600 ⇒ 兩列都進 ⇒ 帳上 1200 / 實際 1200 ⇒ **帳是對的**
· 加鎖後 ⇒ 只有一列進 ⇒ 帳上 600 / 實際 1200 ⇒ **帳短少 600,而錢已經走了**
⇒ 🛑 **把一個【帳面超收、看得見】的狀態,換成一個【帳面短少、看不見】的狀態。**
🎯 而那一片的**驗收條件逐字是「1 列 / 600」—— 那正是出事的那個數字。**

### 11-2 DB 那一半 —— **已經在正式庫上**(本列說「db 先貼、前端後上」,我量的是貼了沒)

🔬 唯讀正式庫:
· `order_manual_refunds` 新增兩欄 **都在**:`over_cap_by integer NULL` · `cap_state text NOT NULL`
· 兩支 guard 函式 **都在**:`pcm_manual_refund_rail_cap_guard()` · `pcm_order_refund_cap_guard()`
· 今天的資料:`cap_state = within` **2 列**(沒有 `over` / `cap_unknown`)
· 🟢 正對照:該表共 **14** 欄(尺會動)

**而 Sean 的甲真的落進碼裡了** —— 線上函式本體逐字:
```
:131      NEW.cap_state := 'over';
:133      NEW.over_cap_by := NEW.refund_amount - GREATEST(v_headroom, 0);
:135    -- ══ 🔴🔴 **Sean 2026-09-02 拍【甲】:從 `RAISE EXCEPTION` 換成 `RAISE WARNING`** ══
:150    RAISE WARNING
:157      NEW.cap_state   := 'within';
:158      NEW.over_cap_by := NULL;
```
⚠️ **而 `PCM01` 這個字仍在那支函式裡** —— 我一度以為它還在擋。逐行印出來:
`:37` 的 `RAISE EXCEPTION` 是**防偽輸入 / immutable 那一道**(`:49-57` 把兩欄搬回 `OLD`、
呼叫者自己預填 `over_cap_by` 會被覆寫成 `cap_unknown`),**不是上限那一道**。
⇒ 📌 **同一支函式裡有兩種 RAISE,而只有上限那一種被降成 WARNING。**

### 11-3 畫面那一半 —— **也接上了**(本列 `:28` 寫「仍未交付」,那格今天過期)

· `manual-refund-read.ts` 在 `origin/dev` 上對 `over_cap_by|capState|cap_state` ⇒ **9 命中**
  (🟢 正對照:該檔在 dev 共 **201** 行)
· 顯示端:`manual-refund-ledger-section.tsx` ⇒ **6 命中**,而它自己的註解逐字寫著關閉條件:
  > `over` ⇒ **紅**,而且**印得出超出幾元** —— 板列關閉條件②逐字要「標得出來」,
  > 沒超過的那一筆不可以被標紅。**留空是最強的「沒被標」。**
  > 🛑 **不可把 `cap_unknown` 併進 `over`** —— 那會讓「超收了」與「我們算不出來」變成同一件事。
· `:125` 逐字「所以下面 `overCap` 是**無條件**的,連『這一區塊本來不渲染』都要為它渲染」
  ⇒ 🎯 **關閉條件③(負對照:沒超過的不可以被標紅)與「不可混同 unknown」兩格都在碼裡有落點。**

### 11-4 結論

**兩半都完成:DB 貼了正式庫、畫面在 dev。板上仍寫 `open`。**
· 本列的四條關閉條件(`:18`):①不再 RAISE ⇒ ✅ 降成 WARNING ②標得出來 ⇒ ✅ 兩欄 + 畫面
  ③負對照不可誤標 ⇒ ✅ 碼裡有落點 ④codex 不降級 ⇒ ✅ 三顆 commit 訊息都帶審查痕跡。

### 11-5 這一段證不到什麼

1. **我沒有造一筆超收去看它真的變紅** —— 今天正式庫 `cap_state` **全部是 `within`**,
   沒有 `over` 的樣本。⇒ 📌 **「碼裡有落點」與「畫面真的紅得出來」是兩個宣稱,我只證得到前者。**
2. **本列 `:25` 自陳的併發限制我沒有複驗**:`cap_state` 記的是「我這一筆當下看到的」,
   兩筆**直接 INSERT** 可同時讀到餘裕 ⇒ **併發下會【少標】不會多標**。
3. **`:2` 那個 A/B/C 三世界的實測我沒重跑** —— 本列自己 `:7` 就寫著 harness 只活在別窗的 scratchpad,
   而 repo 裡那支 `scripts/caprace1-concurrency-probe.sh` 測的是**舊設計**(`:26` 逐字
   「它跑綠也證不到本支」)。
4. **我沒有跑那 12 條驗收** —— 讀的是本列轉述與線上函式本體。

---

## 十二、`⟦b4-AUDITNULLAMBIG⟧` —— **成立、還沒寫壞任何一列、修法不用 migration,而它撞到一個裁定**

> 兩把尺:plans 命中 **2 支**(`plan-audit-before-null-ambiguity.md` = 本列自己的 ·
> `plan-audit-log-three-holes.md` = 線【帳號】`account` 把**三列合成一份**)· 碼 commit **0 顆**。
> **修法被裁定或拍板排除過嗎**:🔴 **沒有被排除,而它被【合併】了** —— 見 12-3。

### 12-1 洞在哪(本列自己給的座標,我複核過檔案存在)

`apps/admin/src/lib/orders/manual-cancel-notice-read.ts` 的
`readManualCancelNoticeRowForAudit()` **三條 `return null` 折成同一個值**:

| # | 世界 | 行 | 留痕 |
|---|---|---|---|
| ① | `res.error`(DB / RLS 出錯) | `:106` | 🔴 **沒有** |
| ② | 真的沒有那一列 | `:110` | — |
| ③ | `catch` 丟出來 | `:121` | 🔴 **沒有** |

⇒ 被寫進 `manual-cancel-notice-actions.ts:233` / `:405` 的
`before: { order_cancelled_outbox_row: null }`,而稽核表是 **append-only**
⇒ 📌 **事後爭議時分不出「本來就沒有」與「我讀不到」,而且回不去。**

### 12-2 今天的實況(唯讀正式庫,2026-09-09)

· `action LIKE 'email.order_cancelled.%'` ⇒ **0 列**
· 🟢 **正對照 A**:全表 **126** 列(plan 09-08 量到 119,今天 +7 —— 其中 4 列是我今晚兩張測試單的
  `order.manual_create` × 2 與 `order.cancel` × 2)
· 🟢 **正對照 B**:相異 `action` **21 個**逐個列出,而**沒有一個是 `email.order_cancelled.*`**
  ⇒ 📌 **那個 0 不是 `LIKE` 問錯**(這格是 `account` 的做法,我照跑一次,結論一致)
· ⚪ 負對照現造 action 名 ⇒ **0**

✅ **⇒ 這個洞【還沒有寫壞任何一列】** ⇒ 修法只要防未來,**不必處理舊資料**(與該 plan 的判定一致)。

🔵 **順帶一個廣度讀數**(不是本列受詞,只記錄):全表 `before IS NULL` **67** 列 /
`before` 裡有 `null` 字樣 **32** 列 / 總 **126** ⇒ ⚠️ **我沒有查那 67 列各自是不是同一種折疊**
(它們來自別的 action,不是本列的兩個)⇒ **標未確認,不當成本列的分母。**

### 12-3 🔴 它撞到一個裁定,而那個裁定的理由對這一格不成立

`plan-audit-log-three-holes.md:0` 節逐字:
> ```
> ⟦b4-AUDITNULLAMBIG⟧  before 分不出【沒有那一列】與【我讀不到】
> ⟦b4-MGR0-RPC⟧        沒有欄位記【那一刻他有沒有權】
> (第三個)             actor 存的是身分字串, 而【權限狀態沒有快照】
> ⇒ 三個都在同一張 admin_audit_log
> ```
> 🔴 **分兩次做 = 為同一張表開兩次 migration, 而第二次會撞第一次剛加的欄。**
> (主視窗 A 2026-09-08 裁:合成一份, 不開兩份。)

🎯 **而洞① 不需要 migration。** 兩個獨立依據:
1. **同一份 plan 自己說的**:洞② 逐字「📌 那不是『寫漏了一個值』,是**schema 裡沒有那個問題的位置**」
   ⇒ 要加欄的是**洞②③**,不是洞①。
2. **本列 `:12-13` 給的字彙**:`manual-cancel-notice-actions.ts:110`
   **同一個欄位已經寫過字串 `'none'`** ⇒ 🎯 **那一欄本來就吃得下狀態字串,不必新設計、不必加欄。**

⇒ 📌 **「合成一份」那個裁定的理由是【避免同表兩次 migration】,而洞① 一次 migration 都不用**
⇒ 🛑 **理由涵蓋不到它。**
🔵 **而我沒有自己決定** —— 那是主視窗的裁定,受詞是別人的檔(`account` 那一族),
命中「範圍會擴張到別人的檔」⇒ **已把這一格發給主視窗裁,我往下走。**

### 12-4 若裁定放行,這一片長什麼樣(四條關閉條件都在本列上)

☐ ① 兩個世界寫出**不同的值** —— 而證據是**一發會變紅的突變**,不是讀碼
☐ ② `res.error` 與 `catch` 兩條**各留一行 log**(有界去重),不是靜默
☐ ③ 既有的稽核列**一列都沒有被改動**
☐ ④ codex 對抗審查跑過(鐵則 12 資料正確性,**不降級**)

🔵 **零重新設計**:形狀抄同檔的 `readManualCancelNoticeEligibility`(`:139-143` 逐字
「unreadable 與『不符合』在回傳上是兩種東西,**這是刻意的**」)· 字彙用既有的 `'none'` 那一欄 ·
log 用 `staff.ts:30` 的 `consumeLogSlot`(用法在 `:69`)。
⇒ 📌 **三個零件都在 repo 裡,沒有一個要發明。**

### 12-5 這一段證不到什麼

1. **我沒有動任何碼** —— 卡在 12-3 那個裁定。
2. **`before IS NULL` 那 67 列我沒有分類** ⇒ 不知道有幾列是同一種折疊。
3. **本列自陳「既有稽核列裡已經有幾筆這種 null 我證不到」** —— 我今天證到的是**這兩個 action** 是 0,
   ⚠️ **不是「全表沒有這種折疊」**。
4. **append-only 的那一格仍然成立**:本片只讓**以後**的分得出來,**讓不了以前的**。
