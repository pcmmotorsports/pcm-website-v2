# A4a 關卡1 審查紀錄(codex gpt-5.6-sol, effort xhigh, -s read-only)

> 對象 = `docs/specs/2026-08-03-e10-a4a-summary-recompute-plan.md`
> R1(2026-08-03 午后,審 v1)= **NO-GO,19 must-fix**;跑前後 `git status --porcelain` 比對零留痕(前 = 僅 plan untracked)。
> 主對話逐條親驗:**折入 17、折入變體 2(F3/F10)、整條駁回 0**;plan 已升 v2(六個設計改動 D1-D6 見 plan 檔頭)。
> 原始輸出(session 存續期):scratchpad `a4a-k1-r1-out.txt`;本檔為 durable 紀錄。

## R1 findings 逐字(處置標注)

1. [must-fix] re-parent 的跨 trigger 鎖序確實可形成 `40P01`,與「無環」宣稱相反。T1 A→B 被 A2b1 先鎖 B、T2 B→A 先鎖 A,兩者再進 A4a 雙 parent 排序後互等;plan:74、a2b1 migration:123,131。
   ✅ 折入(D4):「無環」撤回,列為已知環①,fail-closed 40P01 誠實列界(owner-only 操作、零真 writer;修法需動已 apply 的 a2b1,不成比例)。
2. [must-fix] receipts 依 `procurement_id` 排序後逐列級聯鎖 parent,並未建立全域 `order_item_id` 取鎖序。
   ✅ 折入(D4):已知環②,writer 契約(多列 receipts 按 parent order_item_id 排)寫進 migration 註解,違約後果 40P01 fail-closed。
3. [must-fix] A4b SA1/SA2 無法證明 A4a helper 的 NKU 承重,因採購 INSERT 已被 A2b1 先鎖同一 parent。
   🔀 折入變體:機制陳述親驗**不成立**(mutant 下取消側不取 parent 鎖 ⇒ 不會在 parent 上阻塞;實際短暫等待發生在摘要 PK 的 unique 檢查,且 DO UPDATE 用 stale EXCLUDED 值覆蓋 ⇒ 終態漂移可觀察)。但「阻塞觀察無判別力」的內核為真 ⇒ SA2 斷言改打終態 + 新增 SA2b「取消 vs 取消」純對照(a2b1 鎖完全不在場)。
4. [must-fix] BEFORE guard 只驗「值等於 SUM」,沒有實現「禁止直寫」的來源契約(寫對值滑過)。
   ✅ 折入(D1):改 txn-local GUC 旗標制,任何直改(值對錯皆)P4A01;guard 不再讀 SUM。
5. [must-fix] DEFERRABLE receipt sync 與立即 BEFORE guard 互相衝突,會誤擋合法交易。
   ✅ 折入(D1+D2 連動消解):guard 不讀 SUM ⇒ metadata UPDATE 不誤擋;receipts sync 改 NOT DEFERRABLE ⇒ 原衝突情境不存在。
6. [must-fix] 防禦性 backfill 只重算摘要,沒有修復既存 `received_quantity` drift。
   ✅ 折入(D5):backfill 先走旗標路徑修 received_quantity 再重算摘要。
7. [must-fix] 「bigint 溢位不可構造」錯誤,且 §3.5 漏列 `22003`(quantity=INT_MAX、取消跨過上限 → 指派溢位先於具名 CHECK)。
   ✅ 折入:§3.5 補 22003 列、§3.7④ 更正、harness 補 R15 邊界 pin。
8. [must-fix] N6 隔離閘突變會被另一支 A4a trigger 以相同 tag 代打,存在假綠。
   ✅ 折入(D3):三支閘 tag 逐表具名(`a4a_iso_rc_procurement/_receipts/_cancellation`),N6 拆三格各殺各的 tag。BEFORE guard 依 D1 無閘 ⇒ 「BEFORE gate 代打」分支不再存在。
9. [must-fix] A4a 會自動建立摘要列,現有 B8 與 M2 不再可達,但矩陣完全漏列(B8 INSERT 撞 23505;M2 讀到被維護正確的摘要不翻面)。
   ✅ 折入:§7 補兩列(B8 改 UPDATE 竄改+DELETE;M2 setup 先竄改摘要再攻擊)。主對話親核 a2b1-verify:401-404、:521-530 確認。
10. [must-fix] procurement BEFORE 隔離閘會搶先改變 B9 baseline 與遮蔽 M4,不能只修 M12。
    🔀 折入變體:前半(BEFORE gate 搶先)因 D1 改無閘設計而消解 —— B9 baseline 不變;後半(M4 被 A4a AFTER 閘遮蔽)成立 ⇒ §7 補 M4 列,翻面斷言改 tag 區分。
11. [must-fix] A2b1 的 M5/M6/M9/M11 都會被 A4a 提前攔截,現有 13-mutant 分母失真。
    ✅ 折入:§7 補兩列 —— M5 setup 內 DISABLE 取消 trigger;M6/M9/M11 翻面斷言改「紅但非 P2B01/a2b1 tag」。主對話親核 :537-565、:620-652 確認各格攻擊字面。
12. [must-fix] §7 全序在 a7 harness 即不可執行,from-zero 已含 A7b 對 cancellation header 的 RESTRICT FK,DROP 被擋且錯誤未 fail-fast。
    ✅ 折入 + **主對話交易內試探實錘**(`DROP order_cancellations` 紅在 `orj_cancellation_fk`、ROLLBACK 零留痕):a7-verify 自 07-31 A7b 落地起**本來就壞**、與 A4a 無關 ⇒ 既有 harness 債,§9 Q4 問 Sean 處置。
13. [must-fix] 即使修到 a7 DROP 成功,它也會連帶刪除 A4a cancellation trigger,後續回歸不再是「A4a 世界」。
    ✅ 折入:同上,a7 覆蓋改 standalone probe,reapply 修復不屬本片。
14. [must-fix] 若正確保留 A4a trigger,a7t reapply 的硬編碼物件數(n=6)會立即失敗。
    ✅ 折入:§7 補列,期望 7 + 具名 allowlist。主對話親核 a7t-verify.sh:110-113。
15. [must-fix] a7bt 至少 C5、C3、C7 在 A4a 世界到不了各自預期守門(fixture 依賴「超量取消形狀合法」)。
    ✅ 折入:§7 補列,fixture 段 cell 內暫停 A4a 取消 trigger 塑形(§9 Q5)。主對話親核 a7bt-negative-state.sh:1085-1150(C3 註解逐字引 A7 無上限)。
16. [must-fix] 回滾以 `pg_depend` 證明「消費端只剩四 trigger」不可行(plpgsql body 依賴不入 catalog),A1 DROP 也不會可靠地被 dependency 擋住。
    ✅ 折入(D6):§6③ 重寫三層(pg_trigger 枚舉/repo-grep 清單寫死 runbook/42P01 反例演練)。
17. [must-fix] migration 結構驗收漏掉 function-owner 對齊與 `relforcerowsecurity=false`。
    ✅ 折入:§3.6 補(四表兩層,照 a2b1 :281,:292)。
18. [must-fix] 部署前提與 repo 現況矛盾:A2b1 已 apply(CURRENT.md:5),plan 卻寫未 apply、dry-run 兩支。
    ✅ 折入:§0/§10 更正為單獨一支。主對話親核 CURRENT.md:5 逐字。
19. [must-fix] plan 自行決定 receipts/cancellation trigger 可被 deferred,超出 Q2=A 授權範圍。
    ✅ 折入(D2):兩支改 NOT DEFERRABLE,deferrable 只留 procurement recompute(與 guard 成對 = 先例內)⇒ 不新開交易契約、不需新決策題。

## R1 對 §9 的獨立意見(當時三題)

Q1 不同意現案,須補完整連動(⇒ v2 §7 已擴成 12 處、§9 擴成五題)/ Q2 同意 / Q3 同意延伸但每支 gate 須獨立 kill cell(⇒ D3)。

---

# R2(codex 同模型確認輪,審 v2)= NO-GO

處置稽核 19 條:真修 13 / 部分修 4(#2/#8/#11/#17)/ 假修 2(#4/#14)/ 未修 1(#15)。新 findings 9 條(8 must-fix + 1 nit),主對話逐條親驗與處置:

1. GUC 可被有寫入權者預設 ⇒ 「任何直改皆 P4A01」不成立。🔀 折入變體:三張表對所有 role 零寫 GRANT ⇒ 能寫者僅 owner/SECDEF = 本在天花板之上(§3.7① 既列);降為措辭誠實界(「路徑判別,非不可偽造能力」),機制不變。
2. §3.2 generic tag 與 §3.1 三 tag 自相矛盾;procurement 閘在 I/U 上被 a2b1 搶先。✅ 折入:§3.2 改逐表 tag;R9/N6-procurement 改用 RR 下 DELETE 構造(a2b1 無 DELETE trigger)。
3. 第三死結環(同 parent 雙 proc 列)且要求完整排序。🔀 折入變體:環實錘,但**排序關不掉它**(T2 只碰 P2 本來就有序;主因 = T1 先持 parent 再回頭要 proc 列,per-row trigger 無法先知全集;parent-first 又與 DML tuple 鎖反向)⇒ 誠實列界環③、fail-closed 40P01、writer 建議。
4. a7t reapply 修法不可執行(migration 檔內集合恰等斷言先炸)。✅ 折入 + 親核 `:267-291`:a7t-verify 排除出家族序跑、probe standalone;附帶記債「集合恰等凍死 trigger 集」歸 A7 線。
5. a7t 探針 fixture qty=2 累積取消 3 ⇒ 提前 23514。✅ 折入 + 親核 `:112-118,:232-239`:fixture qty 提高,與 Q2=A 同形、事後回報。
6. a7bt 三腳本指向已 git rm 的 migration。✅ 折入 + 親核(`a7bt-negative-state.sh:58`、migrations 0 命中):排除、記債歸 A7c;**Q5 moot**。
7. M6/M9/M11 oracle「非 P2B01」太鬆。✅ 折入:精確 `23514` + `oiqs_ordered_le_quantity`。
8. owner/FORCE RLS 少算一表(共五表含 order_items)。✅ 折入 §3.6。
9. [nit] SA2b 未入 §10 DoD。✅ 折入。

§9 意見:Q1 加嚴(=#7)/ Q2 探針 32 必須真打(已折入 §7)/ Q3 tag 統一(=#2)/ Q4 同意 / Q5 前提失效。
⇒ plan 升 v3;R3 = 換模型換角度(Fable adversarial-reviewer 框架層,照 A2b1 前例)。

---

# R3(換模型換角度:Fable adversarial-reviewer,框架層,審 v3)= NO-GO → 全折後關卡1 收斂

四角度(假設/災難日/修法回歸/測試假綠)。名稱序地基、read-committed 閘、B13 修訂、DELETE kill cell、旗標巢狀 —— 五個框架假設被獨立確認站得住。findings 1 must-fix + 4 consider + 2 nit,全折入(plan 升 v4):

- **F1[must-fix] 回滾 runbook 只在健康日跑得通**:§6②「不等即 abort」+⑤「凍結=真相」在「摘要壞了才回滾」的正典災難日自我卡死。✅ 折入:②改分流(記錄差異→以真相為準續行;abort 僅留給真相表讀不到、附守門 re-enable)、演練必含竄改變體(b)。
- F2[consider] 停寫/stale 標記無具體形式。✅ 折入:停寫=今日 no-op 佔位+A5a 回寫契約(債⑦);stale=`COMMENT ON TABLE`。
- F3[consider] 漂移 oracle 與 helper 同 SQL ⇒ 等錯恆綠。✅ 折入:oracle 獨立推導 + 自身消融格(R13)。
- F4[consider] 旗標異常路徑未言明未測。✅ 折入:R16 新格(sync abort 後直寫仍 P4A01;GUC 子交易回滾語意本 session 已實測)。
- F5[consider] 排除三 harness 後 A7/A7-t 突變級覆蓋歸零、須明寫誰會發現。✅ 折入 §7 尾。
- F6[nit]「永遠搶先」措辭 → 「immediate 路徑下」。✅ 折入。
- F7[nit] 名稱序 = 一次性斷言非常駐守門(a7t 同型債)。✅ 折入 §11 債⑥。

**判停判定**:R3 findings 全屬加法、審查者總評「折入即可放行」;照 A2b1 前例(Fable R3 折入後不需第四輪全審)⇒ **關卡1 收斂,進實作**。實作後仍有 code-reviewer + 關卡2 codex 審 diff 兩層。

---

# 關卡2 · code-reviewer(Fable,審未 commit 工作樹)R1 = FAIL → 全折 → 64/0

> 第一位 opus reviewer 於額度牆中斷(5pm 重置),換 Fable 從零重審(高風險片在分級授權內)。

- **[Critical] R1-4「值比對」退化版無格可殺**:R8 全家都是「寫錯值」;若 guard 退化回被否決的「NEW=SUM 就放」版,drift 下寫 SUM 值滑過而 harness 零紅。✅ 折入:新增 **R8d drift 判別格**(旗標造 drift received=0/SUM=1 → 清旗標 → 直寫 =SUM 的 1 → P4A01;值比對版會放行轉紅)⇒ 釘數 63/39 → **64/40**;§5 N5 字面同步校準。
- **[Important] s1b 被修但 plan §7 寫「不改」= 字面為假**。✅ 折入:§7 拆出 s1b 列(既有債:seed 全 qty=1 撞 a2b1 守門)+ 演練↔s1b 交互列(A1 重放蓋 COMMENT 實錘)+ §4 產物表補齊。
- [Minor]×6 全折:plan 檔頭補 v4 changelog / a7-25 字面改 1000001(含為何非 1000000)/ a7t 字面改 100 / s1b 死變數移除 / 突變分母「無機械閘」誠實列界 / 演練 S1 基準補 runbook ③(a) 前置 trigger=4 斷言。
- 正向核對(reviewer 親數):計數全中、拍板引用一致、紅線零違反、兄弟修訂全屬加嚴、runbook↔演練逐步對應、migration SQL 逐面過。
- 複驗:fresh code-reviewer R2(下節)。

---

# 關卡2 · codex(gpt-5.6-sol xhigh,審未 commit 工作樹)R1 = NO-GO,9 must-fix + 1 nit → 全折

主對話逐條親驗、駁回 0:

1. **oracle 缺「有活動必有列」第三式** —— 刪活動品項的摘要列 oracle 仍 0。✅ 折入:三腳本 ORACLE_SQL + migration break-glass 全補第三式;H4b 新格(刪列向消融,oracle 必紅)。
2. **SA3 無消融腿、barrier 可被 tuple 鎖代打**。✅ 折入變體:加 SA3b(committed 去 sync 的 proc NKU;判別點 = **終態** received=1≠3 —— B 的 v_sum 在等待前已算好,tuple 鎖擋得住人擋不住過時值)+ 還原後重跑;a4b 釘數 28/5/1 → **33/6/2**。
3. **MUT/CELL 是宣告硬加非執行計數**。✅ 折入:MUT 改逐次執行計數(mutate_fn 內建 + N1/N7/N3 手動位;EXPECTED_MUT=**10** 真突變)、CELL 改 PASS+FAIL 差值執行推導;§C 標頭明寫「setup/clean/restore 為輔助 checkpoint」。
4. **committed DISABLE 中斷 → zc 永久停用污染**。✅ 折入:a2b1/a2b2 trap 補冪等 ENABLE。
5. **a2b2 起跑閘不驗 zc enabled、S3 無條件 ENABLE 洗白現場**。✅ 折入:a2b1/a2b2 起跑閘各加 zc tgenabled='O'(污染世界拒跑、不自行洗白)。
6. **standalone probe 可打遠端 DSN**。✅ 折入:a7/a7t 檔頭加端點閘(port=54329 且 loopback/unix-socket,否則 RAISE)。
7. **s1b trap 失敗仍 exit 0、無基準斷言**。✅ 折入:顯式 cleanup + trap 解除 + orders 回基準/零 S1B 殘留斷言(bad 驅動 exit 1);ORDERS_BASE 移到 fixture 前。
8. **rehearsal 中斷留半回滾 schema**。✅ 折入:trap 自修復(表缺→重放 A1;trigger 缺→DROP IF EXISTS ×9 後重放 A4a + 註解還原;冪等)。
9. **runbook ⑤ 無可複製驗證 SQL**。✅ 折入:貼上演練同款 $s5$ DO(分歧 ⊆ ②留檔集合,違反 RAISE 整段回滾)。
10. [nit] migration 探針「零留痕」口徑。✅ 折入:改「零可見業務列;鎖/WAL/dead tuple 仍在」(A7-2 同口徑)。

⇒ 全部修畢重跑(數字見 plan §5/§8/§10)→ codex R2 確認輪。

---

# 關卡2 · codex R2 確認輪 = NO-GO(7 真修/3 部分修 + 新 3 must-fix + 1 nit)→ 全折

1. **[must-fix] 演練 A4a 重放後、註解還原前中斷 ⇒ trap 跳過修復(trigger=4 條件)、註解污染留下**。✅ 折入:trap 的註解還原改無條件冪等執行。
2. **[must-fix] 災難對帳只從既有摘要列出發,漏「缺列」與「received drift」兩形狀**。✅ 折入:② divergence 改「活動 ∪ 摘要」全集驅動(缺列 = snap 為 NULL 的留檔列)+ 新表 `a4a_rollback_received_drift`;⑤ 三形狀子集檢查;runbook 同步;災難變體升級為**三形狀齊演**(P5 值竄改 + received 2→0 + P3 有活動摘要列被刪)→ 19/0。🔴 過程再抓一坑:**received 竄改的 UPDATE 會級聯 zc 重算把同品項的摘要竄改自癒回去** ⇒ 塑形順序承重(先 received 後 cancelled),首版反序被自癒反咬、實錘入註解。
3. **[must-fix] 端點閘判 listener 位址,SSH tunnel 可繞**。✅ 折入:加 server 端實檢 `pg_stat_file(data_directory || '/../.d1t2-harness')` —— d1t2 ownership marker 為硬證據;誠實界:tunnel 到完整 d1t2 遠端複本仍過(那本身就是 harness)。
4. **[nit] SA3b mutant 腿固定 sleep 1 不穩定**。✅ 折入:SA2/SA2b/SA3b 三個 mutant 腿一律改 `wait_blocked_by` 確定性 barrier(只保時序不當判定;等不到 = 硬停不假跑)。

R2 稽核之三個「部分修」(#2 sleep/#6 tunnel/#8 註解窗)即上列 1/3/4,一併關閉。
⇒ 修畢重跑:演練 **19/0**(三形狀)、其餘見 plan §10-4 終輪-2 紀錄;確認輪 R3 = 換模型(Fable adversarial-reviewer)聚焦本輪修法。

---

# 關卡2 · R3 確認輪(Fable adversarial-reviewer,換模型)= PASS → 關卡2 收斂

R2 四條逐一判**真修**、未開新洞(各中斷點 trap 推演收斂/②⑤ SQL 與 runbook 逐字同步/塑形順序論證成立/pg_stat_file 對 d1t2 佈局恆真且非 superuser fail-closed/三 barrier 有界確定)。4 nit 全數順手清:①trap 註解還原失敗補告警 ②摘要表 STALE 註解隨 trap 一併復原(SUMM_COMMENT0 快照;「trigger 在位卻標 STALE」矛盾世界關閉)③runbook ⑤ 補「成員 vs 形狀」誠實界 + A5a 回寫時重看 ④⑤ 註解補 received drift 全稱。
清完重跑:演練 **19/0**、a4a-verify **65/0**。

**關卡2 總結**:code-reviewer(Fable)R1 FAIL(1C+1I+6m)→ R2 PASS(8/8 真修);codex R1 NO-GO(9+1)→ R2 NO-GO(3+1)→ Fable R3 PASS。工作樹收斂,進 commit。
