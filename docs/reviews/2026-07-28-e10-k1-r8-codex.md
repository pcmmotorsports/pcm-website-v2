# codex 關卡1 R8 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `c4a482e`,審查期間全 repo 零 commit。
> **判定 FAIL —— 17 must-fix + 4 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → 15 → **17**(數字微升、
> 但戰場再下一層:跨 RPC 競態、TapPay 退款外部冪等 `bank_refund_id`(主對話已親驗 tappay-reference.md:94
> 確有此參數、String 20 不可重複)、退款隔日生效 ⇒ completed 語意需拆 submitted、D1 pooler 連線守門)。
> 🔴 兩條上升為**產品決策題**(折入 §8.6 第 3 批開批閘):①混合收款(卡+匯款)退款分軌與分配順序
> ②partiallyPaid 取消的本次應退額語意。

## codex 收斂判斷(逐字)

> 相對 R7,A9h、A9f、N3b-app 順序、`reason_detail`、U1 縮限、HCT 核心定案及 `partiallyRefunded` 的
> 逐品項重算**已關死**;DB claim-token fencing 與 D1 單一 orchestrator 方向也成立。但付款取消仍新增
> active-attempt/跨 RPC 競態,外部退款冪等與累計 reconcile 尚未可施工,多收款管道與 `partiallyPaid`
> 金額語意仍未定,D1 環境守門、HTTP timeout、read-back 及片大小仍有硬傷,文件漂移也未全清。
> 50 片算術正確,但 DAG/矩陣/分片契約尚未達可施工狀態。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:306:A8a 只排除 `pending`，但現行 active attempt 包含 `pending/charged/released`，仍可能取消已扣款但尚未回填的訂單。守門應排除所有 active／未收斂 attempt。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:219:A8a 排在 A8c 前，上線窗口內取消後仍可被既有付款 RPC 收款。DAG 應改成 A8c 先部署，或以同一 migration／feature gate 原子解鎖 A8a。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:307:A8a、begin、confirm 未定義共用 `orders FOR UPDATE` 鎖序，取消與付款仍可同時通過各自檢查。三支 RPC 必須先鎖同一訂單列，再檢查取消紀錄、付款狀態及 active attempt。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:257:「未付款取消閉環」對部分取消不成立，因 A8c 會封鎖整單後續付款，剩餘品項只能人工處理。第 1 批應限整單取消，或改標「安全取消但未閉環」並把應收重算列入第 3 批。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:297:A7b 沒有持久化 TapPay 必須唯一的 `bank_refund_id`，`cancellation_id UNIQUE` 不能充當外部冪等鍵。job 應建立並鎖死 `bank_refund_id`，退款帳本沿用同一值。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:297:`recordQuery.refunded_amount` 是交易累計值，「已含本次」沒有 baseline／target 就可能把前一次部分退款誤認成本次。應持久化交易識別、退款前累計與目標累計，且只在精確達標時 reconcile。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:433:TapPay 退款隔日才生效，但 worker 呼叫成功便把 job 標成 `completed`，混淆「已送出」與「已確認」。應加入 submitted／processing 語意，由 Record API 或 RF8 確認後才進 completed。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:297:`cancellation_id UNIQUE` 每次取消只能有一個 job，但第 3 批允許多筆收款及不同退款管道，無法表達分軌退款。應改成 cancellation＋payment／rail 唯一並定義分配順序，或將混合收款 fail-closed 並新增決策閘。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:432:`partiallyPaid` 只定義退款上限，沒有定義本次應退額，仍可能產生不同合法實作。應明訂以新應收額推導退款差額及運費／折扣處置，或列入 §8.6 等 Sean 拍板。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:235:「A8b＋worker 同批上線」沒有可逐片施工的安全發布序，仍可能短暫出現已 enqueue 卻無 consumer。應定義 dormant worker／schema → A8b → UI 解鎖 → enable worker 的明確順序與 rollback。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:768:D1 wrapper 要求 host 含 project ref，但正式連線使用 `aws-…pooler.supabase.com`，project ref 位於 username，守門會拒絕正確環境。應驗 pooler host allowlist 加 username 的 `.bmpnplmnldofgaohnaok` 後綴或 SQL project identity。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:775:D1 持 DB row lock 時執行六次 `recordQuery`，既有 fetch 無 AbortSignal，`statement_timeout` 也管不到 HTTP。應加單次與總體 API timeout、交易閒置 timeout，逾時一律 rollback。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:790:三筆 pending attempt 的 read-back 未定義查詢鍵、零筆／多筆及狀態判定矩陣，無法可靠證明「未授權／已取消」。應逐筆定死識別鍵、唯一命中條件與允許的 Record 狀態，模糊結果一律 abort。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:289:D1a1 仍混合九表匯出、checksum、加密及 26 組映射；D1a2、D1a3、D1b 也各包含兩套腳本／演練或 read-back＋完整 dry-run，仍明顯超過 45 分鐘。應再按匯出、映射、每支 restore、每次演練、read-back、dry-run 拆片。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:306:A8a 單片含整單／品項取消、冪等、兩層欄位寫入、數量與付款守門；A8c 又同改兩支金流 RPC，均不符 15–45 分鐘。應按核心取消、付款守門及 begin／confirm 分片並逐片負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:229:N3a 在 DAG 與拆片表標成 R，但內容只是 SQL helper，與本檔「SQL helper = T」分類原則不一致。應統一改成 T，並同步後續片數／風險統計。
- [must-fix] docs/handoff/2026-07-27-e10-order-closure-handoff.md:180:作用中的 Git 狀態仍寫未推 0、舊 v1 未追蹤及其他 dirty 檔，與目前 `c4a482e`、ahead 19、乾淨工作樹矛盾。應更新為當前查證結果或把舊快照明確移入歷史區。
- [nit] docs/handoff/2026-07-27-e10-order-closure-handoff.md:21:入口仍寫「五輪審查」，但同檔 §6 已到 R7。應改成不寫死輪數或同步為七輪。
- [nit] docs/handoff/2026-07-27-e10-order-closure-handoff.md:172:A0 現況重驗仍註記「列為 O0」，與 O 系列已退場矛盾。應改成 A0a／A0b／A0c。
- [nit] docs/reference/hct-logistics-api-reference.md:213:已定案的 shipment_reference 仍放在「未確認清單」內。應移到正式契約段，只留下 `epino` 允許字元這個外部待確認項。
- [nit] STATUS.md:19:「D1 是唯一不可逆片」與目前 D1a1–D1c 五片命名不精確。應改成「D1c 是唯一不可逆片」。
**收斂判斷**：相對 R7，A9h、A9f、N3b-app 順序、`reason_detail`、U1 縮限、HCT 核心定案及 `partiallyRefunded` 的逐品項重算已關死；DB claim-token fencing 與 D1 單一 orchestrator 方向也成立。但付款取消仍新增 active-attempt／跨 RPC 競態，外部退款冪等與累計 reconcile 尚未可施工，多收款管道與 `partiallyPaid` 金額語意仍未定，D1 環境守門、HTTP timeout、read-back 及片大小仍有硬傷，文件漂移也未全清。50 片算術正確，但 DAG／矩陣／分片契約尚未達可施工狀態。
