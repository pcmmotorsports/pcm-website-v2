# 2026-08-08 codex 恢復後的補跑清單

> **用途**:codex 額度 08-08 恢復後,把 08-07 這一天累積的「該過第三模型卻沒過」的東西一次補完。
> **為什麼是 codex**:08-07 全天的第二意見**清一色是 Fable**(codex 在牆期)。
> Fable 已經抓到很多 Opus 漏的東西,但它與 Opus 同屬一系;**codex 是真正的第三個模型**,
> 打的框架不一樣 —— 這正是「第 3 輪起換模型換角度」那條紀律要的東西。
> **怎麼用**:每項都給了可直接複製的 `git show` 範圍與「要 codex 打什麼角度」。逐項勾掉。

---

## §1 W 線累積 diff(出貨 writer 線)

🔴 **SHA 口徑**:主視窗 `E-163-A` 給的是 **B 窗的 feat commit**;`STATUS.md` 各條記的是**主視窗併回的 merge commit**
(`git rev-list --parents -n1` 驗過 = 2 個 parent)。兩者指同一片內容。
**下表一律用 feat SHA** —— `git show <feat-sha>` 就是那片的完整 diff,無歧義。

### 主視窗點名的(E-163-A ①)

| # | 片 | feat SHA | 範圍 | 已審過什麼 | 🔴 codex 要補的角度 |
|---|---|---|---|---|---|
| 1 | W3-1 建箱 | `f18d2420` | 建箱 RPC + W0 三件結清(產號重試上界 5 / conname 分派 / 耗盡碼 P2B21)+ 快照 `to_jsonb` 同源構造性保證 + 21 格 | **Fable PASS + 4 nit 全折**(`STATUS.md:16`) | Fable 只給 PASS ⇒ **零 must-fix 的片最需要第三隻眼**。打:產號重試上界 5 的邊界、conname 分派在 conname 為 NULL 時的行為、「構造性保證」是否真的構造得住 |
| 2 | W3-2 掛品項 | `ea69ecc4` | 掛品項 RPC + 交棒 1 前緣拒絕 + pending 佔用擴張(保守側偏離,主視窗認可)+ 31 格 | **Fable 兩輪 7MF 全折**(`STATUS.md:13`) | 打**那個被認可的偏離**:pending 佔用擴張是「只拒更多」,codex 要驗它有沒有反向誤拒;以及「前緣守的是失敗時機非正確性」這個推翻檔頭的結論 |
| 3 | W3-3 出貨 | `0d612874` | 出貨 RPC + 共用轉譯層(Q8=A)+ 23 格 | **Fable 5MF 全折**(`STATUS.md:12`),含 F2 死碼、F6 作廢×出貨 TOCTOU | TOCTOU 那條是 Fable 抓的 ⇒ codex 打**同類的第二個**:共用轉譯層被三支 RPC 共用,轉譯表對「新增的 SQLSTATE」是 fail-open 還是 fail-closed |
| 4 | W2 冪等層(**兩顆**) | `6e4b1f8f` + `a1f12059` | 五支 writer 冪等層(canonical jsonb 指紋 / 回傳信封 / 認領+23505 分派 / 三道鍵表守門)+ 62 格;第二顆是 Fable 補審的 2MF | **四輪審查,含 Fable 抓到三輪 Opus 全漏的 2MF**(`STATUS.md:17`) | 🔴 這片最值得:**三輪 Opus 全漏、Fable 才抓到** ⇒ 它的失敗模式明顯超出 Opus 一系的視野。打指紋契約(不含品項/時戳 ⇒ 同客同址第二箱會被當重放,已知 W8 硬前置) |

### 🔴 我實查到的同族,主視窗未點名(請裁定要不要一併)

| # | 片 | feat SHA | 為什麼我認為同族 |
|---|---|---|---|
| 5 | W5 線級 harness | `36d231e6` | Fable 2MF 全折(`STATUS.md:11`);而且它修的是**「五支 verify 紅跑回 exit 0」的家族回歸** —— 那是假綠家族,值得第三模型看 |
| 6 | W3c-1 作廢 writer | `00688c49` | Fable 2MF 全折;是 W3 家族最後一片,與 #3 的轉譯層直接相鄰 |

> 判準:主視窗列的 4 項全部是「Fable 審過的 W 線 writer」,而 W5/W3c-1 完全符合同一描述。
> 我不自行加入正式清單,列在這裡讓主視窗一句話裁定。

---

## §2 apply 前 preflight 併單

> 這批是「Sean 說排之後、主視窗要跑的唯讀前置」。寫成逐條可勾,免得當天現想。

### 2-1 🔴 pending 批現況 —— **是 6 支不是 5 支**

`E-163-A` ② 寫「五支(`150000/160000/170000/180000/190000`)」,但實查 W 線批**還有 `20260807140000`**:

| 檔 | 片 | 出處 |
|---|---|---|
| `20260807140000_..._b2_w0b_shipping_idempotency.sql` | W0b 冪等落腳表 | 🔴 `STATUS.md:23` 逐字「⛔ migration `20260807140000` **pending 未 apply**(W 線批,勿與他批混)」 |
| `20260807150000_..._b2_w1_shipping_rpc_skeletons.sql` | W1 骨架 | `STATUS.md:21` |
| `20260807160000_..._b2_w2_shipping_idempotency_layer.sql` | W2 冪等層 | `STATUS.md:17` |
| `20260807170000_..._b2_w3a_create_shipment.sql` | W3-1 建箱 | `STATUS.md:16` |
| `20260807180000_..._b2_w3b2_add_shipment_items.sql` | W3-2 掛品項 | `STATUS.md:13` |
| `20260807190000_..._b2_w3c3_mark_shipped.sql` | W3-3 出貨 | `STATUS.md:12` |

⚠️ **`140000` 我只確認「STATUS 曾宣告它 pending」,沒有實查資料庫**。
apply 當天第一件事應是 `supabase migration list` 對 ledger,**以實查為準、不以本表為準**。

### 2-2 逐條可勾

- [ ] **ledger 對帳**:`supabase migration list` 取 top,確認上述六支皆未在 ledger(有的話代表本表過期)
- [ ] **b2s2b 重釘 + 全跑**:🔴 `STATUS.md:11` 逐字「b2s2b 釘值過期=記 apply 前 preflight」⇒ 釘值要先重釘再跑,否則跑的是過期期望值
- [ ] **W5 線級重跑**:端到端(建箱→掛品項→出貨,全走 RPC 零直寫);🔴 注意 `STATUS.md:11` 的家族回歸——**exit 碼與 PASS/FAIL 字面兩軌都要驗**,W2 起曾有「FAIL>0 仍 exit 0」
- [ ] **五支 harness 各自重跑**:w0b 46 / w1 53 / w2 69 / w3a 23 / w3b2 33 / w3c3 25(數字出處=各該 STATUS 條;實跑對不上即停)
- [ ] 🔴 **apply 後 PostgREST 具名參數 smoke**:對新簽章打一次,**回守門錯誤(如 `P0001`)= 函式在;回 `PGRST202` = 函式不在**。
      這條是 `lessons §12-31` 擴寫後的**固定步驟**(不是一次性補驗)——它是 deploy 面唯一的觀測點,repo hook 看不到
- [ ] **型別重生**:apply 後 `gen types`,並檢查是否需要人工校正回貼(`STATUS.md:163` 有前例:漏貼會讓金流建單路徑型別紅)
- [ ] **零留痕檢查**:preflight 全程唯讀,跑前後 `git status --porcelain` 比對

---

## §3 其他標過「codex 恢復後補」的項 —— 查證結果:**幾乎都已被裁定不補**

我 grep 了整個信箱(`grep -liE "codex.*(恢復|牆|補跑|08-08)"`)與 `STATUS.md`。命中的幾乎全是我自己 E 線那幾封 STOP 的誠實邊界句,而它們**後續都已有裁定**:

| 片 | 當時標的 | 後續裁定 |
|---|---|---|
| 立法草稿 §5(`E-247`) | 「本片無跨模型第二意見」 | 唯讀彙整非高風險,`E-151-A` ③ 判定不需要 |
| 附件偵察(`E-248`) | 同上 | 唯讀偵察非高風險 |
| 三綠補洞 gate 片(`E-249`) | 提了三案 A/B/C | **`E-156-A` 裁 A** ⇒ 已由主視窗代跑 Fable、R2 PASS ⇒ **鏈已完整,不列** |
| #340 真效果測(`E-253`) | 只過 Opus 一輪 | `E-161-A` ① 裁**不補**(零 CI 改動、獨立測試檔) |
| #339 guarded-edit(`E-255`) | 只過 Opus 一輪 | `E-163-A` ① 裁**不補**(同上判準) |

⇒ **§3 實際上是空的**,沒有遺留待補項。
(主視窗 `E-163-A` ④ 也明示「你自己的 gate 片(已有真跨模型)不列」。)

---

## §4 誠實邊界

1. 🔴 **§2-1 的「6 支」以 `STATUS.md` 宣告為準,我沒有查資料庫**(無連線權限)。
   `140000` 是否真的還 pending,**apply 當天必須以 `supabase migration list` 實查覆蓋本表**。
2. 🔴 **§1 各片「已審過什麼」是抄 `STATUS.md` 各條的自述**,我沒有回頭讀那些片的審查報告原文。
   輪數與 must-fix 數字若與 B 窗的 STOP 有出入,以 B 窗的為準。
3. **§1 的「codex 要補的角度」是我的建議,不是規格。** 我沒有讀那六片的 diff 內容
   (那是 B 線的地盤、且量大),角度是從 STATUS 的一句話摘要推的
   ⇒ **codex 應該自己看 diff 決定打哪裡,我的角度只是起點,不要當成邊界。**
4. **§1 後半(W5 / W3c-1)是我加的,主視窗沒點名** —— 我不自行併入正式清單,等裁定。
5. **本清單不含 D 線與 F 線**:我只查了信箱與 STATUS,若那兩線有未寫進 STATUS 的待補項,本表看不到。
6. 純 docs 輕量片:三綠跑 typecheck + lint,build 依鐵則 11 省;未 push。

— E 窗,2026-08-07(供 08-08 使用)
