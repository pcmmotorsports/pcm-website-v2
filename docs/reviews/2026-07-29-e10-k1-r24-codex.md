# codex 關卡1 R24(確認輪四)— E10 施工規格 v2(2026-07-29)

> `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 = R23 折入後(commit `5a94432`)。
> **判定 FAIL —— 1 must-fix + 1 consider。** R23 四條全閉;唯剩 sweeper ownership 的
> crash-safe 順序(state 檔與停用的先後、恢復後清除時點、兩個 crash 邊界 fault-inject)。

## codex 收斂判斷(逐字)

> R23 的 A15 脫離 D1、0052 交易環境兩態、NOWAIT 不自動重試及兩項負測落片均已閉合;
> 69 片分布與高風險 42 片也重算正確。但 sweeper ownership 的 hard-crash 視窗仍會留下
> 無法安全恢復或日後誤恢復的正式 job,因此四條尚未全部閉合。

---

FAIL
**Findings**
must-fix  
[master plan:321](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:321)、[master plan:822](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:822)  
Sweeper ownership 仍未形成 crash-safe 合約：
- 未定義 state 檔與 `active=false` 的先後及持久化時點。若 job 已停用、state 尚未寫入就遭 `kill -9`，會留下 `active=false + 無 state`；self-heal 與人工 recovery 都被規格限定必須讀 state，因此無法恢復。
- 未定義成功恢復並驗證 `active=true` 後必須清除 state。殘留的舊 state 日後可能把別人刻意停用的 job 誤開。
應定死：先原子寫入並持久化 ownership state，再停用 job；恢復後 read-back `active=true` 才原子清除 state。D1t3 需分別 fault-inject「停用後、state 落盤前」及「恢復後、state 清除前」兩個 crash 邊界。
consider  
[master plan:14](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:14)、[master plan:15](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:15)  
第 14 行已正確寫成「確認輪通過 → Sean 最終批准」，但第 15 行仍稱「剩餘兩閘」為「關卡1 PASS＋Sean 批准」：前者早已完成，且漏列目前的確認輪。建議同步成「確認輪通過＋Sean 最終批准」。
**收斂判斷**
R23 的 A15 脫離 D1、0052 交易環境兩態、NOWAIT 不自動重試及兩項負測落片均已閉合；69 片分布與高風險 42 片也重算正確。但 sweeper ownership 的 hard-crash 視窗仍會留下無法安全恢復或日後誤恢復的正式 job，因此四條尚未全部閉合，現在不可施工或送 Sean 最終批准。
審查結束仍為 `5a94432`，工作樹乾淨，期間零 commit。
