# codex 關卡1 R27(確認輪七)— E10 施工規格 v2(2026-07-29)

> `gpt-5.6-sol`、零留痕已驗。受審 commit `b436998`。**判定 FAIL —— 1 must-fix。**
> takeover 主體已補,但「前置 assert active=true」寫在分流之前 ⇒ crash 後(active=false)
> 的合法接管者照樣被擋。修法 = state-first 分流,active 斷言只屬正式執行分支。

## codex 收斂判斷(逐字)

> R26 的 run-id 接管方向正確,但 state-first 分流尚未真正凌駕正常路徑的 active=true 守門,
> crash takeover 仍不可施工。其餘核對無新漂移:69 片、高風險 42、§8.6 未變。

---

FAIL
**Findings**
must-fix — [D1t3:321](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:321)、[D1 步驟 8:822](/Users/sean_1/pcm-website-v2/docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:822)
R26 的 takeover 主體已補上，但仍被同段既有的全域前置條件擋死：
1. A 寫 state、停用 sweeper 後 `kill -9`，此時 `active=false`。
2. B 啟動時，規格仍要求「前置 assert `active=true`，否則 abort」。
3. 因此 B 會在 recovery takeover 前 abort，無法執行新增的 CAS 接管、恢復及清除；D1t3 負測⑥與步驟 8 無法同時成立。
須將流程明確定死為：**先取 advisory lock → 先判斷 state**；state 存在就進 recovery mode（`active=false` 才恢復，`active=true` 只 read-back／清 state）並退出；**只有 state 不存在的正式執行分支**才 assert `active=true`、產生正式 run id。D1t3⑥應明寫 A 死後 sweeper 為 `active=false`，以證明 recovery 不會被正常路徑的 assert 擋住。
其餘核對無新漂移：69 片，分布 `U18/runbook14/A16/M7/R6/T5/docs3`；高風險 `7+5+14+6+10=42`；§8.6 未變。審查結束仍為 `b436998`，工作樹乾淨、期間零 commit。
**收斂判斷**
R26 的 run-id 接管方向正確，但 state-first 分流尚未真正凌駕正常路徑的 `active=true` 守門，crash takeover 仍不可施工。因此本輪不能 PASS；修正上述一處後才具備再次確認並送 Sean 最終批准的條件。
