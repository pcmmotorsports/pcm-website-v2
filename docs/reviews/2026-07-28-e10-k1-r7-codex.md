# codex 關卡1 R7 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `2d65e19`,審查期間全 repo 零 commit(R6 的基線瑕疵未重演)。
> **判定 FAIL —— 15 must-fix + 1 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → **15**。
> 🔴 本輪性質:R6 的 24 條逐條驗證**全數關死**;15 條新 finding 集中在**金流取消語意的深水區**
> (未付款單取消後仍可被收原額 / partiallyPaid 無實收真相 / partiallyRefunded quote 公式重複扣 /
> refund worker lease fencing 與 provider 冪等)與 **D1 可執行性**。文件漂移 2 條 = 第 11 次「只同步被點名段落」復發
> (HCT §8 與 handoff §3 我上輪都沒掃到 —— C1 還寫著「§8 已同步」,字面與事實不符)。

## codex 收斂判斷(逐字)

> 相對 R6,T 片型與 A5b SQL helper、alias 查詢/UI 前移、A9e/A9f→A9s/A9r 順序、A2 數量界線與
> 反向 delta、pending invoice 口徑、Q17 四不變式及 48 片數字**已實質關死**;§5.0/§5.0b/48 片表的
> 主要依賴和片型數也一致,§8.6 未發現第四個需要 Sean 拍板的產品題。仍持續產生 finding 的類別是
> **付款取消語意、refund worker 的外部冪等與 lease fencing、跨層逐片安全、D1 可執行性/片大小
> 及「只同步被點名段落」的文件漂移**。**目前不可施工。**

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:297-298、supabase/migrations/20260612150000_m3_s2d_charge_attempts.sql:181-195、supabase/migrations/20260611120000_m3_s2c_confirm_payment_rpc.sql:138-166:未付款單取消把應收調整延到第 3 批，但現行付款 RPC 只看 `payment_status` 與原 `total`，取消後仍可能被收原金額。修法：第 1 批即原子處理取消後可收額、active attempt 與 begin/confirm 守門；做不到則先禁用未付款取消。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:298、supabase/migrations/20260604120000_m3_s2a_orders_order_items.sql:99-109:`partiallyPaid` 要以「實收額」限制退款，但現有 `orders`／charge-attempt 沒有實收金額真相，RPC 無法實作 server 權威上限。修法：把 `order_payments`／captured amount 模型前移，或在第 3 批前 fail-closed 禁止此狀態取消。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:298:`partiallyRefunded = quote − 全單已退累計`會把其他品項的既有退款再次扣除，甚至產生負數。修法：用 `order_refund_items` 算逐品項剩餘可退量後重跑 RF1 quote，另以「原實收－累計已退」作全單上限。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:297-298:A8a 先建立可呼叫的取消 RPC，卻未規定 A8b 上線前須拒絕所有需要退款的付款狀態，形成可取消但不建 job 的中間態。修法：A8a 明訂僅允許安全狀態，其餘 fail-closed；A8b 完成後才放寬。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:288,420:`processing` lease 過期後沒有合法轉移與 claim fencing；舊 worker 可能在新 worker reclaim 後仍完成，造成雙退。修法：定義過期 reclaim 轉移、不可偽造的 claim token／generation，complete/fail RPC 以 token CAS，且第 6 次失敗原子進 `dead`。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:288,298,420:refund job 只寫「冪等鍵」，未定唯一約束、payload hash，也未把鍵固定成 TapPay `bank_refund_id`；provider 成功後 DB 回填前 crash 仍可能重退。修法：定死 job unique key、同鍵異 payload 拒絕、每次重試沿用同一 provider key並先 reconcile。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:250-251,320,357,420:第 1 批會開放已付款取消並宣稱第 19 項全綠，但退款 worker 到第 3 批才存在，job 只會長期 queued。修法：worker 前移，或第 1 批 UI/ action 禁用已付款取消並明示尚不能退款，不能把「看得到 job」當可用閉環。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:305:A9h 的 `batch_id` 沒有傳進 A5a、沒有持久化或 DB unique；同 batch 改 payload 會直接覆寫，無法兌現穩定重放語意。修法：持久化 batch/row request key＋payload hash＋逐列結果，或刪掉 batch 冪等宣稱、只保留明確的 business-key upsert。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:308、apps/storefront/src/lib/orders/order-display.ts:24-40,49-59:A9f 把 `partiallyPaid` 降成「處理中」，仍遮掉現行已鎖定的「付款確認中」付款事實。修法：五種付款狀態 exhaustive 優先映射，只有 `paid` 的 stale 出貨部分降級為「處理中」。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:237,466-468:DAG 仍是 N3b 先讓 live `create_order` 丟新 token，N3b-app 才部署 catch，期間用盡事件沒有 LINE 告警。修法：可安全前置的 N3b-app 先部署，再套 N3b，最後立即 N3c；或明定原子 release。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:287,341-344,362-374:Q18 的 `other` 要求手寫文字，但 A7 合約沒有 `reason_detail` 欄；部分取消又不寫 `orders.cancelled_reason`，文字會遺失。修法：在 cancellation header 加內部 detail，CHECK 鎖 `other` 必填／其他 code 禁填，再由 A8a映射對客文字。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:282,748-754:D1a 仍同片包含環境 wrapper、九表匯出、26 組映射、兩支 restore script及兩次隔離還原演練，明顯超過 45 分鐘。修法：至少拆成備份／映射產物、restore scripts、雙環境演練三片。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:755-758:D1c 要在持有同一 DB transaction 鎖時呼叫 TS TapPay adapter，但沒有定義能維持同一 session 的可執行 orchestrator；dry-run 還誤寫執行批准步驟 7。修法：產出單一 wrapper維持同一 PG session、API 失敗 trap rollback，dry-run 明訂模擬步驟 8-13。
- [must-fix] docs/reference/hct-logistics-api-reference.md:218-225:HCT §8 仍寫格式未定、P1 後綴待 Q19，與同檔 125-128 及主計畫 Q19=A 相反。修法：刪除後綴待決，只保留 `shipment_reference` 定案與允許字元未確認。
- [must-fix] docs/handoff/2026-07-27-e10-order-closure-handoff.md:87,145-170:交接決策表仍把 P1 寫成自動後綴，後段又稱 findings 未逐條處理及「24 片」，與 R1-R6 已折入、48 片及 Q19=A 衝突。修法：更新 P1，並把 159-170 明標歷史或移除過期現況字面。
- [nit] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:168:§4 的 U1 折入表仍只寫「多單併一箱」，未在第一落點帶入 Q16「同客人＋同收件資料」縮限。修法：直接在該格補限定，避免只讀決策表者採到舊語意。
**收斂判斷：**相對 R6，T 片型與 A5b SQL helper、alias 查詢/UI 前移、A9e/A9f→A9s/A9r 順序、A2 數量界線與反向 delta、pending invoice 口徑、Q17 四不變式及 48 片數字已實質關死；§5.0／§5.0b／48 片表的主要依賴和片型數也一致，§8.6 未發現第四個需要 Sean 拍板的產品題。仍持續產生 finding 的類別是付款取消語意、refund worker 的外部冪等與 lease fencing、跨層逐片安全、D1 可執行性／片大小及「只同步被點名段落」的文件漂移。**目前不可施工。**
