# A5a 關卡2 對抗審查逐字紀錄(codex gpt-5.6-sol xhigh、read-only、審 staged diff)

> 對象 = 6 檔 staged diff(migration + harness + plan v4 + reviews + runbook + master plan 一行);判定 = **NO-GO(12 must-fix + 9 nit、駁回 0)**。
> 全數折入,對照見 plan v5 header 與各檔的 🔀 關卡2 註記。折入後重跑:harness 144/0(原 122/0,新增 22 格)、migration 檔內 DO 全綠、家族序零污染。
> 🔴 折入過程另由 harness 自己逼出一條**兩個關卡都沒抓到**的問題:時間界字面不帶偏移會隨呼叫端 session TimeZone 漂移(詳 plan v5 header)。

## R1 原文

- `docs/runbooks/a4a-summary-rollback.md:13-29` — 若 Dashboard 將整段以單一交易執行，REVOKE 尚未提交時新 RPC 仍可能進入，且因 `xact_start > revoke_at` 被 drain 漏掉；Dashboard 多句交易行為 repo 內未確認。— must-fix

- `docs/runbooks/a4a-summary-rollback.md:157-158` — Forward 重建只重放 A1/A4a，沒有恢復 A5a 的 `GRANT EXECUTE`；成功重建後採購寫入仍永久停擺。— must-fix

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:55-58,302-319,357-369,451-452` — 新建路徑實際先鎖 supplier、後 INSERT procurement；部分更新根本不鎖 supplier，與宣稱的單一全序相反，無環論證建立在錯誤鎖序。— must-fix

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:596-604` — 前置 trigger 只驗同名存在；正確 trigger 被停用、掛錯表或指錯函式仍可結構綠，總量守門／摘要重算可實際失效。— must-fix

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:494-516` — ACL DO 只攤直接 grant；若 anon/authenticated 經 role inheritance 取得 service_role 權限，仍會假綠。測試庫 A4 有驗有效權限，但 production apply DO 沒驗。— must-fix

- `scripts/a5a-verify.sh:227-230,246-257,795-836,864-871`; `supabase/migrations/20260801140000_m4b_e10_s1a_suppliers.sql:64-82` — D2/D3 真更新 supplier，cleanup 只還原 `is_active`；touch trigger 已改掉 `updated_at`，最終只比啟用數仍會 122/0，「零留痕」為假。— must-fix

- `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:170-174`; `scripts/a5a-verify.sh:337-430` — plan 要 audit after 逐欄，實際只驗 create 的 allocated、update 的 reply；contact 等 before/after 寫錯，或六種非 reply 更新不寫 audit，整套仍可全綠。— must-fix

- `scripts/a5a-verify.sh:63-84,472-489,795-830,862-871` — 多數拒絕碼只驗回傳；若 SUPPLIER_INACTIVE 等「沒寫資料卻寫 audit」，交易內案例會 rollback，D2/D3 的 committed audit 又被 cleanup 刪除，仍可全綠。— must-fix

- `scripts/a5a-verify.sh:652-679`; `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:196-208` — C 區漏掉已規定的 `(0b,1)`；把 `SET CONSTRAINTS` 移到 actor 驗證後，R9/M8 因 actor 合法仍綠，122 格無法釘住宣稱順序。— must-fix

- `scripts/a5a-verify.sh:776-781,801-806,821-826,845-850`; `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:192-195` — 四組 barrier 只看泛化的 `wait_event_type='Lock'`，未用承諾的 `pg_blocking_pids` 指認 A，也未驗 lock target；「擋你的是誰」沒有被釘住。— must-fix

- `scripts/a5a-verify.sh:518-538` — 驗證矩陣未碰 `price_changed`、時間精確端點、U+200C/U+200D/U+FEFF、單號長度/控制字元、reason 控制字元；這些規則壞掉仍可 122/0。— must-fix

- `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:175`; `scripts/a5a-verify.sh:445-446` — plan 宣稱 B4 驗合法調升後摘要連動，實際 B4 只驗 `UPDATED`；A4a 若漏掉 UPDATE 事件，本 harness 仍綠。— must-fix

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:619-622` — 「不重複 A4a」只禁摘要表字面；直接呼叫 `pcm_a4a_recompute_order_item_summary` 可重算兩次，結構 DO 與最終值 harness 都仍綠。— nit

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:642-672` — FOR SHARE／FOR UPDATE 只計總次數、不綁資料表；兩者對調仍會通過檔內 DO。— nit

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:635-663` — received 錨硬綁 `v_before` 別名；改用等價 scalar 或更名 record 的正確實作會誤紅。— nit

- `scripts/a5a-verify.sh:387-399` — B3b 先覆寫 `v_f1`，再拿它與同一列目前值比較，`first_ordered_at 不動`斷言實為恆真；M4只補到特定突變。— nit

- `scripts/a5a-verify.sh:767-857` — D 區宣告 17 cells，實際 D1=4、D2=2、D3=2、D4=3，共 11；總計 `CELL=107` 正確，但區段字面錯。— nit

- `docs/specs/2026-08-03-e10-a5a-item-procurement-upsert-plan.md:215-217`; `scripts/a5a-verify.sh:722-730` — plan 的 M5 仍宣稱觀察 raw 23514，實際 mutant 改成 contact_channel 並觀察 `UPDATED`。— nit

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:168-194`; `scripts/a5a-verify.sh:605-609` — U+2800/U+3164 等肉眼空白可作 actor/request_id，形成看似空白或分裂的稽核鍵；現有 RAISE 格未覆蓋。— nit

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:168-170` — 註解說已剝空白，下一句卻說 `'alice '` 與 `'alice'` 會分裂；實作實際會合併，字面相反。— nit

- `supabase/migrations/20260803160000_m4b_e10_a5a_admin_upsert_item_procurement.sql:456-461,726-731` — migration 明確新增 service_role EXECUTE，回滾段卻宣稱「零 ACL 放寬」；只能說零表級 ACL 放寬。— nit

- `docs/runbooks/a4a-summary-rollback.md:10-11` — 文件寫「A5a 落地／自此有 writer RPC」，但 staged plan 明載尚未 apply；目前事實大於部署狀態。— nit

查無目前 RPC 本體的七欄 no-op 漏欄、`v_row_id` 落 NULL、實際 before/after 組值錯置，或 DML 成功但 audit INSERT 失敗仍可提交的路徑。

NO-GO
