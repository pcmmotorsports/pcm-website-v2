# B2-S2a-2(harness 結構段)審查總帳 —— code-reviewer R1 + codex 關卡2 R1/R2 + Fable R3

> 2026-08-06,a4a-chain 施工窗。受審 = `scripts/b2s2a-verify.sh`(新檔)。
> 派工 = `B-137-A`(主視窗核收 S2a-1 後開工;同一授權涵蓋整條 S2a 小線)。
> 上一片總帳 = `docs/reviews/2026-08-06-b2-s2a1-reviews.md`。

---

## 0. 這支在做什麼

| 面 | 內容 |
|---|---|
| pre-S2a 基準庫 | **只重放時間戳早於本檔的 migration 前綴**(不是「全部減本檔」——後者會隨日後新增的 migration 靜默漂移)。每個案例用 `CREATE DATABASE … TEMPLATE` 複製乾淨副本。 |
| 外部 oracle | 17 格:欄形狀 1 / 三條 CHECK 定義 3 / CHECK 具名集合雙向 1 / 七物件 COMMENT 必含錨 7 + 排除舊字面 4 + 全集 md5 指紋 1 |
| 突變 | 6 發(MUT0 只剝不改的對照 + 靶①-⑤)。一律**先剝掉 migration 自己的 §4 DO block**,再改一處字面;比對的是**紅在哪些格 ID 的集合**,不是紅幾格。 |
| 凍結值 | migration sha256 / 剝除 235 行 + 內容對帳 / 註解指紋 / 通過格 key 集合 / EXPECT_STRUCT・SRC・MUT |
| `gate` 模式 | 對任意目標 URL 的**唯讀** apply 前置閘(Fable R3 F1 的產物,見 §4) |

**實跑**:`PORT=54355 bash scripts/b2s2a-verify.sh all /tmp/b2s2av` → **PASS=15 FAIL=0 MUT=6**
- MUT0 全綠 / 靶① `[C2-oiqs_shipped_le_instock]` / 靶② `[C2-oiqs_shipped_le_instock C3-set]`
- 靶③ `[C2-oiqs_shipped_nonneg C3-set]` / 靶④ `[C1-col]`
- 靶⑤ `[C9a-COL:shipment_items.shipped_quantity C9c-fp C9x-COL:shipment_items.shipped_quantity]`

`run` 模式同結果;`alll` exit 2;`bash -n` RC=0;三綠 `pnpm typecheck` / `lint` / `build` RC 全 0。

---

## 1. code-reviewer(opus)R1 —— **FAIL / 4 must-fix + 11 nit**,全處置

| # | 級 | finding | 處置 |
|---|---|---|---|
| 1 | must-fix | 🔴 `cmp -s "$MIG" "$file"` **恆為 false**(突變檔一律先剝 §4)⇒ 那道「取代沒命中」守門從不可能觸發,而檔頭判定紀律②逐字宣稱靠它 | ✅ 比對象改成 `mut0.sql`(MUT0 自己比原檔),並改寫紀律② |
| 2 | must-fix | 檔頭「每一條斷言都有突變單獨打紅」大於事實(17 格只有 7 格被證明) | ✅ 改成「4 族 / 7 格由突變證明,其餘 10 格只被對照組證明可滿足」,並附那 10 格非恆真的人工查證出處 |
| 3 | must-fix | `STRIP_AWK` 是起訖區間刪除、會靜默吃掉鄰居 | ✅ 加「剝除行數必須恰等 235」的 Δ 對帳 |
| 4 | must-fix | `$MIG` 沒釘版 ⇒ §1 gate / §4 DO / 檔尾 rollback 被改都不會紅 | ✅ 加凍結 sha256 閘 |
| 5-15 | nit | 內部「項9b」與 plan 的原始碼錨 9b 撞名 / `MUT5_SRC` 無空值閘 / 前置閘在拋棄庫恆真卻計入 PASS / 原始碼錨只驗存在 / `CREATE DATABASE` 偶發 flake / marker 未擋 symlink / printf 轉義不一致 / `drop_db` 參數未引號 / psql 未驗版本 / 指紋 SQL 抄兩份 / `string_agg` 內未排序 | ✅ 全清(其中最後兩條見 §2 的自首) |

---

## 2. codex 關卡2 R1 —— **FAIL / 8 must-fix + 3 nit**,全折入

| # | finding | 處置 |
|---|---|---|
| 1 | 🔴 **只核對紅格「數量」,不核對「是哪一格」** ⇒ 指定格失效、別格意外轉紅但總數相同時照樣印 PASS。檔頭判定紀律①「必須紅在指定的那一格」**從來沒有被機器檢查過** | ✅ `struct_oracle` 改成把紅格的機器可辨識 ID 寫進 `REDS_IDS`(`C1-col` / `C2-<conname>` / `C3-set` / `C9a-<key>` / `C9x-<key>` / `C9c-fp` / `C0-count`),`run_mutant` 比**排序後的 ID 集合逐字相等** |
| 2 | `grep -cF` 數的是**行數**,同一行兩次仍回 1 ⇒ 「恰命中一次」量錯東西 | ✅ 改 `grep -oF … \| wc -l` |
| 3 | `PASS` 沒凍結 ⇒ 整段刪掉檢查會變成 `PASS=14 FAIL=0` 照樣 exit 0 | ✅ 先加 `EXPECT_PASS=15`,R2 再升級成 key 集合(見 §3) |
| 4 | `cmp` 的 rc=2(讀取失敗)被當成「有差異」放行 | ✅ 改 `case` 逐碼處理 |
| 5 | 非 `all` 一律當 `run` ⇒ 打成 `alll` 會靜默重用舊基準庫 | ✅ MODE 白名單,不符 exit 2 |
| 6 | 所有 psql 未加 `-X` ⇒ `.psqlrc` 可覆寫設定甚至執行命令 | ✅ 先 `PSQLRC=/dev/null`,R2 再補逐處 `-X`(見 §3) |
| 7 | 235 只凍結淨行數:錨整體偏移但仍刪 235 行時會通過 | ✅ 另加**內容面**對帳(該消失的消失、該留的都留) |
| 8 | marker 即可授權刪除、且**先停機才驗身分**,順序反了 | ✅ 加 `pgdata` symlink 閘;停機前先查埠上那台的 `data_directory` 是不是本 workdir 的 |
| 9-11 | nit:指紋 SQL 抄兩份 + `ORDER BY` 在子查詢 / PG17 檢查只在 `all` / pre-S2a 定義是「全部減本檔」會漂移 | ✅ 抽 `fp_sql()` 單一來源、`ORDER BY` 進 `string_agg`;兩模式都驗 `server_version`;改**時間序前綴** |

🔴 **我自己的字面 vs 事實(codex 抓到,原樣記錄)**:第 9 條在我送 R2 的 prompt 裡被我寫成「已折入」,
**實際沒折**;codex 逐字回「所稱兩項修法未真正落地」。R2 已真的折入,並在 prompt 裡自己標明。

---

## 3. codex 關卡2 R2 —— **FAIL / 7 must-fix + 3 nit**,全折入(全是我自己修法的二階缺陷)

| # | finding | 處置 |
|---|---|---|
| 1 | 埠連不上時**完全沒驗身分**,仍憑一個空 marker 就 `pg_ctl stop` + `rm -rf` 整個 workdir | ✅ marker 改帶**簽章內容**、三處都驗;`pgdata` 必須有 `PG_VERSION` 才准動 |
| 2 | 🔴 我加的「內容對帳」keep 錨**同時命中檔尾 rollback 註解範例** ⇒ forward 那句被誤刪照樣餵綠 | ✅ 只看**非註解行**(`grep -v '^[[:space:]]*--'`) |
| 3 | `PSQLRC=/dev/null` 擋不掉 **system-wide psqlrc** | ✅ 每個呼叫點都加 `-X`(`PSQLRC` 留作第二道) |
| 4 | `make_mutant` 內的第二個 `cmp` 仍把 rc=2 當「有差異」⇒ 修法 4 未完整折入 | ✅ 逐碼處理 |
| 5 | `EXPECT_PASS` 仍只凍結**總數**:刪一格 + 重複另一格照樣得到 15(與紅格那條同一個病) | ✅ 改凍結**通過格 key 集合**(`EXPECT_PASS_KEYS`,15 個具名 key) |
| 6 | 摘要先印 `FAIL=0` 才檢查 ⇒ 失配時唯一那行計數摘要仍是假綠 | ✅ 檢查移到印摘要之前 |
| 7 | `PORT` 未驗 canonical 整數就進 URI 與 `pg_ctl -o` ⇒ 前導零繞過黑名單、空白可注入啟動選項 | ✅ 純數字 + 無前導零 + 1024-65535 三道,排在黑名單之前 |
| 8-10 | nit:檔頭警語字面未隨修法 11 同步 / 前兩個用法範例沒帶 `PORT` / 檔頭稱 9c 會因 COMMENT 改動轉紅但 sha 閘會先退出 | ✅ 全改 |

---

## 4. 第三輪:adversarial-reviewer(**model = fable**,換模型換角度)—— FAIL → 修完 **改判 PASS**

四個角度前兩輪都沒用過:①這支存在的理由 ②維護者視角 ③失效保險反噬 ④覆蓋率誠實度。
Fable **不推翻整體形狀**(「剝 §4 + 外部 oracle + 逐格 ID 比對的架構是對的,該砍的不是格而是兩句超額宣稱」)。

| # | 級 | finding | 處置 |
|---|---|---|---|
| **F1** | must-fix | 🔴 **apply 前置閘是永不觸發的死保險**:0c 身分閘保證只有本腳本 provision 的拋棄庫走得到 0e ⇒ 它在正式站 apply 當下**結構上不可能執行**,而我寫「把判準寫成可執行的形狀、不讓它停留在文件裡」= 字面 > 事實。它唯一守的 delta(草稿箱有品項但未寄出 → migration §1 精確閘會靜默放行、本應交人看一眼)在唯一要緊的時刻沒有任何可執行載體 | ✅ 走 Fable 給的第二條修法:新增 **`gate` 模式**(`b2s2a-verify.sh gate "<URL>"`),排在 workdir/PORT 閘之前、不需 WORK/PORT、只跑兩個 `count(*)`、零 DDL 零寫入。原本那段的宣稱改寫成「恆為 0、沒有守門價值」,並明寫 S2a-4 runbook 交棒項未完成 |
| **F2** | must-fix | 🔴 **9c 指紋格宣稱的常設價值在本拓樸內無可達路徑**:晚於本檔的 migration 被時間序前綴閘永久排除,而本檔 §3 對七物件全部下 COMMENT 且恆為最後套用 ⇒ 任何先行者的蓋寫都會被覆回。放著不改,S2b 的人會以為註解蓋寫已有守門而不建自己的 | ✅ 改寫成「**本格是突變 oracle 專用**(靶⑤ 第三個紅點)」,並加交棒警語:「註解被日後 migration 蓋寫**目前沒有任何守門**,S2b 那片要自己建,不得因為看到這一格就以為已經有了」 |
| F3 | nit | 「那 10 條**錨**經人工查證非恆真」沒涵蓋 `C2-oiqs_cancelled_shipped_le_quantity`(它是 constraintdef 比對、不是錨) | ✅ 拆出來單獨補論證 |
| F4 | nit | 「不證明」清單漏披露 migration §4 的三族斷言(4b 欄名集合 / 4e A1 七條定義 / 4g 零 trigger)在本 harness **沒有外部對應格** | ✅ 補一段披露 |
| F5 | nit | mut5 的 python 第二次硬編 migration 路徑 | ✅ 改用環境變數收 `$MIG` |
| F6 | nit | sha die 訊息的待更新凍結值清單不全 | ✅ 列全 10 項 |

### `gate` 模式的實測(F1 的驗收)

| 情境 | 結果 |
|---|---|
| 乾淨庫 | `✅ apply 前置閘通過:shipments=0 / shipment_items=0(唯讀查詢,未寫入任何資料)` RC=0 |
| **負測**:副本 seed 後塞一列 `shipments` | `🛑 apply 前置閘不通過:shipments=1 / shipment_items=0` **exit 3** |
| 連不上的 URL | `🔴 查不到出貨表列數 … 當成不通過` **exit 3**(fail-closed) |

🔴 第一次的負測**沒觸發** —— 因為 `INSERT … SELECT FROM customers LIMIT 1` 在未 seed 的副本上是 0 列,
我又把錯誤吞掉了。seed 之後才構造得出來。**負測構造不出來時先懷疑自己,不要當成守門有效。**

### Fable 的複驗(對它自己的 WOULD-CHANGE 兩條)

- 條件 (a) **成立**:gate 模式逐 case 推過,錯誤文字含非數字 → exit 3;兩值皆空字串時 case 不命中但仍落到 `!= "0"` → exit 3,**兩條路都關死、無假綠縫**。
- 條件 (b) **成立(以修代證)**:雙方都找不到可達路徑,宣稱已與效力對齊。
- **改判 PASS**;順帶觀察(不擋收工):gate 的 psql 在 `export LC_ALL=C` 之前執行,錯誤訊息可能非英文 —— 但任何語言的錯誤文字都含非數字,仍 fail-closed。

---

## 5. 交棒(白紙黑字,不留隱形假設)

| # | 落在 | 內容 |
|---|---|---|
| 1 | **S2a-4** | 🔴 apply runbook 必須把「apply 前跑 `b2s2a-verify.sh gate "<正式站 URL>"`,非 0 就停、問人」寫成**實際步驟**。本片只交付了可執行的載體,沒交付流程。 |
| 2 | **大線 S2b** | 🔴 「摘要表七物件的註解被日後 migration 蓋寫」**目前沒有任何守門**;9c 那一格只服務突變靶,不要拿它當蓋寫守門。 |
| 3 | S2a-3 | C8/C9 行為負測與邊界正測、plan §5 的靶⑥-⑨ |
| 4 | S2a-4 | 回滾與故障注入、靶⑩⑪、runbook rehearsal 驗六欄十 CHECK |
| 5 | 任何改本檔 migration 的人 | 10 項凍結值必須一起更新(sha die 訊息已列全),**不是把閘拿掉** |

## 6. 誠實邊界

- 17 格中**只有 7 格**由突變證明會紅;其餘 10 格只被對照組證明「可以滿足」,非恆真的論證是**閱讀**、不是實跑。
- migration §4 的 4b / 4e / 4g 三族在本 harness **沒有外部對應格**;突變環境剝掉 §4 之後那三族零守門。
- 本支不涵蓋行為面(S2a-3)與回滾/故障注入(S2a-4)。
- 拋棄庫是本機 PG17、不是 Supabase;`provision` 沿用 d1t2 家族的 fitments 相容 stub
  ⇒ 這裡的全綠**不等於**「repo 能從零重建正式站 schema」(backlog #299)。


---

# 附:S2a-3 行為段(同檔加段)審查總帳

> 2026-08-06 續。受審 = `scripts/b2s2a-verify.sh` 的**行為段**(+~300 行)。
> 結構段已於 `931ef99` commit 並過四輪,本輪只審新增部分。

## 7.1 這段做什麼

- 基準庫 provision 尾端補跑 `d1t2-seed`(fixture 唯一來源),斷言 `order_items = 41` 列(`run` 模式也驗)。
- 三份 SQL 產物在跑之前由 python 生成:`beh.sql`(五格 oracle)、`v-addcol.sql`(只加欄)、
  `v-constraints.sql`(三條約束)、`v-constraints-noC9.sql`(靶⑩ 用)。
  四段字面是**寫死的副本**,守門是「對 migration 全檔 count 必須恰 1 次」。
- 六格:項4(既有壞列必被 C9 擋)/ 項5(C9 負測)/ 項6(C9 邊界正測)/ 項7(C8 負測)/
  項7b(C8 零值正測)/ 項8(C6′ 代數 smoke)。fixture 五值互異 `q/o/i/c/s = 10/9/2/4/3`。
- 八個突變靶:⑥⑦⑧a⑧b⑧c⑩⑫⑬,比對紅格 ID 集合。

**實跑**:`PORT=54355 bash scripts/b2s2a-verify.sh all /tmp/b2s2av` → **PASS=25 FAIL=0 MUT=6(結構)BMUT=8(行為)**
八靶逐格相符:⑥`[B5-c9-neg]` / ⑦`[B7-c8-neg]` / ⑧a⑧b⑧c 皆 `[B5-c9-neg]` /
⑫`[B6-c9-boundary]` / ⑬`[B7b-c8-zero]` / ⑩ 觀察值 `NOT-BLOCKED`。
`PYTHONOPTIMIZE=1` 下重跑同結果;三綠 RC 全 0;`bash -n` RC=0。

## 7.2 code-reviewer(opus)—— **FAIL / 3 Critical + 2 Important + 8 Minor**,全處置

| # | 級 | finding | 處置 |
|---|---|---|---|
| 1 | Critical | 🔴 **靶⑩ 的 `else` 分支直接 `echo NOT-BLOCKED`、完全繞過量測器** ⇒ 把匹配器改寬時對照組與靶⑩ **雙綠**,項4 的判斷邏輯有一族突變活著 | ✅ 改成走**同一條路徑**:用「拿掉 C9 的約束段」(`v-constraints-noC9.sql`),觀察值由同一個匹配器產生;另加「壞列是否還活著」的第二面 |
| 2 | Critical | 🔴 **項8 是純 `generate_series` 算術、不碰受測 DB**(migration 沒套也回 0),卻被算進 `EXPECT_BEH=6` 與「行為段六格全綠」——**輸入不來自受測物的格被算進覆蓋率** | ✅ 檔頭與 ok 訊息都改成「**5 格真的碰受測 DB**;項8 是代數 smoke、不算行為證據」;保留理由 = 它一旦非 0,§1 的冗餘論證就作廢 |
| 3 | Critical | 🔴 **B6 / B7b / B8 沒有任何突變殺得死** ——殺 B6 要 C9→`<`、殺 B7b 要 C8→`>0`,都不在原本六靶內 | ✅ 補**靶⑫**(C9 收緊成 `<` → 殺 B6)與**靶⑬**(C8 收緊成 `>0` → 殺 B7b);項8 無突變改成明文披露 |
| 4 | Important | `beh_item4` 用 `*oiqs_shipped_le_instock*` 比整段輸出判「被擋」;psql 語法錯的 `LINE n:` 回顯**同樣命中** ⇒ 生成端出錯會誤判成 BLOCKED | ✅ 改比 `ERROR:` 行(R2 再升級成含 SQLSTATE) |
| 5 | Important | `got` 在 DECLARE 宣告一次、每格不重設 ⇒ 抓不到列時沿用上一格的值;**目前靠兩格期望值恰好不同才轉紅 = 巧合不是設計** | ✅ 每格前 `got := NULL` + `COALESCE(…, '(讀不到列)')` |
| 6-13 | Minor | `beh_item4` 放進命令替換(`fresh_db` 內含 die)/ fixture 只驗列數不驗值 / `BEH_RUN` 魔術數字 5 / `run` 模式沒驗 seed / bmut6·7 與 mut2·3 逐字相同未披露 / 檔頭總帳路徑 / 項4 措辭 | ✅ 全清 |

**R1 明確查過沒問題的面**(原樣收錄):`beh_oracle` 的四條解析路徑皆 fail-closed;
`OTHER:` 分支不會被當綠;`BEGIN…ROLLBACK` + 每案例獨立副本未發現污染基準庫;
`d1t2-seed.ts` 純印 stdout、不連任何 DB;⑧a⑧b⑧c **分得開**(fixture `s=3` 只比 `i=2` 大 1,
五個錯式全被 B5 殺、B6 擋住反向收緊)。

## 7.3 codex 關卡2 R1 —— **FAIL / 2 must-fix + 2 nit**,全折入

| # | finding | 處置 |
|---|---|---|
| 1 | 🔴 `_r` **無 key 唯一性**,shell 只驗總列數:拿掉 B8、改塞第二筆合法 B5,仍是 5 列且八靶紅格集合不變 ⇒ **整支可全綠**(「數量 vs 身分」在本檔的**第三次**:前兩次是紅格數量、PASS 總數) | ✅ 改驗 **key 集合逐字相等**,不符轉 `B0-keyset` |
| 2 | 項4 未驗 SQLSTATE:只要 `ERROR:` 含 C9 名就算 BLOCKED ⇒ `42710 duplicate_object` 會被誤認成「成功擋下壞列」 | ✅ psql 加 `-v VERBOSITY=verbose` 讓 SQLSTATE 進 ERROR 行,改比 `ERROR: *23514:.*"oiqs_shipped_le_instock"`,觀察值改 `BLOCKED/23514/…` |
| 3 | nit:PYGEN 註解稱「從 MIG 抽出、不另抄」措辭不準(實為硬編副本 + 對帳) | ✅ 改述為「寫死的副本,機制是 count 恰 1 次、改了當場 die」 |
| 4 | nit:披露漏了 `bmut12` 與 `mut1` 也 byte-identical;`5/8` log 漏列 ⑫⑬ | ✅ 改成「bmut6/7/12 與 mut2/3/1 逐字相同,真正只在行為段出現的是 ⑧a⑧b⑧c⑩⑬ **五發**」 |

## 7.4 codex 關卡2 R2 —— **FAIL / 1 must-fix + 1 nit**,全折入

| # | finding | 處置 |
|---|---|---|
| 1 | 🔴 **第八種假綠**:`PYTHONOPTIMIZE=1`(或 `python -O`)會把 Python `assert` **整句移除** ⇒ 我用 assert 寫的「字面對帳」與「突變錨恰命中一次」兩道守門靜默消失,C8/C6′ 副本漂移後項4 與靶⑩ 仍可能全綠 | ✅ 兩處都改成 `raise SystemExit(...)`。**並實測**:同一段守門在 `PYTHONOPTIMIZE=1` 下,`assert` 版印 `GUARD-BYPASSED`、`raise` 版印 `GUARD-FIRED`;整支 harness 在 `PYTHONOPTIMIZE=1` 下重跑仍 `PASS=25 FAIL=0` |
| 2 | nit:摘要只印結構段 `MUT=6`,沒印行為段的 8 發 | ✅ 改印 `MUT=6(結構段) BMUT=8(行為段) 合計 14 發` |

R2 明確查過沒問題:`VERBOSITY=verbose` 只影響項4;key 集合比對可抓重複 / 未知 / 空檔,
值含 `|` 不影響 `cut -f1`;披露、鐵則、bash 安全、staged 污染均無新增問題。

## 7.5 這段最值得記的三件

1. 🔴 **「數量 vs 身分」在同一支腳本上出現三次**(紅格數量 → PASS 總數 → `_r` 列數)。
   固定修法:凡是「跑了 N 格 / 紅了 N 格」的斷言,一律改成**具名集合逐字比對**。
2. 🔴 **恆真格的第六與第八種**:項8 的輸入不來自受測物;`assert` 在 `PYTHONOPTIMIZE=1` 下整句消失。
   兩者共同形狀 = **守門看起來在,但它與被守護的東西之間沒有因果連線**。
3. 🔴 **繞過量測器的分支**(靶⑩ 的 `else`)比沒有量測器更糟:它讓對照組與突變組**走不同的路**,
   於是「兩邊結果不同」不再能歸因於突變本身。

## 7.6 誠實邊界(S2a-3 部分)

- 行為段六格中**只有 5 格真的碰受測 DB**;項8 是代數 smoke,不是關於本 migration 的證據,且**沒有對應突變**。
- 八發突變**不等於八個獨立的改動面**:bmut6/7/12 與結構段的 mut2/3/1 逐字相同,只是換一組 oracle 跑;
  真正只在行為段出現的是 ⑧a ⑧b ⑧c ⑩ ⑬ **五發**。
- fixture 是**臨時把一筆 `order_items` 的 quantity 改成 10**(`line_total` 同步)後在 `BEGIN…ROLLBACK` 內做的。
- 拋棄庫是本機 PG17、不是 Supabase;`provision` 沿用 d1t2 家族的 fitments 相容 stub(backlog #299)。
- 本段不涵蓋回滾與故障注入、runbook rehearsal 的六欄十 CHECK(兩者都在 **S2a-4**)。
