# 2026-08-07 夜跑收線立法彙整(草稿 → **已落檔,本檔轉為帳本**)

> **🔴 狀態(2026-08-07 下午更新):§1-§4 是當初的唯讀彙整草稿、§5 是落檔前置判定,兩者字面保留不動;
> 落檔已執行,去向見下方「§0 落檔紀錄」。本檔從「待辦清單」轉為「帳本」——
> 要查某條教訓最後去了哪裡,看 §0;要查當初為什麼這樣判,看 §5。**

## §0 落檔紀錄(2026-08-07 執行;repo 側=E 窗、memory 側=主視窗)

> 依 `E-158-A` 分工:**repo 側歸 E 窗**(本 commit)、**memory 側歸主視窗**、
> **`CLAUDE.md` 鐵則 11 字面等 Sean**(權限分級,兩窗都不動)。

**包①(9 條)→ `docs/runbooks/night-run-command-playbook.md`**

| 條 | 去向 |
|---|---|
| ①-1 `ls -lt` 錯字 | ✅ **主視窗已修** `:47`(commit `60c234c1`) |
| ①-2 寄信原子性 | ✅ 併進「發信防撞號 + 寫入原子性」條(暫存→`test -s`→`mv` 三步) |
| ①-3 哨兵寫 log | ✅ 尾註 B 樣板加 `>> $LOG` + 判死條件 |
| ①-4 推播≠一封 | ✅ **機制版**:尾註 B 樣板改成命中時印整份清單 +「以上每封都要處理」(規則文字已寫三處仍失守四次,故改物理限制) |
| ①-5 掃信貼輸出 | ✅ 併進「STOP 即掛哨」條 |
| ①-6 + 補-6 | ✅ **合為一條**「信在兩端各有一種死法」進 §4,兩端框架 + 機制形狀 + 發送端為何做不成機制,全寫進條文 |
| ①-7 guarded-edit 三條 | ➡️ **backlog #339**(腳本本體=機制,本片只做 docs) |
| ①-8 刪除紀律兩層清單 | ➡️ memory 側(主視窗;與 `feedback_range-delete-silently-eats-neighbors` 合併) |
| ①-9 機械序寫成節奏指令 | ❌ **不落檔**(§5.2 判定:與 ①-1/-3/-7 重疊、無獨立可執行內容) |

**包②-A(3 條)**

| 條 | 去向 |
|---|---|
| ②-1 跨 apply 停點 | ✅ **擴寫 `lessons §12-31`**(非新條)。🔴 **「或掛 flag」三字已寫入**(§5.5 回讀原信抓到:省了會禁掉合法做法)+ 機制觀測點=apply 後 PostgREST 具名參數 smoke 登記為固定步驟 |
| ②-2 手寫型別補丁零保護力 | ➡️ memory 側(主視窗;第四態 `D`=已落檔但不可 recall ⇒ **搬家 + 上索引**,不是再寫一次) |
| ②-3 拖兩版=歸錯類 | ❌ **捨棄**(§5.5 回讀:`STATUS.md` 查無對應字面,出處欄過寬等於沒有出處,不憑轉述落檔) |

**包②-B / ②-C(11 條)**

| 條 | 去向 |
|---|---|
| ②-4 約束保證的事發生了該吵不該吞 | ✅ **`lessons §12-38`**(新條) |
| ②-5 round-trip 不變量 | ✅ **`lessons §12-39`**(新條) |
| ②-10 停/復成對不變式 | ✅ **`lessons §12-40`**(新條) |
| ②-6 守門畫在最窄面 | ➡️ memory 加例(既有條全文已足,只加當夜第 N 例) |
| ②-7 突變綠了先懷疑沒打中 | ➡️ memory 擴寫(補「分辨『沒紅』與『沒跑』」) |
| ②-8 改守門連自述一起改 | ➡️ memory 加例 |
| ②-9 照 plan 字面寫出恆真分支 | ➡️ memory 併入恆真族 |
| ②-11 選擇器列表混活規則 | ➡️ memory 加例 |
| ②-13 退出碼三連坑 | ➡️ memory `reference_*`(主視窗;`E-158-A` 明示不歸 E 窗) |
| ②-14 審查者的藥也要親驗 | ➡️ memory 擴寫既有 |
| ②-12 同一對比 bug 換方向再犯 | ❌ **不落檔**(§5.2 判定:規則面不明確、無可執行判準) |

**補 6 條(主視窗補的、我關鍵字抽取會漏的)**

| 條 | 去向 |
|---|---|
| 補-1 + 補-2 三綠恆綠 | ✅ **合為一條** → `docs/patterns/slice-checkpoint.md` §2.2a(新增)+ §2.1/§2.2 定義收緊;並已做成**機制**(`scripts/check-syntax-nonts.ts` 掛 lint-staged,commit `34099c01`/`b171fc01`)。🔴 **另一半未完成**:`CLAUDE.md` 鐵則 11 字面=Sean 權限,建議字面已交主視窗轉呈 |
| 補-3 守門畫在動作 vs 狀態 | ➡️ memory 加例(第 3/4 例) |
| 補-4 探針前提沒成立當實錘 | ➡️ memory 加例 |
| 補-5 沒重讀自己的 plan | ➡️ memory 擴寫(適用面從「報告前」擴成「報告前 **與** 動手前」) |
| 補-6 信的兩端 | ✅ 見包① ①-6(合為一條) |

**帳目結算**:23 + 6 = 29 條候選 → **repo 側落檔 9 條**(§12 新條 3、§12-31 擴寫 1、playbook 5)
+ **backlog 2 條**(#339/#340)+ **memory 側 13 條**(主視窗)+ **捨棄/不落檔 3 條**(②-3 / ①-9 / ②-12)
+ **等 Sean 1 條**(鐵則 11 字面)+ **已由主視窗先修 1 條**(①-1)。

---

> **以下為 2026-08-07 上午的原始草稿字面,保留不動(§1-§4)+ 下午的落檔前置判定(§5)。**

> **狀態:唯讀彙整草稿。本文件不動 `docs/runbooks/night-run-command-playbook.md`、不動 `docs/lessons-learned.md`。**
> 主視窗核完統一落檔(`E-148-A:2` 明示)。
> **來源**:掃 `/Users/sean_1/pcm-mailbox/` 全部信件裡明示「收線立法／立法候選」的段落 + `STATUS.md` 當日條目。
> **每條格式**:一句規則 / 出處 / 建議落點(**機制 > 文字**,依 `~/.claude/rules/00-work-rules.md` §4 機制優先律)。
>
> 🔴 **抽取方法的邊界(先講)**:我是用「立法／收線立法／立法候選」關鍵字**精準抽取**,
> **不是通讀今夜每一封信**。⇒ 主視窗當時沒用這幾個字、但心裡當成候選的東西,**這份清單會漏**。
> 需要完整覆蓋的話要另跑一次全文通讀,成本高很多、我沒有自作主張跑。

---

## 包 ① playbook 修訂案(夜跑工法)

> 落點:`docs/runbooks/night-run-command-playbook.md`(2026-08-06 Sean 拍板常設)。

| # | 規則(一句) | 出處 | 建議落點 |
|---|---|---|---|
| 1 | 🔴 **`ls -lT` 不排時間、要用 `ls -lt`** —— 掃信箱找最新信必須按 mtime 排 | `D-173-A`(主視窗自陳「**我的常設指令寫錯**」,源頭在指令範本)| **機制**:直接改 playbook 裡的指令字面;全窗一次換掉。這是純字面錯,不需要規則文字 |
| 2 | 🔴 **寫信要「暫存 → 回填 → `mv`」,不可直接寫目標檔** | `B-161-A`(新流程首航成功、race 已根治)| **機制**:playbook 的寄信範本改成三步;配 `test -e` 防撞號(已存在) |
| 3 | 🔴 **哨兵一律寫 log,不可只靠推播** —— 推播斷了看不出來,log 看得出來 | `D-175-A`(病根=nohup 孤兒行程、harness 只認 wrapper)| **機制**:哨兵範本加 `>> log` 與「輸出連續 N 分鐘零成長=視為死」的判準 |
| 4 | 🔴 **收到單筆推播也要全量掃信箱**,不可只讀被推播的那一封 | `B-174-A` / `B-181-A`(B 窗**已中四次**)| **機制**:哨兵腳本印出時附一句「請全量掃」;**文字**:playbook 明列「推播 ≠ 只有這一封」 |
| 5 | 🔴 **STOP 的第一動 = 實跑掃信並把輸出貼進信裡**,不寫「我掃過了」 | 本窗每封 STOP 的實作;`feedback_check-mailbox-at-every-stop` 四步協定 | **文字**:playbook 的 STOP 範本加一行「附掃信輸出」 |
| 6 | 🔴 **主視窗每處理完一封 STOP,回信未發不得開下一件** | `E-148-A`(主視窗自陳:本窗空等 40 分;與 D-273「以為機制在跑、其實斷了」同族)| **機制**:主視窗的收尾檢查清單加一格;**文字**:playbook 派工節加一句 |
| 7 | **guarded-edit 三條**:①基準差值不是總量 ②還原用備份、不無腦 `git checkout` ③Δ 擋不了語意、要寫進守門訊息 | `B-163-A`(試跑報告三條全收)| **機制**:guarded-edit 腳本本體;**文字**:playbook 附使用守則 |
| 8 | **刪除紀律的完全體 = 兩層清單 + 二階孤兒化檢查** | `D-167-A`(該次有實質產出)| **文字**:playbook 刪除節 |
| 9 | **機械序要寫成 playbook 的節奏指令**(不是每次臨場想) | `D-173-A` | **文字**:playbook |

## 包 ② `docs/lessons-learned.md` §12 新條候選

> 落點:`docs/lessons-learned.md` §12(事故式教訓;立法程序見 §12-34 / §12-36)。
> ⚠️ 依維護協議,**新增條目屬「立法」** —— 這裡只列候選與理由,不代擬條號。

### ②-A 跨 apply 停點(今夜最貴的一條,有真實損害)

| # | 規則(一句) | 出處 | 為什麼值得立法 |
|---|---|---|---|
| 1 | 🔴🔴 **跨 apply 停點的「應用層」與「migration」不得先上其中一半** | `STATUS.md:13`(A9h-1 應用層昨夜先上線、migration 未 apply ⇒ 正式站採購 upsert **PGRST202、壞約 8 小時**)| **今夜唯一造成正式站真實損害的事故**;且它是**在別的視窗型別重生時才被挖出來**,不是被守門抓到 |
| 2 | 🔴 **手寫型別補丁零保護力,還會掩蓋事故** | `STATUS.md:12`(型別重生時「手寫補丁」那段)| 手寫型別讓 typecheck 綠 ⇒ 正好蓋掉「DB 還沒有這個東西」的訊號 |
| 3 | **拖兩版 = 歸錯類** | `STATUS.md` 當日條目 | 同族:落差被當成「還沒做」而不是「已經壞了」 |

**建議落點**:🔴 **機制優先** —— 這條的機制版本是「apply 停點的 CI 守門」:
凡 diff 同時含 `supabase/migrations/*` 與其消費端應用層檔案時,commit body 必須出現 apply 順序聲明;
或更硬的:**應用層呼叫新簽章的 commit 不得早於 migration 的 apply 紀錄**(需要 ledger 可查)。
若機制做不到,再退成 §12 文字條 —— **但要在條文裡先回答「為何機制做不到」**(§4 機制優先律要求)。

### ②-B 守門與斷言族(今夜復發最密集)

| # | 規則(一句) | 出處 | 備註 |
|---|---|---|---|
| 4 | 🔴 **DB 約束保證「不可能發生」的事若發生了 = 假設破了,該吵不該吞** | `E-143-A`(主視窗指名進立法候選;本窗 A9b2-A 的 `ShapeError`)| 反面就是 `.filter(Boolean)` 那種「靜默濾掉 → 變成查無此單」 |
| 5 | 🔴 **round-trip 不變量(「帶下去的值不會復活」)比「截到某長度」守得對** | `E-144-A`(指名進立法候選;本窗 `too_long` 跨片 bug)| 只斷言實作細節 ⇒ 改個數字就失守 |
| 6 | 🔴 **守門畫在最窄面 = 當時就在壞**(不是日後才壞) | `B-159-A`(活體反例;**同族今天第三次**)| 與既有 `feedback_guard-drawn-at-narrowest-surface-not-invariant` 同族,值得升格 |
| 7 | 🔴 **突變綠了,先懷疑突變沒打中** | `D-162-A` | 本窗今夜**中兩次**(語法錯 ⇒「no tests」;perl 打到同形狀的姊妹函式)⇒ 建議條文附「harness 要能分辨『沒紅』與『沒跑』」 |
| 8 | **「改守門內容」要連「守門的自述」一起改** | `B-148-A`(活字第五犯)| 與 `feedback_claimed-sync-but-only-patched-touched-lines` 同族 |
| 9 | **「照 plan 字面做會寫出恆真分支」** | `B-152-A` | plan 的字面不等於可執行的守門 |
| 10 | **停/復是成對不變式** —— 補了停、沒補所有路徑的復 | `B-154-A` | 形狀值得單獨立條 |
| 11 | **選擇器列表混活規則** | `D-171-A` | CSS 選擇器族的第 N 次 |
| 12 | **同一個對比 bug 換方向再犯** | `D-159-A` | 「修 A 壞 B」的變體 |

### ②-C 工具與退出碼

| # | 規則(一句) | 出處 |
|---|---|---|
| 13 | **退出碼是誰的**:`PIPESTATUS` / `No test files found` / `grep` 斷鏈三連坑 | `D-157-A` |
| 14 | 🔴 **審查者開的藥也要親驗** | 本窗今夜:codex 的 nit 3、主視窗的「撤 9 條」實為 69 條;既有 `feedback_verify-subagent-function-behavior-before-decision-question` 可**擴寫**而非新增 |

---

## §3 我的建議(不是裁定)

1. **包①的 1/2/3/4/7 建議直接做成機制**(改指令範本、改腳本),不要只寫規則文字 ——
   它們全部是「字面寫錯／流程漏一步」,而規則文字對這類的實測效果是 `feedback_claimed-sync-…`
   那條的紀錄:**寫成規則後仍復發 9+ 次、掛 hook 才壓住**。
2. **包②-A 那三條是今夜唯一有真實損害的**,建議**優先於**②-B 的守門族處理。
   ②-B 雖然條數多,但今夜全部被審查鏈接住了(零逃逸到正式站)。
3. **②-14 建議擴寫既有 memory,不要新增條目** —— 同一教訓不寫兩處全文(§4 教訓寫回哪)。

## §4 誠實邊界

1. **本文件零改動 playbook / lessons 本體**,只是草稿。
2. 🔴 **抽取方法是關鍵字精準抽取、不是通讀**(見檔頭)⇒ **這份清單一定有漏**。
   漏的形狀是「主視窗當時沒寫『立法』兩個字的候選」。
3. 出處欄的信件編號我逐封開過確認句子在裡面;**但我沒有回去讀那些信的完整上下文**
   ⇒ 個別條目的「一句規則」可能與原始情境有落差,落檔前請對照原信。
4. `STATUS.md` 我只掃了當日(`2026-08-07`)條目的**第一行摘要**,沒有展開每條的細節。
5. 條數:包① 9 條、包② 14 條。**沒有做去重與既有條文比對** ——
   例如 ②-6 / ②-8 與既有 memory 高度重疊,落檔前應先 grep `lessons-learned.md` §12 與 memory 索引,
   免得同一教訓寫兩處全文(這正是 §4 維護協議禁的)。

---

# §5 落檔前置(E-151-A ①1-4 + E-152-A ③ 指派;2026-08-07 下午)

> 本節補齊 §4 誠實邊界 2 自陳的缺口。**仍零改動 `playbook` / `lessons` / `memory` 本體。**
> 硬前置已讀:`docs/lessons-learned.md` §12-34(寫立法前必 view 末條編號)+ §12-36(必 view 既有 6 段模板真權威),欄位補齊見 §5.7。
> **末條編號實查:`grep "^### 12-" docs/lessons-learned.md | tail -3` = 12-35 / 12-36 / **12-37** ⇒ 新條從 **12-38** 起。**
>
> **一行總結論**:23 條裡真正該當「§12 新條」的**只有 4 條**;其餘 19 條分別是已有全文、需擴寫、或——最貴的那兩條——**已落檔但落在查不到的地方**。

## §5.1 🔴 三個改變落檔決策的發現(先講,因為它們讓「該寫幾條」的答案整個變了)

### 發現 1:②-A 那兩條**已經落檔了**,但落在事實上查不到的位置

`memory/project_m4b-a5a-procurement-upsert-decisions.md:50` **逐字已含 ②-A-1 與 ②-A-2**:

> 教訓:①前後半跨 apply 停點的片,應用層 commit 不得先於 migration apply 上線(或掛 flag)②型別檔手寫 12 參補丁「讓 typecheck 反映合約」實測零保護力(rpc 呼叫路徑無型別守門)且掩蓋了事故

`STATUS.md:20` 也寫了「教訓落 memory」——**字面屬實,不是假報。** 但:

1. **落在 `project_` 檔**,而 §4「教訓寫回哪」的第 ① 順位是 `feedback_*`(跨專案行為教訓)。「應用層不得先於 migration apply 上線」是跨專案通用行為,不是 A5a 的專案事實。
2. **埋在一個拍板檔的第 50 行附註裡**,標題是「A5a 採購 upsert RPC 08-03 晚拍 Q1=A」——與 apply 停點無關。
3. 🔴 **`MEMORY.md` 那一行索引完全沒提它**:索引的 hook 寫的是「no-op 必排在停用閘之前 / `GET STACKED` conname / 衝突矩陣列格前先證可達」,三個 🔴 沒有一個是 apply 停點。

⇒ **recall 路徑不存在**。除非你正好在讀 A5a 的拍板檔,否則永遠不會撞見它。
今夜唯一造成正式站真實損害(壞約 8 小時)的教訓,**寫了等於沒寫**。

> ⚠️ **這代表你給的三選一分類(新條 / 已有全文改指標 / 合併)涵蓋不了實況**,我加了第四態:
> **`D` = 已落檔但不可 recall** —— 內容在,但落點錯 + 索引不可見 ⇒ 保護力為零。
> 處置與前三態都不同:**不是再寫一次(那就真的兩處全文了),是「搬家 + 上索引」。**

### 發現 2:包①-4 在 playbook **已有三處全文**,B 窗仍中四次 ⇒ 機制論證從「建議」升格為「已證」

`night-run-command-playbook.md` 三處逐字已寫:

| 行 | 字面 |
|---|---|
| `:15` | 「每次被任何事喚醒,第一動=全量掃信箱處理所有新信,**不只處理喚醒那一封**」 |
| `:24` | 「哨兵推播→**全量讀所有新信**→接工前先重掛」 |
| `:77` | 「推播後**讀出來的每一封都處理**」 |

⇒ 我草稿裡「建議做成機制」的理由原本是類比(`feedback_claimed-sync-…` 復發 9+ 次)。
**現在有當夜同一條規則的直接實證:寫了三遍,B 窗還是中四次。** 這條的機制化不再是偏好問題。

### 發現 3:`ls -lT` 的錯字**就在 playbook 本體**,而我今天照著它踩了

`night-run-command-playbook.md:47` 逐字:「全量=`ls -lT` 不截斷」。

**實測**(`cd /Users/sean_1/pcm-mailbox`):

```
ls -lT | head → A-01-A.md  A-01-Q.md  A-02-A.md   (檔名字母序)
ls -lt | head → E-152-A.md E-151-A.md D-180-A.md  (mtime 序)
```

macOS `-T` = 完整時間欄位,**不排序**;`-t` 才是按 mtime 排。
⇒ 包①-1 不是「要不要立一條規則」,是**權威檔裡有一個會直接造成漏信的錯字**。
🔴 而且它有活體受害者:**這個 E session 開場漏讀 `E-149-A`/`E-150-A` 兩封**,病根同一個。

## §5.2 去重矩陣(23 條;態別 `N`新條 / `E`已有全文→降單行指標 / `M`合併擴寫 / `D`已落檔但不可 recall)

### 包①(9 條)—— 落點 `docs/runbooks/night-run-command-playbook.md`

| # | 態 | 既有字面在哪(實查) | 處置 |
|---|---|---|---|
| ①-1 `ls -lt` | **E(且是錯字)** | `playbook:47` 寫著錯的 `ls -lT` | **改字,不新增條文**。同時 grep 全樹其他 `ls -lT` 抄本 |
| ①-2 暫存→`mv` | **M** | `playbook:25` 只有 `test -e` 防撞號;無「寫入原子性」 | 併進 `:25` 那條,補第二半 |
| ①-3 哨兵寫 log | **N** | 尾註 B 樣板 `:67-75` 無任何 log | 改樣板(機制),不寫規則文字 |
| ①-4 推播≠只有一封 | **E ×3** | `playbook:15` / `:24` / `:77` | 🔴 **禁止再寫第四遍**。見 §5.1 發現 2 ⇒ 純機制 |
| ①-5 STOP 附掃信輸出 | **M** | `:24` 有「每片收工時全量掃一次」,無「貼輸出」 | 併進 `:24`,補證據要求 |
| ①-6 回信未發不開下一件 | **N** | `playbook` §2/§5 收割工法無此格 | 與 §5.6 補-6 **合為一條**(見下) |
| ①-7 guarded-edit 三條 | **N** | 全樹無 | 腳本本體(機制)+ playbook 附使用守則 |
| ①-8 刪除紀律兩層清單 | **M** | `memory/feedback_range-delete-silently-eats-neighbors`(process 冊:18)已有「Δ 對帳」半 | 併:那條講偵測、本條講**事前兩層清單** |
| ①-9 機械序寫成節奏指令 | **N(弱)** | 無 | 🔻 建議**不落檔**:與 ①-1/-3/-7「改範本」重疊,無獨立可執行內容 |

### 包②-A(3 條)

| # | 態 | 實查 | 處置 |
|---|---|---|---|
| ②-1 跨 apply 停點不得先上一半 | 🔴 **D + M** | 全文在 `memory/project_m4b-a5a-…:50`;`lessons §12-31`「commit 落地 ≠ apply 落地」`:1040-1060` 覆蓋**相鄰但不同**的洞 | **搬家 + 擴寫 §12-31**,見 §5.5 |
| ②-2 手寫型別補丁零保護力 | 🔴 **D** | 同上第 50 行後半 | **搬家到 `feedback_*` + 上 MEMORY.md 索引**。不新增 §12 條 |
| ②-3 拖兩版=歸錯類 | **M** | 同族,無獨立出處 | 併進 ②-1 條文的「症狀」段,不獨立成條 |

### 包②-B(9 條)+ ②-C(2 條)

| # | 態 | 既有全文(實查 檔案:行號) | 處置 |
|---|---|---|---|
| ②-4 約束保證的事發生了該吵不該吞 | **N** | 近親 `MEMORY-quality:13` failsafe 誤判正常態(反向)、`:15` 守門讀非權威快取;無同形 | **新條(12-38 候選)**。形狀獨立:`.filter(Boolean)` 把 `ShapeError` 靜默濾成「查無此單」 |
| ②-5 round-trip 不變量 > 截長度 | **N** | 近親 `MEMORY-quality:9` 正規化致盲;不同形 | **新條**。跨片 `too_long` 復活是活體 |
| ②-6 守門畫在最窄面 | **E** | `MEMORY.md` 主索引 `feedback_guard-drawn-at-narrowest-surface-not-invariant`(全文) | **只加例、不新增**,見 §5.3 |
| ②-7 突變綠了先懷疑沒打中 | **M** | `MEMORY.md` `feedback_negative-test-harness-self-false-green`(全文)+ `MEMORY-quality:25` plpgsql 哨兵撞碼 | 擴寫:補「**分辨『沒紅』與『沒跑』**」那一刀 |
| ②-8 改守門要連自述一起改 | **E** | `MEMORY.md` `feedback_claimed-sync-but-only-patched-touched-lines`(全文,hook 明寫已復發 9+ 次、hook 已掛) | **只加例**,見 §5.3 |
| ②-9 照 plan 字面寫出恆真分支 | **M** | `MEMORY.md` 恆真族兩條:`feedback_guard-checks-existence-not-effect` / `feedback_fixture-value-makes-guard-vacuous` | 併入恆真族,補「plan 字面≠可執行守門」一句 |
| ②-10 停/復是成對不變式 | **N** | 全樹無同形 | **新條**。形狀清楚(補了停沒補所有路徑的復) |
| ②-11 選擇器列表混活規則 | **E** | `MEMORY.md` `project_home-redesign-wire-decisions` 註「🔴CSS 選擇器族四次事故」+ `MEMORY-quality:22` | 加例(第 N 次),不新增 |
| ②-12 同一對比 bug 換方向再犯 | **弱** | 無獨立出處 | 🔻 建議**不落檔**:規則面不明確,無可執行判準 |
| ②-13 退出碼三連坑 | **N** | 同族 `MEMORY.md` `reference_psql-ta-boolean-cast-…`(工具字面坑);內容不重 | **新條(`reference_*` 非 §12)**:`PIPESTATUS` / `No test files found` / `grep` 斷鏈 |
| ②-14 審查者的藥也要親驗 | **E** | `MEMORY.md` `feedback_verify-subagent-function-behavior-before-decision-question` | 擴寫既有(我原判維持) |

**小計**:§12 新條候選 **4 條**(②-4 / ②-5 / ②-10 / ②-1 擴寫 §12-31),`reference_*` 新檔 1(②-13),
playbook 機制改 4(①-1/-3/-4/-7),搬家 2(②-1/②-2),擴寫 8,建議不落檔 2(①-9 / ②-12)。

## §5.3 你指名要看形狀的兩條(②-6 / ②-8)

兩條都是 **`E` 態:既有 memory 已有全文,本次只加「當夜第 N 例」**。形狀如下——

**②-6 → `feedback_guard-drawn-at-narrowest-surface-not-invariant.md`**
既有 description 逐字:「守門要畫在『不變量成立的那個面』,不是畫在『看到實例的那個檔』」。
⇒ 本次**不動 description、不改規則正文**,只在 body 追加一行例:

```
- 2026-08-07 夜(B 之 W0b F4):守門畫在「刪除」路,不變量卻是「鍵寫下不再變」
  ⇒ UPDATE 路零守門、改鍵可釋放原鍵重出貨(B 親驗實錘)。本線第三例。
```

**②-8 → `feedback_claimed-sync-but-only-patched-touched-lines.md`**
既有 hook 已寫「已復發 9+ 次;hook 已掛」。⇒ 同樣只追加一行例:

```
- 2026-08-07 夜(B-148):改了守門內容、沒改守門自己的自述字串(活字第五犯)。
```

> **為什麼是「加一行」不是「寫一條」**:§4 維護協議禁「同一教訓寫兩處全文」。
> 這兩條的規則面既有條文已完整覆蓋,新增的只有**復發計數**——而復發計數正是判斷「該不該升級成機制」的依據,
> 所以它有保存價值,但**價值在計數,不在重述規則**。

## §5.4 機制優先律逐條(§4 要求:退成文字條前必須先答得出「為何機制做不到」)

### 建議做成機制的 6 條(①-1/-2/-3/-4/-7 + E-152-A 那條)

| # | 機制長什麼樣(最小可行,**未實作**) |
|---|---|
| ①-1 `ls -lt` | **改字 + 防復辟**:改 `playbook:47`;加一格 CI/hook `grep -rn 'ls -lT' docs/ ~/.claude/` 命中即紅。字面錯用文字規則防是同語反覆 |
| ①-2 暫存→`mv` | **範本層**:playbook 寄信範本固定成 `cat > $TMP` → `test -s $TMP` → `test -e $DST \|\| mv`。三步寫死,不留自由發揮空間 |
| ①-3 哨兵寫 log | **樣板層**:尾註 B 加 `>> $LOG`;判死條件寫進樣板註解(log 連續 N 分零成長=視為死) |
| ①-4 推播≠一封 | 🔴 **哨兵輸出層**:哨兵命中時**不印檔名,改印整份 `find -newer` 清單 + 一行「以上每封都要處理」**。<br>理由:規則文字已寫三遍仍中四次(§5.1 發現 2)⇒ 要讓「只讀一封」在物理上不方便,而不是在道德上不允許 |
| ①-7 guarded-edit | **腳本本體**:三條(基準差值不是總量 / 還原用備份 / Δ 擋不了語意)全部進 `guarded-edit` 腳本,第③條寫進守門的**失敗訊息字串** |
| **E-152-A 那條** | 見 §5.6 補-6 |

### ②-A 的 3 條:機制做得到嗎

| # | 機制形狀 / 或「為何做不到」 |
|---|---|
| ②-1 跨 apply 停點 | 🔴 **2026-08-11 更正(Q26=A 部署 gate 片實作時推翻本格)** —— 下面刪掉線的兩句當時寫錯,留著是因為那個錯法本身值得記。<br>~~**做不到的那半**:真正要擋的是「應用層 deploy 早於 migration apply」,而 deploy 發生在 Vercel、apply 發生在 Sean 手動 `db push`,**兩者都不經過 git**,repo 側的 hook 對它們沒有觀測點。~~<br>~~⇒ **必須有文字條**,但條文要明說機制只覆蓋 commit 面、deploy 面靠停點紀律。~~<br>**為什麼錯**:兩個 Vercel 專案的 production 部署**都由一次 git push 觸發**(storefront=`push origin dev:main`、admin=`push origin dev`)⇒ **`pre-push` 就是那個觀測點**。把「Vercel 在別處建置」與「觸發它的動作在本機」混為一談,就把一個做得到的機制寫成做不到 —— 而 §4 機制優先律的判準正是「機制做不到才寫規則文字」,所以這一句直接決定了這條教訓當初只落成文字。<br>**現況**:`scripts/deploy-order-gate.sh` + `supabase/APPLIED.tsv`,掛在 `.husky/pre-push`;判準是「未 apply 的 migration 新建的 RPC 函式名,有沒有出現在這次要推的 app 面 diff 裡」,**零宣告欄位**(靠人打字的例外兩度被對抗審查證明是儀式)。<br>⚠️ 它仍**只覆蓋本機 git push**:GitHub 網頁 merge / Vercel Redeploy / Vercel CLI / 別台機器看不到 ⇒ 文字條仍要保留,但要說的是「機制覆蓋本機 push 這條路徑」,不是「deploy 面沒有觀測點」。<br>🔴 **真正的機制其實已經存在了**:`STATUS.md:14` 的 **P6 PostgREST 具名參數 smoke**(回 `P0001` 而非 `PGRST202`)——那是**部署後**驗證合約真的在,正是 deploy 面的觀測點。**建議把它從「本次補驗」提升成 apply 停點的固定步**,不要只寫規則 |
| ②-2 手寫型別補丁 | ✅ **純機制**:`database.types.ts` 加 codegen 標頭 + hook 擋手改(`git diff --cached` 命中該檔且無 codegen 戳記即紅)。<br>**不需要文字條** |
| ②-3 拖兩版 | ❌ 併入 ②-1,不獨立 |

## §5.5 ②-A 回讀原信結果(你指派的第 3 件;其餘 20 條維持「未回讀」)

回讀來源:`STATUS.md:14` / `:20` 全文 + `memory/project_m4b-a5a-procurement-upsert-decisions.md:50`。

| # | 草稿的「一句規則」 | 回讀後是否脫節 | 修正 |
|---|---|---|---|
| ②-1 | 「應用層與 migration 不得先上其中一半」 | ⚠️ **部分脫節** | 原始情境有一個草稿沒寫的關鍵限定:**「(或掛 flag)」**。事故不是「不准分兩次上」——分兩次上是常態且無法避免;是「**先上的那半沒有 flag 保護**」。條文若照草稿字面寫,會禁掉一個合法且必要的做法。🔴 **必改** |
| ②-2 | 「手寫型別補丁零保護力,還會掩蓋事故」 | ✅ 不脫節 | 原文多一層可用的因果:**「rpc 呼叫路徑無型別守門」**——這才是零保護力的**原因**,建議寫進條文,否則讀者會以為是「手寫容易寫錯」 |
| ②-3 | 「拖兩版 = 歸錯類」 | ❌ **查無出處** | 我在 `STATUS.md` 當日條目**找不到對應字面**。草稿出處欄寫「STATUS.md 當日條目」= **過寬,等於沒有出處**。⇒ 這是我 §4-3 誠實邊界所預告的那類落差的**實例**。建議:併進 ②-1 或**捨棄**,不要憑我的轉述落檔 |

> **另一則回讀所得**:`STATUS.md:14` 明寫 a9h 事故缺口「**正式關閉**」的方式是 P6 smoke ——
> 即 ②-1 的機制版本**當天就已經跑過一次了**,只是沒被登記成常設步驟。這比條文本身有用,見 §5.4。

## §5.6 你手上、我抽取一定會漏的 6 條(E-151-A ①-4 五條 + E-152-A 一條)

| # | 一句規則 | 出處 | 態 + 落點 |
|---|---|---|---|
| 補-1 | 🔴 **純 `.sql` / `.sh` 片三綠恆綠 = 鐵則 11 的結構缺口** | B 之 W0b/W1(`STATUS.md:16`) | **N,但落點不是 §12**。<br>🔴 實查 `docs/patterns/slice-checkpoint.md:56` 逐字:「build 在純文件 slice(**只動 .md / .json schema**)可省、但 typecheck + lint 仍跑」——**這句話的世界觀裡沒有 `.sql`/`.sh`/`.yaml` 這種東西**。<br>⇒ 缺口在鐵則 11 的**定義字面**,補在 §12 講事故沒用。應改 `slice-checkpoint.md` + CLAUDE.md 鐵則 11 |
| 補-2 | 🔴 **manifest YAML 沒有任何測試在讀 ⇒ 四綠對它零判別力** | D 線 08-07(`STATUS.md:15`,曾插入未跳脫雙引號打壞 YAML 而四綠全 `exit=0`) | **M → 與補-1 合為一條**。<br>兩者是同一個洞的兩個實例:**「三/四綠的判別力只覆蓋 typecheck+lint 讀得到的副檔名」**。合併後的規則面比拆兩條強,且各自留一個活體例 |
| 補-3 | **守門畫在動作、不變量在狀態(第 3/4 例)** | W0b F4 的 UPDATE 路 / `rm` 掉還活著的 cluster | **E**,同 §5.3 ②-6,加例即可。🔻 **不新增條** |
| 補-4 | **探針誤讀:前提沒成立卻當實錘用** | D 線(`STATUS.md:15` D 自首) | **E**。既有 `feedback_prove-the-page-loaded-your-asset-before-measuring` description 逐字已含「否則量到的是別的東西而全部看起來像通過」。加例即可 |
| 補-5 | **沒重讀自己三小時前寫的 plan** | B 線 W1(重寫了 plan 裡已有的產號器,還用了 plan 明文禁用的 `random()`) | **M**,你的預判正確。<br>但需**擴寫適用面**:既有 `feedback_assert-scope-only-after-reading-source-file` description 逐字是「**報**片數/片界/schema 事實前必先開原始檔」= 只覆蓋**報告前**;B 這次是**施工前**沒重讀。⇒ 擴成「報告前 **與** 動手前」 |
| 補-6 | 🔴 **信在兩端各有一種死法** | `E-148-A`(信沒發出)+ 本窗今日(信發了沒人接) | **N,與 ①-6 合為一條**(照 E-152-A ③ 的要求把「兩端」框架寫進條文本身) |

### 補-6 的完整形狀(E-152-A ③ 指定)

> **規則(一條,兩端)**:信箱協定的失效有對稱的兩端,任一端斷掉症狀相同(對方空等),必須一起立:
> - **發送端**:主視窗每處理完一封 STOP,**回信未發不得開下一件**。
> - **接收端**:施工窗開窗第一動,**比對自己最新 STOP 與最新 `-A` 的 mtime;`-A` 較新 = 漏讀、必須補讀**。
>   **號段高低不能拿來推斷主視窗有沒有回——兩條號段各自獨立遞增。**

**機制形狀**(你要的;mtime 比對確實是純機械判斷):

- **接收端(做得成,建議做)**:開窗儀式腳本一行——
  `find $MB -name '<窗>-1*.md' -newer "$(ls -t $MB/<窗>-2*STOP.md | head -1)"`
  有輸出=有漏讀的信,**印出來並要求逐封處理**。無輸出才准開工。純機械、零判斷。
- **發送端(做不成完整機制,原因如下)**:主視窗「處理完一封 STOP」沒有機器可觀測的完成事件——
  收割動作散落在 merge / 測試 / STATUS 更新,**沒有單一時點可掛 hook**。
  🟡 最接近的機制:主視窗開新任務前跑同一支腳本的**鏡像版**(找出「有 STOP 但我還沒發對應 `-A`」的窗),
  但它需要「哪封 STOP 已回」的狀態,而那個狀態目前只存在主視窗腦裡。
  ⇒ **發送端這半必須是文字條**,且條文要註明「為何機制做不到」= §4 的要求。

> 🔴 **這條有本日活體**:`playbook:15` 主視窗那側**已有**「絕不用號段最大號」(§2-②),
> **施工窗那側沒有對應字面** —— 我今天就是在那個缺口上漏讀了兩封。缺口位置精確可指。

## §5.7 立法程序要求的欄位(§12-36 硬前置;本草稿原本缺)

實查 `§12-34` / `§12-35` / `§12-36` / `§12-37` 四條,**6 段模板 + 條間 `---`**:

```
### 12-N. <標題>

**事故脈絡:**   <段落 prose,含 commit hash / 日期 / 誰在什麼情況下做了什麼>
**規則:**       <bullet;末項固定 **enforce:** …>
**規範定位:**   <對齊 working-style §6.3 第 M 條 + 相關 §12-X 延伸關係>
**教訓來源:**   <事故源一句>
**跨專案適用:** <適用範圍>
**首例:**       <anchor commit / 本立法 commit>

---
```

⇒ **本草稿 23 條全部只有「一句規則 / 出處 / 落點」三欄,缺 4 段。**
補齊工作量:§5.2 判定的 **4 條 §12 新條** × 4 段。**我沒有代擬**,理由:
1. 「規範定位」要指定 `working-style §6.3 第 M 條`的對應編號 —— 依 §12-34 必須 view 末條編號後才能寫,而**新增 working-style 條目本身是另一次立法**,超出「落檔前置」的授權。
2. 「首例」要填 anchor commit,而落檔 commit 尚不存在。
⇒ 建議:落檔那一步由你連同編號一次寫定。**本節的價值是把模板與缺口講清楚,不是代填。**

## §5.8 誠實邊界(本節新增,不覆蓋原 §4)

1. **§5.2 的態別是我的判定,不是裁定**;每格都附了實查 `檔案:行號`,你可以逐格反駁。
2. **去重比對的覆蓋面**:lessons §12 我比對的是 **37 條標題全表 + §12-31 全文**,`§12-1`~`§12-30` **只讀標題沒讀內文**。
   ⇒ 若某條的規則面藏在標題看不出來的內文裡,**我會漏判**。風險最高的是 ②-4/②-5/②-10 三條新條候選。
3. **memory 我比對的是三本索引的 hook 行 + 5 個檔的定向 grep**,276 個 memory 檔**沒有全讀**。
4. **②-3「拖兩版」查無出處**(§5.5)—— 這是草稿誠實邊界 3 預告的落差**真的發生了一次**;
   同類風險存在於其餘 19 條未回讀的條目。
5. 🔴 **我沒有驗證 §5.4 那些機制「做得出來」** —— 都是紙上形狀,一行都沒實作、沒試跑。
   「pre-commit hook 可驗」「codegen 戳記」這類話,**在被寫成腳本並跑過負向測試之前,都只是我的假設**。
6. **`ls -lT` vs `-lt`**(§5.1 發現 3)是本節唯一一個我**實跑驗證過**的字面(輸出見該節)。
   其餘所有 `檔案:行號` 是 grep/Read 實查,**未實跑**。
7. 純 docs 片:三綠跑 typecheck + lint,build 依鐵則 11 省;零改動 playbook / lessons / memory 本體;未 push。
8. 🔴 **本片無跨模型第二意見**(Fable 不可用、codex 額度 08-08 才恢復;本片為唯讀彙整、非高風險,依 E-151-A ③ 判定不需要)。

— E 窗,2026-08-07(§1-§4 上午 / §5 下午)
