# codex 關卡1 R9 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `4e09ece`,審查期間全 repo 零 commit。
> **判定 FAIL —— 16 must-fix + 2 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → 15 → 17 → **16**。
> 本輪性質:全部是**可機械折入的合約層缺口**(submitted 狀態機缺 claim 路徑 / baseline 初始化位置
> 錯放在 DB RPC / 同 rec_trade_id 多 job 序列化 / D1 片大小與名稱對齊 / recordQuery 無 AbortSignal
> 需先有 adapter 片),**零產品決策題**。允許集合的 `refunded` 分支被抓「恆假字面」(真退款單保留
> charged attempt ⇒ 永遠被後半條擋掉)——第 1 批取消收斂到只允許 `unpaid`。

## codex 收斂判斷(逐字)

> 相對 R8,付款守門先上、整單/部分取消誠實邊界、退款產品決策閘、D1 pooler 守門、N3a 片型與
> HCT 定案漂移**多數已關死**,56 片算術也正確。但退款 `submitted` 狀態機、累計退款序列化、
> D1 read-back/timeout 的可執行契約、片大小及 DAG/文件一致性仍有 must-fix;目前仍不可施工。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:307:`submitted` 沒有可 claim 的轉移，隔日 reconciler 無合法路徑把它推進 `completed/failed`。補 `submitted→processing` 的 token-CAS claim、排程時間與完整終態轉移。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:445:仍寫「worker 成功即寫帳本並標 completed」，直接復發 R8 的隔日生效問題。改成 refund API 成功只標 `submitted`，Record 達標後才同交易寫帳本並標 `completed`。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:307:`refunded_before` 定義為 A8b enqueue 時的 Record 累計，但 DB owner RPC 無法在同交易呼叫 TapPay。把 baseline 初始化移到具名 app worker 階段，並定義失敗／NULL 時 fail-closed。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:307:同一 `rec_trade_id` 可由多次取消產生多個 job，兩者可能取得相同 before、各送退款後讓累計越過兩個 target。加 per-transaction 單一 active job／固定鎖與累計 target 序列化。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:307:`bank_refund_id` 只明訂 job 重試沿用，未要求完成時 `order_refunds.bank_refund_id` 沿用同值。補 immutable 等值寫入與 DB assert，禁止另產帳本鍵。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:316:`refunded` 雖列入允許集合，但真 TapPay 退款單會保留 `charged` attempt，因此這個分支實際永遠被後半條擋掉。第 1 批改成只允許 `unpaid`，或定死 `refunded` 的獨立全額退款證據例外。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:316:文件反覆稱「五支 RPC」，實際只具名 `admin_cancel_order`、`begin_charge_attempt`、`confirm_order_payment`，A8a2 只是擴充片。列出全部精確 signature 與數量，若 A8a2 是新 RPC 就必須命名。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:287:56 片表仍把 A8a1/A8a2 編在 A8c1/A8c2 前，與 §5.0「守門先上」相反。重排片號為 A8c→A8a，避免依表施工產生收款窗口。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:813:D1b1 仍把 pending 的允許 `record_status` 延後到施工片才寫，三張退款單也未要求精確終態 `3=REFUNDED`。現在就寫死完整數值矩陣、top status、唯一筆數、金額與身分條件。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:301:D1b1 唯一查詢鍵指定 nullable 的 `payment_charge_attempts.rec_trade_id`，卻未證明六筆皆非 NULL。把非空實查列為硬前置，否則定義既有窄條件 fallback 並同樣要求唯一命中。
- [must-fix] packages/adapters/src/tappay/TapPayChargeAdapter.ts:224:現行 `recordQuery` 沒有 AbortSignal 參數，D1 寫「綁 30 秒 AbortSignal」目前不可實作且沒有承接片。新增先於 D1b1 的 adapter/port 片與 timeout 負測。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:729:`idle_in_transaction_session_timeout` 沒有定值，可能短於三分鐘總體 HTTP 預算而中途斷線。寫死大於總體預算且含餘裕的值，並測 timeout 後 transaction 確實 rollback。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:302:D1b2 要執行 D1c orchestrator，但九片中沒有更早一片負責建立與測試該 script；若塞進 D1b2 又明顯超過 45 分鐘。新增 orchestrator tooling 片，再獨立 dry-run／證據片。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:295:D1a1 仍同時含環境 wrapper、cohort manifest、十表匯出、checksum 與加密，仍明顯超過 45 分鐘。至少拆成守門／匯出腳本與 manifest／正式匯出加密驗證。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:786:§8.4 又把映射、post restore、post 演練寫成 D1a1/D1a2/D1a3，與片表的 D1a2/D1a4/D1a6 衝突。逐項改成片表的唯一責任名稱。
- [must-fix] STATUS.md:25:現存 §8.6 只列 HCT、通知與建單，漏掉 R8 新增的兩個退款開批決策。補上混合收款分軌與 partiallyPaid 應退額，避免第 3 批跳過產品閘。
- [nit] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:382:D1 線仍寫「D1a1-D1c 五片」，與九片表及 56 片統計矛盾。改成「九片，僅 D1c 不可逆」。
- [nit] docs/handoff/2026-07-27-e10-order-closure-handoff.md:172:歷史註記仍宣稱「現為 50 片」。同步為 56，或完全刪掉現行數字只指向 §5.1。
**收斂判斷**：相對 R8，付款守門先上、整單／部分取消誠實邊界、退款產品決策閘、D1 pooler 守門、N3a 片型與 HCT 定案漂移多數已關死，56 片算術也正確。但退款 `submitted` 狀態機、累計退款序列化、D1 read-back／timeout 的可執行契約、片大小及 DAG/文件一致性仍有 must-fix；目前仍不可施工。
