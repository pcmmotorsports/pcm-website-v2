# B2-S2a 關卡1 第 3 輪(Fable 換模型換角度)— 框架級結論

> 2026-08-06 03:10 主視窗派(依 Sean 07-29「第 3 輪起換模型換角度」拍板);受審=`240ed1a` plan v2+`17f0992` 凍結公告;唯讀零留痕。前兩輪(codex)報告已全文讀畢、不重複其 41 條。

## 結論:FAIL(層級錯置),但走法=開工,不是再修 plan

**病灶=plan 把實作用散文寫了一遍**(逐字 SQL/鎖序 code block/fixture 值/突變表)。散文不能執行 ⇒ 每輪都能抓到真 bug、任何修法都無法被驗證,只能等下輪再被抓;code 的收斂機制是「跑 harness」,散文沒有 ⇒ **輪數上限內必然不收斂,與體積無關、與拆線無關**(R2 4/16 條=修法自引入,即此機轉)。

**plan 事前可證偽的最大集合**=①現況事實(catalog/grep)②邏輯/代數性質(蘊含/鎖序/循環依賴/oracle 判別力)③測試設計(fixture/突變靶)④決策規則(量到什麼→停)。**不可證偽集合**=測量值(秒數/耗時)、執行結果(幾格綠)、凍結計數——這三族該是 diff 關卡+harness 的驗收,不是 plan 的。41 條中僅 ~5 條屬後者 ⇒ plan-gate 判別力有(死結面/假 oracle/runbook 退五欄全是真貨),**終點沒有**。

## 前兩輪沒抓到的框架級 finding

- **F1(HIGH)**:plan §0.3 自己記錄三張出貨表全 0 列 ⇒ 三條 CHECK 掃描與 SHARE 鎖窗發生在**空表**上;四輪折衝 lock probe/秒數門檻無人把 §0.3 連到 §3.4。事前判準一行就夠:「apply 當下任一表非 0 → 停、問人」。
- **F2(MEDIUM)**:凍結計數的正確落點=**harness 開頭自斷言格數**(少一格自己紅),不是 plan 散文(枚舉寫下即過期)。
- **F3(MEDIUM)**:quarantine 機制=重新發明 git——migration 不 commit 進 dev 就物理上 push 不到;**施工粒度≠apply 粒度**,S2a-1~4 照樣一片片做,登帳與 apply 等大線同批(=S1「三支同批」既有慣例)。
- **F4(LOW)**:plan §10 由作者指定 R2 審查重點=受審文件導引審查者;作者已知弱點在哪=該動手修的訊號。

## 三題建議(Q3 是假三選一)

- **Q1=A** 開工、把關押在 diff 關卡+harness 實跑;§0.6 已知壞點+R2 #16 在 diff 裡修,不回頭改 plan。
- **Q2=A** plan 寫判準不寫數字;判準第一條=F1 那行。
- **Q3=C** 小線大線**同批 apply、無中間態**(worktree 即隔離;知情推翻 08-05 Q2=A,因其前提「小線先上」在 §0.2 誠實邊界改述後已不成立——小線單獨上線效力≈0)。

**WOULD-CHANGE-MY-VERDICT**:若 apply 前出貨表已有非 0 資料,鎖窗數字族回到必答題、Q3=C 要重估 backfill 時序。
