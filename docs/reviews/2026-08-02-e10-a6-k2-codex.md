# 關卡2 R1 — A6 實作對抗審查(codex `gpt-5.6-sol` xhigh, read-only)

> 2026-08-02。審查對象 = 未 commit 的 A6 migration + `scripts/a6-verify.sh`(規格 = plan v4)。
> 判定 **NO-GO**,12 must-fix + 7 nit。主對話逐條親驗、**駁回 0、全折**。
> 跑前後 `git status --porcelain` 比對零留痕(審查者亦自報未修改工作樹)。
> 🔴 折入後 harness 由 145 → **157 PASS / 0 FAIL / 0 SKIP**,格 64 / 突變 73(三計數器釘死)。

NO-GO

[1] must-fix / migration:160 / dedup 只驗 action+request_id,未驗 target、payload 或 note_id 存活,卻回傳「已成功」。 / 利用既有 service_role audit INSERT 先寫同 request_id 的假稽核,再呼叫 RPC → DUPLICATE_REQUEST、零 note,A9d2-1 卻會當成功。

[2] must-fix / a6-verify.sh:501 / A12 刻意把同 request_id、不同 body 視為成功重送,直接固化 payload collision。 / 第一發 body「第一發」、第二發「第二發」→ 第二段內容從未入庫,呼叫端仍顯示成功。

[3] must-fix / migration:313 / has_table_privilege 不等於 zero-policy RLS 下真的可讀寫。 / audit owner 漂移、另給函式 owner SELECT+INSERT 時檔內驗收全過;非 BYPASSRLS owner 的 dedup SELECT 看不到列、audit INSERT 直接 42501。

[4] must-fix / a6-verify.sh:264 / F③/F④ 改驗「同 owner 或顯式 aclitem」,未驗 RLS 後的實際行為,本機 superuser fixture 正好掩蓋問題。

[5] must-fix / migration:293 / F⑨只驗 RLS on,漏驗 relforcerowsecurity=false 或 owner BYPASSRLS。 / 上游若 FORCE RLS、owner 非 BYPASSRLS,零 policy 狀態仍讓 migration 通過,但第一筆 note INSERT 失敗。

[6] must-fix / a6-verify.sh:1084 / D⑧的 13 個突變是自寫 attack 去確認洩漏存在,不是重跑原基準斷言證明其轉紅;且只突變 audit 寫入,未證 order_notes 恰 +0。

[7] must-fix / a6-verify.sh:454 / IN_FUTURE、BODY_TOO_LONG、DUPLICATE_REQUEST、ALREADY_CORRECTED 四案未斷言兩表恰 +0。 / 在這四條 RETURN 前偷偷 INSERT audit,原 A9/A11/A12/A14 仍全綠。

[8] must-fix / a6-verify.sh:326 / APPENDED 的「逐欄驗」不成立:note.occurred_at 只驗非 NULL,audit.after 完全沒驗 occurred_at 與 corrects_note_id。 / 把實際發生時間改寫成 now()、或刪掉兩個 audit key → harness 仍綠。

[9] must-fix / a6-verify.sh:355 / PII 最小化沒有守門:未驗 after 的精確 key 集合,也未驗 body 全文不存在。 / 在 jsonb_build_object 加入 body=p_body → 全綠,但直接違反 migration 與 COMMENT 的不記全文宣稱。

[10] must-fix / a6-verify.sh:25 / 只釘 EXPECTED_TOTAL,沒有獨立的格數與突變計數器。 / 移除一個 mutant、補一個無關 ok → 仍是同一總數,檔頭「釘死兩數」宣稱不成立。

[11] must-fix / a6-verify.sh:320 / 14 碼只做 COMMENT 包含檢查與 14 個固定向量,未驗函式 RETURN literal 集合恰等於 14 碼。 / 新增 body='magic' → RETURN 'SURPRISE' 的第 15 出口,既有向量與 COMMENT 仍全綠。

[12] must-fix / a6-verify.sh:656 / 步1僅測 actor 大寫,未測 actor/request_id 的 NULL、剝後空、過長與控制字元。 / 刪掉 request_id NULL guard → NULL 一路走到 audit NOT NULL raw 23502;現有 harness 無向量會抓。

[13] nit / a6-verify.sh:24 / SKIP 自初始化後從未遞增,H2 自檢操作的是另一個 _skip_probe。 / 刪除或寫壞最終 H2 gate 不會讓該自我突變轉紅。

[14] nit / a6-verify.sh:315 / G①/G② 用 grep 找字串,無法區分可執行 SQL 與註解。 / 改成 `-- SET LOCAL ...` 仍會通過存在性檢查。

[15] nit / a6-verify.sh:220 / fixture 先正式 COMMIT,腳本沒有 EXIT/INT/TERM cleanup trap。 / 中斷或前段失敗會殘留兩張 PCM-9998 訂單;零污染只對成功收尾成立。

[16] nit / migration:39 / 「無死結」說滿過頭,只證單次 RPC 與已知函式的鎖圖。 / 兩交易各在同一外層交易依 A→B 與 B→A 呼叫兩次 RPC,仍可形成 orders 列鎖死結。

[17] nit / migration:191 / DB COMMENT 未包含檔頭已承認的 runtime 無 lock_timeout、無雙連線 barrier 與 locale 邊界。 / 只看 introspection 的維運者會得到比檔案實況更強的安全印象。

[18] nit / migration:155 / now() 是交易起始時間,不是呼叫當下。 / 外層交易開超過 5 分鐘後以 clock_timestamp() 作 occurred_at 呼叫 → 真正的現在被誤判為 IN_FUTURE。

[19] nit / a6-verify.sh:1299 / §D 直接宣稱正式站 owner 非 superuser,但本輪權威資料只有本機 PG17 結果,沒有正式站 role 屬性 read-back。

## 查證未破(審查者列出的方法)

第 1-13 步實作順序(逐行比對 plan §4.2 與函式 149-187 行,順序一致)/ 現行 RETURN literal 恰 14 種且各一次
/ D1-D3 契約債接線 / SQL 注入與 audit 值注入(查無 EXECUTE 或動態識別字,全走型別值與 jsonb_build_object)
/ SECURITY DEFINER search_path(釘 public, pg_temp,查無 pg_temp 劫持點)/ 目前直接 ACL
/ audit 失敗原子性(audit INSERT 外無 handler)/ 同單並行鎖窗(orders FOR UPDATE 先於 dedup 與 corrects pre-check)
/ 既有跨函式鎖序(掃描全部 migration,查無先鎖 note/refund 再鎖 orders)/ occ 宣告錯誤處理 / 工作樹唯讀性。

## 折入摘要(主對話,全部已實作並重跑驗證)

- [1][2] → 步 11 改「查驗式冪等」:同 request_id 時必須 ①note_id 指向的備註活著且在本單 ②body_sha256 相符,才回 DUPLICATE_REQUEST;否則 **RAISE fail-loud**(偽造稽核 / request_id 重用皆不得謊報成功)。A12 改測四條路。
- [3][4][5] → migration 加 3f-4 / 3g-4 / 3g-5(order_notes 與 audit 的 FORCE RLS off、**函式 owner 必須等於 audit 表 owner**);harness F③/F④ 重定義為 owner 對齊與 FORCE off,突變改成「即使補齊表級 GRANT 仍轉紅」。
- [6] → D⑧ 13 格改成「重跑該碼原本的兩表 delta 謂詞並要求轉紅」,有合法單的 11 碼兩表各洩一筆、無效單的 2 碼只洩 audit。
- [7] → A9/A11/A12/A14 補兩表恰 +0。[8] → occurred_at 逐值比對 + audit.after 驗 occurred_at/corrects_note_id。
- [9] → after 鍵集合恰 7 鍵 + body 全文不得出現。[10] → 新增 CELL/MUT 計數器,收尾釘三數。
- [11] → 新增 E⑦(RETURN 字面集合恰 14 碼)+ 第 15 出口突變。[12] → 步 1 拒收矩陣補到 9 條。
- [13] → skip_gate 抽成函式,自檢與收尾共用同一份。[14] → grep 錨定行首 + 註解化突變。
- [15] → fixture 掛 EXIT/INT/TERM trap。[16][17][19] → 檔頭與 COMMENT 收斂宣稱、harness §D 補三條未驗事項。
- [18] → 步 7 改 `clock_timestamp()`。
