# A7b 關卡1 · **Fable**(換模型)· **R5** findings(逐字保存)

> 受審 = `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md` **v5**,凍結於 commit `299d910`。
> 執行 = 2026-07-31 下午。**唯讀**,零留痕。
> 🔴 **角度**:A = D9 是不是第三層 control-named-beyond-its-actual-power /
> B = v5 有沒有自捅第六次 / C = §7.5 那個「靜態可達性矩陣」是不是真機制 /
> D = §8 誠實邊界清單是否完整。
> 🔴🔴 **本輪結論直接影響 Sean 已答的 §14 Q4** —— 見文末「是否可繼續」。
> 🔴 逐字保存,不摘要、不裁定。裁定寫在 plan v6 §13。

---

**FAIL**

PCM pre-flight:branch=dev、受審檔凍結於 `299d910` 已驗;唯讀全程;slice=A7b(高風險片:錢+DB 結構)。以下行號皆指 `docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md`。

---

## 角度 A:D9 是不是第三層同一個病 —— **是,而且有具體漏洞,不只是哲學問題**

- **[must-fix] [853→§3.5 行243]** — 「D9 把靜默退第二次變成『在錢已入帳的情況下**必定被擋下**』」**沒有任何 DB 機制支撐**:D9a 只要求「填了」、D9b 只管時間、D9c 只比 gen2 與快照。**沒有一條 CHECK 把 `retry_auth_recorded_refunded` 與本列 `refunded_before`/`refunded_target` 比對**。失敗情境:錢在結案前入帳 → 人照 Record 誠實填入(含幽靈退款的)累計額 → D9a/b/c 全過 → gen2 baseline 一致 → 再退 → E9 正常 completed,**全程零告警**。實際擋點是「人看到數字變大要自己起疑」= 人,不是 DB。修法:加同列 CHECK `resolution≠'retry_authorized' OR retry_auth_recorded_refunded IS NOT DISTINCT FROM refunded_before`(E2b 前置保證 baseline 非 NULL 時 first 才可能非 NULL,成立);或把 243 行改寫成「人為唯一擋點」。**這就是第三層 control-named-beyond-its-actual-power。**
- **[must-fix] [行403-406 + 行112]** — **D9b 錨錯端點:風險錨是「最後一次」呼叫,閘門量的是「第一次」**。失敗情境(只用已承認的威脅=worker 逾時誤寫 E5,零新假設):第 1 輪 day0 明確失敗;第 4 輪 day3 逾時但 TapPay 已受理、worker 誤寫 E5;day3 進 dead → D9b 只要求 `checked_at ≥ day1`,**早已滿足** → 當天結案、當天 Record 查不到 day3 那筆(隔日才生效)→ gen2 當天再退 → **雙退,D9 三條全綠**。對照:E3b/E4 的 `next_check_at` 閘用的是 `refund_call_attempted_at`(=最後一次),兩處不一致本身就是證據。修法:新增 `last_refund_call_at`(每次 E2b 更新),D9b 改錨它。
- **[must-fix] [行406、131-132]** — `date_trunc('day', timestamptz)` 隨 **session TimeZone** 走。Supabase 預設 UTC:台北 07:00 首呼 → UTC 日界=台北 08:00 → 閘門**同一個台北營業日早上 8 點就開**,「隔日」名存實亡;且 CHECK 真值隨連線 TZ 而變 ⇒ harness 綠、正式站破(W-R16-6 假保證)。E3b/E4 的隔日基準同病。修法:明定 `(ts AT TIME ZONE 'Asia/Taipei')::date` 語意。
- **[must-fix] [行404-406、239]** — `retry_auth_checked_at` **無 `<= now()` 上界**,由呼叫者自填 ⇒ 填明天的時戳即可當天過 D9b。E13 trigger 一行可擋。
- **[must-fix] [行405、133、654-669]** — D9b 的 `OR first_refund_call_at IS NULL` 分支 **fail-open**,而「NULL ⇒ 沒發過 HTTP」成立的前提正是 D9 威脅模型裡不被信任的 worker 紀律(E2b 先 commit;E5 又不要求 `OLD.attempted_at IS NOT NULL`,六次「未呼叫失敗」全合法)。worker 跳過 E2b 直發 HTTP → 逾時受理 → first 恆 NULL → 閘門全程虛設。**§8 未列這條邊界。**
- 共模失效(prompt 所問):**確認成立** —— D9c 兩邊同源 Record API、且 F1 顯示同日查詢窗根本沒關;Record 錯兩邊一起錯、D9c 恆綠。§8 只認「隔日是否保證反映未實測」,沒認共模。折入上面各條,不另立。
- **結論:D9 的三條輸入全由人/worker 填、DB 無法驗真(§5.4 自認)⇒ D9 是稽核+收斂,不是防護**;§3.5:243 那句是**新的一句名過其實**。

## 角度 B:v5 有沒有自捅第六次 —— **truth-table 級沒有;測試規格級有**

15 條 edge 逐條重跑差集(起點形狀+edge 明文改動 vs 終點要求):**E1-E13 差集皆空**,v4 三死鎖(next_retry owner / E12 清 next_check / lease 錨點)確認已修。prompt 三題明確清掉:①E13 首寫 vs §5.2-4「一旦非 NULL」不可變 **無 D1 型死結**(首寫 NULL→非NULL 未被擋);②E2b 的 coalesce 在 OLD 非 NULL 時=要求 NEW=OLD,與不可變一致,靜態可;③新欄不在 truth table = 全狀態「−」,因它們跨狀態存活,**本身不是漏**——但見角度 C 的矩陣盲區。找到的自捅:

- **[must-fix] [行586、128、461]** — **正向鏈 C 照字面靜態不可能**:寫「`E1→E2→E2b→E5→E6→E1→…`連走 6 輪」,但 E2 要求 baseline「由 NULL 變非 NULL」且 `refunded_before` 一旦非 NULL 不可變 ⇒ **第 2 輪起 E2 必被拒**。harness 照抄必卡死;這正是 v3/v4 被抓的「靜態不可能的必做測試」同型。修法:明寫第 2 輪起跳過 E2。
- **[must-fix] [行585、403-406]** — **正向鏈 B 在 D9b 下同 session 無法構造**:E2b 把 `first_refund_call_at` 錨在 now(),隔日閘要求 checked_at ≥ 明天,加上 F5 修法(≤now())後**互斥** ⇒ B 要嘛做不出來、要嘛只能走 first=NULL 的 vacuous 分支=**假覆蓋**(測到的是 fail-open 分支)。plan 必須明定 harness 怎麼測 D9b 正向(如:僅 harness 的合法時間注入策略),否則 §7.4-B 與 §7.3「fixture 必經合法 edge」自相矛盾。
- **[must-fix] [行385-386、707]** — `processing.tappay_refund_id` 改「−」的理由(「與只有 E4 首寫**重複**且互相遮蔽」)**在 break-glass 情境不成立**:§12.1 的逃生路=`DISABLE TRIGGER` 後手改列,此時 D10(trigger)死了、truth-table CASE CHECK 還活著 —— 放寬等於把**唯一在 break-glass 期間還活著的那層**拆掉。失敗情境:break-glass 手改時誤植 tappay_refund_id 到 processing 列,現無任何東西擋。修法:恢復 N,並在 §7.2 依「被支配約束」紀律標結構字面驗證(R4 F7 已給過同款處理)。

## 角度 C:§7.5 是不是真機制 —— **一半是**

- rule 4(harness 從 §3.1+§4.4 產列形狀、逐 edge 實跑、跑不過中止)**可執行、是真機制**,且會抓到我上面 C 鏈那條。
- **[must-fix] [行641-643]** — 但 rules 1-3 是計畫期人工表,而 **v5 自己沒填**(模板空表「(15 條逐列填)」):v5 改了 E1/E5/E6/E12+truth table 三格,卻沒對自己跑自己宣稱的強制交付物 —— 這輪的差集是**我**替它跑的。= R2 #6「要求未來再列」同型。修法:15 列矩陣+反向兩問**填進 v6 本文**,不是等寫 SQL。
- **[must-fix] [行634、648]** — 矩陣**能力邊界未寫**:它只看得到 truth-table 欄位;`first_refund_call_at`/D9 兩欄/`refunded_before/target` 都不在表內 ⇒ 涉它們的死鎖(如 B 鏈不可構造)矩陣**看不到**;R4-F5 與本輪 A 角度全部五條(語意/時間類)也在它視野外。「讓第五次復發不要有第六次」又是一句名字大於能力 —— 須明寫「§7.5 只擋 truth-table 差集這一類」。

## 角度 D:§8 完整性 —— **至少四句未標邊界的宣稱**

§3.5:243「必定被擋下」(F1)、D9b「正是為了把那個窗關掉」(行244;F2 證明 round≥2 窗沒關)、checked_at 可信性(F4)、first=NULL fail-open(F5)—— 全不在 §8。另兩條小的:
- **[nit] [行347]** — U4 標「TapPay 端的冪等鍵」:它是**本地**唯一索引;TapPay 端行為 §8 自認未實測。同病小劑量,改「本地端的 bank_refund_id 唯一」。
- **[nit] [行583-594]** — 六條正向鏈無一走 **E3**;E13 另兩種 resolution 無正向測。§7.5 rule 4 會補到 E3,但 §7.4 應明列,免得兩節覆蓋標準不一致。

---

**我沒有能力驗證的部分**:①TapPay「隔日」的精確定義(日曆日/營業日/幾點結算)無官方逐字,F3 的修法選 `Asia/Taipei` 日界仍是推斷;②Record API 隔日是否保證反映(檔內自認未測);③master row 25 與本檔逐字同步未核;④R1/R2 共 53 條折入完整性未逐字抽查;⑤harness 三支 script 只當參照讀、未實跑(唯讀紀律)。

**WOULD-CHANGE-MY-VERDICT**:看到 v6 做到這五件事即改判 PASS-with-comments:①D9b 改錨最後一次呼叫時間 + `checked_at <= now()` + 日界明定 Asia/Taipei;②加 `recorded vs refunded_before` 同列 CHECK,或把 §3.5:243/244 兩句降級改寫並進 §8;③§7.5 的 15 列矩陣在 plan 本文填完 + 明寫能力邊界;④正向鏈 B/C 修成靜態可構造並明定 D9b 正向測法;⑤`processing.tappay_refund_id` 恢復 N 或明文認列 break-glass 層差。

**是否可繼續**:**需修正**;其中 D9 補強(F1/F2 改設計)與 §14 Q4 綁定 ⇒ **需 Sean 拍板**(建議 Q4 選項 A 的描述先按本輪 F1/F2 更正再問,現在的 A 選項描述仍含「對不上就自己停住」的過度宣稱)。
