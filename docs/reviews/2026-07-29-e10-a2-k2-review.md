# A2 關卡2 對抗審查紀錄(2026-07-29)

> 審查對象:`supabase/migrations/20260729020000_m4b_e10_a2_order_item_procurement.sql`
> 觸發依據:鐵則 12 ③(DB 結構)。片型 M ⇒ v2 §1「M/T/R 一律高風險」。
> 零留痕已驗:審查前後工作樹只有本片與 review 檔。

---

## 輪次

| 輪 | 審查者 | 結果 |
|---|---|---|
| **R1** | codex `-m gpt-5.6-sol`(`-s read-only`) | **FAIL 4 must-fix + 6 nit**,全部接受、**駁回 0 條** |
| **R2** | 🔴 codex **連兩次逾時**(9-10 分鐘無輸出)⇒ 依 R4「相同錯法第 2 次 = 換路」**不試第三次** | — |
| **R2′** | Fable(`adversarial-reviewer`, `model: fable`)接手確認輪 | **PASS**(R1 四條裁定:3 關閉 / 1 部分關閉)+ 4 條新 findings |

🔴 **換路不是加審**:codex R2 因工具問題無法完成,改由 Fable 做同一件事(確認 R1 四條是否真的關閉 + 攻擊新加物),不是開第二條平行審查線。

---

## R1 四條 must-fix(全數折入)

| # | finding | 修法 | Fable 裁定 |
|---|---|---|---|
| 1 | ACL 驗收只點名 anon/authenticated/service_role 三個 role,**沒查 `relacl` 的 grantee allowlist、沒查欄級 `attacl`** ⇒ production 的 default privileges 若把權限授給我們沒想到的 custom role,`REVOKE` 收不回、驗收照樣全綠、供應商資料外洩 | 改成 **allowlist**:`aclexplode(relacl)` 的 grantee 只准 owner 與 service_role、PUBLIC 零授權、所有欄位 `attacl` 必須 NULL | **關閉** |
| 2 | `ON DELETE CASCADE` 與「採購事實是真相」定位衝突 —— 刪一張單會讓供應商單號、到貨數量、異常原因**無聲消失**,而 D1c 排在本片**之前**、根本不需要靠 CASCADE 清 | 改 **`ON DELETE RESTRICT`**,驗收斷言同步改 | **關閉**(Fable 另證:第 3 批的改單與取消都不刪 `order_items`,RESTRICT 不會鎖死未來;要刪永遠可以先刪子列再刪 parent) |
| 3 | `supplier_order_no` 未正規化 ⇒ 存 `' SO-123 '`、用 `'SO-123'` 等值搜尋會漏單(A9b2 靠等值) | 加 CHECK 強制寫入端已去空白 + 索引改 `upper()` 函式索引 + 註解把 A9b2 搜尋語意定死為「對 `upper(trim(輸入))` 等值」 | **部分關閉 → 已補完**(見下 F1) |
| 4 | rollback 註解只列 A1/A4a,漏掉 A2b1/A5a/A9a/A9b2 等消費端 ⇒ 照著 DROP 會留下「查一張不存在的表」的 RPC 與 adapter | 補全 DB 層(A1/A2b1/A2b2/A4a/A4b/A5a)與應用層(A9a/A9b2/A5c/A9d1/A9h/A10b/A10c2)清單 | **關閉** |

---

## Fable 確認輪新增(4 條,全數折入)

| # | finding | 為什麼重要 | 修法 |
|---|---|---|---|
| **F1** | 🔴 **`btrim` 只剝 U+0020,剝不掉全形空白 U+3000 與 tab** —— 而中文輸入法打出全形空白極常見。R1 第 3 條的病灶因此**沒有真的關掉** | 單號結尾帶一個全形空白就能入庫,A9b2 用乾淨輸入等值查 = **照樣漏單** | CHECK 改成 `supplier_order_no ~ '^[^[:space:]]([^[:space:]]\|.*[^[:space:]])?$'` |
| F2 | 重複鍵探針裡的 `WHEN SQLSTATE 'PC001'` handler 不可達 = 死碼 | 後人抄樣板會複製一段沒有作用的錯誤處理 | 移除 |
| F3 | 誠實註解可再補:**table owner 同樣不受 RLS 擋**(本表刻意不加 `FORCE ROW LEVEL SECURITY`,加了會把 A5a 這支 owner RPC 自己弄斷) | 防止日後有人「好心」加 FORCE 而弄壞寫入路徑 | 註解補上 |
| F4 | allowlist 的天花板:**role 繼承與 `pg_read_all_data` 不在 `relacl` 裡**,表層斷言物理上看不到 | 免得後人以為這個斷言 = 完備的權限證明 | 註解寫明是叢集層治理、不是本片可解 |

---

## F1 的修法驗證(獨立實跑,不只是改字面)

在 production 用一張 scratch 表跑交易模擬(`BEGIN` → 驗 → `ROLLBACK`):

| 案例 | 期望 | 結果 |
|---|---|---|
| `SO-123` / `A` / `SO 123`(中間空白允許)/ `NULL` | 通過 | ✅ 4 列寫入 |
| `' SO-123 '`(半形前後空白) | 擋下 | ✅ |
| 🔴 `'SO-123' + U+3000`(全形空白結尾) | 擋下 | ✅ **舊 btrim 版會漏,新版擋住** |
| `U+3000 + 'SO-123'`(全形空白開頭) | 擋下 | ✅ |
| tab 結尾 / 換行結尾 | 擋下 | ✅ |
| 空字串 / 純半形空白 / 純全形空白 | 擋下 | ✅ |
| **對照斷言**:`btrim('SO-123' \|\| U+3000)` 是否仍等於原值 | 是(證明 btrim 剝不掉) | ✅ **F1 的前提成立、修法必要** |

---

## 本片實跑證據(主對話自跑,不採信審查者的測試宣稱)

| 驗證 | 結果 |
|---|---|
| production 交易模擬(`BEGIN` → 建表 + 索引 + ACL + 驗收 → `ROLLBACK`) | ✅ 通過 |
| 8 個 CHECK 約束 | ✅ |
| 行為探針 9 條 | 合法列可寫 / **全形空白單號被擋** / `received > allocated` 被擋 / `allocated = 0` 被擋 / 負數被擋 / 空白供應商名被擋 / 非法 `reply_status` 被擋 / 同品項同供應商重複被擋 / **同品項不同供應商可並存(1:N 成立)** |
| 負向探針精準度 | 全部核對 `CONSTRAINT_NAME`,擋錯約束不算過 |
| 零留痕 | 表不存在、orders 29、order_items 39,全部未變 |

🔴 **本片未 apply。** apply 由 Sean 早上執行,屬過夜禁區。

---

## 🟡 刻意不在本片解的合約債(已寫進 migration 註解)

1. **DB 尚未強制 `supplier_canonical_key` = A5b 函式對 `supplier_name` 的輸出** —— A5b(SQL 函式)排在本片之後,現在寫不了那條 CHECK。⇒ 唯一 writer A5a 必須「只收顯示值、key 由 SQL 內呼叫 A5b 產」,**列為 A5a 片的驗收條件**。
2. **只記到貨累計數,沒有 `received_at` / `received_by`、也沒有逐批到貨明細** —— 同一筆採購分兩批到貨時,回答不了「哪一批、什麼時候、誰登錄的」。規格 §5.1 A2 的欄位清單本來就沒這兩欄 ⇒ **不自行擴充範圍,列為決策題交 Sean**(見 STATUS 待決策 A2-1)。
