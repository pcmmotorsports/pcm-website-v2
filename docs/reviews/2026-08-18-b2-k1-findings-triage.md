# B2 關卡1(動手前審規格)· codex findings 逐條裁定

> **對象**:`docs/specs/2026-08-18-m4b-e8b-b2-spec.md`(B2 = 登入認人,報價單側)
> **跑的是誰**:codex CLI(`-m gpt-5.6-sol`、`-s read-only`),2026-08-18 · G5 窗,唯讀。
> **主視窗三條紀律(照做)**:①**關卡1 上限兩輪**,不收斂 ⇒ 整理決策題給 Sean,不繼續折衝
> ②**真權威的【內文】貼進 prompt**(母 plan 六段 + 本規格全文,約 15.8K 字元),不是給路徑
> ③**三個未解前提在 prompt 裡明說是未解**,並要求 finding 依賴它們時標 `[依賴未解前提]`
>
> **R1 結果:`VERDICT: FAIL`,16 條(14 must-fix + 2 nit)。全部折入,駁回 0。**
> 🔴 **本檔不是「已修」的證據** —— 證據是 B2 spec 的 diff。

---

## §1 codex R1 原文(逐字,未編輯)

`§3.3-b:103-125` + `middleware.ts:80-89`  
規格只要求關閉 `hasValidLegacy()`，但 middleware 也獨立接受 legacy cookie；照規格施工會讓舊 cookie 仍可讀取整個報價單後台。— must-fix

`§3.3-b:118-125`、`§4:173-174`  
驗收格未拆成 middleware 與 `requireAdmin()` 兩道守門；只測後者時，前者仍放行的壞世界也會綠。— must-fix

`§3.1:68-69` + `lib/session.ts:13-20` + `authorize/route.ts:49-62`  
B2 算出的 `sub` 被刻意丟棄，session 到 B3 前沒有任何身分；後續 `authorize` 無法知道該查哪一列首次改密碼狀態。— must-fix

`§3.4:134-144`  
「authorize 本來就要查映射表」與實碼不符；目前 authorize 只讀無身分的 session，照這句施工沒有可用的 `auth_user_id`。— must-fix

`§3.4:127-144`、`§4:170-172`  
規格沒有定義改密碼 API、如何授權只能改自己的 Auth user、成功後如何清除欄位；測試第 11 格引用了一條不存在的完成路徑。— must-fix

`§3.4:131` + `b1-spec.md:181-199`  
B1 先 seed、B2 再新增「NULL＝不必改」欄位，既有兩列會自然得到 NULL；若 migration 沒明文回填，所有初始密碼都直接免改。— must-fix

`§3.4:138-144`、`§4:170-172`  
規格只列登入頁與 SSO authorize，沒要求一般頁面、API、Server Action 的讀寫閘檢查首次改密碼狀態；員工可直接開 `/dashboard` 或呼叫後台 API 繞過。— must-fix

`§3.1:59-69` + `LoginForm.tsx:44-74`  
現行登入是多階段流程，但規格沒要求 TOTP、recovery、setup 每次都保留同一個 `account`；第二次請求漏帶時會説判成備援登入。— must-fix

`§6.1:212-213` + `lib/supabase-server.ts:22-24`  
[不確定] 若施工者仿照現有 service-role client 建全域 anon client，Supabase Auth session 可能跨請求留在記憶體，改密碼端點可能作用到上一位使用者。— must-fix

`§3.3:93-99`、`§4:168-169` + `login/route.ts:116-132`  
第 7 格只模擬 env 缺失，沒有模擬 env 齊全但 `signSession()` 回 null；實作若仍在該故障下發 legacy cookie，驗收仍可能全綠。— must-fix

`§3.3-b:120-123`  
報價單側開關沒有確定名稱、預設值與兩個執行點；施工者可能只接 `requireAdmin()`，或誤用 admin repo 的 `ADMIN_REQUIRE_REAL_IDENTITY`。— must-fix

`§3.1:59-65`  
`account` 的「留空」未定義；若非字串、空白字串或缺欄都被正規化成空值，壞掉的具名請求會靜默轉成備援分支。— must-fix

`§3.5:148-150`、`§6:197`  
[依賴未解前提③] 前置檢查只問 GoTrue 限速「數值」，沒問計數鍵、失敗嘗試範圍及是否真的 per-account；即使查到數字，也證明不了平台已接手帳號鎖定。— must-fix

`§5:180-184`  
規格宣稱 3.1／3.2／3.3／3.4 可分別上線，但 3.4 在 B3 前沒有持久身分可查；單獨部署不是完整且可驗證的「加東西不改行為」。— must-fix

`§7:243` + `§檔頭:7-20`  
誠實邊界仍宣稱全檔讀自 `482bec5`，與檔頭已複驗 `1149e05` 正面矛盾，後續引用者無法判斷應信哪個基線。— nit

`§5:183`  
同一句列出 3.1／3.2／3.3／3.4 四項，卻稱「這三步」，會讓切片或驗收少算一步。— nit

VERDICT: FAIL

---

## §2 逐條裁定(G5 開檔核實)

| # | 一句話 | 核實 | 裁定 |
|---|---|---|---|
| 1 | **`middleware.ts:80-89` 也獨立接受 legacy**,規格只寫了 `hasValidLegacy()` | ✅ **開檔核實**:`middleware.ts:82-90` 自己讀 `ADMIN_COOKIE`、自己比、直接 `next()`;matcher(`:95-99`)涵蓋除少數靜態資產外的**全部路徑** | **已折** — §3.3-b 改成**接受點 A/B 兩個**,並規定**兩處吃同一顆開關、開關讀取集中一個共用函式** |
| 2 | 驗收沒拆成兩道守門 | ✅ | **已折** — 第 12 格改成打 `requireAdmin()`,**新增 12b 打一般頁面**(走 middleware);13 改成兩條路各一次 |
| 3 | B2 算出 `sub` 卻丟棄 ⇒ 首次改密碼沒東西可查 | ✅ | **已折(改設計)** — 見第 4 條 |
| 4 | 「`authorize` 本來就要查映射表」**與實碼不符** | ✅ **開檔核實**:`authorize/route.ts` 全檔零 `admin_user_staff_map`,它只讀一個沒有身分的 session ⇒ **我拿一個不存在的既有查詢當理由** | **已折(整段重寫)** — 改成**擋在【發 session】那一刻**:要改密碼 ⇒ **不發 session、回 409**。⇒ 同時解掉第 3/5/7 條 |
| 5 | 沒有定義改密碼 API | ✅ | **已折** — 補 `POST /api/admin/password`(**不吃 session**、先驗現行密碼、改完清旗標、要進 `PUBLIC_PATHS`);**跨系統三步不是同一交易**的代價明寫 |
| 6 | B1 先 seed、B2 後加欄 ⇒ 既有兩列得到 NULL ⇒ **所有初始密碼免改** | ✅ 屬實 | **已折** — 加欄那支**同檔回填** + 落地斷言「零列仍是 NULL」+ **負向對照(插一列 NULL 必須紅)** |
| 7 | 只列登入頁與 `authorize` ⇒ `/dashboard`、API 可繞過 | ✅ | **已折** — 被第 4 條的改設計**消滅**:沒有 session 就過不了 `middleware.ts:56-92`,連 `authorize` 也到不了(它不在 `PUBLIC_PATHS`) |
| 8 | 多階段登入沒要求每段帶同一個 `account` | ✅ 屬實(`login/route.ts:20-24` 是單一端點多階段) | **已折** — 規定每段都要帶、漏帶或換人**一律拒(不得退回備援)**;驗收 14/15/16(16 是對照組) |
| 9 | `[不確定]` 照抄全域 client ⇒ GoTrue session 可能跨請求殘留 | 🟡 **未實測**,而修法便宜 | **已折(不賭)** — 硬規定:驗/改密碼的 client **每次請求現造** + `persistSession:false`,並寫明**這是兩件事,兩件都要做** |
| 10 | 第 7 格只模擬 env 缺失,沒模擬 `signSession()` 回 null | ✅ `issueSession:116-133` 的 legacy 分支在簽章失敗時同樣走得到 | **已折** — 新增 7b 格 |
| 11 | 報價單側開關沒有名稱/預設/執行點 | ✅ | **已折** — 定名 `QUOTE_REQUIRE_REAL_IDENTITY`(🔴 **不要用 admin 那顆的名字**)、預設關、正向字面 `=== '1'`、**恰兩個執行點 + 一個共用讀取函式** |
| 12 | `account`「留空」未定義 | ✅ | **已折** — 只有**缺欄**與**空字串**走備援;`' '` 走具名然後失敗;非字串 **400**。判別句:**把壞掉的具名請求變成成功的備援 = 把錯誤降級成權限狀態** |
| 13 | `[依賴未解前提③]` 只問限速「數值」證不到平台接手帳號鎖定 | ✅ | **已折** — 那一問改成**四件**(計數鍵/算哪些失敗/鎖多久/預設開不開);**若計數鍵是 per-IP ⇒ 甲案沒接手鎖定 ⇒ 回頭問 Sean,不是自己補一套** |
| 14 | §5 宣稱四段可分別上線,而 3.4 不行 | ✅ | **已折** — 逐段標可否單獨上線;3.4 **緊接在加欄 migration 之後**;並註明**改寫後 3.4 不再依賴 B3** |
| 15 `nit` | §7 仍寫「全檔讀自 `482bec5`」,與檔頭正面矛盾 | ✅ **矛盾的兩半在同一份檔裡** | **已折** — 劃掉改成 `1149e05`,§2 標題一併改 |
| 16 `nit` | 「這三步」而實際列了四項 | ✅ | **已折** — 改成【四段】,並寫明**數錯的代價是切片或驗收少算一段** |

---

## §3 這一輪最值錢的一條(給下一個人)

**第 4 條**:我為了論證「甲案比較划算」,寫了一句「**`authorize` 那條路本來就要查那張表**」——
**而那句話是我推的,不是查的。實碼裡那個查詢不存在。**
⇒ 📎 **形狀**:我把一個**尚未存在的東西**寫成**既有的**,然後拿它當「成本比較低」的理由。
⇒ **判別句**:**我寫「本來就會 / 既有的 / 順便」的時候,我開檔看過那個「本來」嗎?**

而第 4 條被折掉的方式也值得記:**正解不是把那個查詢補上去,是【換一個不需要它的攔截點】** ——
擋在「發 session」那一刻,四條 finding(3/4/5/7)一起消失。
📎 house 母題再演一次:**先找讓題目不成立的路。**


---

# §4 關卡1 **第二輪** · codex 原文(逐字)

> **本輪換三個角度**(不重跑 R1 的框架):①折入自己製造的新面 ②施工者視角 ③與相鄰片的接縫。
> prompt 另外開了一條:**「若你認為某一條折【折錯了】,明說並標 `[折錯]`」**。
> **結果:`VERDICT: FAIL`,9 must-fix + 3 nit,其中 3 條標 `[折錯]`。**

### 還沒解決

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:195`、`docs/specs/2026-08-16-m4b-e8b-b1b-migration-draft.sql:257`  
**[折錯]** B1 明文禁止 `service_role` UPDATE，改密碼成功後卻要求它清旗標；結果是密碼已換、旗標永遠清不掉，使用者持續收到 409。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:78`、`PCM報價單-V2/app/api/admin/login/route.ts:178`  
**[折錯]** 每階段都是獨立 HTTP 請求，現在沒有 challenge／暫存狀態可證第二段與第一段是同一帳號；驗收 15 無法可靠構造，「換人必拒」只剩前端自律。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:177`、`PCM報價單-V2/middleware.ts:28`  
**[折錯]** 不發 session 後只把 API 加進 `PUBLIC_PATHS`，卻未決定改密碼 UI 是登入頁內狀態還是另一條公開路徑；若施工者建獨立頁面，middleware 會把無 session 使用者擋回登入頁。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:152`  
旗標叫 `QUOTE_REQUIRE_REAL_IDENTITY`，範例函式卻叫 `legacyAcceptanceEnabled()`；施工者若讓它直接回傳 `=== '1'`，開啟「要求真身分」反而會開啟 legacy 接受。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:156`、`docs/specs/2026-08-18-m4b-e8b-b6-b7-spec.md:91`  
B7 最後只設定 `ADMIN_REQUIRE_REAL_IDENTITY=1`，整條線沒有設定 `QUOTE_REQUIRE_REAL_IDENTITY=1`；正式切換後報價單端仍接受 30 天 legacy cookie。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:212`、`docs/specs/2026-08-16-m4b-e8b-b1-spec.md:227`  
欄位未規定 `DEFAULT now()` 或新增人員必填；日後 migration 新增對應列若省略欄位，就得到 NULL，該新帳號首次登入直接免改密碼。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:192`、`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:279`  
驗收 20/21 沒明定「新密碼成功、舊密碼失敗、Auth 更新失敗不得清旗標」；只清旗標、完全沒改 Auth 密碼的壞實作，仍可用舊密碼讓第 21 格綠。`must-fix`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:204`、`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:254`  
新公開改密碼端點雖宣稱套用 §3.5，21 格驗收卻沒有任何重複失敗／429 格；施工者漏接既有 per-IP 限速仍會全綠。`must-fix`

`docs/specs/2026-08-16-m4b-e8b-b1-spec.md:181`、`docs/specs/2026-08-16-m4b-e8b-b3-spec.md:107`  
B1 已定為兩個帳號、兩列，但 B3 仍寫「B2 塞三列」，母 plan 也仍寫 seed 三人；下游施工者可能再開一個無主 `staff_1` 帳號。`must-fix`

### 可以留給施工時處理

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:223`  
「插一列 NULL 必須紅」未說是插在 migration 斷言前，還是 apply 後跑獨立 verifier；一次性斷言在 COMMIT 後不會自行重跑，施工時須釘清測試時點。`nit`

`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:286`  
仍寫「3.1／3.2／3.3／3.4 這三步」，實際是四段；第一輪第 16 條沒有完全消失。`nit`

`docs/specs/2026-08-18-m4b-e8b-b6-b7-spec.md:105`  
B7 誠實邊界仍稱「B2 今天不存在」，但 B2 已存在且完成第二輪審查；施工前更新即可，不影響本輪核心設計。`nit`

VERDICT: FAIL

---

# §5 R2 裁定 —— **7 條折入 / 2 條升成決策題(不折,送 Sean)**

> 🔴 **關卡1 上限兩輪(`00-work-rules §5`)⇒ 不開第三輪。**
> 機械性的修法照折;**真的取捨整理成決策題**,而不是繼續折衝。

| # | 一句話 | 核實 | 裁定 |
|---|---|---|---|
| **R2-1** `[折錯]` | B1-b 禁 `service_role` 寫入,而改密碼要清旗標 ⇒ **永遠清不掉、使用者永久 409** | ✅ **開檔核實**:`b1b:287` 只 `GRANT SELECT`、`:294` 斷言禁四種寫入 ⇒ **我今天親手造的兩半互斥** | 🔴 **升決策題 `D-1`**(B2 spec §8)——它動的是**權限設計**,不在施工窗裁量內 |
| **R2-2** `[折錯]` | 多階段登入每段是獨立請求,**server 端無法證明第二段與第一段同一人** ⇒ 我那條規則只剩前端自律 | ✅ 屬實 | 🔴 **升決策題 `D-2`** —— 它動的是**登入流程範圍** |
| **R2-3** `[折錯]` | 只決定了 API 進 `PUBLIC_PATHS`,**沒決定改密碼 UI 在哪** ⇒ 開新路由會被 middleware 導回登入頁 | ✅ `middleware.ts:28` `PUBLIC_PATHS` 只有三條、matcher 涵蓋幾乎全部路徑 | **已折** — 明定**改密碼是登入頁自己的一個狀態,不開新路由** ⇒ `PUBLIC_PATHS` 只需多那支 API |
| **R2-4** | 旗標 `QUOTE_REQUIRE_REAL_IDENTITY` 與範例函式 `legacyAcceptanceEnabled()` **語意相反** ⇒ 打開「要求真身分」反而打開「接受 legacy」 | ✅ 屬實,而且**錯的方向看起來完全正常** | **已折** — 硬規定函式名與旗標**同向**(`requireRealIdentity()`),**禁止任何語意相反的中間層**。📎 反向命名的 bug 不長得像 bug,長得像「開關沒生效」 |
| **R2-5** | **B7 只設 admin 那顆開關**,整條線沒有設報價單那顆 ⇒ 切換後報價單端仍接受 30 天 legacy | ✅ 跨檔核實(`b6-b7-spec:95`) | **已折(改的是 B6+B7 spec)** — 改成**兩顆開關、兩個專案,缺一整條線就沒收緊** |
| **R2-6** | 欄位沒規定 `DEFAULT now()` ⇒ 日後新增員工的 migration 漏寫該欄 ⇒ **新人免改密碼** | ✅ | **已折** — 加 `DEFAULT now()`,並寫明**`DEFAULT` 管未來的列、回填管現有的列,兩件都要做** |
| **R2-7** | 驗收 20/21 沒規定「舊密碼必須失敗」「Auth 更新失敗不得清旗標」⇒ **只清旗標沒改密碼的實作也會全綠** | ✅ | **已折** — 新增 21b(舊密碼必敗)、21c(更新失敗 ⇒ 旗標不得清) |
| **R2-8** | 改密碼端點是**未登入可達**,而 21 格裡**零限速格** | ✅ | **已折** — 新增 21d(連續打錯 ⇒ 429) |
| **R2-9** | B3 spec 與母 plan 仍寫「B2 塞三列 / seed 三人」 ⇒ 下游可能再開一個無主 `staff_1` | ✅ | **已折(改的是 B3 spec 與母 plan)** — 改成 `B1-c` / **兩列**,並標明**代號與列數兩處都錯** |
| **R2-n1** `nit` | 「插一列 NULL 必須紅」沒說在哪跑;`COMMIT` 後那道斷言不會重跑 | ✅ | **已折** — 明定在**交易內、斷言前**插,可直接掛進 `scripts/e8b-b1-probe/run.sh` |
| **R2-n2** `nit` | 仍寫「這三步」而實際四段 | ✅ **R1 第 16 條沒有完全消失** —— 我上一輪只改了別處那句 | **已折** — 📎 **「折了一處」與「折完了」是兩件事**,這是同一天第二次 |
| **R2-n3** `nit` | B6+B7 spec 仍寫「B2 今天不存在」 | ✅ | **已折** — 加一行過期標註 |

## §6 為什麼停在兩輪(而不是繼續折)

- **紀律**:`00-work-rules §5` 逐字「plan 層…不收斂 ⇒ 整理決策題給 Sean,而非繼續折衝」;主視窗本輪明令**上限兩輪**。
- **而且訊號也對得上**:R2 的 3 條 `[折錯]` **全部是 R1 修法造出來的**,其中 `D-1` 是**我自己上午的決定與下午的決定互斥** ——
  **那不是「規格還不夠細」,是兩個設計決定撞在一起** ⇒ **再折一輪只會再造一次同樣的東西。**
- 📎 母題(今天第三次):**折 finding 的動作本身會製造新的面。** 而這次它跨了兩份檔、隔了幾小時,**兩邊都是我。**
