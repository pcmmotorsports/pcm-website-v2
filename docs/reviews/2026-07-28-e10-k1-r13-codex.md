# codex 關卡1 R13 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `ff8fc59`,審查期間全 repo 零 commit。
> **判定 FAIL —— 7 must-fix + 0 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → 15 → 17 → 16 → 7 → 5 → 4 → **7**。
> 數字回升的原因**不是規格本體退步**(v2 本體只剩 3 條:計數器歸零合約、rail 閉環、D1 匯出 selector)
> —— 是 R13 第一次把「文件同步」掃到**相鄰兩份作用中規格**,抓到 4 條舊字面
> (主規格 §施工序 E11→E8→E10 / 自由品項「待驗證」/ N7-N9 仍標待決;UX 審查 §8 折入規則
> 仍引用已作廢的後綴決策)。
> D1 匯出 selector 是真硬傷:`payment_double_charge_anomaly_events` **沒有 order_id 欄**,
> 通用 `WHERE order_id = ANY(cohort)` 對它必然失敗,須經 anomalies JOIN 以 `old_order_id` 篩。

## codex 收斂判斷(逐字)

> 相對 R12,A7 雙表、既有 `bank_refund_id`、`reconciling` 永不回 `processing` 已解決;但 R12 的
> 計數器與付款管道仍未閉環,另有 D1 備份硬傷及作用中規格未同步。§5.0 DAG、§5.0b 矩陣與 62 片
> 統計一致(高風險 39 片),未見新的明顯超過 45 分鐘片;STATUS、handoff、HCT reference 亦無
> 新增衝突。已複核 `ff8fc59` 且工作樹乾淨,但目前仍不可施工。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:318:`check_fail_count` 未定義成功檢查後歸零，且遞增／歸零未明訂以當前 `claim_token` CAS 寫入，會把非連續失敗累積成 `dead` 或遭舊 worker 覆寫。成功但未達退款目標時同交易歸零；異常才以 token-CAS 原子加一並依新值判斷第 6 次。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:451:`rail` 仍取自 nullable `payment_method` distinct、只回填 `paid` 卡單且沒有 cash/none 合約，會漏掉 D1 後三張 `refunded` TapPay 單並把純現金／無收款誤判為混合。明訂 canonical rail、各管道 writer；TapPay 依交易事實跨付款狀態回填，cash 建腿、none 不建腿，只有多種實收管道才算混合。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:796:通用 `WHERE order_id = ANY(cohort)` 無法匯出只有 `anomaly_id` 的 `payment_double_charge_anomaly_events`，照規格製作備份會失敗。逐表寫死 selector；events 必經 anomalies JOIN，再以 `old_order_id` 篩 cohort，restore 與零殘留驗收使用相同關聯。
- [must-fix] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:36:作用中主規格仍宣告 `E11 → E8 → E10`，與已拍板的 E10 優先施工序衝突。原地標記舊排序作廢並換成目前拍板順序。
- [must-fix] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:441:主規格仍把自由品項視為待驗證，但 E10 C2 已實查 `create_order` 不可用。改成已確認不可用，並明訂第 3 批另建 admin 專用 RPC。
- [must-fix] docs/specs/2026-07-25-admin-backend-rebuild-spec.md:498:N7–N9 仍標示待決，分別與 U2「改全部」、P3「舊九狀態退場」、Q2「E12 本期不做」衝突，也破壞 §8.6 唯一待決清單。將三列改為已拍板並連回各自權威決定。
- [must-fix] docs/specs/2026-07-26-admin-ux-operability-review.md:104:直接折入規則仍要求 C1 後綴決策及自由品項驗證，與 Q19 作廢後綴及 C2 已確認不可用衝突。改成無後綴的 `shipment_reference` 合約與 admin 專用建單 RPC 前置。
**收斂判斷**：相對 R12，A7 雙表、既有 `bank_refund_id`、`reconciling` 永不回 `processing` 已解決；但 R12 的計數器與付款管道仍未閉環，另有 D1 備份硬傷及作用中規格未同步。§5.0 DAG、§5.0b 矩陣與 62 片統計一致（高風險 39 片），未見新的明顯超過 45 分鐘片；STATUS、handoff、HCT reference 亦無新增衝突。已複核 `ff8fc59` 且工作樹乾淨，但目前仍不可施工。
