# codex 關卡1 R11 — E10 施工規格 v2(2026-07-28)

> 模型 `gpt-5.6-sol`、`-s read-only`、零留痕已驗。受審 spec 凍結於 commit `711e1be`,審查期間全 repo 零 commit。
> **判定 FAIL —— 5 must-fix + 1 nit。** 趨勢:67 → 33 → 31 → 32 → 26 → 24 → 15 → 17 → 16 → 7 → **5**。
> 兩條是抓主對話的字面錯(begin_charge_attempt 最新定義在 `20260613140000:73` 非 130000 —— 差一小時的
> 後續 migration 補了 `existing_bank_transaction_id`,照舊基底施工會遺失它;port 實名
> `packages/ports/src/ITapPayAdapter.ts` 非呼叫點),皆已主對話親驗、codex 對。
> 另三條:reconciling 失敗路徑會繞回送款相重呼 Refund / `rec_trade_id NOT NULL` 與純匯款退款衝突
> (⇒ job 表定為卡軌專用、匯款走 UX §1 #3 線)/ D1t1 仍超鐵則 4。

## codex 收斂判斷(逐字)

> 相對 R10,強制兩次 read-back、STATUS 舊字面、DAG/矩陣/61 片算術均已一致;Sean 拍板、handoff
> 與 HCT 也未發現新衝突。但付款起點基準 migration、退款 reconcile 相位、純匯款分流、AbortSignal
> port 定位及 D1t1 切片仍有 5 個 must-fix,因此目前尚未達可施工狀態。

---

FAIL
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:326:`begin_charge_attempt` 最新定義誤標為 `20260613130000:350`，實際是 `20260613140000:73`，若照計畫施工會遺失 `existing_bank_transaction_id`。改以 0c 版本為基底，僅加入鎖與取消守門，並補回傳 bank transaction ID 的回歸測試。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:317:`reconciling` 遇 Record 異常會進 `failed→queued→processing`，重試仍可能再次呼叫 Refund。reconcile 失敗應回 `submitted` 延後或使用獨立 `reconcile_failed`，並測試 Record 異常與 lease reclaim 均不得重送退款。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:317:`rec_trade_id NOT NULL` 與第 3 批純匯款退款衝突，而 A8b 又規定所有 `paid` 一律建立 TapPay job。應依 server 權威付款管道分流，為純匯款定義獨立退款工作流；只有卡片退款才能進此表。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:300:`settle-charge.ts:77` 是呼叫點，不是 TapPay port 介面，R10 要求的型別修改仍可能漏做。應具名指定 `packages/ports/src/ITapPayAdapter.ts:19-43` 的 `recordQuery` 簽章，再同步 adapter、callers 與 mocks。
- [must-fix] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:309:D1t1 仍同片承擔完整 orchestrator、交易／鎖／API rollback、CLI dry-run 與隔離 DB 單元測，明顯超過 45 分鐘。至少再拆「交易執行核心」與「CLI/dry-run 組裝測試」，並重算總片數。
- [nit] docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:733:標題仍寫「R9 拆十二片」，但同段已列入 R10 新增的 D1t2、現況共 13 片。改寫為「R9 12 片、R10 13 片」避免施工者誤讀。
**收斂判斷**：相對 R10，強制兩次 read-back、STATUS 舊字面、DAG／矩陣／61 片算術均已一致；Sean 拍板、handoff 與 HCT 也未發現新衝突。但付款起點基準 migration、退款 reconcile 相位、純匯款分流、AbortSignal port 定位及 D1t1 切片仍有 5 個 must-fix，因此目前尚未達可施工狀態。審查期間維持 `711e1be`、工作樹乾淨，未修改或提交任何檔案。
