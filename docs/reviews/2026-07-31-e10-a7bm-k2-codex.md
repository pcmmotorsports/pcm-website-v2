# A7b-M 關卡2 · codex `gpt-5.6-sol` xhigh(commit 前對抗審查)findings(逐字保存)

> 受審 = `supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql`
> (codex 自報指紋 `fc63f290…d2585`;plan v7 `87dbba55…474e`)
> 執行 = 2026-07-31 12:00-12:14。**唯讀**。
> 🔴 **凍結紀律成立**:受審 migration 的 mtime `11:57:54` **早於** codex 起跑 `12:00:35`,
> 審查期間未被修改(期間我改的是 plan §1.1 / CURRENT.md / 交接檔 / verify 腳本,皆非受審對象)。
> 🔴 鐵則 12(①錢 + ③DB 結構)⇒ **findings 修完才 commit**。
> 🔴 逐字保存,不摘要、不裁定。

---

FAIL

受審指紋：migration `fc63f290…d2585`；plan v7 `87dbba55…474e`。

### 1. 檔案本體 vs v7

五道唯一性、七支 FK、六個查詢索引、ACL 與七態 truth table 均有落地。七條 per-status CHECK 由 `status NOT NULL`＋七值 allowlist 保證窮盡，逐格未見遺漏；拆開比單一 CASE 更利於定位，偏離站得住。

[nit] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:57`] 前段仍宣稱 gate 是 `CHECK(false) NOT VALID`，與後段及目前 v7 的 validated CHECK 相反。
失敗情境：read-back 或接手者依前段檢查 `convalidated=false`，會誤判正確 schema。
修法：刪掉過時的 `NOT VALID` 字面，只保留第 411–421 行的更正。

[nit] [`docs/specs/2026-07-31-e10-a7b-refund-jobs-plan.md:328`] v7 要求把既有 `payment_charge_attempts.rec_trade_id` 字元 read-back 寫進 COMMENT，本 migration 沒有該觀察紀錄。
失敗情境：未來無法區分 ID CHECK 是依正式資料觀察還是純推論。
修法：補具體但不含客戶值的觀察摘要，或從 v7 刪除這項交付要求。

### 2. CHECK 正確性

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:114,274,394`] `btrim(x) <> ''` 只去除一般空格，TAB、換行、NBSP、全形空白組成的「視覺空字串」仍會通過。
失敗情境：`correction_reason=E'\t'`、`failed_reason=E'\n'` 或空白退款原因都可留下不可讀稽核紀錄。
修法：用明列碼位的可見字元規則，不用 locale-dependent POSIX 字元類。

[nit] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:259,464`] 兩個全域計數器範圍被七條狀態 CHECK 嚴格支配；子表 `quantity>0` 與 `unit_price>=0` 也無法逐支獨立負測。
失敗情境：單獨刪除任一全域範圍或任一子表符號 CHECK，其他約束仍會拒絕同一負例，突變測試可能假稱已覆蓋。
修法：移除冗餘約束，或明標結構／成對突變，不宣稱逐支獨立行為證明。

其餘 CHECK 未見恆真、恆假或 NULL 放行；ID 規則未使用 POSIX 字元類，亦無普通 SQL 字串中的 `%%` 問題。

### 3. 錢面四條與 D7／D9

D7 只限制 `dead_reason`；D9a 強制留下成對證據；D9b 只擋同一台北日；D9d 只比較本代兩個本地快照。它們都不證明 TapPay 回覆、Record 值或 worker 行為為真。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:513`] COMMENT 稱 D9c/D9d 是「真正擋錢動過」，但 E2 取樣完成後到 E2b／HTTP 之間仍有外部退款 TOCTOU 窗。
失敗情境：gen2 E2 讀到 0 並通過 D9c，接著有人在 portal 退款，再由 worker 送出新 `bank_refund_id`；所有 D7/D9 仍綠而可能雙退。
修法：降級宣稱並把此窗列入 §8／第 3 批 blocker；真正關閉需禁止 portal 並發或具外部原子 CAS 的協定。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:517`] D7 COMMENT 首句仍把 `retry_exhausted` 寫成「六次都收到明確失敗」，下一句才承認 DB 完全證明不了。
失敗情境：後續 worker／RPC 把 constraint 名與 COMMENT 當成已驗證外部結果，再次形成名字大於實力。
修法：首句改成「只有 `dead_reason='retry_exhausted'` 才准」，外部六次失敗只列為呼叫者責任。

### 4. ACL 與 RLS

目前 PUBLIC 確實排第一；PG17 八種表權限含 `MAINTAIN`；service_role 權限集合符合 v7；anon/authenticated 為零權限，RLS 已啟用且 zero-policy。PG17 權限集合可見[官方 GRANT 文件](https://www.postgresql.org/docs/17/sql-grant.html)；Supabase service role 會繞過 RLS，因此精準 GRANT 才是真正能力邊界，[官方 RLS 文件](https://supabase.com/docs/guides/database/postgres/row-level-security)。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:746`] 所謂「完整 ACL allowlist」只查四個角色的有效 table privilege，未查任意其他 grantee、欄級 ACL 或 `WITH GRANT OPTION`。
失敗情境：對子表授予 service_role 欄級 UPDATE，或授權另一個 BYPASSRLS 角色，八格矩陣仍可全綠。
修法：以 `aclexplode(relacl)`＋`attacl` 比對完整 grantee/privilege/grant-option 集合。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:504`] TABLE COMMENT 稱「寫入只走 owner RPC」，但第 498–500 行明確允許 service_role 直接 INSERT/UPDATE。
失敗情境：後續人員相信 COMMENT，誤以為 direct write 不可達，漏掉 worker 的 CAS／稽核要求。
修法：依真正設計二選一：收回直寫 GRANT，或把 COMMENT 改成 service_role 可依 T 合約直寫。

### 5. 結構驗收 DO block 假綠

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:550`] 「42／7 欄」不是完整 manifest；多數型別及 `generation/retry_count/check_fail_count/manual_review_required` 預設值未驗。
失敗情境：把 `refund_amount` 改成 bigint、`retry_count` 預設改 1，欄數與現有斷言仍全綠。
修法：以 expected `VALUES` manifest 雙向比對每欄 name/type/notnull/default，禁止只數總數。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:581`] 預期欄不存在時 scalar subquery 回 NULL，`IF NOT(NULL)`／`IF NULL <> type` 不會進入 RAISE。
失敗情境：刪除預期 nullable 欄、補一個 dummy 欄維持 42 欄，再同步改相關 CHECK，驗收仍可能全綠。
修法：先逐欄斷言恰一列，再以 `IS DISTINCT FROM` 比對屬性。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:696`] D7、D9a、D9d 與七條 shape 只驗「同名 CHECK 存在」；D9b 也只搜尋兩個字串。
失敗情境：把 D9d 改成 `CHECK(true)`，或 D9b 改成保留兩個關鍵字的 `true OR ...`，DO block 全綠。
修法：逐支比對正規化 `pg_get_constraintdef` 或固定指紋，不接受名稱／substring 代理語意。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:668`] U1、U4 與 helper unique 只驗名稱、種類及 validated，未驗欄組。
失敗情境：U1 改成 `(order_id,generation)` 或 U4 改成 `UNIQUE(payload_hash)`，第二次退款防線消失而 DO 全綠。
修法：逐支比對 `conkey` 對應欄名或完整 constraint definition。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:637`] 七支 FK 只精確驗兩支，其餘只數量與 `RESTRICT`。
失敗情境：`orji_job_fk` 改指 `order_refunds(id,order_id)`，或 reviewed_by FK 重複綁 actor，仍是 7 支 RESTRICT 且全綠。
修法：七支逐一比對來源欄、目標表、目標欄、validated 與 referential action。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:523`] 子表列內 CHECK／unique、§11 六個索引及「零新函式」均無結構斷言。
失敗情境：刪 `orji_line_balances`、`orji_job_item_key`、任一 due index，甚至加入額外函式，DO block 仍全綠。
修法：把子表約束、索引完整定義及 M 型物件集合納入 manifest。

### 6. 鎖與 apply 風險

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:76`] 鎖型判斷正確，但 5s/60s 不會限制整個交易的持鎖時間。
失敗情境：FK 對四張被引用表取 SHARE ROW EXCLUSIVE；`order_items` INSERT 的 ROW EXCLUSIVE 會被擋，且 lock timeout 逐次、statement timeout 逐 statement，取得鎖後會一路持有到 COMMIT。
修法：加 PG17 `transaction_timeout` 硬上限，或把 live-table FK 移到交易尾端／拆成 `NOT VALID`＋`VALIDATE`；官方依據見[鎖衝突表](https://www.postgresql.org/docs/17/explicit-locking.html)、[FK 鎖](https://www.postgresql.org/docs/17/sql-altertable.html)、[timeout 語意](https://www.postgresql.org/docs/17/runtime-config-client.html)。

### 7. rollback

child-first 與兩個 `DROP TABLE ... RESTRICT` 順序正確。

[must-fix] [`supabase/migrations/20260731120000_m4b_e10_a7b_m_refund_jobs.sql:812`] `pg_depend` preflight 不可執行，且寫出的判定方向相反。
失敗情境：`refobjid NOT IN 本片集合` 會抓本片 FK 對外的正常相依關係，卻漏掉「外部 view/constraint 依賴本片表」；`pg_depend` 的 `objid` 才是 dependent、`refobjid` 是 referenced。
修法：提供實際 SQL，條件應從 `refobjid IN 待刪物件` 找 `objid` 在白名單外的 dependent，並在隔離庫實跑；欄位語意見[PG17 pg_depend](https://www.postgresql.org/docs/17/catalog-pg-depend.html)。

### 我沒有能力驗證的部分

- 未重跑本機 apply／新增的 `a7bm-verify.sh`，也未連正式站；依你的已知事實接受本機 PG17.10 結果。
- 未驗正式站實際角色 membership、欄級 ACL、locale、當下流量與真實持鎖時間。
- 未驗 TapPay 同 `bank_refund_id` 冪等性、Record 隔日反映或 portal 競態。
- A7b-T 尚未受審，無法證明十支 trigger 會關閉本片刻意留下的行為面。
- 全程唯讀，未修改、stage、commit、push 或 apply 任何內容。