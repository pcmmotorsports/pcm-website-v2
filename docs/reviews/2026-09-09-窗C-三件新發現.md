# 窗 C · 2026-09-09 傍晚三件新發現(全部唯讀量到)

> 線【權限/信件】窗 C · `pcm_readonly` @ 正式庫唯讀 + repo grep · **零寫入、零寄信、沒改板。**
> 這三件都是**在做別的事時撞到的**,而每一件都推翻或改變了一個板列的現況。

---

## ① 🎯 `⟦db-ORDERDELETENOTRACE⟧` —— **它在等 Sean 答的那個「甲」,已經做掉了**

板列(`docs/launch-todo.md:2488`)在等 **Sean Q35**:`甲 = 上線前補 DELETE trigger 抄進留痕表` / `乙 = 不補`(主視窗推甲)。
板列 2026-09-07 的讀數逐字:「`orders` 上的 trigger **共 4 支,全部是 INSERT / UPDATE OF …** ⇒ 🔴 **沒有任何 DELETE trigger** ⇒ 刪單不會留下任何東西。」

**2026-09-09 唯讀實測 —— 今天 5 支,而第 5 支就是它**:
```
pcm_orders_delete_audit_ad
  CREATE TRIGGER pcm_orders_delete_audit_ad AFTER DELETE ON public.orders
    FOR EACH ROW EXECUTE FUNCTION pcm_log_order_row_delete()
```
**而配套的東西也都在**:
```
public.orders_deleted_log   relkind=r · RLS=t · policies=1
pcm_log_order_row_delete    prosecdef=t · owner=postgres · prosrc 含 'orders_deleted_log' ⇒ t
orders_deleted_log 現有列數 ⇒ 0
⚪ 負對照 現造表名 ⇒ 0
```
🔵 **來源**:`supabase/migrations/20260907070000_m4b_orders_delete_audit_trail.sql`(`:104` 建表 · `:255` 建 policy),`APPLIED.tsv` 記 **2026-09-08 貼入**。
⇒ 🎯 **所以那一題不用問了 —— 甲已經落地。** 板上那一列還掛著 ⟨擋·決策⟩ 等 Sean 一個字。

🛑 **而我證不到什麼**:
- **那個 trigger 有沒有真的抓到過一次刪除** —— `orders_deleted_log` **0 列** ⇒ 貼下去之後**沒有人刪過訂單**。⇒ 📌 **「它在」與「它會動」是兩件事**,而後者今天零讀數。
- **它會不會漏**(例如 `TRUNCATE` 不觸發 `AFTER DELETE`、或 `DELETE` 走到別的路徑)—— **沒查。**
- 那支函式的**完整定義**我沒讀,只量到 `prosrc` 含那張表的名字。

---

## ② 🔴 `⟦db-SAMETRIGGERNAME⟧` —— 前提不成立,而真相**比板列寫的更奇怪**

板列(`:2512`)逐字:「**兩支【還沒貼】的 migration** 建同一個 trigger 名、同一張表、同一個事件 —— 後貼的覆蓋前一支」。

**2026-09-09 用 `APPLIED.tsv`(不用 commit 時間 —— 今天踩過那個坑)**:
```
20260901021000  ⇒ 帳本記 2026-09-02 · 落款逐字「Sean(SQL Editor 本人貼, 回「Success. No rows…」
20260901030000  ⇒ 🔵 不在帳本 ⇒ 未貼
🟢 正對照 20260908110000 ⇒ 帳本記 2026-09-08     ⚪ 負對照 現造版號 ⇒ 0
```
⇒ 🛑 **「兩支都還沒貼」不成立 —— 其中一支 09-02 就貼了。**

**而線上量到的東西更奇怪**:
```
public.orders 上的 trigger 共 5 支,而 trg_coupon_redeem_on_paid ⇒ 【0】(不存在)
public.coupon_redeem_on_paid 這支函式        ⇒ 【0 列】(也不存在)
⚪ 負對照 現造 trigger 名 ⇒ 0
```
⇒ 🔴🔴 **帳本說那支 migration 09-02 貼了,而它該建的 trigger 與函式【線上都沒有】。**

**這代表什麼,我列出可能而不選邊**(我沒有證據挑其中一個):
1. 建了之後被別的 migration 刪掉 / 取代
2. 那支 migration 的建立動作包在條件裡而條件沒過(它回的是「Success. **No rows**」)
3. 帳本記錯

⇒ 🎯 **而不論是哪一種,它都是 `⟦db-ACLVALUEPROVENANCE⟧` 那一列在講的事的一個實例**:
📌 **`APPLIED.tsv` 是一本【自陳帳】——「帳上寫著 apply 了」與「正式庫裡真的有那個東西」是兩個宣稱。**
(`scripts/latest-definition-of.sh` 自己的射程說明也逐字寫過同一句。)

🛑 **我證不到什麼**:
- 上面三種可能我**一個都沒排除** —— 要排除得讀那支 migration 全文 + 掃有沒有別的檔 `DROP` 它。**沒做。**
- **而板列的擔憂反而變得更具體**:第二支 `20260901030000` 還沒貼,**而它貼下去的時候線上沒有同名 trigger 可撞** ⇒ 📌 **「撞名」那個風險今天不成立,而【第一支到底有沒有生效】是一個新的、更該問的問題。**

---

## ③ 🔵 `⟦mail-TXNMAILSPAM⟧` 補一格:交易信的寄件位址,repo 裡**沒有字面**

主視窗要我確認交易信的 From 是不是 `send.*`(對照那筆「忘記密碼信進收件匣」的觀察)。
**實測**:
```
composition.ts:88            from: requireEnv('ORDER_EMAIL_FROM')
ResendEmailSenderAdapter:421 from: this.cfg.from          ← 原封不動交給 Resend
全樹 grep 'send.pcmmotorsports' / 'no-reply@' / 'noreply@' ⇒ 0 命中(排除測試)
```
⇒ 🛑 **交易信的 From 完全來自 `ORDER_EMAIL_FROM` 這顆 env,而 repo 裡零字面、零格式驗證** ⇒ **施工窗看不到它的值。**
⇒ ✅ **所以「兩條路的寄件位址不同」這句我【證不到】** —— 我只能證「repo 裡查不到」。**要 Sean 印那顆 env 的網域那一段**(不用全值)。
🔵 而 `send.pcmmotorsports.com` 是 **Return-Path** 那個網域(SPF `include:amazonses.com`),**它與 From 不一定是同一個** —— DMARC 的 relaxed 對齊允許它們是主網域的不同子網域。

🔵 **順帶一個真的發現(只記,不建議動)**:`_dmarc.pcmmotorsports.com` 今天是 **`v=DMARC1; p=none;`** ⇒ **只監控、不執行**。
🛑 **不建議改成 `quarantine` / `reject`** —— 那會影響真實寄信,是 Sean 的決定,而且**要有 DMARC 報告的資料才判得動**(而 `p=none` 那筆連 `rua=` 都沒有 ⇒ 📌 **今天連報告都沒有人在收**)。

---

## 落點建議(窗 C 沒改板)
1. **① 端 Sean 的那題 Q35 可以撤** —— 甲已經做掉了(而「它會不會動」零讀數,要另外講)。
2. **② 板列的標題要改** —— 不是「兩支還沒貼」,而是「一支貼了而它建的東西不在線上」。
3. **③ 要 Sean 印 `ORDER_EMAIL_FROM` 的網域那一段**,那一格才關得掉。
