# B2-S2 plan 關卡1 對抗審查 R1(codex,`-s read-only`)

> 2026-08-06 夜跑窗。受審 = `docs/specs/2026-08-06-e10-b2-s2-shipped-summary-plan.md` v1。
> 引擎 = codex CLI(`codex exec -s read-only`),唯讀、agent 自行翻 repo。
> **零留痕已驗**:跑前後 `git status --porcelain` 逐字相同(僅三個既有未追蹤檔)。
> 原始輸出 = `/tmp/codex-k1-out.txt`(795KB,含完整翻檔軌跡;非 repo 檔、會隨機器清掉)。
> 攻擊角度依 plan §10 指定的五面 + 自由發揮;codex 自述已「核對至少 5 處行號引用」。

## 判定

**FAIL / 25 must-fix + 2 nit。**

## 逐條(原文,未壓縮)

| # | 級 | 標題 | 位置 | 失敗情境 | 修法 |
|---|---|---|---|---|---|
| 1 | must-fix | a1 restore 在回滾世界會假綠 | plan:339 | down migration 還原三軸 helper、移除 S2 trigger 後,a1 只重套 S2a,舊 helper 仍可成功,卻留下「六欄、零 shipped 重算」斷裂態 | 改用版本感知的 schema 快照還原或獨立拋棄庫 |
| 2 | must-fix | 活體斷言只驗沒噴錯 | plan:213 | helper 被改成空函式或 `RETURN`,直呼仍成功,項 20/24 全綠但摘要未落值 | 造已知四軸 fixture,斷言 psql rc 並回查四個新值及完整 S2a 指紋 |
| 3 | must-fix | U/D「不可達」是錯的 | plan:152 | 拋棄庫只停用 `shipment_items_block_update_bu`／`_delete_bd`,保留新重算 trigger,今天就能構造 UPDATE/DELETE | 現在補兩枝負測與事件消融,不得遞延 |
| 4 | must-fix | U/D 防禦枝沒有 OLD/NEW 語意 | plan:133 | 未來把 `order_item_id` 從 A 改 B,若只重算 NEW,A 的 shipped 永久殘留 | 明訂排序去重後同時重算 OLD、NEW 的 order_item_id,並各配 re-parent/delete 測試 |
| 5 | must-fix | 「兩支都不可少」是過度宣稱 | plan:136 | receipts 已建立摘要列時,拿掉 `shipment_items` 重算 trigger,`shipments` UPDATE 仍會把 shipped 寫對,項 10 不會紅 | 將前者誠實標成未來 U/D 防禦,或補出它今天唯一承擔的可觀察契約 |
| 6 | must-fix | Trigger 結構閘量不完整 | plan:201 | 同名同 tgtype 但指錯函式、非 constraint trigger、或 `UPDATE OF` 變成任意 UPDATE 都可通過 | 同驗 `tgfoid/tgconstraint/tgdeferrable/tginitdeferred/tgattr` 與完整 `pg_get_triggerdef` |
| 7 | must-fix | 新 trigger 函式安全面無驗收 | plan:120 | 兩支新函式可漏 `search_path=''`、保留 PUBLIC EXECUTE 或 owner 錯誤而矩陣全綠 | 把兩函式納入 owner、secdef、proconfig、ACL、函式本體及 trigger 指向的全等斷言 |
| 8 | must-fix | shipments 多品項重算缺固定鎖序 | plan:169 | 兩張含重疊品項的包裹併發出貨,未排序的 DISTINCT 迭代可反序取得多把 order_item 鎖而 40P01 | trigger 內明訂 `ORDER BY order_item_id`,並加雙 session barrier 格 |
| 9 | must-fix | C8 沒有「只紅它」負測 | plan:185 | `shipped=-1, instock=0, cancelled=0` 只違反 C8,現矩陣卻完全沒測 | 補 SQLSTATE+conname 負測、零值正測與移除 C8 的消融靶 |
| 10 | must-fix | 項 18 量不到宣稱的 rollback | plan:211 | 有品項時雖可觀察重算先發生,但 X1 最後會通過;這不能證明「X1 紅時重算一併蒸發」 | 構造重算後再令 X1 於 COMMIT 失敗,並於交易外回查兩邊都回滾 |
| 11 | must-fix | 突變矩陣不可能每靶只紅一格 | plan:229 | 拿掉 trigger 會同時紅項 8 與行為格;靶④甚至不會紅項 10 | 分開結構突變與行為突變環境,逐靶寫明唯一執行的 oracle |
| 12 | must-fix | PR4 引用的造洞法會被舊 oracle 抓到 | plan:220 | 上游 PR4 用 DELETE receipt 降 instock,舊三軸 oracle 本來就會因 instock drift RAISE | 改成只改 shipped 真相、不動前三軸的 fixture,再證明三軸通過、四軸失敗 |
| 13 | must-fix | 「真相式全 repo 只出現一次」與獨立 oracle 衝突 | plan:61 | helper 回傳 void,runbook／漂移 oracle 不可能靠它獨立比對 shipped,勢必需要另一份查詢 | 明列允許的三個位置、逐字錨與消融,取消「只一次」宣稱 |
| 14 | must-fix | Rollback runbook 只改 oracle 會留下活地雷 | plan:219 | 現行 runbook 只枚舉四 trigger／五函式;S2 後仍會漏兩支新 trigger 與兩函式,撤 helper 後下一筆 DML 才爆 | 同片更新完整依賴清單、DROP 序、計數與 rollback rehearsal |
| 15 | must-fix | a4a harness 收尾仍只守五函式 | plan:223 | 新兩函式被竄改或殘留時,現行 md5 收尾仍可全綠 | 將七函式、六 trigger、四軸 oracle 與候選全集納入基準及零殘留閘 |
| 16 | must-fix | md5 pin 範圍與維護者未定 | plan:114 | 若只 hash `prosrc`,`search_path` 等定義漂移不會被發現,OR REPLACE 可靜默覆蓋;forward/down 新基準也無人更新 | pin canonical function definition 加必要 catalog tuple,並在 forward、rollback、未來替換契約中各凍結指紋 |
| 17 | must-fix | md5 負測可因普通重跑而假證明 | plan:199 | 已套 S2b 後再「重跑」,即使零突變也會因期待舊指紋而拒絕 | 在 A4a+S2a、尚未套 S2b 的雙對照庫,用合法註解字元突變證明只有 mutant 被 gate 拒絕 |
| 18 | must-fix | S2a 成功／S2b 失敗的「安全」尚未證實 | plan:254 | 若正式站已有 shipped 真相,gap 內摘要仍全為 0;目前只有舊零 writer 論證,沒有即時真相 gate 或故障注入 | apply 前查三表真相為零,並在 S2b 後段注入失敗驗 helper、trigger、摘要皆回復原狀 |
| 19 | must-fix | 防禦性 backfill 沒有值的 oracle | plan:162 | helper 漏算或漏一個 order_item 時,NOTICE 仍只會印「處理 N 列」而 migration 成功 | backfill 後以獨立四軸公式檢查值漂移與活動品項缺列 |
| 20 | must-fix | PR1 fixture 無法保證 instock=2 | plan:207 | 「到貨 3、刪一筆 receipt」若只有一筆 quantity=3,結果是 instock=0,不是項 15 宣稱的 2 | 明訂 receipts 為 2+1 並刪 quantity=1 那筆 |
| 21 | must-fix | 契約債沒有全樹結清 | `packages/domain/src/order/types.ts:530` | 此處與 `2026-08-05-e10-cancel-ui-wire-plan.md:284` 仍命令未來公式減 shipped,master plan 也仍寫「尚未定案」 | S2 增加全樹 grep 清單,逐處改成 Q1=A/Q2=A 的最終字面 |
| 22 | must-fix | DB COMMENT 清償範圍不足 | plan:70 | A1 FK COMMENT 仍寫「三個數量」、A4a helper COMMENT 仍寫「三張真相表／四支 trigger」 | S2a/S2b 明列並驗收所有受影響 COMMENT 的新字面 |
| 23 | must-fix | 型別重生無人承接 | plan:363 | production 加欄後 `database.types.ts` 仍只有五欄,plan 交棒表未指定 apply 後 regen/typecheck | 增列 Sean apply 後的重生、人工 nullable 校正回貼與 typecheck checkpoint |
| 24 | must-fix | 片界與同 commit 要求直接超過 45 分鐘 | plan:66 | S2b 45 分加 S2c 40 分卻要求同 commit,實際成 85 分;S2c 還跨六檔、約 3,000 行 harness | 拆成可獨立 commit 的 15–45 分鐘片,改以「全部 commit 到齊前禁止 apply」封窗 |
| 25 | must-fix | 「三支 S1 harness 的新格」沒有規格 | plan:72 | 執行者只擴 expected trigger 集合也能宣稱完成,但沒有哪支 harness 應量哪個 S2 行為 | 逐支列出 cell、唯一 mutant、預期計數與值 oracle |
| 26 | nit | C9∧C7 沒有反例,但有限枚舉不是證明 | plan:42 | NOT NULL 整數下代數蘊含成立;`generate_series(0,6)` 只抽樣小範圍,不能稱「機器證明」 | 改稱「代數證明＋有限域 smoke」 |
| 27 | nit | 行號抽查有三處範圍漂移 | plan:47 | bigint 理由實到 A1:121-124、row gate 實到 :63-66、兩個 oracle 都是多行區段而非單行 :147/:152 | 更新成完整實際範圍 |

## 主對話親驗(審查者的外部事實同樣會錯,不照單全收)

| 條 | 親驗結果 |
|---|---|
| **#21** | ✅ **成立,且比 codex 說的更精確**。全樹 grep `− instock − cancelled − shipped` 命中 4 處:<br>🔴 **真汙染 2 處** = `packages/domain/src/order/types.ts:531`(JSDoc 命令未來公式減 shipped)、`docs/specs/2026-08-05-e10-cancel-ui-wire-plan.md:285`(同)。**兩處都與 Sean 08-05 拍板「公式不減 shipped」直接矛盾。**<br>⬜ **非汙染 2 處** = `2026-08-05-shipped-enforcement-analysis.md:30`(F10 標明「master plan 字面」的引述,同檔 F11 已推翻)、`2026-07-28-…master-plan-v2.md:387`(row 37 本體已含 🔴🔴 08-05 就地更正「此字面是錯的、第 2 批不執行」)。<br>⇒ codex 說「master plan 也仍寫尚未定案」= **半對**:那句指的是「shipped 該在哪一層強制」,08-05 拍板後確實過期,但它不是「減 shipped」那條汙染。 |
| **#22** | ✅ 成立。A4a helper COMMENT(`20260803140000:191-192`)逐字寫「由**三張**真相表重算」「只由本片**四支** trigger 與 backfill 呼叫」——S2 之後兩個數字都變。 |
| **#23** | ✅ 成立。`packages/adapters/src/supabase/database.types.ts` 內有 `order_item_quantity_summary` 型別。 |
| **#18** | ⚠️ **部分過期**:codex 要求的「apply 前查三表真相為零」本輪已做(Supabase 唯讀,2026-08-06):`order_item_quantity_summary`=0、`shipments`=0、`shipment_items`=0、`order_item_procurement`=0、`order_items`=42。⇒ 真相 gate **已有實測值**;剩下的「故障注入」部分照收。 |
| **#5** | ✅ 成立,而且是我方矩陣的真缺陷:靶④(拿掉 `shipment_items` 那支)確實不會讓項 10 紅 —— 項 10 走的是 `shipments` UPDATE 那條路。 |
| **#20** | ✅ 成立(字面層):上游分析 PR1 的 fixture 若是單筆 `quantity=3`,刪掉後 instock=0 而非 2。守門仍會紅,但 plan 項 15 宣稱的**觀察值**是錯的。 |

—— R1 完 ——
