# Probe · admin「員工的一天」走一遍(線上正式後台)

> 開檔 2026-08-19T01:43 CST(`date` 實跑) · G5
> 🔴 **狀態:第一站就停住了 —— 我進不去,而那不是工具問題。** 理由與量到的東西在 §1。
> 授權背景:Sean 逐字「我剛剛有刷三筆正式訂單,你可以順便測試所有需要用到的功能,你們去測試吧」
> ⚠️ 該句為**主視窗轉述**,我沒有直接看到他打字。

---

## §0 我改變了什麼

```
🔴 零。本檔至今【沒有任何寫入】—— 只有三發不帶憑證的 GET。
   那三筆單(H7CPXQ / 2DTX42 / 8X3N5Q)我【一次都沒打開過】。
```

---

## §1 🔴 第一站:登入 —— **我進不去,而這是分類②/③ 之外的第四種**

### 量到的(唯讀 GET,不帶任何 cookie,2026-08-19)
```
curl -s -o /dev/null -w '%{http_code} → %{redirect_url}'
  https://admin.pcmmotorsports.com/           ⇒ 303 → /api/sso/start?next=%2F
  https://admin.pcmmotorsports.com/orders     ⇒ 303 → /api/sso/start?next=%2Forders
  https://admin.pcmmotorsports.com/api/sso/start?next=%2Forders
                                              ⇒ 302 → https://quote.pcmmotorsports.com/api/sso/authorize?state=<32hex>
  https://quote.pcmmotorsports.com/api/sso/authorize?state=probe
                                              ⇒ 🔴 401
```

### ⇒ 這條鏈的意思(而它是「員工的一天」的第 0 站)
```
要進 admin ⇒ 必須【先登入報價單站】⇒ 而報價單站要密碼(+ 可能 TOTP)
⇒ 🔴 我沒有那個密碼,而我【不會要】也【不會用】他的
   (密碼貼進對話就是一次洩漏;`.env` 一律不碰 —— 那條界線不因為有授權而移動)
```
🔴 **所以「你們去測試吧」這句授權,缺的不是許可,是【機制】** ——
**他授權了測試,而登入這一關在設計上只有他過得去。**

### ✅ 而這一站本身就是三個 finding(不是「卡住了」)

**finding-1 · 今天沒有「員工帳號」這回事**
員工要進後台 = **先用報價單站的密碼登入**。而那是**共用密碼**(我在 B3 那條線實查過:
報價單側今天沒有具名登入,`login/route.ts` 的四個簽發點一律 `{kind:'fallback'}`)。
⇒ **凡進得了報價單後台的人,就進得了網站後台。** 這正是 E8-B/B2 要解的那件,**而現在它是線上事實**。

**finding-2 · 我靜態讀出來的 deny 行為,線上證實了**
報價單的 `middleware.ts:46-49` `deny()`:`/api/` ⇒ 401、其餘 ⇒ 302 到 `/`。
線上 `authorize` 回 **401**(不是導去登入頁)⇒ **與那段 code 逐字相符**。
📎 那是我先前只有「讀全檔逐行推」的一格,**現在有線上證據**(而它證的是報價單側,不是 admin 側)。

**finding-3 · 而登入牆本身是【健康的】**
未登入 ⇒ 303/302/401,**沒有任何一頁在未登入時漏出內容**。這一格是正面結果,要記。

---

## §2 🔴 我建議的替代做法(要主視窗/Sean 選,我不自己決定)

```
甲 Sean 自己點,我在旁邊記
   他已經是「員工」的最佳替身(非工程師視角、而且那三筆是他刷的)
   我提供:每一站要按哪裡 + 要回報什麼(一行:我按了什麼 → 畫面出現什麼)
   ✅ 這條【現在就走得通】,不需要任何憑證交換
   ⚠️ 代價:他要花時間;而「員工做不做得到」由他親身回答,那其實比我代測更有效

乙 我用拋棄式環境走完整流程
   ✅ 我做得到、可以按到底(含退款)
   ❌ 而它【證不了線上】—— runbook §0/§5 明文:本機 RLS/GRANT/trigger 都是自己補的
   ⇒ 它答的是「這個畫面在這種資料下會畫出什麼」,不是「員工在線上做得到嗎」

丙 等 E8-B 上線(員工帳號)之後再測
   ❌ 那要等 Q9 三題 + plan 鐵則 8,現在不成立
```
🔴 **我推薦【甲 + 乙 併行】**:
甲答「線上員工做不做得到」(唯一能答那題的路);乙答「按下去的邏輯對不對」(我做得到而他不必花時間)。
📌 **而兩條【不可互相冒充】** —— 乙全綠不代表甲會通,這一格要寫在任何結論旁邊。

---

## §3 我沒做的

```
· 沒有登入(沒有憑證,而我不會要也不會用他的)
· 沒有打開那三筆單、沒有按任何按鈕、沒有任何寫入
· 沒有走到退款(照交辦:退款前必停,而我連第一站都沒過)
· 沒有碰另外 7 筆、沒有碰 G3 的 WCYCW5 / Z6QDV9
· 沒有改任何 app code、沒有動任何設定
```

---

# §4 【乙】拋棄式環境:走到哪、為什麼停(2026-08-19T02:22 CST · **已全部收攤驗死**)

## 🔴 乙的結論一句話
**乙【答不了分類②(有畫面但按不動)】,而那不是我沒走完 —— 是它在設計上到不了那一格。**

## ① 環境建起來了,而過程中補了 runbook 一個真缺口
```
拋棄式 PG 17.10 / PostgREST / 前綴代理 / admin dev(ADMIN_DEV_BYPASS=1)
表 10/10、RPC 11/12、migration ok=157 → 失敗 28 → 修鏈頭後 23
🔴 鏈頭:runbook §2 沒有 pgcrypto,而 migration 要的是 extensions.gen_random_bytes
   ⇒ 少了它【出貨那條路的四支 RPC 一支都不存在】⇒ 已補進 runbook §2
```

## ② 🔴 寫入站:**全部走不了,而擋我的是【登入態】不是產品**
自己開檔複驗(不採信轉述):
```
apps/admin/src/lib/session/authorize.ts:29-30  verifySession(cookie) ⇒ !session ⇒ return null   ← 沒有 devBypass
                                        :31    isAllowedOrigin(..., { devBypass: DEV_BYPASS })  ← 只有這裡有
grep -c 'ADMIN_DEV_BYPASS' apps/admin/src/lib/session/session.ts ⇒ 0
```
⇒ `ADMIN_DEV_BYPASS=1` **讓我看得到頁,而它從來沒有給我一個 session**
⇒ 收款/採購/到貨/尾款/出貨/退款/備註/編輯/取消 **九個寫入站全部會回 denied**
⇒ 🔴 **而那不是「員工做不到」,是我的環境沒有登入態。**
**⇒ 我在按任何一個之前先確認了,而不是按完再解釋。**

**而「自簽一個 admin session」我不做** —— 偽造登入態 + 碰 session 密鑰。
**若乙需要偽造登入態才走得動,那就是走不動。**

## ③ 讀取站:資料層通了,**而 UI 顯示 0 筆,我沒有隔離出原因**
```
✅ 塞了一筆 PCM-2026-0001 進拋棄式庫(過了 5 道 CHECK:display_id 格式 / invoice 白名單 /
   收件地址白名單 …)
✅ PostgREST 用【adapter 那條一模一樣的 select】打 admin_order_list_v ⇒ 回得到那一列
✅ service_role 對該 view has_table_privilege ⇒ t
🔴 而後台列表畫出來是「目前沒有符合條件的訂單。共 0 筆」,admin log 零錯誤
⇒ 【資料層拿得到、UI 拿不到】,而我【沒有隔離出斷在哪一層】
```
⚠️ **所以讀取站我也不下結論** —— 一個我解釋不了的 0,不能拿去說「列表壞了」。

## ④ 🔴 我為什麼把伺服器關掉(理由不是診斷卡住)
`apps/admin/.env.local` **裡面有 `NEXT_PUBLIC_SUPABASE_URL`**(我只 `grep -c` 數行數,**沒有讀值**)。
⇒ 我**無法排除**它蓋過我用 shell 設的那個。
⇒ 🔴 **一台我不確定指向哪裡的伺服器,不該繼續開著。** 關掉,而不是一邊查一邊讓它跑。

## ⑤ 收攤(用【不會說謊的量法】,而舊的那把今晚剛被證明會說謊)
```
:3011 / :3998 / :3999 / :55501  ⇒ curl 全 000、lsof 全無人聽
PG ⇒ 連不上;/tmp/pcm-probe ⇒ 已清
⚠️ 同機另有兩支別窗的 next-server(43930 / 81985)⇒ 【沒有動】。收攤是收自己的,不是收乾淨
```

## ⑥ 本機限定(引用本節任何一句都要帶)
```
🔴 拋棄式庫的 orders_display_id_format = '^PCM-[0-9]{4}-[0-9]{4,}$'
   ⇒ 那是 expand【之前】的世界(d0_display_id_expand 在失敗的 23 支裡)
   ⇒ 任何我在本機對 display_id / #477 的觀察【都不算數】
🔴 而整節照舊帶那句:【乙全綠不代表甲會通】—— 本機 RLS/GRANT/trigger 都是我自己補的
```

## ⑦ 乙實際交付了什麼(不誇大)
```
✅ runbook 兩個真缺口(pgcrypto、收攤那把尺會說謊)—— 下一個人省下我繞的兩圈
✅ 「寫入站在拋棄式環境走不了」這個結論本身 ⇒ 它把【甲】從「其中一條路」變成【唯一的路】
❌ 分類② 一格都沒判定
❌ 讀取站也沒下結論(那個 0 我解釋不了)
```

— G5
