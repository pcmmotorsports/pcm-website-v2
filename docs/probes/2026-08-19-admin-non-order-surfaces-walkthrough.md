# 後台【非訂單面】逐面走查(2026-08-19 01:4x CST,G3)

> 分工:訂單那條流(收款→採購→到貨→尾款→出貨→退款)是 **G5** 的,本檔沒有碰它。
> 本檔走的是左側選單上除了「訂單」以外的各面。
> **側欄項目的分母(當場量)**:`document.querySelectorAll('a[href^="/"]').length` 在總覽頁 ⇒ **7 個連結**
> (總覽 / 訂單 / 退款異常 / 客戶 / 商品 / 員工管理 / 供應商)+ 一顆沒有 href 的「設定」按鈕 ⇒ 本檔涵蓋其中 6 面 + 設定 + 稽核。

## 🔴 效度限定(本檔的每一節都在這個限定底下量的 —— 節數:`grep -c '^## §' docs/probes/2026-08-19-admin-non-order-surfaces-walkthrough.md` ⇒ 9;不要拆開引用)

```
量的是   本機 http://localhost:3001 + ADMIN_DEV_BYPASS=1 + 連【正式庫】
         (指向哪個 DB 的數法:`bash scripts/which-db-am-i-pointed-at.sh` ⇒ 三行全紅=正式站)
量的時間 2026-08-19 01:44–01:48 CST(兩端都是 `date` 實跑值),工作樹 HEAD b7c251d0
不是     線上 admin.pcmmotorsports.com(線上那份我進不去,見 §0)
```
🔴 **`ADMIN_DEV_BYPASS=1` 把登入閘拿掉了** ⇒ 本檔證的是「**畫面與功能**」,
**完全沒有證**「員工登入進得去」—— 那一格見下面 §0。
🔴 **本機連的是正式庫** ⇒ 本檔按過的東西是真的寫入。本輪**沒有按任何寫入動作**(逐面記在各節)。

## 三分類(這份走查的重點不是「有沒有做完」,是**沒做完的形狀**)

```
① 做得到          進得去、按得動、事情做得完
② 有畫面但按不動   看得到入口,而那個入口不會帶你到任何地方
③ 根本找不到入口   功能不存在於畫面上(底層可能早就好了)
```
🔴 **三類在「還沒做完」底下長得一樣,而處置完全相反** ——
③ 可能只差接線(底層已就緒),① 與 ② 之間差的是能不能交給員工。

---

## §0 🔴 線上後台:**我進不去**,而那本身是一個結果

```
量法(唯讀 GET,不帶任何憑證):
  curl -s -o /dev/null -D - https://admin.pcmmotorsports.com/
  ⇒ HTTP/2 303   location: /api/sso/start?next=%2F
  curl -L 之後最終落點與狀態:
  ⇒ 401  https://quote.pcmmotorsports.com/api/sso/authorize?state=<32hex>
```
用瀏覽器打開同一個網址,畫面上出現的**逐字**是:
```
{"error":"未登入"}
```

🔴 **這是一個【員工看得見】的問題,不只是我進不去**:
```
一個沒登入的員工打開 admin.pcmmotorsports.com,看到的是一行【給程式看的 JSON】
—— 沒有登入框、沒有「請先登入」、沒有任何一個可以按的東西、沒有指向哪裡去登入。
```
正確入口實查:`https://quote.pcmmotorsports.com/` ⇒ 200,標題「PCM 系統登入」,
有密碼欄與「手機不在身邊?用備用碼登入」。
⇒ **入口是有的,只是那行 401 JSON 不會告訴任何人它在哪。**

**分類:② 有畫面但按不動**(而那個「畫面」是一行 JSON)。
**我沒有做的事**:🔴 **我沒有索取、沒有使用、也不會去找 Sean 的後台密碼。**
他授權的是「測功能」,不是「取得他的憑證」—— 那條界線不因為授權而移動。

---

## §1 總覽(首頁)—— ① 做得到,**而且數字對得起來**

Sean 點名要查首頁金額。**查了,對得上。**

畫面上的三個數(2026-08-19 台北時間):
```
今日實收(淨)         NT$ 22     說明「含沖銷;以實際收到錢的日期計」
今日新單             4 筆       說明「含未付款與當天取消的」
目前待處理退款異常    0 筆       說明「累計未結案,非今日新增」
```

**對帳(可重跑)**:訂單列表 `?show_unpaid_card=1&date_from=2026-08-19&date_to=2026-08-19` ⇒ 共 4 筆
```
H7CPXQ  NT$ 1      已收未定    ← Sean 刷的
2DTX42  NT$ 1      已收未定    ← Sean 刷的
8X3N5Q  NT$ 20     已收未定    ← Sean 刷的(他用來驗那 275 顆的那一筆)
Z6QDV9  NT$ 1,400  未收未定    ← 🔴 我的沙盒探測留下的孤兒單,【沒收到錢】
```
```
今日實收 22 = 1 + 1 + 20   ✅ 三筆真的收到錢的加總
                            ✅ 而且【正確地不含】我那筆 1,400(它未收)
今日新單 4 = 4 筆           ✅ 含我那筆未付款的 —— 與說明文字「含未付款」一致
```
🔴 **這裡有判別力**:如果它把未收的 1,400 也算進實收,總數會是 1,422。**它沒有。**
⇒ 「實收」與「新單」是**兩個不同的口徑**,而畫面上兩句說明各自寫對了自己的口徑。

另記:同頁有「具名身分(M-4a M0-S2)」選擇器,現況「尚未選擇」,並自陳逐字:
> 🔴 這個身分是你自己選的、系統並未驗證 —— 目前登入只確認「有人通過認證」,不確認「是誰」。

⇒ **稽核 log 記到的操作者是自選的。** 這句自陳寫在畫面上、不是藏在 code 裡,記一筆。

---

## §2 退款異常 —— **分不出 ① 還是 ②,因為清單是空的**

畫面出得來。內容逐字:
```
「目前沒有滯留或卡住的退款。」
```
說明文字裡有一句**直接對應 G6 量到的那個缺口**(逐字):
> 另外也會列出已經人工判定失敗、但這裡改不了判定的退款, 那幾列沒有可以按的按鈕。

🔴 **所以這一面同時是兩類**:
```
「執行對帳判定」那條路  ⇒ 分不出 ①/②:**清單是空的,我沒有東西可以按**
「改判定」那條路        ⇒ ③ 根本找不到入口 —— 而它是【畫面自己承認的】,不是我推的
```
📎 對照 G6 量到的:`admin_correct_order_refund_verdict` 這支 RPC **已 apply 五天而應用層零呼叫端**。
⇒ **底層好了、UI 那半沒接** —— 這正是分類 ③ 的教科書形狀。

⚠️ **我不能說這一面「可以用」** —— 空清單在「功能正常」與「功能壞掉」兩個世界會印同一句話。
要證它,需要一筆真的滯留退款,而**製造那個狀態不是我這一輪該做的事**(且訂單那條流是 G5 的)。

---

## §3 客戶 —— ① 做得到

```
共 12 位,單頁列完。有「搜尋」與「篩選」兩顆按鈕(都按得動)。
欄位:姓名 / Email / 電話 / 會員等級 / 訂單數 / 累計金額 / 最近下單 / 註冊日
```
順帶的交叉驗證(對 `#659` 有用):
```
我的測試帳號「G3 沙盒測試」在這一面顯示 **2 筆訂單 / 累計 3,000**
而同一個帳號在【顧客站自己的會員中心】顯示「目前尚無訂單紀錄」、TOTAL ORDERS 0
⇒ 🔴 同一個人,員工看得到 2 筆,他自己看到 0 筆。這就是 #659 那條的實體。
```

---

## §4 商品 —— 🔴 **看得到 20,341 件,而員工找不到其中任何一件**

```
共 20341 件 / 每頁 20 件 / 第 1／1018 頁
篩選:有三顆 chip(全部 / 手動 / 自動)—— 按得動
🔴 搜尋:**沒有**。整頁 `document.querySelectorAll('input,select').length` ⇒ **0**
        全頁文字裡連「搜尋」兩個字都沒有出現
```
⇒ **要找一件特定商品,只能從 1,018 頁裡一頁一頁翻。**
**分類:② 有畫面但按不動** —— 頁面本身沒壞,而**那件事做不完**。
📎 這一格與常載規則那條「基本後台功能不是決策題,自己補」同族:搜尋是任何後台都會有的。

### §4-b 編輯商品 ⇒ ③ 找不到入口,**而它是誠實宣告的**
點進商品詳情(`/products/<uuid>`)實查:
```
input 0 / textarea 0 / select 0 / form 0   ⇒ 整頁零表單元件
可按的按鈕只有側欄那兩顆(設定〔停用〕/ Toggle Sidebar)
```
頁面自己寫著(逐字):
```
「這一頁的東西不能在後台改 —— 它由供應商同步管線維護。」
「這一頁只能查看,不能修改。」
```
🔴 **這比 G5 用 grep 推出來的更值得記的一點**:它**不是一個沉默的死路**,
是**畫面上明白告訴你「這裡不能改」**。⇒ 員工不會卡在那裡試,他會去問人。
**同樣是「做不到」,誠實宣告的做不到比死按鈕便宜得多。**

### §4-c 新增商品 ⇒ ③ 找不到入口
```
curl http://localhost:3001/products/new ⇒ 404
商品列表頁上沒有任何「新增」字樣的按鈕(可按元素清單見量法)
```

---

## §5 員工管理 —— ① 做得到(而權限那半是**標記,還沒生效**)

```
6 列:op4_backfill(系統,已停用)/ payment_confirmer(系統,已停用)/ Sean(老闆,啟用中)/
      員工 1(占位)/ 員工 2(占位)/ TEST
每列可按:儲存資料 / 停用員工(Sean 那列是「救援帳號不可停用」⇒ 正確地 disabled)
底部有「新增員工」表單:代碼(id)/ 顯示名 / 管理者標記
```
🔴 每一列都重複同一句自陳(逐字):
> 此標記目前不影響任何權限;成本報表上線後才會生效。

⇒ **「管理者」是個標籤,不是權限。** 現在把誰標成管理者,對他能看到什麼**沒有任何影響**。
⚠️ 這與 §1 那句「系統不確認你是誰」是同一件事的兩面:**目前後台沒有人員權限分層。**

**我沒有按任何一顆**(儲存 / 停用 / 新增都會寫進正式庫)。

---

## §6 供應商 —— ① 做得到

```
清單完整(安豐達 / 老吳精品 / 店內現貨 / 阿毅物流 / 陳蔚仁 / 豪元國際 / AKOSO / E PLOT /
Extreme-Components / FOWLER / Gbracing / IMPEX / KINEO IN-MOTION / MAPD / MOTOBIKE TH /
Omnia Racing / PROTI / RaceBikeBitz / …)
每列可按:儲存名稱 / 停用(或啟用);底部有「新增供應商」
每列附一句後果說明(逐字):「改後,過去所有採購紀錄都會顯示新名字。」
```
⇒ **改名的後果寫在按鈕旁邊**,不是等按下去才知道。這是好的。
**我沒有按任何一顆。**

---

## §7 設定 —— ② 有畫面但按不動

```
左側選單有「設定」這一項,而它是 <button aria-disabled="true">
Playwright 點它 ⇒ TimeoutError,逐字「element is not enabled」(重試 14 次全部 not enabled)
curl http://localhost:3001/settings ⇒ 404
```
⚠️ 而**它的子頁是通的**:`/settings/staff`、`/settings/suppliers` 都進得去(見 §5 / §6),
只是**要從側欄那兩個獨立項目進**,不是從「設定」進。
⇒ 員工看到「設定」會去按它,而**它什麼都不會發生**。

---

## §8 🔴 操作紀錄(稽核)—— ③ 根本找不到入口,**而那一頁其實寫好了**

```
側欄:**沒有這一項**(側欄全部連結實查 = 總覽 / 訂單 / 退款異常 / 客戶 / 商品 / 員工管理 / 供應商,共 7 個)
直接打網址 /settings/audit ⇒ **404**(瀏覽器與 curl 都是)
```
**而 repo 裡那一頁是存在而且完整的**:
```
apps/admin/src/app/settings/audit/page.tsx        9,065 bytes,最後修改 Aug 18 10:37
apps/admin/src/app/settings/audit/page.test.tsx   9,140 bytes
```
**404 是刻意的,不是壞掉**:該檔第 1 行 `import { notFound } from 'next/navigation'`,
由旗標控制 —— `apps/admin/src/lib/audit/audit-ui-flag.ts:29-30`:
```
export function isAuditUiEnabled(): boolean {
  return process.env.AUDIT_UI_ENABLED === '1';
}
```
旗標現況(只數行、不印值):
```
apps/admin/.env.local  含 ^AUDIT_UI_ENABLED= 的行數 = 0
.env.local             含 ^AUDIT_UI_ENABLED= 的行數 = 0
⇒ 兩處都沒有這個鍵 ⇒ !== '1' ⇒ 頁面 notFound()
```
🔴 **所以「後台記不記得住誰做了什麼」這題,現在的答案是:**
**它記(§1 那個具名身分會被寫進稽核 log),而【沒有人看得到】。**
⚠️ 而 §1 那句自陳同時成立:**記下來的那個操作者是自選的、未經驗證。**

---

## 收斂:八面的分類表

| 面 | 分類 | 一句話 |
|---|---|---|
| §0 線上登入 | ② | 沒登入時看到 `{"error":"未登入"}`,沒有任何可按的東西 |
| §1 總覽 | ① | 數字對得起來(22 = 1+1+20,正確排除未收的 1,400) |
| §2 退款異常 | ①/② **分不出** | 清單空的 ⇒ 沒東西可按;「改判定」那條是 ③(畫面自己承認) |
| §3 客戶 | ① | 12 位、有搜尋與篩選 |
| §4 商品 | ② | 20,341 件、1,018 頁、**零搜尋框** ⇒ 找不到任何一件 |
| §4-b 編輯商品 | ③ | 零表單,而**畫面誠實宣告**「這一頁只能查看」 |
| §4-c 新增商品 | ③ | `/products/new` 404、列表無新增鈕 |
| §5 員工管理 | ① | 可增可停;而「管理者」目前只是標記、不影響權限 |
| §6 供應商 | ① | 可增可改可停,後果寫在按鈕旁 |
| §7 設定 | ② | 側欄那顆 `aria-disabled`,`/settings` 404;子頁另有入口 |
| §8 操作紀錄 | ③ | 頁寫好了(9KB + 測試),旗標 `AUDIT_UI_ENABLED` 未設 ⇒ 404 |

## 🔴 這一輪【沒有】答到的

```
1. 員工登入進不進得去 —— ADMIN_DEV_BYPASS=1 把那道閘拿掉了,我證的是閘後面的東西
2. 線上那份是不是長這樣 —— 我量的是本機工作樹 HEAD b7c251d0,不是線上部署的 84f57ed
3. 退款異常那一面到底能不能用 —— 清單空的,構造不出來的東西我沒有構造
4. 訂單那條流 —— 不是我的範圍(G5)
5. 任何寫入動作的結果 —— 🔴 **本輪一顆寫入按鈕都沒按**,所以「按下去會發生什麼」全部未測
```
⚠️ 特別是第 5 條:本檔說某一面「① 做得到」,依據是**它渲染得出來、按鈕沒有 disabled**,
**不是**「我按下去而它成功了」。⇒ **不要把本檔的 ① 讀成「這一面驗過了」。**

---

# 🔴 補記(2026-08-19 02:0x CST,同一班 G3):第 5 條**已經不是「我沒按」了,是「按不了」**

> 上面第 5 條原本是一句自我限定(「本輪一顆寫入按鈕都沒按」)。
> **我後來去按了。** 而結果把那句限定的性質整個換掉 —— 它不是「還沒做」,是**在這個環境做不到**,
> 而**理由是結構性的**。原句留著不刪,因為它記錄了當時我只知道到哪裡。

## 實際按下去發生什麼(兩發,都在 `員工管理`)

```
發 1  02:07:44 CST  填 g3_probe /「G3 測試(可刪)」→ 按「新增員工」
      ⇒ 轉導 /settings/staff?r=denied
      ⇒ 畫面逐字:「沒有權限或登入狀態已失效,未儲存。」
      ⇒ 列數 6 → 6(沒變);全文 /g3_probe/ ⇒ false
      ⇒ **零寫入、fail-closed、沒有寫一半**

發 2  排除「是不是因為沒選具名身分」:先到總覽把身分切成 TEST
      ⇒ 那一發【成功】(頁面逐字變成「目前身分:TEST」)⇒ 所以不是「所有寫入都不通」
      ⇒ 再按一次新增員工 ⇒ **仍然 ?r=denied**
```

## 為什麼(讀 code,不是從 denied 推的)

```
apps/admin/src/lib/session/authorize.ts:24-35  authorizeAdminMutation 三道閘:
  ① const session = await verifySession(cookieStore.get(ADMIN_SESS_COOKIE)?.value);
     if (!session) return null;
  ② if (!isAllowedOrigin(headerStore.get('origin'), { devBypass: DEV_BYPASS })) return null;
  ③ const actor = await getSessionActor(); if (!actor) return null;
```
🔴 **`DEV_BYPASS` 只餵給【②Origin】那一道,沒有餵給【①session】。**
而 `apps/admin/src/lib/session/session.ts:146` 的 `verifySession` **全檔零 `ADMIN_DEV_BYPASS` 字面**
(數法:``grep -c ADMIN_DEV_BYPASS apps/admin/src/lib/session/session.ts`` ⇒ 0),
且它 `if (!token) return null`。
⇒ **`ADMIN_DEV_BYPASS=1` 讓我【看得到頁】,而它從來沒有給我一個 session**
⇒ **每一個寫入都必定被①擋下。**

## 🔴 射程:這不只影響本檔這幾面

```
呼叫 authorizeAdminMutation 的檔(數法:`grep -rln authorizeAdminMutation apps/admin/src`):
  orders 族:payment / payment-reverse / cancel / procurement / receipt / shipment /
             refund / refund-recovery / amount / note
  customers 族:tier / wallet / profile / keyword-search
  staff-actions / supplier-actions
⇒ 收款→採購→到貨→尾款→出貨→退款,**每一步都經過同一道閘**
```
⇒ **任何人在 `ADMIN_DEV_BYPASS=1` 的本機後台測寫入,都會在每一步撞到同一句 `denied`。**
🔴 **而它與「功能沒做」在畫面上長得一樣** —— 這正是本檔三分類會踩的坑,**不要把 `denied` 讀成 ③。**

## ⇒ 所以本檔那些「① 做得到」要怎麼讀

```
員工管理 / 供應商 / 客戶 那幾面標的「①」,在【這個環境】下正確的讀法是:
  **渲染得到、按鈕不是 disabled,而寫入一律被拒(理由是缺 session,不是缺功能)**
⇒ 它們是不是真的「做得到」,**要有一個真的登入態的人去按才知道**,本檔答不了。
```

## 我沒有做的事(兩條路都在,我兩條都不走)

```
甲-1 用線上後台按 ⇒ 需要 Sean 的 SSO 密碼
     🔴 我沒有索取、沒有使用。他授權的是「測功能」,不是「取得他的憑證」——
        **那條界線不因為授權而移動。**
甲-2 在本機自簽一個 admin session cookie ⇒ 那是**偽造登入態**,還要碰 session 密鑰
     🔴 不做,也不建議別人做。
```

## 一個佐證的限定(不要把它當決定性證據)

```
我另外量了瀏覽器 cookie 鍵名 ⇒ 只有一個 Supabase 的,沒有 admin session cookie
⚠️ 而 document.cookie **看不到 httpOnly** ⇒ 這一發是佐證、不是決定性
   決定性的是上面那段 code + 實際觀察到兩次 denied
```

## 這一班在後台留下的東西

```
零寫入成功。唯一成功的寫是「切換具名身分」(它不走 authorizeAdminMutation)
⇒ 🔴 那顆身分現在停在 **TEST**(原本是「尚未選擇」)—— 我改了它。
   它只影響稽核 log 記名,而**稽核那一頁現在看不到**(§8)⇒ 現階段無可見後果。
   要切回「尚未選擇」隨時可以,在總覽頁那個下拉。
```

