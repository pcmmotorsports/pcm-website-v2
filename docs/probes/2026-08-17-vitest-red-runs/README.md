# 2026-08-17 四綠 harness · vitest 完整輸出 24 支

## 1 這是什麼

`scripts/dev-four-greens.sh` 在 2026-08-17 當天跑過的**每一發** vitest 的**完整** stdout,
24 支(數法 `ls docs/probes/2026-08-17-vitest-red-runs/*.txt | wc -l` ⇒ 落筆當下 `24`),
共 3,349 行(`cat docs/probes/2026-08-17-vitest-red-runs/*.txt | wc -l`)。
檔名 = 當時的 run 目錄名 `<merge hash>-<時點>-<PID>`,與 `logs/four-greens/<同名>/` 一一對應。

⚠️ **副檔名是 `.txt` 不是 `.log`,而那是刻意的** —— `.gitignore:49` 有一條全域 `*.log`,
放 `.log` 進來會被靜默忽略、**看起來搬好了而 git 裡一個檔都沒有**(我第一版就是這樣,
`git check-ignore` 當場抓到;負對照:在本目錄 `touch _x.log` 再 `git check-ignore -q` ⇒ 命中)。
**不要改回 `.log`。**

## 2 為什麼要放進 repo

`logs/` 整個在 `.gitignore:48` ⇒ **原本的四綠 log 一個字都不在 repo 裡**
(數法 `git ls-files logs/ | wc -l` ⇒ 落筆當下 `0`)。
而 `logs/four-greens/*/vitest.log` **只存了尾 60 行**(檔案首行自己寫著「共 N 行」;
數法 `for f in logs/four-greens/*/vitest.log; do head -1 "$f"; done | grep -c '尾 60 行'` ⇒ 落筆當下
`24`,而同一個 glob 的分母 `ls logs/four-greens/*/vitest.log | wc -l` 也是 `24` ⇒ 沒有一支例外),
完整輸出原本在 `/var/folders/.../tmp.XXXX` —— **那是 macOS 會週期清掉的地方**。

⇒ 這 24 支是從那些 tmp 檔搶救出來的。搬進來的當下逐支 `wc -l` 對過各自 log 首行自稱的
「共 N 行」,**24 支對得上、0 支對不上**;並跑過負對照(另造一支首行自稱 594 行、來源只有 3 行的
假靶 ⇒ 比對器印出「自稱 594 實得 3」⇒ 它抓得到,所以那 24 個 ✅ 是量出來的)。

🔴 **搬的是 24 支,不是「紅的那 6 支」**(兩個數的來源:`6` 是 V 窗判斷哪幾發算紅,
`24` 是上面那條 `grep -c` 數出來的被截斷支數)—— **「哪幾支算紅」是判斷,「24 支都被截斷」是事實**;
挑選會少搬,而 `cp` 是零成本的。
📎 副作用是買到了 `F-5` 要的那發綠對照(`9946d24e-20260817T152523`,與同 hash `…152039` 那發紅配成
**同一棵樹的紅綠對照對**)—— 那不是運氣,那正是不挑選買到的東西。

## 3 誰在用它

`docs/phase-1-backlog.md` 的 **`#618`**(vitest 隨機紅)。
指回來的地方數法:`grep -rn '2026-08-17-vitest-red-runs' docs/ scripts/`。

🔴 **不要重跑到綠。** 這批 log 的價值來自「它們是紅的當下留下來的」——
**這句是論證,不是數出來的**(它講的是證據的性質,沒有可數的分母)。
重跑一發綠不會推翻任何東西,只會讓下一個人以為問題不存在。

🔴 **引用「N 次紅」時要標【分母未確認】** —— V 窗從 log 庫數到 6 紅(**我未複驗這個 6**),
而 `afb33c0b`(log 改帶 hash、不再覆寫)**之前**的 log 是整支覆寫的 ⇒ 更早的紅有沒有痕跡 **未確認**。
⇒ 這裡的 24 支是「`afb33c0b` 之後、當天、這台機器」的,**不是那條線的全部證據**。

## 4 什麼時候可以刪

**`#618` 結案之後。** 刪之前跑一次上面第 3 節那條 `grep -rn`,確認沒有別的東西還指著它。

🔴 **這一節不寫的話這批東西永遠不會被刪** —— 事故當晚立的法往往永不退場,退場條件要可量。
