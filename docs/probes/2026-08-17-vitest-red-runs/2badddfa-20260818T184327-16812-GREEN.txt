# 2badddfa-20260818T184327-16812-GREEN — **比紅的那一發更忙,而它是綠的**

落檔人:G1,2026-08-18 18:4x CST。
🔴 **這一份存在的理由只有一個:它是【機器忙 ⇒ 撞 5000ms】那個方向的反例。**
   如果它跟前一發綠一樣是在機器閒的時候跑的,我不會替它開檔(邊際太低)。

## 四項
```
commit  2badddfa   拋棄式鑽機 /Users/sean_1/pcm-g1-greens，工作樹 0 行未提交異動
時點    2026-08-18 18:43:29 CST
typecheck rc=0 / lint rc=0 / build rc=0 / vitest rc=0
Test Files  529 passed | 1 skipped (530)
     Tests  8774 passed | 2 expected fail | 2 skipped | 1 todo (8779)
```

## 🔴 三發並排(這才是重點)
```
                        17:38 RED     18:16 GREEN   18:43 GREEN
vitest rc                1             0             0
run queue 中位            104           62            121   ← 最忙的那一發是綠的
run queue 最大            141           89            154
run queue n               314           120           155
load average 起跑前        181.49        （見該檔）     223.94
load average 結束          181.21        （見該檔）     280.83
```
⇒ **「機器忙 ⇒ 紅」在這三發裡不成立。** 最忙的一發全綠,而紅的那一發比它閒。

## 🔴 它同時作廢我自己在前一份樣本裡寫的一句
`553aa49e-20260818T181655-62485-GREEN.txt` 逐字寫著
「**方向上一致:機器安靜的那一發是綠的**」。
⇒ **那句現在有反例,已在該檔內就地標掉**(不刪原文)。
🔴 **我當時已經寫了「n=1 的綠 vs n=1 的紅,不足以升成結論」而仍然寫了「方向一致」** ——
   **兩句放在一起,讀的人會記住前面那句。** 免責聲明擋不住一個方向性的敘述。
   ⇒ 教訓:**不確定的時候不要寫方向,寫「兩個值,還不知道有沒有關係」。**

## 這對 #618 的意思
```
#608 的 load average 假說：已被 I 窗「load1m=173 之下 512 檔全綠」推翻過一次
本發：load 224→281、run queue 中位 121 之下 8774 格全綠 ⇒ 第二次獨立反例
⇒ 「機器忙」這個【更寬的方向】現在也站不住 —— 而我 18:2x 才寫過「不否定它」
```
⚠️ **仍然不能反過來說「負載無關」** —— 三發都是 n=1,而逾時是機率性的。
   能說的只有:**現有樣本裡,忙不忙分不出綠紅** ⇒ 那個方向**不值得再花時間**,
   照 #618 ① 去量【全體格子的耗時分佈 vs 5000ms 門檻】才是有判別力的下一步。

## 涵蓋性(當場量,會過期)
```
git merge-base --is-ancestor 2badddfa dev    ⇒ 是
git rev-list --count origin/dev..2badddfa    ⇒ 63
git rev-list --count 2badddfa..dev           ⇒ 4
```
🔴 同前一份:「涵蓋 63 顆」= 在 2badddfa 這個樹狀態上四項 rc=0,
   **不是**那 63 顆每一顆各自綠過。


---

## 🔴🔴 第四條限定(2026-08-18 18:5x 主視窗給,而它比前三條硬)

前三條講的是**這一發涵蓋多少**。這一條講的是**涵蓋這件事本身能答什麼**:

> **四綠答的是「這棵樹編得起來、測試綠」,不答「這些改動是對的」。**
> **涵蓋 ≠ 沒有已知缺陷。**

**當天的實例(不是假想)**:GR 對本檔涵蓋範圍內的收藏線四顆跑 R3,開出 3 must-fix,最重一條逐字:
```
換帳號時 :150-151 換新物件、:152-153 用 .clear() 留同物件
⇒ 舊 worker 的 finally 清掉新 worker 的 desired
⇒ 疊上 while 條件把 undefined 讀成「有落差」
⇒ removeFavoriteAction 無限連發 ＋【新帳號真實收藏被刪】
```
⚠️ GR 標明**讀碼推演、未真瀏覽器構造**(G3 正在構造)。
⇒ **一個會刪客人資料的缺陷,住在一發四項 rc=0 的樹裡,而四綠一格都沒紅。**

🔴 **所以引用本檔時,「涵蓋 N 顆」不可以被讀成「那 N 顆可以推」** ——
   它只排除「編不起來 / 測試紅」這兩種死法,**排不掉任何一種【邏輯是錯的】的死法**。
   要排掉後者只有審查與真跑,那不是四綠的職務。
