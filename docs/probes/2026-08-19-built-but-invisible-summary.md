# 「東西做好了,而看不到」—— 今晚撞到四次,而它們是同一個形狀

2026-08-19 03:5x CST,G3。**這一份不產生新事實**,它把今晚**四份分開的走查**裡同一個形狀收在一起。

## 🔴 為什麼值得單獨一份

```
那四格分別出現在四份檔、報給不同的消費者(12 站表 / 旗標清單 / 開關追查 / 付款後走查)
⇒ **沒有人一次看到它們四個**
而重點是:**逐格的修法** 與 **整族的修法** 不一樣 ——
   逐格 = 開一個旗標 / 接一個頁面(各自幾小時)
   整族 = 🔴 **我們沒有任何機制,能在「做完了而沒接上」的時候叫一聲**
```
📌 這正是今晚反覆出現的那句:**知識存在 ≠ 決策的人拿得到。**

---

# 四格(每格的證據都在別的檔,這裡只放指標與一行重驗)

| # | 東西 | 做好了嗎 | 為什麼看不到 | 誰因此卡住 |
|---|---|---|---|---|
| 1 | **稽核紀錄頁** | ✅ `page.tsx` **9,065 bytes** + `page.test.tsx` | 旗標 `AUDIT_UI_ENABLED` 未設(本機 **0 行**)⇒ `notFound()` | 想知道「誰改了這張單」的人 |
| 2 | **退款入口** | ✅ `RefundSection` + `refund-entry-gate.ts:41-51` 六道閘 | 旗標 `REFUND_UI_ENABLED` 未設(本機 **0 行**)⇒ 整塊不渲染 | 要退款的員工 |
| 3 | **信的下落** | ✅ `email_outbox` 表 + 六種狀態(`20260717020000...:297/:317`) | 🔴 **後台零檔引用它**(`grep -rl email_outbox apps/admin/src` ⇒ **0 檔**) | 客人問「我沒收到信」時的員工 |
| 4 | **被藏起來的訂單** | ✅ 資料在、開關在(`order-list-view.ts` 內 `SHOW_UNPAID_CARD_PARAM` **4 處**) | 預設關,而**一般瀏覽時畫面上沒有任何提示** | 以為自己看到全部的員工 |

```
上表每一格今天都重驗過一次(2026-08-19 03:58 CST,不是憑記憶):
  稽核頁 9065 bytes / AUDIT_UI_ENABLED 0 行 / REFUND_UI_ENABLED 0 行 /
  email_outbox 在 admin 0 檔 / SHOW_UNPAID_CARD_PARAM 4 處
詳細證據與負向對照分別在:
  docs/probes/2026-08-19-staff-day-12-stations-what-exists.md
  docs/probes/2026-08-19-feature-flag-inventory.md
  docs/probes/2026-08-19-admin-non-order-surfaces-walkthrough.md(§8 與檔末追查節)
  docs/probes/2026-08-19-after-payment-what-the-customer-sees.md
```

## 而它們**不是同一種原因**(這一格不要合併)
```
1、2  旗標關著        ⇒ 開一個環境變數就會出現(而字面 '1' vs 'true' 兩種混用,見旗標清單)
3     從來沒有接 UI   ⇒ 要蓋一個頁面(工時等級不同)
4     刻意預設隱藏    ⇒ **Sean 要的**(order-list-view.ts:66-72 註解逐字),缺的只是「有東西被藏」的提示
```
🔴 **把四格講成「都只要開個旗標」是錯的** —— 第 3 格要蓋東西,第 4 格根本不該改預設。

---

# 🔴 而真正該問的那一題

```
這四格的共同點不是「旗標」,是:
  **一個功能從「寫完」到「有人用得到」之間,沒有任何東西在盯。**
· 三綠會過(它們不測「入口在不在」)
· 稽核會過(旗標關著的頁面不會報錯)
· 而 `app-sidebar.tsx:26-34` 的檔頭自己寫著這句(逐字):
  「不會報錯、不會紅,只會**靜默把入口關掉** —— 症狀是【功能做完了但員工看不到】,而三綠全綠」
⇒ **那句話已經在 code 裡好幾天了,而它今晚仍然咬到我們四次。**
```

## 便宜的形狀(建議,要 Sean 拍)
```
甲 一份【旗標現況表】,而它必須有「正式站現在是開是關」那一欄
   ⇒ 🔴 那一欄只有 Sean 填得出來(Vercel 面板)
   ⇒ 而填完之後,它就是「哪些功能其實已經好了」的答案
乙 收工前一道機械檢查:**「這一片新增的入口,在預設設定下看得到嗎?」**
   ⇒ 它擋的正好是這四格裡的 1、2、3
   ⚠️ 而它擋不到第 4 格(那是刻意的)⇒ **不要把第 4 格算進去湊數**
```
⚠️ **我不建議現在就蓋乙那道檢查** —— 先有甲那張表,才知道這件事實際上多常發生。
**這是判斷,不是量到的。**

---

# 附:一個**形狀相反**的,不要跟上面混在一起

```
PCM-2026-0102:付款狀態「已退款」,而後台【沒有退款紀錄那一區】
⇒ 上面四格是「**我們有,而看不到**」
⇒ 這一格是「**我們根本沒有**」—— 那筆退款發生在系統外(極可能在 TapPay 後台)
   ⚠️ 「在 TapPay 後台退的」是推論,我沒有去核
⇒ 🔴 修法方向完全相反:上面四格是接線,這一格是**對帳**(要把系統外的動作抓回來)
```

# 這一份沒有答到的
```
1. 🔴 正式站那四格分別是開是關 —— 只有 Sean 看得到
2. 這個形狀今晚出現四次,而【它總共出現過幾次】我沒有掃 ——
   ⇒ 這是**下界不是全集**(我只數了我自己今晚撞到的)
3. 乙那道檢查做不做得出來 —— 我沒有評估,只寫了它擋得到哪三格
```
