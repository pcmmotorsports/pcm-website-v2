# 「我們對客人說過的話」總表 —— 做得到 / 半句 / 做不到(2026-08-19 G1)

> 主視窗要的三堆分類。**純讀,沒有寄任何信、沒有改任何文案。**
> 前置三份:`2026-08-19-g1-promises-vs-capability.md`(載體盤點)、
> `…-account-compromise-capability.md`、`…-order-address-mutability.md`。

## ⓪ 分母:**我們對客人講話的載體有【四族】,而第四族我們看不到**
```
① legal-content.ts        /terms + /privacy
② rpm-policies.ts         /info/shipping + 商品頁保固 pane + 商品頁 FAQ（**三畫面同一份正本**）
③ 我們自己寄的信          sweep-email-outbox.ts 的 buildOrderCreatedText（**只有一封**）
④ 🔴 **Supabase 自己寄的信** —— 模板在 Sean 的面板裡，**我們讀不到內容**
   從 code 找出【會觸發它寄信】的呼叫點，全樹（排測試）:
     `auth.signUp`              ⇒ **1**（SupabaseAuthAdapter.ts:40）⇒ **註冊驗證信**
     `resetPasswordForEmail`    ⇒ **1**（SupabaseAuthAdapter.ts:89）⇒ **重設密碼信**
     `auth.resend` / `signInWithOtp` / `inviteUserByEmail` ⇒ **各 0**
   ⚠️ `admin.generateLink({type:'magiclink'})`（line-admin.ts:50）**只取 hashed_token、不寄信**
      （它拿 token 自己換 session，不走信件）⇒ **不列入**
⇒ 🔴 **有【兩封】寄給客人的信，內容我們看不到、也沒有人審過。**
```
**掃過而零命中的載體**:`ProductInfo` / `CheckoutView` / `CartView`(字集見前一份)。

## ① ✅ 做得到(客人拿這句來要求,員工/系統接得住)
```
「訂單成立後沒辦法取消或改單」  ⇒ 🔴 **實際上做得到**（後台第 9 站取消、第 8 站編輯訂單）
                                ⇒ 講得比實際嚴 = **口徑不一致**，不是缺口
「我要退款」                    ⇒ 後台第 6 站有
「不適用 7 天鑑賞期」+ 瑕疵認定  ⇒ **立場與判準都寫好了**（rpm-policies）
                                ⚠️ 而它寫在【客人看的頁】，員工得自己去讀
「卡片資訊由 TapPay 處理、本公司不儲存完整卡號」⇒ 屬實（repo 內不存卡號）
```

## ② 🟡 半句成立(**最難發現的一族 —— 客人點進去確實看到東西了**)
```
「訂單明細與最新狀態請至 PCM 會員中心查看」（**訂單信裡**）
   ✅ 「最新狀態」給得出（訂單記錄清單有狀態）
   🔴 「訂單【明細】」給不出 ——「查看詳情 →」是死鈕（OrdersTab.tsx:74 無 onClick，= #240）
「五種個資權利」（隱私政策五、）
   ✅ 收件 / 驗身分 / 30 天內回覆 ⇒ SOP §1–§3 現在就能執行
   🔴 刪除範圍 ⇒ 三格待 Sean 拍板（保存期限 / 硬刪 vs 匿名化 / 窗口是誰）
```

## ③ 🔴 做不到
```
「我們將盡快為您安排出貨」（**信裡 + 訂單完成頁各一次**）
   ⇒ 🔴 **出貨之後沒有任何通知** —— `sweep-email-outbox.ts:112` `order_shipped` **直接 throw**
     （「模板未定義(E4 未落地)、fail-closed 不寄」）⇒ **同一句承諾出現兩次，而都沒有下文**
「收貨 7 天內用 LINE 告訴我們，我們會負責換貨處理」（rpm-policies）
   ⇒ 🔴 **後台零換貨/退貨功能**（`退貨|瑕疵|換貨` 在 admin 11 命中，逐個開檔全是 `return_to`）
「知悉帳號遭冒用時將立即暫停」（服務條款第 12 條）
   ⇒ 🔴 後台零顆鈕；**而有第三條路**（客人自走 /login/forgot，前提待證）
「我要改收件地址」（第 7 條交付的自然延伸）
   ⇒ 🔴 客人改不了、員工也改不了（`shipping_address_snapshot` 全樹零寫入路徑）
```

## 🔴 ④ 而訂單完成頁本身是**乾淨的**(這一格是正面結果)
```
`CheckoutSuccess.tsx` 全檔非註解中文字面 **6 句**:
  「訂單已成立」「付款處理中」「付款狀態確認中」「付款未完成」「**我們會盡快為您出貨。**」「返回購物車」
⇒ **沒有承諾天數、沒有叫客人去收信、沒有退換貨字樣** ⇒ **它沒有多說任何做不到的話。**
📌 而唯一那句承諾（盡快出貨）與信裡那句**是同一句** ⇒ 它的缺口在 §③ 已列。
```

## ⑤ 我沒做/沒查的
```
· 🔴 **Supabase 那兩封信的內容我讀不到** —— 它們在 Sean 的面板裡
  ⇒ **那本身是結論:有一族寄給客人的信，沒有人審過它寫什麼**
· 🔴 **LINE 官方帳號的自動回覆與過去客服對話** —— 而 rpm-policies 那句換貨承諾的**管道就是 LINE**
  ⇒ 客人講過什麼、我們回過什麼，全在那裡，而它不在 repo 裡
· 沒有讀正式庫（**已寄出幾封信、幾位客人看過那句話，我查不到**）
· 字集是我列的 ⇒ **寫得含蓄的承諾這把尺撈不到**，本表是下界
```

---

# 🔴 ⑥ 補遺(2026-08-19 04:5x,**用不同的方法重掃之後**)

## 方法變了 —— 而那是這一節存在的原因

上一輪我用 **grep 找句子**;這一輪改成**把富文字結構攤平成純文字再整段讀**
(把 `{ b: '…' }` 換成它的內容,再抓所有含中文的字串字面)。
**那不是更用力的 grep,是不同的方法。**

```
量法（可重跑）：對 ProductFAQ.tsx 剝註解 → 攤平 { b: } → 抓所有含 CJK 的字串字面
⇒ **22 條**（含標題與連接詞）
```

⇒ **撈到三條上一輪沒有的**,其中一條是硬缺口。

---

## 🔴 補進 §③【做不到】:**付款方式承諾了三種,結帳只給一種**

```
客人在商品頁 FAQ 讀到（ProductFAQ.tsx:36）：
  「付款：目前沒有貨到付款，可用 **銀行轉帳、線上刷卡、LINE Pay** 支付。」

而結帳頁實際渲染（CheckoutStep2.tsx:276-287，讀 code 不是讀註解）：
  `.co-pay-list` 內 **只有一個 <label>**
  `<input type="radio" name="pay" checked readOnly aria-label="信用卡付款" />`
  文案「信用卡付款 / VISA · Mastercard · JCB · AE，3D 驗證」
而同檔 :34 註解逐字：「**ATM 轉帳不做（plan §3.2 隱藏）→ 只渲染信用卡單一付款方式**」
```

🔴 **三種裡有兩種在結帳頁不存在**,而其中一種(ATM/轉帳)是**刻意隱藏的決定**、不是還沒做。
🔴 **而這一條比「換貨」更早發生** —— 換貨要等到收貨之後,
**這一條擋在【客人正要付錢】的那一步**:他照 FAQ 選好了 LINE Pay,走到結帳只剩信用卡。

📌 **修法便宜得不成比例**:這是**改一句文案**,不是做兩個金流。
   而依 Sean「不准做一半」的常設偏好,**該改的是文案去對齊能力**,不是把缺口寫進 backlog。
⚠️ 但**改文案要 Sean 拍板口徑**(他可能真的收轉帳、只是不在線上結帳走)
   ⇒ 這一條**不是我可以自己改的** —— 見下方「要問 Sean 的」。

---

## 🟡 補進 §②【半句】:「預約合作店家(全台都有點)」

```
FAQ 講了兩次（ProductFAQ.tsx，安裝那題）：
  「也可以**預約我們的合作店家（全台都有點）**，安裝費用依商品與工時而定」
  「安裝則可**透過全台合作店家協助**」
而 `/stores` 與 `/install` **兩條路都是 ComingSoon**
  （`app/stores/page.tsx:37` / `app/install/page.tsx:30` 都 render `<ComingSoon>`）
```

## ✅ 而這一格**已經被誠實處理過了** —— 它是正面例,不是缺口

```
`/install` 給客人的字（逐字）：
  「線上安裝預約正在開發中…**現在要約安裝一樣沒問題 —— 先用 LINE 找我們，照樣幫您排。**」
`/stores` 給客人的字（逐字）：
  「合作店家地圖正在整理中…**在那之前想找店家安裝，直接用 LINE 跟我們說，我們幫您接。**」
```
📌 🔴 **兩頁都沒有停在「即將上線」就走掉,而是【指出現在可以走的那條路】。**
   —— 與 `cancel-review-section.tsx` 把「要走退貨流程」改成「退貨功能目前還沒有」是**同一種做對**。
⚠️ 而那條可以走的路**又是 LINE** ⇒ 它能不能兌現,一樣落在我們看不到的地方。

---

## ✅ 而運費那條是乾淨的(第三條,無缺口)

FAQ 的運費字面**吃 `packages/domain/src/order/shipping.ts` 的常數、不 hardcode**
(`ProductFAQ.tsx` 該行上方註解逐字寫了為什麼:同一畫面另有兩處講免運門檻,**分歧會被 Google 讀走**)。
⇒ 這一格**在我掃到之前就有人修過了**。

---

## 🔴 而這一節本身要留一句方法論

```
上一輪與這一輪掃的是【同一批檔案】，結論不同 —— 差別只在**方法**。
⇒ 一份「我們對客人說過的話」的總表，它的完整度**取決於你怎麼讀那些檔**，
   而 grep 讀到的是【字串】，客人讀到的是【句子】—— 那是兩個不同的東西。
⇒ 🔴 下一個要重做這件事的人：**先攤平，再讀。不要先想 pattern。**
```

## 要問 Sean 的(這一節新增,兩題)

```
1. 付款方式那句文案要怎麼改？
   （現況：FAQ 寫「銀行轉帳、線上刷卡、LINE Pay」，結帳只有信用卡）
   ⚠️ 我不知道他是不是【線下真的收轉帳】—— 若是，文案沒錯，錯的是它寫在「下單付款」那一題裡
2. 而 LINE Pay 是「還沒做」還是「不打算做」？
   —— 那決定文案是改成「目前僅信用卡」還是「線上僅信用卡，其他方式請 LINE 洽詢」
```

---

# 🔴 ⑦ 補遺之補遺:付款方式那條**不是文案 bug,是【design 跑在實作前面】**

上面 §⑥ 我寫「⚠️ 我不知道他是不是【線下真的收轉帳】」並把它列成要問 Sean 的兩題。
**查完之後那兩題的形狀變了 —— 而其中一題【不成立】。**

## ① 那句文案**忠實照搬 design**(鐵則 1 沒有被違反)
```
OD 專案 `pcm-home-redesign`（本窗以 open-design 親查，非轉述）：
  content-pages-data.js:50   [{ b: '付款：' }, '目前沒有貨到付款，可用 ', { b: '銀行轉帳、線上刷卡、LINE Pay' }, ' 支付。']
  source/components/ProductFAQ.tsx:35   同上，逐字
  product-detail-data.js:338            同一句（markdown 版）
⇒ **storefront 那句與 OD 逐字相同，連 `{ b: }` 的切法都一樣。**
⇒ 🔴 **沒有人「寫錯文案」** —— 有人**正確地**照搬了 design。
```

## ② 而**ATM/轉帳是 Sean 自己拍過的 Phase 2**
```
docs/specs/2026-06-13-m3-3ds-webhook-master-plan.md:109 逐字：
  「**Sean 拍**：… S3=B（**ATM Phase 2**）…」
docs/specs/2026-06-04-m3-checkout-plan.md:76：ATM（隱藏）列在不做清單裡
docs/specs/2026-06-12-m3-stage2-4-tappay-fields-plan.md:23 逐字：「ATM 不做(plan §3.2、階段① 已拍)」
```
⇒ 🔴 **所以「Sean 是不是線下收轉帳」那一題【不成立】** —— 他已經拍過了,
   而拍的內容是**這個功能延到 Phase 2**。**我不該把它端上去當決策題。**
   (照 memory `feedback_look-for-the-path-that-dissolves-the-question`:查了才問,而我這次是先問了才查。)

## ③ ⇒ 真正的形狀:**design 描述三種,實作出一種,而客人讀到的是 design 那一版**

```
🔴 這正是「逐字搬 design 會把上游的東西一起搬進來」的一個【客人看得到】的實例 ——
   而這次搬進來的不是 bug，是**一個 design 已經寫好、而實作依 Sean 拍板延後的功能**。
⇒ 鐵則 1 要求不要重新詮釋 design，**而它沒有要求把 design 的【時間軸】也一起照搬。**
```

## ④ 而它剩下**一題**要 Sean 拍(不是兩題)

```
Q：商品頁 FAQ 的付款那句要不要改？（現況：寫三種，結帳只有信用卡）
   甲：**改 storefront、不動 OD** —— 例如「目前線上結帳僅支援信用卡；
       其他付款方式（銀行轉帳、LINE Pay）規劃中，需要請先用 LINE 與我們聯絡」
       ⇒ 偏離 design，要在 commit body 寫明理由（鐵則 1 的正當偏離）
   乙：**改 OD、storefront 跟著改** ⇒ design 與實作對齊，而要動設計專案
   （推薦甲：OD 描述的是**完成後的樣子**，而客人現在看的是**今天的站**。）
```
⚠️ 而**LINE Pay 我查不到任何拍板** —— 只在 `CheckoutStep2.tsx:23` 的註解裡以
「未來新增 LINE Pay / 虛擬帳號時」出現(**那是實作者的預留,不是拍板**)。
⇒ 這一格標**未確認**:LINE Pay 是「延後」還是「從未討論」,**我答不出來**。

---

# 🔴🔴 ⑧ 更正 §① —— **我把一個做不到的東西放進了「做得到」那一堆**

§① 我寫著:
> 「訂單成立後沒辦法取消或改單」⇒ 🔴 **實際上做得到**(後台第 9 站取消、第 8 站編輯訂單)

**兩半都不成立。** 去驗自己那句最強的宣稱之後(2026-08-19 05:4x):

## 取消:**已付款的單,取消不了** —— 而那正是客人會想取消的那種
```
權威層（不是 advisory）：`admin_cancel_order`
  supabase/migrations/20260805100000_m4b_e10_a8a2_partial_cancel.sql:360-364
    IF v_order.payment_status <> 'unpaid'::public.payment_status
       OR EXISTS (… payment_charge_attempts a WHERE a.status <> 'failed') THEN
      RAISE EXCEPTION …
（`20260805100000` 在 `supabase/APPLIED.tsv` ⇒ 已上線）
⇒ **只有「完全沒付款、而且所有扣款嘗試都是 failed」的單取得消。**
```
🔴 而 `cancel-view.ts:70-74` 的註解自己記著一個**與拍板的落差**:
> 「Sean 2026-07-26 拍板逐字『**已付款可取消**』,而第 1 批的 RPC 對已付款單**一律 fail-closed**
> (母 plan row 65:「取消需退款,**退款線第 3 批開通**」)⇒ 文案要寫成**「這期還不行、第 3 批開通」**,
> 不是「不能取消」—— 否則員工會以為那是永久規則,**與 Sean 的拍板衝突**。」

⇒ ✅ **那段註解做對了兩件事**:記下落差、而且**指定了文案的寫法**,防員工誤以為是永久規則。

## 改單:**只有四個欄位,而沒有一個是客人講的那三種**
```
`order-edit-form.tsx` 全部欄位（量法：grep "AdminFormField label=" 該檔）：
  **出貨方式 / 開票狀態 / 發票號碼 / 發票金額** ⇒ **四個，全是內部與發票**
而客人講「改單」是指：
  改品項 / 改數量 ⇒ 改金額那支 RPC **對已收款單直接拒**（本檔 §⑦ 與 exchange-traces §②）
  改收件地址     ⇒ **全樹零寫入路徑**（見 `docs/probes/2026-08-19-g1-order-address-mutability.md`）
```

## ⇒ 所以那句文案**比我給它的評價準確**
```
「訂單成立後沒辦法取消或改單」
⇒ 🔴 **它基本上是真的。** 我原本判它「講得比實際嚴」——**過寬的是我，不是它。**
⇒ 唯一不準的一格：**未付款的單其實取得消**（而那種單客人多半也不在意）
```

## 📌 而這一格讓「付款之後凍住」從兩條變**三條**
```
① 換貨補寄     ⇒ 已收款 ⇒ 改金額被拒
② 離島運費另議 ⇒ 已收款 ⇒ 改金額被拒
③ **取消**     ⇒ 已付款 ⇒ 取消 RPC 直接 RAISE
🔴 技術上是**兩支不同的 RPC**，而形狀是同一個：
   **付款之後，這張單基本上凍住了，而三條線都在等【退款線第 3 批】。**
```

## 🔴 而我要記自己這一次
```
我把「後台有這個功能」讀成「這件事做得到」——
而中間差的是**那個功能在【客人真的會遇到的狀態】下按不按得動**。
⇒ 判別句（下次寫「做得到」之前跑）：
   **我確認的是「這顆按鈕存在」，還是「它在那張單的狀態下按得下去」？**
⇒ 而 🔴 **「做得到」這一堆是最危險的一堆** —— 一句錯的「做不到」會有人來反駁，
   一句錯的「做得到」**沒有人會反駁，而它會被拿去回覆客人。**
```
