# 我們對客人講過的話 —— 哪幾句員工今天接不住(2026-08-19 G1)

> 這是我自己在「12 站漏了哪一族」標的洞:**只掃了 `legal-content.ts`,`/info/*` 沒掃**。
> 主視窗要我把範圍再放寬一格(商品頁 / 結帳頁 / 訂單完成頁 / **已寄出的信**)。
> **純讀。沒有寄任何信、沒有改任何文案。**

## ⓪ 對客人講話的載體:**只有三處,而其中兩處共用同一份正本**
```
① `apps/storefront/src/data/legal-content.ts`  ⇒ /terms + /privacy（上一份已掃）
② `apps/storefront/src/data/rpm-policies.ts`   ⇒ **三個畫面共用同一份**:
     /info/shipping（InfoShippingPage）
     商品頁「保固與退換」pane（ProductTabs.tsx:343 → RPM_WARRANTY_PARAGRAPHS）
     商品頁 FAQ（ProductFAQ.tsx:83 → **同一個常數**，註解逐字「不分歧(Sean 2026-06-03)」）
   ⇒ ✅ **好消息:沒有第三份副本。改一處三個畫面一起改。**
③ 訂單信正文（`packages/use-cases/src/sweep-email-outbox.ts` 的 buildOrderCreatedText）
```
**掃過而【零命中】的**:`ProductInfo` / `CheckoutView` / `CheckoutSuccess` / `CartView`
(字集 = `保固|保證|包退|可退|退換|免費|負責|一定|承諾|賠|七天|天內|工作天|到貨|出貨時間`)
⇒ **結帳頁與購物車沒有對客人做任何承諾。**

## 🔴 ① 已寄出的信裡那句(**已單獨急報,`ed0e7c3f`**)
```
信的全文:「您的訂單 {id} 已付款成功。/ 我們將盡快為您安排出貨；**訂單明細與最新狀態請至 PCM 會員中心查看。**」
🔴 「訂單【明細】」指向的「查看詳情 →」是**一顆死鈕**（OrdersTab.tsx:74，無 onClick；= backlog #240）
🔴 而「盡快安排出貨」之後**沒有出貨信** —— `sweep-email-outbox.ts:112` `order_shipped` **直接 throw**
⇒ **客人拿到的唯一一封信，一句指向死鈕，一句預告一封永遠不會來的信。**
⚠️ **已經寄出幾封我查不到**（`email_outbox` 在正式庫）—— 那個數字只有有 DB access 的人查得到。
```

## 🔴 ② `rpm-policies.ts` 裡**具體、有時限**的承諾 ⇒ **員工手上沒有工具**
```
逐字:「收到商品請先檢查，如果有【瑕疵】、或是我們出錯（寄錯、出錯件），
      請在【**收貨 7 天內**】用 LINE 告訴我們，**我們會負責換貨處理**。」
```
⇒ 🔴 **後台零換貨/退貨功能**(我上一份實查:`退貨|瑕疵|換貨` 在 `apps/admin/src` **11 命中,逐個開檔全是 `return_to` 路由參數** ⇒ 實際 0)。
⇒ **這一句比服務條款那條更硬**:條款講的是法定瑕疵擔保(抽象),**這一句給了【管道】(LINE)、【時限】(7 天)、【動作】(換貨)。**
⚠️ 而**我沒有查「換貨實務上怎麼跑」** —— 店裡可能有紙上流程(見 §④)。

## 🔴 ③ 一句**與後台能力相反**的話(而方向是安全的)
```
rpm-policies 逐字:「訂單成立後**沒辦法取消或改單**，麻煩下單前先確認好車款與款式。」
而後台:**第 9 站【取消】**、**第 8 站【編輯訂單】** —— 兩站都在，員工做得到
```
⇒ **我們對客人講得【比實際嚴】。** 那個方向是安全的(不會做不到),
**而它的代價是:客人不會問,而員工可能不知道自己其實做得到。**
📌 ⇒ **這一格不是缺口,是【口徑不一致】** —— 而口徑要 Sean 定,不是我們對齊。

## ✅ ④ 而我要**更正自己上一份的一句話**
上一份我寫:「E(七天鑑賞期)…**而沒有任何地方寫給員工看**」。
**那句不準。** `rpm-policies.ts` 把立場與判準寫得很完整:
```
「本賣場屬於**客製化委任代購**，依《消保法》第 19 條第 1 項，這類客製、代訂商品**不適用 7 天鑑賞期**。
  鑑賞期是讓您確認商品符不符合需求，**不是商品的試用期**——這點先跟您說明，下單前確認好就沒問題。」
瑕疵認定:**紋路明顯錯位、表面破損、孔位偏差、超過合理公差範圍**
不在範圍:**人為碰撞、摔車、不當安裝、自行加工**
```
⇒ 🔴 **話術是有的,而它寫在【客人看的頁面】上,不在員工手冊裡。**
⇒ **正確的說法是**:「**員工得自己去讀商品頁才知道公司立場**」,**不是「沒有人寫過」。**
📌 **修法因此也不同**:不是「寫一份話術」,是「**把已經寫好的那份指給員工**」——**便宜很多。**

## ⑤ 我沒做/沒查的
```
· 沒有寄任何信；沒有讀正式庫（**已寄出幾封、幾位客人看過那句話，我查不到**）
· 沒有查「換貨/取消」實務上怎麼跑 —— 那要問店裡（而那正是我上一份建議先做的事）
· 字集是我列的（保固|保證|包退|…）⇒ **寫得很含蓄的承諾這把尺撈不到**，本表是下界
· 沒有掃 LINE 官方帳號的自動回覆 / 過去的客服對話 —— **那也是「我們講過的話」，而它不在 repo 裡**
```
