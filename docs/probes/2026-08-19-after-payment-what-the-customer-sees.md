# 付款【之後】那一段:客人看得到什麼、我們看得到什麼(2026-08-19 03:5x CST,G3)

> 前幾輪走的是「客人走到付款前」與「員工做不做得到」。
> 這一輪是**付款之後** —— 而它有一個我一開始就要講的邊界。

## 🔴 邊界:Sean 那三筆單**不是我的帳號**

```
H7CPXQ / 2DTX42 / 8X3N5Q 都掛在「洪紹閔」名下,而我的測試帳號是 G3 沙盒測試。
⇒ 🔴 **「用客人的眼睛看那三筆」= 登入別人的帳號。我沒有做,也不會做。**
⇒ 所以本檔凡是「客人看得到什麼」的結論,來源是【讀 code】或【後台的員工視角】,
   **不是我扮成他去看的**。每一格都標了來源。
```

## 效度限定
```
量的是   本機後台 localhost:3001 + ADMIN_DEV_BYPASS=1 + 連正式庫(員工視角)
         + repo 原始碼
時間     2026-08-19 03:48–03:55 CST(`date` 實跑)
🔴 唯讀:沒有下單、沒有付款、沒有退款、沒有寄信、沒有登入別人的帳號
```

---

# 一、客人付完錢,他當下看到什麼 ⇒ **沒有獨立的「完成頁」**

```
來源:apps/storefront/src/hooks/useChargePayment.tsx:197-203
成功時做三件事:clear()(清車)、regenerateCartSession()、
  setState({ status: 'paid', displayId: res.displayId })  ← **停在結帳頁,切成 paid 狀態並顯示單號**
⇒ 沒有 router.push、沒有導去 /success 之類的路由(該檔內零 router.push 命中)
```
⇒ **客人看到的是同一頁變成「已付款 + 訂單編號」,不是一個新的完成頁。**
⚠️ 我**沒有實際看到那個畫面**(要真的付款才會出現)⇒ 這一格是**讀 code**,標明來源。

---

# 🔴 二、他會收到信嗎 ⇒ 信的機制在,而**員工看不到那封信的下落**

## 機制存在
```
supabase/migrations/20260717020000_m4a_email_outbox.sql:297  CREATE TABLE public.email_outbox
狀態集(同檔 :317):pending / sending / sent / failed / skipped_no_real_email / skipped_order_ineligible
⇒ **有一張表在記「這封信到哪一步了」。**
```

## 🔴 而後台**沒有任何地方看得到它**
```
數法:grep -rln "email_outbox" apps/admin/src(排除 *.test.*)⇒ **0 個檔**
🔴 負向對照(尺會動):同字串掃 storefront 與 packages ⇒ 3 個檔
   (api/cron/email-sweep/route.ts、lib/email/composition.ts、adapters 的 database.types.ts)
```
⇒ **「客人到底有沒有收到訂單信?」—— 員工在後台【問不出來】。**
⇒ 📎 這與稽核頁那一格同族(**機制在、可見性不在**),而**這一格更常被問到** ——
   客人打來說「我沒收到信」,員工手上沒有任何東西可以查。

## ⚠️ 而 Sean 那三筆到底有沒有進 outbox ⇒ **我答不出來**
```
那要讀正式庫的 email_outbox 那幾列,而我沒有 DB 查詢權限。
⇒ **需要一次唯讀查詢**(或把那張表接進後台)。**我沒有繞。**
```

---

# 🔴 三、退款那條:**兩筆有完整紀錄,而一筆什麼都沒有**

## 有紀錄的兩筆(員工視角,後台實查)
| 單號 | 下單 | 退款發起 | 間隔 | 紀錄內容 |
|---|---|---|---|---|
| `5HGMC5` | 2026-08-11 02:10 | 2026-08-14 02:27 | **約 3 天** | 全額 NT$340 / 狀態「退款完成(客人入帳時間依照各家銀行而定)」/ 原因 `test` / 發起人 `sean` |
| `RCPVVJ` | 2026-08-11 02:07 | 2026-08-14 02:27 | **約 3 天** | 同上格式 |

⇒ ✅ **對照「15 日內退款」那個承諾:這兩筆是 3 天,遠在承諾之內。**
⇒ 而**這是真實樣本,不是推論** —— 上一輪我只證明「沒有機制在數 15 天」,
   這一輪證明**實際操作遠比承諾快**。**兩件都成立,而它們不衝突。**

## 🔴 而第三筆:`PCM-2026-0102`
```
付款狀態:已退款
🔴 而那一頁**完全沒有「退款紀錄」這一區**(實測:整頁純文字找不到「退款紀錄」四個字)
⇒ 員工看到的是「已退款」三個字,而**沒有金額、沒有日期、沒有誰做的、沒有原因**
```
**而這不是 bug,是一個已知類別** —— 同一個後台自己在退款區寫著(逐字):
> 僅計本系統帳本(處理中+已受理佔額);**Portal 場外退款不在其中**,
> 真實可退額以 TapPay Record 對帳為準、只會 ≤ 此數。

⇒ **那筆極可能是直接在 TapPay 後台退的**(⚠️ **這是推論,我沒有去 TapPay 後台核對**)。
⇒ 🔴 **而後果是實的**:那筆的退款**在我們的帳本上不存在**
   ⇒ 對帳、客訴、以及「15 日」那個時鐘,**都沒有起算點**。
⇒ 分類:**不是缺陷,是一個入口在系統外** —— 而它會讓「我們的帳本 = 全部退款」這句話變假。

---

# 四、客人自己看得到「已退款」嗎 ⇒ **看得到**(讀 code,非扮演)

```
apps/storefront/src/lib/orders/order-display.ts:60  return '已退款';
同檔 :14 的對照表逐字:| refunded |(任意)| 已退款 |
⇒ 客人的訂單列表會把 refunded 顯示成「已退款」
```
✅ 而**那道 `.neq('payment_status','unpaid')` 濾不掉它**(refunded ≠ unpaid)⇒ 退款單客人看得到。
⚠️ 「多久之後看得到」= 只要 `orders.payment_status` 翻成 refunded 就看得到 ——
   **而那一刻取決於誰去記那筆退款**;像 `PCM-2026-0102` 那種場外退款,狀態仍是 refunded ⇒ 客人也看得到。

---

# 五、順手關掉我上一輪自己標的「未查」

上一輪我建議把「立即暫停帳號」的文案改成「協助您變更密碼」,而我標了**未查**。**現在查了**:
```
🔴 後台【完全不碰 auth】:
   數法(尺已先驗過):grep -rn "requestPasswordReset|getAuthService" apps/admin/src ⇒ **0**
   負向對照:同一把尺打 apps/storefront/src ⇒ **4 個檔**
     (register/actions.ts、account/actions.ts、login/reset/actions.ts、login/actions.ts)
✅ 而客人【自己】重設得了:/login/forgot ⇒ 200、/login/reset ⇒ 200(線上實測)
```
⇒ **準確的說法是**:
```
「協助您變更密碼」只有在【客人自己還進得去他的信箱】時才成立 ——
因為那條路是**客人自助**,員工手上沒有任何按鈕可以幫他。
🔴 而帳號被盜的典型情境正好是【攻擊者已經改掉信箱】⇒ 那條路也斷了。
⇒ **所以那句替代文案我不建議照用。** 要改文案,得先想清楚員工那一刻【真的做得到什麼】。
```

---

# 這一份沒有答到的

```
1. 🔴 Sean 那三筆有沒有進 email_outbox、狀態是什麼 —— 需要正式庫唯讀查詢,我沒有
2. 客人付完錢那個畫面長什麼樣 —— 讀 code 得到的,**我沒有真的付款**
3. `PCM-2026-0102` 是不是真的在 TapPay 後台退的 —— **推論,我沒有去 TapPay 後台核**
4. 「他付了錢而我們沒收到」那種情況客人看到什麼 —— 那要構造一個失敗付款,
   而我今晚已經留了兩張孤兒單 ⇒ **不再製造**(主視窗已裁定停止刷卡)
5. 那三筆的客人視角 —— **不是我的帳號,我沒有看**
```
