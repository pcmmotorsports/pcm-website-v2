# `#473` 樞紐:體積與順序盤點(**只盤,不寫 plan、不動 code**)

> 2026-08-19T02:39 CST(`date` 實跑) · G5 · **純讀,零改動**
> 交辦:主視窗轉 G6 的發現 —— `#473` 同時是 `#497` 的上游、又是 `#445b` 的前置

---

## §0 三格結論先講

```
① 三條【不是】同一片 —— 是【兩片 + 一個硬順序】
   #473 接線 + #497   ⇒ 🔴 要同一片(理由見 §1)
   #445b              ⇒ 不同片,而 #473 是它的【前置】
② #473 那半是【純接線】—— RPC 已 apply、零呼叫端、且不需要動任何 GRANT
③ ⛔ **本行初版寫錯,已更正 —— 見 §3-b**
   ~~卡在 Sean 的是【#445b 的 apply】,不是 #473 的接線
     ⇒ 所以 #473+#497 那一片【現在就可以做,不卡他】~~
   ✅ 正解:#473 的【接線】不卡人;而 #497 的修法是【改 payment_status 翻轉語意】⇒ **要 Sean 拍**
        而 #473 接線【不能單獨做】(會製造 §1 那個壞狀態)⇒ 🔴 **這一片仍然卡 Sean**
        —— 而卡的是【一題決策】不是【一次 apply】
```

## §1 ① 三條真的要同一片嗎(用 G6 的判別句)

**判別句**:只做 A 不做 B,會不會**新增一個原本不存在的壞狀態**?

### `#473` + `#497` ⇒ 🔴 **要同一片**
```
今天    manual_failed 是死路 ⇒ 沒有人在更正判定 ⇒ #497 的症狀【很少被觸發】
只做 #473 接線 ⇒ 員工開始更正判定
       而客人的訂單頁仍然停在「處理中」(payment_status 走不到 refunded)
       且【員工無從得知】客人看到的是錯的
⇒ 新增的壞狀態:「**我更正好了**」與「**客人看到的仍是錯的**」同時成立,而沒有人會發現
⇒ 那不是把舊 bug 留著,是把一個【很少發生】的東西變成【每次更正都發生】
```

### `#473` + `#445b` ⇒ **不同片,而順序是硬的**
```
backlog:13311 逐字「#445 的 apply 停點是【#473 沒有出口之前不得 apply 445b】」
⇒ #473 是 #445b 的【前置】,不是同一片
⇒ 只做 #445b 不做 #473 ⇒ 那張單永遠退不了款而解鎖入口不存在 ⇒ 所以才有那個停點
⇒ 而反過來(只做 #473 不做 #445b)【不會製造任何壞狀態】—— 那正是它可以先做的理由
```

## §2 ② 體積

### `#473` 那半:**純接線**(已量到,不是估)
```
RPC 已 apply   supabase/APPLIED.tsv:191   20260814190000   2026-08-14
應用層呼叫端   git grep 'admin_correct_order_refund_verdict' -- apps packages
               ⇒ 🔴 只有 packages/adapters/src/supabase/database.types.ts:3515(產生的型別)
               ⇒ 【零真呼叫端】
同族那支       admin_correct_refund_manual_verdict
               ⇒ refund-recovery-state.ts:74,而那是【註解】不是呼叫
GRANT          不需要動 —— admin 走 service_role(G6 已量,我採用並標明轉述)
```

### 要動幾檔 ⚠️ **這一格是【估的】,依據是同族已接好的那條路**
```
對照組 refund-recovery-* 那條路的形狀:
  lib/payment/  refund-recovery-actions.ts / -form.ts / -read.ts / -state.ts(+ 各自的 .test)
  components/orders/  refund-exception-resolve.tsx(+ .test)
⇒ 照同一形狀,#473 接線約 6-10 檔(含測試)
🔴 而那是【類比推估】不是量測 —— 真值要等實作那一步才知道
```

### 主視窗點名的兩條既有約束,對這片成不成立
```
① composition.ts 的 Pick(AdminRefundTapPay = Pick<…,'refund'|'recordQuery'>,:126)
   ⇒ 🔴 【不成立】—— 更正判定不碰金流
      那支 RPC 的參數:p_actor / p_corrected_to / p_expected_correction_id /
                      p_reason / p_refund_id / p_request_id ⇒ 全是帳本欄位,零金流參數
   ⇒ 這片【不需要放寬那個 Pick】
② 「admin 沒有 DB 憑證」
   ⇒ ⚠️ 這一條我讀不懂它指什麼 —— admin 明明走 service_role 才做得到今天的事
   ⇒ 標【未確認】,請交辦者說明它原本要防的是什麼
```

## §3 ③ 順序(不可換的只有一組)

```
可以現在做   #473 接線 + #497   ← 不卡任何人
然後才能     #445b apply         ← 卡 Sean(apply 一律要他)
🔴 而 #497 與 #473 之間【沒有】順序 —— 它們要在同一片裡一起上,不是誰先誰後
```
## §3-b ✅ `#497` 那個「一個量級的不確定性」**收掉了**(2026-08-19T02:52 CST)

**答案在那支 migration 的檔頭,是它的作者自己寫的**(`20260814190000_…473b1_refund_manual_corrections.sql:29-34` 逐字):
```
🔴 已知會漏、而且本片刻意不修的一條(plan §6-3,已立案 `#497`)
`20260803150000:776-779`(admin_finalize_order_refund 步 7)**自己 SUM** status='confirmed'
決定 orders.payment_status,不呼叫本函式。一筆 money_moved 更正不會讓該列變 confirmed
⇒ **payment_status 可能停在 paid 而錢已全退**,而 storefront order-display.ts:49-50
  直接吃它 ⇒ **客人看到的是「處理中」**。改它 = 改 payment_status 的翻轉語意 = 另一個方向題,
  要 Sean 拍 ⇒ **不在本片修**。這是「已知會對客人顯示錯」,不是「未來優化」。
```
同檔 `:388` 再說一次:「**orders.payment_status 不受影響(#497)**」。

### ⇒ 三個結論,逐條
```
① 🔴 接 #473 【不會】關掉 #497 —— 更正 RPC【不重算 payment_status】,作者明文
   ⇒ 我 §1 那個「只做 #473 會新增壞狀態」的推論【成立】,而現在它有作者的逐字背書
② 🔴 #497 這個條目裡有【兩件事】,而 E 窗只答掉一件
   E 窗答的  「兩套 SUM 該不該統一」⇒ 不該,要補的是註解與值域斷言
   E 窗沒答的 「客人頁面永遠停在處理中」⇒ 那要【改 payment_status 的翻轉語意】
③ ⇒ 體積的答案是【大的那一個】:不是補註解,是改狀態流轉
   而作者已經標明「**要 Sean 拍**」⇒ 🔴 **所以 #497 那半【卡 Sean】**
```

### 🔴 而這改變了 §0 的結論,我就地更正
```
⛔ §0 原本寫:「#473 接線 + #497 那一片【現在就可以做,不卡任何人】」
✅ 更正:  #473 的【接線】不卡任何人;而 #497 的【翻轉語意】卡 Sean
⇒ 兩條的正確處置變成:
   · 一起送 Sean:一個決策題(payment_status 的翻轉語意要怎麼改)
   · 而在他答之前,#473 接線【單獨做】會製造 §1 那個壞狀態 ⇒ 🔴 不要單獨做
⇒ 所以這一片【還是卡 Sean】,而卡的是【一題決策】不是【一次 apply】
```
📌 **我上一封跟主視窗說「那一片現在就可以做」—— 那句是【在讀那支 migration 檔頭之前】說的,現在撤回。**
而病根:我讀了 backlog 條目與 E 窗結論,**而沒有讀那支 migration 自己的檔頭** ——
**那份檔頭正是「這件事為什麼沒做」的唯一權威**(memory:契約債與交辦寫在建表 migration 的註解裡)。

---

## §3-c ⛔ 以下是收掉之前的原文(訃聞,不要照它讀)

⚠️ ~~**而 `#497` 的修法範圍我沒有確定**~~:
E 窗 2026-08-16 的結論逐字「**經查不是不一致;統一它們會製造 bug。要補的是註解與值域斷言,不是統一。**」
—— 那段答的是「兩套 SUM 該不該統一」。
🔴 **而它答不答得掉條目標題那個【客人頁面永遠停在處理中】,我看不出來。**
⇒ **標未確認**,而它會直接改變這片的體積(補註解 vs 改狀態流轉,差一個量級)。

## §4 ④ 鐵則判定

```
12① 錢          ✅ 命中 —— 動退款帳本的判定
12③ DB 結構     ❌ 不命中 —— migration 已 apply,這片零 migration
8   跨 3+ 檔    ⚠️ 依 §2 的估(6-10 檔)⇒ 幾乎確定命中,而【要量完才敢說】
⇒ 高風險片:關卡1 + 關卡2 都不降級
```

## §5 卡在誰身上

```
🔴 #445b 的 apply        ⇒ Sean(apply 一律要他)
🔴 §4-1 C 族出口          ⇒ 交辦裡點名的第二格,而我【沒有查它】⇒ 未確認,不假裝
✅ #473 接線 + #497       ⇒ 不卡任何人(RPC 已 apply、不需 GRANT、不碰金流)
```

## §7 ✅ `§4-1 C 族出口` —— **它已經有答案了,而答案又住在 migration 的 COMMENT 裡**(2026-08-19T03:01 CST)

### ① 「C 族」是什麼(定義貼原文,而它住在別人的 plan 裡 —— 我只讀)
`docs/specs/2026-08-19-manual-review-queue-full-scope-plan.md:133` 逐字:
```
| **C 族** `superseded` + `charged` | 舊 attempt 標記已扣款,而訂單仍未付 | **無** |
```
⇒ **C 族 = 舊的付款嘗試被標成「已扣款」,而那張訂單仍然沒付。**

### ② 出口 ⇒ 🔴 **已經存在,而且是 Sean 親自裁的 —— 不是「零出口要新造」**
同 plan `:237-240` 說它發現 Sean 已裁過。**我不採信轉錄,自己開了那支 migration 核**:
`supabase/migrations/20260810170000_m4b_lifecycle_l5b0_reject_superseded_charge.sql:237` 逐字:
```
superseded_at 非 NULL(=L5a-1 判定讓路、客人已改走新單)⇒ **一律 RAISE、永不轉 charged**;
**那筆錢的唯一出口是退款(Sean 2026-08-10 拍板 A)**
```
⇒ ✅ **C 族的出口 = 退款。已裁,2026-08-10。**
⇒ ⛔ **所以 plan 裡「零出口,要新造」那句是【過期的】** —— 而 **plan 自己已經把它劃掉並留痕**,寫著
「下面這段是**在不知道那條拍板的情況下寫的**,留痕不刪、而**不得照它動手**」。

### ③ ⇒ 那還在等 Sean 的到底是什麼(這一格容易被讀錯)
```
❌ 不是「C 族的出口是什麼」        ⇒ 已答:退款
✅ 是「那條拍板讓【完整範圍】縮了,可不可以縮」
   plan 逐字:「若那條拍板成立 ⇒『完整範圍』的定義會縮,而【縮範圍是 Sean 的決定】(他拍過「不准做一半」)」
⇒ 🔴 所以送他的題目不是技術題,是【範圍題】:
   「你 08-10 裁的『唯一出口是退款』成立 ⇒ 佇列那片就不必為 C 族新造出口了。要不要照這樣縮?」
```

### ④ 用交辦給的判別句驗一次
> **有入口而沒有出口 ⇒ 東西會累積,而告警會【永遠響】**
```
C 族有沒有入口?  有(舊 attempt 被標 charged)
C 族有沒有出口?  ✅ 有 —— 退款。而它【不是新造的】,是既有的退款路徑
⇒ 所以「永遠響」那個病在 C 族【不成立】
⚠️ 而我【沒有查】退款那條路今天走不走得通(那要看 #473/#497 那一串)
   ⇒ 出口【在設計上存在】≠ 出口【今天按得到】。這一格標未確認
```

### ⑤ 🔴 而這是今晚【第三次】答案住在 migration 的 COMMENT 裡
```
第一次 #497  「payment_status 不受影響、改它要 Sean 拍」→ 473b1 migration 檔頭
第二次 #477  「0/>0 的交界由 apply 那一刻的機器判」    → 路 A migration 開頭(GR 查到)
第三次 本件  「那筆錢的唯一出口是退款(Sean 拍板 A)」   → l5b0 migration 的 COMMENT
⇒ 🔴 三次的共同點:**條目/plan/結論檔都沒有它,而 migration 的註解有**
⇒ 可機械執行:**問「這件事為什麼沒做 / 誰裁過」⇒ 先 grep 相關 migration 的 COMMENT 與檔頭**,
   而不是只讀 backlog 與 plan
```

---

## §6 我沒做的
```
· 沒寫 plan、沒動任何 code、沒碰 backlog(那是別人的域,我只讀)
· 沒查 §4-1 C 族出口
· #497 的修法範圍沒確定 ⇒ 而它是這片體積的最大不確定性
· 檔數是類比推估,不是量測
```

— G5
