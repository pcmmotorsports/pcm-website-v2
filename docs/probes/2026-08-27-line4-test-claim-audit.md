# 探針 · 線4 自家測試檔的量測宣稱稽核(2026-08-27)

> **產出 = 名單,不是修法。** 主視窗 `-5b` 交辦:不要順手修,那會變成一片沒有 plan 的實作。
> 名單本體在 `§2`;我自己這次踩的那一次在 `§3`;分母與跳過項在 `§0`;天花板在 `§4`。
> 找的五種形狀:①尺看不見對象的全部 ②尺把自己算進分母 ③兩段式的尺第一段漏
> ④算出來/推出來的數字旁邊沒有指令 ⑤恆綠格。
> (五種的今日實例逐條在 `docs/specs/2026-08-25-saved-views-plan.md:592` 起的 §7-1a 與 §8-2a。)

---

## §0 🔴 我自己的分母(先講清楚,不然這份名單也是一個未標範圍的數字)

```
掃了     19 支測試檔
範圍     apps/storefront/src/components/account/**  +  apps/admin/src/lib/customers/**
尺       find … \( -name '*.test.ts' -o -name '*.test.tsx' -o -name '*.spec.ts'
              -o -name '*.spec.tsx' -o -name '*.test.js' -o -name '*.spec.js' \) -print
         ⇒ **不預先過濾行、不限副檔名到 .test.ts***(今天踩過那個病三次)
負對照   同一把尺掃 apps/storefront/src/components/nosuchdir ⇒ 0
         ⚠️ **而它【不報錯】** —— 那正是形狀⑤本身:目錄打錯字與真的零命中印同一個 0。
```

**跳過了什麼、為什麼**(這一節不能省 —— 沒有它,「19」是一個沒有範圍的數字):
```
· `apps/admin/src/components/customers/**`  —— 不在交辦範圍的兩個目錄裡。**沒掃。**
· `apps/storefront/src/app/account/**`(含我今天改的 `page.test.tsx`)—— 不在那兩個目錄裡。**沒掃。**
  🔴 而它正是我今天發現問題最多的地方 ⇒ **這份名單的分母排除了最可疑的那一塊。**
· e2e / 瀏覽器測試 —— vitest config 明文 exclude `**/e2e/**`,本次尺也沒掃到。
· `packages/**` 的 mapper 測試 —— 不在範圍。**沒掃。**
```

---

## §1 頭條數字

```
19 支檔裡, 註解中的【數字宣稱】     26 行
              其中附了可執行數法的     7 行
⇒ **約四分之三的數字宣稱旁邊沒有任何指令。**
數法:`grep -cE '^\s*(//|\*).*([0-9]+\s*(處|條|支|個|筆|格|欄|行|塊)|共\s*[0-9]+)'` 逐檔加總 ⇒ 26
      `grep -cE '\`[^\`]*(grep|rg |wc -l|find )[^\`]*\`'` 逐檔加總 ⇒ 7
負對照:同一把尺掃 `package.json` ⇒ 0
⚠️ **這兩把尺都只看註解行**, 看不見寫在測試名或 docstring 裡的宣稱 ⇒ 26 是**下界不是總數**。
```

---

## §2 名單(逐條:哪支檔哪一行 / 它現在印什麼 / 正確的尺印什麼)

### 🔴 F-1 形狀② —— 數法把自己算進分母(**確認,可重現**)
```
檔    apps/admin/src/lib/customers/load-customer-detail.test.ts:27-28
宣稱  「全 repo 沒有任何測試 import 它(數法:grep -rn 'customers/\[id\]/page' --include='*.test.*' → 0 行)」
現在印 照抄那條指令(排除 node_modules 與 .next)⇒ **1**
那 1 是 **這行註解自己**(它把 pattern 字面寫進了被搜的目錄)
正確的尺 加 `--exclude=load-customer-detail.test.ts`, 或把宣稱改寫成帶時點的
     (原值 0 在【本片之前】是對的 —— 本片自己就是要改變它)
⚠️ 而「1」讀起來正好是「有一個測試 import 它了」= **與結論相反**。
   📌 與我今天在 wallet.css 的 `--c-text-muted` 是同一個形狀。
```

### 🟡 F-2 形狀④ —— 完整性宣稱旁邊沒有指令(`InlineVehicleForm.test.tsx:315-317`)
```
檔    apps/storefront/src/components/account/InlineVehicleForm.test.tsx:315-317
宣稱  「『車型』這一欄有 **7 個入口**可以被動到」+ 逐項列了七個
現在印 那七項逐項可數 ✅ 而**清單本身沒有數法**
     ⇒ 日後元件多一個入口, 這句話會變假, 而**不會有任何東西紅**
     (該註解自己寫著「每個入口各一格, 不是一格代表全部」—— 它知道這件事重要)
正確的尺 未定。候選:對元件檔數會寫 `name` 的 handler 數, 或加一格「入口數 === 測試格數」的守門
⚠️ 我**沒有**斷言它現在是錯的 —— 我斷言的是**它壞掉時沒有訊號**。
```

### 🟡 F-3 形狀④ —— 歷史數字沒有數法(三處,同族)
```
apps/admin/src/lib/customers/keyword-search-action.test.ts:11  「當時全套 8159 個測試綠」
apps/admin/src/lib/customers/customer-list-view.test.ts:121    「炸掉 21 格頁級測試」
apps/admin/src/lib/customers/tier-form.test.ts:166             「當時 12 格全綠」
⇒ 三者都是【那個時點】的值, 而**沒有任何一個附了怎麼數的**
⇒ 它們的作用是「講一個故事」不是「支撐一個結論」⇒ **危害低**
🔴 而它們仍然值得一提, 理由是:**讀的人分不出「這是個修辭」與「這是個可查的事實」。**
建議(不是修法, 是形狀):歷史數字加一個「當時」的錨(commit hash 或日期), 不必附指令。
```

### 🟢 F-4 形狀⑤ 候選 —— 一格沒有會紅的世界(**危害低,有旁證**)
```
檔    apps/storefront/src/components/account/tabs/FavoritesTab.test.tsx:53-59
那格  「沒有收藏時不洩 design mock 商品字面」餵 `favorites={[]}`,
     斷 5 個 not.toContain('LIGHTECH' / 'RIZOMA' / 'Akrapovič' / 'AKRAPOVIČ' / 'NT$')
問題  **全檔沒有任何一格渲染一筆品牌【就是】那些字面的收藏**
     ⇒ 沒有一個世界能證明「這條路真的看得見品牌文字」
     ⇒ 若 textContent 這條路壞掉(例如改成 portal 渲染), 五條斷言一起無聲通過
旁證  ✅ 同檔 `:67` `screen.getByText('PROBE')` 與 `:145` `.acc-fav-price).not.toBeNull()`
     **確實證明了 textContent 看得見品牌與價格** ⇒ 判 **nit 不是 must-fix**
     ⚠️ 而那兩格**不在這格旁邊、也沒有標成它的對照** ⇒ 下一個人重構時不會知道它們是一組。
```

---

## §3 🔴 而我這次稽核自己也踩了一次 —— 這條要跟名單一起看

```
我用來找「有沒有對照組」的尺 = `grep -cE '對照|反面|前提|正對照'`
⇒ 它把兩支檔判成「🔴 零對照」:keyword-search-action.test.ts / FavoritesTab.test.tsx
⇒ **開檔核之後兩支都有對照**, 只是沒用那幾個詞:
     keyword-search-action.test.ts:121 逐字
       「已知鍵名(tier/page)照舊放行 —— 否則上一格會因為『什麼都退回』而恆真」
       ⇒ **那就是一個對照組, 而且它把理由寫出來了**
     FavoritesTab.test.tsx:145  價格存在那半(與 :102 的不存在成對)
⇒ 🔴 **我的「控制組偵測器」是一把關鍵字尺 ⇒ 2 個假陽性 / 命中 2 ⇒ 誤報率 100%。**
📌 **我拿一把關鍵字尺,去找「有沒有做對某件事」—— 而做對那件事不需要用那些字。**
⇒ 這一格與名單上的 F-1~F-4 是**同一族**, 差別只在:**這一次尺是我當場造的, 而我在同一分鐘內就驗了它。**
```

---

## §4 這份名單自己的天花板

```
· **分母排除了最可疑的那一塊**(§0):`apps/storefront/src/app/account/**` 不在交辦的兩個目錄裡,
  而那正是我今天抓到最多問題的地方。⇒ **這份名單的乾淨程度, 有一部分來自它沒去看那裡。**
· 兩把統計尺**只看註解行**(`//` / `*` 開頭)⇒ 寫在測試名、docstring、字串裡的宣稱看不見
  ⇒ 「26」是**下界不是總數**。
· 形狀①(尺看不見對象的全部)與形狀③(兩段式的尺)**在本次零命中** ——
  🔴 而那**不代表沒有** ⇒ 這兩種要抓, 得【重跑每一條數法並與宣稱比對】,
    而 19 支檔裡只有 7 條可執行數法 ⇒ **可重跑的分母本來就小**。
    ⇒ **零命中在這裡的意思是「可查的那 7 條裡沒有」, 不是「這 19 支檔裡沒有」。**
· 我沒有跑任何測試去驗這些格「是不是真的恆綠」——
  要驗得逐格做突變。⇒ F-4 判 nit 靠的是**讀**不是**跑** ⇒ 標未確認。
```


---
---

# §5 補掃:`apps/storefront/src/app/account/**`(2026-08-27 第二輪)

> 起因:`§0` 我自己點名「這份名單的乾淨程度,有一部分來自它沒去看那裡」。
> 主視窗 `-5b` 認了那個窄是他交辦時造成的,並要我補掃。
> **尺與第一輪逐字相同**(六種命名變體、不預先過濾行)⇒ 兩輪可比。

## 5-0 分母

```
命中 6 支:
  account/actions.test.ts · account/address/actions.test.ts
  account/orders/[displayId]/page.test.tsx · account/page.test.tsx
  account/profile/actions.test.ts · account/vehicle/actions.test.ts
該目錄遞迴檔數 13(`find apps/storefront/src/app/account -type f | wc -l`)
數字宣稱 5 行 / 附了可執行數法 1 行
⇒ 兩輪合計:**25 支檔、31 行宣稱、8 行有數法**
  (第一輪的分母與尺見 `§0`;逐檔統計指令與 `§1` 那兩條相同)
```

**查了而【不是】finding 的一格(要寫出來,否則下一個人會重查)**:
```
`account/favorites/actions.ts` 同目錄無測試檔 ⇒ 我一度以為零覆蓋
實查:`grep -rln 'favorites/actions' --include='*.test.ts*' apps packages` ⇒ **1**
負對照 同法查 `account/nosuchmodule` ⇒ **0**(尺是活的)
⇒ **有覆蓋, 只是測試不在它旁邊。** 不列 finding。
```

## 5-1 🔴 F-5 形狀② —— **第二個「數法會被自己命中」的實例**

```
檔    apps/storefront/src/app/account/orders/[displayId]/page.test.tsx:6-7
宣稱  「這個目錄底下只有 page.tsx、**零測試**
      (數法 `grep -rln "account/orders" --include='*.test.*'` 只命中 OrdersTab.test.tsx 與 safe-redirect.test.ts)」
現在印 照抄那條指令(排除 node_modules 與 .next)⇒ **3 支**
      第三支就是 **這支檔自己**
正確的尺 加 `--exclude` 排除本檔, 或把宣稱錨在一個時點(commit hash)
```

## 5-2 🔴🔴 F-6 —— **同一族的第三個,而它是我今天親手寫的,寫完 90 分鐘就過期**

```
檔    apps/storefront/src/app/account/page.test.tsx:305
宣稱  「把 .select() 改成 '*' ⇒ **本檔全部 15 格照樣綠**」
現在印 該檔 **16** 格(`grep -c '^  it('` ⇒ 16;實跑 ⇒ `Tests 16 passed (16)`)
為什麼 🔴 **那第 16 格, 就是這句話所在的那一格。**
      我寫「15 格照樣綠」是在描述【補這一格之前】的世界,
      而**補上去的動作把 15 變成了 16**, 那句話留在原地。
```

### 🔴 5-2a ⇒ 這一族**不是偶發,是結構性的**

F-1 / F-5 / F-6 三個實例共用同一個生成機制:

> **一個描述「我出生之前的世界」的數字,會被【我的出生】改掉 —— 而那句話留在原地。**

```
而它幾乎是【必然】的, 因為:
  一支測試檔在檔頭寫「我為什麼存在」時, 最有說服力的寫法就是
  「在我之前, 這裡 grep 出來是 0 / 是 N」
  ⇒ 而那條 grep 的關鍵字, 正是這支檔的主題
  ⇒ **它一定會命中自己。**
⇒ 📌 **越是誠實地記錄「我出生前的覆蓋率」, 越保證那個數字會在出生當下作廢。**
⇒ 這不是紀律問題(三次都是有紀律的人寫的, 其中一次是我今天全程開著這個意識時寫的)
  ⇒ 🔴 **是形狀問題 ⇒ 該用機制擋**(常載 §4 機制優先律)。
```

**候選機制(不是本片實作,是給折 plan 的人;三個實例的落點見 `§2` F-1、`§5-1` F-5、`§5-2` F-6)**:
```
甲 寫「出生前」的數字時一律附 commit hash 當錨:
   「在 `<hash>` 之前 ⇒ 0」—— 那句話永遠為真, 因為它綁死了一個時點
乙 數法一律加 `--exclude=<本檔>`(治得了自我命中, 治不了「後來又多了一支」)
   —— 這正是 `apps/storefront/src/styles/wallet.css:179` 今天採的那條
丙 乾脆不寫數字, 改寫「這支檔存在的理由是 X 沒有守門」(去掉可過期的部分)
👉 甲最小而且完全治本 —— **它把一個會過期的宣稱換成一個不會過期的宣稱**, 零額外工具。
```

## 5-3 🟡 F-7 形狀④ —— 又一個歷史數字沒有數法

```
檔    apps/storefront/src/app/account/actions.test.ts:9
宣稱  「**整套 3531 格測試沒有一格會紅。**」
問題  沒有數法;而今天 storefront 全套是 3680(2026-08-27 我自己跑過)
     ⇒ 讀的人分不出 3531 是「當時的真值」還是「一個約略」
與 F-3 同族(第一輪三處)⇒ **兩輪合計 4 處歷史數字沒有數法。**
```

## 5-4 補掃後的兩輪對照

| | 第一輪(別人寫的) | 第二輪(含我今天寫的) |
|---|---|---|
| 檔數 | 19 | 6 |
| 數字宣稱 | 26 | 5 |
| 附數法 | 7 | 1 |
| 確認級 finding | 1(F-1) | **2**(F-5 / F-6) |

🔴 **而這張表證實了我在 `§4` 天花板寫的那句話**:
> 「不是那 19 支比較乾淨,是我今天在改的那些檔,我會去重跑它們的數法;而別人的檔我只是讀。」

```
第二輪只有 6 支檔, 而確認級 finding 是第一輪的兩倍。
⇒ **差別不在檔的品質, 在【我有沒有把那條數法照抄去跑一次】。**
⇒ 📌 **稽核的產出量, 由「可重跑的數法有幾條」決定, 不由「掃了幾支檔」決定。**
  兩輪合計 31 行宣稱, 而**只有 8 行是可以被查的** —— 其餘 23 行,
  我掃過它們、讀過它們, 而**我對它們的真假一無所知**。
  (那 8 行裡, 3 行是我今天新寫的; 其中 1 行是 `apps/storefront/src/app/account/page.test.tsx:305`
   —— 也就是 F-6 那一行, 它自己就是過期的那個。)
```


---
---

# §6 第二段:那 23 行「查不了的」到底缺什麼(2026-08-27)

> 主視窗 `-5b` 交辦:**不是去補那 23 行**(那 23 行的來源見 `§1` 與 `§5-0`;補它們是實作、要 plan),
> 是把【每一行缺什麼】列清楚 ⇒ 產出是**折片的素材**,不是 plan 本身。
> 他另給一格判準,而它改變了這一節的結論:
> 🔴 **「每一行都要能回答『這個數字錯了,誰會痛?』—— 答不出來的那些,
>    不是『要補數法』,是『這個數字根本不該寫在那裡』。」**
> (來源屬性=**讀來**,依 `~/.claude/rules/00-work-rules.md:98`;判準本身我認同並照用。)

## 6-0 🔴 結論先講:那 23 行不是一種東西,是四種,而其中一種要補數法

(逐行原文的抓取指令與 `§1` 那條相同;三個已成案的 finding 在 `§2` F-1、`§5-1` F-5、`§5-2` F-6)

我原本的框架(「31 行宣稱,8 行可查,其餘 23 行要補尺」)**是錯的** ——
它假設那 23 行都在宣稱「repo 裡有幾個」。逐行讀完之後不是:

| 類 | 它在宣稱什麼 | 條數 | 錯了誰會痛 | 缺的是什麼 |
|---|---|---|---|---|
| **甲 突變預測** | 「改 X ⇒ N 格紅」 | 6 | 🔴 **會** —— 讀的人據此相信守門有判別力 | **不是數法,是【把那發突變真的跑一次】** |
| **乙 位置參照** | 「第 4 格」「本檔全部 15 格」 | 7 | 🔴 **會** —— 指錯格 / 數字過期 | **沒有任何尺救得了** ⇒ 改成**不用位置** |
| **丙 規格值** | 「4 欄」「超過 2 個」「10 筆只畫 8 筆」 | 8 | ⚪ 不會 —— **測試自己就在驗它** | **什麼都不缺**(尺就是那格測試) |
| **丁 歷史敘事** | 「當時全套 8159 格綠」 | 4 | ⚪ 不會 | **不該以可查數字的形式寫** ⇒ 加時點錨或改寫 |
| — 已在名單上的 finding | F-1/F-5/F-6 那三行 | 3 | — | 見 `§2` / `§5` |
| — 我自己標明「人工分類」的 | `WalletTab.test.tsx:47-52` | 3 | — | 已自陳無指令,**這正是正確做法** |

```
合計 6+7+8+4+3+3 = 31 ✅ 與 §1/§5-0 的總數對得上
⚠️ 這個分類是**我逐行讀出來的判斷**, 不是一條指令產生的 ⇒ 下一個人可以不同意任一行。
```

📌 **我原本要交的東西(23 把待寫的尺)有一半以上不該存在。**
⇒ 而發現它的是那句判準,不是更仔細的掃描。
🔴 **「誰會痛」把一張【工作清單】變成了一張【該不該做】的清單。**

## 6-1 甲類:突變預測 —— **我實跑了一條,它成立**

```
檔    apps/admin/src/lib/customers/keyword-search-action.test.ts:96-97
宣稱  「delete 寫死錯字面 '/' ⇒ 2 格紅(一致性格 + 本格)」
      「常數本身改成 '/'(兩端一起變)⇒ 🔴 1 格紅,【只有本格】」
實跑  把 `customer-keyword-cookie.ts:58` 的 `CUSTOMER_KEYWORD_COOKIE_PATH` 改成 '/'
      `npx vitest run apps/admin/src/lib/customers` ⇒ **Tests 1 failed | 160 passed (161)**
      紅的那一格 = `:100` 的「invalid(超長)也走刪除出口…一樣要帶 path」= **正是本格**
✅ **預測成立**(2026-08-27 複驗)。還原以 sha256 前後比對驗過相同。
```

⇒ 🔴 **這一條的意義不是「它是對的」,是「它【查得了】」**(那條被改的常數在 `apps/admin/src/lib/customers/customer-keyword-cookie.ts:58`)——
   甲類這 6 行**全部都能用同一個手法複驗**,而**在今天之前沒有人跑過任何一條**。
📌 **一個可查而從來沒有人查過的宣稱,與一個查不了的宣稱,在檔案上長得一樣。**

⚠️ **而甲類有一個內建的過期機制**:預測寫的是「N 格紅」,而**同檔多加一格測試,N 就可能變**。
   ⇒ 與 F-6 同族 ⇒ **建議一併採 hash 錨**:「在 `<hash>` 上跑 ⇒ 1 格紅」。

## 6-2 🔴 乙類:位置參照 —— **這 7 行沒有任何尺救得了**

```
實例  `apps/storefront/src/app/account/page.test.tsx:95`「…是下面第 4 格」
     `apps/storefront/src/app/account/page.test.tsx:305`「本檔全部 15 格」   ← 已是 F-6
     `apps/storefront/src/components/account/InlineAddressForm.test.tsx:62-66`「第 1 / 2 / 3 格」
     `apps/admin/src/lib/customers/keyword-search-action.test.ts:88`「下面這格」
病灶  它們指的是【順序】, 而順序會因為「有人在中間插一格」而改變
     ⇒ 🔴 **而插一格是這個 repo 每天都在做的事**(我今天就插了一格, 造出 F-6)
```
🔴 **要求:不要為這 7 行寫尺。** 沒有一把尺能在「第 4 格變成第 5 格」時變紅 ——
   因為**那不是錯誤,那是正常的開發**。
⇒ **修法是取消位置**:改成引用**測試的名字**(`it('🔴 對照組:合法 tier 不得留下那行 log')`)
   ⇒ 名字改了會 grep 不到 ⇒ **那才是一個查得了的形狀**。
📌 **一個宣稱要「可查」,前提是它指的東西有一個【不會因為正常工作而改變】的名字。**

## 6-3 丙類:規格值 —— **什麼都不缺,而我差點把它們列進待辦**

```
實例  ProfileTab.test.tsx:6「姓名·Email·手機·生日 4 欄」
     manual-customer.test.ts:250「超過 2 個就出現警告」(Sean `Q2=甲` 拍板值)
     OverviewTab.test.tsx:217「給滿 10 筆時只畫 8 筆」
     customer-list-view.test.ts:288「~24 個閏日」(算術, 可自己驗)
共同  這些數字**就是那格測試在斷言的東西** ⇒ 數字錯 ⇒ **那格測試會紅**
⇒ **尺已經存在, 而且它是自動跑的。**
```
🔴 **我第一版的框架會把這 8 行列成「缺數法」** —— 而它們是這份稽核裡**守得最好的一族**。
📌 **一個被測試守著的數字, 不需要一條 grep 指令去驗它。
   而我的統計尺(「註解行裡有沒有反引號指令」)看不出這件事 ——
   它只看得見【註解長什麼樣】, 看不見【那格測試在做什麼】。**

🔴 **而上面那句【不是新的】** —— 2026-08-27 線3 `-ed` 跑查重時撈到,我開檔核過:
   `docs/patterns/guard-and-instrument-traps.md:9240` 逐字標題
   > `## 尺量的是【字元】,而宣稱量的是【結構】—— 註解裡的字和 code 長得一模一樣`
   它的同族實例逐字:**「數 `<th>` 當欄數時,把註解裡的 `<th>` 也數進去」**
   ⇒ **與我這一格是同一個機制**,而**它已經有處置**:
     「原始碼層的結構斷言一律先 `stripComments`」,
     共用工具 `apps/admin/src/lib/test-support/strip-comments.ts`(實查存在;負對照查不存在的檔名 ⇒ 0)。
   ⇒ 📌 **我這一節寫的是它在【稽核自己的尺】上的一個實例,不是一條新教訓。**
      本檔只引用、不重寫(常載 `~/.claude/rules/00-work-rules.md` §4)。

### 🔴 6-3a 而 `-ed` 那一發還帶回一格方法論,它比上面那條更值錢

```
它跑查重、逐條開檔讀了前八名, 而這一族撞到的是【兩條】正本, 不是一條:
  第 6 名 :14382 「我的分母裡有【我自己寫的話】」        ← 講為什麼會發生
  第 8 名 :9240  「尺量的是字元, 而宣稱量的是結構」      ← 講機制 + 已有的修法
🔴 兩條**互相沒有交叉引用**, 而排名把它們放在第 6 與第 8
⇒ **只看排名會兩條都放行;而只找到一條就停, 會漏掉【已經有修法的那一條】。**
📌 判別句(`-ed` 立):**撞到一條就停, 會漏掉另一條。**
⚠️ **而我就是只找到一條的那個人** —— 我開了 `:14382` 就停了, 沒有繼續往下讀到第 8 名。
```

## 6-4 丁類:歷史敘事 —— **不是補數法,是不該這樣寫**

```
實例  `apps/admin/src/lib/customers/keyword-search-action.test.ts:11`「當時全套 8159 個測試綠」
     `apps/admin/src/lib/customers/customer-list-view.test.ts:121`「炸掉 21 格頁級測試」
     `apps/admin/src/lib/customers/tier-form.test.ts:166`「當時 12 格全綠」
     `apps/storefront/src/app/account/actions.test.ts:9`「整套 3531 格沒有一格會紅」
「誰會痛?」⇒ **沒有人。** 那四個數字**不支撐任何決定**, 它們在講一個故事
           (「當時全綠而其實有洞」)—— 而**故事不需要那個精確數字也成立**。
⇒ 🔴 **不要為它們寫尺。** 兩條路:
   甲 加時點錨:「在 `<hash>` 上, 全套綠而這一層零覆蓋」
   乙 拿掉數字:「當時全套是綠的 —— 因為這一層一格測試都沒有」
   👉 乙更小, 而且**它把讀者的注意力放回那句真正重要的話上**。
📌 **一個精確而無人依賴的數字, 唯一的作用是讓句子看起來更可信。**
```

## 6-5 ⇒ 折片的素材(**這一節就是主視窗要的產出**)

```
片素材-1(甲類 6 行)  把那 6 條突變預測【逐條實跑一次】並記錄結果 + hash 錨
                     ✅ 我今天已跑掉 1 條(成立)⇒ 剩 5 條
                     工程量:每條一發突變 + 還原, 有現成手法(cp → 改 → 跑 → cp 回 → sha 比對)
片素材-2(乙類 7 行)  把位置參照改成測試名參照
                     🔴 這是**改註解**不是加守門 ⇒ 純文件片, 而它治的是一個真的病
片素材-3(丁類 4 行)  拿掉四個無人依賴的精確數字(或加 hash 錨)
片素材-4(F-1/5/6)   三個「數法命中自己」⇒ 統一採 hash 錨(見 `§5-2a` 候選甲)
❌ **不做**:丙類 8 行 —— 它們已經被測試守著。
```

### 🔴 6-5a 而我要把自己這一節的天花板寫在這裡

```
· 「6 / 7 / 8 / 4」這四個數字**是我逐行讀出來的判斷**, 不是指令產生的
  ⇒ 下一個人可以對任何一行有不同分類 ⇒ **它是一個提案不是一個量測。**
· 🔴 而我差點在這一節犯下**分母太寬**(`-ed` 今晚點名的那個病):
  我原本要交的是「23 把待寫的尺」—— 那會是一張**很好看的待辦表**,
  而其中 8 行根本不該做、4 行做了沒有人受益。
  📌 **一張把不該做的事列進去的清單, 完成度會很高, 而它的完成沒有意義。**
· 甲類我只實跑了 **1 / 6** ⇒ 其餘 5 條「可查」是**推論不是量到的**(標未確認)。

```


---
---

# §7 甲類突變預測:實跑結果(2026-08-27)

> 主視窗 `-5b` 交辦:逐條實跑 + 還原驗 sha;**預測與實際不符的那些才是產出**;
> **不得寫「甲類已驗」**。

## 7-0 🔴 先訂正我自己的計數:是 **5 條預測**,不是「6 條」

```
我在 §6-0 寫「甲類 6 條」—— 那是**數行**數出來的。
逐條讀完之後, 可執行的**預測**只有 5 條:
  第 6 行(`InlineAddressForm.test.tsx:66`「第 3 格守空字串:沒輸入時不該掛一行空灰字」)
  **是在描述那一格守什麼, 不是一個帶數字的預測** ⇒ 沒有東西可以跑。
```
📌 **我又數錯了單位一次** —— 這一次是「行」與「預測」。
   與今天的 `§7-1a`(尺比對象窄)不同族:那些是**尺看不見**,這一次是**我數的東西不是我要數的東西**。

## 7-1 逐條結果

| # | 落點 | 預測 | 實跑 | 判 |
|---|---|---|---|---|
| P1 | `keyword-search-action.test.ts:96` | delete 寫死 `'/'` ⇒ **2 格紅**(一致性格 + 本格) | **2 格紅**,正是那兩格 | ✅ 成立 |
| P2 | `keyword-search-action.test.ts:97` | 常數改 `'/'` ⇒ **1 格紅,只有本格** | **1 格紅**,正是本格 | ✅ 成立 |
| P3 | `InlineAddressForm.test.tsx:62` | 突變 A(拿掉 `<div>`)⇒ **第 1 格紅** | 🔴 **2 格紅**(第 1 格 + 第 2 格) | ❌ **不符** |
| P4 | `InlineAddressForm.test.tsx:63` | 突變 B(灰字寫死)⇒ **第 2 格紅** | **1 格紅**,正是第 2 格 | ✅ 成立 |
| P5 | `InlineAddressForm.test.tsx:65` | 第 2 格若只斷言一次 ⇒ 寫死的版本**照樣綠** | **25 passed (25)** 全綠 | ✅ 成立 |

```
落點:`apps/admin/src/lib/customers/keyword-search-action.ts:127`(P1)·
      `apps/admin/src/lib/customers/customer-keyword-cookie.ts:58`(P2)·
      `apps/storefront/src/components/account/InlineAddressForm.tsx:213-217`(P3/P4/P5)
每一發都:cp 備份 → 突變 → 跑 → cp 還原 → shasum 前後比對
五發的還原全部 sha 相同 ✅(P5 是雙檔突變, 兩支一起比)
```

## 🔴 7-2 P3 不符,而不符的方向是「爆炸半徑比宣稱的大」—— 它帶出一個真發現

```
註解畫的是一個乾淨的 1:1 對應:
    突變 A ⇒ 第 1 格紅        突變 B ⇒ 第 2 格紅
實際:
    突變 A ⇒ 第 1 格 **和** 第 2 格都紅
    突變 B ⇒ 只有第 2 格紅
   (兩格的落點:`apps/storefront/src/components/account/InlineAddressForm.test.tsx:73` 與 `:80`)
⇒ 🔴 **第 2 格抓得到 A 也抓得到 B;第 1 格只抓得到 A。**
⇒ **在這兩發突變的範圍內, 第 1 格被第 2 格完全涵蓋。**
```

⚠️ **而這【不推翻】那段註解的設計理由** —— 它寫的是
「只寫『有沒有這個節點』擋不住 B」⇒ 那句話**由 P4 證實**(B 只紅第 2 格)。
🔴 **被推翻的是它畫的那張對應圖**:讀的人會以為兩格各守一發、缺一不可,
   而實際上**缺了第 1 格, 這兩發突變仍然都抓得到**。

⇒ **我不建議刪掉第 1 格**(`InlineAddressForm.test.tsx:73`),理由是:
```
它斷的是「首次輸入就出現, 且內容逐字相等」—— 那是一個**與突變無關的正向行為**,
而突變覆蓋率不是保留一格測試的唯一理由。
📌 **「被另一格涵蓋」與「沒有用」是兩件事** —— 前者是量測結果, 後者是價值判斷,
   而我只量到了前者。
⇒ 正確的處置是**改註解**(把 1:1 對應改成實測的形狀), 不是刪測試。
```

## 7-3 這一節沒做到的(**逐條寫,而且不寫「甲類已驗」**)

```
· 跑了 5 條 / 共 5 條可執行預測 ⇒ **這一類的可執行部分跑完了**
  ⚠️ 而「跑完了」的範圍是**這 5 條預測**, 不是「甲類這一族沒問題」——
    第 6 行那個描述句沒有東西可跑, 它的真假**仍然未知**。
· 🔴 每一發都是**單一突變**。兩發同時下去會怎樣, 沒試
  ⇒ 而真實的退步常常是複合的。
· P3 的結論(第 1 格被第 2 格涵蓋)**只在這兩發突變的範圍內成立** ——
  我沒有窮舉別的突變 ⇒ 換一發突變, 涵蓋關係可能反過來。**標未確認。**
· 這 5 條的預測值本身**現在會過期** —— 它們寫的是「N 格紅」,
  而同檔多加一格就可能變(F-6 同族)⇒ **建議一併採 hash 錨**(`§5-2a` 候選甲)。
```

## 7-4 一句這一節量出來的

(實例:`apps/storefront/src/components/account/InlineAddressForm.test.tsx:62`)

> **一個預測寫「1 格紅」而實際紅 2 格,不是預測錯得比較少 —— 它是一張【比實際乾淨】的圖。**
> 而乾淨的圖比亂的圖更容易被相信,也更容易讓人以為每一格都不能少。
> (本次實例 `InlineAddressForm.test.tsx:62`;實測數據見上表 P3。)


---
---

# §8 重跑今晚那一次查重 —— **我漏的那一條,是【線4 自己五天前寫的】**

> 起因:線3 `-ed` 證明「這一族撞到的是兩條正本,而只找到一條就停,會漏掉**已經有修法的那一條**」。
> 我今晚跑過一次查重(`fe991c5a` 之前),而**我只開了兩條就停了**(第 3 名與第 6 名)。
> 主視窗 `-5b` 排我回頭重跑,並特別點名要找**「講【我這條的處置該長什麼樣】的」**那種。

## 8-1 重跑條件(照工具自列的第 ⑥ 種失效,給檔路徑不走 stdin)

```
`python3 scripts/traps-neighbours.py --selftest`         ⇒ 自檢 PASS(六個世界印出不同答案)
`python3 scripts/traps-neighbours.py <草稿檔路徑>`        ⇒ 分母 正本 469 + inbox 158 + memory 992 = 1619
前八名與第一次**完全相同** ⇒ 排名不是問題, **我讀得不夠深才是。**
```

## 🔴 8-2 我漏的是**第 7 名,而它是正本**

```
第 7 名  [正本] :22577
標題逐字「**一條為了避開靜默零而寫的藥方, 在我們每天用的那支 `grep` 上自己靜默回零**」
署名逐字「(2026-08-25;**線 4 量**, 主視窗當場重現…」  ⇒ **本條是我這條線量出來的。**
```

它的判別句逐字(`:22595` 附近):
> 🔴 **而第 36 條自己的判別句是「每個 pattern 落筆前拿一個你知道一定在的目標打一發」
>    —— 它對它自己的藥方也成立,而沒有人跑過那一發。**

🔴🔴 **而那正是今晚五次失敗的統一解**(五次逐條在 `§7-1a` 與 `§8-2a`;正本落點 `docs/patterns/guard-and-instrument-traps.md:22577`):
```
我今晚每一次都在為【結果】做正負對照, 而**沒有為【pattern 本身】先打一發**。
  wallet.css 的 ^\.        —— 我驗了「數字對不對」, 沒驗「這個 pattern 看得見縮排嗎」
  --c-text-muted 的數法     —— 我驗了「命中幾個」, 沒驗「這個 pattern 會不會撞到我自己」
  兩段式的 raw\. 尺         —— 我驗了第二段, 沒驗「第一段留不留得住我要的那一行」
⇒ **這一條五天前就寫在正本上, 而它由我這條線寫的。**
📌 **我不是不知道那條規則, 是我在需要它的那一刻沒有讀到它。**
```

## 8-3 ✅ 我照第 1 名那條把今晚的數字重驗了一次(而結果是乾淨的)

第 1 名 `memory/reference_bash-tool-grep-and-find-are-shadowed` 逐字:
**「Claude Code 的 Bash 工具把 `grep` 與 `find` 換成 ugrep/bfs」**
⇒ 今晚我所有的計數都是用 Bash 工具的 `grep -cE` 跑的 ⇒ **它們可能是引擎相關的。**

**三個獨立判官,同一組 pattern、同一份 25 支檔清單**(2026-08-27 當場跑):
```
                 數字宣稱   附數法
Bash 工具 grep      31        8
/usr/bin/grep       31        8
python3 re          31        8
wallet.css 那兩把:  Bash grep ⇒ modal 35 / tier 20  ·  /usr/bin/grep ⇒ 35 / 20
```
⇒ **今晚的數字不是引擎相關的。**
📌 **而這是一個【負結果】,它的價值在於:在跑之前,「一致」與「三支引擎各印各的」
   在我的報告上長得一模一樣 —— 兩種情況我都只會寫一個數字。**

⚠️ **而 `31 / 8` 與我在 `§1` + `§5-0` 寫的「26 + 5 = 31 / 7 + 1 = 8」對得上** ⇒ 兩輪加總無誤。

## 8-4 這一輪的教訓,而它與 `-ed` 那條是**同一句話的兩半**

```
-ed 的:撞到一條就停, 會漏掉另一條 —— 而那另一條往往是【已經有修法的那一條】
我的 :而那條【已經有修法的】, 這次是【我自己這條線五天前寫的】
⇒ 合起來:**查重撈到的東西, 有可能是你自己寫的 —— 而你一樣不會認得它。**
📌 因為查重印的是**標題與分數**, 而人是靠**當時的情境**記住自己寫過的東西的,
   標題與情境不長得一樣。
```

## 8-5 這一節沒做到的

```
· 我開了第 1、3、6、7 名, 以及先前 grep 過標題行 ⇒ **第 2、4、5、8 名我只讀了列表上那一行摘要**
  ⚠️ 那四條的摘要都在講「多位元組 / \b / commit body」, 與本族**看起來**不同 ——
    🔴 **而「看起來不同」正是我這次要修的那個判斷。** ⇒ 標**未確認**, 不宣稱已窮盡。
· 我沒有回頭重跑今晚**另一次**查重(§5-2a 那份 `draft-945.md` 之外的);
  實際上今晚只跑過那一次 ⇒ **沒有第二次可以重跑**(這一格是查過的, 不是漏的)。
```


---
---

# §9 補讀第 2/4/5/8 名 —— **兩條是同族而我用摘要打發了,而第五條把我今晚的產出整個判了一次**

> 起因:`§8-5` 我自己標「第 2、4、5、8 名只讀了列表那一行摘要,而**『看起來不同』正是我這次要修的那個判斷**」。
> 主視窗 `-5b` 另提醒:分母從 1603 長到 1619,**中間 16 則有今晚新寫的**
> ⇒ 撈到今晚剛寫的,不是「早就有了」而是「幾小時前才有」,**兩者意義不同,要分開標**。

## 9-0 先答那個時點問題:**這四條都不是今晚的**

```
`stat -f '%Sm'` 逐支:
  #2 reference_git-grep-silently-ignores-backslash-b        2026-08-21 19:30
  #4 feedback_a-bracket-class-with-a-multibyte-char-…       2026-08-19 05:03
  #5 reference_multibyte-in-bracket-class-is-engine-dep…    2026-08-19 05:01
  #8 feedback_commit-body-written-from-intent-not-from-file 2026-08-19 05:17
⇒ **四條全部早於今晚** ⇒ 「早就有了而我沒讀」, 不是「幾小時前才出現」。
```

## 9-1 🔴 兩條是同族,而我當初用**摘要**把它們打發了

```
#2「`git grep -E` 不支援 `\b`, 而它【靜默不匹配】不是報語法錯」
   ⇒ 我今晚踩的是 `/\b立即儲值\b/` 在 JS 上**永遠不匹配**(CJK 不是 `\w`)
   ⇒ 引擎不同、機制不同, 而**症狀一字不差:`\b` 靜默地不匹配, 而它印「找不到」**
   🔴 **我當初看摘要覺得「那是 git grep 的事,我用的是 JS」⇒ 跳過。**

#4「檔名過寬(我原本寫『matches nothing』), 事實是【退化】不是【零命中】」
   (建檔 2026-08-19 05:03,`stat -f '%Sm'` 量;我今晚那個過寬宣稱的落點
    `docs/specs/2026-08-27-945-submodule-in-sandbox-and-ci-plan.md:32-36`)
   本檔自陳它留下的是「**我把宣稱寫過寬的過程**」
   ⇒ 我今晚踩的是「存在 + 404 ⇒ **私有**」, 而我真正量到的只是「**不具公開讀取權**」
   ⇒ **同一件事:把一個決定所需的事實, 寫成一個比它更強的宣稱。**
   🔴 **我當初看摘要覺得「那是多位元組字元類的事」⇒ 跳過。**
```
📌 **兩次跳過的理由都是【機制看起來不同】,而同族的是【症狀】與【犯錯的過程】,不是機制。**
⇒ 這一格證實了我在 `§8-5` 的自我設限:**「看起來不同」正是要修的那個判斷。**

(#5 我在 `§8-3` 已經照它做過三判官複驗;#8 講 commit body 照意圖寫,
 與本族不同 —— **而這一次我是開檔讀完才判的,不是看摘要。**)

## 🔴🔴 9-2 而主視窗點名的第五條(正本 `:19916`),把我今晚的產出整個判了一次

```
標題逐字「**五次差點誤報,零次是靠「我更小心」擋下的
        —— 判別句的結尾不是動作, 它就只是「要小心」的長版**」
判別句逐字「**我寫的這條判別句, 結尾是一個【動作】, 還是一個【心態】?
          心態 ⇒ 🔴 那是「要小心」的長版, 而它在下一輪會失效。**」
```

**我拿它掃我自己今晚寫下的判別句**:

| 我今晚寫的 | 結尾是 | 判 |
|---|---|---|
| 「量之前先問:我的尺看得見對象的全部嗎?」 | **心態** | 🔴 要小心的長版 |
| 「標『未確認』不等於處理了它」 | **心態** | 🔴 同上 |
| 「一個宣稱要可查,前提是它指的東西有一個不會因為正常工作而改變的名字」 | **心態**(描述) | 🔴 同上 |
| 「『被另一格涵蓋』與『沒有用』是兩件事」 | **心態** | 🔴 同上 |
| 「這個數字錯了,誰會痛?」 | **心態** | 🟡 而它**產生了一次具體重分類** |
| **「每個 pattern 落筆前拿一個你知道一定在的目標打一發」** | ✅ **動作** | 而**那條是我從 `:22577` 讀來的,不是我今晚寫的** |
| **「寫『出生前』的數字時附 commit hash 錨」** | ✅ **動作** | 而它是**修法**,不是判別句 |

🔴 **結論很難看而它是量到的**:
```
我今晚產出的判別句裡, **結尾是動作的有 0 條** ——
兩條動作型的, 一條是【讀來的】、一條是【修法】。
📌 而 `:19916` 早就預告了這件事:「本檔今晚長了十幾條教訓, 而它們絕大多數的處置是一句判別句
   —— 也就是【要小心】的一種形式」。**我今晚做的就是那件事, 做了十幾次。**
```

### 9-2a ⇒ 把它們改寫成動作(**這一格才是本節的產出**)

```
心態版:量之前先問「我的尺看得見對象的全部嗎」
動作版:🔴 **落筆任何 pattern 之前, 拿一個【你知道一定在】的目標打一發正對照,
        再拿一個【你知道一定不在】的打一發負對照。兩發都跑完才可以用那個 pattern 的輸出。**
        (= `:22577` 那條, 我不重寫, 直接採用)

心態版:標「未確認」不等於處理了它
動作版:🔴 **每寫一個「未確認」, 同一句加上「在它被填之前, 不得用它支撐 X」——
        X 要寫出來。寫不出 X ⇒ 那個標記可以刪, 因為沒有人在靠它。**

心態版:一個宣稱要可查, 前提是它指的東西有一個不會因為正常工作而改變的名字
動作版:🔴 **寫下一個指涉時, 先問它是【位置】還是【名字】。是位置(第 N 格 / 行號 / 順序)
        ⇒ 當場換成名字;換不了 ⇒ 加 commit hash 錨。**

心態版:這個數字錯了, 誰會痛?(本檔 `§6-0` 那一問;三個已成案的落點
        `apps/admin/src/lib/customers/load-customer-detail.test.ts:27` 等)
動作版:🔴 **每一行待辦寫下時, 同一行寫出「它錯了誰會痛」。寫不出來 ⇒ 那一行從清單上刪掉。**
        🔴 **今晚實測的效果我第一版寫成「把 31 行砍成 13 行」—— 那個 13 是我當場湊的, 沒有算過。**
        真值(逐項相加):31 = 甲6 + 乙7 + 丙8 + 丁4 + 已成案3 + 已自陳人工3
          判成**不必做** = 丙8 + 已自陳人工3 = **11 行**
          判成**要做**   = 甲6 + 乙7 + 丁4 + 已成案3 = **20 行**
        📌 **我在一節【講捏造數字】的文字裡, 捏了一個數字** —— 落筆到抓到隔了不到五分鐘,
           而抓到它的是本檔 `§1` 那條「數字旁邊要有算式」的紀律, 不是我更小心。
           (組成見本檔 `§6-0` 那張表)
```

⚠️ **而我不宣稱這四條動作版有用** —— 它們今晚只被執行過一次(最後那條,在 `§6`)。
   📌 照 `:19916` 自己的標準:**一條沒有被執行過的動作版, 與一條心態版的差別還沒有被量到。**

## 9-3 這一節沒做到的

```
· 我掃的是**我今晚寫在檔案裡的判別句**, 不是我在對話裡講過的每一句 ⇒ 分母是檔案面
· 那張表的「心態 / 動作」是**我自己判的**, 而判它的人正是寫它的人
  🔴 這正是 `docs/patterns/guard-and-instrument-traps.md` 記過的「判斷者恰好是受益者」形狀
  ⇒ 標**未確認**, 而它值得由別人重判一次
· #8 我判「與本族不同」—— 而那正是我這一節在打的那種判斷。**開檔讀完才判的, 而仍可能錯。**

```


---
---

# §10 乙類動手:位置參照改測試名 —— **而我先發現「7 條」是錯的**

## 10-0 🔴 重掃之後是 **23 行,不是我在 `§6-0` 寫的 7 條**

```
§6-0 那個「7」是從**宣稱掃描**的結果來的 —— 那把尺只抓
  「註解行 + 數字 + 量詞」⇒ 它看得見「第 4 格」, 看不見「上一格 / 下面這格 / 上面那格」
換一把專門找位置參照的寬尺重掃 25 支:
  `grep -nE '第\s*[0-9]+\s*格|下面這格|上面那格|上一格|下一格|本檔全部\s*[0-9]+\s*格|前一格|後一格'`
  ⇒ **23 行**;負對照同尺掃 `package.json` ⇒ **0**
```
📌 **又一次「尺是為了別的用途造的,而我拿它的輸出當這一次的分母」。**
   與 `§8-2a` 那五次同族,而這一次的**新形狀是:尺沒壞、數字沒錯 —— 錯的是【我把它當成另一個問題的答案】。**

## 10-1 而那 23 行分成兩種,**脆弱程度差很多**

(逐行清單見上面那條寬尺的輸出;兩支被改的檔是
`apps/storefront/src/app/account/page.test.tsx:95` 起與
`apps/storefront/src/components/account/InlineAddressForm.test.tsx:61` 起)

| 種 | 形狀 | 幾行 | 什麼時候壞 | 代表落點 |
|---|---|---|---|---|
| **絕對位置** | 「第 N 格」「本檔全部 N 格」 | **11**(2 支檔) | 🔴 **前面任何地方插一格就壞** | `page.test.tsx:305` |
| **相鄰相對** | 「上一格 / 下面這格 / 上面那格」 | **12**(8 支檔) | 只有**插在它們兩個中間**才壞 | `keyword-search-action.test.ts:121` |

(相鄰相對那族的代表:`apps/admin/src/lib/customers/keyword-search-action.test.ts:121`)

```
11 + 12 = 23 ✅(與寬尺 `grep -nE` 掃出來的 23 行對得上)
```

⇒ **本片只改絕對位置那 11 行。** 相鄰相對那 12 行**刻意不動**,理由:
```
① 它們的指涉對象是【緊鄰的那一格】—— 而「插在兩格中間」比「在前面任何地方插一格」罕見得多
② 🔴 更重要:它們大多是**對照組在說明自己在對照誰**
   (例:`keyword-search-action.test.ts:121` 逐字「否則上一格會因為『什麼都退回』而恆真」)
   ⇒ 那種句子換成格名會**變長而更難讀**, 而它換來的安全邊際很小
⇒ 📌 **不是所有位置參照都值得換 —— 而「值不值得」要看它壞掉的機率, 不是看它是不是位置。**
⚠️ 而這個判斷是**我做的取捨, 不是量到的**(我沒有量過「插在兩格中間」發生過幾次)⇒ 標**未確認**。
```

## 10-2 改了什麼(11 行,2 支檔)

(兩支檔的落點:`apps/storefront/src/app/account/page.test.tsx:95` 與 `:305`;
 `apps/storefront/src/components/account/InlineAddressForm.test.tsx:61` 起那一段)
```
apps/storefront/src/app/account/page.test.tsx
  「下面第 4 格」  ⇒ `it('🔴 對照組:合法 tier 不得留下那行 log …')`
  「第 2 格」      ⇒ `it('🔴 DB 給一個本版不認得的 tier ⇒ 退成 general …')`
  「本檔全部 15 格」⇒ 保留 15, 而**加上錨**:「錨在 commit `c1aef746`(本格尚未存在的那一版)」
                    ⇒ **這是 F-6 的正式處置**

apps/storefront/src/components/account/InlineAddressForm.test.tsx
  「第 1 / 2 / 3 格」⇒ 三個**測試名的關鍵字**:`出現` / `跟著變` / `空白`
                     並在同段列出三個關鍵字各自對應哪一個 `it(...)`
```
🔴 **兩支檔各留一行「第 N 格」沒改** —— 那兩行是**我自己的訂正註記在引用舊字面**
   (「原文寫『下面第 4 格』…已改成格名」)⇒ **刻意保留, 不然讀的人看不出改了什麼。**

## 10-3 量測

```
機械驗純註解:`git diff -U0 | grep -vE '^[+-][[:space:]]*(\*|//|/\*)'` ⇒ **0 行非註解**(改動 38 行)
兩支檔實跑:`Test Files 2 passed (2)` · `Tests 41 passed (41)` · rc=0
三綠:**build 沒跑, 不宣稱三綠**(零行為改動)
      typecheck rc=0 `0 cached/8` · lint rc=0 `0 cached/10`(2026-08-27 04:22:25-04:22:39 CST)
```

## 10-4 🔴 而主視窗重判了我 `§9-2` 那張表,它有一格不同意 —— 我照收並記下

```
它同意六格, 不同意的是「這個數字錯了, 誰會痛?」——
**它判那是【動作】不是心態**, 理由:
  「要小心」答不出來 ⇒ 繼續往下
  這一句答不出來    ⇒ **歸【不必做】** ⇒ 它拒絕了東西
⇒ 它提的判準逐字:**「一句判別句是不是動作, 看它有沒有【一個答不出來時的預設處置】。
                   能拒絕東西的就是閘。」**
⚠️ 而它自己標了免責:**「我是那個受益於『這一問有用』的人(那句是我給你的)⇒ 值得第三個人再看。」**
```
📌 **我接受它的判準, 而我不改 `§9-2` 那張表的原判** —— 兩個判斷都留著:
   我判「心態」是看**句子的形狀**;它判「動作」是看**句子的輸出**。
   🔴 **而它的判準比我的好, 因為它可以被套用到下一句上;我的只能事後分類。**
   ⇒ 這一格**留兩個人的判斷並陳, 不合成一個** —— 合成之後就看不出分歧在哪了。
