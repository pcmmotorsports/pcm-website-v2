# 哪些數字**沒有證人**(2026-08-19 04:2x CST,G3)

> 判別句(來自今晚落的 memory `feedback_same-source-removes-the-witness`):
> **這個數字如果錯了,畫面上有沒有任何東西會不一致?**
> **沒有 ⇒ 它沒有證人。** ⚠️ 而**「看起來對」不算證人**。
>
> **純讀。沒有按任何寫入。** 量的是本機後台(`ADMIN_DEV_BYPASS=1`,連正式庫)+ 原始碼,
> 2026-08-19 04:18–04:25 CST(`date` 實跑)。

---

# 🔴 最貴的那一格:**今日實收 NT$22 沒有證人 —— 而那件事已經被寫下來了**

## 它從哪來
```
apps/admin/src/lib/dashboard/today-read.ts:222
  supabase.rpc('admin_today_payment_total', { p_from, p_to })
而同一頁的「今日新單」走的是完全不同的查詢(同檔 :225-226 的 orders count)
⇒ 兩個數字**沒有任何必須相等的關係** ⇒ 一個錯了,另一個不會叫
```

## 🔴 而真正的發現:**那個證人曾經存在,是被【刻意移除】的**
`apps/admin/src/lib/dashboard/today-view.ts:26-37` 檔頭逐字(節錄):
```
🔴🔴 **本檔的 `sumReceived` 已經移除,而且那是一個要講清楚的損失**:
   收款改走 RPC ⇒ 加總搬到 SQL ⇒ 這裡的純函式變成死碼,連同它的 **6 格測試**一起刪掉,
   其中包含鏈式沖銷、**兩條負向對照**(濾負列會變 1000 / 改用筆數會變 3)、零筆回 0…
⇒ **「已收 = SUM(amount)」這條錢的不變式,現在的可執行守門比之前少。**
替代品只有兩個,而且都比原本弱:
  ① migration 的 `COMMENT`(字面,不是守門)
  ② 源碼契約測試 —— 讀 migration 檔、斷言 SQL 沒有 `ABS(`、沒有 `amount > 0`…
⚠️ ② **只證那句 SQL 還在檔裡,不證 DB 真的那樣算**。
   要補回同等強度只有一條路:對真的資料庫跑一次。本片做不到 ⇒ **列為誠實缺口,不假裝等價。**
```
⇒ 📌 **這一格不需要我發現 —— 它已經被誠實地寫下來了,而且寫得比我會寫的好。**
⇒ 🔴 **而它仍然是「沒有證人」的狀態,只是那個狀態是【知情且申報過】的。**

## 那我今天算的那個 22 算什麼?
```
我在走查時對過:22 = 1 + 1 + 20(H7CPXQ / 2DTX42 / 8X3N5Q),而正確排除了未收的 1,400
🔴 而那是【我這個人】翻到另一頁去加的 —— **不是畫面上的證人**
⇒ 它證明「今天這一發是對的」,**不證明「這個數字有人在盯」**
⇒ 而下一次沒有人會做這件事,除非又有人剛好走查
```

---

# 二、逐個數字:有沒有證人

| 數字 | 來源 | 同畫面上的第二來源 | 判 |
|---|---|---|---|
| **今日實收(淨)** | RPC `admin_today_payment_total` | ❌ 無(今日新單是另一個問題,不必相等) | 🔴 **沒有證人**(而已申報,見上) |
| **今日新單** | `orders` count(`today-read.ts:225-226`) | ❌ 無 | 🔴 沒有證人 |
| **客人的 TOTAL ORDERS** | `orders.length`,而 orders 就是被過濾的那支查詢 | ❌ **同源** ⇒ 永遠一致 | 🔴🔴 **最嚴重**:同源使它**永遠不會叫**(見 memory 那條) |
| **後台客戶「訂單數 2」vs 明細「沒有訂單」** | 兩支不同查詢 | ✅ **有**(而它們正在打架) | ✅ **有證人,而且叫了** —— 我就是這樣發現那道過濾器的 |
| **儲值金「目前餘額」** | `customers.wallet_balance`(**存的欄**,由 ledger trigger 維護) | ⚠️ **有一半**:同一頁下方有「儲值金交易紀錄」= 產生那個餘額的那些列 | ⚠️ **弱證人**:要**人自己加總**才對得起來(頁面沒有顯示合計) |
| **訂單詳情「總額 / 已收」** | 訂單 total 與 `sumReceived(rows)`(`payment-list-view.ts:172`) | ✅ **有**:下方逐筆收款列出來,可以自己加 | ✅ **有證人**(同款、而且是**強**的:明細列在旁邊) |
| **商品「共 20,341 件」** | 列表分頁 count | ❌ 無 | ⚠️ 沒有證人,而**錯了不貴**(不影響錢) |

---

# 🔴 三、而順手撞到**同一族的第三個變體**(不是數字,是「日界」)

`apps/admin/src/lib/dashboard/today-view.ts:41-44` 檔頭逐字:
```
🔴 `ymd` 一定要從這裡出去:呼叫端若自己再 `isoBackToTaipeiYmd(now)` 算一次,
   就有**兩份日界規格**;哪天日界改法只改一處,**畫面標題的日期會與查詢區間不同天**,
   而**現有測試一格都不會紅(兩邊各自都「對」)**。
```
⇒ 📌 **「兩邊各自都對」正是今晚那句** —— 而這位作者用的修法是**強迫同源**(`ymd` 一起回)。
⇒ 🔴 **而那正好是我這條 memory 講的交易**:
```
同源解決了「兩份規格會漂移」,而它同時讓「標題日期 vs 查詢區間」不再互相檢查
⇒ **兩種寫法各有各的失效模式,而【選哪一種】要看哪一種失效比較貴**
⇒ 這裡選同源是對的(日界漂移是靜默的、而且會錯整天的數字)
```
📌 **所以本檔不是在說「同源不好」** —— 是在說**那筆交易要被寫下來**,而**這位作者寫下來了**。

---

# 四、收斂:誰要補證人

```
🔴 優先(錢 + 完全沒有證人 + 未申報):
   今日實收 —— 而它**已申報**(today-view.ts:26-37)⇒ 實際上不是「沒有人知道」,是「沒有人排」
   ⇒ 補法作者也寫了:**對真的資料庫跑一次**(那要 DB 存取 ⇒ Sean)

🔴🔴 優先(客人看得到 + 同源 + 未申報):
   客人的 TOTAL ORDERS ⇒ 補法不是拆同源,是**同源 + 明說濾掉幾筆**(= `#659`)

⚠️ 次要:儲值金餘額的證人是「弱」的(要人加總)⇒ 顯示一個合計就會變強
   而儲值金業務 Sean 拍「現在不開」⇒ **不急**
```

# 這一份沒有答到的
```
1. 我只掃了【我走查過的畫面】上的數字 ⇒ **這是下界不是全集**
   (例:出貨/採購那些數字我沒有走到有資料的狀態)
2. 「今日實收」那條要補回的守門實際要怎麼做 —— 作者已經寫了方向(對真 DB 跑一次),我沒有評估工時
3. 儲值金那個弱證人有沒有真的對得起來 —— **我沒有加總過**(那要看別人的 ledger)
```
