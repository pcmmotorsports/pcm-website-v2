# 取消通知信 · 規格草案(線 `-mail`,2026-09-03)

> **狀態:草案,等主視窗-87 過目 ⇒ 過了才動碼。** 本檔不改任何行為。
> **這個檔在什麼情況下會【變成假的】**:
> ① 下面兩題其中一題被 Sean 拍了 ⇒ §1 的問句作廢、答案要搬進 §2 的表;
> ② `20260902140000`(片②)被 apply ⇒ §3 那三道閘從「檔案裡」變成「庫裡」,而**射程不會自己跟著變**;
> ③ 有人改了 `admin_cancel_order` 的七值映射表 ⇒ §2 的分類基底就過期了。
> ⇒ 三個都不是時間到期,是**別人動了東西**。⚠️ **沒有任何機制會叫**,讀本檔的人自己重跑 §5 那幾條。

---

## §0 這份草案在回答誰的問題

主視窗-87 2026-09-03 指派,逐字要求:「**哪些取消要寄 / 哪些不寄**(缺貨?客人沒付款逾時?後台手動取消?)· 文案落點 · 那支逐字鎖住文案的測試要不要跟著加一格」。
起因是 Sean 同夜兩題:
```
① 未付款訂單被取消 ⇒ 要寄取消信            (甲, 2026-09-03 00:5x)
② 信件文案 ⇒ 工程師改, 不做後台可編輯      (甲, 2026-09-03 01:0x)
```
落檔:memory `project_0903-cancel-email-yes.md` · `project_0903-email-copy-engineers-edit.md`(**我沒有開這兩支檔**,轉述自主視窗-87 ⇒ 標**未確認**)。

---

## §0-A 🔴🔴 開工前先擋一格:**片① 到底貼了沒 —— 帳本說【沒有】,而交辦單說【貼了】**

主視窗-87 指派我這條線時逐字寫:「**DB 那一半 Sean 09-02 已貼(`20260902120000`,白名單放寬)**」。
🛑 **而我一落筆要引用它就撞到了**:

```
grep -c "^20260902120000" supabase/APPLIED.tsv   ⇒ 0     <- 片①(白名單放寬)
grep -c "^20260902140000" supabase/APPLIED.tsv   ⇒ 0     <- 片②(標記函式)
🟢 正對照 ^20260902050000 ⇒ 1 · ^20260902060000 ⇒ 1      <- 同一天、同一把尺,印得出 1
🔵 負對照 ^20260902129999 ⇒ 0
```
⇒ **尺會動,而那兩個 0 是真的 0。**

🔴 **兩個世界都與這個讀數相容,而它們對「現在能不能寫碼」給【相反】的答案**:
```
甲 貼了, 只是沒有人回頭記帳本 ⇒ 正式庫的 CHECK 已含 order_cancelled ⇒ 可以往下做
乙 沒貼(或貼了別支)          ⇒ 正式庫仍是兩值 ⇒ 寫進去的 order_cancelled 列會被
                               check_violation 擋 ⇒ 🛑 **寫了的碼連測都測不了**
```
📌 **而乙正是板子 `⟦0e-MAILCANCELSEND⟧`(`docs/launch-todo.md:944`)整列存在的理由**,它逐字寫著:
「『跑得動但沒效果』的世界裡人會先寫碼再等 SQL;而【會被擋】的世界裡,寫了的碼連測都測不了
 ⇒ 兩者對『要不要現在寫』給**相反**的答案 ⇒ 做不到就別做。」

🛑 **我分不出甲與乙 —— 我沒有正式庫 access。** 這一格要**有 DB access 的人跑一句唯讀**:
```sql
SELECT pg_get_constraintdef(oid) FROM pg_constraint
 WHERE conrelid = 'public.email_outbox'::regclass AND contype = 'c';
-- 看回傳裡有沒有 order_cancelled;有 ⇒ 甲(順便補 APPLIED.tsv);沒有 ⇒ 乙(要 Sean 再貼一次)
```
⚠️ **在這一句跑出來之前,§1/§2 的兩題照樣可以端給 Sean**(那是產品決定,不依賴 DB 現況),
**而【動碼】不行** —— 那正是板子那一列講的東西。
🔵 **這一格不是在指誰講錯** —— 帳本沒更新與沒貼在我這一端印同一個東西,
而主視窗那句話的來源我沒查(它可能讀的是別的證據,例如唯讀查過正式庫)。⇒ **要它自己回答一句。**

---

## §1 🛑 先擋一格:①與 2026-09-02 那筆拍板【射程不一樣】,而我不自己選邊

**2026-09-02 Sean 拍甲的射程,逐字寫在那支 migration 的 COMMENT 裡**
(`supabase/migrations/20260902120000_m4b_outbox_order_cancelled_event.sql:201-202`)——
⛔ ~~我第一版寫「已經 apply 到正式庫的 COMMENT」~~ **那句話我證不出來,見 §0-A,已作廢**:

> 「`order_cancelled` 的射程(Sean 2026-09-02 拍甲):只涵蓋【刷卡 且 已全額退款】的整單取消,
>  且那一次取消**一個品項數量都不會動**。**部分退款不涵蓋**、部分品項取消不涵蓋。」

**而片②的函式把那個射程做成了硬閘**(`20260902140000_m4b_mark_order_cancelled.sql`):
```
:319  IF v_order.payment_method IS DISTINCT FROM 'tappay' THEN RAISE '這期只開放刷卡的單…'
:324  IF v_order.payment_status IS DISTINCT FROM 'refunded' THEN RAISE '這張單還沒有全額退款…'
```
🔴 **一張【未付款】的單,`payment_status` 不是 `refunded`、`payment_method` 也常常不是 `tappay`
⇒ 它會被 `:324`(往往連 `:319`)當場擋掉。**

⇒ **09-03 的 ① 不是把碼補完就能達成的,它是把 09-02 的射程【擴大】。**
⚠️ **而我不知道 Sean 知不知道這件事** —— 兩題相隔一天、由不同的窗端出去;
09-03 那題的題面我沒看到(轉述),**所以我答不出「他是否被告知這會推翻昨天那條射程」**。

📌 **這正是 R3 那條:新訊息疑似推翻舊拍板 ⇒ 確認、不假設。** ⇒ 端他一題:

```
Q-取消信射程:一張【還沒付款】的單被取消, 那封信要走哪一條?
A: 甲 = 把 order_cancelled 的射程擴大(改 COMMENT + 放寬片②那兩道閘), 一個事件涵蓋兩種單
   乙 = 未付款那種【另開一個 event_type】(新 migration), 兩條線互不影響
   丙 = 先只做未付款那一種, 刷卡已退款那一種延後
```
🔵 **我的推薦 = 乙。** 理由兩個,都是量到的:
① `event_type` **同時是去重鍵的一半**(唯一索引 `email_outbox_event_uniq (event_type, dedup_key)`,
   COMMENT 逐字「不可以借用既有值來省一個 migration —— 借用會讓兩條線的信互相擋掉對方的唯一鍵 = 製造漏信面」)
   ⇒ **甲把兩種語意塞進同一個值, 正是那句話警告的形狀。**
② 兩種單客人的處境相反:刷卡已退款的人**錢已經回去了**;未付款的人**從頭到尾沒付過錢**
   ⇒ 信裡該不該提退款是相反的 ⇒ 合成一封會逼出一個帶 if 的文案。
🛑 **而乙的代價要一起帶**:多一支 migration、多一個模板、`buildEmailText` 多一個 case
   ⇒ **比甲多做一輪**。而 Sean ② 已拍「文案工程師改」⇒ 他知道每多一份文案就多一份人工維護成本。

---

## §2 哪些取消要寄 / 哪些不寄(**這是待填的表, 不是結論**)

**先把「取消」這個詞拆開 —— 量到的有【三條路】, 而板子與拍板句都只講了其中一條。**

| # | 路徑 | 誰觸發 | 落點(檔案:行號) | 今天寫什麼 | 要不要寄 |
|---|---|---|---|---|---|
| A | `admin_cancel_order` 整單/部分取消 | 後台員工 | `supabase/migrations/20260804180000_m4b_e10_a8a1_admin_cancel_order.sql` · 七值映射 `20260830020000:154-162` | `cancelled_at` + `cancelled_reason`(七值對客文字) | **待 Sean** |
| B | `admin_mark_order_cancelled` 只標記 | 後台員工 | `20260902140000`(**尚未 apply**:`grep -c '^20260902140000' supabase/APPLIED.tsv` ⇒ 0) | 同上,且**一個數量都不動** | 09-02 拍甲 = **寄** |
| C | 🔴 **`expire_unpaid_orders` 逾時自動取消** | **pg_cron,沒有人按** | `20260809160000_m4b_lifecycle_l3a_expire_unpaid_orders_fn.sql:174,192` | `cancelled_reason = 'payment_expired'`,批次上限 500/次 | 🛑 **待 Sean —— 而這一格是我加的, 交辦單裡沒有** |

🔴🔴 **C 是本草案最重要的一格, 理由有三個, 而第三個最貴**:
1. **它符合 09-03 ① 的字面**:「未付款訂單被取消」—— C 取消的**每一張都是未付款的單**。
2. **它不是人按的** ⇒ 「要寄」在這條路上的意思是**批次寄信給一批逾時的客人**,一次最多 500 張單。
   ⇒ 那與「員工按下取消、寄一封給那位客人」是**完全不同量級的產品決定**。
3. 🔴 **`payment_expired` 不在那張七值映射表裡** ⇒ **今天沒有任何一句對客文字對應它**
   (數法:`grep -c "payment_expired" supabase/migrations/20260830020000*.sql` ⇒ 0;
    正對照 `grep -c "out_of_stock" 同檔` ⇒ 4 ⇒ 尺會動)
   ⇒ **要寄的話得先為它寫一句文案, 而那句話沒有人寫過。**

⇒ 端他第二題:
```
Q-逾時信:系統【自動】把逾時未付的單取消時(一次可能上百張), 要不要也寄那封信?
A: 甲 = 寄(那也是「未付款被取消」)   乙 = 不寄, 只有【員工按下取消】才寄
```
🔵 **我的推薦 = 乙。** 客人沒付款、放著過期 ⇒ 收到一封「你的訂單已取消」多半是打擾而不是服務;
而甲的第一次上線會是**一批**信同時出去,**那是最不該拿來當第一次的形狀**。
⚠️ **而這是我的判斷不是量到的** —— 我沒有任何客人反應的資料。**Sean 的生意直覺優先於我這一句。**

### 七個原因碼(A 路徑今天的全部;逐字抄自 `20260830020000:154-162`)
```
customer_request  依您要求取消
out_of_stock      商品供貨中斷,已為您取消      <- 交辦單問的「缺貨」是這一個
long_leadtime     交期無法配合,已為您取消
price_change      訂單已取消,詳情請洽客服
duplicate_order    重複訂單,已為您取消
internal_error    訂單已取消,詳情請洽客服
other             員工自己打的字(reason_detail)
```
🔵 **建議:七個碼【不要】逐碼決定寄不寄** —— 那會長出七格待辦而每一格都要 Sean 拍。
   改成**一條規則**:A 路徑一律寄,信裡帶上面那句對客文字。
   🛑 **而 `other` 那一格要單獨看**:它的文字是**員工自己打的**,會原封進到寄給客人的信裡
   ⇒ **那是一個沒有審稿的對外字面。** 要不要讓它進信是一題(我建議:`other` 的信改用一句通用文字,
   員工打的字只留在後台)。

---

## §3 文案落點(兩份, 而它們今天已經不一樣)

| 份 | 檔案 | 錨 | 現況 |
|---|---|---|---|
| 純文字 | `packages/use-cases/src/sweep-email-outbox.ts` | `function buildOrderCreatedText`(`:361`) | `order_created` 有;`order_shipped` 有 `buildOrderShippedText`;**`order_cancelled` 沒有** |
| HTML | `packages/use-cases/src/paid-email-html.ts` | `renderPaidEmailHtml`(`:184`)· `paidEmailSubject`(`:171`) | 只服務付款成功那封 |

**分派點**:`sweep-email-outbox.ts` 的 `buildEmailText`(`:326-352`)。
🔴 **它今天的 `default` 分支是 `throw`, 不是 `return`** —— 而那是刻意的
(`:341-350` 逐字記著:原本 `satisfies never` 編譯後整個消失 ⇒ 執行期會**把 event_type 字串當內文寄給真客人**)。
⇒ **所以片③ 沒落地之前, outbox 裡若出現 `order_cancelled` 列 ⇒ 計 error、列留 sending、不寄。**
✅ **那是好消息的紅**:失敗方向是「不寄」而不是「寄一封垃圾」。
🛑 **⇒ 部署順序的硬約束(不是建議)**:**模板(片③)必須先於 enqueue(片④)上。**
   而 `20260902120000:203-206` 逐字寫著:**沒有任何機制擋得住反序**
   (`scripts/deploy-order-gate.sh` 掃 `.from()` / `.rpc(`,一個寫在 use-case 裡的 event_type 字串對它是隱形的)⇒ **靠人**。

---

## §4 那支逐字鎖住文案的測試

**落點**:`packages/use-cases/src/sweep-email-outbox.test.ts:810`
逐字標題「🔴🔴 信件全文逐字(三段全開)—— **這一格就是交件時貼給 Sean 看的那份**」。

🔵 **建議:要加一格, 而且形狀照抄它, 不要自己發明。**
理由 = Sean ② 拍了「文案工程師改、不做後台可編輯」⇒ **文案的唯一保護就是這種逐字測試**:
沒有後台可編輯 ⇒ 改文案一定是改碼 ⇒ **改錯了只有測試會叫。**
🛑 **而有一格要小心, 它是這一族踩過的坑**:期望值**不可以從我要寫的那行碼推**,
要從**規格(§2 那張表拍板後的文字)**抄 —— 否則那格測試從出生起就不可能抓到那行碼的缺陷,
而它照樣會紅、照樣通得過自檢。

---

## §5 鐵則 8 判定 + 我還沒查的

**鐵則 8:🔴 是,要先提 plan 等批。** 判準逐條:
- 跨 3+ 檔:**是** —— 至少 `sweep-email-outbox.ts`(分派+模板)· 型別 union(`packages/ports/…IEmailOutbox`)· enqueue 新檔 · 後台入口 · 測試 ⇒ **≥5**。
- 動 schema:**是**(§1 選甲要改 COMMENT、選乙要新 migration)。
- 動共用元件:`packages/use-cases` / `packages/ports` ⇒ **是**。
**鐵則 12:🔴 也命中** —— ③DB 結構 ⑤對外不可回收(**寄信**)⇒ commit 前要跑 codex 對抗審查。

**⚠️ 我沒查的(照實列, 不要當成沒有)**
1. `memory project_0903-cancel-email-yes.md` / `project_0903-email-copy-engineers-edit.md` **我沒開檔** ⇒ 兩題的原話與選項字面**未確認**,我讀到的是主視窗-87 的轉述。
2. 09-03 那兩題**端出去時的題面**我沒看到 ⇒ 答不出 Sean 拍 ① 時是否被告知它會擴大 09-02 的射程。
3. 顧客站有沒有「客人自己取消」的路 —— 我只掃到 `apps/storefront/src/app/api/cron/order-ineligible-gate/route.ts` 與帳號頁,**沒有逐檔開過** ⇒ **第四條路存不存在,未確認**。
4. `tappay` 是不是就等於「刷卡」—— `20260902140000:120-122` 自己標了**未確認**,我沒有推進它。
5. 正式庫 access 我沒有 ⇒ §0-A 那句唯讀 SQL **我跑不了**,甲乙分不出來。
6. 片② (`20260902140000`) **帳本未記 apply**(`grep -c '^20260902140000' supabase/APPLIED.tsv` ⇒ 0;正對照 `^20260902120000` ⇒ 1 ⇒ 尺會動)⇒ §1 那兩道閘今天**只在檔案裡**。
