# 取消通知信 · 規格草案(線 `-mail`,2026-09-03)

> **狀態:草案,等主視窗-87 過目 ⇒ 過了才動碼。** 本檔不改任何行為。
> 🟢 **2026-09-03 02:0x §0-A 已關閉** —— 主視窗-87 對正式庫唯讀查證,**片①②都在正式庫上**,
>    是我列的兩個世界裡的【貼了而帳本沒記】那一個。⇒ **`order_cancelled` 寫得進去、測得了。**
>    逐字證據見 §0-A-3。⇒ **本檔剩下的未決都是【產品題】,不再有技術阻擋。**
> **這個檔在什麼情況下會【變成假的】**:
> ① 下面兩題其中一題被 Sean 拍了 ⇒ §1 的問句作廢、答案要搬進 §2 的表;
> ② `20260902140000`(片②)被 apply ⇒ §3 那三道閘從「檔案裡」變成「庫裡」,而**射程不會自己跟著變**;
> ③ 有人改了 `admin_cancel_order` 的七值映射表 ⇒ §2 的分類基底就過期了。
> ⇒ 三個都不是時間到期,是**別人動了東西**。⚠️ **沒有任何機制會叫**,讀本檔的人自己重跑 §5 那幾條。

---

## §0 這份草案在回答誰的問題

主視窗-87 2026-09-03 指派,逐字要求:「**哪些取消要寄 / 哪些不寄**(缺貨?客人沒付款逾時?後台手動取消?)· 文案落點 · 那支逐字鎖住文案的測試要不要跟著加一格」。
起因是 Sean 同夜兩題:
```
① 未付款訂單被取消 ⇒ 要寄取消信            (甲, 2026-09-03 00:5x)
② 信件文案 ⇒ 工程師改, 不做後台可編輯      (甲, 2026-09-03 01:0x)
```
落檔:memory `project_0903-cancel-email-yes.md` · `project_0903-email-copy-engineers-edit.md`(**我沒有開這兩支檔**,轉述自主視窗-87 ⇒ 標**未確認**)。

---

## §0-A 🔴🔴 開工前先擋一格:**片① 到底貼了沒 —— 帳本說【沒有】,而交辦單說【貼了】**

主視窗-87 指派我這條線時逐字寫:「**DB 那一半 Sean 09-02 已貼(`20260902120000`,白名單放寬)**」。
🛑 **而我一落筆要引用它就撞到了**:

```
grep -c "^20260902120000" supabase/APPLIED.tsv   ⇒ 0     <- 片①(白名單放寬)
grep -c "^20260902140000" supabase/APPLIED.tsv   ⇒ 0     <- 片②(標記函式)
🟢 正對照 ^20260902050000 ⇒ 1 · ^20260902060000 ⇒ 1      <- 同一天、同一把尺,印得出 1
🔵 負對照 ^20260902129999 ⇒ 0
```
⇒ **尺會動,而那兩個 0 是真的 0。**

🔴 **兩個世界都與這個讀數相容,而它們對「現在能不能寫碼」給【相反】的答案**:
```
甲 貼了, 只是沒有人回頭記帳本 ⇒ 正式庫的 CHECK 已含 order_cancelled ⇒ 可以往下做
乙 沒貼(或貼了別支)          ⇒ 正式庫仍是兩值 ⇒ 寫進去的 order_cancelled 列會被
                               check_violation 擋 ⇒ 🛑 **寫了的碼連測都測不了**
```
📌 **而乙正是板子 `⟦0e-MAILCANCELSEND⟧`(`docs/launch-todo.md:944`)整列存在的理由**,它逐字寫著:
「『跑得動但沒效果』的世界裡人會先寫碼再等 SQL;而【會被擋】的世界裡,寫了的碼連測都測不了
 ⇒ 兩者對『要不要現在寫』給**相反**的答案 ⇒ 做不到就別做。」

🛑 **我分不出甲與乙 —— 我沒有正式庫 access。** 這一格要**有 DB access 的人跑一句唯讀**:
```sql
SELECT pg_get_constraintdef(oid) FROM pg_constraint
 WHERE conrelid = 'public.email_outbox'::regclass AND contype = 'c';
-- 看回傳裡有沒有 order_cancelled;有 ⇒ 甲(順便補 APPLIED.tsv);沒有 ⇒ 乙(要 Sean 再貼一次)
```
⚠️ **在這一句跑出來之前,§1/§2 的兩題照樣可以端給 Sean**(那是產品決定,不依賴 DB 現況),
**而【動碼】不行** —— 那正是板子那一列講的東西。
🔵 **這一格不是在指誰講錯** —— 帳本沒更新與沒貼在我這一端印同一個東西。

### §0-A-2 🔴 追查完了(2026-09-03 01:2x,線 `-mail`)—— **證據往【乙 沒貼】倒**

主視窗-87 把它那句話的來源逐字給了我,我開檔看了,並往上游追了一層:

| # | 來源 | 逐字 | 它算什麼 |
|---|---|---|---|
| 1 | `~/pcm-mailbox/題目-取消信要不要寄-20260902.md:44` | 「資料庫那一半 Sean 2026-09-02 已貼(`20260902120000`)⇒ 白名單放寬了, **量到的**」 | 🔴 **一句沒有附方法的斷言** —— 同段落零命令、零 SQL、零回傳值 |
| 2 | `~/pcm-mailbox/那24支未記帳的-分四堆-20260902.md:48-54` | 「① **真的在等他貼** = 21 支 … 其中 3 支是今晚新做的, **已經在他的貼頁上**」,而 `20260902120000` 逐字列在那三支裡 | 🟢 **與帳本一致:等他貼 = 還沒貼** |
| 3 | `~/pcm-mailbox/給Sean-貼這三支SQL-20260902晚.md:13` | 「1️⃣ `20260902120000` 訂單取消:讓「取消通知信」有自己的類別」 | 🟢 **它被寫進【要他貼】的指示裡** |
| 4 | `~/pcm-mailbox/給Sean-還剩這一段-20260902深夜.md:64` | `('20260902120000','constraint','email_outbox_event_type_check')` | 🟢 **一句【還沒有答案】的查證 SQL**(同表末列有負對照 `ZZ 這一列一定要是 false`) |

🎯 **⇒ 三份獨立的檔都說「還在等他貼」,而說「已貼」的那一份【沒有附任何方法】。**
⇒ 主視窗-87 對這句的推測是:那個「量到的」很可能量的是**磁碟上有那支檔**,不是正式庫貼了它
——「而那兩件事在一個 `ls` 底下印同一個東西」。⚠️ **它自己標未確認,我也沒去問原作者 `-0e` ⇒ 仍是推測。**
🛑 **但不論成因是什麼,結論一樣:今天沒有任何一份紀錄證得出正式庫已經有它。**

🔴🔴 **而追這一層時撞到一格更該修的東西**:
**那一句查證 SQL 被包給 Sean 至少【四次】,而零份紀錄寫下它的答案。**
```
grep -rln "pcm_zz_not_a_real_function_20260902" ~/pcm-mailbox/ ⇒ 4 支
  給Sean-明天早上打開這一份-20260902.md:49 · 給Sean-跑這一發查詢-20260902.md:47
  給Sean-還剩這一段-20260902深夜.md:67    · 查詢-那21支在不在正式庫-20260902.md:49
🔵 而回填的答案:`grep -rn "email_outbox_event_type_check" ~/pcm-mailbox/` 的命中裡
   **沒有任何一筆是回傳值**(其餘命中全是 codex 輸出檔在引用 repo 的 `20260717020000:315`)
```
📌 **⇒ 這不是「沒有人問」,是【問了四次而沒有人收答案】。**
⇒ 那與「還沒問」在板子上印同一個東西,而**修法不同**:前者要的是一個**收答案的落點**,不是再包第五份。
✅ **⇒ 對本規格的意思**:解封本線最快的一步**不是寫碼、也不是再包一份 SQL 給 Sean**,
是**去把已經包好的那一發的答案收回來**(四份裡任一份都行,它們問的是同一件事)。

---

## §1 🛑 先擋一格:①與 2026-09-02 那筆拍板【射程不一樣】,而我不自己選邊

**2026-09-02 Sean 拍甲的射程,逐字寫在那支 migration 的 COMMENT 裡**
(`supabase/migrations/20260902120000_m4b_outbox_order_cancelled_event.sql:201-202`)——
⛔ ~~我第一版寫「已經 apply 到正式庫的 COMMENT」~~ **那句話我證不出來,見 §0-A,已作廢**:

> 「`order_cancelled` 的射程(Sean 2026-09-02 拍甲):只涵蓋【刷卡 且 已全額退款】的整單取消,
>  且那一次取消**一個品項數量都不會動**。**部分退款不涵蓋**、部分品項取消不涵蓋。」

**而片②的函式把那個射程做成了硬閘**(`20260902140000_m4b_mark_order_cancelled.sql`):
```
:319  IF v_order.payment_method IS DISTINCT FROM 'tappay' THEN RAISE '這期只開放刷卡的單…'
:324  IF v_order.payment_status IS DISTINCT FROM 'refunded' THEN RAISE '這張單還沒有全額退款…'
```
🔴 **一張【未付款】的單,`payment_status` 不是 `refunded`、`payment_method` 也常常不是 `tappay`
⇒ 它會被 `:324`(往往連 `:319`)當場擋掉。**

⇒ **09-03 的 ① 不是把碼補完就能達成的,它是把 09-02 的射程【擴大】。**
⚠️ **而我不知道 Sean 知不知道這件事** —— 兩題相隔一天、由不同的窗端出去;
09-03 那題的題面我沒看到(轉述),**所以我答不出「他是否被告知這會推翻昨天那條射程」**。

📌 **這正是 R3 那條:新訊息疑似推翻舊拍板 ⇒ 確認、不假設。** ⇒ 端他一題:

```
Q-取消信射程:一張【還沒付款】的單被取消, 那封信要走哪一條?
A: 甲 = 把 order_cancelled 的射程擴大(改 COMMENT + 放寬片②那兩道閘), 一個事件涵蓋兩種單
   乙 = 未付款那種【另開一個 event_type】(新 migration), 兩條線互不影響
   丙 = 先只做未付款那一種, 刷卡已退款那一種延後
```
🔵 **我的推薦 = 乙。** 理由兩個,都是量到的:
① `event_type` **同時是去重鍵的一半**(唯一索引 `email_outbox_event_uniq (event_type, dedup_key)`,
   COMMENT 逐字「不可以借用既有值來省一個 migration —— 借用會讓兩條線的信互相擋掉對方的唯一鍵 = 製造漏信面」)
   ⇒ **甲把兩種語意塞進同一個值, 正是那句話警告的形狀。**
② 兩種單客人的處境相反:刷卡已退款的人**錢已經回去了**;未付款的人**從頭到尾沒付過錢**
   ⇒ 信裡該不該提退款是相反的 ⇒ 合成一封會逼出一個帶 if 的文案。
🛑 **而乙的代價要一起帶**:多一支 migration、多一個模板、`buildEmailText` 多一個 case
   ⇒ **比甲多做一輪**。而 Sean ② 已拍「文案工程師改」⇒ 他知道每多一份文案就多一份人工維護成本。

---

## §2 哪些取消要寄 / 哪些不寄(**這是待填的表, 不是結論**)

**先把「取消」這個詞拆開 —— 量到的有【三條路】, 而板子與拍板句都只講了其中一條。**

| # | 路徑 | 誰觸發 | 落點(檔案:行號) | 今天寫什麼 | 要不要寄 |
|---|---|---|---|---|---|
| A | `admin_cancel_order` 整單/部分取消 | 後台員工 | `supabase/migrations/20260804180000_m4b_e10_a8a1_admin_cancel_order.sql` · 七值映射 `20260830020000:154-162` | `cancelled_at` + `cancelled_reason`(七值對客文字) | **待 Sean** |
| B | `admin_mark_order_cancelled` 只標記 | 後台員工 | `20260902140000`(**尚未 apply**:`grep -c '^20260902140000' supabase/APPLIED.tsv` ⇒ 0) | 同上,且**一個數量都不動** | 09-02 拍甲 = **寄** |
| C | 🔴 **`expire_unpaid_orders` 逾時自動取消** | **pg_cron,沒有人按** | `20260809160000_m4b_lifecycle_l3a_expire_unpaid_orders_fn.sql:174,192` | `cancelled_reason = 'payment_expired'`,批次上限 500/次 | 🛑 **待 Sean —— 而這一格是我加的, 交辦單裡沒有** |

🔴🔴 **C 是本草案最重要的一格, 理由有三個, 而第三個最貴**:
1. **它符合 09-03 ① 的字面**:「未付款訂單被取消」—— C 取消的**每一張都是未付款的單**。
2. **它不是人按的** ⇒ 「要寄」在這條路上的意思是**批次寄信給一批逾時的客人**,一次最多 500 張單。
   ⇒ 那與「員工按下取消、寄一封給那位客人」是**完全不同量級的產品決定**。
3. 🔴 **`payment_expired` 不在那張七值映射表裡** ⇒ **今天沒有任何一句對客文字對應它**
   (數法:`grep -c "payment_expired" supabase/migrations/20260830020000*.sql` ⇒ 0;
    正對照 `grep -c "out_of_stock" 同檔` ⇒ 4 ⇒ 尺會動)
   ⇒ **要寄的話得先為它寫一句文案, 而那句話沒有人寫過。**

⇒ 端他第二題:
```
Q-逾時信:系統【自動】把逾時未付的單取消時(一次可能上百張), 要不要也寄那封信?
A: 甲 = 寄(那也是「未付款被取消」)   乙 = 不寄, 只有【員工按下取消】才寄
```
🔵 **我的推薦 = 乙。** 客人沒付款、放著過期 ⇒ 收到一封「你的訂單已取消」多半是打擾而不是服務;
而甲的第一次上線會是**一批**信同時出去,**那是最不該拿來當第一次的形狀**。
⚠️ **而這是我的判斷不是量到的** —— 我沒有任何客人反應的資料。**Sean 的生意直覺優先於我這一句。**

### 七個原因碼(A 路徑今天的全部;逐字抄自 `20260830020000:154-162`)
```
customer_request  依您要求取消
out_of_stock      商品供貨中斷,已為您取消      <- 交辦單問的「缺貨」是這一個
long_leadtime     交期無法配合,已為您取消
price_change      訂單已取消,詳情請洽客服
duplicate_order    重複訂單,已為您取消
internal_error    訂單已取消,詳情請洽客服
other             員工自己打的字(reason_detail)
```
🔵 **建議:七個碼【不要】逐碼決定寄不寄** —— 那會長出七格待辦而每一格都要 Sean 拍。
   改成**一條規則**:A 路徑一律寄,信裡帶上面那句對客文字。
   🛑 **而 `other` 那一格要單獨看**:它的文字是**員工自己打的**,會原封進到寄給客人的信裡
   ⇒ **那是一個沒有審稿的對外字面。** 要不要讓它進信是一題(我建議:`other` 的信改用一句通用文字,
   員工打的字只留在後台)。

---

## §3 文案落點(兩份, 而它們今天已經不一樣)

| 份 | 檔案 | 錨 | 現況 |
|---|---|---|---|
| 純文字 | `packages/use-cases/src/sweep-email-outbox.ts` | `function buildOrderCreatedText`(`:361`) | `order_created` 有;`order_shipped` 有 `buildOrderShippedText`;**`order_cancelled` 沒有** |
| HTML | `packages/use-cases/src/paid-email-html.ts` | `renderPaidEmailHtml`(`:184`)· `paidEmailSubject`(`:171`) | 只服務付款成功那封 |

**分派點**:`sweep-email-outbox.ts` 的 `buildEmailText`(`:326-352`)。
🔴 **它今天的 `default` 分支是 `throw`, 不是 `return`** —— 而那是刻意的
(`:341-350` 逐字記著:原本 `satisfies never` 編譯後整個消失 ⇒ 執行期會**把 event_type 字串當內文寄給真客人**)。
⇒ **所以片③ 沒落地之前, outbox 裡若出現 `order_cancelled` 列 ⇒ 計 error、列留 sending、不寄。**
✅ **那是好消息的紅**:失敗方向是「不寄」而不是「寄一封垃圾」。
🛑 **⇒ 部署順序的硬約束(不是建議)**:**模板(片③)必須先於 enqueue(片④)上。**
   而 `20260902120000:203-206` 逐字寫著:**沒有任何機制擋得住反序**
   (`scripts/deploy-order-gate.sh` 掃 `.from()` / `.rpc(`,一個寫在 use-case 裡的 event_type 字串對它是隱形的)⇒ **靠人**。

---

## §4 那支逐字鎖住文案的測試

**落點**:`packages/use-cases/src/sweep-email-outbox.test.ts:810`
逐字標題「🔴🔴 信件全文逐字(三段全開)—— **這一格就是交件時貼給 Sean 看的那份**」。

🔵 **建議:要加一格, 而且形狀照抄它, 不要自己發明。**
理由 = Sean ② 拍了「文案工程師改、不做後台可編輯」⇒ **文案的唯一保護就是這種逐字測試**:
沒有後台可編輯 ⇒ 改文案一定是改碼 ⇒ **改錯了只有測試會叫。**
🛑 **而有一格要小心, 它是這一族踩過的坑**:期望值**不可以從我要寫的那行碼推**,
要從**規格(§2 那張表拍板後的文字)**抄 —— 否則那格測試從出生起就不可能抓到那行碼的缺陷,
而它照樣會紅、照樣通得過自檢。

---

## §5 鐵則 8 判定 + 我還沒查的

**鐵則 8:🔴 是,要先提 plan 等批。** 判準逐條:
- 跨 3+ 檔:**是** —— 至少 `sweep-email-outbox.ts`(分派+模板)· 型別 union(`packages/ports/…IEmailOutbox`)· enqueue 新檔 · 後台入口 · 測試 ⇒ **≥5**。
- 動 schema:**是**(§1 選甲要改 COMMENT、選乙要新 migration)。
- 動共用元件:`packages/use-cases` / `packages/ports` ⇒ **是**。
**鐵則 12:🔴 也命中** —— ③DB 結構 ⑤對外不可回收(**寄信**)⇒ commit 前要跑 codex 對抗審查。

**⚠️ 我沒查的(照實列, 不要當成沒有)**
1. `memory project_0903-cancel-email-yes.md` / `project_0903-email-copy-engineers-edit.md` **我沒開檔** ⇒ 兩題的原話與選項字面**未確認**,我讀到的是主視窗-87 的轉述。
2. 09-03 那兩題**端出去時的題面**我沒看到 ⇒ 答不出 Sean 拍 ① 時是否被告知它會擴大 09-02 的射程。
3. 顧客站有沒有「客人自己取消」的路 —— 我只掃到 `apps/storefront/src/app/api/cron/order-ineligible-gate/route.ts` 與帳號頁,**沒有逐檔開過** ⇒ **第四條路存不存在,未確認**。
4. `tappay` 是不是就等於「刷卡」—— `20260902140000:120-122` 自己標了**未確認**,我沒有推進它。
5. 正式庫 access 我沒有 ⇒ §0-A 那句唯讀 SQL **我跑不了**,甲乙分不出來。
6. 片② (`20260902140000`) **帳本未記 apply**(`grep -c '^20260902140000' supabase/APPLIED.tsv` ⇒ 0;正對照 `^20260902120000` ⇒ 1 ⇒ 尺會動)⇒ §1 那兩道閘今天**只在檔案裡**。

---

## §0-A-3 🟢 §0-A 的答案(2026-09-03 02:0x,主視窗-87 對正式庫唯讀查,**我沒有 access、這一格是轉述**)

```
pg_get_constraintdef(email_outbox_event_type_check) 逐字:
  CHECK ((event_type = ANY (ARRAY['order_created'::text,
                                  'order_shipped'::text,
                                  'order_cancelled'::text])))
⇒ order_cancelled 在白名單裡 ⇒ 20260902120000 已 apply
片② admin_mark_order_cancelled 函式存在(pg_proc count = 1)⇒ 20260902140000 也在
```
⇒ 🎯 **是我列的兩個世界裡的【①貼了而帳本沒記】** ⇒ `APPLIED.tsv` 那兩個 0 是真的,
而**它們答的是「帳本有沒有記」,不是「正式庫有沒有」** —— 兩個問題,一個讀數。
⛔ ⇒ 本檔先前寫的「⇒ 照板子 `launch-todo.md:944` 的判準,不動碼」**前提已消失**,可以動碼。
🛑 **而帳本與正式庫不一致這件事本身是一件事**(板上 `⟦b4-LEDGERNOAPPLY1⟧` 同族)⇒ 不歸本檔,主視窗記著。

🔵 **而主視窗自陳它第一發差點交出錯的結論,值得抄在這裡當語料**:
它掛的正對照是「`order_paid` 應該在 CHECK 裡 ⇒ 必須非 0」⇒ **回 0**。
它沒有直接報「尺壞了」,先開檔看 ⇒ **白名單裡根本沒有 `order_paid`**(付款那條走 `order_created`)。
📌 **⇒ 「正對照印 0」有兩種原因而它們印同一個 0**:①尺壞了 ②**我對「應該有什麼」的期望本來就是錯的**。
⇒ **只有讀那條 constraint 的原文分得出來。**

---

## §3-A 🔴 文案兩份的落點與**它們今天差在哪**(逐字量的,不是轉述)

`~/pcm-mailbox/題目-取消信要不要寄-20260902.md:36-38` 逐字警告:
「今天付款成功信有兩份(純文字一份、有排版一份,兩份都寄出去,收信軟體自己挑一份顯示)。
🔴 **而它們今天已經不一樣了** —— 同一個意思寫成兩種說法。」

✅ **我去量了,那句話今天【仍然成立】,而差異有兩層**:

| | 純文字 | HTML |
|---|---|---|
| 檔 | `packages/use-cases/src/sweep-email-outbox.ts` 錨 `buildOrderCreatedText` | `packages/use-cases/src/paid-email-html.ts` 錨 `renderPaidEmailHtml` |
| 主句 | `我們會盡快為您安排出貨,出貨後會再寄一封通知給您。` | `這封信是這筆交易的明細。我們會盡快為您安排出貨，出貨後會再寄一封通知給您。` |
| 差① | —— | **多一句開頭**「這封信是這筆交易的明細。」 |
| 差② 逗號 | ⛔ ~~`,` = **U+002C**(半形 ASCII)~~ **2026-09-03 已統一成全形** | `，` = **U+FF0C**(全形) |
| 會員中心 | 一句話:`訂單明細與最新狀態請至 PCM 會員中心查看。` | 一顆**按鈕**:`到會員中心查看訂單` |

**量法(可重跑)**:
```
python3 -c "…re.search(r\"'(我們會盡快[^']*)'\", 純文字檔) / r'(這封信是這筆交易的明細。[^\n<]*)' (HTML 檔)…"
逐字元印碼位 ⇒ 純文字 ['0x2c'] · HTML ['0xff0c']
```
🎯 **⇒ 同一個客人、同一封信,標點在兩份裡是不同的字元** —— 而**收信軟體挑哪一份是客人那端決定的**
⇒ 📌 **「他看到哪一種」不由我們決定,而我們甚至沒有在維護那個差異。**

### ⇒ 對取消信的意思(**這就是 Sean ② 拍板的成本落地的地方**)
Sean 2026-09-03 拍「信件文案工程師改、不做後台可編輯」⇒ **取消信是第三份要人工維護的對外文案**,
而**上面那張表證明:兩份制在【已經有的那一對】上已經漂了**。
🟢 **2026-09-03 02:5x Sean 拍甲 ⇒ 這一格【已經做掉了】**,逐字「甲 要 —— 一份定義, 兩邊各自取用
(repo 裡有現成的形狀可抄)/ 代價:要動到付款信那一對的結構」。
落點 `packages/use-cases/src/order-email-copy.ts`(唯一定義)+ 兩個消費端各自取用;
守門 `order-email-copy.test.ts` —— 其中一格**掃原始碼**確認 production 裡沒有第二份手打副本
(codex 抓到 `toContain` 證不了【來源】:把消費端改成手打同樣的字面照樣全綠 ⇒ 換分母才擋得住)。
⇒ 📌 **取消信落地時不會再產生第三份手打副本** —— 它從同一支檔取。

🛑 **而有一格【還開著】,不要讀成全部解決了**:統一之後純文字那半的逗號從半形變全形,
而 `sweep-email-outbox.ts` 裡有一條**反向依據**(逐字「不跨載體套用標點」,依據是
Sean 8/30 對【出貨信】答「可以」那次 ⇒ 純文字 20 個半形、零全形)。
🔴 **那是【推出來的偏好】,不是他對本信說的**;而拍甲那次的選項字面**沒有提到標點會變**
⇒ **他可能不知道拍甲會把純文字的逗號一起換掉** ⇒ 照 R3 已端一題,原註解留著加刪除線。
⚠️ **出貨信那一支【沒有跟著改】**(不在他拍板的射程裡)⇒ **現在全站有兩種標點,已知且刻意。**

---

## §4-A 🔴 那支逐字鎖住文案的測試 —— **它的作用比「測試」大,而那是量到的**

**落點**:`packages/use-cases/src/sweep-email-outbox.test.ts` 錨
`🔴🔴 信件全文逐字(三段全開)—— **這一格就是交件時貼給 Sean 看的那份**`,
期望值常數錨 `EXPECTED_ORDER_CREATED_BODY`(整串 `toBe`)。

🔴 **它今天正在擋一件真的事,而那件事有紀錄**:`sweep-email-outbox.ts` 的 `buildOrderCreatedText`
內逐字寫著 —— 2026-09-02 有人提案改那句文案,而**沒有被拍板**;
codex 把它推到更重的一格:**那不是敘述問題,是 cron 會直接把這句話寄給客人,每 5 分鐘一次,
而中間沒有人會再看一眼。**
📌 **而檔內那句結論逐字**:「`EXPECTED_ORDER_CREATED_BODY` 是一個整串 `toBe`
⇒ 它的**副作用**就是『這串對外文案不得無聲改變』—— 而那正是這一族需要的那道閘。
⇒ **改那個期望值 = 把那道鎖重設了,而重設一道鎖需要授權,而授權不存在。**」

✅ **⇒ 取消信要不要加一格:要,而且理由不是「補測試覆蓋率」**:
Sean 拍了「不做後台可編輯」⇒ **改文案一定是改碼** ⇒ **這種整串 `toBe` 是那份文案唯一的鎖。**
🛑 **兩格紀律跟著它一起帶(缺一就不是那道鎖)**:
1. **期望值從【規格】抄,不從我要寫的那行碼抄** —— 否則那格從出生起就抓不到那行碼的缺陷,
   而它照樣會紅、照樣通得過「我有寫測試」的自檢。
2. **改那個常數 = 改一道鎖 ⇒ 要授權。** ⇒ 在常數旁邊寫一句「改我要先有 Sean 的拍板落點」,
   否則下一個人會把它當成「測試過期了,順手更新」。

---

## §3-B 🛑 部署順序是**硬約束**,而**沒有機制擋得住反序**(原文在 migration 裡)

`20260902120000:203-206` 逐字:
「**真正會發生的是【部署順序】** —— enqueue(片④)先上而模板(片③)後上
⇒ sweeper 的 default 分支 throw …**而沒有任何機制擋得住那個順序**
(`scripts/deploy-order-gate.sh` 掃 `.from()` / `.rpc(`,一個寫在 use-case 裡的 event_type 字串對它是隱形的)⇒ **靠人**。」

✅ **而反序的後果是【好消息的紅】**:`sweep-email-outbox.ts` 的 `buildEmailText` `default` 分支
是 `throw`(不是 `return`)—— 而那是刻意的,同檔逐字記著:原本 `satisfies never` 編譯後整個消失
⇒ 執行期會**把 event_type 字串當成信件內文寄給真客人**(例:客人收到一封內容只有 `order_refunded` 的信)。
⇒ 改成 `throw` 之後:**計 error、列留 sending、不寄。**
📌 **⇒ 所以反序不會寄出垃圾,它會【不寄】** —— 失敗方向是安全的那一側。

**⇒ 落成可執行的順序(給實作的人)**
```
1. 片③ 模板(buildEmailText 加 case order_cancelled + 兩份文案)  ← 先上
2. 片④ enqueue(誰在什麼時候寫那一列)                              ← 後上
🔴 而 1 與 2 之間的那段時間, outbox 裡若已經有 order_cancelled 列 ⇒ 計 error、不寄(安全)
🛑 反過來 ⇒ 一樣不寄, 而它會累積 error 與死信 ⇒ 兩種都不寄, 差別在噪音
```
⚠️ **而「誰在什麼時候寫那一列」還沒有答案** —— 它等 §1/§2 那兩題(射程 / 逾時信)。

---

## §10 ⛔ ~~**正式庫已經領先程式碼一個值**~~ ⇒ 🎉 **2026-09-03 稍晚:那個差距【關掉了】**

> 🔵 **Q10 片 A(模板那一半)落地** —— Sean 2026-09-03 拍甲「刷卡已退款的取消要補一封信」。
> `order_cancelled` 已進 TS union、`buildEmailText` 有它的 case、模板與 8 格測試都在。
> ⇒ ✅ `scripts/email-event-type-union-vs-db.test.ts` 的 `KNOWN_GAPS` 已清空(它自己指示要回頭改本節)。
> 🛑 **而【寫入端】還沒做**(掃描式 enqueue 是片 B)⇒ **今天一封都不會寄** ——
>    而那是**惰性的**不是**有洞的**:沒有列被排進來,而不是排進來寄不出去。
> 📌 **順序是刻意的:模板必須先於寫入端**,反過來信會卡進死信,客人一樣收不到,只是多一批死信。

<details><summary>⛔ 舊字面(2026-09-03 03:5x 量到,留著不刪 —— 拿它去搜的人要撞到上面這段)</summary>

**正式庫已經領先程式碼一個值,而那是【當時】的狀態**(線 `-mail` 量到)

```
DB  :email_outbox.event_type 的 CHECK 自 2026-09-02 起含 order_cancelled
      (主視窗-87 對正式庫唯讀讀 pg_get_constraintdef 逐字確認,見 §0-A-3)
TS  :grep -rn "order_cancelled" packages/{ports,adapters,use-cases}/src --include='*.ts'
      排除測試檔 ⇒ **0 命中**
      🟢 正對照 同一把尺問 order_shipped ⇒ **47 命中** ⇒ 尺會動,那個 0 是真的
```
🎯 **⇒ `EmailOutboxEventType` 這個 union 今天仍是兩值** —— `'order_created' | 'order_shipped'`
(`packages/ports/src/IEmailOutbox.ts` 錨 `export type EmailOutboxEventType`)。

📌 **這正是 `20260902120000` 自己預言的那個形狀**,它逐字寫著:
「**『DB 先加了新 event_type、code 還沒跟上』是這個 repo 明文預期會發生的順序**
(`IEmailOutbox` 的 `EmailOutboxEventType` 是**手抄的 union**)」。
⇒ 🛑 **而它今天不是預言了,它是現況。**

### 🟢 而今天不會出事,理由是量到的、不是樂觀
1. **沒有任何寫入端** —— 片②(`admin_mark_order_cancelled`)**未 apply**,而它是唯一預定寫那一列的東西
   ⇒ outbox 裡今天**不會有** `order_cancelled` 的列。
2. **就算有,失敗方向是安全的** —— `sweep-email-outbox.ts` 的 `buildEmailText` `default` 是 `throw`
   (錨 `未知 event_type、fail-closed 不寄`)⇒ **計 error、列留 sending、不寄** ⇒ **不會寄出垃圾**。
   ✅ 而那一格是**刻意**的:同檔逐字記著,原本 `satisfies never` 編譯後整個消失
   ⇒ 執行期會**把 event_type 字串當成信件內文寄給真客人**。

### 🔴 而它在什麼時候會咬人 —— **寫下來,因為那一刻不會有人記得**
```
片②(admin_mark_order_cancelled)被 apply 的【那一刻】
⇒ 員工按下取消 ⇒ 寫一列 order_cancelled ⇒ sweeper 認不得 ⇒ 計 error、列卡在 sending
⇒ 耗盡 attempts 進死信 ⇒ 🛑 **而客人【沒有收到信】,畫面上沒有任何東西說這件事**
```
🛑 **⇒ 部署順序那條硬約束(模板先於 enqueue)在 `order_cancelled` 這條線上【已經被打破一半】**
—— 值域開了、寫入端在排隊,而**模板從來沒有做**。
⚠️ **而我這一片只做 `order_unpaid_cancelled`** ⇒ 我加我的值與模板,**不會順手補 `order_cancelled` 的模板**:
那是**另一條線的射程**(刷卡且已全額退款),而它的文案沒有稿、沒有拍板 ⇒ **我不發明它。**
✅ **⇒ 這一格要有人接,而它不是我的** —— 已回報主視窗-87,列進待派。

---

</details>

## §11 🔴 取消信的文案:**沒有稿,而我不發明** —— 掃過的分母寫在這裡

鐵則 1 要求動客人看得到的字之前先解析真權威。**掃了,查無**:
```
OD 專案(磁碟數)                 12
OD 裡檔名含 email 的              4   ⇒ 其中含 cancel 的 **0** · 🟢 正對照 含 paid 的 **2**
design-reference 的 .html 檔       2   ⇒ 其中提到「取消」的 **0** · 🟢 正對照 提到「購物車」的 **1**
```
⇒ 🎯 **兩個分母、各自帶正對照 ⇒ 那兩個 0 是「真的沒有」,不是「我沒找到」。**

### ⇒ 兩個決定(第一個有先例,第二個要人拍)
🟢 **① 只做純文字,不做 HTML** —— **有先例**:出貨信(`order_shipped`)今天就是純文字
(`buildOrderShippedText`,而 HTML 那條路只服務付款成功信)。⇒ **與現況一致,不是我發明的。**
🔴 **② 文案本身要 Sean 過目** —— 而它**不是「請他寫」**,是**請他看**:
Sean 2026-09-03 拍「文案工程師改、不做後台可編輯」⇒ **我寫草稿是被授權的**;
而鐵則 12⑤(對外不可回收)+ 這一族的既有紀律(付款信那句話**改了兩次都要有依據**)
⇒ **草稿要有依據,不能是我覺得順。**

✅ **而依據找得到 —— 用【既有的、已經在對客的】字面,不要新造**:
`admin_cancel_order` 的七值映射表(`20260830020000:154-162` 逐字)本來就是**寫給客人看的**:
```
customer_request  依您要求取消          out_of_stock    商品供貨中斷,已為您取消
long_leadtime     交期無法配合,已為您取消  price_change    訂單已取消,詳情請洽客服
duplicate_order   重複訂單,已為您取消      internal_error  訂單已取消,詳情請洽客服
other             員工自己打的字(reason_detail)
```
⇒ 📌 **信裡的「為什麼取消」那一句直接帶這個欄位,而不是我另寫一套** ⇒ **零新造文案。**
🛑 **而 `other` 那一格要單獨拍**:它的文字是**員工自己打的**,會**原封**進到寄給客人的信裡
⇒ **那是一個沒有審稿的對外字面。** 建議:`other` 的信改用一句通用文字,員工打的字只留在後台。
