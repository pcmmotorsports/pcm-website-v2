# 退款線地圖 — 從現在到「開燈」還剩幾步(refund-line map)

> **為什麼有這張圖**:Sean 2026-08-11 晚拍板「**退款一次到位**」=當前主線(memory `refund-line-one-shot-mainline`),
> 終點=**UI 開燈 + 已付款取消整合 + 第一筆真退款實測**。主視窗 P-519-A 派 P 窗當線圖官。
> **這張圖回答一個問題**:還剩幾片、卡在誰、Sean 要停幾次。
> **它不是 plan**:每片的做法在各自的 plan/backlog 裡,這裡只排序與標依賴。純 docs、不需批准。

---

## 一眼版

| | 還剩 | 其中要 Sean 停 |
|---|---|---|
| **片數** | **19 片** | **8~9 個已知停點**(6~7 個 apply + 1 個開燈 + 1 個真退款) |
| 已完成 | L5b-2 的 2a/2b/2c/2d 四片(三支已 apply,`supabase/APPLIED.tsv` 可查) | — |

**19 的數法**(逐段加,不是估):①RF7 **1** + ②2e/2f/2g/2h/2i/2j/2k/2l/2m **9** + ③沖銷 **1**
+ ④已付款取消整合 **3**(④-a/④-b/④-d;④-0 是先決語意題、非施工片,不計)
+ ⑤開燈 **1**(其前置 U1/U2/U3 另計 **3**)+ ⑥真退款 **1** = **1+9+1+3+3+1+1 = 19**。
⚠️ 版本史(留著是因為每次變大都是同一個原因:**片界沒切之前的那一格一定低估**):
12(憑印象)→ 14(逐段加)→ **16**(④ 由 1 格切成 3 片,E 七代 `paid-cancel-refund-integration-plan.md:89-90`)
→ **19**(⑤ 的前置畫面片被 S 五代切成 U1/U2/U3,`refund-ledger-ui-plan.md:164-166`)。

**8~9 停點的數法**:②的三批 apply(2e/2f/2g 同批 / 2k / 2m)=**3**,③沖銷片 **1**(Q1=B ⇒ 動 schema;**✅ 2026-08-12 已消耗**),
④ **1~2**(④-a 必有;④-d 可能再一個,E 檔 `:82`/`:84`),⑤ 的 U1 **1**(migration,S 檔 `:169`),
開燈 **1**,真退款 **1** ⇒ **3+1+1~2+1+1+1 = 8~9**。
🔴 **① RF7 的停點=0**(已查證,不是未知):`git show --name-only 3e542afc | grep -c '^supabase/migrations/'` → **0**。
2e-2m 的片界來自母 plan §2 v5 片表(`docs/specs/2026-08-10-l5b-2-compensation-writer-plan.md`),**未重新切片**。

---

## ① RF7-fix —— 舊帳本那條沒接完的路(E 窗,已開工)

| 欄 | 值 |
|---|---|
| 狀態 | 🔨 **進行中(E 窗)** |
| 是什麼 | 舊 PRD 十片裡**唯一沒落地**的一片;`STATUS.md:13` 逐字「退款線『最少還 4 片』真值 **1 片(RF7)**」,三獨立證據含 `settle-charge.ts:409-411` 仍回 `refund_anomaly` |
| 依賴 | 無(動的是 M3 舊帳本 `order_refunds` 那條線) |
| apply 停點 | ✅ **0**(E 七代查證、我複驗:`git show --name-only 3e542afc \| grep -c '^supabase/migrations/'` → **0**;該顆只動 `settle-charge.ts` + 其 test 兩檔)⇒ 知情缺口 1 已收 |
| 為什麼排第一 | 它與 L5b-2 是**兩本帳**;不先收掉,「退款壞掉時看哪本帳」永遠有兩個答案 |

## ② L5b-2 補償寫入線 —— 母 plan 13 片,已完 4、剩 9

**已完成(可查)**:2a `3b3382a3`(claim RPC 加 `superseded_at`)/ 2b `252970d5`(型別接線)/
2c `1a1673db`(帳本三 CHECK)/ 2d `7aa2beaa`(`result_confirmed` + terminal 換人 + `pre_one_success_uniq`)。
三支 migration `20260811060000 / 080000 / 110000` **皆已 apply**(`grep '^2026081106\|^2026081108\|^20260811110000' supabase/APPLIED.tsv`)。

| 片 | 內容(母 plan §2) | 面 | 依賴 | apply 停點 |
|---|---|---|---|---|
| **2e** | `close_released_attempt` 加 advisory lock + 「有未結案退款就拒結案」 | migration(**動已上線金流 RPC**) | 2d | 🔴 **與 2f/2g 同一批**(母 plan 明寫:只上 2g 而 2e/2f 沒上 = 只有一方排隊 = **比不做更危險**) |
| **2f** | `admin_initiate_order_refund` 加 advisory lock + 跨兩本帳的共同否決 | migration(**動已上線金流 RPC**) | 2d | 同上批 |
| **2g** | 補償寫入 RPC(開父列 + `sent`、baseline 持久化) | migration | 2e ‖ 2f | 同上批 ⇒ 🔴 **Sean 停點〔2e/2f/2g 那批〕** |
| **2h** | harness + 突變矩陣(含三方並發) | 純測試 | 2g | — |
| **2i** | 決策函式(核心點 2 的十三條合取) | 純應用層 | 2g | — |
| **2j** | 接線:獨立路由 + env flag 預設 off + 每輪硬上限 1 筆 | 純應用層 | 2i | — |
| **2k** | 對帳 RPC(未結案 → Record → 補寫 terminal) | migration | 2g | 🔴 **Sean 停點〔2k〕** |
| **2l** | T+N 確認(N=3 日曆日)+ 人工佇列四件 | 應用層 + docs | 2k | — |
| **2m** | 三支讀取面排除已 `result_confirmed` 的 attempt | migration | 2l | 🔴 **Sean 停點〔2m〕** |

🔴 **2e / 2f 是全線最危險的兩片**(動正在收錢/退錢的 RPC)⇒ 母 plan 要求**各自單獨過對抗審查、不合併審**。

## ③ 沖銷片(#405 + #417)—— ✅ **已上正式庫(2026-08-12)**

| 欄 | 值 |
|---|---|
| 狀態 | ✅ **完成並 apply**。plan **v13**(`docs/specs/2026-08-11-refund-manual-reversal-plan.md`);migration `20260812140000`;commit `8232dc13`(+`a62ec16d` / `bc8bab64` 兩顆連動)。**Q-425=B 已拍**(2026-08-12,`P-533-A`)⇒ 有效 `manual` + `refunded=false` **可自動結清**,安全網=員工可用 RPC 改回來 |
| 是什麼 | `manual` 判定填錯之後的合法修正路徑(#405)+ 2d 回退閘那句「先由人處理」的出口(#417) |
| 交付物 | ①`manual_reversal` 事件型別 + 三欄 + 複合自我 FK ②「至多一個有效終局」從**索引級降級成 trigger 級**(`pre_one_terminal_uniq` 已 DROP)③**canonical view `payment_refund_effective_terminal`**=有效事件的單一權威 ④RPC `admin_correct_refund_manual_verdict`(七道守門 + CAS)⑤`op6a` 改吃 view |
| 依賴 | 沖銷片排在 2e 之前(Q1=B)—— 它交付的正準語意,**2e/2f 直接蓋在它上面** |
| apply 停點 | ✅ **已消耗**(2026-08-12,Sean 預批〔`P-547-A` 逐字:「08-12 午『好,上吧』」〕、主視窗執行;七組後置斷言 NOTICE 逐字命中檢查表判準) |
| 🔴 上線後仍成立的三件事 | ①**第一筆沖銷寫入後,本片回退永久關閉**(實測 `23505`/`pre_one_terminal_uniq`;要改判定邏輯不需回退,view/trigger 可 `CREATE OR REPLACE` 換述詞)②翻面 C:同顆 refund 有 `result_success`+`result_failed` 者從 `needs_human` **變成可自動結清**(方向變寬,已列給 Sean)③`actor` 欄**不是稽核依據**(存在性驗證非身分驗證,全線既有性質)⇒ **#436** |
| 審查 | R1 code-reviewer FAIL(6MF+7nit)→ R2 FAIL(2MF+5nit,**兩條都是折 R1 的修法製造的**)→ 關卡2 codex 4 findings,全折。逐條在 plan §14/§15/§16 |
| 驗證 | `scripts/refund-manual-reversal-verify.sh` **84/84**、突變 **13 靶**逐靶紅格非空;`op6a-verify` 51/51、`l5b2-2d-verify` 37/37、`l5b2-2c-verify` 42/42 |
| 🔴 給 2e/2f 的交接 | **讀取面一律消費 canonical view,不得自己再問「有沒有 manual」**(plan §2d-1 契約)。母 plan §4b `:457` 要求 2e 的排除條件認 `result_confirmed` —— 現在改成:**認 view**,語意由 view 定義 |

## ④ 已付款取消 × 退款整合 —— **片界已切**(E 七代,2026-08-11)

權威=`docs/specs/2026-08-11-paid-cancel-refund-integration-plan.md`。**它不是一片,是 3 片 + 1 個先決語意題**;
E 的理由逐字:「已付款取消」與退款線**互相不知道對方存在**(該檔 `:13`)。

| 片 | 內容 | 面 | 依賴 | apply 停點 |
|---|---|---|---|---|
| **④-0**(先決,**非施工片**、不計片數) | 語意釘死:取消後 `payment_status` 與 `cancelled_at` 的狀態機關係;取消與退款誰先誰後 | 純決策 + docs | 無 | 無 |
| **④-a** | 放寬 A8a1/A8a2 允許集合 + 重定冪等指紋 + 「不允許取消完不管錢」的 DB 側落點 | migration(**動已上線金流 RPC**) | ④-0、2g、RF7 | 🔴 **Sean 停點** |
| **④-b** | 應用層 + UI:`cancel-view` 拒因改寫、二選一表單、與 `RefundSection` 的互動 | 應用層 + UI | ④-a apply 後 | 無 |
| **④-d** | 復原(未退款可復原、已退款不可)+「復原訂單≠復原採購」 | migration + 應用層 | ④-a/b + **canonical view** | 🔴 **可能再一個** |

⚠️ **④-a 與 ④-b 不得同片**(E 檔 `:86`):④-a 動的是正在收錢/退錢的 RPC,母 plan 對 2e/2f 的
「各自單獨過對抗審查、不合併審」同樣適用。
🔴 **④-d 對 canonical view 的依賴是硬的**:「未退款可復原」要回答「這張單有沒有退款」——
那是 ③ 沖銷片交付的語意,④-d **不得自己寫判準**(E 檔 `:131-133`)。
🔴 E 檔 `:112-115` 的 Q-D3(④-d 排在哪)**已上 Sean 桌**(主視窗與 E 皆薦 A=排 ⑤⑥ 之後),**不阻塞 ④-a**;
它的答案決定一眼版的停點數落在 **8 還是 9**。

## ⑤ 開燈:`REFUND_UI_ENABLED` + repay 七題接線

| 欄 | 值 |
|---|---|
| 狀態 | ⏳ 未開 |
| 是什麼 | `apps/admin/src/lib/payment/refund-ui-flag.ts:22` 的 `REFUND_UI_ENABLED === '1'` 從 off 轉 on;repay 七題(P1-P6,memory `repay-customer-experience-seven-decisions`)的對客接線 |
| 依賴 | ②全線 + ④ + **U1/U2/U3 三片**(下表);開燈前必須先有「退款帳本的畫面」——該片**原本不在任何 plan 裡**(舊缺口 3),S 五代已補上 plan |
| 停點 | 🔴 **Sean 停點〔開燈〕**=開燈本身(env 從 off 轉 on 是他的動作) |

**⑤ 的前置三片**(權威=`docs/specs/2026-08-11-refund-ledger-ui-plan.md`;純讀取面、零寫入鈕):

| 片 | 內容 | 片型 | 依賴 | apply 停點 |
|---|---|---|---|---|
| **U1** | L5b 帳本讀取 RPC(SECDEF + `search_path=''` + ACL 閉世界斷言);**消費 ③ 的 canonical view** | 🔴 高風險(鐵則 12①②③) | **2g 之後**(見下) | 🔴 **Sean 停點** |
| **U2** | 讀取層 + 顯示層純函式(mapper、標籤字典、N+1 截斷旗標)+ 單測 | 標準片 | U1 | 無 |
| **U3** | 訂單頁區塊掛載(`order-detail-route.tsx` 加第四路 `allSettled`)+ 頁層接線測試 | 標準片 | U2 | 無 |

🔴 **U1 的依賴 2026-08-12 精確化成「2g 之後」**(S 檔 Q4=A,主視窗裁;`P-533-A` 轉達)——
比原本的「③ 之後」**更晚**:③ 只給語意,而兩張表現在 **0 列**、寫入面在 2e/2f/2g
⇒ 排在 2g 之後才有真實資料可驗「員工看不看得懂」。
(S 檔 `:183` 的片表字面仍寫「沖銷片 ③ 完成」=Q4 之前的舊字面,那是 S 窗要改的,不是我。)
**下限仍然是 ③**(S 檔 `:152-154`):搶先做就會在 RPC 裡多出**第二份**有效事件判定,
而現況 `op6a:147-154` 已經有一份、③ 自己要改寫它 ⇒ 搶先做等於**從兩份變三份**。
✅ **已裁 A=整片等**(主視窗 `S-079-A`,2026-08-12 00:0x;S 自薦 A、P 附議、裁定一致)⇒ U1/U2/U3 **不拆先後、整組一起排**;
落點由稍後的 **S 檔 Q4=A 精確化成「2g 之後」**(`P-533-A`)—— 兩個裁定不衝突:前者管「拆不拆」、後者管「排哪裡」。

## ⑥ 第一筆真退款實測

| 欄 | 值 |
|---|---|
| 狀態 | ⏳ 未開 |
| 依賴 | ⑤ 開燈 |
| 停點 | 🔴 **Sean 停點〔真退款〕**=他自己刷一筆、退一筆(對照 O7「第一筆真刷卡」的形制) |
| 為什麼它是終點 | 前面每一片證的都是「機制對」;只有這一步證「**錢真的退回去了**」。#404(`record.amount` 單位)也綁在這:它等的正是一筆正式站 Record |

---

## 依賴序(把上面壓成一條線)

```
① RF7(E)                                    零 apply 停點,與下面無依賴

③ 沖銷片  ──[停點:沖銷 ✅已消耗 08-12]     ✅ 已上正式庫 ⇒ 全線共同上游**已解除**
   ├─→ ② 2e ‖ 2f → 2g ──[停點:2efg 同批] → 2h → 2i → 2j
   │        │                └─→ ④-a ──[停點:④-a] → ④-b
   │        │                     (④-a 的依賴=④-0 + 2g + ①RF7,**不是直接掛 ③**)
   │        │                       └─→ ④-d ──[停點:④-d?]  ← 需 ③ 的 canonical view
   │        │                                                 排哪裡待裁,E 檔 Q-D3
   │        └─→ 2k ──[停點:2k] → 2l → 2m ──[停點:2m]
   │
   └─(經 2g)→ U1 ──[停點:U1] → U2 → U3               ← 語意需 ③;**排程排在 2g 之後**(Q4=A)
        ④-0(先決語意題)可與上面任何一段平行,零依賴

                        ⑤ 開燈 ──[停點:開燈]          需 ②全線 + ④ + U1~U3
                           └─→ ⑥ 真退款實測 ──[停點:真退款]
```
🔴 **③ 是三條線的共同上游**(2e 的排除條件、④-d 的「有沒有退款」、U1 的讀取面全部消費它的 canonical view)
⇒ 它卡住 = 全線卡住。這是 Q1=B 之後的新形狀,不是原本的線性鏈。

## 🔴 這張圖沒有回答的(知情缺口,照 gate plan 的寫法)

1. ~~① RF7 的 apply 停點不在我射程~~ ✅ **已收**(2026-08-12):**停點=0**,數法在一眼版。
2. ~~④ 的片界未定~~ ✅ **已收**:E 七代已切成 ④-0/④-a/④-b/④-d。**但 ④-d 排哪裡仍待裁**
   (E 檔 `:112-115` Q-D3)⇒ 停點數還有 ±1 的活動空間,這就是一眼版寫 **8~9** 而不是單一數的原因。
3. ~~⑤ 開燈前需要的「退款帳本畫面」不在任何 plan 裡~~ ✅ **已收**:S 五代已出 plan、切成 U1/U2/U3。
   ✅ 其排序題(整片等 ③ vs U2/U3 先行)**也已裁 A=整片等**(`S-079-A`)⇒ 本缺口全收。
4. 🔴 **仍未收**:停點數只數**已知**的;②的三批 apply 若被拆更細,還會變多。
   而且 ④-d 與 U1 的停點是**這一輪才長出來的** —— 前一版寫「6 個」時它們都還不存在。
5. 🔴 **本檔自己示範過一次「改一處漏三處」**:七代把一眼版的 12→14 改掉之後,這一節的三個舊數字原封不動。
   **2026-08-12 這次改 14→19 有照做**,數法=`grep -nE '[0-9]+ 片|[0-9]+ *個?已知?停點|停點 [0-9]' <本檔>`。
   🔴 **而它當場掃出五處**:2g/2k/2m/開燈/真退款 五個「Sean 停點 N」的**序號全部過期**
   (③ 與 ④-a、U1 插進來之後,原本的 1-5 要變成 2-8)。
   ⇒ **改成機制**:停點不再編號,一律寫成 `停點〔片名〕`。序號會隨插片漂移、片名不會 ——
   這比「記得回頭改序號」可靠,因為前一版正是敗在「記得」。**下一個動這張圖的人請維持不編號。**

## 這一輪(2026-08-12)改了什麼、依據是誰

| 改動 | 依據(我複驗過,不是轉抄) |
|---|---|
| ① 停點 未知 → **0** | `git show --name-only 3e542afc \| grep -c '^supabase/migrations/'` → 0 |
| ④ 1 格 → **3 片 + 1 先決題** | `docs/specs/2026-08-11-paid-cancel-refund-integration-plan.md:79-90` |
| ⑤ 前置 → **U1/U2/U3** | `docs/specs/2026-08-11-refund-ledger-ui-plan.md:164-169`(U1 依賴 ③=`:152-154`) |
| 片數 14 → **19**、停點 6 → **8~9** | 上三條逐段相加,數法寫在一眼版 |

— P 七代起草 2026-08-11 23:0x;P 八代 2026-08-12 00:0x 依 E/S 兩份新 plan 重排(`P-528-A` 轉達)
