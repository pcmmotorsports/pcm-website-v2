# 退款線地圖 — 從現在到「開燈」還剩幾步(refund-line map)

> **為什麼有這張圖**:Sean 2026-08-11 晚拍板「**退款一次到位**」=當前主線(memory `refund-line-one-shot-mainline`),
> 終點=**UI 開燈 + 已付款取消整合 + 第一筆真退款實測**。主視窗 P-519-A 派 P 窗當線圖官。
> **這張圖回答一個問題**:還剩幾片、卡在誰、Sean 要停幾次。
> **它不是 plan**:每片的做法在各自的 plan/backlog 裡,這裡只排序與標依賴。純 docs、不需批准。

---

## 一眼版

| | 還剩 | 其中要 Sean 停 |
|---|---|---|
| **片數** | **14 片** | **6 個已知停點**(4 個 apply + 1 個開燈 + 1 個真退款) |
| 已完成 | L5b-2 的 2a/2b/2c/2d 四片(三支已 apply,`supabase/APPLIED.tsv` 可查) | — |

**14 的數法**(逐段加,不是估):①RF7 **1** + ②2e/2f/2g/2h/2i/2j/2k/2l/2m **9** + ③沖銷 **1**
+ ④已付款取消整合 **1** + ⑤開燈 **1** + ⑥真退款 **1** = **14**。
⚠️ 我第一版寫「12」,是**沒有逐段加、憑印象寫的** —— 落筆後照自己寫的數法重數才發現差 2。
**6 個停點的數法**:②的三批 apply(2e/2f/2g 同批 / 2k / 2m)=3,**③沖銷片 1**(Q1=B ⇒ 它動 schema、有自己的 apply 停點),加開燈 1、真退款 1。
④的停點**仍未定**(片界還沒切)⇒ **這個數只會變大**。
2e-2m 的片界來自母 plan §2 v5 片表(`docs/specs/2026-08-10-l5b-2-compensation-writer-plan.md`),**未重新切片**。

---

## ① RF7-fix —— 舊帳本那條沒接完的路(E 窗,已開工)

| 欄 | 值 |
|---|---|
| 狀態 | 🔨 **進行中(E 窗)** |
| 是什麼 | 舊 PRD 十片裡**唯一沒落地**的一片;`STATUS.md:13` 逐字「退款線『最少還 4 片』真值 **1 片(RF7)**」,三獨立證據含 `settle-charge.ts:409-411` 仍回 `refund_anomaly` |
| 依賴 | 無(動的是 M3 舊帳本 `order_refunds` 那條線) |
| apply 停點 | **未知**(E 的片,不在我射程)⇒ 新代要向主視窗確認 |
| 為什麼排第一 | 它與 L5b-2 是**兩本帳**;不先收掉,「退款壞掉時看哪本帳」永遠有兩個答案 |

## ② L5b-2 補償寫入線 —— 母 plan 13 片,已完 4、剩 9

**已完成(可查)**:2a `3b3382a3`(claim RPC 加 `superseded_at`)/ 2b `252970d5`(型別接線)/
2c `1a1673db`(帳本三 CHECK)/ 2d `7aa2beaa`(`result_confirmed` + terminal 換人 + `pre_one_success_uniq`)。
三支 migration `20260811060000 / 080000 / 110000` **皆已 apply**(`grep '^2026081106\|^2026081108\|^20260811110000' supabase/APPLIED.tsv`)。

| 片 | 內容(母 plan §2) | 面 | 依賴 | apply 停點 |
|---|---|---|---|---|
| **2e** | `close_released_attempt` 加 advisory lock + 「有未結案退款就拒結案」 | migration(**動已上線金流 RPC**) | 2d | 🔴 **與 2f/2g 同一批**(母 plan 明寫:只上 2g 而 2e/2f 沒上 = 只有一方排隊 = **比不做更危險**) |
| **2f** | `admin_initiate_order_refund` 加 advisory lock + 跨兩本帳的共同否決 | migration(**動已上線金流 RPC**) | 2d | 同上批 |
| **2g** | 補償寫入 RPC(開父列 + `sent`、baseline 持久化) | migration | 2e ‖ 2f | 同上批 ⇒ **Sean 停點 1** |
| **2h** | harness + 突變矩陣(含三方並發) | 純測試 | 2g | — |
| **2i** | 決策函式(核心點 2 的十三條合取) | 純應用層 | 2g | — |
| **2j** | 接線:獨立路由 + env flag 預設 off + 每輪硬上限 1 筆 | 純應用層 | 2i | — |
| **2k** | 對帳 RPC(未結案 → Record → 補寫 terminal) | migration | 2g | **Sean 停點 2** |
| **2l** | T+N 確認(N=3 日曆日)+ 人工佇列四件 | 應用層 + docs | 2k | — |
| **2m** | 三支讀取面排除已 `result_confirmed` 的 attempt | migration | 2l | **Sean 停點 3** |

🔴 **2e / 2f 是全線最危險的兩片**(動正在收錢/退錢的 RPC)⇒ 母 plan 要求**各自單獨過對抗審查、不合併審**。

## ③ 沖銷片(#405 + #417)—— plan 已寫,卡在一題

| 欄 | 值 |
|---|---|
| 狀態 | ✅ **plan v5**(關卡1 R1+R2 共 16 條全折;Q1=B / Q2=A / Q3=A 已拍;Q2 語意由 `P-523-A` 校正=每筆終局至多一沖、修正鏈狀)⇒ 實作歸 P 八代(`docs/specs/2026-08-11-refund-manual-reversal-plan.md`)。⏳ **卡一題**:「有效 `manual` 但 `refunded=false` 算什麼」已送 Sean(`P-525-Q`),未回不進實作 |
| 是什麼 | `manual` 判定填錯之後的合法修正路徑(#405)+ 2d 回退閘那句「先由人處理」的出口(#417) |
| 依賴 | 🔴 **Sean 2026-08-11 23:15 拍 Q1=B 之後,序反過來:沖銷片排在 2e 之前** —— 它交付的是「有效事件」的正準語意(canonical predicate),**2e 直接蓋在它上面**,免回頭改 |
| apply 停點 | 視 Q1 而定(最小出口案=**零 schema、零停點**) |
| 🔴 卡點 | 關卡1 推翻了我原本的推薦:**沒有 UI、沒有讀取面時先做 DB+RPC = 凍住一份高風險契約而員工那邊沒變** |

## ④ 已付款取消 × 退款整合

| 欄 | 值 |
|---|---|
| 狀態 | ⏳ 未開;**片界未定**(需要一份 plan) |
| 是什麼 | 「已付款可取消」(Sean 07-26 拍板)與退款線接起來 —— 取消之後錢怎麼退、退到哪本帳 |
| 依賴 | ② 的 2g(要有補償寫入路徑)+ ① RF7(要先只剩一本帳) |
| apply 停點 | 未知(plan 出來才知道) |

## ⑤ 開燈:`REFUND_UI_ENABLED` + repay 七題接線

| 欄 | 值 |
|---|---|
| 狀態 | ⏳ 未開 |
| 是什麼 | `apps/admin/src/lib/payment/refund-ui-flag.ts:22` 的 `REFUND_UI_ENABLED === '1'` 從 off 轉 on;repay 七題(P1-P6,memory `repay-customer-experience-seven-decisions`)的對客接線 |
| 依賴 | ②全線 + ④;**開燈前必須先有「退款帳本的畫面」**(現況:`grep -rl 'payment_refund_events' apps/` → **0**,L5b 帳本零 UI) |
| 停點 | 🔴 **Sean 停點 4 = 開燈本身**(env 從 off 轉 on 是他的動作) |

## ⑥ 第一筆真退款實測

| 欄 | 值 |
|---|---|
| 狀態 | ⏳ 未開 |
| 依賴 | ⑤ 開燈 |
| 停點 | 🔴 **Sean 停點 5 = 他自己刷一筆、退一筆**(對照 O7「第一筆真刷卡」的形制) |
| 為什麼它是終點 | 前面每一片證的都是「機制對」;只有這一步證「**錢真的退回去了**」。#404(`record.amount` 單位)也綁在這:它等的正是一筆正式站 Record |

---

## 依賴序(把上面壓成一條線)

```
① RF7(E,進行中)

③ 沖銷片(定「有效事件」正準語意)──[apply 停點 A]──┐   🔴 Q1=B 之後排到 2e 前面
                                                  ↓
        ② 2e ‖ 2f → 2g ──[Sean apply 停點 1]──→ 2h → 2i → 2j
                     ├─→ 2k ──[停點 2]──→ 2l → 2m ──[停點 3]
        └─→ ④ 已付款取消整合(需 2g + RF7)
                        └─→ ⑤ 開燈 ──[停點 4]──→ ⑥ 真退款實測 ──[停點 5]
```

## 🔴 這張圖沒有回答的(知情缺口,照 gate plan 的寫法)

1. **① RF7 的 apply 停點與完成判準不在我射程**(E 的片)⇒ 「還剩 14 片」這個數字**含一片我沒讀過的內容**。
2. **④ 的片界未定**:它現在是一格佔位,真正切完可能不只一片 ⇒ **14 這個數字只會變大,不會變小**。
3. **⑤ 開燈前需要一個「退款帳本畫面」**,而那片**目前不在任何 plan 裡** —— 上面沒有它的列。
   這是本圖最該被質疑的一格:**開燈的前置比圖上畫的多**。
4. 停點數(6)只數了**已知**的;②的三批 apply 若被拆更細,停點會變多。
5. 🔴 **本檔自己就示範了一次「改一處漏三處」**:我把一眼版的 12→14、6→5 改掉之後,
   這一節裡的三個舊數字**原封不動**,是落檔後回頭掃才發現的。⇒ 引用本圖的數字前,
   請以**一眼版的數法**為準(那裡寫了逐段加法),不要引用散在文中的數字。

— P 七代,2026-08-11 23:0x
