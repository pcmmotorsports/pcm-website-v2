# 混軌單擋住「把這張單結掉」的文案 · ✅ 定案

> **狀態**:🟢 **定案 = 版本 A(Sean 2026-09-13 親自重寫並標「最推薦,邏輯清楚」)。**
> **來源**:Sean 答 Q3′ 甲 = 手動那顆鈕也要加混軌前置閘 ⇒ 混軌的單員工按不動。
> **plan**:`docs/plans/2026-09-13-card-full-refund-auto-cancel-plan.md` §3-quater ⑦-ter
> 🔴 **B 窗寫的第一版被他退回重寫** —— 留痕在本檔最後, 含他的批評逐字。

---

## 🔴🔴 後台文案四條原則(Sean 2026-09-13)—— **這是標準, 不只是這一段的意見**

> 📌 **放在開頭是刻意的**:後面還有好幾段後台文案要寫(下一步欄、發票、備註)
> ⇒ **要寫後台文案的人先讀這四條, 不要再被退一次。**

> **他的總評逐字**:「**語氣太瑣碎、邏輯跳躍,不像標準的電商後台系統 UI 文案(Microcopy)**」

```
① 角色定位
   讀者是【忙碌的客服 / 門市 / 後台操作人員】, 不是工程師。
   ⇒ **簡潔、果斷、專業。**
   🔴 **不要碎念, 不要抒發系統設計的無奈。**

② 三段式結構
   【核心狀態】一句話講為何被擋
   【風險/原因】為什麼要防呆
   【行動指引】不要按什麼 + 要回報給誰

③ 術語簡化
   用「**複合付款**」, 而不是「這張單的錢不是只用一種方式收的」。
   🔴 **不要在正文裡辯解「這張單本身沒有問題」「不是你操作錯誤」** —— 直接給解決方案。

④ 長度
   **120 字以內**, 善用粗體與列點。
```

🎯 **③ 那一條打掉的是一個【我們自己加的要求】** ——
主視窗當初交辦「不要讓他以為是自己做錯了」, 而 Sean 要的是**別廢話、給指令**。
📌 **一個忙碌的人不需要被安慰, 他需要知道下一步。**

---

## ✅ 定案:版本 A(Sean 逐字)

```
無法在此直接結案

本訂單採用複合付款（包含信用卡及匯款/現金），目前系統暫不支援自動計算信用卡剩餘應退金額。
為避免產生退款差額遺漏，請勿直接將此單標記為「已取消」。

👉 處理方式：請記錄本筆訂單編號，聯繫系統工程人員協助結案。（可至本頁「收款」區塊核對實際收款管道與金額）
```

### ⚠️ 標點 —— **照他的字面原樣, 不要統一**

```
🔴 他這一版用的是**全形逗號(,)與全形括號( )**。
⇒ 📌 **原樣照抄, 不要統一成半形。**
🔴🔴 **而我第一次寫本檔時把它全部打成了半形** —— 在這一段警告的**正上方**。
   ⇒ 📌 一條寫在檔裡的規矩，擋不住寫它的人自己。收尾核對才拓到，已修。
⚠️ **而【未經確認那是不是刻意的】** —— 今天已經為標點來回過兩次
   (`docs/specs/2026-09-13-bank-order-amount-changed-email-copy.md` 那一份是
    「正文半形 + LINE 那行全形」, 而那是問過他本人才定的)。
⇒ 🛑 **要改標點 ⇒ 先問他, 不要自己裁。**
```

### 🔬 落地時要核的兩處字面(我已當場核過, 不是憑記憶)

```
· 「收款」區塊存在        ⇒ `payment-list.tsx:234` 的 `<h2>收款</h2>`
· 它真的看得到「管道與金額」⇒ 同檔 `:59` 逐列印 `{e.railLabel}`(收款管道)
                              同檔 `:79` 逐列印 `{e.amountLabelCompact}`(金額)
⇒ 🟢 **所以他那句括號裡的話是真的** —— 指得到一個存在的地方。
📌 而這是這個後台的既有紀律:**指位置, 不指流程名**
  (`cancel-review-section.tsx:78-83` 逐字, 旁邊記著一次「指向一個畫面上不存在的東西」的事故)。
```

### 🔵 一個看起來矛盾、而其實不矛盾的地方(寫下來擋住下一個人)

```
· B 窗那一版的紀律是「**一個金額都不印**」—— 理由:那道閘擋的正是「系統算不清楚金額」,
  印一個數字就是印一個我們自己不相信的數字。
· 而 Sean 這一版**有提到金額** ——「核對實際收款管道與金額」。
⇒ 🟢 **兩者不衝突, 因為它們講的是兩種數字**:
    ⛔ 不可以印的 = **我們算出來的**「還差多少」(那正是算不出來的東西)
    ✅ 可以指的 = **已經記錄在案的**實收金額(那是事實, 而且本來就印在那張清單上)
⇒ 📌 **這一格寫下來, 是因為它看起來像矛盾** —— 下一個人可能會「順手統一」掉其中一邊。
```

---

## 🔴 而那道閘仍然要【可分辨】—— 理由換過, 必做不變

```
⛔ ~~原本的理由:要寫得下「這不是你操作錯誤」那句安慰的話, 就必須分得開~~
   ⇒ 🟢 **Sean 原則 ③ 把那句話刪掉了 ⇒ 這個理由消失。**

🔴🔴 **而必做不變, 理由換成更硬的那一條**:
  今天這條路的**所有**拒絕都收斂成同一句
  (`cancel-actions.ts:400-404`:`outcome.code === 'rejected'` ⇒ 一律 `markRejectedResultQuery()`)。
  而那一句**列舉了它的原因**(`cancel-result-panel.tsx:181-198` 逐字):
    「『把這張單結掉』只適用於刷卡收款、而且錢已經全額退完的單,
      並且這張單先前沒有部分取消過。」
  ⇒ 📌 **加了混軌這道閘而不給它自己的碼 ⇒ 那個列舉少一項 ⇒ 那句話變成假的。**
    員工會對著一張「刷卡、已全退、沒部分取消過」的單, 讀到一句說它應該可以結掉的話。
⇒ ✅ **必做**:混軌那道閘要回一個**與其他拒絕分得開**的碼
  (自己的 SQLSTATE 或自己的 outcome code)。
  🛑 不做的話**兩件事同時發生, 而兩個都是安靜的**:既有那句變成假的, 而這句新的印不出來。
```

> 🔵 **2026-09-16 補記(⟦b4-MARKCANCELNONCARD⟧)—— 上面引的那句文案【已經改了】, 而這一節的必做仍然成立。**
> · 上面逐字引的「只適用於**刷卡收款**、而且錢已經全額退完的單」是**當時**的字面。
>   `20260916210000` 把「只開放刷卡」那道閘放寬掉 ⇒ 那句話改成
>   「適用於錢已經全額退完的單(刷卡或匯款/現金都可以)」。
>   ⚠️ **而抓到它過期的不是走查, 是一發為了驗別的東西而跑的突變測試**
>   ——📌 **一段沒有人讀的 fallback 文案會安靜地過期。**
> · 🟢 **而這一節要的那個機制, 同一天做出來了**:被拒現在會帶一顆 `?mr=<原因碼>`
>   (`cancel-action-state.ts` 的 `classifyMarkRejection` 白名單 → `cancel-result-panel.tsx`
>   的 `MARK_REJECTED_BY_REASON`)⇒ **「與其他拒絕分得開的碼」這條路已經鋪好了。**
> · 🔴 **混軌那道閘仍然【沒有】自己的碼** —— 它要做的只剩「在 RPC 那側給它一個分得開的訊息,
>   再往白名單加一列」。**本節不因為機制做好了就結案。**

🟢 **形狀照舊**:這個後台既有的 `{tone, title, hint}` 擋住文案家族
(`cancel-review-section.tsx` 的 `BLOCK_REASON_TEXT` / `cancel-result-panel.tsx` 的 `MARK_REJECTED_TEXT`)。

---

## 📁 留痕:被否決的兩版

### ⛔ 版本 B(Sean 自己給的第二版, 未採用)

```
🔵 他標「**極簡精準版**, Modal 適用」。
⇒ 📌 **未採用的理由**:主視窗裁 A(他自己標 A 是「最推薦, 邏輯清楚」)。
⚠️ **留著不刪** —— 哪天這段話要塞進一個 Modal(空間更窄), B 就是現成的起點。
🔴 **而 B 的字面本檔【沒有收到】** —— 主視窗轉述時只說了它存在與定位, 沒有給逐字。
  ⇒ 要用 B 的那一天, **要回去跟他拿原文**, 不要照這段描述重寫一版冒充。
```

### ⛔ B 窗第一版(2026-09-13, 被 Sean 退回)

```
無法在此直接結案 —— ⛔ 以下為被退回的原文, 逐字留著

  這張單收過兩種方式的錢,先不開放在這裡結掉

  這張單的錢不是只用一種方式收的(例如刷卡之外還收過匯款或現金),
  而系統現在算不出卡片那邊還有沒有錢沒退回去。
  這種單如果直接標成已取消,少退的那一筆不會出現在任何一張待辦上,不會有人再發現它。
  請先不要按,把單號告知系統維護。
  這張單本身沒有問題 —— 客人確實是用兩種方式付的,也不是你操作錯誤。
  本頁的「收款」裡看得到這張單收過哪幾種方式。
```

**它錯在哪(照 Sean 的四條原則對)**
| 原則 | 第一版的問題 |
|---|---|
| ① 簡潔果斷 | 「不會有人再發現它」是**在抒發系統設計的無奈** —— 對一個忙著處理單子的人沒有用。 |
| ② 三段式 | 三段都在, 而**順序被稀釋**:行動指引後面還跟著兩句解釋 ⇒ 最重要的那句不在最後。 |
| ③ 術語簡化 | 「這張單的錢不是只用一種方式收的」= **把一個名詞拆成一句白話** ⇒ 他要的是「**複合付款**」。 |
| ③ 不要辯解 | 「這張單本身沒有問題 / 也不是你操作錯誤」—— **兩句辯解**, 而他要的是直接給解決方案。 |
| ④ 120 字 | 第一版約 150 字 ⇒ **超標**。 |

🔬 **而有一格值得單獨記**:第一版那兩句辯解**不是憑空加的** ——
  它照的是這個 codebase 的既有先例(`cancel-review-section.tsx:113` 那一則就以
  「**這不是你操作錯誤**」結尾)。
⇒ 📌 **所以這不是「沒有抄先例」, 是【抄了一個 Sean 沒有審過的先例】。**
  那一則是工程師寫的、活在碼裡, 而它不等於他要的標準。
⇒ 🛑 **抄先例的效力止於「那個先例被誰審過」** —— 而那正是本片一路在抓的形狀:
  **一個看起來有依據、而那個依據沒有人拍過板的東西。**
