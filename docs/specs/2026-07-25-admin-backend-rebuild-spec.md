# 後台重建完整規格(提案 v1,待 Sean 拍板)

> **狀態:** 🟡 提案、**尚未拍板、尚未實作**
> **建立:** 2026-07-25
> **北極星(Sean 2026-07-25 定調,逐字):** 「可以完整上線給員工使用,操作,修改網站。而且他們不是工程師,單純可以從後台編輯商品、新增商品、接非網站有的商品訂單、匯款、編輯客人訂單內容、備註、快遞單號輸入、單號追蹤、email 通知之類」
> **範圍演進:** 訂單後台 → 「要做就做全面」全站後台 → 上述北極星。
> **驗收標準不是「功能抄得多齊」,是「員工能不能不找工程師就把一天跑完」。**
> **研究底稿(23 份、5021 行,已入 repo):** `docs/specs/research-2026-07-25/` —— **12 份平台研究**(Shopify / Medusa v2 / Odoo+WooCommerce+AutoDS / 真瀏覽器 admin UX / 商品後台 / 後台通用 UX 規範 / 客戶價格設定供應商 / 內容媒體營運 / **台灣電子發票** / **台灣會計規範**)+ **2 份 PCM 現況盤點**(訂單域 / 訂單以外全域);同目錄 `real-data/` = **11 份真實資料考古**(401 表單結構 / 記帳士「客戶填寫」範本逐欄 / 扣繳與繳款文件 / 銀行與利息 / 影像附件 / Google 試算表 158 個用途字串全量表 等)。每份末段都有「未確認清單」,實作前逐項回查。
> ---
>
> ## 🔴🔴 引用本表的項次之前先讀這段(2026-08-26 線 2 加)
>
> **本表項次(`1`..`33`)與 `docs/phase-1-backlog.md` 的 `#N` 【全部同號、意義無關】。**
>
> ```
> 本表相異項次            1..33 共 33 個(數法:表格首欄純數字去重;
>                         ⚠️ 不去重會得到 86 —— 本檔有多張表, 同一項在不同表各出現一次)
> backlog 條目號          782 個(2026-08-26 當場量:grep -cE '^### #[0-9]+[\.\s]' docs/phase-1-backlog.md)
>                         ⚠️ 同一天另一把尺(不要求號後接 . 或空白)得 785 ——
>                            差在 `#341` 那一族的變體號, 兩把都不算錯
> 兩套【同號】的          33 / 33      只在本表有、backlog 沒有的 ⇒ 0
> ```
> ⇒ backlog 從 `#1` 一路編上去 ⇒ **`#1`-`#33` 這一段【必然】全部被佔滿。這不是巧合,是結構性的。**
>
> ⚠️ **而其中不少 backlog 條目是 ✅ 已完成,主題與本表完全無關**(當天實例三個,皆 ✅):
> ```
> 本表 20 新增編輯商品        backlog #20 = ✅ PaginationParams + Paginated<T>
> 本表 23 匯入供應商 Excel    backlog #23 = ✅ ESLint typescript-aware import resolver
> 本表 26 員工帳號與權限      backlog #26 = ✅ partiallyRefunded transition 評估
> ```
> 🔴 **所以照號去查的人【查到打勾就走人】** —— 而那是最沒有回饋路徑的一種錯:
> 他不會發現自己查錯了,因為他拿到的是一個看起來完整、而且已完成的答案。
>
> ## ⇒ **引用本表一律不要單寫 `#N`。兩種寫法,依情況擇一:**
> ```
> 這一項【已經有線代號】  ⇒ 寫那個代號。例:項次 26 ⇒ 寫 `E8-B`(真登入線;
>                          代號出處 STATUS.md:9「E10 訂單閉環 → E8-B 認證(email+密碼)→ E8-C 2FA」)
> 這一項【沒有線代號】    ⇒ 寫「員工的一天 #N」
> ```
> 📌 **為什麼不統一成一種**:線代號是**可查的專有名詞**(grep `E8-B` 不會撞到別的東西),
> 而「員工的一天 #N」只是**加了前綴的同一個號** —— 它擋得住人眼誤讀,擋不住 grep。
> **有代號就用代號,那是比較強的那一種。**
>
> 📌 **這一段是【入口層】的處置,不是把 33 組引用都改掉** ——
> 改引用是無止境的,而在號的出生地標一次,下一個要引用的人在寫的當下就看得到。
> 🔴 **而選這個做法還有一個理由**:只修其中一個號(例如當天被撞到的 `#26`),
> 會讓它變成 33 個裡**唯一被標註過的那個** ⇒ 下一個人看到其他 32 個沒標,
> **會讀成「那些不需要標」**。局部更正會提高未更正部分的可信度
> (母題見 `docs/patterns/guard-and-instrument-traps.md`「局部更正」那一族)。
>
> ⚠️ **本段自己含 `#20` `#23` `#26` 這些字面 —— 那是【舉例】不是【引用】。**
> 任何掃「誰引用了 `#N`」的尺會命中本段, 掃到請當成 0。
>
> ---

> **訂單域細部對照表**(三平台狀態軸逐值比較)另見 `docs/specs/2026-07-25-order-backend-model-spec.md`。

---

## 0a. 🔴 Sean 2026-07-25/26 拍板:會計線整條延後,回主線「員工上工」

| 題 | 拍板 | 效果 |
|---|---|---|
| **主體身分** | **PCM = 派達有限公司**(統編 `90003020`,公開資訊) | 🔴 **`Desktop/派達資料/` 底下全部是 PCM 自己的資料**。先前誤判「401 那批是別家公司」**作廢**;因該誤判而撤回的第一輪結論(M1-M16、海關代徵 78/79、進口免稅 73、國外勞務 74、二手車 `@`/法院拍定 `#`、零稅率兩子類、免稅⇒403 guard)**全部恢復有效** |
| **Q2 401 誰報** | **A 記帳士報,兩個月一次** | 🔴 後台**不需自產 401**;M1-M16 仍屬 Phase 2 參考、非上線硬需求。目標恆為「產出記帳士要的東西」 |
| **Q3 第一期做什麼** | 🔴 **C 都先不做會計,專心把員工上工那條線做完** | **E13 成本報表 / E14 會計收支帳 / 電子發票串接 整條延後**;本檔 §1.2 系列研究成果**原樣保留**,開工時直接取用、不重查 |
| 承前 N2=B / N4=A / N5 | 兩級角色 / 先做積木 / 訂單可先做但須做到全綠 | 決定下方施工序 |

### 🔴 因 Q3=C 而延後(但研究已完成、不必重做)
`§1.2`(會計三維度)`§1.2a`(codex 十條)`§1.2b`(成本 Q1=B)`§1.2c`(三步記帳卡)`§1.2d`(項目/說明二分)
`§1.2e`(成本五格)`§1.2e-2`(成本公式實證)`§1.2e-3`(匯率快照)+ 兩輪真實資料考古產出。

### 🔴 延後前必須記下的最大缺口:**薪資 domain 完全不存在**
記帳士的主要交付物 D1「扣繳匯總」**主體是薪資扣繳**(25 欄 A–Y:本薪/免稅伙食/應稅伙食/免稅加班/應稅加班/
年終獎金/獎金/勞退公司提繳 6%/員工自付勞健保/補充保費…),PCM 系統與本規格 **零涵蓋**。
⇒ 日後開 E14 時,**薪資是主體、收支帳是配角**,不可再照「先做收支帳」的舊假設排序。

**實錘(留給日後開工當說服材料)**:D1 的 I 欄「所屬月份」只認 `1`–`12` / `去年年終` / `今年年終` 三形態,
無任何驗證擋;**114 年實填資料中確有人只寫「年終」、漏前綴**。此欄改 enum = 後台相對 Excel **最大的實質改善**
(不是介面好看,是它擋得住這個錯)。

### 目前施工序(取代先前所有排序討論)
~~**E11 後台 UI 積木 → E8 員工帳號與權限 → E10 訂單閉環**~~
> 🔴 **本排序已被 Sean 2026-07-27 深夜拍板推翻(Q1=A、二次確認 Q6=A),R13 原地更新**:
> 現行 = **E10 訂單閉環 → E8-B 認證(email+密碼)→ E8-C 2FA**;E11 積木**隨 E10 按需長出**、不獨立成階段。
> E10 總規劃 = `2026-07-28-e10-order-closure-master-plan-v2.md`(該檔 §5.0 為唯一施工序權威)。
(員工能上工後才回頭)E13/E14。

~~理由:①N4=A Sean 拍先做積木 ②E8 是上線硬前置(`staff.ts:3` 名單寫死)③codex 獨立建議同序~~(歷史理由,保留備查)
④積木先以**重構既有 orders/customers 兩頁**驗證可用,再拿去蓋新頁,不做投機抽象。

---

## 0. 🔴 與既有規劃對帳(本檔不另立體系)

**同日稍早已存在** `docs/specs/2026-07-25-site-wide-gap-and-admin-platform-plan.md` **v3**(43KB / 467 行,經 codex 關卡1 兩輪、19 findings 折入),內含 **Sean 已拍板的六題**與 **E0-E9 epic 施工順序**。
🔴 **本檔不推翻它、不重編 epic 編號。** 本檔 = 該 plan 的**訂單域補完 + 員工驗收視角 + 平台研究實證**。

### 0.1 Sean 已拍板、本檔一律沿用(不得重問)

| 題 | 拍板 | 對本檔的約束 |
|---|---|---|
| Q2 圖片存哪 | **A** Supabase Storage(ADR-0004) | §5.2 圖片上傳不再比較 R2 |
| Q4 內容發布模型 | **A** 草稿→預覽→發布→可回上一版;AI 改的一律先當草稿 | §5.5 內容型別沿用此模型 |
| Q5 手動商品邊界 | **B 完整商品**(公開可搜尋/可下單/可管庫存/可下架) | 🔴 北極星第 3 項「接非網站有的商品訂單」**已被此拍板涵蓋且放大**=E6b |
| Q6-1 手動商品價格 | **A** 後台專屬表單 + 每次設價留紀錄 | §5.2 價格走獨立窄路、不進內容 override |
| Q6-2 內容改動頻率 | **B** 一季 1-3 次 = L2 | 內容管理非鐵則 9 強制、可排後 |
| Q1 施工順序 | **A** 先修搜尋(E0) | ~~本檔新增 epic 一律排在 E0 之後~~(🔴 R14:舊排序條款標歷史 —— 現行施工序唯一權威 = E10 v2 §5.0,見本檔頁首更新) |

### 0.2 既有 E0-E9 涵蓋範圍(本檔不重做)

E0 搜尋 / E1 客戶累計消費 / E2 內容發布合約 / E3 圖片上傳 / E4 首頁媒體 / E5 權威鎖地基 / E6a 同步商品後台 / E6b 手動商品 / E7 AI 改商品 / E8 後台強化(dashboard+角色分級+搜尋)/ E9 品牌落地頁。

### 0.3 🔴 本檔補的缺口(既有 plan **沒有**的)

| 新 Epic | 內容 | 為何既有 plan 沒涵蓋 |
|---|---|---|
| **E10 訂單後台補完** | 計數器 / 退款退貨 UI / 快遞單號 / 備註 / 手動建單 / 匯款確認 / 改單 | E0-E9 **完全沒有訂單 epic**;Sean 北極星第 4-13、15、17-19 項全落在此 |
| **E11 後台 UI 積木** | DataTable / Form / Modal / toast / 手機轉卡片 | 既有 plan 未提;現況 0 個共用抽象,不做則每個 epic 各手刻一份 |
| **E12 供應商主檔** | suppliers 表 + 供應商商品價目表 | 既有 plan 未提;PCM 代購本質,Shopify/Medusa 皆無範本、只有 Odoo 有 |
| **E13 成本與報表** | 成本欄 + 管理者專屬可見 + 成本精算報表 + 月報表 | Sean 07-25 第二批(§1.1 #31/#33/#30);🔴 現況 `products_metadata_no_sensitive` CHECK **主動擋 `cost` 寫入**(`20260602135934:88-90`)⇒ 需正規欄位 + 新防護,不可塞 metadata |
| **E14 會計收支帳** | 公司現金/銀行收支 ledger + 科目 + 與訂單收款去重 | Sean 07-25 第二批(§1.2);🔴 全新領域、命中鐵則 12 ①錢、對抗審查不降級 |

**E8「角色分級」的硬前置本檔補明**:🔴 **2026-07-29 更新現況**(原字面「`staff.ts:3` hardcode 名單」已於 E8-A1 過期):`staff.ts:3-4` 逐字「M-4b E8-A1:staff 名單改由資料庫提供,但操作者仍是使用者自行選擇。🔴 這不是登入 / 授權邊界,也沒有驗證『目前使用者是誰』」——**名單已進 DB、但身分仍未驗證**,Sean 北極星「給員工使用」= E8 從「強化」升為**上線硬前置**(此結論不變)。

---

## 1-A. 🔴 現行事實表(**下方 §1 原表為 2026-08-06 快照,已過期、保留供對照 —— 不要動它**)

> **兩次重盤**:2026-08-14(起因 Sean 拍 `Q1`=A)、**2026-08-22**(本次;線 A `-86` 扮員工在
> 真資料鑽機上走一天 + 線 E `-5b` 唯讀重盤那 18 個 ❌)。
> ~~盤點結果:✅ 10 / 🟡 5 / ❌ 18(共 33 項)~~ ⇒ **2026-08-22 現值:**
> ```
> ✅ 10   🟡 8   🔌 2   🧩 5   ❌ 7   🚧 1     共 33
> 🔴 **2026-08-22 07:2x 更新**:~~✅ 11 / 🟡 7~~ ⇒ **`#19` 取消訂單 ✅ 降 🟡**(兩把獨立的尺都是 0,見該格)。
> 📌 **而這是這張表當夜第四次被抓到過期,而【方向不同】**:前三次是低估自己(❌ 其實做得到),**這一次是高估**。
> ⇒ 🔴 **所以 §1-A-0 那句「過期的方向是固定的」要收窄** —— 它對【❌ 那一側】成立(沒有人回來改表),**對 ✅ 那一側不成立**:✅ 會因為【證據被讀成另一個方向】而錯,而那不需要時間經過。
> （🔌 寫好了被旗標關著 / 🧩 備好了沒人接 / 🚧 刻意做了反向的;後兩種 2026-08-22 新增, 見 §1-A-8 §1-A-9）
> 對照 2026-08-14:✅ 10 / 🟡 5 / ❌ 18
> ⇒ 八天之內【六格】換了狀態,而六格全部是同一個方向:往「做得到」那邊。
> ⚠️ 而其中兩格不是「做出來了」,是【一直都在,只是被旗標關著】(#17 / #27)。
> ⚠️ ✅ 裡的 #14 / #15 這次【沒有複查】,沿用 2026-08-14 的值 —— 那兩格的日期欄寫著。
> 🔴 **2026-08-22 夜 新增 `#17b`(非卡退款【作廢】入口),而它【不算進上面這 33】。**
>    刻意的:這次重數的分母就是上面那六個碼,把它算進去下一次重數就不可比。
>    ⇒ **§1-A-3 那張表現在有 3 列,而 `🔌 2` 這個數字仍然指上面這 33 項裡的 `#17` / `#27`。**
>    ⚠️ `#17b` 卡的不是旗標,是**未 apply 的 GRANT** ⇒ 它其實不屬於 🔌,而**這張表沒有它的碼**
>       (`#11` 也是同一類:差一發 apply)。加不加第七碼 = 主視窗的判斷,本次未動。
> ```

---

### 🔴🔴 §1-A-00 **先讀這一句,它決定你跑完之後可以說什麼**(2026-08-28 `-c8` 量到,主視窗裁定寫進表頭)

> **這張表的 34 格裡,只有 18 格是【跑得出來的】。另外 16 格天生不可重跑。**

```
可重跑的 18 格：錨點欄是 grep / git ls-files / 帳本查詢 ⇒ 十分鐘跑得完（2026-08-28 實測）
🔴 不可重跑的 16 格：§1-A-1 ✅ 8 格 + §1-A-2 🟡 8 格
   它們的錨點欄是【真後台的操作紀錄】，逐字例：
     #3「我寫了一筆：摘要由『尚未告知客人 0 筆』→『已告知客人 1 筆』」
     #5「真訂單 PCM-2026-0052 向 RPM Carbon 下訂 1 件」
     #8「建箱並標出貨，包裹 9THVHN · 已出貨 · 新竹物流」
   ⇒ 那不是 grep 跑得出來的，要【一台跑著的後台 + 有人按下去】
```

🔴 **這是【結構天花板】,不是待辦。**
> **一張表要能被【跑】,前提是它每一格的證據都可重跑 —— 而【真的按下去】天生不可重跑。**
> **⇒ 十分鐘只買得到那 18 格,買不到這 16 格。**

⚠️ **而【不要】因此判那 16 格沒價值**:它們記的是**唯一一次有人真的走過那條路**,那比 `grep` 值錢。
**只是它答不了「今天還成不成立」。**

📌 **⇒ 跑完 18 格的人,不要說「重盤完了」。** 正確的說法是:
**「可重跑的那 18 格今天全部重跑過;另外 16 格沿用上一次有人按下去的值,而那是 2026-08-22。」**

🔴 **與這件事同族的兩格(而它們是同一個缺口的兩端)**:
```
#12 手動建單 / #24 匯出訂單商品 ⇒ 【碼齊了、沒人按過】
上面那 16 格                    ⇒ 【有人按過一次，而那一次的證據沒辦法重跑】
```

---

### 🔴 §1-A-0 先讀這段:**這張表的已知失效模式**

**它會過期,而且過期的時候【沒有任何訊號】。**

```
2026-08-14 重盤的起因：#19 取消訂單 / #15 匯款確認收款 / #17 退款操作 都已可用而表內仍標 ❌
2026-08-22 重盤的起因：一模一樣 —— 18 個 ❌ 裡, 8 天內有 5 項已經不是 ❌
⇒ 兩次起因相同 ⇒ 這不是偶發, 是這張表的結構性質:
   🔴 **沒有任何機制會在功能上線的那一天提醒這張表。**
```

🔴 **而過期的方向是【固定的】** —— 永遠是「**低估自己**」(做得到了而表上寫做不到),
**不會反過來**。原因很簡單:功能上線會有人慶祝,功能退場不會有人回來改表。

**⇒ 給讀這張表的人的兩條規矩:**
```
① 看到 ❌ 先問「這一格上次量是什麼時候」, 不要直接排工。
   沒有日期的格 = 【沒人量過】, 不是 ❌。
② 排工之前照那一格的「量法 / 錨點」欄自己跑一次。跑不動 ⇒ 那格的證據已經失效。
```

🔴 **而規矩②【不可以只寫在這裡】** —— 它靠人記得,而這張表過期兩次的成因**它自己寫在上面**:
「沒有任何機制會提醒它」。**再寫一條靠記憶的規矩,就是同一個病的第三次。**

✅ **已搬。落點與最終形狀(2026-08-22 06:5x)**:
```
docs/runbooks/multi-window-command-workflow.md  的 STANDING-ACTIONS 區塊, 第 17 條
（那個區塊由 scripts/window-standing-actions.sh 原樣印出 ⇒ 單一來源, 改一次改所有新窗）
```
🔴 **而它的形狀與我原本想的【相反】** —— 原本要放在派工單(給**派工的人**填),
改成**給收工作的人**:
> **派給你的工作若引用 §1-A 的某一格 —— 先自己跑那一格的「量法」欄,
> 把輸出貼進你的第一則回報,再開工。跑不動、或結果與表上不同 ⇒ 先回報,不要照舊值開工。**

**為什麼換邊**(理由就在那份 runbook 自己的 `§C-0` 裡):
> 🔴 **一份寫對的 runbook,擋不住一份沒有引用它的派工單。**
> (實錘:一個機制在 runbook 裡活了兩個月,而當天**沒有一個窗用它**,因為派工單沒抄它。)

⇒ **派工的人不一定會抄那一行;而收工作的人一定會讀他自己的任務。**
📌 **規則要放在他本來就會經過的地方。**

⚠️ **而它仍然是【約定】不是【機制】** —— 沒有東西會因為誰沒跑而紅。
   它買到的是「**你不會照著一個過期的格子開工**」,不是「**那張表會自己更新**」。

#### 證據等級(**三種狀態不可混寫**;範本取自 `~/pcm-mailbox/A-86-§1A刷新版-扮員工走一天-20260822.md`)

| 標記 | 意思 | 怎麼產生的 |
|---|---|---|
| **做了** | 在真後台上**真的按下去**,並記下畫面上實際的字 | 線 A `-86`,鑽機接**正式站唯讀搬下來的 19 張真訂單** |
| **量了** | curl / grep 計數,**每一發帶正對照**;中文一律 raw + `\uXXXX` 兩種都數 | 線 A `-86` / 線 E `-5b` |
| **沒查** | **一格都沒查** | 🔴 **留白,不得填 ❌ 或 ✅** |

🔴 **「複查仍為 ❌」和「沒查」是兩件事。** 這張表 2026-08-14 那版把兩者混在同一個 ❌ 裡,
**那正是它這次又過期而沒人發現的原因之一。**

#### 🔌 第四種標記(2026-08-22 新增,主視窗批准)

```
🔌 = 程式寫好了、員工按不到 —— 因為被一個【環境變數】關著
```
🔴 **這種格子的下一步跟其他格完全不同:不需要寫任何 code,需要有人去設一個變數。**
而在只有 ✅/❌ 兩格的時候,它們被記成 —— **兩個都錯,而且錯的方向相反**:
```
#17 退款操作   舊記 ✅   （而入口吃 REFUND_UI_ENABLED）
#27 稽核 UI    舊記 ❌   （而整頁都寫好了, 只差 AUDIT_UI_ENABLED）
```

**掃過了,`apps/admin` 就這兩個**(2026-08-22 量;量法與正對照:
`grep -rhoE "process\.env\.[A-Z0-9_]+" apps/admin/src --include=*.ts --include=*.tsx | grep -v '\.test\.' | sort | uniq -c`
⇒ 只有 `REFUND_UI_ENABLED` 35 次 / `AUDIT_UI_ENABLED` 9 次帶 UI 閘;
`export function is*Enabled` 全樹 ⇒ **恰 2 支**;`*flag*.ts` ⇒ **恰 2 支**。
正對照:同一把尺數到 `ADMIN_SESSION_SECRET` 35 次、`VERCEL_ENV` 30 次 ⇒ 尺是活的)。
⚠️ 🔴 **射程**:上面三把尺**全部只掃 `process.env` 的字面**。
**掃不到**:Edge Config / 資料庫欄位 / 建置期常數 / 遠端設定服務關著的旗標。
⇒ 「就這兩個」的正確讀法是「**用 `process.env` 關的就這兩個**」,不是全稱。
⚠️ **storefront 另有四支旗標**(`CHECKOUT_NOTIFICATION_EMAIL_ENABLED` / `CRON_SWEEPER_ENABLED` /
`ANOMALY_ALERT_ENABLED` / `TAPPAY_3DS_ENABLED`)—— **不在本表的 admin 範圍內**,列出來是因為
`#11` 出貨 Email 那一格會有人往那裡找。

---

### §1-A-1 ✅ 現在做得到

| # | 事 | 狀態 | 最後量測 | 證據 / 量法 |
|---|---|---|---|---|
| 2 | 看訂單明細 | ✅ | 2026-08-22 **做了** | 面板打得開、內容齊(`order-detail.tsx`)<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/order-detail.tsx` |
| 3 | 訂單寫備註 | ✅ | 2026-08-22 **做了** | 我寫了一筆:摘要由「尚未告知客人 0 筆」→「**已告知客人 1 筆**」;紀錄含 客人聯繫 · 已告知 · 電話 · 聯絡時間 · 署名。⚠️ 表單自陳「備註為永久紀錄、送出後不可刪除」<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/orders/note-actions.ts` · `components/orders/notes-timeline.tsx`<br>🔴 **2026-08-22 06:1x 追加:連【打錯字怎麼辦】那條路也走完了 —— 而它是今晚設計品質最高的一區。**<br>備註區逐字寫著「**備註為永久紀錄、送出後不可刪除**」,而每一筆旁邊有一顆「更正」。**兩件事怎麼共存,畫面自己講得完整:**<br>① 點更正 ⇒ 表單上方逐字「**正在更正 #1(客人聯繫 · 已告知):<原文>**」+ 一段後果說明:「**更正送出後不可撤回;對更正再做更正,也不會讓最早那筆恢復有效。若誤更正了有效的告知紀錄,唯一的還原方式是重新登記一筆新的「已告知客人」紀錄。**」<br>② 按送出 ⇒ **再跳一個原生 confirm**,把同一段後果**逐字再講一次**(不是「確定嗎?」)。<br>③ 送出之後 ⇒ **舊那筆留著,而且兩邊互相標**:`#2 … 更正 → #1` / `#1 … 已更正(由 #2)`;DB 端是 `order_notes.corrects_note_id`。<br>🔴 **而最準的一格在摘要行**:「**2 筆 · 已告知 1 筆**」—— **兩筆都在,而被更正的那筆不再算進「已告知」。**⇒ 「不可刪除」與「可以更正」不但共存,**連推導出來的數字都是對的**。<br>⚠️ 唯一的皺褶:那顆「更正」是連結、會導到**整頁**(`/orders/[id]?correct=…#note-compose`),從面板點過去會**離開面板**。功能不受影響。 |
| 5 | 標「已向供應商下單」**逐品項** | ✅ | 2026-08-22 **做了** | 真訂單 `PCM-2026-0052` 向 RPM Carbon 下訂 1 件 ⇒「已新增這筆採購。」出貨狀態 未訂貨→**已向廠商訂貨**。🔴 入口要點**兩層**(商品卡 → 琥珀色那條),第一眼看不到<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/item-procurement-form.tsx` · `components/orders/item-procurement-section.tsx` |
| 6 | 記供應商單號 / 預計到貨 | ✅ | 2026-08-22 **做了** | 同上,填了 `A-2026-0822`,存後列在採購表<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/item-procurement-form.tsx` |
| 8 | 輸入快遞單號 | ✅ | 2026-08-22 **做了** | 建箱並標出貨,包裹 `9THVHN` · 已出貨 · 新竹物流。🔴 **我打「ㄉˇ」(兩個注音),系統一句話都沒說就收下**(Sean `Q-C551` 2026-08-16 拍「乙=不限制」,理由見 `tracking-number.ts:23-27` ⇒ **這是刻意的**;開著的題只有「要不要加只警告不擋的」)<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/shipment-dialog.tsx` · `lib/shipping/tracking-number.ts:16-27`(**:23-27 寫著為什麼不擋**) |
| 10 | 列印出貨單 / 揀貨單 | ~~❌~~ → **✅** | 2026-08-22 **做了+量了**<br>🔴 **環境:本機鑽機**(**推的,不是量到的** —— 原量測沒有標;依 §1-A-0 證據等級表「**做了**」一律 = 線 A `-86`、**鑽機接正式站唯讀搬下來的 19 張真訂單**。**這格的數字要有一台跑著的 server 才量得到,而那台不是正式站。**) | ~~「整個能力不存在」~~ **已過期**。`/print/orders/[id]/picking` 200 · 45,201 B(坐墊中心蓋=1 / YAR1-25-G-F=1 / 負對照=0);`/print/orders/[id]/shipping/[shipmentId]` 200 · 50,104 B。🔴🔴 **出貨明細單上沒有貨運單號 —— 這是【2026-08-17 已落地的拍板】,不是缺口。不要把它排進工作。**(`單號`/`追蹤` 皆 0,同頁 `新竹物流`=1 ⇒ 尺是活的)<br>~~⇒ 併入 `Q-C5`~~ **這句話讀起來像待辦,而它不是。** 2026-08-22 夜 更正:**已由 `Q-C5`=丙 拍板拿掉** —— Sean 逐字 `q3: 丙` ⇒ **出貨明細單不印追蹤碼欄位,追蹤碼只走 LINE／Email 給客人。**<br>本窗逐條自己開檔核過(不是照抄):`apps/admin/src/components/print/shipping-doc.tsx:516-526` 的註解逐字寫著這件事(含作廢清單 `docs/specs/2026-08-17-qc5-tracking-off-paper-decommission-list.md` §1);另有**四份互相獨立**的文件同載:`docs/specs/2026-08-16-print-docs-design-brief.md:78` `:97` · `docs/phase-1-backlog.md:17228` `:18137` · `docs/security/2026-08-17-pre-launch-must-close-checklist.md:348`(正對照 同一把尺量 `Q-C551` ⇒ **10 支檔命中** ⇒ 尺是活的)。<br>🔴 **而「丙」沒有說「不用追蹤碼」,說的是「不走這張紙」** ⇒ 這三件**仍然成立、不准順手拆**:① `mark_shipped` 的「非 `other` 且追蹤碼空白 ⇒ 拒絕標記出貨」② `Q-C551` 乙 的入口格式守門 ③ `trackingDisplay` 的三種 `null` 語意(留給 `Q-C9` 的出貨通知信)。<br>⚠️ 同一區的**貨運商**與**日期**留著(`Q-C19`=乙 逐字「只拿掉追蹤碼那列」)<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`app/print/orders/[id]/picking/page.tsx` · `app/print/orders/[id]/shipping/[shipmentId]/page.tsx` |
| 14 | 刷卡收款 | ✅ | ~~2026-08-14~~ **沒有複查** | TapPay 3DS 生產路徑<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):~~2026-08-14 的錨點:TapPay 3DS 生產路徑~~ **本次未複查、無新錨點** |
| 16 | 今日對帳 | ~~❌~~ → **✅** | 2026-08-22 **做了**<br>🔴 **環境:本機鑽機**(**推的,不是量到的** —— 原量測沒有標;依 §1-A-0 證據等級表「**做了**」一律 = 線 A `-86`、**鑽機接正式站唯讀搬下來的 19 張真訂單**。**這格的數字要有一台跑著的 server 才量得到,而那台不是正式站。**)<br>⚠️ **「今日新單 7 筆」還多帶一個【時點】** —— 換一天重跑就不是 7。 | ~~「首頁仍是骨架 `app/page.tsx:5-21`」~~ **已過期**(該檔現 108 行)。畫面三格:今日實收(淨)/ 今日新單 7 筆 / 待處理退款異常 2 筆。🔴 **只有退款異常那格是連結**;「今日新單」點不下去,而日期篩選**沒有「今天」** ⇒ 要看那 7 張得選「自訂」打兩次日期<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`app/page.tsx` · `components/dashboard/today-summary.tsx` · `lib/dashboard/today-read.ts` |

### §1-A-2 🟡 部分做得到

| # | 事 | 缺的那半 | 最後量測 | 證據 |
|---|---|---|---|---|
| 1 | 看今天要處理什麼 | 仍**無可存的自訂檢視** | 2026-08-22 **做了** | ~~「只有寫死兩顆 chip」~~ 今天是**三顆**(全部 / 待收款·待訂貨 / 未到貨)。🔴 另有一個更急的問題,見 §1-A-5-①<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/order-filter-chips.tsx` · `lib/orders/order-list-view.ts` |
| 9 | 單號追蹤 | **只有文字,查不了貨態** | 2026-08-22 **量了** | 訂單明細頁 `查貨態`/`貨態`/`追蹤`/`查詢貨況` 皆 **0**;`hct.com.tw`/`t-cat`/`sf-express` 皆 **0**。正對照同頁 `新竹物流`=4 / `已出貨`=6 ⇒ 尺是活的<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/shipment-section.tsx`<br>🔴 **2026-08-22 追加:它離「有一點工」很遠 —— 那是一條【從來沒有開始過的外部整合】,而資料層早就為它塑形好了。**<br>`shipments` 表有三欄專為新竹物流準備:`hct_status` / `hct_request_id` / `hct_raw_response`,外加**三道 CHECK**(`hct_status ∈ draft\|submitted\|failed`、`submitted` 必須帶非空 `hct_request_id`、非 hct 貨運商只能是 `draft`)。<br>⇒ **而沒有任何東西在填它們**:鑽機上 6 張包裹 **全部 `draft`、`hct_request_id` 全空**;全樹**零檔**命中 `hct.com.tw` / `HCT_API` / `hctApi`(正對照:同一把尺量 `tappay` ⇒ **44 檔**);把 `hct_status` 寫成 `submitted` 的程式碼 **一行都沒有**。<br>📌 **與 `#22`、`capture-state-view`、`#12` 的 `order_source` 三個手動值同族,而這是第四個標本**—— **資料層備好、守門也寫好,而沒有人接**。四個標本停在不同深度:`#22` 有引擎沒入口 / `capture-state` 有函式沒消費端 / `#12` 有 CHECK 沒 writer / **本格有欄位有守門沒整合**。<br>⚠️ 外部 API 本身**已查證可用**(memory `reference_hct-logistics-api`:圖檔一次 5 筆上限、可叫逆物流、同日編號不可重複)⇒ **卡的不是「查得到嗎」,是「沒有人做過」**。<br>🔴🔴 **2026-08-22 07:0x,線 `-3c` 從【畫面】那一側補上另一半 —— 而兩半合起來比任何一半嚴重:**<br>它在真訂單 `PCM-2026-0104`(**我一個字都沒動過的那張**)上量:畫面逐字「已出貨 包裹 1 箱 PDQYJV … **新竹物流 · 單號 123**」,而掃「查不 / 無法查詢 / 尚未串接 / 目前無法」⇒ **零命中**;掃「貨態 / 追蹤 / 查詢貨況」⇒ **零命中**。<br>⇒ **那個單號是純文字:不是連結、沒有按鈕、沒有任何一句話。**<br>🔴 **所以它不只是安靜 —— 它【看起來像做好了】。員工會合理地以為那個單號可以追。**<br>📌 ⇒ **兩半要一起引**:我這半是「**資料層備好、守門也寫好,而沒有人接**」;它那半是「**而畫面看起來像接好了**」。<br>🔴 **合起來那句才是重點**:這是四個標本裡**最深**的一個,而**最深的那個連【畫面】都最像做完了** ——**它的準備工作替它說謊,而它的畫面也替它說謊。**<br>⚠️ 而 `-3c` 自己標了邊界(**原樣帶著,不要幫它升級**):那一頁有 **10 個折疊區、9 個關著,而它沒有打開**;它判那 9 個都不是出貨區 ⇒ **而它明寫「這是推論不是量測」**。 |
| 13 | 改訂單內容 | 🔴 **只能改「出貨方式」+ 發票四格**;品項 / 數量 / 單價 / 地址 / 客人**都不能改** | 2026-08-22 **做了** | ~~❌~~ 已過期。我在真訂單 `H7CPXQ` 上開了發票(已開立 / AB12345678 / NT$1)⇒「已儲存變更。」表單帶 `version` ⇒ **有樂觀鎖**;存完帶回現值。<br>🔴 **「改訂單內容」四個字讀起來比它做得到的大很多 —— 一個名字比它的能力大,會讓盤點的人把它記成做完了。**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/order-edit-form.tsx:45` `action={updateOrderWorkflowAction}`<br>🔴🔴 **2026-08-22 06:5x 再更正一次 —— 這是那張表【同一夜第三次】被抓到過期,而三次方向全部相同(低估自己)。**<br>`-4a` 先量到「**改品項金額**」那條**已經接線**(`item-amount-form.tsx` / `amount-actions.ts` / `order-detail-items-table.tsx` 真的 render),而它**只驗到接線、沒真的改一筆**,並標了「旁邊有 `blockedReason`,可能有條件擋著、它沒查」。<br>✅ **那兩格我補上了,而且是【做了】等級:**<br>**① 兩個世界都量過**(同一把尺,帶正對照 `付款狀態`=2):`7F4TF8`(未付款·零收款)⇒「改金額」**可按**、「無法改金額」**0**;`PCM-2026-0052`(已收足)⇒「**無法改金額**」**1** ⇒ **那道閘真的在擋,而它擋的是【已收款】。**<br>**② 我真的改了一筆**:`7F4TF8` 的單價 **4850 → 4500** ⇒ DB 實查 `line_total` 4500、**訂單總額自己重算 4950 → 4600**(運費 100 保留)、`version` 樂觀鎖在、稽核留下 **`order.item.amount.update`**。<br>⇒ **所以「改訂單內容」現在是:出貨方式 ✅ / 發票四格 ✅ / 品項單價 ✅(未收款時)—— 而【品項本身、數量、收件地址、客人】仍然不能改。**<br>📌 仍判 🟡,而**缺的那半縮小了一大格**;`#22 改價格` 那一格講的是**商品定價**,與本格的**訂單單價**是兩件事,不要合併。 |
| 20 | 新增編輯商品 | **看得到、改不了**;而且**沒有「新增」route** | 2026-08-22 **量了** | ~~「`apps/admin` 底下 products route 零命中」~~ **已過期**:`/products`、`/products/[id]` 都在。頁面**自陳**逐字「這一頁目前只能改上架狀態,其餘欄位仍不能修改」;列表頁自陳「上架/下架請點進商品明細頁,其餘欄位目前不能修改」。`新增商品`/`建立商品` 字面 **0**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`app/products/page.tsx` · `app/products/[id]/page.tsx`<br>🔴 **2026-08-22 補**:`/products/[id]` 上**唯一能按的就是上下架**(見 `#22`),而列表頁與明細頁**都自己講了**這件事 ⇒ 這一格的「缺的那半」是**畫面自陳的**,不是我推的。<br> 🔴🔴 **2026-08-28 `-c8` 換量法,而【為什麼換】寫在這裡不靜靜換掉**:~~原量法 `grep -rln "'use server'" products` ⇒ 0~~ —— **它在 2026-08-22 是對的,而今天回 `2`。**那 2 支是 `lib/products/product-listing-actions.ts` / `product-listing-form.ts`,檔頭逐字「後台**上下架** server action 的純函式核心」⇒ **那是 `#22` 那一格的東西**(而 `#22` 已經寫著「上下架可以了」)。⇒ **照原量法重跑的人會判「`#20` 做了」。而它沒有。**<br> ✅ **新量法(機器跑得出、與 `#22` 分得開)**:`git ls-files 'apps/admin/src/app/products/**' | grep -cE 'new|create'` ⇒ **0**(`-c8` 2026-08-28 實跑)。<br> 📌 **而這一格是這張表【量法欄】這個設計的第一個已知失效模式,判別句**:**一條量法在【它想量的那件事】沒動、而【隔壁那件事】做了的時候,會翻面。**它量的是「products 底下有沒有寫入」,而它想答的是「**能不能新增商品**」——**那兩個問題在 08-22 剛好同答案,今天不是了。**🔴 **⇒ 那不是量法寫錯,是它【借用了一個當時剛好成立的等價關係】。而那種借用不會留下任何訊號。** |
| 22 | 改價格 / 上下架 | **上下架可以了;改價格仍不行** | 2026-08-22 **量了** | ~~「引擎已寫好、**零呼叫端**」~~ 半句已過期:`/products/[id]` 有真表單(上架/下架 + 變更原因 + 「變更會寫入稽核紀錄」)。改價格**沒有入口**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`app/products/[id]/page.tsx` · `packages/ports/src/IProductRepository.ts`<br>🔴 **2026-08-22 補【做了】**:我在真商品上按了「下架這件商品」(變更原因「線 A 鑑機測試,稍後會復原」)⇒ 上架狀態 **上架中 → 已下架**、「誰設定的」**自動 → 手動**、按鈕翻成「重新上架」、說明句也跟著翻(「上架之後客人立刻看得到這件商品」)。**按「重新上架」已復原**(DB 實查 `delisted_at IS NULL` ⇒ `t`)。<br>✅ **兩次都進了稽核**(`product.listing.change` ×2)⇒ 那句「變更會寫入稽核紀錄」不是空話。<br>⇒ **上下架是【做了】等級;改價格仍然沒有入口。** |
| 25 | 改客人資料 | Email 不能改(**頁面自己講了原因**);地址 / 愛車**沒查** | 2026-08-22 **量了** | ~~「姓名/電話/Email/生日 全部唯讀」~~ 已過期:**姓名(必填)/ 電話 / 生日 + 「儲存基本資料」都在**。Email 逐字「是登入帳號,不能在這裡改;要改請另開片處理」。<br>⚠️ **地址 / 愛車判不了** —— 鑽機那兩張表是**空的**,畫面印「目前沒有收件地址」= **沒資料,不是不能改**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/customers/customer-detail.tsx` · `components/customers/customer-detail-sections.tsx`<br>🔴 **2026-08-22 補【做了】**:我改了一位真客人的電話並按「儲存基本資料」⇒「已儲存變更。」上方唯讀區當場變成新號碼,表單也帶回新值。<br>⚠️ **儲值金「加值 / 扣款」與「變更等級」我【沒有按】** —— 前者動錢,後者動權限。**標沒走。**<br>⚠️ 地址 / 愛車仍判不了(鑽機那兩張表是空的 ⇒ 畫面印「目前沒有收件地址」= **沒資料,不是不能改**)。 |
| 26 | 員工帳號與權限 | 「操作者是誰」仍是**自選** | ~~2026-08-14~~ 2026-08-22 **做了(側面)** | 我今晚三個動作在稽核頁上的署名全是「**員工 1(占位)**」,而那是**下拉選單選出來的身分**。首頁自陳逐字「🔴 這個身分是你自己選的、系統並未驗證」。⇒ **一筆刪不掉的永久紀錄,掛在一個沒被驗證過的名字上**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/session/actor.ts` · `lib/session/session.ts:12`<br>🔴 **2026-08-22 補一格更硬的**:`session.ts:32-34` 寫著政策「**`fallback` 一律不得寫入;`bootstrap` 僅限 2FA 綁定端點白名單**」,而**今天沒有任何一行程式在執行它** —— `lib/session/authorize.ts:25-42` 只檢查 ①簽章有效 ②Origin 允許 ③`getSessionActor()` 非 null,**一次都沒有讀 `sub.kind` 或 `amr`**;而 payload 裡**根本沒有 `sub` 這個欄**(我今晚那張票的 payload 逐字 `{v, sid, iat, exp, auth_time, amr:['bootstrap']}`)。<br>⇒ **實證**:我用那張 `amr=bootstrap` 的票,今晚在鑽機上成功寫了**四筆**(建立採購 / 登錄到貨 / 新增備註 / 登錄收款),稽核紀錄都留下了。**那條政策要判的欄位不存在,所以它不是被繞過,是還沒有被接上。**<br>📌 **這不是缺陷,是 B5 那一片的內容**(該註解自己標著「B5 §寫入閘」)—— 寫在這裡是因為**「政策寫在註解裡」與「政策在跑」在 grep 上長得一樣**。 |
| 7 | 缺貨要等 | ✅ **記得下來** / 🔴 **提醒不了他** | 2026-08-22 **做了** | 我在真訂單 `H7CPXQ` 上跑了一次:向 Webike JP 下訂 1 件、回覆狀態選「缺貨」、異常原因打「廠商回覆缺貨,預計 9 月中旬才進貨」。<br>✅ **存得下來、看得到**:商品卡的**收合標題列**就掛著「**供應商回報缺貨**」,不必展開;採購表列出 缺貨 + 那句原因 + 時間。<br>✅ 內部原因**沒有外洩到列表**(欄位自陳「內部;告知客人另走備註」)。<br>🔴 **而關掉這張單之後,沒有任何東西會再提醒他**:訂單列表上「缺貨」兩個字 **0 次**(該列只顯示「已收已定」,與一張健康的單長得一模一樣);出貨狀態篩選只有 未訂貨／已向廠商訂貨／已到貨／已出貨 —— **沒有「缺貨」這一格**;總覽也沒有。<br>⇒ **兩問的第二問答不出來**:他看到了,然後**只能靠自己記得**。而「缺貨要等」正是**唯一一種必須等很久、而且要主動回頭追**的狀態。<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/orders/procurement-form.ts:41-47`(五種回覆狀態)· `components/orders/item-procurement-section.tsx` |
| 15 | 匯款確認收款 | ✅ **登得進去** / 🔴 **登完那張單會不見** | 2026-08-22 **做了** | ~~2026-08-14 只看到入口、沒按~~ **已補做**:在真訂單 `2SQH2P`(從 2026-08-09 卡到現在、8 次刷卡嘗試)上照 Sean 的真實情境走 —— **客人刷卡一直失敗 ⇒ 改用匯款 ⇒ 員工登錄收款**。<br>✅ 銀行匯款 / 1500 / 入帳日 / 單號 / 備註 ⇒「已登錄這筆收款。」<br>✅ **送出鈕預設 `disabled`**,勾了「我已看過上方的收款明細」才 enable(兩個世界都量過)⇒ 防重複入帳的閘在畫面上。<br>🔴 **而送出之後,同一時刻四個畫面互相打架**:總覽「今日實收(淨) NT$ 1,500」/ 面板「1,500 / 1,500 · 尾款 0」/ 面板「付款狀態 **待付款**」/ 訂單列表 **那張單根本不出現**(預設清單 `2SQH2P` ⇒ **0** 命中,正對照 H7CPXQ ⇒ 6;勾「顯示刷卡未付款」⇒ 6 命中)。<br>🔴 **根因不是 `payment_status` 沒更新** —— 開 migration 查過,那是**刻意的**(全 repo 只有 `20260611120000_..._confirm_payment_rpc.sql:178` 會寫 `paid`,而那是刷卡那條路)。**壞的是預設隱藏那道篩選問錯了欄位** ⇒ 已立 **`#841`**(🔴 高)。<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/payment-record-form.tsx:44-60` · `lib/orders/order-list-view.ts`(預設隱藏述詞)· `supabase/migrations/20260810100000_m4b_e10_op1_order_payments_m.sql`(帳本語意) |<br>🔴🔴 **2026-08-25 更正(線 3 重跑, 主視窗收):態欄那句紅字【已經是幽靈, 而它正在教一個錯誤操作】。**`20260823030000_m4b_841_order_paid_total_view.sql` 存在且 `APPLIED.tsv` ⇒ **1(已 apply)**;而 `payment-list-wiring.test.tsx` 裡逐字寫著:「`#841` 治本(2026-08-23)之後, ①③ 兩格的真值【換了】⇒ **淨額 > 0 的單不再被藏** ⇒ 對它講「預設不會出現在訂單列表」變成一句假話, **而且會教員工去勾一個不必勾的勾**。」<br>🔴 **這一格的代價比一般幽靈高一階**:一般幽靈讓人重做一件做完的事;**這一句讓人去教員工勾一個不必勾的勾** —— 一句過期的警告, 生出一個新的錯誤操作, 而受害者是**員工**不是我們。<br>⚠️ **不得整句劃掉** —— 殘留仍為真:**收了又沖(淨額 0)的單仍然被藏**, 守門還在(同檔 `describe('#841 甲:…')`)。**「那張單會不見」對淨額 0 成立、對淨額 > 0 不成立 ⇒ 一句話兩個世界, 要拆開讀。**<br>📌 而本列在「✅ 現在做得到」那張表裡, 態欄卻寫著 🔴 —— 線 3 量到**同表 11 列裡有 3 列這樣**(另兩列 `#7` / `#19`)。**「它在哪一張表裡」是機器讀得到的宣稱, 「格子裡寫什麼」不是** ⇒ 任何人數「我們現在能做幾件」都會數到 11, 而其中 3 件它自己說做不完整。
| 19 | 取消訂單 | 🟡 | 2026-08-22 **做了**<br>🔴 **環境:本機鑽機**(**推的,不是量到的** —— 原量測沒有標;依 §1-A-0 證據等級表「**做了**」一律 = 線 A `-86`、**鑽機接正式站唯讀搬下來的 19 張真訂單**。**這格的數字要有一台跑著的 server 才量得到,而那台不是正式站。**)<br>🔴 **而 07:2x 降級那兩把尺(尺 A / 尺 B)= 正式站** —— 線2 2026-08-22 夜 複量**正式站**得到逐格相同的數字(`orders` 19 / `cancelled_at` 7 / 全部 `payment_expired` / `order_cancellations` 0 列)⇒ 反推線 C 那兩發也是正式站。⚠️ **這是線2 的量測,本窗未複量。** | 刷卡單上它**直接擋住,連按鈕都不給**(該區 `<button>` 數 = **0**),並講原因與下一步。**本次走完全程最好的一格**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/orders/cancel-actions.ts` · `components/orders/cancel-review-section.tsx`<br>🔴🔴 **2026-08-22 07:2x 降級:✅ → 🟡。兩把獨立的尺都是 0。**<br>`-4a` 先指出這一格的證據是【負向驗收】——「直接擋住,連按鈕都不給(`<button>` 數 = 0)」**證明的是守門會擋,不是任何一張單真的被取消過。**<br>線 C 兩發唯讀查證(各帶正對照):<br>**尺 A** `orders` ⇒ 總數 **19**(正對照,尺是活的)/ `cancelled_at` 非 null **7** / 其中 `cancelled_reason='payment_expired'`(排程寫的)**7** / 🔴 **不是 `payment_expired` 的 ⇒ 0**;整個欄位出現過的值只有 `payment_expired` 一種;最新一筆 `2026-08-19 17:00:00.022842+00` —— **整點,排程的形狀**。<br>**尺 B**(不靠單一來源)`order_cancellations` ⇒ **0 列**、`distinct actor` **0**、`order_cancellation_items` **0**;控制組 `orders` **19** ⇒ 尺是活的。<br>⇒ **兩把獨立的尺、不同的表、都說 0 ⇒ 那顆按鈕從來沒有成功取消過任何一張單。**<br>📌 **而最該記的是**:原始證據那句話「連按鈕都不給」**本身就寫著它沒有取消成功過** —— **只是沒有人把它讀成那個意思。**<br>⚠️ 仍未證偽的那一半:**這不代表它壞掉** —— 也可能是【還沒有人需要取消一張非過期的單】。⇒ **要分開「它做不到」與「它沒被走過」,需要有人在真後台上取消一張未過期的單。本次沒有做。** |

> 🔴🔴 **2026-08-25 從 §1-A-1「✅ 現在做得到」搬進來三列(內容逐字未改, 只搬位置)。**
> 搬的理由**不是新的判斷** —— 是**讓機器讀得到的宣稱(它坐在哪張表)對上人寫的宣稱(標記欄)**:
> 這三列的標記欄**本來就自己寫著不是純 ✅**(`✅ … / 🔴 …` 兩半、或一個 🟡), 而它們坐在 ✅ 那張表裡。
> 🔴 **「它在哪一張表裡」是機器讀得到的宣稱, 「格子裡寫什麼」不是** ⇒ 任何人數「我們現在能做幾件」都會數到 **11**, 而其中 **3 件它自己說做不完整**。
> ⇒ §1-A-1 現在是 **8 件**。出處=線 3 2026-08-25 重跑(分母 11 / 命中 3), 守門 `scripts/board-state-consistency.py` ③ 會擋。
> ⚠️ **射程**:`#19` 線 3 明說「我只量到它坐錯了表, **沒查它現在到底是 ✅ 還是 🟡**」⇒ 本次搬家**沒有**判定它的真值,
>    只是把它放到與它自己的標記一致的位置。要定它的真值 ⇒ **在真後台按下去**, 而那一格今夜沒有人做。

### §1-A-3 🔌 寫好了、被旗標關著(2026-08-22 新增)

| # | 事 | 旗標 | 最後量測 | 證據 |
|---|---|---|---|---|
| 17 | 退款操作 | `REFUND_UI_ENABLED` | 2026-08-22 **量了** | ~~✅~~ **標記錯了**:入口吃旗標(`lib/payment/refund-ui-flag.ts:21-23`、`order-detail-route.tsx:301`、server 端 `refund-actions.ts:124`)。旗標關著時該區**只剩帳本**,四張真單上「全額退款 / 部分退款 / 發起退款」字面皆 **0**。<br>✅🔴 **2026-08-28 行為面量測取代了下面那句推論 —— 而它證實「正式站是開的」**:Sean 本人打開正式後台,對「隨便一張已付款的訂單 → 收款那一區」逐字回「**已開**」。⚠️ **證據等級分清楚**:那是**他的判斷句**,不是我請他貼的**畫面字面**(我問的是「看到『全額退款/部分退款』還是『這個環境沒有開放…』」)⇒ **「已上線」成立,而「畫面上印的是哪一句」未取得。**⚠️ **值仍未確認**:`vercel env ls` ⇒ `REFUND_UI_ENABLED` **`Encrypted`**(Preview, Production;15d ago)⇒ **沒有人讀到過那個值** ⇒ 🔴 **落檔一律寫「行為面量到是開的」,不得寫「值 = 1」。**🔴 **redeploy 那一格不必再問** —— 畫面已經在跑,那就是最終證據。<br>~~⚠️ **正式站應該是開的**,而**這句是推的不是量的**~~(依據:`MAIN-113 §3` Sean 照操作單走完 ①②③、第④步才在 TapPay 失敗)。<br>🔴 **我永久標「沒走」不去按** —— 就算鑽機沒接外網,「打不出去」與「沒有發生」是兩件事<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/payment/refund-ui-flag.ts:21-23` · `components/orders/order-detail-route.tsx:301` · `lib/payment/refund-actions.ts:124` · `components/orders/refund-section.tsx`<br>🔴 **2026-08-22 07:3x,線 `-3c` 從畫面那一側更正我一句**:我寫「旗標關著時該區**只剩帳本**」——**不精確。** 實際是「**只剩帳本 + 一句解釋為什麼**」:<br>畫面逐字「**這個環境沒有開放「線上退款(TapPay)」的操作入口 —— 這是系統設定,不是這張單的問題。**」(那句是本片 `#841` 甲/乙-2 那一輪加的)<br>⇒ **那句解釋才是它做得好的地方,而我自己把它寫漏了。**<br>⚠️ **而它差點把這一格寫成【安靜】**:它的自檢字集是 `目前不能\|尚未\|還不能\|無法\|未開放\|停用\|關閉`,而畫面寫的是「**沒有開放**」⇒ **尺回 false**。**救它的是它同時 dump 了全文,不是它更小心。**<br>📌 **與本表 `#11` 那格標的「我的字集裡沒有 `email`/`mail`,而這格的主題就是 email」是同一個病的第二個獨立實例。** |
| 17b | ⚠️ **`#17b` 不算進 33 —— 任何「共幾格」的數字,先講清楚含不含它。**(2026-08-28 標死:它已經害過兩個數字 —— 主視窗的 `10 vs 11`、`-c8` 的 `26 vs 24`。📌 **一個刻意的例外,若不寫在它自己身上,每個數它的人都要重新踩一次。**)<br>非卡退款【作廢】入口 | 🔴 **不吃旗標** —— 卡在**未 apply 的 GRANT** `20260822120000` | 2026-08-22 夜 **量了**(環境:**repo + `supabase/APPLIED.tsv`,不需要跑起來**) | 🔴 **本表原本沒有這一格。** `b39fe584`(2026-08-22 **15:42**)加了它,而 `#17` 最後量測是 **07:31** ⇒ **早了八小時,表收不到。**<br>✅ **UI / action / repository / RPC 全部寫好且真的接線**:`components/orders/manual-refund-ledger-section.tsx:116` 渲染 `ManualRefundVoidButton`。<br>⚠️ 🔴 **它不在 `REFUND_UI_ENABLED` 底下,是獨立區塊** —— `components/orders/order-detail.tsx:609` 註解逐字「非卡退款登記列表(唯讀、**不吃旗標**,同上一塊的立場)」;同檔 `refundEnabled` 只包 `shouldShowRefundEntry(...) && <RefundSection>`(:630-641)⇒ **旗標開不開,這顆鈕都在。**<br>🔴 **卡在哪**:`grep -n 20260822120000 supabase/APPLIED.tsv` ⇒ **0 命中**(正對照 同一把尺量 `20260821113000` ⇒ **1**;負對照 編造 id `29999999999999` ⇒ **0** ⇒ 尺是活的、帳本共 280 列)⇒ `admin_void_manual_refund` 的 EXECUTE 沒開給 `service_role`。該 migration 檔頭自陳逐字:「**本檔不建立、不修改任何函式或表 —— 純授權**」、「③ 在本檔 apply 之前是 **false**」。<br>✅ **按下去是 fail-closed,不是假成功**:`lib/payment/manual-refund-void-repository.ts:39` 把 `42501` 歸成 `bug`(該行註解逐字「ACL 被撤 ⇒ 這片的 GRANT migration 被 rollback 了」);`lib/payment/manual-refund-void-action-state.ts:53` 回「系統呼叫異常,請重新整理頁面確認這筆是否已作廢;不要重複送出,並通知系統維護。」<br>🔴 **它與 `#11` 同屬一個這張表【沒有】的類別:程式寫完了、Sean 也沒有要決定什麼、而它就是不會動 —— 差一發 apply。** 六個狀態碼分不出這一類;**是否加第七碼是主視窗的判斷,本次刻意不動表結構**(這次重數的分母就是那六個碼,改了下次不可比)。<br>⚠️ **編號用 `17b` 是刻意的** —— 它**不改變「33 項」這個分母**,只是掛在 `#17` 底下的一格。<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`components/orders/manual-refund-void-button.tsx` · `components/orders/manual-refund-ledger-section.tsx:116` · `lib/payment/manual-refund-void-repository.ts:39` · `supabase/migrations/20260822120000_m4b_e10_d3c_grant_void_manual_refund.sql`(**未 apply**) |
| 27 | 稽核 UI | `AUDIT_UI_ENABLED` | 2026-08-22 **做了**<br>🔴 **環境:本機鑽機**(**推的,不是量到的** —— 原量測沒有標;依 §1-A-0 證據等級表「**做了**」一律 = 線 A `-86`、**鑽機接正式站唯讀搬下來的 19 張真訂單**。**這格的數字要有一台跑著的 server 才量得到,而那台不是正式站。**)<br>✅🔴🔴 **2026-08-28 行為面量測:Sean 本人打開正式後台,逐字回「有操作記錄」並附一張截圖** —— 「操作紀錄」整頁、側欄有那一項、**點得進去、有真資料**(2026-08-19 ~ 08-24,退款 / 發起退款 / 新增備註等 16 列以上)。⇒ 🔴 **`AUDIT_UI_ENABLED` 在正式站【是開的】,而本格從 08-22 起一直標著「被旗標關著」。**📌 **成因就寫在上面那句【刻意不刪】的話裡**:那個 🔌 量的是**本機鑽機上沒設那顆旗標**,不是正式站關著 —— **而在表上,兩句話長得一模一樣。**⚠️ **值仍未確認**(`vercel env ls` ⇒ `Encrypted`,Production,13d ago)⇒ **寫「行為面量到是開的」,不要寫「值 = 1」。** | ~~「有寫入介面、**連讀取 API 都沒有**」⇒ **本表錯得最遠的一格**~~。不設 ⇒ `/settings/audit` **404**、側欄連那項都不長;設 `=1` 重開 ⇒ **200 · 156,221 B**,頁面逐字「後台每一筆異動的紀錄:誰、什麼時候、對哪張單、做了什麼」,而且是**欄位級的原本→改成**。<br>🔴 **這是整張表裡唯一一個「點頭當天就能用」的功能。**<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/audit/audit-ui-flag.ts:29-31` · `app/settings/audit/page.tsx:73` · `components/audit/audit-log-table.tsx` |

### §1-A-4 ❌ 做不到(**今天量過,仍是做不到**)

> 🔴 **2026-08-22 追加:這 13 格【不是同一件事】** —— 逐格分類見 **§1-A-9**。
> 標記:🧩 = 備好了沒人接(**5 個**)/ ❌ = 真的整條沒做(**7 個**)/ 🚧 = 刻意做了反向的(**1 個**)。

> 量法一律 `git grep -ln`(不接管線、直接讀 rc);負向對照 `orderDigest|order-detail` ⇒ rc=0 / 78 檔 ⇒ 尺是活的。
> 判 ❌ 而依據是 **route 清單**的那幾項,已在該格註明 —— **grep pattern 太鬆的命中不構成證據**。

| # | 事 | 最後量測 | 量法 / rc |
|---|---|---|---|
| 4 | 🧩 批次標商品進度 | 2026-08-22 **做了**(更正同日的 grep 判定) | 🔴 **「畫面上仍沒有批次入口」這句不精確 —— 有批次畫面,只是它做的是【另一個步驟】。**<br>實走:訂單列表勾一張單 ⇒ **13 張立刻變灰**(只留同一位客人的),出現「出貨(N 單)」。按下去是一個**跨單、逐品項**的「建立包裹」對話框,每一項旁邊有 **「貨到了」**。<br>⇒ ✅ **批次出貨**有、✅ **批次登錄到貨**有(4 個品項 3 顆鈕);❌ **批次向供應商下訂沒有**。<br>🔴 而它 fail-closed 得很漂亮:沒有採購列的那一項**沒有那顆鈕**;按下去的表單逐字「**這個品項目前沒有可以登錄到貨的採購(可能還沒下單,或原本那筆已經作廢)。請先到訂單頁的「採購」區塊補上要向誰訂(或選「店內現貨」),再回來登錄。**」;而兩顆送出鈕 `disabled=true`,畫面另有一句「這些品項現在都不能出…登記完這裡就會亮起來」。<br>⇒ **仍判 ❌**,因為「標商品進度」的第一步(訂貨)沒有批次;但**下一個人不要再去找「批次入口在哪」——它在,只是在出貨那條路上**。<br>📌 `lib/orders/procurement-batch-policy.ts` 零呼叫端**仍然成立**(那是 A9h-2 的決策層,與上面這個畫面不是同一條)<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`lib/orders/procurement-batch-policy.ts`(零呼叫端那支)· `components/orders/shipment-dialog.tsx`(批次那個畫面) |
| 11 | 🧩 出貨 Email 通知 | 2026-08-22 **做了+量了**<br>🔴🔴 **2026-08-25 線 3 覆判(`pcm-website-v2-f5`;原句一個字未刪,照本 repo 規矩只加註記)。下方那句「環境:原量測未標,無法確認」**仍然為真**,而【**替它補上環境並不會讓這個 `0` 變成證據**】—— 它對它想答的那個問題是一格【恆綠】。**<br>**量法(2026-08-25 當場跑,可重跑)**:`git grep -n "enqueueOrderShippedEmails" -- apps` ⇒ **0 命中**;正向對照(同一把尺)`git grep -l "sweepEmailOutbox" -- apps` ⇒ **4 支檔**;負對照(與目標不同的寫法)`git grep -n "enqueueOrderZzzFakeEmails" -- apps` ⇒ **0**;⚠️🔴🔴 **2026-08-26 追加:上面那個負對照【已經死了】,而它死掉的方式值得單獨記。**本行寫下 `enqueueOrderZzzFakeEmails` 當靶 ⇒ 它就成了 repo 裡存在的字串。**當天稍晚,另一個窗為了【記錄「這個靶被寫進檔了」這件事】,把同一個字串抄進了 `apps/` 底下一支測試檔的註解**(`apps/admin/src/components/orders/order-toolbar-entry.test.tsx:89-97`,commit `4054b80d`)。⇒ 🔴 **那個靶,是被【記錄它已被消耗】的那個動作本身消耗掉最後一格的。****2026-08-26 當場實測(可重跑)**:`git grep -l "enqueueOrderZzzFakeEmails" -- . | wc -l` ⇒ **2**;`… -- apps | wc -l` ⇒ **1** ⇒ **本行原本靠 `-- apps` 這個 pathspec 保住的那個 `0`,現在是 `1`。**⚠️ **所以不要引用上面那個 `⇒ 0`** —— 它在 `4713a0c4` 那棵樹上為真,今天不是。⇒ **要重驗請當場另挑一個沒有寫進任何檔案的靶,並在同一發裡驗它回 0;而挑完不要公告它。**成因與處置見 `docs/patterns/guard-and-instrument-traps.md`「一個負對照, 只要被公開就會被消耗掉」那一節(**用標題找,不要記行號**)。📌 **「量法帶 pathspec」不是保護,是運氣 —— 而運氣會在別人動手時用完。**全樹 4 處命中逐一開檔 = 它自己 / 它的 test / `packages/ports/src/IShippedOrderScanner.ts` / `packages/use-cases/src/index.ts` 的 re-export ⇒ **零生產呼叫端**。<br>⇒ 🔴 **沒有任何一條路能把「出貨」變成一列 outbox ⇒ 不論在哪一個資料庫量,那一格都會印 `0`。功能好的世界與功能壞的世界印【同一個數字】。**<br>⇒ 📌 **所以修法不是替它補一個環境標籤** —— 補了會讓它**看起來**變可信,而它的判別力一格都沒有動。要讓那一發重新有判別力,前提是**先有呼叫端**;在那之前該量的是三道閘本身,不是 `n_tup_ins`。<br>⚠️ **本註記的射程(不要讀寬)**:我證明的是**這個量測不具判別力**,**不是**「#11 沒做」。狀態 🧩 與「一封信都不會多」**兩句都仍然成立** —— 三道閘 2026-08-25 逐條複驗仍在:閘③ `packages/use-cases/src/sweep-email-outbox.ts:125` 逐字 `throw`;閘④ `grep -c "20260822010000_m4b_e4a_shipped_email_scan_view" supabase/APPLIED.tsv` ⇒ **0**(分母 = 已 apply **214** 支,`grep -vc "^#"`;正向對照 表上真有的 `20260505130758` ⇒ **1**;負對照 ⇒ **0**)。⚠️ 而該 migration **檔本身在** ⇒ 是「差一發 apply」,不是「沒寫」。<br>🔴🔴 **環境:原量測未標,無法確認。**(那個 `n_tup_ins ⇒ 0` 必須在**某一個資料庫**裡才量得到,而本格沒寫是哪一個。線2 2026-08-22 夜 量到**正式站** `email_outbox` 有 **12** 筆 insert ⇒ 🔴 **那個 0 與這個 12 是否同庫,現有證據分不開 —— 不要把它讀成「從 0 變成 12」。**⚠️ **不要替它猜一個環境**:線2 自己先寫了「那個 0 是鑽機量的」,再自己證偽 —— `bash scripts/spec-1a-recheck.sh 11 | grep -cE "鑽機|正式站|本機|production"` ⇒ **0**(正對照 同尺同格量 `email_outbox` ⇒ **3** ⇒ 尺是活的)。) | 🔴 **我真的出了一張貨**,然後量 `email_outbox` 的 `n_tup_ins` ⇒ **0**(用 lifetime insert 數不是 live 數 ⇒ 分得出「從沒寫過」與「寫了又刪」)。<br>碰 `email_outbox` 的 11 支檔**全在 storefront + packages**,`apps/admin` **零**;`shipment-actions.ts` 對 `outbox\|enqueue\|notify\|通知` 零命中。⚠️ **射程**:🔴 **這個字集裡沒有 `email` / `mail`,而這一格的主題就是 email** —— 今天回 0 是對的(審查線把字集加寬重打,兩處命中逐筆開過都是「包裹已寄出」語意),**但下次重驗一定要用加寬過的字集**。<br>🔴🔴 **2026-08-22 夜 理由改寫(狀態仍 🧩,只改理由)—— ~~「`IShippedEmailContext.ts` 存在而零實作、零呼叫端…這邊只有插座」~~ 那句已經死了。**<br>現在是「**插座 + 引擎 + 接線【都有】,而卡在三道各自獨立的閘**」。本窗逐條自己開檔複核,每發帶正對照:<br>① **有實作**:`git grep -ln 'implements IShippedEmailContext|: IShippedEmailContext' -- apps packages` ⇒ `packages/adapters/src/email/SupabaseShippedEmailContextAdapter.ts` · `packages/use-cases/src/sweep-email-outbox.ts`;import 它的 **8 支檔**(正對照 同一把尺量 `IProductRepository` ⇒ **14** ⇒ 尺是活的);**而且真的被 `new` 出來**:`apps/storefront/src/lib/email/composition.ts:72`。<br>② 🔴 **零呼叫端**:`git grep -n 'enqueueOrderShippedEmails' -- apps` ⇒ **rc=1、0 命中**(正對照 同一把尺量 `sweepEmailOutbox` -- apps ⇒ **4 支檔**,那條有 cron 在跑)。全樹命中只有:它自己 / 它的 test / `packages/use-cases/src/index.ts:128` 的 re-export。<br>③ **模板 fail-closed**:`packages/use-cases/src/sweep-email-outbox.ts:125` 逐字 `throw new Error('sweepEmailOutbox:order_shipped 模板未定義(E4 未落地)、fail-closed 不寄')`。<br>④ **掃描 view 未 apply**:`supabase/migrations/20260822010000_m4b_e4a_shipped_email_scan_view.sql` 在 `supabase/APPLIED.tsv` ⇒ **0 命中**(正/負對照同 `#17b` 那格)⇒ **正式庫裡沒有那張 view。**<br>⇒ 🔴 **「一封信都不會多」是真的,而且是三道獨立的閘一起保證的。**<br>📌 **而這一格的移動方向是「往前一階,而格子沒動」** —— 這張表的六個狀態碼**分不出這種移動**;`#11` 與 `#17b` 都停在「**差一發 apply**」<br>**錨點**(下次重驗從這裡開始;皆 `apps/admin/src/` 底下除非另註):`packages/ports/src/IShippedEmailContext.ts`(~~**只有插座**~~ **已有兩支實作,見上**)· `lib/shipping/shipment-actions.ts`(**不碰 outbox**)· `apps/storefront/src/app/api/cron/email-sweep/route.ts` |
| 12 | 🧩 手動建單 | 2026-08-22 **量了** | `orders/new\|createOrder\b\|建立訂單` rc=**1**;無 `/orders/new` route;訂單頁 `新增訂單`/`建立訂單`/`手動建單`/`手動新增` 皆 **0**(正對照同頁 `訂單`=**151**)<br>🔴🔴 **2026-08-22 補:那條掛了一個月的「動前必驗」已經驗出來了。**memory `project_admin-backend-staff-ready-goal`(2026-07-25)逐字掛著:「`create_order` RPC **是否允許自由輸入品項未查證**,動前必驗」⇒ **今天實讀,答案是【不允許】。**<br>`20260730120100_m4b_e10_n3b_create_order_new_display_id.sql:296-320` 逐字:每一列必須以 `variant_id` **或** `(supplier_slug, sku)` 對到一列**已存在的** `product_variants`,否則 `RAISE EXCEPTION 'create_order: line 缺 variant_id 或 (supplier_slug,sku)'` / `'找不到 variant'`;單價只能取 `price_general`(必須 >0)、已下架直接擋。<br>⇒ **而它還有另外兩道讓 admin 走不通的閘**:`:243` 要 `auth.uid()`(**登入中的客人**,不是員工)、`:255` 要 `address_id` **屬於那位客人**。<br>⇒ 🔴 **結論:代購訂單【不能靠包一層 UI 在 create_order 上】,要一個新的 writer。**而它與 `#20 新增商品` **是鏈在一起的** —— 不新增商品,就沒有 variant 可以對。<br>📌 **資料層其實已經備好了**:`orders.order_source` 是 `text` + CHECK `ANY(ARRAY['web','manual_phone','manual_line','manual_other'])`(實查),而**鑽機上 26 張單全部是 `web`** ⇒ 那三個手動值**從來沒有被用過**。<br>**量法與正對照**:`admin_create_order\|create_manual_order\|manual_order` 全樹命中 **0 檔**;同一把尺量 `admin_record_manual_refund` ⇒ **8 檔** ⇒ 尺是活的。 |<br>🔴🔴 **2026-08-25 幽靈(線 3 重跑本列自附的四條量法 —— 四條全部給出不同答案)**:①`orders/new|createOrder\b|建立訂單` 表 rc=1(查無)⇒ 現在 **rc=0, 命中 7 支檔** ②「無 `/orders/new` route」⇒ **那個目錄在**(`git ls-files` ⇒ 2)③訂單頁「手動建單」表 0 ⇒ **2** ④`admin_create_order|create_manual_order|manual_order` 表 0 檔 ⇒ **15 檔**。⚠️ 其中兩格線 3 的尺比原表寬並已逐格標出(③ 正對照 151 vs 57 = 量頁 vs 量目錄;④ 正對照 8 vs 15 = 是否含 `supabase/`)—— **目標值 0→15 且尺是活的 ⇒ 那個變化是真的, 但別把兩種差異讀成一件事。**<br>🔴 **本列的價值不在多一個幽靈, 在於上面那句【估工用的結論】**:「代購訂單不能靠包一層 UI 在 `create_order` 上, **要一個新的 writer**」——**那個 writer 已經有人做了**(page → form → action → repository → RPC `admin_create_manual_order`, migration `c07e5378` 已進版控、帳本 ⇒ 1)。照那句話估工的人會估「從零做一個 writer」, 而真相是「已經有 7 支檔在那裡」。<br>📌 而這正是本表檔頭自己講的最貴形狀, 逐字:「而最貴的一格是 `#27`:表上寫【連讀取 API 都沒有】而**實際整頁都在, 只差一個環境變數**」⇒ **同一個病三天後又發生一次, 只是換了一格。**<br>⚠️ **仍未做完的**:`docs/probes/2026-08-24-858-orders-pair-valid-performs.sql` 已由線 1 在拋棄式 PG 補跑(五格全過);而**沒有人在真後台按下去建過一張單** —— 本表把「做了」定義成真後台真的按下去, 而線 3 明說「我一格都沒按」。**不得把「檔在、route 在、進版控了」讀成「員工現在真的建得出一張單」。**<br>**錨點**(下次重驗從這裡開始):`apps/admin/src/app/orders/new/page.tsx` · `apps/admin/src/lib/orders/manual-order-actions.ts` · RPC `admin_create_manual_order`(`git grep -l admin_create_manual_order` ⇒ **17** 支檔,`-c8` 2026-08-28 實跑)。🔴 **而本格的「做了」需要一次【真瀏覽器點擊】—— 碼齊了不算**(表自己的定義;08-25 線 3 逐字「我一格都沒按」,08-28 仍然沒有人按過)。
| 18 | ❌ 退貨收回 | 2026-08-22 **量了** | 無 route;訂單頁 `退貨` **0**<br>**錨點**:🔴 **本格是「不存在」,所以錨點是【下次去哪裡確認它仍不存在】**:`git grep -l return_request` ⇒ **8**(`-c8` 2026-08-28;**而那 8 支要逐一開檔** —— 上一次的量法只寫 rc、沒寫分母)· 最近親(有做的那條)= `apps/admin/src/lib/orders/cancel-action-state.ts`(取消,不是退貨收回)。 |
| 21 | ❌ 上傳商品圖片 | 2026-08-22 **量了** | `supabase.storage\|.storage.from(\|createSignedUploadUrl` rc=**1**(對照 `createClient` ⇒ rc=0 / 3 檔)。商品明細頁**自陳**逐字「圖片與影音 —— 這一頁的東西不能在後台改,它由供應商同步管線維護」<br>**錨點**:🔴🔴 **`-c8` 2026-08-28 重跑撈到這把尺自己的回音** —— `git grep -l "supabase\.storage\|\.storage\.from("` 全樹 ⇒ **1**,而**那一支就是本 spec 檔自己**(描述這條量法的那一行)⇒ **程式碼端真正的命中 = 0**。📌 **同一個病今晚在本 repo 出現第三次**(`col-ordered` / `line-admin.ts:10` / 本格)。錨點 = 商品明細頁自陳那句「圖片與影音 —— 這一頁的東西不能在後台改,它由供應商同步管線維護」(`apps/admin/src/components/products/`)。 |
| 23 | ❌ 匯入供應商 Excel | 2026-08-22 **量了** | `xlsx\|XLSX\|SheetJS\|exceljs`(`apps/**` `packages/**` `scripts/**`)rc=**1** ⇒ **零程式解析 Excel**。⚠️ 08-14 那條更正仍成立:`scripts/rpm-*.ts` 做的是**跨專案 DB→DB 同步**,不是解析 Excel ⇒ **不能靠把既有 CLI 包一層 UI 解決**<br>**錨點**:`scripts/rpm-*.ts`(**repo 根的 CLI,不是後台畫面**)· `git grep -l xlsx` ⇒ **20**(`-c8` 2026-08-28)⚠️ **而那 20 支要逐一開檔** —— 上一次只寫 rc,沒有分辨「CLI 在用」與「後台在用」。 |
| 24 | ❌ 匯出訂單商品 | 2026-08-22 **量了** | `text/csv\|application/vnd.ms-excel\|Blob(\|createObjectURL` rc=**1**;訂單頁 `匯出` **0**<br>🔴🔴 **錨點 + 2026-08-28 `-c8` 幽靈判定:本格【不是 ❌】。**`git grep -l "text/csv"` ⇒ **3**,其中 `apps/admin/src/components/orders/order-export-button.tsx`(**86 行,真的存在**)被 `apps/admin/src/app/orders/page.tsx` **import 上去了**(該元件被 import 的檔數 ⇒ **4**:訂單頁 / 它的 test / `lib/orders/order-export.ts` / `order-export-page.test.ts`)。該檔檔頭自陳有 `#24` **片B / 片C** 的施工史,並記著 2026-08-26 codex 抓到的一條(CSV prop 其實含整份訂單資料)。⇒ **狀態該從 ❌ 移出去**,而**移到哪由主視窗裁**(照本表定義,仍缺「真瀏覽器按過」那一發)。**錨點**:`components/orders/order-export-button.tsx` · `lib/orders/order-export.ts` · `app/orders/page.tsx`。🔴 **2026-08-28 主視窗裁定(與 `#12` 同一個處置)**:**從 ❌ 移出去,而【不是】移進 ✅** —— 它已經接線了(被 `orders/page.tsx` import)⇒ **不是 🧩「備好沒人接」**;而表自己定義「做了」= **真後台真的按下去**,而**沒有人按過**。⇒ **本格的「做了」需要一次【真瀏覽器點擊】,而碼齊了不算。**📌 **`#12` 與本格用同一個處置 ⇒ 那個缺口現在有形狀了:【碼齊了、沒人按過】。一格是特例,兩格是類別。**(不動表結構、不加第七碼 —— 表明文寫「改了下次重數不可比」。) |
| 28 | 🧩 會員密碼重設 | 2026-08-22 **量了** | `resetPasswordForEmail\|generateLink\|密碼重設` rc=**1**(機制在 `SupabaseAuthAdapter.ts:87-90`,admin 端仍零觸發入口)<br>**錨點**:引擎 `packages/adapters/src/supabase/SupabaseAuthAdapter.ts:89` 逐字 `await this.supabase.auth.resetPasswordForEmail(params.email, {`;而入口那半 `git grep -l "resetPasswordForEmail\|generateLink" -- apps/admin` ⇒ **0**(全樹 ⇒ **13**;`-c8` 2026-08-28 重跑,與 08-22 同值)⇒ **有引擎、沒入口,八天未動。** |
| 29 | 🧩 後台對客發信 | 2026-08-22 **量了** | `sendEmail\|mailer\|resend\|寄送.*客人` rc=**1**。⚠️ **射程**:這個字集**沒有 `email` / `mail`** ⇒ 有人往那支檔加 `sendEmail(...)` 以外的寫法時抓不到。**下次重驗把字集加寬再打。**<br>**錨點**:管線 `packages/adapters/src/email/`(`ResendEmailSenderAdapter.ts` · `SupabaseEmailOutboxAdapter.ts`)+ `apps/storefront/src/app/api/cron/email-sweep/route.ts`;而入口那半 `git grep -l email_outbox -- apps/admin` ⇒ **0**(正對照同尺量 `order_payments` ⇒ **14**;`-c8` 2026-08-28 重跑,與 08-22 同值)⇒ **有管線、沒入口,八天未動。** |
| 30 | ❌ 月報表 | 2026-08-22 **量了(route)** | admin 無任何對應 page.tsx<br>**錨點**:🔴 **本格是「不存在」** ⇒ 錨點 = 下次去哪確認:`git ls-files "apps/admin/src/app/**/page.tsx" | grep -icE "report|ledger|accounting|cost"` ⇒ **0**(`-c8` 2026-08-28)。⚠️ **而它卡的不是碼,是【內容待定義】**(見 `N10`)⇒ **重跑那條 grep 永遠會是 0,直到有人先決定要印什麼。** |
| 31 | 🚧 成本 | 2026-08-22 **量了(route)** | admin 無 route。⚠️ DB 層 CHECK 硬擋(`20260602135934:90,93`)**未複驗**<br>**錨點**:`supabase/migrations/20260602135934_s1_supplier_slug_delisted_clean_metadata.sql` —— `-c8` 2026-08-28 開檔複驗那兩件事**都在**:①把既有 `cost` 從 metadata **刪掉**(`SET metadata = metadata - 'shopee' - 'cost' - …`)②兩張表各加 CHECK `product_variants_metadata_no_sensitive` **硬擋 cost 寫回去**。⇒ 🔴 **「未複驗」那個標記可以拿掉了。**而本格的第一步仍然**不是寫功能,是推翻一個已落地的安全決定**(Sean `N1`)。 |
| 32 | ❌ 會計收支帳 | 2026-08-22 **量了(route)** | admin 無 route<br>**錨點**:🔴 **本格是「不存在」** ⇒ 同 `#30` 那條 `git ls-files … | grep -icE "report|ledger|accounting|cost"` ⇒ **0**。⚠️ 而它卡的是**「他重開的時機」**⇒ **這是一個等外部事件的格,不是等工。** |
| 33 | ❌ 成本精算報表 | 2026-08-22 **量了(route)** | admin 無 route<br>**錨點**:🔴 **本格是「不存在」且【依賴 `#31`】** ⇒ 同 `#30` 那條 grep ⇒ **0**。⚠️ **在 `#31` 那個安全決定被推翻之前,本格的任何量測都會回 0,而那個 0 不含資訊。** |

🔴 **上表有 11 格沒有「錨點」欄,那不是漏寫** —— `#12` `#18` `#21` `#23` `#24` `#28` `#29` `#30` `#31` `#32` `#33`
判 ❌ 的依據**就是「沒有那個檔」**,錨點是**下面這張 route 清單本身**。
⇒ 下次重驗這 11 格的做法是:**重跑那條 `find`,看清單有沒有多出東西**,不是去開某一支檔。

**判 ❌ 的分母 = 2026-08-22 admin 完整 route 清單**(`find apps/admin/src/app -name "page.tsx"`):
```
/                                          ← #16
/orders  /orders/[id]  /orders/refund-exceptions
/customers  /customers/[id]
/products  /products/[id]                  ← #20 唯讀
/settings/audit  /settings/staff  /settings/suppliers   ← #27（audit 被旗標關著）
/print/orders/[id]/picking
/print/orders/[id]/shipping/[shipmentId]   ← #10
/@panel/orders  /@panel/[...catchAll]
```

---

### 🔴 §1-A-5 這張表**沒有**的四件,而它們擋著「員工的一天」

**① 員工早上看到的第一個數字,沒有標籤,而且和它點進去的清單對不起來**(2026-08-22 **量了**)
```
側欄「訂單」那顆 = 未訂貨 且 未取消 的單數
  預設清單（隱藏「刷卡未付款」）  ⇒ 側欄 24 / 清單 14
  勾「顯示刷卡未付款(預設隱藏)」⇒ 側欄 24 / 清單 24   ← 完全相等
  做完一筆採購之後重量            ⇒ 側欄 23 / 清單 23   ← 仍然相等
```
✅ **那 10 筆沒有消失,是一個勾勾的距離。這是標籤問題,不是單子不見。**
🔴 而真正的問題是:那顆數字數「未訂貨」(23),它點進去那張清單數「全部狀態、扣掉隱藏的、近半年」(16)
—— **16 不是 23 的子集,而兩個數字並排在同一個畫面上**;那顆數字 `aria-hidden`、沒有 title、
沒有任何字說它數的是什麼(`<span aria-hidden="true" data-testid="rail-count-slot">23</span>`)。
⇒ **修法推薦「標籤化」**(不要改側欄連結去帶 `show_unpaid_card=1` —— 那會繞過 Sean 定的預設隱藏);
   `sidebar-counts.ts` 的查詢本身**沒有錯,不要動它**。

**② 89% 的真訂單上印著「請通知系統維護」,而那是【正常狀態】**(2026-08-22,量的是**正式庫**)
```
orders 19 / order_items 22
沒有 quantity summary 列的品項  19/22 = 86%
其中有任一品項沒有的訂單        17/19 = 89%
```
而程式自己說那是對的(`lib/orders/order-status-axes.ts:240-243`:惰性建列,**沒有那一列就是還沒訂貨**)。
⇒ 🔴 **員工明天上工打開幾乎每一張單,都會讀到一句叫他去找工程師的話,而什麼事都沒有。**
✅ **已修(2026-08-22,commit `5df1b1c9`)**:文案改成先講常見世界。
   🔴 **刻意沒有寫「這代表還沒下訂」** —— 程式自己承認「摘要列真的不存在」與「投影壞掉讀不到」
   **在畫面上長得一樣**,那句話它證明不了。

**③ 出貨這個動作,稽核紀錄上一筆都沒有**(2026-08-22 **做了**)
稽核頁上有:建立採購 ✅ / 登錄到貨 ✅ / 新增訂單備註 ✅ / order.workflow.update ✅ — **出貨 ❌ 一筆都沒有**。

🔴 **2026-08-22 05:4x 用更大的分母複驗過**(我一整晚在鑽機上做了十幾個動作之後,直接數 `admin_audit_log`):
```
order_note.append          4
order.workflow.update      2
procurement.create         2
procurement_receipt.create 2
product.listing.change     2      ← 連【商品上下架】都記
payment.record             1      ← 連【登錄匯款收款】都記
🔴 shipment / 出貨         0      ← 而我今晚【真的出了一張貨】
```
⇒ **六種動作型別都在記,唯獨出貨不記。**

### 🔴🔴 更正(2026-08-22 09:2x,我自己查 `shipment-actions.ts` 之後推翻自己前一句)

我前一句寫「**這一格被漏掉了,不是還沒排到**」—— **那句話錯了,而且錯在最貴的方向:它把一件【已登記的工】說成【疏忽】。**

`apps/admin/src/lib/shipping/shipment-actions.ts` 逐字:
```
:79   出貨線的稽核用【應用層 log】(不是 admin_audit_log, DB 層那一列是【A2 的事】)
:294  本片(A1)只加閘, 【一列 admin_audit_log 都沒有寫】; DB 層的稽核列是 A2 的事
:130 / :201 / :219   auditLog('shipment.submit', …, 'attempt' / 'ok' / 'fail')   ← 真的有在寫
```
⇒ **正確的說法是**:
```
✅ 出貨【有】留痕 —— 而它寫在【應用層 structured log】, 不在 admin_audit_log
🔴 而【操作紀錄那一頁讀的是 admin_audit_log】⇒ 員工在畫面上看不到出貨
⇒ 所以「員工看不到誰出的貨」仍然成立, 而「沒有人記」不成立
📌 缺的那一段【有名字】:A2 那一片。它是【🧩 備好了沒人接】, 不是 ❌
```

⚠️ **而同一支檔還記了一件更重要的**(`:290-295`,逐字):
> 🔴 **不寫「這個動作會記在稽核紀錄上」** —— 原版那樣寫,而**那是給使用者看的假字面**…
> ⇒ **承諾一個還不存在的東西,而員工沒有任何辦法發現它不存在。**

⇒ **有人已經想過這一格,而且刻意把那句承諾拿掉了。**
🔴 **我原本要把它報成「漏掉」,而真相是【有人為它做過一次判斷】。**

📌 **我是怎麼發現自己錯的**:去數「稽核那張中文對照表收了幾個 action」時,
撞到 `shipment.void` 在**程式碼裡有、而 DB 從沒出現過** ⇒ **那個矛盾逼我去開那支檔。**
⇒ **一個為了別的目的做的清點,推翻了我一個講得很肯定的結論。**
⇒ `MAIN-113 §4` 記著 Sean 已答「出貨要不要記誰做的 ⇒ **好(要)**」⇒ **這是已批准而沒做的工,不是新決策題。**

**④ 「請款狀態」整支是死碼**(2026-08-22 **量了**)
`deriveCaptureDisplay` / `getOrderCaptureState` / `parseCaptureRow` / `CAPTURE_STATE_LABEL` 對外消費端全部 **0**
(正對照:同一把尺量 `order-detail-summary-cards` ⇒ **3 檔**);四張真訂單頁上「請款」兩字 raw + 跳脫皆 **0**
(正對照 `付款狀態`=2 / `已付款`=2~4)。與 `#22` 同族,而**這支沒有在自己的檔頭招**(已於 `a29e8e12` 補上)。
🔴 **不是沒人在寫值**:正式站 `cron.job` jobid=6 `pcm-capture-recheck` 每 10 分鐘跑、`active=true`,
近 6 小時 36 發全 200 —— 它的述詞是 `capture_state='authorized'` 而現存 17 列全是 `unknown`
⇒ **它每 10 分鐘認真地掃一次空集合。**
📌 **「沒人在做」與「有人在做而看的地方是空的」在畫面上、在 grep 上都是零,而處置完全相反。**

---

### §1-A-6 主視窗複驗並更正 subagent 兩處結論(2026-08-14,**仍然成立**)

1. **`#26` 判「不跨 repo」不成立。** subagent 量的是「今天的 actor picker 程式住在哪」,
   而問題是「**要讓每個員工有自己的帳號,得改哪裡**」。
   `apps/admin/src/lib/session/session.ts:12` 逐字:「**報價單=共用密碼登入,SSO 只帶認證(amr/auth_time)、無 per-user 身分**」
   ⇒ 真正的登入在**報價單專案**(且該專案正本在 mac mini)⇒ **確為跨 repo + 跨機器**。
   ⚠️ 同族於「量錯東西」:**量了程式住哪,沒量修法住哪。**
2. **`#23` 比表面更糟,而且它不是「純本 repo」的線。** 見 §1-A-4 該格。

### §1-A-7 兩次重盤各自踩到的量具坑(留著,因為下一次重盤還會踩)

```
2026-08-22 線 E:查 `notification_outbox` ⇒ 零命中, 差點寫成「連 outbox 都沒有」
             真的識別字是 `email_outbox` ⇒ **零命中是我的尺, 不是那個東西不存在**
2026-08-22 線 E:`git grep … | head -5 || echo 零命中` ⇒ 那個 `||` 永遠不會執行（rc 來自 head）
2026-08-22 線 A:用 textContent 判「沒有下訂入口」⇒ 0 命中, 而商品卡是 React 受控 <details>,
             關著時 body 根本沒掛進 DOM ⇒ **真的點開才量得到**
2026-08-22 線 A:`grep -c '訂單' orders.html` ⇒ 1, 而該頁有幾十次
             ⇒ Next 把中文塞進 RSC payload 的 `\uXXXX` 跳脫 ⇒ **raw 與跳脫兩種都要數**
```
📌 完整版見 `docs/patterns/traps-inbox/A-20260822.md` 與 `C-20260822.md`(併入 canonical 後那兩檔可刪)。

### 🔴🔴 §1-A-8 **四個「備好了、沒人接」的標本 —— 而它們停在不同深度**(2026-08-22 線 A `-86`)

> **為什麼要單獨列一張表**:這四個現在散在四個條目裡,而**它們合起來才看得出一件事**。
> 分開讀 ⇒ 四個各自的 ❌ / 🟡;合起來讀 ⇒ **「做到一半」不是一個深度,是一條光譜。**
> 🔴 **而越深的那幾個,越像做完了。**

| 標本 | 停在哪一層 | 已經有的 | 缺的那一段 | 剩下的是**寫 code** 嗎 |
|---|---|---|---|---|
| `#22` 改價格 | **有引擎、沒入口** | adapter 的 `updatePrice` 路徑 | 一個呼叫端 | ✅ 是,而且小 —— **它自己的 JSDoc 招了「今天沒有正式呼叫點…是死路」** |
| `capture-state`(請款狀態) | **有函式、沒消費端** | `deriveCaptureDisplay` 四態 + 40 行判斷順序 | 一支 `.tsx` 去讀它 | ⚠️ 是,**而接了也沒東西可看** —— 正式站 cron 每 10 分鐘掃 `capture_state='authorized'`,而現存 17 列全是 `unknown` ⇒ **它每 10 分鐘認真地掃一次空集合** |
| `#12` 代購訂單 | **有 CHECK、沒 writer** | `orders.order_source` CHECK 含 `manual_phone` / `manual_line` / `manual_other` | **一支新的 writer** | 🔴 **是,而且大** —— `create_order` 走不通(要 `auth.uid()` 是客人、要既有 `product_variants`、要客人自己的地址)⇒ 且**與 `#20 新增商品` 鏈在一起** |
| `#9` 單號追蹤 | 🔴 **有欄位、有守門、沒整合** | `shipments` 三欄(`hct_status` / `hct_request_id` / `hct_raw_response`)+ **三道 CHECK**(含 `submitted` 必須帶非空 request_id) | **一條外部整合** | ❌ **不只是寫 code** —— 是接新竹物流的 API(憑證、批次上限、同日編號不可重複) |

**四格的量法與正對照(2026-08-22,鑽機 + 全樹)**
```
#22            adapter 自陳（該檔 JSDoc）
capture-state  deriveCaptureDisplay / getOrderCaptureState 對外消費端 ⇒ 0
               正對照 order-detail-summary-cards ⇒ 3 檔 ⇒ 尺是活的
               四張真訂單頁上「請款」兩字 raw + \uXXXX 皆 0（正對照 付款狀態=2）
#12            order_source 26 張單全是 'web';admin_create_order\|create_manual_order\|manual_order ⇒ 0 檔
               正對照 admin_record_manual_refund ⇒ 8 檔
#9             6 張包裹全部 hct_status='draft'、request_id 全空
               hct.com.tw\|HCT_API\|hctApi ⇒ 0 檔（正對照 tappay ⇒ 44 檔）
               把 hct_status 寫成 'submitted' 的程式碼 ⇒ 一行都沒有
```

### 🔴 這張表要改變的是**估工的方式**

```
❌ 這個標記把【整條沒做】與【備好了沒人接】混在一起, 而兩者的成本差一個數量級。
   #27 稽核 UI 被記成 ❌「連讀取 API 都沒有」—— 而實際上【整頁都在, 只差一個環境變數】
   #9  被記成 ❌ —— 而它其實已經走到「連 fail-closed 都寫好了」那麼深
```
⇒ **看到 ❌ 的人要先問的不是「要做多久」,是「它停在哪一層」。**
   而那一題**只有開檔才答得出來** —— schema 有欄位、有 CHECK、有三態的東西,**讀起來像它在跑**。

📌 **可複用的判別句**:
> **這個功能「沒做」,是【沒有人想過】,還是【想過、寫了一半、而剩下的那一段沒有人接】?**
> 後者 ⇒ 去看它停在哪一層:**沒入口 / 沒消費端 / 沒 writer / 沒整合**,四層的成本完全不同。

⚠️ **這張表本身也會過期** —— 它列的是 2026-08-22 的四個。**任何一個被接上之後要從表上移走**,
   而移走的時候要問一句:**有沒有第五個?**(找法:資料層有值域/欄位/守門,而 `grep` 那個識別字的消費端回 0。)


### 🔴 §1-A-9 **13 個 ❌ 逐格分類:哪幾個是「整條沒做」,哪幾個是「備好了沒人接」**(2026-08-22 線 A `-86`)

> **為什麼要分**:`❌` 把兩件事混在一起,而**兩者的成本差一個數量級**。
> 判別三問:**有欄位 ⇒ 問誰在寫 / 有函式 ⇒ 問誰在呼叫 / 有守門 ⇒ 問它擋過誰。**
> 每一格帶正對照(同一把尺量 `tappay` ⇒ **70 檔**;量 `order_payments` 在 `apps/admin` ⇒ **13 檔**)。

| # | 事 | 新標記 | 停在哪一層 / 量到什麼 |
|---|---|---|---|
| 4 | 批次標商品進度 | 🧩 | **有函式、沒消費端**:`lib/orders/procurement-batch-policy.ts`(A9h-2 純決策層)對 `components/` 與 `app/` **零呼叫端**。<br>⚠️ 而**批次畫面本身是有的**(出貨對話框跨單逐品項、含「貨到了」)—— 缺的是**批次下訂**那一步 |
| 11 | 出貨 Email 通知 | 🧩 | ~~**只有插座、沒有引擎**~~ **2026-08-22 夜 更正:插座+引擎+接線都有,停的是【沒有人呼叫】** —— `enqueueOrderShippedEmails` 在 `apps` 底下 **0 命中**(正對照 `sweepEmailOutbox` ⇒ 4 支檔),外加模板 fail-closed + 掃描 view 未 apply。**逐條證據見 §1-A-4 的 `#11`。**<br>~~`packages/ports/src/IShippedEmailContext.ts` 存在而**零實作、零呼叫端**~~;`apps/admin` 對 `email_outbox` **0 檔**(正對照:同一把尺量 `order_payments` ⇒ **13 檔**)。<br>🔴 **比 `#22` 更早一階** —— 那邊有引擎沒入口,這邊**連引擎都還沒有** |
| 12 | 手動建單(代購) | 🧩 | **有 CHECK、沒 writer** —— 見 §1-A-8 |
| 28 | 會員密碼重設 | 🧩 | **有引擎、沒入口**:`packages/adapters/src/supabase/SupabaseAuthAdapter.ts` 有機制,而 `apps/admin` **零觸發入口**(`resetPasswordForEmail\|generateLink` 全樹 **2 檔**,兩檔都不在 admin) |
| 29 | 後台對客發信 | 🧩 | **有管線、沒入口**:信件管線在 `packages/adapters/src/email` + `apps/storefront` 的 cron(共 **9 檔**),而 **`apps/admin` 對 `email_outbox` 0 檔** ⇒ 員工沒有任何地方可以主動寄信 |
| 18 | 退貨收回 | ❌ **真的整條沒做** | 全樹 `return_request\|order_return\|退貨收回\|restock` ⇒ **1 檔**,而那一檔是 `cancel-review-section.tsx` **在說它不存在**(`:213` 逐字「退貨線在本 repo 零落點」;`:221` 畫面文案「**而退貨功能目前還沒有**」)。<br>✅ **而這一格做對了一件事:畫面沒有假裝有那條路** |
| 21 | 上傳商品圖片 | ❌ 真的整條沒做 | `supabase.storage\|storage.from(\|createSignedUploadUrl` ⇒ **0 檔**(連底層都沒接);商品明細頁自陳「這一頁的東西不能在後台改」 |
| 23 | 匯入供應商 Excel | ❌ 真的整條沒做 | `xlsx\|XLSX\|SheetJS\|exceljs` ⇒ **0 檔**。⚠️ 08-14 那條更正仍成立:`scripts/rpm-*.ts` 做的是**跨專案 DB→DB 同步**,**不能包一層 UI 解決** |
| 24 | 匯出訂單商品 | ❌ 真的整條沒做 | `text/csv\|application/vnd.ms-excel\|createObjectURL\|toCsv` ⇒ **0 檔** |
| 30 | 月報表 | ❌ 真的整條沒做 | `monthly_report\|月報表\|report_period` ⇒ **0 檔**;admin 無 route |
| 32 | 會計收支帳 | ❌ 真的整條沒做 | `ledger_entry\|accounting\|收支帳` ⇒ **0 檔**;admin 無 route |
| 33 | 成本精算報表 | ❌ 真的整條沒做 | `cost_analysis\|成本精算` ⇒ **0 檔**;admin 無 route |
| 31 | 成本 | 🚧 **第四種:不是沒做,是【刻意做了反向的】** | `products` / `product_variants` 的 cost 欄 ⇒ **0 個**;而 `20260602135934` **加了 CHECK 硬擋 `cost` 寫進 metadata**,同一支 migration 還 **把既有的 cost 值從 metadata 裡刪掉**(`:81-85`)。<br>🔴 ⇒ 要做 `#31` 的第一步**不是寫功能,是推翻一個已經落地的安全決定**(Sean `N1`:員工看得到經銷價、**看不到成本**)⇒ **它與權限設計綁在一起,不是一片 UI** |

### 🔴 §1-A-9b **那 7 個 + `#31` 的「第一步是什麼」** —— 這張表答的是「明天早上從哪一件開始」

> **為什麼要多這一格**:上面那張表答得出「離上線差什麼」,**答不出「明天要從哪一件開始」**。
> 🔴 **而「第一步」常常不是寫 code** —— 接外部 / 決定格式 / 拿一份樣本 / 推翻一個決定。
> **那幾種可以在等 Sean 的時候先做,或者根本就是他的一個字。**

| # | 事 | **第一步是什麼** | 那一步是 | 押在 Sean? |
|---|---|---|---|---|
| 21 | 上傳商品圖片 | 🔴 **不是接 Storage,是先決定「手動上傳的圖怎麼活過同步」**。畫面自己講了:圖片「**沒有『鎖住不給改』的機制** —— 只有『來源那天沒給值就不覆蓋』那一層,**擋不住供應商換圖**」(`components/products/product-detail.tsx:197-198`)。<br>⇒ 沒有這個決定,做出來的上傳功能**會在下一次同步被蓋掉,而且沒有人會發現** | **決定**(先)+ 技術(後) | 🟡 **是一個字的題**:手動上傳的圖要不要贏過供應商? |
| 23 | 匯入供應商 Excel | 🔴 **拿到一份真的 Excel**。全 repo `*.xlsx` / `*.xls` / `*.csv` ⇒ **0 個檔**(實查)。<br>⇒ **不知道欄位長什麼樣,連規格都寫不了** —— 而那份檔在 Sean 手上,不在我們這裡 | **取得樣本** | 🔴 **是,而且是最便宜的一題**:「有沒有一份真的供應商 Excel 可以給我們?」 |
| 24 | 匯出訂單商品 | **決定「匯出給誰、要哪幾欄」**。最可能的消費端是**記帳士**(memory:401 由記帳士報、兩個月一次)⇒ 欄位由他要什麼決定,不由我們決定 | **決定** | 🟡 是(而他可能要先問記帳士) |
| 30 | 月報表 | **決定報表上有哪幾個數字**。而它與 `#24` 很可能是同一個消費端 ⇒ **兩題應該一起問** | **決定** | 🟡 是 |
| 18 | 退貨收回 | **決定「退貨」在 PCM 是什麼流程**(客人寄回 → 收貨 → 檢查 → 退款?還是換貨?)。<br>🔴 而它會**動到一道已經在跑的閘**:取消區現在逐字說「已到貨的部分不能在這裡取消,**而退貨功能目前還沒有**」⇒ 做了之後那句話要改,而**改它就是改那道閘的語意** | **決定** | 🔴 是 |
| 32 | 會計收支帳 | 🔴 **它已經被拍板延後了**(Sean `Q3=C` 都先不做會計)⇒ 第一步不是「做」,是**他決定什麼時候重開**。<br>⚠️ 而重開時**順序與直覺相反**:memory 記著「**薪資 domain 完全不存在**,而記帳士主要交付物 D1『扣繳匯總』25 欄主體是薪資扣繳」⇒ **薪資是主體、收支帳是配角**,不可再照「先做收支帳」排序 | **他重開的時機** | 🔴 是 |
| 33 | 成本精算報表 | 🔴 **它的第一步是 `#31`。** 沒有成本欄就沒有精算。**兩件其實是一件的兩半** | **等 `#31`** | 🔴 是(隨 `#31`) |
| **31** | **成本** | 🔴🔴 **第一步是【推翻一個已經落地的安全決定】,不是寫功能。**<br>`20260602135934` 加了 CHECK 硬擋 `cost` 寫進 metadata,**同一支 migration 還把既有的 cost 值刪掉**(`:81-85`);而 Sean `N1` 逐字:員工**看得到經銷價、看不到成本**。<br>⇒ **這件事不是「排進去就好」** —— 它要先答「誰看得到成本」,而那是權限設計,不是一片 UI | 🔴 **他的決定** | 🔴🔴 **是,而且只有他能答** |

### 🔴 而這張表最有用的一格是最右邊那欄
```
7 個「真的整條沒做」+ #31, 一共 8 件
其中【第一步就是寫 code】的:0 件
              是【決定 / 取得樣本 / 等他重開】的:8 件
```
⇒ **明天早上他答完那幾題,我們立刻有得做;而在他答之前,寫 code 這件事對這 8 格【一格都推不動】。**
📌 而其中 `#23` 最便宜:**它只要一份檔案**,不要任何判斷。

⚠️ **這張表的效度限定**:「第一步」是**我從現況推的**,不是任何人拍板的。
🔴 尤其 `#24` / `#30` 我把消費端猜成**記帳士**(依據是 memory 記的「401 由記帳士報」)——
**那是推的,不是問來的。** 若消費端其實是別人(供應商?Sean 自己?),那兩格的第一步會變。

### 結論:**13 個 ❌ 裡,只有 7 個是真的「整條沒做」**
```
🧩 備好了沒人接   5 個（#4 #11 #12 #28 #29）
❌ 真的整條沒做   7 個（#18 #21 #23 #24 #30 #32 #33）
🚧 刻意反向        1 個（#31）
```
🔴 **而那 5 個 🧩 原本與那 7 個混在同一個 ❌ 裡** —— 讀表的人看到 12 個 ❌,
會以為「還有 12 條要從零蓋」,而**其中 5 條缺的只是一段接線**。

📌 **而 `#31` 那格最值得單獨看**:它讀起來像「還沒做」,而 DB 正在**主動拒絕**它。
⇒ **一個「沒做」的功能,可能是有人做了功夫讓它不能做。** 估工之前要分得出這兩種。

⚠️ **這一節的效度限定**:上面每一格都是**字面掃描 + route 清單**,不是「按下去」。
🔴 **今晚的教訓是字面掃描會低估**(`#27` 被記成「連讀取 API 都沒有」而整頁都在)⇒
**要動任何一格之前,先照它那一欄的量法自己重跑一次,並且【真的去點一次那條路】。**


## 1. 驗收標準:員工的一天

每一片實作都要能回答「這片讓員工多做完哪一件事」。答不出來 → 延後。

| # | 員工要做的事 | 現況 | 證據 |
|---|---|---|---|
| 1 | 看今天要處理什麼 | ❌ 無待辦檢視 | admin 無 saved views |
| 2 | 看訂單明細 | ✅ | `order-detail.tsx` 4 卡 |
| 3 | 訂單寫備註 | ❌ orders 無備註欄 | `pcm-order-inventory.md §1.1` |
| 4 | 標商品進度 | ❌ **列表入口已於 A11a-1(2026-08-06)下架**;明細頁逐品項採購表單(A10b)仍可用,批次要等 A12b | ~~`item-workflow-status-cell.tsx`~~ → `item-procurement-*`(明細頁) |
| 5 | 標「已向供應商下單」 | ⚠️ 整單一個、無法只標一項 | `fulfillment_status` 在 orders 表 |
| 6 | 記供應商單號/預計到貨 | ❌ | 無欄位 |
| 7 | 缺貨要等 | ❌ 無此狀態 | `fulfillment_status` 4 值 |
| 8 | **輸入快遞單號** | ❌ | orders 無 tracking 欄 |
| 9 | **單號追蹤** | ❌ | 同上 |
| 10 | 列印出貨單/揀貨單 | ❌ | 無 |
| 11 | Email 通知客人出貨 | ⚠️ 骨架在、此觸發點未接 | `email_outbox`(M-4a B 線) |
| 12 | **手動建單(非網站商品)** | ❌ | `order_source` 三 manual 值只用於篩選 |
| 13 | 改訂單內容(品項/數量/金額) | ❌ 只能改出貨方式+發票 | `order-edit-form.tsx:90` |
| 14 | 刷卡收款 | ✅ 已上線 | TapPay 3DS,首筆 2026-07-24 |
| 15 | **匯款確認收款** | ❌ 無流程 | `payment_channel` 有 bank_transfer 值 |
| 16 | 今日對帳 | ❌ | 無 |
| 17 | **退款操作** | ❌ 帳本在、UI 全無 | `grep order_refund apps/admin/src` 零命中 |
| 18 | 退貨收回 | ❌ 無「貨的軸」 | 無 return 表 |
| 19 | 取消訂單 | ❌ **取消動作不存在** | `order-detail.tsx:233-239` 唯讀顯示;全樹零 `cancelled_at` 寫入路徑(2026-07-29 A0c) |
| 20 | **新增/編輯商品** | ❌ **完全不存在** | admin 無 products route |
| 21 | **上傳商品圖片** | ❌ | 圖片是供應商 CDN URL |
| 22 | 改價格/上下架 | ❌ | 同 20 |
| 23 | 匯入供應商 Excel | ❌ admin 內無 | 只有 repo 根 `scripts/rpm-*.ts` CLI |
| 24 | 匯出訂單/商品 | ❌ | 無 |
| 25 | 改客人資料 | ⚠️ 只能改等級+儲值金 | `customers/[id]` 二表單 |
| 26 | 員工各自帳號與權限 | ❌ **操作者自選、系統不驗證** | `staff.ts:3-4` 自陳非授權邊界(E8-A1 後名單已在 DB;2026-07-29 A0c) |
| 27 | 誰改了什麼 | ⚠️ 有 log 表、無 UI | `admin_audit_log` |

**✅ 2 項 / ⚠️ 5 項 / ❌ 20 項。**(🔴 **2026-07-29 A0c 對帳更正**,Sean 同日拍板 Q2=A:第 19 列 ⚠️→❌(取消動作根本不存在、不只是原因欄自由文字)、第 26 列證據行過期改寫(狀態仍 ❌)。前值 2/6/19 出自 2026-07-26 fresh read-back;更初版誤寫 3/6/18。✅ 只有第 2 列「看訂單明細」與第 14 列「刷卡收款」。逐列證據見 `docs/reviews/2026-07-29-e10-a0c-27item-reconcile.md`)

### 1.1 🔴 Sean 2026-07-25 第二批補充(讀完 27 項後提出)

**三項是對上表的修正(原判讀不足)**

| # | Sean 逐字 | 修正後要求 |
|---|---|---|
| 4′ | 「可以一次多個」 | 商品進度要能**批次標記**(勾選多列一次改),非逐列點 |
| 5′ | 「這屬於商品狀態,可以一個商品一個商品標記」 | 「已向供應商下單」**屬 item 層商品狀態**、逐品項可標。🔴 與現況矛盾點:`fulfillment_status` 在 **orders 表(整單一個)**,item 層只有 `workflow_status` 文字碼 ⇒ 供應商下單進度要落 item 層,不能再靠訂單層 enum |
| 25′ | 「可以所有會員資料」 | 會員全欄位可編(姓名/電話/Email/生日/地址/愛車),非只有等級+儲值金 |

**六項是全新需求**

| # | 需求 | 性質 | 備註 |
|---|---|---|---|
| 28 | **會員密碼重設** | 後台觸發 | 走 Supabase Auth reset flow;🔴 admin 不得直接設密碼、只能寄重設信 |
| 29 | **Email 通知功能**(後台對客人發信) | 接既有 outbox | 與 M-4a B 線 email_outbox 併用、不另起管道 |
| 30 | **月報表** | 新功能 | 內容待定義(見 N10) |
| 31 | 🔴 **成本功能** | **新資料 + 權限分級** | **僅管理者可見**。⚠️ 推翻我前一則「成本不存在故無人可算毛利」的結論——Sean 要求加進來 |
| 32 | 🔴 **會計收支帳(公司現金/銀行)** | **全新領域** | 見 §1.2 |
| 33 | **成本精算報表** | 管理者專屬 | 依賴 31 |

### 1.2 🔴 會計收支帳(Sean 逐字:「公司還有收入、支出等會計帳要能 key」)

**動機(Sean 逐字)**:「因為我們也會放部分現金在公司,會有支出、收入要填入」= 公司備用金(petty cash)+ 銀行帳戶需要有帳可記。

| 方向 | 科目(Sean 逐字列舉) |
|---|---|
| **支出** | 匯款、現金 |
| **收入** | 額外的現金收入、匯款收入、刷卡收入 |

🔴 **Sean 2026-07-25 澄清(我原先理解有偏差,已更正)**:他列的「現金/匯款/刷卡」**不是科目,是收付方式**。
逐字:「我是說額外支出、收入會有現金、匯款、刷卡,這部分是紀錄**公司的其他支出收入部分**」。

⇒ **會計帳的三個維度(建模結論)**:

| 維度 | 值 | 來源 |
|---|---|---|
| **方向** | 收入 / 支出 | Sean |
| **收付方式** | 現金 / 匯款 / 刷卡 | Sean 逐字 |
| **科目** | 🔴 **待定義**(Sean:「內容要補上」) | 依台灣標準科目表(研究②)提草案給 Sean 確認 |

另需**帳戶餘額**維度:公司備用金(現金)餘額 + 銀行餘額,由分錄累加。

**N11 ✅ Sean 拍 A**:訂單刷卡收入**自動由訂單帶入**、不手 key(避免重複記帳);
**其他收支**(非訂單的:房租、運費、雜支、額外現金收入…)手動 key,每筆標收付方式。

### 1.2a 🔴 codex 第二意見(2026-07-25 唯讀審;推翻上表部分結論)

**總判定逐字**:「規格不宜直接以『三維度+單式流水帳』施工。Sean 已說要正規化,這會和商業會計法的權責發生與雙式簿記要求衝突。」

| # | codex 結論 | 我方處置 |
|---|---|---|
| C1 | **單式流水帳不能當正式會計帳核心** — 現金轉銀行、刷卡待繳卡費、供應商先到貨後付款、老闆增資提款**都不是單純收支**,單式會漏掉資產/負債/權益那一端 | ✅ **採納**。核心改 `journal_entries`(傳票)+ `journal_lines`(借貸分錄,每張傳票借貸必相等);**員工只選「收入/支出/轉帳」模板,系統自動產分錄、員工不手選借貸** |
| C2 | **「現金/匯款/刷卡」不能只是一欄收付方式** — 「銀行 A 轉備用金」不是收入也不是支出;刷卡購買先形成應付卡款、扣款日才減銀行餘額 | ✅ **採納,修正 §1.2 三維度**:改為 `fund_account_id`(零用金/各銀行/信用卡清算)+ 選填 `payment_instrument`;表單明示「從哪個帳戶 → 到哪個帳戶」 |
| C3 | **日期不能只留一個** — 權責認列日、實際收付日、發票開立日、申報歸屬期可能全不同 | ✅ **採納**:`occurred_on` / `paid_on` / `invoice_issued_on` / `filing_period`;期別系統預填,**過帳後不可改,修正一律走沖銷傳票** |
| C4 | **要導出 401 還缺稅務與憑證維度**;且指出**本規格 §7「不做稅額拆分」與 401 目標自相矛盾** | ✅ **成立、是我的錯**,§7 已改。每筆憑證需:`document_type`/`document_number`/`document_date`/`counterparty_tax_id`/未稅額/稅額/含稅額/`tax_category`/`input_creditable`/不得扣抵原因/`filing_period`/附件與保管狀態 |
| C5 | **輸入不該是逐筆大表格,應為「三步記帳卡」** | ✅ **採納**(見 §1.2c) |
| C6 | **`orders.invoice_*` 三欄承擔不了正式發票生命週期** — 無開立日/稅額/稅別/期別/字軌/唯一性,也無「已開立卻部分退款」的多張折讓單模型 | ✅ **採納**:訂單上保留「發票摘要」快速看,另立 `sales_invoices` + `invoice_adjustments`(一訂單可多張正式文件、折讓單連原發票與退款紀錄) |
| C7 | **退款不能只改退款狀態,要建立發票待辦與稅務證據鏈** | ✅ **採納**:退款成功自動建「待判定:作廢或折讓」工作項,保存原發票/折讓發生日/買方類型/同意證據/提交時間/平台回覆與失敗原因 |
| C8 | **進項發票不能只做供應商+號碼+金額** | ✅ **採納**:另立 `purchase_documents`(連供應商/採購/付款/附件 + 可扣抵判定與原因 + 進貨/費用/固定資產分類) |
| C9 | **範圍:先做員工上工,不要先做自製完整會計系統**;建議序 **E8 權限 → E11 積木 → E10 訂單閉環**;E14 先做**會計師訪談與資料模型**、不先做單式帳 | ✅ **採納**(與主對話判斷一致、獨立得出) |
| C10 | 🔴 **第一期只做「可匯出給記帳士的憑證登錄簿 + 附件 + 發票/退款待辦」,不得宣稱已能自行產出正式 401** | ✅ **採納,列為 E14 第一期範圍上限** |

⚠️ **codex 自標需查證**:B2C 2 日 / B2B 7 日折讓傳輸時限「上線前再由會計師或財政部最新公告逐字查證」;哪些項目實際可扣抵、手開發票退款 SOP 亦同。

### 1.2c 🔴 三步記帳卡(codex C5;Sean 問的「更好的顯示方式」)

**首頁三顆大按鈕**:「記收入」/「記支出」/「帳戶轉帳」。點入後三步:

1. 日期(預設今天)+ 對象/摘要
2. **常用情境卡**(房租、運費、進貨、其他收入、銀行轉現金…)← 🔴 情境卡內容取自 Sean 試算表**實際用過的用途字串**,非教科書科目
3. 從/到帳戶 + 含稅總額 + 憑證資料

**底部即時顯示**「將增加/減少哪個帳戶、稅額多少」。🔴 **員工不手選借貸。**

**發票區塊條件式展開**:先問「有統一發票/憑證嗎?」→ 有:展開號碼/賣方統編/日期/稅別/未稅/稅額;無:**強制填原因並標示「不可扣抵」**。附件上傳後才能過帳。

### 🔴 1.2d 項目/說明二分(Sean 2026-07-25 拍板)

**Sean 逐字**:「支出有項目可選,內容我可以自己輸入文字即可,系統負責統計」。

| 欄位 | 型態 | 用途 |
|---|---|---|
| **項目**(科目) | **下拉選單、受控詞彙** | 🔴 **統計唯一依據**;不可自由輸入 |
| **說明**(摘要) | 自由文字 | 給人看的備註,**不進統計** |

**為何非這樣不可(實證,非設計偏好)**:Sean 現行 Google 試算表用途欄為自由文字,實測兩通道共 **158 個相異字串**,同一對象被拆成多種寫法——
`FEDEX`/`Fedex`/`fedex` 三種大小寫並存;`新竹`/`新竹物流`/`新竹貨運`/`新竹5月`/`05月新竹物流` 同一家拆 5 種;
`DHL` 與 `DHL␣`(尾隨空白)、`711寄件` 與 `711␣寄件` 被當成不同字串;
用途欄同時混用**供應商名 / 動作說明(補、補錢、校正) / 人名**三種語意;
**無退款標示欄位,全靠金額正負號隱含判斷**。
⇒ 現況無法產出可信統計。項目受控 + 說明自由 = 保留 Sean 的輸入自由度,同時讓統計成立。

🔴 **另需獨立欄位**:`is_refund`(是否退款/沖回),**取代現行「靠正負號隱含判斷」**。
⚠️ codex 另指出 `is_refund` 布林對 401 定位不足(銷項減項 17-19 / 進項減項 40-43 是不同列)⇒ **報稅面由 `document_type` 承擔、`is_refund` 僅供內部統計**。

#### 🔴 科目草稿(Sean 2026-07-25 已接受、先前只存在對話中,此處補落檔)

Sean 原話:「**我不知道草稿有哪些**」→ 主對話回覆「先用這份上線,有『其他』+ 自由文字兜底不會卡住;
用一陣子後哪個常被寫成『其他+某某』就升格成正式項目」→ Sean 未反對,**視為採用**。
🔴 **本清單由 Sean 試算表實際用過的字串歸類而來,非教科書科目**(來源:`research-2026-07-25/real-data/purpose_tables.md` 158 個相異字串)。

**支出項目(11)**

| 項目 | Sean 實際寫過的字串 |
|---|---|
| 國際運費 | DHL / FEDEX / SKT / 順豐 / 安豐達 |
| 國內物流 | 新竹物流 / 新竹貨運 |
| 進口稅金 | KINEO稅金 / WRS稅金 |
| 進貨成本 | (月報表主表逐筆記的原價) |
| 補款/調整 | 補 / 補錢 / 校正 / 修正 |
| 勞健保 | 勞保 |
| 水電 | 電費 |
| 罰款/規費 | 電費罰單 |
| 辦公雜支 | 桶裝水 |
| 薪資 | — |
| **其他** | 🔴 品牌名類(WRS/BMW/猛健樂/G猛/EP/TWM)一律歸此,內容自由輸入(**Sean 逐字**:「就其他事項,歸類在其他就好,內容自己輸入」) |

**收入項目(3)** — Sean 逐字:「訂單客人來現場付款,收現金,剩下就寫其他」

| 項目 | 來源 |
|---|---|
| 商品銷售 | 🔴 **訂單自動帶入、不手 key**(N11=A) |
| 現場收現 | 客人來店付現 |
| 其他 | 自由文字說明 |

⚠️ **後續研究補充、尚未經 Sean 確認的科目**(真實報稅資料揭露、開工前需 Sean 過目):
利息收入(非營業收益)、利息支出、租金支出(需扣繳)、執行業務報酬 9A(需扣繳)、
人事線整組(薪資/伙食費免稅應稅分/加班費免稅應稅分/年終獎金/獎金/勞保/就保/職災/工資墊償/健保/
投保單位負擔補充保費/勞退雇主提繳 6%)、所得稅費用、稅捐、**滯納金與行政執行費須獨立科目**
(勞保 9 個費用代號、勞退 3 個、健保 14 個類別都把它們分開)。

### 🔴 1.2e-2 成本公式實證(Sean 2026-07-25 提供真實截圖 + 匯率表,主對話驗算確認)

**Sean 現行月報表的實際算法**(欄位順序:數量/原價/運費/稅金/幣值/總計/小記/利潤):

```
總計(成本 TWD) = (原價 + 運費 + 稅金) × 有效匯率
利潤            = 小記(售價 TWD) − 總計(成本 TWD)
有效匯率        = GoogleFinance("CURRENCY:"&幣別&"/TWD") × 1.04
```

**驗算(主對話親算,非引述)**:689.6 EUR→26413 得匯率 38.302;97.3→3727 得 38.304;1101→42171 得 38.303;
233 CNY→1158 得 4.970(與表列 4.97 一致);452 JPY→93 得 0.206(與表列 0.21 一致)。**五筆全部一致。**
利潤欄:31000−26413=4587 ✓、50600−42171=8429 ✓、10000−6822=3178 ✓、3300−1451=1849 ✓。
**小記(售價)留空時利潤 = 負成本**(實例 −859)⇒ 有成本無售價視為純虧損,系統照抄此行為。

**表列匯率**(2026-07-25 當下值,會浮動):EUR 38.30 / GBP 44.84 / USD 33.65 / AUD 23.48 / CNY 4.97 /
JPY 0.21 / THB 1.00 / TWD 1.00 / IDR 0.00187812(🔴 **IDR 需 8 位小數精度**,其餘 2 位;金額型別選擇須容納)。

🔴 **與 §1.2b 記載的 `x*0.63*45*1.15` 是兩套不同東西**:後者是 Eazi-Grip 單一供應商的**進價換算**(供應商折扣 0.63 × 匯率 45 × 加成 1.15),
前者是**全站通用成本表算法**。⇒ §1.2b 的「曾低估 17%」教訓**只適用於 Eazi-Grip 那條線**,不可套用到本通用公式。

### 🔴 1.2e-3 匯率快照(本規格最關鍵的一條資料正確性要求)

**Sean 拍板(Q=C)**:`1.04` 是**匯率加成係數**(Sean 逐字:「避免虧損用」),**會改但很少改** ⇒ 做成**後台可設定參數**。

🔴 **兩個值都必須快照,不是只有匯率**:

| 存什麼 | 為何 |
|---|---|
| `fx_rate_raw` | 下單當下的 GoogleFinance 原始匯率 |
| `fx_markup` | 下單當下的加成係數(當時是 1.04 就存 1.04) |
| `fx_rate_effective` | 兩者相乘;**成本一律用這個算**,不即時重算 |

**理由(Sean 現行試算表的既有缺陷,搬進系統才修得掉)**:
GoogleFinance 回傳的是**「現在」的匯率**,試算表天生即時重算 ⇒ **去年的成本會隨今天的匯率變動而改變、歷史利潤持續漂移**。
同理,**係數一旦改成 1.06,若不快照則全部歷史訂單的成本會一起被改寫**。
⇒ 快照後:歷史單永遠用當時的數字;新單抓當下即時匯率 × 當下係數。

⚠️ 係數目前設計為**全站單一參數**(Sean 現行做法即如此,所有幣別共用 1.04)。若日後需逐幣別不同,再擴充、不預先過度設計。

### 🔴 1.2e 成本結構(實證修正,推翻我方原寫法)

Sean 月報表主表**已在逐筆記成本**,欄位實為五格:

**原價 + 運費 + 稅金 + 幣值 = 總計**

⇒ 規格 §1.2b 原寫「逐品項成本快照」**過於籠統**,應直接對齊此結構:
`cost_base`(原價)/ `cost_freight`(國際運費)/ `cost_duty`(進口稅金)/ `cost_currency`(幣別)/ `cost_total`。

🔴 **連帶推翻 §7「不做多幣別/匯率」**:Sean 現行成本表已有**幣值欄** ⇒ 匯率是既有事實、不是新需求。
成本以外幣計價、售價以 TWD 計價,**匯率快照必須存**,否則歷史毛利算不回去。

⚠️ **數據矛盾待 Sean 確認(實測)**:月報表 row4「本月支出運費」摘要格與同頁明細加總**對不上**
(2025/07:摘要 11,833 vs 明細加總 88,782,差約 7.5 倍)。口徑不明,**在確認前不得沿用該摘要格當報表來源**。

**報表分兩層**(不可混成一張流水表):
- **員工首頁**:今日待辦、各資金帳戶餘額、未過帳/缺憑證/逾期折讓提醒
- **管理者期間報表**:起訖日 + 本月/雙月/季/年捷徑;分頁顯示損益、資金餘額、應收應付、401 彙整、發票異常
- 🔴 **成本與毛利必須 server 端依管理者身分過濾,不可只在前端隱藏**

🔴 **風險層級**:命中鐵則 12 ①錢 → 高風險片、對抗審查不降級。

---

## 1.2b 成本模型與報表(Sean 2026-07-25 拍板)

### 成本 = Q1=B 逐訂單品項快照

**拍板**:成本記在**每張訂單的每個品項**——「這次向供應商買實際付了多少」,員工向供應商下單時填。
Sean 理由(逐字):「也比較像我現在表格工作模式」。

🔴 **Sean 同時揭露的既有資產**:「**其實我們報價單都有成本公式,只是沒顯示成本而已**」→ 列 backlog、先走 B。

⚠️ **接報價單成本公式前的兩個硬前置**:
1. **跨 repo 需提案**:報價單是另一個 repo,依既有判準(memory `feedback_cross-repo-crossing-criteria`)= 唯讀隨時、**改動需提案**、預設不跨。
2. 🔴 **該公式曾被實證算錯**:memory `project_eazi-grip-agency-launch-decisions` 記載「成本公式低估 17%,真值 `x*0.63*45*1.15`」⇒ **搬過來之前必須先驗證公式正確性**,不可直接沿用。

### 報表 = Q3 全選 + 自訂區間

Sean 逐字:「**都要**,還有其他支出、收入的統計,到時候還要可以讓我**選擇幾月幾號到幾號**統計的金額這種」。

⇒ 🔴 **「月報表」正名為「區間報表」**:任意起訖日,月/季/年只是預設快捷鍵,不是唯一粒度。

指標清單(全數納入):營業額 / 毛利 / 訂單數 / 客單價 / 各品牌銷售 / 各員工處理量 / 現金與銀行餘額 / 應收未收 / 退款金額 / **其他收支統計**。

🔴 **毛利依賴成本** ⇒ 報表(E13)硬依賴逐品項成本快照;成本沒填的訂單毛利須顯示「未知」而非 0,**不得用 0 充數**。

---

## 1.3 角色定義(Sean 2026-07-25 拍板 N2=B)

| 角色 | 可見 | 可做 |
|---|---|---|
| **員工** | 定價、**會員經銷價** | 改價錢、退款、日常訂單與商品操作 |
| **管理者** | 員工全部 + **成本** | 員工全部 + **報表(含成本精算)** |

🔴 **成本是唯一的角色分界線**(Sean 明示);其餘操作權限兩者相同 ⇒ 權限模型可極簡:單一 `is_manager` 旗標 + 成本欄的 server 端遮蔽,**不必做細顆粒度 RBAC 矩陣**。
🔴 **經銷價對員工開放**,但**對一般會員 client 仍全擋**(既有三層防護不得放寬)。

---

## 2. 現況總表

### 2.1 admin app 全貌(2026-07-25 實查)

| 項目 | 數量 | 備註 |
|---|---|---|
| 業務頁面 | **5** | 訂單列表、訂單明細、客戶列表、客戶明細、訂單狀態設定 |
| 側邊選單項目 | **4** | 總覽/訂單/客戶/設定(`app-sidebar.tsx:23-28` 硬列,**不含商品**) |
| 共用列表頁抽象 | **0** | orders-table 210 行 vs customers-table 60 行,各寫各的 |
| 共用表單頁抽象 | **0** | 三個編輯表單各自手刻 `<form action>` + FormData |
| 共用 Modal | **0** | 走整頁表單/sheet,無 dialog |
| `@pcm/ui` 匯出 | **1** | 只有 `cascadeFilterReducer`,**不是元件庫** |
| admin server action | **10** | 訂單 5 + 客戶 2 + 設定 2 + actor 1 |
| 商品寫入路徑 | **0**(admin 內) | 唯一路徑是 repo 根 `scripts/rpm-*.ts` CLI |

### 2.2 資料層(已有,不必重建)

- **訂單域 8 張表**:orders / order_items / payment_charge_attempts / payment_double_charge_anomalies(+events)/ order_status_options / **order_refunds / order_refund_items**(2026-07-25 上線)
- **商品域 5 張表**:products / brands / categories / product_variants / product_fitments(+ product_image_trim)
- **會員域 4 張表**:customers / customer_addresses / customer_vehicles / customer_wallet_ledger
- **狀態軸**:`payment_status` 5 值、`fulfillment_status` 4 值
- **稽核**:`admin_audit_log`(tier/wallet 兩個 RPC 已同交易寫入)

### 2.3 已有的好地基(直接複製,不重新設計)

1. **授權閘**:`authorizeAdminMutation()`(`apps/admin/src/lib/session/authorize.ts:24-35`)三道——session 自驗 + Origin fail-closed + 具名 actor。所有新 server action 一律走它。
2. **RPC + 同交易稽核**:`admin_set_customer_tier` / `admin_adjust_wallet` 模式——窄權 SECURITY DEFINER、EXECUTE 只 GRANT service_role、寫 `admin_audit_log`。所有新後台寫入照抄。
3. **經銷價縱深防護**:`price_store` 三層擋(REVOKE ALL + 逐欄 GRANT / security_invoker view 排除 / RLS)。**新後台頁面不得繞過。**
4. **Email outbox**:比 Medusa 官方範例更進階(outbox pattern),接上即可,不重做。
5. **退款帳本**:`order_refunds` 形狀(冪等鍵、複合 FK、DEFERRED 一致性 trigger)——新表照鏡。

---

## 3. 三個結構性決定

### 3.1 ~~訂單:計數器是真相,狀態是顯示~~(🔴 R14:整節已被 E10 v2 取代)

> 🔴 **本節模型已 superseded**:E10 v2 §1 原則 2 —— **真相在明細表**(`order_item_procurement` / `order_cancellation_items`),計數器只是 **trigger 推導的摘要**、且**分軸隨模型增量**(不一次補齊);不變式見 v2 §5.1c。以下原文保留備查。

~~**三家平台一致(Shopify / Medusa / Odoo),且與 PCM 現行做法相反。**~~

Medusa `OrderItem` 每列 8 個數量欄(`packages/modules/order/src/models/order-item.ts`);Shopify `ReturnLineItem` 6 個數量欄 + `RefundLineItem.quantity`;Odoo Reverse Transfer 數量可編輯。

> **這解掉「一單三列退兩列」與「一列 3 個退 1 個」**:不發明狀態值,只記數字。也避開「付款 3 × 出貨 4 × 退貨 3 = 36 格」組合爆炸。

**PCM 目標**:`order_items` 加計數器欄
```
quantity(已有) / ordered_quantity / instock_quantity / shipped_quantity
/ return_requested_quantity / return_received_quantity / cancelled_quantity
```
不變式用 CHECK 鎖:各值 ≤ quantity,且 ordered ≥ instock ≥ shipped 逐級遞減。

~~🔴 **反面教材**:Medusa 把 payment/fulfillment status 做成純衍生值,結果官方 admin **無法用這兩軸篩選排序**(GitHub issue #14095,仍 open)。**PCM 必須維持實體 enum 欄 + 索引**,由 trigger 從計數器同步。~~
> 🔴 **本段已被 Sean 2026-07-28 拍板 Q1=B 推翻、由 E10 總規劃 v2 取代**(`2026-07-28-e10-order-closure-master-plan-v2.md` §8.1):`orders.fulfillment_status` 凍結不維護、**不做** trigger 同步;篩選走品項層條件。理由:上文是研究結論非拍板,PCM 的篩選需求在品項層即滿足。

### 3.2 錢與貨分兩條線

Shopify `Return`(貨)/ `Refund`(錢,無自己的 status);Medusa `Return` / `OrderTransaction`;Odoo Reverse Transfer / Credit Note。三家都分離。

**PCM 目標**:新增 `order_returns` / `order_return_items`(鏡射既有退款帳本形狀),`order_refunds` 加 nullable `return_id`——**可以只退錢不收貨**。

「已出貨才退款、貨還沒收回」= 貨的軸上的中間態(Shopify 叫 Return in progress),與錢無關。

### 3.3 先做積木,再做頁面

現況:0 個共用列表/表單抽象,每頁手刻。要新增商品、供應商、庫存等多個域,**沒有積木就是把手刻重複 N 次**。

**先做**:`<AdminDataTable>`(篩選 pill / 分頁 / 選取 / 批次列 / 手機轉卡片)、`<AdminForm>`(主側兩欄 / sticky 儲存列 / 雙層錯誤 / 未存離開警告)、`<AdminModal>`、統一 toast。

這一步不新增任何員工功能,但**後面每個域都省一份**。

---

## 4. 通用 UI 規範(全後台一致)

來源:Polaris 官方規範 + Odoo demo 真瀏覽器實測(截圖 `scratchpad/shots2/`)。

1. **手機版列表一律轉卡片**,不做橫向捲動表格。主要欄位加粗置頂、次要副行、金額/狀態靠右。(Sean 常用手機遠端操作;現行訂單列表 13 欄在手機上不可用)
2. **已套用篩選用可個別移除的 pill** 顯示在列表上方,支援單一移除 + 全部清除,不藏進彈窗。
3. **表單錯誤雙層**:頂部 critical banner 總結 + 移焦點,同時每欄 inline error。只做一種使用者找不到錯在哪。
4. **批次操作**:選取後動作按鈕**內嵌替換列表標題列**(Polaris + Odoo 都這樣),不做浮動 bar。跨頁全選需獨立文案。
5. **分頁**:>50 筆才出現;用「共 N 筆 + 上一頁/下一頁」,不做頁碼輸入框。
6. **破壞性動作**必須二次確認;**未儲存變更離開**必須警告。
7. **Toast 5 秒**(🔴 **Sean 2026-07-26 拍板,由原「至少 10 秒」下修**;原值來源=Polaris 無障礙建議)。**下修的代償條件(不可省)**:①訊息必須可手動關閉 ②**錯誤類訊息不自動消失**、只能手動關 ③破壞性動作結果不靠 Toast 傳達(走二次確認與頁面狀態)。⇒ WCAG 2.2.1 要求的是「可調整/可暫停/可關閉」而非固定秒數,滿足上述三條即成立。

---

## 5. 領域規格

### 5.1 訂單(補完 §1 的 1-19 項)

- `order_items` 加計數器(~~§3.1~~ 🔴 R15 改連 **E10 v2 §1 原則 2 與 §5.1c**:真相在明細表、計數器 = trigger 推導摘要、**按軸分批增量**,不一次補齊)
- 新增 `order_returns` / `order_return_items`(§3.2)
- ~~`orders` 加:`note`、`internal_note`、`tags`、`tracking_*`、`shipped_at`、`delivered_at`、`cancel_reason`~~
  > 🔴 **本行已被 E10 v2 取代**(原則 3:`orders` 對登入客人整表 SELECT 全開 ⇒ 內部備註/追蹤/取消原因**一律進 service_role-only 新表** `order_notes` / `shipments` / `order_cancellations`;`delivered_at` 因 U7 override(送達完全不做)不建)
- ~~`fulfillment_status` 加 `delivered` + `backorder`~~
  > 🔴 **已被 Q1=B(不加任何 enum 值)與 U7 override(不做送達)推翻**,由 E10 v2 取代
- **手動建單**:`order_source` 三個 manual 值已備,補建單表單。~~🔴 「非網站有的商品」需先驗證 `create_order` RPC 是否允許自由品項~~
  > 🔴 **R13 更新:已驗證、確認不可用**(E10 v2 §3 C2:`:284` `auth.uid` NULL 直接 exception、`:356`/`:360` 品項必須是既有 catalog 變體)⇒ **第 3 批另建 admin 專用建單 RPC**(E10 v2 §5.3 項 10,六題開批閘)
- **匯款收款**:~~`payment_channel='bank_transfer'` 已備~~(🔴 R15:該欄只是**預期管道**、不是實收真相)⇒ **實收與稽核唯一來源 = E10 v2 第 3 批 `order_payments` ledger 與其窄 writer**,「確認收款」= 對 ledger 記腿
- **訂單改單**:抄 Medusa `OrderChange` 的「逐動作 event log + 版本號」,不是存改前改後兩份

### 5.2 商品(§1 的 20-24 項,**從零開始**)

抄 Medusa 商品編輯頁版面(原始碼可驗):
- **主欄**:基本資料 → 圖片 → 規格選項 → 變體
- **側欄**:上架狀態 → 運送設定 → 分類歸屬 → 屬性

重點功能(依 PCM 槓桿排序):
1. **變體矩陣批次生成**(設定選項 → 一鍵生成所有組合 → 批次改價格/庫存)—— PCM 規格變體量大,槓桿最高
2. 圖片上傳 + alt text(現在只有供應商 CDN URL,無上傳)
3. 商品狀態(草稿/上架/下架/封存)
4. 供應商 Excel 匯入 UI(把 `scripts/rpm-*.ts` 包成後台功能)
5. 匯出 CSV

決策點:**圖片能否跨商品重用**(Shopify 有媒體庫、Medusa 綁死單一商品)→ **N6 ✅ 已拍「要共用 → 做媒體庫」**(07-26;R14 更正此處殘句,權威 = §6 N6 列)。
⚠️ 本檔有兩套題號並存,勿混:**Q1-Q6** = 既有 plan `site-wide-gap-and-admin-platform-plan.md` §9 的題(**全部已拍板**,見 §0.1);**N1-N9** = 本檔 §6 新增的題。此處指的是 **N6**。

### 5.3 客戶

補:改姓名/電話/Email/地址/愛車(現在只能改等級+儲值金);客人訂單歷史嵌入明細頁;標籤與備註。
分級經銷價的規則引擎參考 Medusa `PromotionRule`(`customer.group.id` 條件式)而非 Shopify Segment。

### 5.4 供應商 / 採購(PCM 代購本質,兩大平台都沒有)

🔴 **現在沒有 `suppliers` 表**,供應商只是 `products.supplier_slug` 一個字串欄。
抄 Odoo:供應商主檔 + **供應商商品價目表**(Vendor / Product / 量階 Quantity / Unit Price / 有效期 / 折扣%)。
代購狀態鏈參考 AutoDS:待處理 → 已向供應商下單 → 供應商已出貨 → 已送達(+異常)。

### 5.5 內容 / 媒體

- 通用「自訂內容型別」(Metaobject 概念)—— 現在品牌介紹、安裝資源各刻一套,每加一種內容就再刻一次
- 媒體庫(搜尋 / alt text / 重用)
- 🔴 **301 轉址管理表** —— PCM 有改 handle 前科,漏了就是安靜的 SEO 流失

### 5.6 設定 / 權限

🔴 **現況(2026-07-29 A0c 重驗;原字面引 `staff.ts:3` hardcode 名單已於 E8-A1 過期)**:`staff.ts:3-4` 逐字「M-4b E8-A1:staff 名單改由資料庫提供,但操作者仍是使用者自行選擇。🔴 這不是登入 / 授權邊界」——名單已在 `public.staff` 表、`/settings/staff` 可維護,但**員工身分是自己從下拉挑的、系統不驗證**。Sean 的北極星要求多員工使用 → **這是硬前置**(結論不變,只是缺口從「名單寫死」變成「身分未認證」)。
Medusa 官方**沒有 RBAC UI**(要自兜);Shopify 是 19 大類細顆粒度權限。PCM 取中間:角色制。
最關鍵決策:員工看不看得到成本與經銷價 → **N1,已於 2026-07-25 拍板:見經銷價、不見成本**(見 §6 與 §1.3)。~~待決~~

---

## 6. ~~待 Sean 拍板~~ 新增題 N1-N9 拍板紀錄(🔴 R14 改名:**九題已全數拍板**,本節是紀錄不是待辦)

🔴 **已在 §0.1 拍板的六題不重問。** 以下 ~~8 題~~ **9 題(N1-N9)**是既有 plan **未涵蓋**的新決策,**全部已拍**、逐列附權威出處。

| # | 題目 | 為何重要 | 狀態 |
|---|---|---|---|
| N1 | 員工看不看得到成本/經銷價 | 改架構;經銷價三層防護的邊界要重畫 | ✅ **看得到經銷價、看不到成本**(Sean 07-25;非原選項 A/B/C,為第四種組合)🔴 連動:①`price_store` 需開一條 admin-only 讀取路徑(現行三層防護對 client 全擋、admin 走 service_role 本就可讀 → 實作上是 UI 決定要不要顯示,非改防護)②**成本目前全站不存在**:`products_metadata_no_sensitive` CHECK 主動擋 `cost`/`source_amount`/`source_currency` 寫入(`20260602135934:88-90`)⇒ 「看不到成本」自動成立,但也代表**任何人都算不出毛利**,若日後要算需另片設計成本欄與其防護 |
| N2 | 幾個員工、要不要分角色 | 決定 E8 權限模型複雜度;`staff.ts` hardcode 是上線硬前置 | ✅ **B 兩級**(Sean 07-25 二次回覆確認;首次回「fb」非 A/B/C、已回問)。定義見 §1.3 |
| N3 | §1 的 27 項驗收清單還漏了什麼 | 北極星完整性;漏的項目上線後才發現最貴 | ✅ **已答並落檔**:Sean 原回「沒看到 27 項」(清單當時只在本檔、手機讀不到)→ 對話中全文列出後,Sean 給 **3 項修正 + 6 項新增**,**全部已寫入 §1.1**(4′/5′/25′ 修正 + #28-#33 新增)。此列不再是開放項 |
| N4 | E11 積木先做,還是邊做功能邊長 | 先做=慢起步快後續;邊做=快起步但每 epic 重刻一次 | ✅ **A 先做積木**(Sean 07-25) |
| N5 | E10 訂單後台插在 E0-E9 的哪裡 | 既有順序無訂單 epic,但北極星有 12 項落在訂單域 | ✅ **可先做,但附條件**(Sean 07-25 逐字:「目前網站訂單都是假的,但是如果可以完整優化訂單功能到我目標狀態,可以先做」)🔴 兩個推論:①**條件是「做到目標狀態」= §1 清單全綠才算數**,不接受做一半 ②「訂單都是假的」= 目前無真實客流(真刷卡是 Sean 自測的 `PCM-2026-0102`/`0104`)⇒ **改動訂單域的風險窗比想像小**,但既有資料(~~34 筆~~ 🔴 R15:歷史快照數字,現況以 E10 v2 的 A0a 重驗為準)與已上線金流鏈仍不得破壞 |
| N6 | 圖片能否跨商品重用(媒體庫 vs 綁死單一商品) | Q2 已定存 Supabase Storage,但**重用與否未定**;之後改很痛 | ✅ **要共用 → 做媒體庫**(Sean 2026-07-26 逐字「要共用」)。⇒ 圖片為獨立實體、商品↔圖片多對多;媒體庫需「被幾個商品引用」計數(刪圖前置守門)。🔴 **題號注意**:2026-07-26 對話中一度以「Q4」稱呼本題,與 §0.1 既有 plan 的 Q4(內容發布模型)撞號、已更正,**本題永遠是 N6** |
| N7 | 訂單改單要做到多細(能不能改品項/金額) | 涉及金流一致性與稽核;抄 Medusa 逐動作 log 成本較高 | ✅ **已拍(R13 補登)**:07-26 **U2「改全部」**(姓名/電話/地址/品項數量;直改極簡、差異寫時間軸、差額只記不動錢、已裝箱鎖定)——權威 = UX 審查 **§7** U2 列 + E10 v2 §4(復盤L1 修指標:§1 無 U2 條);落第 3 批改單線 |
| N8 | 現行 9 個商品狀態詞彙何去何從 | 新三軸上線後與它重疊;退場/並存/改語意三選一 | ✅ **已拍(R13 補登)**:07-27 **P3 九個壓縮碼退場、改三軸**(付款單層/訂貨·出貨品項層)——權威 = E10 v2 §4/§5.1a |
| N9 | E12 供應商主檔這期做不做 | 代購本質最缺,但會擴張範圍 | ✅ **已拍(R13 補登)**:07-27 **Q2=B 這期不做**(前提修正:報價單 `suppliers` 16 列是改裝品牌非下單對象)——權威 = handoff §3 |

🆕 **2026-07-26 第二輪 UX/營運可用性審查**(Sean 交辦「操作直覺性+客人狀況即時處理+資訊來回」;主對話 + codex xhigh 雙審)產出 **U1-U7 決策題**與直接折入清單,見 `docs/specs/2026-07-26-admin-ux-operability-review.md`(題號 **U 系列**,與本檔 N 系列、既有 plan Q 系列互不重疊;E10/E11 片級 plan 開工必引用該檔 §8)。

---

## 7. 不做(明確排除)

- ❌ 匯入整套 Shopify / Medusa 程式碼(ADR-0005 廢 Medusa-as-API 的理由仍成立)
- ⚠️ ~~多幣別 / 匯率~~ **此項已撤銷**(Sean 成本表既有「幣值」欄 = 匯率是既有事實、非新需求)。改為:**成本以外幣計價、售價 TWD,匯率與加成係數必須快照**,見 §1.2e-3
- ⚠️ ~~稅額拆分~~ **此項已撤銷(codex C4 抓到自相矛盾:要導出 401 就必須拆稅額)**。改為:**未稅額/稅額/含稅額與稅別為必要欄位**,見 §1.2a C4
- ❌ 銷售通路 / B2B Company 三層模型 / 詐騙評分
- ❌ 完整庫存管理(預留/盤點/多倉)—— 代購模式庫存概念弱
- ❌ **BI 式分析報表**(Medusa 核心也沒有、第三方 plugin 才有:自訂維度、下鑽、圖表產生器)
  ⚠️ **與 §1.2b「區間報表」不衝突,別搞混**:區間報表 = 固定指標清單 + 自訂起訖日,**要做**(Sean 拍板「都要」);此處排除的是可自由組維度的 BI 工具。

---

## 8. 未確認(禁止當事實實作)

- ~~`create_order` RPC 是否允許自由輸入品項~~(🔴 R14 移出:**已驗證、不可用** —— E10 v2 §3 C2;第 3 批另建 admin 專用 RPC、六題開批閘)
- Medusa `ReturnStatus` 完整 enum
- Shopify 非 display 版 `fulfillmentStatus` 是否存在
- Shopify 批次操作完整清單、Settings 完整分類
- Odoo PO line ↔ SO line 是否官方保證自動關聯
- 庫存異動歷史 schema(兩平台皆未查到)
- PCM 現有權限模型細節(`staff.ts` hardcode 之外是否另有機制)

完整未確認清單分散在各 research 檔末段,實作前逐項回查。
