# PRD · 優惠券 / 折扣碼(M-4b)

> ⛔ ~~**狀態:✅ Sean 2026-08-26 已答完(`Q10` 除外)。一行碼都還沒寫。**~~
>
> ## 🔴 2026-08-31 就地訂正 —— **上面那一行今天為假**
> 當場量(線【客人帳戶區】`pcm-wt-account`,2026-08-31 15:0x;`git ls-files` 讀 index、`wc -l` 讀工作樹):
> ```
> 後台優惠券已經存在的檔(9 支 / 合計 1,263 行, 非空):
>   apps/admin/src/app/coupons/page.tsx                        90
>   apps/admin/src/components/coupons/coupon-filter-bar.tsx    72
>   apps/admin/src/components/coupons/coupons-table.tsx       197  (+ 測試 156)
>   apps/admin/src/lib/coupons/coupon-list-view.ts            260  (+ 測試 171)
>   apps/admin/src/lib/coupons/coupon-repository.ts            33
>   packages/adapters/src/supabase/SupabaseCouponAdapter.ts   137  (+ 測試 147)
> 而 schema 那半也已 apply:20260829150000(兩張表)/ 20260829170000(後台清單 view)
>   帳本 supabase/APPLIED.tsv 兩支皆 =1 · ⚪ 負對照 '^20260831999999' ⇒ 0
> ⚪ 負對照 `git ls-files | grep -c zzqcoupon9137` ⇒ 0
> ```
> 🛑 **⇒ 「一行碼都還沒寫」在三份優惠券文件裡各寫了一次**(本檔、`2026-08-25-coupon-prd.md`、
> `2026-08-26-coupon-schema-plan.md`),而**碼與 migration 都已經在了**。
> 🔴 **真正還沒做的是【兌換 / 執行】那一半**:
> `INSERT INTO coupon_redemptions` 在 migrations ⇒ 0 · app 側 ⇒ 0
> (🟢 正對照 同把尺打 `order_refunds` 的 app 側 ⇒ 27 支 ⇒ 尺是活的)
> ⇒ 那三個上限今天是**給人看的數字, 不是會執行的規則**(板 `⟦b4-COUPONCAP1⟧`)。
> 🔴 **答案與連動見檔尾 §7 —— 下面 §1 的七題【原句不動】,而其中三題他選了與推薦不同的答案。**
> 來源:Sean 2026-08-24 逐字「優惠券／折扣碼:**開發啊,去做啊**」(memory `project_0824-sean-five-scope-rulings`)。
> 分級:**L3**(鐵則 9 —— 優惠券是週級以上的內容,必後台 CRUD)。
> 命中鐵則 **12①錢**(動訂單金額計算)、**12③schema**(新表)⇒ 對抗審查不降級。
> 產出者:施工窗 pcm-website-v2-1d(線 4 · 客戶與首頁面),2026-08-25。

---

## 0. 🔴 先更正一句:這不是「零基礎」

佇列與 08-24 memory 都寫「零 PRD 零 schema」。**PRD 零、schema 零 —— 這兩句對。**
**而「從零設計」那個形狀是錯的**,理由是量到的:

```
docs/reviews/2026-08-19-coupon-shape-draft.md    169 行   ← 已把大部分決策收斂到二選一
docs/reviews/2026-08-19-coupon-code-research.md  260 行   ← 已調研 Woo / Medusa 做法
量法:wc -l,2026-08-25 當場跑
```

**而 code 那一側是【零命中】,它要另外一把尺**(下手窗 pcm-website-v2-8d 複驗時指出:
0 檔在「真的沒有」與「我的 `--include` 把它濾光了」印同一個數 ⇒ 只有負對照不夠,
要一發**我知道答案的正對照**才證明尺會動【對】,不只是會動):

```
同一條指令、同一組 --include,只換那個字(2026-08-25 當場跑):
  grep -rli "order"    --include="*.ts" --include="*.tsx" --include="*.sql" . | grep -v node_modules | wc -l  ⇒ 978
  grep -rli "wallet"   同上  ⇒ 102
  grep -rli "discount" 同上  ⇒  73
  grep -rli "coupon"   同上  ⇒   0    ← 這一發才是宣稱
⇒ 三個正對照都回非 0 ⇒ 那把尺在這組 --include 底下抓得到東西
⇒ 「文件有 429 行」與「code 零命中」是兩個宣稱,各自量到
⚠️ 而這把尺只涵蓋 .ts / .tsx / .sql —— 若優惠券以別的副檔名或別的字命名存在,它抓不到
```

⇒ **本 PRD 不重做那兩份的工作**,只做它們沒做的:**把待決格收成 Sean 一個字答得完的題**,
並在他答完之後,把答案接成一份可以直接開工的規格。

**而 design 已經替我們定好客人那一面**(逐字,`design-reference/components/CheckoutPage.jsx`):

| 事 | 行號 | 逐字 |
|---|---|---|
| 大小寫/空白 | `:114` | `couponCode.trim().toUpperCase() === 'PCM100'` |
| 🔴 折抵順序 | `:94-95` | `beforeWallet = Math.max(0, subtotal + shipping - couponDiscount)` ⇒ **券先折、儲值金後折** |
| 結帳 state 欄位名 | `:63-64` | `couponCode` / `couponApplied` |
| 後端端點名 | `design-handoff/HANDOFF-v2.0.md:440` | `/api/coupons/validate` |
| 出現在幾個地方 | `:487-493` 輸入格 · `:540` 確認頁 · `:627-628` 金額列 | 三處都要顯示 |

⇒ 這五格 **不進決策題**(鐵則 1:design 是真權威、直接搬不翻譯)。

---

## 1. 🔴 決策題 —— 七題,每題一個字

> 照 §3 回報格式:每格 2-3 選項 + 推薦 + 一句理由。
> **推薦全部選甲時,可以只回一個字「甲」。**

```
Q1 折扣怎麼算
  甲 只做「折 NT$ N 元」          ← 推薦
  乙 折金額 + 打 N 折,兩種都做
  理由:design 現有的就是折金額(:94 hard-coded 折 500),甲能直接接上;
        乙多的是一個欄位加一段驗證,之後要加不必動已有資料。

Q2 使用次數上限
  甲 只做「總共可用 N 次」        ← 推薦
  乙 總量 + 每人可用 N 次,兩個都做
  理由:甲現在就做得動,而乙多一欄。
  🔴 **2026-08-25 深夜更正,原句留痕**:~~「而登入身分那條線還沒收工(線3)」~~ ——
     **那句在我寫下它的時候就已經不成立了。** Sean 當天**傍晚**已補上⚠️ **2026-08-26 更正**:~~`17:24`~~ 是 **memory 建檔時刻**(`stat %SB` ⇒ 2026-08-25 17:24:26),**不是事件時刻** —— 真正部署在他那句話前幾分鐘,**沒有人量過**;memory 正文 `grep '17:24'` ⇒ 0。⇒ 正確寫法:「2026-08-25 傍晚(memory 落檔 17:24:26)」。
     `ADMIN_REQUIRE_REAL_IDENTITY=1` 並重新部署(memory `project_0825-real-identity-gate-is-live`)
     ⇒ 身分**問得到**。⇒ **乙不再有前置阻擋,它只是「多一欄 + 多一段驗證」的工程量。**
     推薦仍是甲,**而理由換了**:不是「乙做不動」,是「甲先出、乙隨時補得上」。
  📌 同一個錯我在 `2026-08-25-saved-views-plan.md` 犯過一次(已由線3 指出、`e5579819` 更正),
     **而這一處是我自己回頭掃才找到的第二處** —— 只修被點名那一處會漏掉它。

Q3 有效期
  甲 只做「結束日」               ← 推薦
  乙 開始日 + 結束日都做
  理由:券建好就想讓它能用是常態;「預約未來才生效」可以先用「停用/啟用」代替。

Q4 🔴 能不能跟會員等級價疊加(這是錢,不是介面)
  甲 可以疊(經銷商也能用券)      ← 推薦
  乙 不能疊(有會員價就不吃券)
  丙 每張券自己一個開關
  理由:乙會讓經銷商看得到用不到、客服天天被問;甲毛利要自己抓,而
        「最低消費」那格(Q6)就是抓毛利的閘。丙最完整但第一版多一個欄位。

Q5 誰能建優惠券
  甲 所有員工都能建               ← 推薦
  乙 只有老闆能建
  理由:後台目前沒有角色分權 ⇒ 乙做不出來只能假做;甲 + 建立者留紀錄,等分權好了再收緊。
  量法(2026-08-25 當場跑):
        grep -rn "isAdmin\|userRole\|hasPermission\|role ===" apps/admin/src --include="*.ts" --include="*.tsx" | wc -l  ⇒ 0
        負對照(不同寫法):grep -rl "export" 同範圍 ⇒ 333 檔  ⇒ 尺會動
  ⚠️ 這把尺只量【字面】,量不到「用別的字命名的分權」;而線3 正在做身分,它的進度我沒查。

Q6 最低消費門檻
  甲 第一版就做                   ← 推薦
  乙 第一版不做
  理由:Q4 選甲之後,我想得到擋住「一張大券把小單折成 0 元」的閘就是它(**未窮舉,只是我想到的**),
        而它只是一個欄位加一個比較。

Q7 用過的券能不能刪
  甲 不能刪,只能停用             ← 推薦(這格我建議不要選乙)
  乙 能刪
  理由:訂單記著「折了多少」而不變式守著它
        (`packages/domain/src/order/errors.ts:20` 逐字:
         `total_mismatch:total ≠ subtotal + shippingFee − discountTotal`)
        ⇒ 券被刪掉,那筆折抵就變成一個沒有來源的數字。
  ⚠️ 未確認:「刪掉會不會真的讓那條不變式紅」我沒有實測,上面是讀不變式推出來的。
```

**還有一題,而它不是選項題,是要他知道一件事:**

```
Q8 你正在把優惠券和儲值金拆開,知道嗎?
   兩件當初是一起被凍結的(`apps/storefront/src/styles/cart.css:8` 逐字:
   「優惠券 .cart-coupon / -row / -ok 不搬(plan v6 §3.2「優惠券」不做、#202 wallet HOLD)」),
   而儲值金你 2026-08-18 說「現在不開」。
   甲 知道,券照做,儲值金維持不開   ← 推薦
   乙 等等,兩件一起再說
   理由:當初排除的理由已查到不是硬擋(`docs/specs/2026-06-04-m3-checkout-plan.md:78`
         逐字「原排除僅 Phase-1 省事非硬擋」)⇒ 拆得開,只是要讓你知道你在拆。
   ⚠️ 未確認:技術上 CSS 那兩塊耦合到什麼程度,還沒查。
```

---

## 2. 假如七題全選推薦,做出來長這樣

> **這一節在 Sean 答完之前不算數。** 他改哪一格,這一節就跟著改。

### 2-1 判準(不是「做多少」,是「員工能不能跑完一張券的一生」)

Sean 08-18 常設令「不准做一半」⇒ 具體化成一句可當場答的:

> **開券 → 客人用掉 → 用完了擋住 → 過期了失效 → 需要時停用。**
> 五步全通 = 完整,少一步 = 做一半。

七題全選推薦 ⇒ **五步全通。**

### 2-2 後台那一頁的欄位

```
【基本】
  優惠碼        員工自己打(Q1 甲之下不需要自動產碼器)
  說明          給自己看,例:雙十一活動
  建立者        自動記,不給改(Q5 甲的配套)

【折多少】
  折抵金額 NT$  Q1 甲 ⇒ 只有這一種

【什麼時候能用】
  結束日        Q3 甲 ⇒ 只有結束日

【能用幾次】
  總量上限 N 次  Q2 甲
  已使用 N 次    自動,唯讀

【門檻】
  最低消費 NT$  Q6 甲

【開關】
  啟用 / 停用    Q7 甲 ⇒ 沒有「刪除」這顆按鈕
```

### 2-3 schema(新表,鐵則 12③)

```
coupons              券本體(上面那些欄位)
coupon_redemptions   誰在哪張單用了哪張券、折了多少
```

🔴 **為什麼要第二張表**:`discountTotal` 是**一個總數**,
記不下「這 500 是哪張券折的」⇒ 退貨要退回次數時查不到來源。

🔴 **併發**:限量券兩個人同時結帳誰拿到 —— 那是 DB 層的原子 claim,
repo 有前例(attempts 線的 `SKIP LOCKED`)。⚠️ **我沒查能不能直接套。**

🔴 **猜碼**:短碼 + 無限次嘗試 = 掃得出所有券 ⇒ `/api/coupons/validate` 要速率限制。
repo 有 `lib/cron/rate-limit.ts` 的形狀。⚠️ **我沒查它套不套得上結帳路徑。**

### 2-4 這一版明講不做的

```
· 打 N 折(Q1 乙)
· 每人限用次數(Q2 乙)
· 開始日(Q3 乙)
· 同一張單用兩張券
· 免運券
· 券綁特定商品 / 分類
· 自動產碼
```
每一項都是「一個欄位 + 一段驗證 + 後台一格」,**可以逐項加,不必回頭改資料**。

---

## 3. 這份 PRD 自己不確定的

```
· Q7 的不變式會不會真的紅 —— 讀出來的,沒實測
· Q8 的 CSS 耦合程度 —— 沒查
· SKIP LOCKED 套不套得上券 —— 沒查
· rate-limit.ts 套不套得上結帳路徑 —— 沒查
· ~~這份 PRD【沒有】涵蓋:退貨時券的次數怎麼退回去~~
  ✅ **2026-08-26 Sean 已答 `c 甲` = 退回去, 客人可以再用一次。**
  ⇒ 落點 `2026-08-26-coupon-schema-plan.md` §1-2b(`reverted_at` 欄 + 每個數次數的地方都要濾)
  ⚠️ **而「誰把它退回去」仍未定** —— 同 §1-2b 末段, 是實作前要確認的一格
```

📌 相關:[shape-draft](../reviews/2026-08-19-coupon-shape-draft.md) ·
[code-research](../reviews/2026-08-19-coupon-code-research.md) ·
memory `project_0819-coupon-code-new-requirement` · `project_0824-sean-five-scope-rulings`

---

# §7 ✅ Sean 2026-08-26 的答案,與它們改變了什麼

> 落檔:memory `project_0826-sean-answers-decision-table`。**§1 的原題原句不動,留痕。**

## 7-1 逐題

| 題 | 答 | 與推薦 |
|---|---|---|
| Q1 折扣怎麼算 | **乙 折金額 + 打 N 折,兩種都做** | 🔴 不同(推甲) |
| Q2 能用幾次 | **乙 總量 + 每人可用 N 次** | 🔴 不同(推甲) |
| Q3 有效期 | 甲 只做結束日 | ✅ |
| Q4 能不能跟會員價疊 | **丙 每張券自己一個開關** | 🔴 不同(推甲) |
| Q5 誰能建券 | 甲 所有員工 | ✅ |
| Q6 最低消費門檻 | 甲 第一版就做 | ✅ |
| Q7 用過的券能不能刪 | 甲 不能刪只能停用 | ✅ |
| Q8 券與儲值金拆開 | ⚠️ **未定,見 7-4** | — |
| (追加)客人看到什麼 | 甲 **一句紅字說明原因** | ✅ |

🔴 **Q2 他補了逐字,而它直接是規格**:
> 「**同一個帳號, email 重複無所謂**」⇒ **「每人」= 帳號,不是 email。**
> ⇒ 同一個人用兩個帳號各用一次 = **兩次,不擋**。

## 7-2 於是這張表長這樣(取代 §2-2 的欄位清單)

```
【基本】 優惠碼 · 說明 · 建立者(自動)
【折多少】🔴 折扣方式(二選一)+ 折扣值        ← Q1乙:金額 與 百分比 都要
【什麼時候】結束日                            ← Q3甲
【能用幾次】總量上限 · 🔴 每人上限            ← Q2乙,而「每人」= 帳號
【門檻】 最低消費                             ← Q6甲
【疊加】 🔴 可否與會員等級價並用(每張券一個開關) ← Q4丙
【開關】 啟用 / 停用(沒有「刪除」)           ← Q7甲
```
⇒ **比 §2-2 假設的大**。§2-4「這一版明講不做的」裡的**打 N 折、每人限用**兩項**已被推翻**,原句留痕。

## 7-3 🔴🔴 而最大的連動在這裡:**拒絕理由的清單,現在是規格的一部分**

`Q22 = 甲(說明原因)`,而他這一輪的答案**各自造出一種拒絕理由**。逐條數:
```
① 券碼不存在(打錯)
② 這張券已停用            ← Q7甲(停用取代刪除)
③ 已過結束日              ← Q3甲
④ 總量已用完              ← Q2乙
⑤ 你這個帳號已經用過了    ← Q2乙(「每人」那一半)
⑥ 還差 NT$ N 才到最低消費 ← Q6甲
⑦ 這張券不能與你的會員價並用 ← Q4丙
                                     ⇒ **至少 7 種**
```

### 🔴 而 design 的合約表達不了「哪一種」—— 這是 design 沒有涵蓋的新面
```
design-reference/HANDOFF.md:202-203 逐字(我當場開檔核):
  "couponDiscount": 500,   // 若 coupon 無效則 0 + invalidCoupon: true
  "invalidCoupon": false,
⇒ 只有一個【布林】。零個原因欄位。
   量法 grep -niE 'couponError|couponReason|invalidReason|reason' <該檔> ⇒ 0
   正對照 grep -c 'couponDiscount' ⇒ 1(尺是活的)
```
⇒ **`invalidCoupon: true` 答得出「不能用」,答不出「為什麼」。**
⇒ 而 `Q22甲` 要求**說明原因** ⇒ **那個契約要擴。**

🔴 **這一格照鐵則 1 要停下來講清楚**:design 是真權威,而**這一面 design 沒有畫**
(§0 已量過:券只有 `cart-coupon` / `-ok` / `-row` 三個 class、**零錯誤態**;
而同一份 design 別處**有** `auth-err` / `spm-geo-error` ⇒ **不是那位作者不寫錯誤態,是這一格沒寫**)。
⇒ **不是「翻譯 design」,是「design 沒有這一面,而 Sean 要了」** ⇒ **要新增,而要讓他知道我們在新增。**

```
待決(下一輪問他,不要自己定):
· 那 7 種原因的【文案】—— 那是他的品味題, 不是我的
· ⑥「還差 NT$ N」要不要真的把差額算出來給客人看(要, 但要確認)
· 錯誤訊息出現在哪裡(輸入框下方 / 取代那行綠字)—— 🔴 design 沒畫 ⇒ 要 Design session
```

## 7-4 ✅ `Q8`(券與儲值金)**已確認 —— 而答案比選項多一半**
```
先前狀態:選項乙的字面 =「等等, 兩件一起再說」(暫停), 而他寫「乙, 兩個都開」⇒ 相反的意思
2026-08-26 確認, Sean 逐字:「兩個都做 —— 優惠券做, 儲值金也一起解凍」
```
⇒ **不是「知道我們在拆」,是【兩件都要做】。**
🔴 **而【儲值金】那半不併進券的規格,它是一條新的線**:
```
· 券 —— 本 PRD 與下游三份規格照常往下, 不受影響
· 儲值金 —— 從【凍結】變成【要做】, 而它零規格零盤點
  ⇒ 已出盤點:`docs/specs/2026-08-26-wallet-unfreeze-recon.md`
  🔴 而盤點的結論是【停下來問】, 不是【開工】——
    `#202` 凍它的理由是**台灣儲值法規**(《電子支付機構管理條例》/ 金管會牌照),
    不是工程排程;而 Sean 自己 2026-07-16 解後台那半時逐字寫過
    「**override 不外溢到會員前台**」⇒ **他畫過兩次那條線。**
  ⇒ 「解凍」至少有四種意思(只顯示 / 結帳可折抵 / 客人可自己儲值 / 只是別藏那個分頁),
    而其中一種今天做不完(它還等法規定案)⇒ **已列成一題請他選。**
```
⚠️ **本 PRD 的範圍不變:券。** 儲值金那條線的進度看那份盤點,不看這裡。

## 7-5 📌 而這一輪最該被下一個人讀到的,不是任何一題

**14 題裡他有 6 題選了「比我們推薦更完整」的那一版**,而我們那 6 題的推薦**全部**是
「先做小的、之後補得上」。
⇒ **推薦系統性偏保守,而他系統性要完整版。**
⇒ 而每一條理由當時都成立(不動 schema / 今天就能出 / 風險低)——
   **它們共用一個沒被檢查的前提:「先出小的」對他有價值。他六次說不是。**
⇒ 🔴 他 2026-08-18 常設令「**不准做一半**」在這裡再次應驗 ——
   **我們六次把「做一半」包裝成「先做能做的」,而那是同一件事換一個講法。**
