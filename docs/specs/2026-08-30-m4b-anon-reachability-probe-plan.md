# 客人可達性探針 — plan(**丁:不查目錄,直接當客人去讀一發**)

> 線A `-e9` · 2026-08-30 · 對應 `docs/launch-todo.md` ⟦b4-ACL1⟧ 的**客人面**
> 🔴 命中鐵則 12②(權限)⇒ 🛑 **不對正式站跑,除非 Sean 本人授權。**
> 🔴 時刻是 sandbox 的 `date`,不是 Sean 那台的鐘。

## 🔴 第一行:**為什麼換路**

> **前三輪 59 條裡,至少 8 條的根因是同一個:我在【用目錄的狀態,推論客人的可達性】。**
> **而那個推論,每一輪都被找到新的反例 —— 因為它本來就不是一個等價關係。**

📌 **⇒ 而那也是為什麼三輪不重複:每一輪找到的是【同一個錯誤前提】的不同反例,不是同一層的更細問題。**
**⇒ ⇒ 三輪加起來不是「還沒修完」,是【這條路不通】。**

**那八條(逐條,都是 ACL 一格不動而客人路徑打開):**
```
· 角色鏈沒遞迴：anon→A→B 已存在，之後 GRANT … TO B ⇒ 探針零差異
· view 的 security_invoker 從 true 改 false ⇒ ACL 六本目錄一格沒動
· 關 RLS / 放寬 policy / 給 authenticated BYPASSRLS ⇒ 同上
· public 的 security_invoker view 引用 private 表 ⇒ 對 private 表 GRANT 即可讀到
· anon 可 SET ROLE 到 service_role 的世界 ⇒ service_role 的 GRANT 客人用得到
· 既有函式改成 SECURITY DEFINER / 換 owner ⇒ proacl 可保持不變
· relkind 白名單漏 f（foreign table）／ 六本目錄漏 pg_type.typacl
· 惡意 GRANT 若在第一次 baseline【之前】就在 ⇒ 被快照成正常
```
✅ **⇒ 丁對這八條【全部不成立】—— 因為它不推論,它去試。**

## 🛑 而「丙」不是丟掉 —— 它答的是 `:313` 的【字面】
```
:313 逐字問的是：「dashboard / SQL Editor 手動改權限，沒有人守」
⇒ 而那正是丙（純漂移偵測、不宣稱安全）答得出來的
   （product_fitments_effective 七物件就是這樣進線上的，repo 零紀錄）
🔵 而丁答的是【另一個問題】：客人現在碰不碰得到
📌 ⇒ 兩個問題都真，而它們不是同一個
🛑 裁定（-48 2026-08-30）：現在做丁；丙不現在做，而【理由要寫進 :313 那一列】——
   否則下一個人看到「ACL 探針做了」，會以為 :313 已經被涵蓋
```

---

## §1 ✅ 已經量到的:**這把尺叫得出來,也不亂叫**

🛑 **為什麼要先做這一發**:**一支【沒有紅過的工具】,與一支【壞掉的工具】,長得一樣。**
`scripts/probe-schema-exposure.sh` 檔頭逐字「**本檔【從來沒有對正式站跑過】**」
⇒ **「它寫好了」與「它跑得動」是兩個宣稱。**

**交件**:`scripts/anon-reachability-probe-verify.sh`(114 行,已跑)
```
拋棄式 PG 17.10 + PostgREST，建兩個世界：
  open_table   GRANT SELECT TO anon   ⇒ HTTP 200，且真的回了資料列
  closed_table 不 GRANT               ⇒ HTTP 401，且內容沒有洩出
  zzz_never_exists（負對照）           ⇒ HTTP 404（不是 200）

🔵 第三個世界（2026-08-30 加，-b4 指出我把推論寫成量測之後補的）：
  rls_table    GRANT SELECT TO anon + RLS 開著且無 policy ⇒ 200 而【零列】
               關掉 RLS ⇒ 資料出來了，而【ACL 一格都沒有動】
  ⇒ ⇒ 這一發實證了「用目錄推論可達性」那八條反例的其中一條（1/8）
  🛑 其餘 7 條仍是【機制推論，未逐條餵給它看】

✅ 連跑兩發：PASS=11 FAIL=0，兩發 HTTP 碼完全相同
🔴 突變（在【複本】上把那行 GRANT 拿掉）⇒ PASS=7 FAIL=2 ⇒ 它【真的會紅】
✅ 突變後原檔未被動到（GRANT 那行仍在，命中 1）
```
📌 **⇒ 這一段就是 v1/v2/v3 三份 plan 都沒有的東西:一個【該紅的世界】,而它真的紅了。**

⚠️ **而我在做這一發時踩了一個坑,寫在腳本裡不刪**:
```
第一發 pg_ctl rc=1 ⇒ FATAL: postmaster became multithreaded during startup
🔴 而答案【逐字寫在 scripts/admin-probe/up.sh:43 的註解裡】，
   而那句話還寫著「顧客站那支實際踩過」⇒ 我是第三個踩的
⇒ 已把那句話連同出處抄進本腳本檔頭
🔴 而第二個坑是我自己造的：ENV-FAIL 時印了 log 路徑，而 trap 已經把它刪了
   ⇒ 已改成【非綠不刪】
```

## §2 分母怎麼產生(**機械來源,不是「我們想得到的那幾張」**)

```
🔴 不手寫清單。來源＝一發 SQL：
   SELECT c.relname FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace
   WHERE n.nspname='public' AND c.relkind IN ('r','p','v','m','f')
⇒ 產出一份【當下的完整清單】，而清單本身是交件的一部分
✅ 而它可以被檢查：清單長度 vs 探針試過幾張 ⇒ 兩個數要相等
   （鐵則 11 的「我餵幾條 vs 它跑幾支」套在這一層）
```
🛑 **而這一步要在正式站上做,所以它也在授權範圍內** —— 本地版只能證明**做法**成立。

## §3 交件形狀(**今晚做得完的是「準備好那一發」,不是「跑那一發」**)

```
🛑 打正式站需要 Sean 本人授權，同僚窗給不了，我也不會自己跑
⇒ ⇒ 交件 = ① 一支他可以照著跑、或授權我們跑的腳本
            ② 它已經在拋棄式庫上【雙向驗過】（§1 那一段）
            ③ 一份「它會讀哪些、不會讀哪些」的清單產生法（§2）
            ④ 一段給 Sean 的白話說明：它做什麼、不做什麼、最壞會怎樣
```
🔵 **最壞會怎樣(先寫,不等人問)**:它只發 `GET`,不寫入、不刪除;打的是**我們自己的站**;
用的是**公開的 anon key**(那把鑰匙本來就在每個客人的瀏覽器裡)⇒ **它做的事,任何一個客人都做得到。**

## §4 驗收(片 A = 本地;片 B = 正式站,等授權)

### 片 A(**已完成,證據在 §1**)
```
☑ 該紅的世界真的紅（open_table 200 + 有資料列）
☑ 該綠的世界真的綠（closed_table 401 + 內容沒洩）
☑ 負對照（不存在的表 404）
☑ 連跑兩發：PASS / FAIL / HTTP 碼三者全同
☑ 突變（拿掉 GRANT）⇒ FAIL=2 ⇒ 尺會紅
☑ 突變跑在複本上，原檔未動
```

### 片 B(**等 Sean 授權,未做**)
```
☐ 分母：SQL 產生的清單長度 == 探針試過的張數（兩個數相等）
☐ 每一張的結果三態分得開：200-有資料 / 200-空 / 非 200
   🔴 而「200 而空」不等於「讀不到」—— 可能是 RLS 擋住了列，而【表本身可達】
     ⇒ 這一態要單獨一欄，不得併進「安全」
☐ 對照組：一張【本來就該公開】的表（例如商品）⇒ 必須 200 有資料
   —— 否則整發都是 401 時，分不出「很安全」與「鑰匙錯了」
☐ 🔴 而那一發【只讀、不寫】：腳本內不得出現 POST/PATCH/DELETE（可機械 grep）
```

## §5 🛑 邊界
```
· 🛑 不對正式站跑，除非 Sean 本人授權 —— 同僚窗給不了這個授權
· 本探針量【HTTP 讀不讀得到】，不量「為什麼讀得到」—— 那正是它的優點，
  也是它的限制：它答不出「該修哪裡」，只答「有沒有漏」
· 它只看得到【清單上那幾張】⇒ 分母 = §2 那一發 SQL 的當下輸出，不是「全部」
· 本地驗證用的 PostgREST 版本與設定，與正式站不同 ⇒ 這裡綠不代表那裡綠
· 🔴 而 :313 那一列【不因為丁做完而 done】—— 丙那一半（repo 以外有沒有人動過）
  仍然沒有人守，理由已寫在該列
```
