# B7 九條負向驗收 · 逐條狀態表(收工檔)

> 🔴 **這份檔【本身】就是 `docs/specs/2026-08-18-m4b-e8b-b6-b7-spec.md` §3 明文的 B7 收工條件**
>    ——「B7 的收工條件是九條逐條標記狀態(通過 / 未做 / 不適用+理由),不得只交『跑過的那些』」。
>    **而它在 2026-08-27 之前不存在** ⇒ 這正是為什麼「B7 做了沒」一直沒有一個查得到的答案。
>
> 🔴 **給下一個 grep「B7」的人**:你會拿到 **0**,而那個 0 是【命名】的 0,不是【覆蓋】的 0。
>    這九條的守門**不掛「B7」這個名字** —— 它們散在 `b5a-identity-acceptance` / `read-gate` /
>    `authorize` / `staff` 這些檔裡。**這份表就是來接住你那個 0 的。**
>    (2026-08-27 主視窗板子 `:129` 一度寫「3 支撞名 ⇒ 這九條一條沒跑」,那是【命名的 0】被讀成【覆蓋的 0】;
>     實測 admin 側五條有測、59 格綠、每條帶正對照。板子已由主視窗劃線更正。)
>
> 分母 = 母 plan `docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md` §6(負向清單)。
> 母 plan 逐字:「正向『三個帳號各登一次』不算驗收」「每一項都要有一格【會紅】的測試,不是『應該會擋』」。

## admin 側 ~~59~~ **83** 格綠(可重跑,不只給數字)

> 🔴 **2026-08-29 線I 重跑訂正 —— 兩件事,而第一件會讓你以為這份表在說謊。**

### ① 上面原本那條指令,照抄會失敗
```
~~npx vitest run --project admin \~~
~~  src/lib/session/b5a-identity-acceptance.test.ts \~~
~~  src/lib/session/read-gate.test.ts \~~
~~  src/lib/session/authorize.test.ts \~~
~~  src/lib/staff.test.ts~~
```
在 `apps/admin/` 底下照抄 ⇒ `No test files found, exiting with code 1`(2026-08-29 實跑)。
成因:vitest 的 `include` 是 `apps/admin/**/*.{test,spec}.{ts,tsx}`
⇒ 🔴 **要在 repo 根跑,而且路徑要帶 `apps/admin/` 前綴。**
⚠️ 它**至少 rc=1**、不是全綠而沉默 —— 但畫面上只有一句話,
   而下一個人最可能的反應是「**那就是還沒做**」,**而那正是這份表存在要擋掉的誤讀**。

### ⇒ 可跑的形狀(2026-08-29 實跑 rc=0)
```
cd /Users/sean_1/pcm-website-v2
npx vitest run --project admin \
  apps/admin/src/lib/session/b5a-identity-acceptance.test.ts \
  apps/admin/src/lib/session/read-gate.test.ts \
  apps/admin/src/lib/session/authorize.test.ts \
  apps/admin/src/lib/staff.test.ts
```

### ② 而 `59` 這個數字腐爛了
```
2026-08-27 11:3x 原量  ⇒ 4 檔 / 59 passed / 0 failed / rc=0
2026-08-29 重量 第一發 ⇒ 4 檔 / 83 passed / 0 failed / rc=0
2026-08-29 重量 第二發 ⇒ 4 檔 / 83 passed / 0 failed / rc=0   (連跑兩發相同)
餵幾條 vs 跑幾支      ⇒ 我餵 4 條、它跑 4 支                  (兩個數相等)
正對照(只餵 1 條)    ⇒ 1 檔 / 22 passed / rc=0               (尺會隨輸入變,非恆綠)
```
🟡 **方向是好的**(測項從 59 長到 83),**而引用 `59` 的人會拿到一個對不上的數,
   並且它不會叫** —— 兩發都綠、兩發都 4 檔,只有總格數不同。
⛔ ~~**本次未做突變測試** ⇒ 「83 格都在守對的東西」我**沒有驗**,只驗了「尺會隨輸入改變」。~~
   ~~原表 2026-08-27 那一輪的逐條正對照(下表「正對照(防恆綠)」欄)**本次沒有重跑**。~~

### ③ 🔴 突變測試 —— **2026-09-04 線【身分】`-auth` 補做,而【它證得比那句話少】**

```
2026-09-04 重量(--maxWorkers=2, 機器 load 高時全隊限制 worker 數):
  第一發 ⇒ 4 檔 / 83 passed / rc=0
  第二發 ⇒ 4 檔 / 83 passed / rc=0        (連跑兩發相同;83 從 08-29 到今天【沒有再漂】)
  餵幾條 vs 跑幾支 ⇒ 我餵 4 條、它跑 4 支
  🟢 正對照(只餵 read-gate 一條)⇒ 1 檔 / 6 passed / rc=0
     ⚠️ 而上面 ② 那一格寫「只餵 1 條 ⇒ 22 passed」—— **那是【另一支檔】**(不是矛盾)。
        📌 引用「只餵 1 條回幾格」時要連【餵的是哪一支】一起寫, 否則兩個都對的數字看起來像打架。

🔬 突變兩發(改【生產碼】, 不是改測試;還原後 shasum 逐位元組相同):
  ① `session.ts:723` 的 `parsed.v === 1 && requireRealIdentity()` ⇒ 改成 `false && …`
     (= 舊版票不再被拒)                          ⇒ **1 格紅**(`[2] v:1 票 + 旗標開 ⇒ 拒`)
  ② `session.ts:175` 的 `ADMIN_REQUIRE_REAL_IDENTITY === '1'` ⇒ 改成 `return false`
     (= **真登入閘整個關掉**)                     ⇒ **3 格紅**
        `[2] v:1 票 + 旗標開 ⇒ 拒`
        `[16] 無 v:2 + 旗標開 ⇒ null, 而且【一次都沒讀】ACTOR_COOKIE`
        `[21] 無 v:2 + 旗標【開】⇒ stale-ticket`
```

🎯 **⇒ 這兩發證的是:【真登入那道閘本身】有測試在守它 —— 有人把它關掉會紅。**

🛑 **而它【不證】那句舊話**:83 格裡只有 **3 格**對突變②有反應 ⇒
**「83 格都在守對的東西」仍然沒有被驗**,而那 80 格是在守別的東西(它們沒紅是**對的**)。
📌 **⇒ 一發突變只證得了【它打到的那一格】** —— 要證 83 格各有判別力,得有 83 發。
⇒ 🔵 而那不是本次要買的東西:本次買的是「**旗標被關掉時會不會叫**」,而答案是**會**。

⚠️ **仍然沒做的**:下表「正對照(防恆綠)」那一欄的逐條重跑(2026-08-27 那一輪之後沒有人再跑過)。

## 九條逐條

| # | 母 plan 那一條 | 狀態 | 證據(檔案:行號)| 正對照(防恆綠)| 誰在跑 |
|---|---|---|---|---|---|
| 1 | 舊 cookie 失效、開關開 ⇒ v:1 拒、不得靜默降級 | ✅ 已覆蓋(不叫 B7)| `b5a-identity-acceptance.test.ts:105` [2] v:1+旗標開⇒拒、reason=`version_rejected` | `:98` [1] v:1+旗標關⇒收(回歸格:它紅=弄壞現況)| admin 單測 |
| 2 | legacy fail-open cookie 發不出來 | ⬜ 分母不在這裡 | 報價單 repo · B2 的面(登入端)| — | 報價單側,本工作樹構造不出來 |
| 3 | 備援登入進得去、而任何寫入被擋 | 🟡 admin 半已覆蓋 / 另一半分母不在這裡 | admin 半:`authorize.test.ts:92` 無具名 actor(停用/查無/DB錯)⇒null | `authorize.test.ts:99` 前一層先失敗就不問 actor 閘(fail-closed 序)| admin 單測 / 報價單半在報價單 repo |
| 4 | 已停用員工仍持有效 session ⇒ 讀取閘擋 | ✅ 已覆蓋(不叫 B7)| `staff.test.ts:59` DB 列 inactive⇒null;`:149` resolveActiveStaffById is_active=false⇒null | `staff.test.ts:48` 在職 id⇒actor(同把尺會回東西)| admin 單測 |
| 5 | SSO code 重放 ⇒ 擋 | ⬜ 分母不在這裡 | 報價單 `exchange` 原子消耗(B4 spec §6 第9格);admin callback 只是【委派】給它 | — | 報價單側 |
| 6 | 開關開、而 session 沒帶身分 ⇒ 擋 | ✅ 已覆蓋(不叫 B7)| `b5a-identity-acceptance.test.ts:281` [16] 無 v:2+旗標開⇒null,且一次都沒讀 ACTOR_COOKIE | `:294` [16b] 同把尺在第 3 層【真的會爆】(否則 [16] 恆綠)| admin 單測 |
| 7 | 混版:admin 新 / 報價單舊 ⇒ 不當機、不放行無身分 | 🟡 admin 半已覆蓋 / 另一半分母不在這裡 | admin 半:`b5a-identity-acceptance.test.ts:142` [7] v:3 或 v 缺⇒拒 | 同檔 [3] v:2 合法⇒收(能收也能拒)| admin 單測 / 報價單半(B4 spec §6 第7/8格)|
| 8 | 查無帳號 vs 密碼錯:訊息、狀態碼、**回應時間**三者一致 | ~~🔴 連【怎麼量】都還沒有人想好~~(2026-08-27 下午起不成立)→ 🟡 **量法已設計**:尺A 白盒 code-path 等價=收工尺 / 尺B 黑盒時序=prod-only backstop | 設計檔 `~/pcm-mailbox/線3-8號怎麼量-20260827.md`;母 plan「沒想好怎麼量之前不要寫成通過」 | 🔴 **實作仍【未做】、落在報價單 repo(B2 面)** —— 尺設計完 ≠ 收工 | 報價單側 |
| 9 | 首次登入沒改密碼 ⇒ 不得經 SSO 繞過 | ⬜ 分母不在這裡 | 報價單側 · 攔截點必須在所有入口之前含 `authorize` | — | 報價單側 |

## 分堆(一眼看完)
```
admin 側已覆蓋(綠、帶正對照,只是不叫 B7):  #1 #4 #6 全條 + #3 #7 的 admin 半  = 5 條有 admin 貢獻
分母不在這裡(報價單 repo,本工作樹構造不出來):#2 #5 #9 全條 + #3 #7 的另一半      = 4 條
連怎麼量都未定(比「沒做」更前):              #8                                  = 1 條
```

## 🔴 這份表【不】宣稱什麼(誠實邊界)
1. 報價單側那半我**沒有下任何結論** —— 我看不到那個 repo,不寫「應該也有測」。狀態一律「分母不在這裡」。
2. admin 側五條寫的是「**已覆蓋而不叫這個名字**」,**不是「B7 已完成」** —— B7 的完成要九條都有著落 + 兩顆開關都開(spec §3 收尾),那不是這份表能宣布的。
3. `#8` 不是「不適用」也不是單純「報價單側」 —— ~~它是**量法未定**~~ **量法 2026-08-27 已設計**(尺A 白盒=收工尺 / 尺B 黑盒=prod backstop,`~/pcm-mailbox/線3-8號怎麼量-20260827.md`);🔴 **但實作仍未做、在報價單 repo,不要讓它看起來收工了**。誰要動它,先讀母 plan「沒想好怎麼量之前不要寫成通過」與尺B 那條「小洩漏我們環境量不出來」。

---

## 🔵 2026-09-03 05:1x `-ship` 複驗(主視窗-87 派;板 `docs/launch-todo.md` B7 那一列)

**九條的分母,我自己數過一次**(不引用任何一邊的數字 —— 板上曾有「~~八~~九」的訂正):
```
spec `2026-08-18-m4b-e8b-b6-b7-spec.md` §3 那張表      ⇒ 9 列
本檔那張表                                            ⇒ 9 列
⇒ 兩份獨立數都是 9 ⇒ 「九條」成立
```

**admin 側那 4 支守門,今天連跑兩發,四個數**:
```
發1 / 發2   Test Files 4 passed (4) / 4 passed (4)
            Tests      83 passed (83) / 83 passed (83)
            紅的格數   0 / 0
🔴 我餵 4 條測試檔 vs 它跑 4 支 ⇒ 相等(沒有靜靜少跑)
🟢 正對照 只餵 1 條(staff.test.ts)⇒ 1 檔 / 22 passed ⇒ 尺隨輸入變, 非恆綠
🔵 負對照 餵一條不存在的路徑          ⇒ rc=1 ⇒ 不會靜靜通過
```
⚠️ **而正對照那個數【漂了】**:板列記的是 `-c8` 2026-08-29 量的「1 檔 / **15** passed」,
今天同一支檔是 **22** ⇒ 📌 **與主數 59 ⇒ 83 同一個方向(測項變多), 而引用 15 的人會拿到對不上的數。**
🛑 **這裡不改板列那個 15** —— 它是**那一天的**讀數,原句留著;本段是**今天的**讀數。

### 🔴 而 B7 這一列【今天仍然關不掉】,理由不是沒人做
```
5 條  admin 側有貢獻(#1 #4 #6 全條 + #3 #7 的 admin 半)⇒ 今天實跑綠, 且本檔第 6 欄逐條記著正對照
4 條  #2 #5 #9 + #3 #7 的另一半 ⇒ 🛑 分母在【報價單 repo】, 本工作樹【構造不出來】
1 條  #8 ⇒ 量法 2026-08-27 已設計(尺A 白盒 / 尺B 黑盒), 而【實作未做, 且在報價單 repo】
```
🛑 **⇒ 那 5 條【不標 pass】** —— 照母 plan 逐字「B7 的完成要九條都有著落 + 兩顆開關都開」。
🔴 **而那 4+1 條我明寫【造不出來】, 不是 fail 也不是 pass** ——
📌 **「我跑了那個 harness」與「那個 harness 到得了那個世界」是兩個宣稱**,而這裡是後者不成立。

### ⚠️ 本次複驗證不到什麼
- **只證了 admin 側那 4 支檔今天是綠的**,以及那 9 條的**分母數目**。
- **沒有**逐條去讀那 5 條的斷言內容 —— 我信的是本檔第 6 欄既有的正對照紀錄(它是 08-27 寫的)。
- **沒有**碰報價單 repo(不同 repo、不同樹,且本樹構造不出那些世界)。
- 🔵 而 `#8` 的量法設計檔在 `~/pcm-mailbox/線3-8號怎麼量-20260827.md` —— **我沒有複核那份設計。**
