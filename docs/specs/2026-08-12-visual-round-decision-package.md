# 視覺輪決策包 —— #350 / #447 / #446 / #437 四題成題 + demo 需求規格

> 產出者 = D 窗十二代(2026-08-12),依 `D-631-DISPATCH` 派工。**純 docs、零 code 改動、不替 Sean 拍板。**
> 本檔所有現況斷言都附 `檔案:行號` 或可重跑的命令;附不出來的一律寫「未確認」。
> 基準 commit = `e45f420a`(dev HEAD,寫本檔時 `git status --porcelain` 空)。

---

## §0 🔴 開工重數:四題裡有**兩題的題目本身是錯的**

派工單把四題並列成「都還沒做的視覺待決題」。逐條對 git log 重數之後,**兩題的前提不成立**。
這一節先更正,否則後面三節會建立在錯的題目上。

| 題 | 派工單/條目寫的 | 實查 | 證據 |
|---|---|---|---|
| **#437** | 「⏳ 已拍板待做」(`docs/phase-1-backlog.md:11689` 狀態欄) | **四點主體已完工並入 dev** | `76aff2e3`(`git merge-base --is-ancestor 76aff2e3 dev` = YES);異動 9 檔 348+/53- |
| **#350** | 「⏸ **未做**」(`docs/specs/2026-08-12-order-admin-acceptance-package.md:27`) | **①側欄 ②分割面板 全部已上線**,只剩 ③④ | 七顆逐顆驗過已入 dev(下表) |
| #446 | 待 Sean 拍徽章語意 | ✅ 前提正確,確實未做 | `docs/phase-1-backlog.md:11837` |
| #447 | 待 Sean 點頭才動 | ✅ 前提正確,確實未做 | `docs/phase-1-backlog.md:11858` |

**#350 七顆的驗法**(`for s in …; do git merge-base --is-ancestor $s dev && echo "$s 已入 dev"; done` ⇒ 七顆全 YES):

| 片 | commit | 內容 |
|---|---|---|
| 350a | `07974229` | 側欄 16rem→9rem(Sean 逐字要求①)|
| 350b | `08e56014` | 工作區殼:`@panel` 平行路由槽 + **可拖曳調寬** + 寬度持久化 |
| 350c | `fe3973bb` | 訂單列表點編號 → 右側面板開詳情(Sean 逐字要求②)|
| 350d-1〜4 | `e0e8bcc2` / `5690b771` / `6b2eaf20` / `e533025e` | 面板內動作做完回到面板(return_to 全線,含退款線)|

⇒ **Sean 這一輪不必想像 #350 的①②,他可以直接在正式站上看到並肉眼驗**。
可調寬的下限實作在 `apps/admin/src/lib/layout/workspace-panel.ts:33`(`MIN_PANEL_WIDTH = 320`),
上限隨視窗算(`:77`)。

> 病根一句話:**backlog 條目的狀態欄與敘述欄都不是事實**,只有 git log 是。
> #437 條目狀態欄停在立案日、#350 條目的「排程:D 窗 #347 收尾後接手」(`:9923`)停在 08-09,
> 而 350a-d 是 08-09 22:05〜08-10 13:11 做完的。這是 STATUS 第 10 行記過的
> 「進度欄不是事實」的**第三、第四例**。建議這輪順手把兩個條目的狀態欄改掉(我沒有自行改,見 §4)。

---

## §1 四題連動圖

```
                     ┌──────────────────────────────────────────┐
                     │  Sean 這輪要拍的 = 三件(四題扣掉 #437    │
                     │  四點已完工那半;數法見 §0 表)          │
                     └──────────────────────────────────────────┘

  #350 ③④ ─────────────┐
  (客人卡/商品卡        │  兩者都是「面板裡放什麼」= 內容題
   要長什麼樣)         │  容器已存在,不需要再決定容器
                        │
  #447 ────────────────┤  列表 markup 重構 = 容器題
  (雙 markup 怎麼收)   │  🔴 四題間唯一的前置關係(§1 八條斷言中僅第 4 條;
                        │     其餘七條是各自獨立):它會重排整張列表視覺
                        │
  #446 ────────────────┤  徽章語意 = 純語意題,與上面兩題零交集
  (已沖銷退不退得回)   │
                        │
  #437 殘題 ───────────┘  訂單標頭要不要再一顆徽章 = 純語意題,零交集
```

**逐條連動斷言(每條附證據)**:

1. **#350③ 的容器已經存在,只差內容** —— `@panel` 槽已上線且能容納任何路由
   (`apps/admin/src/app/@panel/orders/page.tsx` 是第一個租戶);母 plan §6 逐字
   「**不做 #348 客人 360 的內容**…本片只保證**槽位容得下它**」
   (`docs/specs/2026-08-10-350-workspace-shell-plan.md:113-120`)。
   ⇒ 現況缺的是 `app/@panel/customers/` 這個槽頁(`ls apps/admin/src/app/@panel/` = 只有
   `orders/`、`[...catchAll]/`、`default.tsx`、`order-panel-wiring.test.ts`)。
2. **客人 360 的「整頁版」其實已經存在** —— `apps/admin/src/app/customers/[id]/page.tsx` 已在
   dev,內含錢包/愛車/地址/歷史訂單四個 repository 的接線(`:10-19` import 段)。
   ⇒ #350③ 不是「從零做客人頁」,而是「把已有的頁塞進面板槽 + 決定面板裡顯示哪些欄」。
   ⚠️ **未確認**:那頁的完整度(是否等同 #348 驗收條件)我沒有逐條核,只確認檔案存在且有真接線。
3. **訂單列表的客名與料號今天是純文字,點不動** —— 客名 `orders-table.tsx:217`(手機卡 `:354`)
   逐字 `{order.customerName ?? '—'}`,沒有包 `<Link>`;料號是第 5 欄(`:447`)同樣純文字。
   ⇒ ③④ 兩題都要**同時**動 `orders-table.tsx`(加連結)+ 新增面板槽頁。
4. **🔴 #447 是 ③④ 的實質前置(最重要的一條)** —— ③④ 各要在 `orders-table.tsx` 加一個可點的
   東西,而該檔正是雙 markup:桌機列 + 手機卡各寫一份(檔頭警告 `:49-60` 逐字
   「下一個帶互動控件的欄位(按鈕/表單)才是真正的到期日」)。
   客名/料號連結若照 A13 的做法只放 `<a>`,到期日再被繞開一次、代價再收一次(每條紀律寫兩遍);
   若做成帶互動的卡片觸發器,就**正面撞上**那句話。
   ⇒ 先拍 #447 再做 ③④,只重排一次列表;反過來則要重排兩次。
5. **#447 兩案都動共用元件** —— 條目逐字「兩案都動 `shared/admin-data-table.tsx` 的定位
   (共用元件行為 = 鐵則 12⑥)」(`docs/phase-1-backlog.md:11871-11873`);該檔現況 166 行,
   檔頭 `:14-24` 有同一條警告的上游版本。⇒ 不論拍哪案都要跑 codex 對抗審查,不是輕量片。
6. **#446 會連動「已沖銷的列還能不能再沖」** —— 徽章判定與沖銷鈕判定吃**同一個** `isReversed`:
   `payment-list-view.ts:239` 算出 `isReversed`,`:245` 逐字 `canReverseByRow: row.rail !== 'card' && !isReversed`。
   ⇒ 拍「徽章講現況」等於同時把「A 可以再被沖一次」打開,不能只當顯示題拍。
7. **#437 殘題與其他三題零交集** —— 它只動收款卡/訂單標頭,不動列表 markup、不動面板容器。
   ⇒ 可以單獨拍、單獨做,不必等這輪其他任何一題。
8. **拍 A 會讓別題消失嗎** —— 只有一組:**#447 拍案①(單一 markup)會讓 §2-B 的死區題自動消失**
   (死區成因是桌機格獨有的 `relative z-10`,單一 markup 之後那格不再獨立存在;
   死區描述見 `docs/phase-1-backlog.md:11888-11893`)。其餘三題彼此不互相消滅。

---

## §2 每題成題

### A. #350 ③④ —— 客人卡與商品卡(面板內容)

Sean 逐字(`docs/phase-1-backlog.md:9919`):③「點擊客戶名稱…一樣是分割畫面方式顯示」
④「點擊料號變成該商品的內容,包含圖片、車款等資訊」。快照題他已拍 **Q1=A**
(下單當下凍結資料 + 現行商品圖 + 「開賣場頁」鈕,畫面標清楚「下單當下」vs「現行」,同條目)。

| 選項 | 做什麼 | 影響面 | 要不要重測 |
|---|---|---|---|
| **A1 兩張卡一起做** | 新增 `@panel/customers` + `@panel/products` 兩個槽頁,列表客名與料號都變連結 | 動 `orders-table.tsx` 一次(兩欄一起加連結)+ 兩個新槽頁 | 面板互斥行為要測(同時只能開一個) |
| **A2 只做客人卡** | 只做 ③,料號維持純文字 | 動 `orders-table.tsx` 一欄 | 較小 |
| **A3 這輪都不做** | ③④ 排到 #348 自己的片 | 零 | 零 |

**推薦 = A2(只做客人卡)**。理由:客人卡的資料源已經整頁存在(`app/customers/[id]/page.tsx`),
是「搬進槽」而非「從零建」;商品卡則要決定圖片來源(`product_snapshot` 唯一沒凍結的就是圖片,
條目 `:9921` 實查註記)、要不要接賣場頁、車款資料從哪來,是一整片的量,混在這輪會超過鐵則 4。
⚠️ 但 A2 仍受 §1-4 那條前置約束:**要先拍 #447**。

### B. #447 —— 訂單列表雙 markup

| 選項 | 做什麼 | 視覺會變成什麼樣 | 影響面 | 重測 |
|---|---|---|---|---|
| **B1 單一 markup + CSS reflow** | `<table>` 改 grid/flex,桌機手機共用一份 DOM | **會變**:桌機表格線與欄寬會重畫,手機卡片版型可能微調 | 動 `orders-table.tsx`(476 行)+ `shared/admin-data-table.tsx`(166 行,鐵則 12⑥)| 兩套響應式 + 視覺回歸,估 2-3 片 |
| **B2 維持兩份 + 機械守門** | 兩份 markup 留著,加一條守門強制「帶互動的欄位只能在一個槽出現」 | **不變** | 動守門與測試為主 | 估 1 片 |
| **B3 這輪不拍,繼續欠** | 維持現狀 | 不變 | 零 | 零 |

**推薦 = B1**,但**前提是 Sean 願意接受列表視覺重畫一次**。理由:③④ 各會再加一個可點欄位,
B2 的守門只能擋「帶互動的控件」,擋不住「每條紀律寫兩遍」這個真正的成本
(A13 已經實收一次:`relative z-10` / 已取消不給入口 / 目的地各自沿用同槽,三條全寫兩遍,
靠兩支測試各釘一次才擋得住,條目 `:11880-11884`)。
若 Sean 不想動視覺 → 選 B2,但要知道 ③④ 之後每加一欄都再欠一份。
🔴 這一題的鐵則 6 拆檔綁在同一個決定裡(`orders-table.tsx` 476 行 > 400 行必拆線,
`wc -l` 實測):怎麼切檔 = 怎麼切 markup,分開做等於同一件事決定兩次(條目 `:11875-11877`)。

**附帶死區**(B1 選了就自動消失,B2 要另外處理):已取消的單,操作欄那格顯示「—」但仍帶
`relative z-10`,浮在整列 stretched link 之上又自己沒連結 ⇒ 點那格沒反應
(`docs/phase-1-backlog.md:11888-11893`)。

### C. #446 —— 沖銷之沖銷後,「已沖銷」徽章退不退得回

情境:A 被 B 沖銷 ⇒ A 掛「已沖銷」。再把 B 沖掉(誤沖更正路徑,Sean 08-10 拍板)⇒
帳上錢已回到「已收」(6800 − 6800 + 6800 = 6800),但 A 那列**仍掛著「已沖銷」**。

| 選項 | 徽章講什麼 | 副作用 | 影響面 |
|---|---|---|---|
| **C1 講現況** | A 的沖銷被沖掉 ⇒ 撤下徽章 | 🔴 **同時打開「A 可以再被沖一次」**(共用 `isReversed`,`payment-list-view.ts:245`)| 語意層 + 元件 + 測試 |
| **C2 講歷史 + 加註** | 徽章留著,旁邊加一行「這筆沖銷後來被更正了」 | 沖銷鈕維持關閉 | 同上,多一段文案 |
| **C3 彙總行標記** | 列上不動,彙總行旁標「本單有沖銷之沖銷」 | 列與彙總仍不一致,只是有提示 | 最小 |

**推薦 = C2**。理由:條目自己寫的痛點是「員工看列信徽章、彙總行才說真話 ⇒ 以為帳算錯 ⇒
再登一筆收款 ⇒ **重複入帳**」(`:11848-11851`),C2 直接消滅這個誤解而**不改動任何沖銷權限**;
C1 雖然最乾淨,但它把一個顯示題變成權限題(誰能再沖一次),那需要單獨的風險評估。

### D. #437 殘題 —— 訂單標頭要不要再一顆「已收足」徽章

**這題不是條目那四點**(四點已完工,見 §0)。真正的殘題來自實作者的讀法申報:
D 窗十代把條目②尾的「訂單層已收足徽章」與④的已收足徽章**讀成同一顆**,實作一次畫在收款卡頂
彙總行,並逐字寫「若 Sean 要的是訂單標頭另外一顆,那是另一片」(`/Users/sean_1/pcm-mailbox/D-598-NOTE.md:73`)。

| 選項 | 做什麼 | 影響面 |
|---|---|---|
| **D1 就一顆** | 維持現狀(徽章只在收款卡頂),條目②尾字面標為已由④吸收 | 零 code,只改條目字面 |
| **D2 標頭再一顆** | 訂單標頭加一顆狀態徽章 | 動 `order-detail.tsx`(440 行,`wc -l` 實測),要決定與現有標頭徽章的排列 |

**推薦 = D1**。理由:同一個事實在同一個畫面出現兩次,兩處的計算來源若哪天分岔就會互相打架
(#446 就是「同一張卡上兩個面互相矛盾」的實例);而且收款卡頂彙總行本來就是退款線
(已收 − 已退)未來的掛載點(條目 `:11692` 逐字)。要讓 Sean 一眼看到狀態,更省的做法是
等他肉眼驗現況後再說,不是先加一顆。

---

## §3 demo 需求規格(給 Claude Design;我不做視覺)

> 🔴 **範圍已因 §0 縮小**:#350①② 已上線 ⇒ **那兩項不需要 demo,Sean 直接看正式站即可**。
> 真正需要「給他看、不要他想像」的只有兩塊:**#447 的列表重排(B1)** 與 **#350③ 的客人卡**。

### D-1 #447 列表重排 demo(對應選項 B1)

- **要幾個方向**:3 個,差異要夠大、互相打架,不是同款微調。
  - 方向甲:**保表格感** —— grid 排出與現在幾乎相同的欄線與對齊,桌機看起來「沒變」。
  - 方向乙:**卡片化** —— 桌機也走卡片/分區塊,一張訂單一塊,欄位靠標籤而非欄頭。
  - 方向丙:**主從式** —— 訂單層一行(單號/日期/客戶/金額),品項層縮排成子列,操作類入口只出現在訂單層那一行。
- **素材**:用真實列表當底 —— 現行 13 欄依序為
  勾選 / 訂單編號 / 日期 / 品牌 / 料號 / 品名 / 年份廠牌車種 / 數量 / 金額 / 客戶 / 訂貨 / 發票 / 操作
  (`apps/admin/src/components/orders/orders-table.tsx:442-458` 逐字欄名)。
  資料請用真實形狀:**一張訂單多個品項**(訂單層欄目前是 rowSpan 合併,`:18` 檔頭逐字)、
  其中至少一張是**已取消**的單(操作欄顯示「—」的那種)。
- **兩種寬度都要**:桌機(≥1280,且**面板開著時內容區只剩約一半**,面板下限 320px 見
  `workspace-panel.ts:33`)+ 手機(≤430)。🔴 桌機那版**一定要畫面板開著的樣子** ——
  員工 90% 時間是「列表 + 面板」並存,只畫全寬列表等於沒驗到真實工作狀態(Sean 08-09 逐字
  「我們 90% 時間都會是跟截圖一樣的狀態下工作」,`docs/phase-1-backlog.md:9917`)。
- **每個方向必須答**:操作類入口(取消/未來的退款、沖銷)放哪一格、客名與料號變成可點之後長什麼樣。

### D-2 #350③ 客人卡 demo(對應選項 A2)

- **要幾個方向**:2 個。
  - 方向甲:**摘要優先** —— 面板上半顯示基本資料+儲值金餘額,歷史訂單收在下方可捲區。
  - 方向乙:**歷史優先** —— 面板主體就是這位客人的歷史訂單列表,基本資料壓成頂部一行。
- **素材**:欄位以整頁版現有接線為準(基本資料 / 歷史訂單 / 愛車 / 地址 / 錢包,
  `apps/admin/src/app/customers/[id]/page.tsx:10-19` 的 repository import)。
- **寬度**:只需畫面板寬(320〜約半螢幕),因為它只活在面板裡;手機版沿用整頁版、不需另畫。
- **必須答**:面板裡要不要給「開整頁版」的出口。

### D-3 不需要 demo 的

#446 與 #437 殘題是**語意題**,選項用文字就能拍(§2-C、§2-D),畫圖不會讓決定更好做。

---

## §4 這輪不做 / 不是視覺題的

- **#437 條目那四點** —— 已完工(`76aff2e3`),不要再拿出來當待決題。**條目狀態欄是過期的**。
- **#350①②** —— 已上線,Sean 要做的是**肉眼驗**,不是拍板。
- **#447 的鐵則 6 拆檔** —— 看起來像技術債、實際上綁在 B1/B2 這個決定裡(§2-B),
  但**它本身不需要 Sean 拍**:拍完 B1/B2 之後怎麼切檔是實作判斷。
- **#348 客人 360 的完整內容** —— 本輪只拍「面板裡的客人卡長怎樣」,那頁本身的功能完整度
  是 #348 自己的片。⚠️ 該頁現況完整度**未確認**(§1-2)。
- **兩個條目的狀態欄更正** —— 我**沒有自行改** backlog(`git status --porcelain` 空)。
  #437 `:11689` 應由「⏳ 已拍板待做」改為「✅ 四點完工 `76aff2e3`,殘題=標頭徽章」;
  #350 應補「①② 已完工(七顆),剩 ③④」。請主視窗裁定由誰改、何時改。

---

## §5 驗收自核(對 `D-631-DISPATCH` §驗收條件)

| 條件 | 自核 | 說明 |
|---|---|---|
| 四題全成題、選項互斥可執行有推薦 | ✅ | A1-A3 / B1-B3 / C1-C3 / D1-D2,各有推薦與理由 |
| 連動圖每個斷言附 `檔案:行號` | ✅ | §1 八條逐條附;唯一沒把握的一條(客人頁完整度)已寫「未確認」 |
| #350/#447 有 demo 需求規格而非文字想像 | ✅(範圍已修正) | §3;#350①② 因已上線改為「直接看正式站」,#350③ 與 #447 有 demo 規格 |
| 沒動 code、沒做視覺、沒替 Sean 拍板 | ✅ | 本檔是唯一新增檔;`git status --porcelain` 除本檔外空 |
| 檔內引用路徑逐一 `test -e` 驗過 | ✅ | 見下方命令與輸出 |

```bash
for p in apps/admin/src/components/orders/orders-table.tsx \
         apps/admin/src/components/shared/admin-data-table.tsx \
         apps/admin/src/lib/orders/payment-list-view.ts \
         apps/admin/src/lib/layout/workspace-panel.ts \
         apps/admin/src/app/@panel/orders/page.tsx \
         apps/admin/src/app/customers/\[id\]/page.tsx \
         apps/admin/src/components/orders/order-detail.tsx \
         docs/specs/2026-08-10-350-workspace-shell-plan.md \
         docs/specs/2026-08-12-order-admin-acceptance-package.md \
         docs/phase-1-backlog.md; do test -e "$p" && echo "OK $p" || echo "MISSING $p"; done
```

— D 窗十二代
