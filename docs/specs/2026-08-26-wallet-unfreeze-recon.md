# 盤點 · 儲值金現在是什麼狀態(`#202` 解凍前置)

> ✅ **2026-08-26 已答。本檔 §5 的那一題【關掉了】,而答案比選項多一格 —— 見檔頭下方。**
> **這是盤點,不是規格,更不是動工許可。零 code、零 schema、零設計。**

---

# ✅ §-1 Sean 2026-08-26 的答案(逐字,不是轉述)

> 「**甲 只顯示餘額和明細**」
> 「**法規我確定了,解凍,但是之後再做,現在先顯示就好**」
> 「**等網站上線後再補**」

**⇒ 拆成三件,三件的效力不同:**
```
① 這一輪做   = 甲, 只顯示餘額與明細 —— 零法規風險、今天做得完
② 🔴 法規那格 = 【他拍了, 解除】⇒ #202 的 hold 理由「台灣儲值法規未定」不再成立
③ 其餘(結帳折抵 / 客人自己儲值)= **上線後再補** —— 不是不做, 是排在上線之後
```

🔴 **而 ② 有一格我原樣記下、不替他補理由**:
> 他說「**法規我確定了**」,而**他沒有說是怎麼確定的** —— 查過了 / 問過人 / 判斷風險可接受,**我們不知道**。
⇒ **落檔一律寫「法規那格已確定、hold 解除(依據未載明)」。**
⇒ 🔴 **不得寫成「法規已釐清」或「已合規」** —— 那兩句我們沒有證據,
   而**一個關於法規的宣稱寫錯了,下一個人不會回頭查**。

⚠️ **而 §4 那張四選一的表,現在要這樣讀**:
```
甲 只顯示        ⇒ ✅ 這一輪做
乙 結帳可折抵    ⇒ ⏭ 上線後
丙 客人自己儲值  ⇒ ⏭ 上線後(而它原本的法規前置【已解除】, 剩 TapPay 與工程量)
丁 只是別藏分頁  ⇒ 被甲涵蓋
```
⚠️ **§6 那條「我沒有查台灣法規現在是什麼狀況」仍然成立且【更重要】了** ——
**他確定了,而我們仍然不知道依據。那兩件事不衝突。**

🔴 **① 這一輪那一片【還沒開工】** —— 它要動 `apps/storefront` 會員中心
(`AccountView.tsx:102` 的 stub),**不是線 4 的檔案面** ⇒ 先寫規格,動工等主視窗劃面。

---

> 觸發:Sean 2026-08-26 逐字「**兩個都做 —— 優惠券做, 儲值金也一起解凍**」。
> 產出者:施工窗 pcm-website-v2-1d(線 4),2026-08-26。
> 主視窗指示逐字:「**不要自己開工做儲值金** —— 先出一份『它現在是什麼狀態』的盤點」。

---

# 🔴🔴 §0 最重要的一句,放最前面

## **儲值金不是「一件被凍住的事」,是【兩件】—— 而其中一件早就做完了,另一件是【法規】凍的,不是工程凍的。**

```
後台(admin)那半   ✅ 【已經做完並上線】—— Sean 自己 2026-07-16 override 過
前台(顧客站)那半  ⏸ 【仍在 hold】—— 而凍它的理由是【台灣法規】, 不是我們沒空做
```

⇒ **所以「儲值金也一起解凍」這句話,現在指不到一個唯一的東西。**
⇒ 🔴 **而它的其中一個讀法會踩到 Sean 自己畫過【兩次】的那條線** ⇒ **必停問(R3:法規 / 與拍板矛盾)。**

---

# §1 凍它的理由(逐字,不是我轉述)

`docs/phase-1-backlog.md:5946` 條目標題逐字:
> **`### #202. ⏸ 儲值金 deposit 功能 hold(台灣儲值法規未定)+ g-7 wallet 頁推延`**

同條目 `問題 / 背景` 逐字:
> 台灣「儲值」踩法規邊緣 —— **多用途儲值金可能落入《電子支付機構管理條例》、需金管會核准/牌照。**
> Sean 還沒想清楚商業+法規模式。
> 在定案前**不該先把儲值功能(連 mock 寫 ledger)做出來**、避免架構/UI 先承諾未定模式。

`依賴` 那一欄逐字:
> **台灣儲值法規/商業模式定案(Sean)** + TapPay sandbox(`#3`)+ 真扣款合規

⇒ 📌 **這不是「排不進去」,是「還不知道合不合法」。** 兩者的解凍條件完全不同。

---

# §2 而 Sean 自己已經解過一次,**而他當時把範圍限死了**

memory `project_wallet-deposit-taiwan-legal-hold`,2026-07-16 那節逐字:
> Sean 拍:**admin 後台客戶明細「全放儲值金,且儲值金可修改」**。
> **Why:** admin 是**內部系統**,**不考慮台灣儲值法規**、只考慮內部使用方便性(Sean 原話)。
> 這是對下方 05-31 hold 的**刻意 override**(非忘記舊拍板)、**但範圍僅 admin 後台**。

**同一份 memory 同一節,結尾逐字**:
> 🔴 **`storefront` 前台仍 hold**:WalletTab 維持 stub、前台 deposit 不做(下方 05-31 仍有效);
> **override 不外溢到會員前台。**

⇒ 🔴 **他不是忘了,他是【明確地只解了一半】,而且寫下了「不外溢」。**

---

# §3 現在實際上有什麼(我逐項量的,不是讀文件)

```
✅ 已經有的(地基完整)
  customer_wallet_ledger 表        ⇒ 3 支 migration 命中
  depositWallet use-case           ⇒ packages/use-cases/src/deposit-wallet.ts 存在
  後台儲值金調整                    ⇒ apps/admin/src/lib/customers/wallet-actions.ts 存在
  對帳 view customer_wallet_balance_check(條目記載)

❌ 沒有的
  顧客站的 deposit 入口             ⇒ grep 'depositWallet|立即儲值' apps/storefront/src ⇒ 0 支檔
負對照(當天靶)zzzWallet20260826 ⇒ 0
```

### 🔴 3-a 而這一格有兩個數字,兩把尺都對 —— **下一個人要知道自己在數哪一個**
```
我量  「有沒有【入口】」 grep -rln 'depositWallet|立即儲值' apps/storefront/src   ⇒ 0 支
主視窗量「有沒有【提到】」git grep -l 'depositWallet|儲值' -- apps/storefront/src ⇒ 7 支
```
**那 7 支我逐支開過,沒有一支是入口**:
```
AccountView.tsx        nav 上的分頁【標籤】文字
WalletTab.tsx          stub 本體            AccountView.test.tsx / WalletTab.test.tsx  測試
CheckoutStep2.tsx      寫著「不做」的【註解】  CheckoutView.tsx  同上
styles/checkout.css    寫著「不搬」的【註解】
```
⇒ 🔴 **「有沒有提到」= 7,「有沒有做」= 0。**
⇒ **而 7 那個數字會讓下一個人以為前台已經做了一半** —— 它數到的大部分是**寫著「不做」的字**。
📌 **同一個坑今晚出現過**(`L250`:板子的尺數到了它自己的說明書)。
**這裡是它的鏡像:尺數到了【一堆宣告「我沒做」的註解】,而那看起來像做了。**

**顧客站現況**(逐行開檔):
```
AccountView.tsx:102     會員中心【有】一個「儲值金」分頁(nav 上看得到)
tabs/WalletTab.tsx      而它是 stub
CheckoutStep2.tsx:35 逐字  「儲值金折抵(design N°05)+ 優惠券…不做(plan §3.2 + g-7 wallet HOLD #202)」
CheckoutView.tsx:39  逐字  「優惠券 / 儲值金折抵不做(plan §3.2 + #202)」
```
⇒ **結帳頁的「用儲值金折抵」也是關的** —— 而那與「能不能儲值」是**兩件不同的事**。

---

# 🔴 §4 於是「解凍」至少有【四種】意思,成本與風險差非常多

| | 做什麼 | 法規風險 | 成本 |
|---|---|---|---|
| **甲** | 會員中心那頁**只顯示**餘額 + 明細 | **零**(只是讀他自己的資料) | 最小,條目說「零架構改動」 |
| **乙** | 甲 + **結帳時可以用儲值金折抵** | **低-中**(花掉已有的錢,不是收新錢)⚠️ **未確認** | 中,要動結帳金額計算 |
| **丙** | 乙 + **客人自己儲值進去**(真的收錢) | 🔴 **高 —— 這就是 `#202` 凍的那一格** | 大,而且**還要等 TapPay + 法規定案** |
| **丁** | 只是「**別把那個分頁藏起來**」 | 零 | 零 |

🔴 **而 `丙` 那一格,條目自己寫著它【還有一個我們控制不了的前置】**:
> 依賴:**台灣儲值法規/商業模式定案(Sean)** + TapPay + 真扣款合規

⇒ **就算今天說要做,丙 也做不完** —— 它卡在**法規定案**,而那不是工程進度。

---

# 🔴🔴 §5 所以我停在這裡,不往下寫規格。要問 Sean 的一題:

```
Q-儲值金 你說的「儲值金一起解凍」, 是哪一種?
   甲 會員中心那頁【只顯示】餘額和明細, 客人看得到自己有多少錢    ← 推薦
   乙 甲 + 結帳的時候【可以用儲值金折抵】
   丙 乙 + 客人可以【自己儲值進去】(真的線上收錢)
   丁 什麼都不做, 只是別把那個分頁藏起來

   👉 🔴 **選丙之前你要知道一件你自己說過的事**:
      2026-05-31 你拍板凍住它, 理由不是我們沒空 —— 逐字是
      「台灣『儲值』踩法規邊緣, **多用途儲值金可能落入《電子支付機構管理條例》、
        需金管會核准/牌照**」。
      而 2026-07-16 你把後台那半解開時, 明確寫了「**override 不外溢到會員前台**」。
      ⇒ **你畫過兩次那條線。丙 就是跨過它。**
      ⇒ 而就算現在說要做, 丙 **也做不完** —— 條目記著它還要等
        「台灣儲值法規/商業模式定案」+ TapPay, 而前者不是工程進度。

   ⚠️ **另外一件你可能不知道**:後台那半【早就做完了】——
      你 07-16 拍的「後台可顯示可修改」已經上線在跑。
      ⇒ 所以「解凍」如果指的是後台, 那件事已經是完成式。
```

📌 **而我推薦甲的理由不是「甲最安全」,是**:
**甲 是唯一一個【今天就做得完、而且不需要你先解決法規】的。** 乙 也做得動而它動到結帳金額;
丙 不論你多想做,它今天做不完。

---

# §6 這份盤點沒有做的(誠實邊界)

```
· 🔴 我【沒有】查台灣的法規現在是什麼狀況 —— 那不是我能查的, 而且 2026-05-31 之後
  法規可能變了。**條目寫的是【當時未定】, 不是【現在仍未定】** —— 這一格【未確認】。
· 乙(結帳折抵)的法規風險我標「低-中」而**那是我猜的, 不是查來的**。
  花掉已經在帳上的錢 vs 收新的錢, 直覺上不同, 而**我沒有依據**。⇒ 要真的做乙之前要問懂的人。
· 我沒有量「現在有幾個客人的 wallet_balance 不是 0」—— 沒有正式庫權限
  ⇒ 所以「甲 做出來之後客人會看到什麼」我答不出來(可能全部是 0)
· g-7 那份 recon(2026-05-31)我沒開, 只讀了 backlog 條目引用它的那幾句
· WalletTab.tsx 我只確認它是 stub, **沒有逐行讀它現在畫了什麼**
```

📎 座標:`docs/phase-1-backlog.md:5946`(`#202` 條目)· memory `project_wallet-deposit-taiwan-legal-hold` ·
`apps/storefront/src/components/CheckoutStep2.tsx:35` · `CheckoutView.tsx:39` ·
`apps/storefront/src/components/account/AccountView.tsx:102`
