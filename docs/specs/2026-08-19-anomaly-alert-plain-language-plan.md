# `#M-3-250` 告警訊息改成白話 + 帶單號 —— plan(鐵則 8,✅ **2026-08-19 Sean 已批**)

> 2026-08-19 · G2 · **起因是 Sean 今早 9 點收到一封他看不懂的告警。**
> 他逐字:「以後這種訊息可以寫直白一點嗎?**寫上訂單單號**比較方便查詢是哪一筆訂單有問題,
> 然後**訊息內容簡單白話、精簡**,因為真的看不懂。」
> 而主視窗端了 trade-off 給他,他逐字回**「可以」**。
>
> ✅ **2026-08-19 Sean 已批,本檔可開工**(~~原寫「本檔還沒開工。動 code 之前要你點頭」~~)。
> 出處逐條在本檔內、非新增:文案三題見 `:156`(`「依照推薦」= Q1甲 Q2甲 Q3甲`)、
> 長度那題見 `:155`(`乙:全部印出來`)、最後一題見 `## v2-8`(`q5: 甲`)。
> 🔴 **這一段是 2026-08-19 W2 對帳時補的** —— 三處答案本來就在檔內,**而檔頭還寫著「等 Sean 批」**
> ⇒ 打開這份 plan 的人(W4 正在做這條線)會以為不能開工。
> 📌 **拍板落檔是兩個動作:①寫下新裁定 ②把「等他裁」那句劃掉。這份只做了①。**

---

## 0. 🔴 先講一句會改變你判斷的:**「帶單號」不是改文案**

```
現在那支告警程式【拿不到單號】—— 不是沒寫，是【取不到】：
  check-anomaly-alerts.ts:18 逐字
    「reader 走 payment_confirmer SECDEF 受控窗、**對 anomaly 兩表零表權**；
      回傳**只計數、零 PII/零金額/零 id**」
  而那支 RPC `get_payment_anomaly_alert_summary` RETURNS jsonb，裡面**全是 count(*)**
  （20260701130000:43-95 逐行看過）
⇒ 🔴 **沒有任何 id 出得來。**
```

### ⇒ 所以這一片要動 **migration**,而它命中三條鐵則
```
12⑤ 對外發送（LINE 推播）      ← 原本就知道
12② 權限（要讓那個受控窗回 id）← 🔴 新的
12③ DB 結構（改那支 RPC）      ← 🔴 新的
⇒ 標準片、9 步 SOP 全走、commit 前 codex 對抗審查
```

### 🔴 而「零個資」是**刻意的設計**,不是漏寫
```
理由（原設計）：LINE 訊息會留在手機、可能被轉發/截圖
⇒ 而 Sean 2026-08-19 在收到一封他看不懂的告警之後【親自推翻它】
⇒ 他採納的理由：**沒單號他查不到，那則告警等於只說「有事」**
   ⇒ 而一個看不懂又查不到的告警，兩週後會被當成背景噪音
📌 **這一段要寫進 commit body** —— 下一個人要知道那道閘是【被誰、為什麼】打開的，
   而不是以為有人忘了它存在。
```

---

## 1. 現況(逐字,不是摘要)

他收到的那一封:
```
⚠️ PCM 付款異常告警

• 人工待確認(sweeper 放棄:pending 孤兒 / 被讓路轉人工)1 筆 — 兩類共用此計數,請查 plan §4 值班查詢分辨

請登入後台/MCP 查 W1 退款候選報表(open)+ Report C(pending 雙扣候選)對帳後,再決定 refund / dismissed(皆為候選、待查證,勿直接退款)。
(本訊息零個資、僅計數)
```
### 三條紀律(從他的原話推出來的)判它哪裡壞
```
① 白話 ⇒ 🔴 這一封有 8 個詞一個不寫程式的人看不懂：
   sweeper / pending / 孤兒 / 被讓路 / W1 / Report C / plan §4 / dismissed
② 精簡 ⇒ 他說「精簡」，而這封有三段、最長一行 47 字
③ 下一步要做什麼 ⇒ 🔴 現在寫的是【流程】不是【動作】：
   「查 A 對帳 B 再決定 C」—— 而 A 與 B 他打不開（W1/Report C 不是後台的頁面名）
```

## 2. 要改成什麼(草稿;主視窗端過、Sean 說可以)

```
⚠️ PCM 付款需要你確認 1 筆

訂單 PCM-2026-0104
狀況：這筆刷卡卡在中間 —— 可能已經扣款但沒完成，也可能重複扣了

請到後台「退款異常」看這一筆，確認之後選擇退款或標記免處理。
⚠️ 先查清楚再退款。
```
⚠️ **字面未定稿** —— 上面是主視窗的草稿,我可以寫得更好,**而不會更技術**。

> ## 🔴 對外文字的授權邊界(2026-08-19 補;**做這條線的人必讀**)
> **Sean 授權寫進告警的只有兩樣**:①**訂單單號** ②第五類可以描述「**同一位客人、同樣金額買了兩次**」。
> 🔴 **不含任何其他客人資訊** —— 不印客人姓名 / email / 電話 / 金額。
> ✅ **W3 已實查該邊界目前守住**(`~/pcm-mailbox/W3-003-審W4告警片-20260819.md:106`):
> migration `RETURNS` 的五個鍵只有 `display_id`;`customer_user_id` 與 `total` **只出現在 `DISTINCT ON` 與 `JOIN` 條件**
> ⇒ **篩選用,從未被投射出來**;第五類訊息是**描述句**,沒有印出客人或金額。
> ⚠️ **本段的證據等級**:①「授權範圍」是**主視窗轉述**,本窗**未取得 Sean 逐字原話**
> (`grep -rn '同客同額' ~/pcm-mailbox/` ⇒ 只命中 W3 的審查與本輪對帳,**沒有一份是他的原文**)
> ②「邊界守住」是 **W3 實查**,本窗未自驗。⇒ **兩格都不是本窗量的,引用請帶這個限定。**

---

## 3. 🔴 三個要你拍的(而它們會改變做法,不是潤飾)

### Q1 · **多筆的時候長什麼樣**(現在是 1 筆,而 5 筆時不能變成五段)
```
甲 列前 3 筆單號 + 「另有 N 筆」  ⇒ 訊息長度有上界，而他一眼看得到最急的
乙 只給總數 + 一句「請到後台看」  ⇒ 最短，而他還是得自己去找是哪幾筆
👍 我建議甲，理由：他要的就是「哪一筆」，而乙等於把問題退回原點
```

### Q2 · 🔴 **兩類共用一個計數,而它們的處置不同 —— 要不要分開講**
```
現況：那個計數是【pending 孤兒】∪【被讓路轉人工】的聯集
而 code 註解逐字：「兩者的處置完全不同：前者查 Record 對帳、後者是『錢被讓給另一張單』要走退款線」
🔴 而我【沒有查出】怎麼從資料上乾淨分辨這兩類。**數法與我看到的**:
     `git grep -n 'needs_manual_review' -- supabase/migrations | grep -iE 'SET'` ⇒ **2 條路**
       `20260615120000:53`  改的是 `payment_webhook_events`（**不是告警讀的那張表**）
       `20260615120001:94`  改的是 `payment_charge_attempts`（告警讀這張）
     ⇒ 兩條都是 `count >= 8` 轉人工，而它們寫的是**不同的表**
     ⇒ 🔴 **所以「兩類」在告警讀的那張表上共用同一個布林欄，我找不到分辨欄**
     ⚠️ 而**「找不到」的分母是**:我只掃了 `supabase/migrations`，**沒有掃 app 層的寫入者**
⇒ 甲 帶了單號之後【不分類】—— 他點進那一筆就看得到細節，分類是後台的事
   乙 先把分辨方法查清楚，訊息裡分兩句寫
👍 我建議甲，理由：**單號本身就把分類問題溶掉了** —— 而乙要先做一份我還沒做的查證
```

### Q3 · **要不要保留一句「先查清楚再退款」**
```
現況那封有「皆為候選、待查證，勿直接退款」(`check-anomaly-alerts.ts:119` 逐字)⇒ **不白話**，而它防的事重要
👍 我建議保留（草稿已含），理由：那是唯一一句在防【動錯錢】的
```

---

## 4. 不做 / 邊界

```
❌ 不改門檻、不改觸發頻率（那是另一件事）
❌ 不改 Email 那個管道的格式以外的東西（同一支 buildAnomalyAlertMessage 出兩邊）
   ⚠️ 而【Email 也會跟著變】—— 那是同一個函式，要一起看過
❌ 不動 sweeper 本身的行為
```

## 5. 驗收(逐條 yes/no)

```
□ 訊息裡零技術詞（拿 §1① 那 8 個詞逐一 grep 訊息輸出，全部 0）
□ 帶得出單號（而它從哪來 = 改過的 RPC；測試要釘住「RPC 沒回 id 時訊息不得憑空編一個」）
□ 多筆時長度有上界（照 Q1 的答案；測試要有 1 筆 / 3 筆 / 10 筆三個案例）
□ 🔴 零筆時不寄 —— **現況已經是這樣**（`check-anomaly-alerts.ts:136-141` 的 shouldAlert）
   ⇒ 這一格是【回歸守門】，不是新功能
□ Email 那一邊也看過（同一個函式出兩邊）
□ 四綠 TURBO_FORCE=1（動 .ts ⇒ 含 build）
□ 🔴 codex 對抗審查（鐵則 12②③⑤ 三中）
□ 而 migration 走拋棄式 PG 驗過再 apply（照 docs/runbooks/throwaway-postgres-…）
```

## 6. 誠實揭示

```
· 🔴 我【沒有跑過】那封告警 —— 現況那段字是 Sean 收到的原文（主視窗轉貼）
· 而「兩類怎麼分辨」我沒查出來（Q2 的甲案就是繞過它，而那要你同意）
· 那 8 個技術詞是我讀 code 數的，不是問人來的 ⇒ 🔴 **可能有第 9 個我看不出來**
  （我看得懂它們，所以「看不懂」這件事我是推的）⇒ **定稿前請你自己念一遍**
· ✅ 而【它多久寄一次】我補查了:**每天一次,台北時間早上 9 點**
  `20260723120000_m3_s2_settle_sweep_pgcron.sql:12` 逐字 `pcm-anomaly-alert(0 1 * * *)`
  ⇒ UTC 01:00 = 台北 09:00 ⇒ **與 Sean 說「今早 9 點收到」對得上**
  ⇒ 🔴 **所以它每天都會寄一封** ⇒ 「精簡」比我原本想的更重要:
    一封每天出現而看不懂的訊息,兩週後會被當成背景噪音,**而那時它就不再是告警了**
```

---
---

# v2 · 定案實作 plan(2026-08-19 · W4 · ✅ **已批** —— 唯一待答的 §v2-5 見 `## v2-8`「`q5: 甲`」)

> **Sean 已答的四題(來源 `~/pcm-mailbox/MAIN-056-…:173-174` 逐字,我沒有重打)**:
> `告警文案長度 ⇒ 乙:全部印出來,長就長(推翻我推薦的甲)`
> `告警文案三題 ⇒ 「依照推薦」= Q1甲 Q2甲 Q3甲`
>
> 🔴 **而這兩行互相打架,我沒有自己選,我判給【後面那句更明確的】** ——
> Q1甲 = 「前 3 筆 + 另有 N 筆」(有上界);而「長度=乙:全部印出來」= **沒有上界**,
> 且 MAIN-056 自己標了「**推翻我推薦的甲**」⇒ **本 plan 照【乙】做:全部單號列出來、不截斷。**
> ⚠️ 而**沒有上界會帶出一個新風險**,見 §v2-5 那一題 —— **那是本 plan 唯一要他回的字。**
> ✅ **那一題已答**(`## v2-8`:`q5: 甲` = 加保底「前 30 筆 + 另外還有 M 筆」)⇒ **本 plan 無待答項。**

## v2-1 · 五類的現況逐字(檔案:行號;不是摘要)

| # | 類別 | 現況文案在哪 | 拿得到單號嗎 |
|---|---|---|---|
| ① | 雙扣候選 open | `packages/use-cases/src/check-anomaly-alerts.ts:89` | ✅ `payment_double_charge_anomalies.old_order_id` → `orders.display_id` |
| ② | 退款卡逾時 refunding | 同檔 `:92` | ✅ 同上(同一張表) |
| ③ | 人工待確認(兩類聯集) | 同檔 `:98-100` | ✅ RPC 內**已經 JOIN 了 orders**(`20260701130000:81`) |
| ④ | released 死卡 | 同檔 `:108` | ✅ 同上(`20260701130000:88`) |
| ⑤ | 疑似重複扣款(組) | 同檔 `:112-114` | ✅ `o1`/`o2` 兩張單、**要出兩個單號** |

**單號欄 = `orders.display_id`(`PCM-YYYY-NNNN`)**,建表在 `20260604120000_m3_s2a_orders_order_items.sql:94` 逐字 `display_id text NOT NULL UNIQUE`。

## v2-2 · 要改的東西(三處,順序不可換)

```
一 · migration(新一支,不改舊檔)
    CREATE OR REPLACE FUNCTION public.get_payment_anomaly_alert_summary(
      integer, integer, integer) RETURNS jsonb
    🔴 參數與回傳型別【都不變】⇒ 可以 REPLACE、不必 DROP
       ⇒ 而那也代表 **ACL 不會掉**(DROP+CREATE 才會)⇒ 但斷言仍照舊跑一次
    加 5 個 jsonb array 欄(既有 7 個計數欄【一個都不動】):
      open_display_ids / refunding_stuck_display_ids /
      attempt_manual_review_display_ids / released_stuck_display_ids /
      pending_dc_display_id_pairs        ← ⑤ 是「一組兩張」,回 [[a,b],…]
    每個 array 各自 ORDER BY + LIMIT(見 §v2-5)

二 · domain 型別 packages/domain/src/payment/anomaly-alert.ts
    AnomalyAlertSummary 加 5 個欄
    🔴 該檔 :16 逐字寫著「零 PII / 零金額 / 零單號」⇒ **那句要改,不是刪**
       改成「零 PII / 零金額;🔴 單號自 2026-08-19 起【刻意】帶出來,授權人=Sean,理由見下」

三 · 文案 packages/use-cases/src/check-anomaly-alerts.ts:79-123
    buildAnomalyAlertMessage 五段全部重寫(見 §v2-3)
    ⚠️ **LINE 與 Email 是同一個函式出兩邊** ⇒ 兩邊一起變,這是預期不是副作用
```

🔴 **中間那層 adapter 也要改**:`PgAnomalyAlertReaderAdapter`(snake_case jsonb → camelCase domain 的映射點)。
⚠️ **我沒有開過那支檔** —— 我是從 `anomaly-alert.ts:5-6` 的檔頭推的(逐字「adapter 邊界把 DB snake_case jsonb 映射成 camelCase domain」)⇒ **這一行是【推的】,實作第一步要先開它確認。**

## v2-3 · 五段新文案(草稿;字面 Sean 可以直接改)

```
⚠️ PCM 付款要你看 5 筆

【可能被扣了兩次錢】2 筆(最久的已經 2 天)
  PCM-2026-0104
  PCM-2026-0098

【退款卡住,超過 24 小時還沒退成】1 筆
  PCM-2026-0091

【刷卡卡在中間,系統自己處理不了】1 筆
  PCM-2026-0104

【付款沒完成,但錢可能還被鎖著】1 筆
  PCM-2026-0104
  (這一筆和上面那項可能是同一張單,不是兩件事)

【同一位客人、同樣金額買了兩次,其中一次卡很久】1 組
  PCM-2026-0110 ＋ PCM-2026-0111

請到後台找這幾張單,看過之後再決定要退款還是標記免處理。
⚠️ 先查清楚再退款 —— 上面每一筆都只是「可能」,不是已經確定。
```

**判它有沒有做到「白話」的機械尺(驗收用)**:拿原本那 8 個技術詞逐一 grep 訊息輸出 ⇒ 全部 0。
`sweeper` / `pending` / `孤兒` / `被讓路` / `W1` / `Report C` / `plan §4` / `dismissed`
🔴 **而這把尺只證「那 8 個沒了」,不證「沒有第 9 個」** —— 第 9 個只有 Sean 念一遍才找得到。

📌 **④ 那句「可能是同一張單」是既有行為的保留**(`check-anomaly-alerts.ts:107` 的 `overlapNote`),
而**現在可以做得更硬**:帶了單號之後,**比對兩邊的單號一樣就直接講**,不必再寫「可能」。

## v2-4 · 風險面(這一片真正的成本在這裡)

```
🔴 R1 · 個資閘是【被打開】不是【忘了關】
   原設計逐字：check-anomaly-alerts.ts:18「零 PII/零金額/零 id」
   理由：LINE 訊息留在手機、可能被轉發截圖
   ⇒ 2026-08-19 Sean 本人推翻，理由=沒單號他查不到
   ⇒ **這段要逐字寫進 commit body 與 domain 檔註解**，否則下一個人會以為有人漏寫

🔴 R2 · 單號【不是】姓名地址，但它是可識別指標
   拿到單號 + 後台權限 ⇒ 看得到那位客人的全部資料
   ⇒ 而 LINE 推播對象是 env 設定的單一 userId/groupId（LineAlertNotifierAdapter.ts:28-31）
   ⇒ **若那個 to 曾經設成群組，等於把單號送進一個多人房間** ⇒ 上線前要確認它現在指向誰
   ⚠️ **我沒有查那顆 env 的實際值**（那要 Vercel 面板）⇒ 這是【要 Sean 看一眼】的一格

🔴 R3 · 「全部印出來」沒有上界，而 LINE 的訊息有
   LineAlertNotifierAdapter.ts 全檔 58 行、**零截斷、零長度檢查**（我逐行看過）
   ⇒ 若某天有 200 筆，訊息會超過 LINE 單則上限 ⇒ **整封送不出去 ⇒ 靜默失去告警**
   ⚠️ **上限的確切數字我【未確認】** —— 缺的那一道檢查 = 讀 LINE Messaging API
      官方 text message object 的 `text` 欄長度上限（我抓過一次，那一頁沒有列出來）
   ⇒ ⇒ **這就是 §v2-5 那一題**

🟡 R4 · 它【每天早上 9 點都會寄一封】
   20260723120000_m3_s2_settle_sweep_pgcron.sql:12 逐字 `pcm-anomaly-alert(0 1 * * *)`
   ⇒ 舊帳（例如那筆 2SQH2P）會**天天出現在同一個位置**
   ⇒ 帶了單號之後至少他看得出「又是同一張」，而不是以為每天都有新事故
   📌 這不是本片要解的，但它是本片會改變的體感

🟡 R5 · 零筆時不寄 = 現況，本片是【回歸守門】不是新功能
   check-anomaly-alerts.ts:136-141 的 shouldAlert，五個計數任一 >0 才寄
```

## v2-5 · 🔴 唯一要你回的一題(其他都已定案)

```
Q · 「全部印出來」碰到 LINE 的長度上限時要怎麼辦？

甲　加一個保底：超過 N 筆就列前 N 筆 + 一行「另外還有 M 筆，請到後台看」
    ⇒ 訊息一定送得出去；而**極端狀況下**你會看不到完整清單
乙　不加保底，真的全部印
    ⇒ 平常最完整；而**筆數暴增的那一天，整封告警會送不出去 ⇒ 你什麼都收不到**
    🔴 而那一天正好是最需要收到告警的那一天

👍 我建議【甲】，N 先設 30。理由不是「怕長」，是**乙的失敗方式是靜默的** ——
   它不會少寄幾行，它會【整封不寄】，而 route 那邊只會看到一個推播失敗。
   而甲在正常情況（你現在是 1 筆）與乙**逐字相同**。
```

## v2-6 · 驗收(逐條 yes/no)

```
□ 五段各自帶出單號（1 筆 / 多筆 / ⑤ 的兩張一組，三種案例各一測試）
□ RPC 沒回 id 時，訊息**不得憑空編一個**（測試釘死：array 為空 ⇒ 那一段不列單號、只列筆數）
□ 8 個技術詞 grep 訊息輸出 ⇒ 全部 0
□ ④⑤ 重疊時單號一樣 ⇒ 直接講「同一張單」（不再寫「可能」）
□ 零筆時不寄（回歸，check-anomaly-alerts.ts:136-141）
□ Email 那一邊也看過（同一個函式出兩邊）
□ ACL 斷言重跑：payment_confirmer=true、anon/authenticated/service_role=false
  （抄 20260701130000 §3 那個 DO 區塊）
□ migration 走拋棄式 PG 驗過再遞給 Sean（docs/runbooks/throwaway-postgres-…）
□ 四綠 TURBO_FORCE=1（動 .ts ⇒ 含 build）
□ 🔴 codex 對抗審查（鐵則 12②③⑤ 三中）
□ 🔴 不 apply、不 push —— apply 是 Sean 的動作
```

## v2-7 · 誠實揭示(照實,不美化)

```
· 我【沒有跑過】那封告警；現況那段字是 Sean 收到的原文（主視窗轉貼、我沒重打）
· `PgAnomalyAlertReaderAdapter` 那支檔**我沒開過**（§v2-2 已標）⇒ 那一行是推的
· LINE 訊息長度上限**未確認**，缺的檢查已寫在 R3
· LINE 推播對象那顆 env 的實際值**我查不到**（要 Vercel 面板）⇒ R2 是【要 Sean 看】的一格
· 「長度乙 vs Q1甲」我判給了乙，**而那是我的判讀不是他的原話** ⇒ 若判錯，改回甲只影響 §v2-5
```

---

## v2-8 · ✅ §v2-5 那一題 Sean 已答:**甲**(2026-08-19)

> 原文轉錄(主視窗轉,我沒重打):`q5: 甲`

```
= 加保底：前 30 筆 + 一行「另外還有 M 筆」
```

### 🔴 甲【不推翻】乙 —— 這一段要原封寫進 code 註解

```
乙（全部印出來，長就長）＝ 他要的【正常行為】
甲（超過 30 筆才截斷）  ＝ 那一裁的【失效保護】，不是它的相反
⇒ 他現在是 1 筆；在 30 筆以下，甲與乙的輸出【逐字相同】
⇒ 🔴 不寫這一段的話，下一個人讀到 `MAX_ORDERS_PER_CATEGORY = 30`
   會以為有人偷偷把「乙」改回「甲」——而那會讓他去「修正」它
```

### 🔴 `30` 這個數字的來源(它有來源,只是來源不是那份文件)

```
✅ 30 的來源 = 【手機上讀得完】
   Sean 的原話是「看不懂」與「精簡」，不是「太長送不出去」
   ⇒ 這是可讀性常數，不是安全上限

🔴 LINE 單則 text 的長度上限 = **未確認**
   缺的那一道檢查（寫死在這裡，不要再重找一次）：
     在瀏覽器展開 developers.line.biz/en/reference/messaging-api/
     → Message objects → Text message → 讀 `text` 那一列的 Max character limit
   已試四發全部拿不到（WebFetch ×3 個 URL、firecrawl directQuote + markdown
   maxAge=0 waitFor 6s）⇒ 那一頁**每一節都是點開才載入**，抓不到屬性表

⇒ 而【保底不依賴那個數字】：
   30 筆的最壞訊息長度算得出來 —— 單號一行 16 字元 × 5 類 × 30 + 標頭結尾 ≈ **2,800 字元**
   ⇒ 這**沒有解決**未確認那格，只是把風險縮成「除非上限低於 2,800，否則安全」
   ⇒ 🔴 實作時 `LINE_TEXT_MAX_CHARS` 留成具名常數 + 標「未確認」，查到再改一個值
   ⇒ 🔴 commit body 要明寫：30 是照可讀性定的、LINE 上限仍未確認、缺哪一道檢查
```

### ⏳ 而**本 plan 本身仍未被批**(鐵則 8)

```
Sean 回的是【我提的那一題】的答案，不是「plan 批了、可以動」。
⇒ 動 RPC 命中鐵則 8（動 API/schema）+ 鐵則 12②③⑤
⇒ 在拿到一句「開工 / 批了」之前，本片不開工。
```
