# `#1` 看今天要處理什麼 — 查證報告(C 窗夜跑,零 code、**不設計**)

> 事實親查於 `pcm-customers` @ `51ec2472`(branch `customers`)。🔴 **全部來自 repo 檔案,未對正式庫查詢。**
> §1-A 標 🟡,證據逐字「只有寫死兩顆 chip,**無可存的自訂檢視**」—— **兩半都複驗成立**(見 §1、§3)。

## §1 訂單列表現在能表達哪些條件(這是本報告最有用的一節)

**篩選軸七條 + 顯示設定一條,全部走 URL query key**(常數逐一開檔對過):

| 軸 | query key | 值域 / 出處 |
|---|---|---|
| 付款狀態 | `payment_status` | `order-list-view.ts:45`;值域 `PAYMENT_STATUS_VALUES`(`:117`) |
| 貨品軸 | `goods_axis` | `:58`;可**多值**,標籤 `GOODS_AXIS_LABEL`(`:205`) |
| 訂單來源 | `order_source` | `:59`;`web` / `manual_phone` / `manual_line` / `manual_other`(`20260712203000:41-43`) |
| 金流管道 | `payment_channel` | `:60`;`tappay` / `bank_transfer` / `cash` / `none`(`20260712203000:50-52`) |
| 日期區間 | `date_from` / `date_to` | `:468-469`;預設 `m6`「近半年」(`:290`,Sean Q14=A) |
| 刷卡未付款 | `show_unpaid_card=1` | `:73-74`;**只有字面 `1` 算開**(`:67-72`) |
| 頁碼 | `page` | `ORDERS_PAGE_SIZE = 20`(`:42`) |
| (顯示設定)密度 | `den` | `:79`,`loose`/`std`/`tight`(`:92`);**刻意與篩選分開**(`:76`) |

**日期預設四格**:近一個月 / 近三個月 / 近半年 / 近一年 + 自訂(`:273-281`)。

🔴 **第八條軸不在 URL 裡 —— 關鍵字搜尋走 cookie。**
`app/orders/page.tsx:91` 逐字 `readOrderKeywordCookie((await cookies()).get(ORDER_KEYWORD_COOKIE)?.value)`。
而且**這是拍板不是實作偷懶**:`keyword-search-action.ts:18` 逐字「🔴 **PRG 是紅線不是風格(Q-a=B)**:搜尋詞不進 URL ⇒ 表單必須 POST」;
`:25` 逐字「**搜尋詞絕不落 log**:migration `20260809180000` 檔頭 `:50-74` 明令」。
⇒ **任何「把目前畫面存起來」的做法都要先回答:關鍵字算不算在裡面。**(§3 每一階都會撞到,這是本報告的承重發現。)

## §2 兩顆寫死 chip 是什麼、為什麼是那兩顆

`order-filter-chips.tsx:52-55` 逐字只有 `全部` / `未到貨` 兩顆(`CHIPS` 常數)。
`:47-50` 註解逐字:「字面逐字取自 OD `:611-613`」、「⚠️ OD 的順序是 `全部 / 待處理 / 未到貨 / 退貨中` —— **本片缺的兩顆進來時要插回原位**」。
⇒ **設計本來就是四顆,現在只落地兩顆**,而缺的兩顆各自卡在別的地方:
- **待處理** = 就是 `#1` 這件事本身(`#485`,定義已拍 **C = 兩者聯集**:還沒收錢 **或** 還沒訂貨);
- **退貨中** = `#500`,而 `#18` 查證已證實**退貨線整條不存在** ⇒ 這顆**沒有資料面可接**。

⇒ **`#1` 的最小可交付 = 把「待處理」那顆補上**,不是從零蓋一套檢視系統。

## §3 要做「可存的自訂檢視」有幾階(列階梯,**不選**)

| 階 | 做法 | 解決什麼 | **不**解決什麼 | 動幾檔 |
|---|---|---|---|---|
| **ⓐ 多加寫死 chip** | 往 `CHIPS` 常數加項 | 立刻給員工一顆「待處理」;**零 DB、零新概念** | 每個人看到的都一樣;要加就得改 code | **2-3**(`order-filter-chips.tsx` + 條件常數 + 測試)—— **未逐檔數,估** |
| **ⓑ URL 收藏(書籤)** | 什麼都不做,教員工存瀏覽器書籤 | 零工程 | 換電腦就沒了;**存不到關鍵字**(§1);沒人知道別人存了什麼 | **0** |
| **ⓒ 存進 DB** | 新表 `admin_saved_views`(每人多筆)+ CRUD + UI | 每人自訂、跨裝置、可分享 | **關鍵字仍存不進去**,除非推翻 `Q-a=B` | 新 migration + repo + action + UI + 測試 ⇒ **鐵則 8 必中、12③ 必中** |

🔴 **三階共同的硬前提**:`#26` 說「操作者是誰」目前仍是**自選**(`session/actor.ts:6-7` 自陳非授權邊界)
⇒ **ⓒ 的「每個人自己的檢視」沒有可靠的「人」可以綁**。ⓒ 若要做,`#26` 是它的硬前置,不是加分項。
⇒ **ⓐ 是唯一今天就能做、且不依賴任何未決事項的一階。**

## §4 與 `#16 今日對帳` 的邊界(先講清楚,避免兩片互等)

我讀了 A 窗的 `#16` plan(`3cd4c6e4:docs/specs/2026-08-14-e10-16-daily-reconcile-plan.md`),**它已經從自己那側劃好界**:
`§2` 逐字把「待辦檢視 / 每單負責人 / 下次跟進日」列進 **🔴 不做**,並逐字寫「**本片也不假裝第 1 項『看今天要處理什麼』因此變綠**」。

**邊界寫死(從 `#1` 這側對齊)**:

| | `#16 今日對帳` | `#1 看今天要處理什麼` |
|---|---|---|
| 回答的問題 | **今天的錢動了多少 / 哪裡卡住**(金額、筆數) | **哪幾張單現在輪到我動手** |
| 形狀 | 首頁 `/` 的四格 + 兩張短清單,唯讀彙總 | `/orders` 列表上的**篩選入口**(chip / 檢視) |
| 資料面 | `order_payments` / `order_refunds` / `orders` 計數 | 既有七條篩選軸(§1) |
| 交集 | `#16` 的兩張清單各附「看全部」**連到既有列表頁帶日期篩選** ⇒ **那個連結就是交會點** | 同上 |

⇒ **不重疊、不互等**:`#16` 給總覽數字,`#1` 給可點進去的工作佇列;`#16` 的「看全部」連結**正好落在 `#1` 的地盤**。
兩片可獨立進行;若 `#1` 做了 ⓐ,`#16` 的連結未來可以直接指向那顆 chip(**加分,不是依賴**)。

## §5 鐵則判定

- **ⓐ**:2-3 檔 ⇒ **鐵則 8 大概率不命中**(未逐檔數,若跨 3 檔就命中);鐵則 12 逐條過六類 ⇒ **全不命中**(不碰錢寫入/權限/schema/平台設定/對外/`packages/ui`)⇒ **輕量片**。
- **ⓑ**:零 code ⇒ 無鐵則可判(是教學不是工程)。
- **ⓒ**:新表 + CRUD ⇒ **鐵則 8 命中、鐵則 12③ 命中**;若檢視要綁「人」則 `#26` 為硬前置 ⇒ **高風險片**。
- **內容分級**:ⓐ 的 chip 文案 = **L2**(季 1-3 次可能調整)⇒ hardcode + TODO 可;ⓒ 的自訂檢視 = 員工自己維護的資料,**不是 L3 內容**(它不是對客文案)。

## §6 誠實缺口

1. **未對正式庫查詢**;§1 的軸與值域全部來自 repo 常數與 migration 字面。
2. **§3 的檔數是估的,不是數的** —— 我沒有為任何一階列出逐檔清單(ⓐ 標「2-3」也是估)。**不可拿去排工時。**
3. **我沒有讀 `#485`(待處理定義)與 `#500` 的 backlog 條目原文**,只引了 code 註解與交接檔對它們的描述 ⇒ §2 那兩顆 chip 的現況**可能已經前進**。
4. **「員工真正想要的『今天要處理什麼』是什麼」我沒有答,也不該答** —— §1 是「後台**能表達**的條件」清單,給 Sean 從既有條件裡挑;**不是需求調查**。
5. **關鍵字走 cookie 這條我沒有實測**(沒開瀏覽器看 cookie),依據是 `page.tsx:91` 與 `keyword-search-action.ts:18,25` 的字面。
