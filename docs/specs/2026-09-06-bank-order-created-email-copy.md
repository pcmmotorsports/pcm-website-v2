# ⟦b4-BANKNOEMAIL⟧ 匯款單成立信 —— **Sean 核可的那一份文案**(canonical)

> 🔴 **這一份是【程式從我端給 Sean 的那支檔逐字抄過來的】, 不是我重打的。**
> 來源:`~/pcm-mailbox/端Sean-0905早上佇列.md` §B「匯款客人的那封信」。
> Sean 2026-09-06 03:2x 逐字答「**甲 = 可以**」(主視窗 `-f8` 轉述並附他貼的全文)。
>
> 🛑 **括號裡的是佔位詞, 不是字面** —— 碼裡由變數填。而**佔位詞以外的每一個字元**
> (含標點、空格、換行)**就是要寄出去的字面**, 由 `sweep-email-outbox.test.ts` 的整串比對鎖住。
>
> 🔴 **標點是【半形逗號】, 而那是刻意的** —— 本 repo 已知有兩種標點並存
> (`sweep-email-outbox.ts` 出貨信那段逐字記著「全站有兩種標點, 而那是已知且刻意的, 不是漏改」)。
> 判準照那一段立的先例:**碼的字面要與 Sean 核可的那一份完全相同**, 不是與鄰居一致。
> ⇒ 📌 **他看到的是半形, 所以寄出去的就是半形。**

## 主旨

```
訂單 (訂單編號) 已成立,請於期限內完成匯款
```

## 內文

```
您好,

您的訂單 (訂單編號) 已成立,目前尚未付款。
請依下列資訊完成轉帳,我們收到款項後會再通知您。

訂單金額  NT$ (訂單金額)
已收      NT$ (已收金額)
應付餘額  NT$ (還要匯多少)

匯款資訊
銀行      (公司銀行)((分行))
戶名      (公司戶名)
帳號      (公司銀行帳號)
匯款備註請填寫訂單編號 (訂單編號)

請於 (到期日)(含)之前完成匯款,逾期訂單將自動取消。

訂單內容與匯款資訊也可以在這裡查看:
(他的訂單頁連結)

有任何問題,加入官方 LINE (LINE ID)
(LINE 連結)
```

---

## 🔴 已知的一個字元差(等 Sean 複核, **不是我決定的**)

| | 逐字 |
|---|---|
| **Sean 核可的那一份**(上面內文最後第二行) | `有任何問題`**`,`**`加入官方 LINE …`(**半形**逗號) |
| **既有常數 `ORDER_CONTACT_LEAD`** | `有任何問題`**`，`**`加入官方 LINE`(**全形**) |

🔬 **成因**:那一句是我從既有信抄進 markdown 草稿時打成半形的
⇒ 📌 **他點頭的是【語氣與內容】, 而這個字元是我的手滑, 不是他的選擇。**

🛑 **而我不替他改** —— 三條路, 我選了最保守的:
- ✅ **碼用既有常數(全形)** —— 那一行**同時出現在別封信裡**, 硬寫第二份會讓兩封的客服句各自漂,
  而漂掉時客人拿到的是**過期的聯絡方式**。
- ⛔ **不改 `ORDER_CONTACT_LEAD`** —— 改它會**靜靜改掉另外幾封已經在寄的信**。
- ⛔ **不在本檔改掉他核可的字面** —— 這支檔的用途就是「他看到的那一份」。

⇒ 🔵 **測試把這一行當成佔位詞換掉**(`sweep-email-outbox.test.ts` 那一格有逐字說明),
　 所以整串鎖仍然鎖得住**其餘每一個字元**;而這一個字元的差**看得見、有落點、等複核**。
