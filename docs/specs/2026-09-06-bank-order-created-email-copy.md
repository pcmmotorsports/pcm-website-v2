# ⟦b4-BANKNOEMAIL⟧ 匯款單成立信 —— **Sean 核可的那一份文案**(canonical)

> 🔴 **這一份是【程式從我端給 Sean 的那支檔逐字抄過來的】, 不是我重打的。**
> 來源:`~/pcm-mailbox/端Sean-0905早上佇列.md` §B「匯款客人的那封信」。
> Sean 2026-09-06 03:2x 逐字答「**甲 = 可以**」(主視窗 `-f8` 轉述並附他貼的全文)。
>
> 🛑 **括號裡的是佔位詞, 不是字面** —— 碼裡由變數填。而**佔位詞以外的每一個字元**
> (含標點、空格、換行)**就是要寄出去的字面**, 由 `sweep-email-outbox.test.ts` 的整串比對鎖住。
>
> 🔴 **標點是【半形逗號】, 而那是刻意的** —— 本 repo 已知有兩種標點並存
> (`sweep-email-outbox.ts` 出貨信那段逐字記著「全站有兩種標點, 而那是已知且刻意的, 不是漏改」)。
> 判準照那一段立的先例:**碼的字面要與 Sean 核可的那一份完全相同**, 不是與鄰居一致。
> ⇒ 📌 **他看到的是半形, 所以寄出去的就是半形。**

## 主旨

```
訂單 (訂單編號) 已成立,請於期限內完成匯款
```

## 內文

```
您好,

您的訂單 (訂單編號) 已成立,目前尚未付款。
請依下列資訊完成轉帳,我們收到款項後會再通知您。

訂單金額  NT$ (訂單金額)
已收      NT$ (已收金額)
應付餘額  NT$ (還要匯多少)

匯款資訊
銀行      (公司銀行)((分行))
戶名      (公司戶名)
帳號      (公司銀行帳號)
匯款備註請填寫訂單編號 (訂單編號)

請於 (到期日)(含)之前完成匯款,逾期訂單將自動取消。

訂單內容與匯款資訊也可以在這裡查看:
(他的訂單頁連結)

有任何問題，加入官方 LINE (LINE ID)
(LINE 連結)
```

---

## 🔵 那一行的逗號 —— **已裁, 而原始字面留著**

| | 逐字 |
|---|---|
| ⛔ ~~我端給 Sean 的那份草稿~~ | `有任何問題`**`,`**`加入官方 LINE …`(**半形**) |
| ✅ **現行(本檔上面內文那一行)** | `有任何問題`**`，`**`加入官方 LINE …`(**全形**) |

🔬 **成因**:那一句是我從既有信抄進 markdown 草稿時**打成半形的手誤**, **不是 Sean 選的**
⇒ 他點頭的是**語氣與內容**。
✅ **主視窗 `-f8` 2026-09-06 03:1x 裁:依既有常數 `ORDER_CONTACT_LEAD`(全形), 不端 Sean** ——
理由逐字:「**四封信同一句是規則, 半形是你抄稿時的手誤不是他選的**」。

🛑 **舊字面【留在這張表裡】而不是被抹掉** —— 📌 一份「他核可的文案」被改過一個字元, 這件事本身要看得見;
下一個核對「碼 vs 他核可的那份」的人, 要在**同一頁**上撞到這一格, 而不是去翻訊息紀錄。
🔵 ⇒ 而 `sweep-email-outbox.test.ts` 的整串 `toBe` **不再把那一行當佔位詞** —— 它現在鎖住**每一個字元**。
