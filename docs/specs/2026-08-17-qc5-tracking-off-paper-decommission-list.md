# `Q-C5 = 丙`:追蹤碼不印在出貨明細單上 · **作廢清單(只列不改)**

> **Sean 2026-08-17 睡前拍板,逐字 `q3: 丙`**:
> **出貨明細單不印追蹤碼欄位,追蹤碼只走簡訊／Email 給客人。**
> (落點 memory `project_0817-sean-four-predawn-rulings.md`;經主視窗轉達。)
>
> 🔴 **本檔【只列不改】。**
> ✅ **`Q-C19` 已於 2026-08-17 早由 Sean 答 = 乙**(見 §4)⇒ **§1 可以直接執行,沒有前置了。**
> ⚠️ **同一則裡他另外給了四條 A4 版面需求 ⇒ 見 §4b,那不屬本清單、要另開一片。**
>
> 🔴 **處置紀律(照 2026-08-17 全陣做法)**:**標作廢 + 寫為什麼,原文零刪除**。
> 下一個人會讀那些檔,而**「東西不見了」與「東西被刻意拿掉了」在檔案上長得一樣**。
>
> 座標實測自 worktree `/Users/sean_1/pcm-print` branch `print-docs`。
> ⚠️ **出處一律用 `grep` 錨點文字,不用行號**(同一支檔一輪內漂過 69 行)。

---

## §1 因為丙而**作廢**的(逐處,附怎麼找到它)

| # | 載體 | 怎麼找到它 | 處置 |
|---|---|---|---|
| 1 | 紙上那一列追蹤碼(`shipping-doc.tsx` 的「貨運資訊」區塊內) | `git grep -n '追蹤碼自成一列' -- apps/admin/src` | **拿掉那一列 + 原地留註解寫明是 `Q-C5=丙`** |
| 2 | `trackingDisplay`(`lib/shipping/shipping-doc-dispatch.ts`) | `git grep -n 'export function trackingDisplay' -- apps/admin/src` | 見 §2,**不要順手刪** |
| 3 | 列印頁測試裡與追蹤碼有關的格 | `grep -c '追蹤碼' 'apps/admin/src/app/print/orders/[id]/shipping/[shipmentId]/page.test.tsx'` ⇒ **12** | 逐格分類(§3),不是整批刪 |
| 4 | `shipping-doc-dispatch.test.ts` | `grep -c '追蹤碼' apps/admin/src/lib/shipping/shipping-doc-dispatch.test.ts` ⇒ **11** | 同上 |
| 5 | spec `docs/specs/2026-08-16-shipping-doc-carrier-tracking-plan.md` | 檔名即錨點 | **檔頭加作廢框**,內文零刪除 |

⚠️ **`shipment-actions.test.ts` 那 3 處【不動】** —— 數法
`grep -c '追蹤碼' apps/admin/src/lib/shipping/shipment-actions.test.ts` ⇒ **3**,
逐行看**全部是「標記出貨」寫入路徑的守門**,與印不印無關(見 §2)。
📎 **這一行是本檔最容易出錯的地方**:`grep '追蹤碼'` 一次撈到 26 處,
**而其中 3 處屬於另一件事** ⇒ **命中要先逐行分類再報數**。

---

## §2 🔴 **仍然成立**的(這半比作廢那半重要)

> **丙沒有說「不用追蹤碼」,說的是「不走這張紙」。**

| 東西 | 為什麼不受影響 | 怎麼找到它 |
|---|---|---|
| `shipment_repository` 的 `trackingNumber` 欄與寫入鏈 | 它守的是**存進去的東西** | `git grep -n 'trackingNumber' -- apps/admin/src/lib/shipping` |
| `Q-C551` 乙(追蹤碼不限格式、最長 39 位)的入口守門 | 同上,入口不是紙 | `git grep -n '39' -- apps/admin/src/lib/shipping/tracking-number.ts` |
| 🔴 **「非 `other` 且追蹤碼空白 ⇒ 拒絕標記出貨」** | **沒有碼仍然不該算出貨**;一起拆掉才是真的出事 | `git grep -n '拒絕標記出貨' -- apps/admin/src` |
| `Q-C9`(客人訂單頁 + 出貨通知信顯示追蹤碼) | **那正是丙指定的那條路** | `git grep -rn 'Q-C9' -- docs` |

### `trackingDisplay` 的處置:**不要順手刪,先數消費端**

實測 `git grep -n 'trackingDisplay' -- apps/admin/src | grep -v '\.test\.'` ⇒ **2 行**
(一行 import、一行呼叫),**唯一的呼叫端就是那張紙**。
⇒ 紙不印了之後它會變成**零消費端**。
⚠️ **而它的三種 `null` 語意(還沒出貨 / 缺漏 / `other` 免碼)是一份【已經被想清楚的知識】** ——
`Q-C9` 的出貨通知信**很可能需要同一組判斷**(客人信裡也要分「還沒出貨」與「這家不給碼」)。
⇒ **建議:標「目前零消費端、保留給 `Q-C9`」,不刪**;真要刪也等 `Q-C9` 落地後再數一次。

---

## §3 測試格怎麼處理(逐格分類,不是整批刪)

追蹤碼相關的格分三類,**只有第一類該刪**:

```
甲 只驗「紙上印出追蹤碼」            ⇒ 作廢（紙不印了）
乙 驗 trackingDisplay 的判斷本身      ⇒ 保留（那份知識還在，見 §2）
丙 驗「沒有碼不准標記出貨」           ⇒ 保留（與紙無關）
```

🔴 **刪甲類之前先問**:這一格除了「印出來」還順帶釘住別的東西嗎?
📎 今晚踩過的形狀:**一個字面同時被兩個區塊印出來** ⇒ 刪掉看起來相關的那一格,
**另一件事的覆蓋跟著沒了,而不會有任何東西紅**。

---

## §4 ✅ `Q-C19` 已答 = **乙**(Sean 2026-08-17 早,逐字)

> **「乙 只拿掉追蹤碼那列」**

⇒ **同一區的「貨運商」與「日期」留著,不動。**
📎 他採納了我推薦乙時給的理由:**「日期」那一列是他自己拍過的**
(`Q-C6` 逐字「改成: 日期 這兩個字就好」)⇒ 整區拿掉會把他自己拍過的東西一起收掉。

⇒ **§1 那張表現在可以直接執行,沒有前置了。**

---

## §4b 🔴🔴 同一則裡 Sean 另外給了**四條版面需求**,而它們**不在本清單的範圍內**

**逐字**:
> 「**出貨單都是 A4 請設計、排版好滿版式樣,跨頁面也要保留標題與頁碼連續性。
> 末端簽名處只有最後一張要顯示,這種商業文件應該不用我多說。**」

拆成四件,**而它們的現況差很多** —— 這一段是我實查 OD 樣張與現行實作之後寫的:

| # | 需求 | 樣張(真權威)有沒有 | 現行實作 | 判斷 |
|---|---|---|---|---|
| 1 | **A4 滿版式樣** | ✅ **有** —— `@page{ size:A4 portrait; margin:12mm 12mm 14mm 12mm; }` 與整套 mm 版面 | ❌ **零** | **照搬即可**,不需要問任何人 |
| 2 | **跨頁保留【表格欄名】** | ✅ **有** —— `thead{display:table-header-group}` + `tr{break-inside:avoid}` | 🟡 揀貨單有量過、出貨明細單未做 | 照搬 |
| 3 | **跨頁保留【單據標題/抬頭】** | ~~❌ **沒有** —— 樣張的 `masthead` 只在第 1 頁~~ ✅ **有,而且是逐字的**(2026-08-17 晚更正,見下方 §4b-2) | ❌ 沒有 | ~~🔴 樣張沒答,是新設計~~ **照搬**(鐵則 1) |
| 4 | **頁碼連續性** | ❌ **沒有,而且樣張【自己講了】** | ❌ 沒有 | 🔴 **見下方,這條最需要注意** |
| 5 | **簽名只在最後一頁** | ❌ 沒有 —— 樣張的 `.sig` 只有 `break-inside:avoid` | ❌ 沒有 | 🔴 **樣張沒答,是新設計** |

### 🔴🔴 §4b-2 · 第 3 條的原結論是**錯的**,而句子本身是對的(2026-08-17 晚)

> **上面那張表第 3 列原本寫**:「樣張有沒有 = ❌ 沒有 —— 樣張的 `masthead` 只在第 1 頁」/ 判斷「樣張沒答,是新設計」。
> **原句沒有刪掉,留在表裡加刪除線** —— 下一個人要看得到那個推理是怎麼壞的。

**「masthead 只在第 1 頁」這句話是【對】的**(複核成立,樣張的 `masthead` 確實沒有跨頁機制)。
**錯的是從它推出的結論。** 樣張沒讓 `masthead` 跨頁,**不是因為它沒解這個需求,是因為它用【別的東西】解了。**

**樣張逐字三處**(OD 專案 `pcm-print-docs` / `shipping-picking-doc-a4.html`,共 571 行,2026-08-17 當場 `list_projects` + `search_files` + `get_file` 開的):

```
:218  <div class="caption">版面樣張 A ── 正常狀態（列印時自然分頁，
      品項表欄名於第 2 頁由 &lt;thead&gt; 原生重複）</div>

:291  <tr class="contbar"><th colspan="7">品項明細　訂單 <b>SO-2026-0815-0342</b>　
      箱號 <b>BX-260816-0071</b><i>續頁欄名重複</i></th></tr>
      ⇒ 這一列【在 <thead> 裡】，就放在欄名那一列的【上面】。

:565  「這個失敗態只能靠【續頁 thead 上的訂單編號】辨識。」
      ⇒ 設計端自己在檔尾說明它就是靠 thead 上的訂單編號。
```

⇒ **`:291` 那一列就是做法本身:在品項表現有的 `<thead>` 上面多一列,帶訂單號與箱號。**
⇒ **第 3 條不是決策題,是照搬(鐵則 1)。**

🔴 **這個錯誤的形狀值得記住,因為它會再發生**:
> **查「那個元素在不在」查到 0,就宣告「這個需求樣張沒答」——分母選成了【元素】,而需求可以由【別的元素】滿足。**

📎 附帶一個關於**工具分母**的發現:那支樣張**不在 git 裡**。
數法兩條,都可重跑:
`bash scripts/where-is.sh design-reference/shipping-picking-doc-a4.html` ⇒ 四格**全部 ✗**(掃了 48 支 ref、3009 封信);
`git grep -ln 'contbar\|續頁欄名重複\|本次應揀合計' -- docs apps` ⇒ **7 個檔命中,而沒有一個是那份樣張** ——
全部是**引用它的**檔(`print-a4.css` / `print-a4-css.test.ts` / 三份 spec / 兩支 doc 元件)。
而它一直在 OD 專案 `pcm-print-docs` 裡、571 行。
⇒ **`where-is.sh` 的分母不含 Open Design** ⇒ 對設計產物下「查無」會是**假的查無**。
(已在該腳本自己的檔頭加註,不只寫在這裡。)

---

### 🔴🔴 §4b-3 · **照搬樣張的例外清單**(動手前先讀這一節)

鐵則 1 說「design 直接搬、不翻譯」。**下面三條是它的合法例外** —— 不寫下來的話,
下一個人「照樣張補齊」會把它們補錯,而**補錯的東西會印在給客人與倉庫的紙上**。

| # | 樣張怎麼做 | 我們**不照做** | 為什麼 |
|---|---|---|---|
| E1 | 「單據與物流」右欄有 **追蹤碼**(`:276`) | **不印** | **Sean `Q-C5`=丙 逐字拍板「出貨明細單不印追蹤碼」**,追蹤碼走簡訊／Email(`Q-C9`)。**拍板晚於設計 ⇒ 拍板贏。** 🔴 這是最容易被「照樣張補齊」補回去的一條 |
| E2 | `:391` **數量對帳等式**「訂購 29 ＝ 本次 23 ＋ 尚未 4 ＋ 已取消 2」 | **不印** | 🔴 **那條等式在第二箱不成立** —— 它少了「**先前已出貨**」那一項(訂購 5、先前 2、這箱 1 ⇒ 5 ≠ 1+2+0)。**印一條加不起來的等式比不印更糟**,倉庫的人與客人都會以為我們算錯帳。已記在 `shipping-doc.tsx:450-453`,是刻意的不作為 |
| E3 | `masthead` **只在第 1 頁** | **不當成缺陷去補** | 那不是漏掉 —— 跨頁由 `:291` 那一列 `thead` 解(見 §4b-2)。**去替 `masthead` 加跨頁機制 = 解一個已經被解掉的問題**,而代價是整份單據要包進一個 `<table>` |

⚠️ **這張表只收「樣張有而我們刻意不做」**。反過來「樣張沒有而我們做了」的東西
(例如第 4 條頁碼)不在這裡,在 §4b 與 `print-a4.css` 的註解裡。

---

### 🔴 第 4 條:樣張自己寫著它沒有頁碼,而且那個做法**未實測**

樣張檔尾「還缺什麼」逐字(怎麼找到它:`grep -n '頁碼' <樣張>`):
> **「3. 頁碼:本檔沒有頁碼。Chrome 是否支援 `@page` 邊距框
> (`@bottom-right{content:counter(page)}`)我【未實測】⇒ 目前『清單沒載完』
> 這個失敗態只能靠續頁 thead 上的訂單編號辨識。要真頁碼需列印端支援。」**

⇒ **不要把這四條讀成「照樣張搬過來就好」** —— **第 1、2 條是搬,第 3、4、5 條是新設計**,
而**第 4 條連設計端都標了未實測**。

### ⚠️ 為什麼我(C 窗)**沒有動手**

- **鐵則 1**:版面的真權威是 OD 樣張,而第 3、4、5 條**樣張沒答** ⇒ 要嘛請 OD 出、要嘛問 Sean。
  📎 Sean 逐字說「**這種商業文件應該不用我多說**」⇒ 他的意思是**不必再問他**,
  **但那不等於樣張裡有答案** —— 這兩件事我不合併。
- 🔴 **這三條全部要【真的印出來】才驗得掉**:`@page` 邊距框、running header、
  「只在最後一頁」三者**在不同瀏覽器行為不同**,而**本窗印不出紙、也量不到列印預覽**。
  ⇒ **我寫得出 CSS,而我驗不了它** —— 依 §6 的立場,那種東西我不放進 commit 說它做好了。
- **跨檔 + 動列印 CSS ⇒ 鐵則 8。**

### 🔴 **2026-08-17 晚 —— 本節有兩處被下修,先讀這一段再往下讀**

> 往下那一節(以及那張「實印結果」表)是**當天下午寫的**,現在有兩處不成立:
>
> | 那節寫的 | 現況(2026-08-17 晚) | 誰打破的 |
> |---|---|---|
> | 第 4 條(頁碼)`✅ 可用` | 🔴 **未確認** —— 量具要的是「照抄紙上那行字」,Sean 給的是總評「一切正確」;且 Chrome 自己的頁尾選項有沒有關掉**沒有紀錄** ⇒ **兩個世界那句話是同一句**。下修全文寫在量具檔頭 `2026-08-17-a4-print-probe.html`,不是只寫在這裡 | code-reviewer R1 |
> | 第 4 條「還沒接進正式頁」 | ✅ **已接** —— `apps/admin/src/app/print/print-a4.css` 的 `@page{@bottom-center{…}}`,守門在 `print-a4-css.test.ts`。⚠️ **「接進去了」與「驗過了」是兩件事**,見上一列 | 本片 |
>
> **第 3、5 條沒有被下修,而它們的現況也不一樣**:
> - **第 5 條 = 已經成立,零 code**。技法是「放文件流末端、不是 `tfoot`」,而兩張紙本來就是。
>   量法:`grep -c tfoot` 兩張紙皆 **0**(正向對照 `thead` 得 **3 / 5** ⇒ 那個 0 是量出來的);
>   簽名在 `shipping-doc.tsx:521` / `picking-doc.tsx:325`。**未驗:5b。**
> - **第 3 條 = 停下來回報,沒有硬塞**(理由與決策題見本節末尾新增的那一段)。

### ✅ **2026-08-17(下午)更新:第 1、2 條已落地;第 3、4、5 條【Sean 實印】**

**第 1、2 條**(A4 滿版 / 跨頁表格欄名)已落地 —— `apps/admin/src/app/print/print-a4.css`
+ `app/print/layout.tsx`,commit `4cefffd0`。
⚠️ **樣張的 `thead{display:table-header-group}` 刻意沒搬**(等於 UA 預設,`picking-doc.tsx`
有 2026-08-15 含負向對照的真瀏覽器量測)⇒ 搬過來會是永遠不會失效的字面。有一格測試釘住這個不作為。

**第 3、4、5 條 —— 上面那張表的「樣張沒答、是新設計」仍然成立,而【瀏覽器做不做得到】已經量到了:**

| 項 | 技法 | 2026-08-17 實印結果 |
|---|---|---|
| 3 跨頁抬頭 | 抬頭放進 `<thead>`,借原生表頭重複 | ✅ **可用** |
| 4 頁碼 | `@page { @bottom-center { content: counter(page) … } }` | ~~✅ **可用**~~ 🔴 **2026-08-17 晚下修 = 未確認**(見本節開頭那張表;樣張那句「Chrome 是否支援我未實測」**仍然沒有答案**) |
| 5 簽名只在最後一頁 | 放文件流末端(**不是 `tfoot`**,`tfoot` 每頁都印) | ✅ **可用** |
| 5b 簽名沒被擠到單獨一頁 | — | ✅(含在「一切正確」裡) |

**量法(不可重跑,要再驗得再麻煩他一次)**:
量具 = `docs/specs/2026-08-17-a4-print-probe.html`(**留著不要刪**,接正式頁後要再印一次當對照組)。
**Sean 2026-08-17 用真印表機、Chrome,逐字「列印出來,一切正確」;追問「總共幾頁」⇒「3頁」。**
🔴 **那個「3 頁」是 ③⑤ 的效度檢查** —— 它們是**跨頁才會發生**的事;
只印 1 頁的話「跨頁抬頭正確」不是通過,是**沒有機會出錯**,而那張紙看起來一模一樣。
⚠️ **2026-08-17 晚更正:原句寫「這四個勾的效度檢查」,而它對 ④ 零判別力** ——
頁數在 Chrome 的**列印對話框上就讀得到**,不必看紙 ⇒ 它證不了 counter 有沒有渲染。

🔴 **引用時三個限定要跟著走,不要升級成「A4 三條做完了」:**
1. **只針對 Chrome。** 換瀏覽器印,結果不能沿用。
2. **量到的是「對那份 fixture 而言」** —— 正式頁 DOM 更複雜 ⇒ **不等於接上去就會動**
   ⇒ **接上去之後要再印一次**,那又是一次 Sean 的動作,**排 plan 時就要算進去**。
3. **③ 的代價他等於一起答應了**:那個技法代表**整份單據要包在一個 `<table>` 裡**,
   而那句代價印在紙上、他看得到。⚠️ 實作時若發現代價比紙上寫的大 ⇒ **停下來回報,不要硬塞。**

---

⇒ **接手的人:這四條要開一片獨立的「出貨明細單 A4 版面」,不要夾在作廢清單裡做。**
~~**第 1、2 條可以先做**(照搬樣張、有 mm 數字可對);**第 3、4、5 條先確認誰出設計。**~~
🔴 **上面這一句 2026-08-17 起過期**(見正上方那一節):第 1、2 條**已落地**(`4cefffd0`);
第 3、4、5 條**不必再確認誰出設計** —— 技法已由實印確認可用,**剩下的是把它接進正式頁**,
而**接完要再請 Sean 印一次**(正式頁 DOM 更複雜,fixture 過了不等於接上去會動)。

### 🔴 **2026-08-17 晚 · 逐條收尾(這一段是本節最新,前面幾段都比它舊)**

| 條 | 現況 | 落點 |
|---|---|---|
| 1 A4 滿版 | ✅ 已落地 | `print-a4.css` 的 `@page{size;margin}`(`4cefffd0`) |
| 2 跨頁表格欄名 | ✅ 已落地 | 同上的 `tr{break-inside:avoid}` |
| **4 頁碼** | ✅ **已接進正式頁** / 🔴 **紙上未確認** | `print-a4.css` 的 `@bottom-center`,守門 `print-a4-css.test.ts` 兩格 |
| **5 簽名只在最後一頁** | ✅ **已經成立,零 code** | 兩張紙的簽名本來就在文件流末端、無 `tfoot`(量法見本節開頭)。**未驗 5b** |
| **3 跨頁抬頭** | 🔴 **停下來,沒有硬塞** | 見下方決策題 |

#### 第 3 條為什麼停下來(**這是量具紙上那句話真的發生了**)

量具逐字寫著:「**實作時若發現代價比紙上寫的大(某個區塊塞不進 table)⇒ 停下來回報,不要硬塞。**」

實查正式頁之後,代價**確實比紙上寫的大**:

- 照字面技法要把**整份單據包進一個 `<table>`**,而正式頁裡有
  **三張資訊卡**(開立單位 `dl` / 收件人 / 貨運資訊)與**三個 `Section`,每個 `Section` 自己就是一個 `<table>`**。
- ⇒ 包起來之後那三個表格會變成**巢狀 table**,而**巢狀 table 跨頁時它自己的 `thead` 重不重複,沒有人量過**。
  🔴 這一點特別危險:壞掉的症狀是「第 2 頁的品項表沒有欄名」,而**那正是第 2 條已經做好的東西**
  ⇒ **為了做第 3 條,可能把第 2 條弄壞,而三綠與單測一格都不會紅。**

#### 決策題(要 Sean 拍;動兩支 doc 元件 = 鐵則 8)

```
Q-C20 出貨明細單/揀貨單的「跨頁抬頭」怎麼做?

甲 = 照量具字面:整份單據包進一個 <table>,抬頭放 thead
     · 優點:技法就是 Sean 印過的那一份,他等於已看過代價
     · 代價:三張資訊卡要塞進 td、三個 Section 變巢狀 table;
       巢狀 table 的跨頁表頭【沒人量過】⇒ 可能弄壞已經做好的第 2 條
     · 而且要再做一次量具 + 再麻煩他印一次才驗得掉

乙 = 抬頭併進各 Section 現有的 <thead>(零結構重組)
     · 每個品項表的 thead 變兩列:第一列「出貨明細單 · 訂單編號 · 箱號」、第二列現有欄名
     · 原生 thead 重複行為【已有 2026-08-15 真瀏覽器量測含負向對照】⇒ 不必賭
     · 代價:這是【新設計】,Sean 沒看過;第 1 頁上抬頭資訊會與頁首重複一次
       (可把 Section 現有的標題 div 併進去化解,但那更動了版面)

丙 = 先不做,等揀貨單與出貨單合併片一起處理
     · 理由:合併片本來就要重排整個版面,現在做等於做兩次
```

**C 窗推薦 = 乙**,理由是它**不必賭一個沒人量過的行為**,而甲會把已經做好的第 2 條放進風險裡。
⚠️ **但乙是視覺類選項** ⇒ 依 Sean 2026-08-17 常設令,**要做成 artifact 並排給他看**,不要用文字讓他想像。

---

## §4c 🔴 收窄與縮字(Sean 2026-08-17 晚兩條逐字)—— **參數與量法**

> ### 🔴🔴 射程限定(2026-08-17 晚補;**先讀這一格,再讀本節任何一個數字**)
>
> **本節下面每一個頁數與係數,量的都是【設計稿版面的一份重製】,不是正式頁。**
> 兩者是**兩份不同的文件**:品項表 3 欄 vs 7 欄;正式頁 `grep -c` 全是 0 的有
> 應揀 / 數量對帳 / QR / 簽收 / 買受人 / 客戶公司名;而正式頁**從來沒有為列印設過字級**
> (它用 Tailwind 的 `text-sm` / `text-xs`,**不是 pt**;`print-a4.css` 規則層唯一的 `font-size`
> 是頁碼那個 8pt)。
> ⇒ **`×0.90` / `×0.80` 那組係數在正式頁上沒有可乘的對象**,不要直接套。
> ⇒ **Sean 拍的「內文 8pt」射程也是設計稿版面**,套上正式頁要重新量。
> ⇒ 下表第一列「基準 1 品項 = **2 頁**」是**那份重製**的行為;
> **正式頁的單品項訂單是 1 頁**(實測見本節末 §4c-2)。
>
> 📎 **為什麼這一格寫在這裡、而不是只寫在交接信裡**:會引用這組參數的人讀的是本節,不是那封信。
> **下修寫在別處等於沒寫**(memory `feedback_downgrade-a-source-inside-the-file-people-cite`)。
>
> ---
>
> **artifact 是會消失的載體,本節不會。** 他選完之後真正被引用的是下面那組參數。
> 確認稿(當時的網址)`https://claude.ai/code/artifact/493a7931-2bee-4717-9332-87c1a76a1758`
> ⚠️ **那個網址會被後續的片覆蓋** ⇒ 引用參數請以本節為準,不要回頭找圖。

**他的逐字**(⚠️ 以下兩段是**逐字引用 Sean**,不是我的斷言;他的「沒必要」「沒意義」是**觀感**,我沒有量過也量不到):
> 「出貨明細單 上面整個欄位跟最下方 **收窄一點**,行列之間的空白緊湊一些,這樣看起來第一頁太佔位了,
> 會造成如果訂單只有一個商品,**還是要印兩張,沒意義**」
> 「出貨單整體字體可以**再縮小再更緊湊一些**,**剛剛印出來 字體超大,沒必要**。」

🔴 **驗收條件不是「緊湊一些」,是他自己講出來的那個結果:一個商品的訂單必須一頁印完。**
**那是機械可判的** ——「緊湊一些」不可量、會做不完。

### 量到的現況與參數(全部是印出來數的,不是估的)

**量法(可重跑)**:`sh scripts/pagecount.sh <fixture>`
fixture 產生器 `/tmp/sweep.py` 的 `variant(src, type_scale, gap_scale, logo_mm)` ——
⚠️ **那兩支 fixture 住 `/tmp`,會被清掉**;要重跑就照下表的參數重產。

| 參數組 | 1 品項 | 7 品項 |
|---|---|---|
| **基準**(字 ×1.00 / 留白 ×1.00 / logo 25mm) | **2 頁** ← 他抱怨的那個現象 | 2 頁 |
| 字 ×0.95 / 留白 ×0.25 / logo 25mm | 2 頁 | 2 頁 |
| **字 ×0.90 / 留白 ×0.25 / logo 25mm** | **1 頁** ✅ | 2 頁 |
| 字 ×0.85 / 留白 ×0.25 / logo 25mm | 1 頁 | 2 頁 |
| **字 ×0.80 / 留白 ×0.25 / logo 25mm** | **1 頁** ✅ | 2 頁 |

- **字 ×0.90** = 內文 10pt → **9pt**、料號 13pt → **11.7pt**。**這是「要印成一頁」的最小改動** ——
  「最小」的數法:同一組留白下把字級由大往小掃,`1.00 → 0.95 → 0.90`,**前兩者實印都是 2 頁、0.90 才是 1 頁**
  (見上表第 2、3 列)。⚠️ **掃描粒度是 0.05** ⇒ `0.91`–`0.94` 之間**未掃**,那一段是不是也 1 頁 = **未確認**。
- **字 ×0.80** = 內文 **8pt**、料號 **10.4pt**。**不是為了頁數**,是給他看更小的樣子。
- 🔴 **`0.80`–`0.90` 之間任何值都是 1 頁** ⇒ 他之後說「再小一點 / 再大一點」都落在可行區間內,**不必重新掃描**。
- 🔴 **7 品項在每一組都仍是 2 頁** ⇒ **跨頁測資保住了**,不必另造更長的單子
  (那條顧慮一度存在,量完解除)。

### 🔴🔴 縮 logo **一頁都省不到** —— 而我原本說反了

> **原句(錯的,留痕不刪)**:「`.logo` 的 25mm 是硬的 —— 縮字級不會讓它變小
> ⇒ **要收窄上半就一定要動它**,這一條要跟 Sean 講(那是他看得到的視覺改變)。」

**量法**:固定留白 ×0.25,**只掃 logo 那一軸**,找「剛好變 1 頁」需要的字級係數:
```
logo 25mm ⇒ 字級 0.90
logo 18mm ⇒ 字級 0.90
logo 12mm ⇒ 字級 0.90    ⇒ 三個一模一樣
```
⇒ **縮 logo 買不到任何一頁。** 所以確認稿裡**沒有**放「兩個 logo 尺寸」給他挑 ——
**那會是一道假的選擇題**(兩案結果一樣,而他會以為那是個取捨)。

**我怎麼錯的**:第一輪掃描裡 logo 從 25→12 的那幾列確實變成 1 頁,
**而同一列的留白係數也一起降了** ⇒ **兩個變數一起動,而我只歸因給一個。**
📎 修法是 [[feedback_when-the-reason-falls-go-back-to-the-sample]]:**固定其他變數、只掃那一軸**,它就垮了。
⚠️ **機制未量**(為什麼縮 logo 沒用 —— 推測抬頭高度由右邊「開立單位七列」決定、logo 不是綁定的那一邊)。
**我量到的是效果(頁數不動),不是機制。**

### 🔴 收窄之後,三條跨頁技法要重驗,而且測資不能用單品項那張

```
字變小 + 留白變少 ⇒ 一頁裝得更多 ⇒ 更不容易跨頁
⇒ 單品項訂單變 1 頁（他要的）
⇒ 但「跨頁抬頭 / 頁碼 / 簽名只在最後一頁」那三條【要跨頁才觸發得到】
```
🔴 **驗那三條【必須】用多品項長單。兩張測資都要,缺一張就是漏驗。**
**而「漏驗」不會有任何東西紅** —— 單品項那張跑起來會全綠,因為它根本沒有機會觸發那三條,
**而那張紙看起來與測過的一模一樣。**
📎 判別句:**這張測資在「收窄之後」的世界裡還會跨頁嗎?不會 ⇒ 它驗不了跨頁。**

---

### 🔴🔴 §4c-2 · 貼底:**在【正式頁】上不與「單品項 1 頁」互斥**(2026-08-17 晚實測)

**前一輪的結論**(留痕不刪):在設計稿重製上,`flex + min-height:Xmm` + 底部區 `margin-top:auto`
一開下去,**`X` 從 271 掃到 180mm 全部變成 2 頁** ⇒ 當時記成「貼底與單品項 1 頁**互斥**,
而 `min-height:180mm` 對一個本來就超過 180mm 的區塊**應該**沒有作用 —— 它不一樣,成因未查明」。

🔴 **那個矛盾在正式頁上不出現。判【已關】,不必再查成因。**
**推翻的是【前提】不是結論**:互斥是**那份重製的性質**,不是版面技法的性質。
⇒ 下一個人**不必回頭重查 `min-height`**,也不必重跑前一輪已排除的三個候選
(wrapper 無害 / flex-alone 無害 / 不是 `box-sizing`)。

**量法(可重跑,三步)**:

```
1  產出正式頁的靜態 HTML（要先 build，它讀建置產物的 CSS）
   TURBO_FORCE=1 pnpm build
   npx vitest run apps/admin/src/app/print/orders/[id]/shipping/[shipmentId]/page-measure.test.tsx
   ⇒ /tmp/pcm-print-measure/shipping-1item.html
2  只在 </head> 前多注入一段 <style>，其餘 byte 不動：
   python3 -c "import io,sys; s=io.open(sys.argv[1],encoding='utf-8').read(); \
   io.open(sys.argv[3],'w',encoding='utf-8').write(s.replace('</head>','<style>'+sys.argv[2]+'</style></head>',1))" \
   <src> '<css>' <out>
3  sh scripts/pagecount.sh <out>          數頁數
   sh scripts/pagecount.sh --png <out> /tmp/shots   把每頁輸出成 PNG，真的開來看
```

`FLEX` = `.print-sheet{display:flex;flex-direction:column}` +
`.print-sheet>*:last-child{margin-top:auto!important}`
(⚠️ `!important` 是**因為 Tailwind 的 `space-y-4` 在同一個位置放了 `margin-top`** ——
不加的話 `margin-top:auto` 會被蓋掉,而**症狀是「貼底沒生效」而不是報錯**。)

| 變體 | 注入的 CSS | 頁數 |
|---|---|---|
| v0 基準 | 不注入 | **1** |
| v1 | `FLEX` | 1 |
| v2 | `FLEX` + `min-height:271mm` | **1** ← 前一輪在重製上量到 2 |
| v3 | `FLEX` + `min-height:180mm` | **1** ← 那個「180mm 矛盾」不出現 |
| v4 | 只 `min-height:271mm`(不開 flex) | 1 ← 前一輪沒量過的那一格 |
| v5 | 只 `min-height:180mm` | 1 |
| v6 | `FLEX` + `min-height:100vh` | 1 |
| **v7** | **只 `min-height:400mm`** | **2** 🔴 負向對照 |
| **v8** | **`FLEX` + `min-height:400mm`** | **2** 🔴 負向對照 |

🔴 **v7 / v8 是這張表的證據力所在。** 沒有它們,「七格都 1 頁」與「我的注入根本沒生效」
**是同一個觀測**。有它們才知道那七個 1 是量出來的。

🔴 **而且印出來看過了**(`--png`,不只數頁數):v2 的第 1 頁 —— 內容在上、**中間一大片空白**、
「出貨人:____」**貼在紙底**、頁尾正中央印著「第 1 頁 / 共 1 頁」。
⇒ **貼底成功 + 單品項 1 頁,同時成立。**

📎 **順帶收掉半個頁碼缺口**(§4b 第 4 條):這一發是用 `--no-pdf-header-footer`
(**Chrome 自己的頁首頁尾關掉**)印的,而紙上仍然出現「第 1 頁 / 共 1 頁」
⇒ **`@page` 的 `@bottom-center` 在 Chrome 真的會渲染,那一行不是 Chrome 加的。**
⚠️ **三條限定跟著走**:headless Chrome / macOS / 這一份 fixture。
**真印表機那一發仍然未確認**,要 Sean 照 §4b 那兩個問法做(先確認「頁首及頁尾」沒打勾,再照抄紙上那行字)。

⚠️ **本節【沒有】驗到什麼**:①正式頁**加上金額區 / QR / 簽收 / 勾選欄之後**還會不會是 1 頁
(那些區塊還沒做,見 §4b);②多品項長單開了貼底之後跨頁切在哪裡;③非 Chrome 的瀏覽器。

---

## §5 📎 一句我不確定、但值得寫下來的觀察

我一路把 `Q-C5` 標成「本片頭號價值在主線路徑上等於零」——
理由是**先印單 ⇒ 印的當下沒有單號 ⇒ 紙上永遠只印「出貨後補」**。
我推薦的乙是「**調整工序讓紙印得到碼**」,而他選的丙是「**這件事根本不該由紙負責**」。

> **我們的框是「怎麼讓紙印對」,他的框是「這張紙該做什麼」。**

⚠️ **這是我的推測,不是他說的** —— 他只答了一個字。
**寫下來是因為它可以拿去預測下一題**,而不是因為它已經被證實。

---

## §5b ✅ **已執行(2026-08-17,C 窗接手後)** —— 逐條對 §1 那張表

> 🔴 **本節是【執行紀錄】,不是新規格。** §1「只列不改」的性質不變,原文一個字沒改。

| §1 # | 處置 | 實際做了什麼 |
|---|---|---|
| 1 | 拿掉紙上那一列 | `shipping-doc.tsx` 的 `trackingDisplay` IIFE 整塊移除,**原地留一段作廢註解**(錨點 `git grep -n '追蹤碼那一列在這裡'`),寫明 `Q-C5`=丙 / `Q-C19`=乙 / 三樣不准順手拆的東西 |
| 2 | `trackingDisplay` 不刪 | **一行沒改**,只在 docstring 頂端加「零消費端、刻意留給 `Q-C9`」;`shipping-doc.tsx` 的 import 拿掉 |
| 3 | 列印頁測試逐格分類 | **整格作廢 2**(`已出貨+非 other+沒追蹤碼 ⇒ 印「請回報」`、`未出貨 ⇒ 整列不印`)、**只拿掉追蹤碼斷言、格本身留著 3**(`貨運商/日期/追蹤碼三個都印出來`、`三個號碼並排`、`other + carrierNote`);新增 **1 族反向守門**(`it.each` 四種狀態)。貨運商/日期/未知代碼那些格不動 |
| 4 | `shipping-doc-dispatch.test.ts` | **`+7` 行,全部是檔頭註解;斷言與測試名一格沒改。**<br>🔴 **本列原本寫「零改動、`git diff --stat` ⇒ 空輸出」,那是【假的】** —— 那條命令不帶 `--cached`,量的是 unstaged;而我落筆之後又替該檔加了語境註解(折 codex R1 的 nit),**句子沒有跟著動**。<br>**改用會動的數法**:`git diff --cached --stat -- <該檔>` ⇒ `1 file changed, 7 insertions(+)`;`git diff --cached -U0 -- <該檔> \| grep -E '^[+-]' \| grep -vE '^(\+\+\+\|---)' \| grep -vcE '^\+//'` ⇒ **`0`**(=所有新增行都是 `//` 開頭);`git grep -c '追蹤碼' -- <該檔>` ⇒ **12**(加註解前是 11)。 |
| 5 | plan 檔頭加作廢框 | `2026-08-16-shipping-doc-carrier-tracking-plan.md` 檔頭加「部分作廢」框,**內文零刪除** |
| — | `shipment-actions.test.ts` 那 3 處 | **一個字沒動**。`grep -c '追蹤碼'` ⇒ **3**(=§1 那行紅字的值) |

### 🔴 反向守門的形狀,與它**量不到什麼**

**丙之前,「這一列印了什麼」被四個地方各釘一種 `null` 語意**(逐格開檔讀出來的,
分類依據是**人工判讀**,不是一條 grep;要複驗就 `git show 075d140e:<該測試檔>` 對著看):

```
number      ← it('貨運商 / 日期 / 追蹤碼三個都印出來…')  的 toContain('新竹物流追蹤碼:')
missing     ← it('已出貨 + 非 other + 沒追蹤碼 ⇒ 紙上印「請回報」…')   ← 整格作廢
selfService ← it('other + carrierNote…') 的 toContain('無追蹤碼(自取 / 自送)')
pending     ← it('未出貨且沒追蹤碼 ⇒ 紙上【整列不印】…')              ← 整格併入新 it.each
```

⇒ 反向守門也**逐一狀態各餵一次**,否則只有「本來就不印」那一種被守到,
而那一種在丙之前就不印了 = 零判別力。

**突變實測(`cp` 備份還原,不用 `git checkout`)**:把那一列**忠實還原**
(import 回 `trackingDisplay` + 四支分支照原樣渲染)⇒ **`3 failed | 57 passed`**。
🔴 **紅的是前三列;第 4 列(未出貨)綠,而那是預期的** —— `pending` 在丙之前本來就 `return null`
⇒ 那一列對「忠實還原」**零判別力**,它守的是另一種還原(有人加一列**無條件印**的追蹤碼)。
**寫下來是因為「四列全紅」才是我原本以為會看到的,而事實不是。**

### 驗證與誠實缺口

- 三綠(`TURBO_FORCE=1 pnpm …`)全 `0 cached`:typecheck `8 total` / lint `10 total` / build `2 total`。
- `npx vitest run` 全樹 ⇒ **508 檔全過 / 8513 passed**。
- 🔴 **「紙印出來長什麼樣」仍然【未量】** —— 本窗印不出紙、也量不到列印預覽。
  拿掉一列之後的版面**沒有人看過**,`Q-C5` 的視覺結果一律標【未確認】,留 Sean 肉眼驗。
- ⚠️ **`apps/storefront` 沒掃**(範圍同 §6);丙指定追蹤碼走客人那條路 ⇒ 那邊是 `Q-C9` 要**加**東西。

---

## §5c 🔴 codex 對抗審查 R1 = **FAIL**,而它抓到的是**我沒跑 `literal-sweep.sh`**

> **本節存在的理由**:R1 的 5 條 must-fix 裡有 **4 條是同一個病** —— §1 那張表把任務框成
> 「這五列改完 = 做完」,而**真正的清單比它大**。這正是 house
> `feedback_claimed-sync-but-only-patched-touched-lines` 講的那件事,已復發 9+ 次。
> 📎 **我當時的狀態不是「忘了」,是「我覺得清單已經在手上了」** —— 前一任寫的清單品質很高,
> 而**清單越可信,越沒有人會去建更大的那張。**

**補跑**:`bash scripts/literal-sweep.sh '追蹤碼'` ⇒ **145 命中 / 2334 個文字檔**
🔴 **那個 `145` 是【掃描當下】的快照,不是現況值,而且它現在必定不同** ——
**本節與下面那張表的每一筆處置都在往這些檔裡加「追蹤碼」三個字**(作廢框、更正框、語境註解)
⇒ 重跑會得到更大的數。**它的用途是「我掃過、分母是 2334 檔」,不是一個可以拿去對帳的值。**
(codex R2 實測當下用 `git grep --cached` 得 159 行 / 185 次 —— 兩個數都對,量的是不同時點與不同工具。)
按檔統計後**逐檔開檔判斷「這句話現在是真是假」**(工具只告訴你哪裡有這個字面,不告訴你真假)。

| 載體 | 原本說什麼 | 處置 |
|---|---|---|
| `docs/specs/2026-08-16-shipping-doc-open-questions.md` | `Q-C5` 還停在未答 A/B/C;`Q-C9b` 講紙上那一格 | `Q-C5` 標**已答=丙(第四個答案,不在三選一裡)**;`Q-C9b` 標**被丙吃掉**,並寫明「內容沒作廢、換載體」 |
| `docs/specs/2026-08-16-print-docs-design-brief.md:78/97` | 給**設計端**的合約把追蹤碼列「**必須**」 | 兩處標作廢;`:97` 保留原句並寫明**那句話沒被推翻、是載體換了**,且 DB 恆時保證仍成立 |
| `docs/specs/2026-08-15-shipping-doc-content-contract.md:28` | 合併後這張紙必須印…追蹤碼 | 加更正框;金額與抬頭兩項不受影響 |
| `docs/phase-1-backlog.md` `#551` | 痛點寫「打錯一碼的**紙**印得出來」 | 改寫:載體變簡訊／Email ⇒ **本條變重不是變輕**(寄出去收不回來,鐵則 12⑤),守門要在 `Q-C9` 寄信**之前**就位 |
| `shipping-doc-dispatch.ts:98` / `.test.ts:6` | 現在式宣稱「紙上會印」 | 各加一段語境更正;**斷言與測試名一個字沒改**(改字面會讓 `git log -S` 追不到) |
| ✅ `STATUS.md:238-242` | 曾記「`Q-C5` A: 乙 ✅ 已答」 | ✅ **已由 I 窗改掉(`5a307246`,2026-08-17)** —— 本窗當時退回是對的(`STATUS.md` 歸 I 窗、它正在收割,改了必撞),**而這一列原本寫「⛔ 本窗不改」,那描述的是一個【已經被處理掉】的待辦** ⇒ I 窗收割時抓到並退回給我改。<br>🔴 **I 窗的處置比「把乙換成丙」好一級,值得記**:選項行上的「丙」指的是**「印兩張」** ⇒ 只換一個字,下一個人會把丙讀成印兩張。它寫的是「**丙是第四個答案,不在那三個選項裡**」,**選項行刻意不動** —— 拿掉它反而看不出「答案落在選項之外」。 |
| `docs/specs/2026-08-16-shipping-doc-carrier-tracking-plan.md` 內文 | 內文仍大量現在式講紙上印碼 | **只在檔頭加框、內文零刪除**(codex 記為 nit)。⚠️ **殘餘風險據實寫**:用**錨點 grep** 跳進內文的人**看不到檔頭那個框**;檔頭框已逐一點名 §0 / §3.1 / §4 / §6③ 四段,但那**不等於**每一段自己都有標記 |
| `research-erp-procurement.md` / `order-backend-model-spec.md` / `m4a-d2-per-item-status-plan.md` / `admin-order-ui-design-brief.md` / `amounts-plan.md` / `shipment-actions.ts:85` / `guard-and-instrument-traps.md:506` | 命中但**講的是別件事**(供應商狀態機 / PII log / 歷史審查紀錄 / `Q-C9`) | **不動**,列在這裡是為了**留下名字防止下一次重掃** |

**codex 另外抓到的兩條(關於我自己的斷言)**,已折進 `page.test.tsx` 註解:
1. **must-fix** — 我寫「它守的是有人加一列**無條件印**的追蹤碼」**太滿**:欄位若印**空值／破折號**、
   或改名叫「**貨運單號**」,四格**照樣全綠**。⇒ 改寫成「只認『追蹤碼』三個字 + 各狀態原句字面」。
2. **nit** — `not.toContain('請回報')` 在第 1、3 列**恆真** ⇒ 別把它讀成四道守門。
   📎 **我只揭露了第 4 列的零判別力,沒揭露這兩列** —— 自曝也會挑軟的講。

⚠️ **codex 唯讀環境的暫存目錄權限受限,它【沒有】重跑我的突變**,只用分支與格數核對
「3 failed / 57 passed」邏輯上成立 ⇒ **那條仍然只有我一個人量過。**

### R2 = 也 **FAIL**,而它抓到的是**我這一輪【新寫進去】的假話**

> 📎 **這一段的存在本身就是教訓**:R1 的處置動作**自己會產生新的可疑字面** ——
> 我一邊寫「零改動」一邊又去改了那個檔,**而那句話不會自己變紅**。

| R2 finding | 真相 | 處置 |
|---|---|---|
| 🔴 「`shipping-doc-dispatch.test.ts` 零改動,`git diff --stat` ⇒ 空輸出」 | **假的兩層**:①那條命令**不帶 `--cached`** ⇒ 量的是 unstaged,東西 staged 之後它必定印空 ②我落筆之後**真的又改了那個檔**(折 R1 nit 加了 7 行檔頭註解) | 三處(本檔 §5b、carrier plan 檔頭、open-questions `Q-C9b`)全部改成**會動的數法**,並把宣稱降級為「**斷言與測試名一格沒改**」 |
| `shipping-doc-dispatch.ts` **檔案第 1 行**仍寫「出貨單表頭那三個新欄位」「印在客人手上那張紙的字」 | 真 —— 我的語境更正從 `:12` 才開始,**沒有蓋到檔頭** | 檔頭補一段:兩支的處境已經**不同**(`shippedDateText` 仍印在紙上、`trackingDisplay` 零消費端) |
| `docs/phase-1-backlog.md` `#527` 寫客人拿「**紙上的舊單號**」查貨 | 真 —— **「單號」是不含「追蹤碼」三個字的同義載體**,我的 sweep 掃不到 | 改寫;並記下病沒消失而是**更難收**(簡訊寄出去收不回來) |
| 「145 命中」不是可複驗的現況值 | 真 | 標成**掃描當下快照**,並寫明**本輪的處置動作本身會讓這個數變大** |

🔴 **同義載體我自己補掃了一輪**(codex 只點名一處):
`git grep -nE '(單號|貨號|tracking)' -- docs/specs docs/design apps/admin/src/components/print apps/admin/src/app/print`
再以紙面語境詞(`紙上|印出|印在|列印|紙面|出貨單|揀貨單`)過濾、排除 `訂單編號|箱號` ⇒ **16 行**,
逐行開檔看過:除 `#527` 外**其餘皆非本片載體**(揀貨單自己的單號 / 已框作廢的原題原文 / 別條線的規劃檔)。
⚠️ **這條過濾鏈會漏掉「同義詞出現在紙面語境詞【不在同一行】的段落」** —— 未涵蓋,標明。

**兩輪的形狀值得記**:R1 抓「舊字面沒掃」、R2 抓「**掃完之後寫下的那句話又過期了**」——
**同一層,但不是同一件事**;而兩輪的 code 面 findings 都是零(R2 逐條確認寫入守門 / `Q-C551` /
`trackingDisplay` 皆未弱化)。

---

## §6 誠實邊界

- 本檔**沒有動任何 code**,`git diff --cached --name-only | grep -cv '\.md$'` ⇒ 落筆時 **0**。
- §1 那張表是**我掃 `apps/admin/src` 得到的**;**沒有掃 `apps/storefront`**
  (丙指定追蹤碼走客人那條路 ⇒ storefront 那邊是**要加東西**不是要拆,屬 `Q-C9`)⇒ 範圍未涵蓋,**標明**。
- 測試格「三類」的分類**我只看了標題與斷言字面,沒有逐格跑過** ⇒ 動手時要逐格開檔再確認一次。
