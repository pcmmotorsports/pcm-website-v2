# `Q-C5 = 丙`:追蹤碼不印在出貨明細單上 · **作廢清單(只列不改)**

> **Sean 2026-08-17 睡前拍板,逐字 `q3: 丙`**:
> **出貨明細單不印追蹤碼欄位,追蹤碼只走簡訊／Email 給客人。**
> (落點 memory `project_0817-sean-four-predawn-rulings.md`;經主視窗轉達。)
>
> 🔴 **本檔【只列不改】。**
> ✅ **`Q-C19` 已於 2026-08-17 早由 Sean 答 = 乙**(見 §4)⇒ **§1 可以直接執行,沒有前置了。**
> ⚠️ **同一則裡他另外給了四條 A4 版面需求 ⇒ 見 §4b,那不屬本清單、要另開一片。**
>
> 🔴 **處置紀律(照 2026-08-17 全陣做法)**:**標作廢 + 寫為什麼,原文零刪除**。
> 下一個人會讀那些檔,而**「東西不見了」與「東西被刻意拿掉了」在檔案上長得一樣**。
>
> 座標實測自 worktree `/Users/sean_1/pcm-print` branch `print-docs`。
> ⚠️ **出處一律用 `grep` 錨點文字,不用行號**(同一支檔一輪內漂過 69 行)。

---

## §1 因為丙而**作廢**的(逐處,附怎麼找到它)

| # | 載體 | 怎麼找到它 | 處置 |
|---|---|---|---|
| 1 | 紙上那一列追蹤碼(`shipping-doc.tsx` 的「貨運資訊」區塊內) | `git grep -n '追蹤碼自成一列' -- apps/admin/src` | **拿掉那一列 + 原地留註解寫明是 `Q-C5=丙`** |
| 2 | `trackingDisplay`(`lib/shipping/shipping-doc-dispatch.ts`) | `git grep -n 'export function trackingDisplay' -- apps/admin/src` | 見 §2,**不要順手刪** |
| 3 | 列印頁測試裡與追蹤碼有關的格 | `grep -c '追蹤碼' 'apps/admin/src/app/print/orders/[id]/shipping/[shipmentId]/page.test.tsx'` ⇒ **12** | 逐格分類(§3),不是整批刪 |
| 4 | `shipping-doc-dispatch.test.ts` | `grep -c '追蹤碼' apps/admin/src/lib/shipping/shipping-doc-dispatch.test.ts` ⇒ **11** | 同上 |
| 5 | spec `docs/specs/2026-08-16-shipping-doc-carrier-tracking-plan.md` | 檔名即錨點 | **檔頭加作廢框**,內文零刪除 |

⚠️ **`shipment-actions.test.ts` 那 3 處【不動】** —— 數法
`grep -c '追蹤碼' apps/admin/src/lib/shipping/shipment-actions.test.ts` ⇒ **3**,
逐行看**全部是「標記出貨」寫入路徑的守門**,與印不印無關(見 §2)。
📎 **這一行是本檔最容易出錯的地方**:`grep '追蹤碼'` 一次撈到 26 處,
**而其中 3 處屬於另一件事** ⇒ **命中要先逐行分類再報數**。

---

## §2 🔴 **仍然成立**的(這半比作廢那半重要)

> **丙沒有說「不用追蹤碼」,說的是「不走這張紙」。**

| 東西 | 為什麼不受影響 | 怎麼找到它 |
|---|---|---|
| `shipment_repository` 的 `trackingNumber` 欄與寫入鏈 | 它守的是**存進去的東西** | `git grep -n 'trackingNumber' -- apps/admin/src/lib/shipping` |
| `Q-C551` 乙(追蹤碼不限格式、最長 39 位)的入口守門 | 同上,入口不是紙 | `git grep -n '39' -- apps/admin/src/lib/shipping/tracking-number.ts` |
| 🔴 **「非 `other` 且追蹤碼空白 ⇒ 拒絕標記出貨」** | **沒有碼仍然不該算出貨**;一起拆掉才是真的出事 | `git grep -n '拒絕標記出貨' -- apps/admin/src` |
| `Q-C9`(客人訂單頁 + 出貨通知信顯示追蹤碼) | **那正是丙指定的那條路** | `git grep -rn 'Q-C9' -- docs` |

### `trackingDisplay` 的處置:**不要順手刪,先數消費端**

實測 `git grep -n 'trackingDisplay' -- apps/admin/src | grep -v '\.test\.'` ⇒ **2 行**
(一行 import、一行呼叫),**唯一的呼叫端就是那張紙**。
⇒ 紙不印了之後它會變成**零消費端**。
⚠️ **而它的三種 `null` 語意(還沒出貨 / 缺漏 / `other` 免碼)是一份【已經被想清楚的知識】** ——
`Q-C9` 的出貨通知信**很可能需要同一組判斷**(客人信裡也要分「還沒出貨」與「這家不給碼」)。
⇒ **建議:標「目前零消費端、保留給 `Q-C9`」,不刪**;真要刪也等 `Q-C9` 落地後再數一次。

---

## §3 測試格怎麼處理(逐格分類,不是整批刪)

追蹤碼相關的格分三類,**只有第一類該刪**:

```
甲 只驗「紙上印出追蹤碼」            ⇒ 作廢（紙不印了）
乙 驗 trackingDisplay 的判斷本身      ⇒ 保留（那份知識還在，見 §2）
丙 驗「沒有碼不准標記出貨」           ⇒ 保留（與紙無關）
```

🔴 **刪甲類之前先問**:這一格除了「印出來」還順帶釘住別的東西嗎?
📎 今晚踩過的形狀:**一個字面同時被兩個區塊印出來** ⇒ 刪掉看起來相關的那一格,
**另一件事的覆蓋跟著沒了,而不會有任何東西紅**。

---

## §4 ✅ `Q-C19` 已答 = **乙**(Sean 2026-08-17 早,逐字)

> **「乙 只拿掉追蹤碼那列」**

⇒ **同一區的「貨運商」與「日期」留著,不動。**
📎 他採納了我推薦乙時給的理由:**「日期」那一列是他自己拍過的**
(`Q-C6` 逐字「改成: 日期 這兩個字就好」)⇒ 整區拿掉會把他自己拍過的東西一起收掉。

⇒ **§1 那張表現在可以直接執行,沒有前置了。**

---

## §4b 🔴🔴 同一則裡 Sean 另外給了**四條版面需求**,而它們**不在本清單的範圍內**

**逐字**:
> 「**出貨單都是 A4 請設計、排版好滿版式樣,跨頁面也要保留標題與頁碼連續性。
> 末端簽名處只有最後一張要顯示,這種商業文件應該不用我多說。**」

拆成四件,**而它們的現況差很多** —— 這一段是我實查 OD 樣張與現行實作之後寫的:

| # | 需求 | 樣張(真權威)有沒有 | 現行實作 | 判斷 |
|---|---|---|---|---|
| 1 | **A4 滿版式樣** | ✅ **有** —— `@page{ size:A4 portrait; margin:12mm 12mm 14mm 12mm; }` 與整套 mm 版面 | ❌ **零** | **照搬即可**,不需要問任何人 |
| 2 | **跨頁保留【表格欄名】** | ✅ **有** —— `thead{display:table-header-group}` + `tr{break-inside:avoid}` | 🟡 揀貨單有量過、出貨明細單未做 | 照搬 |
| 3 | **跨頁保留【單據標題/抬頭】** | ❌ **沒有** —— 樣張的 `masthead` 只在第 1 頁 | ❌ 沒有 | 🔴 **樣張沒答,是新設計** |
| 4 | **頁碼連續性** | ❌ **沒有,而且樣張【自己講了】** | ❌ 沒有 | 🔴 **見下方,這條最需要注意** |
| 5 | **簽名只在最後一頁** | ❌ 沒有 —— 樣張的 `.sig` 只有 `break-inside:avoid` | ❌ 沒有 | 🔴 **樣張沒答,是新設計** |

### 🔴 第 4 條:樣張自己寫著它沒有頁碼,而且那個做法**未實測**

樣張檔尾「還缺什麼」逐字(怎麼找到它:`grep -n '頁碼' <樣張>`):
> **「3. 頁碼:本檔沒有頁碼。Chrome 是否支援 `@page` 邊距框
> (`@bottom-right{content:counter(page)}`)我【未實測】⇒ 目前『清單沒載完』
> 這個失敗態只能靠續頁 thead 上的訂單編號辨識。要真頁碼需列印端支援。」**

⇒ **不要把這四條讀成「照樣張搬過來就好」** —— **第 1、2 條是搬,第 3、4、5 條是新設計**,
而**第 4 條連設計端都標了未實測**。

### ⚠️ 為什麼我(C 窗)**沒有動手**

- **鐵則 1**:版面的真權威是 OD 樣張,而第 3、4、5 條**樣張沒答** ⇒ 要嘛請 OD 出、要嘛問 Sean。
  📎 Sean 逐字說「**這種商業文件應該不用我多說**」⇒ 他的意思是**不必再問他**,
  **但那不等於樣張裡有答案** —— 這兩件事我不合併。
- 🔴 **這三條全部要【真的印出來】才驗得掉**:`@page` 邊距框、running header、
  「只在最後一頁」三者**在不同瀏覽器行為不同**,而**本窗印不出紙、也量不到列印預覽**。
  ⇒ **我寫得出 CSS,而我驗不了它** —— 依 §6 的立場,那種東西我不放進 commit 說它做好了。
- **跨檔 + 動列印 CSS ⇒ 鐵則 8。**

⇒ **接手的人:這四條要開一片獨立的「出貨明細單 A4 版面」,不要夾在作廢清單裡做。**
**第 1、2 條可以先做**(照搬樣張、有 mm 數字可對);**第 3、4、5 條先確認誰出設計。**

---

## §5 📎 一句我不確定、但值得寫下來的觀察

我一路把 `Q-C5` 標成「本片頭號價值在主線路徑上等於零」——
理由是**先印單 ⇒ 印的當下沒有單號 ⇒ 紙上永遠只印「出貨後補」**。
我推薦的乙是「**調整工序讓紙印得到碼**」,而他選的丙是「**這件事根本不該由紙負責**」。

> **我們的框是「怎麼讓紙印對」,他的框是「這張紙該做什麼」。**

⚠️ **這是我的推測,不是他說的** —— 他只答了一個字。
**寫下來是因為它可以拿去預測下一題**,而不是因為它已經被證實。

---

## §6 誠實邊界

- 本檔**沒有動任何 code**,`git diff --cached --name-only | grep -cv '\.md$'` ⇒ 落筆時 **0**。
- §1 那張表是**我掃 `apps/admin/src` 得到的**;**沒有掃 `apps/storefront`**
  (丙指定追蹤碼走客人那條路 ⇒ storefront 那邊是**要加東西**不是要拆,屬 `Q-C9`)⇒ 範圍未涵蓋,**標明**。
- 測試格「三類」的分類**我只看了標題與斷言字面,沒有逐格跑過** ⇒ 動手時要逐格開檔再確認一次。
