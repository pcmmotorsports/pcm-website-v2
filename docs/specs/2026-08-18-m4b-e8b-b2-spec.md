# B2 spec · 登入認人(登入頁加帳號欄 + 備援分支 + 關掉 legacy fail-open + 首次登入強制改密碼)

> **狀態:規格,尚未實作。** plan v4 已由 Sean 批(鐵則 8)。
> **母 plan**:`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md` §4 `B2`(`:187`,前置 B1)。
> 寫於 2026-08-18 · G5 窗。
>
> 🔴🔴 **口徑(2026-08-18T16:0x 更新,取代原本那段「讀的是 08-14 那顆」的限定)**:
> **報價單已 pull(fast-forward),本機 HEAD = `1149e05`(2026-08-18 15:09)= `origin/main`,落後 0 / 超前 0 / 工作樹 0 dirty。**
> **本檔原本讀自 `482bec5`(08-14),而那之後的 22 顆【一支都沒碰到本檔引用的檔】** ——
> 量法(當場跑,含正向對照):
> ```
> git diff --name-only 482bec5..1149e05        ⇒ 9 個檔，全部是 fetchers / translators / scripts / tests
> 逐支問「這 22 顆碰過嗎」：login/route.ts、sso/authorize、sso/exchange、lib/session.ts、
>   lib/supabase-server.ts、baseline_schema.sql  ⇒ 各 0 次
> 正向對照：同一條命令對 tests/ ⇒ 3 個檔（尺是活的）
> ```
> **⇒ 本檔的每一條引用【在新 HEAD 上逐行複驗過】**(直接讀工作樹,不是 `git show`):
> `login/route.ts:136` / `exchange/route.ts:66` 與 `:71` / `authorize/route.ts:58-64` /
> `lib/session.ts:13-20` / `baseline_schema.sql:4764-4766` —— **字面與行號都不變。**
> ⚠️ **仍然成立的限定**:`origin/main` 是 Sean 08-18 15:09 從 mac mini 推的那一顆;**再往後他又推的話,這一段就過期了**。
>
> **以下是原本那段口徑,劃掉不刪(它解釋了本檔為什麼長這樣)**:
> ⛔ ~~🔴🔴 **口徑硬約束(引用本檔必須連這句一起引)**:~~
> ⛔ ~~**本檔對報價單的所有引用讀自 `origin/main` = `482bec5`(2026-08-14 16:48),用 `git show` 讀,~~
> ⛔ ~~沒有 pull、沒有 checkout。** 而 `origin/main` **也不保證是現況** —— mac mini 可能有已 commit 未推的東西~~
> ⛔ ~~(`~/pcm-mailbox/G5-001-…` §4-1 已送 Sean)。**動手前重跑一次同樣的 `git show`。**~~
>
> 🔴 **本片動 auth ⇒ 鐵則 12 ②;若採 §3.4 的加欄方案則另中 ③。commit 前對抗審查不降級。**
> 🔴 **本片是這條線最大的一片,而它有【四個我查不到的前提】** —— 全部列在 §6,不藏在內文裡。

---

## §1 這片要做什麼(一句)

**讓登入這一刻知道「是誰」** —— 現在它只知道「有人打對了那組全公司共用的密碼」。

---

## §2 現況(2026-08-18 當場讀 `origin/main:482bec5`)

| 事實 | 逐字 / `檔案:行號` |
|---|---|
| 第一因子 = 全站共用密碼 | `app/api/admin/login/route.ts:136` `const expected = process.env.ADMIN_PASSWORD;` |
| body **零 per-user 欄位** | 同檔 `:146-151`,只有 `password / code / recoveryCode / setupSecret` |
| 端點結構 = 單一端點兩階段 2FA | 同檔 `:20-24` 檔頭逐字 |
| 限速是 **per-IP** 的(不是 per-account) | 同檔 `:92-110` `failResponse` → `recordFailAndCount` |
| 常數時間比對已有 | 同檔 `:43-47` `safeEqualHex` |
| 🔴 **legacy fail-open 在這裡** | 同檔 `:116-133` `issueSession`:新 cookie 基礎建設(`SESSION_SECRET` + `EDGE_CONFIG`)缺 ⇒ **`:127-130` 退回 legacy `ADMIN_COOKIE`(30 天、無 `amr`、無身分)** |
| Supabase client **只有 service_role 那一個** | `lib/supabase-server.ts:22`;全 repo 無 anon-key client(量法 `git grep -n 'ANON_KEY\|createBrowserClient' origin/main -- lib app` ⇒ 只命中 `lib/line.ts:10` 的另一個用途) |
| 零密碼雜湊函式庫 | `b1-spec.md` §1.3 表(`bcrypt|argon2|jose|next-auth|jsonwebtoken` 全零命中) |

📎 **最後兩列合起來說明了一件事**:**這個 repo 現在沒有任何「驗一個人的密碼」的能力** ——
`Q-AUTH-1=甲` 拍的就是「不要自己長出這個能力」。

---

## §3 設計

### 3.1 登入 body 加一個**選填**欄 `account`

```
{ account?: string, password: string, code?, recoveryCode?, setupSecret? }

account 留空  ⇒ 走【現行這一整條路】，一行都不動 ⇒ sub = { kind: 'fallback' }
account 有值  ⇒ 走【新的個人帳號路】                ⇒ sub = { kind: 'user', staff_id }
```
🔴 **「留空走舊路」是 D0-b 的硬要求**(母 plan §3):本片單獨上線 = **零行為改變**。
🔴 **`sub` 要等 B3 才進 payload** ⇒ 本片先把 `sub` **算出來**,B3 才把它**簽進去**。
⚠️ 兩片之間 `sub` 算出來沒地方放 —— **那是刻意的**,不要為了「別浪費」而提前改 payload(那會變成 B3)。

### 3.2 個人帳號怎麼驗(`Q-AUTH-1=甲:用平台內建`)

```
① 用 Supabase Auth 驗 email + 密碼          ← 我們【不碰】密碼、不存雜湊、不自己做鎖定
② 拿它回來的 auth user id（uuid）
③ 用那顆 uuid 查 admin_user_staff_map → staff_id      ← B1-b 建的表，service_role 只有 SELECT
④ 恰一筆 ⇒ sub = { kind:'user', staff_id }
   查無 / 不是恰一筆 / DB 例外 ⇒ 🔴 該次登入【失敗】
```
🔴 **第 ④ 步的三條明文禁止(照抄 `…-b3-spec.md:140-142`,同一個誘惑在這一層長得一樣)**:
① **不得**在查無時改走 `kind:'fallback'` ② **不得**採用請求端傳入的 `staff_id`
③ **不得**把 DB 逾時/例外當成「暫時放行」。
⚠️ **「有限次重試沒有被禁止」** —— 禁的是「重試用完之後改走別的身分」。

🔴 **`CHECK` 白名單不在這一刻執行**(`b3-spec` 那張表已定):簽發端做的是 `SELECT`,
而 **`SELECT` 不會重跑 `CHECK`**。這裡讀到什麼,取決於那一列**當初**寫進了什麼。

### 3.3 🔴 關掉 legacy fail-open(本片最容易漏、而漏了最貴的一件)

`issueSession`(`:116-133`)現在的行為:新 cookie 基礎建設缺 ⇒ **退回 legacy `ADMIN_COOKIE`**。
**而 legacy cookie 帶不動 `amr`,更帶不動 `sub`。**

⇒ **本片硬規定**:
```
account 有值（= 這是一次具名登入）而新 cookie 發不出來
   ⇒ 整個登入【失敗】，回一個明確的設定錯誤
   ⇒ 🔴 不得退回 legacy 分支（那會發出一顆「登入成功但沒有身分」的 cookie）
account 留空（備援）⇒ 現行行為一字不動，legacy 分支照舊可用
```
📎 **為什麼這是最貴的一條**:退回 legacy 的那一顆 cookie **活 30 天**(`:65` `maxAge: 60*60*24*30`),
而且**在畫面上與成功登入完全一樣**。⇒ **一次設定失誤 = 一個月的無身分 session,零徵兆。**

### 3.3-b 🔴🔴 legacy 的**另一半**:不是只有「發不發得出來」,還有「**收不收**」

> **2026-08-18 pull 後在最新樹上實地盤點才發現的缺口** —— 本檔 §3.3 只寫了**發**那一半。

`lib/auth-server.ts:41-47` `hasValidLegacy()` 逐字註解「**過渡雙讀**」,而 `:50` `requireAdmin()` 的邏輯是:
```
有新 cookie 且 SESSION_SECRET 設好 ⇒ 驗新 cookie
否則                              ⇒ 【接受 legacy】（除非 require_2fa 開著）
```
⇒ **就算 B2 讓具名登入不再【發】legacy cookie,一顆【已經在瀏覽器裡】的 legacy cookie
仍然可以通過 `requireAdmin` —— 而它最長活 30 天(`login/route.ts:65`)。**

🔴 **這一半為什麼比發那一半更容易漏**:發那半在**登入端點**裡,你改 B2 一定會讀到它;
收那半在**另一支檔的守門函式**裡,而它**今天是正常運作的功能**(過渡雙讀是刻意的)。

**⇒ 本片的規定(兩條,不要只做第一條)**:
1. **具名登入不得發 legacy**(§3.3 已寫)。
2. 🆕 **開關(`ADMIN_REQUIRE_REAL_IDENTITY` 的報價單對應物 / 或 B7 那一步)打開之後,
   `hasValidLegacy()` 這條路必須關掉** —— 否則「每個人都有自己的帳號」與
   「**任何人拿著一顆舊 cookie 就是管理員**」同時成立。
⚠️ **本片【不刪】那支函式**(它是 rollback 的一部分,同 B6 對 `actor-actions.ts` 的處置)⇒ **開關控**。
⚠️ **而它今天已經有一道天然的窄化**:`require_2fa` 開著時 legacy 一律拒(`:66` 逐字)——
   **而 `require_2fa` 現在是 `false`**(母 plan §1.3 複量) ⇒ **那道窄化今天不生效,不能當成緩解。**

### 3.4 首次登入強制改密碼:狀態存哪(**我拍板,附理由與代價**)

| 案 | 做法 | 代價 |
|---|---|---|
| **甲(本規格採用)** | `admin_user_staff_map` **加一欄** `must_change_password_at timestamptz`(NULL = 不必改) | 🔴 **要另一支 migration** ⇒ 又一次「Sean 在場 apply」;動 B1-b 建的表 ⇒ 鐵則 12 ③ |
| 乙 | 存進 Supabase Auth 的 `user_metadata` | 狀態住在平台那邊;**而我沒查過**能不能在 server 端可靠讀寫它(§6-② ) |

**選甲的理由(一句)**:`authorize` 那條路**本來就要查那張表**(§3.2 第 ③ 步)
⇒ **一次查詢同時回答「他是誰」與「他該不該先改密碼」**,不必多一次跨系統呼叫。
⚠️ **而代價要講清楚**:多一支 migration = 多一次 Sean 在場,這條線的排程本來就卡在那裡。

🔴🔴 **攔截點必須在【所有入口之前,包含 SSO `authorize`】**(母 plan §6 第 9 條逐字,
且母 plan 自己標「這是最容易漏的一條」):
```
app/api/sso/authorize/route.ts:49  const session = await getSession();
                                   ⇒ 這裡就要問「他需不需要先改密碼」
```
**只擋登入頁不擋 `authorize`** ⇒ 員工登入後直接點「網站管理」就進了 admin,強制改密碼形同虛設。

### 3.5 限速與訊息

- **per-IP 限速維持現況**(`:92-110`),**不新增 per-account 鎖定** ——
  `Q-AUTH-1=甲` 的整個重點就是「鎖定次數不要我們自己寫」(母 plan §3 D0-a 乙案的代價那一列)。
  ⚠️ **GoTrue 自己的限速值我沒查**(§6-③)。
- **查無帳號 vs 密碼錯** ⇒ **同一句訊息、同一個狀態碼**(母 plan §6 第 8 條)。
  🔴 **而「回應時間一致」那一半,本規格【不宣稱做到】** ——
  母 plan 逐字「**沒有想好怎麼量之前,不要把這條寫成通過**」。
  ⇒ B7 的九條狀態表裡,第 8 條標「**未做**」,不標「通過」。

---

## §4 驗收(負向為主;每一格都要能紅)

| # | 構造 | 預期 |
|---|---|---|
| 1 | `account` 留空 + 對的共用密碼 | ✅ 與今天**逐字相同**的行為(回歸格:它紅 = 本片弄壞了現況) |
| 2 | `account` 有值 + 對的個人密碼 + 映射有那一列 | ✅ 算出 `sub={kind:'user',staff_id}` |
| 3 | `account` 有值 + **錯**的密碼 | 🔴 失敗,**且訊息/狀態碼與第 4 格完全相同** |
| 4 | `account` **不存在的帳號** + 任意密碼 | 🔴 失敗,**且訊息/狀態碼與第 3 格完全相同**(這兩格互為對照,少一格就證不到「一致」) |
| 5 | `account` 有值 + 密碼對 + **映射查無** | 🔴 **登入失敗**;不得降級成 fallback、不得放行 |
| 6 | `account` 有值 + 密碼對 + **映射查詢丟例外** | 🔴 **登入失敗**(fail-closed) |
| 7 | 🔴 `account` 有值 + `SESSION_SECRET` / `EDGE_CONFIG` 缺 | 🔴 **整個登入失敗**;**不得**設 `ADMIN_COOKIE`(這格證 §3.3;**驗法要看有沒有 Set-Cookie,不是看畫面**) |
| 8 | `account` 留空 + 同樣缺那兩個 env | ✅ **照舊退回 legacy**(對照組:沒有這格,第 7 格會在「legacy 整條路壞掉」的世界裡也綠) |
| 9 | 帳號被標記「首次登入未改密碼」⇒ 打 **登入頁** | 🔴 導去改密碼頁 |
| 10 | 同上帳號 ⇒ **直接打 `/api/sso/authorize`** | 🔴 **擋**(這格是母 plan 第 9 條的本體;沒有它,第 9 格是裝飾) |
| 11 | 改完密碼後 ⇒ 第 9/10 格 | ✅ 都放行(對照組:證那道閘會關掉,不是永遠擋) |
| 12 | 🆕 **開關開之後**,瀏覽器帶著一顆**有效的 legacy `pcm_admin` cookie**、沒有新 session | 🔴 **擋**(§3.3-b 第 2 條;**沒有這格,「每個人有自己帳號」與「拿舊 cookie 就是管理員」會同時成立**) |
| 13 | 🆕 **對照組**:開關**關**時同一顆 legacy cookie | ✅ **照舊放行**(證第 12 格擋的是開關,不是把 legacy 整個弄壞了) |

---

## §5 順序與 rollback

```
① B1（表 + seed）        ← 已有草稿，等 Sean 在場 apply
② 3.4 甲案的加欄 migration ← 又一次 Sean 在場（本片新增的排程成本）
③ 應用層：3.1 / 3.2 / 3.3 / 3.4  ← 這三步都是「加東西不改行為」，可以分別上線
```
- **rollback ③** = 一般部署回退,零 DB 相依(`account` 留空的路徑從頭到尾沒動)。
- **rollback ②** = 🔴 **不要 `DROP COLUMN`**,留著 NULL 欄不礙事(expand/contract,母 plan §8)。
- 🔴 **`ADMIN_PASSWORD` 備援全程保留**(`Q2=A`)—— 整條線壞掉時 Sean 仍進得去。

---

## §6 🔴 我查不到的四個前提(全部會改變 §3 的做法)

| # | 前提 | 為什麼它承重 | 缺的那一道檢查 |
|---|---|---|---|
| ① | **這個專案有沒有設 anon key 的 env**<br>🔴 **2026-08-18 pull 後補量(見 §6.1)⇒ 這一格縮小了,沒有消失** | §3.2 第 ① 步要用 anon key 呼叫 Supabase Auth,而**本 repo 現在只有 service_role client**(`lib/supabase-server.ts:22`) | 在 Vercel / `.env` 看 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 在不在(**我不讀 `.env*`**,lessons §12-15) |
| ② | **server 端能不能可靠地用它驗密碼** | 同上;若不行,§3.2 要改成打 GoTrue token 端點 | 用測試帳號實跑一次驗證呼叫,看回什麼 |
| ③ | **GoTrue 自己的登入限速值** | §3.5 把 per-account 鎖定**整個交給平台**了 | 查 Supabase Auth 的 rate limit 設定頁 |
| ④ | **報價單 production schema 與 repo migration 是否一致** | B0 第 3 項,**至今未查**(`b1-spec.md:22` 逐字「未查,判可並行」) | 需要 DB access |

### §6.1 🆕 2026-08-18 pull 後補量:**不讀 `.env`,改問「程式碼有沒有引用它」**(HEAD `1149e05`)

| 問法 | 命中 | 讀出什麼 |
|---|---|---|
| `git grep -l 'NEXT_PUBLIC_SUPABASE_ANON_KEY'` | **0 檔** | Next 那一側**沒有任何地方**引用 public anon key |
| `git grep -l 'SUPABASE_ANON_KEY'` | **7 檔** —— `.env.example` / `docs/DEPLOY.md` / `docs/SECRETS.md` / `docs/STOREFRONT_CATALOG_CONTRACT.md` / `scripts/setup_fuse_vault.py` / `scripts/viewer.command` / `scripts/viewer.html` | 🔴 **全部是文件、`.env.example` 與 Python/檢視器腳本 —— `lib/` 與 `app/` 零命中** |
| `git grep -lE 'signInWithPassword\|auth\.admin\|auth\.getUser'` | **0 檔** | **這個 repo 現在沒有任何一條路在用 Supabase Auth 驗人** |
| 正向對照 `SUPABASE_SERVICE_ROLE_KEY` / `supabaseServer` | **78 檔 / 106 檔** | 尺是活的,不是搜不到東西 |

**⇒ 這一格從「完全未查」縮成兩個更小的問題**:
1. **`SUPABASE_ANON_KEY` 這個名字在 `.env.example` 裡** ⇒ 它是這個專案**登記過**的變數
   ⚠️ **而「登記過」不等於「正式環境設了值」** —— 這一半仍要有平台權限的人答(**我不讀 `.env*`**)。
2. **Next 那一側要新造一個 anon-key client**(現在只有 `lib/supabase-server.ts:22` 的 service_role 那一個)
   ⇒ **這是 B2 的工作量,不是未知數** —— 寫進 §3.2 的施工範圍。
📎 **形狀**:原本那一格把「env 有沒有設」與「code 有沒有能力用它」綁成同一個未知數;
**拆開之後,只剩前者需要別人。**

⚠️ **①②③ 都要能連到那個平台的人才查得到** ⇒ **這一片在那三題有答案之前不要開工**,
否則 §3.2 可能整段重寫。**這不是我拖,是排程上的硬約束**(母 plan §5 的同一句話)。

## §6.5 🔴 下游繼承:「表裡有那一列」不等於「這個人還能上工」(codex 關卡1 R5 角度 4)

`docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1 定案:**在職與否由 A 庫 `staff.is_active` 說了算,
而且是【每次用的時候】在 admin 收端問**(走 `resolveStaff()`)。
⇒ **本片(登入認人)【不查】A 庫** —— 它查的是報價單庫的映射表,而那張表只回答「這顆 uuid 是誰」。

**所以有一個狀態是走得通的**:
```
某人在 A 庫已被停用（is_active=false），而他的 Supabase Auth 帳號還開著
   ⇒ 他【登得進報價單後台】（本片放行，因為映射列還在）
   ⇒ 他【進不了 admin 的任何寫入】（B5 的 resolveStaff 回 null）
```
🔴 **這不是 bug,是兩層邊界的必然結果** —— 而**不寫下來的話,下一個人會把「登入成功」讀成「這個人還在職」。**

**⇒ 營運上的硬規定(寫進交接,不是寫進 code)**:
**員工離職要做【兩件事】** ①Supabase Auth 側停用他的帳號 ②A 庫 `staff.is_active = false`。
**只做 ② ⇒ 他還進得了報價單後台;只做 ① ⇒ 他進不來(而 A 庫看起來還在職)。**
⚠️ 而映射表的那一列**永遠不刪**(`no_delete`,代號永不重用)——**停用不是刪除**。

---

## §7 誠實邊界

1. **全檔讀自 `origin/main:482bec5`**,見檔頭。
2. **§3.4 的加欄是我拍的**(規格層),而它**多一次 Sean 在場 apply** —— 若 Sean 認為排程上不划算,
   乙案(`user_metadata`)仍在,**換案不需要改 §3 其他任何一段**。
3. **回應時間一致(母 plan §6 第 8 條)本片不宣稱做到**,見 §3.5。
4. **本檔未經 codex 對抗審查**(鐵則 12 ②)。動手前要跑。
5. **§6.5 那條「離職要做兩件事」是營運規定,不是機制** —— 目前**沒有任何東西**會在只做一件時提醒你。
   要做成機制得等 E8-C 或另立一片,**本片不做,明寫成缺口**。
