# 訂單列表:把「形狀」也搬到 BMW M 版(片 A-1)

> **狀態:🔴 等 Sean 批。命中鐵則 8(跨 3+ 檔)⇒ 本檔寫完不動手。**
> **檔數 ~~5~~ → 實際 10**(2026-08-18 收 codex `§3:40`;完整 manifest 在 §3-0。
> 原寫 5 是**只數了產品程式碼**,測試三支與本檔自己都沒算 —— 那不是零成本):
> `app/globals.css` · `components/orders/orders-table.tsx` · `app/orders/page.tsx` ·
> `components/orders/order-detail-items-table.tsx` · `lib/orders/order-status-axes.ts`(只改註解)
> **不動 `packages/ui`、不碰錢/權限/schema/平台設定 ⇒ 不命中鐵則 12 六類。**
> 2026-08-17 A 窗。對照表(Sean 看的那份)= artifact `f41decca-8db8-4539-b894-f45d337286e3`。
> 前置閱讀:`docs/specs/2026-08-12-admin-order-ui-design-brief.md`(849 行、十輪拍板)、
> `docs/design/admin-design-system.md` 檔頭落地狀態表。

---

## 0-V 🔴 V 窗對抗審查:**已做、已折入**(2026-08-18 A 窗處理)

> 主視窗兩次把這件事記成「開著的洞、V 窗可能卡在 codex」——
> **而 V 窗其實做完並發出了**(`~/pcm-mailbox/V-046-STOP-20260817.md` §②,`SendMessage` msg_id `81bfef7b`)。
> 審查基準 = `b174532f`;第二意見 = codex `gpt-5.6-sol`(零留痕已比對)。
> ⚠️ **我手上只有 V-046 的【摘要版】,沒有那 5 條的原文** —— 下面逐條標了我怎麼處理。

| # | 嚴重度 | V 窗說什麼 | 我怎麼處理 |
|---|---|---|---|
| P1 | must-fix | §3.2 末行「保留展開列的既有行為」= 片 2(已作廢)殘留語境 | 🔴 **定位對不上,見下方** |
| P2 | finding | §3.4 `n/m` 安全依賴「shippedQuantity 單列完整值」未寫 | ✅ 已補 → `§3.4` |
| P3 | must-fix | §7#13 寫「做不到就標未驗」= 允許本片最需要那格缺席,而 harness 是現成的 | ✅ 豁免已拿掉 → `§7#13` |
| P4 | nit | §8「純樣式」「回滾零副作用」是推的;中途放棄漏 §3.4 | ✅ 兩句已更正 → `§8` |
| P5 | nit | 全篇無工期、無 15-45 分鐘切片(鐵則 4) | ✅ 已補一節 → `§6-b`(**含一題要裁**) |

### 🔴 P1:引用的字面**在基準版與現版都零命中** —— 我沒有默默改掉,也沒有默默放過

**三道查證(可重跑)**
```
現版   grep -n '既有行為' 本檔                                    ⇒ 唯一命中 :661（在 §9-d-2 片 2 分析區）
基準版 git show b174532f:<本檔> | grep -n '展開列的既有行為'      ⇒ rc=1 零命中
基準版 git show b174532f:<本檔> | awk '/^### 3\.2 /,/^### 3\.3 /' ⇒ 四條 bullet，與現版【逐字相同】
```
⇒ **§3.2 從審查當時到現在一個字沒動,而那句話不在裡面。**
🔴 **這是【轉述】的形狀,不是 V 窗判錯** —— STOP 是摘要,那 5 條原文在 `SendMessage` 裡,我拿不到。

**而它指向的風險我獨立查過一次**(不是靠「找不到就當沒有」):
```
grep -c '展開列' 本檔 ⇒ 19 命中
  減去 4 處【本節自己寫的】（:23 / :34 / :42 / :45 —— 偵測字串自命中，不計）
  ⇒ 剩 15 處，最小行號 :634，而 §9-d 起始 = :625（grep -n '^## 9-d '）
  ⇒ 15 處【全部】落在 §9-d（片 2 / 片 3 分析區，已標作廢保留）
§1–§8（片 1 本體）⇒ 零命中
🔴 行號會漂:驗式是「最小命中行號 > §9-d 起始行號」，兩個數當場重跑、不要抄
```
⇒ **片 1 本體今天沒有「讀起來像要顧展開列」的殘留語境。**
⚠️ **但這隻能算「我用這個字面掃過、沒掃到」,不能算「P1 不成立」** ——
**缺的那道檢查 = 那 5 條的原文。** 拿得到原文的人請回頭核一次 P1。

---

## 1. 要改什麼 —— 一句話

**訂單列表已經照 BMW M 換好了【顏色】,但【形狀】還是照 OD 舊版做的。這片把形狀補上。**

---

## 2. 為什麼 —— 病根是「抄錯版本」,不是「漏做幾條」

Sean 2026-08-17 逐字:「原本設計的很多顏色或者是動畫等等好像都沒有跟上耶」
「**不是單純顏色、膠囊圓角還是直角這種問題而已**」。

量下去的結果 —— **兩個獨立實錘,同一個成因**:

| | 舊版 `overview-desktop.html` | BMW M 版 `overview-desktop-bmw-m.html` | repo 現況 |
|---|---|---|---|
| 未收款標記 | `:191` `box-shadow:0 0 0 1.5px …, 0 0 0 3px …` = **雙層外環** | `:218` `box-shadow:inset 3px 0 0 var(--danger)` = **左緣紅條** | **雙層外環**(抄舊版) |
| 篩選 chip 選中 | `:99` `.fchip[aria-pressed=true]{background:var(--fg);color:#fff}` | **沒有 `.fchip`**;工具列改 `.tab` `:148-154`,選中 = `border-bottom-color:var(--fg)` | **黑底白字膠囊**(抄舊版) |

🔴 **而 `order-status-axes.ts` 的 `PAY_MARK` 註解逐字寫著「雙層(外深內淡)是刻意的,逐字照 OD」**
—— 那句**對舊版成立、對 BMW M 版是錯的字面**。
**零機械訊號**:檔案沒改、測試沒紅、grep 那句話也看不出它指錯了版本。

📎 本片同時把這句錯字面改掉 —— 否則下一個人會拿它當「不要動」的依據。

---

## 3. 範圍 —— ~~五檔~~ **實際 10 檔,完整 manifest 見 §3-0**,逐條附 OD 行號

### 3-0 🔴 完整 manifest(2026-08-18 A 窗補,收 codex must-fix `§3:40`)

> **codex 原話**:「範圍寫 5 檔,但實作 commit 實際 9 檔;§7 的 browser harness、§10 的兩份文件
> 也未納入。§7.8 還寫『四或五』,須列完整 manifest,**不能把測試/文件當零成本**。」
> 🔴 **病的形狀**:`§3` 只列了「我要動的產品程式碼」,而**測試與文件是同一片的工作、同一顆 commit
> 進的**。少列它們會讓工期、rollback 面、review 面**三個都被低估**。

**實作那顆 `5e46360e` 實際動的 9 檔(數法 `git show --name-only --format='' 5e46360e`)**
```
apps/admin/src/app/globals.css                                    §3.1
apps/admin/src/components/orders/orders-table.tsx                 §3.2
apps/admin/src/app/orders/page.tsx                                §3.3
apps/admin/src/components/orders/order-detail-items-table.tsx     §3.4
apps/admin/src/lib/orders/order-status-axes.ts                    §3.5
─── 上面五支 = 原 §3 列的「五檔」；下面四支【原本一支都沒列】───
apps/admin/src/app/design-tokens.test.ts                          測試
apps/admin/src/components/orders/orders-table.test.tsx            測試
apps/admin/src/lib/orders/order-status-axes.test.ts               測試
docs/specs/2026-08-17-admin-list-bmw-m-shape-plan.md              本檔自己
```
**本班(2026-08-18)為驗收 12/13 補的**
```
apps/admin/src/components/orders/orders-status-visibility-browser.test.tsx   🆕 驗收 13(4 格)
apps/admin/src/components/orders/orders-table.test.tsx                       驗收 12 + §7:268 兩組(已在上表)
apps/admin/src/components/orders/orders-table.tsx                            §7:268 的 code 改動(已在上表)
```
⇒ **片 A-1 完整 manifest = 10 檔**(9 + 新增的 browser harness 1 支)。

⚠️ **不要用 `git diff 5e46360e..HEAD` 去數** —— 那會撈到全窗每個人的東西(實測 170+ 檔)。
   **要數這一片,數那一顆 commit 的 `--name-only`,再加上後續明確為它補的檔。**

### 3.1 `apps/admin/src/app/globals.css`

| # | 加什麼 | OD 逐字 | 備註 |
|---|---|---|---|
| a | 選中訂單整組:淺藍底 + 左緣 3px M 藍 | `:177-178` `tbody tr.sel td{background:color-mix(in oklab,var(--accent) 8%,var(--surface));box-shadow:inset 3px 0 0 var(--accent)}` | 🔴 **參數順序要翻**:寫成 `color-mix(in oklab, var(--card) 92%, var(--primary))`。數學等值,但 lightningcss 的降級值取**第一個參數** —— 照 OD 原順序寫,降級值會變成純 `--primary`(整列變實心藍)。理由已寫在同檔 `.cap-*` 那段,本片沿用同一條 |
| b | 退貨中整組:左緣 3px 琥珀 | `:179-180` `tbody.returning td{...var(--warn)}` + `tbody.returning tr.sel td{...var(--warn)}` | **選中時橘仍勝出**,不是藍蓋橘。兩條都要寫,少一條就變成「選中的退貨單看不出在退貨」 |
| c | 未收款膠囊:雙層外環 → 左緣 3px 紅 | `:218` `.cap.m-unpaid{box-shadow:inset 3px 0 0 var(--danger)}` | ⚠️ **等 Sean 答第 4 題**(見 §5)。他選乙就不動這條 |
| d | 未收出貨:**不加**紅條 | `:219` `.cap.risk.m-unpaid{box-shadow:none}` | 🔴 它整顆已是實心紅。**主視窗從截圖讀成「未收四格都有紅條」是錯的,四格裡只有三格有** |
| e | 表頭 36px / 12px / 字距 1.5px | `:166-169` | **不搬 `text-transform:uppercase`** —— 對中文 no-op,設計參照已裁「永不採用」 |
| f | 單號欄:等寬字 + 700 + `--foreground` | `:181` `td.oid{font-family:var(--font-mono);font-weight:700;color:var(--fg)}` | 現況是一般無襯線 400 |
| g | 狀態膠囊上下內距 3px、字距 0.5px | `:206-207` | 現況 `0px 8px` / `normal` ⇒ 膠囊被壓扁 |

### 3.2 `apps/admin/src/components/orders/orders-table.tsx`

- `OrdersTable` 加一個 optional prop `selectedOrderId?: string | null`。
- `OrderGroup` 的 `<tbody className='orders-group'>` 加 `data-selected={order.id === selectedOrderId || undefined}`。
- 退貨中:`data-returning` —— 🔴 **這一格有前置缺口,見 §4。**
- 🔴 **`data-*` 不用 `='false'`,用 `undefined` 讓屬性整個不存在** —— CSS 選的是 `[data-selected]` 的存在性,寫 `false` 會全部命中。

### 3.3 `apps/admin/src/app/orders/page.tsx`

- `:98` 現在是 `const panelOpen = readOpenPanelOrderId(rawSearchParams) !== null;`
  ⇒ 改成留住 id:`const panelOrderId = readOpenPanelOrderId(rawSearchParams); const panelOpen = panelOrderId !== null;`
- `:250` `<OrdersTable … selectedOrderId={panelOrderId} />`
- **零新增查詢、零 schema、零 API 改動** —— 那個 id 本來就在 URL 裡、本來就**已經被讀出來了**
  (`app/orders/page.tsx:98` 已呼叫 `readOpenPanelOrderId(rawSearchParams)`,只是把回傳值丟掉、只留 `!== null`)。
  **數法**:`grep -n 'readOpenPanelOrderId' apps/admin/src/app/orders/page.tsx` ⇒ 命中 `:98`。

### 3.4 🆕 商品列補「出 n/m」第三顆進度(**不依賴 §5 那四題的答案**)

> 2026-08-17 併入。主視窗背書為可執行片,但要求併進本 plan 讓 Sean 一次批,不另開片。

**檔**:`apps/admin/src/components/orders/order-detail-items-table.tsx`(232 行,單一元件)

| | 現況 | 要變成 |
|---|---|---|
| 欄頭 `:131` | `訂貨 · 到貨 · 取消` | `訂貨 · 到貨 · 出貨 · 取消` |
| `ItemAxisCell` `:35-60` | `orderedQuantity/quantity`(`:49`)、`instockQuantity/quantity`(`:55`)、`cancelledQuantity`(`:58`,>0 才顯示) | 中間插一顆 `shippedQuantity/quantity` |

**OD 逐字**(`overview-desktop-bmw-m.html:574-576` 的 `pip()`,與定案版 `overview-desktop.html:1036-1038` 的 `.pcstep` 同構):
```
cur >= total → 'on'（完成）   cur > 0 → 'mid'（進行中）   否則 → 'off'（還沒開始）
```
現有 `ItemAxisCell` 已經在做同一件事,**照它既有的寫法加第三顆,不要另立一套。**

🔴 **資料早就在了,不是新撈**:
```
packages/domain/src/order/types.ts:940            shippedQuantity: number
grep -rn 'shippedQuantity' apps/admin/src --include='*.tsx' --include='*.ts' | grep -v '\.test\.' | wc -l  ⇒ 20
而那 20 處【沒有一處】在 order-detail-items-table.tsx（該檔 232 行已開檔看完）
```
⇒ **零 schema、零查詢、零投影改動。** 純顯示。

🔴 **`n/m` 的截斷安全性依賴什麼 —— 寫下來**(2026-08-17 V 窗 P2,已複驗折入):
```
codex 原判「§3.4 有 embed 截斷陷阱」⇒ 🔴 V 窗複驗判【機制錯】:
  ItemAxisCell:35 吃的是【單列 summary】，embed limit 截的是【列數】，不截單列裡的值
  ⇒ 少幾列 ≠ 某一列的 n/m 變成半份資料算出來的
⇒ 但方向對:那個安全【沒有寫在任何地方】。補這一行:
  🔴 n/m 只有在「shippedQuantity 是【該列完整值】」時才是誠實的。
     它不是跨列聚合 ⇒ 截掉幾列不影響剩下每一列的 n/m。
     ⚠️ 哪天這一格改成【跨列加總】，這個安全就消失 —— 那時要回頭套 Q-EMBED-2。
```
⚠️ **這是一行寫下依賴,不是新守門。**
🔴 **2026-08-18 更正**:~~原句「與 §7 `shouldMergeAmount` 那條同型(為構造不出來的世界站崗 = 浪費)」~~
—— **那個類比錯了,而且錯得剛好相反**:`shouldMergeAmount` 那個世界 **fixture 構造得出來**
(codex must-fix `§7:268`;已於 2026-08-18 補上雙向兩格 + 突變實證,見 `§7`)。
**本條(`n/m` 單列完整值)才是真的構造不出來** —— 因為它根本不是跨列聚合,沒有「半份」這個狀態。

⚠️ **範圍限定,不要講大**:這是**往定案版靠一步**,**不是**「做完商品導向」。
定案版的三段在**商品卡**上(`.pcard` + 三個收合段),repo 的在 **`ItemsTable` 的一欄**裡。
**結構不同,只是同一個資訊多顯示一顆。** 講成前者會讓人以為 §0-G 做完了。

🔴 **更正:我原本寫「多一顆膠囊會變寬,要量會不會撐爆欄寬」—— 那個前提是錯的。已量。**

`ItemAxisCell`(`:44-62`)**不是膠囊、也不是並排** —— 它是 `<div class="text-xs leading-5">` 裡的
**垂直堆疊文字行**(`訂貨 3/3` 一行、`到貨 1/3` 一行、`已取消 N` 條件行)。
`display` 實測 `block`。⇒ **加第三行不吃寬度,吃【列高】。**

**實測(class 字串逐字取自 `:44-62`,注入真編譯 CSS 量)**:
```
                        現況      加「出貨 n/q」後
一般列（2 行 → 3 行）    56px  →  76px      （+20）
有取消的列（3 → 4 行）   76px  →  96px      （+20）
每行行高                 20px（leading-5）
寬度                     scrollWidth == clientWidth ⇒ 零截斷、不受影響
```
⚠️ **代價講明白**:訂單詳情的品項表**每一列都會高 20px**。三品項的單 = 全表高 60px。
**這不是可以忽略的數字** —— Sean 從第二輪起一路在講「上下留白太多、我們習慣看資訊很密集」(§0-A)。

### 🏁 2026-08-17 Sean 看圖後拍板:**乙 = 橫排**(`Q5`)

> **上面那段「+20px 列高」的分析仍然正確,但它描述的是【沒有被採用的甲案】。留著是為了讓人看得懂乙為什麼贏。**

**他是看實體版本選的,不是看文字**(兩案並排、各附整個商品區總高):
```
甲 直排（往下疊）  439px   一般列 77 / 有取消 97
乙 橫排（一行）    219px   全部 37
⇒ 甲比乙高一倍（多 220px）
⚠️ 他在【文字階段】答過甲、看完圖改成乙。以【最終】為準，不回頭問。
```

**⇒ 本片要做的是【乙】**:
```
目標  OD 定案版 .pcstep（overview-desktop.html:1036-1038）橫排一行
      訂貨 n/q · 到貨 n/q · 出貨 0/q
現況  ItemAxisCell（order-detail-items-table.tsx:44-62）垂直堆疊文字行
🔴 這【不是】加一行，是【改這一格的排版】
```
**做法**:把 `<div class="text-xs leading-5">` 內的三個 `<div>` 改成同一行的 `<span>`
(flex 橫排 + `gap-x-3`),並加入第三段「出貨」。「已取消」仍條件顯示、仍 `text-destructive`。

**已量(預覽,注入真編譯 CSS)**:
```
5 品項的單（含一筆有取消）  商品區總高 219px   每列 37px（= 現況列高，不變）
表格橫向溢出               無（scrollWidth == clientWidth）
```
⚠️ **仍要在實作後重量一次** —— 預覽用的是我手寫的 flex 版,**不是最終 code**。

🔴 **不得動任何一欄的欄寬** —— 那會踩到 `col-title` 的「全表最緊、任何新增不得再從這裡扣」(`globals.css:704`)
與另外四筆綠勾債。**改排版時若發現非動欄寬不可 ⇒ 停下來回報,不自行決定。**

### 3.5 同片順手改掉的錯字面

`apps/admin/src/lib/orders/order-status-axes.ts` 的 `PAY_MARK` 註解:
「雙層(外深內淡)是刻意的,**逐字照 OD**」⇒ 改成指明**哪一版**,並記錄本片換成 M 版的左緣紅條。
🔴 **改前先跑** `bash scripts/literal-sweep.sh '雙層'`,把同族字面一次掃乾淨(Sean 2026-08-15 拍板)。

---

## 4. 🔴🔴 前置缺口:「退貨中」不是欄位沒撈,是**整條退貨線在本 repo 不存在**

初稿我寫「列表投影裡有沒有這個欄位我還沒查」。**查完了,而答案比那嚴重一級。**

**數法(可重跑)**:
```
grep -rn '<pattern>' packages/domain/src packages/adapters/src apps/admin/src \
  --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | wc -l
```
| pattern | 命中 |
|---|---|
| `return_status` / `returnStatus` / `returning` / `returnedQuantity` | **0** |
| `returned_quantity` / `return_requested` / `order_returns` | 各 **1** —— 而**那三個命中全在同一段註解裡**(`apps/admin/src/components/orders/cancel-review-section.tsx:119-120`),逐字寫著「**退貨線在本 repo 零落點**」 |
| `grep -rln 'order_returns\|return_requested' supabase/migrations/` | **0 支檔** ⇒ DB 也沒有退貨表 |

⇒ **不是投影漏撈一個欄位,是這個功能整條沒有。**
⇒ OD 的 `returning` 來自它自己的假資料 `o.ret`;真後台**產不出這個狀態**。

**處置**:
- 3.1-b(退貨中橘線)與 3.2 的 `data-returning` **從本片整條移除**,不是拆到 A-1b。
- **它不是視覺片,是功能片** ⇒ 要做得先有退貨資料模型(§0-I 的兩段式:登記退貨 → 確認收到貨 → 才退款)。
  **那是 Sean 的題,不是我的。**
- 📎 我差點把「§0-I 說退貨中要看得見」讀成「資料已經在了」——
  **需求說要看得見,不代表有東西可以看。** 那是兩件事。

⇒ **本片只做「選中指示」那半。** 那半的資料來源 100% 確定 = URL 上的 panel id
(`app/orders/page.tsx:98` 已經在讀了)。

---

## 5. 等 Sean 答的~~四題~~ **五題(Q1–Q5)**,🏁 **全部已答**(答案會改變本片內容)

> 🔴 2026-08-18 收 codex nit「§5:186」:標題寫四題而表內已有五題;`Q5` 那格寫「未問」
> 而 `§201` 已記 Sean 看圖選乙。**兩個字面各自都對,合起來是矛盾的。** 統一成最終狀態如下。

| # | 題 | 影響本片哪一條 |
|---|---|---|
| 1 | 工具列收成一行? | **不在本片**(那是版面改動,會動篩選器結構)⇒ 答了另開片 |
| 2 | `--warn` 用設計稿 `#f59e0b` 還是現行 `#a56a07` | 影響 3.1-b 那條橘線的顏色。**他不答就用現行 `#a56a07`**(不動 token = 較保守) |
| 3 | 動畫做到哪 | **不在本片**。本片零動效改動 |
| 4 | 未收款紅記號要不要換成左緣紅條 | **直接決定 3.1-c 與 3.1-d 做不做**。他答乙 ⇒ 這兩條整條拿掉 |
| 5 🆕 | **出貨進度要直排(+20px 列高)還是照 OD 改橫排(不增高)** | 見 3.4 末段。**這題是量完之後才長出來的,原本不存在** |

---

### 🏁 2026-08-17 Sean 已回(逐字)

```
Q1 工具列   「先試試看甲，要調整我在說」   ⇒ 甲（全收）。🔴 可逆授權，不是定案，做出來要給他看
Q2 --warn   「Q-視覺2,3,4 全部依照設計稿」 ⇒ 甲 #f59e0b
Q3 動畫     同上                          ⇒ 甲 只做按鈕 130ms
Q4 紅記號   同上                          ⇒ 甲 左緣紅槓；未收出貨不加（OD :218/:219）
Q5          🏁 **已答:乙 = 橫排**（2026-08-17 看圖後拍板，見 §201）
            ~~🔴 未問 —— 它是量完 3.4 之後才長出來的~~（那句是它剛長出來時寫的，已過期）
```
⇒ **`3.1-b` 的橘線仍然不做**(§4:資料不存在)。**`3.1-c` / `3.1-d` 解鎖。**
⇒ **`Q1` 工具列不在本片**,另開片。
⇒ **「全部依照設計稿」= 往後同類分歧的預設** —— 但前提是**先確認哪一份是設計稿**
(今天已證明會出錯:三支 `*-directions.html` 是選項稿、`overview-desktop.html` 才是定案版)。

⇒ **本片可以在四題未答的情況下開工的部分**(逐條列,不是「其餘」):
`3.1-a` / `3.1-e` / `3.1-f` / `3.1-g` / `3.2`(僅 selected)/ `3.3` / `3.4`(出貨 pip)/ `3.5`(錯字面)
= **8 條**。
**3.1-b 卡 §4 缺口;3.1-c 與 3.1-d 卡第 4 題。**

---

## 6. 預期影響面

| 面 | 影響 |
|---|---|
| 錢 / 權限 / schema / migration | **零**。不讀新欄位、不動查詢、不動 RLS |
| `packages/ui` | **零** ⇒ 不命中鐵則 12⑥ |
| 平台設定 | **零** |
| 前台 storefront | **零**(`globals.css` 是 `apps/admin` 自己那支) |
| 其他後台頁 | **零 —— 已查。** `grep -rln "orders-grid" apps/admin/src --include='*.tsx' --include='*.css' \| grep -v '\.test\.'` ⇒ **3 支檔**:`app/globals.css`(規則本體)、`orders-table.tsx`、`order-toolbar.tsx`。<br>其中 **`.tsx` 的 7 行命中裡有 6 行是註解**,唯一的**渲染點** = `orders-table.tsx:528` `<div className='orders-grid …'>`。<br>⇒ `商品` / `客戶` 列表**沒有**用這個 class ⇒ 不連動 |
| 既有測試 | `design-tokens.test.ts` 釘 `:root` 的 token,本片不動 token 值 ⇒ 預期不紅。**預期不等於實測** |
| 手機卡片模式 | ⚠️ `@container` 卡片化下 `inset` 陰影落在哪、`<tbody>` 是不是還是定位脈絡(`globals.css` 有一條把定位脈絡抬到 `tbody.orders-group`)⇒ **要在 390px 實看,不能只看桌機** |

---

## 6-b 🔴 工期與切片(2026-08-17 V 窗 P5 must-fix 折入;原稿**全篇無工期**)

**為什麼要有這一節**:鐵則 4 = 一片 15-45 分鐘、Sean 可肉眼驗。
本片動 **10 支檔**(完整 manifest 見 `§3-0`;~~原寫「5 支檔(§3.1–§3.5)」~~ ——
那是**只數產品程式碼**的版本,收 codex `§3:40` 已更正),而它從頭到尾被叫「片 1」——
🔴 **一個名字讀起來像小片,而它的體積不是。** 不寫工期 = 讓批准的人估不出他在批什麼。

**估時(逐段;🔴 這是【估】不是量,實作後回填實際值)**
```
§3.1 globals.css 七條規則         ~15 分   （含 color-mix 參數順序那個坑）
§3.2 orders-table prop + data-*   ~10 分
§3.3 page.tsx 傳 id                ~5 分
§3.4 items-table 直排→橫排 + 出貨  ~25 分   🔴 這一段自己就吃掉一片的額度
§3.5 錯字面 + literal-sweep        ~10 分
驗收 1-14（含真瀏覽器兩條）        ~30 分   🔴 13 沒有豁免（見 §7）
                                  ────────
                                  約 95 分
```
🔴 **⇒ 超過 45 分鐘上限一倍以上。** 兩案,**要 Sean 或主視窗裁**(我不自己拆,拆片會動到已批的範圍):

```
甲 照批   當成【一片】做完，明寫「本片超出鐵則 4 上限、已知情」，commit body 記偏離
          理由:五段共用同一次真瀏覽器驗收（跑一次量完），拆開要跑三次
乙 拆二   片 1-A = §3.1/3.2/3.3（列表外觀，~30 分 + 驗收 3/4/5/6）
          片 1-B = §3.4/3.5（品項表橫排，~35 分 + 驗收 9/10/11）
          代價:真瀏覽器驗收要跑兩次；好處:每片都在 45 分內、Sean 分兩次肉眼驗
```
⚠️ **驗收 12/13/14 兩案都跟著 §3.1-3.3 走**(它們守的是列表那半)。

---

## 7. 驗收(每條可 yes/no)

1. `TURBO_FORCE=1 pnpm typecheck` / `lint` / `build` 三綠,各項貼 `0 cached, N total`
2. `pnpm test` 綠
3. **真瀏覽器 computed value 複驗**(不是讀原始碼):選中那組 `<tbody>` 的 `td` 量到
   `boxShadow` 含 `inset` 且 3px、`backgroundColor` 非 `--card`;**未選中那組量到 `none`**
   —— 🔴 **兩個世界都要餵:該亮的亮、該不亮的不亮。** 只量選中那組 = 恆真
4. 表頭量到 `36px / 12px / letterSpacing 1.5px`;單號量到等寬字 + `700`
5. 膠囊量到 `padding 3px 8px`、`letterSpacing 0.5px`
6. **390px 手機卡片模式肉眼看過**,截圖附上
7. `scripts/literal-sweep.sh '雙層'` 零殘留
8. `git status --porcelain` 只有 **§3-0 manifest 上的那些檔**
   (~~原寫「四 (或五) 支檔」~~ —— 收 codex `§3:40`:那個數字連測試都沒算進去)
9. **出貨行(3.4)**:`shippedQuantity = 0` 與 `= quantity` 兩筆各餵一次,顯示的分子分母都對 —— **兩個世界都餵**
10. 🔴 **commit body 必須寫出「這一格的【排版】被改掉了」,不准只寫「已補上出貨進度」。**
    做的是**乙案(直排 → 橫排)**,不是「多加一行」。
    ⚠️ **原第 10 條(列高 56→76)已隨甲案作廢** —— 乙案**列高不變**,那個數字不再是代價。
    **代價換成:這一格從上下堆疊變成橫排,是版面改動,不是加值。**
11. **實作後重量並貼數字**:5 品項的單(含一筆有取消)商品區總高、每列高、
    以及 `scrollWidth == clientWidth`(表格沒有被撐出橫向捲軸)。
    🔴 **同時要驗「欄寬一顆都沒動」**:`grep -n 'width' apps/admin/src/app/globals.css` 的 `.col-*` 六顆值
    與 `132/188/104/104/56/154` 逐字相同
12. 🔴 **`title` 屬性斷言**(`§9-d-1b` V1;**現在零斷言**):
    截斷時那顆膠囊的 `title` **存在且字面正確**
    (`orders-table.tsx:429`「這張單的品項清單這次沒有完整載入,算不出狀態。請重新整理。」)。
    **數法**:`grep -rn "算不出狀態" --include='*.test.tsx'` 目前 ⇒ **0**(量具對照「未知」同法 ⇒ **15**)。
    ⚠️ **jsdom 就做得到,這條沒有藉口。**
13. 🔴 **可見性一格**(`§9-d-1b` V2):截斷時那顆膠囊 **computed style 可見**
    (非 `display:none` / `visibility:hidden` / 零尺寸)。
    ⚠️ **jsdom 算不出真實 computed style** ⇒ 走真瀏覽器 harness。
    🔴 **這一格【沒有豁免】**(2026-08-17 V 窗 P3 must-fix,已折):
    原稿寫「做不到就明寫本片未驗」—— **而 harness 是現成的**,`where-is.sh` 當場驗:
    `apps/admin/src/components/orders/cancel-forms-browser.test.tsx` ⇒ **工作樹 291 行、在 dev 上**。
    ⇒ **藉口不成立。做不出來 = 停下來回報,不是標未驗放行。**
    **為什麼這一格不能缺席**:本片是**樣式改版片**,而既有那兩格
    (`orders-table.test.tsx:1769` / `:1783`)**斷言的是 `textContent`** ⇒ **樣式吃掉那兩個字的世界,它們照綠**。
    13 是 §7 驗收 1-14 裡**唯一**針對「截斷態那顆膠囊被樣式隱藏」的格 ——
    **數法**(落筆當下重跑過;🔴 **行號會隨編輯漂,重跑那條命令、不要抄這些數字**):
    `grep -n 'display:none\|visibility' 本檔` ⇒ 6 命中。§7 驗收區(`:233`–`:288`)內只有 **`:260`**(第 13 條本體)。
    ⚠️ 同區的 `:269` 是**這條數法自己**(偵測字串自命中,已標,不計);
    區外**四處** `:393` / `:395` / `:622` / `:632` 屬 §7 掃描段與 §9-d-1b,非驗收條目。
    ⇒ 讓它缺席 = 讓這一片最需要的那道檢查缺席。
    🔴 **量的是「被【隱藏】」不是「被擠掉」**(`docs/specs/2026-08-17-col-status-slack-measurement.md` 已量):
    `88px` 欄「未知」餘裕 **48px**、四字狀態餘裕 **24px** ⇒ **寬度不會是「未知」那格的死法**,
    先爆的是四字狀態。⚠️ 該量測**本機沒有 BMWTypeNext、用系統備援字寬** ⇒ 是**樂觀版**,真字型較寬會讓餘裕縮小。

14. 🔴 **「共 N 筆」雙向兩格**(本節下方 finding 的驗收,提到主清單免得被漏掉)
```
14a 截斷時（keywordTruncated=true）⇒ 畫面上【不得】出現裸的「共 N 筆」
14b 非截斷時（false）        ⇒ 【必須】印得出「共 N 筆」
🔴 沒有 14b，這個守門可以靠「永遠不印」通過 = 恆真格
```
    ⚠️ **性質**:套用既有拍板 `Q-EMBED-2`,**不是新增守門**;只有【文案字面】要 Sean 挑(兩案在下方)。
    **為什麼必要**:現有兩格(`orders-table.test.tsx:1769` / `:1783`)雙向、有判別力,
    **而它們斷言的是 `textContent`** ⇒ **樣式吃掉那兩個字的世界,那兩格照綠** —— **而本片正是樣式改版片。**

---

### 🔴 片 1「改什麼」的 happy-path 掃描(2026-08-17;主視窗指定的問句)

**問句**:**這一欄在「資料只載到一半」的時候,會印出「我不知道」,還是會印出一個由半份資料算出來的答案?**

**掃描分母(完整枚舉,不是抽驗)** —— `orders-table.tsx` 裡**所有**從 `order.lines` 算出來的東西:
```
grep -nE 'order\.lines|\.reduce\(|\.every\(|\.some\(|\.filter\(|\.length' orders-table.tsx
⇒ 只有兩處是【真的在算】:
   :135  shouldMergeAmount(order) = lines.length > 1 || lines.some(l => l.quantity > 1)
   :181  rows = lines.length > 0 ? lines : [null]        （純渲染，不產生數字）
其餘命中全在【註解】裡（:122 / :330 / :416-417）與一個無關的 :541 orders.length === 0
```

**結論:片 1 這一支檔裡【沒有第二個】。** 逐處判:

| 處 | 截斷時會不會印出半份資料算的答案 | 判定 |
|---|---|---|
| 狀態欄 `:427` | **不會** —— 印「未知」+ `title` | ✅ 已有閘(且是本片唯一的閘) |
| 金額欄 `:371-386` | **不會** —— 合併態印 `order.total`(**訂單層欄位,不是從 lines 加總的**);非合併態印該列 `lineTotal`(逐列自己的錢) | ✅ 兩態都不做聚合 |
| `shouldMergeAmount` `:135` | **不會** —— 見下方推導 | ✅ **但它的安全是【意外得來的】** |

🔴 **`shouldMergeAmount` 為什麼今天安全 —— 而這個理由沒有寫在任何地方**
```
截斷發生在 ADMIN_ORDER_LIST_ITEMS_EMBED_LIMIT = 500（mappers/order.ts:428）
⇒ 截斷時看得見的 lines 恆為 500 ⇒ lines.length > 1 恆真 ⇒ 恆走【合併態】⇒ 印 order.total（訂單層）
⇒ 那個「由半份資料決定要印哪一種語意」的分支，今天到不了危險的那一半
```
🔴 **而它的安全條件是「截斷上限 > 1」,而【沒有任何一行字或一格測試寫著這個依賴】。**
同一支 mapper 的註解自己也留了一個口子(`order.ts:425-426` 逐字):
> 「若專案 `max-rows` 日後被設到低於本值,截斷會發生在那個更低的數字上而**本判定看不見**」

⇒ **假想的壞世界**:截斷後只剩 1 列且該列 `quantity === 1` ⇒ `shouldMergeAmount` 翻 `false`
⇒ 同一張單,**資料載全與沒載全,那一欄印的是兩個【不同語意】的數字**
(整單的錢 vs 品項的錢;而 Sean 2026-08-06 拍 B **已知情接受兩種語意並存**,所以畫面上**沒有任何差別可看**)。
⚠️ **今天不可達,我沒有把它當成 finding** —— 它是**一條沒有被寫下來的依賴**。
⇒ **本片動作**:在 `shouldMergeAmount` 的 docstring 補一行「**本函式的截斷安全性依賴 `EMBED_LIMIT > 1`**」。
🔴🔴 **2026-08-18 收 codex must-fix `§7:268` —— 上面這個豁免【被推翻了】,而 codex 是對的。**

~~「一行註解,不是新守門 —— 為它加測試等於為一個構造不出來的世界站崗。」~~ **原句留痕。**

```
codex 原文（~/pcm-mailbox/V-047-…原文搶救-20260818.md §1）:
  有第二個:itemsTruncated + 僅剩一列 quantity=1 會讓 shouldMergeAmount 改印品項小計;
  §299 已抓到,卻以「構造不出」豁免。fixture 明明可構造,§7 應驗「仍印 order.total、不印 lineTotal」。
```

🔴 **我錯在把兩個不同的「構造不出來」混成一個**:
```
真實世界構造不出來  ✅ 成立 —— 今天截斷恆發生在 500 ⇒ lines.length > 1 恆真
fixture 構造不出來  ❌ 不成立 —— 測試直接餵 { lines: [單列 qty=1], itemsTruncated: true } 就有了
```
⇒ **而守門守的是後者。** 這條命中已立的判準:
**「誠實缺口只收構造不出來的;拿它豁免十幾行就能測的守門 = 把機制份量用光」。**

### 🔴🔴 2026-08-18 codex 對抗審查:**FAIL** —— 甲案做完之後被擊破 4 條,**其中兩條要 Sean 拍板**

> 跑法:`codex exec -s read-only --cd /Users/sean_1/pcm-bmw-m … < /dev/null`(零留痕已比對)。
> **我請它逐條擊破我自己的五個宣稱。結果:1 條成立、4 條不成立。**

| # | 我的宣稱 | codex 判 | 現況 |
|---|---|---|---|
| 1 | 今天實際影響面 = 零 | **成立**(限正式資料路徑) | ✅ 它同時指出:**測試構造得出來** ⇒ 守門有意義 |
| 2 | 不需新拍板 | **不成立** | 🔴 **要 Sean 拍板,見下** |
| 3 | 新增兩格有判別力 | **不成立** | ✅ **已修** |
| 4 | `data-l` 只是文字標籤 | **不成立** | 🔴 **要 Sean 拍板,見下** |
| 5 | 沒有其他漏網 | **不成立** | 🔴 **撿到一條更嚴重的,見下** |

**🔴 #3 我怎麼修的(已完成)**
```
病  fixture 的 lineTotal 與 order.total 都是 12,000
    ⇒「印的是 order.total」那條【零判別力】——改成印 lineTotal 照樣全綠
修  fixture 改用 line('l1', 1, 5000) ⇒ 兩值分開（5,000 vs 12,000）
    並補一條反向 expect(not.toContain('NT$ 5,000'))
證  發6 突變:標籤照舊「金額」但印 lineTotal ⇒ 我那格【紅】（連帶既有 4 格紅）
🔴 最刺的是:我自己在同一支檔的別處寫過「兩值都是 12,000 ⇒ 零判別力」，然後又踩一次。
```

**🔴🔴 #2 + #4 合起來 = 一題要 Sean 拍板(我不自己決定)**
```
codex 逐字  改動確實讓截斷態從「小計／品項段末」變成「金額／訂單抬頭區＋order.total」，
           是使用者可見行為。globals.css:1240-1247、:1362-1369
           「金額」排手機卡片訂單抬頭【第 7 位】，「小計」排品項段【第 15 位】
           ⇒ data-l 不只是文字標籤，它有【直接的 CSS 排序下游】
           既有拍板明載「改兩種語意須重拍」；Q-EMBED-2 拍的是「整格未知」，
           【沒有拍過這個 fallback】
⇒ 🔴 我原本寫「套用既有拍板、不需新拍板」——【那句被擊破了】。
```

**🔴🔴 #5 撿到一條更嚴重的(同族,本片沒收到)**
```
orders-table.tsx:449-452  截斷時印「未知」兩個字
                          而 className={status.capsuleClass}【仍是半份 lines 算出來的顏色】
orders-table.tsx:202-205  status = orderStatusView(order) 在 map 外面先算，不看 itemsTruncated
⇒ 🔴 文字說「我不知道」，顏色說「出貨完成」（綠）或「未收出貨」（紅）
⇒ 這比金額那條更貼近 Q-EMBED-2 的原意：顏色也是【算出來的值】
⚠️ 我【沒有修它】——它是視覺面（歸 Sean / Design），且與 #2 同屬「使用者看得到的改動」。
```

### 🔴 要 Sean 拍的兩題(`Q-EMBED-3` / `Q-EMBED-4`;白話版,**技術細節在上面**)

> **背景一句話**:訂單列表一次最多載 500 個品項。品項比 500 多的單,**畫面上那張單的資料是半份的**。
> 半份資料算出來的東西可能是錯的 —— 你 08-16 已經拍過「**寧可印「未知」也不要印算錯的值**」。
> **現在發現同一個問題還有兩個地方沒收到。**

```
Q-EMBED-3  那張單的【金額】欄，半份資料時要印哪一個?
  甲  印【整單總額】（我已經做好了，在等你點頭）
      好處:數字是對的（總額是訂單自己的欄位，不受載幾個品項影響）
      代價:🔴 手機上那個數字會【換位置】——從品項清單裡面，搬到卡片最上面那一區
           （因為「整單的錢」與「這個品項的錢」在版面上本來就住不同地方）
  乙  維持現況，不改
      好處:畫面一個像素都不動
      代價:品項超過 500 的單，金額欄可能印出【某一個品項的錢】而看起來像整單的錢
      ⚠️ 今天沒有這種單（要 500 個品項以上）；這是為了以後
```
```
Q-EMBED-4  那張單的【狀態】膠囊，半份資料時顏色要怎麼辦?
  現況  文字已經改成「未知」了（你 08-16 拍的），🔴 但【顏色沒改】
        ⇒ 畫面上會出現「一顆綠色的膠囊，上面寫著『未知』」
        （綠色 = 出貨完成。那個綠是半份資料算出來的）
  甲  顏色也改成中性灰
      好處:文字與顏色說同一件事
      代價:要動視覺，需要你或 Design 看過
  乙  維持現況
      代價:員工掃過去先看到顏色。🔴 綠色會讓他以為這張單好了。
```
🔴 **兩題都是「今天不會發生、以後會」** —— 要 500 個品項以上的單才碰得到。
⚠️ **我沒有替你選。** Q-EMBED-3 的甲我已經寫好放著(未推),你說乙我就撤掉。

---

**🏁 已做(2026-08-18,甲案 —— 🔴 依 #2/#4 仍待 Sean 拍板,已 commit 但【未推】)**
```
code  orders-table.tsx  shouldMergeAmount 加 order.itemsTruncated 為第一判準
      ⇒ 截斷時恆走訂單層語意，不再由半份 lines 決定
      ⚠️ 性質 = 套用既有拍板 Q-EMBED-2，不是新規格 ⇒ 不需新拍板
測試  orders-table.test.tsx  雙向兩格:
      截斷 + 單列 qty=1 ⇒ data-l='金額' + 印 NT$ 12,000（order.total）
      負向對照:同一組 lines、旗標關掉 ⇒ data-l='小計'（回品項層）
突變  拿掉 order.itemsTruncated ⇒ 該格紅（expected '小計' to be '金額'）
影響面 加上這一行之後，本檔既有 86 格【一格都沒紅】⇒ 印證「今天實際影響面為零」
```

### 🔴 片 1 finding:「共 N 筆」漏套 `Q-EMBED-2` —— **這是套用既有拍板,不是新增守門**

> **性質先講死**(批准流程不同,不要寫錯):
> **Sean 已經拍過 `Q-EMBED-2` = 甲(資料不完整就不要印算出來的值)。**
> 狀態欄守了、**這個載體漏了** ⇒ **修法不需要新拍板,只有【文案字面】是他的。**

**事實(逐條可重跑)**
```
order-toolbar.tsx:81   {!loadFailed && <p>共 {total} 筆</p>}      閘只有 loadFailed
OrderToolbarProps:18-25 沒有 keywordTruncated
grep -n 'keywordTruncated' order-toolbar.tsx ⇒ 0
🔴 ⇒ 這個元件【結構上看不到截斷】—— 不是有人忘了加判斷，是介面上不可能知道
```
**契約自己寫著那個數字在截斷時是什麼**(`packages/domain/src/order/types.ts:398-400` 逐字):
> `true` ⇒ …`total` 只代表「**前 100 筆再套其他篩選後的數量**」,**不是完整命中數**。

**現有減損(寫清楚、不誇大)**:`page.tsx:196+` 有一條琥珀橫幅無條件顯示(含 0 筆),
逐字說「符合這個關鍵字的訂單超過 100 筆,目前只找了最新的 100 筆」。
🔴 **但它沒有一個字說「所以上面那個『共 N 筆』不是總數」** ——
**警告在頁首,而被警告的那個數字在工具列**,兩者不在同一個視線上。

**最小修法(成本形狀由「要先開介面」決定)**
```
1 OrderToolbarProps 加 keywordTruncated: boolean
2 page.tsx 傳 result?.keywordTruncated ?? false
3 :81 那行在截斷時改字面（字面見下方兩案）
```

**文案兩案,給 Sean 挑(我不替他選;判準是「員工看到會不會誤解」,不是措辭品味)**
```
甲  共 100+ 筆（只找了最新 100 筆）
    畫面上:數字還在原位，後面直接掛限定 ⇒ 掃過去仍是一個數字，但它不再假裝自己是總數
乙  找到超過 100 筆，目前只顯示最新 100 筆
    畫面上:那一格【沒有裸數字】⇒ 不可能被當成總數抄走，代價是它比旁邊幾格長
```

**驗收 14(雙向,缺一不可)**
```
14a 截斷時（keywordTruncated=true）⇒ 畫面上【不得】出現裸的「共 N 筆」
14b 非截斷時（false）⇒ 【必須】印得出「共 N 筆」
🔴 沒有 14b 的話，這個守門可以靠「永遠不印」通過 —— 那是恆真格
```

**③ 這條與那條琥珀橫幅要一起改還是分開改 —— 我判【分開,而且先改數字】**
```
理由:橫幅【本身沒有說謊】，它說的是真話而且無條件顯示；說謊的是那個數字。
     數字改掉之後，畫面上就【不再有兩個東西各說各話】—— 橫幅講「為什麼只有這些」，
     數字講「這些是多少」，兩句都真、且各自站得住。
⇒ 不動橫幅。動它只會多一個要同步的字面（而今晚已經有兩則教訓在講同步字面會漏）。
⚠️ 這是我判的，可以推翻。若要一起改，那是「把警告移到數字旁邊」的版面題，不是文案題。
```

### 🔴 片 1 掃描 3/3:`globals.css .orders-grid` —— **零個「算答案」,而它會【藏東西】**

**分母與數法(可重跑)**
```
.orders-grid 起到檔尾           951 行
counter( / content: 命中        6 → 逐處開檔判:
                                  :972 / :1252  justify-content（grep 誤命中，不是產生內容）
                                  :1055         註解
                                  :921 / :1306  content:''（觸控熱區，無文字）
                                  :1182         content: attr(data-l)（手機卡片的靜態欄名）
⇒ 🔴 CSS 端【零個 counter、零個算式】⇒ 它【不可能】從半份資料算出一個答案。第三個 finding 沒有。
```
**而掃描撿到一件與驗收 13 直接相關的**:
```
:1154  .orders-grid td:empty { display:none }        ← 空格子整格消失（卡片模式的必要規則）
:848 / :1037 / :1223  唯三命中 .col-status 的規則 ⇒ 全部只動 width(88/84px) 與卡片 order
⇒ 🔴 col-status【今天沒有任何 display:none】⇒ 「未知」那格今天藏不掉
   （:303 註解宣稱過這件事，我獨立量過一次，成立）
```
🔴 **所以驗收 13(computed-style 可見性)守的是【本片自己可能造成的改動】,不是既有 bug** ——
**而本片正是會動 `.col-status` 寬度與卡片 order 的那一片。** 這句要留著,
否則下一個人會問「今天又沒壞,為什麼要這格」。

---

### 🔴 收工時**不准**寫的一句話

> ~~「商品導向做完了」~~ / ~~「§0-G 已落地」~~

**3.4 只是把現有 `ItemsTable` 那一欄的三個數字改成橫排、並補上第三段。**
定案版的商品導向是 **`.pcard` 商品卡 + 三個收合段 + 到貨登錄移出 9 欄表**
(`overview-desktop.html:1024-1101`),**本片一個字都沒做。**

⇒ **改成橫排是「借用定案版那一格的排法」,不是「做完商品導向」。**
沒有這句,下一個人會拿本片的 commit 去結 `§0-G` 的案。
⚠️ **而它現在更容易被誤讀** —— 因為橫排這個形狀**看起來就像 OD 的 `.pcstep`**,
而 `.pcstep` 在定案版裡是**商品卡的一部分**。**長得像不等於做到了。**

⚠️ **第 3 條是本片最容易假綠的一格**:`box-shadow` 在 Tailwind 下會疊一長串 `rgba(0,0,0,0) 0px 0px…`,
用 `!== 'none'` 判定會**恆真**。判準要寫成「字串含 `inset`」+ **未選中那組必須不含**。

---

## 8. Rollback

單一 commit。`git revert <sha>` 即可,**無資料遷移、無 URL 契約改動、無 API 契約改動**。

🔴 **兩句已更正**(2026-08-17 V 窗 P4,已折):

**① ~~「純樣式」~~ 不成立** —— §3.4 改的是**欄頭字面**(`訂貨 · 到貨 · 取消` → 多一段「出貨」)
與**那一格的排版**(直排 → 橫排)。**欄頭是使用者讀的字,不是樣式。**
⇒ 本片 = 樣式 + 一個 optional prop + **一處欄頭字面改動** + 一顆多顯示的進度段。

**② ~~「回滾零副作用」~~ 是【推的】—— 而 codex 去量了,結果是【revert 根本套不上】**

🔴 **codex 原文逐字**(`~/pcm-mailbox/V-047-…原文搶救-20260818.md` §1,`§8:326` must-fix):
> 「`git revert` 零副作用」是由無 DB/API 推論,不是量到;
> **目前對實作 commit `5e46360e` 跑 reverse-check 已直接 `patch does not apply`。**
> 改寫成「資料面無遷移;Git 回滾可能衝突,須先 dry-run」。

⇒ **正確寫法(照 codex 的話改)**:
```
資料面    無遷移、無 URL 契約改動、無 API 契約改動   ← 這三個是量得到的
Git 面    🔴 回滾【可能衝突】—— 5e46360e 之後這幾支檔又被動過
          ⇒ 撤退前必須先 dry-run:git revert -n <sha> 或 git apply --check -R
          ⇒ 「零副作用」在跑完那一步之前【寫不出來】
```
⚠️ **這條的形狀值得記**:原句每個字都對(確實沒有 DB、沒有 API)——
**而它從那兩個「沒有」推出了第三件事,那第三件事是關於 git 的,跟前兩個沒有因果關係。**

**中途放棄(逐處,🔴 原稿漏了 §3.4)**
```
§3.1 globals.css   拿掉那段 ⇒ 回現況
§3.2 orders-table  prop 傳了沒人用 = 無效果，不會壞
§3.3 page.tsx      同上（只是把已讀出的 id 往下傳）
§3.4 items-table   🔴 【不是】拿掉就好 —— 它改了欄頭字面與排版
                   ⇒ 放棄它要把 :131 欄頭與 ItemAxisCell:35-60 一起還原，
                     且還原後要重跑驗收 11（列高與 scrollWidth）
```

---

## 9. 本片【不做】什麼(寫出來,免得被當成漏做)

- ❌ **出貨進度「直排多一行」(甲案)** —— 🔴 **Sean 2026-08-17 看圖後選【乙 橫排】**
  (兩案並排、各附商品區總高 `439px vs 219px`)。⇒ **本片做橫排,直排整條不做。**
  ⚠️ 他在文字階段答過甲、看圖後改乙。**§3.4 上半段那份「+20px 列高」的分析描述的是【沒被採用的甲案】**,留著只為看得懂乙為什麼贏
- ❌ 工具列收一行 / 篩選器結構(`Q1`=甲 已批,但**那是另一片**,不在本片)
- ❌ 任何動效(`Q3`=甲 只做按鈕 130ms,**另一片**)
- ❌ 退貨中橘線(卡 §4 資料缺口)
- ❌ `.fchip` → `.tab` 改造 —— **它是版面改動,不是換值**,且 §0-D 第 6 列記著我們是**知情地**用 `aria-current` 偏離 OD。改它要重讀那一列
- ❌ 斑馬紋、建立包裹改名、詳情頁單頁化 —— 各自獨立,不塞進本片
- ❌ 欄寬 —— 六顆已與 §0-D 拍板值逐字相同,**不要動**
- ❌ **商品卡 `.pcard` 改造** —— 定案版 `overview-desktop.html:1024-1101` 把採購/到貨/出貨收進**每張商品卡的三個收合段**,
  並把「到貨登錄」表單**移出 9 欄表**(那正是 brief §1 病灶的解法)。
  🔴 **那是換結構,不是換樣式** ⇒ 不塞進本片。3.4 只是把現有 `ItemsTable` 那一欄改成橫排,**兩件不要混為一談** ——
  ⚠️ **而橫排這個形狀看起來就像 `.pcstep`,所以更容易被誤讀成「商品導向做完了」**
- ❌ **五筆綠勾債**(`col-oid` 132 / `col-vehicle` 180 / `col-title` 154 / `col-brand` 188 / `col-invoice` 56 只剩 1px)
  —— **本片不解、也不得惡化。** 🔴 **改排版時若非動欄寬不可 ⇒ 停下來回報,不自行決定**
- ❌ **§0-G ③ 供應商只顯示名字** —— **已合格,不用做**:
  OD 定案版 `<td>RPM Carbon</td>` / `<td>鴻鎧國際</td>`;repo `item-procurement-section.tsx:165` 是 `p.supplierLabel`。
  §0-G 罵的那句長敘述是**當時 OD 的寫法**,repo 從來沒那樣寫過

---

## 9-b 🔴 三軸擺法三欄圖(artifact `8f6faf25`)的引用限定 —— **數字可以引,但不能單獨引**

**Sean 2026-08-17 拍板:訂貨狀況採【丙案】** —— 膠囊只留 `1/1 1/1 0/1`,標題移到欄頭。

| 案 | 膠囊佔 | 品名剩 | 中文字 | 三個品名被切 |
|---|---|---|---|---|
| 甲 同一列(他原本說的) | 191px | **33px** | 2 | **3 / 3** |
| 丙 只留數字(**採用**) | 109px | 115px | 8 | 1 / 3 |
| 戊 膠囊自己一行 | 191px | 234px | 18 | 0 / 3 |

🔴 **引用「1/3 vs 0/3」時必須帶這個限定,單獨引用那兩個數字是錯的**:
- 三欄圖第三顆品名原本是 **`全段排氣管鈦合金消音器`,那是編的** —— V 窗實查兩支 `globals.css` **0 命中**,
  全 repo 搜「全段排氣管」只在 3 支測試檔、真字面是 `Akrapovič 鈦合金全段排氣管`。
  圖上原本還宣稱它是「`globals.css` 註解自己點名的同一顆」⇒ **那句出處是假的**(同一句也曾被抄進
  `docs/design/admin-design-system.md`,已於同日留痕改正)。
- 🔴 **丙與戊在「被切」那一欄的差,全掛在那顆長品名上**;**今天示範單裡的品名都短 ⇒ 兩案打平。**
- `33px` 是**最壞那一列**(三列實得 33/40/40,33 出現在金額最寬那列;金額進六位數會更糟)。
- 「品名裝得下幾字」= 可用寬 ÷ 13(單一中文字實測)⇒ **等寬估算,英數混排會偏多**。

📌 **Sean 對「換掉那顆假品名」的裁定是「不重要,就這樣就好」** ⇒ **圖不重做、不重量**。
   ⚠️ 但 A 窗在收到該裁定**之前**已重發過一次(v2,換成 `Akrapovič 鈦合金全段排氣管`);
   **九個數字與三個被切計數一個都沒變**(該真品名 166px 同樣超過丙的 122px 可用寬)⇒ **結論未受影響**,
   不再回退。**本節是那張圖的引用說明,以本節為準。**

🔴 **「數字是對的、來源是編的」與「數字是編的」嚴重度不同,不要混講**:
本案是**前者**。換成 repo 真品名 `Akrapovič 鈦合金全段排氣管`(166px)之後重量,
**九個數字與三個被切計數一個都沒變** ⇒ 那顆編的品名**沒有製造出假結論,只製造了假出處**。
⇒ **那張圖的數字可信,出處那一句不可信。** 下一個人不必因為這條 finding 就丟掉整張圖。

🔴 **設計稿的每個寬度都可能是【算出來的】,改它之前先找那個算式**:
`overview-desktop.html:339` 給料號欄 `flex:0 0 158px`,而該檔示範單裡最寬的真料號
`BRM-GP4RX-108-DUC-V4S`(`:767`)實測 **151.7px** ⇒ **158 是照最寬料號 + 餘裕訂的,不是隨手填的**。
同理面板 720px(`overview-desktop-bmw-m.html:341`)與五欄需求 715px 幾乎重合。
⇒ **看到設計稿的某個數字覺得「可以改小」之前,先找它蓋住的是哪一顆最長值。**

📎 **紀律(這件事留下來的)**:**任何進圖的示範值,旁邊要寫得出來源(`檔案:行號`),寫不出來就不准進圖。**
   今天在同一條線上「量錯東西 / 字面對不上事實」發生 4 次,共同形狀是
   **「我需要一個值,而手邊沒有,就先放一個看起來合理的」。**

---

## 9-c 🔴 `A-221-STOP-20260817.md` §4 逐節稽核 —— **§4-⑧ 是唯一有假宣稱的那一節,其餘七節可引用**

**為什麼做**:`§4-⑧` 一節裡已證實**三句**假出處/假宣稱,而其中一句**已經進了 repo**
(`abf1e947` 的假出處)。那封信是這條線的來源檔、正在被引用 ⇒ 這是止血不是清理歷史。
**做法**:逐句、不抽樣;每一句的判別動作 = **去那句話描述的那一行 code 旁邊查**,不先去 `git log`。
**落點**:**不改那封信**(信不回改),結果寫在這裡 —— 這是會被引用的那份檔。

| 節 | 判定 | 依據(可重跑) |
|---|---|---|
| **§4-①** 選中色塊未做渲染級驗證 | ✅ **真**(可驗的兩句成立)+ ⚠️ **一句查不到** | 「`apps/admin` 底下沒有 `.env.local`」⇒ `ls -a apps/admin \| grep -c '^\.env'` = **0**(量具對照:同命令在 `apps/storefront` ⇒ 命中 `.env.example`,所以那個 0 有判別力)。「computed `inset 3px 0 0 rgb(0,102,177)`」與 `globals.css:700` `box-shadow: inset 3px 0 0 var(--primary)` **一致**(`--primary` = `#0066b1` = `rgb(0,102,177)`)。<br>⚠️ **「DOM 守門 4 格 / CSS 守門 2 顆」查不到具體位置**:`grep -rln 'inset 3px' --include='*.test.ts*' apps/admin/src` ⇒ **0**(守門沒有釘這個字面)。**分母 = `apps/admin/src` 全部測試檔。** ⇒ 引用那個「4 格 / 2 顆」時要向原作者要位置。 |
| **§4-②** `pnpm test` 在那台機器跑不出可信結果 | ⚠️ **不可重驗**(機器狀態) | 原文**自己已標**「基準線只跑了一次 ⇒ 『基準線不會 flake』未證實」「我自己加了 4 格測試,不能說完全無關」⇒ **它的射程限定寫在原文裡,照原字引用即可。** |
| **§4-③** Vercel build cache 殘餘風險 | ⚠️ **不可重驗**(外部平台) | 原文**自己已標「未驗」**。 |
| **§4-④** 五筆綠勾債 | ✅ **全真,五個數字逐個有出處** | `col-oid` 132 `:783`;「已取消舊格式單需 **190px**」`:779` 逐字;`col-vehicle` 180 `:803`,Ducati 需 229 `:795`,**2/11 列被截** `:796`(逐字「兩個 oracle 一致:文字寬法 2/11、`scrollWidth` 法也 2/11」);`col-title` 154 `:825`,「只裝得下約 **9 個中文字**」`:821` 逐字;`col-brand` 188 `:809`,`EVOTECH PERFORMANCE` 187 `:807`;`col-invoice` 56 `:855`。 |
| **§4-⑤** `.kb` 那顆 ⋯ 方鈕 | ✅ **全真,三個數字逐字吻合** | 定案版 `overview-desktop.html:214-215` ⇒ `border-radius:6px; width:26px; height:26px`;bmw-m `:187-189` ⇒ `width:24px; height:24px; border-radius:0`;repo `globals.css:969-974` `.orders-grid .col-ops a` ⇒ **26×26**。<br>🔴 連它那句「**圓角碰巧對了,是靠『禁裸 radius』修的、不是照 bmw-m**」都有出處:`globals.css:976-979` 逐字「這裡原本寫死 `6px`…改成 `var(--radius)`」。 |
| **§4-⑥** bmw-m 沒有 `.fchip`,工具列改 `.tab` | ✅ **真,而且那個「7」精確** | `grep -c 'fchip' overview-desktop-bmw-m.html` ⇒ **0**;定案版同命令 ⇒ **14**(量具對照,證明這把尺抓得到 `fchip`);`grep -c '\.tab' overview-desktop-bmw-m.html` ⇒ **7** —— **與原文的「7 命中」逐字相同**。<br>⚠️ 用別的 pattern 會得到別的數(`grep -o 'tab'` ⇒ 48、`class="tab` ⇒ 4)⇒ **引用「7」時要帶 pattern `\.tab`。** |
| **§4-⑦** 退貨線整條不存在 | ✅ **真,兩條 pattern 重跑皆 0** | `grep -rlE 'return_status\|returnStatus\|returning\|returnedQuantity' apps packages`(排除 `.test.`)⇒ **0**;`grep -rlE 'order_returns\|return_requested' supabase/migrations/` ⇒ **0 支檔**;`cancel-review-section.tsx:118-127` 那段字條**仍在**(該段落 `grep -c 退貨` ⇒ 3)。 |

### 結論(這一格才是重點)

🔴 **`§4-⑧` 是那封信裡唯一有假宣稱的一節。其餘七節逐句過完,沒有第四句是假的。**
⇒ **那封信可以繼續被引用**,條件三個:
1. **`§4-⑧` 的兩句不得引用**(「沒有人記過」/「`globals.css` 註解自己點名的那顆品名」)——
   正確版在 `docs/design/admin-design-system.md` 檔頭 `§0-D` 那一列。
2. **`§4-①` 的「DOM 守門 4 格 / CSS 守門 2 顆」要向原作者要位置**,repo 內查無該字面。
3. **`§4-②③` 是原作者自標的「不可重驗」** ⇒ 照原字引用,**不要把它們讀成已驗**。

📎 **這次稽核自己驗證的一件事**:`§4-⑧` 之所以是唯一出事的那一節,是因為它是**唯一一節在講「別人沒記過的新發現」**——
其餘七節都在講「我量到什麼、出處在哪」。
⇒ 🔴 **判別句:一節裡若出現「沒有人 X 過」「這是第一次 Y」,那一句就是該節最該被查的一句。**

---

## 9-d 🔴 A 甲(一個品項的三件事收在同一張卡)的盤點與切法 —— **去看的,不是估的**

**背景**:Sean 08-17 選 A 甲,而**片 1 的 plan 只做了「列的形狀」,沒有做那個合併** ——
**那是交付不足,不是他加需求。**

### 盤點三個數字(主視窗要求「去盤點,不要估」)

**① 要動的檔(7 支,不含測試;角色標明)**
```
容器   order-detail-items-table.tsx     展開列內容從「只有改金額」→ 三段
       item-amount-row.tsx              展開列的殼（只握「展開誰」的 client state）
被搬的 item-procurement-section.tsx     每一輪搬進對應品項的展開列
       receipt-record-form.tsx          到貨登錄（已掛在上一支的 :201，跟著走）
       receipt-undo-bar.tsx             撤銷回條（跟著走）
       shipment-launcher.tsx            🔴 唯一真的「改行為」的那支（見片 3）
上游   order-detail.tsx                 少渲染一個區塊、多傳 props
```

**② 資料流:不用動 server**
```
item-procurement-section.tsx:269   {detail.items.map((item) => {…})}
packages/domain/.../types.ts:698   AdminOrderItemProcurement = 單一品項的【一筆採購】
                          :503     每個 item 自帶 quantitySummary
⇒ 那個區塊【現在就已經是逐品項渲染】，只是把 N 個品項並排在訂單層的一張卡裡
⇒ 搬進展開層 = 把同一個迴圈的每一輪【放到不同的 DOM 位置】
⇒ 🔴 不動 server、不動投影、不動 RPC ⇒ 鐵則 12 六類【不命中】
```
🔴 **而展開容器也已經存在**:`order-detail-items-table.tsx:168 <ItemAmountRowGroup>` /
`:174 <ItemAmountRow … colSpan={ITEMS_TABLE_COLSPAN}>` / `:137` 註解逐字
「展開列的 `colSpan` 與表頭欄數必須一致」
⇒ **A 甲 = 換掉／擴充那個展開列的【內容】,不是新建展開機制。**

> 🔴 **留痕:「`ItemProcurementSection` 從【訂單層一次列全部】→ 逐品項渲染」那句是【錯的,已撤回】。**
> **它錯在:我看區塊位置推的,沒開檔** —— 開了 `:269` 才知道它本來就 `detail.items.map`。
> ⚠️ **而主視窗曾採用那句當「結構理由比排程理由硬」的依據** ⇒ **結論(走三片)仍對,而引的理由裡有一條是錯的。**
> **這是雙方的:一邊歸類錯,一邊在上面推了結論。**

**③ 測試射程**
```
item-procurement-section.test.tsx   29 格
shipment-launcher.test.tsx          18 格
item-amount-row.test.tsx             8 格
procurement-wiring.test.tsx          4 格
order-detail-items-table            （無測試檔）
⇒ 59 格
```
> 🔴 **`59` 是【這些檔裡的總格數】,不是【會紅的格數】—— 未實跑改動來數紅的。**
> 要真數,得先做一次改動再跑,**而那本身就是第一片的工作。**

### ~~三片切法~~ **兩個有效片 + 一個作廢紀錄**(依風險由低到高)

> 🔴 2026-08-18 收 codex nit「§9-d:479」:片 2 已整片作廢(Sean 拍甲),
> 而「三片切法」這個名字會讓**作廢片繼續像一個排程節點**。
> **有效片 = 片 1(列表形狀)、片 3(出貨建箱);片 2 = 作廢紀錄,原文加刪除線保留。**

```
片 1  列五欄 + 收款併一格（含尾款）+ 三處對齊 + 兩處改名 + 鍵盤 cookie 順手修
      🔴「對齊」在「改什麼」要寫成:欄頭與列【共用同一組明確軌道】
         不是「調寬一點」—— 後者畫面會變好而【量到仍差 8px】（2026-08-17 實錘;
         根因＝欄頭與列是兩個獨立 grid，auto 欄各自依內容算寬）
片 2  🏁 【整片作廢，2026-08-17 Sean 拍甲＝採購留在訂單層，不搬】
      ~~展開列內容:訂貨段 + 到貨登錄段搬進去~~
      ~~資料現成、容器現成、不動 server ⇒ 風險低，動的是 DOM 位置與 props~~
      到貨登錄住在採購表裡（item-procurement-section.tsx:201）⇒ 採購不搬它機械上跟著不搬
      ⇒ 兩件事都不做了 = 這一片沒有剩下的工作（詳 §9-d-3）
      ✅ 仍成立且與本片無關:品項層備註【Sean 08-17 裁定不做】（「備註留在訂單層就好」）
片 3  🔴 出貨段「這一項出幾件」——【拆】訂單層彈窗，這是行為改動不是搬家
      shipment-launcher.tsx:188 註解逐字「單張訂單版的勾單流程（預選 = 本單全部還能出的品項）」
      ⇒ 獨立提 plan，可能命中鐵則 12（動共用的建箱流程）
```

### 🔴 片 3 與 `S1=A` 的關係(寫下來,免得下一個做片 3 的人重踩)

```
· S1=A：建箱走訂單總覽勾單
  shipment-section.tsx:3 逐字「這張卡的定位是【查看與補救，不是主要建箱動線】」
· 2026-08-09 Sean 實測後追加：出貨卡要能直接出貨
  ⇒ 已經有【兩個入口】（shipment-launcher.tsx:5 逐字）
· 🔴 而 OD 展開層那個【不是第三個入口】：
  overview-desktop.html:1091-1099 只有一個 checkbox「這次出 N 件」，
  建立出貨那顆鈕仍在訂單層 ⑤出貨區（:1222）
  ⇒ 那是【把預選攤開給人改】，不是新的建箱動線 ⇒ 與 S1=A 不牴觸
```
> 🔴 **「我判不算改動線」——【那是我判的】。** 遞 plan 時要帶給 Sean,**讓他有機會說「不對,那算」。**

📎 **這一節記下的判別句(來自查 `S1=A` 那一輪)**:
> **「撈到一條拍板」不等於「撞到那條拍板」—— 中間還有一步:讀那條拍板管的是什麼。**
> 三步版:① 有沒有人決定過 ② 那條決定管的是什麼 ③ 我要做的落在它的射程裡嗎

### 9-d-1b 🔴 片 1 驗收要補的兩格 —— **現有兩格守的是【另一個層】**(2026-08-17 V 窗查證,我複核)

**背景**:`itemsTruncated=true` 時狀態欄印「未知」而不印算出來的狀態。**那格畫面會對員工說謊而不會紅**,
所以它有守門 —— **而守門守的層不對**。

```
現有兩格（雙向、有判別力，fixture 刻意選「不截斷時會算出最危險答案」那組）:
  orders-table.test.tsx:1769  截斷 ⇒ 含「未知」+ not.toContain('出貨完成')
  orders-table.test.tsx:1783  正向對照:旗標關 ⇒「出貨完成」回來
🔴 兩格斷言的都是 textContent
⇒ 我擔心的那個世界（改版面時那兩個字或 title 被【樣式】吃掉）這兩格【照綠】——
   display:none 或膠囊覆蓋樣式【不改 DOM 文字】。
🔴 而 :429 那個 title（「這張單的品項清單這次沒有完整載入,算不出狀態。請重新整理。」）零斷言:
   grep -rn "算不出狀態" --include='*.test.tsx' ⇒ 0     量具對照 grep -rn "未知" 同法 ⇒ 15
```
> 🔴 **這正是那一族的又一面:格子有、雙向、有判別力 —— 而它守的是另一個層。**
> **而它恰好活在【樣式改版片】裡,也就是最可能觸發那個世界的那一片。**

**⇒ 片 1 驗收補兩條(每條可 yes/no)**
```
V1  斷言 title 屬性存在且字面正確（現在零斷言）—— jsdom 就做得到，這條沒有藉口
V2  可見性一格：截斷時那顆膠囊【computed style 可見】（非 display:none / visibility:hidden / 零尺寸）
    ⚠️ jsdom 算不出真實 computed style ⇒ 這條要等真瀏覽器 harness
       （`cancel-forms-browser.test.tsx` 那條現成的），或明寫「本片未驗、缺口記在哪」
```
🔴 **`title` 零斷言那件我判【併進片 1 驗收,不另開 backlog】** —— 與那格畫面同一個根、同一片會動到它,
另開會長出第二個真相(同 `#17` 的處置)。**這是我判的,你可以推翻。**

### 9-d-2 🔴 三片切法 × 「保護」那一型的對帳(2026-08-17 A 窗)

> 🏁 **拍板後的現況(2026-08-17 晚補記,原文一字未改)**:
> **F3 已由 Sean 拍板 —— 甲,採購留在訂單層、不搬**(原字 `採購留在訂單層 ：甲 · 現在就是這樣`)。
> ⇒ **F1 / F2 / F4 三條都是「搬完之後」的問題,連同片 2 一起作廢。**
> 🔴 **而它們的分析本身沒有被推翻,只是暫時沒有適用對象** ——
> **任何未來想搬這個區塊的人,要先解掉 F1 那三樣訂單層的東西。原文全部保留在下面。**

**問句**:那三片的驗收條件,有沒有哪一條是在 happy path 上寫的、
而「保護」那型會讓它**在真實畫面上不成立**?

**對帳來源**:`docs/specs/2026-08-17-order-panel-feature-inventory.md` §2 的 5 條「保護」列(#14–#18)。
**結論:片 2 命中四條,其中 F1 與 F3 我判是【片界問題】不是 nit。**

**F1 🔴 片界:`ItemProcurementSection` 有三個【訂單層】的東西,搬進展開列之後沒有家**

```
item-procurement-section.tsx:251      <h2>採購(向供應商訂貨)</h2>        區塊標題
                          :253        detail.itemsTruncated ⇒ scope='order' 截斷警告
                          :255-266    suppliersFailed 紅字（供應商清單載入失敗）
```
片 2 寫的是「**每一輪**搬進對應品項的展開列」—— 而上面三個**不屬於任何一輪**。
- 🔴 `suppliersFailed` 那條,code 自己逐字寫著「**不可靜默:選單空掉會讓員工以為『這家不存在』**」(`:247`)
  ⇒ 搬完沒替它找位置 = **親手把它靜默化**,而那正是它存在的理由。
- 🔴 `itemsTruncated` 更硬:**品項被截掉時,那些品項根本沒有列可以展開**
  ⇒ 「逐品項的警告」在定義上蓋不到它。這一條**不可能**靠搬家解決,只能留在訂單層。
⇒ **片 2 的「改什麼」要多一句:這三個留在訂單層(或搬到品項表頭),不跟著品項走。**

**F2 ⚠️ 驗收:片 2 若寫「每個品項的展開列都看得到訂貨段與到貨登錄段」——那是 happy path**

```
#14 截斷       ⇒ 整份表單 disabled + 送出鈕【不渲染】(item-procurement-form.tsx:379)
#16 停用供應商 ⇒ 送出鈕 disabled + 一段說明文字
#17 作廢列     ⇒ 到貨入口整格變成「—」(item-procurement-section.tsx:198)
```
⇒ **驗收要分三個世界寫**(正常 / 清單截斷 / 該列已作廢),否則「看得到那兩段」在真實畫面上會是假的。
⚠️ 這一條是**驗收條款的措辭問題,不是片界問題** —— 改字就好。

**F3 🔴 這不只是搬家:一次看得到 N 項 → 一次只看得到 1 項(要讓 Sean 知道)**

```
item-amount-row.tsx:38     const [openId, setOpenId] = useState<string | null>(null)
                :72,83     open = ctx?.openId === orderItemId;  點另一列會把前一列關掉
⇒ 展開列【一次只開一項】，這是既有行為，不是片 2 新增的
```
**現況**:採購區塊在訂單層一次列出**全部品項**(`:269` `detail.items.map`)⇒ 一眼掃得完。
**片 2 之後**:同樣的內容一次只看得到**一項**。
⇒ 員工做「比較兩家供應商」「看哪幾項還沒到」時,從**一次掃完**變成**逐項開關**。

🔴 **plan 原句「風險低,動的是 DOM 位置與 props」在【資料面】是對的**(不動 server / 投影 / RPC,
鐵則 12 確實不命中)—— **而它在【使用面】少講了這一件**。
⇒ **這是取捨,不是 bug。Sean 選 A 甲時看到的是那張稿,不是這個代價**
⇒ 遞 plan 時要一起講,讓他有機會說「那採購那塊留在訂單層」。**我不替他決定。**

**F4 ⚠️ 字面:「到貨登錄段」是【每筆採購一個】,不是每個品項一個**

`item-procurement-section.tsx:201` 的 `<ReceiptRecordForm>` 在 `item.procurements.map` **裡面**
⇒ 一個品項對 3 家供應商下單 ⇒ 展開列裡會有 **3 個**到貨登錄入口。
片 2 現在的字面讀起來像一個 ⇒ **改成「每一筆採購各一個」**,否則做的人會照字面畫成一個。

### 9-d-3 ~~片 2 的「改什麼」條列(F1–F4 已收進;這一份是可以直接照做的版本)~~ 🏁 **整節作廢,見下方框**

> 🔴 **2026-08-18 A 窗:標題也要標。** 原標題逐字保留在刪除線裡。
> **為什麼連標題都要動**:內文的作廢框在標題【下面】——
> **而目錄、`grep '可以直接照做'`、以及只掃標題的人,都停在標題那一行。**
> 這與 codex `§687` 那條是同一型:**現役語氣的字面 + 已作廢的內容**。

> # 🏁 2026-08-17 Sean 已拍板:**甲 = 採購留在訂單層,不搬。**
> **他的原字**:`採購留在訂單層 ：甲 · 現在就是這樣`(看完 artifact `1ff3e1de` 當場答的)
>
> 🔴 **第 1、2 條【作廢】** —— 下面**保留原文加刪除線,不刪掉**(刪掉的話,下一個人不知道這裡曾經打算搬)。
> 🔴🔴 **而作廢的不只那兩條 —— 片 2 這一片實質上沒有剩下的工作了**,理由在本節最後一段。
> ⚠️ **本節在拍板之前寫的每一句話,描述的都是一個【已經不會發生的世界】** ——
> 它讀起來完全正常,所以標記必須留在原地,不能只寫在別的地方。

**~~1. 搬(片 2 本體)~~ 🏁 作廢(2026-08-17 Sean 拍甲)** —— ~~`item-procurement-section.tsx` 的**每一輪迴圈內容**搬進對應品項的展開列~~
```
搬的是   :274-304 那個 <div key={item.id}> 裡的東西
         （品名列 + UnsourcedNotice + ProcurementRows + ItemProcurementForm）
容器是   order-detail-items-table.tsx:174 <ItemAmountRow … colSpan={ITEMS_TABLE_COLSPAN}>
不動     server / 投影 / RPC（:269 本來就 detail.items.map）
```

**~~2. 三個【訂單層】的東西留在訂單層(F1)~~ 🏁 作廢(2026-08-17 Sean 拍甲)**
> ⚠️ **作廢的理由是「不搬了所以沒有這個問題」,不是「F1 判錯了」** ——
> **F1 那個分析仍然成立**,而它現在的用途變成:**任何未來想搬這個區塊的人,要先解掉那三樣東西。**
> 🔴 那三樣是:`:251` 區塊標題 / `:253` `itemsTruncated` 警告 / `:255-266` `suppliersFailed` 紅字。
> 逐字理由保留在下面,**不要因為這一片不做了就把它刪掉**。

~~原條列:~~
```
:251      區塊標題「採購(向供應商訂貨)」   ⇒ 搬成品項表的表頭一行，或留一張薄卡
:253      itemsTruncated 截斷警告          ⇒ 🔴 只能留訂單層
:255-266  suppliersFailed 紅字              ⇒ 🔴 只能留訂單層
```
- `suppliersFailed`:code 自己逐字寫著「**不可靜默:選單空掉會讓員工以為這家不存在**」(`:247`)
  ⇒ **搬完沒替它找位置,就是親手把它靜默化。** 那正是它存在的理由。
- `itemsTruncated`:**品項被截掉時,那些品項根本沒有列可以展開**
  ⇒ 逐品項的警告**在定義上**蓋不到它。**這條不可能靠搬家解決** —— 不是「需再評估」,是**做不到**。
- ⚠️ **驗收要有一格負測**:造一個 `suppliersFailed=true` 的世界,**那行紅字必須仍然看得到**。
  (沒有這一格,搬完之後它靜默失效**不會有任何東西紅**。)

**3. 到貨登錄是【每一筆採購各一個】,不是每個品項一個(F4)**
```
item-procurement-section.tsx:201  <ReceiptRecordForm> 在 item.procurements.map 【裡面】
⇒ 一個品項對三家供應商下單 ⇒ 展開列裡會有三個到貨登錄入口
```
⇒ 版面要先想好三個入口怎麼排,不要照字面畫成一個。

**4. 驗收條款分三個世界寫,不要只寫 happy path(F2)**
```
正常       展開列看得到:採購列表 + 表單 + 每筆採購的到貨登錄
清單截斷   表單整份變灰、送出鈕【不渲染】(item-procurement-form.tsx:379)、到貨入口變「—」
該列已作廢 到貨入口變「—」(item-procurement-section.tsx:198)
```
⚠️ 三條都要能 yes/no。只寫第一條的話,後兩個世界在真實畫面上會讓驗收變成假的。

**🔴 3–6 條在拍板之後還成不成立(逐條重新確認,不是假設它們沒事)**

```
3 到貨登錄是「每一筆採購各一個」   🏁 一起作廢
  理由:到貨登錄【住在採購表裡面】(item-procurement-section.tsx:201 在 item.procurements.map 內)
  ⇒ 採購不搬，它【機械上】跟著不搬。這不是我判的，是它掛在哪決定的。
4 驗收分三個世界                  🏁 一起作廢（那三個世界是「搬完之後」的驗收）
  ⚠️ 但那三個世界【本身仍然存在於現況】—— 截斷/停用/作廢照樣會發生。
     ⇒ 它們的正確歸屬是【逐項表 §2.1】，那裡已經寫著，不會因本片作廢而消失。
5 零新增（品項層備註不做）        ✅ 仍成立，與搬不搬無關（那是 Sean 另一條裁定）
6 作廢列的「—」要有一句話說為什麼  🏁 已【真的】掛回 #623，不是只在這裡宣告
  片 2 作廢後它沒有片可以掛（不屬於片 1 列表、也不屬於片 3 建箱）
  ⇒ 主視窗核可掛回 `#623` 的收尾動作（同一個根、同一段畫面，另開會長第二個真相）
  🔴 落檔位置:`docs/phase-1-backlog.md` 的 `#623` 條目內，標題「收尾動作（兩種收尾都要做）」
     含那句「它今天不會痛，所以今天不會有人修它」與「選收尾 2 的話它跟著顯示一起消失」
  ⚠️ 這一行原本寫的是「我判:把它掛回 #623」——【那是宣告，不是落檔】。
     宣告當下 `#623` 那條【一個字都沒被改到】，是我自己回頭發現的。
```

**🔴🔴 片 2 這一片還剩下什麼:沒有了**

```
片 2 原本 = 訂貨段 + 到貨登錄段 搬進展開列
訂貨段   = 採購      ⇒ Sean 拍甲，不搬
到貨登錄 = 住在採購表裡 ⇒ 跟著不搬
⇒ 片 2 的兩件事都不做了 ⇒ 這一片沒有剩下的工作
```
⚠️ **一件要讓 Sean 知道、我不替他決定的**:
他 08-17 選過 **A 甲**(「一個品項的訂貨 / 到貨 / 出貨三件事收在同一張卡」),
而今天這一板**把其中的訂貨與到貨拿掉了** ⇒ **A 甲 只剩下出貨那一段(= 片 3)**。
🔴 **這可能是他的本意(他是看著圖答的,不是被文字誘導),也可能他沒意識到會連帶動到 A 甲。**
⇒ **不要替他推論**;要問就一句話問「A 甲 只剩出貨段,這樣對嗎」,**不要重問採購那題**(他答過了)。

**5. 零新增(不變)** —— 品項層備註 Sean 08-17 裁定不做(「備註留在訂單層就好」)。

**6. 🔴 作廢列的「—」要有一句話說為什麼(逐項表 `#17`;🏁 **落點 = `#623` 的收尾動作**)**

> 🔴 **2026-08-18 A 窗修 codex must-fix `§9-d-3:655 / §687`(原文 `~/pcm-mailbox/V-047-…原文搶救-20260818.md` §1)。**
> **原字面逐字**:~~「主視窗裁定不另立案、**收進片 2**」~~ —— 留痕不刪。
> **病的形狀**:片 2 已整片作廢(Sean 拍甲),而這一條**以現役條目**寫著「收進片 2」,
> 與上方 `§9-d-2` 第 6 條「已【真的】掛回 `#623`」**互撞**。
> 🔴 **codex 判 must-fix 的理由不是「字不一致」,是「老闆會批准到一個不存在的工作」** ——
> 兩句都讀得通,而它們指向**兩個不同的落點**,批准的人不知道自己批的是哪一個。
> ⇒ **現行唯一落點 = `docs/phase-1-backlog.md` 的 `#623` 條目內「收尾動作(兩種收尾都要做)」。**

```
現況  item-procurement-section.tsx:198  作廢列的到貨登錄整格 = 「—」
      同列左邊有「(已作廢)」徽章、下一列有作廢原因
      ⇒ 推得出來，而【沒有一個字在講「所以這裡不能登錄到貨」】
```
⇒ 「—」旁邊(或用 `title`)補一句,例:「這筆採購已作廢,不能再登錄到貨」。
🔴 **為什麼現在就要做,而不是等它痛**:
> **撤銷採購上線之前它極少見**(「已作廢」目前只有工程師手動下 SQL 才生得出來,見 `#623`)——
> **而 `#623` 做完之後它會變成常見狀態。**
⇒ **它今天不會痛,所以今天不會有人修它。** 這句遞 plan 時要在,別被當成 nit 砍掉。
⚠️ 不另開 backlog 條目:它與 `#623` **同一個根**(作廢/撤銷那條線沒做完),另開會長出第二個真相。

**片 1 / 片 3:本次對帳零命中**,而這是**射程造成的、不是查過沒事**:
逐項表只涵蓋訂單面板的五塊,**片 1 動的是訂單【列表】、片 3 動的是出貨建箱流程,兩者都不在表的射程內**
(見該檔 §1)⇒ **這兩片的「保護」型風險目前【未盤點】,不要讀成「已確認沒問題」。**

---

## 9-附錄 🔴 引用 OD 哪一份 —— **判準 + 全樹逐處分層**

> **為什麼在這裡**:`.fchip`(§9 那條「本片不做」)只是這一族的其中一處。
> 這張表把整族攤開,免得下一個人拿到一個總數就去「全部改成 bmw-m」。

### 判準(先讀這條,再看表)

```
overview-desktop.html      = 【定案版】1,983 行，十二輪累積，資訊架構 / 版面 / 文案的權威
overview-desktop-bmw-m.html= 【外觀 reskin】778 行，只 reskin 列表 + 詳情面板的【外觀】
                             檔頭逐字：「資訊架構一個字都沒動…改的只有外觀」

引【版面 / 資訊架構 / 欄寬 / 密度 / 落點 / 文案】→ 定案版  ✅ 正確，不要改
引【顏色 / 形狀 / 字距 / 膠囊 / 選中態】        → 要改引 -bmw-m
```
🔴 **「一個成立的引用」與「引錯版本」長得一模一樣,差別在【它引的是哪一層】。**

🔴🔴 **但這條規則只是【分流的第一刀】,不是判準本身** ——
**實測反例:`.kb` 那顆按鈕,bmw-m 把它從 `26px / radius 6px` 改成 `24px / radius 0`。**
⇒ **bmw-m 確實動了幾何,不只動顏色** ⇒ **每一處仍要逐處開兩份檔比對,規則只用來排序要先看哪幾處。**

### ⚠️ 數字先更正:**36(含本片工作區)/ 31(乾淨樹)** —— 而且**兩個都會過期**

```
git grep -n 'overview-desktop\.html' -- apps packages | grep -v bmw-m | wc -l
  HEAD 43abb127 ＋ 本片未 commit 的工作區  ⇒ 36
  HEAD 43abb127 乾淨樹（git stash 之後實測）⇒ 31
🔴 量測時點 2026-08-17。兩個數字都是對的，差在【工作區乾不乾淨】。
```
🔴 **本片新增 5 處**(實測 `31 → 36`,逐檔比對):
`globals.css` +1、`order-status-axes.ts` +2、`order-detail-items-table.tsx` +2。

> ### 🔴🔴 這一段我自己踩了兩次,兩次都留著
>
> **① 我流通出去的「32」** —— 而乾淨樹實測是 **31**。那個 32 是**更早的某次量測**,
> 我沒有記時點就把它當現況轉述。
> **② 更糟的是**:我第一版寫這段時,句子是「**從乾淨的 43abb127 量會得到 32**」——
> **我沒有量就寫了那個數字,而它就寫在一段【警告不要寫沒量過的數字】的文字裡面。**
> ⇒ 是回頭跑 `git stash` 實測才發現是 31。**寫警告不會讓人免疫於那個警告。**
>
> ⇒ **通則(這才是這段的價值,不是那兩個數字)**:
> **報這類數字一律帶【量測時點 + 當時 HEAD + 工作區乾不乾淨】,三個缺一個就複現不了。**

🔴🔴 **這不是量錯,是【動態分母】** —— **這一輪的動作改變了被量的東西。**
⇒ **通則:報這類數字一律帶【量測時點 + 當時 HEAD + 工作區乾不乾淨】**,三個缺一個就複現不了。
📎 同型坑設計參照檔記過(`git show <commit>:<檔>` 才複現得了舊數);**這次是我踩,而且是我自己造成的。**

### 逐處分層(36 處 = 乾淨樹 31 + 本片新增 5)

| # | 位置 | 引的是哪一層 | 結論 |
|---|---|---|---|
| 1 | `globals.css:523` 欄寬 13 顆 CSS 宣告 | 版面 | ✅ |
| 2 | `globals.css:607` 密度三檔值 | 版面 | ✅ |
| 3 | `globals.css:731` `table-layout:fixed` | 版面 | ✅ |
| 4 | `globals.css:743` nowrap+overflow+ellipsis 三件 | 版面 | ✅ |
| 5 | `globals.css:749` `padding:0 8px` | 版面 | ✅ |
| 6 | `globals.css:987` 窄版收哪些欄 | 版面 | ✅ |
| 7 | `globals.css:1062` 520 斷點 | 版面 | ✅ |
| 8 | `globals.css:1139` 解除桌機截斷 | 版面 | ✅ |
| 9 | `globals.css:1154` 收欄位規則作廢 | 版面 | ✅ |
| 10 | `globals.css:1212` 卡片落點 `k-oid`→`k-st` | 版面 | ✅ |
| 11 | `globals.css:1269` 卡片 ⋯ 36×36 | 版面 | ✅ |
| 12 | `refund-wiring.test.tsx:968` 危險操作沉底 | 資訊架構 | ✅ |
| 13 | `customer-panel.tsx:47` 手機雙標題列病 | 版面 | ✅ |
| 14 | `order-detail.tsx:162` 客人明細入口位置 | 版面 | ✅ |
| 15 | `order-detail.tsx:217` 備註在發票下方 | 版面 | ✅ |
| 16 | `order-detail.tsx:300` 危險操作沉底 | 資訊架構 | ✅ |
| 17 | `orders-table.test.tsx:924` 520 斷點 | 版面 | ✅ |
| 18 | `orders-table.tsx:22` 泛指真權威(rowSpan 收斂) | 版面 | ✅ |
| 19 | `orders-table.tsx:32` `rowspan` 命中 0 | 版面 | ✅ |
| 20 | 🔴 `orders-table.tsx:340` **欄序 / 卡片模式 order** | 版面 | ✅ **見下方專段** |
| 21 | `orders-table.tsx:526` 密度值單一來源 | 版面 | ✅ |
| 22 | `order-list-view.ts:96` 密度值域 | 版面 | ✅ |
| 23 | `order-return-to.ts:54` 返回導航行為 | 行為 | ✅ |
| 24 | `order-status-axes.ts:10` 狀態八值字面 | 文案 | ✅ |
| 25 | `notes-timeline.tsx:26` note_type 三類分色 | 外觀 | ✅ **bmw-m 沒有這東西**(`nt-internal\|nt-contact\|nt-notified` ⇒ 0 命中)⇒ 定案版是唯一來源 |
| 26 | `order-density-toggle.tsx:13` 密度鈕形狀 | 外觀 | ✅ **bmw-m 沒有密度鈕**(`denbtn\|data-den\|密度` ⇒ 0)⇒ 同上 |
| 27 | `orders-table.test.tsx:1709` `⋯` = U+22EF | 字元 | ✅ bmw-m 也是同一顆(`⋯` ⇒ 1 命中) |
| 28 | `order-detail-items-table.tsx:53` `.pcstep` | 版面 | ✅ 本片新增,已明寫「定案版」;bmw-m 無 `.pcstep` |
| 29 | `order-detail-items-table.tsx:67` `.pcard` 商品卡 | 資訊架構 | ✅ 本片新增 |
| 30 | `globals.css:443` | 說明 | 📝 本片新增的**對照說明**,不是引它當權威 |
| 31 | `order-status-axes.ts:97` | 說明 | 📝 同上 |
| 32 | `order-status-axes.ts:103` | 說明 | 📝 同上(判準本身) |
| 33 | 🔴 `globals.css:854` `.fchip` 常態 + 選中 | **外觀** | ❌ **引錯版本**(bmw-m `.fchip` ⇒ **0 命中**、改成 `.tab` ⇒ 7)。**本片不做**(§9) |
| 34 | 🔴 `order-filter-chips.tsx:7` `.bar .fchip` | **外觀** | ❌ **引錯版本**,同 33。**本片不做** |
| 35 | 🔴 `globals.css:953` `.kb` 10 個宣告 | **外觀 + 幾何** | ❌ **引錯版本,本次新發現**(見下) |
| 36 | ⚠️ `order-toolbar.tsx:29` `<h2>訂單</h2>` 後緊接四顆 `.fchip` | **判不出來** | ⚠️ 見下 |

### 🔴 #35 `.kb` —— 本次新發現的第三處引錯

```
舊版 :214-215   border-radius: 6px   width/height: 26px
BMW M :187-189  border-radius: 0     width/height: 24px      ← 兩顆都不同
repo            width/height: 26px（舊版值）
                border-radius 原本寫死 6px ⇒ 已被 R3 抓到、改吃 --radius（現為 0）
```
⇒ **圓角碰巧對了**(靠「禁裸 radius」那條規矩修的,**不是照 bmw-m 修的**);**尺寸仍是舊版的 26px。**
🔴 **本片不做**,兩個理由:
① `Q-視覺` 那批他答的是列表與膠囊,**沒有問過這顆按鈕**;
② 那顆按鈕**有熱區實測紀錄**(`#486` 乙案的 hit test)⇒ 26→24 要重驗熱區,不是換個數字。

### ⚠️ #36 為什麼判不出來(不硬歸類)

`order-toolbar.tsx:29` 引的是「**標題之後緊接四顆 chip**」這個**排列順序** ⇒ 看起來是版面 ✅。
**但 bmw-m 把那一列的元件整個換掉了**(`.fchip` 膠囊 → `.tab` 底線頁籤),
⇒ **「順序」在 bmw-m 仍成立,「那四顆是什麼」不成立。**
⇒ **同一個引用有一半對、一半錯,而我判不出該算哪一邊。**
**缺的那道檢查**:`.fchip → .tab` 要不要做本身還沒拍板(§9 列為本片不做)⇒ **那題答了,這格才判得出來。**

### 統計(可重跑)

```
36 處 ── ✅ 正確 29 ／ 📝 說明性 3 ／ ❌ 引錯版本 3 ／ ⚠️ 判不出來 1     （29+3+3+1 = 36 ✅）
🔴 2026-08-18 更正(收 codex nit「§9 附錄:815」):原寫 ✅ 30 ⇒ 加總 37 ≠ 表列 36。
   數法(可重跑,直接數表格資料列):
     sed -n '/### 逐處分層(36 處/,/### 🔴 #35/p' <本檔> | grep -E '^\| *#?[0-9]+' \
       | awk '{if(/✅/)a++;else if(/📝/)b++;else if(/❌/)c++;else if(/⚠️/)d++} END{print a,b,c,d}'
     ⇒ 29 3 3 1
   ⚠️ 這一格的教訓:**分類數是我一列一列看著打的,而總數是我另外寫的** —— 兩個數各自都「看起來對」。
❌ 三處全部【本片不做】：.fchip ×2（等拍板）、.kb ×1（要重驗熱區）
🔴 本片修掉的那一處（PAY_MARK 未收款紅框）不在上表 —— 它已經改成引 -bmw-m 了。
```

---

## 10. 相關既有紀錄與連動面

- `docs/specs/2026-08-12-admin-order-ui-design-brief.md` §0-D 那張「OD 內部字面不可盡信」表 ⇒ **本片發現的「抄錯版本」是同一個模式的第 7 例,收尾時要加進那張表,不另開一節**(該節明文要求)
- `docs/design/admin-design-system.md` 檔頭落地狀態表 ⇒ 本片完成後要更新「動效 token 消費端 0」以外的兩列
- memory `feedback_claimed-sync-but-only-patched-touched-lines` ⇒ 改註解字面時走 literal-sweep
- memory `project_0817-od-impeccable-polish-for-admin-visuals` ⇒ 🔴 **附一行指標:`impeccable-design-polish` 會【就地改寫】OD 專案的入口檔。要打磨先 `create_project` 複製一份,不要在被 repo 用 hash 釘住的專案裡跑**(2026-08-17 A 窗實際踩過並還原;全文由主視窗落 memory)
