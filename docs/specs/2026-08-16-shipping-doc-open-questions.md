# 出貨單這條線的未決題(等 Sean)· C 窗 2026-08-16

> 🔴 **為什麼要有這個檔**:這四題原本只活在 `~/pcm-mailbox/C-212-STOP.md` 與對話裡。
> I 窗 2026-08-16 實查:`Q-C8` 全樹**零命中**、`Q-C5/6/7` 只在 `STATUS.md`(而那是 I 窗自己寫的)。
> **窗會關,信不會被下一個人讀,檔會。** —— 這正是 `Q-A7` 變成無頭公案的那個形狀。
> ⚠️ 有答案之後**回到這裡劃掉並寫進對應的 plan**,不要只在訊息裡回。

**相關 plan**:`2026-08-16-shipping-doc-carrier-tracking-plan.md`(已批,`Q-C5/6/7` 影響它)
· `~/pcm-mailbox/D-027-PLAN-片2b-2-對外出貨單金額.md`(未批,`Q-C8` 影響它;🔴 那份**只在信箱**)

---

## ✅ `Q-C5` · 已答 = **丙**(Sean 2026-08-17 睡前,逐字 `q3: 丙`)

> 🔴🔴 **他給的是【第四個答案】,不在下面 A/B/C 三選一裡:**
> **出貨明細單不印追蹤碼欄位,追蹤碼只走LINE／Email 給客人。**
> ⇒ 不是「紙上那句改成指向管道」(B),是**那一列整個不存在**。
> `Q-C19`=乙(2026-08-17 早,逐字「**乙 只拿掉追蹤碼那列**」)⇒ 同一區「貨運商」與「日期」留著。
>
> ⚠️ **`STATUS.md` 曾記成「A: 乙 ✅ 已答」,那是 2026-08-16 的狀態,已被丙取代。**
> 看到「Q-C5=乙」一律當**過期來源**。
> ✅ **`STATUS.md` 已由 I 窗於 2026-08-17 改掉(`5a307246`)** —— 本節原本寫「本窗不改它,已另行通報」,
> 那是**通報當下**的狀態;通報已經被兌現了。
> 🔴 **而 I 窗改的方式值得抄**:它**沒有**只把「乙」換成「丙」—— 下方選項行上的「丙」指的是
> **「印兩張」**,只換一個字會讓下一個人把丙讀成印兩張。它寫的是「**丙是第四個答案,不在三個選項裡**」,
> **選項行刻意留著** —— 拿掉它反而看不出「答案落在選項之外」這件事。
>
> **已執行**:`docs/specs/2026-08-17-qc5-tracking-off-paper-decommission-list.md`(清單 + §5b 執行紀錄)。
> **框的差別值得記**:我們的框是「怎麼讓紙印對」,他的框是「**這張紙該做什麼**」。
>
> 🔴 **丙沒有說「不用追蹤碼」** —— 寫入守門(非 `other` 空白 ⇒ 拒絕標記出貨)、
> `Q-C551` 格式守門、`trackingDisplay` 的三種 `null` 語意**全部仍然成立**。

**以下為原題原文,零刪除(下面 A/B/C 三案皆未獲採納):**

**兩條各自都是 Sean 拍的,而沒有人檢查過它們會撞在一起:**

```
① 設計需求書（2026-08-16-print-docs-design-brief.md，錨點「員工先印單」）逐字：
   「Sean 拍甲：員工先印單、單子跟著貨走」
② apps/admin/src/lib/shipping/shipment-actions.ts（錨點 `if (input.markShipped)`）：
   單號【只在標記出貨時】才寫進 DB
```
⇒ 先印單 ⇒ 印的當下 `shippedAt = null`、無單號 ⇒ 紙上印「尚未出貨,出貨後補」,
**而紙已經在箱子裡了,沒有任何東西補得上去。**
而同一份需求書把追蹤碼的理由寫成**「客人查貨的唯一依據」**(錨點「唯一依據」)。

```
A：改工序 —— 先標出貨（填單號）再印單。紙上一定有追蹤碼，
   代價是員工多一步，且「單子跟著貨走」那句要改
B：維持先印單 —— 接受客人手上那張紙沒有追蹤碼，追蹤碼改用別的管道給
   （出貨通知信 / 訂單頁），紙上那句「出貨後補」改成指向那個管道
C：兩張紙 —— 揀貨時印一張、標出貨後補印一張正式的給客人
```
**C 窗推薦 = B。** A 動的是員工每天的動作,而 M-4a 訂單通知信線本來就要做;
C 讓一次出貨變成兩次列印,而北極星是「員工不用人教能做對」。
⚠️ **這題我沒把握 —— 我沒看過倉庫實際怎麼跑。**

---

## 🔴🔴 `Q-C8` · 出貨單「取消/退款」那一行,錢從哪裡來?

**照 `Q-D-11 = 甲`(付款金額用權威值、取消/退款用減的湊)實作,那一行會印出【折扣】,不是取消。**

我自己開檔驗過的三條(不是轉述 `D-027`):
```
① 兩支取消 RPC 都【只】動 cancelled_at / cancelled_reason / updated_at
   20260805100000_…a8a2_partial_cancel.sql（錨點 SET cancelled_at = pg_catalog.now()）
   20260804180000_…a8a1_admin_cancel_order.sql
   數法：grep -rc "UPDATE public.orders" 兩檔 ⇒ 各 1 處，逐處開檔看過
② grep -rn "SET total|total =" supabase/migrations/*cancel*.sql
   ⇒ 唯一命中是 a8c2_confirm_cancel_guard.sql 的 v_order.total = p_amount ——【讀取比對】不是寫入
③ 不變式：packages/domain/src/order/types.ts（錨點 `total = subtotal + shippingFee`）
```
⇒ `小記 + 運費 − 付款金額` = `subtotal + shippingFee − total` **代進不變式 ⇒ 恆等於 `discountTotal`**。
而 `discountTotal` 的 docstring 逐字「**Phase 1 多為 0**」
⇒ **客人退了 3000 塊,那一行會印 0。**

```
A：不印那一行 —— 取消只用「訂單取消」那一區的【件數】表達，紙上不出現取消金額
B：從 order_cancellation_items 自己算 —— 但那是【紙上新增一種金額算法】，
   而 Q-D-7 只放行過「unitPrice × 本區數量」
C：取消時讓 RPC 真的改 total —— 🔴 動的是金流寫入鏈，跟本片完全不同量級
```
**C 窗推薦 = A**(紙上零新增金額算法、也不動寫入鏈;客人要金額對帳走退款通知/訂單頁)。
⚠️ **同時命中鐵則 12 ①錢 + ⑤對外不可回收 ⇒ 這題我不自己拍。**

---

## ✅ `Q-C6` · 已答 —— **Sean 給了第三個答案,不在我的選項裡**

原題:箱還沒標出貨時,同一張紙並排出現「出貨日:2026-08-16」與「追蹤碼:尚未出貨,出貨後補」。
我給 A(維持)/ B(改叫「列印日」)。

> **Sean 2026-08-16 逐字:「改成: 日期 這兩個字就好」**

⇒ **那格叫「日期」,兩個字。不是「出貨日」也不是「列印日」。不要自作聰明加字。**
📎 它正好解掉矛盾:**「日期」不宣稱任何事**,所以與同一區的任何狀態都不打架。
🔴 **落地** `14c3a1ac` 之後那批;守門在 `page.test.tsx`(錨點 `日期:`)。
⚠️ **教訓**:我給的兩個選項都預設「這格必須宣稱它是哪一種日期」,而**他的答案是不要宣稱**。
   下次寫決策題,選項不要都建在同一個沒說出口的前提上。

## ✅ `Q-C551` · 已答 —— **他推翻的是規則本身,不只是嚴厲度**

原題:貨號守門「長度必須 10 碼就擋」上線後可能鎖住員工出貨,要 A 不動 / B 降成警告 / C revert。

> **Sean 2026-08-16 逐字:「乙」+「我們每一家貨運都不一樣,不限制多少數字或者號碼,最長 39位數」**

⇒ 不只是降級:**「長度=10」與「必須純數字」兩條【整條刪除】**,改成
**只驗上限 39、且只警告**;`hct` 的檢查碼維持只警告。**本 lib 現在不擋任何東西。**
⚠️ **`39` 的來源 = Sean 口述,未見書面來源** —— 三來源律的①,不是②官方文件。引用時要帶這個限定。
🔴 **落地**:`lib/shipping/tracking-number.ts`(錨點「上限 39」);守門含 39/40 正負對照
與一格「4 貨運商 × 13 輸入,沒有一組回 block」的全稱斷言。

---

## ⛔ `Q-C9b` · 已答,而 **2026-08-17 被 `Q-C5`=丙 整條吃掉(紙上已無那一格)**

> **`Q-C9b` 問的是「追蹤碼那格在還沒出貨時寫什麼」,而丙之後【那一格不存在】** ⇒ 本節在**紙**這一面
> 已無效:留空、fail-loud「請立即回報」、交叉守門那一段,實作與測試都已隨那一列拿掉。
> ⚠️ **它的【內容】沒有作廢,只是換了載體**:「還沒出貨 / 該有卻沒有 / 這家本來就不給碼」
> 三種要分開講的判斷**搬去 `Q-C9`**(客人訂單頁 + 出貨通知信),
> 實作 `trackingDisplay` 與它的測試**一格斷言都沒改**
> (數法 `git diff --cached -U0 -- <該測試檔> | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vcE '^\+//'` ⇒ **`0`**
> = 新增行全是 `//` 註解。⚠️ **不要寫成「檔案零 diff」** —— 該檔實際 `+7` 行檔頭語境註解)。
> 🔴 **下面那句「Sean 逐字:乙｜什麼都不寫,空格」是真的、不要當它沒說過** —— 它答的是
> 「**沒有值的時候不要填一個看起來比較完整的字**」,那個原則到了信裡照樣適用。
>
> **原文以下零刪除。**

`Q-C5`=乙(維持先印單、追蹤碼改走別的管道)之後,我實查發現**那些管道一個都不存在**
(通知信暫緩 / 沒有會員訂單頁 / storefront 零 `trackingNumber`)⇒ 追問「在那之前紙上寫什麼」。

> **Sean 2026-08-16 逐字:「乙｜什麼都不寫 ,空格」**

⇒ 追蹤碼那格在**還沒出貨**時**整列不印**(連「追蹤碼:」四個字都不印)。
**空格就是空格,不要填一個看起來比較完整的字。**

🔴🔴 **而它與 fail-loud 那條【必須】是兩條分支,雖然它們長得一模一樣**:
```
還沒出貨、沒有追蹤碼      ⇒ 正常          ⇒ 紙上【空白】
已出貨、非 other、沒有    ⇒ DB 說不可能   ⇒ 紙上【印「請立即回報」】
```
**合併成「都留空」會把一個資料鏈的洞變成看不見的。** 兩條各有守門,
另有一格交叉守門(`page.test.tsx` 斷言留空那格連「追蹤碼」三個字都不出現,
而 fail-loud 的文案正好以「追蹤碼缺漏」開頭 ⇒ 合併時它會紅)。

📌 ~~**`Q-C5` 乙案的完整版仍未做**~~ ⇒ ⛔ **這句 2026-08-17 起指向一個已作廢的案**
(I 窗 `5a307246` 收割時抓到):**乙已被丙取代**,「乙案的完整版」不再是任何人的待辦。
🔴 **而它講的那【兩件事】沒有作廢,反而變成【唯一】的路** —— 丙拿掉了紙這個備援:
「客人網站訂單頁顯示出貨管道+追蹤碼」+「寄信給客人含出貨通知與出貨單電子檔」,
**兩件現在都不存在,是兩條線的工作量**,而它們現在**掛在 `Q-C9` 底下**。
⚠️ **原句的份量要往上調不是往下調**:乙之下那兩件沒做,客人至少還有紙;
**丙之下那兩件沒做,客人手上一個追蹤碼都沒有。**
(原文以刪除線保留,零刪除。`Q-C9b` 那句「只是過渡」已在本節檔頭框說明。)

---

## ✅ `Q-C7` · 已答 = **丙**(頁尾手寫日期格拿掉)· **已落地、有守門**

> 🔴 **本節的 ✅ 是 2026-08-17 補在【標題】上的,而內文早就寫著已答。**
> **為什麼要專程補**:同一份檔裡 `Q-C6` 的標題是「`## ✅ Q-C6 · 已答`」,而本節標題原本是
> 「`## Q-C7 · 頁尾手寫的「日期」與表頭印死的出貨日會打架(較輕)`」——
> **掃標題的人看到的是一題未答**,要往下讀六行才會看到 `:165` 那個 ✅。
> ⇒ **2026-08-17 主視窗真的照這個誤讀派了工**(要我做 `Q-C6`/`Q-C7` 的實體樣張去問 Sean),
> 而**那會是拿他已經拍過的板再問一次**。
> 📎 教訓的形狀:**「答案寫在檔裡」與「答案找得到」是兩件事** ——
> 而這裡的落差只有一個 ✅ 的位置。

員工實際交寄跨日時兩個不一致,客人不知道信哪一個。
```
A：維持（兩個日期各有用途）    B：頁尾那格改名「實際交寄日」    C：頁尾那格拿掉
```
✅ **已答:Sean 逐字「丙,拿掉頁尾手寫日期」。** 落地 `14c3a1ac`,守門在 `page.test.tsx`
(錨點 `不得】有手寫日期格`);突變:把那行加回去 ⇒ 紅 1 格。
⚠️ ~~`Q-C6` / `Q-C7` 都是紙面文案 = Sean 的品味範圍,**C 窗不自己改**。~~
🔴 **這一行 2026-08-17 起讀起來像「還在等他」,而它講的是【當時】的分工** ——
兩題**都已經由 Sean 親自答完並落地**:`Q-C6` 逐字「改成: 日期 這兩個字就好」、`Q-C7` 逐字「丙」。
⇒ **不要再為這兩題做樣張、也不要再問他。** 現況數法(可重跑):
`git grep -c 'Q-C6\|Q-C7' -- apps/admin/src` ⇒ `page.test.tsx:2` + `shipping-doc.tsx:5` = **7 行**
(`page.test.tsx:712` 是 `Q-C7 = 丙` 的守門 describe)⇒ **兩題都在 code 裡,不只在文件裡。**
⚠️ **這個數字我第一次寫成「5 行」,是憑印象寫的** —— 當場跑 `| wc -l` 得 **7**,已更正。
📎 **在一段【專講「不要憑舊印象再問一次」的文字】裡憑印象寫了一個數字** ——
留著這行,因為它比那段話本身更有說服力。
