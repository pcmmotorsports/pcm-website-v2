# ⟦部分取消補寄信⟧ `bank_order_amount_changed` 文案 —— **🔴 草稿, 尚未核可**

> 🛑 **這一份還【不是】canonical。** Sean 2026-09-13 答 §3-bis-9 **A3 甲 = 文案要他先看過再上**
> ⇒ 📌 **他點頭之前, 碼不得接上寄信那一步。**
> 核可之後要做兩件事:① 把本檔標題的「草稿」改成「Sean 核可的那一份」並附他的逐字答覆與時間
> ② 讓 `sweep-email-outbox.test.ts` 加一發**讀本檔、把佔位詞換掉、與函式輸出整串比對**的測試
> (形狀照 `docs/specs/2026-09-06-bank-order-created-email-copy.md` 那一份的既有做法 ——
> 📌 那不是「測我寫對了」, 是把【他核可的字】與【寄出去的字】綁在一起)。
>
> **為什麼要有這封信**:`docs/plans/2026-09-12-partcancel-storefront-balance-plan.md` §3-bis-0 ——
> 甲 已經把顧客頁那個偏高的數字拿掉了, 而客人因此**手上沒有任何正確金額**
> (匯款成立信只在建單時寄一次, 他那封是取消前的全額)⇒ 這封就是取消之後補一封新金額。
>
> 🔴 **形狀刻意貼著 `2026-09-06-bank-order-created-email-copy.md`** —— 只改該改的:
> 開頭兩句、多一句「已經照舊金額匯了怎麼辦」。**三行金額、匯款資訊、期限、連結、LINE 一字不動。**
> ⇒ 📌 理由:客人已經看過那封信一次, 兩封長得像**是對的**, 不是偷懶。
>
> 🔴 **標點沿用那一份的既有裁示**:三行金額與正文是**半形逗號**;
> 最後 LINE 那一行是**全形**(常數 `ORDER_CONTACT_LEAD`, 主視窗 2026-09-06 03:1x 裁「四封信同一句是規則」)。
>
> 🛑 **括號裡的是佔位詞, 不是字面。** 佔位詞以外每一個字元(含標點、空格、換行)就是要寄出去的字面。

## 主旨

```
訂單 (訂單編號) 的應付金額已變更,請依新金額匯款
```

## 內文

```
您好,

您的訂單 (訂單編號) 已取消部分商品,應付金額因此變更。
請依下列新的金額完成轉帳,我們收到款項後會再通知您。

訂單金額  NT$ (訂單金額)
已收      NT$ (已收金額)
應付餘額  NT$ (還要匯多少)

匯款資訊
銀行      (公司銀行)((分行))
戶名      (公司戶名)
帳號      (公司銀行帳號)
匯款備註請填寫訂單編號 (訂單編號)

請於 (到期日)(含)之前完成匯款,逾期訂單將自動取消。

如果您已經依先前的金額完成匯款,請與我們聯絡,我們會協助您處理差額。

訂單內容與匯款資訊也可以在這裡查看:
(他的訂單頁連結)

有任何問題，加入官方 LINE (LINE ID)
(LINE 連結)
```

---

## 佔位詞對到哪個欄位(**實際開檔核過, 不是憑記憶**)

| 佔位詞 | 來源 | 核處 |
|---|---|---|
| (訂單編號) | `payload.display_id` | `sweep-email-outbox.ts:675-676` |
| (訂單金額) | `payload.total` ← **`effective_total`** | plan §3-bis-3 · view `20260907210000:60-100` |
| (還要匯多少) | `payload.balance_due` ← **`effective_balance_due`** | 同上 |
| (已收金額) | **算出來的**:`total - balance_due` | `sweep-email-outbox.ts:680` 同一條算式 |
| (到期日) | `remittanceDeadlineSentence(payload.created_at)` | `sweep-email-outbox.ts:704` |
| (公司銀行) / (分行) / (公司戶名) / (公司銀行帳號) | 常數 `PCM_REMITTANCE_*` | `sweep-email-outbox.ts:698-701` |
| (LINE ID) / (LINE 連結) | 常數 `PCM_LINE_ID` / `PCM_LINE_URL` | `sweep-email-outbox.ts:713` |

🔵 **期限那一句用的是【訂單的 created_at】, 不是取消的時間** ⇒ 📌 **期限不會因為取消而延後**,
與那封成立信講的是同一天。⚠️ 而那是**沿用既有行為**, 不是我為本片決定的 —— 若 Sean 要「取消後重新給期限」
那是另一件事(要動 `payment_due_at`), 本片不含。

---

## 🔴 這份草稿【沒有解掉】的一件事 —— 要 Sean 自己答

```
金額可能【比他下單時看到的高】, 而這封信沒有一個字解釋為什麼。

成因(plan §2 逐字):effective_total 不只是「扣掉取消的件」, 它還會
  ① 券整張作廢、算回原價(Sean Q39 逐字「那優惠卷就不能使用, 這樣最簡單, 算回原價」)
  ② 運費照規則重算(Sean 09-05 拍乙:跌破 5000 門檻要補收 100)
⇒ 一張用過券、取消之後跌破門檻的單, 這封信的數字會比他原本要付的【多】。

🛑 Sean 2026-09-13 已答 A2 甲 =「照寄」⇒ 寄不寄已經定了, 本格不重問。
🔴 **而他沒有被問過的是:要不要在信裡【講一句為什麼】。**
   不講 ⇒ 客人看到取消了東西反而要多付, 他會打電話來, 而客服也要現場解釋券的規則。
   講   ⇒ 信裡多一句規則說明, 而那句話等於把「取消就失去優惠」寫給客人看。
```

---

## 附:對照那封已核可的成立信,我改了哪幾行(逐字 diff)

| | 成立信(已核可) | 本草稿 |
|---|---|---|
| 主旨 | `訂單 (訂單編號) 已成立,請於期限內完成匯款` | `訂單 (訂單編號) 的應付金額已變更,請依新金額匯款` |
| 第 1 句 | `您的訂單 (訂單編號) 已成立,目前尚未付款。` | `您的訂單 (訂單編號) 已取消部分商品,應付金額因此變更。` |
| 第 2 句 | `請依下列資訊完成轉帳,我們收到款項後會再通知您。` | `請依下列新的金額完成轉帳,我們收到款項後會再通知您。` |
| 期限句之後 | (無) | **新增一行** `如果您已經依先前的金額完成匯款,請與我們聯絡,我們會協助您處理差額。` |
| 其餘每一行 | — | **一字不動** |

🔵 **那個新增的一行為什麼要有**:這封信創造出一個既有信沒有的情境 ——
客人**可能昨天就照舊金額把錢匯出去了**(那正是本片要防的傷害, 而它對已經匯的人已經發生)。
⇒ 📌 沒有這一行, 那個人讀完信不知道該做什麼。
