# ⟦部分取消補寄信⟧ `bank_order_amount_changed` 文案 —— **✅ Sean 核可的那一份**(canonical)

> 🔴🔴 **本檔的主旨與內文不是我手打的** —— 我用程式從端給 Sean 的那支檔抽出來貼過來。
> 來源:`~/pcm-mailbox/0913-Sean文案-部分取消補寄信.md`(他親手給的優化版)。
> 🔬 **而「方法」不可查(抽的腳本沒留在 repo), 所以要看的是【結果】**:
>   兩檔的 `## 主旨` 與 `## 內文` code block **bytes 相等**
>   —— 我抽完比對過, 而 Fable 5.1 2026-09-13 用獨立的腳本複驗過一次(兩邊都是 `==` 比對, 含換行)。
> **兩份必須逐字相同** —— 本檔是正本(測試讀它), 那一份是端題的存檔。
> 🟢 **2026-09-13:那一半也驗完了 —— 從【未證】改成【已驗】。**
>   ⛔ ~~本行原本寫「Sean 貼的原文不在磁碟 ⇒ 沒有人證得到『只改了標點』」~~
>   ✅ 主視窗把他在對話裡逐字貼的原文落到磁碟, 與本檔內文做字元級比對;**我拿到原文之後自己重跑了一次**。
>   詳見下面〈他貼的原文〉那一節(**原文已收進本檔**, 所以下一個人不必找任何人就能重驗)。
>
> **Sean 2026-09-13 的三次答覆, 逐字**:
> ① 看完 B 窗第二版草稿 ⇒ 「**乙 = 要改**」, 並直接貼出整份優化版
> ② 主旨二選一 ⇒ **A 版**(先點出「部分商品已取消」, 免得客人以為整筆訂單被重算)
> ③ 標點 ⇒ **甲 = A 版半形, 與既有四封一致**
>
> 🛑 **括號裡的是佔位詞, 不是字面** —— 碼裡由變數填。而**佔位詞以外的每一個字元**
> (含標點、空格、換行)**就是要寄出去的字面**。
> 🔴 **而「被鎖住」今天只涵蓋【主旨】**(Fable 2026-09-13 F2 抓到我這句寫過寬了):
>   · **主旨** ⇒ `order-email-assembly.test.ts` 讀本檔、換掉佔位詞、整串比對 ✅
>   · **內文** ⇒ 🛑 **今天零鎖、零模板** —— sweep 對本型別仍是 fail-closed throw。
>     ⇒ 📌 **內文的那一道鎖要在【接線片】隨模板一起補**(形狀照 09-06 那封:
>       它的內文是由 `sweep-email-outbox.test.ts` 鎖的, 不是由 assembly 那支)。
>   ⚠️ **寫模板的人不要以為內文已經有鎖** —— 手打漏一個字, 三綠會照過。

## 主旨

```
訂單 (訂單編號) 部分商品已取消,應付金額更新通知
```

## 內文

```
您好,

您的訂單 (訂單編號) 已完成部分商品取消,應付金額已同步更新。
請依下列最新金額完成轉帳,我們確認款項後將儘速為您處理。

訂單金額  NT$ (訂單金額)
已收      NT$ (已收金額)
應付餘額  NT$ (還要匯多少)

匯款資訊
銀行      (公司銀行)((分行))
戶名      (公司戶名)
帳號      (公司銀行帳號)
匯款備註請填寫訂單編號 (訂單編號)

請於 (到期日)(含)之前完成匯款,逾期訂單將自動取消。

※ 特別提醒:
部分商品取消後,原訂單套用之優惠折扣可能隨之失效,運費亦將依調整後的金額重新計算;因此最終應付金額可能與您下單時有所不同(或略有增加),敬請見諒。

若您先前已完成舊金額之匯款,請直接透過 LINE 與我們聯繫,我們將主動協助您辦理差額處理。

完整訂單明細與匯款資訊亦可點擊此處查閱:
(他的訂單頁連結)

有任何問題，加入官方 LINE (LINE ID)
(LINE 連結)
```

---

## 🔴 標點:**正文半形 + 最後那一行全形**, 而那不是不一致

```
· 正文與三行金額 ⇒ **半形**(,;():)—— Sean 2026-09-13 答甲。
· 最後 `有任何問題，加入官方 LINE (LINE ID)` 那一行 ⇒ **全形逗號**,
  因為它是常數 `ORDER_CONTACT_LEAD`, 主視窗 2026-09-06 03:1x 裁「四封信同一句是規則」。
⇒ 📌 **所以本檔的標點不是特例。**
⚠️ ⛔ ~~我原本寫「與既有四封**逐字同款**」~~ —— **那句過寬**(Fable 2026-09-13 F5):
   「逐字同款」只對**匯款成立信**與那句共用的 LINE 尾成立;
   而 `sweep-email-outbox.ts:905` 那封的 LINE 句**不同**(無 lead)、
   `orderShippedSubject` 的主旨用**全形括號**。
   ✅ 準確的說法:**與匯款成立信同款, 而 LINE 那一句是共用常數。**

🔬 **他原本親手打的是【全篇全形】, 而那被改了 —— 留痕**:
   ⚠️ 與 2026-09-06 那一次**形狀相同而性質不同**:
     · 那一次是我抄稿時打成半形的**手誤** ⇒ 主視窗代裁改回全形(**沒有問他**)
     · 這一次是他自己打的全形 ⇒ 🛑 **問過他本人才改**(他答甲), 不是我們自己裁
   ⇒ 📌 判準都是「那個字元是誰選的」, 而答案來源不同, 兩次都要記著。
```

---

## 🔬 他貼的原文(全形, 未改)—— **與本檔內文的差異已逐處分類**

> 🔴 **這一節存在的唯一理由是【可重驗】。** 主視窗把原文落在 scratchpad,
> 而 **scratchpad 會消失** ⇒ 收進本檔, 下一個人不必找任何人。

```
您好，

您的訂單 (訂單編號) 已完成部分商品取消，應付金額已同步更新。
請依下列最新金額完成轉帳，我們確認款項後將儘速為您處理。

訂單金額  NT$ (訂單金額)
已收      NT$ (已收金額)
應付餘額  NT$ (還要匯多少)

匯款資訊
銀行      (公司銀行)((分行))
戶名      (公司戶名)
帳號      (公司銀行帳號)
匯款備註請填寫訂單編號 (訂單編號)

請於 (到期日)(含)之前完成匯款，逾期訂單將自動取消。

※ 特別提醒：
部分商品取消後，原訂單套用之優惠折扣可能隨之失效，運費亦將依調整後的金額重新計算；因此最終應付金額可能與您下單時有所不同（或略有增加），敬請見諒。

若您先前已完成舊金額之匯款，請直接透過 LINE 與我們聯繫，我們將主動協助您辦理差額處理。

完整訂單明細與匯款資訊亦可點擊此處查閱：
(他的訂單頁連結)

有任何問題，加入官方 LINE (LINE ID)
(LINE 連結)
```

**驗法(誰、什麼時候、怎麼驗)**
```
① 主視窗 pcm-website-v2-4f 2026-09-13 把 Sean 在對話裡逐字貼的原文落到磁碟
② 與本檔〈## 內文〉那個 code block 做**字元級**比對(逐行 zip、逐字元比)
③ B 窗 pcm-website-v2-29 **拿到原文之後自己重跑了一次**(不照抄主視窗的結論)
```

**結果(B 窗實跑的數字)**
| 量 | 值 |
|---|---|
| 行數 | 原文 **27** / 本檔 **27** |
| 有差異的行 | **8 行** |
| 字元級差異 | **14 處** |
| 分類 | 全形逗號→半形 **×9** · 全形冒號→半形 **×2** · 全形分號→半形 **×1** · 全形括號→半形 **×2** |
| 🟢 **非標點的差異** | **0 處**(零個字被更動、零行被增刪) |

🔬 **而這裡有一格數字對不上, 照實寫**:主視窗回報的是「差異 **13** 處,逗號 **×8**」,
而我自己重跑得到 **14 處 / 逗號 ×9**。逐行列出來之後看得到:**第 19 行那一句有三個逗號**
(「取消後,」「失效,」「不同(或略有增加),」)而不是兩個。
⇒ 📌 **結論不變**(兩邊都是「全部是標點、零個字被改」)**, 而數字我用自己數的那一個。**
⇒ 🛑 兩個人各數一次而差一個 —— 那正是為什麼原文要收進來:**下一個人可以自己數。**

🔵 **LINE 那一行不在差異清單裡** ⇒ 它的全形逗號**確實保留了**(`ORDER_CONTACT_LEAD`)。
   那不是「我們忘了改」,是刻意的,而這份逐行 diff 就是它的證人。

⚠️ **一件 Sean 還沒答的 —— 不要因為它看起來合理就當已定**:
```
他答的 Q3 只講【逗號】, 而我們把**括號**也一併半形化了(第 19 行那兩處)。
🔵 依據是:其他四封信的括號本來就是半形 ⇒ 比照。
🛑 **而那是【比照】, 不是他逐字裁示的。** 主視窗 2026-09-13 已端給他, 他還沒回。
⇒ 他若說不對, 那兩處要回全形 —— 而那會讓本節的分類從「括號 ×2」變成 0。
```

⚠️ **這份驗證的承重點**:原文的來源是**主視窗手上那份對話上下文**, 不是一份原本就在磁碟上的檔。
   ⇒ 📌 它證的是「**主視窗轉述的那份原文**與本檔只差標點」——
     若那份轉述本身有偏差, 這一節驗不到。(而原文現在收在上面, 之後可以拿真的對話再對一次。)

---

## ⛔ 被取代的第二版草稿(留痕, 不要照它實作)

```
主旨   訂單 (訂單編號) 的應付金額已變更,請依新金額匯款
第1句  您的訂單 (訂單編號) 已取消部分商品,應付金額因此變更。
第2句  請依下列新的金額完成轉帳,我們收到款項後會再通知您。
那兩段 取消部分商品後,原本使用的折扣可能不再適用,運費也可能隨金額變動,
       因此新的應付金額有可能比您下單時看到的金額高。
       如果您已經依先前的金額完成匯款,請與我們聯絡,我們會協助您處理差額。
```
**他改了什麼(對照用)**
1. **主旨**點出「部分商品已取消」, 不只說金額變更。
2. 開頭兩句語氣改柔(「已完成部分商品取消」/「我們確認款項後將儘速為您處理」)。
3. 「為什麼金額可能變高」獨立成 **`※ 特別提醒:`** 一段, 並加「敬請見諒」;
   用語從「折扣可能不再適用」改成「原訂單套用之優惠折扣可能隨之失效」。
4. 「已照舊金額匯了」那句**指定走 LINE**, 並改成「我們將主動協助您辦理差額處理」。
5. 查詢連結那句改成「完整訂單明細與匯款資訊亦可點擊此處查閱」。

🔵 **舊字面留在這裡而不是被抹掉** —— 📌 一份「他核可的文案」是**改過兩輪**才定案的,
而下一個核對「碼 vs 他核可的那份」的人, 要在**同一頁**上撞到這一格。
(形狀照 `2026-09-06-bank-order-created-email-copy.md` 末尾那張留痕表立的先例。)

---

## 佔位詞對到哪個欄位

> 🔬 ⛔ **2026-09-13:這張表被我【切掉了】而我還宣稱「保留了它」**(Fable F1 抓到)。
> 我用程式把舊檔的這一節搬過來,而切點抓到標題列後面那個 `---` ⇒ **七列全沒了, 只剩表頭。**
> ⇒ 📌 **一個「我保留了 X」的宣稱, 而 X 是空的** —— 今晚同一族的第 N 次。
> ✅ 補回來了,而**行號是我自己重 grep 的, 不照抄舊表也不照抄 Fable** ——
>   舊表那七個行號**今天全漂了**(sweep 那支檔在這幾片之間長了)。

| 佔位詞 | 來源 | 核處(2026-09-13 自己 grep) |
|---|---|---|
| (訂單編號) | `payload.display_id` | `sweep-email-outbox.ts:706` |
| (訂單金額) | `payload.total` ← **`effective_total`** | plan §3-bis-3 · view `20260907210000:60-100` |
| (還要匯多少) | `payload.balance_due` ← **`effective_balance_due`** | 同上 |
| (已收金額) | **算出來的**:`total - balance_due` | `sweep-email-outbox.ts:715` 同一條算式 |
| (到期日) | `remittanceDeadlineSentence(payload.created_at)` | `sweep-email-outbox.ts:734` |
| (公司銀行) / (分行) / (公司戶名) / (公司銀行帳號) | 常數 `PCM_REMITTANCE_*` | `sweep-email-outbox.ts:729` 起 |
| (LINE ID) / (LINE 連結) | 常數 `PCM_LINE_ID` / `PCM_LINE_URL` | `sweep-email-outbox.ts:741` |

🔵 **期限那一句用的是【訂單的 created_at】, 不是取消的時間** ⇒ 📌 **期限不會因為取消而延後**,
與那封成立信講的是同一天。⚠️ 沿用既有行為,非本片決定;Sean 未答「取消後要不要重新給期限」。

⚠️ **而這張表對應的是【匯款成立信】那支模板的欄位讀法**(`buildBankOrderCreatedText`)——
本型別今天**還沒有自己的模板**(sweep 對它是 throw)⇒ 📌 **接線片寫模板時要照這張表,
而寫完要回來把行號換成【自己那支】的。**

---

## 🔵 那兩句「金額可能變高」為什麼要有

```
🔴 病灶:金額可能【比他下單時看到的高】——
   effective_total 不只是「扣掉取消的件」, 它還會
     ① 券整張作廢、算回原價(Sean Q39 逐字「那優惠卷就不能使用, 這樣最簡單, 算回原價」)
     ② 運費照規則重算(Sean 09-05 拍乙:跌破 5000 門檻要補收 100)
   ⇒ 一張用過券、取消之後跌破門檻的單, 這封信的數字會比他原本要付的【多】。

✅ Sean 2026-09-13 兩次拍板:
   · A2 甲 = **照寄**(與 2026-09-07 Q82 同向)
   · 另答 **要在信裡講一句為什麼** ⇒ 就是那段 `※ 特別提醒:`
🔵 措辭邊界(主視窗轉達, 而他的版本自己也守著):零內部說法 ——
   沒有「券作廢」「原價回算」「門檻」, 用「可能」不用「將」, 不提 5000、不提百分比。
```
