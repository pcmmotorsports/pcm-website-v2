# 訂單明細面板 · 後台現有功能逐項表(五塊補畫版)

> **這是什麼**:訂單相關畫面裡,後台今天做得到的每一項,逐項附 `檔案:行號`。
> ⚠️ **不是整站盤點**,涵蓋與不涵蓋見 §1。
> 🔴 **標題寫「五塊補畫版」是它出生時的名字** —— 2026-08-17 已擴到 **§9 訂單列表(片 1)** 與
> **§10 建箱出貨(片 3)**。**檔名與標題沒改**(改了外部引用會斷),**射程以 §1 為準,不以標題為準。**
> **為什麼在 repo 不在 artifact**:確認稿(`c3c6cc94`)會被下一版覆蓋,而**片 1 實作要照著這張表做**。
> **作者**:A 窗(前台 storefront + 後台外觀),2026-08-17。

---

## 0. 🔴 先講一件事:「26 項」那個數字,**我四種方法都沒找到它的清單**(數法與分母在本節下方)

前一任 `A-222-STOP` §4-② 與 §5-④ 兩處提到「26 項忘記畫」,而**那 26 項是什麼,repo 與信箱都查不到**。

**數法(可重跑,附分母與量具對照)**
```
repo docs/    grep -rlE '忘記畫|漏畫的.*項|逐項表' docs/           ⇒ 0
量具對照      grep -rlE '死顯示路徑' docs/                          ⇒ 1   （所以那個 0 不是 grep 壞了）
信箱          grep -rlE '忘記畫|逐項表' ~/pcm-mailbox/*.md          ⇒ 2
              └ 命中的兩處:A-222-STOP:180 與 :203（就是講「26」的那兩行本身）
                            B-196-A:8（講 W3 拆片，與本主題無關）
全 ref        git log --all --oneline -S'忘記畫' -- docs             ⇒ 0
```
🔴 **只有數字活下來,清單沒有。** 而那個數字在信裡看起來完全像一份已完成的盤點。
📎 這正是 `docs/patterns/guard-and-instrument-traps.md` 那一節的形狀
(「一個活的數字與一個寫死的數字」的近親:**一個沒有本體的數字**)。

⇒ **本表不是「把那 26 項抄過來」,是重新從 code 盤一次。**
⚠️ 所以**本表的項數與 26 對不上是正常的**,不要把兩個數字當成同一個東西比對。

---

## 1. 射程與限定(先讀這段再讀表)

```
涵蓋 §2-§6  訂單面板五塊（原本只有標題與清單、沒有長相的那五個）:
            採購 / 到貨登錄 / 編輯訂單 / 備註與聯絡紀錄 / 退款帳本
涵蓋 §9      訂單【列表】頁 = 片 1 的射程（2026-08-17 補盤）
涵蓋 §10     建箱 / 出貨流程 = 片 3 的射程（2026-08-17 補盤）
不涵蓋       訂單面板的收款、商品那兩塊 —— 確認稿【本來就畫了】，本表只放指標不重列
            （要看那兩塊：確認稿 c3c6cc94 各區塊的「這一塊底下有什麼」摺疊列）
不涵蓋       訂單以外的任何頁（客戶 / 商品 / 供應商 / 儀表板…）
```
🔴 **§9 §10 是補的,而補之前本表對片 1 / 片 3 的「零命中」是【射程造成的】** ——
那時的零**不是掃過沒事**。這段留著,免得下一個人把兩個時期的「零」讀成同一句話。
🔴 **本表全部來自【讀元件檔】,不是看畫面。** 本機 `apps/admin` 跑得起來
(`cd apps/admin && ADMIN_DEV_BYPASS=1 npx next dev -p 3001`)但**我沒有跑、也沒有資料**
⇒ **「畫面上長什麼樣」未確認**,本表宣稱的是「code 裡寫了什麼」。

**型別欄的意思**
```
動作  員工按得下去、會寫資料的東西（= #btncheck 的射程）
欄位  表單裡要填的格子
顯示  唯讀呈現，按不了
保護  擋下錯誤操作的規則（沒有 UI，但決定畫面長怎樣）
```

---

## 2. 採購(向供應商訂貨)

**容器**:`item-procurement-section.tsx:250-308`(每個品項一張卡,`:269` `detail.items.map`)

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 1 | 動作 | **新增採購 / 更新這筆採購**(同一顆鈕,選到既有供應商就變成更新) | `item-procurement-form.tsx:387` |
| 2 | 欄位 | 供應商(下拉;已停用的標「(已停用)」) | `item-procurement-form.tsx:265-280` |
| 3 | 欄位 | 訂購數量(1–100000 整數,必填) | `:282-294` |
| 4 | 欄位 | 預計到貨日(date) | `:296-304` |
| 5 | 欄位 | 聯絡管道(text ≤200) | `:306-315` |
| 6 | 欄位 | 供應商單號(text ≤200) | `:317-326` |
| 7 | 欄位 | 送出採購的時間(datetime-local) | `:328-336` |
| 8 | 欄位 | 回覆狀態(五選一必填:未回覆 / 已確認 / 改價 / 缺貨 / 部分出貨) | `:340-355`、字面 `procurement-view.ts:16-22` |
| 9 | 欄位 | 異常原因(內部;告知客人另走備註)(textarea ≤500) | `:359-367` |
| 10 | 顯示 | 採購列表九欄:供應商 / 訂購 / 到貨 / 回覆狀態 / 供應商單號 / 預計到貨 / 異常原因 / 送出時間 / 到貨登錄 | `item-procurement-section.tsx:138-146` |
| 11 | 顯示 | 「還有 N 件沒有登記來源」(算出來的,不是記住的;`null` 時誠實說算不出來) | `:53-74` |
| 12 | 顯示 | 供應商三態標記:(已作廢)/(已停用)/(狀態不明) | `:169-175` |
| 13 | 顯示 | 作廢原因(跨欄第二列;>60 字收合;`null` 顯示「(沒有帶回來)」) | `:94-109`、`:216-222` |
| 14 | 保護 | 清單沒完整載入 ⇒ 出警告 + 整份表單 disabled + 送出鈕不渲染 + hidden `stale=1` | `:27-39`、`item-procurement-form.tsx:263` |
| 15 | 保護 | hydration 完成前不給送(否則全量 payload 會把既有欄位靜默清成 NULL) | `item-procurement-form.tsx:51-55`、`:379-399` |
| 16 | 保護 | 已停用供應商 × 沒有生效採購 ⇒ 鈕按不下去 + 講出下一步 | `:240-241`、`:372-378` |
| 17 | 保護 | 作廢的採購列**不給到貨入口**(目前唯一的守門,不是 UX 便利) | `item-procurement-section.tsx:188-199` |
| 18 | 保護 | 供應商清單載入失敗 ⇒ 出紅字說明選單為何變短 | `:255-266` |

🔴 **缺口**:**撤銷這筆採購**沒有入口 —— 已落 `#623`(死顯示路徑)。

### 2.1 🔴 那 5 條「保護」的**觸發條件** —— 以及畫面有沒有告訴員工為什麼

> **為什麼要這一節**:「保護」沒有 UI,但它一旦觸發,員工看到的就是**一個變灰的表單或一個「—」**。
> **員工看到「—」的時候,畫面要能告訴他為什麼** —— 下表最後一欄就是在盤這件事。

| # | 什麼情況會觸發 | 員工看到什麼 | 畫面有沒有說為什麼 |
|---|---|---|---|
| 14 | 這張單的品項清單、或這個品項的採購清單**沒有完整載入**(`item.procurementTruncated \|\| detail.itemsTruncated`) | 整份採購表單變灰、**送出鈕整顆不見**;到貨入口變「—」 | ✅ **有**,黃底警告逐字講了原因與下一步(重新整理)`:27-39`。<br>⚠️ 但**送出鈕是「消失」不是「變灰」**(`item-procurement-form.tsx:379`)—— 警告有解釋,靠的是那段文字,不是鈕本身 |
| 15 | 頁面還沒 hydrate 完 / bfcache 返回鍵還原後重取中 | 鈕在,但寫「載入中…」或「重新載入中…」,按不下去 | ✅ **有**,鈕上的字就是原因(`:392-398`);**刻意不讓鈕消失**,否則員工會以為「這張單不能改採購」 |
| 16 | 選到**已停用**的供應商,而這個品項沒有生效中的採購 | 送出鈕變灰 | ✅ **有**,鈕左邊一段黃字講原因**與下一步**(`:372-378`)。<br>🔴 **兩種文案**:有生效列 ⇒「可以更新但不能新建」;沒有 ⇒「請先重新整理;若還是這樣,到供應商管理確認」 |
| 17 | 這一列採購**已作廢**(`p.voidedAt != null`) | 到貨登錄那一格變成「**—**」 | 🔴🔴 **沒有。這一格是本表唯一「不說為什麼」的保護。**<br>同一列左邊有「(已作廢)」徽章、下一列有作廢原因 ⇒ **推得出來,但沒有一個字在講「所以這裡不能登錄到貨」**。<br>⚠️ 顯示「—」而不是整欄拿掉是刻意的(欄位消失會讓表格對不齊)`:197` |
| 18 | 供應商清單(S3a 讀模型)載入失敗 | 供應商下拉**變短**(只剩這張單用過、且沒被作廢的) | ✅ **有**,紅字說明選單為何變短 + 叫人先別新增(`:255-266`)。<br>🔴 **而這條紅字住在【訂單層】** ⇒ 片 2 把區塊搬進展開列時它會沒有家,見 `admin-list-bmw-m-shape-plan.md §9-d-2 F1` |

🔴 **本節唯一一條紅的是 #17**,而它的嚴重度取決於 `#623`:
撤銷採購上線之前,「已作廢」**只有工程師手動下 SQL 才生得出來**(見 `#623`)⇒ 今天員工撞到的機率極低;
**而 `#623` 做完之後它會變成常見狀態** ⇒ **那時候「—」旁邊一定要有一句話。**
⚠️ 這是**我的判斷,不是量出來的** —— 我沒有真實資料可以看那一格在畫面上長怎樣(見 §1)。

---

## 3. 到貨登錄

**容器**:採購表最後一欄的 `<details>`,`receipt-record-form.tsx:204-207`

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 19 | 動作 | **登錄到貨** | `receipt-record-form.tsx:272-278` |
| 20 | 動作 | **撤銷剛剛那筆** | `receipt-undo-bar.tsx:67-73` |
| 21 | 欄位 | 到貨幾件(`min=0`;0 件 + 溢收 N 是正式登記法,不是筆誤) | `:225-237` |
| 22 | 欄位 | 什麼時候到的(預設填現在的台北時間) | `:239-247` |
| 23 | 欄位 | 溢收幾件(供應商多送的,不掛回這張單) | `:249-259` |
| 24 | 欄位 | 備註(選填,≤500) | `:261-270` |
| 25 | 顯示 | 撤銷的射程界線寫在畫面上:「只能撤掉剛剛這一筆」 | `receipt-undo-bar.tsx:75-77` |
| 26 | 顯示 | 撤銷被擋時**逐箱列出**未出貨包裹編號與件數(DB 原文照畫,保留換行) | `:89-96` |
| 27 | 保護 | `request_id` 冪等鍵掛載時產一次、成功後換新(否則第二批永遠 `DUPLICATE_REQUEST`) | `:30-37`、`:167-190` |
| 28 | 保護 | `failed` / `blocked` 都留著重試表單;`already_gone` 刻意不給 | `:53-59`、`:60` |

🔴 **刻意不做**:「清空」鈕(OD 有)—— 理由逐字在 `receipt-record-form.tsx:150`
「員工正在打第二批的數字會被當場清空」。**照 OD 補它 = 把一條修好的 bug 放回來。**

---

## 4. 編輯訂單(發票與出貨方式)

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 29 | 動作 | **儲存**(四欄一起送,只有真的改過的欄會寫) | `order-edit-form.tsx:48-53`、`:18-19` |
| 30 | 欄位 | 出貨方式(text ≤64,必填) | `:56-65` |
| 31 | 欄位 | 開票狀態(未開立 / 已開立 / 已作廢) | `:67-77` |
| 32 | 欄位 | 發票號碼(≤64;留空=清除) | `:79-88` |
| 33 | 欄位 | 發票金額(元)(留空=清除) | `:90-99` |
| 34 | 保護 | 樂觀鎖:`version` 隨表單送出 | `:44` |

🔴 **已退場**:「訂單狀態」下拉 —— 狀態改成逐品項算(Sean `Q-A=A`),`:20-21` 逐字記著。
**不要照舊版 OD 把它補回來。**

---

## 5. 備註與聯絡紀錄

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 35 | 動作 | **新增備註 / 送出更正**(同一顆鈕) | `note-compose-form.tsx:251-257` |
| 36 | 動作 | **更正**(可更正時是連結;已更正過是 disabled 鈕) | `notes-timeline.tsx:73-89` |
| 37 | 欄位 | 型別三選一:內部備註 / 聯絡紀錄 / 已告知客人 | `note-compose-form.tsx:190-203`、字面 `note-timeline.ts:22-26` |
| 38 | 欄位 | 聯絡管道(LINE / 電話 / Email / 當面 / 其他;只有非內部備註才出現) | `:205-218`、字面 `note-timeline.ts:29-35` |
| 39 | 欄位 | 聯絡時間(台北時間)—— **什麼時候聯絡的**,不是寫入時間 | `:219-229` |
| 40 | 欄位 | 內容(≤4000;placeholder 就寫著刪不掉) | `:233-245` |
| 41 | 顯示 | 每列:#seq · 型別徽章(三類分色)· 管道 · 聯絡於 · 作者登記於 | `notes-timeline.tsx:45-59` |
| 42 | 顯示 | 已更正徽章(由 #N)與「更正 → #N」對照 | `:60-70` |
| 43 | 顯示 | 收合摘要:「N 筆 · 已告知客人 M 筆 · 最後 …」 | `:154-161` |
| 44 | 顯示 | 讀不到時**不畫成「尚無備註」**,直說「不知道有沒有」 | `:174-178` |
| 45 | 顯示 | 截斷時不報「已告知 M 筆」,改報「僅最新 N 筆」 | `:152-159`、`:165-170` |
| 46 | 保護 | 預設收合,但有未被更正的「已告知客人」/ 讀取失敗 / 截斷 ⇒ 自己展開 | `:120-130` |
| 47 | 保護 | 更正前跳 confirm(不可撤回) | `note-compose-form.tsx:170-180` |
| 48 | 保護 | 一筆只能更正一次(規則單一真相在 lib) | `notes-timeline.tsx:71-72` |

---

## 6. 退款帳本(整區唯讀)

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 49 | 顯示 | 六欄:發起時間 / 種類 / 金額 / 狀態 / 原因·說明 / 發起人 | `refund-ledger-section.tsx:131-137` |
| 50 | 顯示 | 帳本未登記額**四態**:讀取失敗 / 查無 / 負值(對帳異常)/ 正常金額 | `:98-113` |
| 51 | 顯示 | 「⚠ 已列入退款異常清單(勿重複發起,待對帳處理)」 | `:172-179` |
| 52 | 顯示 | 「⚠ 已列入退款異常清單(這裡沒有可以改的動作…)」—— **與上一顆刻意不同措辭** | `:181-203` |
| 53 | 顯示 | 帳本列被上限截斷時明講 | `:121-125` |
| 54 | 保護 | 帳本讀取失敗 ⇒ 整區換成紅字、叫人別發起退款 | `:46-55` |
| 55 | 保護 | 零列 + 未登記額讀取失敗 ⇒ **排在零列早退之前**(順序寫反症狀原樣復活) | `:62-83` |

🔴 **措辭鐵律**:「帳本未登記額」**不得**寫成「還能退」「剩餘可退」
(場外退款不在帳本,真實剩餘只會 ≤ 此值)—— `:22-23`。

---

## 7. 這張表怎麼用(片 1 實作時)

1. **畫任何一顆鈕之前**,先在本表找得到它。找不到 ⇒ **先當作後台沒有**,
   回頭用兩種以上叫法 grep 一次再下斷言(本表射程見 §1,它只涵蓋五塊)。**不要照 OD 補。**
2. 本表的「保護」列**決定畫面長怎樣**(例如第 14、17 條會讓整段變成 disabled 或「—」)
   ⇒ 只畫 happy path 的稿,實作時會發現對不上。
3. 確認稿 `c3c6cc94` 頁尾有可重跑的 `#btncheck` 雙向對帳,**只涵蓋「動作」那一型**
   (欄位 / 顯示 / 保護不在它的射程內)⇒ **那三型只有這張表在管。**

## 8. 這張表的過期條件(寫出來,免得下一個人當它永遠有效)

```
它會過期，而且不會有人通知你。最可能的三個來源：
① 中文字面全部是【暫定稿、待 Sean 肉眼定稿】—— 五個元件檔頭都逐字寫著這句
   ⇒ 引用本表的字面去做對帳，Sean 改一次文案就全錯
② 行號會漂 —— 引用前用【字面 anchor】重新 grep，不要相信本表的行號
③ #623 做完（撤銷採購上線）之後，第 2 節要加一列、第 17 條的理由要重寫
```

---

## 9. 片 1 射程:訂單【列表】頁(2026-08-17 補盤;原本標「未盤點」的那一半)

> **為什麼補**:本表原本只涵蓋訂單面板五塊,而片 1 動的是列表 ⇒ 對帳時「零命中」是**射程造成的**。
> 這一節把射程補上。**「保護」型排最前面** —— 它沒有 UI 卻決定畫面長怎樣,而片 1 的驗收全寫在 happy path。

**主要檔**:`orders-table.tsx`(620 行)、`app/orders/page.tsx`、`order-toolbar.tsx`、
`order-filter-controls.tsx`、`order-filter-chips.tsx`、`order-keyword-search.tsx`、`order-density-toggle.tsx`

### 9.1 🔴 保護(先看這格)

| # | 什麼情況會觸發 | 員工看到什麼 | 畫面有沒有說為什麼 |
|---|---|---|---|
| 56 | `order.itemsTruncated` —— 這張單的品項清單沒完整載入 | 狀態欄不印狀態,**印「未知」** | ✅ **有**,`title` 逐字「這張單的品項清單這次沒有完整載入,算不出狀態。請重新整理。」`orders-table.tsx:427-436` |
| 57 | 同上 | **不得印 0、不得留空** —— 兩者都會被讀成「就是沒有」,而語意是「我們不知道」 | ✅ 理由寫在 `:415-424`:`goodsAxisOfLines` 三條判定都是 `.every(...)`,**對子集單調** ⇒ 子集算出來的階段**恆 ≥ 真實階段** ⇒ 看得見的都出貨了就答「出貨完成」,而沒載進來的可能一件都沒出 |
| 58 | `keywordTruncated` —— 關鍵字命中超過 100 筆 | 琥珀橫幅,**含 0 筆時也顯示** | ✅ **有**,而且 `page.tsx:196-200` 逐字寫明這個條件式**不得**加 `orders.length > 0`:RPC 先取全域最新 100 筆再與其他篩選取交集 ⇒ **0 筆 + 沒有提示 = 員工得到「查無此單」的錯誤結論** |
| 59 | 關鍵字 + 未含未付卡單 + 0 筆 | 第二條琥珀橫幅(有單被藏) | ✅ 有。⚠️ `page.tsx:150-155` 已知**可能與 58 同時出現兩條橫幅**,判斷=**不合併**(是真的兩個原因) |
| 60 | 已取消的單 | 操作欄顯示「**—**」 | ⚠️ **這不是權威閘**,`:466-468` 逐字:能不能取消由明細端 `buildOrderCancelView` / `cancelFormsAllowed` 判(fail-closed),這裡不重算一份 |

### 9.2 動作 / 顯示(片 1 會動到版面的那些)

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 61 | 動作 | 勾選單 → 浮出動作列 → **出貨(N 單)** | `shipping-selection.tsx:162-172` |
| 62 | 動作 | **取消勾選** | `shipping-selection.tsx:173-179` |
| 63 | 顯示 | 狀態欄字面與配色**全部由 `orderStatusView` 算**,本檔不自己拼 | `orders-table.tsx:402-407`(第二份會漂) |
| 64 | 顯示 | 發票欄複用 `INVOICE_STATUS_LABEL`(與明細頁同一份) | `:440-448` |
| 65 | 顯示 | 單號整列 stretched link;操作欄靠 `relative z-10` 才點得到 | `:452-460` 🔴 **z-10 是承重不是排版**:沒有它那顆連結點不到,**而點下去畫面確實有反應**(整列連結把人帶進面板)⇒ **肉眼驗抓不到**,守門釘在 `orders-table.test.tsx` |

🔴 **片 1 的真風險與 F1 同形狀**:第 56–57 條的「未知」是**訂單層的資料完整度**在講話,
而片 1 要改的是**列的形狀**。⇒ **改版面時那格「未知」與它的 `title` 不可以被樣式吃掉**
(它不是一個狀態,是「我們不知道狀態」)。驗收要有一格:`itemsTruncated=true` 時**那兩個字仍看得到**。

---

## 10. 片 3 射程:建箱 / 出貨流程(2026-08-17 補盤)

**主要檔**:`shipment-launcher.tsx`、`shipment-dialog.tsx`(510 行)、`shipment-section.tsx`、
`shipping-selection.tsx`、`shipment-mark-shipped-button.tsx`、`shipment-void-button.tsx`

### 10.1 🔴 保護(先看這格)

| # | 什麼情況會觸發 | 員工看到什麼 | 畫面有沒有說為什麼 |
|---|---|---|---|
| 66 | 本單箱品項清單**可能不完整**(截斷)⇒ `groups === null` | **整段包裹清單不列** | ✅ **有**,`shipment-section.tsx:48-52` 逐字「寧可不列,也不要列一份少了東西的清單」+「不要憑印象出貨或作廢」。🔴 fail-closed 的理由:**看到少一箱會以為貨不見了、少一件會以為已經出完** |
| 67 | 空箱區算不出來(查不到客人 / 品項列被截斷)⇒ `empties === null` | 空箱區出現、但說「這次沒能算出來」 | ✅ **有**,而且這是修過的:`:162-166` 逐字記著「**算不出來**」與「**沒有空箱**」原本畫成一模一樣的畫面(都是整區不出現),而建箱彈窗剛叫員工來這裡找箱 ⇒ 他會找到一張什麼都沒有的頁面 |
| 68 | 勾選了別的客人的單 | 該列 checkbox `disabled` | ✅ 有(視覺)。🔴 **而 `disabled` 只擋滑鼠** —— 鍵盤 / 輔助技術 / 擴充 / 程式化路徑都繞得過 ⇒ 狀態層第二道閘 `nextSelection()` `shipping-selection.tsx:66-78`。**兩道都要在** |
| 69 | 送出中(`busy`) | 彈窗的 ✕ 也按不下去 | ✅ 有(`disabled` 同時擋滑鼠與鍵盤)。理由 `shipment-dialog.tsx:17`:允許關窗會開出兩個洞 |
| 70 | 某品項 `remaining === 0` | 該列數量欄 `disabled` | 🔴 `:327-339` 逐字:判「能不能出」一律看 `remaining`,**不看 `blockedReason`** |
| 71 | 非「其他」貨運商而單號空白 | 「建箱並標出貨」變灰 + **紅字**;「只建箱、先不出貨」**仍可按** | ✅ 有。🔴 `:497-503`:**擋走紅、警告走琥珀,兩顆分開** —— R2 抓到原本擋走全站最不顯眼的顏色、**顯著度是反的** |
| 72 | 貨號格式可疑 | 琥珀警告,**不擋送出** | ✅ 有。🔴 `#551` Sean `Q-C551`=乙:**本片不擋任何東西**(每家貨運不一樣)⇒ 與 71 刻意不同色,長得一樣員工會以為被擋住,然後去改一個其實正確的貨號 |
| 73 | 建箱成功但沒掛上品項(半成品箱) | 錯誤區塊給出**箱號** + 「再按一次沿用同一箱、不會重複建」+ 去哪裡作廢 | ✅ 有。🔴 `:448-462`:兩句都不能拿掉 —— 前者是 Sean 08-09 逐字「我也找不到那個箱子在哪裡」的答案,後者解除「不敢再按」 |

### 10.2 動作(片 3 射程內)

| # | 型別 | 項目 | 出處 |
|---|---|---|---|
| 74 | 動作 | **建立包裹**(訂單頁出貨卡;= 單張訂單版的勾單流程,預選本單全部還能出的品項) | `shipment-launcher.tsx:207` |
| 75 | 動作 | **建箱並標出貨** / **只建箱、先不出貨** | `shipment-dialog.tsx:483` / `:491` |
| 76 | 動作 | **填單號並標記出貨** → 第二步 **標記出貨** | `shipment-mark-shipped-button.tsx:92` / `:116` |
| 77 | 動作 | **作廢這箱** → 填原因(必填)→ **確認作廢**;已作廢的箱有 **復原作廢** | `shipment-void-button.tsx:79` / `:99` / `:65` |
| 78 | 顯示 | 建箱成功後 `router.refresh()` —— 不刷的話新箱不會出現,員工會以為沒建成功而再按一次(**第二次是不同冪等鍵 = 真的第二箱**) | `shipment-launcher.tsx:191-193` |

### 10.3 🔴 片 3 的片界風險(與 F1 同形狀,寫出來讓 Sean 有機會說「那算」)

```
片 3 要做的:把「這一項出幾件」攤到品項展開層
而 66/67 兩條 fail-closed 是【訂單層 / 客人層】的：
  groups === null   本單箱清單不完整 ⇒ 整段不列
  empties === null  空箱區算不出來（空箱掛【客人】不掛訂單，shipments 沒有 order_id）
⇒ 🔴 空箱那條【在定義上】不可能搬進某一個品項的展開層 —— 它與這張訂單之間一條線都沒有。
⇒ 與片 2 的 itemsTruncated 是同一種病：不是「需再評估」，是【做不到】。
```
⚠️ **這是我讀 code 推的,不是量到的**(沒有真實資料可看畫面)⇒ 片 3 提 plan 時要帶給 Sean 與審查者。
