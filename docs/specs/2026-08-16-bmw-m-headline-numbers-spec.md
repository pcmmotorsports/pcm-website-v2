# BMW M 片4b — 訂單詳情「頭條數字」規格(✅ 已實作 2026-08-16)

> ## ✅ **已實作(2026-08-16 A 窗)。本檔自此是【規格 + 落地紀錄】。**
>
> 落地位置:`components/orders/order-detail-summary-cards.tsx` 搜 `HeadlineNumbers`;
> 算式 `apps/admin/src/lib/orders/order-status-axes.ts` 搜 `goodsQuantityHeadline`;
> 守門 `app/orders/[id]/refund-wiring.test.tsx` 搜 `describe('訂單明細頭條數字')`(六格)。
>
> **`Q-A7` 第三次獨立覆核(A 窗,不挑檔、用查的)** —— §3 的結論(扣)**成立**,證據錨點:
> - `order-status-axes.ts` 搜 `function lineNeed` ⇒ `Math.max(0, l.quantity - (l.quantitySummary?.cancelledQuantity ?? 0))`
> - 同檔搜 `已知限制` ⇒ 逐字「**上面那條「已知限制」已於 2026-08-16 結清 —— 本函式與 `goodsAxisOfLines` 都扣已取消數量了。**」
>
> 🔴 **本節取代 `~/pcm-mailbox/A-215-STOP.md` 約束表第 ④ 條**(那條寫「不扣」)。
> 那封信**已投遞、不回改**(規矩如此)⇒ 它會一直躺在信箱裡讀起來像現況。
> **下一個人若在任何地方看到「頭條不扣已取消」,那是同一個過期來源的迴音,以本檔為準。**
>
> ## ⚠️ 肉眼驗:**舊版面已驗 / 現版面【待複驗】** —— 缺口**沒有關掉**,只縮小了
>
> **已驗的是哪一版**:Sean 2026-08-16 於真路由看過,逐字「**目前這樣OK**」——
> 而他看的是**帶 `NT$`、小標寫「品項數」**的那一版。
> 🔴🔴 **那一版已經不存在了** —— 他在同一次就要了兩個改動(見下),而**本檔記錄的正是改完之後的版本**。
> ⇒ **不得引用那句「OK」為現版面背書。**
>
> **還開著的缺口(逐字,給下一個人)**:
> ```
> ⬜ 現行版面（金額不帶 NT$、小標「件數」）沒有人用眼睛看過
>    要看三件：①兩格會不會擠 ②數字會不會折行 ③兩組裸數字並排分不分得出來
> ```
> 📎 **我第一版把這一段寫成「✅ 誠實缺口關閉」,那在本片落地的那一刻就是假的**
> (code-reviewer 2026-08-16 抓到)—— 前件(舊版面已驗)為真,
> 而我把後件寫成**無條件關閉**。**改了前件、後件沒跟著翻。**
>
> **同一次他要了兩個改動,都已落地**:
> ```
> 「NT$ 2,480 / NT$ 0 => 改成 2,480 / 0，不用NT」      ⇒ 金額不帶 NT$
> Q-A216-F2b = 甲                                    ⇒ 小標「品項數」改「件數」
> ```
> 🔴 **拿掉 `NT$` 之後兩格都變成裸數字對**:`2,480 / 0`(金額)與 `4 / 2`(件數)**長得一模一樣**。
>
> 📎 **這個風險不是推測,已經有第一個實證後果**:守門那邊,金額 `100 / 0` 的字面
> **包含** `0 / 0`,把件數那格的反向斷言撞紅 ⇒ 所有跨這兩格的斷言現在都必須**帶小標當定位**。
> **字串層分不開的東西,眼睛通常也分不開。**
>
> ---
>
> **實作時比規格多做的一件(誠實申報)**:格2 小標寫成「**件數 已訂 / 到貨**」而不是 §1 表格的「品項數」。
> **兩層,都已由 Sean 2026-08-16 拍板結案**:
> - **「已訂 / 到貨」四個字是我加的** —— 理由是兩個裸數字掛在一個詞底下分不出誰是誰
>   (後台 UI 常設驗收條款「不用人教能做對嗎」,Sean 2026-08-11 拍板)。**他看過沒有要求拿掉。**
> - 🔴 **「品項數」→「件數」是【正確性修正】不是文案調整**(`Q-A216-F2b` 拍甲):
>   `goodsQuantityHeadline` 加總的是**件數**,而品項數是 `lines.length` —— **兩個不同的數**。
>   **OD 稿自己就寫成「3 項 · 4 件」**(`overview-desktop-bmw-m.html` 搜 `項 ·`)。
>   ⇒ **用「品項數」會讓員工照字面去對帳、然後對不上。不要改回去。**
>
> ---
>
> **狀態**:~~規格完備、零程式改動~~ **已實作(見上方)**。
> **來源**:Sean 2026-08-16 拍板(頭條放哪三個數字)+ 主視窗 `Q-A7` 兩次裁定(第二次推翻第一次,見 §3)。
> **為什麼落成 repo 檔而不是留在信箱**:**信箱不在任何守門的掃描範圍內**(I 窗 2026-08-16 提醒)。

---

## 1. 要做什麼

```
格1   小標「總額 / 已收」        兩個金額並列
格2   小標 ~~「品項數」~~ →【件數】   已訂數 / 到貨數    ← 2026-08-16 Q-A216-F2b 拍甲改掉
形狀  照 OD .spec：小標(.l) + 大數字(.v)，小標【要留著】
呈現  數值本身直接給數字，不在數字旁加解釋文字
```

🔴 **上表的「品項數」已於 2026-08-16 作廢**(`Q-A216-F2b` 拍甲,改成「件數」)——
留刪節線不刪字,因為 §3 與檔頭都在引用它當對照。**現行字面以「件數」為準。**

⚠️ **「小標留著」是 Sean 的更正,不是原始指示** —— 他先說「寫數字就好 我們看得懂」,
主視窗一度讀成「拿掉標籤」,他隔一輪逐字更正「**小標留著**」。
🔴 **那句話講的是【數值呈現】,不是【標籤存廢】** —— 兩者都能從同一句話推出來,而只有一個是對的。

**視覺形狀**:OD `overview-desktop-bmw-m.html:250-258` 的 `.specs` / `.spec` / `.spec .l` / `.spec .v`。
📎 髮絲線格與小標字體**已在片4a 落地**(`order-detail-summary-cards.tsx`)⇒ 本片沿用同一套,不重寫。

## 2. 資料來源 —— **全部用現成的,不自己算**

| 要顯示 | 取自 | 位置 |
|---|---|---|
| 總額 | `detail.total` | `AdminOrderDetail`(Money) |
| 已收 | `toPaymentSummary(...).received` | `apps/admin/src/lib/orders/payment-list-view.ts:167` |
| 已訂數 | 各 line 的 `quantitySummary.orderedQuantity` 加總 | 與 `order-status-axes.ts:325` 同一顆欄位 |
| 到貨數 | 各 line 的 `quantitySummary.instockQuantity` 加總 | 與 `order-status-axes.ts` 判 `instock` 同一顆欄位 |

🔴 **已收直接消費 `toPaymentSummary()`,不另立算法** —— 它與付款卡是**同一個函式**
⇒ **結構上不可能與付款卡對不起來。**
🔴 **「到貨數」已與 C 窗的定義對過**:`instockQuantity`,兩邊同一個欄位、同一個語意 ⇒ **不會各算一套。**

## 3. 🔴🔴 `Q-A7`:**扣掉已取消件數**(第二次裁定,推翻第一次)

```
2026-08-16 第一次裁 A：不扣 —— 理由是「與軸/小字保持一致」
2026-08-16 第二次改裁：扣   —— 理由【判準沒變，事實變了】
```

**為什麼翻**:`#522`(`20260816050000_m4b_522_goods_axis_subtract_cancelled.sql`,已進 dev 且已 apply)
把貨品軸的分母改成 `quantity − cancelled_quantity`,TS 側同步(`order-status-axes.ts` 搜 `lineNeed`);
**隨後 `692849c8` 把小字的分母也一起改成 `lineNeed`。**
⇒ **軸與小字【兩邊都扣了】** ⇒ 頭條也要扣,才與它們一致。

⚠️ **這不是推翻裁定,是前提被換掉**:判準始終是「與軸一致」,只是「軸怎麼算」變了。

> 🔴 **本節的第一版理由是錯的,留痕在此**:我原本寫「軸已扣、**小字沒跟上**」——
> **那是讀到過期的工作樹**(我的樹當時落後 dev,`692849c8` 還沒進來)。
> **事實是兩邊都已經扣了。** 結論(頭條要扣)不變,**理由改掉。**
> 📎 留這段是因為:**下一個人若在別處看到「小字沒扣」的說法,那是同一個過期來源的迴音。**

### 🔴 註解要釘在【一致性】,不要釘在某個數字上

**第一版的觸發條件是錯的**(主視窗自己收回):
> ~~「這個決定建立在『正式庫 `cancelled_quantity > 0` 為 0 筆』…一旦真的出現取消件數…」~~
> 🔴 **它把觸發條件釘在【資料變化】上,而實際先變的是【程式碼】** ——
> `#522` 改了算法,**而正式庫那個 0 筆一點都沒變** ⇒ **那個觸發條件永遠不會被觸發,即使前提早就失效。**

**⇒ 正確形狀(照這個寫)**:
```
本頭條的扣法必須與 `goodsAxisOfLines`（order-status-axes.ts）一致。
🔴 那邊改了扣法，這邊要跟 —— 不一致時員工會看到頭條與狀態膠囊互相矛盾，
   而【沒有任何東西會紅】。
📎 歷史：#522 就是這樣把前一版的裁定作廢的（前提變了、結論沒跟著翻）。
```

## 4. 🔴🔴 四條「不知道 ≠ 是 0」的約束 —— **全部既有,不是本片新立**

> 🔴 **出處一律用【grep 得到的錨點文字】,不用行號。**
> **理由是本片自己踩到的**:我第一版全寫行號,而在我寫下它們到現在之間,
> `#522` + `692849c8` 讓同一支檔的行號漂了 **69~100 行**(`:296`→`:365`、`:240`→ 已重構)。
> **行號漂掉時不會有任何東西紅,而讀的人會去看到錯的那一段。**

| # | 規則 | 出處(用這串去 grep) |
|---|---|---|
| ① | `quantitySummary === null` ⇒ **一個數字都不印** | `order-status-axes.ts` 搜 `不能跟著搬到` |
| ② | `PaymentSummary.kind === 'unknown'` ⇒ **不畫任何金額** | `payment-list.tsx` 搜 `不畫任何金額` |
| ③ | `itemsTruncated === true` ⇒ 加總會**少算** ⇒ 不印 | `packages/domain/src/order/types.ts` 搜 `itemsTruncated: boolean;`(**兩處,都要讀**:一處在取消列、一處在明細) |
| ④ | 扣已取消件數(§3) | `order-status-axes.ts` 搜 `lineNeed` |

### 收斂句

> **「不知道」與「是 0」不是同一件事,而【頭條數字】是最不能把這兩者講成同一句的位置。**

**為什麼在顯示層更嚴重**(主視窗補):
```
後端把「不知道」當 0  ⇒ 下游還有機會發現
畫在畫面 / 印在紙上   ⇒ 那就是最終答案，看的人沒有第二個來源可以對
```

### ① 必須**複述全文**,不可只寫「見 `order-status-axes.ts:296`」

原文逐字:
> 「`goodsAxisOfLines` 把 null 當 0 是**它自己判階段用的**語意裁定;
> **那個 `?? 0` 不能跟著搬到【印件數給人看】這個用途上** ——
> 印『已訂 0 件』會把『摘要列不存在』與『真的一件都沒訂』講成同一句話。
> **同一個值,兩種用途,只有一種合法。**」

🔴 **為什麼要複述而不是給行號**:那句警告**是直接寫給本片的**,
而它住在**本片作者不會打開的那支檔**。**指過去的人不會過去。**

### ③ 的風險已量:**低但不是零**

`ORDER_ITEMS_EMBED_LIMIT = 200`(`packages/adapters/src/supabase/mappers/order.ts:390`)
⇒ 一張單超過 200 個品項才會被截,**機車零件單實務上到不了**。
🔴 **但旗標存在就要讀** —— **代價只是「截斷時改印『未知』而不是印一個少算的數」**,
而**不讀它的話,那個少算沒有任何東西會紅**。⇒ **成本不對稱,這題沒有討論空間。**

## 5. 守門怎麼寫

🔴 **全部斷言在【整頁渲染輸出】(`container.textContent`),不斷言函式回傳值。**
理由逐字取自 `order-status-axes.ts:305-308`:「**算得對不等於那個畫面有印出來**」。
⚠️ **本片更該如此**:頭條數字是**使用者一眼就看的那個**,量回傳值等於沒量。
📎 同族既有守門位置:`app/orders/[id]/refund-wiring.test.tsx` 的 `describe('出貨狀態的解釋小字')` 六格。

**至少要有的格**:
1. 四個數字都印得出來(正常單)
2. `quantitySummary` 任一為 null ⇒ **件數**那格**不出現任何數字**
3. `PaymentSummary.kind === 'unknown'` ⇒ 已收那半**不出現任何金額**
4. `itemsTruncated` ⇒ **件數**那格**不出現任何數字**
5. 有取消件數時,已訂/到貨數**與 `goodsAxisOfLines` 的扣法一致**(正向對照:無取消時兩者相同)

## 6. 片型與批准狀態

- **標準片**(新功能、動接線)⇒ **要跑 code-reviewer**
- **不命中鐵則 8**(單一元件 + 既有資料來源)、**不命中鐵則 12**
- **Sean 已批內容**;**`Q-A7` 已裁(第二版:扣)**;**無待決策項**
