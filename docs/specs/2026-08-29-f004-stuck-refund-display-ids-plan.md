# 片級 plan — F-004「客人的退款卡住」那一類**帶單號**(**v3**,2026-08-29,線C)

> # 🛑🛑 **停:不值得現在做。而那是【產品理由】, 不是「太難所以放棄」。**
>
> **關卡1 跑了三輪(codex ×2 + adversarial-reviewer/opus ×1),三輪都 FAIL,共 **23 條**,而【三輪之間零重複】。**
> **v3 折完前兩輪 14 條;R3 的 9 條【未折】(見 §9c)。**
>
> ## ⇒ 停的理由(產品面, 主視窗 2026-08-29 裁, 線C 同意並更正一格)
> ```
> ① 它省的**不是「一次點擊」**（那是主視窗原本的說法，線C 更正）——
>    信裡逐字寫著「到後台『退款異常清單』那一頁看」，而那一頁上六列都有單號
>    ⇒ 🔴 它真正省的是：**「今天這 2 筆，跟昨天是同一批嗎？」**
>       —— 而那正是 2026-08-29 花掉一整輪的那個問題（問 Sean → 挖板子 :473 → 比對單號字面）
>    ⚠️ **而那個判斷一年發生的次數 ≈ 他主動提起那封信的次數** ⇒ 不是每天
>       （今天那一次，是因為他自己貼了截圖才發生的）
> 🔴 ② 而三輪 23 條顯示：真正的難處**不在「讓 RPC 回單號」**，
>    在【**證明 SQL 的 key 與 TS 的 key 是同一個**】—— 而那是一個**一般問題**
>    ⇒ **為了一個判斷題去解一個一般問題 ⇒ 順序反了。**
> ③ 而排隊的是【真的錢】：`#890` + `#445`（Sean 退不了那 6 筆）與他剛拍的優惠券線
> ```
> 🛑 **⇒ 所以這【不是】「太難所以放棄」** —— v3 的技術路線(丙)是對的,拆法也在(§9d)。
> **⇒ 它是「這個好處今天排不進前面」。**
>
> ## ⚠️ 而有一件【沒有問過 Sean】, 明寫
> ```
> 我們沒有問他「你希望那封信直接印出是哪幾張單嗎」。
> 🔴 理由：這一題我們判它值不值得排 —— 而**不是**因為我們預先決定了他會怎麼答。
> ⇒ 建議：**等他下次自己再提那封信時順口問**，不主動端。
> ```
>
> 🔗 **姊妹檔(同一天、同一個人、三條線上犯同一組錯)**:
> `docs/specs/2026-08-29-890-manual-verdict-correction-ui-plan.md` §10
> `~/pcm-mailbox/線C-2g交接單-兩輪18條與我失敗的形狀-20260829.md`

> **鐵則 8**(新 migration + 跨檔)⇒ **需 Sean 批准**;apply 是他的動作。
> **鐵則 12 ③DB** ⇒ plan 過關卡1、diff 過關卡2。
> **狀態:⏳ 未批准、未開工。零 code、零 apply。**
> **一句話:那封每天寄的信說「有 N 筆客人的錢卡住」,而【不說是哪幾筆】。這片讓它說。**
>
> 🔴 **關卡1 已跑兩輪, 兩輪都 FAIL, 共 14 條, 而【兩輪之間零重複】。v1/v2 錯在哪記在 §9 / §9b。**
>
> ## 🛑 而 v3 帶一個新的自檢(來自 R2 打中的那條)
> **R2 抓到:我 v2 【同一次折裡的兩個決定互相排斥】**(選路線丙 / 用共用 CTE)。
> ⇒ **而那比前四次難防:前四次是「我沒想清楚一個修法」;這一次是「兩個各自想清楚的決定, 而我沒有把它們放在一起看」。**
> ✅ **⇒ 所以 v3 起, 折完一輪要多做一個動作(便宜)**:
> **把【這一輪做的所有決定】列出來, 兩兩問一次「它們相容嗎」。**
>
> **v3 這一輪的決定與相容檢查**:
> ```
> ① 路線改丙（新開一支）  ② A 只承擔 identity 正對照  ③ E 獨占 status 那個變因
> ④ B 撤掉「結構同源」改行為對帳  ⑤ 新增 A2（述詞逐條）  ⑥ F 升成端到端
> 相容檢查：①×④ ⇒ 相容（丙不能共用 CTE，而④正是因此改的）
>           ②×③ ⇒ 相容（正是為了讓 E 承重才把那個世界從 A 移走）
>           ⑤×① ⇒ 相容（A2 只碰新 RPC）  ⑥×① ⇒ 相容（F 走 adapter，不動舊端點）
> ⚠️ 而這張表【本身】也可能漏 —— 它只涵蓋我列得出來的決定。
> ```

---

## 1. ① 改什麼 —— **路線【丙】:新開一支退款專用的 IDs RPC**

| # | 檔 | 新/改 | 做什麼 / 照哪一支抄 |
|---|---|---|---|
| 1 | `supabase/migrations/<新>_f004_stuck_refund_display_ids.sql` | 新 | 建 `public.get_order_refunds_stuck_display_ids()`;**形制逐項鏡 `20260819130000`**(SECDEF / `search_path=''` / **逐個角色 REVOKE** / GRANT `payment_confirmer` / COMMENT / apply 期交叉斷言) |
| 2 | `packages/adapters/src/payment/PgAnomalyAlertReaderAdapter.ts` | **改** | 加一段呼叫 + `UNDEFINED_FUNCTION` catch + `to_regprocedure` probe(**鏡同檔 `:123-134` 那一段,逐字同形**) |
| 3 | `packages/domain/src/payment/anomaly-alert.ts` | **改** | `AnomalyAlertSummary` 加兩個欄位(**v1 漏列 —— R1 must-fix**) |
| 4 | `packages/use-cases/src/check-anomaly-alerts.ts` | **改** | 那個 block 的 `section(...)` 第三引數由**寫死的 `[]`** 改成新欄位(`:386-411`) |
| 5 | `packages/use-cases/src/check-anomaly-alerts.test.ts` | **改** | 正/負渲染兩格(§7b-C) |
| 6 | `packages/adapters/src/payment/PgAnomalyAlertReaderAdapter.test.ts` | **改** | 缺 key / RPC 不存在 兩格降級(§7b-F) |
| 7 | `scripts/f004-display-ids-verify.sh` | 新 | 🔴 **獨立、可重跑的 DB 整合驗收**(§7b-A/B/E)—— **R1 must-fix:不得只寫在 migration 內** |
| 8 | 跨檔守門測試(落點見 §7b-D) | 新/改 | `LIMIT` 與 `MAX_ORDERS_PER_CATEGORY` 的不變式 |

### 1a. **定稿四項**(R1 must-fix:v1 這四項還是候選 ⇒ 送不了批准)
```
① 兩個 key 名（**我定的**）：
     `order_refunds_stuck_display_ids`
     `order_refunds_stuck_overnight_display_ids`
   理由：與既有計數 key（`order_refunds_stuck_count` / `_overnight_count`）**同字根**，
   一眼看得出誰配誰。
② `LIMIT 100`（**照 20260819130000 的既有數字**）+ 不變式 **100 >= 應用層顯示上限 30**
③ `ORDER BY created_at, display_id`（**帶唯一鍵** —— 照 `pagination-loop-review` 那條）
   ⚠️ 而 `display_id` 在 `orders` 上有唯一性（`20260729010000` 的 CHECK 值域）⇒ 平手可解
🔴 ④ **adapter 缺 key 的行為 ⇒ 已開檔查, 量到的**：
   `parseDisplayIds`（`PgAnomalyAlertReaderAdapter.ts:249`）逐字
     `if (v === undefined || v === null) return [];`  ⇒ **缺 key ⇒ 空陣列, 不 throw**（fail-soft）
   而 `parseCount`（`:230-235`）⇒ NaN **throw**（fail-loud）⇒ **兩者刻意不同**
   而整支 RPC 不存在 ⇒ 同檔 `:123-134` 的 `UNDEFINED_FUNCTION` catch + `to_regprocedure` probe
     ⇒ **降級成空，不炸**
   ✅ **⇒ 所以【回退序不是硬的】**：應用層先上而 RPC 沒上 ⇒ 信上只是不印單號；
      RPC 先上而應用層沒上 ⇒ 那兩個 key 沒有人讀。**兩個方向都 fail-soft。**
      🔴 而那正是 v1 §6 標「未查 ⇒ 它決定回退序是不是硬的」那一格的答案。
```

---

## 2. ② 路線:**丙**(而 v1 的「甲 vs 乙」是**假二選一** —— R1 must-fix)

| | 做法 | 判 |
|---|---|---|
| 甲 | 改 `get_payment_anomaly_alert_display_ids`,加第 6 組 | ❌ 分母不同(見下) |
| 乙 | 改 `get_order_refunds_stuck_summary`,讓它回 ids | ❌ **擴權**(見下) |
| **✅丙** | **新開一支退款專用的 IDs RPC** | ✅ **選這條** |

**甲為什麼否決**:那支的 COMMENT 自寫「五組 WHERE 與 `get_payment_anomaly_alert_summary` 的對應計數**逐字同源**,apply 期有交叉斷言」⇒ **第 6 組的對應計數在另一支函式、另一張表** ⇒ 進不了那個框架;
而把 `order_refunds` 塞進一支自稱是 anomalies 家族的函式,**正是 `20260824040000:162` 逐字警告的「名字比分母寬,不要混用」**。

**🔴 乙為什麼否決(而這一格 v1 完全沒有寫出來 —— R1 must-fix)**:
```
`get_order_refunds_stuck_summary` 的 COMMENT 逐字：「唯讀讀**聚合計數**，**零 PII / 零金額 / 零 id**」
⇒ 乙 讓那支**開始回單號** ⇒ **一個既有的、明文零 id 的受控窗被擴權**
✅ 而丙：那支**一個字都不動** ⇒ 沒有東西被擴權，而**產品結果一模一樣**
📌 ⇒ 兩者的差別不在功能，在【爆炸半徑】
```

### ⚠️ 2a. 丙的代價 —— **明寫, 而且寫明是【誰裁的】**
```
🔴 多一支 RPC ⇒ **多一個 Sean 的 apply 停點**（他要多按一次）
⇒ 換到的是：**不動任何既有端點**
🛑 **而這個取捨是【主視窗 2026-08-29 裁的】, 不是 Sean 拍的**
   ⇒ **他看到本 plan 時可以推翻它**（推翻 = 改回乙 = 少一個 apply 停點、而那支既有端點開始回單號）
```

---

## 3. 🔴 隱私:**這是一個【新的揭露決定】—— 而它有先例, 而先例是他本人拍的**

> ⛔ ~~v1 §3 寫「這是改變 `get_order_refunds_stuck_summary` 那個刻意的設計」~~ **作廢** ——
> **走丙之後那支沒有被改變** ⇒ 那段論證的對象不存在了。

**丙 真正的揭露決定是:【新開一支會回單號的函式】。**

✅ **而先例是 Sean 本人 2026-08-19 拍的**(`20260819130000` COMMENT 逐字):
> 🔴 這是【**刻意打開的隱私閘**】:原設計零 id(**LINE 訊息會被轉發截圖**),
> **2026-08-19 Sean 本人在知道該代價的情況下拍板打開,理由「沒單號我查不到」**;
> 要關回去要**再問他**,不是修 bug。

📌 **⇒ 他 08-19 為【另外五類】做過同一個取捨。本片是把它套到第六類。**
🔴 **⇒ 而這一類正是【唯一一類已經確定卡住的】**(信自己寫著「這一類是已經確定卡住的,不是『可能』」)
⇒ **也就是說:他 08-19 要解決的那個痛(「沒單號我查不到」), 在 08-24 新加的這一類上原封不動地回來了。**

⚠️ **代價照抄不弱化**:那封信會被轉發、被截圖 ⇒ **多印的是訂單編號**(不是姓名/金額/卡號)。
⚠️ **本片預設照先例打開、不重問他**;他若要只對這一類關著,那要他一句話。

---

## 4. 為什麼現在做
```
🔴 Sean 2026-08-29 逐字（回主視窗）：**每天都有**
⇒ 而那封信說「N 筆客人的錢卡住、其中 N 筆超過一天」，**而不說是哪幾筆**
⇒ ⇒ **他每天要自己開那一頁找一次。**
```
⚠️ **而「超過一天」那個數字要跟著範圍走**(端他時要帶):
它從 `order_refunds.created_at` 起算 = **我們按下退款那一刻**,**不是客人要求退款那一刻** ⇒ **可能低估**。

---

## 5. ③ 預期影響面

| 面 | 影響 |
|---|---|
| 新表 / 新欄 / backfill | **無** |
| 既有函式 | 🔴 **零改動**(丙的全部價值就在這一格) |
| 既有資料 | 零寫入 |
| 對外可見 | 🔴 **那封信會多印訂單編號** ⇒ 唯一對外可見的改動 |
| 應用層 | domain 型別 + adapter + use-case + 兩支測試(§1 清單) |
| 部署耦合 | ✅ **鬆的** —— 兩個方向都 fail-soft(§1a-④) |

---

## 6. ④ Rollback
```
撤 migration：DROP FUNCTION（新函式，零相依）
撤應用層：revert 那四支檔
✅ 而【順序不硬】—— §1a-④ 量到兩個方向都 fail-soft
⚠️ 而仍建議 **應用層先撤**：那樣中間態是「信上不印單號」（= 今天的樣子），
   而不是「adapter 讀一個沒有人回的 key」（雖然那也只是空陣列）
```

---

## 7b. 🔴 驗收

> **自查兩題(來自 `#890` 三輪的教訓)**:①旁邊有沒有寫**它怎麼被證明**?②**它量的是不是這一片**?
> 🔴 **而 v1 的 §7b 自己犯了第②題(R1 must-fix)**:A/B/E 寫在新 migration 內
> ⇒ **把那支 migration 刪掉, 驗收也一起消失, 而剩下的應用層測試仍會全綠**。
> 📌 **⇒ 一個【為了解決 X 而補的東西】, 自己犯了 X。而它比第一次難看到, 因為那個補救看起來就是在做對的事。**

> 🔴 **v2 的 §7b 六格被 R2 打掉五格, 而共同形狀只有一句(值得先讀)**:
> ## **「這一格能過, 而它證的不是它宣稱的那件事。」**
> ⇒ **而每一條的修法都是同一個動作:【一次只改一個變因】。**
> 📌 **⇒ 哨兵今天播的「等價突變」是【怎麼發現一發改了兩件事】;本節是它的正面版 ——**
> **【怎麼設計一發只改一件事的驗收】。而正面版讓那個錯【構造不出來】。**

| # | 驗收 | 兩個世界(**一次只差一個變因**) | **怎麼證明 / 它量的是誰** |
|---|---|---|---|
| **A** | 新 RPC 回的單號**就是那一筆的** | 造**一筆** stuck 退款,其訂單用**唯一 sentinel `display_id`** ⇒ 回傳陣列**逐字等於** `[sentinel]` | 🔴 **獨立腳本 `scripts/f004-display-ids-verify.sh`**(**不寫在 migration 內** —— R1)⇒ migration 缺席 / 新 key 缺席 / 述詞突變 **三個世界都要紅**。<br>🔴 **R2:A 只承擔【單筆 identity 正對照】** —— ~~「移出條件 ⇒ 回 `[]`」~~ 移交 E,否則 **E 被 A 嚴格蘊含、自己不承重** |
| **A2** 🆕 | **述詞逐條承重**(R2 must-fix) | **同一 fixture, 一次只改一個**:`created_at` 跨/未跨 **30 分** / `provider_refund_id_evidence` 空/非空 / `created_at` 跨/未跨 **24 小時** | 🔴 v2 只用 `processing→confirmed` ⇒ **一支「只看 status」的錯 RPC 也會全過**;那三個述詞**一格都沒被測到**。<br>每一個門檻各一發,**兩組 count 與兩組 ids 都要驗** |
| **B** | count 與 ids **不會漂** | 同一快照下,**非對稱 fixture**(stuck 有而 overnight 無、反之)⇒ 新 RPC 的兩組 ids 與**既有 summary** 的兩個 count **行為對帳** | 🔴 **R2 must-fix:~~「共用 CTE ⇒ 同源變成結構上成立」~~ 作廢** ——<br>**路線丙保證既有 summary 零改動 ⇒ 兩支 RPC 不可能共享同一個 CTE**;新 RPC 內部有 CTE 也**證不到舊 RPC 的 count 同源**。<br>📌 **⇒ 那是我同一次折裡的兩個決定(選丙 / 用共用 CTE)【互相排斥】** ⇒ 改成**行為對帳**,而**不再宣稱結構同源** |
| **C** | 信上印得出來,**而兩欄不會互換** | 🔴 **stuck 與 overnight 各放【不同的 sentinel】**(R2 must-fix)⇒ 各自**只**出現在自己那一行;ids 空 ⇒ 不印那一行而其餘三行 ↳ 照常 | 應用層測試。⚠️ **明寫:這一格【證不了本片的 migration】**。<br>🔴 **R2**:兩欄若共用同一個 fixture ⇒ **欄位互換、或兩段都讀同一欄, 仍然全綠** |
| **D** | `LIMIT` 護欄的不變式 | **兩個 key 各綁自己的 `LIMIT`**,各突變成 **29 / 101** ⇒ 各自要紅 | 🔴 **R2 must-fix:不得只讀字面** —— 讀字面**可被註解、無關子查詢、或只釘第一個命中騙過**。<br>⇒ **只讀可執行 SQL**;而跨檔那一半(`MAX_ORDERS_PER_CATEGORY`)仍要同時讀 ⇒ **任一路徑或字面缺失也要紅**(R1) |
| **E** | 負對照 —— **它獨占這個變因** | 同一筆 fixture,**只改 `status`**(`processing` ↔ `confirmed`)⇒ 非空轉空 | 🔴 **R1**:~~「現造一個不存在的 status」~~ 作廢(**可能在寫入時就被 CHECK 拒絕** ⇒ 那個紅不是查詢的負對照)⇒ 用**可合法寫入而應被排除**的狀態,**先斷言 fixture 寫入成功**。<br>🔴 **R2**:A 不得再包含這個世界,否則 **拿掉 E, A 會替它紅** |
| **F** | **整條鏈真的接起來** | RPC **存在且回兩個不同 sentinel** ⇒ adapter 逐字映到**兩個正確的 domain 欄位** ⇒ 流進 C | 🔴🔴 **R2 must-fix, 而它是這一節最重的**:v2 的 A–F 全過, **而 adapter 若「永遠不呼叫新 RPC」或「永遠回 `[]`」, 六格照樣全綠 ⇒ 信上永遠沒有單號。**<br>📌 **⇒ 而這片的產品目的就是【那封信上要出現單號】** ⇒ 那是最徹底的一種假綠。<br>⇒ 降級那兩格(缺 key ⇒ `[]`;RPC 不存在 ⇒ 不炸)**留著,但它們只是負對照** |

### ⚠️ 7b-1. 而有一格我【證不了】, 列出來, 不換說法
```
🔴 「信上印的那個單號，確實是【那一列】的」——**我證不了它**（多列時）。
   `check-anomaly-alerts.ts` 的 `shownIds` 有截斷（`MAX_ORDERS_PER_CATEGORY`）
   ⇒ 🔴 **而我沒有讀那段** ⇒ 不知道截斷之後的順序保證是什麼
⇒ **缺的那一道檢查**：讀 `shownIds` 與 `MAX_ORDERS_PER_CATEGORY` 的本體
🛑 **在那段被讀完之前, 本片不得宣稱「員工看到的單號對得上那一列」。**
✅ 而 A 那一格**刻意用單筆 sentinel** ⇒ 它不依賴我理解多筆截斷 ⇒ **那一格不受這個缺口影響。**
```

---

## 8. 誠實邊界
```
· §1a-④ 的 adapter 行為 = **開檔讀到的**（附行號與逐字）⇒ 這一格是量到的
· §1 的檔案清單是【讀鄰居堆疊估的】；行數要等實作
· §7b 的六格**尚未跑過** —— 本檔只寫出形狀與「怎麼證明」
· §7b-1 那一格**明列證不了**，缺的檢查已寫出
🔴 · 我**沒有讀完** `20260819130000` 全文（207 行）—— 只讀了 COMMENT / 簽章 / ACL 段 /
   refunding_stuck 那一組 SQL ⇒ **它可能還有本片該照抄的形制**（尤其 apply 期交叉斷言的寫法）
🔴 · 本 plan v2 **尚未過 R2**。在那之前不得說「已過對抗審查」
```

---

## 9. 🔴 v1 錯在哪(不刪除,供追線)

| # | v1 寫的 | 事實 |
|---|---|---|
| 1 | 「甲 vs 乙」二選一 | **假二選一** —— 漏了**丙**;而**乙會擴權**一個明文「零 id」的既有端點,v1 沒寫出來 |
| 2 | §7b 的 A/B/E | **寫在 migration 內** ⇒ 刪掉 migration 驗收也消失 ⇒ **它自己犯了「量的是不是這一片」** |
| 3 | A「count = 陣列長度」 | **錯的單號、重複 N 次照樣通過** ⇒ 改單筆 sentinel 逐字相等 |
| 4 | B「同一支函式、同一發 SELECT」 | **不等於同源** ⇒ 改共用 CTE |
| 5 | D | **SQL 讀不到 TS 常數** ⇒ 改跨檔測試 |
| 6 | E「不存在的狀態」 | **可能在寫入時就被拒** ⇒ 那個紅不是查詢的負對照 |
| 7 | 四項「批准前定稿」 | **仍是候選** ⇒ 送不了批准 ⇒ v2 定死(§1a) |
| 8 | 影響面 | 漏 `AnomalyAlertSummary` 型別、adapter 確切檔、新增測試檔 |

---

## 9b. 🔴 v2 錯在哪(R2 打中 6 條, 全新、無一重複)

| # | v2 寫的 | 事實 |
|---|---|---|
| 1 | A/B/E 只用 `processing→confirmed` | **一支「只看 status」的錯 RPC 也會全過** —— 30 分 / provider evidence / 24 小時 三個述詞**一格都沒測到** ⇒ 新增 **A2** |
| 2 | B「共用 CTE ⇒ 同源變成結構上成立」 | 🔴 **路線丙保證既有 summary 零改動 ⇒ 兩支 RPC 不可能共享 CTE** ⇒ 改**行為對帳**、不再宣稱結構同源 |
| 3 | A–F 六格 | 🔴 **adapter 若永遠不呼叫新 RPC 或永遠回 `[]`, 六格照樣全綠 ⇒ 信上永遠沒有單號** ⇒ **F 升成端到端** |
| 4 | D 讀 `LIMIT` 字面 | 可被**註解 / 無關子查詢 / 只釘第一個命中**騙過 ⇒ 只讀可執行 SQL、兩個 key 各綁自己的 |
| 5 | E | **被 A 嚴格蘊含** ⇒ 拿掉 E,A 會替它紅 ⇒ E 自己不承重 ⇒ 讓 E 獨占那個變因 |
| 6 | C 兩個新欄位 | 共用同一個 fixture ⇒ **欄位互換、或兩段都讀同一欄, 仍全綠** ⇒ 兩個不同 sentinel |

📌 **⇒ 六條裡有五條是同一句話的不同說法**:
> ## **「這一格能過, 而它證的不是它宣稱的那件事。」**

🔴 **⇒ 而第 2 條是這一族的【新形狀】:不是我沒想清楚一個修法, 是【兩個各自想清楚的決定, 而我沒有把它們放在一起看】。**
✅ **⇒ 處置已寫進檔頭:折完一輪, 把這一輪做的所有決定列出來, 兩兩問一次「它們相容嗎」。**

---

## 9c. 🛑 R3 的 9 條 must-fix(**未折**;adversarial-reviewer / opus / 換角度)

**角度**:「假設這份 plan 的每一個驗收都是恆真的。證明給我看。」

```
🔴 1. F 的正對照落在 stub 測試 ⇒ **stub 回的 key 是作者自己打的那一份**
   SQL 端 key 打成 A、TS 端讀成 A′、stub 用 A′ ⇒ 全綠而信上永遠沒單號
   （R2 第 3 條的同一個病**下移一層**）⇒ 正對照要跑在真 DB + 真 adapter 那條路上
🔴 2. §1a-① 兩個 key 名【沒有任何一格會在它被寫錯時變紅】
   ⇒ 而 §1a-④ 的 fail-soft（缺 key 回 []）被我寫進 §5 當「部署耦合鬆的」✅
   ⇒ **而它同時就是 key 名唯一的守門被關掉的原因**
   📌 **同一個機制, 我只看到它好的那一面, 而它的另一面正好殺掉這片的目的。**
   修法：鏡 20260819130000:303-337 的 apply 期 key-pair 表 + 跨檔測試把兩側字面對起來
🔴 3. §1a-③ `ORDER BY created_at, display_id` **零守門** —— 七格全部 ≤1 列/key
   ⇒ **整條 ORDER BY 刪掉、或改成不帶唯一鍵，全綠**
   修法：加一格多列（同 created_at、不同 display_id）連跑兩發比陣列逐字相同
4. §7b-A「新 key 缺席要紅」是假的：key 缺席 ⇒ `->` 回 SQL NULL，
   **`NULL = '...'::jsonb` 是 NULL 不是 false** ⇒ IF 不成立 ⇒ 放行
   （20260819130000:328-333 逐字記過這個洞；而 §8 自承沒讀完該檔 ⇒ 沒繼承）
5. §7b-A2 對一個 **OR** 述詞做「一次只改一個」= **恆真配方**
   ⇒ 要明寫「另一個 disjunct 釘 false」（測 30 分時 evidence 必須 NULL，反之亦然）
🔴 6. §7b-D **自我矛盾**：不變式是 `100 >= 30`，而 **101 滿足它**
   ⇒「突變成 101 要紅」與「這是不變式」**兩句必有一句是假的**
7. §7b-B 的第二個世界**構造不出來**：overnight ⊊ stuck **恆成立**
   （述詞就是 stuck AND 24h，20260824040000:137-143）
🔴 8. 檔頭那張相容檢查表漏的那一對 = **⑤×④**：A2 在每個述詞邊界都做了 B 的行為對帳
   ⇒ **拿掉 B，A2 會替它紅** ⇒ B 不承重。而 15 對我只做了 4 對
   📌 **⇒ 而它與 R2 抓到的「E 被 A 嚴格蘊含」是【同一個形狀】——**
      **我加那張表正是為了防那個形狀, 而它在那張表上又發生了一次。**
9. §7b-E 用 `confirmed` **沒有來源**：可見範圍內 order_refunds.status 只出現過
   `processing`(20260824040000:132) 與 `failed`(:155)
   ⇒ 若不在值域，fixture 寫入就被 CHECK 拒 ⇒ **R1 第 6 條原封回來, 只是換了一個字面**
```
✅ **而 R3 也點名一格【真的承重】**(附它怎麼確認的):`§1a-④` 前半(缺 key ⇒ `[]` 不 throw)
對應 §7b-F 的降級負對照 —— 兩個世界只差 `PgAnomalyAlertReaderAdapter.ts:249` 那一行的分支,
而**沒有別的格會替它紅**。

---

## 9d. ⇒ 若哪天要重啟:**拆法已經想好了, 照這個走**

```
✅ 第一片【DB 那一半】：新 RPC + 獨立驗收腳本（§7b 的 A / A2 / B / E）
   ⇒ 產出：**「資料出得來」是確定的**，而它可以被證明
   ⇒ Sean 只要批一次、apply 一次
   ⚠️ **而代價要寫進那一片的第一段：交完之後, 信上【還是不會印單號】。**
🔴 第二片【adapter + use-case】：而它真正的難處是 R3 的第 1、2 條 ——
   **要證明「SQL 的 key」與「TS 的 key」是同一個, 而今天沒有任何機制在維護它**
   ⇒ 而那是一個**一般問題**，不只這一次會發生 ⇒ **它值得單獨開一列**（已交主視窗）
```

📌 **⇒ 而三輪 23 條真正的產出, 是上面那句紅字 —— 它與 F-004 無關, 而它是這三輪唯一會再發生的東西。**
