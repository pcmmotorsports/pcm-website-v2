# `#24` 匯出訂單 / 商品 — 查證報告(A 窗夜跑,零 code、**不設計**)

> 事實親查於 `pcm-void-readers` @ `81d4520a`。🔴 **全部來自 repo 檔案,未對正式庫查詢。**

## §1 匯出能力 = 零(三組 pattern + 分母 + 正向對照)

| 組 | pattern | 範圍 | 命中 |
|---|---|---|---|
| A 傳輸機制 | `Content-Disposition` \| `toCsv` \| `to_csv` \| `exportCsv` \| `downloadCsv` \| `createObjectURL` \| `attachment;` | `apps`+`packages`(排 `.next`) | **0** |
| B 檔案格式 | `.csv` \| `.xlsx` \| `text/csv` \| `application/vnd.openxml` | 同上 | **0** |
| C 中文 | `匯出` \| `下載` \| `報表` | `apps/admin/src` | **20** |

**正向對照**:同範圍同寫法換 `訂單` ⇒ **528 行** ⇒ 掃描範圍不是空的。

🔴 **C 組那 20 行沒有一行是資料匯出** —— 逐行看過,19 行的「匯出」是 **ES module `export`** 的中文寫法
(例:`result-banner.tsx:30`「匯出是為了讓『零碰撞』那道守門有判別力」),
剩下 1 行是 `settings/staff-edit-row.tsx:42` 逐字「此標記目前不影響任何權限;**成本報表上線後才會生效**」= 指向 `#31`/`#33`,不是本片。
⚠️ **這正是「只看數字不看內容」會踩的坑**:20 這個數字看起來像「有東西」,實際是同音異義。

## §2 `scripts/rpm-*.ts` 沿用不了(你要我確認的那條)

**確認:它是 DB→DB,不是檔案匯出。** `scripts/rpm-import.ts:4-8` 逐字:
「**來源**(唯讀、絕不寫):報價單 B庫 `dllwkkfanaebrsuyuedy` 乾淨 view `storefront_catalog_v`…
**目標**(寫):pcm-website-v2 `bmpnplmnldofgaohnaok` `products` + `product_variants`」。
⇒ 它的形狀是「跨庫同步 + 一整排安全 gate」,**與「產一個檔案給人下載」沒有共用面**。

🟢 **但有一件事值得抄**:同檔 `:5` 逐字「用 anon publishable key(**讀不到成本/蝦皮/經銷**)」——
**用「連讀都讀不到」的憑證來保證敏感欄不外流**,而不是靠 select 清單自律。這個思路對 `#24` 有用(見 §4)。

## §3 Sean 要匯出來做什麼 —— 三種常見用途,各要的欄不同(給他用選的)

**我答不了這題,以下是把選項攤開,不是推薦。**

| 用途 | 一列代表 | 大概要哪些欄 | 敏感度 |
|---|---|---|---|
| **① 報稅 / 給會計** | 一張訂單 | 訂單編號、日期、金額(小計/運費/總額)、發票號碼與狀態、付款方式 | 中:**不需要客人地址、不需要品項** |
| **② 對帳 / 查帳** | 一張訂單 或 一筆收款 | 訂單編號、日期、應收、已收(`order_payments`)、已退(`order_refunds`)、付款管道 | 中:**不需要客人個資** |
| **③ 上架到別的平台 / 商品盤點** | 一個商品變體 | 料號、品名、規格、售價、品牌、供應商 | 🔴 **高:一不小心就帶到 `price_store`(經銷價)與 `supplier_slug`** |

🔴 **①②③ 是三張不同的表,不是「一個匯出功能加選項」** —— 一列代表的東西都不一樣(訂單 / 收款 / 商品)。
**這題答完之前,片界畫不出來。** 建議排進 Sean 的批次題。

## §4 🔴 鐵則 12:匯出 = 資料離開系統,而這個 repo 已經有一整條防線可以抄

**既有防線(不是我發明的,是實查到的)**:

1. **`order_items.product_snapshot` 是白名單**:`20260604120000:170` 逐字「🔴 鐵則 12 經銷價零滲入:逐欄白名單 `title/sku/spec`…DB CHECK `order_items_snapshot_whitelist` **縱深拒經銷價鍵**」;
   同檔 `:169` 逐字「**無 `price_store`/`price_by_tier`/`cost`**」。
2. **顯示投影明文挑欄、不用 `select('*')`**:`SupabaseOrderAdapter.ts:231` 逐字「鐵則 12:**仍禁** `select('*')`、零成本欄」。
3. 🔴 **已經有一格 forbidden-token 測試**:`SupabaseOrderAdapter.ts:327` 逐字對
   `price_store / price_by_tier / price_general / cost / address_id …` **逐字比對零碰撞**。
   ⇒ **匯出片應該把那格擴到匯出欄清單上,而不是新寫一套。**

**我判定不該出現在匯出檔裡的欄(逐欄附理由)**:

| 欄 | 為什麼不該出去 |
|---|---|
| `price_store` / `price_by_tier` / `cost` | 經銷價與成本 —— 鐵則 12①,而且 `product_variants` **真的有 `price_store`**(`database.types.ts` 該表 Row 內) |
| `tappay_rec_trade_id` | 金流交易識別碼;既有列表投影 `ADMIN_ORDER_LIST_SELECT`(`SupabaseOrderAdapter.ts:93`)**本來就沒有它** |
| `shipping_address_snapshot` / `notification_email` / `customers.phone` | 客人個資;用途①②都不需要 |
| `invoice`(Json) | 內含統編 / 載具號碼 |
| `customer_user_id` / `cart_session_id` | 內部識別碼,外流無益 |
| `supplier_slug` | ⚠️ **視用途而定**:用途③盤點時員工可能真的要看,但**匯出檔會被轉寄** ⇒ 這格要 Sean 拍 |

⇒ **鐵則 12 逐條**:①錢 = **是**(匯出檔含金額,雖只讀不寫,但**資料離開系統**這件事本身就是 12 的精神);
②權限 = **是**(新路由要在 SSO 保護面內、且要決定誰能匯出);③schema = 若走既有 view 則否;
④平台設定 = 若引入 CSV/xlsx 套件則**是**;⑤對外不可回收 = 🔴 **是 —— 檔案一旦下載就收不回來**;⑥`packages/ui` = 否。
⇒ **命中 ①②⑤(至少)⇒ codex 關卡2 不降級、要提 plan 等 Sean 批。**

## §5 誠實缺口

1. **未對正式庫查詢。** §4 的 CHECK 與欄位存在性都只是 repo 檔案這樣寫。
2. **我沒有讀 `SupabaseOrderAdapter.ts:327` 那格測試的本體**,只讀了它上方的 docstring
   ⇒ 「可以擴用」是**基於註解的推論**,不是我確認過那格的形狀真的擴得動。
3. **§3 的三種用途是我從一般電商經驗列的,不是從 Sean 的話推的。** 他真正要的可能是第四種,
   也可能只是「把訂單列表存下來備份」。**這張表的用途是讓他快速選,不是我替他縮範圍。**
4. **CSV 的中文編碼與 Excel 相容性我沒查**(BOM / 逗號 / 換行逃逸)—— 那會決定檔案打開是不是亂碼,是實作面的已知坑。
5. **沒查匯出量級**:一次匯多少列、要不要分頁或串流。若訂單量是每月 100-300 筆(memory 記的量級),
   **很可能一次全撈就夠**,但我沒數過正式庫。
