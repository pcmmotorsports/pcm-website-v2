# `#24` 匯出訂單 / 商品 — 查證報告(A 窗夜跑,零 code、**不設計**)

> 事實親查於 `pcm-void-readers` @ `81d4520a`。🔴 **全部來自 repo 檔案,未對正式庫查詢。**

## §1 匯出能力 = 零(三組 pattern + 分母 + 正向對照)

| 組 | pattern | 範圍 | 命中 |
|---|---|---|---|
| A 傳輸機制 | `Content-Disposition` \| `toCsv` \| `to_csv` \| `exportCsv` \| `downloadCsv` \| `createObjectURL` \| `attachment;` | `apps`+`packages`(排 `.next`) | **0** |
| B 檔案格式 | `.csv` \| `.xlsx` \| `text/csv` \| `application/vnd.openxml` | 同上 | **0** |
| C 中文 | `匯出` \| `下載` \| `報表` | 🔴 `apps/admin/src`(**與 A/B 不同範圍,見下**) | **20** |

**正向對照**:同範圍同寫法換 `訂單` ⇒ **528 行** ⇒ 掃描範圍不是空的。

⚠️ **第二版更正(B 窗 N1):C 組的掃描範圍與 A/B 不一致** —— A/B 掃 `apps`+`packages`,C 只掃 `apps/admin/src`。
拉齊重跑 `grep -rn "匯出\|下載\|報表" apps packages --include="*.ts" --include="*.tsx" | grep -v .next` ⇒ **59 行**(非 20)。
多出的 39 行**集中在 storefront 的說明書 PDF / 音檔下載 chip**(`InstallResources.tsx:196`、`SoundClips.tsx:48`)
= **靜態資產下載 UI,不是資料匯出** ⇒ **「匯出能力零」的結論存活**。
🔴 但「匯出能力零」是**全系統宣稱**,而我當時只掃了 admin ⇒ **宣稱的作用域大於量測的作用域**。
⚠️ 那 39 行**我與 B 窗都只抽看、未逐行讀完**。

🔴 **C 組那 20 行沒有一行是資料匯出**(這句仍成立),但**我第一版的分類數字是錯的**。

⚠️ **第二版更正(B 窗 MF1;我用 python 逐行取命中關鍵字重判,避免 CJK grep 截斷)**:
**不是 19 / 1,是 16 / 0 / 4**——`匯出` **16 行**(全是 ES module `export` 的中文寫法)、`下載` **0 行**、`報表` **4 行**。
`報表` 那 4 行我第一版只算到 1 行,漏了三行:`staff-create-form.tsx:59`、`staff-table.test.tsx:114`(與 `:42` 同一句文案),
以及 🔴 **`apps/admin/src/lib/orders/cancel-view.ts:473` 逐字「要讓它可見得另立管道(例如**對帳報表**),不在取消 UI 的職責內」**。

🔴🔴 **最後那一行不是雜訊,它是能撐住我 §3 用途②的證據,而我把它當雜訊丟掉了。**
我在 §5-3 自陳「三種用途是我從**一般電商經驗**列的,**不是從 Sean 的話推的**」——
而 `cancel-view.ts:473` **就是 repo 內部對「對帳報表」需求的既有指標**。
⇒ **我示範了「只看數字不看內容」,但反方向的錯也真的發生在同一次判讀裡:我看了內容,卻只在找「證明它是雜訊」的理由。**
**下次逐行判讀時,「有沒有一行其實支持我自己的另一段結論」要當成一個判準。**

## §2 `scripts/rpm-*.ts` 沿用不了(你要我確認的那條)

**確認:它是 DB→DB,不是檔案匯出。** `scripts/rpm-import.ts:4-8` 逐字:
「**來源**(唯讀、絕不寫):報價單 B庫 `dllwkkfanaebrsuyuedy` 乾淨 view `storefront_catalog_v`…
**目標**(寫):pcm-website-v2 `bmpnplmnldofgaohnaok` `products` + `product_variants`」。
⇒ 它的形狀是「跨庫同步 + 一整排安全 gate」,**與「產一個檔案給人下載」沒有共用面**。

🟡 **那個思路對 `#24` 有用,但成本不是零**(第二版更正,B 窗 MF2):
同檔 `:5` 逐字「用 anon publishable key(**讀不到成本/蝦皮/經銷**)」——**用「連讀都讀不到」的憑證**,而不是靠 select 清單自律。
🔴 **但那招的前提是「來源資料本來就不含成本/經銷」,而匯出要的正是 admin 才讀得到的東西**:
admin 走 **service_role**(`apps/admin/src/lib/customers/customer-repository.ts:11-12` 逐字「admin 走 **service_role**…**BYPASSRLS 看全客人**」),
而 `authenticated` 對別人的訂單被 RLS 擋死(`20260604120000:195` 逐字 `USING (customer_user_id = (select auth.uid()))`)
⇒ **沒有一把「弱一級」的現成憑證可以換。**
**要做到「連讀都讀不到」必須新造 DB 物件**(受限角色 / 專用 view)= **migration + Sean apply 停點**,不是換個 key。

## §3 Sean 要匯出來做什麼 —— 三種常見用途,各要的欄不同(給他用選的)

**我答不了這題,以下是把選項攤開,不是推薦。**

| 用途 | 一列代表 | 大概要哪些欄 | 敏感度 |
|---|---|---|---|
| **① 報稅 / 給會計** | 一張訂單 | 訂單編號、日期、金額(小計/運費/總額)、發票號碼與狀態、付款方式 | 中:**不需要客人地址、不需要品項** |
| **② 對帳 / 查帳** | 一張訂單 或 一筆收款 | 訂單編號、日期、應收、已收(`order_payments`)、已退(`order_refunds`)、付款管道 | 中:**不需要客人個資** |
| **③ 上架到別的平台 / 商品盤點** | 一個商品變體 | 料號、品名、規格、售價、品牌、供應商 | 🔴 **高:一不小心就帶到 `price_store`(經銷價)與 `supplier_slug`** |

🟡 **③ 分得掉,①② 不一定**(第二版更正,B 窗 N2):③ 是**商品**粒度、確實是另一張表;
但 ②「一列代表」那格我自己就寫著「**一張訂單 或 一筆收款**」⇒ **② 可以是訂單粒度**,而 ① 也是訂單粒度
⇒ **①② 可能只差欄集、合得起來**,不是必然兩張表。
🔴 **我原本寫「三張不同的表」會讓片界被切成三片;若 ①② 合得起來,那是兩片。**
**這題答完之前,片界畫不出來。** 建議排進 Sean 的批次題。

## §4 🔴 鐵則 12:匯出 = 資料離開系統

⚠️ **第二版更正(B 窗 MF3):我原本的小標寫「**已經有一整條防線可以抄**」= 過度樂觀,已拿掉。**
下面三層**沒有一層對「產檔案」這個新出口自動成立**:

- ① `product_snapshot` 白名單守的是**那一欄的內容**;匯出若直接 join `products` / `product_variants` 取欄,**它不在路徑上**。
- ② 「禁 `select('*')`」是**單一 adapter 內的慣例,不是機制** —— 新匯出路徑可以自由 `select('*')`,沒有東西擋。
- ③ 🔴 **forbidden-token 那格的盲區,就寫在它上方兩行**:`SupabaseOrderAdapter.ts:320-321` 逐字
  「leak-guard 那道掃的是原始碼**全文**…**它的盲區是掃描根之外(`packages/`)**」——
  而匯出的 select **很可能就放在 `packages/`**(要共用)⇒ **正落在明文盲區**。**我引了那格、卻沒把它自己寫的盲區一起帶出來。**
- 🔴 **而且三層守的都是「查詢選了哪些欄」,沒有一層守檔案本身**(下載後被轉寄 / 留存)。

⇒ **正確講法**:有**可借鑑的前例**,但**沒有一條現成防線覆蓋匯出**;要覆蓋得自己造(見 §2 末的受限角色 / 專用 view)。

**既有前例(不是我發明的,是實查到的;可借鑑 ≠ 可直接抄)**:

1. **`order_items.product_snapshot` 是白名單**:`20260604120000:170` 逐字「🔴 鐵則 12 經銷價零滲入:逐欄白名單 `title/sku/spec`…DB CHECK `order_items_snapshot_whitelist` **縱深拒經銷價鍵**」;
   同檔 `:169` 逐字「**無 `price_store`/`price_by_tier`/`cost`**」。
2. **顯示投影明文挑欄、不用 `select('*')`**:`SupabaseOrderAdapter.ts:231` 逐字「鐵則 12:**仍禁** `select('*')`、零成本欄」。
3. 🔴 **已經有一格 forbidden-token 測試**:`SupabaseOrderAdapter.ts:327` 逐字對
   `price_store / price_by_tier / price_general / cost / address_id …` **逐字比對零碰撞**。
   ⚠️ **但擴用它之前要先解掉 ③ 那個盲區** —— 若匯出的 select 放在 `packages/`,擴了也掃不到。

**我判定不該出現在匯出檔裡的欄(逐欄附理由)**:

| 欄 | 為什麼不該出去 |
|---|---|
| `price_store` / `price_by_tier` / `cost` | 經銷價與成本 —— 鐵則 12①,而且 `product_variants` **真的有 `price_store`**(`database.types.ts` 該表 Row 內) |
| `tappay_rec_trade_id` | 金流交易識別碼;既有列表投影 `ADMIN_ORDER_LIST_SELECT`(`SupabaseOrderAdapter.ts:93`)**本來就沒有它** |
| `shipping_address_snapshot` / `notification_email` / `customers.phone` | 客人個資;用途①②都不需要 |
| `invoice`(Json) | 內含統編 / 載具號碼 |
| `customer_user_id` / `cart_session_id` | 內部識別碼,外流無益 |
| `supplier_slug` | ⚠️ **視用途而定**:用途③盤點時員工可能真的要看,但**匯出檔會被轉寄** ⇒ 這格要 Sean 拍 |

⇒ **鐵則 12 逐條**:①錢 = **是**(匯出檔含金額,雖只讀不寫,但**資料離開系統**這件事本身就是 12 的精神);
②權限 = **是**(新路由要在 SSO 保護面內、且要決定誰能匯出);③schema = 若走既有 view 則否;
④平台設定 = 若引入 CSV/xlsx 套件則**是**;⑤對外不可回收 = 🔴 **是 —— 檔案一旦下載就收不回來**;⑥`packages/ui` = 否。
⇒ **命中 ①②⑤(至少)⇒ codex 關卡2 不降級、要提 plan 等 Sean 批。**

## §5 誠實缺口

1. **未對正式庫查詢。** §4 的 CHECK 與欄位存在性都只是 repo 檔案這樣寫。
2. **我沒有讀 `SupabaseOrderAdapter.ts:327` 那格測試的本體**,只讀了它上方的 docstring
   ⇒ 「可以擴用」是**基於註解的推論**,不是我確認過那格的形狀真的擴得動。
3. **§3 的三種用途是我從一般電商經驗列的,不是從 Sean 的話推的。** 他真正要的可能是第四種,
   也可能只是「把訂單列表存下來備份」。**這張表的用途是讓他快速選,不是我替他縮範圍。**
4. **CSV 的中文編碼與 Excel 相容性我沒查**(BOM / 逗號 / 換行逃逸)—— 那會決定檔案打開是不是亂碼,是實作面的已知坑。
6. 🔴 **第二版新增(B 窗 N3):全篇零提及 `#10 列印出貨單/揀貨單`。**
   它與 `#24` 共用「**把資料變成一個檔案給人**」的機制與選型(CSV/PDF 套件、中文編碼、下載傳輸)
   ⇒ **選型時該一起看**,否則 `#24` 選的套件到 `#10` 可能要重選一次。(不是要我做 `#10`,是別讓兩片各選各的。)
7. 🔴 **第二版新增的自曝**:MF1 那條 —— 我逐行看了 20 行、**卻把唯一能撐住我自己 §3 用途②的那行當成雜訊丟掉**
   (`cancel-view.ts:473` 的「對帳報表」)。**我在找「證明它是雜訊」的理由,不是在找證據。**
   ⇒ 逐行判讀時要多問一句:**「有沒有一行其實支持我自己的另一段結論?」**
5. **沒查匯出量級**:一次匯多少列、要不要分頁或串流。若訂單量是每月 100-300 筆(memory 記的量級),
   **很可能一次全撈就夠**,但我沒數過正式庫。
