# B3 slice 規格 · session payload 帶身分(`sub` + 版本欄 `v:2`)

> **狀態:規格,尚未實作。** plan v4 已由 Sean 批(鐵則 8)。
> **母 plan**:`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md` §4 B3。
> 寫於 2026-08-16 · B 窗。**每一個宣稱都附 `檔案:行號` 或量法。**
>
> 🔴 **本片動的是【兩個 repo 的 session 型別】,不動 DB。** 前片 B1/B2 動 DB、本片不動。

---

## §1 這片要做什麼(一句)

讓 session cookie **帶得動「這是誰」** —— 現在它只帶「有人通過了那道密碼」。

---

## §2 現況(2026-08-16 實查,逐條附行號)

### 2.1 兩邊的 payload 型別

| 在哪 | 型別 | `檔案:行號` |
|---|---|---|
| 報價單(簽發端) | `{ v:1, sid, iat, exp, amr[], ver }` | `~/API大量上架/PCM報價單-V2` `lib/session.ts:13-20`(origin/main) |
| admin(收端) | `{ v:1, sid, iat, exp, amr[], auth_time }` | `apps/admin/src/lib/session/session.ts:20-30` |

🔴 **兩邊都沒有任何欄位在講「誰」。** 這正是本線要補的洞。

### 2.2 ✅ **plan v4 §8 依賴的那個機制,已實查證實**

plan v4 §8 寫:「**靠版本欄 `v:2` 擋(舊 validator 見 v=2 直接 reject)**,
不靠『舊 validator 會 reject』這個錯誤假設。」

**兩邊的 validator 逐字都是 `if (o.v !== 1) return false;`**:
- 報價單 `lib/session.ts:110`
- admin `apps/admin/src/lib/session/session.ts:110`

⇒ **`v:2` 的 cookie 會被舊 validator【硬拒】,不是被忽略。**
⇒ **plan 那條 rollback 策略成立,而且現在有證據,不是假設。**

### 2.3 ⚠️ 而 plan §8 另一句要**加限定**

plan `:238` 寫「報價單 `isPayload()` **容許額外欄**」——
**那句對,但它只在「不動 `v`」的情況下才是風險**:
`isPayload` 逐欄檢查、**不拒絕未知的 key**(`lib/session.ts:107-118`)
⇒ 若 B3 只加 `sub` 而**不**把 `v` 升到 2,舊 validator 會**靜默忽略 `sub`**,
而 cookie 仍然有效 ⇒ **那正是「靜默退回無身分語意」。**

**⇒ `v:2` 不是可選的,它是唯一擋住這個退化的東西。**
🔴 **本片不得只加欄不升版。**

---

## §3 `sub` 的形狀(plan §7-Q1=A 拍板)

```ts
type SessionSub =
  | { kind: 'user';     staff_id: string }   // 真人登入
  | { kind: 'fallback' };                    // 備援共用密碼登入（Q2=A 保留）
```

🔴 **型別上不存在「沒有 `sub` 的 v:2 session」** —— 這是 plan `§7-Q1=A` 的原話,
它的意思是:**備援路徑不是「沒有身分」,是「一種明確的身分」。**
⇒ 若寫成 `sub?: …`(可選),那個「不存在」就沒有了 ⇒ **必須是必填的 union。**

**`staff_id` 的值域**:與 B1-b 的 CHECK 白名單同一組(`sean` / `staff_1` / `staff_2`)。
⚠️ **兩邊各自硬編碼會漂**(A 庫改了名單、這裡沒改)⇒ **本片要決定一個單一來源**,見 §7-1。

---

## §4 順序:B3 **必須在 B2 之後**

```
B1-b  建映射表（空）
B2    塞三列（Auth user ↔ staff_id）
B3    session 帶 sub          ← 本片
```
**理由**:`sub.staff_id` 的值要從**映射表**查出來。B2 之前那張表是空的
⇒ 登入成功但查不到 staff_id ⇒ **要嘛簽出一個沒有身分的 v:2(型別上不允許)、要嘛整個登入失敗。**
🔴 **兩種都不能接受,所以順序是硬的。**

---

## §5 混版部署(這片最容易出事的地方)

兩個 repo 分開部署 ⇒ **四種組合都會真實發生**:

| 報價單(簽發) | admin(收) | 會發生什麼 | 可接受嗎 |
|---|---|---|---|
| 舊 v:1 | 舊 v:1 | 現況 | ✅ |
| **新 v:2** | **舊 v:1** | admin 的 `o.v !== 1` **硬拒** ⇒ **所有人被登出** | ⚠️ **要規劃,不能撞上** |
| 舊 v:1 | **新** | 新 admin 收到 v:1 ⇒ **必須決定:接受(過渡)還是拒絕** | 見下 |
| 新 | 新 | 目標狀態 | ✅ |

🔴 **決定(本規格拍,不佔 Sean 時間):新 admin 的 validator【同時接受 v:1 與 v:2】,
但 v:1 一律視為「無身分」,而在 `ADMIN_REQUIRE_REAL_IDENTITY` 開關打開之後【拒絕 v:1】。**

**為什麼**:
- 過渡期要能同時活著(否則部署順序稍差就全員登出)
- 而開關打開 = 我們宣告「從現在起身分是必需的」⇒ 那一刻 v:1 就該死
- 📎 這讓 §5 那個「開關開著而 session 沒帶身分」的驗收格(plan §6-6)**有東西可測**

⚠️ **部署順序因此是硬的:先部署 admin(能收兩種),再部署報價單(開始發 v:2)。**
反過來會撞上表格第二列 ⇒ **所有人被登出**。

---

## §6 驗收(負向為主)

| # | 構造 | 預期 |
|---|---|---|
| 1 | 舊 v:1 cookie 送到**新** admin,開關**關** | ✅ 接受,視為無身分 |
| 2 | 舊 v:1 cookie 送到**新** admin,開關**開** | 🔴 **拒絕** |
| 3 | v:2 cookie 送到**舊** admin | 🔴 **拒絕**(這是 rollback 的安全網,要證明它真的擋) |
| 4 | v:2 但 `sub` 欄缺 | 🔴 **拒絕**(型別上不該存在,但要證明 validator 也擋) |
| 5 | v:2 且 `sub.kind='fallback'` | ✅ 接受,而**任何寫入都被擋**(備援唯讀,plan §7-Q1=A) |
| 6 | v:2 且 `sub.staff_id` 不在白名單 | 🔴 **拒絕** |
| 7 | 簽章正確但 `sub` 被竄改 | 🔴 **拒絕**(HMAC 涵蓋整個 payload,這格證明 `sub` 在簽章範圍內) |

🔴 **第 7 格最容易被跳過** —— 它證的不是「HMAC 有效」,是「**`sub` 沒有被放在簽章之外**」。
📎 兩邊的簽章都是 `base64url(payloadJSON) + '.' + base64url(HMAC(payloadJSON))`
(`lib/session.ts:7` 檔頭註解)⇒ 只要 `sub` 在 `payloadJSON` 裡就涵蓋得到。
**而「只要」那兩個字,正是需要一格測試的地方。**

---

## §7 未決 / 未驗

1. 🔴 **`staff_id` 白名單的單一來源未定。** B1-b 的 CHECK 硬編碼一份、B3 的型別會需要另一份
   ⇒ **兩份會漂**。三個候選(本片動手前要選):
   - (a) admin 啟動時從 A 庫 `staff` 讀一次 —— 最準,但多一條啟動期相依
   - (b) 兩邊都不驗值域,只驗「映射表查得到」—— 把值域檢查推給 DB
   - (c) 共用常數檔 —— 最簡單,**但那正是 house 教訓「兩處硬編碼會漂」的形狀**
   ⚠️ **我傾向 (b)** —— 它讓白名單只有一個地方(B1-b 的 CHECK),而不是同步兩份。
   **但這條要在動手前定,不是邊做邊決定。**
2. **`Q-AUTH-1` 甲案前提:②已證、③未證**
   (②=那些帳號有 email 身分與密碼,SQL 查得到;③=Email provider 現在是開的,**SQL 原理上查不到**,
   它住在 GoTrue config 不在 DB)。
3. **本片尚未經對抗審查**(不動 DB ⇒ 不命中鐵則 12③,但動 auth ⇒ 命中②)。
4. **`auth_time` 的語意問題未解**(admin `session.ts:26-27` 自述:它隨 sliding refresh 更新,
   **不是不變的登入時刻**)⇒ 若日後要做 step-up「近期驗證過」,那個欄位形同虛設。
   **本片不解決,但寫進來免得下一個人拿它當登入時刻用。**
