# B3 slice 規格 · session payload 帶身分(`sub` + 版本欄 `v:2`)

> **狀態:規格,尚未實作。** plan v4 已由 Sean 批(鐵則 8)。
> **母 plan**:`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md` §4 B3。
> 寫於 2026-08-16 · B 窗。**每一個宣稱都附 `檔案:行號` 或量法。**
>
> ⛔ ~~🔴 **本片動的是【兩個 repo 的 session 型別】**~~
> 🔴🔴 **2026-08-18 關卡1 更正(codex must-fix):檔頭與後文/母 plan 互相矛盾。**
> **本片只動【報價單】那一端。** admin 收端(payload 型別 / callback / `actor.ts`)**歸 B5**
> —— 母 plan `§4` 那一列逐字把 admin 放在 B5,而本檔 §6 的第 1-3 格早在 2026-08-17 就搬去 B5 spec 了。
> ⚠️ **照舊檔頭施工的人會提前改 admin ⇒ 跨過 B4/B5 兩個 checkpoint。**
> **不動 DB**(前片 B1/B2 動 DB、本片不動)這半仍然成立。

---

## §1 這片要做什麼(一句)

讓 session cookie **帶得動「這是誰」** —— 現在它只帶「有人通過了那道密碼」。

---

## §2 現況(2026-08-16 實查,逐條附行號)

### 2.1 兩邊的 payload 型別

| 在哪 | 型別 | `檔案:行號` |
|---|---|---|
| 報價單(簽發端) | `{ v:1, sid, iat, exp, amr[], ver }` | `~/API大量上架/PCM報價單-V2` `lib/session.ts:13-20`(origin/main) |
| admin(收端) | `{ v:1, sid, iat, exp, amr[], auth_time }` | `apps/admin/src/lib/session/session.ts:20-30` |

🔴 **兩邊都沒有任何欄位在講「誰」。** 這正是本線要補的洞。

### 2.2 ✅ **plan v4 §8 依賴的那個機制,已實查證實**

plan v4 §8 寫:「**靠版本欄 `v:2` 擋(舊 validator 見 v=2 直接 reject)**,
不靠『舊 validator 會 reject』這個錯誤假設。」

**兩邊的 validator 逐字都是 `if (o.v !== 1) return false;`**:
- 報價單 `lib/session.ts:110`
- admin `apps/admin/src/lib/session/session.ts:110`

⇒ **`v:2` 的 cookie 會被舊 validator【硬拒】,不是被忽略。**
⇒ **plan 那條 rollback 策略成立,而且現在有證據,不是假設。**

> 🔴🔴 **範圍限定(2026-08-17 補;不補的話這段會被再套錯一次)**:
> 上面那個事實成立的邊界是「**同一個 repo 內:誰簽的誰驗**」。
> **兩邊各有一個 `o.v !== 1`,不代表它們會互相檢查對方** ——
> admin 驗的是 admin 自己簽的 cookie,報價單驗的是報價單自己簽的 cookie。
> 跨 repo 傳的是 `{ ok, amr[], auth_time }` 這份 JSON(`apps/admin/src/lib/sso/exchange.ts:7`),**裡面沒有 `v`**。
> ⇒ **原 §5 就是把這一段讀成跨 repo 耦合而整張表寫錯的**(V 窗 `F-1`)。詳見 §5 檔頭那個 ⛔ 區塊。

### 2.3 ⚠️ 而 plan §8 另一句要**加限定**

plan `:238` 寫「報價單 `isPayload()` **容許額外欄**」——
**那句對,但它只在「不動 `v`」的情況下才是風險**:
`isPayload` 逐欄檢查、**不拒絕未知的 key**(`lib/session.ts:107-118`)
⇒ 若 B3 只加 `sub` 而**不**把 `v` 升到 2,舊 validator 會**靜默忽略 `sub`**,
而 cookie 仍然有效 ⇒ **那正是「靜默退回無身分語意」。**

**⇒ `v:2` 不是可選的,它是唯一擋住這個退化的東西。**
🔴 **本片不得只加欄不升版。**

---

## §3 `sub` 的形狀(plan §7-Q1=A 拍板)

```ts
type SessionSub =
  | { kind: 'user';     staff_id: string }   // 真人登入
  | { kind: 'fallback' }                     // 備援共用密碼登入（Q2=A 保留）
  | { kind: 'bootstrap' };                   // 🆕 首次建置（SETUP_SECRET）—— 見下方紅框
```
🔴🔴 **第三變體 `bootstrap` 是 2026-08-18 加的,而它【不是為了語義好看,是為了不卡死】**
**(GR-021 擊破 → 主視窗 MAIN-048 裁定;標【主視窗代裁】,Sean 可推翻)**:
```
原本要把 :267（SETUP_SECRET 那條）判成 fallback。看起來對 —— SETUP_SECRET 就是共用密碼。
🔴 而 fallback 不是一個標籤，它是【三張執法矩陣的鍵】：
     B5:290    備援 ⇒ 寫入一律擋
     B5:298    寫入閘
     B6-B7:45  備援唯讀橫幅
   （⚠️ 這三處的行號是 GR 報的，我【沒有開檔核】⇒ 標轉述）
而 bootstrap 簽完 session 的【下一步就是兩筆寫入】——【這兩行我自己開檔核過】：
     ~/API大量上架/PCM報價單-V2 app/api/admin/2fa/enroll/confirm/route.ts
       :62-63  .from('totp_devices').update({ status:'active', activated_at: … })
       :83-84  .from('recovery_codes').insert(…)
     量法 awk 'NR>=55 && NR<=92 {if ($0 ~ /from\(|rpc\(|insert|update|signSession/) print NR": "$0}'
⇒ 執法蓋齊那天，【首次建置會卡死在綁定步】。延遲引爆，裁定當下零東西紅。
```
🔴 **為什麼是新變體、不是「fallback 加一份豁免清單」(主視窗理由,最重的那條)**:
`fallback` 若同時代表「日常備援登入」與「首次建置」,那就是**一個旗標兩個世界** ——
而我們**今天剛為了同一個形狀批准 `#646`**。**在 A 處認定那是病、在 B 處自己造一個,說不過去。**

🔴🔴 **而【只加這個變體不夠】(主視窗 2026-08-18 自己開 `B5:288-300` 核出來的,MAIN-048 已補)**:
```
B5 同節「三條硬規定」第 3 條逐字：
  「寫入閘不接受【沒有具名 staff】的任何形狀 —— fallback 的寫入攔截點必須是寫入端自己」
🔴 那條規則的鍵【不是 kind==='fallback'】，是【有沒有具名 staff】
⇒ { kind:'bootstrap' } 也沒有 staff_id ⇒ 它照樣被擋
⇒ 光加一個 union 變體，bootstrap 那兩筆綁定寫入【還是會卡死】
```
**⇒ 必須兩件一起做**:
```
① 加 { kind:'bootstrap' } 變體                    ← 本檔（B3）做，它讓「例外」有辦法被表達
② B5 §寫入閘 第 3 條改寫成明確列舉：
     具名 staff ⇒ 允許 ／ fallback ⇒ 擋 ／ bootstrap ⇒【僅限 2FA 綁定端點】允許，其餘一律擋
   🔴 那個「僅限」要寫成【端點白名單】，不是「綁定期間」——
      期間是時間狀態，沒有東西量得到；端點是靜態的、grep 得出來
```
⚠️ **② 不是本片** —— `B5` 那份規格由主視窗排,**本檔不動它**,這裡只留指標。
🔴 **在 ② 落地之前,① 單獨上線【不會讓首次建置活過來】** —— 不要把 ① 讀成「這件事解決了」。

📎 **這一格連著被打了兩次,兩次的形狀值得記(主視窗與我各一次)**:
```
第一次：論證了 fallback 這個標籤的【語義】對不對，沒問它【會觸發什麼】
第二次：改用新變體閃開那個觸發，卻沒讀那條規則【真正的鍵是什麼】
⇒ 兩個人、同一天、同一格 —— 主視窗逐字：「這不是個人失誤，是這條線的形狀。」
```

📌 **行為說明(是行為,不是缺陷,而要寫下來)**:
bootstrap 完成綁定之後,`2fa/enroll/confirm:89` 重簽出來的 session **仍然是 `bootstrap`**
⇒ **創始管理員要【登出重登】才拿得到具名身分。**

⛔ **本節第一版的理由要跟著作廢**(主視窗 MAIN-048 自述):
~~「`SETUP_SECRET` 就是共用密碼,而 `fallback` 的定義逐字就是備援共用密碼登入
⇒ 貼上 fallback 是把事實寫進稽核軌跡」~~
🔴 那段**論證的是這個標籤【語義】對不對,沒問這個標籤【會觸發什麼】** ——
**標籤與開關是同一個欄位,而它只看了作為標籤的那一半。**
📎 **我照抄了那段理由進規格,所以它是我的錯誤的一半** ——
我把「語義正確」當成了「可以用」,而我在同一小時裡才剛因為沒量就下全稱句被抓過一次。
**兩次是同一族:我檢查了「這個說法對不對」,沒檢查「照它做會發生什麼」。**

🔴 **型別上不存在「沒有 `sub` 的 v:2 session」** —— 這是 plan `§7-Q1=A` 的原話,
它的意思是:**備援路徑不是「沒有身分」,是「一種明確的身分」。**
⇒ 若寫成 `sub?: …`(可選),那個「不存在」就沒有了 ⇒ **必須是必填的 union。**

**`staff_id` 的值域**:與 B1-b 的 CHECK 白名單同一組(`sean` / `staff_1` / `staff_2`)。
⚠️ **兩邊各自硬編碼會漂**(A 庫改了名單、這裡沒改)⇒ **本片要決定一個單一來源**,見 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1(2026-08-17 搬走,原寫「見 §7-1」已是跨檔)。
🔴🔴 **「單一來源」≠「一份共用清單」(codex R3 `must-fix`,2026-08-17 補;不補的話這句會把人導向相反實作)**:
**`docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1 已明文拒絕 (c) 共用常數檔** —— 定案是**每個問題各自指向【它自己的權威】**
(綁定 → 報價單庫 CHECK;在職 → A 庫 `is_active`),**不是把兩張清單合併成一份放在中間**。
⇒ 讀到「單一來源」四個字要接著讀下面那張表,**不要去建一個新的共用檔**。

> 🔴🔴 **「白名單」這三個字在本檔指兩張不同的清單,不要當成同一張(codex `#9`,2026-08-17 折)**
> codex 逐字:驗收要求拒絕白名單外 `staff_id`,後文卻禁止 admin 持有白名單
> ⇒ **施工者只能複製 DB CHECK 或無法完成驗收,執行位置未定。**
>
> | 這張清單 | 是什麼 | 權威在哪 | **誰執行、在哪一刻** |
> |---|---|---|---|
> | **綁定白名單** | 誰**可以被綁定**成 `staff_id` | 報價單庫 `admin_user_staff_map` 的 **CHECK**(B1-b) | 🔴 **寫入映射列的那一刻**(該表的 `INSERT` / `UPDATE`)—— **不是簽發 session 的那一刻** |
> | **在職名單** | 誰**現在可以操作** | **A 庫 `staff` 表的 `is_active`** | **收端(B5),每次用的時候**,走既有 `resolveStaff()`(`docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1) |
>
> 🔴 **admin 端【永遠不持有】綁定白名單** —— 它拿到 `sub.staff_id` 之後做的是 `resolveStaff()`,
> 那查的是**在職名單**,不是綁定白名單。**兩張清單各自有權威,不互相抄** ⇒ 這正是 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1 拒絕 (c) 共用常數檔的理由。
>
> ⛔⛔ **本表 2026-08-17 第一版把執行時機寫錯,同日修正(codex R2 `must-fix`)**
> 原寫「綁定白名單在**報價單側、簽發 v:2 的那一刻**執行」—— **不成立**:
> 🔴 **簽發時做的是 `SELECT`,而 `SELECT` 不會重跑 `CHECK`。**
> `CHECK` 只在**那一列被寫進去**的時候擋;簽發端讀到什麼,取決於那一列**當初**寫進了什麼。
> ⇒ **簽發端該做的是**:依**已驗證的 `auth_user_id`** 精確查出 `staff_id`;
>   **查無 / 不是恰一筆 ⇒ 該次登入失敗**(§4)。**它不比對任何清單。**
>
> 🔴🔴 **而這留下一個【未決】,我不假裝它被折掉了**:
> **§6 第 6 格要驗的「`staff_id` 不在綁定白名單 ⇒ 拒絕」,在 runtime 沒有執行者。**
> 若 `CHECK` 成立,那個狀態**構造不出來**(⇒ 該格恆綠、零判別力);
> 若要讓它在 runtime 真的擋(防 `CHECK` 漂移 / 繞過 / 直接改庫),**就必須另定一道應用層驗法**,
> **而那道驗法需要一份清單 —— 等於把 (c) 共用常數檔那個「兩份會漂」的病請回來。**
> ⇒ **這是取捨,不是漏寫;歸 plan 層決定,`#9` 記為【未折】。**

---

## §3.9 🔴🔴 **B2 ↔ B3 的介面契約 —— 本節是【唯一正本】**

> **2026-08-18 關卡1 R2 之後改成單一正本(主視窗裁定)。**
> ⛔ ~~原本的做法是「兩份規格各存一份逐字相同的段落 + 改一邊要同時改另一邊」~~
> 🔴 **那個做法在【第一次落檔就漂了】** —— 我用手打了第二遍,而機械比對顯示**兩邊每一行都不同**
> (主詞、換行、以及 B3 那邊多出來的一句)。
> 📎 **判別句(主視窗落交接)**:**能消除重複就不要去偵測不一致。**
> ⇒ **`docs/specs/2026-08-18-m4b-e8b-b2-spec.md` 那邊只留一行指標指到這裡,沒有第二份可以漂。**

### 契約本體

```
B2 的登入處理器：驗完密碼 → 查映射拿 staff_id → 在同一個函式作用域裡算出 sub
                 ⇒ sub 是區域變數：不進儲存、不進 cookie、不進 DB
B3 的改動      ：issueSession() 多收一個參數 sub: SessionSub（🔴 必填，不是選填）
                 四個呼叫點的值怎麼決定 ⇒ 見下方那張表（:242 依 account／:209:221 拒或 fallback／:267 bootstrap）
🔴 B3 上線之前：B2 算出來的那個值就丟掉 —— 刻意的，不是漏接
              （它讓 B2 可以單獨上線而零行為改變）
🔴 B3 不得自己再查一次映射表 —— 查兩次 = 兩個地方各自可能查到不同的東西
```

### 🔴 `sub` 這個參數:**必填 `SessionSub`,而它的值由【既有那條規則】決定**

> ✅ **2026-08-18T18:5x 暫停解除** —— 主視窗 `MAIN-048` 已裁定(`~/pcm-mailbox/MAIN-048-…`)。
> **停過的是 `:267` 那一格**(GR-021 擊破 `fallback` 是執法矩陣的鍵)⇒ 改判 **`{kind:'bootstrap'}`**,見 §3 紅框。
> **`:242` / `:209` / `:221` 依 `account` 判、型別刪掉 `| 'none'` 這三點從頭到尾沒被停過。**

**既有那條規則(`docs/specs/2026-08-18-m4b-e8b-b2-spec.md:63-64` 逐字,B2 已拍板)**:
```
account 留空  ⇒ 走【現行這一整條路】，一行都不動 ⇒ sub = { kind: 'fallback' }
account 有值  ⇒ 走【新的個人帳號路】                ⇒ sub = { kind: 'user', staff_id }
```
**⇒ B3 的規定:四個呼叫點各自釘死,而【每一格都指回一份既有拍板】,不自己發明。**
⛔ ~~四個呼叫點【同一條規則】~~ —— 那是 18:4x 的版本,已被 B2 `D-2`(`:209`/`:221`)與
GR-021(`:267`)各打掉一格。**留這行是因為它曾經寫進規格,不是因為它還成立。**

> ✅🔴 **2026-08-18T19:1x 定案:`:209` / `:221` 走【拒絕】那條,不是「依 `account` 判」。**
> 主視窗**自己開檔核過** B2 `:475` / `:479` / `:484` 之後**整條撤回 `Q-B2-2′`,裁定甲維持**。
> 訃聞、撤回理由與拒絕文案**只寫在本節末**(不在這裡重述,免得長出第二份真相)。

**先數清楚(當場量,量法附在旁邊)**:
```
量法（🔴 2026-08-18T17:3x 換成【全 repo、無 pathspec】的尺重量一次，理由見本節末）：
  cd ~/API大量上架/PCM報價單-V2 && git grep -n 'issueSession' -- .
當場量到【四個呼叫點】（2026-08-18T17:3x 複量，`main` `HEAD = 1149e05`；另有 :116 是函式定義本身）：
  :209  ['pwd','recovery']  ← 備用碼登入
  :221  ['pwd','totp']      ← TOTP 第二段
  :242  ['pwd']             ← password-only
  :267  ['pwd','bootstrap'] ← bootstrap（SETUP_SECRET）
```

| 呼叫點 | `sub` 怎麼決定 | 依據 |
|---|---|---|
| `:242` | 依 `account` 有無 | B2 `:63-64` |
| `:209` | `account` **有值 ⇒ 🔴 拒絕**;**留空 ⇒ `{kind:'fallback'}`** | B2 `D-2` **裁定甲**(具名登入只走單段)⇒ 走到這裡的必然是備援登入;拒絕文案見下方訃聞區塊 |
| `:221` | 同上 | 同上 |
| **`:267`** | 🔴 **一律 `{kind:'bootstrap'}`,`account` 帶了也忽略** | **本片的例外**;為什麼不是 `fallback` 見 §3 紅框(執法矩陣);為什麼不能依 `account` 見下 |

🔴🔴 **`:267` 是唯一需要自己一條規則的點,而我一開始寫錯了**:
我原句寫「`:267` 走 `SETUP_SECRET`、**結構上不可能有 `account`** ⇒ 自然落在 fallback」。
**主視窗要我附量法(那是一句全稱句而旁邊沒有尺)。量完:那句是錯的。**
```
量法：cd ~/API大量上架/PCM報價單-V2 &&
      awk 'NR>=155 && NR<=270 {if ($0 ~ /if *\(|return /) printf "%4d: %s\n", NR, $0}' \
        app/api/admin/login/route.ts
⇒ 從 :155（body 解析）到 :267 之間【零個分支檢查 account】。
   帶 account 的請求走到 :267 的完整路徑（2026-08-18T18:3x，HEAD 1149e05，逐行開檔核過）：
     :155 hasSecondFactor = Boolean(code)||Boolean(recoveryCode) ⇒ 只帶 setupSecret ⇒ false
     :179 if (hasSecondFactor)  ⇒ 跳過
     :239 if (!require2fa)      ⇒ require2fa=true ⇒ 跳過
     :253 if (active >= 1)      ⇒ active==0 ⇒ 跳過
     :262 if (setup && provSetup && ctEqual(...)) ⇒ 進入 ⇒ :267
```
⇒ **`{ account:'sean', password:<對>, setupSecret:<對> }` 走得到 `:267`。**

🔴🔴 **所以 `:267` 的兩個「自然」候選【都不能用】,而這是本片最貴的一格**:
```
候選 A：依 account 判 ⇒ {kind:'user'}
  ✗ 一次【共用密碼建置】會掛上一個真人的名字 —— 稽核軌跡會說某個人做了初始建置
候選 B：一律 {kind:'fallback'}
  ✗ fallback 是三張執法矩陣的【鍵】，而 bootstrap 下一步就是兩筆寫入
    ⇒ 執法蓋齊那天首次建置卡死在綁定步（GR-021，§3 紅框有量法）
⇒ 兩個都錯 ⇒ 必須是【第三變體】{kind:'bootstrap'}（MAIN-048 裁定）
```
📎 **這一格值得記的不是答案,是形狀**:兩個候選各自都有一段**讀起來完全合理**的理由,
而**打死它們的東西都不在「這個標籤語義對不對」那一層** ——
一個在**稽核軌跡**,一個在**執法矩陣**。
🔴 **判別句:一個 enum 值如果被拿去當【開關】用,那它就不只是標籤 ——
換掉它之前要先數【誰在讀它】,不是只問【它的名字對不對】。**

**⇒ 型別**:
```
issueSession(res, amr, expected, sub)   // 第四個參數【必填】
   sub: SessionSub        ← 不是選填、不是 union 加一個 'none'
🔴 沒有「沒有身分」這種 session —— 備援【是一種身分】（§3、plan §7-Q1=A 原話）
```

⚠️ **量法的證據等級(主視窗要求的寫法)**:上面寫的是「**當場量到四個,量法是 X**」,
**不是「有四個」** —— 因為我今天已經把「一個」更正成「兩個」而真值是**三個**(見 §3.10)。
**一個更正過的數字,讀起來比沒更正過的更可信,而它一樣可能是錯的。**
🔴 **而上面那把尺已經換過**:原本寫的是 `grep -n "issueSession(" app/api/admin/login/route.ts`(**只掃一個檔**)。
**它這次剛好也是四個** —— 而那是運氣,不是方法對:**同樣的窄尺正是 §3.10 漏掉 `middleware.ts` 的原因**。
⇒ **本檔起,凡是數「呼叫點有幾個」,尺一律是 `git grep -n '<名字>' -- .`(repo 根、無 pathspec)。**
⚠️ **不要在管線後面接 `grep -v`** —— 這台機器上的 `grep` 是 shell function(`type grep` 當場答得出來),
**人手打的與 `#!/bin/bash` 腳本裡的是兩支不同程式**,同字串同樹會得到不同的數而兩邊都不報錯。

⛔⛔ **這裡翻過一次,留訃聞不靜默刪(主視窗要求)**:
~~`Q-B2-2` 原裁「具名登入只走單段」作廢 ⇒ `Q-B2-2′` = 具名登入【支援】既有的第二段~~
🔴 **`Q-B2-2′` 於 2026-08-18T18:5x 整條撤回,`Q-B2-2` 裁定甲維持**
(主視窗**自己開檔核過** B2 `:475` / `:479` / `:484` 之後撤的)。

**撤回理由 = 把【事實】當成【決定】**:
```
G5 量到的  ：:209 / :221 在程式裡活著、閘是 hasSecondFactor、不看 require_2fa   ← 事實
裁定甲說的 ：【具名】登入不走第二段                                             ← 決定
⇒ 兩者不衝突。一條路存在，不代表具名登入會走它。
```
📎 **判別句(已落 memory `feedback_a-fact-cannot-overturn-a-decision`)**:
**「程式裡有這條路」與「我們決定讓誰走這條路」是兩件事 —— 拿前者去推翻後者,是把【事實】當成【決定】。**
🔴 **而更貴的是另一半**:**提供那個事實的人(我)看著它被拿去推翻一個決定,沒有出聲。**
**量測的人最有機會知道自己的量測撐不撐得起那個結論。**
📎 我當初引 B2 `:78-85`(每段都要帶同一個 `account`)當依據 ——
**那條規則在同一份檔的後面(`D-2`)已經被撤掉了,而我沒讀到那一段。**

**⇒ `:209` / `:221` 的拒絕形狀,照 B2 `D-2` 甲(不得只寫「拒絕」)**:
```
為什麼：這個帳號需要第二階段驗證，而個人帳號登入這一期不支援第二階段
怎麼辦：改用共用密碼那條備援路（Q2=A 全程保留），並回報給 Sean
🔴 不得靜默降級成備援 —— D-2 自己的判別句逐字：
   「把壞掉的具名請求變成【成功的備援登入】＝把一個錯誤降級成一個【權限狀態】」
```
**B2 `D-2` 那份裁定是正本,本檔不重述它的內容,只指過去。**

### 🔴 加變體的紀律(主視窗 2026-08-18;GR 打掉「加變體 ⇒ 消費端被迫決定」那個假設)

**加一個 union 變體【不會】自動讓每個執法點在編譯期被迫決定** ——
消費端照樣可以寫 `if (kind === 'fallback')`,而 `bootstrap` 就這樣漏過去。
```
⇒ 紀律（寫進本規格，不是口頭）：每一個讀 sub.kind 的執法點，一律寫成
     switch (sub.kind) {
       case 'user': … case 'fallback': … case 'bootstrap': …
       default: assertNever(sub);
     }
   🔴 沒有 exhaustive switch + assertNever 的執法點，不算完成
```
⚠️ **真實成本(GR 量的,主視窗轉,我沒重跑 ⇒ 標轉述)**:**每張執法表都要多一列** ——
`ACTOR_COOKIE` 規定 2、`§7.1` 讀取/`resolveStaff` 欄、驗收格 6-8 鏡像、B6-B7 橫幅、B4 `CHECK` 值域。
📎 **主視窗逐字:「我不再說『加變體很便宜』,那份清單就是交辦單。」**
**B5 那幾格由主視窗排,本檔只折 B3/B4 側。**

---

## §3.10 🔴 **簽發點:當場量到【三個】**(而我上一輪把「一個」更正成「兩個」)

**量法(2026-08-18T17:3x 複量,報價單 `main` `HEAD = 1149e05`)**:
```
cd ~/API大量上架/PCM報價單-V2 && git grep -n 'signSession' -- .
⚠️ 🔴 上一版寫的是 `git grep -n "signSession(" | grep -v '\.test\.'` ——
   **不要在管線後面接 `grep -v`**：這台機器上的 `grep` 是 shell function（`type grep` 當場答得出來），
   人手打的與 `#!/bin/bash` 腳本裡的是兩支不同程式 ⇒ 同字串同樹會得到不同的數，而兩邊都不報錯。
   ⇒ 改成不接管線、自己看那 9 行。
⇒ 當場量到【9 行】，其中【三個是呼叫點】（其餘：3 行 import、1 行 lib/session.ts:95 定義本身、2 行在 docs/ 裡）：
   app/api/admin/login/route.ts:120            ← 登入（四條路徑都經過它，見 §3.9）
   app/api/admin/2fa/enroll/confirm/route.ts:89 ← 2FA 綁定確認後重簽
   🔴 middleware.ts:74                          ← sliding refresh（我上一輪【漏了這支根層檔】）
```
🔴 **證據等級**:這是「**當場量到三個,量法如上**」,**不是「有三個」** ——
**我上一輪把「一個」更正成「兩個」,而真值是三個。一個更正過的數字一樣可能是錯的。**
📎 我漏掉它的原因很機械:我的 `git grep` 加了 `-- lib app`,而 **`middleware.ts` 在 repo 根層**。

### 三個點各自的規定

| 點 | 現行行為 | B3 的規定 |
|---|---|---|
| `login:120` | `buildPayload(amr, tokenVersion)` | 照 §3.9 收 `sub`(**必填 `SessionSub`**;四個呼叫點的值怎麼決定見 §3.9 那張表) |
| `2fa/enroll/confirm:89` | `buildPayload(['pwd','totp'], tokenVersion)` **重新造一個 payload** | 🔴 **必須原封沿用原 session 的 `sub`**;**禁止憑空補一個** —— 那等於用沒經過密碼驗證的路徑產生身分 |
| `middleware.ts:74` | `signSession({ ...payload, iat, exp })` **展開既有 payload** | ✅ **今天就會保留 `sub`**(展開寫法的直接後果)⇒ **本片不改它**,而**規格必須點名它** |

⚠️ **middleware 那一列的誠實界線**:codex 用的是條件句(「**若**它重建 payload 丟掉 `sub`」)。
**我開檔核過:它現在是 `{...payload}` 的展開,所以今天不會掉。**
⇒ **它是「規格要點名的東西」,不是「現在會壞的東西」** —— 而點名的理由是:
**哪天有人把它改成 `buildPayload(...)`,`sub` 會靜默消失,而所有驗收格照樣綠。**

🔴 **本表三列各自對應的驗收格(2026-08-18 R2 must-fix 折入 —— 原本三列都只有【規定】、沒有【會跑的格】)**:
`login:120` ⇒ §6 第 9 / 10 格;`2fa/enroll/confirm:89` ⇒ §6 第 **11 / 11b** 格;`middleware.ts:74` ⇒ §6 第 **12** 格。
📎 **判別句**:**規定寫在表格裡不會自己變成守門** —— 上一輪就是這樣讓三個簽發點只有一個被驗到。

---

## §3.11 🔴🔴 **每一句關於行為的規格,要標明它描述的是【開關前】還是【開關後】**

> **2026-08-18 關卡1 R2 裁定(主視窗)。** 病根不是那兩句話寫錯,是**規格把一個【有兩種行為的系統】寫成好像只有一種**。

**R2 抓到的那對矛盾**:
```
「新 validator 一律拒 v:1」        ← 這是【開關後】的終態
「無值 ⇒ 簽 v:1（上線前後皆然）」  ← 這是【開關前】的行為
⇒ 兩句都對，而它們描述【兩個不同的世界】；規格沒標，所以兩種施工都能引用它自保
🔴 而「初始就拒 v:1」的後果是：所有還沒重新登入的人【當場被踢出去】
```

**⇒ 本檔起的硬規定(而它適用整條 E8-B 線,不只 B3)**:
```
每一句寫「必須／一律／不得」的行為規格，句首要標其中一個：
  【開關前】 QUOTE_REQUIRE_REAL_IDENTITY 尚未打開的世界（＝今天，以及整個施工期）
  【開關後】 B7 打開之後的終態
  🔴 標之前先確認【是哪一顆開關、管的是哪一個東西】：
     QUOTE_REQUIRE_REAL_IDENTITY  管【legacy 雙讀的兩個接受點】，不管 v:1
     ADMIN_REQUIRE_REAL_IDENTITY  管【admin 端的 v:1】，那是 B5
     報價單端的 v:1 不歸任何開關管 —— 走甲，B3 一部署就拒（§5-A `:323`）
  【兩者皆】 兩個世界都成立（例如：簽章涵蓋 sub、不得補假身分）
🔴 標不出來的那一句 ⇒ 它八成同時在描述兩個世界 ⇒ 拆成兩句
```
⛔ ~~**⇒ 立刻套用在那對矛盾上**~~ 🔴🔴 **2026-08-18 本輪更正:那個套用【主詞放錯了】,而且方向是危險的那一邊。**

**被劃掉的原文(留著,因為它讀起來完全合理)**:
```
⛔ 【開關前】v:1 仍然【被接受】—— 否則沒有人進得去（所有既有 session 都是 v:1）
⛔ 【開關後】v:1 一律拒 —— 那時每個人都已經用個人帳號重新登入過至少一次
⛔ 🔴 兩者之間的切換點 = B7 那一步
```
🔴 **它錯在哪(逐字對得起來的證據,不是我的判斷)**:
```
§5-A `:323` 逐字 「決定:走【甲】—— B3 一部署就簽 v:2、舊 v:1 一律拒、報價單使用者被登出一次」
母 plan `:198` §6-1 逐字 「舊 cookie（本線之前簽的）⇒ 必須失效，不得靜默降級成無身分」
§5-A-2 逐字 「B3（報價單）＝ 直接拒 v:1；B5（admin）＝ 暫時接受 v:1，
             直到 ADMIN_REQUIRE_REAL_IDENTITY 打開才拒」
⇒ 「開關前接受 v:1」是【B5／admin】那一半的行為。我把它寫成了報價單的。
⇒ 而 QUOTE_REQUIRE_REAL_IDENTITY 管的根本不是 v:1，是【legacy 雙讀的兩個接受點】
   （B2 spec §3.3-b：hasValidLegacy() 與 middleware.ts:82-90；B6+B7 spec §3 第 2 步逐字）
```
**⇒ 正確的套用**:
```
【兩者皆】報價單:v:1 從 B3 部署那一刻起一律拒 —— 與開關無關，代價＝使用者被登出一次（已拍板 甲）
【兩者皆】報價單:登入簽出的一律是 v:2 且【一定帶 sub】——「沒有身分的 session」不存在
         （備援＝ {kind:'fallback'} 是一種身分；四個呼叫點的判法見 §3.9）
【開關前】報價單:legacy cookie（ADMIN_COOKIE）的兩個接受點仍然放行
【開關後】報價單:那兩個接受點關閉 ← 這才是 QUOTE_REQUIRE_REAL_IDENTITY 的切換點
【開關前／後】admin 的 v:1 由 ADMIN_REQUIRE_REAL_IDENTITY 分界 —— 那是 B5，不在本片
```
🔴 **這次差一點就把錯的那版寫成驗收格**(一格「開關前 v:1 要接受」)——
**那格若綠,證明的正好是 plan §6-1 禁止的行為**,而它會在正式站上通過。
📎 **成因與 §5-A 那兩次是同一個**:`feedback_wrong-subject-narration-every-word-true` ——
**每個字都對,只是主詞放錯**;這條線同一個病這是第三次。
📎 **抓到它的動作很機械**:給驗收表逐列標世界的時候,標到 3b 那一列**標不下去** ——
**因為表上的答案(拒絕)與我剛寫的規定(接受)互斥。**
⇒ **「標不出來」本身就是訊號**,那正是上面硬規定最後一句寫的東西。
📎 **為什麼要寫成小節而不是改那兩句(主視窗原話)**:**它會再長出第三句。**

---

## §3.12 🔴🔴 **執法是【兩軸】,不是一個鍵**(主視窗 2026-08-18 定稿;前後裁了兩版才到這裡)

```
身分／授權閘   只准 sub.kind   —— 誰可以做這件事（寫入閘、備援唯讀橫幅、resolveStaff 分支）
認證強度閘     只准 amr        —— 這個 session 證明得夠不夠強
               🔴 且必須走【具名 predicate】（例 isFull2faSession），散裝比較要收編
稽核／顯示     兩者皆可
```
📎 **它為什麼是兩軸,一句話**:bootstrap 完成 `confirm` 之後 **`amr` 升級、`sub` 不動**
⇒ **身分沒變、證明變強了** ⇒ 兩個軸各自都是對的。
**若硬指定「唯一執法鍵」,那一刻必定有一個軸講錯話。**

⛔ **這一節的前一版是錯的,留訃聞**:
~~「執法鍵唯一 = `sub.kind`;`amr` 不得出現在任何 branch 的條件式裡」~~
🔴 **照那句字面折,會拆掉四道活著的閘**,而第一個紅的是「**純密碼 session 不得自綁裝置**」
(其中兩道是 codex p3 立的 critical)。**主視窗撤回,我沒有折它。**
📎 **主視窗自述(逐字,已落 memory,本檔不重寫)**:
「我在這條線上裁了五次、被打死三次,**三次都是同一個形狀:我把兩件事收斂成一個鍵**」
——標籤 vs 開關 → 列舉值 vs 性質 → 身分 vs 認證強度。
**收斂讀起來像簡化,而它是在刪掉一個真實存在的維度。**

### 逐處標軸(主視窗要求的動作:不是找缺陷,是替每一處標軸)

**尺(repo 根、無 pathspec、不接管線,2026-08-18T19:2x,`main` `HEAD = 1149e05`)**:
```
cd ~/API大量上架/PCM報價單-V2 && git grep -n 'amr\.\(includes\|length\)\|amr\[0\]' \
  -- 'lib/*.ts' 'middleware.ts' 'app/**/*.ts'
⇒ 6 處
```

| `檔案:行號` | 做什麼 | 軸 | 形狀 |
|---|---|---|---|
| `middleware.ts:62` | `require_2fa` 開 ⇒ 拒純密碼 session | **強度閘** | 🔴 散裝,要收編 |
| `lib/auth-server.ts:59` | 同上,在 `requireAdmin()` 裡 | **強度閘** | 🔴 散裝,要收編 |
| `app/api/admin/login/route.ts:127` | 只有純密碼 session 才可退回 legacy | **強度閘** | 🔴 散裝,要收編 |
| `lib/session.ts:166` | `isFull2faSession()` 本體 | **強度閘** | ✅ 已是具名 predicate |
| `app/api/admin/2fa/enroll/begin/route.ts:45` | `created_by` 寫進 DB | **稽核** | ✅ 散裝可接受(稽核軸) |
| `lib/session.ts:115` | `isPayload()` 陣列非空 | **形狀驗證,不是閘** | ✅ 不受本節管 |

✅ **六處【全部標得出軸】,零個標不出來** —— 依主視窗的判準(「標不出來的那一處才是問題」)
⇒ **本輪零問題**。分母 = 上面那把尺的 6 處,**不是「我看過的地方」**。
⚠️ **射程**:這把尺只掃 `amr.includes` / `amr.length` / `amr[0]` 三種寫法。
**別的比較方式(例如 `JSON.stringify(amr)===` 或先 destructure 再比)掃不到** ⇒ 標未確認。

🔴 **要收編成具名 predicate 的是【三處】,全是強度閘**(`middleware.ts:62` / `auth-server.ts:59` /
`login/route.ts:127`)。⚠️ **而 `login/route.ts:127` 就在 `issueSession()` 裡面 ——
本片正在改那支函式** ⇒ **收編那一處落在 B3 的體積內,另外兩處不是。**

### admin 側(本片不動,寫在這裡是為了不被誤讀成「兩邊都有閘」)
`apps/admin/src/lib/session/session.ts:162` 有一支 `isFull2fa`,而**同行註解逐字**:
「供**未來** step-up slice 判斷,**本 slice 不強制**」⇒ **零執法呼叫端**。
`apps/admin/src/lib/sso/exchange.ts:81-82` 的 `sanitizeAmr` 是**白名單過濾**,不是 branch-on-value。
⇒ **admin 側今天沒有認證強度閘。** 那是 B5 的事,不是本片。

---

## §4 順序:B3 **必須在 B2 之後**

```
B1-b  建映射表（空）
B1-c  塞【兩列】（Auth user ↔ staff_id）  ← ⛔ 原寫「B2 塞三列」兩處都錯：代號是 B1-c 不是 B2、列數是 2 不是 3（2026-08-18 B2 關卡1 R2）
B3    session 帶 sub          ← 本片
```
**理由**:`sub.staff_id` 的值要從**映射表**查出來。B2 之前那張表是空的
⇒ 登入成功但查不到 staff_id ⇒ **要嘛簽出一個沒有身分的 v:2(型別上不允許)、要嘛整個登入失敗。**
⛔ ~~**兩種都不能接受,所以順序是硬的。**~~
🔴 **上面這句 2026-08-17 已修正,不要照它讀** —— 「整個登入失敗」**是正確的 runtime 結果**,
**不能接受的是【營運上停在那個狀態】**。**正解在下面這個框裡,不要跳過。**

> 🔴🔴 **「不能接受」的主詞是【營運狀態】,不是【runtime 行為】(codex `#10`,2026-08-17 折;
> 原句未加刪除線 ⇒ 跳過本框的人會讀到相反結論,codex R3 `must-fix`,同日補刪除線)**
> 原句只寫到這裡就停,而 codex 逐字指出:**「個人密碼驗證成功但映射查無/逾時 ⇒ 正確安全結果就是登入失敗」**
> —— 我把那個**正確結果**寫成「不能接受」,而規格又**沒有禁止**任何替代作法
> ⇒ **照字面做的施工者會去發明一個「比登入失敗好」的行為。**
> ⚠️ 我**沒有窮舉**他會發明出什麼(數不出來、也不假裝數得出來);
> 下面只**明文禁止我想得到的三條**(codex 點名兩條 + fail-open 一條),**這張禁止清單不是窮舉**
> ⇒ **不在清單上的變通,一律回來問,不得自行判定「這條不算」。**
>
> **⇒ 本片的硬規定,拆成兩層,不要混:**
>
> | 層 | 規定 |
> |---|---|
> | **runtime(不可違反)** | 映射**查無 / 逾時 / DB 例外** ⇒ **該次登入失敗,fail-closed**。這是**正確結果,不是待改善項**。 |
> | ↳ **這一列不禁止的事**(codex R2 `nit`) | **同一次請求內的有限次重試(bounded retry)沒有被禁止** —— 本列管的是「**重試用完之後**」:那時**必須失敗**,不得改走別的身分。⇒ **不要把這列讀成「第一次逾時就不准再試」。** |
> | **排程(所以順序是硬的)** | 不希望**營運上**進入「人人登入失敗」那個狀態 ⇒ 所以 **B3 必須排在 B2 之後**。 |
>
> 🔴 **明文禁止(三條,施工者不得自行變通)**:
> ① **不得**在映射查無時改簽 `kind:'fallback'` —— fallback 是**一種身分**(§3),不是「查不到時的退路」;
>    那等於讓「映射壞掉」自動取得備援權限。
> ② **不得**採用 client / 請求端傳入的 `staff_id` —— 它必須來自**報價單側查映射表的結果**。
> ③ **不得**把 DB 逾時/例外當成「暫時放行」 —— 見 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1「兩個世界長得一樣」那段,
>    fail-closed 的代價已經寫在那裡,**代價是知情接受的,不是拿來換 fail-open 的理由**。

---

## §5 混版部署(這片最容易出事的地方)

> ## ⛔⛔ 本節原版**整張表是錯的**,2026-08-17 重寫(V 窗 `F-1`,主視窗已裁;B 窗自己從 source 複驗過才改)
>
> **原表逐字保留**(「為什麼曾經這樣想」比結論值錢):
>
> | 報價單(簽發) | admin(收) | 會發生什麼 | 可接受嗎 |
> |---|---|---|---|
> | 舊 v:1 | 舊 v:1 | 現況 | ✅ |
> | **新 v:2** | **舊 v:1** | admin 的 `o.v !== 1` **硬拒** ⇒ **所有人被登出** | ⚠️ 要規劃 |
> | 舊 v:1 | **新** | 新 admin 收到 v:1 ⇒ 必須決定 | 見下 |
> | 新 | 新 | 目標狀態 | ✅ |
>
> 🔴 **錯在哪:那張表假設「報價單簽的 cookie 會被 admin 收下來驗」。admin 從來沒有收過它。**
> 跨 repo 傳的是**一份 server-to-server 的 JSON**,不是 cookie:
> · `apps/admin/src/lib/sso/exchange.ts:7` 逐字「回 200 `{ ok:true, amr:string[], auth_time:number }`」
>   ⇒ **回應裡沒有 `v`,也沒有 cookie。**
> · `apps/admin/src/app/api/sso/callback/route.ts:69`:`signSession(buildAdminSession(result.amr, result.auth_time))`
>   ⇒ admin 拿到 `amr` + `auth_time` 之後,**自己簽一個全新的 session**。
> · `apps/admin/src/lib/session/session.ts:93`:`return { v: 1, sid: …, amr, auth_time }`
>   ⇒ **那個 `v` 是 admin 自己給的**,不是報價單給的。
> · `apps/admin/src/lib/session/session.ts:112`:`if (o.v !== 1) return false`
>   ⇒ 它驗的是 **admin 自己簽的那個 cookie**。
>
> ⇒ **報價單把它的 payload 升到 `v:2`,admin 一個位元都看不到。「所有人被登出」那件事不會發生。**
> ⚠️ 而 §2.2「兩邊 validator 都是 `o.v !== 1`」那段**本身沒錯**,§2.3 的結論也不受影響
>   ——錯的是把那個事實讀成**跨 repo** 的耦合。**同一個事實,套錯了邊界。**

混版有**兩個軸**,而它們**不是同一件事**:

### 5-A 真正的 `v` 混版面:**同一個 repo 內,瀏覽器裡的舊 cookie vs 新部署的 code**

`v` 從頭到尾是 repo **內部**的事(誰簽的誰驗),所以混版發生在**時間軸**上,不是兩個 repo 之間。

> 🔴🔴 **主詞:B3 這一片動的是【報價單】,不是 admin。**
> `docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md:165` 逐字
> 「**B3** | session payload 加 `sub` + 版本欄 `v:2` | **報價單** auth」;admin 那半是 **B5**(`:167`)。
> ⛔ 本節第一版把這張表寫成「admin 的 cookie vs admin 的 code」—— **主詞錯了**,codex 2026-08-17 抓。
> 📎 而那句話**每個字都是對的**,只是主詞放錯 ⇒ 那正是 memory `feedback_wrong-subject-narration-every-word-true` 的形狀。

**5-A-1 · B3 自己造成的那個(主詞 = 報價單)**

| 使用者瀏覽器裡的**報價單** cookie | 正在服務的**報價單**實例 | 會發生什麼 | 可接受嗎 |
|---|---|---|---|
| 舊 `v:1` | 舊 code | 現況 | ✅ |
| 舊 `v:1` | **新 code** | 新 validator 遇到 `v:1` ⇒ **必須決定:接受(過渡)還是拒絕** | 見下 |
| **新 `v:2`** | **舊 code**(部署切換期間還在服務的實例) | 舊 validator `o.v !== 1` **硬拒** ⇒ 那些人**被登出**,連 SSO authorize 都進不去 | ⚠️ **要規劃的就是這一格** |
| 新 `v:2` | 新 code | 目標狀態 | ✅ |

🔴 **決定:走【甲】—— B3 一部署就簽 `v:2`、舊 `v:1` 一律拒、報價單使用者被登出一次。**
**而這【不是本規格拍的,是 plan v4 早就拍過的】**:
`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md:198` 逐字
「舊 cookie(本線之前簽的)⇒ **必須失效,不得靜默降級成無身分**」。

> ⛔ **本節寫過兩個錯的版本,兩個方向相反,兩個都留著:**
> · **第一版**:我拍「報價單新 validator 同時接受 v:1、視為無身分」
>   ⇒ **逐字就是 plan §6-1 禁止的那件事**(codex 第二輪抓)。
> · **第二版**:我把它升成「要 Sean 拍的決策題」
>   ⇒ **也錯,而且是反方向**(codex 第三輪抓):**那題母 plan 已經拍過了**,
>     我升級的結果是**施工者無謂停工等一個不需要的拍板**。
> 🔴 判別句:**這題是「還沒有人決定」,還是「已經有人決定了而我沒去查」?**
>   前者才升級。而我兩次都沒有先去讀母 plan 的驗收清單 —— **它就在同一條線的 §6。**
> 📎 需要 Sean 的只有一種情況:**要推翻 §6-1 走乙/丙**(見下)。

**其他兩條路(列出來是為了讓「走甲」是個選擇,不是預設值;要走它們=推翻 plan §6-1=要 Sean)**:

| | 作法 | 代價 / 為什麼沒選 |
|---|---|---|
| **甲(採用)** | 一部署就簽 `v:2`,舊 `v:1` 拒 | 報價單使用者**被登出一次**;零新增機制;**符合 plan §6-1 字面** |
| **乙** | 報價單側新增簽發版本開關(先部署仍簽 v:1 → 翻開關才簽 v:2)+ 過渡期雙讀 | 沒人被登出;⛔ **成本欄已下修** —— 見下方更正,**不是「全新機制」**;仍要 Sean(因為要推翻 §6-1) |
| **丙**(codex 第三輪補,我原本沒想到) | **換一個新的 cookie 名稱**簽 v:2 + 明確清掉舊 cookie ⇒ 舊 code 根本讀不到新 session | 不動 `v` 也不會靜默降級;代價=多一個 cookie 名要維護、清除時機要想清楚;**同樣是新機制** ⇒ 要 Sean |

**報價單側目前沒有「簽 v:1 還是 v:2」這個開關** —— `v` 是**寫死的字面**:
`lib/session.ts:14` 型別 `v: 1`、`:91` 產生處 `return { v: 1, … }`(`origin/main` = `482bec5`,2026-08-17 當場 fetch 後讀)。
⇒ **乙 不是「設定一下」,是要寫 code。** 現況 validator 逐字在同檔 `:110`。

> ⛔ **成本欄更正(V 窗 R1 `nit 2`,2026-08-17;我自己到報價單 repo 複驗過才改)**
>
> 我原本寫「乙 = **新增常駐開關 = 全新機制**(鐵則 8)」。**那個成本高估了。**
> 🔴 **報價單側已經有一個活的版本旋鈕族**,我漏掉是因為**我的 grep 只掃 `process.env`**:
> · `lib/edge-config.ts` `ForceReloginState = { tokenVersion, minValidIat, require2fa }`
>   —— 存在 Vercel Edge Config、**讀失敗 fail-closed**(檔頭逐字「讀失敗 → 高水位 tv/iat」)
> · `lib/edge-config-write.ts:41-43` `writeTokenVersion()` —— **可寫端已存在**
>   (`upsert auth_token_version` / `auth_min_valid_iat`)
> ⇒ **force-relogin 這個機制族已經在跑了**;乙若真要走,**可以沿同族**,不必從零長一個常駐開關。
>
> 🔴 **我漏掉它的原因值得記**:我下的結論是「**零簽發版本開關**」,而我量的是「**零 `process.env`**」——
> **量的東西比結論窄。** 版本旋鈕不一定住在 env 裡,它可以住在 Edge Config、DB、或任何地方。
> ⇒ 判別句:**我的結論講的是「這個東西不存在」,而我的量法只覆蓋了「它可能住的其中一個地方」嗎?**
>
> ⚠️ **這不改變走【甲】**(§6-1 已拍),但**那張表哪天端給 Sean,他會在成本欄上做決定** ⇒ 成本欄必須是對的。

⚠️ **走甲的操作面(這才是施工者要規劃的東西)**:沒有部署順序可排,B3 一上線就是切換點
⇒ **挑一個沒人在用的時段 + 事先告知**。⚠️ 「沒人在用的時段」是什麼時候,**我沒有資料**(要問 Sean 或看日誌)。

**5-A-2 · admin 那半(主詞 = admin;⚠️ 這是 **B5** 的範圍,寫在這裡是為了對照)**

> 🔴 **兩邊的決定【不一樣】,不要讀成同一個**(codex 第四輪抓;我原本寫「同一個決定的另一半」「兩邊同理」):
> · **B3(報價單)= 直接拒 v:1**(走甲、依 plan §6-1)⇒ 使用者被登出一次。
> · **B5(admin)= 暫時接受 v:1**,直到 `ADMIN_REQUIRE_REAL_IDENTITY` 打開才拒。
> **差別的來源**:admin 有那個開關當「之後就死」的出口,報價單沒有(已實查,見上)。

admin 的 `v` 是 **admin 自己簽的**,與報價單的 `v` **互不相干**(見上面 ⛔ 區塊的四條證據)。
🔴 **決定:admin 的新 validator【同時接受 v:1 與 v:2】,
而在 `ADMIN_REQUIRE_REAL_IDENTITY` 開關打開之後【拒絕 v:1】。**
(⛔ 原句寫「**同樣**同時接受」—— 那個「同樣」會再把讀者拉回同理誤讀,codex 第五輪抓;B3 並不接受。)
現況 validator 逐字 `apps/admin/src/lib/session/session.ts:112`。

**admin 為什麼可以暫時接受(這一段【只講 admin】,不適用報價單)**:
- 過渡期要能同時活著(舊 admin cookie 還在使用者瀏覽器裡、TTL 沒到)
- 而開關打開 = 我們宣告「從現在起身分是必需的」⇒ 那一刻 v:1 就該死
  ⇒ **plan §6-1「舊 cookie 必須失效」在 admin 這半是【延後兌現】,不是不兌現** —— 兌現點 = 開關。
- 📎 這讓「開關開著而 session 沒帶身分」的驗收格(plan §6-6)**有東西可測**
- 🔴 **報價單沒有這個延後兌現的機制**(零簽發版本開關,已實查)⇒ 它只能立刻兌現 = 走甲。

⚠️ **順序建議 —— 🔴 只有在上面那題拍【乙】的時候才存在**:
「新 code 先部署但**仍簽 v:1**,之後才翻開關開始簽 v:2」**需要那個開關**,而報價單側現在沒有 ——
數法:`cd ~/API大量上架/PCM報價單-V2 && git show origin/main:lib/session.ts | grep -n 'process.env'`
⇒ 只命中 `NODE_ENV`(`:24`)與 `SESSION_SECRET`(`:44`/`:60`),**零個簽發版本開關**(2026-08-17 當場跑)。
⇒ 拍【甲】的話**沒有順序可排**,B3 一上線就是切換點,要做的是**挑一個沒人在用的時段 + 事先告知**。

🔴🔴 **這條的證據強度,誠實寫**(codex 2026-08-17 抓我原句超出證據):
· **有證據的**:舊 validator 收到 `v:2` 會拒 —— 兩邊 code 逐字 `o.v !== 1`(行號如上)。
· **沒有證據的**:「部署切換期間**一定會**有舊實例還在服務新簽出來的 cookie」。
  那取決於 Vercel 的 alias 切換與舊實例排空行為,**我沒有量過,也沒查過官方文件**。
  ⇒ 上表第三列**會不會真的發生,是未確認的**;上面那條順序建議因此是**廉價保險**,不是「已證實的硬前置」。
  缺的檢查 = 部署後觀察舊實例存活窗,或查 Vercel 對 production alias 切換的官方說明。

### 5-B 真正的跨 repo fail-closed 點:**`amr` enum**(V 窗 `F-1` 指出的那個)

跨 repo 會互相咬到的欄位,依 `apps/admin/src/lib/sso/exchange.ts:7` 的合約逐字有**兩個**:`amr` 與 `auth_time`
(數法:`grep -n 'ok:true' apps/admin/src/lib/sso/exchange.ts` ⇒ 回應形狀就那一行)。
⛔ **本節第一版寫「會咬到的只剩 `amr`」—— 錯,codex 2026-08-17 抓。`auth_time` 一樣會咬:**
`parseAuthTime()`(`exchange.ts:39-46`)只收 `number` 或**純數字字串**,且要 `0 < n <= now+30`
⇒ 報價單若改送 ISO 字串、毫秒、或時鐘快超過 30 秒 ⇒ 回 `null` ⇒ `exchange.ts:84` `if (authTime === null) return null`
⇒ **同樣是登入失敗**。⇒ **兩個欄位都是跨 repo fail-closed 點,而兩個都沒有版本號在保護。**

以下先講 `amr` 這一半(`auth_time` 那一半見本節末):

| 在哪 | 逐字 | `檔案:行號` |
|---|---|---|
| 報價單(送出端) | `type SessionAmr = 'pwd' \| 'totp' \| 'bootstrap' \| 'recovery'` | `~/API大量上架/PCM報價單-V2` `lib/session.ts:11`(`origin/main` = `482bec5`;2026-08-17 當場 `git fetch` 後只讀 origin/main) |
| admin(收下端) | `type AdminSessionAmr = 'pwd' \| 'totp' \| 'bootstrap' \| 'recovery'` | `apps/admin/src/lib/session/session.ts:20` |

**兩邊目前逐字相同。** 而 `apps/admin/src/lib/sso/exchange.ts:11` 逐字寫著:
「`amr` admin 端自驗(**報價單 exchange 直透 DB 值、無白名單**)」
⇒ 報價單**不過濾**,admin 才過濾(`exchange.ts:81-82`:`const amr = sanitizeAmr(rec.amr); if (!amr) return null;`)。

🔴 **後果:報價單若新增一個 amr 值(例如 `passkey`)而 admin 還沒加進 enum ⇒ 那次登入直接失敗。**
⛔ **本節第一版寫「admin 濾掉它;若過濾後為空才失敗」—— 錯,codex 2026-08-17 抓。它根本不過濾。**
`sanitizeAmr()` 逐字(`apps/admin/src/lib/sso/exchange.ts:33-35`):
「🔴 fail-closed(Fable/Codex MF1):**任一元素不在 enum → 整包拒**(非靜默過濾降級)」
⇒ `['pwd','passkey']` **也會失敗**,不需要「只剩一個未知值」這種條件。
⇒ **失敗面比我原本寫的大得多**:只要那次登入的 amr 陣列裡出現一個 admin 不認得的值,那個人就登不進去。
📎 而這個「整包拒」是**刻意設計的**(註解自述是 Fable/Codex 的 must-fix 修法),不是 bug ——
   它換來的代價就是上面那條硬順序。
⇒ **這就是真正的跨 repo fail-closed 點。** 方向是安全的(擋住而不是放行),
   但它**擋的是登入本身**,而且**沒有任何版本號會先警告你** —— `v` 保護不到它。

**`auth_time` 那一半(同族,合約同樣沒有版本號)**

| 送出端 | 收下端的要求 | `檔案:行號` |
|---|---|---|
| 報價單 `exchange` 回 `auth_time` | `number` 或**純數字字串**;`Number.isSafeInteger` 且 `0 < n <= now+30` | `apps/admin/src/lib/sso/exchange.ts:39-46` |

🔴 報價單若改送 ISO 8601、毫秒、或它的時鐘比 admin 快超過 30 秒 ⇒ `parseAuthTime` 回 `null`
⇒ `exchange.ts:84` ⇒ **登入失敗**。方向一樣是安全的,一樣**沒有版本號會先警告**。
⚠️ **時鐘那一項我未量**:兩邊實際的時鐘偏差多少、有沒有 NTP,我沒查 ⇒ 這是「理論上會咬」不是「量到會咬」。

⚠️ **硬順序(跨 repo 這一軸)**:**先在 admin 的 enum 加新值並部署,再讓報價單開始送。**
(`auth_time` 同理:先改 admin 的 parser 再改報價單的送出格式。)
⚠️ **而 B3 這一片不動 `amr`** ⇒ 5-B 對本片**不觸發**;寫在這裡是因為原表把 5-A 誤植成跨 repo,
   **下一個人會照那張錯的表去規劃跨 repo 部署,而那張表指的方向是錯的。**

### 5-C 我未查證的(不要當我查過)

· 報價單那側**有沒有計畫要加 amr 值** —— 我沒問、沒查它的 backlog。
· 報價單的 `ver`(token_version,`lib/session.ts:19`)與 admin **無關**
  (admin 的 payload 沒有這一欄,`apps/admin/src/lib/session/session.ts:22-30`),
  但它是報價單自己的強制重登機制;若那邊為了 B3 動 `ver`,影響面在報價單側,**本規格未涵蓋**。
· 5-A 那張表的第三列(舊實例硬拒 v:2)**是我從 code 推的,沒有在真部署上觀察過**。

---

## §6 驗收(負向為主)

> 🔴🔴 **主詞警告(codex 第三輪抓,2026-08-17)+ 歸屬已裁定(同日)**:
> 下表原第 1-3 格寫「新／舊 **admin**」,而 **B3 這一片動的是【報價單】**
> (`…real-auth-line-plan-v4.md:165`「B3 … **報價單** auth」;admin 那半是 **B5**,同檔 `:167`)。
> ⇒ **照字面做的施工者會去改錯 repo、驗錯 validator。**
> ⚠️ 那幾格的內容**本身沒有錯**,只是它們描述的是 **B5** 的驗收 —— 兩片的 payload 各自升版、各自要測。
>
> ⛔ ~~它們的最終歸屬(留在本檔還是搬去 B5 spec)未定,不要當它已經分好。~~
> **2026-08-17 主視窗裁定 `Q-B3範圍` = 甲(縮回母 plan 邊界)⇒ 歸屬已分好:
> 第 1-3 格與第 8 / 8b 格已搬至 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md`。**
> ⇒ 下表**只留報價單側該跑的格**,加兩列**指標列**(不是複本)。
>
> 🔴 **第 4 / 4b / 4c / 4d / 7 格的主詞(V 窗 `V2` 當時寫的是「第 4-7 格」,2026-08-17 補;拍板下來之後那條 R3 must-fix 就不再有「等」的名義)**:
> 依母 plan `:165`「B3 = **報價單** auth,session payload 加 `sub` + `v:2`」
> ⇒ **第 4 / 4b / 4c / 4d / 7 格是【報價單側】,留在本檔**,已逐格標主詞。
> 🔴 **R2 must-fix(2026-08-18 本輪折入):原句寫「第 4-7 格【全部】留在本檔」,而第 6 格已被判定移出** ——
> **那次移出只寫在下方紅框(敘事),表頭與表列都還在說它留著(結構)。**
> ⇒ 本次同時改了三處:表頭這一句、第 6 格改成指標列、下方紅框保留論證。
> 📎 判別句:**「已移出」是關於【檔案結構】的宣稱,不是關於【我寫了一段話】的宣稱。**

> 🔴🔴 **R2 must-fix(2026-08-18 本輪折入):每一列都標了 §3.11 的世界。**
> 病根與 §3.11 同一個:**規格把一個【有兩種行為的系統】寫成好像只有一種**,
> 而 §3.11 只把規定寫進正文、**驗收表一格都沒標** ⇒ 那條規定沒有執行者。
> ⚠️ **標【開關前】的格有到期日**:B7 打開兩顆開關之後它構造不出來 ⇒ **必須從表上移除**
>    (收尾步驟與撈法在 `docs/specs/2026-08-18-m4b-e8b-b6-b7-spec.md` §3 第 5 步)。
> 📎 判別句:**「規定」寫在正文,「會不會被做到」寫在驗收表 —— 兩邊不同步,做到的是驗收表那一版。**

| # | 世界(§3.11)+ 構造 | 預期 |
|---|---|---|
| 1 / 2 / 3<br>**↗ 已搬走** | ⛔ 三格(舊 v:1 cookie 送新 admin 開關關 / 開關開 / v:2 送舊 admin)**已於 2026-08-17 搬至 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §B5-1**,原因 = 範圍越界(codex `#2`:admin 收端歸 B5)。 | **本檔不跑這三格。** ⚠️ 留此一列是為了讓下一個人知道「它們被搬走了」,**不是「它們被刪了」,也不是「B3 要跑」**。 |
| 3c | 【**兩者皆**】🆕【**B3** · 報價單】**新簽舊驗**:`v:2` cookie 送到**舊版報價單**(rollback / 混版部署那一刻) | 🔴 **拒絕(=使用者被登出)**,**不得忽略 `sub` 後放行**。**2026-08-18 關卡1 must-fix**:§5 混版表**描述**了這個方向,而驗收**只測了反方向** ⇒ **部署與 rollback 那一刻的世界沒有可重跑的驗收**<br>🔴 **R2 must-fix(本輪折入):「舊 validator」必須釘死成一個【可取回的 artifact】,不得手寫替身。**<br>**量法(照抄即可跑)**:<br>`cd ~/API大量上架/PCM報價單-V2 && git rev-parse HEAD` ⇒ 把這顆 hash **抄進測試檔的註解與本表**(它就是「舊版」的定義);<br>`git show <那顆 hash>:lib/session.ts > <tmp>/session-old.ts` ⇒ 測試 **import 這一份**,呼叫它的 `verifySession()`。<br>📎 **為什麼要釘**:手寫一份「會拒 v:2」的替身,**證明的是我會寫拒絕,不是舊版會拒絕** —— 而真正要問的是「**rollback 回去的那一份**會不會拒」。<br>⚠️ **本表落筆當下量到的值**:`1149e05`(2026-08-18T17:3x,`main`)。**它會過期** —— 🔴 **以 B3 那顆 commit 的 `HEAD^` 為準重新量一次**,不要直接用這裡的 `1149e05`。 |
| 3b | 【**兩者皆**】🆕【**B3** · 報價單】舊 v:1 cookie 送到**新報價單** | 🔴 **拒絕**(走甲的直接後果;這格才是本片的;對應 plan v4 `:198` §6-1)。<br>🔴 **本格標【兩者皆】,不是【開關後】** —— 報價單這一側 **`v:1` 從 B3 部署那一刻起就一律拒**,**與任何開關無關**(§5-A 甲已採用:『一部署就簽 v:2、舊 v:1 拒、使用者被登出一次』,依母 plan `:198` §6-1)。<br>⛔ **本輪一度要加一格『【開關前】v:1 仍然接受』—— 那是錯的,而且錯得很貴**:它逐字就是 plan §6-1 明文禁止的『靜默降級成無身分』。成因 = **主詞放錯**:那個行為是 **B5(admin)** 的(§5-A-2:admin 暫時接受 v:1,直到 `ADMIN_REQUIRE_REAL_IDENTITY` 打開才拒),被我當成報價單的。詳見 §3.11 的更正段。 |
| 4 | 【**兩者皆**】【**B3** · 報價單】v:2 但 `sub` 欄缺 | 🔴 **拒絕**(型別上不該存在,但要證明 validator 也擋) |
| 4b | 【**兩者皆**】🆕【**B3** · 報價單】v:2 且 `sub` 是 **fallback 多帶任一額外鍵**(至少測兩種:`{kind:'fallback', staff_id:'sean'}` 與 `{kind:'fallback', x:1}`) | 🔴 **拒絕**。**2026-08-18 關卡1 must-fix**:規格只畫了 TypeScript union,**沒要求 runtime 拒巢狀額外欄** ⇒ 只檢查 `kind` 的實作會**接受**它,而下游可能把 fallback **升格**成具名身分。<br>🔴 **R2 must-fix(本輪折入):原本只寫 `staff_id` 一個欄名 ⇒ 一個【硬編擋 `staff_id`】的實作就能過這一格**,而它對其他任何額外鍵都放行。⇒ **必須是「額外鍵一律拒」,不是「拒某一個欄名」**;測資至少兩種、其中一種是**沒有語意的鍵**(`x:1`)。<br>📎 **現況實查(2026-08-18T17:3x,報價單 `HEAD 1149e05`)**:`lib/session.ts:107-121` 的 `isPayload()` **逐欄檢查、完全不擋額外鍵** ⇒ 這不是假想的失敗世界,是**今天的預設行為**。<br>⚠️ 同一個病 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §B5-3 已記過(admin 端)—— **兩端都要擋,不要指望對方擋** |
| 4c | 【**兩者皆**】🆕 **對照組**:v:2 且 `sub = {kind:'fallback'}`(乾淨的) | ✅ **接受**(證 4b 擋的是「多帶了欄」,不是把 fallback 整個弄壞了) |
| 4d | 【**兩者皆**】🆕 **R2 must-fix(本輪折入)**:v:2 且 `sub = {kind:'user', staff_id:'sean', role:'admin'}`(**user variant 多帶一個欄**) | 🔴 **拒絕**。**理由**:4b 只證了 fallback 那一支會擋額外鍵;**user 那一支是另一條 code path**,一個「只對 fallback 做 exact-shape、對 user 放行」的實作**照樣讓 4b/4c 全綠**。<br>⚠️ **兩格合起來才叫「巢狀 exact shape」** —— 少任何一格,證到的都只有一半。 |
| 5 | 【**兩者皆**】⛔ **2026-08-18 關卡1:本格的後半【移出本片】** —— 見下方紅框 | ✅ **本片只驗前半:接受這顆 cookie**。~~而報價單側的任何寫入都被擋~~(備援唯讀,plan `§7-Q1=A`)。⛔ ~~任何寫入~~ 🔴 **宣稱已於 2026-08-17 縮到報價單側(V 窗 `V2`)** —— 原字的「任何」**跨兩個 repo**,而 **admin 那半的執法已搬去 B5**(`docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1 read/write 矩陣 + 涵蓋證明)⇒ **本格拿到綠,不代表 admin 那半也擋住了。** |
| 6<br>**↗ 已移出** | ⛔ **本格 2026-08-18 關卡1【移出驗收表】**(原文:v:2 且 `sub.staff_id` 不在白名單 ⇒ 拒絕)。理由 = 它**沒有 runtime 執行者**:`CHECK` 只在**寫入映射列**時擋,簽發時的 `SELECT` 不重跑它 ⇒ **構造不出來(恆綠、零判別力)**。 | **本檔不跑本格。** 🔴 **R2 must-fix(本輪折入):上一輪的「移出」只發生在【敘事】(下方紅框),沒有發生在【結構】(它還留在表內)** ⇒ 本列改成**指標列**,不是可跑的格。它指向的風險留在 §7 未決;若哪天要它真的擋,**必須另定應用層驗法**(那會複製清單,取捨見 §3 表下方 ⛔ 區塊)。<br>📎 判別句:**一格「已知構造不出來」的驗收,留在表上只會讓表的通過率說謊。** |
| 7 | 【**兩者皆**】⛔ ~~簽章正確但 `sub` 被竄改~~ ⇒ 🔴 **2026-08-18 關卡1 更正:原文自相矛盾** —— 若竄改後 HMAC 仍正確,validator **本來就該接受**(那表示簽發者簽了它)。**真正該構造的是:【保留原簽章、只改 payload 裡的 `sub`】** | 🔴 **拒絕**(這格證的是「`sub` 在簽章涵蓋範圍內」,不是「HMAC 有效」)<br>⚠️ **驗法要明寫**:拿一顆合法 cookie,**只動 `.` 前面那一段的 `sub` 欄、簽章那一段一個字不改**<br>🔴 **R2 must-fix(本輪折入):竄改後的形狀必須【仍然合法】** —— 具體:`{kind:'user',staff_id:'sean'}` **改成** `{kind:'user',staff_id:'kevin'}`(換一個值,形狀一模一樣)。<br>📎 **為什麼要釘死這一點**:若照 4b 那種**非法**形狀去改(多帶一個欄),它會被 **schema 驗證**擋掉 ⇒ **即使 HMAC 根本沒涵蓋 `sub`,這一格照樣紅** ⇒ **這格就變成 4b 的複本,零判別力**。<br>⚠️ **對照**:同一顆 cookie 只改 `iat` 之外的**已涵蓋欄**也該紅 —— 若要加一格,那是 nit,不是本片必跑。 |
| 9 | 【**兩者皆**】🆕 **正向對照(本表原本缺這一格)**:B2 算出 `sub={kind:'user',staff_id:'sean'}` 的那次登入 ⇒ 看簽出來的 cookie | ✅ **`v:2` 且 `sub` 逐字相同**。**2026-08-18 關卡1 must-fix**:沒有這格,一個「**user 永遠被 validator 拒絕**」的壞實作**照樣讓上面每一格全綠**。<br>🔴 **R2 must-fix(本輪折入):只看簽出來的 cookie 還不夠** —— 那只證了「簽發端把 `sub` 放進去了」,**沒證「validator 收得下它」**。⇒ **本格必須把簽出來的那顆 cookie 再送進 `verifySession()`,拿回 payload 後逐欄比對**。<br>⚠️ **量法(避免測到替身)**:`verifySession` 要從 **live `lib/session.ts`** import,**不得在測試檔裡手寫一份 validator** —— 手寫的那份永遠會接受你剛簽的東西。<br>📎 **這格擋的失敗世界**:validator 的 `isPayload()` 加了 `sub` 檢查而寫錯(例如只認 `fallback`),簽發端一切正常 ⇒ **具名登入的人全部登不進去,而 4/4b/4c/7 全綠**。 |
| 10 | 【**兩者皆**】🆕 **同上,而 B2 算出的是 `fallback`** | ✅ **`v:2` 且 `sub.kind='fallback'`**(證兩種 `sub` 都走得通,不是只有一種)。**2026-08-18 關卡1 R2 must-fix(同第 9 格)**:同樣要**送回 `verifySession()`**,理由與量法逐字見第 9 格。 |
| 11 | 【**兩者皆**】🆕 **R2 must-fix(本輪折入)**:已有一顆 `v:2` + `sub={kind:'user',staff_id:'sean'}` 的 session,走 **2FA 綁定確認**(`app/api/admin/2fa/enroll/confirm/route.ts:89`)重簽 | ✅ **重簽出來的 cookie 仍是 `v:2` 且 `sub` 逐字相同**(送回 `verifySession()` 比對,理由同第 9 格)。<br>📎 §3.10 已把「必須原封沿用」寫成規定,**而規定沒有格會跑** —— 那一點正是 R2 打中的:現行 `2fa/enroll/confirm:89` 是 `buildPayload(['pwd','totp'], tokenVersion)` **重新造一個 payload** ⇒ **照現行寫法施工,`sub` 會靜默消失,而 3b/3c/4/4b/4c/4d/7/9/10 全綠**。 |
| 11b | 【**兩者皆**】🆕 **R2 must-fix(本輪折入)· 反向**:原 session 是 `sub={kind:'fallback'}`,走同一條 2FA 重簽 | ✅ **仍是 `fallback`**,🔴 **不得被補成 `user`,也不得憑空補一個 `staff_id`**。<br>📎 **這格擋的是「順手補齊」**:重簽時去查一次映射表把 fallback 升格成具名身分 —— 那等於**用一條沒有經過密碼驗證的路徑產生身分**(§3.10 逐字)。 |
| 12 | 【**兩者皆**】🆕 **R2 must-fix(本輪折入)**:一顆 `v:2` + `sub` 的 session 走 **sliding refresh**(`middleware.ts:74`,構造法 = 讓 `exp - now < SESSION_MAX_AGE_SEC / 2` 且走 GET 真實導航) | ✅ **refresh 出來的 cookie 仍帶原本的 `sub`**。<br>⚠️ **誠實界線(§3.10 逐字,不重述論證)**:現行寫法是 `signSession({ ...payload, iat, exp })` **展開**⇒ **今天本來就會保留** ⇒ **本格【一裝就綠】**。<br>🔴 **它仍然要有,理由是【回歸】不是【現在會壞】**:哪天有人把它改成 `buildPayload(...)`,`sub` 會靜默消失,而**上面每一格照樣綠**(它們都不經過 middleware)。<br>📎 判別句(照 `docs/patterns/guard-and-instrument-traps.md`):**一裝就綠的格,要在檔頭寫明它是回歸格、並寫出【它會在什麼改動下轉紅】** —— 否則下一個人會把它當成沒用的格刪掉。 |
| **8 / 8b**<br>**↗ 指標列** | 🔴 **部署前必須執行 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §B5-2 的第 8 / 8b 格**(跨 repo amr 一致性:報價單送得出的值 ⊆ admin 兩份 `VALID_AMR`,且兩份彼此相等)。**本列不是那一格的複本,是它的入口** —— 判準、量法、射程限定一律以 B5 正本為準,不要在這裡重述。 | 🔴 **B3 部署日必跑**;不成立 ⇒ **不得部署**。⚠️ **本列刻意不寫判準** —— 寫了就會變成第二個真相,而它會跟正本漂。 |

> 🔴 **第 8 / 8b 兩格的正本與它們底下那整塊論證,已於 2026-08-17 搬至
> `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §B5-2**(原因 = 範圍越界 codex `#2`;歸屬 = V 窗判 B5,理由與反方都寫在新家)。
> **內容逐字未改**,包含:量法量的尺 / 兩份 Set desync 的部署日世界 / 量法 ①②③④ / 射程限定 `N-1` /
> `auth_time` 那一半沒有驗收格的缺口。
>
> 🔴🔴 **而閘搬走了,【觸發】沒有搬走** —— 這是 V 窗開本條件的理由,逐字記在這裡:
> §5-B 白紙黑字寫著「**B3 就是報價單 auth 線 = 最可能引入新 amr 值的那一條**」
> ⇒ **最可能引入 desync 的部署日是【B3 的】,而閘的正本在 B5。**
> ⇒ 所以本檔的驗收表**不歸零**,上表留一列**指標列**;
> **正本在 B5、可達性留在 B3**。(否則就是「描述不會在部署日擋人」那個原病復發,只是這次描述搬到別的檔。)

## 🔴🔴 2026-08-18 關卡1:**第 5 格的後半與第 6 格,都不該留在這一片**

**第 5 格後半(「報價單側任何寫入都被擋」)**:codex 逐字 ——
要把 fallback 的寫入阻擋做出來,**必須改共用授權閘並盤點至少 31 個既有呼叫點**
⇒ **那已經不是「payload 片」,而且明顯超過鐵則 4 的 15-45 分鐘。**
且 `docs/specs/2026-08-18-m4b-e8b-b6-b7-spec.md` §3 正本自己寫著**報價單那半仍缺**
⇒ **照 payload-only 的規格施工,本格【不可能變綠】。**
**⇒ 處置:後半移出 B3。** 歸屬 = **報價單側的備援唯讀執法**,而那一片**目前沒有主人**
(admin 那半在 B5/B6;報價單那半 = 缺)⇒ **與 `#645` 同族的「沒有編號的一片」,已回報主視窗。**

**第 6 格(白名單外 `staff_id` ⇒ 拒)**:本檔自己已標「**沒有 runtime 執行者、先不要照它施工**」——
codex 逐字:「**一份仍含未決安全驗收格的規格,不能通過動手前關卡**」。**成立。**
**⇒ 處置:本格【不在 B3 驗收範圍】**,它指向的風險留在 §7 未決,
**不留在驗收表裡當一格永遠不會被跑的格。**
📎 判別句:**一格「已知構造不出來」的驗收,留在表上只會讓表的通過率說謊。**

🔴 **第 7 格最容易被跳過** —— 它證的不是「HMAC 有效」,是「**`sub` 沒有被放在簽章之外**」。
📎 兩邊的簽章都是 `base64url(payloadJSON) + '.' + base64url(HMAC(payloadJSON))`
(`lib/session.ts:7` 檔頭註解)⇒ 只要 `sub` 在 `payloadJSON` 裡就涵蓋得到。
**而「只要」那兩個字,正是需要一格測試的地方。**

---

## §7 未決 / 未驗

1. ~~`staff_id` 白名單的單一來源未定~~ **✅ 2026-08-16 定案 = (b)。**
   ⛔ ~~而且它【已經實作好了】~~ **這半句 2026-08-17 撤回(codex `#13`/`#14`)** ——
   **已實作的是 `resolveStaff()` 這支函式,不是「B5 的讀取閘已經在用它」。詳見 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1 那個 ⛔ 區塊(⛔ 原寫「下方」—— 2026-08-17 搬走後「下方」已是錯的)。**
2. **`Q-AUTH-1` 甲案前提:②已證、③未證**
   (②=那些帳號有 email 身分與密碼,SQL 查得到;③=Email provider 現在是開的,**SQL 原理上查不到**,
   它住在 GoTrue config 不在 DB)。
3. ⛔ ~~**本片尚未經對抗審查**~~ **已過期,2026-08-17 更正(codex R3 `nit`:世界狀態寫進 spec 會持續過期)**。
   **現況(綁輪次,不寫「已審過」這種會再過期的字)**:
   `docs/reviews/2026-08-17-b3-spec-codex-findings.md` = **codex R1,21 must-fix**;
   同日 **R2 / R3** 兩輪審的是**折法**(R2 = 4MF+3nit、R3 = 換角度 8MF+2nit,皆已折或已明列未折)。
   🔴 **「審過」不等於「審完」** —— 21 條中仍有多數未折,逐條判定看那份 findings 檔的「折況」表。
4. **`auth_time` 的語意問題未解**(admin `session.ts:26-27` 自述:它隨 sliding refresh 更新,
   **不是不變的登入時刻**)⇒ 若日後要做 step-up「近期驗證過」,那個欄位形同虛設。
   **本片不解決,但寫進來免得下一個人拿它當登入時刻用。**

---

## §7.1 ⛔ 本節已於 2026-08-17 **搬至 `docs/specs/2026-08-17-m4b-e8b-b5-spec.md` §7.1**

**原因** = 範圍越界(codex `#2`):母 plan `2026-08-16-m4b-e8b-real-auth-line-plan-v4.md:167` 逐字
把 **admin 收端放在 B5**,而本節整節講的是 admin 收端(`resolveStaff` 進讀取閘 / read-write 矩陣 /
涵蓋證明 / B5 驗收表)。**主視窗 2026-08-17 裁定 `Q-B3範圍` = 甲** ⇒ 整節搬走。

🔴 **它不是被刪掉,也不需要有人重寫一份。** 正本在上面那個路徑,**內容逐字未改**。
⚠️ 搬走的內容裡有幾處引用本檔(§3 那個 union / §6 第 5 格),已在新家改成跨檔指標。