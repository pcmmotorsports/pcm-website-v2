# B3 slice 規格 · session payload 帶身分(`sub` + 版本欄 `v:2`)

> **狀態:規格,尚未實作。** plan v4 已由 Sean 批(鐵則 8)。
> **母 plan**:`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md` §4 B3。
> 寫於 2026-08-16 · B 窗。**每一個宣稱都附 `檔案:行號` 或量法。**
>
> 🔴 **本片動的是【兩個 repo 的 session 型別】,不動 DB。** 前片 B1/B2 動 DB、本片不動。

---

## §1 這片要做什麼(一句)

讓 session cookie **帶得動「這是誰」** —— 現在它只帶「有人通過了那道密碼」。

---

## §2 現況(2026-08-16 實查,逐條附行號)

### 2.1 兩邊的 payload 型別

| 在哪 | 型別 | `檔案:行號` |
|---|---|---|
| 報價單(簽發端) | `{ v:1, sid, iat, exp, amr[], ver }` | `~/API大量上架/PCM報價單-V2` `lib/session.ts:13-20`(origin/main) |
| admin(收端) | `{ v:1, sid, iat, exp, amr[], auth_time }` | `apps/admin/src/lib/session/session.ts:20-30` |

🔴 **兩邊都沒有任何欄位在講「誰」。** 這正是本線要補的洞。

### 2.2 ✅ **plan v4 §8 依賴的那個機制,已實查證實**

plan v4 §8 寫:「**靠版本欄 `v:2` 擋(舊 validator 見 v=2 直接 reject)**,
不靠『舊 validator 會 reject』這個錯誤假設。」

**兩邊的 validator 逐字都是 `if (o.v !== 1) return false;`**:
- 報價單 `lib/session.ts:110`
- admin `apps/admin/src/lib/session/session.ts:110`

⇒ **`v:2` 的 cookie 會被舊 validator【硬拒】,不是被忽略。**
⇒ **plan 那條 rollback 策略成立,而且現在有證據,不是假設。**

> 🔴🔴 **範圍限定(2026-08-17 補;不補的話這段會被再套錯一次)**:
> 上面那個事實成立的邊界是「**同一個 repo 內:誰簽的誰驗**」。
> **兩邊各有一個 `o.v !== 1`,不代表它們會互相檢查對方** ——
> admin 驗的是 admin 自己簽的 cookie,報價單驗的是報價單自己簽的 cookie。
> 跨 repo 傳的是 `{ ok, amr[], auth_time }` 這份 JSON(`apps/admin/src/lib/sso/exchange.ts:7`),**裡面沒有 `v`**。
> ⇒ **原 §5 就是把這一段讀成跨 repo 耦合而整張表寫錯的**(V 窗 `F-1`)。詳見 §5 檔頭那個 ⛔ 區塊。

### 2.3 ⚠️ 而 plan §8 另一句要**加限定**

plan `:238` 寫「報價單 `isPayload()` **容許額外欄**」——
**那句對,但它只在「不動 `v`」的情況下才是風險**:
`isPayload` 逐欄檢查、**不拒絕未知的 key**(`lib/session.ts:107-118`)
⇒ 若 B3 只加 `sub` 而**不**把 `v` 升到 2,舊 validator 會**靜默忽略 `sub`**,
而 cookie 仍然有效 ⇒ **那正是「靜默退回無身分語意」。**

**⇒ `v:2` 不是可選的,它是唯一擋住這個退化的東西。**
🔴 **本片不得只加欄不升版。**

---

## §3 `sub` 的形狀(plan §7-Q1=A 拍板)

```ts
type SessionSub =
  | { kind: 'user';     staff_id: string }   // 真人登入
  | { kind: 'fallback' };                    // 備援共用密碼登入（Q2=A 保留）
```

🔴 **型別上不存在「沒有 `sub` 的 v:2 session」** —— 這是 plan `§7-Q1=A` 的原話,
它的意思是:**備援路徑不是「沒有身分」,是「一種明確的身分」。**
⇒ 若寫成 `sub?: …`(可選),那個「不存在」就沒有了 ⇒ **必須是必填的 union。**

**`staff_id` 的值域**:與 B1-b 的 CHECK 白名單同一組(`sean` / `staff_1` / `staff_2`)。
⚠️ **兩邊各自硬編碼會漂**(A 庫改了名單、這裡沒改)⇒ **本片要決定一個單一來源**,見 §7-1。
🔴🔴 **「單一來源」≠「一份共用清單」(codex R3 `must-fix`,2026-08-17 補;不補的話這句會把人導向相反實作)**:
**§7.1 已明文拒絕 (c) 共用常數檔** —— 定案是**每個問題各自指向【它自己的權威】**
(綁定 → 報價單庫 CHECK;在職 → A 庫 `is_active`),**不是把兩張清單合併成一份放在中間**。
⇒ 讀到「單一來源」四個字要接著讀下面那張表,**不要去建一個新的共用檔**。

> 🔴🔴 **「白名單」這三個字在本檔指兩張不同的清單,不要當成同一張(codex `#9`,2026-08-17 折)**
> codex 逐字:驗收要求拒絕白名單外 `staff_id`,後文卻禁止 admin 持有白名單
> ⇒ **施工者只能複製 DB CHECK 或無法完成驗收,執行位置未定。**
>
> | 這張清單 | 是什麼 | 權威在哪 | **誰執行、在哪一刻** |
> |---|---|---|---|
> | **綁定白名單** | 誰**可以被綁定**成 `staff_id` | 報價單庫 `admin_user_staff_map` 的 **CHECK**(B1-b) | 🔴 **寫入映射列的那一刻**(該表的 `INSERT` / `UPDATE`)—— **不是簽發 session 的那一刻** |
> | **在職名單** | 誰**現在可以操作** | **A 庫 `staff` 表的 `is_active`** | **收端(B5),每次用的時候**,走既有 `resolveStaff()`(§7.1) |
>
> 🔴 **admin 端【永遠不持有】綁定白名單** —— 它拿到 `sub.staff_id` 之後做的是 `resolveStaff()`,
> 那查的是**在職名單**,不是綁定白名單。**兩張清單各自有權威,不互相抄** ⇒ 這正是 §7.1 拒絕 (c) 共用常數檔的理由。
>
> ⛔⛔ **本表 2026-08-17 第一版把執行時機寫錯,同日修正(codex R2 `must-fix`)**
> 原寫「綁定白名單在**報價單側、簽發 v:2 的那一刻**執行」—— **不成立**:
> 🔴 **簽發時做的是 `SELECT`,而 `SELECT` 不會重跑 `CHECK`。**
> `CHECK` 只在**那一列被寫進去**的時候擋;簽發端讀到什麼,取決於那一列**當初**寫進了什麼。
> ⇒ **簽發端該做的是**:依**已驗證的 `auth_user_id`** 精確查出 `staff_id`;
>   **查無 / 不是恰一筆 ⇒ 該次登入失敗**(§4)。**它不比對任何清單。**
>
> 🔴🔴 **而這留下一個【未決】,我不假裝它被折掉了**:
> **§6 第 6 格要驗的「`staff_id` 不在綁定白名單 ⇒ 拒絕」,在 runtime 沒有執行者。**
> 若 `CHECK` 成立,那個狀態**構造不出來**(⇒ 該格恆綠、零判別力);
> 若要讓它在 runtime 真的擋(防 `CHECK` 漂移 / 繞過 / 直接改庫),**就必須另定一道應用層驗法**,
> **而那道驗法需要一份清單 —— 等於把 (c) 共用常數檔那個「兩份會漂」的病請回來。**
> ⇒ **這是取捨,不是漏寫;歸 plan 層決定,`#9` 記為【未折】。**

---

## §4 順序:B3 **必須在 B2 之後**

```
B1-b  建映射表（空）
B2    塞三列（Auth user ↔ staff_id）
B3    session 帶 sub          ← 本片
```
**理由**:`sub.staff_id` 的值要從**映射表**查出來。B2 之前那張表是空的
⇒ 登入成功但查不到 staff_id ⇒ **要嘛簽出一個沒有身分的 v:2(型別上不允許)、要嘛整個登入失敗。**
⛔ ~~**兩種都不能接受,所以順序是硬的。**~~
🔴 **上面這句 2026-08-17 已修正,不要照它讀** —— 「整個登入失敗」**是正確的 runtime 結果**,
**不能接受的是【營運上停在那個狀態】**。**正解在下面這個框裡,不要跳過。**

> 🔴🔴 **「不能接受」的主詞是【營運狀態】,不是【runtime 行為】(codex `#10`,2026-08-17 折;
> 原句未加刪除線 ⇒ 跳過本框的人會讀到相反結論,codex R3 `must-fix`,同日補刪除線)**
> 原句只寫到這裡就停,而 codex 逐字指出:**「個人密碼驗證成功但映射查無/逾時 ⇒ 正確安全結果就是登入失敗」**
> —— 我把那個**正確結果**寫成「不能接受」,而規格又**沒有禁止**任何替代作法
> ⇒ **照字面做的施工者會去發明一個「比登入失敗好」的行為。**
> ⚠️ 我**沒有窮舉**他會發明出什麼(數不出來、也不假裝數得出來);
> 下面只**明文禁止我想得到的三條**(codex 點名兩條 + fail-open 一條),**這張禁止清單不是窮舉**
> ⇒ **不在清單上的變通,一律回來問,不得自行判定「這條不算」。**
>
> **⇒ 本片的硬規定,拆成兩層,不要混:**
>
> | 層 | 規定 |
> |---|---|
> | **runtime(不可違反)** | 映射**查無 / 逾時 / DB 例外** ⇒ **該次登入失敗,fail-closed**。這是**正確結果,不是待改善項**。 |
> | ↳ **這一列不禁止的事**(codex R2 `nit`) | **同一次請求內的有限次重試(bounded retry)沒有被禁止** —— 本列管的是「**重試用完之後**」:那時**必須失敗**,不得改走別的身分。⇒ **不要把這列讀成「第一次逾時就不准再試」。** |
> | **排程(所以順序是硬的)** | 不希望**營運上**進入「人人登入失敗」那個狀態 ⇒ 所以 **B3 必須排在 B2 之後**。 |
>
> 🔴 **明文禁止(三條,施工者不得自行變通)**:
> ① **不得**在映射查無時改簽 `kind:'fallback'` —— fallback 是**一種身分**(§3),不是「查不到時的退路」;
>    那等於讓「映射壞掉」自動取得備援權限。
> ② **不得**採用 client / 請求端傳入的 `staff_id` —— 它必須來自**報價單側查映射表的結果**。
> ③ **不得**把 DB 逾時/例外當成「暫時放行」 —— 見 §7.1「兩個世界長得一樣」那段,
>    fail-closed 的代價已經寫在那裡,**代價是知情接受的,不是拿來換 fail-open 的理由**。

---

## §5 混版部署(這片最容易出事的地方)

> ## ⛔⛔ 本節原版**整張表是錯的**,2026-08-17 重寫(V 窗 `F-1`,主視窗已裁;B 窗自己從 source 複驗過才改)
>
> **原表逐字保留**(「為什麼曾經這樣想」比結論值錢):
>
> | 報價單(簽發) | admin(收) | 會發生什麼 | 可接受嗎 |
> |---|---|---|---|
> | 舊 v:1 | 舊 v:1 | 現況 | ✅ |
> | **新 v:2** | **舊 v:1** | admin 的 `o.v !== 1` **硬拒** ⇒ **所有人被登出** | ⚠️ 要規劃 |
> | 舊 v:1 | **新** | 新 admin 收到 v:1 ⇒ 必須決定 | 見下 |
> | 新 | 新 | 目標狀態 | ✅ |
>
> 🔴 **錯在哪:那張表假設「報價單簽的 cookie 會被 admin 收下來驗」。admin 從來沒有收過它。**
> 跨 repo 傳的是**一份 server-to-server 的 JSON**,不是 cookie:
> · `apps/admin/src/lib/sso/exchange.ts:7` 逐字「回 200 `{ ok:true, amr:string[], auth_time:number }`」
>   ⇒ **回應裡沒有 `v`,也沒有 cookie。**
> · `apps/admin/src/app/api/sso/callback/route.ts:69`:`signSession(buildAdminSession(result.amr, result.auth_time))`
>   ⇒ admin 拿到 `amr` + `auth_time` 之後,**自己簽一個全新的 session**。
> · `apps/admin/src/lib/session/session.ts:93`:`return { v: 1, sid: …, amr, auth_time }`
>   ⇒ **那個 `v` 是 admin 自己給的**,不是報價單給的。
> · `apps/admin/src/lib/session/session.ts:112`:`if (o.v !== 1) return false`
>   ⇒ 它驗的是 **admin 自己簽的那個 cookie**。
>
> ⇒ **報價單把它的 payload 升到 `v:2`,admin 一個位元都看不到。「所有人被登出」那件事不會發生。**
> ⚠️ 而 §2.2「兩邊 validator 都是 `o.v !== 1`」那段**本身沒錯**,§2.3 的結論也不受影響
>   ——錯的是把那個事實讀成**跨 repo** 的耦合。**同一個事實,套錯了邊界。**

混版有**兩個軸**,而它們**不是同一件事**:

### 5-A 真正的 `v` 混版面:**同一個 repo 內,瀏覽器裡的舊 cookie vs 新部署的 code**

`v` 從頭到尾是 repo **內部**的事(誰簽的誰驗),所以混版發生在**時間軸**上,不是兩個 repo 之間。

> 🔴🔴 **主詞:B3 這一片動的是【報價單】,不是 admin。**
> `docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md:165` 逐字
> 「**B3** | session payload 加 `sub` + 版本欄 `v:2` | **報價單** auth」;admin 那半是 **B5**(`:167`)。
> ⛔ 本節第一版把這張表寫成「admin 的 cookie vs admin 的 code」—— **主詞錯了**,codex 2026-08-17 抓。
> 📎 而那句話**每個字都是對的**,只是主詞放錯 ⇒ 那正是 memory `feedback_wrong-subject-narration-every-word-true` 的形狀。

**5-A-1 · B3 自己造成的那個(主詞 = 報價單)**

| 使用者瀏覽器裡的**報價單** cookie | 正在服務的**報價單**實例 | 會發生什麼 | 可接受嗎 |
|---|---|---|---|
| 舊 `v:1` | 舊 code | 現況 | ✅ |
| 舊 `v:1` | **新 code** | 新 validator 遇到 `v:1` ⇒ **必須決定:接受(過渡)還是拒絕** | 見下 |
| **新 `v:2`** | **舊 code**(部署切換期間還在服務的實例) | 舊 validator `o.v !== 1` **硬拒** ⇒ 那些人**被登出**,連 SSO authorize 都進不去 | ⚠️ **要規劃的就是這一格** |
| 新 `v:2` | 新 code | 目標狀態 | ✅ |

🔴 **決定:走【甲】—— B3 一部署就簽 `v:2`、舊 `v:1` 一律拒、報價單使用者被登出一次。**
**而這【不是本規格拍的,是 plan v4 早就拍過的】**:
`docs/specs/2026-08-16-m4b-e8b-real-auth-line-plan-v4.md:198` 逐字
「舊 cookie(本線之前簽的)⇒ **必須失效,不得靜默降級成無身分**」。

> ⛔ **本節寫過兩個錯的版本,兩個方向相反,兩個都留著:**
> · **第一版**:我拍「報價單新 validator 同時接受 v:1、視為無身分」
>   ⇒ **逐字就是 plan §6-1 禁止的那件事**(codex 第二輪抓)。
> · **第二版**:我把它升成「要 Sean 拍的決策題」
>   ⇒ **也錯,而且是反方向**(codex 第三輪抓):**那題母 plan 已經拍過了**,
>     我升級的結果是**施工者無謂停工等一個不需要的拍板**。
> 🔴 判別句:**這題是「還沒有人決定」,還是「已經有人決定了而我沒去查」?**
>   前者才升級。而我兩次都沒有先去讀母 plan 的驗收清單 —— **它就在同一條線的 §6。**
> 📎 需要 Sean 的只有一種情況:**要推翻 §6-1 走乙/丙**(見下)。

**其他兩條路(列出來是為了讓「走甲」是個選擇,不是預設值;要走它們=推翻 plan §6-1=要 Sean)**:

| | 作法 | 代價 / 為什麼沒選 |
|---|---|---|
| **甲(採用)** | 一部署就簽 `v:2`,舊 `v:1` 拒 | 報價單使用者**被登出一次**;零新增機制;**符合 plan §6-1 字面** |
| **乙** | 報價單側新增簽發版本開關(先部署仍簽 v:1 → 翻開關才簽 v:2)+ 過渡期雙讀 | 沒人被登出;⛔ **成本欄已下修** —— 見下方更正,**不是「全新機制」**;仍要 Sean(因為要推翻 §6-1) |
| **丙**(codex 第三輪補,我原本沒想到) | **換一個新的 cookie 名稱**簽 v:2 + 明確清掉舊 cookie ⇒ 舊 code 根本讀不到新 session | 不動 `v` 也不會靜默降級;代價=多一個 cookie 名要維護、清除時機要想清楚;**同樣是新機制** ⇒ 要 Sean |

**報價單側目前沒有「簽 v:1 還是 v:2」這個開關** —— `v` 是**寫死的字面**:
`lib/session.ts:14` 型別 `v: 1`、`:91` 產生處 `return { v: 1, … }`(`origin/main` = `482bec5`,2026-08-17 當場 fetch 後讀)。
⇒ **乙 不是「設定一下」,是要寫 code。** 現況 validator 逐字在同檔 `:110`。

> ⛔ **成本欄更正(V 窗 R1 `nit 2`,2026-08-17;我自己到報價單 repo 複驗過才改)**
>
> 我原本寫「乙 = **新增常駐開關 = 全新機制**(鐵則 8)」。**那個成本高估了。**
> 🔴 **報價單側已經有一個活的版本旋鈕族**,我漏掉是因為**我的 grep 只掃 `process.env`**:
> · `lib/edge-config.ts` `ForceReloginState = { tokenVersion, minValidIat, require2fa }`
>   —— 存在 Vercel Edge Config、**讀失敗 fail-closed**(檔頭逐字「讀失敗 → 高水位 tv/iat」)
> · `lib/edge-config-write.ts:41-43` `writeTokenVersion()` —— **可寫端已存在**
>   (`upsert auth_token_version` / `auth_min_valid_iat`)
> ⇒ **force-relogin 這個機制族已經在跑了**;乙若真要走,**可以沿同族**,不必從零長一個常駐開關。
>
> 🔴 **我漏掉它的原因值得記**:我下的結論是「**零簽發版本開關**」,而我量的是「**零 `process.env`**」——
> **量的東西比結論窄。** 版本旋鈕不一定住在 env 裡,它可以住在 Edge Config、DB、或任何地方。
> ⇒ 判別句:**我的結論講的是「這個東西不存在」,而我的量法只覆蓋了「它可能住的其中一個地方」嗎?**
>
> ⚠️ **這不改變走【甲】**(§6-1 已拍),但**那張表哪天端給 Sean,他會在成本欄上做決定** ⇒ 成本欄必須是對的。

⚠️ **走甲的操作面(這才是施工者要規劃的東西)**:沒有部署順序可排,B3 一上線就是切換點
⇒ **挑一個沒人在用的時段 + 事先告知**。⚠️ 「沒人在用的時段」是什麼時候,**我沒有資料**(要問 Sean 或看日誌)。

**5-A-2 · admin 那半(主詞 = admin;⚠️ 這是 **B5** 的範圍,寫在這裡是為了對照)**

> 🔴 **兩邊的決定【不一樣】,不要讀成同一個**(codex 第四輪抓;我原本寫「同一個決定的另一半」「兩邊同理」):
> · **B3(報價單)= 直接拒 v:1**(走甲、依 plan §6-1)⇒ 使用者被登出一次。
> · **B5(admin)= 暫時接受 v:1**,直到 `ADMIN_REQUIRE_REAL_IDENTITY` 打開才拒。
> **差別的來源**:admin 有那個開關當「之後就死」的出口,報價單沒有(已實查,見上)。

admin 的 `v` 是 **admin 自己簽的**,與報價單的 `v` **互不相干**(見上面 ⛔ 區塊的四條證據)。
🔴 **決定:admin 的新 validator【同時接受 v:1 與 v:2】,
而在 `ADMIN_REQUIRE_REAL_IDENTITY` 開關打開之後【拒絕 v:1】。**
(⛔ 原句寫「**同樣**同時接受」—— 那個「同樣」會再把讀者拉回同理誤讀,codex 第五輪抓;B3 並不接受。)
現況 validator 逐字 `apps/admin/src/lib/session/session.ts:112`。

**admin 為什麼可以暫時接受(這一段【只講 admin】,不適用報價單)**:
- 過渡期要能同時活著(舊 admin cookie 還在使用者瀏覽器裡、TTL 沒到)
- 而開關打開 = 我們宣告「從現在起身分是必需的」⇒ 那一刻 v:1 就該死
  ⇒ **plan §6-1「舊 cookie 必須失效」在 admin 這半是【延後兌現】,不是不兌現** —— 兌現點 = 開關。
- 📎 這讓「開關開著而 session 沒帶身分」的驗收格(plan §6-6)**有東西可測**
- 🔴 **報價單沒有這個延後兌現的機制**(零簽發版本開關,已實查)⇒ 它只能立刻兌現 = 走甲。

⚠️ **順序建議 —— 🔴 只有在上面那題拍【乙】的時候才存在**:
「新 code 先部署但**仍簽 v:1**,之後才翻開關開始簽 v:2」**需要那個開關**,而報價單側現在沒有 ——
數法:`cd ~/API大量上架/PCM報價單-V2 && git show origin/main:lib/session.ts | grep -n 'process.env'`
⇒ 只命中 `NODE_ENV`(`:24`)與 `SESSION_SECRET`(`:44`/`:60`),**零個簽發版本開關**(2026-08-17 當場跑)。
⇒ 拍【甲】的話**沒有順序可排**,B3 一上線就是切換點,要做的是**挑一個沒人在用的時段 + 事先告知**。

🔴🔴 **這條的證據強度,誠實寫**(codex 2026-08-17 抓我原句超出證據):
· **有證據的**:舊 validator 收到 `v:2` 會拒 —— 兩邊 code 逐字 `o.v !== 1`(行號如上)。
· **沒有證據的**:「部署切換期間**一定會**有舊實例還在服務新簽出來的 cookie」。
  那取決於 Vercel 的 alias 切換與舊實例排空行為,**我沒有量過,也沒查過官方文件**。
  ⇒ 上表第三列**會不會真的發生,是未確認的**;上面那條順序建議因此是**廉價保險**,不是「已證實的硬前置」。
  缺的檢查 = 部署後觀察舊實例存活窗,或查 Vercel 對 production alias 切換的官方說明。

### 5-B 真正的跨 repo fail-closed 點:**`amr` enum**(V 窗 `F-1` 指出的那個)

跨 repo 會互相咬到的欄位,依 `apps/admin/src/lib/sso/exchange.ts:7` 的合約逐字有**兩個**:`amr` 與 `auth_time`
(數法:`grep -n 'ok:true' apps/admin/src/lib/sso/exchange.ts` ⇒ 回應形狀就那一行)。
⛔ **本節第一版寫「會咬到的只剩 `amr`」—— 錯,codex 2026-08-17 抓。`auth_time` 一樣會咬:**
`parseAuthTime()`(`exchange.ts:39-46`)只收 `number` 或**純數字字串**,且要 `0 < n <= now+30`
⇒ 報價單若改送 ISO 字串、毫秒、或時鐘快超過 30 秒 ⇒ 回 `null` ⇒ `exchange.ts:84` `if (authTime === null) return null`
⇒ **同樣是登入失敗**。⇒ **兩個欄位都是跨 repo fail-closed 點,而兩個都沒有版本號在保護。**

以下先講 `amr` 這一半(`auth_time` 那一半見本節末):

| 在哪 | 逐字 | `檔案:行號` |
|---|---|---|
| 報價單(送出端) | `type SessionAmr = 'pwd' \| 'totp' \| 'bootstrap' \| 'recovery'` | `~/API大量上架/PCM報價單-V2` `lib/session.ts:11`(`origin/main` = `482bec5`;2026-08-17 當場 `git fetch` 後只讀 origin/main) |
| admin(收下端) | `type AdminSessionAmr = 'pwd' \| 'totp' \| 'bootstrap' \| 'recovery'` | `apps/admin/src/lib/session/session.ts:20` |

**兩邊目前逐字相同。** 而 `apps/admin/src/lib/sso/exchange.ts:11` 逐字寫著:
「`amr` admin 端自驗(**報價單 exchange 直透 DB 值、無白名單**)」
⇒ 報價單**不過濾**,admin 才過濾(`exchange.ts:81-82`:`const amr = sanitizeAmr(rec.amr); if (!amr) return null;`)。

🔴 **後果:報價單若新增一個 amr 值(例如 `passkey`)而 admin 還沒加進 enum ⇒ 那次登入直接失敗。**
⛔ **本節第一版寫「admin 濾掉它;若過濾後為空才失敗」—— 錯,codex 2026-08-17 抓。它根本不過濾。**
`sanitizeAmr()` 逐字(`apps/admin/src/lib/sso/exchange.ts:33-35`):
「🔴 fail-closed(Fable/Codex MF1):**任一元素不在 enum → 整包拒**(非靜默過濾降級)」
⇒ `['pwd','passkey']` **也會失敗**,不需要「只剩一個未知值」這種條件。
⇒ **失敗面比我原本寫的大得多**:只要那次登入的 amr 陣列裡出現一個 admin 不認得的值,那個人就登不進去。
📎 而這個「整包拒」是**刻意設計的**(註解自述是 Fable/Codex 的 must-fix 修法),不是 bug ——
   它換來的代價就是上面那條硬順序。
⇒ **這就是真正的跨 repo fail-closed 點。** 方向是安全的(擋住而不是放行),
   但它**擋的是登入本身**,而且**沒有任何版本號會先警告你** —— `v` 保護不到它。

**`auth_time` 那一半(同族,合約同樣沒有版本號)**

| 送出端 | 收下端的要求 | `檔案:行號` |
|---|---|---|
| 報價單 `exchange` 回 `auth_time` | `number` 或**純數字字串**;`Number.isSafeInteger` 且 `0 < n <= now+30` | `apps/admin/src/lib/sso/exchange.ts:39-46` |

🔴 報價單若改送 ISO 8601、毫秒、或它的時鐘比 admin 快超過 30 秒 ⇒ `parseAuthTime` 回 `null`
⇒ `exchange.ts:84` ⇒ **登入失敗**。方向一樣是安全的,一樣**沒有版本號會先警告**。
⚠️ **時鐘那一項我未量**:兩邊實際的時鐘偏差多少、有沒有 NTP,我沒查 ⇒ 這是「理論上會咬」不是「量到會咬」。

⚠️ **硬順序(跨 repo 這一軸)**:**先在 admin 的 enum 加新值並部署,再讓報價單開始送。**
(`auth_time` 同理:先改 admin 的 parser 再改報價單的送出格式。)
⚠️ **而 B3 這一片不動 `amr`** ⇒ 5-B 對本片**不觸發**;寫在這裡是因為原表把 5-A 誤植成跨 repo,
   **下一個人會照那張錯的表去規劃跨 repo 部署,而那張表指的方向是錯的。**

### 5-C 我未查證的(不要當我查過)

· 報價單那側**有沒有計畫要加 amr 值** —— 我沒問、沒查它的 backlog。
· 報價單的 `ver`(token_version,`lib/session.ts:19`)與 admin **無關**
  (admin 的 payload 沒有這一欄,`apps/admin/src/lib/session/session.ts:22-30`),
  但它是報價單自己的強制重登機制;若那邊為了 B3 動 `ver`,影響面在報價單側,**本規格未涵蓋**。
· 5-A 那張表的第三列(舊實例硬拒 v:2)**是我從 code 推的,沒有在真部署上觀察過**。

---

## §6 驗收(負向為主)

> 🔴🔴 **主詞警告(codex 第三輪抓,2026-08-17)**:下表第 1-3 格寫「新／舊 **admin**」,
> 而 **B3 這一片動的是【報價單】**(`…real-auth-line-plan-v4.md:165`「B3 … **報價單** auth」;
> admin 那半是 **B5**,同檔 `:167`)。
> ⇒ **照字面做的施工者會去改錯 repo、驗錯 validator。**
> ⚠️ 而這幾格的內容**本身沒有錯**,只是它們描述的是 **B5** 的驗收 —— 兩片的 payload 各自升版、各自要測。
> ⇒ **1-3 格逐格標主詞如下;它們的最終歸屬(留在本檔還是搬去 B5 spec)未定,不要當它已經分好。**

| # | 構造 | 預期 |
|---|---|---|
| 1 | 【**B5** · admin】舊 v:1 cookie 送到**新** admin,開關**關** | ✅ 接受,視為無身分 |
| 2 | 【**B5** · admin】舊 v:1 cookie 送到**新** admin,開關**開** | 🔴 **拒絕** |
| 3 | 【**B5** · admin】v:2 cookie 送到**舊** admin | 🔴 **拒絕**(rollback 安全網,要證明它真的擋) |
| 3b | 🆕【**B3** · 報價單】舊 v:1 cookie 送到**新報價單** | 🔴 **拒絕**(走甲的直接後果;這格才是本片的;對應 plan v4 `:198` §6-1) |
| 4 | v:2 但 `sub` 欄缺 | 🔴 **拒絕**(型別上不該存在,但要證明 validator 也擋) |
| 5 | v:2 且 `sub.kind='fallback'` | ✅ 接受,而**任何寫入都被擋**(備援唯讀,plan §7-Q1=A) |
| 6 | ⛔ **本格 2026-08-17 標為【未決,先不要照它施工】**(codex `#9`,R2 判定第一版折法錯)。原文:v:2 且 `sub.staff_id` 不在白名單 ⇒ 拒絕 | 🔴 **這一格目前【沒有 runtime 執行者】**:`CHECK` 只在**寫入映射列**時擋,簽發時的 `SELECT` 不重跑它 ⇒ 若 `CHECK` 成立,本格的狀態**構造不出來(恆綠、零判別力)**;若要它真的擋,**必須另定應用層驗法**,而那會複製清單。**分工與取捨見 §3 那張表下方的 ⛔ 區塊。** ⚠️ **admin 端不跑本格** —— admin 拿到 `staff_id` 之後跑的是 `resolveStaff()`(查**在職名單**),那些格在 §7.1 |
| 7 | 簽章正確但 `sub` 被竄改 | 🔴 **拒絕**(HMAC 涵蓋整個 payload,這格證明 `sub` 在簽章範圍內) |
| **8** | 🆕【**B3 · 跨 repo**】部署當下,**報價單 exchange 送得出的 amr 值集合 ⊆ admin `VALID_AMR` 現值**。🔴 **admin 的 `VALID_AMR` 有【兩份】**(`sso/exchange.ts:30` 與 `session/session.ts:105`)⇒ **兩份都要比,而且兩份彼此也要相等** | ✅ **必須成立**;不成立 ⇒ **不得部署** |
| **8b** | 🆕【**B3 · 跨 repo**】拿一個**不在** admin enum 內的 amr 值餵**兌換路徑**。⚠️ **不要指名 `sanitizeAmr`** —— `exchange.ts:32` 逐字 `function sanitizeAmr`(**未 export**);**現成格已經有了**:`apps/admin/src/lib/sso/exchange.test.ts:53` 逐字 `rejects whole exchange if ANY amr value is unknown (fail-closed, MF1)`(經 `exchangeCode` + 注入 fetch;正向對照在同檔 `:60`)⇒ **直接指到那格,不要為了餵它去 export 私有函式或另寫一格** | 🔴 **整包拒**(證明 8 那道比對不是恆真 —— 它真的分得出兩個世界)。⚠️ **但 8b 證的是「閘有判別力」,不證「8 的字面比對追蹤得住閘」** |

> 🔴🔴 **第 8 / 8b 兩格是本次補的,而它們補的是【我自己剛寫出來的失敗面】**(V 窗 R1 `must-fix`):
> §5-B 白紙黑字寫著「報價單新增一個 amr 值而 admin 還沒加 enum ⇒ **那次登入直接失敗**」,
> 而原 §6 **零 amr / enum 相關驗收**(數法:`sed -n '/^## §6/,/^## §7/p' <本檔> | grep -c 'amr\|enum'` ⇒ 補這兩格之前是 **0**)。
> ⇒ **我把失敗面【描述】出來了,而描述不會在部署日阻止任何人。一列驗收會。**
> 📎 形狀 = memory `feedback_seen-written-handled-are-three-steps`:看到 → 寫下來 → **停在第二步**。
> ⚠️ 而 **B3 就是報價單 auth 線 = 最可能引入新 amr 值的那一條** ⇒ 這格不是理論風險。
>
> 🔴🔴 **量法量的尺,不是閘用的尺(V 窗 R1 `must-fix`,2026-08-17 折)**:
> 本格原本 ①②③ 讀的都是**型別字面**,而 **admin 真正拒人的閘是兩份 runtime Set**:
> ```
> apps/admin/src/lib/sso/exchange.ts:30        const VALID_AMR = new Set<AdminSessionAmr>(['pwd', 'totp', 'bootstrap', 'recovery']);
> apps/admin/src/lib/session/session.ts:105    const VALID_AMR = new Set<AdminSessionAmr>(['pwd', 'totp', 'bootstrap', 'recovery']);
> ```
> (2026-08-17 本窗當場 `git grep -n 'VALID_AMR' apps/admin`,非轉述。)
> **TS 不要求 `new Set<T>([...])` 窮舉 `T` 的成員** ⇒ 型別加了新值、兩份 Set 不動,
> **`tsc --noEmit --strict` 照樣綠,而閘照拒**。
> 🔴 **本窗獨立複驗(不是照抄 V 窗)**:純核心 4 行(型別加 `'webauthn'`、Set 不動)
> 跑 `tsc --noEmit --strict` ⇒ **rc=0 綠**;同一組值跑 runtime ⇒ **`gate accepts webauthn: false`**。
> ⇒ **型別量的是「宣告得出什麼」,閘判的是「Set 裡有什麼」——量法量的尺不是閘用的尺。**
>
> 🔴 **兩份 Set 不同步的部署日世界,比 §5-B 寫的更難診斷**:
> 若 `exchange.ts:30` 加了新值而 `session.ts:105` 沒加 ⇒ `sanitizeAmr` **放行**,
> admin **簽出**含該值的 cookie(`session.ts:27` 逐字「報價單傳來、admin 已白名單過濾」,簽在 `:88`),
> 下一個 request 走 `verifySession` → `isPayload` `:118` **整包拒**
> ⇒ **「登入成功、下一秒被登出」的迴圈**;而 §5-B `:238` 描述的是「**那次登入直接失敗**」
> —— **後者一眼看得出,前者會被當成 cookie 或時鐘問題查半天。**
>
> **量法(可重跑;2026-08-17 改版)**:
> ```bash
> # ① admin 端【閘用的尺】—— 兩份 runtime Set，兩份都要印出來
> git grep -n 'VALID_AMR = new Set' apps/admin
> # ② 報價單端送得出的值（🔴 只讀 origin/main，絕不 db push）
> cd ~/API大量上架/PCM報價單-V2 && git fetch -q origin \
>   && git show origin/main:lib/session.ts | grep -n 'SessionAmr ='
> # ③ 逐字比對值集合：② ⊄ ①的任一份 ⇒ 紅；① 兩份彼此不相等 ⇒ 也紅
> # ④ 附加旁證（🔴 不可取代 ①）：型別字面，用來看型別與 Set 有沒有各走各的
> git grep -n 'AdminSessionAmr =' apps/admin/src/lib/session/session.ts
> ```
> **2026-08-17 本窗當場跑的結果**:① 兩份 Set 逐字相同(見上方引用塊);
> ② 報價單 `origin/main` = `482bec5`,`lib/session.ts:11` 逐字
> `export type SessionAmr = 'pwd' | 'totp' | 'bootstrap' | 'recovery';` ⇒ **今天成立**。
> 🔴 **而「今天成立」不等於「部署日成立」—— 這一格的價值就在部署日重跑,不在今天。**
> ⚠️ 上面 ①②③④ 都是**字面比對**;`8b` 才是行為對照(證明比對這件事有判別力)。
>
> ⚠️ **射程限定(V 窗 `N-1`,2026-08-17 收)**:本格左句寫「報價單 exchange **送得出**的 amr 值集合」,
> 而 `apps/admin/src/lib/sso/exchange.ts:11` 檔頭逐字「**報價單 exchange 直透 DB 值、無白名單**」
> ⇒ **真正送得出的是報價單 DB 的存量值,而 ② 讀的型別只是它的【代理】**。
> **DB 裡若有型別外的存量值,② 看不到它。**
> ⇒ 本格是**代理比對,不是母體比對**;要更硬只能在報價單側查 DB 的 distinct 值,
> **本片不做,明寫成射程上限 —— 不讓宣稱超過量法射程。**
> ⚠️ **`auth_time` 那一半沒有對應的驗收格** —— 它的漂移(ISO / 毫秒 / 時鐘快 30 秒)我想不出便宜的部署日檢查,
>   **明寫成缺口,不假裝有守**。

🔴 **第 7 格最容易被跳過** —— 它證的不是「HMAC 有效」,是「**`sub` 沒有被放在簽章之外**」。
📎 兩邊的簽章都是 `base64url(payloadJSON) + '.' + base64url(HMAC(payloadJSON))`
(`lib/session.ts:7` 檔頭註解)⇒ 只要 `sub` 在 `payloadJSON` 裡就涵蓋得到。
**而「只要」那兩個字,正是需要一格測試的地方。**

---

## §7 未決 / 未驗

1. ~~`staff_id` 白名單的單一來源未定~~ **✅ 2026-08-16 定案 = (b)。**
   ⛔ ~~而且它【已經實作好了】~~ **這半句 2026-08-17 撤回(codex `#13`/`#14`)** ——
   **已實作的是 `resolveStaff()` 這支函式,不是「B5 的讀取閘已經在用它」。詳見下方 §7.1 那個 ⛔ 區塊。**
2. **`Q-AUTH-1` 甲案前提:②已證、③未證**
   (②=那些帳號有 email 身分與密碼,SQL 查得到;③=Email provider 現在是開的,**SQL 原理上查不到**,
   它住在 GoTrue config 不在 DB)。
3. ⛔ ~~**本片尚未經對抗審查**~~ **已過期,2026-08-17 更正(codex R3 `nit`:世界狀態寫進 spec 會持續過期)**。
   **現況(綁輪次,不寫「已審過」這種會再過期的字)**:
   `docs/reviews/2026-08-17-b3-spec-codex-findings.md` = **codex R1,21 must-fix**;
   同日 **R2 / R3** 兩輪審的是**折法**(R2 = 4MF+3nit、R3 = 換角度 8MF+2nit,皆已折或已明列未折)。
   🔴 **「審過」不等於「審完」** —— 21 條中仍有多數未折,逐條判定看那份 findings 檔的「折況」表。
4. **`auth_time` 的語意問題未解**(admin `session.ts:26-27` 自述:它隨 sliding refresh 更新,
   **不是不變的登入時刻**)⇒ 若日後要做 step-up「近期驗證過」,那個欄位形同虛設。
   **本片不解決,但寫進來免得下一個人拿它當登入時刻用。**

---

## §7.1 定案:`staff_id` 的驗證走 **(b)** —— ⛔ ~~而查完之後發現它已經實作好了~~

> ## ⛔⛔ **標題後半 2026-08-17 撤回(codex `#13` / `#14`,B 窗當場複驗屬實)**
>
> **已經實作好的是 `resolveStaff()` 這支函式。**
> **「B5 的讀取閘已經在用它」——【不成立】。**
>
> 逐字證據(2026-08-17 本窗當場 `git grep -n 'resolveStaff' apps/admin packages`,非轉述):
> ```
> apps/admin/src/lib/session/actor.ts:35        resolveStaff(store.get(ACTOR_COOKIE)?.value)   ← 吃的是 actor cookie
> apps/admin/src/lib/session/actor-actions.ts:24 raw === null ? null : await resolveStaff(raw)  ← 吃的是「選人」動作的輸入
> ```
> **非測試原始碼的呼叫端就這兩處(其餘命中皆為 `*.test.ts`)** ⇒ **沒有任何一處吃 `session.sub`。**
> ⚠️ **「非測試原始碼」≠「正式部署中的產物」**(codex R2 `nit`):
> 這是**當前工作樹**量的;**正式部署上是不是同一版,本窗未確認** ——
> **缺的那一道檢查 = 正式部署 SHA 與本工作樹 HEAD 的對照。**
> ⇒ **`#13`**:v:2 只有 `session.sub`、沒有 actor cookie 時,**現有路徑解析不到人**。
>
> **⇒ `#14` 更嚴重 —— 讀取閘從來沒有呼叫過它**:
> ```
> apps/admin/src/proxy.ts:39-50   登入閘只做 verifySession(...)；整支 proxy.ts 零 resolveStaff
> ```
> (數法,2026-08-17 當場跑:`grep -c resolveStaff apps/admin/src/proxy.ts` ⇒ **0**;
>  正向對照 `grep -c verifySession apps/admin/src/proxy.ts` ⇒ **2**
>  ⇒ **同一把量具在兩個世界印出不同的數 ⇒ 那個 0 是量出來的,不是「我沒找到」。**
>  ⚠️ **那個 `2` 是【文字命中數】,不是呼叫端數**(codex R2 `nit`):
>  一筆是 `:3` 的 `import`、一筆是 `:40` 的實際呼叫 ⇒ **runtime 呼叫只有一處**。
>  它在這裡的用途只是**證明量具會動**,不要把它讀成「兩個呼叫端」。)
> ⇒ **一個已停用的員工,只要手上那顆 HMAC session 還沒過期,現在就走得過讀取閘。**
>
> 🔴 **病根(B 窗自陳,已寫進 findings 檔 §「我先前的複驗結論錯了一半」)**:
> 我問的是「誰呼叫 `resolveStaff`?有沒有人繞過它?」—— 答案確實是「沒有人繞過」;
> **該問的是「該走它的那些路徑,有沒有走它?」** ——
> **一條從來不呼叫它的路徑,在「呼叫端清單」裡是看不見的。**
>
> ⚠️ **歸屬未定**:`#13`/`#14`/`#15` 修的都是 **B5 讀取閘**的內容,而 codex `#2` 指出
> 母 plan(`real-auth-line-plan-v4.md:165-168`)把 admin 收端放在 **B5**。
> ⇒ **本節現在只【更正事實】,不決定這些內容留在本檔還是搬去 B5 spec** —— 那是 plan 層的決定,`#2` 未折。

### 決定(一句)

> **B5 收到 `sub.staff_id` 之後,必須把它丟進 `resolveStaff()`,不得直接使用。**
> **admin 端不持有任何 `staff_id` 白名單。**
> 🔴 **而「必須」=【還要做】,不是【已經在做】** —— 見上方 ⛔ 區塊。

### 🔴 為什麼這不是「少做一道檢查」—— 兩個問題,兩個地方,不是重複

| 問題 | 誰是權威 | 在哪 |
|---|---|---|
| **誰【可以被綁定】帳號** | 報價單庫 `admin_user_staff_map` 的 CHECK | B1-b |
| **誰【現在可以操作】** | **A 庫 `staff` 表的 `is_active`** | 既有的 `resolveStaff()` |

**這兩個問題不同,所以兩個地方不是「同一份清單抄兩次」。**
⇒ (c) 共用常數檔會把它們**合併成同一份**,而那正是 house 教訓「兩處硬編碼會漂」的形狀。

### ✅ 而 (b) 需要的**那支函式** repo 裡已經有了(2026-08-17 重新實查行號)

⚠️ **這一節說的是「函式在」,不是「閘在用它」** —— 後者被 `#13`/`#14` 打掉,見本節開頭 ⛔ 區塊。

```
apps/admin/src/lib/session/actor.ts:33      getSessionActor()
apps/admin/src/lib/session/actor.ts:35      └─ resolveStaff(store.get(ACTOR_COOKIE)?.value)   ← 🔴 吃 actor cookie，不吃 session.sub
apps/admin/src/lib/staff.ts:38              resolveStaff(id)  → pickStaff(await listActiveStaff(), id)
apps/admin/src/lib/staff.ts:25              listActiveStaff() → 讀 staff 列
apps/admin/src/lib/staff.ts:29              └─ .filter((row) => row.is_active)
apps/admin/src/lib/staff.ts:33              └─ DB 失敗 ⇒ return []
apps/admin/src/lib/staff.ts:16              pickStaff()       → 找不到就 null
```
⚠️ **舊版本節寫 `actor.ts:32` / `staff.ts:38-43` / `:25-35` / `:16-22`,行號已漂**(2026-08-17 當場重量,上表為現值)。
⇒ **一次查詢同時給三件事:**
1. **成員資格**(不在 A 庫 `staff` ⇒ null)
2. **存活**(`is_active=false` ⇒ 被 filter 掉 ⇒ null)
3. **fail-closed**(`listActiveStaff` 在 DB 失敗時回**空陣列** ⇒ 一律 null,`staff.ts:31-34`)

⛔ **原句**:「我先前釘給 `B5` 的那條 R2 finding(「每次用的時候重新確認 `is_active`,不可只在綁定當下驗一次」)
**【現有這條路徑已經滿足】—— 只要 B5 不繞過它**。」
🔴 **2026-08-17 更正(codex `#13`/`#14`)**:正確說法是
**「這條路徑【走到的時候】滿足 R2;而讀取閘【根本不走這條路徑】」。**
⇒ **R2 目前【未關閉】**,關閉條件 = **B5 讓讀取閘真的呼叫 `resolveStaff(sub.staff_id)`**。
⚠️ 原句「只要 B5 不繞過它」把問題框成「別繞過」,而事實是**還沒有接上去** ——
**「不要繞過既有的那一條」與「既有的那一條已經在路上」是兩句話,我把後者當成前者的贈品。**

📎 **形狀值得記**:我本來要為 B3 設計一套新的驗證機制,
**而正確答案是「不要繞過既有的那一條」。**
⇒ **判別句:我要加的這道檢查,現有的哪一條路徑已經在做了?**

### 為什麼不是 (a):admin 啟動時讀一次

- **啟動期相依**:讀失敗 ⇒ 要嘛啟不動、要嘛拿著空白名單啟動(fail-open 或 fail-closed 都難處理)
- 🔴 **更關鍵:它是【快取】** —— 而 `is_active` 是**會變的**。
  啟動時讀一次 ⇒ **一個離職員工在下一次重啟前都還是「有效」的**
  ⇒ **這正好把「每次用的時候驗」退化回「綁定那一刻驗」**,而那是 R2 抓到的原病。

### 為什麼不是 (c):共用常數檔

- **兩處硬編碼必然漂**:A 庫加了人而常數檔沒改 ⇒ 那個人登入後被當成未知
- 🔴 **而漂的方向是靜默的**:常數檔不會因為 A 庫改了而紅
- 📎 house 教訓原句:**「兩份會漂」** —— 而 (c) 是製造第二份的唯一一案

### ⚠️ (b) 的代價,寫下來不掩蓋

1. **每次操作多一次 DB 查詢**(`listActiveStaff`)。
   ⛔ ~~現況已經如此(`getSessionActor` 本來就這樣),**本片不引入新成本**~~
   🔴 **2026-08-17 更正(codex `#17`,由 `#14` 帶出的同一個事實)**:
   「現況已經如此」**只對【寫入/選人】那條路成立**(`getSessionActor` 那條);
   **讀取閘現在零 staff 查詢**(`proxy.ts:39-50` 只做 `verifySession`)
   ⇒ **B5 接上之後,每個頁面都會新增一次 DB 相依** —— **那是新成本,不是既有成本。**
   ⚠️ 另外 codex 同條指出 **proxy 載入 server adapter 的 runtime 相容性未確認** ——
   **本窗未查證**(缺:在 proxy 執行環境實跑一次 `listActiveStaff` 的對照)。
   📌 `#17` 的**事實半邊**在此更正;它要不要因此改設計(例如讀取閘改用別的機制),
   **是 plan 層的決定,`#17` 形式上仍未折。**
2. 🔴 **DB 失敗時,所有人都變成「未知身分」而不是「系統錯誤」** ——
   `listActiveStaff` 逐字「DB 失敗時回空陣列並留 server log」(`staff.ts:24`)。
   **方向是安全的(fail-closed),但症狀會是「全公司突然都沒權限」而不是「資料庫掛了」。**
   ⇒ **本片不改它**(改它是另一個決定),但**列在這裡**,免得那天有人查半天。

### 🆕 read / write 矩陣(codex `#15`,2026-08-17 補)

> codex 逐字:「若讀取閘一律要求 `resolveStaff(sub.staff_id)`,fallback 因無 `staff_id` 而**連唯讀都進不去**;
> 若跳過又沿用 actor cookie,則**可能寫入**,規格缺 read/write 矩陣。」
> 🔴 **這個洞是 §3 那個 union 直接長出來的**:`kind:'fallback'` **型別上就沒有 `staff_id`**
> ⇒ **`resolveStaff()` 對它無從呼叫**,而規格先前只寫了 `kind:'user'` 那一半。

| `sub` | **讀** | **寫** | 閘怎麼判 |
|---|---|---|---|
| `{kind:'user', staff_id}` **且** `resolveStaff` 回**非 null** | ✅ 允許 | ✅ 允許,稽核 actor = 該 `staff_id` | 走 `resolveStaff(sub.staff_id)` |
| `{kind:'user', staff_id}` **而** `resolveStaff` 回 **null**(停用/查無/DB 失敗) | 🔴 **拒絕** | 🔴 **拒絕** | 同上;**三種 null 一律拒**(§下方「兩個世界長得一樣」) |
| `{kind:'fallback'}` | ✅ **允許**(對應 §6 第 5 格) | 🔴 **一律擋**(備援唯讀,plan `§7-Q1=A`) | 🔴 **不呼叫 `resolveStaff()`** —— 它沒有 `staff_id`,呼叫本身就是型別錯誤 |

🔴 **三條硬規定,缺一就回到 codex 描述的那兩個壞世界:**
1. **讀取閘必須先 branch `sub.kind`,再決定要不要呼叫 `resolveStaff()`** ——
   **不是「一律呼叫」**(那會讓 fallback 連唯讀都進不去,= codex 說的第一個壞世界)。
2. 🔴 **fallback 走的那一支【不得】沿用 `ACTOR_COOKIE` 補身分** ——
   那正是 codex 說的第二個壞世界(**跳過解析 ⇒ 又從 actor cookie 取得寫入能力**),
   而 `actor.ts:35` 現行行為逐字就是讀 `ACTOR_COOKIE`。
3. **寫入閘不接受「沒有具名 staff」的任何形狀** —— fallback 的寫入攔截點必須是**寫入端自己**,
   不能靠「讀取閘擋掉它就不會走到寫入」(讀取是允許的,它一定走得到)。

⚠️ **本矩陣的作用域 = admin(收端)這一半**(codex R3 `must-fix`,2026-08-17 標明):
§6 第 5 格寫 fallback「**任何**寫入都被擋」,而那個「任何」**跨兩個 repo**;
**本矩陣與下方驗收 6/7/8 格只定義 admin 側** ⇒ **報價單側的對應矩陣【缺】**。
🔴 **不得拿一組 admin 綠燈當成「任何寫入都擋住了」** —— 缺的那一道 = **報價單側 fallback 的寫入面盤點**。

⚠️ **本矩陣不決定 `ACTOR_COOKIE` 的清除時機與優先序** —— 那是 codex `#4`/`#5`/`#12` 的範圍,**未折**。
🔴 **而「未折」不是免責貼紙,缺的那一道寫在這裡**(codex R3 `must-fix`):
本矩陣已經要求「稽核 actor **必須等於** `sub.staff_id`」,**但沒有規定當兩者【不一致】時誰贏** ——
`{kind:'user', staff_id:'sean'}` 的 session 配上 `pcm_admin_actor=staff_2` 的舊 cookie,**現在無解**。
⇒ **缺的決定 = 明文寫「`session.sub` 永遠勝過 `ACTOR_COOKIE`,且不一致時要不要清 cookie / 要不要拒絕該次請求」**,
  **並在 user / fallback 兩支各配一格優先序測試。** 這條**歸 `#4`/`#5`/`#12`,由 plan 層決定,本片不自行拍板。**

#### 🔴🔴 第 3 條的**涵蓋證明**(codex R2 `must-fix`,2026-08-17 補)

> codex 逐字:「只拿**一支**寫入路徑跑第 7／8 格 ⇒ 該測試全綠,但**另一支**未經 `authorizeAdminMutation()`
> 的寫入仍可讓 fallback 寫入。」
> ⇒ **矩陣寫對了「該擋什麼」,沒有寫「怎麼證明每一條寫入路徑都被擋到」。**
> 📎 這正是 house 教訓「**一格綠只證那一格**」的形狀:第 7/8 格是**行為格**,它的分母是施工者挑的。

**⇒ B5 驗收追加一條【結構】要求(不是再加一格行為測試):**

> **admin 的【業務】寫入必須全部經過單一授權閘 `authorizeAdminMutation()`,並附一條可重跑的涵蓋數法。**
>
> 🔴🔴 **「業務」兩個字是必要的,不是修飾(codex R2 `NEW-MF1`,2026-08-17 當場折)**:
> 第一版寫「**admin 寫入全部**經 `authorizeAdminMutation()`」—— **照字面會鎖死登入**。
> **SSO 的 `start` / `callback` 依定義必須【未登入可達】**(`proxy.ts:12` 的
> `SSO_OPEN_PATHS = new Set(['/api/sso/start', '/api/sso/callback'])`),
> **而它們會寫 state / session cookie** ⇒ 給它們加授權閘 = **登入前要先登入**。
> ⇒ **本規則的範圍 = 「已登入之後、代表某個 staff 做的資料變更」**;
>   **明文排除:SSO 入口那兩支**(排除清單就是 `SSO_OPEN_PATHS` 那兩條,**不另立第二份名單**)。
> 📎 形狀:**一條「全部都要」的規則,它的價值在【例外寫在哪裡】** —— 沒寫,下一個人要嘛照做鎖死系統、要嘛自己判斷而不留痕。

**2026-08-17 本窗當場量的現況(數法與分母都寫在這裡,不要只抄結論)**:

分母 A = `'use server'` 模組(單/雙引號、行首容許空白),**逐行貼上、`#` 註解已移到行外**
(🔴 zsh 命令內不得含獨立 `#` 註解 —— `CLAUDE.md` 終端機紀律;codex R3 `nit`,同日修):
```bash
git grep -lE "^[[:space:]]*['\"]use server['\"]" apps/admin | grep -v '\.test\.' | wc -l
```
⇒ **19**。未經授權閘的是哪些:
```bash
comm -23 <(git grep -lE "^[[:space:]]*['\"]use server['\"]" apps/admin | grep -v '\.test\.' | sort) \
         <(git grep -l 'authorizeAdminMutation' apps/admin | grep -v '\.test\.' | sort)
```
⇒ **恰好一支:`apps/admin/src/lib/session/actor-actions.ts`**。

🔴🔴 **而那唯一一支,正好就是【設定 `ACTOR_COOKIE` 的那一支】** ——
也就是本矩陣**第 2 條硬規定**要防的那個機制本身。**這不是理論缺口,它今天只有一個成員,而那個成員是最相關的那個。**
⚠️ **但它的處置【未定】,本節不指定(codex R3 `must-fix`)**:
「加現行閘」是最直覺的一步,而 `actor-actions.ts` 是**自選身分機制**本身 ——
真登入線接上之後它可能該**去權威化或整支退場**,而不是加閘保留。
⇒ **三條路(加閘 / 去權威化 / 退場)都還沒有人拍板** ⇒ **施工者不得自行選一條**;
**這一格的正確動作是回報,不是實作。**

🔴🔴 **這條數法自己的假設,寫出來(codex R3 `must-fix`,角度=假設審查)**:
`git grep -l` 只證明**該檔出現過**這個符號,**不證明檔內每一支寫入函式都先呼叫了它**
⇒ **一個檔裡有 5 支 action、只有 1 支加了閘,這條數法一樣把它算成「已涵蓋」。**
⇒ **本數法是【檔級】篩子,不是【函式級】證明。**
**缺的那一道檢查 = 函式級:逐支 exported action 確認它的第一個授權動作是 `authorizeAdminMutation()`**
(便宜做法 = 加一格結構測試或 lint rule,**本片不做,明寫成缺口**)。

**第二個面 = route handler(2026-08-17 已逐支定性,不是估的)**:
```bash
git ls-files 'apps/admin/src/app/api/**/route.ts' | grep -v '\.test\.'
# ⇒ 恰兩支：apps/admin/src/app/api/sso/callback/route.ts
#            apps/admin/src/app/api/sso/start/route.ts
git grep -l 'authorizeAdminMutation' -- 'apps/admin/src/app/api/**/route.ts' | wc -l   # ⇒ 0
```
🔴 **那個 `0` 是【對的】,不是缺口** —— 這兩支**就是** `SSO_OPEN_PATHS` 那兩條,
**依定義必須未登入可達**,而上面的排除條款正是為它們寫的。
⇒ **兩個面加起來 = 19 + 2,而未經授權閘的只有 `actor-actions.ts` 一支(真缺口)+ SSO 兩支(明文排除)。**

⚠️ **射程限定(仍然不宣稱全涵蓋)**:上面兩個分母是 **`'use server'` 模組**與 **`app/api/**/route.ts`**。
**第三個面 = server component / 其他非 action 路徑裡的直接寫入,本窗【未量】。**
**缺的那一道檢查 = 從 DB 寫入端(repository / RPC 呼叫)反向盤點呼叫者,而不是從 action 檔正向數。**
📎 這正是 `#13`/`#14` 那個病根的同一形狀:**正向數「用它的人」,數不出「該用它而沒用的人」。**

### 驗收(B5 那片要跑)

| # | 構造 | 預期 |
|---|---|---|
| 1 | `sub.staff_id` 是 A 庫啟用中的員工 | ✅ 解析成功 |
| 2 | `sub.staff_id` 在 A 庫**已停用** | 🔴 **拒絕**(這格證 R2 那條被關掉了) |
| 3 | `sub.staff_id` 在 A 庫**查無** | 🔴 拒絕 |
| 4 | A 庫**連不上** | 🔴 拒絕(fail-closed,不是放行) |
| 5 | **對照組**:第 1 格若也紅,前四格零判別力 | 第 1 格必須綠 |
| 6 | 🆕 `{kind:'fallback'}` 打**讀取**路徑 | ✅ **進得去**(這格證第 1 條硬規定:不是一律呼叫 `resolveStaff`) |
| 7 | 🆕 `{kind:'fallback'}` 打**寫入**路徑 | 🔴 **擋**(這格證第 3 條:攔在寫入端,不是靠讀取閘) |
| 8 | 🆕 `{kind:'fallback'}` **且瀏覽器仍帶著舊 `pcm_admin_actor`** | 🔴 **仍然擋寫入、稽核不得記成那個人**(這格證第 2 條;**沒有這格,前一格會在有 actor cookie 的真實瀏覽器裡假綠**) |
