# 後台完成地圖(2026-08-01 建;**2026-08-11 夜 = §-4 27 項 + §-5 40 片與退款線,兩節合起來才是現況**)

> 🔴 **本檔的口徑(2026-08-11 立,以後每次刷新都照這句)**:這張地圖量的是
> **「畫面/檔案在不在、有沒有掛上去」**,**不是「員工按下去會不會對」**。
> ⇒ 標 ✅ 只代表**路徑通了**;**能不能用是 Sean 肉眼驗的事,任何 session 都不得代他宣告**。
> 每一格都附**檔名或已試 pattern**;寫不出證據的格子一律標 ❓,不留白也不猜。
> ⚠️ **「X 全缺 / 0 命中」這種全稱句有保鮮期** —— 它是**某一天**的觀察,不是屬性。
> 引用前重跑那條 grep;08-09 版就是因為被當成屬性沿用,害主視窗用它派了一次重複的工。

## §-5 · 08-11 夜 **40 片分類 + 退款線重走**(E 窗六代;樹=`2f663880`;還 §-2.5-4 的債)

**一句話:§3 那 40 片(08-03 口徑=「還沒做的」)現在 **31 片已完成**、剩 8 片沒動 + 1 片半套;
§4 退款線五列裡 **有三列的 ❌ 已經是假的**,其中「`refund()` 仍一行 throw」在 §-2.1 就被推翻過,
但 §4 那張表當時沒跟著改 —— 這正是 §-2.5-4 這筆債的實際損害。**

### §-5.0 數法與**兩個不同的母體**(先講,不然下面每個數字都會被誤讀)

| 名字 | 值 | 母體是什麼 | 數法 |
|---|---|---|---|
| 母 plan §5.1 資料列 | **69** | 第 1 批**所有**片(含早已完成的) | `awk` 數 `### 5.1` 到 `#### 5.1b` 之間第一欄為數字的列 |
| 其中片名標作廢 | **2** | 同上 | `A5b`(row 27)、`A5c`(row 45)—— 只認**片名格**被 `~~` 包住的列。⚠️ 用 `/~~/` 掃整列會撈到 8 列(D1b1/A2/A1/A2b1/A8a1/A8a2/A10b 內文都有刪節號),**那是錯的數法** |
| 供應商線 | **+5** | 刻意不列進 §5.1 | 權威在 `docs/specs/2026-08-01-e10-supplier-master-plan.md` §4 |
| **本檔 §3 的「40 片」** | **40** | 🔴 **只有「08-03 當下還沒做的片」**,不是第 1 批全部 | 08-01 口徑 43 − A5b − A5c − A6 |

⇒ **69 與 40 不是同一件事的兩個版本,是兩個母體**。本節重走的是**後者**。
(母 plan `:471-475` 自己就寫了「片數一律當場 `awk` 數、不寫死」——本節照做。)

### §-5.1 40 片逐片現況(每片附證據;`✅` = 有落地成品或 apply 收帳 commit)

**判準與它證明不了的事**:主要證據是**成品在不在**(檔案/常數/掛載鏈);
commit 主旨只當**輔證**,數法 = `grep -E "\bID([^A-Za-z0-9-]|\|)"` 掃 `git log dev --format='%h|%s'`
(2486 筆)。⚠️ 這條 regex 我先用正負對照驗過(`A11a` 要中、`A11a-1` 要不中)才拿來數;
`(^|…)` 那種寫法在本機 BSD grep 下**恆為 0 命中**(實測),別照抄。

#### 高風險 9 片 —— **9/9 完成**

| 片 | 狀態 | 證據 |
|---|---|---|
| A2b1 | ✅ | `687ef769` 主旨「A2b1 apply 完成 — 正式站 read-back 逐項全符」 |
| A4a / A4b | ✅ | `e32d3d51`「A4a+A4b 收割收帳 — apply 佇列」 |
| A5a | ✅ | `4eeb1d89`「A5a 保留模式下聯絡管道必填」 |
| A9v | ✅ | `c2a5e467`「E 批兩支 apply 正式站(A9v…)」 |
| A8c1 / A8c2 | ✅ | `f9614620`「A8c1+A8c2 apply 收帳 — 取消守門正式站」 |
| A8a1 / A8a2 | ✅ | `a5a1e0ff`「A8a1+A8a2 apply 收帳 — 取消線 4/4 正式站」 |
| A9d2-2 | ✅ | 成品:`lib/orders/cancel-actions.ts`;`123d0445`「A9d2-2 片0+片1」 |

#### 機械 19 片 —— **15 ✅ / 1 🟡 / 3 ❌**

| 片 | 狀態 | 證據 |
|---|---|---|
| A2b2 · A4b · A9a-2 · A9c · A9h · A9f | ✅ | 各有收割/收工 commit;A9f 另有實作片 `68988ea8`「顧客訂單 paid 一律顯示『處理中』」 |
| A9b2 | ✅ | migration `20260807130000_m4b_e10_a9b2_m_supplier_order_no_upper.sql`(`SupabaseOrderAdapter.ts:587` 引用) |
| A9g | ✅ | 成品:`lib/orders/cancel-repository.ts` + `cancel-view.ts` + `lib/payment/refund-read.ts` |
| A9w1 · A9w2 · A9w3 · A9w4a · A9w4b · A9w4c | ✅ | 六片各有 `refactor(...)` 實作 commit(九碼退場) |
| A10c2 | ✅ | `order-filter-controls.tsx:74/134`;搜尋面另有逐批啟用閘(`#397` 在 Sean 桌) |
| **A9d1** | **🟡 半套** | `procurement` 支在(`lib/orders/procurement-actions.ts`);**`counters` 支查無**:`grep -rln counters apps/admin/src/lib --include='*.ts' \| grep -v '\.test\.'` = **0 命中**;主旨命中也是 0 |
| **A9e** | **❌** | 「admin stale `fulfillment_status` 顯示端下架」**沒下架**:`FULFILLMENT_STATUS_LABEL` 仍在 `order-detail.tsx:4` import、`customer-detail-sections.tsx:71` 實際渲染 |
| **A9r** | **❌** | 「admin 資料契約收縮」**沒收縮**:`SupabaseOrderAdapter.ts:91` 的 `ADMIN_ORDER_LIST_SELECT` 字串**仍含 `fulfillment_status`**(`sed -n '90,93p' \| grep -c` = 1) |
| **A9s** | **❌** | 「storefront 契約收縮」**沒收縮**,而且 code 自己承認:`apps/storefront/src/lib/orders/order-display.ts:23` 逐字「簽章保留雙參數…`fulfillmentStatus` 契約收縮 = A9s 片」 |

#### 需拍板 / 需肉眼 12 片 —— **7 ✅ / 5 ❌**

| 片 | 狀態 | 證據 |
|---|---|---|
| A10a · A10b | ✅ | 成品掛載鏈(§-4.1 第 3、5 項);⚠️ **不可用 commit 主旨當證據,見 §-5.3** |
| A11a · A11b · A11c | ✅ | 各有收割 commit(`5ccbe773` / `8841c86e` / `1047ff1c`) |
| A13a · A13b | ✅ | 成品:取消 UI 四支掛上(§-4.1 第 19 項) |
| **A12a · A12b** | **❌** | 批次選取零實作:`grep -rn 'type="checkbox"\|批次\|bulk\|selectedIds' components/orders/` = **0 命中**(母體=該目錄 30 支非測試檔);主旨命中也是 0 |
| **A14a · A14b · A14c** | **❌** | 主旨命中 **0**;母 plan `:466` 仍列 A14a 為未做的 U 片 |

#### 小計(機械複點:9+19+12=40)

| | 值 | 片號 |
|---|---|---|
| ✅ 完成 | **31** | 高風險 9 + 機械 15 + 需拍板 7 |
| 🟡 半套 | **1** | A9d1(counters 支缺) |
| ❌ 沒動 | **8** | A9e · A9r · A9s(契約/顯示端收縮三片)+ A12a · A12b(批次)+ A14a · A14b · A14c(詞彙/入口/空狀態) |

🔴 **剩下的 8 片有形狀**:三片是**收縮型**(把舊欄位/舊顯示拿掉,沒人急著做、不做也不會壞),
五片是**Sean 肉眼型**(批次 + 三片 UX 詞彙)。⇒ 「還剩幾片」這個度量對這 8 片會高估難度:
它們沒有 DB 依賴、也不在解鎖鏈上。

### §-5.2 §4 退款線五列重走 —— **三列的 ❌ 已經是假的**

| §4 原列 | 原狀態 | **今天** | 證據 |
|---|---|---|---|
| A7c 退款帳本 | ✅ | ✅(未變) | — |
| sandbox 退款探測 | ✅ | ✅(未變) | 探測、非施工 |
| **`TapPayChargeAdapter.refund()` 實作** | ❌「仍一行 throw、全樹 `.refund(` 呼叫端 = 0」 | **✅** | `packages/adapters/src/tappay/TapPayChargeAdapter.ts:241` `async refund(`;呼叫端 `apps/admin/src/lib/payment/refund-actions.ts:318`(**不是 0**)。🔴 §-2.1 早在 08-11 午前就推翻過這句,**但 §4 這張表沒跟著改** —— 這就是 §-2.5-4 的實際損害 |
| **後台退款按鈕 + 讀模型** | ❌「admin 零 TapPay 接線,**composition 不存在**」 | **🟡** | `ls apps/admin/src/lib/payment/ \| grep -v '\.test\.'` = **14 支**,`composition.ts` **就在裡面**;UI 三支已掛(§-4.1 第 17 項);缺的是 flag 與肉眼驗 |
| **worker / 對帳線(RF2b-RF8)** | ❌「第 3 批」 | **🟡** | `291a0506`「RW4 人工結案(標記失敗/Portal 真碼恢復)+對帳判定+**RF8 讀數**」;成品 `app/orders/refund-exceptions/page.tsx` + `refund-recovery-*.ts` 四支 |

⇒ **§4 結語「第 17 項要全綠,最少還要 4 片」失真**:四片裡兩片(adapter、按鈕+讀模型)已落地或半落地,
真正沒完的是**後兩列的尾巴**與 **flag 沒開**。要精確片數得回母 plan 退款線片表重數,**本次沒數**(見 §-5.4)。

### §-5.3 🔴 撞號警告:**A10a / A10b 在 storefront 也有同名片**

拿 commit 主旨查片時,`A10a` 最新命中是 `103791c0`「feat(**storefront**): A10a 首頁愛車 chips…」、
`A10b` 是 `c67423bf`「feat(**storefront**): A10b+c PDP §7…」—— 那是**選車線的 A10a/A10b**,
不是本表的 admin 備註畫面 / 採購表單。⇒ **片號的唯一性只在它自己那條線裡成立**;
跨線用片號查 commit 會撈到別條線的片,而兩邊的主旨都長得像「做完了」。
本節這兩格改用**成品掛載鏈**當證據,不用主旨。

### §-5.4 誠實界

1. **`✅` 只代表「成品在」或「有 apply/收割 commit」**,不代表跑得對、更不代表 Sean 驗過。
   高風險 9 片我**沒有**逐片去讀 migration 內容或跑驗證腳本,只認 apply 收帳 commit。
2. **沒重數退款線的片數**(§-5.2 結語只說原字面失真,沒給新數字)——
   要給就得回母 plan 退款線片表當場數,那是**下一筆**,不在本次範圍。
3. **供應商線 5 片、母 plan §5.1 其餘 29 片(69−40)本次沒走** —— 它們不在 §3 那 40 片的母體裡。
4. A9d1 的 `counters` 支我用「查無」結案:數法 = 檔名關鍵字 + 主旨關鍵字**兩條都 0 命中**;
   若它其實叫別的名字,這格就是錯的 —— 這是本節最弱的一格,先標出來。
5. 同樣有保鮮期:每句「0 命中」都是 **19:2x** 的觀察。實例見 §-5.5,**13 分鐘就過期一格**。

### §-5.5 🔴 §-4 的第 15 項在**13 分鐘後就過期了**(自報)

§-4.1 第 15 列(19:0x 落筆)寫:`order_payments|manual_payment|payment_ledger` 在 `apps/admin/src`
**0 命中** ⇒ 標 ❌。**19:20 同一條命令重跑 = 命中 2 支非測試檔**(`lib/orders/payment-repository.ts`
+ `payment-list-view.ts`,隨 `a14d78aa` 那批進 dev)。

- 元件也已存在:`components/orders/payment-list.tsx`。
- **但沒掛上**:`grep -rn '<PaymentList' apps/admin/src --include='*.tsx' | grep -v '\.test\.'` = **0**;
  `order-detail.tsx` 對 `payment-list` 的 import 數 = **0**。
- ⇒ **第 15 項 ❌ → 🟡**(資料讀取層與元件都有了、員工按不到),**§-4.2 的小計隨之改為
  ✅8 / 🟡3 / ⚠️3 / ❌13**。

📌 這格的價值不在數字,在於它是「**0 命中 = 時點觀察、不是屬性**」這條規則**在同一份文件裡
13 分鐘內自證**的實例 —— 而且我是因為 `git merge` 後順手重跑才發現的,不是因為記得規則。

---

## §-4 · 08-11 夜 **27 項逐項重走**(E 窗六代;樹=`9a5174b5`;還 §-2.5-1 的債)

**一句話:✅ 從 2 變 8。六格新 ✅(3/5/6/7/8/19)不是今晚做出來的 —— 它們在 §-1~§-3 期間陸續掛上,
本次是第一次**把 27 格放在同一個口徑下一起數**;而第 13 項要從 §-2.3 的 🟡 **降回 ❌**、第 9 項降為 ⚠️(全帶 `*` 的 ✅ 有六格)。**

> **本節是 §-2.5-1 那筆債的償還**:§-2 只重驗了被證偽的那幾格、其餘沿用 §2 舊判定,
> §-2.3 因此**刻意不給小計**(兩張表口徑不同、相減會錯)。本節 27 格**全部當場重跑**,
> 所以小計是算得出來的 —— 這是本次唯一的新可交付物。
> 驗收基準用 **spec 原文**(`docs/specs/2026-07-25-admin-backend-rebuild-spec.md:85-113`),
> 不是本檔 §2 那張快照表(它自己檔頭就標了 7 格過期)。

### §-4.1 27 項全表(每格當場實查;`*` = 畫面通、待 Sean 肉眼)

| # | 員工要做的事 | **§-4** | 證據座標(可複跑) | 缺什麼 |
|---|---|---|---|---|
| 1 | 看今天要處理什麼 | ❌ | `app/page.tsx` 全檔 64 行、`grep -n '待辦\|todo\|dashboard\|saved'` = **0 命中**;唯一 `<h` 是 `:13` 標題 | 待辦檢視整條 |
| 2 | 看訂單明細 | ✅ | `app/orders/[id]/page.tsx` → `OrderDetailRoute` → `order-detail.tsx`;頂層 8 區塊逐個 `grep -c '<元件名'` = 1 | — |
| 3 | 訂單寫備註 | ✅\* | `<NoteComposeForm` in `order-detail.tsx` = **1** | 肉眼驗 |
| 4 | 標商品進度(**4′ 要批次**) | 🟡 | 逐品項在:`item-procurement-section.tsx:114` `item.procurements.map`。**批次零**:`grep -rn 'type="checkbox"\|批次\|bulk\|selectedIds' components/orders/ --include='*.tsx' \| grep -v '\.test\.'` = **0 命中**(母體=`components/orders/` 29 支非測試檔) | A12a/A12b 批次選取與批次動作 |
| 5 | 標「已向供應商下單」逐品項 | ✅\* | `<ItemProcurementSection` in `order-detail.tsx` = 1;item 層(5′ 要的軸)已對 | 肉眼驗 |
| 6 | 記供應商單號 / 預計到貨 | ✅\* | `item-procurement-form.tsx:309` `supplierOrderNo`、`:287` `expectedArrivalDate` | 跨單搜尋(A10c2)另有逐批啟用閘,見 §-4.3 |
| 7 | 缺貨要等 | ✅\* | 寫:`item-procurement-form.tsx:350` `exceptionReason`;顯示:`item-procurement-section.tsx:131` | 肉眼驗 |
| 8 | 輸入快遞單號 | ✅\* | 欄位 `shipment-dialog.tsx:304`「貨運單號」+ 必填閘 `:140`(非「其他」快遞商必填);顯示 `shipment-section.tsx:91` | 肉眼驗。**以 §2(08-01)那一欄為母體**,27 格裡唯一走完 ❌→✅ 的是本項;它中途在 §-1(08-09)停過 🟡,**不是一步到位**,而且轉 ✅ 發生在 §-2.3(08-11 午前)、**不是今晚** |
| 9 | 單號追蹤 | ⚠️ | `grep -rn 'tracking_url\|trackingUrl' apps/admin/src` = **0 命中**;`追蹤` 3 處命中**全是註解**(`proxy.ts:6` / `shipment-error-view.ts:15` / `note-actions.ts:129`,講的是 x-request-id 跨層追蹤) | 缺的**只有連結面**:單號存得下也顯示得出來(`shipment-section.tsx:91`),員工看得到、可自行貼到快遞商網站 ⇒ 照本檔圖例算**半套 ⚠️**,不是 ❌(🟡「員工按不到」也不適用,他看得到)。五代標的「未確認」本次結案 |
| 10 | 列印出貨單 / 揀貨單 | ❌ | `grep -rn 'window.print\|揀貨\|列印'` = 6 命中,**逐條開檔後全是註解**(「可列印 ASCII」×4 / `order-detail.tsx:119`「每列印一個 0」的斷詞巧合 / `supplier-table.tsx:46`「列印、色弱」) | 整條。🔴 這格是「grep 數關鍵字會把註解算進去」的當日實例 |
| 11 | Email 通知客人出貨 | ❌ | `packages/use-cases/src/sweep-email-outbox.ts:112-113`:`case 'order_shipped': throw new Error('…模板未定義(E4 未落地)、fail-closed 不寄')` | **模板不存在**,不只是觸發點沒接 |
| 12 | 手動建單(非網站商品) | ❌ | `grep -rn 'createOrderAction\|manual_order\|建立訂單\|手動建單' apps/admin/src` = 1 命中,是 `components/shared/admin-form.tsx:12` 的註解「等 E10 手動建單…再做」 | 整條(六題未拍板) |
| 13 | 改訂單內容(**品項/數量/金額**) | **❌**(§-2.3 標 🟡) | `order-edit-form.tsx` 全檔 102 行,可編欄位**恰 4 個**:`SHIPPING_METHOD_FIELD:59` / `INVOICE_STATUS_FIELD:69` / `INVOICE_NUMBER_FIELD:82` / `INVOICE_AMOUNT_FIELD:94` | 🔴 **降級**:表單存在 ≠ 這一項存在。spec 要的品項/數量/金額**一個都不可編**,與 spec `:107` 原判「只能改出貨方式+發票」完全一致 ⇒ §-2.3 標 🟡 是「看到檔案就升格」 |
| 14 | 刷卡收款 | ✅ | 已上線(首筆 2026-07-24) | — |
| 15 | 匯款確認收款 | ~~❌~~ **🟡(19:20 改標,見 §-5.5)** | 19:0x 落筆時該條 grep = **0 命中**;**19:20 重跑 = 2 支非測試檔**(`lib/orders/payment-repository.ts` + `payment-list-view.ts`) | 元件 `payment-list.tsx` 也在,但 `<PaymentList` 掛載數 = **0** ⇒ 員工按不到 |
| 16 | 今日對帳 | ❌ | `對帳` 4 處命中**全在** `app/orders/refund-exceptions/page.tsx:14/19/20/57`,那是**退款對帳判定**,不是現金日結 | 依賴 15 |
| 17 | 退款操作 | 🟡 | UI 三支掛上(`refund-section` / `refund-ledger-section` / `refund-exception-resolve`);閘 `lib/payment/refund-ui-flag.ts:22` `REFUND_UI_ENABLED === '1'`,兩端同閘:顯示 `order-detail-route.tsx:193`、server `refund-actions.ts:109` | 正式站 env 值**未確認**(`.env*` 不是我能碰的);沿用 §-3.4 的誠實標法 |
| 18 | 退貨收回 | ❌ | `grep -rn 'order_returns\|orderReturn' apps/admin/src` = **0 命中**;`退貨` 命中 `cancel-review-section.tsx:101/155` 是**指路文案**(叫人去走退貨流程),不是實作 | 貨的軸整條 |
| 19 | 取消訂單 | ✅\* | `<OrderCancelBlock` in `order-detail.tsx:351`;下掛 `<CancelReviewSection` in `order-cancel-block.tsx:69` | 肉眼驗。⚠️ 表單受 `cancelFormsAllowedOnResultPage(?r=)` 閘、**預設 fail-closed false**(`order-detail.tsx:234`) |
| 20 | 新增/編輯商品 | ❌ | `ls apps/admin/src/app` = `@panel api customers globals.css layout.tsx orders page.tsx settings` ⇒ **無 products route**(母體=admin app 目錄第一層) | 零規劃 |
| 21 | 上傳商品圖片 | ❌ | 同 20(無商品頁面就無上傳面) | 零規劃 |
| 22 | 改價格 / 上下架 | ❌ | 同 20 | 零規劃 |
| 23 | 匯入供應商 Excel | ❌ | admin 內無;CLI 仍在 repo 根 `scripts/` | 零規劃 |
| 24 | 匯出訂單 / 商品 | ❌ | `grep -rn 'csv\|匯出\|export.*download' apps/admin/src --include='*.tsx'` = 2 命中,**都是 `result-banner.tsx:26/112` 註解裡的「匯出給測試讀」**(JS module export,不是資料匯出) | 整條 |
| 25 | 改客人資料(**25′ 要全欄位**) | ⚠️ | `ls components/customers/ \| grep -v '\.test\.'` = 8 支,可寫的只有 `tier-edit-form` + `wallet-adjust-form`;`customer-detail-sections.tsx` `grep -n '<form\|input\|Form'` = **0 命中** | 仍只有等級+儲值金 |
| 26 | 員工各自帳號與權限 | ❌ | `lib/staff.ts:3-4` 自陳:「操作者仍是使用者自行選擇…**這不是登入 / 授權邊界**,也沒有驗證『目前使用者是誰』」 | E8-B 整條 |
| 27 | 誰改了什麼 | ⚠️ | 寫入端 `grep -rl admin_audit_log apps/admin/src --include='*.ts' \| grep -v '\.test\.' \| wc -l` = **28**;讀取端 `--include='*.tsx'` = **1 命中且是註解**(`cancel-order-forms.tsx:53`)⇒ **無 UI** | 讀取 RPC + 畫面 |

### §-4.2 小計 —— **§-2.3 欠的那個數字,現在算得出來了**

| | §1 spec(07-25) | §2 地圖(08-01) | **§-4(08-11 夜)** |
|---|---|---|---|
| ✅ 能用 | 2 | 2 | **8**(其中 **6 格帶 `*`**) |
| 🟡 資料層好、按不到 | —(spec 無此級) | 6 | **3**(4、15、17)🔴 15 於 19:20 由 ❌ 改標,見 §-5.5 |
| ⚠️ 半套 | 5 | 2 | **3**(9、25、27) |
| ❌ 不存在 | 20 | 17 | ~~**14**~~ **13**(19:20 起) |
| 合計 | 27 | 27 | **27** ✅ 對得起來 |

🔴 **這個小計的三個限制,讀的人必須一起收**:
1. **6/8 的 ✅ 是 `*` 級** —— 只證「畫面掛上去了」,照本檔口徑**不代 Sean 宣告能用**。
   真正被人驗過的只有 2(看明細)與 14(刷卡)兩格,和 07-26 一樣。
2. **口徑跨欄不可比**:spec 那欄沒有 🟡 級(它把「資料層好但按不到」歸在 ❌/⚠️)⇒
   「20 → 15」不等於「消滅了 5 項」,**只有同一欄內部的變化才可以相減**。
3. **有兩格是往下走的(13 降回 ❌、9 降為 ⚠️)** —— 逐項重走的價值主要在這裡:
   只重驗「被證偽的格」永遠只會往上修,因為沒人會回頭去證偽一個綠燈。

### §-4.3 順手釐清的兩格(五代標「未確認」的)

- **9(追蹤連結)**:結案 = ⚠️(半套)。`tracking_url` / `trackingUrl` 在 `apps/admin/src` **零命中**,
  出貨線只存/顯示單號字串(`shipment-section.tsx:91`),沒有任何連得出去的面。
- **13(可編欄位範圍)**:結案 = ❌(降級)。四個可編欄位逐個列在上表,與 spec 要求零交集。
- **6 附帶**:A10c2 跨單搜尋的**程式在**(`order-filter-controls.tsx:74/134` + `app/orders/page.tsx:49/72`),
  但它掛在「§7.1 逐批啟用閘」上 ⇒ 這格的 ✅ 只認「記得下來」,搜尋面另計(`#397` 在 Sean 桌上)。

### §-4.4 本次沒做的事(誠實界)

1. **零肉眼驗、零跑 server** —— 全部是 `grep` / `find` / 讀掛載鏈。所有 `*` 都還在。
2. **沒重走 §3 的 40 片分類與 §4 退款線片數**(§-2.5-4 那筆債仍在,本次未還)。
3. **沒查 28-33**(§1.1 第二批六項新需求)—— 本次口徑嚴格限定 spec `:85-113` 的 27 項。
4. **17 的正式站 flag 值查不到**(`.env*` 不碰)⇒ 標未確認,不寫「等於沒開」。
5. 本節同樣會過期:上面每一句「0 命中」都是 **19:0x-19:1x 的觀察、不是屬性**,引用前重跑。

---

## §-3 · 08-11 傍晚刷新(E 窗;樹=`82f24b03`;每格當場實查,非抄交接)

**一句話:到貨收據的 UI 進 dev 了(§-2 說它「還沒進」已過期);但**收款帳本**那條落差**沒有縮小
—— 今天它又多了一支 migration,而 admin 端仍然一個呼叫端都沒有。**

### §-3.1 §-2 有一句已經過期(同一個病:全稱句被當成屬性)

| §-2 逐字 | 今天實查 | 證據 |
|---|---|---|
| §-2.3 末列「到貨收據登錄 —— UI 在 **D 窗凍結中、尚未 commit** ⇒ dev 樹上還沒有」 | **過期**,UI 已在 dev,而且兩條路都接通了 | 見 §-3.2 的逐跳表 |

⚠️ 它當時是對的 —— 這正是檔頭那句「全稱句有保鮮期」的意思,**不是誰寫錯**。
本次刷新自己也一定會產生同類句子,下次引用前照樣要重跑。

### §-3.2 到貨收據:**兩條路逐跳實數**(不是只數 import)

`ReceiptPanel` / `ReceiptRecordForm` **不是**詳情頁的第 9 個頂層區塊,它們掛在既有區塊底下:

| 跳 | 數法 `grep -c '<元件名' <檔>` | 值 |
|---|---|---|
| `order-detail.tsx` → `<ItemProcurementSection` | 同左 | 1 |
| `item-procurement-section.tsx` → `<ReceiptRecordForm` | 同左 | 1 |
| `order-detail.tsx` → `<ShipmentSection` | 同左 | 1 |
| `shipment-section.tsx` → `<OrderShipButton` | 同左 | 1 |
| `shipment-launcher.tsx` → `<ShipmentDialog` | 同左 | 1 |
| `shipment-dialog.tsx` → `<ReceiptPanel` | 同左 | 1 |

⇒ 兩條路(採購區塊底下、建箱彈窗裡)**每一跳都非零** = 路徑通。
⚠️ **我一開始把中間那跳猜成 `<ShipmentLauncher`,實數 = 0** ——
真正的匯出名是 `OrderShipButton`(`shipment-launcher.tsx:15` 被 import 的名字)。
⇒ 逐跳數的價值就在這:**猜錯元件名時它會給你 0,而不是讓你以為斷鏈**。

### §-3.3 三個數字(都附數法;括號=§-2 值)

| | 值 | 數法 |
|---|---|---|
| admin 頁面 | **10**(10,未變) | `find apps/admin/src/app -name page.tsx \| wc -l` |
| `components/orders/` 非測試檔 | **29**(26,+3) | `ls apps/admin/src/components/orders/ \| grep -vc '\.test\.'` |
| 詳情頁**頂層**區塊 | **8**(8,未變) | `order-detail.tsx:17-24` 八個本地 import,逐個 `grep -c '<元件名'` = 1。收據是**巢狀**不是第 9 塊 |

### §-3.4 27 項:這次動了的格

| # | 項 | §-2 | **§-3** | 證據 / 缺什麼 |
|---|---|---|---|---|
| 15 | 匯款確認收款 | 🟡 | **🟡(原地)** | DB 又多一支:`20260811090000`(#15-B1 `admin_list_order_payments` 唯讀 RPC,`f46b6af3`;**17:5x 已 apply**,見 §-3.5)。**UI 仍為零**:`grep -rl record_manual_payment apps/admin/src \| wc -l` = **0**、`grep -rl admin_list_order_payments apps/admin/src \| wc -l` = **0**(17:1x 重跑,apply 之後仍是 0) |
| 16 | 今日對帳 | ❌ | **❌(原地)** | 仍依賴 15 的 UI |
| 17 | 退款操作 | 🟡(UI 齊、flag 關) | **🟡(UI 齊、flag 值未確認)** | 閘本體仍在:`lib/payment/refund-ui-flag.ts` 的 `REFUND_UI_ENABLED === '1'`。⚠️ **「正式站等於沒開」這句我這次不重述** —— 要證它得讀正式站 env,而 `.env*` 不是我能碰的 ⇒ 標**未確認**比沿用舊斷言誠實 |
| — | 到貨收據登錄 | 🟡(UI 未進 dev) | **✅\*** | 兩條路逐跳通(§-3.2);`\*` = 待 Sean 肉眼,照本檔口徑不代他宣告 |
| — | (新)日期範圍自訂輸入 | 不在表上 | **🔴 已知壞** | Sean 08-11 七項肉眼驗**六過一不過** ⇒ `#406` 立案(派 D)。**這是本檔第一格「Sean 驗過而且不過」的紀錄** |

### §-3.5 apply 佇列 —— 🔴 **本節在寫下 5 分鐘後就過期了,已更正(2026-08-11 17:5x)**

> 這節是本檔「全稱句有保鮮期」最短命的一次實例:17:10 寫「三支待 apply」,
> **17:5x 三支就全上了**。留著原文與更正,是因為保鮮期短到這種程度**本身就是要記的事**
> —— 施工中的佇列狀態不適合寫進地圖當現況,下次應該只寫「見 STATUS 當下佇列」並指過去。

| migration | **現況(17:5x 更正)** | ~~17:10 原文~~ |
|---|---|---|
| `20260811080000`(L5b-2 2c 退款帳本 CHECK) | **已 apply** | ~~已寫好、待 apply~~ |
| `20260811090000`(#15-B1 收款列表 RPC) | **已 apply**;主視窗回報正式站煙測過(真單 1 列 / 亂 uuid 回 NULL) | ~~已寫好、待 apply~~ |
| `20260811100000`(S #277c) | **已 apply,且檔已進 dev** | ~~號已發、檔尚未進 dev~~ |

- **apply 狀態的來源 = 主視窗 E-289-A 回報(Sean 批的那一批)**,不是我實查 —— 我這端看不到正式庫,
  照三來源律標明出處,不冒充自己驗過。
- **我實查得到、也真的重跑了的**:`ls supabase/migrations/20260811100000*.sql` 現在**命中**
  (`…_m4b_storefront_277c_vehicle_taxonomy_direct_years.sql`)⇒ 「檔尚未進 dev」那句確實已過期。

🔴 **但落差那句沒有過期,反而更硬了**(這點與主視窗來信的概括不同,我重量過才寫):
`grep -rl record_manual_payment apps/admin/src | wc -l` = **0**、
`grep -rl admin_list_order_payments apps/admin/src | wc -l` = **0**(17:1x 重跑)。
⇒ 15 那格仍然 🟡,但**理由換了**:不再是「新 RPC 卡在 apply 佇列」,而是
**「DB 那邊已經上線了,UI 這邊一個呼叫端都還沒有」**。
⇒ §-3.2 那句「先建 DB、UI 落後」在今天之內又深一層:資料層往前走了三支,應用層原地。
(D 窗的 B2-a = 第一個呼叫端,主視窗回報施工中;它進 dev 後這格才有得動。)

### §-3.6 本次沒做的事(誠實界,與 §-2.5 疊加)

1. ~~**仍沒有逐項重走 27 項**(§-2.5-1 那筆債**沒還**)~~ **🏁 2026-08-11 夜由 §-4 償還**。本次只驗了「今天 dev 有變動」牽動的格。
2. **零肉眼驗**:全部是 `grep` / `find` / 逐跳數,沒有跑起 server 看任何畫面。
3. **17 的 flag 值未查**(理由見 §-3.4);**9(追蹤連結)、13(可編欄位範圍)兩格仍是 §-2 標的未確認**,本次沒補。
4. ~~**仍不給總計**(理由同 §-2:唯一誠實的算法是重走 27 項,而那正是沒做的事)~~ **🏁 總計已於 §-4.2 補上**(8/2/2/15)。
5. 前台側今天進了 `#315`(未知網址參數保留)與 `#331`(側欄 sticky 吃 token),
   **兩者都不在 27 項裡**,故不動任何一格 —— 記在這裡只為說明「今天的 commit 不是全部都會動地圖」。

---

## §-2 · 08-11 午前刷新(E 窗;樹=`f0e19343`;每格當場實查)

> ⚠️ **§-2.3 末列「到貨收據 UI 尚未進 dev」已被 §-3.1 推翻**;其餘各格 §-3 未重驗,沿用時照樣有保鮮期。

**一句話:訂單線的畫面在這兩天追上來了,而落差搬了家 —— 現在跑在 UI 前面的是「收款帳本」與「到貨收據」。**

### §-2.1 🔴 08-09 版有四句已經是假的(它們都是「0 命中/全缺」型全稱句)

| 08-09 逐字 | 今天實查 | 證據 |
|---|---|---|
| 8/9「admin `shipments` 關鍵字 0 命中=畫面全缺」 | **假**,出貨 UI 五支都在且已掛詳情頁 | `shipment-section` / `-dialog` / `-launcher` / `-void-button` / `shipping-selection`;掛載點 `order-detail.tsx:22` |
| 19「admin `order_cancellations` 0 命中=畫面全缺(A13)」 | **假**,取消 UI 四支都在且已掛 | `cancel-order-forms` / `cancel-review-section` / `cancel-form-body` / `cancel-result-panel`;掛載點 `order-detail.tsx:21`(`OrderCancelBlock`) |
| 17「`TapPayChargeAdapter.refund()` 仍一行 throw、全樹 `.refund(` 呼叫端 = 0」 | **假**,已實作且有呼叫端 | `packages/adapters/src/tappay/TapPayChargeAdapter.ts:241`;呼叫端 `apps/admin/src/lib/payment/refund-actions.ts:318` |
| 13「改訂單內容 ❌ 無」 | **假**(至少有表單) | `components/orders/order-edit-form.tsx`,掛載點 `order-detail.tsx:17` |
| §-1 表「admin UI `shipments` 關鍵字 0 命中=畫面全缺」(**08-11 D 窗回報、E 窗複量**) | **假** | `grep -rl shipment apps/admin/src/components --include='*.ts' --include='*.tsx' \| grep -v '\.test\.' \| wc -l` = **8**(其中檔名帶 shipment 的非測試檔 5 支) |
| §0 與 §2 第 3 項「備註 action **零呼叫端**」(同上) | **假** | `grep -rln appendOrderNoteAction apps/admin/src --include='*.tsx' \| grep -v '\.test\.'` = **1**(`note-compose-form.tsx`)⇒ 不是零 |

⚠️ **這兩列的數字與 D 窗回報的不同(它報 7 與 2),因為口徑不同,不是誰數錯** ——
「7」應是只算檔名帶 `shipment` 的另一種切法、「2」應是把渲染 `NoteComposeForm` 的 `order-detail.tsx` 也算成呼叫端。
⇒ 這正是本檔檔頭那條規則的意思:**數字要跟著數法一起寫**,否則兩個都對的人會以為對方錯。

⚠️ 第三句的檔案路徑在 08-09 版寫成 `packages/adapters/src/payment/TapPayChargeAdapter.ts` ——
**那個路徑今天不存在**(真身在 `…/src/tappay/`)。引用舊行號前先 `find`。

### §-2.2 現況三個數字(都附數法)

| | 值 | 數法 |
|---|---|---|
| admin 頁面 | **10** | `find apps/admin/src/app -name page.tsx \| wc -l` |
| `components/orders/` 非測試檔 | **26** | `ls … \| grep -vc '\.test\.'` |
| 訂單詳情頁**真的畫出來**的區塊 | **8** | `order-detail.tsx:17-24` 八個 import,**逐個再驗 `grep -c '<元件名'` = 1**:編輯 / 備註時間軸 / 寫備註 / 逐品項採購 / 取消 / 出貨 / 退款 / 退款帳本。🔴 只數 import 會把「引了沒用」算成掛上,所以兩道都數 |

### §-2.3 27 項:這次動了的格(其餘沿用 §2,狀態未變)

| # | 項 | 08-09 | **08-11** | 缺什麼 / 證據 |
|---|---|---|---|---|
| 5/6 | 標供應商下單・記單號 | ✅* | ✅* | 未變(`ItemProcurementSection` 已掛) |
| 8 | 輸入快遞單號 | 🟡 | **✅\*** | UI 已掛(見 §-2.1);\*=待 Sean 肉眼 |
| 9 | 單號追蹤 | 🟡 | **🟡** | 出貨 UI 在,但**追蹤連結面未查**(本次沒驗,標 ❓ 比標綠誠實) |
| 13 | 改訂單內容 | ❌ | ~~**🟡**~~ **🔴 已被 §-4.1 降回 ❌** | 有 `order-edit-form.tsx`;~~**它的可編欄位是否等同該項需求 = 未確認**~~ **已比對:四個可編欄位與 spec 要求零交集**(§-4.1 第 13 列) |
| 15 | 匯款確認收款 | ❌「無 `order_payments`」 | **🟡** | **DB 已建**:`20260810100000`(OP1)+ `160000`(OP3 card 腿)+ `200000`(OP5 手動收款)+ `210000`(OPa12 沖銷)+ `20260811030000`(OP6a 結清狀態);**UI 零** —— `record_manual_payment` 在 `apps/admin/src` **0 命中**(只在 `packages` 的產生型別裡 1 命中) |
| 16 | 今日對帳 | ❌ | 🟡→❌ | 依賴 15 的 UI,15 的 UI 還沒有 |
| 17 | 退款操作 | 🟡➚ | **🟡(UI 齊、flag 關)** | `refund-section` / `refund-ledger-section` / `refund-exception-resolve` 皆在並掛上;閘 `lib/payment/refund-ui-flag.ts:22`(`REFUND_UI_ENABLED==='1'`)⇒ **正式站等於沒開** |
| 19 | 取消訂單 | 🟡 | **✅\*** | UI 已掛(見 §-2.1);\*=待 Sean 肉眼 |
| — | (新)到貨收據登錄 | 不在 27 項 | **🟡** | DB 三支已 apply(`230000`/`233000`/`20260811010000`);UI 在 **D 窗凍結中、尚未 commit** ⇒ dev 樹上還沒有 |

🔴 **本次刻意不給「✅ N / 🟡 N / ❌ N」的新小計。** 理由是算不出誠實的:§2 那張表的小計是
**08-03 口徑**,而 08-09 的 §-1 又改了 3/5/6/8/9/17 六格**卻沒有重算小計** ⇒ 拿任一邊當基準都會錯,
而唯一算得出正確小計的方法是**把 27 項全部重走一遍**,那正是本次沒做的事(見 §-2.5-1)。
🏁 **2026-08-11 夜已重走完畢,小計在 §-4.2**;本段保留原文供追溯,別再拿「算不出來」當現況。
⇒ 想知道總數的人:重走一遍再寫,不要從這兩張表相減。
🔴 另外提醒:8/19 轉 ✅ 靠的是**畫面掛上去了**,**沒有任何一格是 Sean 驗過的** ——
「能用」與「畫得出來」在這張地圖上一直是兩件事,別把 ✅\* 讀成員工可以上工。

### §-2.4 落差的形狀第三次改變(這才是這次刷新的重點)

- 08-01:資料層跑在 UI 前面 6 天。
- 08-09:兩條線都推到「只剩畫面」。
- **08-11:訂單線的畫面追上了**(詳情頁 8 個區塊全掛),**而新開的兩條帳本又跑到 UI 前面**:
  **收款帳本(OP 線 5 支 migration)admin 端 0 命中**、**到貨收據(3 支 migration)UI 還沒進 dev**。
  ⇒ 同一個病復發在新的地方:**先建 DB、UI 落後**。下次排片時把「誰接這條帳本的畫面」跟 migration 一起排。

### §-2.5 本次刷新沒做的事(誠實界)

1. ~~**沒有逐項重走 27 項**~~ **🏁 已由 §-4 償還(2026-08-11 夜)** —— 本次只重驗了 08-09 標「全缺/0 命中」的那幾格 + 被它們牽動的格。
   其餘格子沿用 §2 的舊判定,**它們同樣有保鮮期問題**,下次刷新要補。
2. **9(追蹤連結)、13(可編欄位範圍)兩格我標了未確認**,沒有硬猜。
3. **零肉眼驗**:本次全部是 `grep` / `find` / 讀掛載鏈,沒有跑起 server 看任何一個畫面。
4. ~~§3 的 40 片分類、§4 退款線片數**未重走**~~ **🏁 已由 §-5 償還(2026-08-11 夜)**;原文:§3 的 40 片分類、§4 退款線片數未重走(§4 的「`refund()` ❌」已被 §-2.1 推翻,但那張表的其餘列沒重驗)。

---

## §-1 · 08-09 刷新(當場實查,非抄交接;驗證命令=本檔 §5 慣例 grep+掛載鏈追蹤)

> ⚠️ **本節的狀態判定已被 §-2 取代**(四句被證偽,見 §-2.1);保留原文供追溯,**不要再拿它當現況**。

**✅ 能用:2 → 5**(07-26 以來第一次動)。admin 頁 8、`'use server'` 14→**16**。

| # | 項 | 舊 | 新 | 證據 |
|---|---|---|---|---|
| 3 | 訂單寫備註 | 🟡 | **✅** | `NoteComposeForm` 經 `order-detail.tsx` 掛上詳情頁(A10a 落地) |
| 5 | 標「已向供應商下單」逐品項 | 🟡 | **✅*** | `ItemProcurementSection`+form+供應商下拉掛上詳情頁;*=待 Sean 肉眼 |
| 6 | 記供應商單號/預計到貨 | 🟡 | **✅*** | 同上表單;`supplier_order_no` migration 已 apply;跨單搜尋(A10c2)未驗 |
| 8/9 | 快遞單號/追蹤 | ❌ | **🟡** | 出貨線 DB 全 apply(shipments 家族+九支 RPC+冪等+死結重試);~~admin UI `shipments` 關鍵字 0 命中=畫面全缺~~ 🔴 **這句 08-11 已過期,見 §-2.1**(D 窗回報、E 窗複量) |
| 17 | 退款操作 | 🟡 | **🟡➚** | `TapPayChargeAdapter.refund()` **已實作**(08-03 的一行 throw 已亡)+admin refund UI 7 檔+refund-exceptions 頁(`isRefundUiEnabled` flag 閘);缺=worker/對帳線+flag 未開 |
| 19 | 取消訂單 | 🟡 | 🟡 | DB+RPC+死結重試全齊;admin `order_cancellations` 0 命中=畫面全缺(A13) |

**前台側同期**(不在 27 項但 Sean 有感):LINE 刷卡修復全案結(email 落地址)/聲音檔全線貫通/手機 RWD 五修/h1 三級/發票暫藏。

**未動**:12 手動建單(六題未拍)/15 匯款/20-24 商品線(零規劃)/26 認證(E8-B)/27 稽核 UI。
**測試單清理**:33 筆 unpaid 測試單 08-09 全清(Sean Studio SQL),後台訂單列表首次乾淨。
**新拍板落片**:未付款 1 天自動失效+防雙付窗縮短+殭屍裁決=「訂單付款生命週期片」(P 窗 plan 中)。

---

# 〔08-03 版原文以下保留〕

> **Sean 08-01 拍板 Q1=A 的產出。** 從今天起,進度度量 = **這張地圖上還剩幾片**,
> 不看 commit 數、不看守門數(memory `project_workmode-0801-nightrun-decisions`)。
> 08-03 凌晨刷新內容:供應商主檔線(S1a-S3b)與備註線前三片(A6/A9a-1/A9d2-1)收工折入、
> 退款 sandbox 實測結果折入、§3 加「DAG 可開工實查」——刷新皆為當場 grep/實查,非抄交接檔。
>
> 驗收基準 = `docs/specs/2026-07-25-admin-backend-rebuild-spec.md` §1「員工的一天」27 項
> (+ §1.1 的第二批補充 28-33)。
> 前提(Sean 逐字):「整個網站後台完成之前,員工根本不會去用訂單系統」
> ⇒ **後台是二元切換,沒有分批採用**;排序只為建造效率服務,不為「讓員工早點用」。

---

## §0 一句話現況

**27 項裡真正能用的仍只有 2 項(看訂單明細、刷卡收款)—— 從 2026-07-26 至今沒變過。**
但 08-01 的「畫面一格都沒動」已失真(08-03 凌晨實查):

- admin 頁面 7 → **8**(`/settings/suppliers` 供應商設定頁,S3b 全線 08-02 收工)、
  `'use server'` 檔 10 → **14**。這是後台第一個員工可獨立操作的新頁面,**但它不在 27 項清單上**
  (它是第 5/6 項的上游基礎建設)。
- 備註線前三片收工:**A6**(owner RPC 已 apply 正式站)+ **A9a-1**(明細讀模型 + U6 告知義務)+
  **A9d2-1**(寫入 server action)—— 寫入路全通,**唯獨零呼叫端**:員工仍按不到,
  第 3 項卡在最後一片畫面(A10a)。
- `order_item_procurement` / `order_cancellations` / `order_refunds` 三字串在 `apps/admin/src`
  仍**零命中**;`order_notes` 已有寫入面命中(4 檔)。

⇒ 落差的形狀變了:08-01 是「資料層跑在 UI 層前面 6 天」,現在是**兩條線都推到「只剩畫面」**
(備註線差 A10a、供應商線已通),而 27 項的驗收全部發生在畫面那一步。

---

## §1 三個數字

| | 數量 | 說明 |
|---|---|---|
| **已拆好、待開工的片** | **40** | 08-01 口徑 43 − A5b − A5c(標作廢,master plan `:420`)− A6(完成)= **40**;其中 **A9a 與 A9d2 兩列各完成一半**(A9a-1 / A9d2-1 ✅,A9a-2 / A9d2-2 留在表上)。供應商線 S1a-S3b 五片**新增且全部完成**、刻意不列入 §5.1 口徑(`:420`)。🔴 第 25 列「A7b-M / A7b-T」算進「已完成」的理由:A7b-M 已 apply、A7b-T 換路退場、由 **A7c** 承接並已 apply(A7c 不在 §5.1 表上,見 §4) |
| **已知但還沒拆片的區塊** | **13** | 第 2 批 10 項 + 第 3 批 10 項(合併去重後);開批時才拆 |
| **連區塊都沒有的領域** | **5** | 商品線(20-24)/ 客戶全欄位(25′)/ 員工認證(26)/ 稽核 UI(27)/ 新需求 28-30 |

🔴 **「40 片」是唯一可信的數字。** 13 個區塊與 5 個領域**刻意不預先拆成片** ——
7 月的教訓就是巨型前置規劃(E10 總規劃 69 片 + 關卡1 審 20 輪,結果 A7b 105 道守門被一句換路作廢):
**規劃準確度隨距離衰減,拆得越遠越白拆。**

---

## §2 27 項逐項現況(2026-08-01 實查)

> ⚠️ **本表是 08-01/08-03 的快照,已知有 7 格過期**(第 8、9、13、15、16、17、19 項;
> 另有「到貨收據登錄」是本表**根本沒有**的新項目,見 §-2.3 末列)——
> **現況以 §-2.3 為準**;本表未被 §-2.3 點名的格子則是**沿用而非重驗**,引用前自己再跑一次 grep。

圖例:✅ 能用 / 🟡 資料層好了但員工按不到 / ❌ 不存在

| # | 員工要做的事 | 狀態 | 資料層 | 缺的是什麼 | 對應片/區塊 |
|---|---|---|---|---|---|
| 1 | 看今天要處理什麼 | ❌ | 無 | 待辦檢視整條 | 第 3 批區塊⑧(依賴收款帳本 + 出貨) |
| 2 | 看訂單明細 | ✅ | — | — | — |
| 3 | 訂單寫備註 | 🟡 | ✅ 全鏈到 action | ~~只剩畫面(action 零呼叫端)~~ 🔴 **08-11 已過期**:A10a 已落地,呼叫端存在,見 §-2.1 | ~~**A10a**~~ 已完成 |
| 4 | 標商品進度(批次) | 🟡 | 既有 | 批次選取與批次動作 | **A12a / A12b**(2 片) |
| 5 | 標「已向供應商下單」逐品項 | 🟡 | ✅ `order_item_procurement` + 供應商主檔線收工(S1b 換 FK 軸) | RPC + 讀模型 + 表單 | **A5a / A9a-2 / A9c / A10b** |
| 6 | 記供應商單號 / 預計到貨 | 🟡 | ✅ 欄位齊 + 供應商可下拉選(S3b) | 同上 + 跨單搜尋 | **A5a / A10b / A9b2 / A10c2** |
| 7 | 缺貨要等 | 🟡 | ✅ `exception_reason` 欄在 | 表單與顯示 | **A10a + A10b 聯合驗收** |
| 8 | 輸入快遞單號 | ❌ | 無 `shipments` 表 | 整條出貨模型 | 第 2 批區塊①②③ |
| 9 | 單號追蹤 | ❌ | 無 | 同上 + 追蹤連結 | 第 2 批區塊③ |
| 10 | 列印出貨單 / 揀貨單 | ❌ | 無 | — | 第 2 批區塊⑧ |
| 11 | Email 通知客人出貨 | ❌ | outbox 骨架在、0 列 | 觸發點 | 第 2 批區塊⑥ |
| 12 | 手動建單(非網站商品) | ❌ | `create_order` 要 `auth.uid` | admin 專用建單 RPC | 第 3 批區塊⑩ 🔴 **六題未拍板不開工** |
| 13 | 改訂單內容 | ❌ | 無 | 改單模型 + event log | 第 3 批區塊⑥ |
| 14 | 刷卡收款 | ✅ | — | — | — |
| 15 | 匯款確認收款 | ❌ | 無 `order_payments` | 收款帳本整條 | 第 3 批區塊①(本批最先) |
| 16 | 今日對帳 | ❌ | 無 | 依賴 15 | 第 3 批區塊⑧ |
| 17 | **退款操作** | 🟡 | ✅ A7c 已 apply + **sandbox 實測 ✅**(08-01 深夜) | **`refund()` 仍一行 throw** + 零 UI + 無 worker | **現行線**(見 §4) |
| 18 | 退貨收回 | ❌ | 無 `order_returns` | 貨的軸整條 | 第 3 批區塊③ |
| 19 | **取消訂單** | 🟡 | ✅ `order_cancellations` + 一致性 trigger | 金流守門 + RPC + 讀模型 + 畫面 | **A8c1 / A8c2 / A8a1 / A8a2 / A9g / A9d2-2 / A13a / A13b**(8 片) |
| 20 | 新增/編輯商品 | ❌ | products 表在 | **後台零商品頁面** | 🔴 零規劃 |
| 21 | 上傳商品圖片 | ❌ | 只有供應商 CDN URL | 上傳 + 媒體庫 | 🔴 零規劃 |
| 22 | 改價格 / 上下架 | ❌ | — | 同 20 | 🔴 零規劃 |
| 23 | 匯入供應商 Excel | ❌ | CLI 在 repo 根 | 包成後台功能 | 🔴 零規劃 |
| 24 | 匯出訂單 / 商品 | ❌ | — | — | 第 3 批區塊⑨(只含訂單) |
| 25 | 改客人資料 | ⚠️ | — | 只能改等級+儲值金;25′ 要全欄位可編 | 🔴 零規劃 |
| 26 | 員工各自帳號與權限 | ❌ | staff 表在 | **身分是 cookie 裡自選的** | E8-B(plan v3 已 FAIL、需重寫) |
| 27 | 誰改了什麼 | ⚠️ | `admin_audit_log` 有寫入、無 UI | 讀取 RPC + 畫面 | 第 3 批區塊⑦;🔴 **天花板 = E8-B** |

**小計:✅ 2 / 🟡 6 / ⚠️ 2 / ❌ 17。**
🔴 與 07-29 A0c 的「2/5/20」比較:**沒有任何一項從 ❌ 變 ✅**;變的是 6 項從 ❌ 升到 🟡(資料層就位)。
🔴 08-03 刷新後小計**仍一格未動** —— 兩天的產出(S 線五片 + 備註線三片)全數落在 🟡 內部的推進:
第 3 項從「缺 3 片」推到「缺 1 片」,第 5/6 項上游供應商基建通了。**最接近轉 ✅ 的一項 = 第 3 項,差 A10a 一片。**

### §2.1 §1.1 的六項新需求

| # | 需求 | 狀態 |
|---|---|---|
| 28 | 會員密碼重設 | ❌ 零規劃(後台寄重設信,不得直接設密碼) |
| 29 | 後台對客人發 Email | ❌ 零規劃(接既有 outbox) |
| 30 | 月報表 | ❌ 零規劃、內容未定義 |
| 31/32/33 | 成本 / 會計收支帳 / 成本報表 | ⏸️ **Sean Q3=C 整條延後**,研究成果保留不重查 |

---

## §3 40 片逐片分類(E10 第 1 批;08-03 刷新)

三類定義:**機械** = 照既有 pattern、驗收可機器判定 ⇒ 可排夜跑 /
**需拍板** = 有語意未定或要 Sean 肉眼看 ⇒ 留白天 /
**高風險** = 命中鐵則 12 六類 ⇒ 對抗審查不降級。

| 類 | 片數 | 片號 |
|---|---|---|
| **高風險** | **9** | A2b1 · A4a(DB 守門 trigger)/ A5a · A9v(owner RPC 與 REVOKE = 權限;~~A6~~ ✅ 完成移出)/ A8c1 · A8c2 · A8a1 · A8a2(金流取消 = 錢)/ A9d2-2(cancel server action;note 支 A9d2-1 ✅) |
| **機械** | **19** | A2b2 · A4b(負測)/ ~~A5b · A5c~~(作廢)/ A9a-2 · A9b2 · A9c · A9d1 · A9g · A9h · A9r · A9s / A9e · A9f(顯示端下架)/ A9w1 · A9w2 · A9w3 · A9w4a · A9w4b · A9w4c(九碼退場六片)/ A10c2 |
| **需拍板 / 需肉眼** | **12** | A10a · A10b(表單欄位長相)/ A11a · A11b · A11c(13 欄 + 三軸膠囊 + 手機卡片)/ A12a · A12b(批次 + 部分失敗顯示)/ A13a · A13b(取消流程畫面)/ A14a · A14b · A14c(下一步詞彙 / 編輯入口 / 空狀態) |

🔴 **「機械 = 可夜跑」在 08-03 凌晨被 DAG 實查推翻:19 片機械裡,上游全齊、真的能開工的只有 1 片 —— A2b1。**
(master plan §5.0 `:251-256`:S3b 收工後輪到 A2b1;其餘 18 片全被 **`A2b1 → A2b2 → A4a → A4b → A5a`
五片解鎖鏈**與其後的 A8 層(A8c1/A8c2/A8a1/A8a2,全未動)擋住。)分類是「性質」,不是「可開工」——
08-01 版把兩者混在一起講,已失真。兩個零資料依賴的**縱切候選**是例外:A9f(storefront paid 文案
改固定「處理中」,`:394`)與 A9w2(九碼週邊純下架,`:398`),但縱切 = 偏離 DAG 序,要 Sean 拍板。
另:高風險 ≠ 不能夜跑 —— 前提是該夜 codex 對抗審查可用(鐵則 12);需肉眼 12 片才是真的不能夜跑。

---

## §4 現行線(第 17 項退款)不在那 40 片裡

A7c 換路後,退款線的片是新長出來的、不在 §5.1 表上:

| 片 | 狀態 | 類 |
|---|---|---|
| A7c 退款帳本(記金額 + 14 道守門) | ✅ 已 apply 正式站 | 高風險 |
| **sandbox API 退款探測** | ✅ **08-01 深夜實測收案**(交易 `D202607314b3cIL`):多次部分退款成立(每次獨立 `refund_id`)/ 超額被 `10051` **乾淨擋下、零副作用**;權威 `docs/reference/tappay-reference.md:110-149` | 探測、非施工 |
| **`TapPayChargeAdapter.refund()` 實作** | ❌ `TapPayChargeAdapter.ts:213-216` 仍一行 throw、全樹 `.refund(` 呼叫端 = 0。**材料已齊**:規格(endpoint/參數/回應,`tappay-reference.md:96-108`)+ 可複製 pattern(`charge`/`recordQuery` 同檔)+ 要動 6 檔已盤點(domain types / port / wire 解析器 / adapter+config / composition / 測試)。🔴 **兩個從未被實測的假設不得依賴**:`bank_refund_id` 冪等(probe 從未送過它,`scripts/tappay-sandbox-refund-probe.py:130`)/ 併發重複退(`tappay-reference.md:125-127` 明寫只證序列) | 🔴 高風險(我們的程式真的會動客人的錢) |
| 後台退款按鈕 + 讀模型 | ❌(admin 零 TapPay 接線,composition 不存在) | 高風險 |
| worker / 對帳線(RF2b-RF8) | ❌ 第 3 批 | 高風險 |

⇒ **第 17 項要全綠,最少還要 4 片**,每一片都是高風險;高風險可否夜跑取決於當夜 codex 是否可用(鐵則 12)。

---

## §5 誠實邊界

- **40 是「已拆片」的數字,不是「總共還剩幾片」。** 13 個未拆區塊與 5 個零規劃領域
  展開後的片數目前**無法誠實估計** —— 硬估就是重犯 7 月的錯。
- 27 項的狀態欄 08-01 首查、**08-03 凌晨重查**(admin 路由數、`'use server'` 檔數、
  四關鍵字 grep 全部重跑),不是抄文件。資料層狀態依 STATUS.md 稽核段口徑
  (S1a/S1b/S2、A6 已 apply 正式站;08-01 ledger 錨 `20260801120000` 之後的 apply 未逐筆重驗)。
- §3 的三類標記是**我的判斷**,不是 Sean 拍板過的。特別是「需拍板」那 12 片,
  有些可能只要一次批次問完就變機械。
- **E10 完成 ≠ 後台完成**:E10 v2 §6.1 定義的完成只涵蓋 27 項中的 17 項,
  商品線(20-24)、客戶全欄位(25′)、認證(26)四塊**完全在 E10 之外**。
  ⇒ 依 Sean「後台是二元切換」的前提,**E10 做完員工還是不能上工**。

— END —
