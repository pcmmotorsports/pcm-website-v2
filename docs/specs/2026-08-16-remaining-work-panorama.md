# 剩餘工作全景(2026-08-16 夜 · I 窗)

> **為什麼有這份檔**:主視窗逐字「**我手上沒有一份可靠的『還剩什麼』,一直在用各窗自己建議的下三件派工
> —— 那是【由下往上】的,會漏掉沒有窗在看的那些。**」
>
> **範圍(刻意收窄)**:**由現在往前看**。R 窗做「由過去往回看(活的未結線)」,兩份不重疊。
>
> ## 🔴 三條落筆紀律(格式要求來自派工,逐條照做)
> 1. **每一項附可重跑的判斷依據**(`檔案:行號` / `grep` / commit hash)。**不寫「印象中」。**
> 2. **查不出來的明寫「未確認」** —— 一份 20 項確定 + 5 項未確認的地圖,比 25 項都很像確定的有用得多。
> 3. **不估工時** —— 那會變成一個沒人量過而所有人引用的數字。
>
> ## 🔴 「未開工」這一格分成兩種(2026-08-16 立,本檔第一次實施)
> | | 意思 | 是不是缺口 |
> |---|---|---|
> | 🅿️ **排隊中** | **有人決定過先不做**(拍板 / 明文延後) | **不是** |
> | 🕳️ **沒人在看** | **沒有人決定過**,只是沒出現在任何佇列 | **是** |
>
> **兩者在任何進度表上都長成「未開工」** —— 混在一起會讓真缺口被稀釋掉。
> 📎 起因:搜尋線一直被讀成「沒人在做」,實際是 Sean 拍板要它排在 M-4b 之後。
> R 窗逐字:**「照拍板在等」與「沒人在做」外觀一樣。**

---

## 0. 🔴 先講一件會影響所有人引用的事:**基準表換了,而且項數不是 27**

主視窗派工時寫「**依據 §1「員工的一天」27 項**」。**實查兩處都要更正**:

```
docs/specs/2026-07-25-admin-backend-rebuild-spec.md
  §1    「員工的一天」原表  —— 檔內【自己標著】「2026-08-06 快照,已過期、保留供對照」
  §1-A  2026-08-14 重盤     —— 檔內【自己標著】「這一節是現行事實」
項數   §1-A 逐字「共 33 項」，不是 27
```
⇒ **本檔以 §1-A 為基準。** 用 §1 會拿舊尺量新東西 —— 那正是 §1-A 當初被建立的理由
(它逐字寫著「原表其間 `#19`/`#15`/`#17` 皆已可用而表內仍標 ❌ ⇒ 拿舊尺量新東西,回答不了『今天前進幾項』」)。

⚠️ **而 §1-A 自己也已經是 2026-08-14 的快照** —— 本檔是 **2026-08-16 夜**的重盤,見下一節。

---

## 1. 後台 admin —— 33 項對 §1-A 的增量(**只列有變動的**)

> **量法**:逐項開檔 / `grep`,證據在每一列。**未變動的 26 項不重列**,去 §1-A 看。

| # | 事 | §1-A(08-14) | **現況(08-16 夜)** | 證據(可重跑) |
|---|---|---|---|---|
| **10** | 列印出貨單 / 揀貨單 | ❌「整個能力不存在」 | 🟡 **兩條列印路都做出來了** | `apps/admin/src/app/print/orders/[id]/picking/page.tsx`、`.../shipping/[shipmentId]/page.tsx`;`ls apps/admin/src/components/print/` ⇒ 3 支 |
| **13** | 改訂單內容(品項/數量/金額) | ❌ | 🟡 **金額那半有了** | `ls apps/admin/src/lib/orders/ \| grep -c amount` ⇒ 8;`grep -rln 'admin_update_order_item_amount'` ⇒ 8 檔 |
| **16** | 今日對帳 | ❌「首頁仍是骨架」 | ✅ **三格對帳數字已上** | `apps/admin/src/app/page.tsx` 檔頭逐字「骨架說明卡下架,換成對帳數字…現為三格」 |

### 🔴 三個我**差點誤判**的,留痕(判別法都是「目錄存在 ≠ 做得到」)

| # | 表面訊號 | 開檔之後 | 結論 |
|---|---|---|---|
| **20** 新增編輯商品 | `apps/admin/src/app/products/` **有 3 個項目** | 只有 `page.tsx`(列表)+ `[id]/page.tsx`(明細)。`find … -name new -o -name create` ⇒ **0**;`grep -rln "'use server'" products` ⇒ **0** | **仍 ❌** —— **只有讀,零寫入** |
| **9** 單號追蹤 | `grep -rl 'hct\.com\|順豐\|logistics.*api'` ⇒ **3 檔** | 三檔都是 `carrier-label.*` 與 `tracking-number.ts` —— 命中的是**中文標籤字面**,不是外部查貨 API | **仍 🟡** —— 只顯示文字 |
| **11** 出貨 Email | M-4a 有 `email_outbox` 骨架 | `grep -rln 'email_outbox' apps/admin/src/lib/shipping apps/admin/src/components/orders` ⇒ **0** | **仍 ❌** —— 出貨路徑零 writer |

📎 **這三個是本節最值錢的部分**:它們都會讓「今天前進幾項」多算,而**三個的表面訊號都是正的**。

> ### 🔁 2026-08-17 04:5x I 窗逐格重跑本節每一支量法 —— **三個數字過期,而【結論一個都沒變】**
>
> | 格 | 檔內寫 | 重跑得到 | 結論變了嗎 |
> |---|---|---|---|
> | `#26` docs/specs 草稿 | `3` | **`5`** | ❌ 沒變(仍「一支都沒 apply」,migration 側仍 `0`) |
> | `#9` 外部查貨命中 | `3` 檔 | **`6`** 檔 | ❌ 沒變(仍 🟡 只顯示文字) |
> | `#13` RPC 檔數 | `8` | **`13`** | ❌ 沒變(金額那半有了) |
>
> **未動的**:`#12`/`#21`/`#24` 皆仍 `0`、`#26` migration 仍 `0`、`#20` 仍 `0`、`#11` 仍 `0`、
> `#10` 列印元件仍 `3` 支、`#13` amount lib 仍 `8`。
>
> #### 🔴 `#9` 那格我逐檔開來看,而**我用來佐證的那支 grep 沒有判別力**
>
> 六個命中**逐檔分類**:五檔是中文標籤 `順豐`、一檔(`tracking-number.ts`)是
> **文件檔名引用** `hct-logistics-api-reference.md`(`:6`/`:59`/`:120` 三處都是註解)
> ⇒ **零個是真的對外查貨呼叫** ⇒ 原結論成立。
>
> ⚠️ **但我另外跑的「對外 fetch 有幾支」正向對照回 `0`**(全 `apps`+`packages`)
> ⇒ **那支 pattern 在「有對外 fetch」的世界會不會動,我沒有證出來** ⇒ **它是一把沒有判別力的尺,不採用。**
> 🔴 **這格的結論靠的是【逐檔開檔分類】,不是那支 grep 的計數。** 兩者證據強度不同,不要混用。

### 仍然 ❌ 且**確認過沒動**的(抽驗,附量法)

```
#12 手動建單      find apps/admin/src/app/orders -type d -name new   ⇒ 0
#21 上傳商品圖片  grep -rl 'supabase.*\.storage' apps/admin/src packages ⇒ 0（連底層都沒接）
#24 匯出訂單商品  grep -rli 'export.*csv|toCsv|downloadCsv' apps/admin/src ⇒ 0
#26 員工帳號      grep -rl 'admin_user_staff_map' supabase/migrations ⇒ 0
                  grep -rl 'admin_user_staff_map' docs/specs          ⇒ 3
                  ⇒ 🔴 只有【草稿】，一支都沒 apply。B 窗在做，卡在「要 Sean 在場才能 apply」
```

---

## 2. 前台 storefront —— 🕳️ **這塊我拿不出可靠的地圖,明寫未確認**

**主視窗自陳「這塊我完全沒有畫面」,而我實查後的結論是:我也沒有。**

**為什麼(不是我懶)**:Phase 1 北極星是「**把 design 上架**」,
⇒ 要回答「還有哪些 design 沒上架」,需要**逐頁比對 design-reference 與 storefront 實作**,
而 **design 真權威的位置本身有例外**(首頁改版線的真權威是 Open Design 的 `pcm-home-redesign`,
而 submodule 裡的 `components/HomePage.jsx` 是**過期假稿、明文不得引用**)。
⇒ **一次沒有先確認真權威位置的比對,會產出一份看起來完整而基準是錯的地圖** —— 那比沒有更糟。

🔴 **已知的兩個確定項(不是全景,是我確定的兩點)**:
```
· 搜尋框是死的           🅿️ 排隊中但【搜尋框那半不排隊】
                          Sean 拍 Q-SEARCH-0=乙：維持「M-4b 之後」，但先處理死掉的搜尋框
· A 窗有一份前台建置盤點  ⚠️ 在 bmw-m-headline 分支的 f7bd720f，【尚未收割進 dev】
                          ⇒ 收割後那份才是這一節該引用的東西
```
**⇒ 本節的正確下一步不是「我再猜一遍」,是【收割 A 窗那份盤點】然後以它為底。**

> ### ✅ 2026-08-17 I 窗重跑本節量法:**上面那一格已過期,而它擋著的事還沒做**
>
>     git merge-base --is-ancestor f7bd720f HEAD   ⇒ rc=0（已在 dev）
>
> ⇒ **「尚未收割進 dev」那句現在是假的** —— 那份盤點**已經在 dev 上**。
> 🔴 **但這一節【不會因此自己變成完成】**:前置解除 ≠ 事情做完。
> 本節的下一步從「**等收割**」變成「**以 `f7bd720f` 那份為底,把 §2 重寫一次**」,
> **而那件事現在沒有任何窗在做** ⇒ 依本檔 §4 的判別法,它剛剛從 🅿️ **變成 🕳️**。
> ⚠️ **我(I 窗)不自己重寫 §2** —— 那是**前台建置的內容判斷**,不是搬運;
> 我能做的只有把這格標成過期並指出它現在歸誰都不歸。
>
> 📎 **留原句不刪**:它記錄的是當時為什麼停在這裡。**過期的是狀態,不是那段判斷。**

---

## 3. 報價單 `pcm-quote-v2` —— ⚠️ **跨 repo + 跨機器,量得到的很少**

```
真身位置    mac mini（本機 clone 已知落後）
量法        用前必 git fetch 並對 origin/main 驗；跨機器派工走 Sean 轉貼
出處        memory reference_quote-repo-truth-moved-to-mac-mini
```

**本機量得到的兩件(都已由 E 窗查證,不是我查的)**:
```
· 整個 repo 從未被安全稽核過      —— 唯讀稽核帳號待 Sean 貼 E-674b 的 SQL
· cron.job 有 9 個排程、2 個疑似把祕密寫在排程定義裡
  anon 進不去 ⇒ 外部安全；但 postgres/supabase_admin 可讀，而那是 Dashboard 的身分
  🔴 立案時只寫排程名不寫 command 內容 —— 把祕密寫進 backlog 等於再洩一次
  正本：docs/security/2026-08-16-secrets-in-cron-job-definitions.md（E 窗自己落的檔）
```
🔴 **除此之外的一切,本檔標【未確認】** —— 需要 Sean 或 mac mini 上的人補。
**不要把「我在本機量不到」寫成「那邊沒有東西」。**

---

## 4. 🕳️ **目前沒有任何窗在看的線** —— 這一節是本檔的重點

> 判別法:①它在某個檔裡被立過案或被 Sean 拍過 ②而**現在沒有任何窗的佇列提到它**。
> ⚠️ 本節**不含**「照拍板排隊中」的東西(那些不是缺口)。

| 線 | 依據 | 為什麼算沒人在看 |
|---|---|---|
| **客戶篩選四欄**(年齡 / 性別 / 居住地區 / 登入方式) | Sean 早先拍「**能做的話都做**」;性別已拍甲(男/女/不願透露)。memory `project_0816-sean-three-rulings-and-taxonomy-count` §③ | 該 memory 逐字:「**2026-08-16 當下沒有任何窗擁有這條線**」。📎 「登入方式」那欄標**待查**(資料在 Supabase `auth` schema,admin 這側讀不讀得到未確認) |
| **`#26` 真登入的【報價單那一半】** | `apps/admin/src/lib/session/session.ts:12` 逐字「報價單=共用密碼登入,SSO 只帶認證、無 per-user 身分」 | B 窗做的是 **admin 這一半**;**報價單那一半跨 repo 跨機器,沒有窗在做** |
| **`#22` 改價格/上下架的呼叫端** | `SupabaseProductAdapter.ts:431-466` **引擎已寫好、零呼叫端**,adapter 自己的 JSDoc 承認「今天沒有正式呼叫點…是死路」 | 沒有任何窗的佇列提到接上它 |
| **`#11` 出貨 Email 的 enqueue** | `grep -rln 'email_outbox' <出貨路徑>` ⇒ **0** | M-4a 通知線標**暫緩**,而「出貨時 enqueue」是**另一件**、不在暫緩範圍內 ⚠️ **未確認**:這是不是也算在暫緩裡,要 Sean 一句話 |

---

## 5. 🅿️ 排隊中(**不是缺口,列出來是為了讓上面那一節乾淨**)

```
搜尋線主體        Sean 拍 Q-SEARCH-0=乙：M-4b 之後
                  ⚠️ 但「死掉的搜尋框」那半不排隊
M-3 TapPay 正式化 S3 起、退刷線 RF2b 起   —— STATUS 標「暫緩非作廢」
M-4a 通知線 B-4                            —— 同上
會計線整條                                  spec §0a 逐字「Sean 2026-07-25/26 拍板：整條延後」
9 大藍圖 schema                             不在 Phase 1（NORTHSTAR）
```

---

## 6. 本檔的誠實邊界

```
· §1 節只列【相對 §1-A 有變動的】3 項 + 3 個差點誤判 + 4 項抽驗，
  不是 33 項逐項重盤 —— 沒重盤的那些以 §1-A 為準，而 §1-A 是 08-14 的快照
· §2 前台整節【未確認】，正確下一步是收割 A 窗的 f7bd720f 再以它為底
· §3 報價單只有兩件是量得到的，其餘未確認
· §4 的四條裡，最後一條（出貨 Email 算不算在暫緩範圍）需要 Sean 一句話才確定它是不是缺口
· 全檔【不估工時】
```

🔴 **本檔會過期,而且過期時零訊號。** 引用前重跑每一列的量法 ——
📎 那正是 `STATUS.md` 檔頭那條常設規矩:**凡描述世界狀態的句子,旁邊固定附一條可重跑的量法。**
