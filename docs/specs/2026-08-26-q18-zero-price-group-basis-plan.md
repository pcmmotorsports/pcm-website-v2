# `Q18` 群基準價 0 元 —— 片A 已交 / 片B 緩做 · plan(信箱正本搬進版控)

> 2026-08-26 · 線2 `ff` · **本檔 = `~/pcm-mailbox/63-線2-Q18-群基準價0元-plan-20260826.md` 的副本**
> 🔴 **信箱那份【留著沒刪】** —— 刪掉的話,任何引用舊路徑的東西當場斷線,**而斷線是安靜的**。
> ⚠️ 而**兩份會漂**:以後只改本檔;信箱那份自本行起視為快照,不要再改它。

---

## 🚦 讀本檔之前必看的三格(狀態已經變了,下面的內文是拍板【當時】的樣子)

### ① 片A **已交件**
```
commit  977bbf02
落點    scripts/rpm-mixed-zero-scan.sql(238 行)
形狀    一發【唯讀 SQL】, 人工貼進報價單專案的 Supabase SQL Editor 跑
        —— 🔴 不是內文 §2.4 寫的「在 rpm-import.ts 裡加 TS 掃描」, 那個方案已作廢(見內文 MF-5)
2026-08-26 實跑  混 0 群 0 · groups_scanned 20,909 · canary=1 · 負對照 0
```
🔴 **它【不會叫】。** 送達是**【已排,未做】**(不是不做)。誰跑 / 何時跑 = 內文 §2.0b,
**而那張表已抄進 `scripts/rpm-mixed-zero-scan.sql` 檔頭** ⇒ 那支檔自帶觸發值,不依賴本檔。

### ② 片B **Sean 2026-08-26 拍板【緩做】** —— 不是不做
```
他逐字「全部依照建議」, 而端過去的那題是:
  Q: 改客人看到的價格那片, 現在做嗎?  甲 現在做 | 乙 等真的出現 0 元再做   ⇒ 【乙】
推薦理由(字面是視窗寫的, 他貢獻的是「照建議」這個授權動作):
  今天量到來源 0 元的群 = 0, 而片A 已在 ⇒ 【偵測得到才做】
```
🔴 **所以「等真的出現 0 元」這個條件,誰去看、多久看一次 = 內文 §2.0b 那張觸發值表。**
**沒有那張表,這個「緩做」就會變成「忘了做」** —— 條件沒有人盯,等於沒有條件。
⇒ **一律寫「片B 緩做,條件見 §2.0b」。不要寫「片B 不做」,也不要寫「待評估」。**

### ③ 🔴 `G6`:**`mixed = 0` 不代表 `any_zero = 0`,而卡片一樣會印 `NT$ 0`**
```
正價那支下架了、活的只剩 0 元贈品
⇒ liveVariantsOf 只回那一支 ⇒ 基準價 = 0 ⇒ 【卡片照樣印 NT$ 0】
⇒ 而群裡已經沒有東西可以「混」⇒ 本 plan 的 mixed 定義【抓不到它】
⇒ 接住它的是 any_zero_groups 那一格
```
📌 **這一格是 2026-08-26 自檢撈到的:我的文件寫 `true`,機器回 `false`。改的是文件,不是那把尺。**
⇒ **引用快篩結果時兩格都要看。** 而「整群都是 0 該不該當贈品組放行」**是 Sean 的題,還沒拍。**

---

## ⚠️ 撞號警告:`Q18` 在本 repo 是**三件不同的事**
```
2026-07-28  Q18 = 取消原因 allowlist 與對客映射   docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md:538
2026-08-12  Q18 = 訂單面板收合                    docs/specs/2026-08-12-admin-order-ui-design-brief.md:674
2026-08-26  Q18 = 群基準價 0 元(本檔)
```
🔴 **拿 `Q18` 去 grep 會撈到三個不相干的拍板。** 引用時一律帶日期。
(同族:`docs/specs/2026-07-25-admin-backend-rebuild-spec.md` 檔頭記著 33 之 33 全撞號。)

**相關落點**:backlog `#934`(商品層)· `#936`(JSON-LD `lowPrice`,同一個 0 的另一個下游)·
`docs/specs/2026-08-26-migration-silent-apply-path-plan.md`(§0 帳本漂移那條線的續集)。

---
---

# 線2 · `Q18` 群基準價 0 元 —— plan **v5**(折完 R3 · Fable 複量窗)

⚠️ **版號本身漂過**:上一版檔頭寫 `v2`,而口頭與交接檔一度稱它 `v4`。**本行起以檔頭為準,前面那兩個字面都不要再引用。**
> R3 全文 `~/pcm-mailbox/R3-複量窗-63-Q18-plan-對抗審查-20260826.md`(`c3` 產,主視窗轉)
> R3 = 5 條 must-fix + 3 條 nit,**其中 MF-1 / MF-2 打的是本 plan 的框架,MF-3 打的是主視窗的裁定**

> session `pcm-website-v2-ff` · **零 code 改動** · backlog 正本 `#934`(`d85514a7`)
> Sean 2026-08-26 拍 `Q18 = 丙`(兩個都做)· 主視窗 `-96` 裁「片A 先出」「送達走丙」
> 🔴 **鐵則 12①(錢)+ 12③(批次匯入)⇒ codex 對抗審查不降級,本窗自己跑**
> **v1 已被 codex R1 打成 NO-GO,3 條 top must-fix + 多條;v1 存 `/tmp/l2-63-v1.md`**

---

## §0 🔴🔴 **帳本漂移(v2 這一節原本是【錯的】,已撤回改寫)**

### ~~v2 原文:「DB 那一半沒上」「部署前置 = 先 apply」~~ ⇒ **撤回**
🔴 **撤回理由有兩層,而第一層不依賴任何人的複驗:**
```
我實際量到的  grep -c '20260825120000' supabase/APPLIED.tsv ⇒ 0
              ⇒ 它只支持一句話:「它不在那本帳上」
我寫出去的    「DB 那一半沒上」
              ⇒ 這是【推出來的】, 不是量到的
```
⇒ **「帳上沒有」與「沒有部署」是兩個宣稱。** 就算下面 codex 那半是錯的,**我原句在我自己的證據上就站不住。**

### codex R2 用正式庫證偽(⚠️ **我方無存取權,未獨立複驗**)
```
sync_product_variant_group(...) 的 live prosrc MD5 = fb93f08db68acffb49057c19226bb0a3
本地 20260825120000...sql 的函式 body MD5          = 相同
live 的判斷是 `< 0`                                ⇒ 【已經允許 0】
而 20260825120000 不在 Supabase migration history, 也不在 APPLIED.tsv
```
### ✅ 現行結論
> **DB 行為疑似【已經上了】,而【兩本 migration 帳都沒有這個版本】⇒ 帳本漂移,套用路徑未留紀錄。**

🔴 **而這比原本那句更該讓人知道**:一個**已經生效、而帳上查不到**的 DB 變更,
**下一個做部署盤點的人會判它「還沒上」然後再套一次** —— **那正是我剛剛做的事。**
⇒ **本 plan 的前置改成**:動片B 之前先確認那支函式的 live 行為(不是查帳本)。

### 🏁 2026-08-26 **Sean 本人跑完那四發,這一節從【疑似】變【確定】**
```
1-3  正式庫 supabase_migrations 帳裡【也沒有】那幾個版本號  ⇒ 兩本帳都空
4    live_md5 = fb93f08db68acffb49057c19226bb0a3 · allows_zero = true
```
⇒ **不是「還沒 apply」,是【apply 了而兩本帳都沒記】= 帳本漂移,確定。**
⇒ 🔴 **處置因此【反過來】:不要去套它(會重複執行),要去【補帳】。**
🔴 **而那個 md5 與 codex 先前報的逐字相同,而那是兩條獨立路徑**(codex 一次、Sean 手跑一次)
⇒ **這次的一致是真的兩個證據,不是同源複印。**

### 🔴 `nit-6` 修正:「我方做不到」那句**寫得比事實寬**
```
❌ 原句  「查法我方目前做不到」
✅ 事實  網站【正式庫】 ⇒ 本窗確實無存取權(這四發是 Sean 手跑的)
         報價單【來源庫】⇒ 🔴 本窗【有】唯讀 MCP, 而且本輪就用它量了(見 §1 MF-1 表)
⇒ 一句「做不到」把兩顆不同的庫黏成一顆, 而其中一顆我做得到。
```
📌 **「做不到」是會關掉下一個人動作的句子** —— 寫寬了,等於替別人放棄了一條他其實走得通的路。

---

## §1 兩片,片A 先出 —— 🔴 **而理由換掉了,v1 那個是錯的**

| | 片A · 偵測 | 片B · 跳過 0 元款 |
|---|---|---|
| 改什麼 | 只偵測 | **客人看到的價格(三個載體)** |
| 鐵則 | 12③ | **12①(錢)+ 12③** |
| 壞掉會怎樣 | 多印 / 沒印 | **卡片價變了,而且是對外的** |

### 🔴 v1 的理由是錯的,原樣留著當紀錄
> ~~「沒有片A,片B 交出去之後我們**沒有任何辦法知道它有沒有生效**」~~
**那是一個無數法的全稱句,而且它是假的** —— codex R1 指出:
**DB read-back / fixture / 來源群掃描,三條路都能驗片B。**
📌 **而最刺的一格**:那句話是我**用來說服主視窗「片A 是必要第一步」的核心論據**,
**而主視窗採用了它、還加碼寫成「這不是先做小的,是先做量具」——兩個人各自覺得對,而沒有人量。**

### ✅ 新理由(它單獨站得住,不需要那個全稱句)
> **片A 先出,是因為它【不改客人看到的價格】,而片B 會改。**
> **那是爆炸半徑的差,不是「無法知道」。**

### 🏁 2026-08-26 裁定:**這輪只出片A;片B【已排,等片A 的資料】**

🔴 **這【不是推翻 Sean 的 `Q18 = 丙`】,講清楚**:
```
他拍的丙 = 兩個都做。而【兩個都做】沒有規定順序。
片B 排在片A 之後 = 排序, 不是刪除。
⇒ 一律寫「片B 已排、等片A 的資料」
   ❌ 不寫「片B 不做」
   ❌ 也不寫「片B 待評估」—— 那讀起來像可能不做
```
⚠️ **本段自己含那兩個禁用字面 ⇒ 拿它們去 grep 會命中【這三行】。**
數法要排掉本段:`grep -n '片B 待評估' <本檔> | grep -vc '不寫'` ⇒ **0 rc=1**(同理另一個)。
📌 **一條禁止某個字的規則,自己一定含那個字** —— 與本 repo「負對照的靶被寫下就燒掉」是同一族,
**而這一個更乾淨:它【百分之百必然發生】,不是機率問題。**
🔴 **本行自己是命中之一。** 掃到本段請當成 0。
⚠️ **兩層都要**:①同段註明「本行自己是命中之一」②數法要能排掉本段(上面那條 `grep -vc`)——
只做①,下一個人跑尺還是拿到 1;只做②,他看不懂那個 `grep -vc` 在排什麼。
### 🔴🔴 **`MF-1` + `MF-2`:上一版支持這個排序的兩個理由,【兩個都倒了】**

#### ~~「症狀一次都沒有被觀察到」「資料進來之後,片B 的規模才有分母」~~ ⇒ **兩句都撤回**

**`MF-2` —— 那個「從未觀察到」是【被閘保證的】,不是稀有性:**
```
rpm-transform.ts:315-316 自己寫著:08-25 之前 isAbnormal `<= 0` 硬 abort
⇒ 在那道閘底下, 0 元列【不可能】被觀察到
⇒ 🔴 我拿一個【被閘擋出來的零】當成「這件事很罕見」的證據
而三道閘(DB RPC / importer / checkout)2026-08-25 同日全開
加上 Sean 拍板逐字「乙:會, 偶爾有」
⇒ 推遲的依據要換成【本輪量到的 0】, 不是「沒觀察過」
```
📌 **這是今晚第四次同一個形狀**:`#934` 標題二那句「未量」是**我自己寫的**,
而我後來**拿它當論據**,中間沒有人去量 —— **一個自己寫的空格,被自己讀成一個事實。**

**`MF-1` —— 分母不用等片A,一發唯讀 SQL 今天就有。而它已經被跑了兩次:**

| | `c3`(Fable)2026-08-26 | 本窗 `ff` 獨立複量 2026-08-26 |
|---|---|---|
| 範圍 | 整張 `storefront_catalog_v` | **只算 `delisted_at is null`** |
| 寫法 | `min` / `max` 聚合 | **`bool_or` 聚合** |
| 列 / 群 | 52,741 / 20,879 | **51,579 / 20,625** |
| **混 0 群** | **0** | **0** |
| 任何 0 元列 | 0 | **0** |
| 任何 null 價 | 0(view 內) | **0** |
| 正對照 | `min≠max` 有高低群 ⇒ 3,446 | **以 1000 為界的混群 ⇒ 427** |
| 負對照 | `supplier_slug='zzz-q18-20260826'` ⇒ 0 | **`'zzz-q18-ff-20260826'` ⇒ 0** |

🔴 **兩組列數不同,而那【不是矛盾】,是我先問了「這兩個數是不是同一種東西」:**
```
52,741 − 51,579 = 1,162 = 已下架列(當場查 delisted_at is not null ⇒ 1,162)
20,879 − 20,625 = 254   = 只由已下架列構成的群
⇒ 兩組數字對得起來, 而差的那一格【正好是本 plan §2.2a 的洞②】
⇒ 而我這一側才是【匯入實際看得到的範圍】(liveVariantsOf 的語意)
```
📌 **量法**(唯讀,無寫入;本窗有報價單庫的唯讀 MCP、**沒有**網站正式庫權限):
`bool_or(round(price_retail)=0)` 與 `bool_or(round(price_retail)>0)` 同群同時為真 ⇒ 混 0 群。

### ✅ **排序的新依據(舊的兩句都不要再用)**
> **今天量到 0 ⇒ 片B 現在【沒有任何一筆資料會走到它】。**
> **而片A 也一樣 —— 它今天也偵測不到東西。兩片今天都沒有輸入。**
> ⇒ **真正的差別只剩爆炸半徑:片A 不改客人看到的價,片B 會改。**

🔴 **`MF-3` 打的是【排序這件事本身】,而那不是我裁的** ——
「片B 排在片A 之後」是**主視窗裁的**,而 **Sean 沒有被問過順序**,
而他同夜六題全部否決了「先做小的」這個形狀(`project_0826-sean-answers-decision-table.md:52-58`)。
⇒ 🔴 **本段的排序標記為【待 Sean 確認】,主視窗已表示由他端過去、不再自裁。**
⇒ **在他回覆之前,本 plan 不宣稱片B 的順序已定。**

---

## §2 片A · 偵測 —— 🔴 **送達【未做】,而這件事要寫在標題上**

### 2.0 🔴🔴 **`MF-5` 答案:片A = 【一發唯讀 SQL】,不是 `rpm-import` 裡的 TS 掃描**

**這一格是主視窗指定要先答的,因為它改整片的形狀。答案:走 SQL。**

| | **SQL 版(採用)** | TS 版(原 §2.4,作廢) |
|---|---|---|
| 動到匯入主路徑 | **零** | **要改 `rpm-import.ts` 的群迴圈** ⇒ 那就是鐵則 12③ 那條路本身 |
| 洞①(`rpm-delta` 拿不到來源列) | **不存在** | 要靠落點選對才避開 |
| 洞②(已停產款誤報) | **一個 `where delisted_at is null`** | 要靠呼叫 `liveVariantsOf` 才避開 |
| 跑得動嗎 | **今天跑過兩次**(`c3` 一次、本窗一次,不同寫法) | 未跑過 |
| 三綠 / 突變 / 回歸風險 | **零**(不進 build) | 要補測試、要突變、要一發實跑 |

**🔴 而掃哪一張的疑慮,本輪查掉了:**
```
rpm-import.ts:152  const source = createClient(QUOTE_SUPABASE_URL, ...)
rpm-import.ts:165  fetchAllSupplierProducts(source, config.supplierSlug)
                   ← grep -n '(source' ⇒ source 這個變數只在這一行被傳出去
rpm-fetch.ts:130   export async function fetchAllSupplierProducts(src, ...)
rpm-fetch.ts:133     fetchPageWithRetry(src, from, supplierSlug)   ← 唯一呼叫處
rpm-fetch.ts:108   async function fetchPageWithRetry(src, ...)
rpm-fetch.ts:112     src.from('storefront_catalog_v')             ← 唯一一處
⇒ 匯入讀的就是我掃的那一張 ⇒ 掃 view 是【對齊匯入】, 不是偷懶
```
🔴 **上一版這裡引的是 `rpm-import.ts:4` —— 那是【註解】,是一句宣稱,不是行為。**
**而註解會過期,碼不會。**(`c3` 指出,我複驗成立。)
⚠️ **而我複驗時抓到 `c3` 那條鏈【少了一跳】**:`:112` 住在 `fetchPageWithRetry` 裡,
而 `fetchAllSupplierProducts` 從 `:130` 才開始 ⇒ **`:112` 不在它的範圍內,是它呼叫的下一層。**
**結論不變,而引用要對得上行號。**
📌 分母:`grep -h '\.from(' scripts/rpm-*.ts --exclude='*.test.ts' | wc -l` ⇒ **14**(1 讀 / 13 寫);
**含測試檔則是 18** ⇒ 🔴 **兩個數都對,量的不是同一堆東西 —— 引用時要說是哪一種。**
⇒ 🔴 **這同時關掉 `nit-7`**:view 尾端 `_group_has_zh` 會濾掉無中文名的群,
**而匯入自己也讀這張** ⇒ 那些群**根本不會被同步進來** ⇒ **它們產不出 NT$0 的卡片。**
**對片A 的目的而言,那個盲區是【正確的邊界】,不是漏掉。**
⚠️ 而它仍是**一個要寫下來的限制**:那些群若哪天補上中文名就會一次湧進來,而 SQL 那天才看得到。

### 2.0a ⚠️ **SQL 版的代價,寫在正面不要藏**
```
SQL 是【拉】 ⇒ 要有人跑它, 沒人跑就沒有答案
TS  是【推】 ⇒ 自己會跑, 而它印進一份【沒有讀者】的 log(MF-3)
🔴 而「推進一個沒有人看的地方」不比「拉但是有人看」好 —— 送達【未做】是兩邊共有的洞
⇒ 所以觸發值一定要定(見 2.0b), 不定的話 SQL 版就真的只是一次性快照
```
另外兩格 SQL 版**看不到**、要照實寫:
```
① 它量的是【來源】, 不是【我們寫進去的結果】
   ⇒ rpm-transform 自己算錯基準價的世界, SQL 看不到(那是片B 的驗收要處理的)
② 它是一個時點 ⇒ 「今天 0」不等於「明天 0」, 而 Sean 逐字說「會, 偶爾有」
```

### 2.0b ✅ **觸發值(`MF-3` 要的那一格,寫死不留白)**
```
誰跑    上贈品的人 / 做部署盤點的人 / 任何要動 rpm-* 的窗
何時跑  ① 報價單側【要灌第一支贈品之前】     ← MF-4 的交接點, 見 2.0c
        ② 每次要動 rpm-transform 的價格挑法之前
        ③ milestone 收尾盤點
命中怎樣 混 0 群 > 0 ⇒ 🔴【當日開工片B】, 不是記進 backlog 等
沒命中怎樣 把數字與日期寫進 #934, 不要只寫「查過了」
```

### 2.0c 🔴 `MF-4`:**時鐘的起點不在本 repo**
```
症狀開始的時刻 = 有人把第一支贈品灌進【報價單 DB】
而那個動作在報價單專案(/Users/sean_1/API大量上架/PCM報價單-V2), 本 repo 沒有任何東西會知道
⇒ 要一句跨 repo 交接寫進報價單側:「上贈品之前先通知網站側, 網站側要開片B」
⚠️ 本窗的檔案面【不含報價單專案】⇒ 這一句我不自己去寫, 端給主視窗指派
```

### 2.1 措辭(主視窗釘死,不准軟化)
```
✅ 允許寫  「偵測已裝、送達【未做】—— 現在沒有人會被通知」
❌ 不准寫  「已加一道會叫的閘」
❌ 不准寫  「已加一道觀測」        ← 這個也不行, 它讀起來像做完了
```
🔴 **而「送達」是【已排、未做】,不是【不做】**(2026-08-26 Sean 選 B,逐字
「這輪只做偵測,明寫『目前沒有人會被通知』,**送達另外再做**」;主視窗把 CI 總結步驟那件
單獨留在他手上當**另開一片** —— **他沒有否決它,他是把它排到後面**)。
⇒ **plan / commit / backlog 一律標「送達已排,未做」。標成「不做」是另一個意思,不准。**
🔴 **連帶:`#934` 的關閉條件要同批改掉。** 它現在寫「日常匯入遇到群內混 0 元時**有一條會叫的路徑**」——
**片A 不滿足它。不要讓它被判成滿足。**

### 2.2 偵測條件 —— **改成掃「來源混 0 群」,不是只看 new-item delta**
v1 寫的是「新商品 + `newPrice === 0`」。codex 指出**三個漏掉的世界**:
```
① 既有商品新增一支 0 元變體 ⇒ 商品層基準價變 0, 而它【不是新商品】⇒ v1 漏
② 既有列本來 price 為 NULL ⇒ NULL→0 會被誤報成新品;而 0→0 完全沒事件
③ `newPrice === 0` 也會命中 `-0` ⇒ 把「負價取整異常」誤叫成贈品
   ⇒ 要用 Object.is(newPrice, 0) 才分得開(而 -0 那條本來就該由 isAbnormal 硬擋)
```
⇒ **正確的偵測對象是【來源資料裡的群】**:同一群內同時存在**取整後為正零**的列
與 `roundTwd(price_retail) > 0` 的列。**那是 `#934` 標題二一直缺的那道檢查本身。**

### 2.2a 🔴 而 codex R2 指出這個新規格自己還有兩個洞,**我複驗成立**
```
洞① 資料落點不對:rpm-delta.ts 拿不到來源列
     實查 grep -c "SourceProductRow" scripts/rpm-delta.ts  ⇒ 0 rc=1
          對照 grep -c "SourceProductRow" scripts/rpm-import.ts ⇒ 2 rc=0
          (rpm-delta.ts:16 只 import ProductRow / VariantRow)
     ⇒ 偵測必須住在 rpm-import.ts, 不是 rpm-delta.ts
洞② 要掃 liveVariantsOf(variants) 不是原始 groups
     ⇒ 否則【已停產的 0 元款】會被誤報成混 0 群
洞③ 我自己寫的 `roundTwd(...) === 0` 與同段要求的 Object.is(..., 0) 【互相衝突】
     ⇒ `=== 0` 會收進 -0, 而 -0 是【負價取整異常】不是贈品(它該由 isAbnormal 硬擋)
     🔴 我在同一節裡寫了兩個互斥的判準, 而兩個都是我自己寫的。
```

### 2.3 這片的驗收
```
① 該印的餵一發  群內混 0 元 + 非 0 元 ⇒ 命中
② 不該印的餵一發 群內全非 0            ⇒ 零命中
③ 群內全 0(真的整群贈品)             ⇒ 零命中(它不是「混」)
④ -0 那一列                            ⇒ 不算贈品(由 isAbnormal 硬擋, 本片不接手)
⑤ 突變:拿掉偵測那一行 ⇒ ①必須紅
⑥ 負對照 餵一個當天挑的假供應商代號 zzz-supplier-20260826 ⇒ 零命中
```
⚠️ **本片【沒有】送達的驗收,因為本片沒有送達。** 不要補一格假的。

### 2.4 ~~🔴 實作落點(已查實,不是規劃)~~ ⇒ **`MF-5` 之後【整節作廢】**

🔴 **本節描述的是 TS 版。§2.0 已判走 SQL ⇒ 下面整段【不要照著做】。**
**保留原文當紀錄**(它查到的落點是真的,只是那條路不走了):

```
純函式放  scripts/rpm-transform.ts —— 那裡已經有同族的 liveVariantsOf / isFullyDelisted,
          而且 roundTwd 已經是 export(1651fe76 為了「能被真的跑到」加的)
          ⇒ export function mixedZeroGroup(liveVariants: SourceProductRow[]): {...} | null
呼叫端    scripts/rpm-import.ts:253 —— 那一行已經算好 liveVariants, 在 :246 的群迴圈裡
          ⇒ 逐群呼叫、收進一個陣列, 迴圈跑完印一次總表(不是每群印一行)
```
**為什麼不放 `rpm-delta.ts`**:它拿不到來源列(`grep -c "SourceProductRow" scripts/rpm-delta.ts` ⇒ **0 rc=1**)。
**為什麼吃 `liveVariants` 不吃原始 `variants`**:`liveVariantsOf`(`rpm-transform.ts`)會濾掉來源側墓碑
⇒ **不濾的話,一支【已停產的】0 元款會讓整群被誤報。**

**判準(三行,而每一行都有理由)**
```
零那一側  Object.is(roundTwd(v.price_retail), 0)     ← 正零。用 Object.is 才排得掉 -0
正那一側  (roundTwd(v.price_retail) ?? -1) > 0
命中      兩側【同時存在】才算「混」;全 0 不算、全正不算
```
🔴 **`-0` 為什麼不能算進來**:它是**負價取整來的異常**,不是贈品
(`rpm-delta.ts:74` 的 `isAbnormal` 用 `Object.is(newP, -0)` 硬擋它)⇒ **本片不接手它,也不該把它印成贈品。**

⚠️ **而我在 §2.2 原本寫過 `roundTwd(...) === 0`** —— 那個寫法會收進 `-0`,
**與本節的 `Object.is` 互相衝突。以本節為準,§2.2 那個字面作廢。**

---

## §3 片B · 跳過 0 元款 —— **v1 的做法會讓正式匯入整批 abort**

### 3.1 🔴 v1 錯在哪(codex R1,我逐條複驗成立)
```
錯① 用【原值 > 0】濾 ⇒ 擋不住 0.4
     實跑 node -e:0.4 -> 0 | 0.5 -> 1 | 0.6 -> 1
     ⇒ 群 [0.4, 5000] 挑到 0.4, roundTwd 後仍是 0 ⇒ 卡片還是 NT$ 0 ⇒ 核心需求直接失敗
錯② 只改 transform 不改 independentGroupPrice(rpm-delta.ts:280-289)
     它取的是【含 0 在內的 min】, 而 docstring 逐字「對齊 rpm-transform 的規則、但另寫一份」
     ⇒ 📌 它是【刻意寫成第二份】用來抓 transform 出錯的
     ⇒ 兩份分歧 ⇒ rpm-import.ts:514 判 source-mismatch ⇒ 寫入前 abort
     🔴🔴 **而「一定 abort」這句是【錯的】, codex R2 抓到, 我複驗成立**:
     `checkNewItemPrices` 只吃 `delta.newProductKeys` / `delta.newVariantKeys`(rpm-import.ts:516,522)
     ⇒ **既有商品新增一支 0 元變體時, 商品不在 newProductKeys ⇒ 那道閘根本不會被觸發**
     ⇒ **不是 abort, 是【靜默寫入】。而靜默比掛掉糟。**
     📌 我寫的「改一份 = 每天同步掛掉」**又是一句無數法的全稱句** —— 今晚第三次自己抓自己。
錯③ fallback 只考慮「全 0」⇒ 而實際還會遇到 NULL / 空字串 / NaN / 負數
     例:[null, 0] 的 basis 可能依 sku tie-break 落到 null ⇒ 推翻 v1「不是 null」那句
```

### 3.2 v2 的做法
```
判準改成  在【roundTwd 之後 > 0】的變體裡挑最低(不是原值 > 0)
fallback  沒有任何合法正價時 ⇒ 落回現行行為(全群排序取 sorted[0])
          ⚠️ 而「現行行為」對 NULL / NaN 的處理本來就存在, 本片不改它、也不假裝它被處理了
同批改    independentGroupPrice(rpm-delta.ts:280-289)必須套【同一條判準】
          🔴 兩份一定要一起改。改一份 = 每天的同步掛掉。
```

### 3.3 🔴 影響面比 v1 寫的多兩欄
片B 實際會同時改**三個價格載體**(`rpm-transform.ts:377-382`):
```
price_general · price_by_tier.general · price_by_tier.store(placeholder)
```
⇒ **v1 的驗收與 rollback 只寫了 `price_general`,少算兩欄。** 三個都要 read-back。
⚠️ codex 查過:目前 storefront/admin 讀路徑用 `price_general`,公開 view 排除 `price_by_tier`
⇒ **未找到立即顯示故障** —— 而那是「今天沒有下游」,不是「這欄不重要」。

### 3.3a 🔴 而影響面比「三個價格載體」還大兩格(codex R2,我複驗成立)

**① `basis` 不只決定價格,還決定【身分】**
`basis` 同時被拿去填 `title` / `metadata.name_en` / 來源列 ⇒ **改了挑法,商品標題可能跟著換。**
⇒ **處置**:實作要拆成 `contentBasis`(身分,維持現行挑法)與 `priceBasis`(價格,套新判準),
**不要用同一個 `basis` 兩用** —— 那正是這一格的成因。

**② `product-jsonld.ts` 會餵 Google 一個 `lowPrice: 0`,而片B【修不到它】**
```
apps/storefront/src/lib/product-jsonld.ts:92   const variantPrices = (product.variants ?? []).map((v) => v.price)
                                        :97   const lowPrice = Math.min(...variantPrices)
⇒ 它吃的是【變體層】的價, 而片B 只改【商品層】的基準價
⇒ 卡片改成 5000 之後, Google 那邊仍然是 AggregateOffer lowPrice = 0
負對照(當天靶)zzz-jsonld-20260826 ⇒ 0 rc=1
```
🔴 **而它是【對外可見、而我們看不到它壞】的那一種** —— 要去 Google 搜尋結果才看得到。

**⚠️ 它命中鐵則 12⑤ 嗎?我的判斷:【不命中,而它值得被當成同一級處理】**
```
12⑤ 的定義是「對外【不可回收】」(寄信 / 對外發布 / 法律頁)
JSON-LD ⇒ 對外發布 ✅ 而【可回收】—— 改掉之後 Google 會重新抓, 只是有時間差
⇒ 嚴格講不命中「不可回收」那一半
```
🔴 **而我不拿這個判斷去降級它**:它的危險不在不可回收,在**我們沒有任何訊號會告訴我們它壞了**。
📌 **而最該講清楚的一格**:這個 `lowPrice = 0` **是既有的,不是片B 造成的** ——
**片B 不會讓它變糟,片B 會讓「卡片 5000 / Google 0」這個矛盾【變得明顯】。**
⇒ **所以它不該擋住片B,而它應該同批進 backlog。**

### 3.4 這片的驗收
```
① 群內混 0 + 非 0 ⇒ price_general = 【roundTwd 後 > 0 的那些】裡的最低
② 群 [0.4, 5000]  ⇒ 5000(這一格直接守錯①)
③ 群內全 0        ⇒ 落回現行行為
④ independentGroupPrice 與 transform 對同一組輸入給【相同】結果(守錯②)
⑤ 突變:只改 transform 不改 independentGroupPrice ⇒ ④必須紅
⑥ 三個價格載體都 read-back, 不只 price_general
⑦ 一發實跑觸及改動路徑(鐵則 R5)
⑧ codex 對抗審查(12①+12③, 不降級)
```

### 3.5 🔴 一格**不是我能判的**,查完歸 Sean
變體層的 `price_general`(`rpm-transform.ts:418`)**不受本片影響** ⇒ 商品層與變體層會不一致
⇒ **卡片與商品頁會顯示不同的價。**
⇒ **我的工作是把兩種顯示的樣子寫出來(卡片印什麼 / 商品頁印什麼),然後歸 Sean。**
**不要在這一格自己挑一個「比較合理」的。**

---

## §4 Rollback

**片A**:拿掉偵測那一段。**零價格改動 ⇒ 零 DB 殘留。**
**片B** 🔴 **v1 寫錯了一半**:改回 code **不會**自動修復已寫進 DB 的值 —— 這句對,
**而要修的是【三個】價格載體,不只 `price_general`**;而且要等下一次同步覆蓋回來,
**同步未涵蓋、失敗、或那家被跳過時都會殘留。**

---

## §5 我最不放心的三格

**① §0 那支 migration 的 DB 層到底擋不擋 0 元 —— 我沒查。** 只查了「它不在帳上」。
**②** ~~`#934` 的關閉條件是我自己寫的,而我寫的時候沒有去查「叫」在這個 repo 是什麼意思。~~
 ⇒ 已於 `1333743b` 改寫。**而 §2.0b 定了觸發值之後,關閉條件要再對一次** —— 現在的片A 是 SQL,
 **它不會「叫」,它要人去跑。關閉條件不可以寫成「有一條會叫的路徑」。**
 ⇒ 一個關閉條件用了一個**它沒有定義的動詞**,而那個動詞會在**收工那天由趕著收工的人**填。
**③ `nit-8`:`Q18 = 丙` 這一板【查無 memory 落檔】** —— `c3` 掃了 memory:`Q18` ⇒ 7 檔無一是它;
 `基準價|贈品` ⇒ 10 檔全是 08-25 的板。而 08-26 的答案檔自己寫著「別的線的題各自落檔」。
 ⇒ 🔴 **拍板即落檔,這一板漏了。補寫 `project_0826-q18-*.md` 是收工前的必做項。**

**④** ⚠️ **v2 的 §3 現在比 §2 完整了,而那仍然不是把握度** ——
 那只代表 **codex 打了 §3 比較多下**。**被打得多的地方比較完整,不代表比較安全。**
