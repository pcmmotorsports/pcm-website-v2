# 出貨單:**樣張 vs 現行實作** 對照 + 10 條 codex findings 重判(C-213,2026-08-16)

> 🔴🔴 **本檔的存在理由**:我們花了一整輪去**問 Sean 四題、跑 codex 關卡1 審 10 條**,
> 而**那份設計樣張昨天就存在**,並且把四題全部答完了。
> Sean 逐字:「**你昨天才給我這個東西**」「**我們不是已經有設計範本出來了嗎? 不是照做就好?**」
>
> **樣張位置**(我自己開過,不是轉述):
> ```
> OD 專案   pcm-print-docs
> 檔案      shipping-picking-doc-a4.html（271KB，2026-08-15 建立，571 行）
> 同專案還有 brief.md / contract.md
> ```

---

## 🔴 §0 我為什麼沒開它 —— 可以做成機制的那個原因

**不是不知道它存在。** `2026-08-16-shipping-doc-information-architecture-plan.md:45` 逐字寫著:

> 「Sean 看的是 **OD 的**新設計樣張(`出貨單-新設計.png`)」

**而我讀的是它的下一行**(`:46-47`):

> 「⚠️ 該檔 2026-08-16 傍晚**已不在桌面、被移進 `~/.Trash/`** —— …
>  但**下一個人可能讀不到它** ⇒ §3.5 把逐列數字抄進本檔,**不要只留一個檔名指標。**」

⇒ **我把「那個 PNG 副本沒了」讀成「那份設計讀不到了」,於是停止找。**
而 `:45` 就寫著它是 **OD 的**樣張 —— **PNG 是匯出檔,OD 才是家。**

🔴 **這與今晚其他每一條是同一個形狀**:
**一句關於【某一個副本】的敘述,被讀成關於【那個東西本身】的敘述。**

**⇒ 可做成機制的修法**(不是「下次記得」):
```
① 任何 plan 寫「設計稿/樣張」時，【位置欄】必須寫【canonical 來源】而不是某個副本路徑。
   本例正確寫法：「OD 專案 pcm-print-docs / shipping-picking-doc-a4.html」，
   而不是「桌面的 出貨單-新設計.png」。
② 寫「該檔已不在/讀不到」時，同一句要回答：【那 canonical 那份呢？】
   答不出來就不准寫那句 —— 它會讓下一個人停止找。
③ 開工前的 design 檢查，判準從「有沒有 grep design-reference」
   改成「有沒有【開過 OD 的那個專案】」—— 本片的樣張根本不在 design-reference 裡。
```

---

## §1 樣張 vs 現行實作(逐項,附可重跑量法)

**量法**:`grep -rl '<字面>' apps/admin/src | wc -l`(正向對照:`本次出貨` ⇒ **3** 個檔)

| # | 樣張上有的 | 現行實作 | 命中數 |
|---|---|---|---|
| 1 | **抬頭七值**(公司名/電話/信箱/統編/地址/LINE/英文名) | ❌ 完全沒有 | `派達有限公司` ⇒ **0** |
| 2 | 標題「**出貨明細單**」+ 英文 + 用途說明 | ❌ 只有「出貨單」 | `出貨明細單` ⇒ **0** |
| 3 | **數量對帳**一行式:`訂購 29 ＝ 本次 23 ＋ 尚未 4 ＋ 已取消 2` | ❌ 沒有,而且**我刻意寫了不印跨區等式** | `數量對帳` ⇒ **0** |
| 4 | **金額四行**:小計 / 運費 / 折扣 / **訂單金額** | ❌ 沒有(金額片還沒做) | `訂單金額` ⇒ **0** |
| 5 | **退款附註**「取消品項之退款將另行處理…」 | ❌ 沒有 | `取消品項之退款` ⇒ **0** |
| 6 | **LINE QR + 客服說明**(缺件/損傷/規格不符怎麼辦) | ❌ 沒有 | `lin.ee` ⇒ **0** |
| 7 | **阻印狀態獨立版面**(「本單不得出貨」+ 四條照這樣做 + 六種情形) | 🟡 有擋,但只是一行 `<Alert>` | `本單不得出貨` ⇒ **0** |
| 8 | 三區分法 | ✅ 有(本次出貨/尚未出貨/訂單取消) | `本次出貨` ⇒ 3 |
| 9 | 取消品標「已全數取消 / 不提供勾選框」 | 🟡 做成了**獨立一區**而非明細列標記 | — |

---

## 🔴🔴 §2 樣張**沒有**的東西 —— 而我們做了 / 想做

| 我們有的 | 樣張上 | 判定 |
|---|---|---|
| **各區金額欄**(`Q-C13` 我還去問了字面) | **根本沒有這個東西** | 🔴 **整個問題不存在** |
| 「兩區合計 ≠ 商品小計」那句人話(`Q-C14`) | 用**一行算式**解決,而且比我擬的好 | 🔴 **我的文案作廢** |
| 「付款金額」(`Q-C12`) | 逐字排除:註解寫**「付款金額 / 應付金額 / 訂單總計」四個是被明文排除過的** | 🔴 **我推薦的正是被排除的** |
| 付款狀態 | 註解逐字「**明文裁定先不印,不是漏掉,不要補上去**」 | ✅ 我沒做,對 |

---

## 🔴🔴 §3 10 條 codex findings 重判

| # | codex 說 | 重判 |
|---|---|---|
| ① `total` 不是實收、標「付款金額」是假的 | ✅ **作廢** —— 樣張早就叫「訂單金額」,且明文排除「付款金額」。**codex 是對的,而答案早就在樣張裡。** |
| ② 折扣前/後並列會被讀成實付分攤 | ✅ **作廢** —— 樣張**沒有各區金額**,不存在並列 |
| ③ 「兩區合計 ≠ 商品小計」人話沒擬 | ✅ **作廢** —— 樣張用一行對帳式解決 |
| ④ 抬頭不做不足以放行 | ✅ **保留,且升級** —— 樣張有七值逐字,**照搬即可,不需要問任何人** |
| ⑤ 折扣 0 印 `−0` | 🟡 **保留** —— 樣張只畫了「金額待補」佔位,零折扣的呈現**樣張沒答** |
| ⑥ `quantitySummary === null` 時上無小計下有結算 | 🟡 **保留** —— 樣張沒有這個失敗態(它只有阻印那張) |
| ⑦ 雙查詢競態被金額放大 | ✅ **保留** —— 資料層,與版面無關 |
| ⑧ 箱品項截斷訊號 | ✅ **保留** —— 資料層真缺陷,**已修**(未 commit) |
| ⑨ 補印不保證重現原紙 | 🟡 **保留** —— 樣張沒答 |
| ⑩ 驗收未涵蓋六族 | ✅ **保留** —— 但要**照樣張重寫**,不是照我想像的版面 |

**⇒ 10 條裡 3 條作廢(①②③)、1 條升級(④)、6 條保留。**
🔴 **作廢的那三條,是 codex 花力氣審一個【不該存在的版面】審出來的。**

---

## §4 照樣張還缺什麼(給主視窗的清單)

```
🔴 必做（樣張有、實作沒有）
1  抬頭七值 —— 逐字搬，不得正規化（全形半形/+886/LTD 無句點）
2  標題「出貨明細單」+ 英文 + 用途說明
3  數量對帳一行式
   ⚠️ 但它在【第二箱】不成立 —— 見下方風險
4  金額四行（小計/運費/折扣/訂單金額）
5  退款附註
6  LINE QR + 客服說明
7  阻印狀態改成獨立版面（現在只有一行 Alert）

🟡 樣張沒答、仍要決定
8  零折扣怎麼呈現       9  quantitySummary=null 的失敗態     10  補印改價

✅ 資料層（與版面無關，可平行做）
11 箱品項截斷訊號（已修，未 commit）   12 雙查詢競態
```

## 🔴🔴 §5 一條我要主動提的風險:**樣張那條對帳式在第二箱不成立**

樣張逐字:`訂購 29 ＝ 本次出貨 23 ＋ 尚未出貨 4 ＋ 已取消 2`(23+4+2 = 29 ✅)

**而那是【第一箱】的數字** —— 它沒有「先前已出貨」那一項。
第二箱時正確的式子是:`訂購 = 先前已出 + 本次 + 尚未 + 已取消`(**四項**)。

📎 而 **Sean 2026-08-16 自己拍過這件事**:
> 「會算錯就不印,**只計算本次出貨**,然後第二或這第三批出到沒貨時尚未出貨那邊就空了就好」

⇒ **樣張(08-15)早於那次拍板(08-16)** ⇒ 兩者可能只是時序差,不是矛盾。
```
Q-C17：第二箱時那條對帳式怎麼辦？
A：加第四項「先前已出貨」，式子變四項（誠實，但比樣張多一個數字）
B：第二箱起【不印】對帳式（樣張的簡潔留給第一箱）
C：維持三項 —— 🔴 不推薦：第二箱會印出一條加不起來的等式，
   而「印一條加不起來的等式，比不印更糟」
```
**C 窗推薦 A。** 但**這一題我不自己拍** —— 它動的是樣張上的字面。
⚠️ **在它答之前,我不實作對帳式那一列**,其餘六項可以照搬。

---

## 🔴🔴 §6 · **揀貨單那張紙的阻印狀態盤點**(2026-08-17 夜,`#601` 落地後;**只盤不動手**)

`#601` 的整幅阻印版面**只做了出貨明細單**。本節盤揀貨單,結論是
**它有同一個洞,而且其中一種比出貨明細單那邊更危險** —— 但**修法不一樣,不可以照抄**。

### 6-a 揀貨單現在有幾種「不該拿去揀」的狀態

逐條開檔數的(`apps/admin/src/components/print/picking-doc.tsx`):

| # | 條件 | 落地前渲染什麼 | 品項表 | 列印鈕 | 🔴 **可與誰疊** |
|---|---|---|---|---|---|
| A | `cancelledAt !== null`(`:120`) | 一行 `<Alert>`「已取消…不要揀貨。品項清單已隱藏」 | **隱藏** | 隱藏 | **與誰都不疊** |
| B | `detail.itemsTruncated`(`:133`) | 一行 `<Alert>`「達到 200 **項**上限…不要拿這張去揀貨」+ 🔴 **表身標記帶(甲案,2026-08-18)** | 🔴 **照印** | 隱藏 | **可與 C、D 疊** |
| C | `detail.items.length === 0`(`:173`) | 一行 `<Alert>`「讀不到任何品項…不要拿這張去揀貨」 | 取代 | **照給** | **可與 B、D 疊** |
| D | `unknownCount > 0`(`:150`) | 一行 `<Alert>`(**這是提醒不是阻印**,紙仍然要用) | 照印 | 照給 | **可與 B、C 疊** |

> ### 🔴🔴 上面那張表**曾經讀起來像四態互斥,而 code 不是**(2026-08-17 夜 V 窗查證後補)
>
> **A 是唯一真的互斥的那一個**:`:128` 那個 `cancelledAt === null &&` **包住整個下半**
> ⇒ A 成立時 B / C / D 的 JSX 一個都不會 render。
> **而 B(`:133`)/ D(`:150`)/ C(`:173`)是三個【獨立的 if】,可以同時成立。**
>
> 🔴 **而 code 已經正確處理 `B ∧ D`**:`:155` 逐字
> 「**(而且清單沒載完,未載入的列裡可能還有)**」⇒ 那句話只在兩者同時成立時出現
> ⇒ **B ∧ D 會同時顯示兩個 `<Alert>`,那是刻意的、不是重複。**
>
> 📎 **為什麼要補這一欄**:原本那張表沒有它,而**一張四列的狀態表讀起來就是「四選一」**
> ⇒ 下一個人會照它寫出「只可能是其中一種」的修法或測資。**表的形狀本身在說謊。**

**列印鈕的條件**(`:117`,**已於 2026-08-17 改過**):
落地前逐字 `cancelledAt === null && !detail.itemsTruncated`
⇒ **C 那一種鈕還在**(`items.length === 0` 不在那個條件裡);現在補上 `&& detail.items.length > 0`。

> ### ⚠️ 而那條修法的價值**比我原本以為的低**(V 窗查證,我接受並記在這裡)
>
> **⌘P 繞得過鈕** —— `print-button.tsx:5-7` 自己承認、`onClick` 就是 `window.print()`
> ⇒ **鈕條件只控「鈕顯不顯示」,不控紙印不印。真守門只能在紙上。**
> ⇒ **C 態的紙已經守住了**(`<Alert>` **取代整個表** ⇒ 就算 ⌘P 印出來,那張紙上
> 本來就沒有東西可以照著揀)⇒ **補鈕條件對 C 態是「讓 UI 與紙一致」,不是新增一道守門。**
> 🔴 **我仍然做了它,理由要寫清楚**:那個不一致本身會誤導下一個讀 code 的人、
> 讓他以為鈕是守門。**價值低,但不是零 —— 而它【不是】B 態該學的東西。**

### 🔴🔴 6-a-2 · 這支檔的**守門天花板**(V 窗指出,寫成射程上限)

> **`itemsTruncated` 是【上游給的旗標】,`picking-doc` 只是忠實反映它。**
> ⇒ **若上游該 `true` 卻算成 `false`,紙會少列而 B 根本不觸發**
> ⇒ **一張看起來完整、零 Alert、可誤揀的紙。**
> 🔴 **洞在 `itemsTruncated` 的來源**(orders 投影 / `ORDER_ITEMS_EMBED_LIMIT`),
> **`picking-doc` 無責、也修不到。**
>
> ⇒ **本節所有修法的射程上限 = 「`itemsTruncated` 這個旗標本身可信」。**
> **那個前提不在這支檔裡,而它沒有被本節任何一條守門覆蓋。**

#### ✅ 2026-08-18 C 窗:**那個前提去追了,對揀貨單這張紙【成立】**

上面那段是**沒追過**的時候寫的。追法 = 問一句:
> **「去要 200 筆」的那個常數,跟「判有沒有滿 200」的那個常數,是不是同一個?**

```
要      packages/adapters/src/supabase/SupabaseOrderAdapter.ts:847
        .limit(ORDER_ITEMS_EMBED_LIMIT, { referencedTable: 'order_items' })
判      packages/adapters/src/supabase/mappers/order.ts:873
        itemsTruncated: row.order_items.length >= ORDER_ITEMS_EMBED_LIMIT
定義    同檔 :407   export const ORDER_ITEMS_EMBED_LIMIT = 200
數法    grep -rn 'ORDER_ITEMS_EMBED_LIMIT' --include='*.ts' packages apps | grep -v '\.test\.'
        ⇒ 16 命中，而其中【是 code 的只有 4 行】，其餘 12 行是註解/docstring：
             mappers/order.ts:407          定義（= 200）
             SupabaseOrderAdapter.ts:41    import
             SupabaseOrderAdapter.ts:847   拿它當 .limit()
             mappers/order.ts:873          拿它當判定門檻
        ⇒ 這條路上【沒有第二個字面】，要幾筆與判幾筆讀的是同一個 const
```
⚠️ **「4 行」的分法**:上面那支 grep 再過 `| grep -v '//'` 並濾掉行首是 `*` 的 docstring 行。
**這個濾法是機械的、可能濾錯**(例如同一行既有 code 又有尾註)——
我**逐行看過那 4 行的內容**確認它們是 code;**那 12 行我沒有逐行開檔**,只確認它們被濾掉。
⇒ **要幾筆與判幾筆綁在同一個常數上** ⇒ 「拿回 200 筆」與「判定 `>= 200`」不會分家
⇒ 🔴 **對揀貨單這張紙,那個旗標是可信的。射程上限解除。**

#### ⚠️ 而它**換成了一個更窄的前提**,不是消失了

`mappers/order.ts:404-406` 自己逐字寫著剩下的那個洞:
> 「**若專案 `max-rows` 日後被設到低於本值,截斷會發生在那個更低的數字上而本判定看不見**」

也就是說:PostgREST 先夾到 150 筆 ⇒ 回來 150 列 ⇒ `150 >= 200` 為 **false**
⇒ **旗標說沒事、而紙真的少列** —— 正是 6-a-2 原本擔心的那個世界。

```
🔴 現在的前提 = 「專案 max-rows > 200」
   狀態：✅ 【量到的】max-rows = 2000 ⇒ 2000 > 200 ⇒ 本病今天不成立。

   探法（2026-08-18，V 窗親手打的 —— 不是 dashboard、不是轉述）：
     storefront anon key 打正式站 REST：products?select=id&limit=5000
     ⇒ HTTP 206、content-range: 0-1999/19777、實回 2000 列
     🔴 分母 19,777 > 2000 ⇒ 量到的是【天花板本人】，不是「資料只有這麼多」
   ⚠️ 效度限定（跟著這個數字走，不要單獨把 2000 複製出去）：
     正式站 / storefront anon key / 2026-08-18 那一刻。
     換專案、換 key、或之後被人改掉，都不在這次量測的射程內。
   📎 這一格 2026-08-18 之前寫的是「未自驗（Sean 口頭 1000、主視窗轉述 2000）」——
      留著這句是為了記住：那時候有兩個轉述來的數字互相打架，而【打架本身】才是去量的理由。

🔴 而它【沒有守門】：max-rows 是伺服器端設定，repo 內沒有字面可掃、測試也看不到
   ⇒ 有人把它調低的那一天，這裡是靜默的。
   ⇒ 已發 backlog `#629`（git ref CAS 保留；條目寫了病的形狀、痛在哪、兩個治本候選）。
   治本寫在同一段註解裡：count: 'exact'（改共用讀取路徑 ⇒ 高風險片，不在本節範圍）。
```
📎 **這一則的形狀值得留**:原本的天花板寫成「前提不可信」,而追下去發現
**前提成立、只是它自己還站在另一個前提上**。
⇒ **「射程上限」不會消失,只會往上搬一層。** 每搬一層都要問一次「這一層有沒有守門」。

### 6-b 🔴 B 那一種比出貨明細單的任何一面都危險

    A / C ：品項表【不在紙上】⇒ 印出來明顯不能用
    B     ：品項表【在紙上】，而它【少了幾列】
    ⇒ 印出來是一張【看起來完整、可以照著揀】的紙
    ⇒ 揀貨的人一列一列勾完 ⇒ 紙上看起來就是揀完了
    ⇒ 而少的那一件【不會有任何症狀】，到客人收到貨才發現

🔴 **而 `#601` 那一幅【不能直接套到 B】**:那一幅的定位是「**本頁不含品項明細**」
(逐字印在紙上),而 B **必須**印品項表 —— 揀貨的人要看得到已載入的那幾列。
⇒ **B 的修法是「整幅警告 **+** 品項表」,不是「整幅警告 **取代** 品項表」。**
**照抄 `#601` 會把 B 從「少幾列」變成「一列都沒有」,那是另一種壞。**

### 6-c 各條的判斷(給主視窗排序用,**本節不動手**)

| # | 建議 | 為什麼 |
|---|---|---|
| A | **照 `#601` 那一幅做**,語意一致(不含品項明細) | 同型,零新設計 |
| C | **同 A**;順手把列印鈕的條件補上 `items.length === 0` | 🔴 **鈕的條件與紙的條件不一致 = 守門裝在一半的路上**;而它只是不再遞刀、擋不住 ⌘P |
| B | 🔴 **要新設計,不可照抄** | 見 6-b。要「整幅警告 + 表」的版面,而**樣張沒有這一款** |
| D | **不動** | 它是提醒不是阻印,紙要用 |

### 6-d ⚠️ 這一節**沒有**做的事

- **沒有動任何 code。**(`git status --porcelain` 落筆當下只有本檔與 traps-inbox。)
- **沒有印出來看過** —— 揀貨單目前**沒有**量測 fixture
  (`page-measure.test.tsx` 那三種都是出貨明細單:1 品項 / 12 品項 / 阻印)。
  ⇒ 要驗揀貨單的版面,**先得多一種 fixture**;那是動手那一片的第一步。
- **沒有查樣張有沒有揀貨單專屬的阻印款** —— 樣張 B 的標題是「出貨明細單」,
  而那份樣張的前提是「揀貨單與出貨單已合併成一張」,**那個世界還不存在**
  (同一份 spec 早先那條:`shipping-doc.tsx` 刻意還沒印樣張的「同一張紙兩用」兩行)。
  ⇒ **B 的新設計要不要請 OD 出,由主視窗判**,我不自己決定。
