# 合併 plan · 客戶篩選(性別 / 生日)—— **三份寫法合成一份**

> 產出者:施工窗 `pcm-website-v2-1d`(線 4),2026-08-26。派工 = 主視窗 `pcm-website-v2-96`。
> **零 code。等批(鐵則 8 + 鐵則 12③⑤)。**
>
> 🔴 **為什麼有這一份**:同一件事在 repo 裡有【三份】寫法,而它們互相不知道對方存在。
> 主視窗逐字:「**合併完再動手,不然會做出第三份**」。

# 🔴🔴🔴 上線順序 —— **讀這一段之前不要推 `f8f62cab`**

**片A 已經寫完,而它的上線【是三段不是兩段】,而中間那一段沒有人做過:**

```
① schema migration 進版控        ✅ 已上   13db25ab
② 🔴 那支 migration【真的 apply 到正式庫】  ⛔ 沒有人做過 —— 而它【只有 Sean 做得到】
③ 接線程式碼上線                  ⏳ f8f62cab 未推
```

## 🔴 **`f8f62cab` 推之前,`20260826140000` 必須先 apply。**

**為什麼**:接線那半會對 `admin_customer_list_v` 下 `birth_month` / `birthday` 的 filter。
**那兩欄現在只存在於版控裡,不存在於正式庫。**
⇒ ③ 早於 ② ⇒ **後台去讀兩個不存在的欄**,而它會在**部署完成之後**才炸 ——
   不是 build 失敗、不是測試紅,是**員工打開客戶頁的那一刻**。

⚠️ **而今天那一次順序是對的,那是【運氣不是安排】**:
`13db25ab` 被推上去而 `f8f62cab` 剛好還沒 —— **沒有任何東西在守這個順序。**
📌 **`git push` 不會問你「這顆的 schema apply 了嗎」。**

---

## §0 被合併的三份(全部保留,不刪)

| 檔 | 作者 / 日期 | 狀態 |
|---|---|---|
| [2026-08-16-customer-filters-four-fields-plan.md](2026-08-16-customer-filters-four-fields-plan.md) | **A 窗** 08-16 | 等批。四欄:年齡 / 性別 / 居住地區 / 登入方式 |
| [2026-08-26-customer-gender-birthday-spec.md](2026-08-26-customer-gender-birthday-spec.md) | 線4 08-26 | 等批。性別 + 生日 + 後台篩選 |
| [2026-08-14-25-customer-profile-edit-plan.md](2026-08-14-25-customer-profile-edit-plan.md) | **C 窗** 08-14 | 等批。`C1` = 把姓名/電話/生日接上,**8 支檔** |

🔴 **本檔【不取代】它們。** 它只負責一件事:**把互相矛盾的地方指出來並裁掉**。
各檔的完整推導留在原處,**本檔不重抄**。

---

# §1 🔴🔴 合併撈到的第一件事:**隱私政策沒有列「性別」,也沒有列「生日」**

**這是本次合併最重的一格,而三份 plan 沒有一份提到它。**

```
apps/storefront/src/data/legal-content.ts:201-203 逐字
  heading: '三、蒐集之個人資料類別',
  '姓名、聯絡電話、電子郵件、收件地址、訂單與交易紀錄、站內搜尋紀錄、會員帳號資訊。'

量法  grep -c '性別' apps/storefront/src/data/legal-content.ts ⇒ 0
      grep -c '生日' 同檔                                      ⇒ 0
負對照 'zzz性別20260826'                                       ⇒ 0
```

## 1-a 兩個結論,而它們不一樣 —— 不要合起來讀

```
① 性別  = 【本片要新增的蒐集】而政策沒列 ⇒ 🔴 這是本片造成的, 要一起處理
② 生日  = 🔴🔴 【已經在收了】而政策也沒列 ⇒ **這是一個【既有】缺口, 不是本片引入的**
   證據:customers.birthday 欄 08-05 就存在(init migration :19)
        design 會員中心本來就有生日輸入格(AccountPages.jsx:669)
   ⇒ **不得寫成「本片發現並修好了一個 bug」** —— 本片只是【撞到】它。
   ⇒ 而它也【不是本片可以自己修的】:改法律頁 = 鐵則 12⑤ 對外不可回收。
```

## 1-b 🔴 而改那份文字有一道機制在守,不是靠自律

```
apps/storefront/src/data/legal-content-hash.test.ts 檔頭逐字:
  「legal_terms_versions 那一列說『版本 X 的內容雜湊是 H』, 客人簽的是 X,
    而客人實際讀到的字必須真的雜湊出 H, 且 migration 裡寫的也必須是同一個 H。
    沒有這些測試, 以後有人順手改一句條款, version 不動 → 稽核時
   【客人同意的內容】查無對證, 而三綠 / 型別 / lint 全部都是綠的(改字串不會讓任何東西紅)」
```
⇒ **改一個字 ⇒ 那支測試會紅 ⇒ 要同時發新版本號 + 新雜湊 + migration。**
⇒ 🔴 **所以「順手在政策裡加兩個詞」這件事【不存在】。它是一片有體積的工作。**
⇒ **本片不做它。列成前置,交給主視窗排。**

## 1-c ✅ **已答:Sean 2026-08-26「不用改隱私正政策,直接做性別」**(逐字)
⇒ **缺口已知並保留,不是已合規。** 片C 轉 backlog。**全文與紅線見 §6-c(一)。**
⚠️ 下面這張原始選項表保留備查,**已不是待答題**:

## ~~1-c 這一格要 Sean 拍的題~~(已答,留存)
```
Q-隱私  性別要不要收, 而政策要不要一起改?
  甲 政策先改(加「性別、生日」進蒐集類別 + 發新版本號)⇒ 然後才開始收性別
  乙 性別先不收, 這一片只做【生日篩選】(生日已經在收了)⇒ 政策那格另外排
  丙 兩件並行, 而性別欄先建、註冊表單先不問
  👉 這一題我【不推薦】—— 它是法規題不是工程題, 而我沒有依據。
     ⚠️ 而【不論選哪個】, 生日那個既有缺口都還在, 它不會因為選了乙就消失。
✅ **他選的不在上面三個**:他直接說不改政策而性別照做 ⇒ 缺口保留、片C 轉 backlog。
```

---

# §2 三份互相矛盾的地方 —— 逐條裁掉

## 2-a 🔴 **年齡篩選是【區間】還是【分組】—— 兩份寫的不一樣**
```
A 窗 08-16 §2甲   「年齡分組要 Sean 定(20 以下 / 20-29 / 30-39 …?)」 ⇒ 【分組 bucket】
線4  08-26 §1-4   「幾歲到幾歲」                                        ⇒ 【自由區間】
```
✅ **裁決:照【自由區間】,因為那是 Sean 自己的字。**
`~/pcm-mailbox/DECISIONS-Sean-2-20260826.md` 的 `E` 選項字面逐字是「**乙 幾歲到幾歲**」,他答**丙**(兩個都要)。
⇒ 🔴 **A 窗那題「分組怎麼切」因此【自動答掉了】—— 不用再問他。**
⇒ ⚠️ **而這不是我判 A 窗錯** —— A 窗 08-16 問那題時,Sean 還沒答過 `E`。**它當時是對的。**

## 2-b 🔴🔴 **birthday 要不要進讀模型 —— 兩份直接衝突,而一份會踩到既有決定**
```
A 窗 08-16 §2甲 逐字「① 投影進讀模型(AdminCustomer 加 birthday 或直接加 ageBucket)」
線4  08-26 §1-4 「篩得到、不顯示」——【不動白名單】
```
✅ **裁決:照【篩得到、不顯示】。而理由不是我比較好,是【有一條既有決定在那裡】:**
```
apps/admin/src/lib/customers/customer-repository.ts:15-17 逐字
  「列表走 ADMIN_CUSTOMER_LIST_SELECT 具名白名單(不帶 wallet/birthday);
    明細-a 起 findById 含 …/birthday(… PII 只在明細頁、登入閘後)」
```
⇒ 🔴 **A 窗那條會把 `birthday` 放進【列表】的讀模型 ⇒ 那正好是那條決定禁止的事。**
⇒ ⚠️ **而 A 窗看不到這條決定不奇怪** —— 它寫在 `customer-repository.ts` 的**檔頭註解**裡,
   **不在任何一份 plan 或 docs 裡**。⇒ 📌 **又一次「答案存在,而沒有一條路通到它」。**
⇒ ✅ 做法:PostgREST **可以對 view 上有的欄下 filter 而不放進 `select`** ⇒ 白名單一個字不動。

## 2-c ✅ **「性別不要跟其他欄綁同一片」—— A 窗說對了,我採納**
A 窗 §2丙 逐字:
> 🔴🔴 **鐵則 9 內容分級**:性別是**個資**。**收集個資要有目的與告知**
> ⇒ 隱私政策(`#291`…)可能要動。⇒ **本欄不建議與其他三欄綁在同一片。**

⇒ **§1 證實了它的擔心是對的,而且比它想的更硬(有 hash 守門)。**
⇒ 🔴 **所以我原本「性別 + 生日一片」的切法要改** —— 見 §3。

## 2-d ⏭ **兩欄不在本片,原封留在 A 窗那份**
```
居住地區  A 窗建議暫緩(自由文字解析是本 repo 明文反對的形狀)⇒ 本片不碰
登入方式  A 窗找到一條零 migration 的路(email 網域判 LINE)⇒ 本片不碰
          🔴 而它有一道【未驗】的前置:service_role 讀不讀得到 auth.identities
             A 窗逐字「零命中只證明現在沒人讀, 不證明讀不到」—— 那道驗證仍然欠著
```

## 2-e ⚠️ **C 窗那份(把生日接上)是【上游】不是競品**
`2026-08-14-25-customer-profile-edit-plan.md:63` 逐字「`C1` | 姓名/電話/生日 | **8**(檔)| 標準片 | **鐵則 8 命中**」
⇒ **它做的是「客人/員工能填生日」,我做的是「填了之後能篩」。**
⇒ 🔴 **順序是硬的:沒有人填,篩選器篩不到東西。**
⇒ ⚠️ 而同檔 `:81` 記了一格我沒想到的:
> **C1 生日留空存檔 → DB `birthday IS NULL` 且不報錯**…「**知道 ≠ 有東西擋**」

⇒ **那就是「沒填生日的人」的來源之一。** 接下 §2-f。

## 2-f 🔴 **仍然沒有人答的一題(三份都指向它)**
A 窗 §2甲 逐字:
> ②**沒填生日的人**歸哪一堆(「未填」要當成一個可篩的值,**不能靜靜地從結果裡消失**
> —— 那是本 repo 反覆記過的「不知道 ≠ 是 0」)

⇒ **`E=丙` 讓它更嚴重**:那些人在「這個月生日」與「幾歲到幾歲」**兩種篩法下都會消失**。
⇒ 而他們**結構上必然存在**(Google / LINE 註冊那兩條路根本沒看過表單,見 gender-birthday-spec §0)。
```
Q-未填  沒填生日的客人, 在篩選結果裡要怎麼處理?
  甲 當成一個可以篩的值(多一個「未填」選項)          ← 推薦
  乙 篩生日的時候他們就是不出現
  👉 推薦甲的理由不是「比較完整」, 是【乙 在畫面上看不出來少了誰】——
     而少掉的那批不是隨機的, 是【用 Google / LINE 註冊的那批】。
```

---

# §3 🔴 合併之後的切法(**與我原本的不一樣,理由在 §2-c**)

```
片 A  生日篩選           零 migration on customers(birthday 欄已存在)
                         要動:view 加 2 欄 + 2 支索引 + URL 參數 + UI
                         ⇒ 🔴 仍命中鐵則 12③(view 加欄 = migration)
                         ⇒ 前置:Q-未填 · C 窗 C1(有人填才篩得到)
片 B  性別欄 + 註冊表單    ✅ **已解鎖**(Sean「不用改隱私正政策,直接做性別」)⇒ 可開工
                         A 窗 §2丙 的判斷 + §1 的 hash 守門, 兩個理由都指向「單獨批」
片 C  隱私政策改版        🔴 鐵則 12⑤ 對外不可回收 + hash 守門 + 新版本號 + migration
                         ⇒ **不是本線的片。列給主視窗排。**
                         ⚠️ 而它同時要處理【生日那個既有缺口】, 不只性別
```
⇒ 🔴 **原本「性別 + 生日一片」作廢。改成三片,而只有片 A 是這一輪做得動的。**

---

# §4 鐵則 12 判定(主視窗要我自己判,理由寫在這裡)

```
✅ 命中 ③ DB 結構
   片 A:ADMIN_CUSTOMER_LIST_VIEW 加 birthday / birth_month 兩欄 = 新 migration
   片 B:customers 加 gender 欄 + enum = 新 migration
   ⇒ 兩片都要 codex 對抗審查, 由做那片的窗自己跑
✅ 命中 ⑤ 對外不可回收
   片 C 改法律頁 ⇒ 命中。而它還有 hash 守門 + 同意版本綁定 ⇒ 體積遠大於「改兩個詞」
❌ 不命中 ①錢 ②權限 ④平台設定 ⑥共用元件
   篩選走既有 admin-data-table / select-filter, 不改它們的 props 或邏輯(純使用)
   ⚠️ ~~而【若實作時發現要改 select-filter 的行為】⇒ 立刻升級成命中⑥~~
   🔴 **這句我自己更正**(2026-08-26,查證見 §6-a):`components/shared/` **不是** `packages/ui`,
   而 `apps/admin` import `@pcm/ui` 的檔數**量到是 0** ⇒ **片A 結構上碰不到⑥。**
   ⇒ 🔴 **但那個自我約束【不撤】,只換理由**:要改 `select-filter` 的行為 ⇒ **停下回報主視窗**
   ⇒ ⚠️ **主視窗 2026-08-26 最後一則仍寫「升⑥」** —— 那是照我上一版的錯句轉的,**以本節為準**。
```
⇒ **結論:三片全部命中鐵則 12,一片都不能省對抗審查。**
⚠️ **而 `code-reviewer` 我不憑轉述叫** —— 主視窗自己更正過那則授權轉述,
   **要叫之前我會問 Sean 本人**(這是他對我下的限制,peer 解除不了)。

---

# §5 🔴 片A 的 rollback(鐵則 8 要求,上一版漏了 —— 量法 `grep -c rollback` ⇒ 0)

```
片A 動三樣, 而它們的可逆程度【不一樣】, 不要當成一件事:

① view 加兩欄(birthday / birth_month)  🟢 可逆, 而【回滾比想像麻煩】
   Postgres 的 CREATE OR REPLACE VIEW **不允許刪既有欄或改欄序**
   ⇒ 回滾要 DROP VIEW + 重建 ⇒ 🔴 而 DROP VIEW 會【連它的 GRANT 一起丟掉】
   ⇒ 回滾腳本必須把 GRANT 一起寫回去, 否則後台列表整個讀不到,
     **而錯誤訊息會指向權限, 不是指向欄位** ⇒ 修的人會找錯方向
② 兩支索引                              🟢 可逆   DROP INDEX;零資料風險
③ URL 參數 + UI                         🟢 可逆   純前端, revert commit 即可

🔴 而【真正不可逆的那一格不在片A 裡】—— 是片B 的 gender enum:
   Postgres 不支援移除 enum 值 ⇒ **加值便宜、移除不可能**
   ⇒ 值域(`male`/`female`/`undisclosed`)要一次想清楚, 而【加第四個值】隨時可以
   ⇒ 📌 **這是片A/片B 分開的第三個理由:片A 全可逆, 片B 有一格永久。**
```
⚠️ **上面三條是照 Postgres 語義寫的,我沒有實際跑過任何一條回滾。**

---

# §6 🔴 `select-filter` 查證 —— 主視窗要我實作前確認。查了,**而它自己寫錯了**

```
apps/admin/src/components/shared/select-filter.tsx:1 逐字
  「後台列表通用「帶標籤的下拉篩選」(**訂單 / 客戶等 filter bar 共用**)」

實際用它的檔 = 【1 支】= customer-filter-bar.tsx(:3 import · :38 使用)
量法 grep -rn 'SelectFilter' apps/admin/src --include='*.tsx' --include='*.ts', 排除定義檔本身
負對照 'SelectZzzFilter' ⇒ 0
🔴 訂單【沒有】用它 —— 訂單走自己的 order-filter-controls.tsx
```
⇒ 📌 **檔頭那句「訂單 / 客戶等共用」不成立。** 這是 memory 記過的「**檔會對自己說謊**」。
⇒ ✅ **而這對片A 是好消息**:改它**只影響客戶那一頁**。風險比我原本估的低。

## 6-a 鐵則 12⑥ 的判定 —— 現在是量到的,不是推的
```
鐵則 12⑥ 逐字管的是「共用元件 **packages/ui** 行為改動」
量法 grep -rl '@pcm/ui' apps/admin/src --include='*.tsx'(排除 test)⇒ **0 支**
     packages/ui/src 底下只有 filters/cascadeFilterReducer.ts 與 index.ts
⇒ 🔴 apps/admin 根本沒有 import packages/ui ⇒ **片A 在結構上碰不到⑥。**
```

## 6-b 🔴 查證撈到一件會改變片A 形狀的事
```
SelectFilter 是一顆 native <select>(36 行, form GET, 置頂「全部」= 空值)
「這個月生日」  = 12 個月 + 全部     ⇒ ✅ **純使用, 零改動**
「幾歲到幾歲」  = 🔴 **兩個數字輸入框, 不是下拉** ⇒ **SelectFilter 做不到**
```
⇒ **片A 要一支【新控制項】,而不是【改 SelectFilter】。**
⇒ ✅ **不用從零寫** —— 訂單那邊已經有同形狀的:`orders/order-filter-controls.tsx`
   (`:69 dateFrom` · `:109 [DATE_FROM_PARAM]` · `:257 applyCustomDate({dateFrom,dateTo})`)
   ⇒ 「幾歲到幾歲」在資料層**就是一個 birthday 的日期區間** ⇒ 同一個形狀
   ⇒ 🔴 而它是**訂單那一域的檔** ⇒ **抄形狀、不 import**
     (import 會製造 customers → orders 的相依,那比重寫貴)
⚠️ **我只讀了那三行,沒讀完那支檔(250+ 行)** ⇒ 「同形狀」是照三行判的,抄之前要開全文。

---

# §6-c ✅ Sean 2026-08-26 三格答案(逐字,不重打)

## (一)隱私政策 ⇒ **不改。性別直接做。**
Sean 自由打的字(**不是選項字面**):
> **不用改隱私正政策,直接做性別**

🔴 **主視窗已把法規缺口明白告知他,他知情後仍選這個 ⇒ 他的商業判斷,不再回頭質疑。**
🔴🔴 **落檔紅線**:
```
✅ 只能寫「Sean 2026-08-26 拍板不改政策, 缺口已知並保留」
❌ 不得寫「已合規」/「政策已涵蓋」/「本片已處理」
```
⇒ **片C 不作廢,改成 backlog**,而條目要寫 **性別 + 生日兩個都缺**(生日那個是既有缺口,見 §1-a)。

## (二)鐵則 8 ⇒ **批了**(`A: 甲 = 批, 開`)⇒ **片A 開工。**

## (三)沒填生日的人 ⇒ **甲 = 篩選結果旁邊寫「另有 N 人沒填生日」**
Sean 貼回的**選項字面**(⚠️ **那串措辭的作者是主視窗,他貢獻的是「甲」**)。
⇒ **要顯示那個 N,不是靜靜濾掉。**

### 🔴 (三)-a 那個 `N` 的分母 —— 主視窗要我現在定,定在這裡
```
兩個都合理, 而數字不同:
  (a) 分母 = 【符合其他篩選條件】的人裡, 沒填生日的
  (b) 分母 = 【全部客戶】裡, 沒填生日的
✅ 定 (a)。理由:那句話就印在【這一批結果】旁邊
   ⇒ 員工篩「經銷商 + 30-40 歲」時看到「另有 N 人沒填生日」,
     他會讀成「**經銷商裡**還有 N 個我沒看到」—— 而 (b) 給的是一個跟畫面無關的數字。
🔴 **而這是我判的, 不是 Sean 拍的** ⇒ 列進 §7 待確認, 一句話就能推翻。
⚠️ 實作註記:(a) 的查詢是【同一組 where 條件, 只把生日那一軸換成 IS NULL】
   ⇒ 🔴 不得另寫一條 where —— 兩條會漂, 而漂了之後那個 N 仍然印得出一個合理的數字。
```

---

# §7 這份合併沒有做的(誠實邊界)

```
· 🔴 我【沒有】重量 A 窗那三個數字(客戶 11 / 有填生日 2 / 真實 0)——
  沒有正式庫權限。而 Sean 已在【知道可能是 0】的情況下拍「現在做」(q4=甲)
  ⇒ 那三個數字**不再是成本問題**, 只回答「上線後多久才有用」
· 🔴 A 窗那道「service_role 讀不讀得到 auth.identities」的驗證【仍然欠著】——
  它屬於「登入方式」那欄, 不在本片, 而**它沒有因為本次合併而被解決**
· 我【沒有】開 C 窗那份的全文, 只讀了 :24 / :63 / :76 / :81 四段
  ⇒ 🔴 所以「C1 是上游不是競品」這個判斷, 是照那四段做的
· 我沒有查隱私政策改版要走什麼流程 —— 只查到它有 hash 守門與同意版本綁定
· 我沒有估任何一片的工時
· 🔴 §5 那三條 rollback 是照 Postgres 語義寫的, **我沒有實際跑過任何一條**
· 🔴 §6-b「訂單那支的形狀可以抄」是照 `order-filter-controls.tsx` 的 **三行** 判的,
  **我沒讀完那支檔**(250+ 行)⇒ 抄之前要開全文
· 🔴 §6-c(三)-a 的分母選 (a), **是我判的不是 Sean 拍的** —— 一句話就能推翻
· ⚠️ **年齡區間會進 URL** ⇒ 進 access log / Referer。主視窗 2026-08-26 判**可接受**,
  理由:那是【員工的查詢條件】不是客人的個資 —— access log 裡出現「25-35」識別不到任何人。
  🔴 **而替代方案留在這裡, 不因為這次判可接受就刪掉**:
    搜尋詞那條路**已經**走 httpOnly cookie + PRG(`customers/page.tsx` 的 `#525` 那段),
    理由是**搜尋詞是姓名/Email/電話**。⇒ **下一個人面對的可能是不同的欄位。**
    判別句:**進 URL 的那個值, 單獨看得出是誰嗎?** 看得出 ⇒ 走 cookie, 不要走 query string。
· 🔴 **報三綠要連【量測時點】一起報**(2026-08-26 立):同一發 `pnpm typecheck` 今天量到
  **5 個錯 / 1 個錯 / 2 個錯**三種結果, 而三次都沒錯 —— 八個窗共用一棵樹, typecheck 是**全樹**的。
  ⇒ **沒有時點的 rc, 指的是誰的世界不確定。**
· 🔴 本檔把三份的【矛盾】挑出來裁了, 而【三份各自漏掉的東西】沒有人查 ——
  「有沒有第四份」我沒查, 只查了 grep -rln '生日' docs/specs/*.md ⇒ 7 支(分母 343)
```

📎 三份原檔見 §0 · 隱私政策 `apps/storefront/src/data/legal-content.ts:201-203` ·
hash 守門 `legal-content-hash.test.ts` · PII 決定 `apps/admin/src/lib/customers/customer-repository.ts:15-17`
