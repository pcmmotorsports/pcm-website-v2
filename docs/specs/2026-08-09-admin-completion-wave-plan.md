# 後台完整化總 plan(2026-08-09 晚,Sean Q1=A/Q2=B 拍板產物)

> **目標(Sean 逐字方向)**:「先把整個訂單後台功能先一口氣建立完整我再測試」。完成定義=完成地圖 27 項裡**員工日常閉環全部按得到**(認證 26/稽核 27/月報 30/成本 31-33 另線或已延後,不擋總測試);完成時交付「功能地圖+日常操作流程」給 Sean 一輪總測試+UX 評估,期間**不再請 Sean 逐片測試**。
> **刻意不做的事**:不在本檔預先拆片(7 月教訓:規劃準確度隨距離衰減)。本檔只管**塊的順序與閘**;各塊開工時才拆片,拆片照各線既有 SOP。
> 塊狀態的真權威=`docs/specs/2026-08-01-admin-completion-map.md`(開塊前刷新該塊那幾行)。

## §1 已拍板的閘(全部已落檔,出處=memory `project_m4b-order-admin-complete-then-test`)

- Q1=A 範圍如上;Q2=**B 商品線完整版**(編輯+改價上下架+換圖+新增商品+媒體庫+Excel 匯入入後台);Q3=A。
- 建單六題(master-plan-v2 §8.6 已銷案):一口價含稅手填/**逐品項折扣+稅額分列**/必掛會員/建單一律未付款/不進庫存採購鏈(#352 補記到貨)/經銷價零接觸。
- Q10=A:一般 migration apply 主視窗照守門自行執行;**動錢或大量/不可逆寫入照舊停問 Sean**。
- ✅ 最後三包已拍(08-09 晚,至此**零開放決策**):Q11=A 通知矩陣照 07-26 草案全收;Q12=A 混合收款各自原路退;Q13=Sean 自案 **退款額=max(0, 已收未退−取消後剩餘應收)**、不退時已收轉部分款;Q14=A 搜尋加日期範圍、**未選預設近半年**(⇒ RPC 日期參數隨 347-3 做,不塞 R3 修復輪)。逐字與公式=memory `project_m4b-order-admin-complete-then-test` 與 master-plan §8.6 銷案行。

## §2 四窗流水(塊序;→=依賴,∥=可並行)

| 窗 | 現行 | 之後(順序) |
|---|---|---|
| **D**(admin UI 主線) | 347-1 R3 退修中 | →347-2 接線(**硬要求 POST**+PostgREST smoke=主視窗)→#350 工作區分割視窗殼(側欄收窄+訂單/客人/商品卡三面板;含 #348 客人 360 讀面、Q1=A 商品卡)→347-3 搜尋 UI(落進殼;含 Q14 日期範圍下拉+RPC 加日期參數、未選預設近半年)→#351 出貨文案/口徑/空箱可見 |
| **E**(訂單流程線) | A13b-1 取消表單 | →A13b-2 逐品項勾選→取消接線(order-detail.tsx,與 D 殼合併序=主視窗排)→#352 現貨補記到貨→手動建單塊(六題已解閘;含 A12a/b 批次標進度順路收) |
| **B**(DB/harness 線) | 四支掃格 | →收款帳本塊 DB(匯款確認 #15→今日對帳 #16 讀面)→改單模型塊(#13)→退貨收回塊(#18)→列印出貨單資料面(#10) |
| **P**(金流線) | L4 | →L5a 零等待→L5b 補償退款(單獨對抗審查)→L5c→退款線收尾(#17:worker/對帳+flag 開;需 §8.6 退款兩題拍板) |

**跨窗匯流點(主視窗管)**:①D 的殼落地後,E 的取消畫面與 B 的收款/對帳讀面都掛進同一個右側面板系統 ②通知塊(#11 出貨信/催款信+內部通知 U4)等通知矩陣拍板後派給先騰出的窗 ③**商品線塊(Q2=B,零規劃)**=四線裡最大的新塊,D 完成 #351 後由 D 開規劃片(media 上傳/R2/匯入 CLI 包裝都在它名下)④營運週邊(#1 今日待辦首頁)排最後——它是其他塊的匯總讀面。

## §3 全程有效的紀律

- 高風險片(鐵則 12 六類)codex/跨模型審查不降級;**R1/R2 有真 finding 則 R3 換模型換角度**(本波已兩次證明:42501 判準擴寬、347-1 五條)。
- migration apply=主視窗,dry-run 核對只此一支;app 層不得先於 apply 上線(或掛 flag)。
- 收割=只收乾淨樹、check/root test 綠才 push、STATUS 收帳同 commit。
- 搜尋詞/PII 不落 log 不進 URL(347-2 起 POST-only,加機制守門)。
- Sean 只在完成時收到一次總測試通知;期間唯一會打擾他的=動錢/大量寫入的 apply 確認。

## §4 完成判準(總測試觸發條件)

完成地圖 27 項中,扣除 26/27/30/31-33 之外**全部=✅(員工按得到、走得完)**,且:搜尋八維度可用、工作區分割視窗上線、取消/出貨/收款/退款/手動建單/商品線全流程在正式站走通(寫入 smoke 由施工窗以測試資料完成、不再依賴 Sean 點擊)。屆時交付:①功能地圖刷新 ②「員工的一天」白話操作流程 ③Design 視覺 demo(文字大小/密度,Sean 選版)。

— END —
