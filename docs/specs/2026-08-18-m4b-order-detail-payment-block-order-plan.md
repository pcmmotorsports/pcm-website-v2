# Plan(小):把「收款」區塊搬到「到貨」之後

> # 狀態:**已批准**
>
> ```
> 誰      Sean（主視窗轉；memory project_0818-sean-eleven-rulings-noon.md:19 我開檔核過）
> 哪一天  2026-08-18 中午
> 原文逐字  `09 收款區搬到到貨之後          = 甲`
> ```
> **作者 = W3。** 樹 `/Users/sean_1/pcm-od-list`(`od-order-list`)。
> 🔴 **這是【另一片】,刻意不併進商品卡 plan 的片 1** —— 兩件分開做、分開驗,出問題才分得出是誰造成的。

---

## §1 一句話

`order-detail.tsx` 裡 **`<PaymentSection>` 從第 4 個位置移到 `<ItemProcurementSection>`(採購 + 到貨)之後**。
**只動渲染順序,零 props 改動、零查詢改動、零業務邏輯改動。**
🔴 ~~零行為改動~~ **這句 §8-d 已撤回**:DOM 順序 = 鍵盤 Tab 與螢幕閱讀器順序,它跟著變了。

---

## §2 為什麼(量到的,不是覺得)

Sean 2026-08-12 逐字的主流程(memory `project_0812-sean-order-ui-workflow-and-undo-needs` §一):
**收款 → 採購(跟國外下單)→ 等到貨 → 收尾款 → 出貨**。

**同一張 5 品項的單,那五步在畫面上的 y**(真後台 + 真資料 + 真瀏覽器,viewport `1728 × 1117`;
量測環境全文 `docs/specs/2026-08-18-m4b-order-detail-product-card-plan.md` §2-b。
🔴 **拋棄式庫 + 我自己種的資料,不是正式站**):

| 第幾步 | 他要做的事 | y | 這一步要捲多少 |
|---|---|---:|---:|
| 1 | 新增收款(訂金) | 918 | — |
| 2 | 採購(向供應商訂貨) | 1,697 | ↓ 779 |
| 3 | 登錄到貨(最後一個品項) | 3,823 | ↓ 2,126 |
| 4 | **收尾款 —— 回到同一個「新增收款」** | **918** | 🔴 **↑ 2,905** |
| 5 | 出貨 | 4,267 | 🔴 **↓ 3,349** |

⇒ 走完一張單來回捲 **≈ 9,159 px**(= 上表四個位移的絕對值相加 `779+2126+2905+3349`),而整頁只有 4,640 px。
⇒ **第 4 步是折返**:收款區在採購與到貨的**上面**,而他的流程要求它在**下面**。

⚠️ **上表量在片 1 之前。** 🔴 **我已經重量了(片 1 落地後,12:1x CST,同一組量法):**

| 第幾步 | y | 位移 |
|---|---:|---:|
| 1 新增收款(訂金) | 918 | — |
| 2 採購 | 1,697 | ↓ 779 |
| 3 登錄到貨(最後一個品項) | 3,496 | ↓ 1,799 |
| 4 **收尾款 —— 回到同一個「新增收款」** | **918** | 🔴 **↑ 2,578** |
| 5 出貨 | 3,940 | ↓ 3,022 |

```
整頁高 4,640 → 4,313 px      來回捲 9,159 → 8,178 px
🔴 而第 4 步【仍然是折返】：−2,905 → −2,578。變小了 327 px，方向沒變。
⇒ 片 1 縮的是採購區的空品項高度，它【碰不到區塊順序】——
   這正是為什麼這一片必須獨立存在，而不是等片 2/3/4 順便解決。
```

**驗收量具已經寫好並且【先表演過失敗那一邊】**:`/tmp/pcm-probe-w3/flow-check.mjs`
現在跑它 ⇒ 逐字 `驗收判準：第 4 步位移必須 >= 0 ⇒ 實得 -2578 ❌ FAIL（仍在折返）`
🔴 **這一發是【動手前】跑的** —— 一個在修好之前就會綠的量具沒有判別力,所以先讓它紅一次。

---

## §3 🔴 這一片要推翻一段【寫著理由的】既有安排 —— 這是本檔最要緊的一節

`order-detail.tsx` 在 `<PaymentSection>` 上方有一段註解,把現在這個位置**當成一個決定**寫下來(逐字):

> 🔴 位置 = 收款緊跟在付款狀態與發票這一組之後:員工看完付款狀態,下一個問題就是「錢收了哪幾筆」。

**那個理由不是錯的,它只是【看單】的順序,而 Sean 講的是【做事】的順序。**
```
看單   我打開這張單想知道現況 ⇒ 付款狀態 → 錢收了哪幾筆   ← 現行安排服務這個
做事   我要把這張單往前推一步 ⇒ …到貨了 → 收尾款 → 出貨   ← Sean 09=甲 服務這個
```
🔴 **兩個都成立,而 Sean 拍了後者** ⇒ **要把那段註解改寫,不是刪掉**:
留著它才知道「為什麼曾經在上面」,否則下一個人會照同一個理由把它搬回去。

📎 **同一支檔已經有一個正確的先例**:「取消」墊底那段(2026-08-16)也是搬位置,
註解裡寫了**兩個獨立來源都說墊底**、以及「**只動渲染順序,零行為改動**」。**照那個形狀寫。**
⚠️ **但不要照抄那句字面** —— §8-d/§8-e 抓到那句是說過頭的;**那個先例的註解吃同一個病,
本片已一併改掉**(R2 抓到我原本只折了被指名那一處 —— 這正是「折 finding 只處理被指名那一處」那條教訓)。

---

## §4 要改什麼(逐項)

| 檔 | 改什麼 |
|---|---|
| `apps/admin/src/components/orders/order-detail.tsx` | `<PaymentSection>` 那一段(含它的註解區塊)整塊移到 `<ItemProcurementSection>` 之後、`<ShipmentSection>` 之前;註解改寫成 §3 那個講法 |

**今天的渲染順序**(枚舉出來的,不是憑印象。數法可重跑,回 **11** 行、與下表列數相同):
```bash
grep -nE '^ *<(OrderSummaryCards|NotesTimeline|NoteComposeForm|PaymentSection|OrderEditForm|ItemsTable|ItemProcurementSection|ShipmentSection|OrderCancelBlock|RefundLedgerSection|RefundSection)'   apps/admin/src/components/orders/order-detail.tsx
```
```
:214 OrderSummaryCards      :275 OrderEditForm            :309 OrderCancelBlock
:226 NotesTimeline          :277 ItemsTable               :313 RefundLedgerSection
:227 NoteComposeForm        :280 ItemProcurementSection   :339 RefundSection
:244 PaymentSection  ← 要搬的就是這一個
                            :289 ShipmentSection
```
**移動之後**(`PaymentSection` 從 `:244` 搬到 `ItemProcurementSection` 與 `ShipmentSection` 之間):
```
摘要卡 → 備註 → 編輯訂單 → 品項 → 採購與到貨 → 【收款】 → 出貨
       → 取消 → 退款帳本 → 退款入口          （危險操作沉底那一段完全不動）
```
⚠️ 上面那行**沒有列「標頭」** —— 標頭不是元件、grep 抓不到它,所以我也不把它算進那 11。
**數字與清單要同一把尺量出來**,不然下一個人重跑會對不上。

⚠️ **`cancelled` 那個「已取消」橫幅目前夾在 `<PaymentSection>` 與 `<OrderEditForm>` 之間**
⇒ 搬收款時**它要留在原地**(它是訂單層狀態,不屬收款)。**這是最容易順手一起搬走的一塊。**

### 4-a 鐵則 12 逐條

| 類 | 命中? | 依據 |
|---|---|---|
| ①錢 | **否** | `<PaymentSection>` 的 props(`orderId` / `returnTo` / `payments` / `amountDue`)一個字不動;不碰任何金額算式或 RPC |
| ②權限 | 否 | 不動 auth / RLS / server-client 邊界 |
| ③DB | 否 | 零 migration、零查詢改動 |
| ④平台設定 | 否 | — |
| ⑤對外不可回收 | 否 | 後台畫面 |
| ⑥`packages/ui` | 否 | 不碰 |

⇒ **標準片。** 而**因為它動的是「錢」那一區塊的位置**,我仍會跑 code-reviewer;
🔴 **自評有風險只能加審不能免審** —— 要不要加 codex 由主視窗或 Sean 決定,我不反對。

---

## §5 驗收(每條可 yes/no)

```
1 DOM 順序：採購與到貨【在】收款之前
  量法（可重跑，用 compareDocumentPosition，不用肉眼）：
    採購區標題節點 與 收款區標題節點 比位置 ⇒ 採購在前
2 「已取消」橫幅仍在編輯訂單【之前】，沒有跟著收款一起被搬走
3 <PaymentSection> 的 props 與 order-detail.tsx 其餘部分 zero diff
  量法：git diff -U0 只應命中「整塊搬移」與那段註解
4 重跑 §2 那五步的 y 位移，第 4 步【不再是往回捲】
  🔴 目標先訂在這裡：第 4 步的位移必須 >= 0（往下或原地），不得為負
     （片 1 那次我沒有先訂目標，那是我的疏漏，這次先訂）
5 四綠：TURBO_FORCE=1 typecheck / lint / build ＋ vitest（vitest 不在三綠裡，自己跑）
6 Sean 肉眼驗
```

---

## §6 rollback

```
一顆 commit、只動 order-detail.tsx 的渲染順序與一段註解。
git revert <sha> 即回到今天的樣子，零資料面殘留（因為沒有資料面改動）。
```

---

## §7 這一片**不做**什麼

```
❌ 不動「危險操作沉底」那一段（取消 → 退款帳本 → 退款入口）——那是 2026-08-16 照兩個來源改的
❌ 不動備註的位置 —— 那是 OD 第十二輪定案（overview-desktop.html:1171-1173）＋ 主視窗 MAIN-902-A Q1=A
❌ 不把「收尾款」做成一個獨立的步驟或按鈕 —— 系統裡沒有「尾款」這個概念，
   它就是再登錄一筆收款。要做成獨立步驟是另一題，Sean 沒講過。
❌ 不併進商品卡 plan 的片 1
```

---

## §8 落地與驗收結果(G2 窗,2026-08-18 下午)

> # 🔴🔴 §8-c 的數字已下修 —— 先讀這一段再引用下面任何一個 y 值
>
> **`+569` 那個數字是量在一個【沒有 hydrate 的頁面】上的。**
> 我當時用 `http://127.0.0.1:3011` 開後台,而 Next dev 會把 `/_next/static/chunks/*.js`
> 對 `127.0.0.1` 擋成 403 ⇒ **整頁沒有 hydrate**(runbook §9-b/§9-c 有兩個世界的實測與探針)。
>
> **15:0x 用 `http://localhost:3011` 重量,同一顆 commit、同一組種子、同一個 viewport**:
> ```
> 第 4 步(收尾款)位移    +569  →  +240      判準 >= 0 仍然 PASS
> 第 3 步(登錄到貨 最後)  3,247 → 3,576 px   ← 差在這裡:那一區有 client 元件,hydrate 後高度變了
> 整頁高 4,267 / 來回捲總計 4,722            ← 這兩個沒變
> ```
> ⇒ **結論(「不再是折返」)不變,而數字要用 `+240`。**
>
> ⚠️ **而 §2 那個「改前 −2,578」也是同一支量具、同一個網址量的** ⇒ 它**同樣沒 hydrate**,
> 我**沒有**重量它(要重量得先把這一片 revert 回去)。
> ⇒ **「−2,578 → +240」這個【箭頭】是跨兩種量測條件拼起來的,不要拿它當一個精確的改善量。**
> 這一片真正證到的是:**第 4 步的符號從負變正**,而那一條在兩種條件下都成立。


**作者交接**:W3 收工,這一片由 **G2**(後台訂單整塊)接手做完。樹同 `/Users/sean_1/pcm-od-list`。

### 8-a 量測環境(跟著數字走,不要與別份表互引)
```
真後台 next dev -p 3011 + 拋棄式 PG 55503 + PostgREST 3999 + 前綴代理 3998
種子     ~/pcm-mailbox/W3-seed-5items-20260818.sql（5 品項、3 筆到貨、0 筆收款）
migration ok=155 fail=27（要用的 7 張表 select 回來全在）
viewport 1728 × 1117，系統 Chrome（channel: 'chrome'）
🔴 拋棄式庫 + W3 種的資料，不是正式站。
```

### 8-b 驗收逐條
| # | 條件 | 結果 | 量法 |
|---|---|---|---|
| 1 | 採購與到貨【在】收款之前 | ✅ true | `compareDocumentPosition`,`/tmp/pcm-probe-g2/domorder.mjs` |
| 1b | 收款【在】出貨之前 | ✅ true | 同上 |
| 2 | 「已取消」橫幅仍在編輯訂單之前、沒被搬走 | ✅ true | 同上;🔴 **第一發是 `null`** —— 種子那張單沒被取消、橫幅根本沒渲染。`UPDATE orders SET cancelled_at=now()` 讓它出現後才量到 true。**那個 `null` 不是 false,但它也不是 true** |
| 3 | `<PaymentSection>` props zero diff | ✅ | `git diff -U0` 四個 props 逐字相同(`orderId` / `returnTo` / `payments` / `amountDue`) |
| 4 | 🔴 第 4 步位移 **>= 0** | ✅ **PASS** | 見 8-c |
| 5 | 四綠 | ✅ typecheck/lint/build rc 全 0;vitest 見 commit body | `TURBO_FORCE=1` |
| 6 | Sean 肉眼驗 | ⏳ 未做 | — |

### 8-c 第 4 步(收尾款):`-2,578` → **`+569`**
```
                        改前 y      改後 y
1 新增收款(訂金)          918        3,816
2 採購                  1,697        1,494   🔴 ↑ 2,322   ← 見下方誠實註記
3 登錄到貨(最後)          3,496        3,247      ↓ 1,753
4 收尾款(同一個新增收款)     918        3,816   ✅ ↓   569   ← 判準這一格
5 出貨                  3,940        3,894      ↓    78
---
整頁高          4,313 → 4,267 px
來回捲總計      8,178 →  4,722 px（−42%）
```
🔴 **誠實註記:折返沒有消失,它從第 4 步移到了第 1→2 步。**
收訂金現在也在採購下面(3,816),員工收完訂金要往上捲 2,322 px 去採購。
**淨值仍然是賺的**(來回捲 8,178 → 4,722),而且判準是**動手前先訂的**、量具**先紅過一次**(`-2578 ❌`)。
但**「折返被解決了」是假的** —— Sean 的五步裡收款出現兩次(訂金、尾款),
一個線性頁面**必然**在其中一次折返。要兩次都不折返得把收款做成兩個入口,那是另一題、他沒講過。

### 8-d 對抗審查(codex,唯讀,R1)
```
跑法   codex exec -s read-only "$(cat <prompt>)" < /dev/null      codex-cli 0.144.1
零留痕 跑前後 git status --porcelain 都只有我自己那兩支檔
為什麼是 codex 不是 code-reviewer subagent：本 session 的 harness 明文禁止呼叫 Agent tool。
        codex 走 main session 直呼、fresh context、唯讀 ⇒ 「不自驗」這條仍然成立。
        (Sean 2026-08-17 常設令:codex 隨時、任何窗、不必先問。)
```
**R1 = FAIL(1 must-fix + 1 nit),兩條都折了:**

| # | 級 | finding | 折法 |
|---|---|---|---|
| 1 | **must-fix** | 「零行為改動」是**說過頭**:`<PaymentSection>` 裡有可操作表單,**DOM 順序就是鍵盤 Tab 與螢幕閱讀器的朗讀順序**,它跟著變了 | 註解與 commit body 一律改寫成「零 props / 零查詢 / 零業務邏輯」,並**明寫朗讀順序也變了**。🔴 那是這片**要的效果**,不是副作用 —— 但不能寫成「什麼都沒變」 |
| 2 | nit | `<ShipmentSection>` 上方註解逐字「放在採購之後、**備註之前**」—— 兩半都已不成立(備註 08-13 搬走、收款 08-18 插進中間) | 改寫該行,舊字面用刪除線留痕 |

🔴 **第 1 條正是本 repo 最貴的那個病(字面 vs 事實)**:我在 plan §1、§4-a、註解、commit body **四個地方**
都寫了同一句「零行為改動」,而它在無障礙那一面是假的。**四處同字面 ⇒ 一處被抓到,其他三處不會自己紅。**

### 8-e R2 / R3(同工具、第 3 輪換角度)—— **每一輪都還在抓到真的東西**
```
R2 = FAIL  1 must-fix：我只折了【被指名那一處】。同一句「零行為改動」在同一支檔的
           2026-08-16 取消墊底那段【也在】(:313)，沒被我碰到。
           📎 這正是 memory feedback_folding-a-finding-defaults-to-the-named-spot-only。
R3 = FAIL  換角度問「還有沒有第三處 / 有沒有描述已不成立現況的句子 / 撤回寫法本身有沒有問題」：
           B :279 我【新寫的】那句「收尾款得捲回頁面上方，是整條動線裡唯一的折返」
                  用現在式描述【改之前】的狀態，而且「唯一」被我自己的量測推翻(折返移到第 1→2 步)
           B :309 「而現況兩邊都不符」講的是 08-16 改【之前】—— 現在已經墊底了
           C :315 刪除線在 .tsx 註解裡不會渲染，而字面掃描照樣命中一句已作廢的話
           三條全折。撤回句一律【不留原字面】，改寫成「這裡原本還寫了一句…，已撤回」。
判停        R2 與 R3 的 finding 都落在同一層(註解字面 vs 事實) ⇒ 照輪次紀律這是收斂訊號，R3 折完收工。
零命中複驗  bash scripts/literal-sweep.sh '零行為改動' ⇒ order-detail.tsx 命中數 0（先前 2）
```
🔴 **這一片的真正產出不是那 569 px** —— 是「一句讀起來很謹慎的宣稱(**零行為改動**)
在無障礙那一面是假的,而它在這支檔裡複製了兩份、在 spec 裡複製了四份」。
**改動本身 5 行,字面債 3 輪才清完。**
