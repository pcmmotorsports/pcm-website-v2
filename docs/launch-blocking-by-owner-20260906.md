# 擋上線 83 列 · 按【誰欄】分堆(2026-09-06 · `tidy` 產)

> 🔴 **這一份是派工用的,不是給 Sean 讀的** —— 白話版在 `docs/launch-status-20260906.md`。
> 分母 = `docs/launch-todo.md` 的 `open` 388 + `doing` 22 = **410 列**,其中判「擋」的 **83** 列。
> 現值當場跑:`bash scripts/launch-blocking-count.sh`(這一份是快照,板子還在動)。
> **每列只給錨 + 一句,不抄整列** —— 整列在板上,用錨去找:`bash scripts/board-row-by-anchor.sh '<錨>'`。
> ⚠️ 錨欄是 `#編號` 或 `:行號` 的,是那 194 個【錨欄無錨】的列之一 ⇒ 用那個編號找,不要用行號跨樹引用。

## 🔴🔴 派工最該先看的一格:**83 個擋裡,53 列不在任何一條線手上**

```
(誰欄看不出窗) 27   ← 誰欄有字, 而它不是窗名(是理由/證據/舊窗名)
待派           21   ← 誰欄逐字寫著「待派」
等 Sean         5   ← 阻塞點是 Sean, 不是缺人
---- 以上 53 列不在任何一條線手上 ----
mail 9 · account 5 · ship 5 · db 4 · tidy 3 · front 2 · auth 1 · 報價單 1  = 30 列有窗
```
🎯 **⇒ 30 / 83 有主人。** 而剩下 53 列要分成兩種,**因為它們要的動作不一樣**:
　 · **48 列(27 + 21)= 沒有人被指名** ⇒ 要的是【派工】。
　 　 那 27 列的誰欄**有字而不是窗名**(是理由、是證據、是已作廢的舊窗名)
　 　 ⇒ 🛑 **它們在畫面上與「有人負責」長得一樣** —— 那一欄不是空的。
　 · **5 列 = 等 Sean** ⇒ 要的是【端他一個字】,派人過去也沒用。
🔴 **兩個數字都要,而不可互換**:48 是「缺人」,53 是「今晚不會有人動它」。
　 板上**零欄位**會提醒這件事,而它在每一次盤點都要重數一次。

## (誰欄看不出窗)(27 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-PAIDTHENOVERPAID⟧` | ⟨擋(tidy 判 ①③)⟩ | 已付款的單被多匯一次 —— 而它今天零觀眾 |
| `⟦b4-NCPCRONRACE⟧` | ⟨擋(tidy 判 ①③)⟩ | 逾期 cron 先鎖住那張單, 收款才落帳 最後變成「已取消而錢在裡面」 |
| `⟦b4-NCPCANCELROLLBACK⟧` | ⟨擋(tidy 判 ①)⟩ | 重算撞上 statement timeout 時, 客人那筆收款會【跟著回滾】—— 而那是本片自己宣稱不會發 |
| `⟦b4-BANKCARDRACE⟧` | ⟨擋(tidy 判 ①③)⟩ | 兩個分頁:我取消完之後,另一個交易才建出同一台車的匯款單 —— 這一發掃不到它 |
| `—` | ⟨擋(tidy 判 ④)⟩ | ACL 漂移守門裝上了,而【路⑤ dashboard / SQL Editor 手動改權限】仍然完全沒有人守 |
| `#858` | ⟨擋(tidy 判 ①)⟩ | 手動建訂單(客人匯款那條路)—— 2026-08-26 拆號:本列是【觀察條目】,實作那一片在 #939(本 |
| `⟦f3-ALLOWLISTMANUAL1⟧` | ⟨擋(tidy 判 ⑥)⟩ | 白名單是手寫的 —— 有排程在跑而沒有人在看,不會有任何訊號 |
| `—` | ⟨擋(tidy 判 ④)⟩ | 我們的回退方案是 222 段從來沒有人執行過的註解 |
| `—` | ⟨擋(tidy 判 ④)⟩ | 「回退指令在半夜是 20ms, 在上班時間是一個不動的游標」 |
| `—` | ⟨擋(tidy 判 ④)⟩ | 稽核紀錄會說一句不真的話 —— 它記「管理者 A 做的」, 而事發當下 A 已經不是管理者(查核與寫入不在同 |
| `⟦0e-PROBENOSCHED⟧` | ⟨擋(tidy 判 ⑥)⟩ | scripts/probe-schema-exposure.sh 沒有排程 —— 規格說「由人或排程跑」,  |
| `⟦0b-TYPESNOTREGEN⟧` | ⟨擋(tidy 判 ⑥)⟩ | database.types.ts 自 20260902030000 apply 之後【沒有人重生成過】 p |
| `—` | ⟨擋(tidy 判 ③)⟩ | 他答了退款畫面那個數字要「顯示函式一起改」(B, 貴的那一邊)—— 而板上沒有列 ⟦b9-REFUNDNUM |
| `⟦f3-AUTOREFUND2⟧` | ⟨擋(tidy 判 ①)⟩ | 已收款單取消要【自動開待退款】—— Sean 2026-08-27 拍甲; 而落地零次 2026-09-02 |
| `⟦f3-RPCOWNER1⟧` | ⟨擋(tidy 判 ④)⟩ | 金流 RPC 的歸屬只到「某個有後台密碼的人」 |
| `⟦b4-PARTPAIDNOCANCEL1⟧` | ⟨擋(tidy 判 ①)⟩ | 收了訂金的匯款單【會變成不能取消】—— 而今天它可以。這是一個【上線會製造出來的】缺陷,不是現存的 |
| `⟦f3-ADPNARROWER1⟧` | ⟨擋(tidy 判 ④)⟩ | 那道被當成「根治」的 ALTER DEFAULT PRIVILEGES,射程比報的窄一格 |
| `⟦f3-RECIPIENTBIND1⟧` | ⟨擋(tidy 判 ⑤)⟩ | 寄件信箱與信件內容沒有「同一張單」的不變式 —— 而換成 HTML 會把洩漏面放大一個量級 |
| `⟦f3-PAIDCANCELRACE1⟧` | ⟨擋(tidy 判 ⑤)⟩ | 取消狀態只讀一次 讀完之後才被取消的單,照樣收到「付款成功」 |
| `⟦c7-LEDGERGATEREFUSES⟧` | ⟨擋(tidy 判 ③)⟩ | Sean 退了現金給客人,而系統【不讓他記】—— 那道閘擋下的是【紀錄】,而錢早就離開了 |
| `⟦b4-EMAILTRIAGE⟧` | ⟨擋(tidy 判 ④⑤)⟩ | 標籤列:寄信路徑 52 條 codex findings 的逐條複核正本 —— 板上只開兩條,其餘 10 條 |
| `⟦b4-RESEND409⟧` | ⟨擋(tidy 判 ⑤)⟩ | 同一把冪等 key 配不同 payload Resend 回 409,而它【不是去重】 —— 重試一路燒到死 |
| `⟦b4-MANREFUNDNOOWNER⟧` | ⟨擋(tidy 判 ③)⟩ | 人工退款之後,那張訂單仍然顯示【已付款】—— 而 Sean 本人明早開後台就會看到 |
| `⟦b4-PAYCONCUR1⟧` | ⟨擋(tidy 判 ③ —— 🔴 同 ⟦5b-REPORTEDNOTLANDED1⟧ 的形狀:本列主旨是流程反思, 而擋來自內嵌事實(併發收款觸發器競態算錯金額, 拋棄式 PG 實測過;修法 NONCARDPAID1 未上線 ⇒ 現況是活的))⟩ | 同一張單兩筆收款同時進來 實收 1000 的單停在「部分付款」 員工打電話去要一筆【已經收到】的錢 |
| `⟦b4-RECIPLIVEVALUE⟧` | ⟨擋(tidy 判 ⑤)⟩ | customers.email 是【註冊那一刻的化石】—— 沒有任何一條路會更新它 信寄到客人已經不看的舊地 |
| `⟦front-PDPTAXONOMYEMPTY⟧` | ⟨擋(tidy 判 ⑤)⟩ | 商品詳情頁的「適用車款」會【整區空掉】, 而頁面回 200、畫面不說 |
| `⟦search-TAXONOMYTIMEOUT⟧` | ⟨擋(tidy 判 ⑤)⟩ | 車款查詢會逾時, 而它【靜默降級】—— 客人拿到一份殘缺的車款下拉, 畫面不會說 |

## 待派(21 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-BANKDUPPERCART⟧` | ⟨擋(tidy 判 ①)⟩ | 同一個 cart 可以建出兩張匯款單, 而今天沒有任何東西擋 |
| `⟦f3-FWBYPASSORDER⟧` | ⟨擋(tidy 判 ⑥)⟩ | 未來在 Vercel 防火牆加規則時, 它【必須排在放行規則的下面】—— 而沒有任何東西會提醒你 |
| `—` | ⟨擋(tidy 判 ⑥)⟩ | 心跳白名單今天對得上, 而【那個零不能被讀成「已經驗過了」】 ⟦b4-CRONWL1⟧ |
| `⟦b9-ENUMWATCH⟧` | ⟨擋(tidy 判 ④⑥)⟩ | 稽核紀錄寫進去了, 而【沒有任何告警器在讀它】—— 而那正是 ⟦b4-ENUM3⟧ 原本的病 |
| `⟦b9-SRVMIN⟧` | ⟨擋(tidy 判 ④)⟩ | 那幾道持有的【範圍】是不是最小 —— 今天沒有人量過 |
| `⟦b4-MGRENV1⟧` | ⟨擋(tidy 判 ④⑥)⟩ | 後台所有 manager 閘的效力綁在一顆 env 上, 而那顆拿掉時它們會【放行】—— 而值讀不到、三綠全 |
| `⟦c7-ACLGATEANCHOR⟧` | ⟨擋(tidy 判 ⑥)⟩ | ACL 守門的錨認【八碼】, 而它自己印給人看的說明寫「版本號」—— 而 migration 版本號是十四碼 |
| `⟦b4-TESTACCT1⟧` | ⟨擋(tidy 判 ③)⟩ | 正式站 auth 裡還躺著一個測試帳號 —— 而【它有沒有被算進任何數字】沒有人知道 |
| `⟦b4-CANCELRACE1⟧` | ⟨擋(tidy 判 ①③)⟩ | 查「有沒有出貨」與「取消並退款」不在同一個交易 —— 中間那一瞬間有人按出貨, 錢就白退了 |
| `⟦b4-PARTCANCEL1⟧` | ⟨擋(tidy 判 ①③)⟩ | 【部分取消】收了的錢, 今天起【仍然】零紀錄 —— 而整單取消那一半已經修好了 |
| `#530 / #872` | ⟨擋(tidy 判 ⑥)⟩ | 那道 migration 守門寫好了、沒接線,而且從來沒有正確過 已接線(40cb0486,.husky/m |
| `⟦b4-REPLAY1⟧` | ⟨擋(tidy 判 ⑥)⟩ | supabase/migrations/ 從零 apply 重現不了正式庫 |
| `**Sean**` | ⟨擋(tidy 判 ①⑥)⟩ | 那封每日告警叫的是一件後台做不到的事,已叫 15 天 |
| `⟦b4-WITHHELD1⟧` | ⟨擋(tidy 判 ②③⑥)⟩ | 「該刪而沒刪」那張清單今天【只活在一行 log 裡】—— 而它是一張會長的清單 |
| `⟦b4-FITSYNC1⟧` | ⟨擋(tidy 判 ⑥)⟩ | 車款搜尋的資料停在 08-26 2026-08-31 -f7 實查:已恢復 —— 而「零告警」那半一個字都沒 |
| `⟦b4-CAPRACE1⟧` | ⟨擋(tidy 判 ③)⟩ | 這張表的閘在防一件它防不到的事 —— 而「修好它」會讓帳變得更錯 |
| `#903` | ⟨擋(tidy 判 ④)⟩ | B5-a 的 rollback 是紙上約束 |
| `⟦5b-REPORTEDNOTLANDED1⟧` | ⟨擋(tidy 判 ③ —— 🔴 本列【主旨是流程教訓】, 擋來自它內嵌的一個具體事實:wallet ledger entry_date 用 current_date、修法 20260829180000 未 applied ⇒ 記帳日期錯誤仍在 production。判的是那個事實, 不是那篇反思)⟩ | 一句「已回報、已立 backlog」如果不帶錨, 它不是交接 —— 它是一個【關掉下一個人尋找動作】的免責 |
| `⟦5b-AUDITGRANTEXPIRY⟧` | ⟨擋(tidy 判 ④)⟩ | 稽核表那個 SELECT 權限的「到期日」【不是一個日期, 是一個事件】—— 而那個事件可能已經發生了, 而 |
| `⟦f3-REDNEEDSEXIT⟧` | ⟨擋(tidy 判 ⑥)⟩ | 紅字要有【出口】與【觀眾】—— 事前擋的觀眾一定在現場(他被擋了);事後紅的觀眾可能永遠不來 |
| `⟦0a-CARDCANCELNOREFUND⟧` | ⟨擋(tidy 判 ①③)⟩ | 取消一張刷卡單, 今天【沒有任何東西】會【自動】去退那筆錢( 2026-09-05 訂正:退款的鈕【是有的】 |

## 等 Sean(5 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-ALERTENV⟧` | ⟨擋(tidy 判 ⑥)⟩ | ANOMALY_ALERT_ENABLED 今天是什麼值,沒有人重量過( 「沒有人查過」 2026-08-3 |
| `⟦b9-ACLDRIFT5⟧` | ⟨擋(tidy 判 ④)⟩ | ACL 漂移守門裝上了,而【路⑤ dashboard / SQL Editor 手動改權限】仍然完全沒有人守 |
| `⟦f3-TAPPAYNORATELIMIT1⟧` | ⟨擋(tidy 判 ①)⟩ | TapPay 通知端點沒有限流 —— 兩個結構性障礙今天都拆了 |
| `—` | ⟨擋(tidy 判 ②)⟩ | 手動建單的品項規格(line_spec)沒有畫面入口 快照永遠是 {} 且補不回來 補在應用層的東西, DB |
| `#299` | ⟨擋(tidy 判 ④)⟩ | 整張正式資料表不在版控裡 是【三張】,全在同一條 fitments 管線上(2026-08-30 盤點;舊字 |

## mail(9 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-BANKNOEMAIL⟧` | ⟨擋(tidy 判 ⑤)⟩ | 選了匯款的客人, 【一封信都收不到】—— 而他要匯去哪只寫在一頁他得自己走回去的畫面上 |
| `⟦b4-BANKCHARGESCARD⟧` | ⟨擋(tidy 判 ①)⟩ | 翻開匯款開關的那一刻, 系統會拿客人的卡去扣一張【匯款單】的錢 |
| `—` | ⟨擋(tidy 判 ⑥)⟩ | 6 支 pg_cron 可能安靜停了,而其中兩支在金流路徑上 —— 沒有人會抱怨,因為沒有客人看得到 |
| `⟦f3-ALERTSAMELINE1⟧` | ⟨擋(tidy 判 ⑥)⟩ | 告警器與它要監控的排程走【同一條線】—— 那條線壞掉,兩個一起停,沒有人會叫 |
| `⟦b4-MAILRENDER1⟧` | ⟨擋(tidy 判 ⑤ —— 而這是 11 個擋裡最不確定的一個:它是「沒驗過」不是「已知會錯」)⟩ | 那封付款成功信【沒有任何一格是在真的信箱裡看過的】—— 而它現在有版面了, 所以看起來像做完了 |
| `—` | ⟨擋(tidy 判 ③④)⟩ | 災難還原會把整張客戶表從 CSV 蓋回去, 而稽核表一列都不會有 —— 而它【不在護欄那片的範圍裡】 ⟦b4 |
| `⟦b4-INVOICE5PCT⟧` | ⟨擋(tidy 判 ①③)⟩ | 手動建單加【開發票】勾選欄:勾了單價 +5%, 沒勾不加(Sean 2026-09-04 本人提的新規格) |
| `⟦b4-DEALERSIGNUPUNSEEN⟧` | ⟨擋(tidy 判 ③⑤)⟩ | 經銷會員登錄那條路【沒有人驗收過】—— 而說這句話的是 Sean 本人 |
| `⟦b4-CANCELMAILMIXEDRAIL⟧` | ⟨擋(tidy 判 ⑤)⟩ | 卡 + 現金混合退款的取消單【系統不寄】, 要人工寄 —— 而今天沒有任何東西會提醒人工去寄 |

## account(5 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-CARDPENDINGWINDOW⟧` | ⟨擋(tidy 判 ①③)⟩ | 刷卡【進行中】那段窗口:客人另開分頁建匯款單, 守門看不到 兩張單仍同時活著 |
| `⟦b4-TAXSURFACES⟧` | ⟨擋(tidy 判 ③)⟩ | 會對客人顯示那五個金額的地方是【五個】, 而 spec 與板子都只寫了兩個 |
| `⟦b4-PRICECOPYTAX⟧` | ⟨擋(tidy 判 ①③)⟩ | 建單畫面逐字教員工「單價填【含稅】」—— 而 ⟦b4-INVOICE5PCT⟧ 第 9 步會讓這句話變成【錯 |
| `⟦b4-TAXADMINSURFACES⟧` | ⟨擋(tidy 判 ③)⟩ | 後台那兩個金額面在有稅時四個數加不起來 —— 而 CSV 的欄名逐字寫著「可直接加總」 |
| `⟦acct-LOGOUTTESTBLIND⟧` | ⟨擋(tidy 判 ⑥)⟩ | 一格名字叫「登出」的測試, 而它測的世界裡【沒有登出】—— 而它綠著 |

## ship(5 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦5b-SHIPPEDNUMNOTRECORDED1⟧` | ⟨擋(tidy 判 ⑤)⟩ | 我們沒有存下「那封出貨信實際寄了哪個號碼」 一個競態下客人會永久拿著錯號碼, 而沒有東西會叫 |
| `⟦ship-HCTACTIVATION⟧` | ⟨擋(tidy 判 ②)⟩ | 只開 env 不等於上線 —— 而傳輸層做完之後, 沒有任何一份受審的文件在管「誰按下第一箱」 |
| `⟦ship-HCTUNKNOWNREAD⟧` | ⟨擋(tidy 判 ②)⟩ | 乙型:新竹【回了】而我們讀不懂 —— 那箱一樣卡在 unknown, 而成因與甲型完全不同 |
| `⟦ship-HCTLABEL⟧` | ⟨擋(tidy 判 ②)⟩ | 新竹的貨號我們拿得到了, 而【那張要貼在箱子上的紙】今天沒有人印得出來 2026-09-06 起印得出來了( |
| `⟦ship-HCTLABELCAPTURE⟧` | ⟨擋(tidy 判 ②)⟩ | 那張標籤圖【現在就會被存下來】—— 而我們一箱都還沒送過, 所以庫裡是空的 |

## db(4 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-REFUND10016⟧` | ⟨擋(tidy 判 ①③)⟩ | 同一張單、同一個碼、隔 10 天卡了兩次 —— 而中間有人查過、寫下來了 <br> [2026-09-01  |
| `⟦b4-REFUNDID1⟧` | ⟨擋(tidy 判 ①③)⟩ | 退款 ID 在傳遞途中被丟掉 |
| `⟦db-MERGEBLINDGATE⟧` | ⟨擋(tidy 判 ⑥)⟩ | git merge 不跑 pre-commit 掛在那裡的每一道閘, 對【合併帶進來的】那一半是盲的 |
| `⟦db-DOGBLINDBRANCH⟧` | ⟨擋(tidy 判 ⑥)⟩ | 部署時序閘的「0 pending」有兩個世界, 而它們印同一行字 —— 其中一個放行的正是這道閘存在的理由 |

## front(2 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-SIG4ERRORS⟧` | ⟨擋(tidy 判 ⑥)⟩ | 訊號4 的 errors 桶【持續失敗】那一種,本片不會叫 |
| `—` | ⟨擋(tidy 判 ④)⟩ | storefront 有 5 道 service_role 受控例外,而其中 4 道說不出是誰批的 |

## auth(1 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦search-LOGFLOOD⟧` | ⟨擋(tidy 判 ⑥ · 近 ④ —— 作者明言殘餘風險未經 Sean 拍板接受, codex R2 仍 FAIL)⟩ | 搜尋語料表可以被灌 —— 而我只把攻擊面【降低】, 沒有消掉 |

## tidy(3 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b9-PROBESCHED⟧` | ⟨擋(tidy 判 ⑥)⟩ | 那支曝露探針【沒有人在定期跑它】—— 它只在有人想到的時候跑一次 |
| `⟦b4-2FAOWNER1⟧` | ⟨擋(tidy 判 ④)⟩ | 2FA 蓋好了,但沒有「這支手機是誰的」 |
| `BL-37` | ⟨擋(tidy 判 ②)⟩ | 部件講錯／方向講反(15 筆; 2026-08-25 線 4 補跨 repo 座標:/Users/sean_ |

## 報價單(1 列)

| 錨 | 擋的理由 | 一句 |
|---|---|---|
| `⟦b4-QUOTE2FA1⟧` | ⟨擋(tidy 判 ④)⟩ | 報價單 2FA【裝好了而關著】—— 而那是後台前面唯一一道門(現值 34 天沒量;原標題:報價單那邊 2FA |

