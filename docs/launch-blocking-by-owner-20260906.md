# 擋上線 ⛔ ~~98~~ ⇒ **96** 列 · 派工分堆(2026-09-06 · `tidy` 第三版 + 23:0x 差集訂正)

> ⛔ ~~第一版 83 列~~ ⛔ ~~第二版 102 列~~ ⛔ ~~第三版 98 列(基準 `ecaceab85`)~~ ⇒ ✅ **現值 96 列**(基準 `c0060d29f`, 2026-09-06 23:0x)。
>
> 🔴🔴 **而【下面的分堆表還是 98 那一版】** —— 我沒有重跑分堆, 只訂正了數字與差在哪。**兩個差都指得出名字**:
> ① **`#64` 在本檔列了兩次**(`:84` `:85`, 同一件事「客人拿不到發票」)⇒ 表上 98 格, 真值 97。板上兩列已合併。
> ② **`⟦ship-HCTACTIVATION⟧` 被 ship 在 39f 改掉了** —— 從 `open`+`⟨擋⟩` 改成 `done`+`⟨未判⟩`,
>    ship 逐字「啟用文件有了, 能不能送是 `⟦ship-HCTAPI⟧`/§二① 另一題」⇒ 97 ⇒ 96。
>    🛑 **那一列的擋不是消失, 是被拆到另一題去** ⇒ 派工時要跟著看 `⟦ship-HCTAPI⟧`。
> 📌 **⇒ 拿本檔派工可以;拿本檔【數擋幾件】不行** —— 現值當場跑 `bash scripts/launch-blocking-count.sh`。
> **為什麼降**:Sean 2026-09-06 21:58 + 22:03 一次答了 **16 題** ⇒ 5 列轉不擋、10 列拿到主人。
> 🔵 **最明顯的變化:「等 Sean」從 12 列 ⇒ 1 列。**
> 
> 🔴 派工用,不是給 Sean 讀的(白話版 `docs/launch-status-20260906.md`)。現值當場跑 `bash scripts/launch-blocking-count.sh`。
> **每列只給錨 + 一句**;用錨找:`bash scripts/board-row-by-anchor.sh '<錨>'`。
> 🛑 **未派工前先跑 `python3 scripts/what-happened-to.py <錨>`** —— 2026-09-06 實錘兩列比板上寫的輕。

## 🔴 第一格:**53 列還是沒有人被指名**(而它比上一版只少 3)

```
(誰欄看不出窗) 29   ← 誰欄有字, 而它不是窗名 ⇒ 在畫面上與「有人負責」長得一樣
待派           24
等 Sean         1   ← ⛔ ~~上一版 12~~ ⇒ 他 21:58/22:03 答完了
---- 以上 54 列不在任何一條線手上(其中 53 列是缺人)----
mail 10 · account 9 · db 7 · ship 6 · auth 5 · front 3 · tidy 3 · 報價單 1  = 44 列有窗
驗算 29+24+1+44 = 98 ✅ ⛔ **而那個 98 含上面兩個已知差** ⇒ 真值 **96**(訂正見檔頭, 分堆未重跑)
```

## 🆕 那 53 列【按碰哪些檔】再分一次 —— 這一版新增的解析度

> 🎯 **為什麼要這一層**:上一版只按誰欄分,而「沒有人被指名」那一堆對派工**零解析度** ——
> 53 列擺在一起,派工的人還是要一列一列看。**按面分之後,可以整族丟給同一個窗。**
> ⚠️ **這一層是【猜的】**:靠列內文字裡的可單發探測名(`apps/storefront` / `supabase/` / `vercel` …)比對,
> **不是開檔看的** ⇒ 每列仍要開檔確認。有 5 列**判不出面**,照實列出來不塞進任何一堆。

```
後台             19 列
DB/migration   11 列
顧客站            10 列
信件/排程           7 列
(判不出面)          5 列
工具/CI           1 列
```

### 後台(19 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `—` | open | ACL 漂移守門裝上了,而【路⑤ dashboard / SQL Editor 手動改權限】仍然完全沒有 |
| `⟦b9-Q15GAP⟧` | parked | service_role 讀不到 40 張 45 張開了 RLS 的表 —— 而唯一補過的那一張【被撤下 |
| `⟦f3-FWBYPASSORDER⟧` | open | 未來在 Vercel 防火牆加規則時, 它【必須排在放行規則的下面】—— 而沒有任何東西會提醒你 |
| `#858` | open | 手動建訂單(客人匯款那條路)—— 2026-08-26 拆號:本列是【觀察條目】,實作那一片在 #939 |
| `—` | open | 心跳白名單今天對得上, 而【那個零不能被讀成「已經驗過了」】 ⟦b4-CRONWL1⟧ |
| `⟦f3-ALLOWLISTMANUAL1⟧` | open | 白名單是手寫的 —— 有排程在跑而沒有人在看,不會有任何訊號 |
| `—` | open | 稽核紀錄會說一句不真的話 —— 它記「管理者 A 做的」, 而事發當下 A 已經不是管理者(查核與寫入不 |
| `⟦b4-ANOMCLAIM1⟧` | parked | 兩個人同時按下同一筆雙扣異常的「認領」—— 而兩個人都以為自己在退那筆錢 |
| `⟦b4-MGRENV1⟧` | open | 後台所有 manager 閘的效力綁在一顆 env 上, 而那顆拿掉時它們會【放行】—— 而值讀不到、三 |
| `⟦0b-TYPESNOTREGEN⟧` | open | database.types.ts 自 20260902030000 apply 之後【沒有人重生成過】 |
| `⟦f3-AUTOREFUND2⟧` | open | 已收款單取消要【自動開待退款】—— Sean 2026-08-27 拍甲; 而落地零次 2026-09- |
| `—` | parked | 等時機:有新訂單進來之後(現在 orders=0 這把尺沒有材料) —— 寄信死人開關的訊號 4:訂單已 |
| `⟦f3-RPCOWNER1⟧` | open | 金流 RPC 的歸屬只到「某個有後台密碼的人」 |
| `**Sean**` | open | 那封每日告警叫的是一件後台做不到的事,已叫 15 天 |
| `⟦b4-CAPRACE1⟧` | open | 這張表的閘在防一件它防不到的事 —— 而「修好它」會讓帳變得更錯 |
| `#903` | open | B5-a 的 rollback 是紙上約束 |
| `⟦5b-AUDITGRANTEXPIRY⟧` | open | 稽核表那個 SELECT 權限的「到期日」【不是一個日期, 是一個事件】—— 而那個事件可能已經發生了, |
| `⟦b4-MANREFUNDNOOWNER⟧` | open | 人工退款之後,那張訂單仍然顯示【已付款】—— 而 Sean 本人明早開後台就會看到 |
| `⟦0a-CARDCANCELNOREFUND⟧` | open | 取消一張刷卡單, 今天【沒有任何東西】會【自動】去退那筆錢( 2026-09-05 訂正:退款的鈕【是有 |

### DB/migration(11 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-BANKCARDRACE⟧` | doing | 兩個分頁:我取消完之後,另一個交易才建出同一台車的匯款單 —— 這一發掃不到它 |
| `—` | open | 我們的回退方案是 222 段從來沒有人執行過的註解 |
| `⟦b9-SRVMIN⟧` | open | 那幾道持有的【範圍】是不是最小 —— 今天沒有人量過 |
| `⟦c7-ACLGATEANCHOR⟧` | open | ACL 守門的錨認【八碼】, 而它自己印給人看的說明寫「版本號」—— 而 migration 版本號是十 |
| `—` | open | 他答了退款畫面那個數字要「顯示函式一起改」(B, 貴的那一邊)—— 而板上沒有列 ⟦b9-REFUNDN |
| `⟦b4-PARTCANCEL1⟧` | open | 【部分取消】收了的錢, 今天起【仍然】零紀錄 —— 而整單取消那一半已經修好了 |
| `#530 / #872` | open | 那道 migration 守門寫好了、沒接線,而且從來沒有正確過 已接線(40cb0486,.husky |
| `⟦b4-REPLAY1⟧` | open | supabase/migrations/ 從零 apply 重現不了正式庫 |
| `⟦b4-WITHHELD1⟧` | open | 「該刪而沒刪」那張清單今天【只活在一行 log 裡】—— 而它是一張會長的清單 |
| `⟦15-SHIPGATE-F1⟧` | parked | 停線期間的出貨信會把死信告警的訊號1【永久推成非零】 那個告警從此不會再叫 |
| `⟦f3-ADPNARROWER1⟧` | open | 那道被當成「根治」的 ALTER DEFAULT PRIVILEGES,射程比報的窄一格 |

### 顧客站(10 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-PAIDTHENOVERPAID⟧` | doing | 已付款的單被多匯一次 —— 而它今天零觀眾 |
| `⟦b4-NCPCRONRACE⟧` | open | 逾期 cron 先鎖住那張單, 收款才落帳 最後變成「已取消而錢在裡面」 |
| `#64` | parked | 客人拿不到發票 |
| `#64` | parked | 客人拿不到發票 ⛔ ~~重複列~~ **與上一列是同一件事**, 板上已合併(`docs/launch-todo.md`)⇒ **不要重複派** |
| `⟦b9-ENUMWATCH⟧` | open | 稽核紀錄寫進去了, 而【沒有任何告警器在讀它】—— 而那正是 ⟦b4-ENUM3⟧ 原本的病 |
| `⟦b4-TESTACCT1⟧` | open | 正式站 auth 裡還躺著一個測試帳號 —— 而【它有沒有被算進任何數字】沒有人知道 |
| `⟦b4-FITSYNC1⟧` | open | 車款搜尋的資料停在 08-26 2026-08-31 -f7 實查:已恢復 —— 而「零告警」那半一個字 |
| `⟦5b-REPORTEDNOTLANDED1⟧` | open | 一句「已回報、已立 backlog」如果不帶錨, 它不是交接 —— 它是一個【關掉下一個人尋找動作】的免 |
| `⟦f3-REDNEEDSEXIT⟧` | open | 紅字要有【出口】與【觀眾】—— 事前擋的觀眾一定在現場(他被擋了);事後紅的觀眾可能永遠不來 |
| `⟦front-PDPTAXONOMYEMPTY⟧` | open | 商品詳情頁的「適用車款」會【整區空掉】, 而頁面回 200、畫面不說 |

### 信件/排程(7 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦0e-PROBENOSCHED⟧` | open | scripts/probe-schema-exposure.sh 沒有排程 —— 規格說「由人或排程跑」 |
| `⟦f3-RECIPIENTBIND1⟧` | open | 寄件信箱與信件內容沒有「同一張單」的不變式 —— 而換成 HTML 會把洩漏面放大一個量級 |
| `⟦f3-PAIDCANCELRACE1⟧` | open | 取消狀態只讀一次 讀完之後才被取消的單,照樣收到「付款成功」 |
| `⟦b4-EMAILTRIAGE⟧` | open | 標籤列:寄信路徑 52 條 codex findings 的逐條複核正本 —— 板上只開兩條,其餘 10 |
| `⟦b4-RESENDNOTIMEOUT⟧` | parked | 寄信的 fetch 沒有任何 timeout 一封卡住,同批 49 封白燒重試額度、整批進死信 |
| `⟦b4-RESEND409⟧` | open | 同一把冪等 key 配不同 payload Resend 回 409,而它【不是去重】 —— 重試一路燒 |
| `⟦b4-RECIPLIVEVALUE⟧` | open | customers.email 是【註冊那一刻的化石】—— 沒有任何一條路會更新它 信寄到客人已經不看的 |

### (判不出面)(5 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-NCPCANCELROLLBACK⟧` | open | 重算撞上 statement timeout 時, 客人那筆收款會【跟著回滾】—— 而那是本片自己宣稱不 |
| `⟦b4-BANKDUPPERCART⟧` | open | 同一個 cart 可以建出兩張匯款單, 而今天沒有任何東西擋 |
| `—` | open | 「回退指令在半夜是 20ms, 在上班時間是一個不動的游標」 |
| `⟦c7-LEDGERGATEREFUSES⟧` | open | Sean 退了現金給客人,而系統【不讓他記】—— 那道閘擋下的是【紀錄】,而錢早就離開了 |
| `⟦b4-PAYCONCUR1⟧` | open | 同一張單兩筆收款同時進來 實收 1000 的單停在「部分付款」 員工打電話去要一筆【已經收到】的錢 |

### 工具/CI(1 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-PARTPAIDNOCANCEL1⟧` | open | 收了訂金的匯款單【會變成不能取消】—— 而今天它可以。這是一個【上線會製造出來的】缺陷,不是現存的 |

## 有窗的 44 列(按窗)

### mail(10 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-BANKNOEMAIL⟧` | open | 選了匯款的客人, 【一封信都收不到】—— 而他要匯去哪只寫在一頁他得自己走回去的畫面上 |
| `⟦b4-NOSENTBODY⟧` | standing | 客人說「你們寄給我的帳號是錯的」—— 而客服【調不出他當時收到的那封信】 |
| `⟦b4-BANKCHARGESCARD⟧` | doing | 翻開匯款開關的那一刻, 系統會拿客人的卡去扣一張【匯款單】的錢 |
| `—` | open | 6 支 pg_cron 可能安靜停了,而其中兩支在金流路徑上 —— 沒有人會抱怨,因為沒有客人看得到 |
| `⟦f3-ALERTSAMELINE1⟧` | open | 告警器與它要監控的排程走【同一條線】—— 那條線壞掉,兩個一起停,沒有人會叫 |
| `⟦b4-MAILRENDER1⟧` | open | 那封付款成功信【沒有任何一格是在真的信箱裡看過的】—— 而它現在有版面了, 所以看起來像做完了 |
| `⟦b4-INVOICE5PCT⟧` | doing | 手動建單加【開發票】勾選欄:勾了單價 +5%, 沒勾不加(Sean 2026-09-04 本人提的新規格 |
| `⟦b4-DEALERSIGNUPUNSEEN⟧` | open | 經銷會員登錄那條路【沒有人驗收過】—— 而說這句話的是 Sean 本人 |
| `—` | parked | 客人刷不出卡,我們這邊不會響 |
| `⟦b4-CANCELMAILMIXEDRAIL⟧` | open | 卡 + 現金混合退款的取消單【系統不寄】, 要人工寄 —— 而今天沒有任何東西會提醒人工去寄 |

### account(9 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-WALLETDEDUPE⟧` | doing | 員工扣 500、DB 已提交而回應遺失 同一操作再送 扣 1000 —— FOR UPDATE 只排順序 |
| `—` | parked | 代購品項的單價【沒有任何含稅保證】—— 而錯的錢會配一個全綠的守門 ⟦b4-PURCHTAX1⟧ |
| `⟦b4-TAPPAYDIRECT⟧` | parked | 在 TapPay 後台直接退款這條路,不經過我們的系統 —— 而我們要不要知道,是 Sean 的決定 |
| `—` | doing | 手動建單的品項規格(line_spec)沒有畫面入口 快照永遠是 {} 且補不回來 補在應用層的東西,  |
| `⟦b4-TAXSURFACES⟧` | open | 會對客人顯示那五個金額的地方是【五個】, 而 spec 與板子都只寫了兩個 |
| `⟦b4-PRICECOPYTAX⟧` | doing | 建單畫面逐字教員工「單價填【含稅】」—— 而 ⟦b4-INVOICE5PCT⟧ 第 9 步會讓這句話變成 |
| `⟦b4-TAXADMINSURFACES⟧` | open | 後台那兩個金額面在有稅時四個數加不起來 —— 而 CSV 的欄名逐字寫著「可直接加總」 |
| `⟦acct-LOGOUTTESTBLIND⟧` | open | 一格名字叫「登出」的測試, 而它測的世界裡【沒有登出】—— 而它綠著 |
| `⟦supply-BRANDFILTERZERO⟧` | parked | 顧客站品牌目錄頁曾經一次印「0 件商品 · 找不到符合條件的商品」, 而同一時間伺服器回的 HTML 是 |

### db(7 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-REFUND10016⟧` | open | 同一張單、同一個碼、隔 10 天卡了兩次 —— 而中間有人查過、寫下來了 <br> [2026-09-0 |
| `#299` | open | 整張正式資料表不在版控裡 是【三張】,全在同一條 fitments 管線上(2026-08-30 盤點; |
| `⟦b4-PFEREPLAY1⟧` | parked | 「從零重放」還是做不到 —— 而補版控那支的【版號比第一個讀它的人晚兩個月】 |
| `⟦b4-REFUNDID1⟧` | open | 退款 ID 在傳遞途中被丟掉 |
| `⟦db-MERGEBLINDGATE⟧` | open | git merge 不跑 pre-commit 掛在那裡的每一道閘, 對【合併帶進來的】那一半是盲的 |
| `⟦db-NOROLLBACKARTIFACT⟧` | open | 351 支 migration, 【寫在 migration 檔裡的】可執行反向 SQL = 0 支 — |
| `⟦db-DOGBLINDBRANCH⟧` | doing | 部署時序閘的「0 pending」有兩個世界, 而它們印同一行字 —— 其中一個放行的正是這道閘存在的理 |

### ship(6 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b9-SHIPUI⟧` | parked | 顧客站進度軸「已出貨」上線, 而它帶著兩個【客人看得到】的已知缺陷 —— 兩條都要 Sean 拍 |
| `⟦5b-SHIPPEDNUMNOTRECORDED1⟧` | open | 我們沒有存下「那封出貨信實際寄了哪個號碼」 一個競態下客人會永久拿著錯號碼, 而沒有東西會叫 |
| `⟦ship-HCTACTIVATION⟧` | open | 只開 env 不等於上線 —— 而傳輸層做完之後, 沒有任何一份受審的文件在管「誰按下第一箱」 |
| `⟦ship-HCTUNKNOWNREAD⟧` | open | 乙型:新竹【回了】而我們讀不懂 —— 那箱一樣卡在 unknown, 而成因與甲型完全不同 |
| `⟦ship-HCTLABEL⟧` | open | 新竹的貨號我們拿得到了, 而【那張要貼在箱子上的紙】今天沒有人印得出來 2026-09-06 起印得出來 |
| `⟦ship-HCTLABELCAPTURE⟧` | open | 那張標籤圖【現在就會被存下來】—— 而我們一箱都還沒送過, 所以庫裡是空的 |

### auth(5 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b9-SRVCONSUMERGAP⟧` | parked | service_role 消費者盤點還有四個沒補的洞 —— 而每一個的偏向都是【少報會壞的】 |
| `⟦b9-ZEROPOLICYSEAM⟧` | parked | 兩支「應為零 policy」的漂移偵測器, 在 20260904270000 貼下去那一刻就已經不成立  |
| `⟦f3-TAPPAYNORATELIMIT1⟧` | open | TapPay 通知端點沒有限流 —— 兩個結構性障礙今天都拆了 |
| `②` | parked | 有人可灌爆通知信箱 |
| `⟦b4-SEQACL1⟧` | parked | 正式庫有 4 支 sequence 讓 anon 可以 nextval / setval —— 而 RL |

### front(3 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-SIG4ERRORS⟧` | open | 訊號4 的 errors 桶【持續失敗】那一種,本片不會叫 |
| `—` | open | storefront 有 5 道 service_role 受控例外,而其中 4 道說不出是誰批的 |
| `⟦search-TAXONOMYTIMEOUT⟧` | open | 車款查詢會逾時, 而它【靜默降級】—— 客人拿到一份殘缺的車款下拉, 畫面不會說 |

### tidy(3 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b9-PROBESCHED⟧` | open | 那支曝露探針【沒有人在定期跑它】—— 它只在有人想到的時候跑一次 |
| `⟦b4-2FAOWNER1⟧` | open | 2FA 蓋好了,但沒有「這支手機是誰的」 |
| `BL-37` | open | 部件講錯／方向講反(15 筆; 2026-08-25 線 4 補跨 repo 座標:/Users/sea |

### 等 Sean(1 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b9-ACLDRIFT5⟧` | open | ACL 漂移守門裝上了,而【路⑤ dashboard / SQL Editor 手動改權限】仍然完全沒有 |

### 報價單(1 列)

| 錨 | 態 | 一句 |
|---|---|---|
| `⟦b4-QUOTE2FA1⟧` | open | 報價單 2FA【裝好了而關著】—— 而那是後台前面唯一一道門(現值 34 天沒量;原標題:報價單那邊 2 |


---

## 📮 派工單開頭語(主視窗可直接複製貼給窗)

### 🔴🔴 **先讀這一段 —— 我實查之後,把「按面整族派」降級成「候選入口」**

```
無主的擋 53 列 · 而【同時命中一個以上的面】的有 42 列 ⇒ 79%
分堆規則 = 「第一個命中就停」, 而我的清單順序是
  顧客站 → 後台 → DB/migration → 信件排程 → 出貨物流 → 平台設定 → 工具CI → 報價單
```
🔬 **兩個把它降級的實測(不是顧慮,是量到的)**:
1. **換順序 ⇒ 分堆整個變**:我第二次寫這支撈法時把「後台」放前面 ⇒ **顧客站從 10 列變成 0 列。**
2. 🔴 **第一個命中常常是【證據裡的檔案路徑】,不是那一列的主體** —— 逐列查過:
   `⟦b4-PAIDTHENOVERPAID⟧`(已付款的單被多匯)命中「顧客站」的原因是它的關鍵事實欄裡有
   一個 `apps/storefront` 路徑;`⟦b4-NCPCRONRACE⟧`(cron 與收款競態)同理。
   ⇒ **它們是錢的列,而被分進顧客站。**

📌 **⇒ 所以這四族是【派工的入口】,不是【那一列屬於誰】的答案。**
🛑 **建議的用法**:主視窗**照族丟出去,而在派工單裡帶上這一段** ——
　 派到的窗開檔後說「這其實是別人的」⇒ **那不是分錯,那是這個分法的性質** ⇒ 退回來換族,不用道歉。
　 ⚠️ **不要用這四個數字回答「顧客站還剩幾件擋」** —— 它答不了那個問題。

### 🛑 四族共同的前置(每一族的開頭語都含這一段)

```
1. 未動手前先跑  python3 scripts/what-happened-to.py <錨>
   2026-09-06 一夜實錘兩列【比板上寫的輕】:#868(寄信那半早已接上)·
   ⟦b4-RESTORE2⟧(兩半都不對:非蓋回、稽核 8 天前補上)
2. 整列讀完再判, 不要只讀開頭 —— 標題寫【症狀】, 而結論常常寫在【尾巴】
   (2026-09-06 實錘:只讀某列前 620 字元 ⇒ 把一個已結案的東西當成未解問題)
3. 那一列若自己寫了結論或訂正, 逐字抄回你的回報 —— 一列自己寫的結論比外人的判斷硬
4. 動板列前 git fetch origin dev 對齊;改態要留刪除線 + 日期 + 證據 檔案:行號, 不刪列文字
5. 🔴 新內容【接在 token 後面】, 不要插在 token 前面
   (2026-09-06 實錘:兩個人往同一格同一端寫 ⇒ 零衝突的 merge 把 15 列的 token 推走)
```

### 🔴 順手撈到一個板子本身的缺陷:**`#64` 有兩列**

```
:678  parked  客人拿不到發票
:681  parked  客人拿不到發票
```
兩列同一個編號、同一件事 ⇒ **而板上沒有任何東西會叫**(`board-state-consistency.py` 的錨唯一性檢查
只看 `⟦…⟧` 形狀的錨,而 `#64` 是總帳編號、不在那個分母裡)。
⇒ 🛑 **這一件我沒有自己合併**(合併別人的列要有人認領)⇒ **端主視窗裁。**

### 四族的錨清單與共同前置

### 派 → account / ship 分:後台(`apps/admin`)(19 列)

```
錨清單(19 列):
  — ⟦b9-Q15GAP⟧ ⟦f3-FWBYPASSORDER⟧ #858 — ⟦f3-ALLOWLISTMANUAL1⟧ — ⟦b4-ANOMCLAIM1⟧ ⟦b4-MGRENV1⟧ ⟦0b-TYPESNOTREGEN⟧ ⟦f3-AUTOREFUND2⟧ — ⟦f3-RPCOWNER1⟧ **Sean** ⟦b4-CAPRACE1⟧ #903 ⟦5b-AUDITGRANTEXPIRY⟧ ⟦b4-MANREFUNDNOOWNER⟧ ⟦0a-CARDCANCELNOREFUND⟧
```
**共同前置**:它們的共同點是**員工做不了工作或看到錯的數字** —— 而客人看不到,所以沒有人會來告訴我們。
**派前規矩**:上面那五條共同前置,**第 1 條不可省** —— 這一族裡已經有兩列被證明比板上寫的輕。

### 派 → db:DB / migration(11 列)

```
錨清單(11 列):
  ⟦b4-BANKCARDRACE⟧ — ⟦b9-SRVMIN⟧ ⟦c7-ACLGATEANCHOR⟧ — ⟦b4-PARTCANCEL1⟧ #530 / #872 ⟦b4-REPLAY1⟧ ⟦b4-WITHHELD1⟧ ⟦15-SHIPGATE-F1⟧ ⟦f3-ADPNARROWER1⟧
```
**共同前置**:它們的共同點是**要動正式庫或版控帳本** ⇒ 鐵則 12 ③,對抗審查不降級、貼板一次一支。
**派前規矩**:上面那五條共同前置,**第 1 條不可省** —— 這一族裡這一夜還沒有人逐列開過檔。

### 派 → front:顧客站(`apps/storefront`)(10 列)

```
錨清單(10 列):
  ⟦b4-PAIDTHENOVERPAID⟧ ⟦b4-NCPCRONRACE⟧ #64 #64 ⟦b9-ENUMWATCH⟧ ⟦b4-TESTACCT1⟧ ⟦b4-FITSYNC1⟧ ⟦5b-REPORTEDNOTLANDED1⟧ ⟦f3-REDNEEDSEXIT⟧ ⟦front-PDPTAXONOMYEMPTY⟧
```
**共同前置**:它們的共同點是**客人看得到而畫面不說** —— 逾時靜默降級、整區空掉而頁面回 200。
**派前規矩**:上面那五條共同前置,**第 1 條不可省** —— 這一族裡這一夜還沒有人逐列開過檔。

### 派 → mail:信件 / 排程(7 列)

```
錨清單(7 列):
  ⟦0e-PROBENOSCHED⟧ ⟦f3-RECIPIENTBIND1⟧ ⟦f3-PAIDCANCELRACE1⟧ ⟦b4-EMAILTRIAGE⟧ ⟦b4-RESENDNOTIMEOUT⟧ ⟦b4-RESEND409⟧ ⟦b4-RECIPLIVEVALUE⟧
```
**共同前置**:它們的共同點是**寄出去就收不回來**(鐵則 12 ⑤)⇒ 動之前先確認 cutoff 那類時間切點的值。
**派前規矩**:上面那五條共同前置,**第 1 條不可省** —— 這一族裡已經有兩列被證明比板上寫的輕。


---

## 🆕 那 5 列【判不出面】的 —— **我開檔看了,不是再猜一次**

> 🔴 **為什麼要開檔**:上面那個分堆靠 regex 比對可單發探測名,而這 5 列**一個面都沒命中**
> ⇒ **再換一組 pattern 也只是換一次猜**。所以這一批我逐列讀了「事」欄與「卡什麼」欄。
> ✅ **結果:5 列都判得出來, 而 4 列同一族** —— regex 抓不到它們, 是因為它們談的是
> **鎖、觸發器、唯一索引、`WHEN OTHERS`** 這些**沒有檔案路徑的東西**。
> 📌 **⇒ 那個分堆對「講機制而不講檔案」的列結構上失明。**

| 錨 / 行 | 判給 | 一句(開檔讀到的) |
|---|---|---|
| `⟦b4-NCPCANCELROLLBACK⟧` | **DB/migration** | `20260904230000` 的 `WHEN OTHERS` 不接 `query_canceled` ⇒ 重算撞 statement timeout 時**客人那筆收款跟著回滾** |
| `⟦b4-BANKDUPPERCART⟧` | **DB/migration** | 修法是一條 `CREATE UNIQUE INDEX … ON orders`(部分索引)⇒ 純 DB 片 |
| `:698`(無錨) | **DB/migration** | `DROP TABLE` 拿不到 `ACCESS EXCLUSIVE` 而 psql 預設**沒有 `lock_timeout`** ⇒ 回退指令在上班時間會無限等 |
| `⟦b4-PAYCONCUR1⟧` | **DB/migration** | 兩個 `AFTER` trigger 各自在自己的快照裡算 ⇒ 併發收款把金額算成「部分付款」,**而它是安靜的** |
| `⟦c7-LEDGERGATEREFUSES⟧` | **後台** | 主體在 `apps/admin` 的 `manual-refund-entry-section.tsx:88`(那張表是**帳本不是授權**),而 PCM01 上限閘拒絕登記 ⇒ **Sean 退了現金而系統不讓他記** |

⇒ **修正後的分堆**:DB/migration **11 + 4 = 15** · 後台 **19 + 1 = 20** · 顧客站 10 · 信件排程 7 · 工具/CI 1
　 **判不出面 5 ⇒ 0。**

🛑 **而 `:698` 那一列自己記了一句值得一起帶給 db 的**(逐字):
> 線C 剛好量完一組「很快」的數字(0 / 1k / 100k 列全部約 20ms)⇒ **它的量測本身會提高下一個人踩雷的機率**
> —— 那組數字是真的,而**它會被讀成一個保證**。

📌 **⇒ 派這一列的時候要一起說:那組 20ms 是【半夜沒人用】量到的,而要用回退的那一天通常是白天。**
