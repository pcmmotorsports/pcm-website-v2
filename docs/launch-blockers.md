# 擋上線清單(這一份才有「做完」)

> 🔴 **這是 `9af09e4de` 那一刻的快照。要最新的,重跑下面那一段 —— 不要相信這裡的數字。**
> 板子(`docs/launch-todo.md`)會一直長,這份不會自己跟著變。**兩份不一致時,以重跑的結果為準。**

## 怎麼重新產生這份清單

```bash
cd ~/pcm-shop          # 換成你的樹
git fetch origin dev && git merge --ff-only origin/dev
bash scripts/launch-blocking-count.sh | grep -E '^(open|doing)'
```

那支印的 `open` 與 `doing` 兩列的「擋」欄加起來,就是這份清單的列數。
要列出是哪幾列(路徑同樣換成你的樹):

```bash
python3 - <<'PY'
import re
P='docs/launch-todo.md'
CLOSED={'open','doing','parked','done','standing'}
n=0
for i,line in enumerate(open(P,encoding='utf-8'),1):
    line=line.rstrip('\n')
    if not line.startswith('| '): continue
    esc=line.replace('\\|','\x01')
    f=[x.replace('\x01','\\|') for x in esc.split('|')]
    if f[1].strip() not in CLOSED: continue
    tail = re.search(r'\|[ \t]*$',esc) is not None
    last=(f[-2] if tail else f[-1]).strip()
    if f[1].strip() in ('open','doing') and last.startswith('⟨擋'):
        n+=1; print(i, f[1].strip(), f[2].strip()[:34])
print('共', n)
PY
```

---

## 分母:49 / 409

| | |
|---|---|
| 板上 `open` + `doing` | **409** |
| 其中**擋上線** | **49** |
| 不擋的 | 360 |

🔵 **2026-09-20 補判三輪之後,這個數字從 38 走到 49(+11)。**
主視窗量到「`open` 有 48 列沒有人判過擋不擋」。拆開之後:**15 列早就判過**(末格開頭是
`⛔ ~~⟨擋⟩~~` 的刪除線,而數數那支只讀開頭 ⇒ 兩邊都漏)· 7 列開頭是別的 token · 3 列明寫未判 ·
**23 列真的沒有任何裁定**。

| 輪 | 做了什麼 | 結果 | commit |
|---|---|---|---|
| 一 | 那 23 列逐列開檔判 | 擋 **7** · 不擋 15 · 未判 1 | `8d226623e` |
| 二 | 14 列把裁定搬到末格開頭 + 剩下 10 列判 | 擋 **3** · 不擋 6 · 未判 2 | `a01547e32` |
| 三 | Sean 拍乙:`:527` 那個「不做」的舊裁定不算數 ⇒ 重判 | 擋 **1** | `9af09e4de` |

📌 **⇒ 清單變長不是因為今天壞了 11 件,是因為那 11 件【本來就在,而沒有人判過它】。**
🔬 `open` 的「沒填 token」從 **45 列降到 1 列** —— 剩的那一列是 `⟦tidy-NETPUBLICALL⟧`(`:2558`),
它畫掉 `⟨擋⟩` 之後接的是 `⟨記著⟩`,而 **`⟨記著⟩` 不在那支腳本的字彙裡**(擋 / 不擋 / 未判 / —),
板上也查不到它對應哪一邊。**把它映成「不擋」等於替一個沒有人裁過的東西下定義 ⇒ 留著不動。**

🔴 **而 Sean 2026-09-20 拍甲要把 `⟨記著⟩` 的定義寫進板頭 —— 那一步【卡住了,而且是好事】。**
寫之前先 grep 板上還有誰在用這個標記,結果:**真的帶 `⟨記著⟩` token 的只有 6 列,而它們有兩種意思。**
```
甲種(1 列 · :2558)  「現在不擋, 而保護它的東西很脆, 壞了不會有人發現」
                      —— 保護是一個面板設定, 而面板設定會被人改而沒有人會知道
乙種(5 列 · :1049 :1092 :1093 :1094 :1095)  「Sean 拍甲 = 這一列算在【經銷價整包排
                      上線後跟 B2B 子網域一起做】裡面 ⇒ 態 open ⇒ parked, ⟨擋⟩ ⇒ ⟨記著⟩」
                      —— 那是【他知情而排到上線後】, 跟「很脆」無關
```
⇒ 📌 **一個定義蓋不住兩種用法。** 硬寫進板頭,會讓那 5 列經銷價的列在讀的人眼裡變成
「有一道很脆的保護」,而它們其實是**他排程過的**。**已停下報主視窗,板頭沒動。**

🔴 **第一版主視窗給的是 70,那個數字【不對】—— 重跑是 38;補判三輪之後 49。**
差的 32 列全是 `⛔ ~~⟨擋⟩~~ ⇒ ⟨不擋⟩` 這種**畫掉的舊標記**(多數是 Sean 2026-09-10 自己批准翻成「不擋」的)。
用「整列有沒有出現 `⟨擋`」去數,會把**他已經拍過的決定重新算成待辦**。

**這份用的尺**(與 `scripts/launch-blocking-count.sh` 同一把):
> 態 是 `open` 或 `doing`,**而且**那一列**最後一格的開頭**是 `⟨擋`。
> 板上的規矩寫得很清楚:擋不擋由**最後一格開頭那個 token** 回答,不由內文有沒有出現那兩個字回答。

🔬 對照都跑過:
- **正對照**(尺會動):把 `⟦b4-PARTCANCELTAX⟧` 的 `⟨擋⟩` 改成 `⟨不擋⟩` ⇒ open 的擋 31 → **30**。
- **負對照**(尺不會亂叫):用一個不存在的假 token `⟨擋XYZZY` 去數 ⇒ **0**。

⚠️ **`parked` 另外還有 53 列、`standing` 2 列也掛著 `⟨擋⟩`** —— 那是「Sean 知道那個痛而先不做」的。
**這份不收它們**,但它們沒有消失。要看:同一支腳本的 `parked` / `standing` 那兩行。

---

## 清單

每一列只寫三件事:**沒做會怎樣**(受詞是客人或員工)、**在等誰**、**板上第幾行**。
細節不搬過來 —— 開 `docs/launch-todo.md` 跳到那一行。

### 甲 · 只有 Sean 做得到(2 列)

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦b4-TESTACCT1⟧` | 正式站的登入資料裡還躺著一個測試帳號,而沒人知道它有沒有被算進任何數字。清帳號是**不可逆**,只有你的手能按。 | `:1142` |
| `#906 ⟦b4-REFUNDID1⟧` | 退款編號在傳遞途中被丟掉。要解開它,**你要花 10 分鐘用 TapPay 測試環境真的按一次退款**(你 09-06 答過 q14 甲),步驟由線【DB】寫給你。 | `:1296` |

### 乙 · 不在這個 repo,本地的窗做不動(5 列)

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦b4-QUOTE2FA1⟧` | 報價單後台前面**唯一那道門**(2FA)裝好了而**關著** ⇒ 拿到密碼的人直接進得去。 | `:787` |
| `⟦b4-2FAOWNER1⟧` | 報價單 2FA 蓋好了,而系統不知道「這支手機是誰的」⇒ 出事查不出是誰。 | `:1247` |
| `⟦f3-RPCOWNER1⟧` | 金流動作的歸屬只記到「某個有後台密碼的人」,查不出是哪個員工做的。 | `:1271` |
| `BL-37` | 報價單那邊 15 筆零件描述**講錯 / 方向講反** ⇒ 客人照著買會買到錯的零件。 | `:1248` |
| (B7 負向驗收) | 開關已經開了,而**失敗情境從來沒有人走過** ⇒ 壞掉的時候員工不知道會長什麼樣。 | `:602` |

### 丙 · 板上說有人正在做(7 列,態 = `doing`)

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦b4-PAIDTHENOVERPAID⟧` | 客人已經付過的單**又匯了一次**,系統不會發現,那筆多的錢沒有人去退。 | `:506` |
| `⟦f3-PDPSKUSTATIC⟧` | 客人在商品頁**選了紅色,上面印的還是母料號** ⇒ 照著料號買會買錯。(你 09-06 親眼看到那一格) | `:561` |
| (心跳白名單 `⟦b4-CRONWL1⟧`) | 排程的守門今天對得上是**巧合不是驗過** ⇒ 排程死掉不會有人叫。 | `:846` |
| `⟦b4-CRON6HB⟧` | **6 支排程可能安靜停掉,其中 2 支在收錢那條路上** ⇒ 客人付了錢而單子不動,沒有人會抱怨,因為沒人看得到。 | `:877` |
| `⟦b4-MGRENV1⟧` | 那顆管權限的設定被拿掉時,**拿舊登入票的人會被放行**進後台。 | `:1050` |
| `⟦b4-INVOICE5PCT⟧` | 員工手動建單勾「開發票」要**單價 +5%**(你 09-04 自己提的規格)—— 今天沒做。 | `:1084` |
| `⟦db-TAXONOMYVIEW⟧` | 客人在顧客站**切分類要等 3 秒**的真正原因。 | `:2550` |

### 丁 · 沒有人接(34 列)

**客人直接看得到 / 摸得到的**

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦search-DUPCATNAMES⟧` | 目錄裡有**三組同名分類**,客人點進去拿到哪一個沒有保證,而畫面上看不出來。(這件要你或管目錄的人裁) | `:1077` |
| `⟦search-TAXONOMYTIMEOUT⟧` | 選車的下拉**查詢逾時就默默少一截** ⇒ 客人看到殘缺的車款清單,而畫面不說。 | `:2531` |
| `⟦b4-TAXSURFACES⟧` | 會對客人顯示金額的地方有 **5 個**,而規格只寫了 2 個 ⇒ 稅可能印錯。(等真資料) | `:1083` |
| `⟦mail-FAILEDMAXNORESCAN⟧` | 付款信重試次數燒完之後**再也不會被重排** ⇒ 客人**永遠**收不到那封信。 | `:1684` |
| `⟦b4-CUTOFFWRONGCOLUMN⟧` | 決定「誰收得到取消信」的欄位跟契約寫的不是同一欄 ⇒ 有一種單會**靜靜地漏掉**。 | `:2461` |
| `⟦mail-CUTOFFENQUEUEONLY⟧` | 出貨信少一道以**出貨時間**為準的停寄線。 | `:1681` |
| `⟦db-SEARCHFACETMUTEX⟧` 🆕 | 客人搜了字再**改一下排序,他打的字就不見了**。站上有四個會清掉關鍵字的地方,09-11 只修了「分類」那一個。 | `:1096` |
| `⟦mail-SENDINGSTUCKTIMEOUT⟧` 🆕 | 一封信重試到**第 5 次**時資料庫剛好卡一下 ⇒ 那封信**從來沒送出就永久死亡**。 | `:1685` |
| `⟦wrs-FITPARSEEMPTY⟧` 🆕 | 商品的原文明明寫著配哪台車,而系統解析出**空的** ⇒ 客人用車款搜,那件商品**不存在**。 | `:2454` |
| `⟦front-VEHFACETSILENTDROP⟧` 🆕 | 客人選了一台系統**認不得的車**,畫面不報錯也不回 0,而是**默默改成別的篩選** ⇒ 他以為在看自己那台車的零件。 | `:901` |
| `⟦mail-REQUEUERESETSGEN⟧` 🆕 | 有人手動救一封卡住的信,會把一個**同時管著「誰能改這封信」**的欄位歸零 ⇒ 一封**沒寄出去的信會被標成已寄**。 | `:590` |

**員工在後台會卡住 / 錢會錯的**

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦b4-MANUALORDERDEADEND⟧` | 員工手動建單**走得完,走完之後出不了貨** —— 那是設計,只是畫面從來沒說 ⇒ 他會做完一整套然後卡住。 | `:1099` |
| `#939` | 手動建單(電話 / LINE 來的單)是**上線後每天要用的路**,而整條路**零實績**。 | `:1269` |
| `⟦b4-PARTCANCEL1⟧` | 三條會讓訂單金額變小的路(部分取消 / 改單價 / 改運送方式),**收了的錢零紀錄**。 | `:1209` |
| `⟦b4-PARTCANCELTAX⟧` | 未稅的手動單算「多收」會**算太大** ⇒ 錢往外多退。 | `:1210` |
| `⟦b4-CARDALREADYREFUNDED⟧` | 卡片那半**已經退成功**的混合單,員工到今天**登記不了**,兩邊都錯。 | `:1213` |
| `⟦b4-BANKORDERINVISIBLE⟧` 🆕 | 匯款單在資料庫眼裡**不存在** ⇒ 一個客人會**兩邊都付**。(你 09-20 拍乙:09-06 那個「不做」不算數了) | `:527` |
| `⟦f3-AUTOREFUND2⟧` | 已收款的單取消要**自動開待退款**(你 08-27 拍甲)—— 貼上去了,而**跑對了沒有人驗過**。🛑 這一列主視窗交代過**不要派**。 | `:1204` |
| `⟦b4-SIG4ERRORS⟧` | 某支排程**連續失敗**的時候,這片不會叫 ⇒ 沒有人收到通知。 | `:566` |
| (員工的一天 33 項) | 員工每天要走的 33 件事,**沒有人完整走過一遍**。 | `:843` |
| `⟦b4-NETRECEIVEDLEFTOVERS⟧` 🆕 | 一張**全額退完**的單,畫面還印著「尾款 1,200」⇒ 員工會去跟一個已經退完錢的客人要錢。 | `:589` |
| `⟦auth-PARTIALREFUNDCANCELGAP⟧` 🆕 | 一張**取消了而只退一部分**的單,兩條寄信線**都不寄** ⇒ 客人的錢動了而他零通知。 | `:2546` |
| `⟦db-WEBHOOKMANUALBACKLOG⟧` 🆕 | 有 3 筆刷卡通知**等人工等了 46 / 29 / 29 天**,而**沒有任何東西會說「這裡有東西等人工」**。(那 3 筆本身查過了,是我們自己造的,曝險 0;擋的是那個缺口) | `:2566` |

**權限 / 資料安全**

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| (service_role 例外) | 顧客站有 **5 道繞過權限的後門,其中 4 道說不出是誰批的**。 | `:925` |
| `⟦b9-UNTRACKEDGRANT⟧` | 有一個**版控之外**的角色、加一道版控之外的授權 ⇒ 拿版控從零重建,建不出現在這個世界。 | `:2572` |
| `⟦f3-ADPNARROWER1⟧` | 那道被當成「根治」的權限設定,**射程比回報的窄一格**。 | `:1518` |
| `⟦db-RLSHARDENZEROROWS⟧` | 做權限收緊的那天,一個唯讀授權會**安靜地變成零列**,而每一道檢查都還是綠的。 | `:2580` |
| `⟦db-ACLGATEPROSEMISFIRE⟧` | 一道閘**多報**、另一道**少報** ⇒「那道閘掃過了」這句話不成立。 | `:2576` |
| `⟦B-CICHECKALWAYSRED⟧` 🆕 | 那道「碼沒過就不准推」的閘,面板上寫著開著而**實質是關的**(CI 一直紅,每次推都繞過)⇒ 而推 `dev` 就是**後台上線**。 | `:585` |
| `⟦wrs-DAILYNOEXPECT⟧` 🆕 | 每天自動抓商品的那一班,**對「供應商今天少了一半」完全看不見**(19 家全部)⇒ 商品會靜靜消失。 | `:2455` |

**資料庫改動會咬人的**

| 錨 | 沒做會怎樣 | 板上 |
|---|---|---|
| `⟦db-SAMETRIGGERNAME⟧` | 兩支還沒貼的 migration 建**同一個名字**的東西 ⇒ 後貼的會把前一支**蓋掉**,而沒有人查過誰該先。 | `:2578` |
| `⟦db-NOROLLBACKARTIFACT⟧` | 351 支 migration,**寫在檔案裡的可執行回退 SQL = 0 支** ⇒ 貼錯了要退回去,沒有現成的東西可以用。 | `:2489` |
| `⟦b4-AUDITNULLAMBIG⟧` | 稽核紀錄分不出「**本來就沒有那一列**」與「**我讀不到**」,而那是**寫進去就改不了**的 ⇒ 事後有爭議時回不去。 | `:2565` |
| `⟦db-REVIEWLEFTNOTRACE⟧` | 一份核對只留了結論沒留過程 ⇒ 下一個要複驗的人只能**整份重做**。 | `:2575` |

---

## 🔴 一列要拿掉(不是我改,是給下一個人)

`docs/launch-todo.md:741` 那一列**自己寫著**「重複列 · 正本 `⟦b9-ACLDRIFT5⟧`」,
而**正本在 `:740`,態是 `parked`、誰欄逐字寫「本件已判不再做」**。
⇒ 這一列掛著 `⟨擋⟩` 而**它指的那件事已經拍不做了** ⇒ 真正還有效的是 **48 列**,不是 49。

🛑 **我沒有動板**(這一趟不改板、不改態、不開新列)。要不要翻它,是下一輪的事。

---

## 我的判準(主視窗要我寫出來)

1. **擋不擋,只看最後一格開頭那個 token。** 內文出現 `⟨擋` 不算 —— 板上有 32 列的 `⟨擋⟩` 是**被畫掉的舊標記**。
2. **`parked` + `⟨擋⟩` 不收進這份。** 那是「知情而先不做」,不是「待辦」。但它在,另外標出來了。
3. **「在等誰」照【誰做得動】分,不照板上寫的窗名分。** 板上那些 `-db` / `-account` 是**當初開列的窗**,那些窗今天多半不在了 ⇒ 只有態 `doing` 那 7 列我才寫「有人在做」。
4. **主視窗問的那一格(「擋而其實在等別人」)**:49 列裡有 **7 列**是這種 —— 甲組 2 列(等你)+ 乙組 5 列(在報價單那個 repo)。**它們留在清單上**,因為它們**確實會擋**;但它們**不該被派給這邊的窗**,所以分成獨立兩組。

### 2026-09-20 補判那 23 列用的判準(每一條都有出處,沒有一條是我發明的)

| 出處 | 逐字 |
|---|---|
| `launch-todo.md:281` | token 答的是「**上線那天客人會不會痛**」 |
| `launch-todo.md:261-262` | 實際在用的六類:① 付不了錢 / 單子不對 · ② 客人買錯東西 · ③ 錢會錯 · ④ 權限 / 資料丟或給錯人 · ⑤ 對外發錯東西 · ⑥ 守門沒裝, 而它守的是錢或產線 |
| `launch-todo.md:265-266` | **Sean 2026-09-07 答 Q43 乙** ——「客人的資料被不該看的人看到」**不算**擋上線 ⇒ ④ 是分類, 不是擋的理由 |
| `launch-status-20260906.md:90` | 本輪這把尺:**拿不準 ⇒ 標「未判」**(舊尺是「一律算擋」) |

🔴 **而主視窗一開始要我去找的「板頭那四句判準」—— 它不存在。**
`git grep` 全歷史:「四句判準」在 repo 裡出現**恰 1 次**,而那一次是 `launch-todo.md:263`
**引用**它(「它們不是板頭那四句判準的細分」)。⇒ 📌 **它被引用過,而從來沒有被定義過。**
那與 `aeb49b4e9` 那顆 commit 修的是**同一種病**(①…⑥ 當時也是「板上 134 處在用、全 repo 零處定義」)。
✅ 主視窗已收回那個指示,改用上面這一組。
