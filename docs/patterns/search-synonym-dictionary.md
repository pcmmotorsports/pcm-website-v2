# 搜尋俗稱字典 —— 接這條線的人先讀這支

> 🔴 **這份是【收攏】不是新結論**:2026-09-04 線【身分】`-auth` 量到的東西原本散在跨窗訊息、
> 三個板列與四支 commit 裡。⇒ **訊息會消失,這支檔不會。**
> 🛑 **每一格都帶【射程與失效條件】** —— 沒有那一半的讀數會在幾週後變成一句沒人敢動的話。

## 0. 一句話:這張表在做什麼
客人在搜尋框打**台灣車友的俗稱**,而我們的分類是**正式名**。
`apps/storefront/src/data/search-synonyms.ts` 把前者對到後者。

---

## 1. 🎯 為什麼字典【不能】被「比對規則」取代(2026-09-04 量,分母 = 當時 32 列)

```
把比對從「開頭」改成「包含」之後:
  5 列  變冗餘(規則自己就找得到)
  1 列  看順序(防倒球 ⇒ 車身防倒球與滑塊 777 / 輪軸防倒球 326)
  0 列  會被送去【錯的】分類
 26 列  規則一個都找不到  ← 🎯 這 26 列就是字典的價值
```
🎯 **那 26 列是「俗稱與正式名【字面上毫無關係】」** ——
`來令片 ⇒ 煞車皮` · `卡夢 ⇒ 碳纖維部品` · `香菇頭 ⇒ 空氣濾芯` · `下巴 ⇒ 整流罩與下導流`。
⇒ 📌 **任何字面規則都吃不到它們。** 所以「改解析器就不用字典了」是錯的。

⚠️ **射程**:5/1/0/26 這組數字是**當時 32 列**下的。字典長大它會變 ⇒ **引用前重跑**
(做法:拿 `foldSearchTerm` 對「有貨的分類名」做 `includes`,見 `search-synonyms.test.ts` 的形狀)。

---

## 2. 🎯 「多顆膠囊」對這一半的影響 ≈ 0(2026-09-04 量)

```
32 列裡, 在「一個俗稱可以帶多顆膠囊」的世界下會變的:【1 列】(防倒球)
其餘 31 列 union 恰好 = 1 ⇒ 與今天一模一樣
```
🔴 **而那是【下界】** —— 它只看得到「俗稱是分類名的一部分」那種多重性。
**語意上的多重性(俗稱本身就指兩類東西)量不到 —— 沒有語料**(見 §5)。
🔵 **而我試過一個代理指標並判它壞掉,寫在這裡免得下一個人重做**:
查每個 `to` 的**兄弟分類**(同父層、有貨)⇒ `整流罩與下導流` 有 **12 個兄弟**,
而 `下巴` 不是那 12 個的意思 ⇒ 📌 **「有幾個兄弟」與「這個俗稱指幾個」沒有關係。**

⚠️ **而單值是寫在型別裡的**:`search-synonyms.ts` 的 `to: string`(單數)、
`synonymFor()` 回 `SearchSynonym | null`(**只回第一個命中的那一列**)。
⇒ 要多值就得改型別。**而改型別是解析器那一半的事**,不要單邊動。

---

## 3. 🔴 三道閘併成一道,而它【呼叫解析器】不複述規則

`search-synonyms.test.ts` 現在問的是:
> **把 `from` 餵進真的 `parseSearchFacets`,它是不是靠【這一列】解出 `to` 的?**

⛔ ~~舊版是兩道,各自把 `parse-search-facets.ts` 的規則重打一份~~(`foldStartsWith` / `splitWords`)
⇒ 🛑 而那支測試檔**沒有 import 解析器** ⇒ 它守的是那兩份副本
⇒ 生產碼改規則的那一刻,它**還在測一條不存在的規則而且全綠**(當時分母:兩道 × 32 列 = **64 格**)。

✅ **一道取代兩道而且嚴格更強** —— 它順帶擋住「`to` 不在目錄樹」「`to` 沒有商品」,
因為那些在真的解析器裡本來就會表現出來。
📌 **問一個【不隨實作改變】的問題,那把尺就不會因為實作改了而失效。**

⚠️ **它自己的天花板(不要假裝它是機制)**:餵給解析器的目錄樹是**用快照造的**,
不是真的 `buildCategoryTree` 輸出 ⇒ **驗得了「規則與字典怎麼互動」,驗不了「快照跟不跟得上正式庫」**。

🔴 **本檔【刻意不用行號】指 `parse-search-facets.ts`** —— 那支檔現在有人在改(多顆膠囊那片)
⇒ 行號會漂,而漂掉之後【指錯的行號比死連結難發現】(它會指到一個真實存在但不相干的地方)。
⇒ 📌 判準不是「行號好不好」,是**那支檔現在有沒有人在寫它**。

📎 完整病歷與「怎麼驗一把尺有沒有真的接上」⇒ `docs/patterns/guard-and-instrument-traps.md`
(2026-09-04 三條,搜「複述」與「截斷」)。

---

## 4. 🔴 一列字典可以【完全合法而永遠沒有效果】—— 四種死法

| 死法 | 今天誰在擋 | 失效條件 |
|---|---|---|
| `to` 打錯字 / 分類不存在 | §3 那道閘(解析器回 null) | 分類名快照過期 |
| `to` 的分類**沒有商品** | §3 那道閘 + `SEARCH_CATEGORY_EMPTY_NAMES` | **快照要人重跑**,見 §5 |
| `from` 被**有貨的**分類名前綴搶先命中 | §3 那道閘 | 規則改了 ⇒ 它會紅(那是對的) |
| `from` **帶空白** | §3 那道閘 | —— |

🛑 **最後一種是結構性的,不是資料錯**:`parse-search-facets.ts` 的 `splitWords()` 先用空白切開,
然後 `parseSearchFacets()` 裡那個 `for (let i = 0; i < words.length && category === null;` 迴圈**一個字一個字**(`words[i]`)拿去查字典 ⇒ **字典永遠只會被餵到單一個沒有空白的詞**。
⇒ 📌 `DB Killer` 那種俗稱**是真的**(台灣車友確實這樣講),**而這條路吃不到它**
⇒ **要它得改解析器,不是加一列字典。**

---

## 5. ⚠️ 三個【量不到】—— 不要拿估計去填

```
① 客人實際打了什麼      ⇒ 沒有搜尋日誌(⟦search-NOSEARCHLOG⟧ / backlog #183, parked)
   🔴 而外部研究不能替代它:第一波 4 個候選 0 個過關 —— 那批來源真實、看起來很合理,
      而【沒有一個是客人真的打過的】。第二波 13 個候選, 查無比例明顯上升 ⇒ 判停的依據是報酬率。
② 一個分類【什麼時候變空】 ⇒ products_public 只有 created_at / updated_at,
   **沒有刪除歷史** ⇒ 那不是沒查, 是資料裡沒有痕跡。
③ 俗稱的語意多重性        ⇒ 見 §2, 需要 ① 才答得出來。
```

---

## 6. `SEARCH_CATEGORY_EMPTY_NAMES` 那份快照:今天還準,而它靠人

```
2026-09-04 量:碼裡 29 個 · 正式庫 29 個 · 兩個方向零差
🛑 而那是一把【沒有被壓過的尺】印出來的綠 —— 快照約兩小時前取的,
   而當天唯一一次上架(rizoma)已經包含在裡面 ⇒ 那段時間沒有事件去壓它。
📌 「它今天還準」與「它會一直準」是兩個宣稱, 只量到前者。
```
**形狀**:那 29 個裡 **15 個是父層**(有子分類 ⇒ 結構上不放商品,穩定)、**16 個是葉節點**。
(按 id 算是 31,而名字 29 —— `維修零件` 兩邊都出現,那是已知的同名分類。)

🔴 **而真正安靜的方向不在那 29 個裡** ——是「一個**本來有貨**的分類被清空」。
⇒ ✅ **而那個曝露面比想像小**:字典只指向 **27 個**分類(2026-09-04;**這個數會跟著字典長**)
⇒ **要盯的不是 113 個分類,是字典指到的那些。**

**現在誰在擋** ⇒ 上架 runbook `§5-b`(`docs/runbooks/supplier-storefront-onboarding.md`)。
🔵 **它掛在「寫後驗證」是刻意的** —— 上架/下架/清空價格是唯一會改變這件事的動作,
而 §5 是上架的人**本來就會跑**的一步 ⇒ **它不是多一件要記得的事,是已經在跑的那一步多一行。**
(這個形狀是抄同檔 §6-b 的。)
🔴 **而 `§5-b` 的分母是 `grep` 字典檔【當場長出來的】,不是寫死清單** ——
📌 **一個會自己跟著長的分母,與一份要人維護的快照,差別不是成本,是【會不會過期】。**

⚠️ **而它仍然靠人跑。** 自動化(CI 排程)已量過誤報率:**6 次 / 30 天,而全都發生在有人上架的那天**
⇒ 🔴 **一個在正常操作日會紅的閘,不管它紅得多有道理,都會被關掉** ⇒ 主視窗判**先不做**,
關閉條件寫在板列 `⟦search-SYNONYMDEADROW⟧`。
⚠️ 90 天那個數**不能用** —— 商品資料最早 `2026-06-01`,那一欄大半是首次灌資料不是持續變動。

---

## 7. 證據落在哪(要查原始讀數去這裡)
```
64014acc6  字典 +16 列(第一波外部研究)
252ff6b02  9 列死列修法 + 件數閘        ← 那 9 列是我自己 commit 出來的, commit body 有病歷
2f7563d7   訂正板上「0 列死列」那句
53c57f0c0  字典 +11 列(第二波)+ 空白閘
0b83b119c  服飾 / 傳動 兩列 + 前綴閘分母改成「有貨的」
40e9bfdcb  三道閘 ⇒ 一道【呼叫解析器】的
8834663b4  上架 runbook §5-b
```
板列:`⟦search-SYNONYMDEADROW⟧` · `⟦search-ZEROCAPSULE⟧` · `⟦search-NOSEARCHLOG⟧` · `⟦search-SOPSAYSNOTOPEN⟧`
