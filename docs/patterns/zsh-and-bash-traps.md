# zsh / Bash 陷阱簿(Sean 的 zsh 環境)
> 🔴 **2026-09-02 從 `CLAUDE.md`「終端機 / Bash 紀律」節【原封搬過來】—— 一個字都沒刪、沒改寫。**
> 成因:那 13 條每一條都寫成「規則 + 實測 + 實錘 + 射程」,合計 3,013 字元,而**規則本身**只佔約三成。
>
> 🎯 **`CLAUDE.md` 留 13 條一行條文;實測數字與踩過的坑住這裡。**
>
> 🛑 **這個檔在什麼情況下會【變成假的】**:
> 當 Sean 的 shell 從 `/bin/zsh` 換掉的那一天(多數條目只在 zsh 5.9 實測過),
> 或 `CLAUDE.md` 那 13 條條文被改字面而本檔沒跟的那一天。
> ⇒ 引用前先確認你在哪個 shell:`echo $0` / `ps -p $$ -o comm=`。
>
> 🔵 **查重跑過(2026-09-02, `traps-neighbours.py`, 分母 2,719 則)** ——
> 最高分 0.4836 = memory `reference_exit-code-provenance-three-traps`,**開檔讀過, 判【不併】**:
> 那一份問的是「這個退出碼是**誰**的」, 本檔問的是「這 13 個寫法**各自**會怎麼壞」——
> 兩份在 `PIPESTATUS` 那一格重疊, 而**其餘十二條它沒有**。
> ⇒ 同族相關:`reference_exit-code-provenance-three-traps` · `reference_zsh-eats-ref-colon-path-in-git-show`
> · `reference_measure-tool-dialect-and-silence-traps`

---
## 原文(逐字)

- **zsh 禁忌**:命令內**禁 `#` 註解**(報 command not found)、**禁全形標點「」():;**(報 unknown file attribute);註解寫 prose、不寫進命令。
- 🔴 **雙引號內禁反引號**(2026-08-16 A 窗實錘):`git commit -m "…\`foo\`…"` 的反引號被 zsh 當**命令替換**吃掉 ⇒ **那段字從 commit body 消失**,而**句子還讀得通、只是掉了主詞** ⇒ 比「掉了字」難發現。**commit body / 多行訊息一律用檔案餵**(`git commit -F <檔>`)或單引號。
- 🔴 **`echo` 遇到 `\c` 會【停止輸出】**(2026-08-29 線A 做 `#958` 踩到;主視窗當場複驗) —— **它不報錯、`rc=0`,畫面上只是【少印一行】**。實測 `/bin/zsh`:`echo "\copy tt (a,b) FROM …"` ⇒ **整行一個字都沒有**;`printf '%s\n' "\copy …"` ⇒ 正常。⚠️ **而 `\copy` 是 psql 最常用的 meta-command** ⇒ 這條專打【產生 SQL 腳本】那類工作:少掉的那行 `\copy` 沒跑,而 psql 照樣 `rc=0` ⇒ **「我驗過了」那一發驗的東西整個沒有被執行**。🔴 **它比同族前兩條難發現一格**:反引號那條事後【讀得到】少了什麼、`$?` 那條事後【比得出來】值不對,**而這一條少的那行【從來沒有存在過】—— 檔案裡沒有它,rc 也沒有它**。⇒ **要把含反斜線的字寫進檔案,一律 `printf '%s\n'`,不要 `echo`**;而可機械執行的自檢是**餵給 psql 之前先數一次**(`grep -c '^\\copy' <產生的檔>` vs 你期望的條數)—— **那就是鐵則 11「我餵幾條 vs 它跑幾支」套在【腳本產生】這一層**。⚠️ 只在 `/bin/zsh` 實測過、只講 `\c`(bash 的 `echo` 預設不解讀跳脫序列需 `-e`;而**人在終端機打的那一行是 zsh**,這條打的是那一層、不是腳本層)。
- 🔴 **`$?` 是【每一個指令都會覆寫】的全域變數, 不是「上一個指令的欄位」**(2026-08-25 實測 40 種寫法: 會蓋掉 **40** / 不會 **0**; `/bin/sh`·bash·zsh 三者一致)。~~原句只點名「pipeline 後面」~~ ⇒ 那是 40 個成員裡的 **1 個**, 而**一個看完名單、確認自己不在上面的人, 信心比沒看名單的人更高**。⇒ **唯一安全形狀 `cmd > file 2>&1 ; RC=$?`, 中間不准有任何東西**(連 `X="字串"` 這種純指派都會蓋掉; `A=$?; B=$?` 的 `B` 已經是 `0`), 之後一律用 `$RC`。
- 🔴 **另一個【不同】的病: `local X=$(cmd)` 會【吞掉】rc**(不是蓋掉)⇒ 提前讀也救不回, **必須拆兩行** `local X; X=$(cmd); RC=$?`。**蓋掉 = 晚一步讀不到, 提前讀就救得回;吞掉 = 它從來沒存在過。**
- 🔴 **要管線【每一段】的結果 ⇒ 管線跑完【立刻】整條收進陣列, 中間不准有任何東西**(`PS=("${PIPESTATUS[@]}")`;zsh 是小寫 `pipestatus`)。⚠️ **這一條只能寫成「你必須怎麼做」, 不可以寫成「它會怎樣」** —— bash 與 zsh 行為【相反】(bash 被下一個指令清成 `[0]`, zsh 不會), **任何事實描述一定對其中一邊是錯的**;而本 repo 180 支腳本裡 zsh 是 **0** 支(134 `env bash` / 27 `/bin/sh` / 12 `/bin/bash` / 2 `env sh` / 5 無 shebang)。🔴 **為了拿輸出而把管線包進 `$(...)` ⇒ 它就沒了**(bash 塌成一格且值是最後一段, zsh 變空) —— 而 **bash 那格【不是空的, 它印 0】** ⇒ 逐格檢查非零的守門在那裡會**恆綠**。
- 🔴 **同族: 背景任務的輸出檔會被截斷**(2026-08-25 實測: 一支跑 14 分鐘的只剩 **80 bytes**)⇒ **證據會消失, 而「它到底綠沒綠」事後查不到**。⇒ 要 rc 就寫 `cmd > file 2>&1 ; echo "rc=$?" >> file`。
- 🔴 **輸出的標籤要由【結果】決定,不能無條件印**(2026-08-16 主視窗自己踩):`cmd; echo "(空 = 零命中)"` —— 那行 `echo` 在**有命中時照樣印**,而它就印在命中的正下方。⇒ **要嘛用 `|| echo`,要嘛把判定寫進條件式。**
- **多步驟用 `&&` 串接**(任一步失敗自動停)、**禁裸換行 batch 多命令**。🔴 **而 `&&` 會被一個【合法的零】截斷**(2026-08-25 兩個窗各踩一次):`grep -c "nothere" f` **印出 `0` 而 `rc=1`** ⇒ 後面整串不跑, 而畫面上**只是少印一行**。同族成員:`grep -c` / `grep -q` / `diff` / `test` / glob —— **下一個人踩的會是另一個成員**。⇒ 量測鏈不要用 `&&` 串;要串就每一段自己收 rc。🔴 **同族第三條:在錯的目錄跑 `vitest` ⇒ `No test files found`** ⇒ 濾掉那句之後**畫面全空, 與「零失敗」長得一樣**。
- **「產生新檔→驗證→覆蓋」**:`cat > /tmp/x <<'EOF'` → `test -s /tmp/x || exit 1` → `mv /tmp/x target`。
- **不假設非 macOS CLI 已裝**:`jq`/`yq` 用前 `command -v` 確認、或改 Python 內建。
- **zsh nomatch**:glob 無匹配 exit 1、含 glob 加 `|| true` 或用 `find`。🔴 **而 zsh 【不】對未加引號的變數斷詞**(2026-08-25 `/bin/zsh 5.9` 實測:`for x in $V` 跑 **1 次** / `while IFS= read -r x` 跑 **3 次**)⇒ **迴圈只跑一次而它印出一個乾淨、合理的結論**。⇒ 迴圈一律 `while IFS= read -r f; do … done < 檔案`, **不要 `for f in $VAR`**;路徑一律逐一列出, **禁 `git add $P`**。
  <br>🔴🔴 **而它【壞掉時長什麼樣】—— 這一半 2026-09-03 `-ship` 補, 而它與上面那半【不可分家】**:
  上面講的是**行為**(不斷詞 ⇒ 迴圈只跑一次);**這一段講的是【你會在畫面上看到什麼】**。
  <br>　實例(`-ship` 2026-09-03 05:1x):`npx vitest run $F`(四條路徑塞在 `$F` 裡)
  ⇒ 四條被當成**一個**參數 ⇒ 整發失敗。
  <br>　🎯 **而我接了 `| grep -E "Test Files|Tests"`** ⇒ **失敗訊息走 stderr, 而 `grep` 只吃 stdout**
  ⇒ ⇒ **畫面 = 兩行空白**。
  <br>　🛑 **而【兩行空白】與【跑了而零命中】逐字相同** —— 而**零命中正是我們最常拿來下斷言的那個結果**
  ⇒ 🔴 我差一點把它讀成「這四支測試沒有東西」。**唯一的訊號是 `rc=1`。**
  <br>✅ **判別句(落筆前問)**:**「我這一發的畫面是空的 —— 那是【跑了而沒東西】, 還是【根本沒跑成】?」**
  ⇒ 🔴 **`| grep` 的下游【結構上分不出這兩個】** ⇒ 要分得出來, 得先看 `rc`, 或把 stderr 併進來(`2>&1`)。
  <br>📌 **為什麼寫在這一條底下而不是另開一條**:它是這一條的**驗法那一半** ——
  分家會讓下一個人只讀到「要怎麼寫」而讀不到「寫錯了長什麼樣」(同常載規矩「不得下放判準」)。
  <br>── ── ──
  <br>🔴🔴 **[2026-09-03 `-ship` 盤點:今晚【同一條】被三個窗各踩一次 —— 主視窗-87 派我判「同條還不同條」]**
  <br>```
  同一條(不斷詞)  b407db04 別的窗  `set -- $pair` 拆欄        ⇒ 它自己的 body 寫「第 N 次」
                  -ship          `npx vitest run $F`(四條路徑)⇒ 失敗形狀=兩行空白(見上)
                  -account       `for a in $DONE`             ⇒ 印出一個假的 42(主視窗轉述)
  另一條(nomatch) -ship 今晚三次  `grep --include=*.ts` 被展開 ⇒ 那一發三個計數全 0
  ```
  <br>🎯 **⇒ 判定:【兩者都有】, 而它們是兩條【不同】的規矩**:
  ⇒ **不斷詞那條 = 同一條、三個窗、一夜** ⇒ 🔴 **那不是「zsh 坑很多」, 是【這一條寫在這裡而沒有用】。**
  ⇒ **nomatch 那條 = 另一條, 而它是同一個人一夜三次** ⇒ 也是同一種失效。
  <br>⚠️🔴 **而這個「三」是【下界】不是總數** —— 我的分母是**今晚 commit body 有寫的**
  (`git log --since` 掃 196 顆);**踩了而沒寫進 body 的, 這把尺結構上看不到。**
  <br>⚠️ 而那把尺**還多報過一次**:它撈到 `472a60f1` 的「**中文斷詞**」——
  那是**搜尋的中文分詞**, 與本條零關係 ⇒ 📌 **命中之後要開檔看它在哪種上下文。**
  <br>🛑 **本段【刻意不提修法】**(主視窗-87 指定):候選是「pre-commit 掃 `for X in $VAR`」,
  而**它會不會誤報要先量**(`scripts/` 底下合法的 `for x in $(cmd)` 有幾處?)——
  📌 **沒量之前提修法, 就是板列 `⟦ship-CARRIERDISTANCE⟧` 在講的那個病的下一個實例。**
  <br>── ── ──
  <br>🔴🔴 **[2026-09-03 `-ship` 量完了 ⇒ 結論:【不要做那道閘】, 而理由比「誤報太多」更前面]**
  <br>```
  ① 合法的 for x in $(cmd)(排除註解, 只掃 *.sh)        ⇒ 106 處
  ② 裸變數 for X in $VAR(排除註解, 只掃 *.sh)          ⇒  69 處 / 分佈在 38 支檔
  ③ 那道閘今天會叫幾次 = ②(或 ①+② = 175)             ⇒ **69 次**
     🛑 判準先寫的是「>5 次就不要做」⇒ **69 ≫ 5 ⇒ 不做**
  🔵 負對照 現造一行 `for f in $LIST` ⇒ 尺**叫**(1)  🟢 正對照 一支乾淨的 hook ⇒ 尺**不叫**(0)
  ```
  <br>🎯 **而真正的理由不是 69 這個數 —— 是那 69 處【全部都是對的】**:
  <br>　那 38 支檔的 shebang:`#!/usr/bin/env bash` **31** · `#!/bin/sh` **6** · `#!/bin/bash` **1**
  <br>　⇒ 🔴 **零支 zsh。而 bash / sh 【會】對未加引號的變數斷詞** ⇒ `for x in $VAR` 在那裡**是正確、慣用、不是 bug**。
  <br>　⇒ ⇒ 🛑 **那道閘會 69 次全部誤報。**
  <br>🔴🔴 **而更前面那一格**:`grep -rl zsh --include='*.sh'` 之後逐支看 shebang ⇒ **跑 zsh 的腳本 = 0 支**
  <br>　⇒ 📌 **這條規矩要保護的那個母體, 在【檔案】裡是空的。**
  <br>　⇒ ⇒ 🎯 **它的失效發生在【人在終端機打的那一行】** —— 而那不是一個檔案, **沒有東西可以掃。**
  <br>　⇒ ⇒ ⇒ **所以「做一道 pre-commit 閘」不是誤報率的問題, 是【它掃的population 與病發生的 population 不同】。**
  <br>✅ **⇒ 那第三條路是什麼(主視窗-87 順帶問的)**:兩個候選裡
  <br>　❌ 「提供現成正確寫法讓人抄」—— **對這一格沒用**:出問題的那三次都不是在寫腳本, 是**臨時打一行**。
  <br>　✅ **「讓失敗出聲」是唯一作用在正確時刻的** —— 我今晚那一發是**兩行空白**;
  <br>　　 若它印一行紅字就不會被誤讀 ⇒ 而那正是上面「失敗形狀」那一段的**正面**。
  <br>　⚠️ **而我沒有設計它** —— 「怎麼讓它出聲」還沒量過(候選:命令一律 `2>&1`、或不要接 `| grep`)。
  <br>── ── ──
  <br>🔵 **[同族第三條:尺在量我自己剛餵過的語料 —— 一夜三個窗、三種受詞]**
  <br>　`-ship` `git log --grep=zsh` ⇒ 12 顆, **而前三顆是我今晚自己寫的那三顆 commit**(我把坑寫進 body)
  <br>　`-account` 它的 commit 進了它要查的語料 · 主視窗-87 `/api/search` 那 108 筆**全是我們自己打的流量**
  <br>　⇒ 📌 **三種受詞(commit / log / 流量), 同一個形狀:我量的是我剛剛才餵進去的東西。**
  <br>── ── ──
  <br>🔴 **[同族:而「寫下來」對防止復發的效果, 今晚量到的間隔是【兩顆 commit】]**
  <br>　`-ship` 在 `ba2498c4` 的 body 逐字寫「**命中之後要開檔看它在哪種上下文**」;
  <br>　而兩顆之後的 `1a04653c`(量那道候選閘)**自己踩了同一件事兩次**:
  <br>　　第一發沒分 shell 與 Python ⇒ 分母 **981**;第二發沒排除註解 ⇒ **505**
  <br>　⇒ 而真值是 **69**。🛑 **兩次都往【多報】偏, 而 981 與 505 都是「看起來很嚇人而合理」的數。**
  <br>　⇒ 📌 **⇒ 間隔【兩顆 commit】** —— 而那句話是我自己寫的、我記得我寫過。
  <br>　⇒ ⇒ 🎯 **所以「寫下來」在這一格的效果不是小, 是【零】** —— 它與板列 `⟦ship-CARRIERDISTANCE⟧`
  <br>　　 是同一件事的兩個粒度:那一列問「放到誰會先打開的載體」, 這一格說**連自己寫的、自己記得的, 也擋不住。**
- **CJK / str_replace 切策略**:見常載 `~/.claude/rules/00-work-rules.md` §5(單一權威,此處不重複)。- 🔴🔴 **`git show "$ref:apps/…"` 在 zsh 裡是壞的,而它【安靜地回 0】** —— `:a` 被當成參數修飾符
  (`${ref:a}` = 取絕對路徑)⇒ `HEAD` 變成 `/…/HEAD`、那個 `a` 被吃掉 ⇒ git 噴 `fatal`,
  而 `2>/dev/null` 把它吞了 ⇒ `grep -c` 印出一個**看起來就是答案**的 `0`。**雙引號救不了,要 `"${ref}:apps/…"`。**
  📌 **判別句:`2>/dev/null` 把「我問錯了」變成「答案是 0」⇒ 拿到意外的 0,第一個動作是拿掉 `2>/dev/null` 再跑一次。**
  ⚠️ **本條在這裡只放指標(一處全文、他處指標)** —— 全文含**哪些路徑前綴會安靜壞掉 vs 會大聲報錯**的實測表
  在 memory `reference_zsh-eats-ref-colon-path-in-git-show`,PCM 版與三個實例在
  `docs/patterns/guard-and-instrument-traps.md:18352` / `:18446`。
  🔵 **本行 2026-09-03 由線 `-account` 補** —— 我在這支檔查無而重新發現了一次(`grep -cE 'ref:apps|參數修飾符'` ⇒ **0**),
  跑 `scripts/traps-neighbours.py` 第一名 **0.5338** 就是那份 memory ⇒ 🎯 **這支檔缺的不是那條教訓,是【指到它的一行】。**

## 🔴🔴 heredoc:`<<'PY'` 那對單引號才是保護 —— **「改用 python3」不是**

2026-09-03 `-account` 第四次踩(前三次見 memory `feedback_unquoted-heredoc-ate-the-path-twice-in-one-night`)。
🛑 **而這一次的價值在它推翻了那條教訓自己寫的建議。**

```
那條 memory 的 How to apply 逐字:「寫檔一律用 <<'EOF'(加單引號), **或改用 python3 直接寫**」
🔴 而我【確實用了 python3】—— python3 - <<PY   ⇒ 照樣被咬
   因為我需要把 $M 插進去 ⇒ 所以拿掉了那對單引號
⇒ 🎯 **被吃掉的不是 python 的東西, 是 heredoc 的內文** —— python 根本還沒拿到它
```
📌 **⇒ 保護來自【那對單引號】, 不是來自 python。**
**⇒ 「改用 python3」那句話可以被讀成「python 讓我安全」, 而它不會。**

### ✅ 正解:單引號留著, 變數走 **argv**
```bash
python3 - "$M" "$OTHER" <<'PY'
import sys
p, other = sys.argv[1], sys.argv[2]
PY
```
🔵 **判別句(要拿掉那對單引號之前問一次)**:
> **我是不是為了「插一個變數進去」, 而把【整段內文】的保護拿掉了?**
> 是 ⇒ **那個變數走 argv, 引號留著。**

🛑 **而它壞掉時的樣子照舊**:zsh 印幾行 `command not found` / `no matches found`,
**`rc` 不紅**, 而檔案**寫了一半或掉了主詞** ⇒ 🔴 **一定要回頭 `grep -c '<剛寫的字面>'` 驗。**
✅ 我這次是**有備份 + 逐字比對**才發現的 —— 那兩步不能省。
