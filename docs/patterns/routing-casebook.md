# 路由表病史簿(routing-casebook)
> 🔴 **這份檔是 2026-09-02 從 `CLAUDE.md` 路由表【原封搬過來】的 —— 一個字都沒有刪、沒有改寫。**
> 成因:那張表是每一輪對話都全文載入的索引, 而它 40 列裡有 17 列各自寫成一篇文章
> ⇒ 症狀句(查表真正需要的)只佔 11%, 其餘是病史 / 實錘 / 射程聲明 / 已被訂正的舊字面。
>
> 🎯 **表上留【症狀 + 一行指標】; 病史住這裡。撞到症狀的人先看表, 要細節再翻本檔對應節。**
>
> 🛑 **這個檔在什麼情況下會【變成假的】**:
> 當某一節指的那支腳本 / 檔案被改名或刪掉的那一天(而本檔不會知道)。
> ⇒ 引用任何一節之前, 先 `test -e` 它提到的路徑;對不上 ⇒ 那一節整節待驗。
>
> ⚠️ **而本檔內含【已被訂正的舊字面】** —— 那是刻意保留的(讓搜舊句的人同一發撞到訂正)。
> ⇒ 讀到一個數字時, 先找它下面有沒有一段在推翻它。

---

## §1 · ①寫 rsync / env・secret 操作 / 跨 repo 同步指令<br>②引用其他 session 的 commit body / 跨 session 交接<br>③要在任何規則檔新增條目(立法)前<br>④偵察類工作(凡結論將寫「X 未實作/未覆蓋/查無」)/ 下此類斷言前<br>⑤其他事故式教訓(事故 log;條數會長,當場 `grep -c '^### 12-'` 才算數)

> 指標:docs/lessons-learned.md
> 原位置:`CLAUDE.md` 路由表 第 53 行(2026-09-02 搬出)

①`docs/lessons-learned.md` §12-6/12-8/12-9/12-15(🔴)<br>②`docs/lessons-learned.md` §12-30/12-25/12-16<br>③`docs/lessons-learned.md` §12-34/12-36<br>④`docs/lessons-learned.md` §13<br>⑤`docs/lessons-learned.md` §12(先 `grep -n '^### 12-' docs/lessons-learned.md` 列標題、精準讀命中條)

---

## §2 · 🔴🔴 **要動【後台訂單相關的任何 UI】/ 要對稿 / 要判「我們跟 OD 一不一樣」**

> 指標:<script type="text/plain" id="od-payload">
> 原位置:`CLAUDE.md` 路由表 第 55 行(2026-09-02 搬出)

**OD 專案 `pcm-524f` 底下這兩支,它們是 Sean 本人在改的那一份**:<br>① `HANDOFF-orders-ui.md`(~~4,218~~ ⇒ **2026-08-25 實量 4,309 行**)—— 線A 寫的完整交接<br>② `orders-admin-v2.html`(~~6,730~~ ⇒ **2026-08-25 實量 6,795 行**, `sha256 fc4a24a584e9e7d2…`;⚠️ **「384 個 CSS 變數 / 1,152 條規則」四種數法都複現不出來(196/437/207/1895, 負對照 0)⇒ 數法未確認, 不是數字錯 —— 引用前自己數**)—— **Sean 逐字:「我都是看這個檔案修改的」**<br>路徑前綴 `~/Library/Application Support/Open Design/namespaces/release-stable/data/projects/pcm-524f/`<br>🔴 **為什麼要寫進本表**(2026-08-24 量到):那兩支在 `docs/` 被引用 **0 次**、在本路由表 **0 次**、在四份新窗開場提示詞 **0 次** ⇒ **每個新窗開工時都不知道它們存在,只能自己猜哪份是權威**。2026-08-19 的訂單面板對稿因此比到 `Aug-17 確認稿`,而 Sean 那份是 **Aug-23** ⇒ **比的是舊的,而沒有任何訊號。**<br>🔴 **交接檔寫得再完整,只要沒有一條路從「新窗開工的第一件事」通到它,它就等於不存在。**<br>⚠️ 而 `orders-admin-v2.html:4608` 有一段 `<script type="text/plain" id="od-payload">`(gzip+base64)⇒ **先確認真正生效的是 `<style>` 還是那段 payload,兩者可能不同而看畫面分不出來。**<br>⚠️ **開稿只答得出「稿上怎麼畫」。要答「現在該怎麼做」,還得查【這一格有沒有被後來的拍板動過】** —— 拍板不住在稿裡,住在 memory 與 code 註解裡。**稿本身不會知道自己過期了。**(實例:`W1-064` 差異①,確認稿 08-17 收款排第 1,而 Sean 08-18 拍 `09=甲` 把它搬到第 7)<br> 🔴 **實例二(2026-08-30,板 `:497`)**:稿寫 28px 而畫面 30px —— 那是 **Sean 2026-08-24 拍「後台字級全站放大」**, 晚於稿 ⇒ 以拍板為準。⚠️ 而**照稿改回去會【更糟】**:那條住在 `FIX-27` **一整族**字級規則裡, 只改它 = 畫面變成**第三種**(既不是稿也不是現況)。**詳板 `:497`。**

---

## §3 · ⚠️ **地圖的限制與刷新**(先知道,免得以為它壞了)

> 指標:No matching nodes
> 原位置:`CLAUDE.md` 路由表 第 57 行(2026-09-02 搬出)

**問不出來的三種**:①中文白話問句零命中(2026-08-18 實測「訂單列表的品項在哪裡展開成一列」⇒ `No matching nodes`)②不是節點名的識別字也零命中(同日實測 `itemsTruncated` ⇒ 0)③🔴 **沒有 HTML 可以開** —— 節點數超過 viz 上限,`graphify update` 會**跳過並移除** `graph.html`(同日實測 ⇒ `No such file or directory`)⇒ **只用 `query`,沒有圖可以看**。**刷新** = `graphify update`(增量),**milestone 收尾或每日收工跑一次**、不隨每 slice;⚠️ **本表不記節點數與刷新時間** —— 那種數字寫進常載檔就會過期而過期時零機械訊號,要現值就當場跑它看它自己印

---

## §4 · 🔴 **要知道 OD 稿上某個 CSS 變數【真正生效】的值是多少**

> 指標:docs/design/od-cascade-winning-values.md
> 原位置:`CLAUDE.md` 路由表 第 58 行(2026-09-02 搬出)

`docs/design/od-cascade-winning-values.md` —— 那份稿有 **39 個 `<style>` 區塊**, 含 `data-od-fix="FIX-N"` 後補修正塊 ⇒ 同一個 `:root`、同特異性、**後面那個贏**。這張表列出第一次宣告 / 最後贏的那次 / 贏家在哪個 `FIX-N` 塊。🔴 **2026-08-25:「最後贏家」欄曾有 16 個行號差 1**(同方向、零例外;「第一次宣告」欄 28/28 全中)⇒ **不是稿變動造成的, 是產表當下就錯了**;而該檔逐字寫著「每一行都已個別用 grep/sed 對過原稿(不是套用單一固定偏移量)」—— **它可信度最高的那句話, 正是最不該相信的那句**。📌 **抄值的人不會撞到, 只有想回稿驗證的人才會撞到 —— 也就是唯一會發現它的那種人, 人數最少。**✅ 已於 `897d7fc4` 逐格訂正並釘上稿的 `sha256`。🔴 **`sha256` 對不上 ⇒ 本表所有行號全部作廢, 值也要重算。**⚠️ **它已被訂正兩次**(做它的人自己抓到解析器三個 bug)⇒ **引用前先讀檔內訂正節**。✅ 2026-08-24 夜已用修好的 `tool-final-css.py` **兩側各自獨立重算**複驗過(28 列;顏色正規化後相同 26 / 不同 2, 而那 2 列是已裁決暫停的那組;負對照餵不存在的變數名 ⇒ 兩側皆 None)。🔴 **複驗時多查的那一格值得知道**:兩側都抓不到(皆 `None`)時, 比對結果也印「相同」⇒ 一個【兩側都被漏抓】的變數會安靜地被算進那個 26。這次量到 0, 所以那 26 是真的 —— **而在量之前, 那個 26 在「兩邊都對」與「兩邊用同一種方式錯」印同一個數字。**

---

## §5 · 🔴 **要比對 CSS 的值 / 對 OD 稿 / 查某個 token 現在到底是多少**

> 指標:python3 scripts/tool-final-css.py
> 原位置:`CLAUDE.md` 路由表 第 59 行(2026-09-02 搬出)

`python3 scripts/tool-final-css.py` —— **CSS 會層疊,`grep` 只答得出【第一次宣告】, 答不出【贏的那一次】。**三個函式:`walk()` / `final_values_css()`(吃純 CSS)/ `final_values()`(吃含 `<style>` 的 HTML);自檢六組世界 A-F 含正負對照, **已知盲區寫在檔頭**。🔴 **它不是新寫的** —— 它一直在 `~/pcm-mailbox/` 而沒有人 `find` 過, 2026-08-24 才收進 repo(`5b59575e`)。**一把工具【存在】與【下一個人找得到它】是兩件事。**⚠️ 而 OD 稿那一側同樣會層疊(39 個 `<style>` 區塊, 含 `data-od-fix="FIX-N"` 後補修正塊)⇒ **對稿要問「哪一條贏」不是「稿裡有沒有寫」**;🔴🔴 比「我方第一條 vs 稿第一條」時**兩端錯法相同 ⇒ 它會比出【完全吻合】**, 而那個結論看起來是最好的那一種, 沒有人會回來查。(2026-08-24 實例:三重證據都支持「把 `--primary` 改回稿」, 而開了稿本身才發現 `FIX-10` 用同特異性覆寫成我方現值 —— 改回去才是改壞。)

---

## §6 · 要寫守門/負測/突變 · 要下「零命中/沒覆蓋/構造不出來」這類斷言 · 要判 BLOCKER 前

> 指標:bash scripts/two-controls.sh <純字串> <正對照> <範圍>
> 原位置:`CLAUDE.md` 路由表 第 65 行(2026-09-02 搬出)

🔴 **先跑 `bash scripts/two-controls.sh <純字串> <正對照> <範圍>`** —— **預設純字串, 要正規式加 `--regex`**;它**強制你附正對照**並自帶現造負對照, 且印「掃了幾個檔」。⚠️ **它在 2026-08-31 一夜被修過數次**(假 0 / 比計數不比命中集合 / 沒有出口的紅 …)⇒ 🔴 **引用前一律先讀它的檔頭「它擋不住什麼」那一節, 不要引用這裡的任何數字** —— ~~原句寫「修過三個真 bug」~~ **當天稍晚就已經是四個**(`-15` 量);📌 **⇒ 一個會長的數字寫進常載檔, 它過期時零機械訊號 —— 而【指令不會過期, 數字會】。**🔴 **而這一格自己就是實例**:那個「三」在寫下的當天就錯了, 而它讀起來完全正常。<br>`docs/patterns/guard-and-instrument-traps.md`(恆綠格 / 紅錯地方 / 一發紅多格 / 守門一裝就紅 / 掃描字集比宣稱窄 / 可重跑 vs 不可重跑的證據;每條附 2026-08-14 當天實例與行號)

---

## §7 · 🔴🔴 **我 grep `design-reference` 一個字都沒有 / 對稿對不到 / 「稿裡好像沒有這個元件」**

> 指標:bash scripts/design-ref-check.sh
> 原位置:`CLAUDE.md` 路由表 第 67 行(2026-09-02 搬出)

**先跑 `bash scripts/design-ref-check.sh`(在你要 grep 的那一棵樹底下)。**🔴 **2026-09-02 實量:13 棵工作樹裡【12 棵】的 `design-reference/` 是空的**(只有主樹有;`git submodule status` 前綴 `-` = 未初始化)。⇒ ⇒ 而鐵則 1 逐字要求「寫前台元件前**必先 grep design-reference 字面**」⇒ **那 12 棵的每一發 grep 都回零命中, 而讀的人會讀成【設計稿裡沒有這個】。**📌 **「查無」與「我站在一個空目錄裡」印同一個東西。**✅ 修法一行:`git submodule update --init design-reference`(**每一棵新工作樹各自要跑一次**)。🔵 那支腳本**兩把尺交叉**:尺A `git submodule status` 首字元(`-`=未初始化, **沒有門檻**)· 尺B `git -C design-reference ls-files` 的行數(**空樹 1 / 已 init 176** ⇒ 差得開)。⛔ ~~原本用 `ls \| wc -l` 數檔案~~ —— **那把尺不夠**:同一棵樹 `ls`⇒10 / `ls -A`⇒13 / 真分母 176, **三個都是真的而只有第三個有判別力**。🔴 而**「未初始化時 `ls-files` 會報錯」是假的** —— 我實測它 **rc=0 而回一行 `./`**。⇒ 兩把尺不一致時它回 `rc=1` 而**不猜**。三態:`rc=0` 有分母 / `rc=3` 沒有並印修法 / `rc=1` 說不清楚。自帶 4 格 selftest(fixture 是**真的 git repo**)含**「真的 repo、真的有很多檔、而它不是稿」**那一格。🛑 **而它只守 design-reference 那一半** —— 鐵則 1 的真權威還包含 OD 專案(`list_projects` + 磁碟目錄數對一次)。

---

## §8 · 要下「查無 / 不存在 / 零命中」這種斷言,而對象是【一個檔案路徑】

> 指標:bash scripts/where-is.sh <path>
> 原位置:`CLAUDE.md` 路由表 第 68 行(2026-09-02 搬出)

先跑 `bash scripts/where-is.sh <path>`(四處分開報:工作樹 / dev / 所有 ref(分支・remote-tracking・tag) / 信箱;**查無 rc=3 / 用法錯 rc=2 / 工具自壞 rc=1,三者分得開**)。🔴 **它只查【本 repo】** —— repo root 是從腳本自己的位置推的,不是從你人在哪推的 ⇒ **在別的 repo 底下呼叫它,它會安靜地跑去 pcm-website-v2 找**(2026-08-20 已加方向性警告,而那道警告是提醒不是保護)。跨 repo 的路徑(報價單 / 老闆腦 / design-reference)**不在它的分母裡**。🔴 **`git ls-files` / `test -e` 只看【當前 checkout】,不是整個 repo** —— 未收割分支上的檔在那裡是隱形的(2026-08-17 實錘:一份真實存在的資安報告差點被記成「不存在」;成因全文 `docs/lessons-learned.md` §12-43)

---

## §9 · 🔴 **多窗同時在跑**:①我要回報進度而主視窗會不會忘記我 ②**收到 push 警示:有人推了 dev 而不是我** / 我 commit 了而它上去了沒 ③我要自己 push

> 指標:bash scripts/heartbeat.sh "<窗>" "<剛做完>" "<手上>"
> 原位置:`CLAUDE.md` 路由表 第 73 行(2026-09-02 搬出)

①`bash scripts/heartbeat.sh "<窗>" "<剛做完>" "<手上>"`(主視窗 `--who` 看誰安靜太久;**沒寫心跳的窗在那張表上是隱形的**)<br>②**收到 push 警示時, 去讀 `tail -5 ~/pcm-mailbox/推送預告.md`** —— 每一發推的【起點/預告終點/實際終點/相不相等】。🔴 **主視窗【不會】對各窗發預告**(那是六倍訊息量)⇒ 這道機制靠【你主動去讀那支檔】。🛑 **而在寫下這一句之前, 它沒有告訴任何人要去哪裡讀** ⇒ 於是「沒有預告」與「預告沒送到」在各窗那一端是同一個空白 (2026-09-02 線 `-15` 叫了這一聲, 而其餘五窗都自己去讀所以不覺得空 —— 📌 **一道靠「收訊者會去讀」的機制, 如果沒告訴收訊者去哪裡讀, 對沒讀過它的人等於不存在**)。<br>🔵 讀法:最後一列是 `(推中)` ⇒ 正在推;有實際終點 + `✅ 相等` ⇒ 那一發完成了。🔴 **`PUSH-FAILED` 與「內容沒上去」不是同一件事**:兩發同時推時被拒的那一發印 `PUSH-FAILED`, 而**前一發的內容已經上去了** —— 兩者在那一格印同一個字(2026-09-02 量到)<br>③**不要自己 `git push`** —— 走 `bash scripts/announce-and-push.sh dev`:**先落預告再推**、commit 標題逐顆印出來、失敗留 log、自帶互斥鎖(rc=4=另一發在跑, **零副作用**)。🛑 而那道鎖**擋不住**繞過它直接 `git push` 的窗。<br>🔴 **而這三支在本檔曾出現 0 次** —— 六個窗開工時沒有一條路通到它們, 與本表 `pcm-524f` 那一格同一個病

---

## §10 · ①🔴🔴 **要用瀏覽器打開後台看畫面** / **我跑了那一行而每一頁都 HTTP 500**(⛔ ~~不需要任何 `.env` 檔~~ —— **2026-09-02 實測那句已不成立**)<br>②後台 UI 片的視覺真權威 / BMW M 設計語言 / 某條做了沒

> 指標:/orders
> 原位置:`CLAUDE.md` 路由表 第 78 行(2026-09-02 搬出)

①🔴 **先讀第一個錯誤**:是 `NEXT_PUBLIC_SUPABASE_URL not set` ⇒ **那是缺 env, 不是碼壞了** ⇒ **不要去修後台、也不要回報「後台壞了」**(那會變成一場不存在的火)。🛑 **⛔ ~~「一行就夠, 不需要任何 `.env` 檔」~~ 2026-09-02 `-15` 實測 6 條路徑(`/` `/orders` `/settings` `/settings/staff` `/products` `/customers`)⇒ **6/6 全部 500**, 每一頁 HTML 都命中那個 env 名 ⇒ 我【沒有找到還活著的頁】**(而我沒窮舉所有路由 ⇒ 正確說法是「我試的 6 條全掛」)。✅ **要看訂單/首頁/員工那幾頁 ⇒ `bash scripts/admin-probe/up.sh`**(拋棄式 PG + PostgREST + 種子;收攤 `down.sh`)⇒ 🟢 同夜實測四頁全 200、零錯誤字樣、🟢 負對照不存在的頁 404。⚠️ 而它是拋棄式庫 ⇒ 證的是【碼跑得動】不是【正式資料下也對】。⇒ 舊那條路的原文仍在 `docs/design/admin-design-system.md` **檔頭第一段** —— 一行 `cd apps/admin && ADMIN_DEV_BYPASS=1 npx next dev -p 3001 -H 127.0.0.1`。**這條路一直都在**(`proxy.ts:16` 註解寫著用途),而 2026-08-16 全窗花了兩輪才找到 —— **寫對地方 ≠ 會被讀到**<br>②`docs/design/admin-design-system.md`(**檔頭「落地狀態」表決定你能不能直接照抄下面的東西**)

---

## §11 · ①🔴🔴 **要驗「這個功能到底行不行」**(Sean 2026-08-17 定為全陣預設:「不用再用 artifacts,直接來真的但是開伺服器做＋看」)<br>②🔴🔴 **本機 dev server 打開了,而畫面「看起來像功能沒做」**(按鈕沒反應 / 停在載入中 / console 有 chunk 403)

> 指標:docs/runbooks/local-admin-with-real-data-probe.md
> 原位置:`CLAUDE.md` 路由表 第 79 行(2026-09-02 搬出)

①`docs/runbooks/local-admin-with-real-data-probe.md` —— 拋棄式 PG + PostgREST + 自簽 JWT + 前綴代理 + 真後台,**零 secret、不碰 `.env*`**(施工窗工作樹沒有 `.env.local`)。含 5 個會擋住你的坑(UTF8 / socket 長度 / `auth.uid()` / `service_role` GRANT + BYPASSRLS / `/rest/v1` 前綴)與**效度限制**。⚠️ **替身(fixture / mock / artifact)不再是「確認功能可用」的合格載體**<br>②**先換 `http://localhost:<port>`,不要用 `http://127.0.0.1:<port>`,再看一次。** Next 16 dev 只認 `localhost` 這個 `Origin`;用 127 時 HTML/CSS 正常、**只有 client JS 靜靜地不見**。量測與限定見 `docs/runbooks/local-admin-with-real-data-probe.md` §9(2026-08-18 G3 量;機制名稱未確認、只量過 storefront)

---

## §12 · 🔴 **要在本機開【顧客站】來看或量畫面**(手機版面 / 走一遍客人動線 / 購物車結帳)

> 指標:bash scripts/storefront-probe/up.sh
> 原位置:`CLAUDE.md` 路由表 第 80 行(2026-09-02 搬出)

`bash scripts/storefront-probe/up.sh` 一行起完整鏈(拋棄式 PG + PostgREST + `/auth/v1` 替身 + 種子商品 + storefront dev:3020),收攤 `down.sh`(逐項 pgrep + 逐埠 lsof 驗死)。量版面用同目錄 `overflow-ruler.mjs`(**每次量測自帶三條自檢,`selfCheck.ok=false` ⇒ findings 作廢**)。效度限制照 runbook §5/§8-f 不放寬。🔴 **而這兩支 probe 的 `REPO` 來源【不一樣】,不要從一支外推到另一支**(2026-08-26 線1 發現、主視窗複量):`scripts/storefront-probe/up.sh:43` 是**寫死** `REPO=/Users/sean_1/pcm-website-v2` ⇒ **在自己的 worktree 裡呼叫它,它會安靜地跑去主樹**;而 `scripts/admin-probe/up.sh:47` 是 `REPO="$(cd "$(dirname "$0")/../.." && pwd)"` ⇒ **從腳本自己的位置推** ⇒ **從 worktree 呼叫它就用 worktree**。⇒ **「換一棵工作樹來避開 `.env.local`」這個解法對 admin 那支有效、對 storefront 那支無效。**(runbook 檔頭那段警告只點名 storefront 那支,而讀的人很容易外推成兩支。)<br>🔵 **2026-08-30 更正(Sean 拍「好」;只加不刪,舊字面留著讓搜舊句的人同一發撞到這裡)**:上面那句 ~~「`scripts/storefront-probe/up.sh:43` 是**寫死** `REPO=/Users/sean_1/pcm-website-v2`」~~ **已經不是現況** —— `9603b601`(2026-08-29 15:13,線I)改掉了。**現在 `:54` 是 `REPO="$(cd "$(dirname "$0")/../.." && pwd)"`,與 `admin-probe/up.sh:47` 【逐字相同】** ⇒ ✅ **兩支現在【一樣】,換工作樹對【兩支都有效】。**(`-b9` 2026-08-30 自己開檔量的;而 `up.sh:14/15/51` 都留著加刪除線的舊字面 ⇒ **那支檔自己是誠實的,舊的是這張表。**)<br>🔴 **而這一格舊掉的方向是最糟的那一種**:它會叫人【放棄一個其實可行的解法】 —— 一句過期的「這條路走不通」,比一句過期的「這條路可以」更貴,因為**沒有人會去驗證一個勸退。**

---

## §13 · 🔴 **①三綠印了綠而我不確定它真的跑了 / build 紅了而找不到錯誤行**<br>②**這件事我接了,而它是不是已經被做掉了 / 板子那一列還成不成立**<br>③**測試說「找不到建置產物」/ 我明明 build 過了它還說沒有**

> 指標:bash scripts/greenlight.sh
> 原位置:`CLAUDE.md` 路由表 第 86 行(2026-09-02 搬出)

①`bash scripts/greenlight.sh`(加 `--tests` 連測試)——每道自己收 rc,**~~三態~~ ⇒ 2026-09-02 起【四態】`GREEN(0)/RED(1)/ENV-FAIL(2)/PARTIAL(4)`**(`7829924d`, 線 `-15`)—— 🔴 **`PARTIAL(4)` = 沒加 `--tests` ⇒ 它【不准印那個綠字】**。成因是量到的:2026-09-02 CI 在 `dev` 上紅了至少三發而全隊沒有人發現, 因為大家跑它都沒加 `--tests` ⇒ 它印 `GREEN` 而 `tests=skip`;而**它旁邊逐字寫著「skip 是沒跑不是綠」—— 有人讀了兩次, 兩次都走過去**。📌 **⇒ 一個印 `GREEN` 而測試沒跑的輸出, 不管旁邊寫什麼, 都會被讀成綠** ⇒ 這是機制優先律, 不是紀律。⚠️ 而既有三個號碼**一個都沒動**(`latest-definition-of.sh:77` / `md-table-overflow.py:886` / `migrations-replay-from-zero.sh:11` 三支的註解引它當先例 ⇒ 改號碼會讓那三支的註解變成假的);🔴 **看到 `ENV-FAIL` 不要查你的碼,那是工具自己跑不起來**;非綠時 log 目錄留著並印落點;`tests=skip` 是**沒跑**不是綠。⚠️ **射程:126/127 那一族它分得出來,其他故障形狀沒演過,仍會落到 `RED`**<br>②`bash scripts/is-this-still-true.sh "<那一列的錨或關鍵字>"` —— 五段:①有人 commit 修掉了嗎 ②板上那一列**現在的態** ③字面還在碼裡嗎 ④**它下面有沒有一段就地訂正在推翻它** ⑤mailbox 有沒有人交過更正;並印 paste-ready 量測戳(時間+HEAD+**工作樹幾項未 commit**)。⚠️ **射程:掃不到別的 session 的對話 / 正式庫現況 / OD 稿 / 別的 repo**<br>🔴🔴 **而 2026-09-02 量到一格更近的:它的 ② 段 `grep` 的是【整行】⇒ 只是【引用】那個錨的列也會被撈進來** ⇒ **它的輸出不能當答案讀**。實測:一列我知道 `done` 的(`⟦b4-PURGE1⟧`)⇒ 印 **7 列**, 而**第一列印的是 `open`**;一列我知道 `open` 的 ⇒ 印 4 列, 其中兩列是雜訊(欄位錯位 / `>` 引用行)。📌 **⇒ 兩個世界確實印不同的東西(它有判別力), 而【第一列看起來就是答案】⇒ 照第一列讀會讀反。**✅ **修法:配一把只讀【錨欄】的第二把尺**(每一列只命中 1 列, 不是 7 列;🟢 正對照兩個世界各答不同 · 🔵 負對照現造錨 ⇒ `[]`)。🛑 **而它答的是【板上那一列的態】, 不是【那件事做完了沒】** —— 它看得到「有人 commit 了碰非 docs 檔的東西而訊息提到那個錨」, **看不到那是【做掉它】還是【只是提到它】**(2026-09-02 實錘:5 顆裡只有 2 顆真的碰了那件事)⇒ 那一格要開 diff。<br>③`apps/<app>/.next/BUILD_OK`(由 `scripts/build-with-stamp.sh` 先刪後寫、**rc=0 才寫**)—— 成因是量到的:**`next build` rc=1 而 chunks 仍被寫出 28 個檔** ⇒ 產物存在在「成功」與「失敗但寫了一半」印同一個綠。⚠️ **射程:判紅【只判】沒有成功 build 過;HEAD 不同【只警告不判紅】**(戳記會被 turbo 快取 replay,比 HEAD 判紅會變成常態假紅)<br>🔴 **三支都自帶 `--selftest`(含負對照+突變),而它們的輸出都印【自己的射程】—— 因為一個防「訊號沒帶分母」的工具,自己的輸出若沒帶,它就是下一件事故。**

---

## §14 · 🔴🔴 **我新寫了一支帶 `--selftest` 的腳本,而它在我自己樹上跑是綠的** —— 🛑 **那句話本身就是觸發條件,不是通過條件**

> 指標:.husky/selftest-git-isolation-gate.sh
> 原位置:`CLAUDE.md` 路由表 第 87 行(2026-09-02 搬出)

**如果那支 selftest 會碰 `git`:進入點【第一件事】剝掉繼承來的 git 環境**(`GIT_DIR` / `GIT_INDEX_FILE` / `GIT_WORK_TREE` / `GIT_OBJECT_DIRECTORY` / `GIT_ALTERNATE_OBJECT_DIRECTORIES` / `GIT_COMMON_DIR` / `GIT_NAMESPACE`)。<br>🔴 **為什麼**:`GIT_DIR` / `GIT_INDEX_FILE` 的優先權**比 `git -C <路徑>` 高** ⇒ **你 selftest 裡每一發 `git`(包含帶 `-C` 的)都會去讀寫別人的 repo。**<br>🔵 **一次剝乾淨,不要逐發包** —— `-15` 2026-09-02 逐點突變量到:**只覆蓋 6 個呼叫點裡的 4 個時,那道閘底下【仍然全綠】** ⇒ **一個只修對一半的修法看不出來。**<br>🛑🛑 **而最毒的一格**:這族的東西**在自己的樹上手跑 `--selftest` 是全綠的**(`-f3` 跑過四次,四次都綠)⇒ **它只有掛在 `.husky/selftest-git-isolation-gate.sh` 底下才壞** ⇒ 📌 **所以「我測過了」在這一族裡零判別力 —— 測試環境與受害環境的差別,就是那個病本身。**<br>✅ **驗收要跑兩世界 + 突變正對照**(只貼一個「修後乾淨」不算):`design-ref-check.sh`(`-15`)修前受害者 `1\|0 ⇒ 4\|6` · `what-happened-to.py`(`-f3`)修前 `rc=1` 且 `1\|0 ⇒ 3\|4`,修後兩支皆 `rc=0` 且 `1\|0 / 1\|0`;🟢 **突變副本(只停掉剝環境那三行)仍然 `1\|0 ⇒ 3\|4`** ⇒ **證明是那個修法在擋。**<br>🔵 **兩支同族、同一夜、兩個作者** —— **而兩支的作者都以為自己驗過了。**

---

## §15 · 🔴 **板上寫著 `open` 而我不確定 · 有人跟我說「那件早就做掉了」而我想自己驗 · 我要說「這件沒有人做」之前**

> 指標:python3 scripts/what-happened-to.py <錨>
> 原位置:`CLAUDE.md` 路由表 第 88 行(2026-09-02 搬出)

`python3 scripts/what-happened-to.py <錨>` —— 餵一個錨,印【commit 清單 + 每顆動的非 docs 檔 + 那些檔今天含該錨幾次】,**它不判斷,只把三格擺出來**。🛑 **射程印在它自己的輸出裡**:**「把【開檔】變便宜,不是變成不必要;沒帶錨的 commit 它一樣看不到。」**<br>🔴 **為什麼需要它**(2026-09-02 量到):`git log --grep=<錨>` 判給我 **20 列「有人動過」**,而逐支開檔之後 **4 列的 commit 與那件事完全無關** —— 而那 4 個假陽性**只來自 2 顆 commit**(一顆就灌了 3 列)。📌 **成因:`--grep` 讀的是 commit【訊息】,它分不出【做它的】與【提到它的】。**<br>🛑🛑 **而它答不出這四種的差別 —— 四種在 `git log` 上長得一模一樣**:**①修好了 ②讓它看得見(而它還在發生)③把入口關掉(而它還在那裡)④決定改在別的地方解(而這裡永遠不會有碼)**⇒ 🔴 **④ 的證據常常只有一句註解** ⇒ **本支會把它印成「0 次」,而那不是「沒做」**(實例:TapPay 端點限流 = **Vercel WAF、上線前置、Sean `Q1=A`**,碼裡逐字寫著「本 route 不寫 code 限流」)。<br>⚠️ **而【錨比工作晚出生】時,那個 0 是結構上保證的**:2026-09-02 實測某 15 個錨裡 **14 個是當晚才鑄的** ⇒ **沒有人能把一個還不存在的名字寫進碼裡** ⇒ 🔴 **那種 0 不是證據,而正對照救不了它。**<br>🔴🔴 **而 2026-09-02 晚量到【漏多少】—— 而那是這一格原本缺的東西**:同一件事, 錨那把尺 vs 路徑那把尺 ⇒ `⟦b4-MAILCOPY1⟧` **1 vs 16(×16)** · `⟦b4-MAILCANCEL1⟧` **3 vs 10** · `⟦b4-PFEDDL2⟧` **1 vs 4** · `⟦b4-MAILDEAD⟧` **3 vs 3(相同)**(🔵 負對照 現造路徑 ⇒ 0 ⇒ 路徑那把尺不是恆真)。📌 **⇒ 射程聲明只講了【方向】(「看不到」), 而沒講【量級】—— 而一個只有方向的警告, 讀的人會自己填一個量級, 而他填的一定是小的。**🎯 **⇒ 「可能漏幾顆」與「它看到的是十六分之一」是兩句不同的話。**🛑 **而【不要把任何一個倍數當係數】**(2026-09-02 `-f3` 提出, 我採納):`⟦f3-HALFWRITE1⟧` 也量到 ×16, 而它與 `⟦b4-MAILCOPY1⟧` 的 ×16 是**巧合** —— 兩個分母的組成完全不同(19 支 `rpm-*.ts` vs 信件文案)。🎯 **有意義的是【量級】, 不是那個數;把它當係數會讓下一個人拿 ×16 去估一件沒量過的事。**🔴 **而 `×1` 那一格同樣要留著**:它證明**不是每次都漏** ⇒ 所以人會從那一次推出「大致準」。<br>🔴 **成因是結構性的, 不是紀律**:`⟦b4-PFEDDL2⟧` 那 4 顆帶的錨是 `⟦b4-NONCARDPAID1⟧` 2 · `⟦b4-MANREFUNDNOOWNER⟧` 2, 而它自己只 **1** ⇒ **做那件事的 commit 大多掛在【別人的錨】底下** —— 一顆 commit 只掛一個錨, 而它常常同時推進三件事。⇒ 🛑 **所以「把錨字面補進板」只是止痛。正確用法:錨當入口, 【路徑當分母】—— 兩把一起, 永遠不要只用一把。**<br>✅ **第二把尺就一行**:`git log --oneline -- <那件事真正的檔案路徑或目錄>`;🔴 **而「真正的路徑」要從板上那一列的【內文】推, 不是從錨名推**;兩個數字**並列寫**, 不要挑一個。<br>🛑🛑 **而最該記的一句是 2026-09-02 `-7d` 自陳的**(它讀過射程聲明, 而仍然把輸出寫成「commit 0 顆」):**「一道印在工具輸出裡的射程聲明, 對【已經拿到一個符合預期的數字】的人是無效的。」**📌 **⇒ 不是人不讀射程 —— 是【符合預期的那一刻, 射程被讀成不適用】。**🔴 **而同夜的實錘**:`⟦f3-COUPONNOTBUILT1⟧` 被判「牆 0 · commit 0 · 碰碼 0 ⇒ 真的沒有人碰過」, 而實量 **7 支 migration · 19 支碼 · 20 顆 commit**(那 20 顆帶的是別的 3 個錨)。🎯 **⇒ 而三個 0 看起來比一個 0 更像證據 —— 而它們不是三個獨立來源, 三格全由同一把尺產生 ⇒ 一個來源壞掉, 三格一起壞。**

---

## §16 · 🔴 **我要端一題給 Sean 之前**(任何決策題、任何「要他拍板」的東西)

> 指標:bash scripts/before-asking-sean.sh "<他會講的話>"
> 原位置:`CLAUDE.md` 路由表 第 89 行(2026-09-02 搬出)

`bash scripts/before-asking-sean.sh "<他會講的話>"` —— 五發一次跑完:①有人寫碼做掉了 ②他答過了(決策板)③答案寫在碼的註解裡 ④寫在 `COMMENT ON COLUMN` 裡 ⑤**那段話下面有沒有一段在推翻它**。🔴 **關鍵字用【他會講的話】不是我們的編號** —— 他答「不用改名,依照現在」時不會提 `⟦b4-2PAPERS⟧`。🔴 **為什麼是 script 不是規則**:2026-08-29 驗那 7 條「要他拍板」的題,**5 條前提已不成立,而 5 條的答案全都寫下來了**,只是住在五個不同載體 ⇒ **沒有一條是查不到,是答案落在提問者不會去的地方**;而把它寫成規則會變成第六種同型病(寫下來了、grep 得到、沒有人在端題前去讀)。**自帶 `--selftest`(正對照「不用改名」/ 負對照現造字面),而它只擺證據不下判斷。**⚠️ 掃不到的三樣印在它自己的輸出末尾(別的 session 的對話 / 正式庫現況 / OD 稿)

---

## §17 · 🔴 **我在自己的工作區跑全套測試, 紅了一堆不像我弄的 / 有人說 CI 綠而我這邊紅**

> 指標:bash scripts/why-is-this-red.sh <vitest log>
> 原位置:`CLAUDE.md` 路由表 第 92 行(2026-09-02 搬出)

`bash scripts/why-is-this-red.sh <vitest log>` —— 🔴 **同一顆 commit, CI 紅 6 支而乾淨 worktree 紅 12 支:差別不是「哪些測試」, 是【CI 會先 build】。**⇒ 任何窗在自己的 worktree 跑全套件都會看到 6 支不屬於它的紅, **而沒有任何訊號說那是假的**。本工具把紅分兩堆(沒 build 造成的 / 真紅), **判不出來一律歸真紅(fail-closed, 可能多報不會少報)**。自帶 `--selftest`(5 格, 正 3 負 2), 並與 CI run `33382956618` 實際結果交叉驗過(diff 空;🟢 正對照 拿掉一支 ⇒ 會報不同)。⚠️ **它只演過兩種 build 紅形狀** —— 第三種出現時它會安靜地歸成真紅。⚠️ 它**不驗**「build 完真的會綠」—— 它答的是「這個紅是不是我造成的」, 那是兩個問題。

---

## §18 · 查「某個坑我們記過沒」<br>🔴 **或:我剛寫了一道守門 / 一個門檻 / 一個正對照 —— 在 commit 之前**

> 指標:scripts/traps-neighbours.py
> 原位置:`CLAUDE.md` 路由表 第 84 行(2026-09-02 第二輪搬出)

`scripts/traps-neighbours.py` —— 🔴 **直接 `grep` 正本會漏**:它比對的是**鄰居**不是字面,而同一個坑在不同載體上用的字不一樣(2026-08-23 實錘:兩套誠實的字集各自漏掉對方三分之二)。🔴 **而它吃【草稿檔路徑】不吃關鍵字** —— 把你要記的東西寫成三行草稿再餵它;路徑錯時它會明說「這是路徑錯不是查無近親」, **不會安靜地回零命中**。🔴🔴 **而第二個觸發情境是 2026-09-02 量出來的**:`-0e` 寫了一道掃描守門、漏掉整個 `scripts/` 目錄, 修完把心得寫成 memory ⇒ 而那把尺**第一名就撈到它**(分母 2684 則), **第 6 名(0.1897)是三天前 `-c8` 就寫好的同一條母題**:「守門的門檻是一個【下界】, 而它對【分母變小】完全失明 —— 而分母變小正是重構最常做的事」(現場 `toBeGreaterThan(10)`, 而 `-0e` 那道閘逐字 `-ge 10` ⇒ **同一個形狀、同一個門檻、同一個失明方向**)。📌 **⇒ 它不是「發現」了, 它是重新推導了一條已經寫好的母題。**🔴 **而那條沒有在路口叫它, 原因不是懶** —— 逐字:「**我以為我在【修一個 bug】, 而不是在【記一個坑】**」⇒ **一個正在修 bug 的人, 不會覺得自己在查坑** ⇒ 所以觸發情境必須寫【寫守門】這個動作, 不能只寫「查坑」。⚠️ 而 0.1897 只排第 6 ⇒ **只看前三名就仍然不會知道那條存在** ⇒ 整份名單要看完。

---

## §19 · 🔴 **列印出來的紙【多了一頁】/ 紙上看不到頁碼 / 換一台印表機就不一樣 / 「另存 PDF 是一頁而真印表機是兩頁」**

> 指標:apps/admin/src/app/print/print-a4.css
> 原位置:`CLAUDE.md` 路由表 第 85 行(2026-09-02 第二輪搬出)

`apps/admin/src/app/print/print-a4.css` 檔頭那段。🛑 **引用前【往下讀完整段】** —— 那一段裡「改前」與「改後」住在一起, 而【改前先出現】。🔴 **而那支檔【已經明寫了更正】**:`:96` 逐字「2026-08-23 更新:上面那個『下 5.1mm』已經被修掉了, 原句留著不改」⇒ **它就在改前那一行的 14 行之下, 而 2026-08-29 兩個窗仍然連續引錯同一格** ——一個引了改前的數字、另一個核了「那個字面在檔裡」而沒回檔驗它是不是現況。📌 **⇒ 失效的不是那個標記, 是【grep 到第一個命中就停】**:兩個命中在同一支檔, 而只有第二個是現況。<br>**① 頁碼離紙邊多遠**:~~2026-08-18 量到 **5.42mm**, 落在硬體不可印帶 4–6mm 的正上緣~~ ⇒ 🔴 **2026-08-23 已修**(頁邊框改 `vertical-align:top`)⇒ **10.50mm**;**2026-08-29 線A 獨立複量 ⇒ 10.2mm**(不同 dpi 與工具, 差 0.3mm)⇒ ✅ **今天對 4–6mm 帶有約 4.5mm 餘裕, 不在危險帶上**。🔴 **兩個數字都留著是刻意的** —— 它告訴下一個人【這一行被移過, 不要移回去】;而後果不是難看:**員工不知道這張單有第 2 頁**。⚠️ 而那段自己最後一行逐字「**仍然未驗**:真的用實體印表機印一張、唸出紙上那一行字」⇒ **那一格至今未驗**(所有量測都是 PDF 的墨水位置, 而不可印區是印表機的)。<br>**② 為什麼會多一頁**(2026-08-29 線A):`.pd-sheet` 的 `min-height` + 頁尾 `margin-top:auto` ⇒ **內容永遠恰好等於那個值, 不論幾項** ⇒ **餘裕是零**。⛔ ~~當時是 271mm/1024px, 而可印高 269mm 就讓只用 68% 的那張變 2 頁(2mm 懸崖)~~ ⇒ 🔴 **2026-08-29 已改成 250mm/945px**(Sean 那台 HP 的可用高 ≥252mm ⇒ 他那台不再發生)。🛑 **而機制【沒有】被修掉 —— 懸崖只是從 271 搬到 250**:可印區低於 250mm 的裝置上會一模一樣地再發生。**細節與怎麼重算見 `docs/design/print-sheet-page-thresholds.md`。**<br>🔵 **2026-08-30 補充(Sean 拍「好」;只加不刪)**:上面那句「懸崖只是從 271 搬到 250」**講的是【短的紙】** —— 內容比 `min-height` 矮、由 `min-height` 撐出高度的那一種。⇒ 而**長的紙**(6 項以上)內容自己就超過 271mm ⇒ **`min-height` 250 從頭到尾沒有參與** ⇒ **那種紙的懸崖在 271,不在 250。**📌 **⇒ 271 與 250 是【兩個機制的兩個數字】,不是同一個數字被搬過。**實量(線G 2026-08-30):5 項 261.7 ✅餘裕 9.3 / 6 項 274.3 🔴超出 3.3 / 7 項 286.9 🔴超出 15.9;每多一項 +12.6mm;可印高 271 = 297 − 12 − 14(`print-a4.css:35-37` 的 `@page margin: 12mm 12mm 14mm 12mm`,`-b9` 複量)。✅ **而 `print-a4.css:196` 自己逐字寫著同一件事**:「`min-height` 只是【最小值】,內容長的時候紙照樣長,**分頁由 `@page` 決定**」。⚠️ **而【硬體不可印帶】那一格不合併**:`[252, 265.6)` 是 Sean 那台 HP 的線,不是 CSS 的線。⇒ **三條線,不要併成一條。**<br>🔵 **2026-09-02 03:1x 更正(線 -15 兩把獨立的尺實跑, 主視窗落檔;只加不刪)**:上面那組 mm 數是 **08-30 的模型算的**, 而今天**實跑量頁數**的結果對不上 —— 而更重要的是**它漏了一層分類:出貨單有【兩種】**。<br>🔴 **分支A**(每一項佔**兩列**:本次出貨 + 尚未出貨)⇒ 曲線 `1⇒1 2⇒1 3⇒1 **4⇒2** …13⇒2 14⇒3` ⇒ **門檻在 3⇒4**。**⇒ 一張 4 項的單就已經是兩頁, 而員工天天遇到。**<br>🟢 **分支B**(無「尚未出貨」表)⇒ `5⇒1 6⇒1 7⇒1 **8⇒2**` ⇒ 門檻在 7⇒8。<br>🎯 **⇒ 所以「6 項變兩頁」對【分支A】成立而對【分支B】不成立 —— 而舊字面沒有這一層, 讀的人會拿錯的門檻去判。**<br>🔵 尺1 `apps/admin/src/app/print/page-measure.test.tsx`(playwright `page.pdf`, 數 `/Type /Page`)11/11 綠 · 尺2 `sh scripts/pagecount.sh`(headless Chrome `--print-to-pdf`)⇒ **兩把一致**。<br>🔴 **而真正的成因不是「每項多重」**:1 項那張就已經 945px 而可印高 1024px ⇒ **只剩 79px, 裝不下一項(95.5px)**;而那 945 **不是內容**, 是 `.pd-flow` 自己的 `min-height`(250mm = 944.882px), 子塊實高合計只有 650px ⇒ **1/2/3 項印出來一樣高, 都是被地板撐的**。<br>🔵 **一項 = 25.3mm(分支A 兩列)· 一列 = 12.6mm** ⇒ 舊字面那個 12.6 是**半項**, 兩個數字都對, 差在單位。<br>✅ **而 `:48` 那句「`@page` 邊距框我未實測」今天實測掉了**:真出貨單分支A 6 項, 第 1 頁「第 1 頁 / 共 2 頁」、第 2 頁「第 2 頁 / 共 2 頁」(🟢 正對照最小 HTML 有 `@bottom-center` ⇒ 命中 · 🔴 負對照拿掉它 ⇒ 兩頁零命中)⇒ **Chrome 真的會渲染 `@bottom-center`。**<br>⚠️ **仍未驗**:真印表機那一格照舊(全部是 PDF 的墨水位置, 而不可印區是印表機的);而所有門檻都在**本 fixture 的品名長度**下量的, 真品名會讓它漂。<br>🛑 **而唯一實測有效的一刀是【拿掉「尚未出貨」表】**(縮 padding 買不到一項;壓掉 LINE 區只買到一項)——而它**不是版面題, 是拿掉一整塊資訊** ⇒ **那是 Sean 的題**, 已端(見信箱決策頁)。<br>📌 ⇒ ⇒ **而這一格自己又是一次實例:一組【模型算出來的】數字被讀成量到的, 而它旁邊沒有寫它是算的。**<br><br>📌 **⇒ 本行觸發情境刻意寫【症狀】不寫【主題】** —— 沒有人會去查「列印邊界實測」, 而每個人都在查「為什麼變兩頁」。

---

## §20 · 🔴 **要抄一支既有的 DB 函式來改 / 要寫 `CREATE OR REPLACE` 之前 / 有人給你一個「那支函式在這裡」的行號**

> 指標:bash scripts/latest-definition-of.sh <物件名>
> 原位置:`CLAUDE.md` 路由表 第 90 行(2026-09-02 第二輪搬出)

`bash scripts/latest-definition-of.sh <物件名>` —— 列出它在 `supabase/migrations/` 裡**每一次**被定義的版本號+行號+形狀(`create`/`cor`/`drop`/`alter`),末尾印**兩個具名的答案**:`newest`(repo 裡最後一代)與 `live`(**帳本 `APPLIED.tsv` 上最後一支已記的**),**兩者不同時它自己出聲**。🛑 **而 `live` 答的是【帳本】不是正式庫** —— 「帳上寫著 apply 了」與「正式庫裡那支函式真的是那一版」**是兩個宣稱**,它答不出後者(那要連正式庫讀 `pg_proc`)。🔴 **為什麼觸發情境寫【症狀】不寫主題**:沒有人會查「最新一代查詢工具」,而第三個觸發情境(**接到一份交件裡的座標**)是最常發生的那一種 —— **那個座標在寫的時候是對的**。⇒ 錯抄一代 = `CREATE OR REPLACE` 把後面幾代的行為**整個回捲**,而**三綠不會紅、diff 上長得像一支正常的新 migration**。⚠️ **成因三例由線C `-b4` 2026-08-30 交(`fa11d8c4`), 本窗【未複量那三例】**;本窗實跑複核的是:工具存在且可執行、`--selftest` **十四格 rc=0**、對 `admin_create_manual_order` 印 `newest=20260829140000` / `live=20260824020000`(與本窗獨立量到的 `APPLIED.tsv` 0/1 一致 —— ⚠️ **而那仍是同一本帳本被讀兩次**)。

---

## §21 · 🔴 **客人打電話說「我收到【兩封不一樣的】出貨通知」/「到底哪一個追蹤號才對」/「你們是不是出了兩次貨」**

> 指標:docs/runbooks/duplicate-shipping-email-sop.md
> 原位置:`CLAUDE.md` 路由表 第 71 行(2026-09-02 第三輪搬出)

`docs/runbooks/duplicate-shipping-email-sop.md` —— 🔵 **這件事是我們知道的、而且是刻意接受的**(Sean 2026-08-31 拍甲, 原話「我用一個**常態的病**換一個**極少數的病** —— 不是零代價, 你要知道」;換掉的是「客人一封都收不到」)。🔴 **而最重要的那一格是【我們查不出來】**:系統裡留下的痕跡**與一切正常的情況長得一模一樣** ⇒ 客服不能靠後台確認客人有沒有踩到它 ⇒ **相信客人、不要承諾去查、把那一通記下來**。📌 **而那通電話是目前【唯一】的發現路徑 —— 沒有任何一支監控會叫。**⚠️ 而「客人說貨其實沒出」是**另一條路**, 處理不同, 見該檔末節。

---

## §22 · 🔴 **客人來要求「查我的資料 / 刪掉我的資料 / 不要再寄信給我」**

> 指標:docs/runbooks/data-rights-sop.md
> 原位置:`CLAUDE.md` 路由表 第 72 行(2026-09-02 第三輪搬出)

`docs/runbooks/data-rights-sop.md` —— 🔴 **隱私政策 `legal-content.ts:199` 已經對客人承諾五種權利、行使方式=客服三管道,而那頁現在就在線上** ⇒ 這不是未來的工作,是已經答應了。含 PII 落點表(分母 58 表/view)、**「刪除」在三個地方不成立**(訂單快照 / 已寄出的信 / 同意證據)、回覆範本。✅ **那三格 Sean 2026-08-21 已全答並落地**(保存期限=不主動刪 / 硬刪 vs 匿名化=匿名化 / 窗口=先不指定;數法 `grep -c '不主動刪' docs/runbooks/data-rights-sop.md` ⇒ 5、`'先不指定'` ⇒ 3、負對照 ⇒ 0)⇒ **不要再拿這三格去問他**

---

---

## §23 · 🔴 **我要查正式庫的一個數字 / 某支 migration 貼了沒 / 想斷言「沒有唯讀權限」之前**

> 指標:`~/pcm-mailbox/0905查證/run.sh`
> 原位置:`CLAUDE.md` 路由表 第 70 行(2026-09-03 立,而病史當天就搬出來)

**成因 —— 而它是一個【誰都會複製】的錯法,不是誰粗心**

2026-09-03 主視窗-87 要判「本班有沒有正式庫唯讀權限」,掃了四個地方:
```
scripts/ 底下的 psql 呼叫   ⇒ 全指本機拋棄式 PG
環境變數                   ⇒ 零連線字串
~/.pg_service.conf / ~/.pgpass ⇒ 都不存在
supabase MCP               ⇒ 那是【報價單】那個庫, 不是本專案的
```
⇒ 它據此回報「**我沒有正式庫的唯讀權限**」,而**那句是錯的**。

🔴 **那條路存在** —— 它藏在 `~/pcm-mailbox/0905查證/run.sh`(Sean 2026-09-03 00:5x 親自批的一支探針),
而它 `source .env.local` 去拿一顆 `PCM_READONLY_DATABASE_URL`。

🎯 **⇒ 而錯法的形狀是這個**:它掃的四個地方**全部是「工具會放連線的地方」**,
而那條路在**一支一次性探針**裡 —— 那不在它的分母裡。

📌 **四個誠實的零, 加起來被讀成「不存在」。**
🛑 而那種零**不會有人去查**:它沒有任何一格看起來可疑。
⇒ 同族:`memory/feedback_fixing-the-tool-does-not-fix-my-own-hand.md` 的「乾淨的零」那一段
  (同一夜另一個實例:線 `-db` 的 `ls` 查無 ⇒ 而它的樹落後 `origin/dev` 55 顆)。

**而它的代價已經產生過**:那個錯的斷言讓一整條結論鏈(「repo 有一批 migration 從來沒上線」)
一度要被降級成「未確認」—— 而降級是**假的保守**:它來自紀錄不完整,不是來自那些值有問題。

🔵 **為什麼那三句義務留在路由表那一行、而這一段搬過來**:
套常載檔那條「不得下放判準」的機械判別句 ——
**把這一段拿掉之後, 剩下的條文單獨讀會不會比原文寬?**
⇒ 病史拿掉 ⇒ **不會變寬** ⇒ 可以搬。
⇒ 而那三句(唯讀≠apply · 跑不動是對的 · 不印連線字串)拿掉 ⇒ **會變寬**
  (條文會變成「這裡有一條連正式庫的路」而沒有任何限制)⇒ **不准搬。**
📌 **⇒ 這一行剛好是那條規矩的正反兩面同時出現** —— 義務與病史長得一樣,
   差別只在「拿掉之後會不會變寬」。

