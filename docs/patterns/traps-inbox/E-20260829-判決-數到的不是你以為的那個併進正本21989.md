# 【判決 · 不開新條】「數到的不是你以為的那個」⇒ **併進正本 `:21989` 當【第四個追問】**

> 2026-08-29 03:49 · 線B `pcm-website-v2-0c`(舊名 `d3`)· 從池子撿的。
> 素材檔:`~/pcm-mailbox/素材-數到的不是你以為的那個-20260829.md`(270 行, 十個實例)
> **本檔只給判決與可貼的字, 不動正本**(正本已停止擴充;真正貼進去由它的擁有者做)。

## 🔴 我做了前一個人明說自己沒做的那一步:**我開檔讀了 `:21989`**
```
主視窗當初逐字:「⚠️ 而我【沒有開檔讀它】…所以這一格我只能寫【疑似同族】, 不能寫【同族】。」
⇒ 我開了 :21989 到 :22060(含它的三個實例、判別句、三個追問、與最近鄰的分界)
```

## 判決:**同族, 而是同一個母題的【另一個維度】** ⇒ 併,不開新條

### 兩邊的命中都是【真的】—— 這一點兩邊一致(`:21989` 自己與 memory 那條的分界就寫著這句)
```
memory a-broken-ruler-… ⇒ 【假的命中】, 尺錯了
:21989 與本素材      ⇒ 【真的命中】, 尺沒錯
```

### 而分界在【它錯在哪一個維度】
```
:21989  真的命中, 而它們落在**錯的那一側**
        追問:是真貨還是替身?是產生它的那一側還是消費它的那一側?是尺看得見的形狀還是目標的形狀?
        📌 主詞是【側】

本素材   真的命中, 而它們不是**你要數的那個種類**
        · `grep -c` 把【註解裡的一行指令】算成一句 ALTER TABLE
        · socket **檔數** 30 被當成【活窗數】(實際 9)—— 死掉的東西留下的檔
        · 負對照撈到【自己在別處寫過的那個字串】
        · 數 codex 開了幾條 finding, 實際數的是【自己寫了幾條註解】
        📌 主詞是【種類】
```
🔴 **⇒ 「側」與「種類」是兩個維度, 而它們的追問句不同 ⇒ 值得各有一句, 而不值得各有一條。**
📌 而 `:21989` 的結構**剛好是「判別句 + 三個追問」** ⇒ **加第四個追問, 一格就進得去。**

## ⇒ 可以直接貼的字(給正本擁有者)

在 `:21989` 的「三個追問」那一段末尾加第四條:
```
是【我要數的那個種類】嗎? —— 撈到的每一筆, 我有沒有逐筆開過?
  🔴 這一問與上面三問的差別:上面問【側】, 這一問問【種類】。
     實例:`grep -c` 把註解裡的一行指令算成一句 ALTER TABLE;
           socket 檔數 30 被當成活窗數(實際 9)—— 死掉的東西留下的檔還在;
           負對照用了自己在別處寫過的字串 ⇒ 撈到的 10 次全是自己的回音。
  ⇒ 沒有逐筆開過 ⇒ 只能寫「尺回 N」, **不能寫「有 N 個 X」**。
  ⇒ 負對照要用【現造的識別字】, 不要用一個你可能在別處用過的字串。
```

## 查重(兩把尺都跑, 而我沒有引用前一個人的查重結果)
```
scripts/traps-neighbours.py --selftest ⇒ PASS(八個世界印出不同答案)
本判決的草稿餵進去 ⇒ 前五名:
  0.2184 :22378「一個【假的】對照組比【沒有】對照組更危險」
  0.2121 :21989 ← **本判決要併進去的那一條**
  0.2117 :18038「引用一個段落名時:我是複製的還是重述的?」
  0.2046 inbox/E-20260822:877 · 0.2025 memory a-broken-ruler-…
grep 尺:掃 `^## .*(非 0|命中|數到|計數|整數)` ⇒ 命中的最高一條就是 :21989
```
⚠️ **兩把尺指向同一條 ⇒ 那條是真的。** 而我開檔讀了它才下判決, 不是靠排名。

## ⚠️ 而有一格我要標:**我沒有讀那份素材檔的全部十個實例**
```
我讀了檔頭四個實例 + 它的判別式與修法, 以及第五~九個實例的【標題】
⇒ 我的判決建立在【那四個 + 判別式】上
🔴 若第五~九個實例裡有一個不屬於「種類」這個維度, 那一個要另外處理
⇒ 缺的那一道:逐個讀完那十個。**我沒做, 而我沒有假裝做了。**
```
