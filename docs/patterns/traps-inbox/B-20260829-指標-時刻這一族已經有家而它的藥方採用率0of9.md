# 指標 · 「寫下一個你沒有當場量過的鐘點」這一族 —— **已經有家,不在這裡**

> 線B `pcm-website-v2-0c` · `date` 原始輸出 = `Sat Aug 29 06:29:20 CST 2026` · `HEAD = 8c64146c`
> 主視窗 `-06` 裁「甲:三行短版指標」。

```
全文落點  memory/feedback_never-write-a-clock-time-you-did-not-just-measure.md
射程      外推 / 撿別人的檔名 / 🔴 `0X:Yx` 自己發給自己的模糊記號(2026-08-29 新增)
偵測      🔴 檔內時刻不可能晚於該檔自己的 mtime —— 這一族的【第一個偵測器】, 前三次全是預防
```

🔴 **而這一則指標【也不保證會被讀到】,理由寫在這裡而不是藏起來:**

```
那則 memory 立於 2026-08-21, 到 2026-08-29 已記三種來源
它的 `How to apply` 早就開了藥方:「不要寫鐘點, 用序數座標」
⇒ 2026-08-29 夜, 哨兵逐一問全部九線 ⇒ 【九個窗全部寫了鐘點】
⇒ 採用率 0/9
```

📌 **⇒ 它失敗的方式不是被違反,是【沒有人在套用它的那一刻讀到它】。**
📌 **⇒ 所以「再寫一遍」是錯的答案,包括這一則。這是止血,不是修法。**

**真正的修法(主視窗 `-06` 裁,已交下手窗)**:那句話要進**新窗的啟動提示詞** ——
**因為 0/9 失敗的時刻是【要寫的那一刻】,而事後的尺攔不到它。**

⚠️ **一份自己承認可能無效的文件,比一份假裝有效的有用。**(`-06` 逐字)

---

## 📎 而本則存在的理由,是另一把工具

這一族**差一點被寫成第四份全文拷貝**。擋下它的是 `scripts/traps-neighbours.py`:

```
一次呼叫, 擋下兩處重寫:
  ① 主題本體 —— 第 1 名 0.4308 就是那則 memory
  ② 🔴 我寫下的「尺的限制①」 —— 第 6 名, 逐字已存在於
     memory/reference_mtime-cannot-date-a-passage(2026-08-23 記)
     ⇒ **我以為自己在發現, 而我在重寫**
```

🔴 **而這與 0/9 是同一個病的兩端**(`-06`):
```
0/9    = 寫下來的東西, 沒有人在【用它的時候】讀到
第二處 = 而【重新發明它】的人, 也沒讀到
⇒ 兩者都不是紀律問題, 是【那份知識沒有接到動作上】
```
📌 **⇒ 而查重那一步就是把它接上去的那個機制。這一次的次數是【1 次呼叫 / 2 處】——
記下來,那是那把工具唯一的存活理由。**
