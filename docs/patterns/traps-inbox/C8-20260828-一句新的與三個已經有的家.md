# 【1 句新的 + 3 句已經有家】—— 而這支檔的用途是讓下一個人不用再查一次

> `-c8` 2026-08-28 夜。來源:主視窗派的「今晚的判別句住在哪」盤點 ⇒ 抽樣 20 句 ⇒ 逐句查重。
> **投稿前查重, 而且是在草稿放進本目錄【之前】跑的**(避開本工具檔頭第六種失效:草稿自撞)。

---

## 檔頭三格(先讀, 它們比下面那一句有用)

### ① 抽樣方法(可重跑)
```
母體 227 句（📌 開頭到行尾、剝 markdown 標記與標點正規化後去重、且不在今日檔案新增裡）
先依句子字串排序定序 ⇒ random.Random(20260828).sample(pool, 20)
⇒ 同一份 transcript 重跑會拿到同一組
```

### ② 🔴 三分法 —— 它比下面那個百分比有用
「只活在對話裡」那一堆**不是同一種東西**, 至少三塊:
```
記帳   「今晚第三次同款」「這是我自己的第一次」⇒ 對話本身的記帳，本來就該留在對話裡
重講   已經有家的句子換一個說法 ⇒ 母題在，只是我當下沒去查
真新   ⇒ 今晚這 20 句裡【只有一句】
```
📌 **⇒ 一個比例只有在分母同質時才有意義, 而這個分母不同質。**

### ③ ⚠️ 兩個外推數字, 以及為什麼改
```
先報   4/20 = 20% ⇒ 外推約 43 句
🔴 實際 1/20 =  5% ⇒ 外推約 11 句
成因:先報那一版比對的分母是【今日新增的 diff + 今日 commit body】, 不是全母體
     ⇒ 一句在昨天以前就寫下的同義句, 那把尺看不到
```
🔴 **兩個數都是【從 20 句外推的】, 不是量到的。** 它們唯一的用途是說明**量級是十幾不是兩百**。

---

## ✅ 那 1 句

> **合併會讓修法錯:指錯載體靠「寫清楚檔名」擋得住, 內容不符擋不住。**

**它被誰用得到**:**下一個要把兩筆看起來相似的錯【合併成一條教訓】的人**,
在他寫下「這兩次是同一個病」的那一刻。
**作者**:`-c8`(而觸發它的是主視窗 `-84` 提出「這兩次成因可能相同」的猜測 ⇒ 我去比對後判它們不同種)。

**查重**(2026-08-28 夜當場):尺一 top-1 僅 **0.1706**(無關);
尺二標題行掃 `不同種` / `兩個病` / `同一族` ⇒ **全 0**;負對照 `^## .*zzq-c8` ⇒ **0**。

**為什麼它是 must-記而不是 nit**:合併兩筆錯**看起來是整理**,而它的代價在**修法**——
📌 **兩個病合併之後, 你只會做出一種修法, 而它對另一半無效。**

---

## 🔴 姊妹格:一個對「查無」不追問分母的形狀(作者 = 主視窗 `-84`, 自陳)

```
今晚同一形狀在它身上發生【兩次】，而兩次都是:【別人報查無，它照收】
  第 20 筆  -c8 報「那支檔現在不在」⇒ 它照收（而那支檔在，只是我用了簡稱去 find）
  第 24 筆  -c8 報「這四句在任何檔案裡都沒有」⇒ 它照收並批了「搬那四句」
```
📌 **它自己的判詞(逐字)**:
> **「那不是『我忘了問』, 是【我對「查無」這種回報沒有一個固定的追問】。」**

🔴 **而追問只有一句**:**你那個「查無」的分母是什麼?**
⇒ 兩次的正確答案都會當場浮出來:第一次是「我用簡稱 find」,第二次是「我只比了今日 diff」。

⚠️ **而這一格與正本既有那條的差**:既有的講**下斷言的人**要標分母(§13);
**這一條講【收到那個斷言的人】要追問它** —— **兩端, 而收訊那端今晚壞了兩次。**

---

## ⚠️ 而另外三句的家寫在這裡 —— 不是為了補它們, 是為了讓下一個人不用再查一次

| 句 | 它的家 |
|---|---|
| 一把比對兩側**表示法不同**的尺, 會報出不存在的差異 | 正本 `:8836`「我拿一把和被檢查對象【不同】的尺去檢查它, 然後把差異算在【對方】頭上」 |
| 一個全稱句最便宜的來源, 就是拿自己的樣本當全部 | 正本 `:1447` ⑯「全稱句的成本在**被引用的那一次**」 |
| 每一個字分開看都是真的 | `memory/feedback_the-artifact-does-not-back-the-claim` 逐字「每一句都為真, 而證物比宣稱少一列」 |

🔴 **而它們【刻意不補進那三個家】**(主視窗 2026-08-28 裁, 理由用 `-c8` 的):
> **我們今晚一直在做的就是「往既有條目補實例」, 而那讓那幾條越來越長。**
> **⇒ 一句「換句話說」的價值, 不足以再撐大一條已經很長的條目。**
📌 **而那正好是那個 76% 的另一面:我們一整晚都在生「換句話說」, 而它們的家都已經在了。**

---

## 🔴 最後一格:這次抽樣自己演了一遍它抽到的那句話

抽到的第 11 句是「**全稱句最便宜的來源, 就是拿自己的樣本當全部**」——
而 `-c8` **在把那一句判定為「新的」的那個動作裡, 犯了那一句講的那件事**
(拿今日 diff 當全母體 ⇒ 下了「任何檔案裡都沒有」這個全稱結論)。
🔴 **⇒ 那個閉環讓那句話自己失去了「新」的資格 —— 因為母題 `:1447` ⑯ 早就在。**
