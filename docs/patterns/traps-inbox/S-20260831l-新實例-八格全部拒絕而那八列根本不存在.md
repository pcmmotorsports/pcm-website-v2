# 🔵 新實例(**母題已存在**)· 八格全部印「拒絕」,而那八列根本不存在

> 線【出貨】`-1e` · 2026-08-31 · 🛑 **既有母題的新實例,請【併進去】不要開新條。**

## 併到哪

```
正本 字面錨:「一張矩陣**四格一致**本身就是量具壞掉的訊號 —— 而救它的是【正向對照】」
行號參考(會漂):我這棵樹 :7637
```
🔵 查重跑過(`--selftest` 九世界 PASS),開檔讀了。

## 🔴 而兩個實例的機制【逐字同構】—— 連細節都一樣

```
母題(2026-08-19 G4)  `RETURNING id` 的輸出被 `INSERT 0 1` 污染 ⇒ **uuid 抓錯**
                     ⇒ 每一發都在 uuid 語法就炸 ⇒ 分類器只看「有沒有 ERROR」
                     ⇒ 把【我的測試壞了】讀成【RPC 擋下了】⇒ 四格一致

本例(2026-08-31)     我插八列測試資料, `order_id` 欄填成 `'a1'` 這種**非 uuid 字串**
                     ⇒ **整批 INSERT 失敗**, 而我把 stderr 導掉了
                     ⇒ 八列都不存在 ⇒ 八發全部落「找不到那一列」
                     ⇒ 把【我的資料沒進去】讀成【白名單擋下了】⇒ 八格一致
```
📌 **⇒ 同一個病、同一個資料型別(uuid)、同一種誤讀方向,隔了 12 天。**

## 🔴 而本例多出來的一格

```
母題那次是「四格【全紅】」—— 而紅本身就有點可疑。
本例是「八格【全部拒絕】」, 而**我期待的方向本來就是「大部分應該被拒」**
⇒ 📌 **那個一致的答案正好落在我期待的方向上 ⇒ 它讀起來不是可疑, 是【符合預期】。**
```
✅ 修好之後同樣八個世界印**三種**不同裁決(2 接受 / 5 白名單拒 / 1 未燒完拒)。

## 判別句

```
一組答案全部相同的時候, **先問那把尺有沒有跑到, 再問答案對不對。**
🔴 而最危險的是:那個相同的答案正好是你期待的方向。
✅ 救它的與母題相同 —— **正向對照**:安排一個【你知道應該印不同答案】的世界。
```

## ⚠️ 射程

```
· 我是【事後】才發現的 —— 當下那八個「拒絕」我看了一眼就往下走了。
· 抓到它的是「有一列應該成功而它沒成功」, 不是任何一道機制。**未機制化。**
```
