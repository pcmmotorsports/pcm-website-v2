# graphify 的表頭說「101 nodes found」而實際印出 `NODE` 行只有 64 —— 兩個數不一致

**2026-09-07 16:4x · tidy · 主視窗裁「記一行, 不追成因」**

```
Traversal: BFS depth=2 | Start: [...] | 101 nodes found
```
而 `grep -c '^NODE'` 同一份輸出 ⇒ **64**。

🛑 **本條【不追成因】**(可能是截斷、去重、或表頭數的定義不同)——
記它的理由只有一個:**別人也會撞, 而撞到的人會拿其中一個數去報告。**

📌 **要用哪一個**:報「掃過幾個」時用**你自己數出來的那個**(`grep -c '^NODE'`),
不要抄表頭 —— 🔴 **表頭那個數我沒有驗過它算的是什麼。**

🔵 而本次真正的結論不受影響:我要問的是「有沒有既有紀錄」,
做法是**對那 64 行跑關鍵字過濾**(`email|mail|outbox|resend` ⇒ **0**)——
⇒ 📌 **一個【對印出來的東西】跑的過濾, 不依賴表頭那個數對不對。**

⚠️ 同一發還有兩個已知的可信度上限(主視窗量的):
圖上次建於 `032e1339`(2026-09-07 11:25)、**落後 137 顆**、**文件那半沒重跑**。
