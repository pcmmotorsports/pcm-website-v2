# 🔵 新實例(**母題已存在**)· 我上午把它寫成教訓,下午在新碼裡犯它,而抓到的是別人

> 線【出貨】`-1e` · 2026-08-31 · 🛑 **這是既有母題的一個新實例,請【併進去】不要開新條。**

## 併到哪

```
memory `feedback_writing-the-rule-feels-like-obeying-it`
判別句(它的,逐字):「我剛剛寫下的這條規則,我在【寫它的這一封/這一片】裡遵守了嗎?」
📎 同族但不同層:memory `feedback_knowing-the-trap-does-not-stop-you-in-the-next-paragraph`
   (那條是「同一份交件、下一段、載體換了」)
```
🔵 **查重跑過**(`traps-neighbours --selftest` 九世界 PASS);那兩則都開檔讀了。

## 🔴 而本實例多出來的三格 —— 母題原本涵蓋不到

母題的兩個實例都是**同一份交件內、幾分鐘之隔、同一個載體**。本次三格都不同:

```
① 時間差是【幾小時】不是幾分鐘 —— 而中間我做完了整整兩片別的東西
② 載體換了【兩次】:教訓寫成 traps 檔 ⇒ 犯它的地方是 production code
③ 🔴 發現它的【不是那則教訓, 也不是我】—— 是 codex(外部對抗審查)
```

## 兩個實例(同一天、同一個 session)

```
上午 我把「一條斷言若自己的輸入與期望相同, 它什麼都沒防到」寫成一則 traps
下午 我在新碼裡寫:輸入 orderCreatedCutoffIso = null, 期望也寫 null
     ⇒ 一個把它寫死成 null、完全忽略呼叫端傳值的實作【照樣通過】
     ⇒ codex R1 must-fix 抓到。**不是我。**
```
```
上午 codex 打我一次:我沒有消費 `shippedGapUnknown` ⇒ 下游把「讀不到」讀成健康
下午 我接訊號 4, 在【同一支 route】裡完全沒有消費 `orderCreatedGapUnknown`
     ⇒ 一模一樣的形狀, 而我還在那顆 commit 的訊息裡引用了上午那一格
```

## 📌 判別句

```
**寫下來, 對作者本人沒有保護力。**
🔴 一則教訓的讀者裡, 最不會受益的那一個, 就是【剛寫完它的人】——
   因為他覺得自己已經在防它了, 而那個感覺本身就是母題那條講的東西。
```
🛑 **⇒ 可執行的那一步不是「再寫一遍」** —— 我今天已經寫過了。
✅ 而今天真的接住它的是**外部對抗審查**(codex,不共用我的前提)。
⇒ 📌 **一條靠「記得」執行的規則,對它自己的作者也一樣靠不住。**

## ⚠️ 射程

```
· 兩個實例, 同一個 session、同一個人、同一天。**不是一個比例。**
· 我沒有量過「寫過 traps 的人後續犯同一條的頻率」—— 那要跨 session 追。**未數。**
· 🔴 而本則自己也在射程內:寫下它, 不會讓我明天不犯第三次。
```
