# traps-inbox · A 窗(線 A `pcm-website-v2-86`)· 2026-08-22

> 暫存區。canonical = `../guard-and-instrument-traps.md`,**引用引 canonical、不要引這裡**。
> 併入後本檔可刪。
> **A / B / C 三條都是我今晚自己踩到、自己攔下來的 —— 那三個錯的宣稱都沒有送出去。**
> **D 不是坑,是一個正反對照的範本**(同一個後台裡,同一晚量到的一好一壞)。

> 🔴 **本檔四條**(A/B 是量具盲點,C 是同族第三個載體,D 是一個正反對照的範本)。
> **A/B 兩條 canonical 都沒有【這個形狀】,而兩條都有【半重複的鄰居】—— 收的時候不要當全新的。**
> **C/D 兩條我【沒有】去 canonical 查有沒有鄰居**(寫的時候已經接近收工)⇒ **收的人要自己查一次。**
> 數法(在 `docs/patterns/` 底下跑,含正負對照):
> ```
> grep -c 'DOM 裡還沒有'   guard-and-instrument-traps.md  ⇒ 0    (條 A 的判別句)
> grep -c '只有一種寫法嗎' guard-and-instrument-traps.md  ⇒ 0    (條 B 的判別句)
> grep -c 'RSC payload'    guard-and-instrument-traps.md  ⇒ 2    🔴 鄰居, 見下方「半重複②」
> grep -c '受控'           guard-and-instrument-traps.md  ⇒ 3    🔴 鄰居, 見下方「半重複①」
> grep -c 'zzznotatrap'    guard-and-instrument-traps.md  ⇒ 0    負對照
> grep -c '恆綠'           guard-and-instrument-traps.md  ⇒ 46   正對照 ⇒ 尺是活的
> ```

> ## 🔴🔴 上面那些 0 只證明【canonical 沒有】—— 不證明「沒有人寫過」
>
> **2026-08-22 審查線量到**:這個 inbox 從 2026-08-17 起 **一條都沒有被併進 canonical**。
> 我自己當場重量(含正對照):
> ```
> ls docs/patterns/traps-inbox/*.md | grep -v README | wc -l   ⇒ 14 個檔
> 總行數（python 逐檔數）                                      ⇒ 3,530 行
> ```
> ⇒ 🔴 **我一條都沒有讀過那 3,530 行。** 所以本檔檔頭那些 `⇒ 0` 的正確讀法是:
> **「canonical 裡沒有」,不是「這是新的」。**
>
> 📌 而這件事本身就是一條教訓,主視窗講得最好:
> > **有些人的 grep 回 0 是對的,因為東西根本不在正本裡。**
>
> ⇒ 「重新發現」不是有人不用心,是**結構問題**:投稿沒有被收,所以每個人都得重新踩一次。
>    🔴 **而那個成本是複利的** —— inbox 越厚,重新發現的機率越高,而**厚度本身沒有任何訊號**。
>    (判別句:**我在 canonical 上量到的 0,分母是 canonical 還是「所有人寫過的東西」?**)

> ## 🔴🔴 鄰居工具補跑了(主視窗要求),而**結果改變了我對自己這份投稿的判斷**
>
> `python3 scripts/traps-neighbours.py docs/patterns/traps-inbox/A-20260822.md`
> 分母:**正本 408 條 + inbox 87 條 = 495 條**(工具當場切的)。
>
> **我開了四條(排除我自己那三行),逐條判:**
>
> | 排名 | 位置 | 我的判定 |
> |---|---|---|
> | 3 (0.5353) | 正本 `:16930`「量具裡藏著一個假設,而它回給你的是一個**乾淨的數字**」(**同日**,線 D `-4a`) | 🔴🔴 **同族,而且是同一個母題。見下方。** |
> | 4 (0.4921) | 正本 `:13489`「量具給了你一個【講得通的錯誤故事】」 | **不同**:那條講的是**錯的那一發長得像一個發現**(結論方向被帶偏);我這三條是**同一個結論方向下的乾淨 0**,沒有故事,只有空。 |
> | 7 (0.4659) | 正本 `:3369`「輸出合法、格式正確、數量合理 —— 而它量的不是那件事」 | **是父母題,不是同族**:那條涵蓋所有「尺與對象之間那一步壞掉」;我這三條是它底下**回傳值恰好是 0** 的那個子族。可以掛在它下面,不必另立。 |
> | 8 (0.4596) | 正本 `:9984`「量具少一個欄位時,要換的是【判別軸】不是【結論】」 | **不同**:那條是「量具答不出來時怎麼換軸」;我這三條是「量具**答了**,而答案是錯的 0」。 |
>
> ### 🔴 而第 3 名那條,我判它與我的 **B / C 是同一個母題** —— 這份投稿要照這個收
> `:16930` 的兩個實例是:
> ```
> ① python 掃 CSS 只寫了 @media, 漏掉 @container ⇒ 整欄空白 ⇒ 讀起來像「都在桌機層」
> ② count(*) filter (display_id ~ '^[0-9]{6}$') ⇒ 回 0, 而真格式是【6 碼英數大寫】
> ```
> **①②的形狀,與我的條 B(只數 raw、漏掉 `\uXXXX` 跳脫)是同一件事:字集比對象窄 ⇒ 乾淨的 0。**
> 條 C(`scrollHeight === clientHeight` 讀成「滿了」)也是同一個母題,只是那個假設藏在**幾何**裡而不是字集裡。
>
> ⇒ **建議收法:B / C 併成 `:16930` 的第三、第四個成員,不要另立條目。**
>   而 **A 我建議獨立**,理由在下面「半重複①」那一段:它的判別句與 `:17714` 那條**互補而不重疊**。
>
> ⚠️ **這一格是工具救的,不是我想到的** —— 我原本準備把 B/C 當成兩條新的交出去。
>    📌 而它印在**第 3 名**,不是第 1 名(前兩名是我自己的檔)⇒ **只看第一名會漏掉它。**

> ## ⚠️ 兩處半重複(我開檔讀過了,不是看數字猜的)
>
> **半重複① `:17714`** —— canonical 已有一條受控 `<details>` 的教訓,而**機制不同**:
> 那條是「**我點了它**,所以我量到的是我自己造成的狀態」(母題:我對它做的第一件事不是觀察)。
> **本檔條 A 我一次都沒碰它** —— 病是「關著的時候 body 根本沒掛進 DOM」。
> ⇒ 兩條可以合併成同一個母題的兩個成員,但**不要把 A 併進那條當旁註** ——
>   那條的判別句是「我碰過它嗎」,答「沒有」之後**還是會掉進 A**。
>
> **半重複② `:6659`** —— canonical 已記過「RSC payload 造成的 0」,而**成因不同**:
> 那條逐字是「**我的量法把 `<script>` 剝掉了**」(是我主動丟掉了資料)。
> **本檔條 B 我一個字都沒剝** —— 病是**同一個中文字在同一份檔裡有兩種寫法**,而 grep 只認我打的那種。
> ⇒ 同一個 0、同一個檔案、**兩個各自獨立的成因** —— 而 canonical `:6653` 那條本身就在講
>   「一個 0 可以有兩個獨立原因」。**條 B 是那條的第三個成員。**

---

## 條 A · 我平常用來繞開這個坑的那把尺,在這一族上一樣是瞎的【安靜】

**母題**:`textContent` 讀得到收合的 `<details>`,`innerText` 讀不到 ——
**所以我養成了「量畫面上有沒有這東西就用 `textContent`」的習慣。而它有一個例外,我不知道。**

### 當天發生什麼(2026-08-22,線 A 在鑽機上扮員工走一天)
我要答一個很具體的問題:**「這張真訂單上,員工有沒有辦法登錄『我跟供應商訂貨了』?」**

```
量法:main 的 textContent 裡找「採購(向供應商訂貨)」 ⇒ 0 命中
對照:同一把尺在另一張訂單 PCM-2026-0104 上        ⇒ 1 命中
⇒ 看起來是完美的兩世界對照:有採購紀錄的那張有入口, 沒有的那張沒有
⇒ 我差一步就要寫下「一張還沒下訂的單, 畫面上沒有下訂的入口」——那會是一個很嚴重的假缺陷
```

**而它是錯的。** 商品卡是 **React 受控的 `<details>`**:關著的時候 body **根本沒有掛進 DOM**。
真的用滑鼠點開再量 ⇒ 入口、表單、24 家供應商的下拉、7 個欄位,**全部都在**。

### 為什麼這個坑特別會咬人
```
一般的 <details>   ：關著時內容【在 DOM 裡】只是不顯示 ⇒ textContent 讀得到
                     ⇒ 這正是它被當成「安全的尺」的原因
React 受控 details ：關著時內容【不在 DOM 裡】        ⇒ textContent 也是 0
🔴 而兩者在 querySelector('details') 上長得一樣、在 outerHTML 上也長得一樣。
```
⇒ **「我已經避開 innerText 那個坑了」這件事本身,讓我更不會去懷疑這一發。**

### 判別句
> **我這個 0,是「畫面上沒有」還是「DOM 裡還沒有」?**

**過關的唯一做法**:**真的點開它,再量一次。** 讀 DOM 的任何一把尺都答不出這一題。
📌 而「點開」要用**真的點擊**(Playwright `locator.click()`),不是 `evaluate` 裡合成的 `.click()` ——
   後者 2026-08-21 已經讓另一條線產出過三條假 finding。

### 這條擋得住什麼
一整族「**這個功能沒做**」的假缺陷。而假缺陷的下場不是被發現,是**有人真的去把它做第二次**。

### 🔴 同一晚我在同一族上又差點犯第二次(不同讀法、同一個病)
我用「編輯訂單」表單存了一張發票,存完之後量,以為表單被清空了 ——
**那是 `innerText` 讀不到 `<input>` 的 `value`。** 直接讀 `.value` ⇒ 值都在。
⇒ **一個晚上、同一族、三種不同的讀法**(`innerText` 對 input / `textContent` 對受控 details /
   剝 `<script>` 對 RSC)⇒ **共通形狀:讀法選錯 ⇒ 得到一個乾淨而錯的 0。**

---

## 條 B · Next 把中文塞進跳脫字串 ⇒ 只數 raw 會得到一個乾淨的 0【安靜】

### 當天發生什麼(同日,查「請款狀態」那一格在不在畫面上)
```
grep -c '訂單' orders.html  ⇒ 1
```
那頁上「訂單」兩個字**至少出現幾十次**。⇒ **我的正對照當場告訴我尺壞了。**

**原因**:Next 的 RSC payload 走 `self.__next_f.push([...])`,裡面的中文是 **`\uXXXX` 跳脫**的。
同一個字在同一份 HTML 裡有**兩種寫法**,而 `grep` 只認你打的那一種。

```python
def count(html, w):
    esc = ''.join('\\u%04x' % ord(c) for c in w)
    return html.count(w) + html.count(esc)
```
改成兩種都數之後:`訂單` ⇒ **151**。

### 為什麼危險
🔴 **只數 raw 會得到一個乾淨的 0,而那個 0 是量法造的。**
而這一族的用途正好是**下否定斷言**(「這個字沒有出現在畫面上」)
⇒ **錯的方向剛好是最貴的那個方向。**

### 判別句
> **我在數的這個東西,在這份檔裡只有一種寫法嗎?**

**過關的做法**:每一發都帶**正對照** —— 拿一個你**確定一定在**的字用同一把尺數一次。
回 0 ⇒ 是語法/編碼問題,不是資料問題。

📌 **同族**(canonical 已有前兩個成員,這是第三個):
```
:6659  剝掉 <script> ⇒ RSC payload 整塊消失      （我主動丟掉資料）
:6659  curl 逾時截斷 ⇒ 同一個 URL 兩次不同大小   （傳輸層丟掉資料）
本條   中文在 payload 裡是跳脫的                 （資料在, 而我的字集比它窄）
memory `reference_git-grep-silently-ignores-backslash-b`：git grep -E 不吃 \b 且靜默不匹配
⇒ 四者同一形狀:**尺的字集比你以為的窄,而它不會抗議。**
```

---

## 兩條共用的一句(這句是我這一晚學到最貴的)

**這兩次我都有正對照,而只有第二次是正對照救了我。**

條 A 的正對照是**乾淨的、非零的、而且結論仍然是錯的** ——
因為我的對照組與實驗組**在另一個維度上本來就不同**(一張卡開著、一張關著)。

🔴 **正對照證明的是「尺會動」,不是「我量對了東西」。**

— A(線 A `pcm-website-v2-86`)· 2026-08-22

---

## 條 C · 一個 `0` 讓我差點判定「這條路走不通」,而它是量法造的【安靜】

**同一晚第三次同族,而這次的載體是【版面】不是文字。**

### 當天發生什麼
我要回答「84px 的側欄軌,加一行字塞不塞得下」。我量:
```js
nav.scrollHeight === nav.clientHeight   // 876 === 876
```
⇒ 我讀成「**餘裕 0,已經滿了**」⇒ 差一步就要寫下「這條路走不通,只能改字級或換詞」。

**而它是錯的。** `nav` 是 **flex 撐開的容器** —— 那兩個值相等**只代表沒有溢出**,
不代表內容剛好填滿。真正的餘裕要拿**最後一個項目的 `bottom`** 減 `nav` 的 `top`:
```
誤讀:餘裕 0px
實際:內容高 506 / 容器高 876 ⇒ 餘裕 370px
在最緊的 1280×720 也還有 98px, 加一行(+51px)之後還剩 47px
```
⇒ **差了 370px,而兩個讀數都是「真的量到的數字」。**

### 判別句
> **我量的這個 0,是「東西剛好填滿」還是「我的量法看不到剩下的空間」?**

**過關的做法**:量**內容自己的邊界**(最後一個子元素的 `bottom`),不要量容器的自陳。
📌 `scrollHeight === clientHeight` 只回答「有沒有溢出」,**它不回答「還剩多少」**。

### 這條與本檔前兩條是同一族,而三條的載體都不同
```
條 A  textContent   對受控 <details>   ⇒ 0        （DOM 讀法）
條 B  grep raw      對 RSC 跳脫中文    ⇒ 0        （字集）
條 C  scrollHeight  對 flex 容器       ⇒ 餘裕 0    （幾何）
🔴 三種完全不同的工具, 同一個形狀:**量法選錯 ⇒ 得到一個乾淨而錯的 0。**
   而 0 特別危險, 因為它讀起來像「我已經確認過了」。
```

---

## 條 D ⇒ **不新開節,併進正本「守門只驗有沒有被擋住,沒驗當事人看不看得見自己被擋」那一條的 UI 層那一半**

> 🔴🔴 **落點更正(`-4a` 2026-08-22 手動解析母節時抓到,不是工具抓到的)**:
> 正本已有一條同母題,判別句逐字「**我這一格證明了『壞事沒發生』。那當事人怎麼知道?**」
> ⇒ **本條不該獨立**,要當那一條的 **UI 層成員**收進去。
>
> ⚠️ **而工具連 top-8 都沒撈到它,本條最高分只有 0.1311** ——
> 成因是**兩邊用的詞彙幾乎不重疊**(我寫「不給/渲染/員工/沉默/等級」,它寫「守門/斷言/靜默出口/當事人」)。
> 🔴 **所以那個排名不只是抽樣,它會【整個漏掉】換一套詞彙寫的同一件事。**

### 段首兩句(`-4a` 指定放最前面)

> ### **有沒有任何一段文字,正在把人指向我這個【不給】的地方?**
> ### 🔴 **這一族的缺陷不住在任何一支檔裡,它住在兩支檔【中間】** —— 所以沒有任何一支檔的測試會紅。

**為什麼是「中間」**:寫取消區文案的人**正確地**告訴員工去退款區;
寫退款區的人**正確地**在旗標關著時不渲染入口。**兩邊都沒有錯,而員工走到的是一個空盒子。**
⇒ 母條的判別句問的是「當事人怎麼知道」;**這一半要多問一句:「有沒有別人正在把他送過來?」**
   因為**寫那段文字的人,不會知道我在這裡沉默。**

### 母條沒有、而這一半新增的三件(`-4a` 逐條確認)

**一、三個等級(母條是二元的:靜默 vs 有訊號)**
```
① 不給   按鈕不渲染 / 區塊空白
② 說     擋住, 而且講原因與下一步去哪
③ 預告   擋住 + 講原因 + 【告訴他什麼時候會亮起來】   ← 🔴 ③ 是母條沒有的
```

**二、🔴 「不給」什麼時候是【可以】的** —— 母條隱含「一律要有訊號」,而那不對。
判準就是段首那句:**有沒有別人正在把他送過來。**

**三、層級不同** —— 母條講**守門與測試**,這一半講 **UI 設計**。

### 同一個後台裡,同一晚量到的一好一壞(這是本條的證據)

**①(壞的,我已修)**:旗標關著時「退款」區塊渲染成**只有標題、底下一片空白**,
而**同一張畫面上的取消區正在叫他去那裡**(逐字「請到本頁最下方的「退款」裡處理」)。

**②③(好的,批次出貨那個對話框)**:
```
沒有採購列的品項    ⇒ 那顆「貨到了」根本不渲染      （①的形狀, 而這裡是【對的】—— 見下)
按下有鈕的那一顆    ⇒ 「這個品項目前沒有可以登錄到貨的採購（可能還沒下單,或原本那筆已經
                      作廢）。請先到訂單頁的「採購」區塊補上要向誰訂（或選「店內現貨」）,
                      再回來登錄。」                                                （②)
兩顆送出鈕 disabled ⇒ 「這些品項現在都不能出,其中還有在等到貨的。貨到了就按右邊的
                      「貨到了」先登記到貨,**登記完這裡就會亮起來**。」              （③)
```
🔴 **那顆不渲染的「貨到了」是對的** —— 因為同一個畫面上有一整段話在解釋整批的狀況。
   **空的退款區是錯的** —— 因為指著它的那句話**在另一個區塊,而那個區塊不知道這裡是空的。**

### 🔴 第四級(2026-08-22 06:1x 追加;主視窗批准並指定標題要帶「不是縮寫」)

```
④ 把代價講兩次, 而第二次【不是縮寫, 是逐字重複】, 並且擋在動作前面
```
**實例(同一個後台,備註的「更正」)**:
```
第一次  表單上方:把原文整段印出來 + 「更正送出後不可撤回;對更正再做更正, 也不會讓最早那筆
        恢復有效。若誤更正了有效的告知紀錄, 唯一的還原方式是重新登記一筆新的『已告知客人』紀錄。」
第二次  按送出 ⇒ 原生 confirm, 把【同一段逐字再講一次】
```
🔴 **而「不是縮寫」這半是反直覺的** —— 一般會覺得二次確認要簡短。
```
「確定要更正嗎?」⇒ 那句話【沒有新資訊】⇒ 人會用同一個動作跨過它
   ⇒ 📌 **簡短的二次確認等於沒有二次確認:它只是多一個按鈕。**
整段後果搬進 confirm ⇒ 它【強迫你在按下去之前再讀一次】
```

### 而這一級只值得用在**不可逆**的地方
用在可逆的動作上,它會變成雜訊,而雜訊會訓練人**閉著眼睛按 confirm** ——
那會讓真正需要它的那一次也被跨過去。**判準:這個動作按錯了,還原得回來嗎?**

📌 **①②③ 在程式碼裡看起來一樣安全**(都 fail-closed、都不會漏行為),
   差別只在**員工下一步走去哪** ⇒ **靜態檢查與測試都分不出這三級。**

---

# 📮 合稿用 · A 的那一半:「**關著的時候,尺讀不到**」

> **收件人 = `-4a`(審查線,它合稿)。** 另一半(「**打開它,就改變了被量的東西**」)由 `-5b` 寫。
> ⚠️ 照 `-4a` 的兩條要求:**不編號**(canonical ①–㊽ 已滿,上一輪在這裡造了五個重號)、
> **不寫行號**(`-5b` 寫的行號隔幾分鐘就位移)。
> 🔴 **本段刻意與上面的「條 A」重複** —— 上面那份是我自己的原始紀錄,這一段是**交出去的成品**。
> 合稿之後上面那份可以刪。

## 我量了什麼

我要答一個很具體的問題:**「這張真訂單上,員工有沒有辦法登錄『我跟供應商訂貨了』?」**

```
量法:main 的 textContent 裡找「採購(向供應商訂貨)」 ⇒ 0 命中
對照:同一把尺, 另一張【已經有採購紀錄】的訂單      ⇒ 1 命中
⇒ 讀起來是一組乾淨的兩世界對照:有採購的那張有入口, 沒有的那張沒有
⇒ 我差一步就要寫下「一張還沒下訂的單, 畫面上沒有下訂的入口」——那會是一個很嚴重的假缺陷
```

## 為什麼那個 0 是假的

商品卡是 **React 受控的 `<details>`**。關著的時候,**它的 body 根本沒有掛進 DOM**。
真的用滑鼠點開之後再量:入口、表單、二十幾家供應商的下拉、七個欄位,**全部都在**。

🔴 **而這個坑特別會咬人,原因是我以為我已經避開它了:**
```
一般的 <details>   ：關著時內容【在 DOM 裡】只是不顯示 ⇒ textContent 讀得到
                     ⇒ 這正是「別用 innerText、要用 textContent」變成常識的原因
React 受控 details ：關著時內容【不在 DOM 裡】        ⇒ textContent 也是 0
```
兩者在 `querySelector('details')` 上長得一樣,在 `outerHTML` 上也長得一樣。
⇒ **「我已經避開 innerText 那個坑了」這件事本身,讓我更不會去懷疑這一發。**

## 判別句

> **我這個 0,是「畫面上沒有」還是「DOM 裡還沒有」?**

**過關的唯一做法**:**真的點開它,再量一次。** 讀 DOM 的任何一把尺都答不出這一題 ——
因為它們讀的都是「現在掛著什麼」,而問題正是「還沒掛上去的那些」。

### 🔴 而它有**第二種用法,而且比較便宜**(用法由 `-3c` 回報,2026-08-22)

上面那句「真的點開它」讀起來像**一個你必須做的動作**。而它還有另一種用途:

> **如果你【決定不點開】—— 這條的用途是讓你把結論寫成【推論】,而不是【量測】。**

`-3c` 當天的實例:它要量一頁上「有沒有一句自陳」,那頁有 **10 個折疊區、9 個關著**。
它**判斷那 9 個都不是相關區塊,而沒有打開** —— 而它在報告裡**明寫「這是推論不是量測」**。
```
🔴 沒有這條警告 ⇒ 它會寫「全頁掃過都沒有」（一句量測句, 而它是假的)
   有這條警告   ⇒ 它寫「我掃過看得見的部分, 而九個收合區我沒開」（一句推論句, 而它是真的)
```
📌 **它逐字回報的那句比我原本寫的準**:
> **這個警告的價值不是讓我去點開九個,是讓我知道【我那句話該寫多重】。**

⇒ **兩種用法都對,而第二種大多數時候夠用** —— 而它們的成本差很多:
```
點開九個   ⇒ 九次互動, 而且【每次點開都改變了版面】(見下方「接到另一半」那段)
寫成推論   ⇒ 零成本, 而且【它讓下一個人知道要去打開那九個】
```

📌 而「點開」要用**真的點擊**(Playwright `locator.click()`),不是 `evaluate` 裡合成的 `.click()`。

## 🔴🔴 而這裡就是接到另一半的地方 —— 而它是**我自己另一條的適用邊界**

**點開它,就改變了被量的東西。**
```
不打開 ⇒ 尺讀不到（我這一半）
打開了 ⇒ 讀得到, 而版面已經不是原來那個（-5b 那一半）
🔴 沒有一個狀態是「既讀得到, 又沒被我動過」。
```

🔴🔴 **落點(`-5b` 2026-08-22 提,我同意)**:正本已有一條**同母題,而且是我今晚自己落的** ——
「**我對它做的第一件事,不是【觀察】—— 我的第一個動作破壞了我要量的那個狀態**」。
⇒ **這一條不要寫成平行的新母題,寫成那一條的「更硬的一格」:**
```
我那條      = 先動作後觀察 ⇒ 量到自己造成的狀態
              🔑 修法乾淨:【先觀察再動作】—— 重新導航、完全不碰、量初始狀態 ⇒ 就好了

這一條      = 怎麼排順序都沒用
              🔑 【先觀察再動作】那條修法, 在這個對象上【無法執行】
              ⇒ 要換的不是順序, 是量法（不碰 DOM 的算法 / 截圖 / 存原值+驗還原）
```
📌 **這一條的價值不是再講一次「觀察者會擾動被觀察者」,是明確指出【那條修法的適用邊界】。**

⇒ 實務結論不變:**要量兩次,而且要講清楚每一次量的是哪個世界。**
**只做其中一次、卻用全稱句寫結論,就是這一族出事的方式。**

### 🔴 而漏掉那個母題的四方裡,有一個是它的作者(這一格 `-5b` 提的,我同意留著)
```
鄰居工具 top-8   ⇒ 沒有它
-4a 審查         ⇒ 沒抓到（它自己標了「只開了 top-3 的 1-2 條」）
-5b 自審         ⇒ 沒抓到
🔴 我            ⇒ 也沒提 —— 而【那條是我幾小時前親手寫下的】
```
⇒ **連作者本人都不會記得自己幾小時前落了什麼。** 正本一長就是這樣,**而它沒有任何機械訊號。**
   撈到它的不是分數,是**拿「機制關鍵字」去 grep 標題行**(`grep '^## ' canonical | grep 觀察\|擾動\|動到\|注入`)。

## 這一半擋得住什麼
一整族「**這個功能沒做**」的假缺陷。
而假缺陷的下場不是被發現,是**有人真的去把它做第二次**。

— A(線 A `pcm-website-v2-86`)· 2026-08-22 · 具名
