# `-b9` 2026-08-29 夜 · 五個坑 —— **兩條判【併】, 三條判【新開】**

> **母題**:一把尺印出的那個數字, 與「它量對了東西」是兩個宣稱。
> 今晚這五個全部是**尺印了一個合法的數字, 而那個數字在回答另一個問題**。

## 🔴 查重紀錄(照工具要求:我開了哪幾條、為什麼判它不同族)
```
python3 scripts/traps-neighbours.py --selftest ⇒ PASS（九個世界印出不同答案）
python3 scripts/traps-neighbours.py <草稿>     ⇒ logs/traps-neighbours.jsonl:350
分母：正本 500 + inbox 317 + memory 1041 = 1858 則
```
**我開檔讀了四條**(不是只看排名):正本 `㊺`(:2792)· `⑭`(:1358)· `:14292` · `⑤`(:482)。
⚠️ 而工具自己列的第 ⑦ 種失效(母題級通則撈不到)成立 ⇒ **排名沒撞不代表沒撞**, 下面的判定是開檔後的, 不是分數。

---

# 一、判【併】的兩條

## ①→ 併進正本 `㊺`「正在書寫某條規則的當下, 是它最容易被違反的時刻」(:2792)
**它是那條的第 4 個實例, 而它多一個【新的形狀】。**

`㊺` 現有三個實例都是「寫規則時違反了那條規則」。
🔴 **我這一個多的是:那段文字【用了那個坑本身的材料】。**
```
現場：板子是 markdown 表格，一列五格用直線分開
我要警告下一個人「這一列的結尾沒有表格收尾符號」
⇒ 而我把【那個符號本身】寫進反引號當字面去解釋它
⇒ 該列欄數當場 6 變 7，而板子守門印 rc=0（它驗態欄、驗列數，不驗欄數）
✅ 抓到它的是拿隔壁兩列當正對照（兩列都是 7）
```
📌 **一段關於格式的警告, 用了會破壞格式的材料 —— 而破壞只發生在欄數上, 不在文字上, 所以它讀起來完全正常。**

🔴 **而這一格值得單獨記在 `㊺` 底下的理由**:今晚同族發生 **4 次**
(`546ab67c` 字接在收尾符號外 / `6a0a86b0` 與 `6245d11b` 把含直線的 grep 指令寫進表格 / 第四次是本則)。

> ⛔🔴🔴 ~~**而第 4 次的抓法與前三次不同** —— 前三次是**下一顆 commit 才發現**, 第 4 次是**改完當場量**。
> ⇒ **那是一條學習曲線的證據, 而它的證據是【抓到的時點】, 不是【次數】。**~~
> **這一段 2026-08-30 00:4x 被作者自己推翻 —— 而推翻它的是【一發我本來不必跑的量測】。**
>
> **實量**(用錨定位, 量那一列在該顆 commit 裡的形狀):
> ```
> 546ab67c  取消閘那列 ⇒ 欄數 7 而【結尾缺收尾符號】⇒ 🔴 真的出去了，修在 62a7c689
> 6a0a86b0  該列 ⇒ 7  ⇒ ✅ commit 之前就抓到了
> 6245d11b  該列 ⇒ 7  ⇒ ✅ commit 之前就抓到了
> 6d76bc6f  該列 ⇒ 6  ⇒ ✅ commit 之前就抓到了
> ```
> 🔴 **⇒ 只有【第 1 次】真的出去了;第 2、3、4 次【全部】是改完當場量抓到的。**
> ⇒ **所以「前三次不是當場量」是假的, 而那句話正是本條的賣點** ——
> 📌 **一個關於【我學會了】的宣稱, 是最不會被回頭查的那一種, 因為它讓人心情好。**
>
> ⚠️ **而我第一發用的尺沒有判別力**:我數「欄數不是 6 也不是 7 的資料列」⇒ 四顆都印 **65/66**
> (板上本來就有大量含直線的列)⇒ **四個一樣的數字看起來像「沒有變化」, 而它只是尺太鈍。**
> ⇒ **換成用錨逐列量才分得開。**
>
> ✅ **而【真正成立的那一半】留下來**:四次裡**有三次是改完當場量抓到的**,
> 而**板子守門對這四次全部印 `rc=0`** ⇒ **抓到它們的一直是「拿隔壁兩列當正對照」這個動作, 不是守門。**

**可執行**:寫進表格的任何指令與符號, 先問「它含不含那個分隔符」;要講那個符號就**寫中文字**。
改完**當場** `awk -F'|' '{print NF}'` 比隔壁兩列 —— **不要等守門, 守門不驗這個。**

## ⑤→ 併進正本 `⑭`「結構性結論一律要去讀建它的那支 migration 的註解」(:1358)當第 5 列
**而它把 `⑭` 從【讀註解】擴到【讀 DDL 子句本身】。**
```
現場：交接檔明寫「舊單發票回填做了沒 = 另一格，接手窗自查」
grep -rlE 'UPDATE …orders…invoice_requested' supabase/migrations ⇒ 零命中
🔴 而那個零的意思是【不需要】，不是【沒做】：
   20260828100000…:253 逐字
     ALTER TABLE public.orders ADD COLUMN invoice_requested boolean NOT NULL DEFAULT true;
   ⇒ NOT NULL DEFAULT true 這個組合，PostgreSQL 把【既有每一列】都填成 true
   ⇒ 那正是 Sean 答的「全部填要開」⇒ 沒有東西要補做
```
📌 **分得開那兩個世界的不是那個 0, 是去讀 `ADD COLUMN` 那一行的 `DEFAULT`。**
🔴 **這是「零命中」那一族裡最少見的變體**:一般的零命中坑是**零是錯的**(尺太窄);
**這一個零是對的, 而從它推出的結論相反。**
📎 **與 memory `feedback_no-caller-means-either-not-built-or-explicitly-blocked` 是同族第三種**:
那條講一個零有兩種意思(**還沒做** / **被明文擋住**);**本條是第三種:【不必做】。**
📎 而 `⑭` 現有第 4 列正好也是 `invoice_*` 欄那一族 ⇒ **同一支檔家族, 兩個不同的機制。**

**可執行**:對「找不到 X 的實作」的零命中, 下結論前多問一句
**「X 有沒有可能是由別的機制順便完成的?」** —— 對 DB 而言最常見的是
`DEFAULT` / `GENERATED` / trigger / 約束, **而它們在 grep「做這件事的動詞」時全部隱形。**

---

# 二、判【新開】的三條(附為什麼不併)

## ② 一把不剝註解的尺, 會把「明文說自己沒做這件事」的檔算成犯規者
```
現場：要數「哪幾支 migration 在 view 那一句裡用了 別名星號」
第一發用「整支檔裡有沒有」⇒ 10 支
逐支開檔 ⇒ 4 支誤抓，而其中【3 支的原文逐字寫著「刻意不用 c 星號」】
```
🔴 **那三支是最謹慎的作者 —— 他們把「我沒有這樣做」寫下來了, 而正是那句話讓他們被算成犯規者。**
📌 **⇒ 一把不剝註解與字串的尺, 懲罰的是【把決定寫下來的人】。**
⚠️ 同夜另一個窗撞到同族的**另一種藏身處**:誤報全在 `CREATE OR REPLACE FUNCTION … AS $tag$` **函式體**裡。
⇒ **同一個母題, 兩個不同的藏身處。**

**🔴 為什麼不併進 `⑤`「掃描的字集/範圍比你宣稱的窄」(:482)**:
```
⑤ 講的是【窄】——漏抓
本條講的是【寬】——誤抓，而誤抓的分布【不是隨機的】：它集中在最謹慎的那些檔
⇒ 併進一個標題寫著「窄」的條目，會讓這一半被埋掉
```
⇒ **新開, 而與 `⑤` 明確互指為【反向的兩半】。**

**可執行**:掃 SQL 的尺**剝三樣**(`--` 行註解 · 單引號字串字面 · `$tag$` 區塊),
然後**把範圍收到你真正在問的那個語句之內**;負對照用**現造識別字**, 跑之前先確認回 0。

## ③ migration 的時間戳是【全域唯一】的資源, 而沒有任何東西在管它
```
八窗共用一棵工作樹。我新開 20260829190000_…
⇒ 而另一個窗同一時間也開了 20260829190000_…
ls supabase/migrations/*.sql | sed 's#.*/##' | cut -c1-14 | sort | uniq -d
⇒ 1 組（就是我造成的那一組）⇒ 全 repo 唯一一組
```
🔴 **三綠不叫、板子守門不叫、lint 不叫 —— 抓到它的是 codex 對抗審查的第一條 must-fix。**
⚠️ **而後果不是 conflict**:兩支檔都在、都合法、`git status` 乾淨,
**而 migration ledger 可能安靜地跳過第二支** ⇒ **那支的內容從來沒有被套用, 而沒有東西會叫。**

**🔴 為什麼不併進 `:14292`(`CREATE OR REPLACE` 讓 migration 資料夾變成互相覆蓋的定義)**:
```
那條的機制是【同一個物件被後面的檔覆寫】—— 兩支檔都會跑
本條的機制是【兩支檔的檔名撞號】—— 其中一支可能【根本不會跑】
⇒ 同一個資料夾、兩個完全不同的失效
```
⇒ **新開, 互指。**

**可執行**:開任何 `supabase/migrations/` 新檔**之前**跑 `ls supabase/migrations/<你要用的14位>*`;
**收工前**再跑 `cut -c1-14 | sort | uniq -d` ⇒ **期望 0**。

## ④ 字面完全相同, 而它在講另一件事 —— **而「多讀幾個命中」救不了你**
```
要查 Sean 有沒有答過「那兩張紙要不要改名」
grep -rn '不用改名' ~/pcm-mailbox/ ⇒ 3 個命中
🔴 而【第一個不是答案】：
   C-A75-CORRECTION-2026-08-16.md:75「實測是:不用改名。只要 import 活著就會漏。」
   ⇒ 那在講【import 洩漏】，與紙張命名完全無關，而字面一模一樣
✅ 真答案在第三個命中：等Sean決策-20260829.md:2388「A:  不用改名，依照現在」
```
📌 **一個只看第一個命中的人, 會拿一份關於 import 的實測去回答一個關於紙張命名的問題 —— 而他會覺得自己查到了。**

**🔴 為什麼不併進「grep 到第一個命中就停」那一族**:
```
那一條講的是【同一支檔裡有更新的那個命中】⇒ 多讀幾行就救得回
🔴 本條是【不同的檔、不同的主題，而字面完全相同】
⇒ ⇒ 「多讀幾個命中」也救不了你，除非你去讀那個命中的【上下文】
```
⇒ **它比那一條狠一格, 而修法不同 ⇒ 新開。**

🔴🔴 **而這一條在【同一夜有三個現場, 跨三個窗】—— 這是本份唯一一條有跨窗複現的**
(2026-08-30 00:3x 補;前兩個是我自己的與 `-b4` 主動送來的):
```
現場一（-b9）grep '不用改名' ⇒ 撞到 import 洩漏那則，字面一模一樣、主題無關
現場二（-b4）grep 'price_store' 在 admin_create_manual_order 那支 ⇒ 2 行
            🔴 一度讀成「它有查價、前人寫錯了」
            開檔（-b9 獨立複驗）：那 2 行是 :383-384 一道【拒絕】守門
            RAISE EXCEPTION 禁止 line_spec 出現價格欄名（防經銷價洩漏）
            📌 它不是在查價，它在【禁止】那些字出現
現場三（-b4）A7C_VERIFY_PORT —— 那個識別字真的存在，而職務不同
```
📌 **⇒ 三個現場的共同形狀:字面在, 而它扮演的角色與你假設的【相反或無關】。**
🔴 **而現場二最貴** —— 一個【禁止】的守門, 被讀成一個【查詢】的實作
⇒ **那會讓人得出「這裡已經有防護了」而停止追查。**

**判別句**:**一個字面命中告訴你「這幾個字在那裡」, 它不告訴你「那幾個字在講你問的那件事」。**
⇒ **加一句(現場二帶出來的)**:**尤其分不出【做這件事】與【禁止這件事】—— 而它們的字面一樣。**
🔴 **而這個病長在 `scripts/before-asking-sean.sh` 的主要用法上** —— 它的第②發正是**用他的話去 grep**。
⇒ **那一句要寫進那支工具自己的射程節**(主視窗 `-48` 已派給該支檔的作者 `-e9`, **不是我**)。

---

## ⚠️ 本份自己的射程
```
· ①②③⑤ 來自【同一夜、同一個窗】⇒ 沒有跨窗複現，n=1 的方向要打折
· 🔴 而 ④ 是例外：同一夜三個現場、跨三個窗（-b9 一個、-b4 兩個）
  ⚠️ 而我原本在這一行寫「五個坑全部沒有跨窗複現」—— 那句話在我寫下它之後
     二十分鐘就被推翻了，而推翻它的是 -b4 主動送來的一則訊息。
  📌 ⇒ 一份交件的射程宣稱，在它落檔的那一刻就開始過期，而沒有東西會叫。
· ③ 那條我只量到【一組】撞號，而那一組是我自己造成的
  ⇒ 「全 repo 唯一一組」是量到的；「這種事很少發生」【沒有量】
· ①的「學習曲線」是四個時點的觀察，不是一個統計
· 併/新開的判定是我開檔讀完之後的判斷 —— 而工具自己明寫分數對這個問題【零判別力】
  ⇒ 判錯的話：判【併】錯了下一個人可以拆開，判【新】錯了另一半會永遠留在 inbox
  ⇒ 所以 ②③④ 三條新開，我各自寫了「為什麼不併」，讓拆併的人有得反駁
```

---
---

# 三、追加第 6 條(2026-08-30 01:4x)⇒ **判【併】進正本 `:10898`「尺撞到講述它自己的文本」的第六次**

## 查重紀錄
```
python3 scripts/traps-neighbours.py --selftest ⇒ PASS
草稿餵它 ⇒ 第 3 名 0.2686 正本 :10898「尺撞到講述它自己的文本（2026-08-19 一天內五次）」
         第 1 名 0.3070 正本 :15524「我的分母裡有【我自己寫的話】」
✅ 兩條【都開檔讀了】⇒ 判【併】進 :10898，並與 :15524 互指
```
🔴 **為什麼是 `:10898` 不是 `:15524`**:`:15524` 講的是**我為了做這件事而寫的字**(prompt / 交辦 / 檔頭)進了分母;
**本則的那段字不是我寫的, 是【另一個人】為了記錄這個缺口而寫的** ⇒ **形狀更接近 `:10898` 的「產物自己解釋自己」。**
📎 而 `:10898` 現有的第三個實例逐字就是「`grep -c "lib/session"` 命中 1 ⇒ 而那 1 是【註解】」⇒ **同一個形狀。**

## ⇒ 現場
```
-01 廣播（量於 2026-08-30 01:xx）：storefront 的 @tailwind 指令 ⇒ 0
-b9 自己重量（約 20 分鐘後）    ⇒ 1
開檔看那個命中：
  apps/storefront/src/app/account/orders/[displayId]/statement/page.tsx:96
  逐字 //      storefront 的 `@tailwind` 指令 ⇒ **0**。
```
✅ **廣播的 0 在它量測的當下是對的。**🔴 **是那段註解讓它從 0 變成 1。**

## 🔴 而**本則多的那一半**(`:10898` 現有五個實例都沒有這一格)
```
現有五例的形狀：尺撞到【剛好談論那件事】的文字 ⇒ 那是巧合
🔴 本則：那段文字是【專門為了記錄那個 0】而寫的
⇒ ⇒ 記錄一個缺口的動作，親手把那個缺口的量測值改掉了
```
📌 **⇒ 而傷害的方向是【反過來的】**:下一個人量到 **1** ⇒ 得出「storefront 有 Tailwind」
⇒ **那正是那段註解要防的事。**
🔴 **⇒ 一段誠實的、寫得很好的、目的正確的註解, 讓下一個人得到與它相反的結論。**

📎 **而 `:15524` 那句要一起帶**(它解釋了為什麼沒有人會發現):
> **「0 會讓我懷疑, 1 不會。」**
⇒ **一個從 0 變成 1 的假訊號, 恰好落在【最不會被懷疑】的那個值上。**

## ⇒ 可執行(兩端各一條)
```
量的那一端：用 grep 數「某個東西存不存在」⇒ 命中之後【開檔看那一行是碼還是註解】
           （正本已有 stripComments / 限定區塊 / 排除 ~~…~~ 三種做法）
🔴 寫的那一端（本則新增）：寫下「X 在這裡是 0」這種註解的人，
   要知道自己剛剛讓那個 0 變成 1 ⇒ 那句話要寫成【不會被自己的尺撈到】的形狀
   （例：不要重複那個字面，改寫「此 app 未接 Tailwind 管線」）
```

## 🔴🔴 而**它不是 n=1** —— 主視窗 `-48` 當場把同一夜的四個排出來, **而其中一個是它自己**

> ✍️ **具名**(主視窗 `-48` 自己要求具名, 逐字「**我那一次要具名 —— 那讓它不是一條『小心一點』, 是一個有分母的東西**」)

| # | 誰 | 那段字是為了什麼寫的 | 它對哪把尺造成什麼 |
|---|---|---|---|
| ① | `-b9`(我) | 在交件裡寫射程宣稱「五個坑全部沒有跨窗複現」 | **二十分鐘後被推翻** —— 寫下的那一刻就開始過期 |
| ② | `-e9` | `is-this-still-true.sh` 的**負對照字面寫在它自己檔裡** | **一 commit(untracked ⇒ tracked)就撈到自己** |
| 🔴 ③ | **`-48`(主視窗本人)** | 把「負對照 `zzq8842nothere` ⇒ 0」那句話**寫進它自己要掃的那個檔** | 同上 |
| 🔴 ④ | 本則 | 一段記錄「storefront `@tailwind` ⇒ 0」的註解 | **讓那個 0 變成 1** ⇒ 下一個人得出相反的結論 |

📌 **⇒ 四個裡有三個是【負對照或射程宣稱】** —— **也就是我們為了證明尺是活的而寫下的那句話, 自己進了分母。**
🔴 **⇒ 而 ④ 最狠, 因為它的傷害方向是【反過來的】**:①②③ 造成的是**假的命中**(看得出不對勁);
**④ 造成的是一個【看起來完全正常】的 1, 而它推翻的正是那段註解要保護的結論。**

## ⇒ 而修法**不是「不要寫那段註解」**(主視窗指定寫進來)
```
🔴 修法是【那把尺要排除註解】
⇒ 而那正是本檔 ② 那一條在講的同一件事：
   「一把不剝註解的尺，會把明文說自己沒做這件事的檔算成犯規者」
📌 ⇒ 本則與本檔 ② 是【同一個機制的兩個受害者】：
   ② 的受害者是【被誤判成犯規的檔】、本則的受害者是【下一個量它的人】
```

## ⚠️ 射程
```
· 「專門為了記錄而寫」這個子型（④）目前 n=1；而整族今晚 n=4（上表）
· 而它與本檔漏出① 是【同一夜、同一個母題的兩個方向】：
  漏出① 是【我寫下的射程宣稱過期】、本則是【別人寫下的記錄改變了被記錄的東西】
  ⇒ 兩者都不是「量錯」，是【寫下來這個動作本身有副作用】
· 🛑 我沒有回去問那段註解的作者 —— 我不知道他有沒有意識到這件事
```

---
---

# 四、追加第 7 條(2026-08-30 02:2x)⇒ **判【併】進正本 `:1431`「一句對的話, 在它描述的世界消失之後, 會原封不動地繼續正確著」**

## 查重紀錄
```
--selftest ⇒ PASS
草稿餵它 ⇒ 第 8 名 0.2122 正本 :1431（就是這一條）
         第 5 名 0.2185 memory feedback_a-number-moving-the-right-way-stops-inquiry
         第 3 名 0.2212 正本 :23506「一個修好的缺陷，不會自己去找它的兄弟」
✅ 三條都開檔讀了
```
🔴 **判併進 `:1431` 而不是新開** —— 它就是母題。**而 `:23506` 判不併**:那條講的是
**一個修好的缺陷【沒有去找它的兄弟】**(橫向:同族還有幾個);**本則講的是【開處方的那份文件沒有被劃掉】**(縱向:處方 ↔ 執行)。

## ⇒ 而**本則多的兩樣**(`:1431` 現有版本沒有)

### ① **它有分母 —— 同一夜四次, 而四次都有落點**
| # | 那份文件說 | 而實際上 | 誰撞到的 |
|---|---|---|---|
| 1 | `⟦b4-MAILQUOTA1⟧`「寄信額度會爆而**沒有任何東西在看它**」 | 偵測器**已經做出來了**(`check-anomaly-alerts.ts:97/98/545/548/829`) | `-b4` |
| 2 | `#957`「那支 migration 今天重跑就會炸」 | 那條路**走不到**(窄 view 已在正式庫上, `APPLIED.tsv:284`) | `-e9` |
| 3 | `⟦b4-EMAILNOISE⟧`「貼修法前要先讀正式庫兩個數」 | **那兩個數當晚已經讀到了**, 而**它就記在那一列自己身上** | `-e9` |
| 4 | 「永久漏掉, **而沒有人知道**」 | 死信告警**已經接上**(`check-anomaly-alerts.ts:544/827`) | `-b9`(我) |

### ② 🔴🔴 **方向是【一致的】—— 四次全部是「往好的方向」**
```
沒有一次是「文件說已經修好，而其實還壞著」
四次都是「文件說還壞著，而其實已經修好」
```
📌 **⇒ 而那個方向【最不會被回頭查】** —— 一個說「還沒做」的紀錄, 讀起來像個待辦,
**而沒有人會去查一個待辦是不是已經不存在了。**
📎 **這正是 memory `feedback_a-number-moving-the-right-way-stops-inquiry` 的【文字版】**
(那條講**數字**往好的方向走 ⇒ 沒有人回頭查;本則講**文件**停在壞的那一刻 ⇒ 沒有人回頭劃掉)
🔴 **⇒ 兩者合起來是:【好消息與壞消息的複查率不對稱】。**(`-e4` 2026-08-30 提出這個合併, 我採。)

### ③ 而**它與 `:1431` 的機制【不同】, 那個差別要留著**
```
:1431 的機制：那句話與【它自己造成的改變】放在【同一份檔】裡，而它沒有跟著改
🔴 本則的機制：處方與執行在【兩個不同的地方】——
   板子的一列 / 交接檔的一段話    ←→    某一支 .ts 檔裡的幾行
   而【它們之間沒有任何連結】
⇒ ⇒ 所以 :1431 那條的修法（改檔時順手看一眼同檔的舊句）對本則【無效】
```

## ⇒ 可執行(而它不是「小心一點」)
```
🔴 寫下「這件事還沒做」的時候，同時寫下【它做完的時候長什麼樣】——
   一個可以被 grep 的錨（函式名 / 欄位名 / 檔案路徑 / 旗標名）
⇒ 那個錨就是【處方 ↔ 執行】之間唯一可能建得起來的連結
📌 而那與 -0b 的錨紀律（板子每一列至少一個可單發探測名）是【同一半】——
   錨紀律讓人查得到那一列，本條讓那一列查得到【它自己是不是已經過期】
```
🛑 **而本條【不主張立刻做成機制】** —— 主視窗 `-48` 已派 `-e9` 去問「做不做得成 / 做不成的理由」。
⇒ **在那個答案回來之前, 本條只主張【寫的時候多寫一個錨】, 那是零成本的一半。**

## ⚠️ 射程
```
· 四個實例【全部來自同一夜、同一支艦隊】⇒ 不是「這個 repo 的長期比例」
· 🔴 而「四次都往好的方向」這個觀察本身有一個選擇偏誤：
  往壞的方向那種（文件說修好了而其實還壞著）——
  ⇒ 它【不會在盤點時被撞到】，它會在【出事的時候】被撞到
  ⇒ ⇒ 所以「今晚零次」不代表它比較少，只代表【今晚沒有出事】
· 我沒有量過整張板上有幾列是這種狀態（那要逐列去核，沒有人做過）
```
