## 🔴🔴 三個 17 —— 我為了記錄缺口而寫的註解,被【偵測那個缺口的量具】數了進去,而假訊號**剛好等於真值**(2026-08-27 Gilles 線下游窗,具名自寫)

> **一句話**:補漏時我在 manifest 裡寫了一段註解記錄「還有三支沒登記」,
> 那段註解裡的**三個檔名**被 `grep -c` 數了進去 ⇒ **量具印 17,而磁碟也是 17**
> ⇒ 看起來**完全同步**。真值 **14**,缺 **3** 支 —— 正是註解裡那三個。

## §0 我開了哪幾條、為什麼這樣判(§12-36)

跑了 `traps-neighbours.py --selftest`(六世界 PASS)+ 餵草稿(分母 1610)。**逐條開檔讀過**:

```
第 6 名 0.1566  正本 :14382「我的分母裡有【我自己寫的話】—— 註解/prompt/指令會親手把守門餵綠」
   ⇒ 🔴 **同族,而且是【意圖】那一半的正本。**判別句逐字:
     「我掃的這個字,會不會出現在【我自己為了這件事寫下的那段文字】裡?」
     既有實例 A=W4(重 gen 清單)/ B=主視窗,兩者 2026-08-20 同夜。**我是實例 C。**

第 8 名 0.1551  正本 :9240「尺量的是【字元】,而宣稱量的是【結構】—— 註解裡的字和 code 長得一模一樣」
   ⇒ 🔴 **同族,而且是【機制 + 修法】那一半的正本。**它的實例逐字:
     「數 `<th>` 當欄數時,把註解裡的 `<th>` 也數進去」——**與我這格一字不差的機制。**
     它的處置:「原始碼層的結構斷言一律先 `stripComments`」。
     ⚠️ 而它的載體是 **TS/TSX 原始碼**(共用工具 `strip-comments.ts`,2026-08-26 追記 `.sql` 有兩份實作);
     **YAML 清單檔沒有對應工具** ⇒ 我的修法是它在 YAML 上的等價物(只認結構化的清單行)。

第 1 名 0.4571  memory/feedback_documenting-a-gap-hides-it-from-the-ruler.md
   ⇒ **那是我自己今晚剛寫的**(自撞)。已改寫成指回本檔與上面兩條正本,不留第二份全文。
第 5 名 0.1587  正本 :14670「量具給了你一個【講得通的錯誤故事】」⇒ 近親但不同刀:
   那條講「錯的那發長得像一個發現」;本條講「錯的那發長得像**沒事**」。
第 2/3/4/7 名 ⇒ 開過,不同族(寫死兩支檔名 / 換載體就再踩 / 保留條款要寫在會被搬走那句 / 量具站錯位置)。
⚠️ **我沒有掃到**:`lessons-learned.md` §12 全文、其他窗今晚的 inbox。
   另照工具指示拿機制關鍵字 grep 過標題行。
```

**⇒ 判定:不立新條。** 本檔 = **:14382 的實例 C** + **:9240 在 YAML 載體上的新實例**。
🔴 **而它同時撞到兩條正本這件事本身值得記** —— 那兩條**互相沒有交叉引用**,
一條講「為什麼會發生」(自己寫的話進了分母),一條講「怎麼修」(先剝註解)。
**踩到的人要同時找到兩條,而排名把它們放在第 6 與第 8。**

🔴 **查重那一格**:真正同族的兩條排在 **6 與 8**,前五名沒有一條同族。
⇒ **救回來的是「開檔讀」那個動作,不是排名。**(正本自列六種失效的第 ①。)
📌 而**第一次發現這件事的不是我,是主視窗** —— 它看到 `17 = 17` 時說
「**兩者印同一個 17**,你自己確認」。**那個懷疑沒有任何機械訊號觸發,是「這數字太剛好」的直覺。**

## §0-b 🔴 這一族有【四種】,而只有三種能靠「重跑」抓到

彙整本檔四個實例(我 1 + 線4 `-26` 3)之後,浮出一個分類 —— **請先讀這格,不要只讀實例**:

| # | 形狀 | 曾經為真嗎 | 重跑抓得到嗎 |
|---|---|---|---|
| ① | 數法把**自己**算進分母(`-26` F-1 / 第三例) | 曾經真,出生後假 | ✅ 數字對不上 |
| ② | 數字描述**我出生前**的世界,而我的出生改掉它(`-26` F-6) | 曾經真,出生後假 | ✅ 數字對不上 |
| ③ | 註解把檔名寫進去 ⇒ 印出**完美同步**(本檔 §1) | 曾經真,寫下註解後假 | ⚠️ 抓得到,**但沒有人會去抓** —— 它印的是「相等」 |
| ④ | **量測的時點是 T2,而句子描述的是 T1**(`-26` F-5) | 🔴 **從來沒有為真過** | ❌ **重跑也發現不了** |

🔴 **④ 為什麼特別**:前三種重跑一次就露餡(數字對不上);
**④ 你重跑一百次都對不上,而你不會知道那個數字屬於哪一刻** —— 得**逐點回溯**才看得出來。

> **「量測的時點」與「宣稱的時點」是兩件事,而寫下來的時候它們長得一樣。**

📌 **而 `-26` 的 hash 錨同時治 ② 與 ④**:
**它逼你寫出「在哪一顆上量的」,而那一寫,T1 與 T2 就分開了。**

---

## §1 現場

```
起點:codex 對抗審查抓到 GillesShowcase.tsx 沒登記進 docs/design-storefront-manifest.yaml
我補上它, 並【在同一個檔案裡寫了一段註解】記錄「還有三支也沒登記」
  ⇒ Kspeed / ExtremeComponents / Dna 三個檔名, 就這樣進了 manifest 的文字內容

主視窗複量:
  grep -c 'Showcase.tsx' <manifest>                    ⇒ 17
  find apps/storefront/src -name '*Showcase.tsx'|wc -l ⇒ 17     ⇒ 「同步了」

真值:
  grep -cE '^\s*- "apps/storefront/src/components/[A-Za-z0-9]*Showcase\.tsx"' <manifest> ⇒ 14
  負對照 zzzShowcase.tsx ⇒ 0
  ⇒ 缺 3 支, 正是我註解裡提到的那三個
```

## §2 🔴 三個實例合起來,多告訴我們一件單看一個看不出來的事

| 實例 | 假訊號 | 真值 | 長相 |
|---|---|---|---|
| A(W4,重 gen 清單) | **7** | 0 | 偏大 ⇒「怎麼多出來?」會引人去查 |
| B(主視窗) | **1** | 5 | 偏小 ⇒「怎麼少這麼多?」會引人去查 |
| **C(本檔,manifest)** | **17** | 14 | 🔴 **剛好等於另一側的真值 17** |

🔴 **C 是三種裡最兇的**:A 與 B 至少「數字看起來怪」,
而 **C 印出來的是一個【完美同步】** —— 它不引起任何人去查,**它是結案的形狀**。

📌 一般化:**假訊號的危險程度不看誤差大小,看它【長得像不像一個好消息】。**
偏離真值 3 的數字,若那個 3 剛好把它推到對照組的值上,它比偏離 7 的更難被發現。

### 🔴 這一族裡的「吻合」亞種,值得單獨標出來(線4 `-26` 提)
線4 同夜另撞一次**同形狀**:用一把窄尺數 URL 參數得到 **11**,
**剛好等於 repo 裡另一份權威清單的 11** ⇒ **兩個錯誤互相印證**,
而那個吻合**看起來正是「我量對了」的證據**。
```
一般的假訊號:   印出一個對不上的數字 ⇒ 會被追
「吻合」亞種:   印出【兩邊相等】     ⇒ 它是結案的形狀, 沒有人會回來查
⇒ 判別句:我這個「相等」, 是兩邊各自量到的, 還是【其中一邊的誤差剛好等於另一邊】?
```

⚠️ 而這也補了 :9240 的方向表 —— 它列了「多報 ⇒ 假紅(吵、會被修)」與
「少報遮蔽 ⇒ 恆綠」兩個方向,**而本例是第三個:多報,而多出來的量剛好把它推到看起來正確的值上。**

## §3 我的一般化那半(兩條正本都沒有,值得往上收)

> 🔴 **任何「掃檔名 / 掃關鍵字」的量具,都會把註解、待辦、訃聞(`~~舊字面~~`)、交接檔、backlog 條目一起數進去**
> —— 而這幾種文字**恰好最常出現在「這裡有問題」的旁邊**
> ⇒ **問題越被好好記錄,量具越可能說沒問題。**

📌 與今晚全隊在打的「分母太窄」**反向**的同一族:
**分母太寬,而寬的來源正是我們自己的誠實。**
⇒ 一個團隊越守「發現問題就寫下來」的紀律,越容易生出這種假綠。

## §4 修法(:9240 的 YAML 等價物;與 :14382 方向正交,兩個都要)

```
:14382 的處置 = 「驗清單上寫的東西【真的還在】」                ← 防清單長出幽靈
:9240  的處置 = 「原始碼結構斷言先 stripComments」              ← 防註解混進分母(TS/TSX 有共用工具)
本條的處置    = 「只認【結構化的那一種行】, 比對【集合】不比對【筆數】」 ← 同一件事在 YAML 上的做法
```

落地:`apps/storefront/src/components/showcase-manifest-coverage.test.ts`(commit `5ca2a291`)
- 只認 `^\s*- "apps/storefront/src/components/XxxShowcase\.tsx"` 這種**清單項**,不認註解行
- 比對**集合**(誰在誰不在),不比對**筆數**(`N == M`)
- 抽取邏輯自帶**自檢兩條**(含負對照:全是註解 ⇒ 回空集合)
- **分母非空才有判別力**:磁碟或 manifest 抽不到東西 ⇒ 紅,不是靜靜變「零缺口」
- 反向也守:manifest 登記了磁碟沒有的檔(改名/刪除)⇒ 紅
- 雙向突變實測(跑完全數還原、零留痕):磁碟多一支未登記 ⇒ 紅;欠帳表已補卻沒移除 ⇒ 紅

## §5 🔴 附帶解掉的另一個通病:白名單不會提醒任何人它該被清空

三支既有欠帳放進 `KNOWN_UNREGISTERED`,**明寫成「一張要被清空的欠帳表,不是白名單」**,
並且**另有一條斷言盯著它**:某支補進 manifest 卻沒從表裡移除 ⇒ **紅**。

```
一般白名單: 債還了 ⇒ 白名單多一筆沒用的 ⇒ 零訊號 ⇒ 它永遠留著
本條做法:   債還了 ⇒ 立刻紅, 訊息直說「請從 KNOWN_UNREGISTERED 移除」
```
📌 判別句:**我加的這筆豁免,在它不再需要的那一刻,會不會有東西告訴我?**
答不出來 ⇒ 那是白名單,不是欠帳表。

## §6 根因那一格(為什麼判「缺閘」不是「缺注意力」)

```
git log -S KspeedShowcase -- docs/design-storefront-manifest.yaml  ⇒ 1 次命中
                                        而那一次就是 2026-08-27 補漏那顆 e92ad884
ExtremeComponentsShowcase / DnaShowcase 同 ⇒ 各 1 次, 也都是 e92ad884
git show 6a5ffca3 -- <manifest> | grep "^-" | grep -c Showcase      ⇒ 0
```
⇒ 這三支**從來沒有被登記過**,也不是被 2026-08-07 那顆還原 35 檔的 revert 刪掉的。
⇒ **三次獨立地漏掉同一件事**(2026-07-24 ×2、2026-08-21 ×1),2026-08-27 差點第四次。
⇒ **缺的是一道閘,不是一次注意力。**(`~/.claude/rules/00-work-rules.md` §4 機制優先律)

---

## §7 🔴 線4(`-26`)同夜的三個實例 + 一句正本沒有的判詞(**併入本檔,它不另開檔**)

線4 自己先開檔核過 `:14382` 才判同族。它的原文我逐字收在下面;
⚠️ **它明說「沒有開我的檔核過我那格」,對我那格的描述屬讀來** —— 我這邊複查過,描述與事實相符(14 → 17,而磁碟 17)。

### 三個實例(commit `fa3c1cff` / `4e5dbcc3`;完整記錄 `docs/probes/2026-08-27-line4-test-claim-audit.md` §2 F-1 / §5-1 / §5-2)

```
F-5  apps/storefront/src/app/account/orders/[displayId]/page.test.tsx:6-7   ← 🔴 這支是【第 ④ 種】
     檔頭宣稱「這個目錄零測試」, 數法逐字 grep -rln "account/orders" --include='*.test.*'
     宣稱只命中 2 支。
     🔴 而它【從來沒有為真過】—— 本窗獨立逐點重跑那條數法複核(不是轉述):
        ae7dffa9(建檔那顆的父)⇒ 1 支
        883ee024(建檔那顆本身, 2026-08-23)⇒ 3 支
        HEAD ⇒ 3 支(page.test.tsx 自己 / OrdersTab.test.tsx / safe-redirect.test.ts)
     ⇒ 「2」在任何一顆 commit 上都不成立。
     成因:被引用的那支檔在父 commit 上【存在而零命中】, 是在建檔那一顆才被加上連結
     ⇒ 「2 支」是「已改 A、而 B 還沒寫進去」那一瞬間的狀態,
        **只存在於作者當時的工作樹裡, 從來沒有進過版控。**

F-6  apps/storefront/src/app/account/page.test.tsx:305   ← 🔴 線4 當天親手寫的
     逐字「本檔全部 15 格照樣綠」⇒ 該檔現在 16 格
     (`grep -c '^  it('` ⇒ 16;實跑 Tests 16 passed)
     🔴 而那第 16 格, 就是這句話所在的那一格
     ⚠️ 寫完到過期只隔 90 分鐘, 而寫的人【全程開著這個意識】

第三例 apps/admin/src/lib/customers/load-customer-detail.test.ts:27-28
     宣稱「數法 ⇒ 0 行」而現在印 1, 那 1 也是註解自己
```

### 🔴 時間軸判詞(正本 `:14382` 沒有這一句 —— 它講「分母裡有我寫的話」,這句講**為什麼它必然發生**)

> **一個描述「我出生之前的世界」的數字,會被【我的出生】改掉 —— 而那句話留在原地。**

它幾乎是**結構性必然**:
```
一支測試檔在檔頭寫「我為什麼存在」, 最有說服力的寫法就是
  「在我之前, 這裡 grep 出來是 0 / 是 N」
⇒ 而那條 grep 的關鍵字, 正是這支檔的主題
⇒ 它一定會命中自己
```
📌 **越是誠實地記錄「我出生前的覆蓋率」,越保證那個數字會在出生當下作廢。**
⇒ 三個實例都是有紀律的人寫的(其中一次是意識全開時寫的)⇒ **不是紀律問題,是形狀問題。**

📌 而它與本檔 §3 那半**互補**:
```
§3(我的)  :  分母太寬 —— 我寫的字被【現在的量具】數進去
§7(線4的) :  時點錯位 —— 我寫的數字描述【我出生前】, 而我的出生把它改掉了
⇒ 兩者都是「作者本人成為被量對象的一部分」, 而一個發生在【空間】(分母)、一個在【時間】(時點)。
```

### 修法:附 commit hash 錨(與正本 `:14382`、`:9240` 方向都不同,三個都要)

```
:14382 的處置 = 驗清單上寫的東西真的還在
:9240  的處置 = 結構斷言先 stripComments(TS/TSX 有共用工具;YAML 見本檔 §4)
線4 的處置    = 寫「出生前」的數字時【附 commit hash 當錨】
                「在 `<hash>` 之前 ⇒ 0」—— 那句話永遠為真, 因為它綁死了一個時點
🔴 而它是三個候選裡唯一【零額外工具】的:
   甲 附 hash 錨                ← 線4 推薦。把一個會過期的宣稱, 換成一個不會過期的
   乙 數法加 --exclude=<本檔>   治得了自我命中, 治不了「後來又多了一支」
   丙 不寫數字, 改寫「這支檔存在的理由是 X 沒有守門」(去掉可過期的部分)
```
