# 一個**真的讀數**,加上一條**我沒察覺自己做了的接線** —— 同一夜三次,方向都朝著我要的故事

> 線 `-f3` · 2026-09-06 · ⟦search-TAXONOMY2MB⟧ 查因途中。主視窗指定投收件匣(兩格併一篇)。
> 🔎 **查重跑過**(`traps-neighbours.py`,分母 2934):第 1 名
> `memory/feedback_described-intent-not-the-write-result`(0.2014)· 第 2 名 正本 `:21026`(0.1989)。
> **兩條都讀過** ⇒ 🔵 **另立**,理由在最後一節。

## 三次

| # | 我看到的(**都是真的**) | 我加上去的那條線(**沒察覺**) | 真相 |
|---|---|---|---|
| 1 | log 印 `rows=12197` | 「所以**快取了** 12,197 列」 | `rows=` 是**它撈了多少**,不是**它存了多少** |
| 2 | log 印 `items over 2MB … (2679379 bytes)`,**就在車款表那行旁邊** | 「所以車款表是 2.68 MB」 | 那行的鍵帶 `per=100&vehicle=…` ⇒ 是**商品清單**那一支;**相鄰不是因果** |
| 3 | 七發 log 都印 `[vehicleTaxonomy] cold …` | 「所以**每一發**都 cold」 | 那行**只在 miss 時印** ⇒ **命中的什麼都不印**;我看到的是**倖存者** |

實測佐證:同一窗 `group_by=requestPath` 印 `/products` **23 筆**,而帶那行 log 的只有 **7 筆**。
(⚠️ 我的查詢帶 `limit` ⇒ 7 是**我抓回來的**不是那一窗全部的;能斷言的只有「**不是每一發**」。)

## 🎯 三次的共同形狀

> **讀數是真的。錯的是我在讀數與結論之間加的那一步,而那一步【沒有出現在任何一份輸出裡】。**

· 它不像「量具壞了」—— **量具是好的**。
· 它不像「我沒查證」—— **我查證的是那個讀數,而讀數通過了**。
· 🔴 **而三次的方向都朝著我當時正在找的那個故事**:
  找迴圈時它把數字放大、找 2MB 兇手時它把兇手指給我、找「為什麼慢」時它說「每一發都這樣」。

## ✅ 判別句(落筆寫「所以」之前問一次)

> **這個「所以」，是那行輸出【自己說的】，還是我接上去的?**

三個具體的追問(各對應上表一列):
1. 這個數字的**主詞**是什麼?(撈 / 存 / 送 / 收 —— 四個不同的動詞常共用一個名詞)
2. 這兩行 log **有沒有共同的鍵**?沒有 ⇒ 它們相鄰只代表**時間相近**。
3. **沒印出來的那些是什麼意思?** —— 只在某條件下印的 log,它的**沉默也是資料**。

## 🔵 與最近兩條的差別(為什麼另立)

· `feedback_described-intent-not-the-write-result`:講**寫入**動作失敗時不出聲 ⇒ 我描述意圖而非結果。
  **本條的動作全部成功**,錯在**讀出來之後**。
· 正本 `:21026`(量具裡藏著假設、回一個乾淨的數字):**那裡的尺是壞的**;
  **本條的尺是好的** —— 三個讀數今天拿去重跑都會印同一件事。
📌 **⇒ 前兩條治「不要相信壞掉的輸出」;本條治「輸出是對的,而你的下一句話不是它說的」。**

## 近親
· `docs/patterns/traps-inbox/front-20260906a-計數器沒歸零讓後面每一格都假紅.md`(同夜、同人;那條是**尺的作用域**)
· memory `feedback_absence-read-as-verified`(沉默被讀成驗證過)—— **第 3 列與它同族**
