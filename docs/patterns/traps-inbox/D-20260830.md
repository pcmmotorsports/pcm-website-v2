# 線D traps(2026-08-30)

> 依 `README.md` 慣例:一窗一檔、暫存待併入 canonical `../guard-and-instrument-traps.md`。
> ✅ **查重已跑**(`--selftest` ⇒ PASS 九個世界;而集中度那一行印「沒有單一檔案占掉 2 名以上」)。

---

## D-1 一個 view 的【行為】由 `WHERE` 決定,而它的【約定】住在 `COMMENT` 裡 —— 兩個都要讀

**當班實錘(2026-08-30)**。

我要判「出貨通知信第一次接上線會不會對舊單補寄」,於是去讀那支 view 的 `WHERE`:
```
WHERE s.shipped_at IS NOT NULL AND s.deleted_at IS NULL AND (…) AND NOT EXISTS (…)
🔴 零時間界線（interval 0 ／ now() - 0 ／ days 0 ／ shipped_at > 0，負對照 0）
```
⇒ 我得出「它會撈到所有歷史單」(**那是對的**),
⇒ 然後把「那顆 cutoff 要用自己一顆、還是共用既有那顆」寫成一個**要 Sean 拍的設計題**。

### 🔴 而答案早就在【同一支檔】的 `COMMENT ON VIEW` 裡
```
'⚠️ 本 view 【不】自帶起始線 —— 它回的是【歷史上全部】還沒排過信的配對。'
'cutoff 由呼叫端以 shipped_at 條件加上（env SHIPPED_EMAIL_CUTOFF，
  鏡像 order_created 那條的 B4_DEPLOY_CUTOFF）'
```
✅ **⇒ 那顆 env 的【名字】與【它是自己一顆】,寫這支 view 的人早就定了。**
🛑 **⇒ 所以那個「設計題」不存在 —— 而我差點拿它去佔 Sean 一次注意力。**

### 📌 判別句
> **一個 view 的【行為】由 `WHERE` / 索引 / 觸發器決定;**
> **而它的【約定】(誰負責給什麼、為什麼這樣切、哪些東西刻意不在裡面)住在 `COMMENT` 裡。**
> **⇒ 兩個都要讀。而讀碼的人的習慣是只讀前者。**

🔴 **而它為什麼特別容易漏**:
```
· WHERE 讀起來像「事實」，COMMENT 讀起來像「說明」
  ⇒ 而我們被訓練成「不要相信註解，要看碼」
  ⇒ ⇒ 而那條訓練在這裡【剛好害了我】：那段 COMMENT 不是在描述碼，是在【指派責任】
· 而 COMMENT ON VIEW 在檔案的【最下面】，離 WHERE 幾十行
· grep 一個欄位名會命中 WHERE，而不會讓你讀到 COMMENT
```

### 下次那一刻要做的動作
> 讀一支 view / 表 / 函式要下任何「它會不會…」的結論之前,
> **把那支檔的 `COMMENT ON` 段落整段讀完** —— 不是 grep,是讀。
> ⇒ 而具體到可執行:`grep -n 'COMMENT ON' <檔>` 拿到行號,然後**開那幾段**。

⚠️ **鄰居但不同**:正本 `:10097`「守門掃【來源】,而人讀的是【輸出】」——
那一條講的是**兩個不同的產物**(原始碼 vs 渲染結果),而**尺與人不在同一個平面**;
**本條講的是【同一支檔的兩段】,而讀的人只讀了其中一段。**
📌 **⇒ 共通的母題是「答案在一個你的習慣不會去的地方」,而落點不同。**

### ✅ 而它有一個【正面】的對照,同一支檔裡
同一支 migration 另有 `public.pcm_shipped_email_unsendable`,而它的 COMMENT 逐字:
> 「分開的理由…它們永遠不會消失,留在這裡會由最舊那端永久擋住後面的有效信」

📌 **⇒ 也就是:這個 repo 已經有一個慣例 ——【被排除的東西要有自己的一張表,不是消失】。**
🔴 **⇒ 而那個慣例也只寫在 `COMMENT` 裡。** 我是因為這一次去讀了才知道它存在。
⇒ **一個沒有人讀的慣例,與一個不存在的慣例,在下一個人身上是同一件事。**

---

## D-2 一個【在 repo 裡而不會再變】的東西,守它會得到一個永遠綠的守門

**當班實錘(2026-08-30)**。我要替 `a8a3g` 那條約定補守門
(`begin_charge_attempt` 的 sibling dedup 要保留 `o.cancelled_at IS NULL`,
違反 ⇒ 客人取消一張再下一張,新單被判成重複 ⇒ 結帳被擋、那筆錢收不到)。

**我原本要守的是那條約定住的那支檔** `20260820020000_..._cancel_guard_sibling_dedup.sql`。

### 🔴 那守不到東西 —— 而它會全綠
```
已 apply 的 migration【沒有人會去改】⇒ 它的內容是凍住的
⇒ 守它 = 守一個不會動的東西 ⇒ 那道守門【永遠綠】
⇒ ⇒ 而它每天都在跑、每天都印綠 ⇒ 看起來像一道很健康的守門
```
📌 **⇒ 這是「恆綠」的一種新來源:不是斷言寫錯,是【守錯了對象】。**
**⇒ 前者(`toBeGreaterThan(-1)` 那種)在檔案裡看得出來;**
**⇒ 這一種【每一行都寫得對】,錯的是它指的那個東西不會變。**

### ✅ 修法:守【最新那支定義它的東西】,不是【約定寫在哪】
實測 `begin_charge_attempt` 至今被重新定義過 **6 次**
(`m3_s2d` / `m3_3ds_0b` / `m3_3ds_0c` / `a8c1` / `l4a1` / `a8a3g`)⇒ **風險在第 7 次**。
`find_active_sibling_own` 2 次。⇒ 守門改成:掃出最新那支,斷言它的**生效碼**含那個條件。
落地在 `scripts/a8a3g-cancel-guard-contract.test.ts`(`1daeed8b`)。

📌 **⇒ 而那個 6 把一個設計選擇變成一個有分母的判斷 ——**
**在量它之前,「守哪一支」看起來只是品味問題。**

---

## D-3 「repo 裡有」不等於「今天跑得起來」—— 一個看起來在的答案,而它不在

**當班實錘(2026-08-30)**,而它是「答案已經在 repo 裡而沒有人套過來」那一族的**反例**。

`a8a3g` 的 COMMENT 逐字:「⚠️ **本 repo 測試層碰不到 DB ⇒ 這條沒有常設守門**」(2026-08-20)。
我去查那句話今天成不成立,量到:

| | 實測 |
|---|---|
| `supabase/tests/database/` | **1 支** `.test.sql`(pgTAP,`SELECT plan(25)`) |
| 誰在跑它 | **0**(`package.json` / CI / `*.sh` 引用 0 次;正對照:同樣的 grep 抓得到 5 處 `vitest`) |
| 10 支 `*-probe.sh` | 被 CI / `package.json` 引用 **0** 次 |

⇒ 我第一版的結論是「**碰得到,只是沒有人在碰**」⇒ 而那句話**只對了一半**。

### 🔴 再量一層才發現路有兩條,而其中一條【今天走不了】
```
路 A  拋棄式 PG（initdb / pg_ctl）→ homebrew postgres ⇒ ✅ 本機有
路 B  supabase test db（pgTAP）   → Docker           ⇒ 🔴 本機沒裝
                                                      （command -v docker 查無）
```
⇒ 那支唯一的 pgTAP 測試**不只是沒人跑,是今天在這台機器上跑不動**。

### 📌 判別句
> **一個檔案存在,會讓下一個人以為那條路是通的。**
> **而「repo 裡有這個東西」與「今天在我手上跑得起來」是兩個宣稱 ——**
> **而它們在 `ls` 底下印同一個結果。**

⚠️ **而處置完全不同**:
「碰不到」⇒ 要蓋一條路 ⇒ 貴;「碰得到而沒人碰」⇒ 只差接上跑 ⇒ 便宜。
**⇒ 讀錯哪一種,估的成本會差一個量級。**

🛑 **而 D-2 與 D-3 是同一件事的兩面**:
D-2 是**守了一個不會變的東西** ⇒ 恆綠;D-3 是**指了一條走不通的路** ⇒ 恆有希望。
**兩者都因為「那個東西在 repo 裡」而看起來成立。**

### 查重紀錄(D-2 / D-3)
`traps-neighbours.py` 分母 1893(草稿自己 3 則已剔除)。**開檔讀了 2 條,都判不同族**:
- **#8** `reference_count-objects-from-catalog-not-create-statements`(0.4405)——
  它講**怎麼數現況**(要問 catalog,不能數 `CREATE` 敘述,因為後面的片會 DROP)。
  ✅ **同一個根**:migration 檔記的是**歷史**不是**現況**。
  🔴 **而不同族**:它管「數出來的數字對不對」,D-2 管「**守門該指哪裡**」——
  而 D-2 的後果(得到一道**永遠綠**的守門)在那條裡不存在。⇒ 併進去會把後果洗掉。
- **#5** `feedback_blocker-must-prove-scenario-reachable-first`(0.4515)——
  它講**下 BLOCKER 前要先證場景可達**;D-3 講**一條看起來可走而今天走不通的路**。
  🔴 不同族:那條的對象是**我寫的斷言**,D-3 的對象是**別人會踩上去的路**。
⚠️ 而工具自己印的第八種失效(「分數最高的那條也可能不同族」)在這一輪成立:
第 1 名 0.5212「守門掃【來源】,而人讀的是【輸出】」開檔讀 ⇒ **不同族**(那條講兩個層,D-2 講一個層裡的凍結物)。

---

## D-4 一句【在警告某個坑】的話,被計成了它警告的那個東西

**當班實錘(2026-08-30)**,而它是今晚同族的**第八次**。

我要判 `20260812170000`(2f)有沒有真守門,量了 `grep -c 'strpos'` ⇒ **10**,
拿它當「它有 10 處字串守門」報出去。

### 🔴 剝掉整行註解之後是 7 —— 灌水 30%
而灌水的來源是這一行(`20260812170000:432`,SQL 註解):
```
-- **「找得到這個字串」不是守門**。寫 LIKE/strpos 守門前先問「加一個否定」
```
📌 **⇒ 一句【正好在警告 strpos 守門天花板】的註解,被我數成了一處 strpos 守門。**

### 為什麼它比一般的「註解被數進去」深一層
```
一般的：一句無關的註解剛好含那個字 ⇒ 數字虛胖，而讀的人沒有被誤導方向
🔴 這一種：那句話【在講這個東西的限制】⇒ 所以它【一定】含那個字
⇒ ⇒ 而一個坑被記錄得越好，記錄它的那句話就越會被計進【它記錄的那個東西】
```
⇒ **越誠實的 repo,這個誤差越大。**

### ⚠️ 今晚同族八次的清單(**七次是轉述,只有第 ⑧ 個是我自己量到的**)
> 🔴 **來源屬性照 §6-b 標(主視窗 `-48` 自己把這句改硬了,原句比真相寬一格)**:
> **那七次主視窗也沒有複量過** —— 它們是**各窗的自陳**,經過它轉述;
> 它做的只有【把它們放在一起、發現它們同族】。
> ⇒ 這張清單的正確描述是:**七則自陳 + 一次量測,而把它們串成一族的是主視窗的判斷,不是任何一發量測。**
> 🔴 **⇒ 一句話:【這張清單的分母是 8,而可重跑的證據是 1】。**
>
> **落點(主視窗給的;而落點不等於複量 —— ①-⑦ 全是訊息或 commit body,不是一發可重跑的檢查)**:
> ① `-48`:`~/pcm-mailbox/等Sean決策-20260829.md`(那一行仍在,而它沒有回去驗今天長什麼樣)
> ②③ `-e9`:`0b0a4932` / `c5ba2972` 的 commit body ・ ④ `-b9`:03:0x 那則的 `@tailwind` 量測
> ⑤ `-e4`(我):`5653cdc2` ・ ⑥ `-b4`:`3d7bd1fc` ・ ⑦ `-0b`:01:0x 那則的 `ls` 段頭
> ⑧ `-e4`(我):**可重跑**

① 工具的負對照 ② 主視窗的量測紀錄 ③ 記錄「`@tailwind` ⇒ 0」的註解
④ traps 條目本身 ⑤ 解釋那個坑的句子 ⑥ 被排除的 pathspec
⑦ `-0b` 的 `ls` 段頭 ⑧ 🔴 **本條**(線D:一句警告 strpos 的註解)
✅ 而 ① 是主視窗 `-48` 自己那一次(**把負對照寫進被掃的檔**),依它要求具名。

### 📌 可執行
> **不要問「那個字面在不在」,要問「【生效的】那一行是哪一行」。**
> `grep -v '^\s*--' <檔> | grep -c '<字面>'`,或釘住行首:`grep -n '^REPO=' <檔>`。
> 🔴 **而必附負對照**:剝註解之後行數要真的變少 ——
> 沒變少 ⇒ 你的註解樣式沒對上這個語言,而**那把尺會安靜地退回原來那個數**。

⚠️ **而 `grep` 不渲染刪除線** ⇒ `~~已作廢~~` 與生效的碼在輸出裡長得一模一樣
(來源:`-b4` 2026-08-30 的 worktree 誤報;**我未獨立複量**)。

### 🔴 後記(同日,回頭補分母時撞到):我這條的可執行寫得比 repo 裡既有的弱

我把「剝註解」寫成 `grep -v '^\s*--'` ⇒ **那只剝【整行註解】,漏掉【行尾註解】**:
```sql
WHERE o.customer_user_id = x  -- o.cancelled_at IS NULL
```
⇒ 這一行在我的版本底下是**生效行**,而它含那個字串 ⇒ **守門會綠**。
✅ 實測:舊實作看這一行 ⇒ `true`(放行);改成剝任何位置的 `--` 到行尾 ⇒ `false`(抓到);
正常案例兩邊都仍是 `true`(⇒ 我沒有改壞它)。

**⇒ 而答案早就在 repo 裡,十八天前**:`20260812170000:747`
```sql
v_stripped := pg_catalog.regexp_replace(v_src, '--[^' || chr(10) || ']*', '', 'g');
```
📌 **⇒ 而我是為了寫另一支守門去讀那支檔,才撞到它的 —— 不是查到的。**
**⇒ 一個「答案已經在 repo 裡」的實例,而它被找到的方式是【運氣】。**
🛑 已修在 `scripts/a8a3g-cancel-guard-contract.test.ts`,並把那個行尾形狀加成一格測項。

⚠️ **而順帶補一個我先前的分母**:我說「`2f` 是真守門」時**只讀了 3 行,而分母是 7**。
回頭逐處讀完 ⇒ **7 處全部生效、全部吃 `v_stripped`/`v_norm`(源頭是 `prosrc`)**
⇒ 原結論成立,**而它比我當時說的強一格**:其中 `:847` 是一道**否定斷言**
(剝註解+正規化+大寫化之後仍出現 `FOR UPDATE` ⇒ `RAISE`),不只是「找得到某個字」。

---

## D-5 · 我製造了一個【我早就知道會發生】的紅,而它沒有名字

**現場(2026-08-30 02:2x,線D 與 `-e9`)**:我在共用工作樹裡先改介面
(`IIneligibleOrderEmailScanner` 加一個方法)、**後**改消費端(那支測試檔裡的 mock)。
中間那段時間 `pnpm typecheck` 必紅。`-e9` 跑三綠撞到,花時間查、確認不是自己的,
在他自己的 commit body 裡寫清楚「紅不是我的、成因是誰、HEAD 是綠的」,然後來通知我。

### 🔴 病灶不是「先改介面」——那是對的順序
反過來(先改消費端)會讓消費端引用一個還不存在的方法,一樣紅,而且更難讀。
**兩種順序都會紅,而紅是必然的。**

### ⇒ 所以修法不可能是「不要製造紅」,只能是【製造之前先認領】
```
我明明知道它會紅,而我沒有留下任何一句話讓下一個看到紅的人知道那是我。
```
📌 **判別句:那段紅有沒有名字?**
· 有名字 ⇒ 下一個人看到紅,一眼知道是誰、預計多久、紅在哪一行 ⇒ 成本 0
· 沒名字 ⇒ 他必須從頭做一次歸屬判定(`git status` + 讀 diff + 推理)⇒ 成本落在他身上

### 🔴 而這是 `-e9` 那句更寬的話的一個窄形狀,兩句都要留
> **八窗共用一棵樹的時候,「三綠紅了」這個訊號本身沒有歸屬。**(`-e9` 原句)

⇒ 寬的那句講的是**訊號的性質**(它本來就不帶歸屬);
   窄的這句講的是**唯一做得到的補救**(帶歸屬的資訊只能由製造者在製造前放進去,
   因為紅出現之後,**只有製造者知道它是預期的**,而看到的人不知道)。

### 操作化(下一次我怎麼做)
```
在敲下【第一個會讓樹變紅的鍵】之前,先寫進 ~/pcm-mailbox/現在誰在做什麼-<日>.md:
  「線D 02:2x 起會讓 typecheck 紅約 N 分鐘 —— 我在改 <介面檔>,消費端還沒跟上,
    紅會出現在 <檔:行>。不要修,不是你的。」
```
⚠️ **而它必須寫在【下一個人本來就會讀的載體】**,不是寫在我自己的進度檔裡 ——
   一個跑三綠撞到紅的人,不會先去讀別人的進度檔。

### ✅ 而 `-e9` 做對的那一格也要記(它是這件事的正面對照)
```
① 他【沒有動我的檔】—— 順手替別人修在途改動,會製造出比原本那個紅難十倍的東西
② 他在自己的 commit body 裡把偏離寫清楚,而不是靜靜地繞過
③ 🔴 我回報「修好了、8/8 綠」之後,他【自己再量了一發】,理由逐字:
   「我在 commit body 裡寫了『那個紅不是我的』,那句話要成立,得由我自己這一側量到它變綠,
     不能只轉述你的數字。」
   ⇒ 📌 **一個要寫進 commit 的宣稱,不能靠轉述來成立** —— 那正是轉述契約 §5 那條的
      收訊端版本,而這是我看過最乾淨的一次執行。
```

### 查重(`traps-neighbours.py`,分母 1972 則;開了三條、判都不同族)
```
1. 0.5492 memory/feedback_knowing-the-trap-does-not-stop-you-in-the-next-paragraph
   ⇒ 講「同一份交件裡講對了坑, 下一段又踩同一個坑, 因為載體換了」
   ⇒ 不同族:那條講【重複踩坑】, 本條講【我製造的訊號沒有歸屬】。我這次沒有踩兩次。
6. 0.5286 memory/feedback_a-rule-belongs-where-the-person-will-pass
   ⇒ 最接近的一條, 而它是本條【操作化那一段所引用的原則】, 不是本條本身:
     它答「規矩該寫在哪」, 本條答「一個必然會出現的紅, 誰該在什麼時候替它命名」。
   ⇒ 判不併, 而在操作化那段明寫要寫進「下一個人本來就會讀的載體」= 引用它。
3. 0.5383 正本 :9910「尺量的是字元而宣稱量的是結構」
   ⇒ 不同族:那條講尺的分母, 本條與尺無關 —— 這次的紅是【真的紅】, 沒有任何量具出錯。
```
🔴 **而本條與它們的差別可以寫成一句**:前面那些講的都是**訊號本身錯了**;
本條講的是**訊號完全正確,而它缺少一個只有製造者放得進去的欄位:是誰、預期多久。**

### 🔴🔴 而這條有一個【完成式】,而它是在寫完上面那一段之後才長出來的
```
02:3x —— 我寫完上面整條 D-5、也把「我還會動哪五支檔」寫進艦隊表了
      ⇒ 然後【又製造了一顆同族的紅】(`OutboxFake` 少列一個 mock 欄)
      ⇒ 這一次收稅的是 -1c,而它一支檔都沒動
```
📌 **⇒ 一個剛剛寫完某條教訓的人,是最相信自己不會再犯那條的人。**(`-48` 03:0x 的歸納)

### 🔴🔴 而 03:5x 我**第三次**犯它,而這一次我當場說得出【為什麼會犯】——那個理由才是這條的本體
```
我加了一個 port 方法 ⇒ 消費端的 fake 少一欄 ⇒ typecheck 紅約 2 分鐘
而我【沒有事先認領】, 當下的判斷逐字是:「一分鐘內修掉比寫認領快」
```
⚠️ 這次**沒有人來問** ⇒ 沒有收到稅。**而「沒有人付稅」與「沒有產生稅」是兩個宣稱** ——
這次是**運氣**,不是我做對了。

🔴 **而那個判斷錯在哪,不是「我估錯秒數」**:
```
我比的是「寫認領」與「修好它」—— 而那兩個【都是我的成本】
真正該進比較的第三項(別人的成本)【根本沒有出現在我的算式裡】
```
📌 **⇒ 不是我偷懶,是我算的那條式子【結構性地缺一項】。**

⇒ ⇒ 而這正好是本檔那條稅的**上游描述**:
> **收稅的對象不是造成它的人 —— 因為造成它的人在做決定的當下,那一項不在他的算式裡。**

### ⇒ 所以判準不需要「寫認領要幾秒」(而我一度真的去量了)
```
❌ 我量到:機器把一行字寫進檔案 ⇒ 0.03 秒
   ⇒ 而那是【打字那一下】的成本, 不是【寫認領】的成本
   ⇒ 真正的成本含「想出那句話要寫什麼」, 而它與思考時間混在一起, 我量不到
   ⇒ 📌 **所以我不能說「認領只要 0.03 秒」** —— 那會是一個誠實的數字回答錯的問題
✅ 判準只需要這個不對稱:
   我方成本 = 一句話 + 一次 tool call         ⇒ **有界且小**(不管確切幾秒)
   他方成本 = 撞紅 → git status → 讀 diff → 推理歸屬 → 來問 → 等回覆
             ⇒ **一次完整的調查, 沒有上界**
```
🔴 **⇒ 一個【有界且小】對上一個【無界】—— 那個比較不需要任何一個秒數就成立。**
⚠️ 而我原本要去量那個秒數,是因為**「量一下」聽起來永遠像是更嚴謹的做法** ——
📌 **而它在這裡會把一條成立的判準,換成一個量不準的數字。**

🔴 **而差別在哪,可以量**:
```
第一顆:紅出現時,樹上沒有任何一句話說那是我 ⇒ -e9 從零做歸屬判定
第二顆:紅出現時,艦隊表上【已經有】我認領那五支檔的那一段
        ⇒ 而 -1c 仍然花了一輪 —— 因為它撞到的是【第六支】(測試檔),不在我列的五支裡
```
⇒ 📌 **所以真正的距離不是「有沒有寫」,是【機制裝上的時點】與【事故發生的時點】之間那一段。**
我的認領是在第一顆事故**之後**寫的,而它涵蓋的是**我當時已經知道要動的檔**;
第二顆用的是一支我**當下才發現也要改**的檔 ⇒ 認領清單天生落後於施工面。

### ⇒ 所以操作化要再往上一層(前一版那條不夠)
```
❌ 不夠:「動手前列出我要動哪幾支檔」—— 清單永遠追不上,因為改到一半才知道還要動誰
✅ 夠  :「**我這段時間在動 <某一族>,這一族上的紅先當成是我的**」
        —— 認領【一個範圍】,不是認領【一張清單】
```
🛑 **而這一段刻意不改寫上面那條** —— 上面那條是我在第一顆之後寫的,而它當時是誠實的。
把它改寫成「後來我就沒再犯了」會刪掉這條最有用的那一格:**它記錄了一次「知道了而沒做到」。**

---

## D-6 · 我貼了原文,而**貼的過程本身把它改壞了** —— 而旁邊還掛著「逐字」兩個字

**現場(2026-08-30 02:4x,R3 對抗審查抓到)**:我在 `scripts/a8a1-verify.sh` / `a8a2-verify.sh`
的檔頭寫了一段過期警語,裡面引 Sean 的原話。用 python heredoc 寫檔時,
我在字串裡多寫了一組 shell quoting 的跳脫 ⇒ **殘渣 `"'"'"` 被原封寫進檔案**:

```
檔內實際字面(cat -v 實證)
  # …(Sean 拍「2 乙」+ 逐字「寫很官方就好"'"'"」):
應該是
  # …(Sean 拍「2 乙」+ 逐字「寫很官方就好」):
```

### 🔴 為什麼這比「引錯字」嚴重一格
```
那段檔頭【自己】逐字寫著:「誰寫繼任的 a8a3/a8a4-verify,請從這裡抄期望值」
⇒ 我親手在那段話上掛了一個【來這裡抄】的招牌,而招牌底下那句是壞的
```
📌 **而真正的機制在這裡:**
> **一段被污染的引句,只要旁邊還掛著「逐字」兩個字,它的可信度反而比沒掛章的更高。**

⇒ 一句沒掛章的引文,下一個人會回去核;**一句掛了「逐字」的,他不會** —— 那兩個字的全部功能
就是**關掉他的核對動作**。⇒ 所以**掛章的引文出錯,比不掛章的出錯貴**,而它們在畫面上長得一樣。

### 🔴 它是「值與授權句貼原文、不重打」那條的**失敗案例**,不是反例
那條規矩(轉述契約 §5-①)是對的,而我照做了 —— **我沒有重打,我貼了原文**。
```
病灶不在【重打】那一步,在【貼的那一步本身會改寫內容】:
  heredoc / shell quoting / echo 的跳脫 / 全形轉半形 / 編輯器自動換行
  ⇒ 這些都在「我以為我在貼原文」與「檔案裡真正的字」之間
```
⇒ 📌 **「貼原文不重打」防的是【我的記憶】,防不了【載體】。**

### 操作化(可機械執行,不是提醒)
```
把引句寫進檔案之後,當場撈回來比一次:
  grep -n "<引句的一段>" <檔> | cat -v
  ⇒ 看到任何 M-… 以外的 ASCII 雜訊(引號、反斜線、'"'"')= 殘渣
或更嚴:把來源那一行與寫入後那一行【逐位元組 diff】
```
🔴 而**寫含引號/反斜線的字進檔案,一律 `printf '%s\n'`,不要 `echo`,也不要在 heredoc
   裡自己疊跳脫** —— 同族已記:CLAUDE.md 的「`echo` 遇到 `\c` 會停止輸出」那條。
   **那一條講的是【少了東西】,本條講的是【多了東西】;而多出來的比少掉的難發現,
   因為句子還讀得通。**

### ⚠️ 射程:**這條的原始證據【不存在】,而那不是疏忽,是它的性質**
```
壞掉的那一版【從來沒有被 commit 過】—— 它活在工作樹裡, 30 分鐘後被 R3 抓到就修掉了
量法（線D 與 -b9 兩個窗、兩套指令、沒有先對過答案，結論相同）:
  git show 5f96851b^:scripts/a8a1-verify.sh | grep -n "寫很官方就好" ⇒ 零命中
  git show 5f96851b :scripts/a8a1-verify.sh | grep -n "寫很官方就好" ⇒ :27, 而無殘渣
  git log --all -S'寫很官方就好' -- 那支檔                          ⇒ 只有 5f96851b 一顆
  git show --stat 5f96851b -- 那支檔                                ⇒ 13 insertions, 零 deletions
  負對照 兩版各 grep -c 'zzq6641' ⇒ 0 / 0（尺不是恆回東西）
```
⇒ **它存在過的唯一憑據是 ①R3 findings 原文 ②本條作者的自述** —— 而那兩個是
**同一次事件的兩份描述,不是兩個獨立來源**。
🔴 **⇒ 引用本條時不得寫成「有 commit 可查」。**「壞掉那版曾經存在過」**證不到也證不否**
—— 工作樹不留痕。

### 🔴 而本條有一個【第三實例】,是作者在寫完上面兩節【之後】犯的
```
我為了讓 -b9 能複核 D-6, 給了它一組 git 指令
⇒ 而那組指令我【沒有跑過】⇒ 它會回空的
⇒ 而收訊者拿到空的時候, 分不出是【自己打錯】還是【我講錯】
```
📌 **⇒ 一個【打開核對】的動作,如果它自己沒被核過,它會製造一次【假的核對】。**
⇒ 與 D-6 本體同一個形狀、方向相反:
· D-6:**貼原文,而貼的那一步改壞了它**
· 第三實例:**給複核路徑,而那條路徑自己沒走過**
⇒ **兩者都是「把一個【減少懷疑】的東西交出去,而它本身沒被驗過」。**

**可執行(不依賴懷疑)**:
> **每當我在訊息裡寫「你跑這個會看到 X」,送出後(或送出前)當場跑一次。**

### 🔴 而這條規矩【太窄】,同一夜就被第四個實例穿過去
```
03:2x 我在交件檔裡寫「下面這段含標點 240 字（我數過）」
⇒ 去量 ⇒ 279。**我寫了「我數過」而我沒數。**
```
⚠️ **而上面那條擋不到它** —— 它的觸發條件是「你跑這個會看到 X」的句形,
而「我數過」**不是那個句形**。⇒ 規矩要擴:

> **凡是句子裡出現「我量過 / 我數過 / 實測 / 當場跑過」,送出前當場跑一次。**

### 📌 而 `-b9` 指出的那一格,比這條規矩本身重要
```
第三實例（沒跑過的指令）：有下游會撞到 —— 他跑一發, 拿到空的, 就發現了
第四實例（沒數過的字數）：**沒有下游** —— 一個字元數不會有人重跑,
                          除非他剛好也要把東西塞進同一個上限
```
🔴 **⇒ 「有沒有人會發現」不是【它有多錯】的函數,是【有沒有人剛好在做同一件事】。**

⚠️ 而這一次**剛好有**:`-b9` 正在算那張表的餘裕 ⇒ 它重跑了 ⇒ 得到 252(與我更正後相同)。
📌 **⇒ 所以這一格被抓到是【運氣】,不是紀律** —— 而事後看,運氣抓到的與紀律抓到的**印同一個綠**。
(同族母題:`docs/patterns/guard-and-instrument-traps.md`「錯的那次和對的那次長得一樣」;
 以及本檔 D-5 那條「沒撞到那個紅是運氣不是紀律」。)

✅ **而擴充版強在哪(`-b9` 的話,我照抄)**:
> **它的觸發條件是【我的句子長什麼形狀】,不是【對方會不會去跑】。**
⇒ 前者我控制得了,後者我控制不了 —— 而**依賴後者的規矩,在沒有下游的那一次會靜靜失效。**

---

## D-7 · **一條規矩(或一道守門)的射程,由它的【觸發條件】決定,不由它【想防的東西】決定**

這句話是 D-6 第四實例逼出來的:我原本那條規矩想防的是「未經驗證的斷言」,
而它的觸發條件只寫了「你跑這個會看到 X」⇒ **「我數過」那個句形整個穿過去。**

📌 **判別問法**:
> **把我想防的那件事,換一個【句子的形狀 / 檔案的位置 / 呼叫的寫法】再做一次 ——
> 我這道閘還看得見嗎?**

看不見 ⇒ **它防的不是那件事,是那件事的一種寫法。**

### 實例(三個;而**只有第一個是我親自量到的**)
```
① ✅ 我量到的:那道 block-unconditional-echo-label 的 hook
   我 03:2x 被它擋下一次 —— 它看得懂【單行 echo/printf 帶結果標籤】
   而 -48 說它【看不懂 if 區塊裡的同一句】⇒ 同一個宣稱換一個語法就穿過去
   🔴 我親自撞到的是【被擋】那一半;【穿過去】那一半是 -48 說的, 我沒複核。
② ⚠️ -48 提供, 我未複核:`cat .env` 擋得住而 `grep -r` 擋不住
   ⇒ 同一個「讀 .env」的意圖, 換一個指令名就穿過去
③ ⚠️ -48 提供, 我未複核:某道守門的分母 231 vs 233
   ⇒ 兩個都是誠實的數, 而它們數的是不同的集合
```
🛑 **②③ 我沒有開檔驗過,所以掛「未複核」的標記**(做法照 `-b9` 2026-08-30 那一格:
**一段推導出來的字面要掛「不是我量的」**,而那個標記的功能是**打開**下一個人的核對)。
⇒ 📌 **而這正好是本條自己的一個實例**:我若把 ②③ 寫得跟 ① 一樣肯定,
**這條 traps 的觸發條件就會漏掉「我引用了別人的量測」那個句形。**

### 可執行
```
寫一條規矩 / 裝一道守門的當下, 當場問:
  「我想防的那件事, 有沒有第二種寫法?」
  ⇒ 有 ⇒ 那第二種寫法【現在就會穿過去】, 不是「以後可能」
  ⇒ 而它穿過去的時候, 這道閘會印綠
```
⚠️ 這條**不保證**你想得出第二種寫法(想不出來的那一種正是會出事的那一種);
它只保證**你有去想**。⇒ 更強的做法:**當場餵一發第二種寫法給它看,不要用想的。**

### 查重(分母 1972;開了三條)
```
3. 0.5935 memory/feedback_a-rule-belongs-where-the-person-will-pass
   ⇒ 🔴 最近的一條, 而**判不併** —— 它們是同一個問題的【兩半】:
     那條答「**要攔的人會不會經過這裡**」(位置)
     本條答「**經過了, 這道閘認不認得他**」(觸發條件)
   ⇒ 📌 兩半都成立才擋得住:**放對地方而觸發條件太窄, 他會經過而不被攔;
     觸發條件夠寬而放錯地方, 他根本不會經過。** 兩條互相補, 不重複。
1. 0.6194 memory/feedback_knowing-the-trap-does-not-stop-you-in-the-next-paragraph
   ⇒ 不同族(那條講重複踩坑)。而本條解釋了它的一個成因:
     **下一段換了載體 ⇒ 你腦中那條規矩的觸發條件認不出它。**
2. 0.5995 正本 :15836「量具給了你一個講得通的錯誤故事」
   ⇒ 不同族:那條是量具給錯答案, 本條是量具【根本沒被觸發】—— 它連答案都沒給。
```


⚠️ 而**依賴懷疑的紀律都會在忙的時候失效** —— 這一條的觸發條件是**句子的形狀**
(「你跑這個會…」),不是「我覺得可疑」。

### 查重(`traps-neighbours.py`,分母 1972 則;開了三條)
```
1. 0.5904 memory/feedback_knowing-the-trap-does-not-stop-you-in-the-next-paragraph
   ⇒ 🔴 **最接近, 而且它的一句話與本條字面撞上**:「因為【載體換了】」。
   ⇒ 判**不併**:那條講【同一個人在下一段重複踩同一個坑】(行為的重複),
     本條講【我沒有重複任何動作, 是載體在我不知情時改寫了內容】(內容的污染)。
   ⇒ 📌 兩條共用「載體」這個字而**指的不是同一件事**:那條的載體是【換了一種形式的同一個推理】,
     本條的載體是【字串經過 shell / heredoc 的那一段路】。**字面撞上不是同族。**
3. 0.5568 memory/feedback_a-rule-belongs-where-the-person-will-pass
   ⇒ 不同族(它答「規矩寫在哪」)。而本條有一格與它相鄰:那段壞引句底下掛著「來這裡抄」的招牌
     ⇒ 它**正好寫在下一個人一定會經過的地方**, 所以壞得更貴。
5. 正本 :9910「尺量的是字元而宣稱量的是結構」
   ⇒ 不同族:那條是尺的分母, 本條沒有任何尺出錯 —— 出錯的是我寫檔那一步。
```
🔴 **而真正最近的不在名單上**:轉述契約 §5-①「值與授權句貼原文、不重打」——
**本條是那一條的【失敗案例】,不是它的反例**(我照做了、我沒重打,而它仍然壞了)⇒ 開新條,
關係寫在條內(見上一節),不併進那條、也不改寫那條。


---

## D-8 · **被引用的東西真的存在,而它與「它被用來支持的那個宣稱」之間的關係是假的**

**現場(2026-08-30 03:4x,線D)**:我在 plan 裡寫

> 「`voided` ⇒ 不寄、不計 error、**落一條跳過的痕跡(先例 `markSkippedOrderIneligible`)**」

那句話**每一格都通得過檢查**:
```
`markSkippedOrderIneligible` 真的存在      ✅ grep 得到
它真的是一個「跳過而留痕跡」的方法          ✅ 開檔看得到
它真的是這條線上的先例                     ✅ 同一支 port、同一個作者、同一個設計理由
```
🔴 **而它不做我要它做的事** —— 它寫的狀態是 `skipped_order_ineligible`,
而**資料庫的 `status` CHECK 只認六個值,沒有一個對應「箱被作廢」**
(`20260717020000:317` 實查;正對照:那個值域沒被任何後續 migration 改過)。
⇒ 我引的是一個**形狀相同、而落點不存在**的先例。

### 📌 這是一族,而它有三個成員(2026-08-30 一夜之內)
```
· 指標存在, 而指到隔壁          （-b9 / -48, 兩次）
· 承重的話存在, 而住錯地方      （-b4 / -1c）
· 🔴 先例存在, 而它不做那件事    （線D, 本條）
```
🔴 **共同形狀:被引用的東西【真的存在】,而它與「它被用來支持的那個宣稱」之間的關係是假的。**

### 🔴 而三個成員都通過同一種檢查 —— 這才是它難的地方
```
grep 得到 ✅   開檔看得到 ✅   逐字驗會過 ✅
⇒ 「引用的字面對不對」這個檢查, 對這一族【完全沒有判別力】
⇒ 唯一抓得到的動作是:**打開它, 讀它到底【做什麼】**, 而不是確認它【在不在】
```

### ⚠️ 而這一次它不是被任何機制抓到的
```
我是【為了寫另一段東西】(async 的形狀)去讀那支 port 的四個 mark 方法, 才順手撞到的。
⇒ 沒有任何一道閘會紅、沒有任何一個 grep 會空、沒有任何一次審查問過這句
⇒ **它被抓到是【路徑碰巧經過】, 不是紀律**
```
📌 而這與 D-6 第四實例那格是同一句話的兩個方向:
**「有沒有人會發現」不是【它有多錯】的函數,是【有沒有人剛好在做同一件事】。**

### 可執行(而它很貴,所以要挑)
```
寫下「先例 / 照抄 / 同款 / 沿用 <某個既有的東西>」時 ——
  ❌ 不夠:確認那個東西存在
  ✅ 要做:打開它, 回答「它【寫進哪裡】/【回什麼】/【落在哪個值】」
⇒ 而只有【我要靠它成立的那一格】需要這樣做, 不是每一次引用都要
   (判別:如果那個先例不成立, 我這句話會不會垮?會 ⇒ 打開它)
```
🛑 **而本條與 D-7 是兩件事,不要合併**:D-7 是「我的閘認不出那個形狀」(觸發條件太窄);
本條是「**閘認得出,而它檢查的那一格本來就不是重點**」—— 字面對了,關係假了。

---

## D-9 · **我的等待迴圈在等它自己結束**

**現場(2026-08-30 04:0x-04:2x,線D)**:我用這一行等 codex 跑完 ——
```bash
until ! pgrep -f "gpt-5.6-sol" >/dev/null 2>&1; do sleep 20; done
```
🔴 **那個 pattern 命中【這一行自己】** —— `pgrep -f` 比對的是完整命令列,
而我的 `until` 迴圈的命令列裡就有 `gpt-5.6-sol` 這幾個字。
⇒ **迴圈在等自己結束** ⇒ 它永遠不會結束。

### 量到的
```
codex 實際完成:04:03(log 的 mtime)
我還在回報「還在跑」:04:20
⇒ 白等 17 分鐘
而卡住的迴圈有【五個】—— 逐個 ps 比對 command 有沒有 'codex exec' ⇒ 真 codex 0 個
```

### 🔴 而代價不是 17 分鐘
```
我對主視窗回報了【三次】「codex 還在跑」—— 而那三次都是假的
📌 在收訊端, 那三則與真的完全一樣
```
⇒ **一個壞掉的等待迴圈,產生的不是延遲,是【一串假的狀態回報】。**

### ⚠️ 而更難的一格:**它污染了後續每一次檢查**
第一個迴圈起來之後,我每一次「還在跑嗎」用的都是同一個 pattern
⇒ **從那一刻起,那個問題就再也不可能回答「結束了」。**
📌 **一個壞掉的量具,不只給錯一次答案 —— 它讓那個問題【永遠】答不對。**

### 🛑 而這條**就寫在 `CLAUDE.md` 裡,而我今晚讀過它**
> 「只看 `pgrep -f` ⇒ 它命中你自己那行, 而八窗各跑各的 codex ⇒ 也命中別窗」

⇒ 📌 **這是今晚第二次「規矩寫得清清楚楚而它擋不住」**(前一次是 `cat .env` 擋不住 `grep -r`)。
**共同形狀:規矩描述的是【那個坑】,而人在寫的當下不知道自己正站在坑上。**
⇒ 它與 D-7 是一體兩面:D-7 講**閘的觸發條件太窄**;這裡講**人腦裡那條規矩的觸發條件太窄**
—— 我讀過那句話,而我寫 `until ! pgrep -f` 的那一刻,腦裡沒有任何東西說「這就是那句話」。

### ✅ 正解(就寫在同一段規矩裡)
```bash
nohup codex exec ... > log 2>&1 < /dev/null &
PID=$!                 # 🔴 這一行前後不准有任何東西（$! 會被下一個指令蓋掉）
wait "$PID"            # 它等到【真的】結束, 不必輪詢, 也不會命中自己
```
⚠️ 而若真的要用 `pgrep`,**必須排除自己**:逐個 `ps -o command= -p <pid>` 確認它真的是 `codex exec`
—— 而那正是我最後抓到這件事的方法。**判別句:那個 pattern 會不會命中【正在問問題的那一行】?**

### 查重(分母 1972;開了三條)
```
1. 0.6340 memory/feedback_knowing-the-trap-does-not-stop-you-in-the-next-paragraph
   ⇒ 🔴 **這一條真的相關, 而仍判不併**:它講「講對了坑, 下一段又踩同一個」;
     本條是【讀過那條規矩, 而寫的當下沒有認出自己站在坑上】——
     📌 差別在:那條的人**知道自己在處理同一個主題**(下一段是同一份交件);
       本條的人**根本不覺得這件事與那條規矩有關**(我在等一個背景程序, 不是在「用 pgrep 判歸屬」)。
   ⇒ 本條在條內已寫明它與 D-7 是一體兩面, 那才是它的家。
2. 0.6178 正本 :15836「量具給了你一個講得通的錯誤故事」
   ⇒ 不同族而**相鄰**:那條是量具給了一個錯的答案;
     本條是量具**永遠答不出對的答案**(第一個迴圈起來之後, 那個問題就被污染了)。
     ⇒ 本條多的那一格是【污染會持續】, 不是【這一次錯了】。
3. 0.6105 memory/feedback_a-rule-belongs-where-the-person-will-pass
   ⇒ 不同族(位置);而它與本條的關係已寫在 D-7 的查重段。
```

---

## D-10 · **「我會在安全的地方驗」完全不涵蓋「我在哪裡改」**

**現場(2026-08-30 05:2x,線D;`-0b` 抓、主視窗轉)**:我在修 `.husky/reviewer-gate.sh` ——
**那道閘是每一個窗每一次 commit 都會執行的東西**。

我的 plan 裡有一條自認為謹慎的規矩:
> 「四個世界都在**拋棄式 repo** 上演,不在共用主樹試 —— 這一片壞掉的代價是別人的。」

🔴 **而我照著做了,然後在共用主樹上編輯那兩支檔。**

### 病灶:那條規矩管的是【驗證】,而風險在【編輯】
```
我寫的:  「驗證在哪裡做」 ⇒ 拋棄式 repo ✅
沒寫的:  「編輯在哪裡做」 ⇒ **共用主樹** 🔴
⇒ 而在共用工作樹上, **改的那個地方就是全隊正在用的那個地方**
```
📌 **`-0b` 的原句(我原樣收)**:
> **一支正在被編輯的 hook,與一支壞掉的 hook,對其他六個窗是同一件事 —— 而它們不會知道。**

⇒ 而更刺的一格是它接著講的:
> **閘本身正是今晚的事故源,而修它的改動半途被別人撞到,會是最諷刺的一種重演。**

### 🔴 而「還原重來」不是解 —— 這一格是算出來的,不是偏好
主視窗建議「還原主樹、改在拋棄式 repo、改好再搬回來」。**我推翻了它,理由是機械的**:
```
曝露窗 = 那兩支處在「既不是舊版、也不是驗證過的新版」的那段時間
· 已經發生的那一段 ⇒ **已經過去了, 還原救不回來**
· 而【現在還原】⇒ 主樹那兩支再變一次(新⇒舊)
  ⇒ 改完再搬回來 ⇒ **再變第三次**
⇒ ⇒ 還原這條路 = 多開兩次變動, 而【每一次變動都是一個新的窗】
```
📌 **⇒ 我做錯的是【在主樹上開始編輯】,而那一步已經做了。**
**從這一點往前,最短的路是【commit 它】,不是把它變回去再變過來。**
✅ 而主視窗接受了這個推翻,理由是它自己講的:**他的建議建立在「它是半成品」這個前提上,
而我先量了三發把那個前提推翻了**(語法 OK / 七世界自檢符合預期 / 三綠全綠 / 突變證過)。

### ⇒ 可執行(兩條,分開寫因為它們管不同的東西)
```
① 驗證:高風險改動的驗證一律在拋棄式環境(既有規矩, 不變)
② 🔴 編輯:**改一支「別人每次操作都會執行」的檔案時, 編輯本身也要在拋棄式環境**
   —— 改好、演完、才【一次性覆蓋】主樹那一份
   ⇒ 這樣主樹上的每一次變動都是「從一個驗證過的版本, 換成另一個驗證過的版本」
```
⚠️ **判別:哪些檔算「別人每次操作都會執行」?** —— `.husky/**`、`scripts/` 裡被 hook 引用的、
`package.json` 的 scripts、CI 設定。**判別句:我存檔的那一秒,有沒有別人正在跑它?**

### ⚠️ 而我要留一格自己的射程
「codex 回來之前沒有人動那兩支」這件事**我證不出來** —— 我只能在覆蓋之前再 `git diff` 一次。
📌 **一個依賴「別人剛好沒動」的計畫,不是一個計畫,是一個賭注 —— 而寫出來至少讓它是個【明說的】賭注。**

---

## D-11 · **兩邊用同一個壞方法,於是它們一致 —— 而一致就是放行的條件**

**現場(2026-08-30 05:3x,codex 抓並實測重現)**:我在 reviewer 閘的兩側各寫了一行:
```sh
# writer 那側
staged_hash=$(git -c core.quotePath=false diff --cached | shasum -a 256 | cut -d' ' -f1)
# gate 那側(一模一樣的一行)
now_staged=$(git -c core.quotePath=false diff --cached | shasum -a 256 | cut -d' ' -f1)
```
而閘的判定是「兩邊相等 ⇒ 放行」。

🔴 **`sh` 的管線只回【最後一段】的 rc** ⇒ `git diff` 失敗會被 `cut` 的 rc=0 吃掉,
而 `shasum` 拿到空輸入 ⇒ 算出一個**合法的、空內容的 sha256**。
⇒ **兩側用同一個壞方法 ⇒ 兩側算出同一個空雜湊 ⇒ 相等 ⇒ 放行。**

📌 **⇒ 我以為我寫的是 fail-closed,而它是 fail-open。**
而差別不在我寫了什麼判斷,**在那條管線的 rc 被誰吃掉**。

### 🔴 而這是今晚同一個母題的第三次,而這一次它裝在一道守門裡面
```
· 對稿:兩端錯法相同 ⇒ 它會比出【完全吻合】, 而那個結論看起來是最好的那一種
· 覆蓋腳本:兩份手抄的 CHECK 一起抄錯 ⇒ harness 全綠而正式庫會失敗
· 🔴 本條:兩側同一個壞管線 ⇒ 一致 ⇒ **而一致就是放行的條件**
```
⇒ 📌 **前兩個的後果是「你以為你驗過了」;這一個的後果是「守門自己開門」。**
**同一個形狀,而它裝的位置決定了它有多貴。**

### ✅ 修法不是「把 rc 收好」,是**換一個不需要管線的來源**
```
git write-tree      # 單一指令、沒有管線、沒有外部工具、不受 diff config / locale / textconv 影響
```
⇒ 而它**順手關掉了另一個我們已經決定要接受的風險**(見下)。
📌 **判別句:我這兩側是不是用【同一段可能一起壞掉的碼】算出要比對的那個值?**
是 ⇒ 它們會一起壞,而**一起壞 = 相等 = 通過**。

---

## D-12 · **一個為了【別的理由】做的修正,關掉了一個我們已經決定要接受的風險**

接 D-11。codex 要我換掉雜湊來源的理由是**第三條 must-fix**(雜湊「人類看的 diff 輸出」而非
index 身分;`diff.noprefix` 實測會讓同內容算出不同雜湊)。**與那個風險無關。**

而換成 `git write-tree` 之後,我去實測那個「已知殘留」——
```
標記裡的 tree(A+B)     58e021d6…
hook 看到的 write-tree   f75f49d6…      ← 不一樣 ⇒ 擋
HOOK: GIT_INDEX_FILE=…/next-index-29835.lock   ← **git 給了 hook 一個暫時 index**
```
🔵 **它關了。** 機制:git 在 **partial commit(帶 pathspec)** 時餵給 hook 的是
「HEAD + 只有 pathspec 那幾支」的暫時 index ⇒ **`git write-tree` 在 hook 底下回的是
【這一顆 commit 會長成什麼】,不是「共用 index 現在有什麼」** —— 那正是我們一開始想釘的東西。

### 🔴 而最該記的不是「洞關了」,是**它為什麼差點沒被發現**
```
我已經把那個風險寫進三個地方(writer 註解 / selftest V7 / plan)
主視窗已經【正式接受】它並準備寫進決策板讓 Sean 醒來看
⇒ 而它從那一刻起就是一個【不存在的風險】
⇒ 若不是主視窗要求「V7 明確演一次、記錄它會過」, 我們會永遠帶著它
```
📌 **⇒ 一個被接受的風險,不會有人再回頭驗它** —— 接受本身就是「停止查證」的動作。
⇒ ⇒ **所以「演一次那個我們決定接受的洞」比它看起來更值得**:
**它是唯一會發現「那個洞其實不在」的動作。**

⚠️ 而射程要往兩個方向都寫準,不要因為是好消息就放寬:
```
· 帶 pathspec 的 commit(本 repo 明令的寫法)⇒ 關了, 有實測
· 不帶 pathspec ⇒ 仍會過 —— 而那一種情況下 B 本來就是在 commit A 的檔
  ⇒ 那是「不帶 pathspec」自己的病, 不是這道閘的洞
```

---

## D-13 · **我為了寫一段「防止繞過守門」的碼,而那段文字自己被執行了一次**

**現場(2026-08-30 05:3x)**:我用 `python3 - <<PY`(**未加引號的 heredoc**)把一段內容餵給 python,
而那段內容裡有 `$(git add ...)` 與 `commit` 這些字。
⇒ **shell 在把 heredoc 交給 python 之前,先做了命令替換** ⇒ 那幾個指令**真的被執行了**。

```
畫面上出現:Nothing specified, nothing added. / (eval):2: command not found: commit
⇒ 我當下才知道它跑過
```
✅ 事後查:`git diff --cached --name-only | grep -c .` ⇒ **0**
🔴 **而「零損害」是【運氣】,不是【無害】** —— 那發 `git add` 剛好**沒帶 pathspec** 所以什麼都沒做。
**同一段文字如果寫的是 `git add .`,它會把七個窗的在途改動全部 staged。**

### ⇒ 可執行
```
heredoc 一律 <<'EOF'(加引號)—— 需要插值時, 用參數傳進去, 不要靠 shell 展開
判別句:**我這段文字裡有沒有 $( ) 或反引號?有 ⇒ 加引號。**
```
📌 而它與 CLAUDE.md 那條「雙引號內禁反引號(commit body 會被吃掉)」是**同一族的另一個成員**:
那一條是**字被吃掉**,本條是**字被執行**。⇒ **前者少東西,後者多做事。**
