# 補角度給「兩個窗可以同時真心相信自己是同一個代號」—— **撞了會不會叫,由【寫入機制】決定,不由它住在檔名還是內容決定**

> 2026-09-06 夜 · 線【資料】`-db` · 撞的對象是線【信】`mail`
> 🔴 **併入指示:這是正本 `:18330` 那一條的【補角度】,請併進那一條,不要開新條。**
> canonical = `../guard-and-instrument-traps.md`,引用一律引那邊。

## 查重做了什麼(照收件匣規矩寫「開了哪幾條、為什麼判它不同族」)

`traps-neighbours.py --selftest` 十世界 PASS;餵草稿跑一發(`logs/traps-neighbours.jsonl` 第 59 行)。
**分數前八名沒有一條是同族**,最高 0.3020(`feedback_fixing-the-tool-does-not-fix-my-own-hand.md`,
講「修好工具沒修好自己的手」)。
🔴 **真正同族的那條【一分都沒拿到、不在名單上】** —— 那是工具已知的失效 ⑦(bigram 結構性盲區):
我的草稿講「編號 / 圈號 / 釘子」,正本那條講「代號 / 信箱檔名 / `wc -l`」⇒ 字面重疊接近零而概念完全同族。
✅ **撈到它的是工具自己建議的後手**:`grep -nE '^## .*(並行|競態|同時|兩個窗|多窗|編號)' 正本`。

**開檔讀了兩條**:
- `:18330`「兩個窗可以同時真心相信自己是同一個代號,而檔名沒有防碰撞」⇒ **同族,本檔補它。**
- `:2087` ㉖「跨窗的『同時性』不是一個可以被規劃的東西」⇒ **不同族**。
  它講的是**協調關係的壽命**(窗會消失);本檔講的是**兩個窗都還活著、都做對了事**。

## 這一次發生什麼

`database.types.ts` 檔頭有一份手動校正清單,條目用圈號編號,另有釘子 `EXPECTED_TOTAL_ENTRIES`。
同一夜兩個窗各自要加一條新條目,兩邊都跑了同一道量測:

```
grep -cE '^// {3}[①-⑳]' packages/adapters/src/supabase/database.types.ts   ⇒ 16
EXPECTED_TOTAL_ENTRIES                                                     ⇒ 16
```

⇒ **兩邊都取 ㉑,都把釘子改成 17。**
兩邊三綠全綠、兩支釘子測試全綠 ⇒ **沒有任何一把尺紅。**

🔴 **病灶不是「有人沒量」** —— 兩邊的讀數都是對的,各自從同一顆 `origin/dev` 出發,那一刻就是 16。

> **判別句:我要寫進去的這個值,是【我讀到的】,還是【我推出來的下一個】?**
> 是後者 ⇒ 📌 **它在多窗環境下沒有效期,而「我的量測是對的」不能保護它。**

## 🎯 本檔補的那個角度:`:18330` 的結論在 git 底下會反過來

`:18330` 的判別句是「這個檔名只有我一個人會寫嗎 ⇒ 檔名帶上 session 後兩碼」。
**那條的載體是信箱檔案,寫入機制是【覆蓋】** ⇒ 檔名撞 = 靜默蓋掉,先寫的 98 行憑空消失。

🔴 **而在 git 管的檔案裡,同一件事的兩端【剛好相反】**:

| 編號住哪 | 兩窗同取 | 撞了會不會叫 |
|---|---|---|
| 檔案**內容**裡(`//   ㉑ …`) | 兩邊各加在**不同錨點** | 🔴 **git 自動合得起來** ⇒ 合出一份有**兩條 ㉑** 的檔,**靜默** |
| 檔案**名字**裡(`traps-inbox/db-20260906n-….md`) | 兩邊取同一個字母 | ✅ **`added in both` 衝突,git 當場叫** |

⇒ 📌 **所以「檔名 vs 內容」不是判準,【寫入機制】才是** ——
**覆蓋式寫入(cp / `>` / 信箱檔)對檔名靜默;git 對檔名會叫、對內容靜默。**
🛑 **同一句忠告在兩個載體上指向相反的方向** ⇒ 引用 `:18330` 之前,先問「這個東西是被覆蓋的還是被合併的」。

⚠️ **本檔自己就是那張表的下半格** —— 我挑 `n` 用的是**跟挑 ㉑ 一模一樣的機制**,而它撞了會被 git 攔下。

## 🛑 而「釘子會接住這個」是【推的】,不是量到的

寫這一段的當下 39f 還沒推上來,**我還沒有合過**。
推理是:合出來 18 條條目 vs 釘子 17 ⇒ `database-types-apply-state.test.ts` 應該紅。

**那是預測。** 合的那一刻要實際跑一發,並看清楚兩件事:
1. 它紅在**哪一句** —— 「條目數不符」才算;紅在別的地方 = 這個推理沒被驗到。
2. **兩條 ㉑ 有沒有各自被數到** —— ⑳ 那次的教訓:重複的錨會被**吸進同一條**,
   而那會讓數字**碰巧對**(18 條被數成 17)⇒ **綠燈,而問題還在。**

⇒ 📌 **在跑那一發之前,「釘子會接住這個」是一句宣稱,不是一道防線。**

## 修法(主視窗 2026-09-06 裁)

先合 `origin/dev`,再把**後到的那一條**改號:`㉑ ⇒ ㉒`、釘子 `17 ⇒ 18`、檔頭處數 `40 ⇒ 41`
—— 🔴 **照守門紅出來的數,不照自己算的**(那把尺對 `**整段**` 一律算 1 處;我第一版自己算 42,它當場紅)。
正規式已擴到 `[①-⑳㉑-㉟]`,不用再動。
