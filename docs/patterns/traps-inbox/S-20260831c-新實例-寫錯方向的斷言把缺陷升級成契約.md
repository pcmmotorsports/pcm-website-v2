# 🔵 新實例(**不是新母題**)· 一條寫錯方向的斷言,把缺陷升級成契約

> 線【出貨】`-1e` · 2026-08-31 · codex R1 must-fix
> 🛑 **這是正本既有那條的一個新實例,請【併進去】不要開新條。**

## 併到哪

```
正本 docs/patterns/guard-and-instrument-traps.md
標題(用字面錨, 不用行號 —— 行號會漂):
  「這條測試斷言的是【我要的行為】,還是【現在的行為】?」
行號參考(會漂, 引用前自己 grep):我這棵樹 :9846 · origin/dev(7b9e5862) :9842
```
🛑 **我【沒有】直接改正本,而那是刻意的**:我這棵樹的正本落後 `origin/dev` **4,019 行**
(`git diff --numstat <merge-base> origin/dev -- <該檔>` ⇒ `4019+ / 0-`)
⇒ **在這裡編輯它,合上去會抹掉那 4,019 行。** 由在 dev 上的人併。

## 這一發實例

```
我在 route 加了「bad-format ⇒ 當場 return 503」, 並寫了一條測試:
  expect(checkSpy).not.toHaveBeenCalled();
```
codex 逐字:
> bad-format 測試強制斷言 `checkAnomalyAlerts` 不得執行 ⇒ 修成「出貨段停用但其他告警照送」時
> 測試反而變紅,**將上述缺陷鎖成契約**。

✅ 改成斷言**我要的行為**:`checkAnomalyAlerts` **有**被呼叫,且 `shippedCutoffIso: null`。

## 🔵 這一發比正本既有兩個實例多出來的一格

正本那兩例是**事後**發現「這條測試在保護一個 bug」。
**本例是【同一顆 commit 裡】作者一邊寫下缺陷、一邊寫下把它鎖住的斷言** ——
```
缺陷與鎖它的斷言, 由同一個人、在同一分鐘、為了同一個理由寫下。
⇒ 📌 沒有任何時間差可以讓人察覺不對;它們互相印證。
```
🔴 **而修它的人會先看到紅** —— 那個紅指著他,而不是指著寫測試的我。

## ⚠️ 射程

```
· 一個實例。我沒有掃過 repo 裡還有幾條斷言是「釘現況」而不是「釘期望」。**未數。**
· 抓到它的是 codex, 不是三綠, 也不是我。**這一類缺陷在任何自動化底下都是綠的。**
```
