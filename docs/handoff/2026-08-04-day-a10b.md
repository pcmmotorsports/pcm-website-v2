# A10b 交接 —— 訂單明細的採購表單 UI(2026-08-04 夜跑,worktree `a4a-chain`)

> **一句話**:master plan `:404` row 57(A10b UI)+ **併入 `:388` row 41(A9d1)的 procurement server action 半**
> —— **counters 那半未做**(收帳時 row 41 只完成一半,主視窗信箱 `B-01-A` 已同意這個口徑)。
> 零 migration、零 apply、未 push;三綠 + 強制重建 + 完整套件全綠;**27 個突變全紅**;
> 關卡2 **雙引擎三輪**:code-reviewer(opus)FAIL 1C+3I+6nit → codex R1 FAIL 10MF+6nit → codex R2 FAIL 5MF+2nit
> → **全數修完**。**突變 42/42 全紅**。🔴 我在 codex R1 反駁的兩條,R2 裁決**我錯、它對**(見 §5c)。
> **27 項驗收(逐項核對 `docs/specs/2026-08-01-admin-completion-map.md:58-60`,不是憑 row 57 的綠燈欄)**:
> · **第 7 項**(缺貨要等)= `A10a + A10b 聯合驗收`,A10a 已完成 ⇒ **本片是它的最後一塊**(待 Sean 驗)。
> · **第 5 項**還缺 **A9c**(列表三軸投影)、**第 6 項**還缺 **A9b2**(供應商單號跨單搜尋)與 **A10c2**
>   ⇒ **本片不足以讓 5/6 轉綠**。~~原本寫「第 5/6 項靠本片轉綠」~~ 是口徑大於事實,關卡2 codex 抓到、已更正。

---

## 1. 硬前置(開工第一件事,已完成)

`packages/adapters/src/supabase/database.types.ts` 補 **A5a 五處 nullable Args**
(`p_contact_channel` / `p_submitted_at` / `p_supplier_order_no` / `p_exception_reason` /
`p_expected_arrival_date` 的 `| null`)+ 檔頭計數 **十處 → 十五處**、新增 ④ 條目。

- 為什麼現在補:A5a 落地時刻意延後(呼叫端不存在 ⇒ 補了沒有 typecheck 會守住);**本片就是第一個呼叫端**。
- 機器實查(非目測):四個函式的 Args 真實 `| null` 行數 = `create_order` 3 / `admin_upsert_supplier` 4 /
  `admin_append_order_note` 3 / `admin_upsert_item_procurement` 5 = **合計 15**,與檔頭字面相符。
- 🔴 檔頭已改寫「RW2c 接線時計數要從**十五處**改二十處」——下一支動它的人負責。

## 2. 產物

| 檔案 | 性質 |
|---|---|
| `apps/admin/src/lib/orders/procurement-action-state.ts` | 🆕 欄位名 + 18 失敗碼 + 訊息表 + state |
| `apps/admin/src/lib/orders/procurement-form.ts` | 🆕 純解析器(形狀閘;值域留給 RPC) |
| `apps/admin/src/lib/orders/procurement-repository.ts` | 🆕 RPC 唯一呼叫端(17 碼窮盡收斂) |
| `apps/admin/src/lib/orders/procurement-actions.ts` | 🆕 server action(授權 → hydration → 截斷 → 解析 → 寫入) |
| `apps/admin/src/lib/orders/procurement-view.ts` | 🆕 **client-safe** 純函式(hydrate / 時區換算 / 標籤) |
| `apps/admin/src/lib/orders/procurement-suppliers.ts` | 🆕 **server-only** 選單合併(用 S3a 的排序) |
| `apps/admin/src/components/orders/item-procurement-form.tsx` | 🆕 client 表單 |
| `apps/admin/src/components/orders/item-procurement-section.tsx` | 🆕 server 區塊(清單 + 逐品項表單) |
| 七支 `.test.ts(x)`(含頁層接線)| 🆕 共 190 案 |
| `app/orders/[id]/page.tsx` / `components/orders/order-detail.tsx` | 接線(S3a 供應商各自容錯) |
| `components/orders/result-banner.tsx` | 三個成功碼各自一則 |
| `packages/adapters/src/supabase/database.types.ts` | 見 §1 |
| `docs/specs/…master-plan-v2.md:404` | row 57 落帳(見 §5 finding 4) |

## 3. 幾個刻意的設計決定(不是照抄樣板)

1. **沒有表單冪等 token**(A6/A9d2-1 有,本片刻意不做)。親驗 A5a 全檔:`p_request_id` **只**寫進
   `admin_audit_log.request_id`(`:426`/`:450`),**零唯一性約束、零「同 id 不同內容就 RAISE」**。
   冪等來自業務鍵 upsert + 同值 no-op(`:300-322`)⇒ 每次送出用新的 HTTP `x-request-id` 是對的。
   搬 token 機制過來只是多一個會壞的東西。
2. **一個品項一份表單,由「選供應商」決定新建/編輯** —— 照 A5a 的 upsert 鍵 `(order_item_id, supplier_id)` 畫。
   畫成「每列一個編輯鈕 + 另一個新增表單」要自己再發明一套狀態,而那套狀態可以和 DB 真相不一致。
3. **停用的供應商仍在選單裡**(前提:本品項已有一列指向它)—— 見 §5 finding 4,已回頭改 master plan row 57。
   ✅ **Sean 2026-08-04 拍板 A:保留**(信箱 `B-02-A`)—— 與 08-03 Q1=A 一致,不退回。
4. **`procurement-view` / `procurement-suppliers` 拆兩檔**:前者被 client 表單 import,
   後者要用 S3a 的 `sortSuppliersByLabel`(→ `supplier-repository` → `server-only`)。
   施工當場踩到(同檔會把 server-only 拖進 client bundle、直接建置失敗),當場拆的。

## 4. 🔴 下一個人必讀的三條

1. **`procurementTruncated` / `itemsTruncated` 為 true ⇒ 拒送**(A9a-2 MF1 的下游義務)。UI 端不渲染送出鈕
   + fieldset disabled;action 端 hidden `procurement_stale` 第二道。**hidden 欄放在 fieldset 之外**
   (disabled 的 fieldset 內欄位不會送出 ⇒ 放裡面等於閘自己消失)。
2. **hydration 前不給送**(見 §5 Critical):hidden `procurement_hydrated`,action 端 fail-closed
   —— **不是 '1' 一律拒,含完全沒帶**。代價 = 本表單放棄無 JS 的漸進增強,知情取捨。
3. **A5a 是全量 payload**:表單少填一欄 = 該欄被寫成 NULL。`hydrateFormValues` 是這條契約的承重,
   加欄位時型別會逼你去改它;`procurement-view.test.ts` 有一條「每個 key 都被填了」的結構斷言。

## 5. 關卡2:code-reviewer(opus)FAIL → 全數修完

| finding | 處置 |
|---|---|
| **Critical** React 19 form action 在 hydration 前送得出去 ⇒ 選填欄全空 + 全量 payload = 靜默清空五個欄位;而我檔頭原本宣稱的「安全底 = 解析器 fail-closed + RPC 第二道」**兩道都擋不住**(空白是合法的「沒填」) | 加 hydration 閘(hidden 旗標 + action fail-closed + 掛載前不渲染 submit 鈕);檔頭那句錯的宣稱已刪改 |
| **Imp.2** `useState(carried ?? …)` 初值只在首次掛載求值 ⇒ `state.values` 從未被任何 render 消費、「保留輸入」是空頭支票(受控欄位剛好自己活著) | 改成 useEffect 明確套用,且**只在本品項的失敗**時套 |
| **Imp.3** 「全欄 hydrate 自**最新**列」的「最新」只到頁面載入那一刻;bfcache 返回會拿更舊的快照 | `pageshow persisted → router.refresh()`;**明說只縮小窗口不消滅**(A5a 無 version/CAS)—— 見 §6 |
| **Imp.4** master plan row 57 逐字「停用的供應商**不得**出現在這個選單裡」與實作相反 | **回頭改 row 57**(加 2026-08-04 修正 + 理由 + typeahead 未實作也落帳);理由 = S1b `:183` 不阻止指向已停用者、Sean 08-03 晚 Q1=A 只擋新建與調升 ⇒ 照原字面拿掉,那一列**永遠改不了** |
| nit 三處行號漂移(`:16`→`:15`、`:225-228`→`:231`、`:327-330`→`:326`) | 逐條實查後更正 |
| nit `EMPTY_PROCUREMENT_VALUES` 回傳同一個參考 | 改回新物件 |
| nit `pnpm typecheck` 在本 worktree exit=1 | 見 §7 誠實命令 |
| nit `order_id` 與 `order_item_id` 可以不同單 | 列為已知邊界,見 §6 |

🔴 **修完之後,測試又逼出一個 reviewer 也沒抓到的**:**React 19 的 form action 完成後會 reset form,
而受控 `<select>` 不會被寫回**(文字欄會)⇒ 畫面變成「欄位有值、供應商沒選」,員工再按一次就是 `invalid`,
重選供應商又會觸發 hydrate 把他剛打的字沖掉。修法 = action 有結果就強制重繪一次(突變 N28 證明它承重)。

## 5b. 關卡2 第二輪:codex(`gpt-5.6-sol`,跨模型)FAIL 10 must-fix + 6 nit

**認並修的 8 條**:①bfcache 只 refresh 不夠(state 不重建)⇒ 連同選擇與欄位一起清 ②`datetime-local` 只到分鐘
⇒ 既有 `14:30:45` 只改別欄也會被靜默改成 `14:30:00` ⇒ 沒動過就原樣送回原始 ISO ③可見欄與 hidden 欄可分岔
(自動填不觸發 onChange)⇒ 改成在送出當下讀**真正的 FormData**、畫面不再有那個 hidden 節點
④`denied` 帶 `orderItemId=null` ⇒ `failedHere` 恆 false ⇒ **員工按下去像沒反應**(顯示條件放寬到 null,
但**套值**仍嚴格比對,免得 denied 的空值把輸入清掉)⑤`OVER_ALLOCATION` 訊息在「別家掛缺貨仍佔配額」時是死巷
⇒ 訊息改成叫他去調低那一家 ⑥typeahead 落帳口徑(見 §6-4)⑦本檔完成口徑(見頂節)⑧頁層接線無測試
⇒ 補 `procurement-wiring.test.tsx`(把 `listSuppliers()` 改成回空陣列會紅)。

**實查後不認的 2 條(附證據,非嘴硬)**:
- 「`new Date(local)` 用裝置時區、違反上游契約要求伺服器按 `Asia/Taipei` 補 offset」——
  **契約是相反的**:`note-form.ts:48-50` 逐字「**絕不**自行假設 `Asia/Taipei` 補上去 —— 那等於用猜的去寫一筆
  法律證據。A10a 負責產帶偏移的 ISO」;我的 `toOffsetIso` 與 A10a-3 `note-compose-form.tsx:57-62` **逐行相同**。
  🟡 殘餘為真且與 A10a-3 共有:員工裝置時區設錯 ⇒ 存錯時刻。要治得換成「顯式選時區」的 UI,兩片一起改。
- 「`order_id` 與 `order_item_id` 跨單時**會真的改錯訂單品項**」—— 寫入只吃 `order_item_id`,
  改到的**恰好就是被送上來的那個品項**,不會「改錯品項」。真正的後果只有 redirect/revalidate 指向另一張單
  (§6-2 已記);且動手的人本來就有權改任一張單,非權限提升。

**nit 全清**:hydration 旗標的宣稱改成只講它真的保證的事;失敗碼數目更正(14+**6**=**20**,原寫 18);
兩處檔名 `procurement-section.tsx` → `item-procurement-section.tsx`;actor 驗證行號 `:200-208` → `:189-197`;
result-banner 補三個採購碼的渲染測試;**「server-render」不等於資料沒進瀏覽器**的誠實更正
(`procurements` 會經 RSC props 序列化到 client;可接受的理由是這頁在 SSO 閘後,不是「它沒離開伺服器」)。

## 5c. 關卡2 第三輪:codex R2 —— 🔴 我反駁的兩條,我是錯的

**① 時區(我錯得最離譜的一條)**:我在 R1 用 `note-form.ts:48-50`(備註線契約「絕不自行假設 Asia/Taipei」)
反駁 codex,說它把契約講反了。**A5a 有它自己的呼叫端契約,而且就寫在函式 COMMENT 裡**:
`20260803160000:475-476` 逐字 —— 「呼叫端契約:…**submitted_at 的 offset 由 server 補 Asia/Taipei**」。
我讀了 A5a 的驗證邏輯(`:228-291`)卻沒讀它的 COMMENT,然後拿**別片**的契約去否定審查者。
⇒ 這正是 memory `feedback_assert-scope-only-after-reading-source-file` 那條:**契約債寫在 migration 註解裡,
`information_schema` 查不到**;而我這次連註解都只讀了一半。
**修法**:客戶端送**無偏移的台北牆上時間**、偏移由 **server action** 補 `+08:00`
(台灣固定 UTC+8、1979 年後無日光節約 ⇒ 常數偏移即精確);回填顯示也一律用台北、不用裝置時區;
解析器**拒收帶偏移的字面**(fail-closed:帶偏移 = 有人繞過了這條契約)。

**② 跨單**:我說「不會改錯品項,只是畫面跳錯單」。字面上沒錯(寫入的就是送上來的那個品項 id),
但 codex 的框法更有用:**從 A 單的表單可以寫進 B 單的品項**,而畫面跳回 A 單、A 單還不 revalidate
= 靜默的資料錯置。⇒ 不再爭字面,直接關掉:寫入前用 PK 查一次 `order_items.order_id`,
對不起來就當呼叫端 bug 擋掉(查無 / 查詢自己炸掉也一律不寫)。

**其餘 3 條 must-fix 也都認並修**:③`router.refresh()` 飛行中表單仍可送(舊 props)⇒ 加 `refreshing` 鎖,
新 props 到達才解鎖 ④選填欄「整個沒送」被當成合法 `null` ⇒ 全量 payload 靜默清值;解析器改成
**缺鍵即拒**(空字串才算「沒填」)⑤`isPending` 期間欄位仍可編輯 ⇒ 送出後才改的字不在那份 FormData 裡、
會靜默消失 ⇒ 一併鎖住。
**2 nit**:多個品項表單各掛 `pageshow` ⇒ 已知重複呼叫 refresh(見 §6-10);訊息表裡的 markdown 星號
(員工會直接看到 `**`)已全部清掉並掃過全表。

## 6. 明說沒做 / 邊界(不宣稱涵蓋)

1. **lost-update 窗口沒有消滅**:A5a 無 version / CAS 參數,「先讀後送」的「讀」只到頁面載入那一刻。
   bfcache refresh 只縮小窗口;**頁面開著的整段時間,別人改過的欄位仍可能被本表單的舊值覆蓋**。
   要治本得在 A5a 加 version 參數(migration 級改動,不在本片)。
2. **`order_id`(hidden)與 `order_item_id` 不同單**:RPC 只認 item id ⇒ 已授權者改 hidden 會「寫進 A 單、
   畫面跳 B 單、A 單不 revalidate」。uuid 形狀閘擋住開放重導 ⇒ 不是安全洞,但是已知的不一致面。
3. **hydration 旗標由 client 送** ⇒ 它保證的是「誠實路徑上不會 pre-hydration 送出」,
   **不保證** `hydrateFormValues` 跑完、**不保證**資料仍最新、**不保證**繞過畫面者不能自己帶 `'1'`。
   真正的 fail-closed 在 RPC 的 17 個固定碼。
8. **`page.tsx` 的 `@/` import 改成相對路徑**:root vitest 的 `@` alias 指向 storefront ⇒ 原本這一頁
   **完全無法被單測載入**(補頁層測試時當場踩到)。同 `lib/session/actor.ts:2` 既有慣例;行為零變更、
   由 typecheck + `--force` 重建把關。
9. ~~員工裝置時區設錯 ⇒ 存錯時刻~~ ✅ **已消滅**:偏移改由 server 補 Asia/Taipei(§5c),
   裝置時區不再參與。**代價**:員工若真的人在別的時區出差,畫面顯示與輸入都是台北時間 —— 這是契約要的。
10. **多個品項表單各掛一個 `pageshow` listener**(codex R2 nit):N 個品項的 bfcache 還原會呼叫 N 次
    `router.refresh()`。Next 會合併同一輪的 refresh,實害是多餘的工作而非錯誤資料 ⇒ 未做去重,誠實記下。
11. **`findOrderIdForItem` 每次寫入多一次 PK 查詢**:單列、有索引,換掉「從 A 單寫進 B 單品項」那個洞。
4. **typeahead 未實作**(row 57 明列)—— ✅ **Sean 2026-08-04 拍板 A:這期不做,需求移入具名 backlog
   `#308`**(信箱 `B-02-A`)。master plan row 57 的註記已改成指向 #308,需求有家、不是被施工端註記關掉。
   #308 內含「不修未來會痛在哪」= 供應商成長到 50+ 家時純下拉難找(現況 26 家字母序可用),
   並把 A10b 的兩條既有約束寫進預期解法(停用者仍可選 / 排序單一入口)。
5. **無 JS 路徑刻意不支援**(hydration 閘的代價)。
6. **逐批到貨明細仍未投影**(A9a-2 的邊界,第 2 批):UI 上「到貨」只有累計數,答不出「哪一批何時到」。
7. **本片零真資料往返**:`order_item_procurement` 目前 0 列、service_role only ⇒ 驗證止於單元 + 型別 + 強制重建。
   第一次真資料要等 Sean 在正式站肉眼驗(**留給早上**)。

## 7. 驗證(全部本 session 實跑)

- `pnpm turbo run typecheck` = **8/8**;root 的 `tsc -p tsconfig.scripts.json` 另外用 workspace 內的 tsc 跑 = 綠。
  ⚠️ **`pnpm typecheck` 這個命令在本 worktree exit=1**(root 無 `typescript`、主樹靠 hoist);
  寫「pnpm typecheck 綠」= 與事實不符。實際跑的是:
  `node $(find node_modules/.pnpm -maxdepth 4 -path '*typescript@*/node_modules/typescript/bin/tsc' | head -1) -p tsconfig.scripts.json --noEmit`
- `pnpm lint` = **10/10** ✅
- `pnpm turbo run build --force` = **2/2、0 cached**(🔴 用 `--force`:turbo cache hit ≠ 獨立驗證;
  這一步是 client/server 邊界唯一真正的守門)
- `pnpm test`(完整套件)= **4024 passed + 1 todo** ✅
- **突變 42/42 全紅**(逐一還原後 byte-equal 驗過)。**五個曾經存活**的突變值得記 ——
  每一個都是「斷言在量錯東西」,補了才紅:
  ① `hydrated` 旗標寫死 `'1'` —— 只驗「掛載後 = 1」的話全綠;改用 **server render 那份 HTML** 才量得到
  (pre-hydration 使用者送出的就是那份)。
  ② 套值 effect 不比對 `orderItemId` —— 斷言跑在 React 更新之前(`waitFor(mock 被呼叫)` 太早),
  改用 `act` 把 promise 與 effect 一次 flush 完才量得到。
  ③ 拿掉「action 後強制重繪」—— 就是上面那個 `<select>` 被 reset 清空的真 bug。
  ④ hidden 值改回用 state 算 —— 測試全用 `fireEvent.change`(state 與 DOM 恆同步)⇒ 量不到分岔;
  改成**繞過 React 直接寫 DOM value**(= 瀏覽器自動填的行為)才有判別力。
  ⑤ bfcache 只 refresh 不清狀態 —— 只驗「refresh 被呼叫」量不到;要驗**欄位真的被清空**。

## 8. 紅線遵守

不 push / 不動 STATUS 與 CURRENT / 不 apply(**零 migration**)/ 不碰 `.env*` / `git add` 精準 /
codex `-m gpt-5.6-sol -s read-only` 背景 `< /dev/null` / 肉眼驗留給 Sean(本檔零「肉眼驗」字樣)。
主視窗信箱往返 2 筆:`B-01-Q`/`B-01-A`(片界與收帳口徑)+ `B-02-A`(主視窗主動通知:Sean 拍 typeahead=A 開
backlog #308、row 57 修正=A 保留)。收工照協定補訂 2 寫 `B-03-STOP.md`。
