# `bc61afe6` 推不推 —— **不修的話會怎樣**(給 Sean 看的白話表)

> 線3(`pcm-website-v2-6e`)2026-08-27 04:2x。**這張表回答的是「不修會怎樣」,不是「技術上錯在哪」。**
> 背景:片二 `5276411e` **已經在正式站上跑**(`git merge-base --is-ancestor 5276411e origin/dev` ⇒ 在),
> 補審抓到 15 條 must-fix,修在 `bc61afe6`(**未推**)。

## 🔴 先講這張表怎麼讀
```
「線上正在發生」欄有三種值, 而它們對【推不推】的意義【不一樣】:
  ✅ 正在壞      現在就在傷害使用者或資料 ⇒ 推它是【止血】
  ⏳ 埋著        條件沒到就看不到, 條件到了就一次全發 ⇒ 推它是【拆引信】
  ⚪ 沒在壞      是「沒有東西看著」或「話寫錯了」⇒ 推它不會讓今天變好, 但會讓明天不退步
🔴 而最後一列是【反方向】的:修法自己引進的新面。它是【推的風險】, 不是推的理由。
```

## 一、✅ 現在就在壞

| # | 不修會怎樣(白話) | 怎麼量到的 |
|---|---|---|
| **M1** | **每個開著的後台分頁,每 60 秒去資料庫問一次「這個人還在職嗎」。** 一個人開 5 個分頁、上 8 小時班 ⇒ 一天約 **2,400 次**多餘查詢。 | 線上那版 `CHECK_INTERVAL_MS = 60_000` 且**沒有**「還早就直接回」那一段(`grep -c "fresh"` 於 `5276411e` 的 route ⇒ **0**)⇒ 每一發都查 DB |
| **N1** | **操作紀錄裡,同一個人同一次連線做的事會散成一堆對不起來的編號。** 要查一組可疑的員工變更時,答不出「這幾筆是不是同一次做的」。 | 線上那版沒有沿用舊編號(`grep -c "sid: payload.sid"` ⇒ **0**)⇒ 每 60 秒換一個。而那個編號被寫進 **7 處**操作紀錄(數法 `grep -rn "sid: auth.sid" apps/admin/src/lib --include='*.ts' | grep -v '.test.' | wc -l` ⇒ 7) |
| **MF1** | **一張被偷的票,最壞可以比我們宣稱的多活近 15 分鐘。** 而程式碼裡逐字寫著「最壞情況與以前完全一樣」—— **那句話現在是假的**。 | 線上那版沒有夾住新票的死期(`grep -c "maxAgeSec"` ⇒ **0**)。修法實測:鏈只剩 120 秒時,新票活 **120** 秒(沒夾是 900) |

## 二、⏳ 埋著 —— 條件到了會一次全發

(這一區兩條的碼都在線上那版;數法 `grep -c "fresh" <5276411e 的 route>` ⇒ 0)

| # | 不修會怎樣(白話) | 怎麼量到的 |
|---|---|---|
| **M3** | 🔴 **資料庫抖兩秒 ⇒ 每一個開著的分頁【永久】停止換票 ⇒ 15 分鐘內全體被踢去重新登入。** 而畫面上**沒有任何訊號**,員工只會覺得「突然被登出了」。 | 線上那版 `res.status === 401 \|\| 403 ⇒ stopped = true`(永久)。而查員工的那支函式對「被停用」與「DB 讀不到」**回同一個值**(既有歧義 `#933`;那支函式在 `apps/admin/src/lib/staff.ts:138`) |
| **M4** | **筆電闔上 20 分鐘,醒來票早就死了**,而分頁不會自己補救 —— 下一次點連結才發現要重新登入。 | 線上那版只有定時器、**沒有**「開頁立刻跑一次」與「回到前景補跑一次」 |

## 三、⚪ 沒在壞 —— 但不修,明天會再退步一次

| # | 意思 |
|---|---|
| **M2** | 後端費工回了三種可分辨的結果,而前端**從來沒讀過**它們 ⇒ 那份契約沒有消費者,下一個人會以為它有在用 |
| **M5 / MF2 / N2** | **沒有東西在看著**那幾道防護。M5:一支 82 行的前端元件零測試 ⇒ 上面四條全在裡面而三綠全綠。N2:改一行就能讓自己升級成「二階段驗證過」的 session,而六格測試全綠 |
| **MF4** | 「每次續期都會查在職」這句話**不成立**(三種票不查),而**行為是刻意的**(DB 掛掉時後台不該整個停擺)⇒ 我只改了那句話,**沒改行為** |
| **MF3** | 那支會發新登入憑證的請求**沒有驗來源**。同站的子網域擋不住。⚠️ **我沒有構造攻擊**,這是讀碼得到的 |
| **M6 / M7 / N3** | 流程面(紀錄沒同步、審查欠一場、我自己寫錯一句註解)—— **不影響使用者** |

## 四、🔴 反方向:**修法自己引進的風險**(這是推的風險,不是推的理由)

| 什麼 | 最壞會怎樣 | 為什麼我仍然認為可以推 |
|---|---|---|
| 新加的「驗來源」那道閘 | 如果正式站的來源字串對不上 ⇒ **換票永遠失敗 ⇒ 每個人 15 分鐘被登出一次** | 那個字串**不是我新寫的**,是後台**每一次改單都在跑**的同一道閘(`lib/orders/workflow-form.ts:49`)⇒ 它若是錯的,後台早就整片壞了。而瀏覽器同源 POST 會帶來源這件事,我用真瀏覽器**實測過** |
| 換票沿用舊編號 | 編號旋轉的「衛生」變少 | 那個編號的原意就是「一次登入一個」,而換票**不是**新的一次登入。修法是**回到原意**,不是放寬 |
| ⚠️ **未確認** | preview 驗不了這片(來源清單只認正式站網址)⇒ **沒有一個「先在別的地方試一次」的選項** | 🔴 **這一格填起來之前,不得拿它支撐「推」或「不推」** |

---

## 🔴 這張表有沒有哪一格比實際乾淨?我自己問一次
```
🔴 **而這一節自己就抓到一格**:我第一版寫「那個編號被寫進 **5 處**操作紀錄」,
   實際是 **7 處** —— 我先前用的尺加了 `grep -iE "audit|log|record|actor"` 這層過濾,
   **比宣稱窄**。數法換成 `grep -rn "sid: auth.sid" … | wc -l` ⇒ 7。
   📌 **一張「給人拿去做決定」的表, 每少一處就是把後果講小一點。**

① 「一天 2,400 次」是【算的】不是量的(**未量**):60 秒一次 × 8 小時 × 5 分頁。
   而它有一個我沒算進去的折扣:M3 會讓分頁在第一次失敗後永久停下來
   ⇒ 真實次數只會【更少】, 而少的原因是另一個 bug。**兩個 bug 互相遮蔽。**
② 「資料庫抖兩秒」我【沒有實際造過】那個世界 —— 它是從
   「查員工那支對兩種情況回同一個值」推的。推論鏈短而清楚, 但它是推的。
③ 表裡沒有一格是「我實際在正式站上看到的」—— **全部是從線上那版的【程式碼】量的**
   (量法一律 `git show 5276411e:<檔>` 之後 `grep -c`, 每一格的數字都寫在「怎麼量到的」那欄)。
   我沒有正式站的存取權, 也沒有去要。
④ MF3 那條我【沒有構造攻擊】。它是「補了一道閘」, 不是「堵了一個已證實的洞」。
```

## 建議(而決定是 Sean 的)
```
推   ⇒ 止住上面第一、二區。最會咬人的是 M3, 而它【沒有預告】。
不推 ⇒ 上面第一、二區繼續跑, 而 M3 會在某一次 DB 抖動時一次全發。
⏰ 而現在是深夜、沒有人在用 —— 這是一天裡出事代價最小的時段。
```
🔴 **推完怎麼確認它沒壞**:登入後台,**讓那個分頁開著別關,過 16 分鐘再回來看**。
```
還在後台裡      ⇒ 換票成功(那正是這片要修的東西)
被叫去重新登入  ⇒ 失敗 ⇒ 叫我 revert(一顆指令)
```
📌 這個判準在「成功」與「失敗」兩個世界**印不同的東西**,而且**用你的眼睛就看得到**。
