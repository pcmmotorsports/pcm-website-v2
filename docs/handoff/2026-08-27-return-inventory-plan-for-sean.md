# 退貨＋店內庫存 —— **你批准之後會發生什麼**(給 Sean 看的一頁)

> 線1(`pcm-website-v2-21`)2026-08-27 05:xx。**這張表回答的是「批下去之後會怎樣」, 不是「plan 裡有什麼」。**
> 對應的 plan:`~/pcm-mailbox/PLAN-v9-退貨與庫存-codex改-20260827.md`(1,428 行, 過了三輪審查)。
> **作者是 codex, 審查是 Fable, 我只是遞資料的。這張表是我寫的。**

## 🔴 先講這張表怎麼讀
```
「怎麼知道的」欄有三種值, 而它們的份量【不一樣】:
  量到   有人真的跑過 / 開檔數過, 附得出數法
  推的   從檔案內容推導出來的, 沒有實跑
  未確認 沒有人查過 —— 而它【不是】小事, 有幾格會直接卡住你
```

---

## 一、你批的是什麼

**一條 18-21 片的線, 淨施工 6 小時到 15 小時 45 分。**

| | |
|---|---|
| 怎麼算的 | 片 0-12 = 13 片 · 片 13 拆 2-4 子片 · 片 14 拆 2-3 子片 · 片 15 = 1 片;每片 20-45 分鐘 |
| 🔴 **不含什麼** | 等設計稿、等你回決策、**你自己跑正式庫探針**、migration apply、你肉眼驗收、每一片的審查 |
| 怎麼知道的 | **推的**。而 13/14 那兩片**沒有設計稿** ⇒ 連「拆成幾片」都是估的 ⇒ **上限那個 15h45 不是上限, 是下限的上緣** |

三個選項(plan 自己寫的):
```
甲【它推薦】批准整條線 —— 先開片 0、片 1, 後面各自等各自的閘
乙            只批片 0、片 1(鎖圖 + 測試基座), 做完重新看
丙            暫停整條線
```

---

## 二、🔴 這一批做完, 你**仍然不會有**的東西(這一節最重要)

| 你不會有 | 為什麼 | 怎麼知道的 |
|---|---|---|
| **顧客站看得到庫存** | 舊拍板 `#161`/`#214a`:顧客站不顯庫存、買鈕永遠可點、缺貨可賣 ⇒ **本輪不動顧客站那一面** | 量到(`20260614130000:13` 逐字) |
| **溢收的貨變成可賣庫存** | 你拍 `G=甲`:這一輪**只接入口①(客人退回)**。入口②(溢收)與③(訂單外進貨)**已排、未做** | 量到(拍板紀錄) |
| **成本 / 進貨價相關的任何東西** | 你拍 `Q-庫2`:這一輪不碰成本(一碰就綁報價單那個 repo) | 量到(拍板紀錄) |
| **與報價單那邊整合** | `#374` 的定義子句之一, 本輪劃出範圍 | 量到 |
| 🔴 **「今天有哪些退貨還沒退款」這個問題的答案** | 你拍 `Q21=乙`:退貨不綁退款。而退款帳本裡**零退貨關聯**, 人工註記又明文禁止拿來當篩選 ⇒ **系統裡沒有任何介面回答得了它** | 量到, **而不是我量的** —— 審查 R3 於 2026-08-26 逐欄看過 `20260810140000:75-117`。**我沒有重開那支 migration** |

🔴 **最後一列要特別看一眼。**
你當初接受的代價是「**靠人記**」。而實際的代價是「**無處可記、無處可查**」——
漏掉一張, **沒有人會發現**。⇒ **這比你點頭時看到的那句大。**(審查 R3-F3 抓的, 我沒有改掉它, 端給你。)

---

## 三、你批准之後, **還會回來找你**的格(不寫的話你會以為批完就沒事)

| 幾時 | 要你做什麼 | 沒做會怎樣 | 怎麼知道的 |
|---|---|---|---|
| 片 2 之前 | 回答「**哪些動作只能管理者做**」 | 片 2 開不了工 | 量到(plan 前置欄) |
| 片 3-5 之前 | 🔴 **你本人把一段 SQL 貼進 Supabase SQL Editor 跑**(撞名核對) | 片 3-5 寫不了 | 量到 |
| 同上 | 🔴 **去 Dashboard 抄回網站庫的連線識別**(填進探針的 `expected` 那格) | **探針跑了也只會印「⚠️ 不算過」** —— 那不是它壞了, 是那一格還沒有人填 | 量到(七個世界實跑) |
| migration 要上線時 | **你關掉 G7 那道閘** | 寫好的 SQL 一支都上不了正式站 | 量到 |
| 片 6-15 陸續 | **再回答 13 個營運問題**(核准要幾人、數量上限、告警給誰、盤點誰負責、逾期幾天…) | 對應的功能接不了線 | 量到(plan §11 列了 14 條, 我數過) |
| 片 13/14 之前 | 🔴 **退貨 UI 與庫存 UI 目前【沒有設計稿】** | 那兩片做不出來, 或做出來不是你要的 | 量到, **而不是我量的** —— 線1 前一班 2026-08-26 對 OD 兩支原檔複驗:「退貨」命中 **0**, 正對照「退款」51、「訂單」21 ⇒ 尺是活的。**我沒有重跑** |

📌 **每一次撞名核對都要佔用你一次。** 而 manifest 之後若新增或改名, **整個流程要重來一遍**(重產探針、重審、你重跑)。

---

## 四、這一批做完, 你**會**看到什麼

| 你會有 | 什麼時候 |
|---|---|
| 客人退貨可以登記, 而**貨沒回來之前不會有庫存** | 片 4、片 6 |
| 退回的貨進「待檢」, **員工看過才變成可賣**(你拍的 `H=乙`) | 片 7 |
| 壞掉的貨有地方放(隔離), 不會混進可賣的 | 片 5、片 7 |
| 店內的貨可以配給訂單, 而且**走既有的出貨守門**(不另開一條路) | 片 9、片 10 |
| 登錯身分**可以合併/拆開**(你拍的 `Q2=甲`) | 片 11 |
| 員工在畫面上走得完整套 | 片 13、片 14、片 15 |

⚠️ **而它是一整條**:plan 明文禁止發布「只有退貨登記、沒有可用庫存」的半套版本。
⇒ **中途停下來 = 你不會拿到可用的東西**, 只會拿到一堆還沒接起來的地基。

---

## 五、我自己回頭問「這張表有沒有哪一格比實際乾淨」

**有四格, 逐格講:**

0. 🔴 **這張表裡有兩格標「量到」, 而【不是我量的】** —— OD 沒有退貨稿那一格、退款帳本零退貨關聯那一格,
   都是**別人 2026-08-26 量的, 我照抄**。⇒ 它們的效期跟著那一天走, 不跟著今天走。**我已在該列標明。**


1. 🔴 **第一節那個「6 小時到 15 小時 45 分」看起來像一個工期。它不是。**
   它是**淨施工**估算, 不含上面第三節那一整欄要你做的事, 也不含審查與返工。
   而**今晚這條線光是【寫 plan】就跑了 9 輪、【一支探針】就跑了 4 版**。⇒ **真實時間會比那個數字大很多。**
2. **第四節那張「你會有什麼」的表, 每一列都是 plan 的承諾, 不是已經做出來的東西。** 一片都還沒開工。
3. **第二節我列了 5 條「你不會有」, 而那個 5 是我從 plan 讀出來的, 不是窮舉。**
   plan §11 自己列了 **14 條**缺口 ⇒ **我挑了與你最有關的幾條**, 其餘沒有進這張表。
   ⇒ **這張表比 plan 樂觀, 因為它比 plan 短。**

---

## 六、我沒有替你決定的事

**這張表不推薦任何一個選項。** plan 自己推薦甲, 而那是 codex 寫的, 不是我背書的。
⇒ 我能講的只有:**乙(只批片 0、片 1)的代價最小, 而它換到的是「鎖圖與測試基座」——**
  **那兩樣東西你看不到, 也不會讓任何員工少做一件事。**
