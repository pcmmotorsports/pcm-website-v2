# M-1-16c-4c(+ 4d)Kickoff Handoff — 詳情頁 SEO/AI 友善 + 選變體換圖

> 產出:2026-06-01 / pcm-website-v2 **16c 執行 session**(做完 16c-1~4b)交接給新開的執行 + 審查 session。
> 對象:① 16c-4c 執行 session ② 平行 fresh-context 審查 session。照 CLAUDE.md「Claude Code 自驅 slice SOP」。
> 本檔接續 `docs/handoff/2026-06-01-16c-kickoff.md`(16c 原始 kickoff、§1-5 背景仍有效)。

---

## 0. 一句話 + 現況

M-1-16c「前台接真變體」**5 子片已完成**(16c-1~4b);剩 **16c-4c 詳情頁 SEO/AI 友善**(🔴 碰 price 進結構化資料、鐵則 12)+ **16c-4d 選變體換圖**(D1=A 往後排)。

### commit 序(branch dev)
| 片 | commit | push? | 內容 |
|---|---|---|---|
| 16c-1 | `27dac47` | ✅ pushed | 商品卡接 domain 真圖 + 無圖 fallback |
| 16c-2 | `59d4143` | ✅ pushed(origin/dev 在此) | 變體資料層 findByHandle + embed + mapVariantRow |
| 16c-3 | `81b5480` | ❌ 未 push | 詳情頁接真 + 選變體換價(ProductInfo 資料驅動選擇器) |
| 16c-4a | `2b7d270` | ❌ 未 push | pd-sku 真 sku + pd-sub DB subtitle 去義大利(v2 修 ProductPage.test 紅) |
| 16c-4b | `fe5b059` | ❌ 未 push(HEAD) | 規格表產品型號 productCode + 產地泰國 |

**未 push = 3 commit(81b5480 / 2b7d270 / fe5b059)、等 Sean 手動推。新 session 不代推。**
起手 `git status` 應乾淨、HEAD=`fe5b059`、ahead origin/dev by 4(含 ecfc4dc handoff)。

---

## 1. 16c-4c 要做什麼(本片 scope、Sean brief 落檔)

**Sean 拍板:** 詳情頁加 **AI 友善 + SEO 優化**(結構化資料 + metadata 強化)。

🔴 **碰 price 進結構化資料 = 鐵則 12 → 動手前 plan + codex 關卡1 + commit 前 codex 關卡2**(+ commit 前 grep .next/static)。

### 1.1 page.tsx 加 schema.org/Product JSON-LD
- `app/products/[slug]/page.tsx` 注入 `<script type="application/ld+json">` schema.org/Product:
  - `name`(product.name)/ `image`(product.images[])/ `description`(真 subtitle 或 description)/
    `sku` ← **productCode**(16c-4b 加的真主碼、如 RPM-DCC01)/ `brand`「RPM CARBON」/ `category` / `offers`。
- 🔴 **offers.price 只用 general 公開價**:UIProduct/UIVariant **本就只帶 general、無 priceByTier**(16c-2/3 已 strip、編譯期擋)。
  **絕不放 price_store / priceByTier / dealer / cost / shopee。**
- 多變體可用 **AggregateOffer**`lowPrice`/`highPrice`(全 general;從 product.variants[].price 取 min/max)。
  單一價可用單 Offer。priceCurrency 'TWD'、availability 對齊(注意 #161 不顯庫存政策、availability 結構化資料是否放需判)。

### 1.2 generateMetadata 強化
- `description` ← 真 subtitle(product.subtitle、16c-4a 已 plumb)。
- **Open Graph**:og:title / og:description / og:image(product.images[0])/ og:type=product。
- **canonical** URL。
- 先**查 M-1-09 SEO slice 有無既有 JSON-LD / metadata 慣例**(`grep -rn "application/ld+json\|openGraph\|alternates\|canonical" apps/storefront/src`)、沿用不重造。

### 1.3 審查觸發(鐵則 12 pricing)
- 動手前:plan + **codex 關卡1**(寫 packet `docs/reviews/2026-06-01-16c-4c-plan-k1.md`)。
- commit 前:三綠 + 完整 `pnpm test` + code-reviewer + **codex 關卡2**(diff、PCM 鐵則 + 經銷洩漏)。
- 🔴 **commit 前 grep `.next/static`** 無 `price_store` / `price_by_tier` / `priceByTier` / `shopee` / `cost`
  (JSON-LD 是新洩漏面、price 直接進 client HTML、盯死)。

---

## 2. 16c-4d 要做什麼(D1=A 往後排、16c-4c 之後)

**選變體換圖 + 狀態提升**(修 16c-3 兩個已知限制):
- `ProductPage` 把 selectedVariant 狀態**提升**(建議 `selectedSku` state + 從 `product.variants` derive currentVariant、避免 stale object;codex 16c-3 k2 consider)。
- `ProductInfo` 改**受控**(收 `selectedVariant` + `onSelectVariant` props、移除 local state + reset effect;selectSpec snap 邏輯改 call onSelectVariant)。pd-sku 改讀提升後的 selectedVariant(JSX `{selectedVariant?.sku ?? slug}` 不變、只改來源)。
- `ProductGallery` 吃 **currentVariant.images**(空 → fallback 商品 images、Q3C;68% 變體無專屬圖故 fallback 常見)。已有 activeImg reset effect([gallery] 變更 reset 0、16c-3 加)。
- **mobile buybar**(ProductPage)addToCart 改用**使用者選中的 selectedVariant.sku**(修 16c-3 限制:現用 product.variants[0]?.sku 預設變體)。
- 非經銷敏感(sku/image 公開)→ 三綠 + code-reviewer、**不跑 codex**(估)。
- **收尾**:`/pcm-roadmap`(更新進度地圖)+ `/graphify --update`(知識圖譜增量、16c 整組動程式碼後一次跑、本 session 延後未跑)。

---

## 3. Sean 拍板彙整(16c 全程、必遵)

| 決策 | 內容 |
|---|---|
| Q1=A | 規格選擇器全用**文字鈕**(.pd-size-grid/.pd-size-btn) |
| Q2=A | 規格顯**中文**:Forged=鍛造紋/Honeycomb=蜂巢紋/Plain=平紋/Twill=斜紋;Glossy=亮面/Matt=消光;12K=12K碳纖/Kevlar=Kevlar芳綸/(無)=標準 |
| Q3=A | **資料驅動**第三排:每 distinct>1 spec key 一排(weave/finish/special;25 商品/64 變體有 special) |
| Q4=A | 沿用 **#161 不顯庫存**(按鈕永遠可點、訂貨型業務) |
| 產品型號=A | 接 external_id(16c-4b 已做、domain `productCode` 欄、顯 RPM-DCC01) |
| 產地=甲 | 去義大利→**泰國**(16c-4b 已做、hardcode 非 DB、Phase-1 RPM-only 臨時值) |
| D1=A | 「選變體換圖+狀態提升」往後排成 **16c-4d**(本 brief 後做) |
| D2=C | ProductTabs L116「義大利原廠保固 24 個月」**留著不動**(同屏產地泰國殘差矛盾、刻意留後續內容片清、已程式註+manifest override+commit body 揭示) |
| 詳情頁 tier | **釘 general**(16c-3:public view 無真經銷價、store/premiumStore dummy 0、釘 general 防 NT$0;tier-aware 延 M-2-08) |

---

## 4. 🔴 經銷價防護(16c-4c 最重點、JSON-LD price 盯死)

三層防護(16c-1~3 建立、16c-4c 必延續):
1. **DB**:`products_public` / `product_variants_public` view 物理排除 `price_store` / `metadata`;透過 embed select 亦 PG 42703「column does not exist」。
2. **型別**:`UIVariant` = `{ sku, spec, price:number, images }`(**無 priceByTier**、編譯期擋);`MockProduct.price` / `variants[].price` 皆 general。
3. **bundle**:commit 前 `cd apps/storefront && grep -rl "price_store\|price_by_tier\|priceByTier\|shopee\|cost" .next/static`(必 0)。

🔴 **16c-4c JSON-LD offers.price 只能取 general**(product.price / variants[].price、皆已 strip 成 general)。AggregateOffer lowPrice/highPrice 從 `product.variants.map(v=>v.price)` min/max。**絕不從任何帶 priceByTier/經銷的來源取價**(根本沒有、編譯期就擋,但 codex k2 + grep 雙保險)。

---

## 5. 關鍵檔案 + 接線現況(新 session 起手讀)

- **`apps/storefront/src/lib/products.ts`**:`toUIProduct(product, tier)`(strip:image/images/variants〔只 general price〕/productCode/subtitle)+ `fetchProductByHandle(handle)`(**釘 general**、findByHandle + embed 變體)+ `fetchFeaturedProducts`。server-only。
- **`apps/storefront/src/data/mock-products.ts`**:`MockProduct`(欄:+image?/images?/subtitle?/productCode?/variants?)+ `UIVariant` 型別(sku/spec/price/images、無 priceByTier)。
- **`apps/storefront/src/app/products/[slug]/page.tsx`**:詳情頁 route、`fetchProductByHandle` + `generateMetadata`、tier 釘 'general'(16c-4c 在此加 JSON-LD + 強化 metadata)。
- **`apps/storefront/src/components/ProductInfo.tsx`**:資料驅動變體選擇器(specGroups/selectSpec snap/SPEC_NONE='__PCM_SPEC_NONE__'/displayPrice/pd-sku 真 sku/pd-sub 真 subtitle);**local selectedVariant state**(16c-4d 提升)。
- **`apps/storefront/src/components/ProductGallery.tsx`**:吃 product.images(resolveSrc 真圖/seed fallback)+ activeImg reset effect([gallery]);16c-4d 改吃 currentVariant.images。
- **`apps/storefront/src/components/ProductTabs.tsx`**:規格分頁;產品型號 `{productCode ?? slug}`、產地「泰國」、L116 義大利保固(D2=C 留)。
- **`apps/storefront/src/components/ProductPage.tsx`**:詳情頁主元件;mobile buybar addToCart 用 `product.variants?.[0]?.sku`(預設變體、16c-4d 改 selectedVariant)。
- **domain**:`packages/domain/src/catalog/types.ts` Product 含 `productCode: string`(必填)+ `subtitle`/`variants`/`images`;`ProductVariant`{sku/spec/priceByTier/availability/images/sortOrder}。
- **adapter mapper**:`packages/adapters/src/supabase/mappers/product.ts` mapSupabaseProductToDomain(productCode←external_id、variants 排序 sortOrder+sku)+ mapVariantRow(store/premiumStore dummy 0 + runtime guard)+ mapDomainProductToSupabase(external_id←productCode)。
- **目標 DB**:`bmpnplmnldofgaohnaok`(933 商品/7277 變體)。範例 handle(含 special 三排):`rpm-bms1k2kr03`(單座蓋)/ `rpm-bms1k2k307` / `rpm-bms1k2kr06`;`rpm-dcc01`(卡鉗散熱導風管、9 變體)。external_id 如 `RPM-DCC01`。

---

## 6. SOP + 踩坑(本 session 學到、新 session 必避)

1. **完整 `pnpm test`、非子集**(memory `feedback_run-full-vitest-after-shared-component-change`):動共用元件後別只 `vitest run <單檔>`、其他元件整合測試會 cross-effect 紅;16c-4a v1 中招(ProductPage.test 漏抓)。commit body 寫測試綠前先跑完整套件。
2. **stage 後又編輯必重新 `git add`**(memory `feedback_restage-after-edit-lint-staged-trap`):lint-staged hide unstaged、靜默提交舊版;commit 後 `git status --porcelain` 驗乾淨。
3. **sentinel/字串別誤入 NUL byte**:16c-3 SPEC_NONE 曾誤入 `\0` → git 判 binary、codex 沒審到;用純文字常數。
4. **manifest hash**:動 storefront 元件 → `design-storefront-manifest.yaml` 對應條目 last_modified 用 `PENDING_<slice>` 占位、commit 後 sed→**可達父 commit hash**(非 slice 自身、避 orphan)+ amend;`git merge-base --is-ancestor <hash> HEAD` 驗(memory `project_status-top-hash-off-by-one-normal`)。
5. **STATUS 7 欄 + 表頂 hash 記可達祖先**(同上 memory):slice commit 自身 hash 不入「最近3commit」表、記父 commit;手動更 STATUS(busboy 例外規則 7:已手動更不再 amend)。
6. **codex 紀律**(memory `feedback_cowork-out-codex-adversary-flow` + skill `codex-adversary`):只 main session 跑、`codex exec -s read-only`、尾接 `< /dev/null`、`dangerouslyDisableSandbox:true`、timeout ≥180000、跑前後 `git status --porcelain` 比對 codex 無動手;每 slice 全程硬上限 2 輪、round2 仍 FAIL raise Sean。**16c-4c 命中 pricing → k1(plan)+ k2(diff)都跑。**
7. **決策 prose multi-select**(memory `feedback_decision-delivery-prose` + `..._plain-language`):需 Sean 拍板用白話 prose 2-4 選項 + copy-paste 回覆行、不走 AskUserQuestion、不堆術語。決策岔路一次性批次問。
8. **視覺/字面 Sean 自理**(memory `feedback_sean-owns-visual-design-going-forward`):2026-06-01 起執行/審查 session 不主動做設計 polish、只做資料接線/結構/安全;確切排版字面交 Sean 用設計 skill。
9. **不 push**(memory `feedback_push-is-sean-manual-do-not-offer`):default 不代推、不問「要不要 push」;只 Sean 明確講才推那次;收工固定「未 push N commit、等你手動推、停」。

---

## 7. 審查 session

平行 fresh-context 審查 session 審每片 commit(三綠 + 字面 vs 事實 + 鐵則 + manifest + hash 可達 + 經銷防護)。16c-4a v1 的 ProductPage.test cross-effect 紅就是審查 session 完整 vitest 抓出來的。16c-4c 因 pricing,審查 session 會盯 JSON-LD price 防護。

## 8. backlog / 已知(留後續、非 16c-4c/4d scope)
- **#162**:原產國 country 欄位債(產地泰國 hardcode、ProductTabs L116 義大利保固殘差);Phase 2 brand.country / product_specs / site_policies 真接。
- **#203**:save-variant 寫路徑未做(16c-2 只讀路徑、#203 未全閉);前台只讀、import 腳本直寫。
- **#205**:featured 通用機制(旗標/跨分類)。
- proper variantSku cart key(現用 sku 走 color 欄 discriminator、不改 CartContext 契約)。
- `/products` 列表頁真資料 = 獨立 slice(Q4=A、不混 16c)。
- **16d** 每週重抓同步(留)。
- g-7 wallet HOLD #202 不變。

— END —
