# 早上那一推 —— **整批 64 顆合起來會不會出事**(給 Sean 看的表)

> 線1(`pcm-website-v2-21`)2026-08-27 06:xx。**八個窗各自驗了自己那幾顆, 而【這 64 顆合起來】在本檔之前沒有人看過。**
> 而 `dev` = `pcm-admin` 的 production 分支 ⇒ **推 = 直接上線。**

## 🔴 先講這張表怎麼讀
```
「怎麼知道的」欄:
  量到   本檔當場跑過 / 開檔數過, 附得出數法
  推的   從檔案內容推的, 沒有實跑
  未確認 沒有人查過
🔴 而本檔【不是逐顆重審】—— 是只打【合起來才看得到】的那幾種。逐顆的審查各窗自己做過。
```

## 〇、分母(當場量的, 不是主視窗給的 61)

```
git log --oneline origin/dev..HEAD | wc -l            ⇒ 64      ← 主視窗稍早量到 61, 之後又長了 3 顆
git rev-parse --short origin/dev                      ⇒ 9f9771b9
相異檔數                                              ⇒ 67
  docs/ 20 · apps/ 35 · scripts/ 5 · packages/ 2 · supabase/migrations/ 2 · STATUS.md 1 · package.json 1
```

## 一、✅ 整批的機械檢查 —— **全綠**

| 檢查 | 結果 | 怎麼知道的 |
|---|---|---|
| 三綠(`TURBO_FORCE=1`) | typecheck / lint / build **rc 皆 0** | 量到, 2026-08-27 06:0x 當場跑 |
| admin 測試 | 282 檔 / 5120 測項 / **紅 0** | 量到(**只跑一發**) |
| storefront 測試 | 256 檔 / 3692 測項 / **紅 0** | 量到(只跑一發) |
| node 測試 | 136 檔 / 2575 測項 / **紅 0** | 量到(只跑一發) |

⚠️ **只跑一發** ⇒ 照鐵則 11 該連跑兩發比三個數。**我沒跑第二發**(整批三個 project 一輪約數分鐘, 而我要先把這張表交出去)。**標明, 不假裝。**

## 二、① 順序依賴 —— **沒找到, 而尺的範圍寫在旁邊**

| 問的 | 答案 | 怎麼知道的 |
|---|---|---|
| 有沒有 code 依賴那兩支**沒 apply** 的 migration(`20260826150000` / `160000`)? | **沒找到** | 量到:`git diff origin/dev..HEAD -- apps/ packages/` 的**新增行**裡 `shipments\|shipment_items\|email_outbox` ⇒ **0 命中** |
| 那兩支是新推的嗎? | **不是** —— 它們**已經在 `origin/dev` 上**(`git cat-file -e origin/dev:<檔>` ⇒ 兩支都在), 這批只是改它們的內容 | 量到 |
| `20260826140000`(生日兩欄 view, Sean 已親 apply)有沒有 code 依賴? | **未確認** —— 我沒有查這一支 | 🔴 **未確認** |

⚠️ **尺的範圍**:我掃的是 `apps/` 與 `packages/` 的**新增行**裡那三個表名。
**若依賴是透過 view 名、RPC 名或字串組出來的, 這把尺抓不到。**

## 三、② 同一支檔被多個窗改過 —— 四格要看

| 檔 | 幾顆碰過 | 合起來的狀態 | 怎麼知道的 |
|---|---|---|---|
| `apps/admin/src/lib/session/session.ts` + `session-renew.tsx` + `api/session/renew/route.ts` | 各 **2** | 🔴 **見第五節, 這是本批最重要的一格** | 量到 |
| `apps/storefront/src/styles/tokens.css` | **2**(`e92ad884` GILLES 上架 / `b6176c4f`) | ✅ **補量完了 —— 見第五節之二**。既有 token **一顆都沒被改** | 量到 |
| `CLAUDE.md` | **2** | ✅ **淨差異 = 0 行** —— 一顆改、一顆撤回(`652271ef` 逐字「撤回我越權的那一改」)⇒ **規則檔這一推不會變** | 量到:`git diff origin/dev..HEAD -- CLAUDE.md \| grep -cE '^[+-][^+-]'` ⇒ **0** |
| `package.json` | 1 | 只加一條 lint-staged entry(`scripts/citation-rot-check.py`), 而那支腳本**在同一批裡** | 量到 |

## 四、③ commit body 說 X 而現況是 Y —— **一張別被 body 誤導的清單**

**這批有 8 顆的標題自己就寫著在訂正別的東西**(數法:`git log --oneline origin/dev..HEAD | grep -E "訂正|更正|推翻|過期|假|不成立|作廢"` ⇒ 8)。
⇒ **意思是:早上讀 body 時, 有些 body 已經被後面的顆推翻了, 而 body 改不掉。** 挑三格:

| body 說 | 而現況是 | 怎麼知道的 |
|---|---|---|
| `7228d8d0` 說某 2 格紅的成因是「index 上有別窗 staged 的 migration」 | **假的。** 已訂正在 `f4b36ea2` | 量到 |
| 🔴 `f4b36ea2`(**上面那個訂正**)說真成因是「harness 暫時工作區看不到三支檔」 | 🔴 **也不成立。** 那三支在這 64 顆裡**一顆都沒被動過**(各 `⇒ 0`), 而測試**現在全綠**(連跑三發 3/3/3) | 量到 |
| `9b2c3fe3` 逐字「我把自己擬的選項標籤寫成了 Sean 的逐字 —— 五處訂正」 | 那是**別的窗自己抓到自己**把擬稿當成 Sean 原話 ⇒ **早上引用那幾處拍板時, 要看訂正後的版本** | 量到(標題) |

🔴 **第二列是我自己的, 而它是本檔最該被讀到的一格:**
**我對同一個紅講錯了兩次, 而第二次我有把握到去 commit 一份訂正。**
⇒ 第三次的訂正已寫進 `scripts/migration-new-file-gate.test.ts` 檔頭, **而它明說自己是推測、並寫出缺哪一道檢查**
  (缺的是:**弄髒工作樹 ⇒ 重跑 ⇒ 看它會不會紅**。沒有人做過, 我也沒做 —— 那會影響別的窗)。

## 五、④⑤ **有沒有不該現在推的** —— 我找到的是反過來的

| | |
|---|---|
| **④ 有沒有動到 `.env` / secret / 對外可見的東西?** | **沒有。** 相異 67 檔裡 `.env` / `.github` / `vercel` / `next.config` ⇒ **0**;唯一的設定檔是 `package.json` 的一條 lint-staged entry。**量到** |
| 🔴 **⑤ 不該推的?** | **我沒有找到「不該推」的。而我找到一顆【現在不推才是問題】的。** |

**那一顆是 `bc61afe6`(後台換票那片的補審修正)。**
```
5276411e(有 bug 的那版)⇒ git merge-base --is-ancestor 5276411e origin/dev ⇒ 在   🔴 已經在正式站上跑
bc61afe6(修好的那版)  ⇒ 同法 ⇒ 不在                                          🔴 還沒推
```
⇒ **現在線上跑的是有 bug 的那版。** 那一片的白話影響表線3 已經寫好:
`docs/handoff/2026-08-27-bc61afe6-impact-table.md`(**那張表不是我寫的, 我沒有複驗它的每一格**)。
⇒ **對這一顆而言, 「推」是止血, 「不推」才是風險。**

## 五之二、🔴 `tokens.css` 補量(主視窗指定補的那一格)

**結論:這一格從「未確認」變成「量到」, 而且是安全的。**

| 問的 | 答案 | 數法(可重跑) |
|---|---|---|
| 既有 token 有沒有被改值或刪掉? | **一顆都沒有** | 逐檔比對 `origin/dev` 與 `HEAD` 的 token 宣告集合:`product-page.css` 10→10 消失 0 · `tokens.css` 68→**69** 消失 0 · `wallet.css` 0→0 消失 0。**正對照**(餵一個真的少一項的集合)⇒ 印出少的那一項 ⇒ 尺會動 |
| 新增的是什麼? | **一顆** `--gil-blue: #002a58`(第 17 家品牌的特色色) | 量到 |
| 它會不會被別的地方蓋掉? | **不會。** 贏的值 = 宣告的值;`[data-theme="dark"]` 那一桶**沒有**它 | 量到:`scripts/tool-final-css.py` 的 `final_values_css`, `:root` 桶 ⇒ `--gil-blue` = `#002a58`;dark 桶 10 顆、不含它 |
| 它影響幾個地方? | **一個** —— `product-page.css:1190` `.pd-bs--gilles`, 與其他 7 家同一個形狀 | 量到 |
| `wallet.css` 那行被刪的 `color: var(--c-text-muted, …)` 呢? | **是修不是退步** —— 那顆 token **全 repo 零定義** ⇒ 它一直在走 fallback;`a87b4827` 把它改成明確的值並寫明理由 | 量到 |

🔴 **而我在這一格用錯了兩次尺, 兩次都值得寫下來:**
```
① 第一把尺(數 diff 裡被刪的 `--` 行)⇒ 印 1;第二把(比對宣告集合)⇒ 印 0。
   **兩把不同意。** 開檔看:那 1 行是 `- color: var(--c-text-muted, …)` —— 一個【用法】不是【宣告】。
   ⇒ 第一把尺量的範圍比我宣稱的寬, 而**它印出來的 1 看起來完全像一個真的刪除**。
② 我第一次呼叫 CSS 工具時用錯了 key 的形狀(它是 `(at-rule, selector)` 不是屬性名)
   ⇒ `--gil-blue` 回 None、負對照也回 None ⇒ **兩個世界印同一個東西, 我分不出來。**
   ⇒ 而我第二次的「正對照」挑了一個**不存在的 token 名** ⇒ **又是兩邊都 None。**
   ✅ 挑到真的存在的(`--samco-blue` `#24418f` / `--ebc-blue` `#243588`)之後, 尺才真的會動。
```
📌 **一個負對照回 None, 而正對照也回 None ⇒ 那不是「乾淨」, 那是【這把尺今天沒有上線】。**

⚠️ **殘餘限制**:上面是**解析器算出來的贏家值**, **不是真瀏覽器的 computed value**。
   我**沒有開瀏覽器**。⇒ 若有 `@supports` / 更高特異性的規則在別的檔覆寫它, 這把尺看不到
   (而我另外查過:`--gil-blue` 在全 `apps/storefront/src` 只出現 2 次 = 宣告 1 + 使用 1)。

---

## 六、我回頭問「這張表有沒有哪一格比實際乾淨」

**有五格:**

1. 🔴 **三個測試 project 我只跑了一發。** 鐵則 11 要連跑兩發比三個數 —— **我沒做。**
2. 🔴 **這不是逐顆重審。** 64 顆裡我**開過 body 的不到十顆**, 其餘只看標題。
   ⇒ **第四節那張「body 說 X」的清單是【抽樣】, 不是窮舉。**
3. **順序依賴那格的尺只掃三個表名的字面** —— view / RPC / 組字串的依賴抓不到(已寫在該節)。
4. **`tokens.css` 兩個窗改同一支, 我沒跑真瀏覽器** ⇒ 那一格是**未確認**, 不是「沒問題」。
5. **`20260826140000` 那支已 apply 的 migration 有沒有 code 依賴, 我完全沒查。**

### 🔴 而剩下沒補的兩格, 是【判斷不補】不是【沒時間】(主視窗 2026-08-27 裁定, 理由照貼)

**一份自己說「我沒看完」的檔, 如果沒有寫「而剩下的為什麼可以不看」, 下一個人會以為是沒時間。**

```
❌ 第 2 格(逐顆讀 64 顆 body)⇒ 不補
   · 成本高而產出是【一張更完整的抽樣清單】—— 它擋不住任何東西
   · 🔴 body 改不了。逐顆讀完, 產出仍然只是「哪幾顆的 body 是假的」
     ⇒ 那份清單的用途是【他早上不要被 body 誤導】, 而抽的三格已足夠示範風險存在
   · 「抽樣不是窮舉」那個標註已經做了它該做的事

❌ 第 5 格(20260826140000 有沒有 code 依賴)⇒ 不補
   · 那支【已經 apply 了】(Sean 親跑)
   · ⇒ 就算有 code 依賴它, 依賴的東西也已經存在 ⇒ 推它不會炸
   · 🔴 這一格的風險方向是反的:風險在【apply 之前推 code】, 而那個時刻已經過了

✅ 第 4 格(tokens.css)⇒ **補了**, 見第五節之二。理由:它是設計 token、dev = production、
   而視覺退步【三綠不會紅】, 只有人眼或 computed value 抓得到。
```
⚠️ 第 1 格(只跑一發)與第 3 格(尺只掃字面)**仍然沒補**, 而它們沒有被判「可以不看」——
   **它們只是我沒做。**

## 七、一句話

**機械面全綠, 而綠的射程只到「我跑過的那幾把尺」。**
本檔沒有找到「不該推」的東西;**而它也沒有看完 64 顆。**
⇒ 🔴 **最該處理的不是這批有沒有問題, 是【線上現在跑著一版已知有 bug 的後台換票碼, 而修它的那顆還沒推】。**
