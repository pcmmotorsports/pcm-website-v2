# ⛔ 這不是 handoff 正本 —— 正本在 `~/pcm-mailbox/handoff-account-20260907.md`

🔴 這支檔是 2026-09-07 03:0x 我**誤建**的(該追加的是 mailbox 那一支)。
內容已**逐字**搬進正本, 這裡不留第二份全文 —— 兩份會分岔, 而分岔的時候沒有人會知道哪一份是對的。

⇒ **要讀 handoff 去** `~/pcm-mailbox/handoff-account-20260907.md`。

## ⚠️ 2026-09-07 22:0x —— 我又在這裡寫了三段, 已搬走

21:2x / 21:5x / 22:0x 三次存檔誤追加在本檔, **已逐字搬進正本並在那裡標明成因**。
🔴 **本檔【第二次】被當成正本寫了**(03:0x 一次、21:2x-22:0x 一次)⇒ 📌 **檔頭那句警告攔不住我, 兩次都沒有。**
⇒ 🔵 **要真的攔住, 得是【機制】不是【文字】** —— 而那超出本檔範圍, 記在這裡給下一個人。

---

## 2026-09-08 · zsh 坑補正本 + P1-2 全 repo 掃(存檔點)

**已 commit(未推)**
- `9be417d34` docs(patterns): 補 zsh `$fn[^;]` 下標坑 —— 正本 `docs/patterns/zsh-and-bash-traps.md` 檔尾
- `860379f81` docs(patterns): 把「負對照對這族天生失明」提到節首(主視窗訂正)

**已發出**
- 排序檢查結果 → 主視窗 `pcm-website-v2-1a`(57 支分母、0 支先給後收、尺雙向驗過)
- 兩個尺失效實例 → `refund` `pcm-website-v2-37`(併進他那一節、不另開)
- P1-3 報告檔頭補「分母 = 相對 `payment_confirmer` 這一個角色」

**P1-2 產物**
`~/pcm-mailbox/盤點-P1-2-兩個世界印同一個東西-account-20260908.md`(136 行)
- 形狀命中:shell 111 / TS 34 / SQL 0(SQL 那個 0 已用構造檔做過正對照)
- 抽開 10 格 ⇒ **9 格檔案自己已經寫明是刻意的**
- 唯一值得端的:`manual-cancel-notice-read.ts:121` 的 `null` 被拿去當稽核 `before`
  ⇒ 註解說「我沒看到那一列」而存進去的 JSON 說「沒有那一列」
- 🔴 已量到我這把尺自己會多報(`vitest-ran-gate.sh:112` 是假陽性)⇒ **25/14 不是派工單**

**下一件**:等主視窗派。手上沒有卡住的東西。

---

## 2026-09-08 · 新制後第一批(P2-3 + P2-2 + 跨線發現)

**commit(未推)**
- `f261414d4` 補量曝光期 —— 三顆都在 origin/main
- `6683020e9` 「一半錯誤一半讀數」trap + plan 訂正 production 是 dev
- `cca93458a` P2-3 兩份 plan(A 刪掉 / B 補版控)+ plan B 跨線補格

**🔴 跨線發現(已報主視窗, 未擴散)**
`20260902210000:301-305` 的守門宣告「pcm_readonly 不該帶 BYPASSRLS」,
而正式庫的它 `rolbypassrls = t`(auth 量、tidy 覆量, 我沒有複量)。
該支標 `pcm:never-apply` ⇒ 正式庫不會跑到那個 EXCEPTION。
⇒ 重放庫的 pcm_readonly(NOLOGIN, 無 BYPASSRLS)與正式庫的**不是同一個東西**
⇒ 重放庫上用它驗的 RLS 結論不轉移, 而錯的方向是**重放庫看起來比較安全**。
⚠️ 我沒查有沒有人真的這樣驗過。建議讓 `-db`(跑重放庫)與 `-auth` 看到,
   已問主視窗要我直接寄還是他併進 P1-5(那碰到 tidy 交件的射程)。

**P2-2 產物(不需落板, 是既有列的前置閱讀)**
`~/pcm-mailbox/範本筆記-P2-2-讀完1920行RPC轉接器-account-20260908.md`(115 行)
- 檔:`packages/adapters/src/payment/PgAnomalyAlertReaderAdapter.ts` 實測 1920 行
- 核心:`to_regprocedure(...) IS NULL` 分開「還沒 apply」與「函式體壞了」——
  兩個世界 SQLSTATE 都是 42883
- 三條非照抄不可的紀律:RPC 名寫字面字串(禁樣板, 否則契約測試抽不到而全綠)/
  catch 落「查不到」不是「沒有事故」/ 每支各自 try-catch
- 🔴 我初稿寫「十三支」是目測, 當場量到 **18 支** / `to_regprocedure` **27** 發 ⇒ 已訂正並留痕

**領工前的查重(新制照做)**
P1-4 · P1-5 tidy 做完了、P1-1 refund 做完了 ⇒ 都沒重工。
🔴 而佇列的「誰」欄**是空的** ⇒ 那三列看起來像沒人做。**空白與沒人做印同一個東西。**

**下一件**:佇列 P1/P2 只剩 P2-2(已做完)。等主視窗回那個跨線題, 或自己照 R7 找。
