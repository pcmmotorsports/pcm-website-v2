# ⛔ 這不是 handoff 正本 —— 正本在 `~/pcm-mailbox/handoff-account-20260907.md`

🔴 這支檔是 2026-09-07 03:0x 我**誤建**的(該追加的是 mailbox 那一支)。
內容已**逐字**搬進正本, 這裡不留第二份全文 —— 兩份會分岔, 而分岔的時候沒有人會知道哪一份是對的。

⇒ **要讀 handoff 去** `~/pcm-mailbox/handoff-account-20260907.md`。

## ⚠️ 2026-09-07 22:0x —— 我又在這裡寫了三段, 已搬走

21:2x / 21:5x / 22:0x 三次存檔誤追加在本檔, **已逐字搬進正本並在那裡標明成因**。
🔴 **本檔【第二次】被當成正本寫了**(03:0x 一次、21:2x-22:0x 一次)⇒ 📌 **檔頭那句警告攔不住我, 兩次都沒有。**
⇒ 🔵 **要真的攔住, 得是【機制】不是【文字】** —— 而那超出本檔範圍, 記在這裡給下一個人。

---

## 2026-09-08 · zsh 坑補正本 + P1-2 全 repo 掃(存檔點)

**已 commit(未推)**
- `9be417d34` docs(patterns): 補 zsh `$fn[^;]` 下標坑 —— 正本 `docs/patterns/zsh-and-bash-traps.md` 檔尾
- `860379f81` docs(patterns): 把「負對照對這族天生失明」提到節首(主視窗訂正)

**已發出**
- 排序檢查結果 → 主視窗 `pcm-website-v2-1a`(57 支分母、0 支先給後收、尺雙向驗過)
- 兩個尺失效實例 → `refund` `pcm-website-v2-37`(併進他那一節、不另開)
- P1-3 報告檔頭補「分母 = 相對 `payment_confirmer` 這一個角色」

**P1-2 產物**
`~/pcm-mailbox/盤點-P1-2-兩個世界印同一個東西-account-20260908.md`(136 行)
- 形狀命中:shell 111 / TS 34 / SQL 0(SQL 那個 0 已用構造檔做過正對照)
- 抽開 10 格 ⇒ **9 格檔案自己已經寫明是刻意的**
- 唯一值得端的:`manual-cancel-notice-read.ts:121` 的 `null` 被拿去當稽核 `before`
  ⇒ 註解說「我沒看到那一列」而存進去的 JSON 說「沒有那一列」
- 🔴 已量到我這把尺自己會多報(`vitest-ran-gate.sh:112` 是假陽性)⇒ **25/14 不是派工單**

**下一件**:等主視窗派。手上沒有卡住的東西。

---

## 2026-09-08 · 新制後第一批(P2-3 + P2-2 + 跨線發現)

**commit(未推)**
- `f261414d4` 補量曝光期 —— 三顆都在 origin/main
- `6683020e9` 「一半錯誤一半讀數」trap + plan 訂正 production 是 dev
- `cca93458a` P2-3 兩份 plan(A 刪掉 / B 補版控)+ plan B 跨線補格

**🔴 跨線發現(已報主視窗, 未擴散)**
`20260902210000:301-305` 的守門宣告「pcm_readonly 不該帶 BYPASSRLS」,
而正式庫的它 `rolbypassrls = t`(auth 量、tidy 覆量, 我沒有複量)。
該支標 `pcm:never-apply` ⇒ 正式庫不會跑到那個 EXCEPTION。
⇒ 重放庫的 pcm_readonly(NOLOGIN, 無 BYPASSRLS)與正式庫的**不是同一個東西**
⇒ 重放庫上用它驗的 RLS 結論不轉移, 而錯的方向是**重放庫看起來比較安全**。
⚠️ 我沒查有沒有人真的這樣驗過。建議讓 `-db`(跑重放庫)與 `-auth` 看到,
   已問主視窗要我直接寄還是他併進 P1-5(那碰到 tidy 交件的射程)。

**P2-2 產物(不需落板, 是既有列的前置閱讀)**
`~/pcm-mailbox/範本筆記-P2-2-讀完1920行RPC轉接器-account-20260908.md`(115 行)
- 檔:`packages/adapters/src/payment/PgAnomalyAlertReaderAdapter.ts` 實測 1920 行
- 核心:`to_regprocedure(...) IS NULL` 分開「還沒 apply」與「函式體壞了」——
  兩個世界 SQLSTATE 都是 42883
- 三條非照抄不可的紀律:RPC 名寫字面字串(禁樣板, 否則契約測試抽不到而全綠)/
  catch 落「查不到」不是「沒有事故」/ 每支各自 try-catch
- 🔴 我初稿寫「十三支」是目測, 當場量到 **18 支** / `to_regprocedure` **27** 發 ⇒ 已訂正並留痕

**領工前的查重(新制照做)**
P1-4 · P1-5 tidy 做完了、P1-1 refund 做完了 ⇒ 都沒重工。
🔴 而佇列的「誰」欄**是空的** ⇒ 那三列看起來像沒人做。**空白與沒人做印同一個東西。**

**下一件**:佇列 P1/P2 只剩 P2-2(已做完)。等主視窗回那個跨線題, 或自己照 R7 找。

---

## 2026-09-08 · 第二批(traps 增量 · 兩張落板文字 · mail 交辦掃描)

**commit(未推)**
- `7f1524696` `scripts/pcm-audit-ro-snapshot.sql`(唯讀快照)+ plan 推翻 net 那格
- `c80fa2b11` traps 未確認標籤那條補第 4 動作 + plan 關掉 database 缺口

**🔴 我今天最貴的一次錯**
`has_schema_privilege(pcm_audit_ro,'net','USAGE') ⇒ t` 我讀成「刻意給這個帳號」,
往下推出「刪掉會少一個視角」的成本。`-tidy` 讀 nspacl 原文推翻:
那條 grantee 欄是空的 = **PUBLIC**, 五個角色橫向全 t。
⇒ 🔴 **has_*_privilege 對「專門授的」與「PUBLIC 順便的」印同一個 `t`。**
⇒ 📌 我整天在掃「兩個世界印同一個 **0**」—— **同一族換一個值就過去了。**
⇒ 修法 `scripts/pcm-audit-ro-snapshot.sql` §③-b(讀 `aclexplode`, grantee=0 就是 PUBLIC)。
🛑 而更難看的:我**自己寫了**「我沒量還有沒有別的角色站得到同一個位置」——
   標中缺口然後沒補就往下推。⇒ traps 補第 4 條(標了未確認的格子不得支撐可執行的結論)。

**寄出(窗對窗, 沒繞主視窗)**
- `ship`:新列 `⟦b9-UNTRACKEDGRANT⟧` 落板文字(96 行)—— 版控外的角色 + 版控外的 GRANT
- `refund`:第九個實例的**編號與落點**(tidy 寄全文, 我不重寫第二版)
- `mail`:account 線的甲/乙/丙掃描(109 行)

**mail 那件帶回兩個新機制**
- **丙-②(還在, 而看不到)**:`settings/audit/page.tsx:79 LIMIT = 50` 的滾動窗
  ⇒ 資料在庫裡而唯一在看的畫面把它擠掉 ⇒ 查 DB 的人會說「資料有啊」
- **丁(看得到, 而它是錯的)**:擋門拒絕登記 ⇒ 使用者改一個數字記
  🛑 兩個各只有 1 例, 而兩個都是那一列自己寫的 ⇒ **不是分佈**

**下一件**:自己領。P1/P2 已空, 照 R7 找。

---

## 2026-09-08 · 第三批(has_*_privilege 的 PUBLIC 歧義)

**產物** `~/pcm-mailbox/盤點-has-privilege的PUBLIC歧義-account-20260908.md`(183 行)

**讀數**
- 全 repo `has_*_privilege` ⇒ 2203 處;剝行註解後**會跑的 1506 處 / 323 支檔**(註解佔 19%)
  🔴 `-refund` 原本的「至少 5 支」是它自己那發帶了 `head -10` 造成的(它已自查並回報)
- 按【敘述】判(795 句), 兩個獨立旗標:
  🔴 只有 B(期望 t, 沒有任何一格問低權角色)⇒ **237** ← 最會被 PUBLIC 騙
  ⚪ 只有 A ⇒ 234 · 🔵 兩者都有 ⇒ **76** · ⬜ 都不是 ⇒ 248

**🔴 這一批我錯了兩次, 兩次都值得記**
1. 第一版**逐行**判 ⇒ 5/8。成因:這個 repo 寫 `IF [NOT] has_… THEN RAISE`,
   方向寫在 `IF`/`NOT` 上而我的 regex 找 `= true`/`= false`
   ⇒ **我在做「尺看不到一種合法寫法」那一節的工作時踩了它。**
2. 改按敘述 ⇒ 7/8, 而**錯的那一個是我的 ground truth**:
   `20260630120000:315` 那句**同時是 A 也是 B**(`NOT has_(authenticated)` + `has_(anon)`)
   ⇒ 🛑 **A/B 是我發明的二分法而它不是分割** ⇒ 改兩個獨立旗標
   ⇒ 📌 **一份手讀的 ground truth 只有【我讀的時候想到的那些維度】** —— 同一個病換到人身上

**🔵 而那 76 句同時給出修法**:它們自帶對照(同一句既問該有的也問不該有的)
⇒ 一個 PUBLIC 造成的權限會同時讓 B 那半印綠、A 那半印紅
⇒ **修法不是改 `has_*_privilege`, 是在同一句加一格問低權角色。範例已在 repo 裡。**

**🛑 237 不是 237 個缺陷** —— 要連正式庫讀 `proacl`/`relacl` 才知道哪些真的來自 PUBLIC。
⇒ 尺篩的 N 不是派工單。

**寄出**:`refund`(全份)· `mail`(分類改短名 + 撞名成因是「代號由位置決定」)

---

## 2026-09-08 · 第四批(唯讀量測 · 三列的「那一天」)

**🔵 能力更新(全艦隊已廣播)**:`bash scripts/readonly-prod-sql.sh <x.sql>` **從施工窗跑得動**
—— 它自己 `cd` 回主樹 source `.env.local`。
🔴 **我今天在三份產物寫過「施工窗沒有連線 ⇒ 證不到」** ⇒ 📌 **「我沒有連線」與「我沒試過」印同一句話。**

**量到的(全部正式庫唯讀, 零改動;分母都很小 ⇒ 訂單總數 4)**
| 列 | 問的 | 答案 |
|---|---|---|
| `⟦b4-AUDITNULLAMBIG⟧` | 稽核裡有幾筆模糊 `before` | **0**(那兩個 action 從沒跑過;21 個相異 action 逐個列出當正對照) |
| `⟦b4-NCPCRONRACE⟧` | 它在等的那一天到了沒 | **沒到**(pending_refunds 0 · 已取消且有收款 0;而已取消 1、有收款 2 ⇒ 交集為 0 有判別力) |
| `⟦b4-BANKDUPPERCART⟧` | 索引複量 + 結果面 | 索引 `唯一=f` ✅ 對上(17 支索引 5 唯一 ⇒ 尺分得開)· 重複 cart **0**(3 cart 對 3 單) |
| `⟦b4-TAPPAYDIRECT⟧` | 有沒有指紋 | **0**, 而 🔴 **那是代理指標不是觀測** —— 真正那條路造成的是「兩邊都靜悄悄」, 本份抓不到 |

**🔴 而最重要的一次自我更正**
我早上寫上板(ship 已貼)的「這一列**已經在寫**分不出來的資料」是**推出來的** ——
我從「三顆 commit 已部署」推出「它已經在寫」。🛑 **部署了 ≠ 跑過。**
📌 形狀:**我寫的是完成式的動詞, 而我手上只有它的前置條件。** 而這次**我根本不覺得那是推論**。

**🛑 四份共同的限定(每一份都寫在讀數旁邊)**
① 都是今天的讀數, 而**過期那一刻沒有訊號** ② 訂單總數 4 ⇒ **這些 0 說的是【系統還沒被真的用過】**
③ 述詞/表名是我選的 ④ 態一律不動(Sean Q57 甲歸 ship)

⏭ **下一件**:`⟦account-SETTLEVOIDEDPRED⟧`(板 :648, parked 等 #76)—— 去看它 parked 的理由過期了沒。
