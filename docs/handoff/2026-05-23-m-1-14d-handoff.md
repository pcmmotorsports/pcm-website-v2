# M-1-14d 交接 Handoff — packages/adapters Supabase adapter(PRD §7)

> **產出:** 2026-05-23 / Claude Code(M-1-14c 收尾後)
> **給誰:** 接手 M-1-14d 的新 Claude Code session
> **工作流:** Claude Code 自驅 SOP(CLAUDE.md L515「Claude Code 自驅 slice SOP」9 步)、不經 Cowork。對抗審查用 Codex(`codex-adversary` skill)+ Claude `code-reviewer`。決策一律白話 prose multi-select 問 Sean(memory `feedback_decision-delivery-prose` + `feedback_decision-questions-plain-language`)。
> **M-1-14d 是重大 / 接 DB slice** → 鐵則 8 先給 Sean 批 plan;`codex-adversary` 雙關卡(關卡1 審 plan / 關卡2 審 diff)+ code-reviewer 都要跑(**不可跳**,與 M-1-14c 低風險不同)。

---

## 0. 新 session 起手(照 CLAUDE.md 第一天清單)

```bash
cd /Users/sean_1/pcm-website-v2
git branch --show-current      # 預期 dev
git status                     # 預期 clean(Sean 已 push)
git log --oneline -5
```

讀順序:**STATUS.md → 本 handoff → PRD `docs/specs/m-1-14-customer-schema.md` §7(adapter skeleton)→ M-1-14b ports(`packages/ports/src`)→ M-1-14a DB(`supabase/migrations/20260523034911_*.sql`)**。

---

## 1. 上一段 M-1-14c 結果 ✅(已 push origin/dev)

- commit **`3e67016`** `feat(schemas): M-1-14c 填 6 組 zod v4 表單驗證 schema + z.infer`。
- `packages/schemas/src/index.ts`:6 組 zod **v4** schema(Login / Register / Address / Vehicle / Profile / Deposit)+ 同名 `z.infer` type。
- zod 入 **catalog**(`pnpm-workspace.yaml` + root devDep + `@pcm/schemas` dep 全 `catalog:`)。
- 三綠 typecheck 7/7 + lint 10/10 + build 1/1 storefront、code-reviewer **PASS 0 must-fix**。

---

## 2. 本 session Sean 拍板(別重做 / 別推翻)

- **Q1=B**:STATUS「最近 3 commit」頂端 hash 是 busboy off-by-one orphan、**維持現狀別修**
  (memory `project_status-top-hash-off-by-one-normal`、單 slice 穩態可容忍;下次 commit 自然以可達 hash 重寫即可、別 sed+amend 迴圈修)。
- **Q2=B**:`/pcm-roadmap` 進度地圖**等 M-1-14 整區(d~h)蓋完再一次刷新**、不逐 micro-slice 更新。
- **zod = v4**(4.4.3、Sean 早先 Q=A)。實查 `node_modules/zod/v4/classic/schemas.d.ts`:`superRefine` 在 4.4.3 **未** 標 `@deprecated`(僅 `.email()` 等方法被標),故 M-1-14c 沿用 `superRefine`、非 `.check()`(舊 m-1-14c handoff §2 表格此點寫錯、已在 commit/STATUS/index.ts 註明)。

---

## 3. M-1-14d 任務目標 + 前後鏈位置

把 M-1-14b 定義的 4 個 port 介面用 supabase-js 實作出來,接上 M-1-14a 的 4 張表。

```
M-1-14c ✅ schemas(input 驗證)
      │
M-1-14e use-case ────呼叫 port
      │
M-1-14b ✅ ports(ICustomerRepository / IAddress / IVehicle / IWalletRepository)
      │  ← M-1-14d ★本段:用 supabase-js 實作這些 port
M-1-14d Supabase adapter + mapper(camelCase domain ↔ snake_case DB)
      │
M-1-14a ✅ Supabase DB(customers / customer_addresses / customer_vehicles / customer_wallet_ledger + RLS + CHECK + trigger)
```

**產出檔(對齊 PRD §7 skeleton):** `packages/adapters/src/supabase/` 下
- `SupabaseCustomerAdapter`(implements `ICustomerRepository`:findById / findByEmail / update)
- `SupabaseAddressAdapter` / `SupabaseVehicleAdapter` / `SupabaseWalletAdapter`(3 子 repo)
- mapper(domain camelCase ↔ DB snake_case;PRD §7 已給 `CUSTOMER_SELECT` 等欄位字串)

---

## 4. 落地要點 / 鐵則

- **鐵則 8(重大改動)**:跨多檔 + 動共用 package + 接 DB → **先給 Sean 批 plan**(要改什麼 / 為什麼 / 影響面 / rollback)再動手。
- **codex-adversary 雙關卡(不可跳)**:關卡1 `codex exec -s read-only` 審 plan(vs PRD §7 + ports + DB schema)→ 自修 ≤2 輪 → 決策岔路一次性批次 prose multi-select 問 Sean;關卡2 審 diff。完命中鐵則 12(接 DB / 動 adapter)。
- **mapper 紀律**:camelCase ↔ snake_case 對照要逐欄對齊 M-1-14a 實際欄名(`user_id` / `wallet_balance` / `total_deposit` / `created_at` 等),**grep migration 不憑記憶**。
- **敏感欄**:tier / wallet_balance 等讀回 domain 可以,但**寫入路徑**受 M-1-14a column GRANT + RLS 限制(adapter 不該嘗試寫 client 寫不到的欄)。對齊 server 端重新驗證鐵則。
- **金額**:wallet ledger amount 整數(分/角或 Decimal、禁 number 浮點)。
- **PGRST116**(supabase-js 查無資料的 code)→ findById/findByEmail 回 null,別當 error throw。PRD §7 skeleton 已示範。
- **不擴張**:M-1-14d 只做 adapter + mapper、不碰 use-case(e)/ storefront(f/g)。
- **三綠 + 字面 vs 事實**:動 `.ts` → build 跑(只 storefront target、type-only package 無 build script);commit 訊息對齊實際(實作幾個 adapter、mapper 對了幾欄)。
- **smoke test**:adapter 屬 infra 層、看 `docs/architecture/testing-strategy.md` 判是否補(mapper 純函式易測)。

---

## 5. 後段排程(停等式逐段、Sean 逐段推)

e(use-case register / login / CRUD / deposit、server 端用 `@pcm/schemas` re-parse)/ f1(Login·Register·Google OAuth)/ f2(LINE OAuth、Sean 須先做 PRD §13 dashboard checklist)/ g(AccountPage 7 tab 拆檔)/ h(MobileTabBar #158)。M-1-14 整區(d~h)收完 → 跑 `/pcm-roadmap`(Q2=B)。

---

## 6. 一句話交棒

> M-1-14d = 把 M-1-14b 的 4 個 port 用 supabase-js 實作、接 M-1-14a 的 4 張表,中間 mapper 做 camelCase↔snake_case;**重大/接 DB slice** → 鐵則 8 先批 plan、codex-adversary 雙關卡 + code-reviewer 都跑、不可跳;不碰 use-case/storefront、不 push。

— END —
