# 2f 交接檔 ②(P 十代 → P 十一代)—— diff 層 R1 **第二次 FAIL**,12 MF + 18 IMP + 3 nit

> **你的第一讀物就是本檔。** 讀完再動工。
> 交接時點:2026-08-12 深夜,P 十代 session `0b9bb727` 收窗(主視窗 `P-599-A` 裁 A 換窗)。
> **工作區五檔一律 untracked、未 commit、未 apply、未 push。**
> 前一份交接檔 `docs/handoff/2026-08-12-2f-r1-fold-handover.md` **仍然有效**(釘值/拍板/§5 不重做清單在那裡),
> 本檔只講**這一輪之後**的新事實。

---

## 0. 一句話:為什麼換窗

**上一輪 33 條 findings 裡,有一半是「我上一輪的修法自己製造的」。**
我在 plan §18-0 逐字寫下判準——「加一個否定詞 / 換個寫法 / 多一份會怎樣」——
**然後在同一輪的修法裡三個都沒真的問完**。病不是不熟,是**在自己的框架裡看不見自己的盲點**。
主視窗裁 A 的主要依據就是這句(`P-599-A` §裁決)。

⇒ 所以本檔的核心交付物**不是敘述,是下面那張表**。

---

## 1. 🔴 第一個機械動作(動工前先做,不要跳過)

**改任何一道判準之前,先 grep 全樹找它的所有鏡像。**

實錘:我修了 migration 的後置②(鎖鍵等價),**沒回頭修 harness 裡同一判準的鏡像**
(`scripts/l5b2-2f-verify.sh` 的 B4 格),codex 直接抓出來。這是 memory
`feedback_folding-a-finding-defaults-to-the-named-spot-only`,**當天第二次**。

```
grep -rn "substr\\\\(pg_catalog" scripts/ supabase/ docs/
grep -rn "event_type" scripts/ supabase/migrations/20260812170000_*.sql
grep -rn "'result'," scripts/ supabase/migrations/20260812170000_*.sql
```
判準至少有三份鏡像:migration 後置斷言 / harness 的 B、E 段格 / plan §7-9 表。**三份一起改。**

---

## 2. 🔴 繞過測試表(**交付物**;每改一道守門就回頭更新對應列)

> 四欄 = §18-0 的判準。**逐格填答案,不是寫「已考慮」。填不出來的格寫「未驗」,不要留空當通過。**
> `破` = codex 已實際指出可繞過(附件有逐字);`擋` = 有實測負測背書(靶號在括號)。
>
> 🟢 **P 十一代已逐列回填,並在 diff 層 R2 之後 = v8 現況全表重驗。** 標記說明:
> **`已擋`** = 有**實測負測背書**(靶號在括號,`neg`/`mut` 逐格);
> **`擋`** = 更早的輪次就有背書、之後未動;
> **`未驗`** = 沒有負測背書,**理由寫在同一格**;沒有任何一格是空的、也沒有把未驗寫成已擋。
> 🔴 **R2 只抽驗 5-8 列就抓到兩列填錯**(§2-3 回退後置、§2-2 ①c)——**表填錯比沒有表更危險**,
> 因為後人會照它認定某一面已守。兩列的共同病:**把「有一個負測存在」讀成「這一面被守住」**,
> 而那個負測驗的是隔壁那一面。⇒ v8 不只改被點名的兩格,是**整張逐列重走**。
> 逐條形狀 = plan **§7-12**(那張才是實作現況);結構斷言與行為靶的分工見 plan **§7-11**。

### 2-1 前置閘

> 🟢 **v8 全表逐列重驗**(R2 抽驗 5-8 列就抓到兩列填錯 ⇒ 這一輪不只改被點名的格,是整張重走一遍)。
> 判準:一格只能寫「已擋」如果**指得出是哪一格在驗哪一面**;指不出來就寫「未驗」並寫理由。

| 守門 | ①加否定詞 | ②換寫法 | ③多一份 | ④改大小寫 |
|---|---|---|---|---|
| P1 2e prosrc md5 | n/a(md5 相等) | n/a | **已擋(N6b)**:P1-0 先證 `close_released_attempt` 恰一顆 | n/a |
| P1b 2e 屬性 | **已擋(N6c)**:`lock_timeout=0`(無限等)當場紅;v7 只有「被拿掉」那一支(N6)有測,**當時把一支讀成整道** | **已擋(N6/N6c)**:比的是元素值 | **已擋**:`cardinality=2`,多一個 SET 就紅(無專屬負測,由 N6/N6c 的形狀共證) | **未驗**:GUC 名應由 PG 正規化成小寫,**沒實測** |
| P1c 2e ACL | n/a | **n/a(刻意不加)**:非 owner 一律不得有 EXECUTE ⇒ 可轉授被**嚴格蘊含**,加了是紅不了的死規則 | 擋(集合比對,N7) | n/a |
| P2 pre-image md5 | n/a | n/a | 擋(有 identity_arguments 限定;M13) | n/a |
| P3 屬性逐欄 | n/a | n/a | 擋(N8) | n/a |
| P3b ② ACL 集合 | n/a | **已擋(N9b)**:可轉授的 EXECUTE 當場紅;v7 只有形狀、無負測 | 擋(N9) | n/a |
| P4 viewdef md5 | n/a | 擋(PG 正規化吸收寫法差異;M10 餵錯值) | n/a | n/a;⚠️ 跨版本可能**誤擋** |
| P4b security_invoker | 擋(N10) | **已擋(N10b 反向格)**:`= on` 等合法等價寫法**實測不再被誤擋** | **n/a**:reloptions 同鍵不可重複(**未另實測**) | 已擋(值先 `lower()`) |
| P5 / P5b | n/a | n/a | **已擋(N11b)**:函式 owner 換成無 BYPASSRLS 的角色當場紅。🔴 v7 寫的「本機恆綠、負測紅不了」是**假的誠實缺口**(R2 命中) | n/a |
| P6 值域 | **已擋(N12b 結構 + N12d 行為)**:`<>` 由 P6-op 擋;`NOT (...)` **結構層擋不住**(R2 的繞法)⇒ 由**行為探針**擋 | **未驗(已認邊界)**:等價寫法是開放集合,結構層不再加碼(換路裁決);語意面由 N12c/N12d 一視同仁 | 擋(N12) | **未驗**:小寫 `any` 會落進 P6-op 的 fail-closed 方向,未實測 |
| P6b 狀態機 trigger | n/a | **已擋**:四支子述詞各有負測(N3/N3b/N3c/N3d)+ 反向格 N3e(ALWAYS 不得誤擋)+ **行為探針 N2b** | 擋(tgrelid+tgname 唯一) | n/a |
| P6c 沿 tgfoid | n/a | n/a | 擋(N2) | n/a |
| P7 索引 | 擋(N1) | ⚠️ 跨版本/等價寫法可能**誤擋**(fail-closed,已裁定接受;檔內寫明症狀與值班動作)。三面各有負測 N1/N1b/N1c + **行為探針 N1d** | n/a | n/a |
| P8 COMMENT md5 | n/a | n/a | 擋(M14 餵錯值) | n/a |

### 2-2 後置斷言

| 守門 | ①加否定詞 | ②換寫法 | ③多一份 | ④改大小寫 |
|---|---|---|---|---|
| ①a advisory<列鎖 | n/a | 擋:位置比較不受空白影響 | 由②a 管 | 已擋(M21:大寫寫法讓 i_adv=0 ⇒ ①a 當場紅) |
| ①a2 advisory>步 2 | n/a | 未驗 | n/a | **已擋(M23)**:🔴 v7 寫「sed 搬不動整段」是**假的誠實缺口** —— 不必搬,**插入**一顆 advisory 就破得掉(R2 命中) |
| ①b / ①b2 | **已擋(M18)**:①e 三條述詞的 `NOT EXISTS` 包裝計數 | 未驗(位置錨對換寫法本來就不敏感) | 擋(①e 計數) | 未驗 |
| ①c 鎖強度 | n/a | **已擋(M17 整串替換 + M25 雙空格)**。🔴 v7 這格填「擋(M17)」是**錯的**:M17 驗的是整串被換掉、由存在性擋下,**空白變體當時全盲**(R2 命中);v8 改走 `upper(v_norm)` 才真的吸收空白 | 擋(M22/M25) | 已擋(M22:小寫 `for update`) |
| ①d advisory 種類 | n/a | 擋(M1) | 擋 | 已擋(M21;掃描 `'gi'`) |
| ② 鍵等價 | n/a | 已擋(M2/M11;白名單正規化)+ **B4b 對同一顆 order_id 實際求值比 bigint** | 擋(②a) | 擋(大寫先被 ①a/①d 擋,M21 實測) |
| ②a 呼叫恰一次 | n/a | 擋(M12) | 擋(M12) | 已擋(計數 `'gi'`) |
| ③ 屬性保留 | n/a | n/a | 擋(M5) | n/a |
| ④ proconfig | n/a | 擋(整串相等) | 已擋(整串相等;M3/M4) | 擋(整串相等) |
| ⑤ COMMENT 前綴 | n/a | 擋(M16;`starts_with`) | n/a | n/a |
| ⑤b 合約句在位 | **未驗(結構上不可機械證)**:自由文字,後面再加一句否定前面的擋不住;只把字面拉長成合約句,並在檔內寫明「這只是字面在位」 | 未驗 | 未驗 | 未驗 |
| ⑥ carve-out | 已擋(M19) | 擋:`IN (...)`/換別名都讓合格數 ≠ 總數 | 擋(計數) | 擋(大寫讓總數 ≠ 1) |
| ⑦ 回傳碼閉集 | n/a | 已擋(M20 `concat` 組鍵 ⇒ 絕對數 10 擋下) | 擋(計數;M15) | 擋(大寫讓絕對數 ≠ 10) |
| ⑧ 在途觀察 | 未驗 | 未驗 | 未驗 | 未驗 —— 這道是**觀察 + fail-closed 停點**,失效方式是「整道被刪掉」,由審查與 §7-12 對帳擋,不由靶擋 |

### 2-3 回退腳本

| 守門 | ①加否定詞 | ②換寫法 | ③多一份 | ④改大小寫 |
|---|---|---|---|---|
| 閘① 2g 偵測 | n/a | **已擋(N7c)**:v8 把剝註解順序**反過來**(先 `--` 後 `/* */`)+ **另掃未剝除原文**,兩者不一致就停下讓人判。🔴 v7 的順序是 **fail-open**:`--` 註解或字串含 `/*` 會讓區塊剝除吃掉真正的 INSERT(R2 命中);動詞另擴到 `UPDATE`/`COPY` | 顯式名單仍為空(2g 未落地) | 已 `~*` 大小寫不敏感 |
| 閘② 三態 prosrc | n/a | n/a | n/a | n/a |
| 閘②b(兩態) | n/a | 擋(N4) | 擋(N4b proconfig / N4c 七屬性) | n/a |
| 閘②c 核准出口 | n/a | **已擋(N5c)**:v8 綁**身分指紋**不綁筆數 —— 🔴 v7 綁 count 之下「一筆走終局、一筆新在途」count 不變就放行(R2 命中,動錢不可逆);另擋 `ALTER ROLE/DATABASE` 常駐的核准值 | 擋(指紋含全部 id) | n/a |
| 閘②c 留痕 | n/a | n/a | n/a | **已擋(N5d-audit)**:v8 加寫 `admin_audit_log` 一列(包 EXCEPTION、寫失敗不擋回退)。第一版 `source_app='script'` 撞 CHECK 被**吞成 WARNING**,就是這格抓到的 |
| ②c-recheck | n/a | n/a | n/a | ⚠️ **未配競態負測**(要雙連線構造);窗口**縮小不是關閉**。v9 另加:view 缺席時**跳過並大聲說**,不再 42P01 整筆 abort |
| 🆕 ②c 連線拓樸(v9) | n/a | **已寫成前提**:核准出口靠 session GUC ⇒ **必須直連 5432**;走 transaction pooler(6543)時 `-c` 與 `-f` 可能落在不同 backend ⇒ **指紋帶對也永遠被擋**。檔頭 + ②c 失敗訊息 + plan §9-10 三處都寫了 | n/a | ⚠️ **未配負測**:要真的架一座 pooler;症狀已可從訊息當場判讀(「這是拓樸問題不是你抄錯」) |
| 🆕 ②c view 缺席(v9) | n/a | **已擋(N13a/N13b)**:`to_regclass` 先測 ⇒ 缺席時擋在具名閘並給 `view-absent` 核准出口。🔴 舊寫法在**2f 最壞事故形態**(view 被 drop)那天,回退自己 42P01 = D1 前科同型 | n/a | n/a |
| 🆕 閘②b 第三態出口(v9) | n/a | **訊息已寫下一步**:把旁支 ALTER 回 post-image 字面再重跑。🔴 製造第三態最可能的人**就是值班自己**(凌晨第一手緩解 = 把 lock_timeout 調小)⇒ 舊訊息只有「先查清楚誰改的」= 不對稱壓在自救過的人身上 | n/a | ⚠️ 刻意**不給核准出口**(理由寫在訊息裡) |
| 回退後置 | n/a | **已擋(N7b)** | **已擋(N7b + N4c)**:🔴 v7 這兩格填「擋 / 已擋」是**錯的** —— 當時 proconfig 是**子字串比對**,`SET row_security = off` 四面全綠(R2 命中);而 N4b/N4c 驗的是還原**前**的閘②b,不是還原**後** | 未驗 |

### 2-4 harness 自己

| 判準 | 問題 | 處置 |
|---|---|---|
| B4 鍵等價鏡像 | v6 是舊的片段比對 | v7 改成與 migration ② 同形狀 ➕ **B4b** 實際求值比 bigint |
| E1/E2 枚舉式 regex | 與 migration ⑥⑦ 同病 | v7 改計數相等 + 絕對數 + E3(NOT EXISTS 包裝) |
| 差集判準只要求「包含」 | 突變多打壞別的行為仍算通過 | v7 改**集合相等**(M8 當場現形:它一直多紅 C6) |
| M0 沒斷言 `BASE_RED` | 基線壞掉會被扣進每個靶 | v7 釘 `BASE_RED` 恰為 `[0-1]` |
| `rm -rf` 邊界 | 誤傳 `/tmp/` 清整個暫存區 | v7 workdir 由本檔自產、唯一入口 `safe_rm_workdir()` |
| `/tmp` 固定報告檔名 | 多窗互相覆寫 | v7 檔名帶 PORT |
| lock_timeout 只有 catalog 檢查 | 「設定寫著 10s」≠「真的等 10 秒」 | v7 加 **L2** 實測(本輪 = 10 秒) |
| 🆕 靶清單欄位分隔 | sed 含 `\|` 會讓四欄整排錯位、報告印出 18 格全紅 | v7 改 `concat()` + `kind` 合法值自檢 |
| 🆕 行為探針量錯東西 | 既有 trigger 排在 CHECK 之前 ⇒ 釘死 23514 會量到 trigger | v8 加 `probe_blocked_any`(只斷言被擋、印出實際攔截碼) |
| 🆕 探針從訊息文字撈 SQLSTATE | 本機 psql 是**繁中語系**,`SQLSTATE` 字面根本不出現 ⇒ 五格全被判成「沒被擋」 | v8 改由 DB 自己吐固定字串(與語系無關) |
| 🆕 **L2 是同族第四例**(R3 加題答案) | v8 只修了 `probe_*` 家族,**L2 還在 `grep -qi 'lock timeout'` 撈英文訊息** —— 它在這台機器上會過是**運氣不是設計** | v9 改判 `SQLSTATE=55P03`。教訓:**修一族的時候要問「這族還有誰」** |
| 🆕 **N13 放錯位置 = 同族第五例** | 放在 neg 前段(現況 pre-image)⇒ 閘② 直接 RETURN、走不到 ②c ⇒ 兩格都印「守門沒有判別力」 | v9 移到 mode_neg 尾端並先套 2f。**量具擺錯位置,長得跟被測物失效一模一樣** |
| 🆕 在途述詞九份手抄、零等價釘 | 2g 上線時定義必被重新談判,那時沒有任何東西提醒改的人還有八份 | v9 加 **B5**(九份逐字等價)+ **B5b**(份數 ≥5,防抽取式子過期讓 B5 恆綠) |

## 3. 主視窗已裁的處置(`P-599-A`;**不是建議,是裁決**)

### 3-1 第一優先:`verify.sh:84` 的 `rm -rf` 邊界
這 33 條裡**唯一會傷到「本片以外」**的東西。修法至少要:拒絕 `/tmp` 與 `/tmp/`、
要求路徑深度 ≥2、且**路徑由腳本自己產生而非外部傳入**。

### 3-2 必修且優先於所有 important:`rollback.sql:146` 閘②c
**我加的這道讓災難當天的可用性變差** —— 任一在途列就全域封死回退,
而「2f 自己造成卡單」正是最需要撤掉它的情境 ⇒ **恰好撤不掉**。
要有:先凍結新退款 / 排空 / 或**批准式 override**(明確、留痕、不是預設放行)。
⚠️ **修法要自己寫理由,不得照抄任何既有段落** —— memory
`feedback_new-rationale-written-while-folding-a-finding-is-unverified` 那條,我已經被咬過。

### 3-3 `plan:904` 死結消融 —— **不降驗收,也不本片補做**
- 事實層主視窗已確認:**沒有宣稱不實的事**(§17-3 與檔頭一直逐字寫「未證、只有結構前哨」)。
- 問題在 **§13-4 當初寫得比本片範圍大**(要求實構 40P01 消融)。
- 處置:plan 明確標記「**§13-4 本片未達成**」+ **另立 backlog 條目承接消融實證**
  (號等目前這批 backlog 進 dev 後補);對外宣稱維持「結構前哨、未證消融」。
- 🔴 **新窗不得自行改回、也不得悄悄打勾。** 這是把錯置的驗收項移到對的片,不是降低標準。

### 3-4 P6b / P7
- P6b `tgenabled='A'`:合法更強設定被誤擋 ⇒ **照修**。
- P7 釘重建文字:fail-closed 論證主視窗接受,但**要在檔內寫明**
  「跨版本可能誤擋、症狀 = apply 被擋且訊息會印出實際定義」,讓災難當天的人看得懂。

---

## 4. 這一輪已經做完、**不要重做**的事

- **A1/A2/A3/A4/A5/D 六處形狀重做的方向是對的**,codex 沒有推翻任何一處的**方向**,
  打的是**同一形狀裡殘留的繞過面**(見 §2 的「破」)。不要把它們改回舊形狀。
- `neg` 模式(18 格)與 mut 17 靶 + M0 基線的骨架可用;要補的是 §2-4 那四條判準缺陷。
- 實跑數字(**這一輪的**,檔案 SHA 已重釘、跑的就是現在樹上那份):
  `all` = run 30/30 + rb 7/7 + teardown rc=0、`neg` = 18/18、`mut` = 17 靶+M0 全過、三者 `echo $?` 皆 0。
  三綠:`pnpm typecheck` rc=0 / `pnpm lint` rc=0(無 .ts 改動 ⇒ 不需 build)/ `bash -n` 過。
  ⚠️ **改完之後這些數字全部作廢,要重跑重填。**
  🟢 **v9 已重跑重填(P 十一代,2026-08-13,折完 diff 層 R3 = 最後一輪)**:
  `all` = run **37/37** + rb **7/7** + teardown rc=0、`neg` = **51/51**、`mut` = **25 靶 + M0 全過**,
  三者 `echo $?` 皆 0;三綠 `pnpm typecheck` rc=0 / `pnpm lint` rc=0 / `bash -n` rc=0。
  逐格結果與量法逐字寫在 plan §17 / §17-4。
  ⚠️ 歷代數字留著只為對照,**不要再引用**:v6 = 30/18/17、v7 = 35/31/22、v8 = 35/48/25。

---

## 5. 檔案與座標

> ⚠️ **行數會隨每一輪折疊過期**(v6 那版寫 829/397/780/1251,到 v8 全部不對了,R2 逐字抓到)。
> 引用前重數:`wc -l <檔>`。下表是 **v8 收工當下**的實數。

| 檔 | 行數(v9 實測 `wc -l`) | 位置 |
|---|---|---|
| migration | 1048 | `supabase/migrations/20260812170000_m4b_lifecycle_l5b2_2f_initiate_advisory.sql` |
| rollback | 685 | `scripts/l5b2-2f-rollback.sql` |
| harness | 1324 | `scripts/l5b2-2f-verify.sh` |
| plan v9 | 1661 | `docs/specs/2026-08-12-l5b2-2f-initiate-advisory-plan.md` |
| backlog | +43 行 | `docs/phase-1-backlog.md`(唯一 tracked,狀態 ` M`;`git diff --stat` 實測) |

**findings 逐字(去重 39 行)**:`/Users/sean_1/pcm-mailbox/附件-P-2f-diffR1v6-findings.md`
**codex 全文 5764 行**:`/Users/sean_1/pcm-mailbox/P-2f-diffR1v6-codex-full.txt`
> 🔴 R2 換模型時**要對照全文**看 R1 已經打過哪些面,不能只靠 39 行去重版(主視窗 `P-599-A` 庶務①)。
> 這兩份在 `pcm-mailbox/` = 耐久位置,**不是 session scratchpad**(上一代那份路徑會被清)。

---

## 6. 兩個環境地雷(我這輪各踩一次)

1. **臨時叢集要先 teardown 再 `rm -rf`**。直接刪還在跑的資料目錄 ⇒ postmaster 抱著死檔繼續佔埠,
   下一次 provision 綁不到,錯誤訊息完全看不出真因。埠:`all`/`neg`=54763、`mut`=54771。
2. **不要在 `mut`/`all` 跑動時編輯那三支檔**。子行程 `check_sha` 會當場死、輸出零個 ❌,
   舊判準把它讀成「守門沒有判別力」。守門已補(沒印 `run 小計` 就報「這靶沒有結果」),但別再製造那個情境。

---

## 7. 收尾鏈(未變)

改完 → 三綠 → diff 層 R1 = codex → **主視窗派 R2、換模型** → 五檔 + backlog **同一顆 commit**
→ **不 apply、不 push**(apply = Sean 停點,主視窗執行)。

— P 十代(session `0b9bb727`)
