# CURRENT HANDOFF — pcm-website-v2

> **2026-09-09 減法版(Sean 拍 Q2 甲,試行一週到 09-16)。** 舊版 2,263 行逐字在 `docs/handoff/archive/CURRENT-20260909-pre-cut.md`。
> 本檔由主視窗維護,施工窗把「做到哪 / 卡在哪 / 下一件」交主視窗寫。每個 session 開工只讀這一支。

## 為什麼改
一週 3,114 顆 commit 只有 26% 碰產品;1,143 顆在改待辦板;本週新增 163 支腳本、380 支 memory、9 支閘。四方(主視窗、兩個讀檔幫手、GPT-6)同一結論:產品要批准而制度不用批准、規則又不准窗停 ⇒ 水往量具流。全文 `~/pcm-mailbox/診斷-艦隊為什麼一直做量具-20260909.md`。

## 這週兩條線(Sean 09-09 逐字定義)

### 窗 A · 前台:客人找商品 → 看商品 → 結帳 → 付款 → 收到信
Sean:「前台大致上好了,只差細節。包含商品是否完整呈現、所有搜尋是否正常、快速、方便。」
- **第一件**:首頁「最新商品」區塊點進去「最新上架」頁,顯示的商品不同步(Sean 親眼看到)。找出兩邊各吃哪個資料源,讓它們用同一個。
- 然後:搜尋(正確、快、方便)、商品頁完整呈現、結帳到付款到收信整條走一遍。
- 驗收:Sean 在顧客站從首頁走到收到信,每一步他看到的都對。
- 做到哪:(施工窗填)
- 卡在哪:(施工窗填)

### 窗 B · 後台:員工收單 → 收款 / 退款 → 建單 / 訂貨 / 到貨 → 出貨 → 客人收出貨信
Sean:「包含後台所有功能是否正確營運,收款、退款、建立訂單、商品訂貨、到貨、出貨狀態與文字是否正確。」
- 從一張真的單開始,員工每一步操作一次,狀態與文字每一格對。
- 新竹物流 API 這條到 09-05 為止是零進度(`~/pcm-mailbox/上線還差什麼-20260905.md` §④),它在這條線裡。開送之前讀 `docs/runbooks/hct-first-shipment-activation.md`,按鈕是 Sean 按。
- 驗收:Sean 在後台從收單走到出貨信寄出。
- 做到哪:(施工窗填)
- 卡在哪:(施工窗填)

## 做完就停
兩條線之外的事不做。工具壞了且正擋著這兩條線才修。不寫規則、不加閘、不寫教訓、不改 `docs/launch-todo.md`。

## 等 Sean
```
Q4: 安全審查要留多少?
A: 甲 = 只留一道:錢 / 權限 / schema / 對外發送的 commit 前 codex 一輪,有 must-fix 才第二輪;其他片不審(推薦,已寫進 CLAUDE.md 鐵則 12,你說乙我再改回來)
   乙 = 照舊(每片 code-reviewer + codex 多輪 + 第三輪換模型)
```
另外三支在 `~/.claude` 底下的檔我改不動(分類器擋),要你貼:見 `~/pcm-mailbox/請Sean貼-全域規則減法-20260909.md`。

## 一週後怎麼看有沒有效(09-16)
```bash
git log --since=2026-09-09 --no-merges --format='%H' | while IFS= read -r h; do git show --name-only --format='' "$h" | grep -qE '^(apps|packages|supabase/migrations)/' && echo prod || echo meta; done | sort | uniq -c
```
本週基線 prod 802 / meta 2312。比例升但 Sean 沒有走完任何一條線 = 不算成功。
