# 過夜施工回報(2026-07-29 凌晨 → 清晨)

> Sean 最終批准後從 A0a 起跑,照 `docs/specs/2026-07-28-e10-order-closure-master-plan-v2.md` §5.0 DAG。
> **未 push 9 筆,全部留著等你推。** 工作樹乾淨,三綠全過(typecheck / lint / build),完整 `pnpm test` **3076 passed**。
> 🔴 **禁區全程沒碰**:零 apply、零 `db push`、零 push、零 deploy、沒動 `.env`。

---

## ① 進度表

| # | 片 | 型 | 狀態 | commit | 證據 |
|---|---|---|---|---|---|
| 1 | **A0a** 資料現況重驗 | docs | ✅ | `5baba51` | production 唯讀實查;與 v2 §8.4 **逐格相符**;🔴 命中硬停條件(見 ③) |
| 2 | **A0b** 程式現況重驗 | docs | ✅ | `ff255c4` | v2 §2.2 的 repo 斷言**逐條成立、零更正** |
| 3 | **A0c** 27 項對帳 | docs | ✅ | `9210a79` | 25 項一致、**2 項漂移**;分母更正 2/5/20 |
| 4 | **D0** 編號 expand | M | ✅ 檔已寫、**未 apply** | `f0f176e` | 交易模擬 + 7 條負向探針 + **突變測試證明非假綠** |
| 5 | **A9b1** 單號搜尋合約 | A | ✅ | `31b702b` | 三綠 + 3050 tests + SQL 模擬證明舊號查得到 |
| 6 | **A10c1** 單號搜尋 UI | U | ✅ 掛 flag、**預設 off** | `0ff0b6d` | 三綠 + 3071 tests;🔴 端到端未驗(見 ③) |
| 7 | **A15** TapPay 中止訊號 | A(高風險金流) | ✅ | `7615853` | **雙審 PASS**(codex + Fable)+ 突變測試 |
| 8 | **A2** 採購真相表 | M | ✅ 檔已寫、**未 apply** | `bd8070f` | 交易模擬 + 9 條探針 + 8 CHECK |
| 9 | **A3** 備註/告知義務表 | M | ✅ 檔已寫、**未 apply** | `b444aee` | 交易模擬 + 7 CHECK;連帶修好 A2 的零寬字元洞 |

**第 1 批 69 片中完成 9 片。** 全部照 §5.0 DAG 順序,沒有自創順序。

### 審查實績(全部由我逐條裁定,**零駁回**)

| 片 | 審查 | 結果 |
|---|---|---|
| D0 | codex 關卡2 兩輪 | R1 **FAIL 9 must-fix** → R2 7 關閉/2 部分關閉 + 新 6 條 → 達 2 輪上限停止折衝 |
| A9b1 | code-reviewer(opus) | **FAIL 3 must-fix** + 5 nit,全修 |
| A10c1 | code-reviewer(opus) | **FAIL 3 must-fix** + 6 nit,全修 |
| A15 | codex 關卡2 **+ Fable 深審** | **兩者皆 PASS** |
| A2 | codex R1 + Fable 確認輪 | R1 **FAIL 4 must-fix** → Fable **PASS** + 再抓 4 條 |
| A3 | codex 關卡2 | **FAIL 4 must-fix**,3 條修完、1 條交你決定 |

**審查抓出來、最值得你知道的三件事:**

1. **假綠**:D0 原本用「找子字串」驗 CHECK,regex 被放寬成 `{1,6}` 也照樣全綠。已改成精確定義比對 + 行為探針,並用突變測試證明斷言真的會 fire。
2. **`[[:space:]]` 擋不住零寬字元**(U+200B/FEFF,實測確認)—— 一整串「肉眼全白」的備註會被當成有內容;供應商單號夾一個零寬空格就會讓搜尋漏單。A3 抓到,**回頭把 A2 也修了**。
3. **`AbortSignal.timeout()` 丟的是 `TimeoutError` 不是 `AbortError`** —— 我原本在 port 契約寫死 `AbortError`,退款 worker 若照 `err.name` 分支,自家逾時會被誤判成真失敗、退款狀態機走錯枝。Fable 抓到,契約已改寫。

---

## ② 待你批准 / 待你決定

### 2.1 🔴 `supabase db push` 清單(你親手跑;順序不可換)

```
supabase/migrations/20260729010000_m4b_e10_d0_display_id_expand.sql
supabase/migrations/20260729020000_m4b_e10_a2_order_item_procurement.sql
supabase/migrations/20260729030000_m4b_e10_a3_order_notes.sql
```

- 三支**都已在 production 跑過交易模擬**(`BEGIN` → 驗 → `ROLLBACK`),零留痕已驗。
- 三支都是 expand（加欄 / 建表），**沒有任何破壞性動作**。
- D0 必須最先(A2/A3 不依賴它,但 DAG 就是這個序)。
- 🔴 **A10c1 的 flag 要等 D0 apply 完才可以開**(`ADMIN_E10_ORDER_NUMBER_SEARCH=1`)。沒 apply 就開 = PostgREST 42703 = **整個後台訂單列表**掛掉,不只搜尋。

### 2.2 D1 證據包

**沒有。** D1 線一片都沒動 —— 被 A0a 擋住了,理由見 ③。

### 2.3 五個決策題(照重要性排)

```
Q1(A0a-1、擋住整條 D1 線):
PCM-2026-0101(NT$2,400、unpaid、刷卡 pending)沒有 rec_trade_id,
而規格把它寫死成 D1b1 唯一的查詢鍵。三條路:
  A. 改用它的 bank_transaction_id 去查 TapPay（=當場改一條已審 28 輪的規格）
  B. 把 0101 移出待刪 cohort（26→25 張,它不刪不改,其餘照走）
  C. 整條 D1 暫停,等你先去 TapPay 後台查這筆

A: A|B|C
```
我推薦 **B**:D1 的目的是清掉沒有金流的假單,而 0101 恰好是「不確定有沒有扣到錢」那一類。留著的成本只是列表多一張舊單;刪掉的成本是永久失去唯一紀錄。

```
Q2(A0c-1):27 項驗收表有兩列跟事實對不上 ——
「取消訂單」記 ⚠️ 但其實整個動作不存在(全樹零 cancelled_at 寫入路徑);
「員工帳號」那列的證據行在 E8-A1 之後過期了。要不要我改掉主規格那張表?
  A. 改,兩列都更正、小計改成 2/5/20
  B. 只加註解說明、不動原字面
  C. 先不動,等三批都做完再一次校正

A: A|B|C
```
推薦 **A**:那張表是 E10 的唯一驗收標準,分母記錯會讓每一批的「做完幾項」都對不上帳。

```
Q3(A0b-1、擋 A11a 不擋現在):新版 12 欄列表把兩欄拿掉了,規格只列結果沒寫理由:
  ①「年份廠牌車種」(V-3b 專門做的欄,顯示這個品項是給哪台車的)
  ②「來源 · 管道」
  A. 兩欄都拿掉,照規格 12 欄
  B. 保留「年份廠牌車種」,拿掉「來源·管道」(13 欄)
  C. 兩欄都保留(14 欄、會擠)

A: A|B|C
```
推薦 **B**:車種是員工揀貨/確認相容性時真的會看的;來源·管道在明細頁已經有了。

```
Q4(A3-1、非阻擋):order_notes 是 append-only,員工若誤把備註選成「已告知客人」
就改不掉也刪不掉,U6 告知義務的稽核查詢會永遠把那筆算成已履行。
  A. 加一個 corrects_note_id 自我 FK(後來的紀錄可標記更正前一筆,仍是純 append-only)
  B. 先不加,等第 1 批 UI 做出來、看員工實際會不會誤選
  C. 不做,靠人工小心

A: A|B|C
```
推薦 **B**。我沒有自行加欄:依 §1 原則 2「欄位不得早於它的模型」,更正語意還沒定義。

```
Q5(A2-1、非阻擋):採購表只記到貨累計數,沒有 received_at / received_by、
也沒有逐批到貨明細 ⇒ 同一筆採購分兩批到貨時,答不出「哪一批、什麼時候、誰登錄的」。
規格 §5.1 A2 的欄位清單本來就沒這兩欄,我沒自行擴充。
  A. 現在就加(A2 還沒 apply,改檔很便宜)
  B. 先不加,等第 2 批批次到貨 UI 做出來確定真的需要
  C. 永遠不加

A: A|B|C
```
推薦 **B**。

**另有一題非阻擋、可以慢慢答**(D0-1):codex 指出全 repo 的 migration 都自己寫 `BEGIN/COMMIT`,與 Supabase CLI 記帳理論上有個窄縫。我維持現狀沒動(40 多支既有 migration 都這樣寫、從沒出事),要不要開 backlog 統一評估?

---

## ③ Blocker 與跳過的片

### 🔴 D1 線 14 片(D1a0 → D1c)全部沒動 —— 被 A0a 擋住

`PCM-2026-0101` 的 `payment_charge_attempts.rec_trade_id` 是 **NULL**(六筆裡唯一一筆)。

規格 §5.1 D1b1 逐字寫:
> 六筆 attempts 的 `rec_trade_id` **全部非 NULL**;任一 NULL = 停下 raise Sean,**不得用寬條件替代**

⇒ 我照規格停下,**沒有自作主張改查詢鍵**。那筆**有** `bank_transaction_id`,技術上查得到,但那正是規格禁止的「用寬條件替代」—— 屬你的決策,不屬我可拍的板。

**這條解開之前,D1a0 到 D1c 一片都不能做**(它們全都依賴同一份 cohort 定義)。

### 其他沒做的

- **A0a 之後的 DAG 順序沒有被打亂**:D1 線跳過,但我沒有把後面的片提前到「應該先 apply D1」的位置 —— 三支 migration 的 **apply 順序留給你,DAG 序不變**。
- **A9b1 + A10c1 目前不構成 D1 的開工前置**。規格說這兩片驗收通過 = D1 前置,但 D0 沒 apply、flag 預設 off ⇒ **端到端一次都沒跑過**。要成立必須:apply D0 → 開 flag → 真瀏覽器驗過新舊號皆命中。這件事我在 commit body 與 STATUS 都寫死了,沒有含糊帶過。
- **codex 連兩次逾時**(A15 R1、A2 R2),依 R4「相同錯法第 2 次 = 換路」沒試第三次,改由 Fable 接手。這是換路、不是多開一條平行審查線。

---

## ④ 誠實邊界

- 三支 migration **只寫檔 + 交易模擬,一次都沒 apply**。模擬證明的是「這段 SQL 在 production 現況下跑得過、且約束真的擋得住」,**不等於** apply 後不會有事 —— apply 是你的動作,建議一支一支跑、每支跑完看驗收 NOTICE。
- A10c1 的 UI **我沒有用真瀏覽器看過**(D0 未 apply、flag off,看了也不會有真資料)。視覺驗收留給你的分批肉眼驗。
- 我在 STATUS 主表加了幾行,主表已超過「≤30 行」的規定(本來就已超過)。要精簡的話那是另一件事,我沒順手動。
- commit 的 scope 欄用了 `schema` / `schemas` / `storefront`,其中 A10c1 是 admin 應用層、`storefront` 這個 scope 名稱不精確 —— CLAUDE.md 的 scope 清單裡沒有 admin。要不要加一個 scope 也是可以順手處理的小事。
