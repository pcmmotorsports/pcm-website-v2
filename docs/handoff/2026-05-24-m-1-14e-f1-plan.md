M-1-14e-f1 登入註冊頁 + Google OAuth UI — 定稿 PLAN v4(自驅 SOP、Cowork 不在 loop)
[v2 = 折入 codex round1 的 11 findings;v3 = 折入 codex round2 的 4 findings + 陪審腦第二輪 flags [A]-[H];v4 = 折入 codex 最後確認的 3 findings(final-1 CSS 步驟一致性 / final-3 OAuth 失敗 /login?error 顯示 / final-2 D-g vs Google OAuth 無手機會員→採 A);codex 已核三處 redirect 全 '/'、D-f/D-g 折法收斂]

基線:dev / HEAD=9d5a56f=origin/dev / 工作樹乾淨。
決策已拍(Sean 2026-05-24 + 陪審腦 verdict dev-dapper-perlis.md):
  D-a=A(地圖代號維持現狀、f1 算 M-1-15 引用、#178 留 M-1-14 收尾對齊、不動 roadmap-data.json/藍圖)
  D-b=A(composition root 單檔 + inline eslint-disable + 意圖註解;不選 B 搬 public)
  D-c=Sean 端 dashboard 前置三件(見 §5);未做的段照「先 infra、卡 Sean 完成才肉眼驗」序
  D-d=A(deposit/vehicle 屬 stage g、f1 不碰;DepositInput.parse 與 #177 vehicle service ''→null 留 g)
  D-e=依 design 不放 Register 社交鈕(design L256-308 確無、PRD §8.2 誤抄、鐵則 1 以 design 為準)

  D-f=A(Sean 2026-05-24 拍):Header 會員圖示 NAV_ROUTE_MAP.account '/account'→'/login'。
     理由[A]:不改則 /login 無站內入口 = 孤兒頁(只能手打網址到達);會員圖示是全站到 /login 現實入口、故 D-f=A 是「讓 f1 可用」必要條件、非順手清 404。
     折入[B]:① f1-a 改 NAV_ROUTE_MAP.account→'/login';② Header.test.tsx 目前無 nav 路由斷言 → f1-a 必「新增」會員圖示→/login smoke 斷言(鐵則 11);③ A 是 stopgap(不分登入態)、stage g 須補「登入→/account、未登入→/login」會員態判斷 → 入 backlog(否則遺忘)。
  D-g=A(Sean 2026-05-24 拍):註冊手機「必填」(業務現實:PCM 接單/配送需手機)。
     折入[E]:記為鐵則 1 design override(design 前端 L261 放行空手機、本案業務要必填);f1-b client 端加「手機必填」presence 檢查 + 顯式必填 affordance(非靠 server 靜默報錯);權威驗證仍 server RegisterInput.parse(已 required regex、不改 schema)+ §1 finding-9 三層 error copy。B(選填)已否決(=改 @pcm/schemas + 驗 customers.phone DB nullability + 型別下游 ripple、非一行)。

═══════════════════════════════════════════
0. 範圍 + 鐵則 8 判定 + 內容分級
═══════════════════════════════════════════
範圍:f1 = LoginPage + RegisterPage + Google 一鍵登入(完整閉環)。LINE=f2、會員中心 7 tab=stage g、MobileTabBar=h,皆不在本 f1。
鐵則 8 重大改動 = 是(跨 storefront+adapters 可見邊界 + 裝多個新依賴 + 動 eslint inline 例外)→ 已提 plan、Sean 已批 D-a~D-e、本檔為定稿 plan。
內容分級:LoginPage/RegisterPage 文案 + 社交鈕 = L1(design 成品、年改 0-1 次)→ hardcode 直接搬 design 可接受。無 L3。

═══════════════════════════════════════════
1. design 真權威(鐵則 1 直接搬、已 grep、不憑記憶)
═══════════════════════════════════════════
LoginPage  = AccountPages.jsx L181-253(含 Google 鈕 L231-239 + LINE 鈕 L240-243)
RegisterPage = AccountPages.jsx L256-308(無社交鈕、表單收於 L298「建立帳號」鈕 + L300-302 auth-foot)
auth-* CSS = account.css L5-16(.ap-page L5-8 + .ap-mono L10-16 base、LoginPage/RegisterPage 都用)+ L326-476(含 .auth-foot a L471-476)+ mobile L877-879
  [FIX finding-1:原 plan 寫 L326-471 漏 .auth-foot a 連結樣式(L471-476);account.css auth-* 區實際 L326-476、mobile L877-879]
  [FIX round2-1:LoginPage L196 className="ap-page"、L200 className="ap-mono"(N°01 label)用到 base class、原 plan 漏 → drift;auth.css 範圍補 account.css L5-16。stage g 建 account/cart 頁若同需 ap-page/ap-mono、屆時抽 shared base 避免重複定義(本 f1 先放 auth.css、不前置抽)]
字面:N°01 · Sign in / 歡迎回來 / 登入你的 PCM 帳號，查看訂單與收藏。/ Email / 密碼 / 記住我 / 忘記密碼？/ 登入 / 或 / 使用 Google 登入 / 使用 LINE 登入 / 第一次來？建立帳號
       N°02 · Sign up / 加入 PCM / 建立帳號，享會員價與專屬優惠。/ 姓名 / 手機 / 我同意 服務條款 與 隱私政策 / 建立帳號 / 已有帳號？登入

[FIDELITY-3] f1-a 的 LoginPage 必搬「Google 鈕 + LINE 鈕」兩個 markup(含 svg + 字面),不因「Google=f1-c、LINE=f2」而砍 markup。鐵則 1 = 搬字面含未接線鈕。

[FIX finding-8:LINE 鈕處置拍定唯一行為] f1-a 搬 LINE 鈕 markup、設 type="button"、無 onClick(惰性、不啟動任何流程)、視覺嚴守 .auth-social-line 不偏離。f2 才接線。不可用 alert/placeholder(非 design 字面)。Google 鈕 f1-a 同樣先搬 markup 設 type="button" 惰性,真接線在 f1-c(見 finding-7)。

[FIX finding-11:Header/Footer composition adaptation] design LoginPage/RegisterPage 用 <Header currentPage="login|register" onNav={onNav}> + <Footer onNav={onNav}>。storefront 既有慣例(ProductPage.tsx L7-9)= <Header currentPage="..." />(丟 onNav、Header 內走 Next Link/router)+ <HomeFooter />。f1 照搬此 adaptation:用 storefront <Header currentPage="login"/"register" /> + <HomeFooter />;design 的 onNav('login')/onNav('register') 內部連結 → Next <Link href="/login">/<Link href="/register">;onNav('account') 成功後導向見 finding-6。屬技術實作 routing adaptation(鐵則 1 例外類別 2、列 fidelity disclosure)。

[FIX finding-9:error copy 映射規則 + 不用 raw FormData] LoginPage/RegisterPage 為 client component(controlled inputs、鏡像 design useState 模式);submit handler 呼叫 server action 傳「結構化 plain object」({email,password,remember} / {name,email,phone,password,agree})、非 raw FormData → remember/agree 已是 boolean、避開 FormData checkbox 'on'/缺值地雷。error copy 三層:
  (a) client 端 presence 檢查用 design 字面(LoginPage'請輸入 Email 與密碼' L186 / RegisterPage'請填寫必要欄位' L261 + '請同意服務條款' L262)→ auth-err div 顯示(鐵則 1)。
  (b) server action 內 zod LoginInput/RegisterInput.parse 作信任邊界重驗(client 繞過時防線);zod message('Email 格式不正確'/'密碼至少 8 碼'等)非正常 UX 路徑、回傳映射成 auth-err 顯示。
  (c) AuthError(credentials_invalid/email_already_registered 等)= 真實 auth 失敗 → 最小 net-new 用戶字面(design mock 無真實 auth 錯誤路徑、無對應字面、屬技術必要 net-new、非 design 偏離、列 fidelity disclosure):登入失敗「Email 或密碼錯誤」、註冊 email 已註冊「此 Email 已註冊」、其他「登入/註冊失敗，請稍後再試」。

═══════════════════════════════════════════
2. composition point(D-b=A、PRECISION-4 + finding-4 + finding-5 折入)
═══════════════════════════════════════════
衝突事實:SupabaseAuthAdapter 在 @pcm/adapters/server(server-only subpath、import 'server-only');eslint.config.js L110-126 no-restricted-imports 禁整個 apps/storefront/**/*.{ts,tsx} import @pcm/adapters/server。@supabase/ssr 未裝。Next 16 App Router 需 cookie-aware client 才能讓登入 session 落地。

[FIX finding-4:inline eslint-disable 正確語序] composition root 單檔(apps/storefront/src/lib/auth/composition.ts、server-only)在 import SupabaseAuthAdapter 該行「正上方緊貼一行」放:
    // eslint-disable-next-line no-restricted-imports -- 受控例外:composition root 注入 IAuthService;SupabaseAuthAdapter 不持 service_role(收注入 anon-ssr client)、本檔永不 import createSupabaseServiceClient/SupabaseWalletAdapter
    import { SupabaseAuthAdapter } from '@pcm/adapters/server';
  (eslint-disable-next-line 必須是 import 的「前一行」、disable 才作用在 import;意圖用 `-- reason` 內聯或前一獨立註解行補述。原 v1 把意圖註解夾在 disable 與 import 之間 → disable 作用到註解行、import 仍紅、錯。)
  不動 eslint.config.js files-override block(更可審、code-reviewer 在 import 點即見例外)。

[GAP-1 + FIX finding-5:browser + server 雙 client factory、server 端 async per-request、不可 singleton]
  - apps/storefront/src/lib/supabase/server.ts:
      export async function createServerSupabaseClient() {
        const cookieStore = await cookies();   // Next 16 cookies() 為 async(context7 確認)
        return createServerClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, {
          cookies: { getAll(){return cookieStore.getAll()}, setAll(list){ try{list.forEach(({name,value,options})=>cookieStore.set(name,value,options))}catch{} } }
        });
      }
    request-scoped、每次呼叫新建、禁 module-level singleton(對齊既有 client.ts「singleton 由 DI/runtime 決定、本檔不抽 module singleton」紀律)。
    用既有 env NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY(不改 env 命名、不碰 .env*)。
  - apps/storefront/src/lib/supabase/browser.ts:
      export function createBrowserSupabaseClient(){ return createBrowserClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY) }
    給 Google 鈕 client 端 signInWithOAuth(PRD §8.4 client 端發起 redirect)。
  - apps/storefront/src/lib/auth/composition.ts:
      export async function getAuthService(): Promise<IAuthService> {  // async per-request(finding-5)
        const supabase = await createServerSupabaseClient();
        return new SupabaseAuthAdapter(supabase);
      }
    server action / route handler 全部 await getAuthService()。
  framework 相依(cookies()/window)→ 三檔落 storefront、不落 @pcm/adapters(維持框架無關)。AuthAdapter 維持收注入 SupabaseClient。

[finding-5b 釐清] middleware(session 自動刷新 / route gating)f1 不做:f1 無受保護路由(/account = stage g)、登入/註冊走 server action(cookies 可寫、setAll 正常)。middleware 留 stage g(/account 需 auth gating 時)再加。

[finding-5 已查清、無須回修(原 finding-5 收回)] f1-c Google OAuth 不擴 IAuthService port:PRD §8.4 刻意讓 Google OAuth 繞 port(client signInWithOAuth + /auth/callback exchangeCodeForSession),屬 storefront 層 net-new、非動共用 port。register/login/logout 走 port + AuthAdapter;Google OAuth 不走 port。

═══════════════════════════════════════════
3. 信任邊界(架構決策 A 落點、從 use-case 移 delivery)
═══════════════════════════════════════════
register/login server action(server 端、'use server'):
  - 收結構化 object(非 raw FormData、finding-9)→ @pcm/schemas parse:login 用 LoginInput.parse、register 用 RegisterInput.parse(server 端重新驗證、不信 client)。
  - strip 未知欄:zod parse 後只取 schema 欄位映射成 domain 型別(AuthCredentials / AuthSignUpParams{metadata:{name,phone}}),不把 client 多送欄位(如 tier/wallet_balance)透傳。
  - 失敗:AuthError(domain code)→ 映射 finding-9(c) 用戶字面;不上洩 Supabase 原始 error。
  - trust-boundary test:server action 收惡意/多餘欄位(試圖塞 tier/wallet_balance)被 strip;非法 email/短密碼/agree≠true 被 zod 擋。
  remember(LoginInput.remember)屬 session 持久化、由注入 SSR client 行為決定、不進 use-case。

═══════════════════════════════════════════
4. 登入成功導向(FIX finding-6:拍定唯一行為、不留二選一)
═══════════════════════════════════════════
登入/註冊成功 → 統一導 `/`(首頁、已登入態);定義單一具名常數 POST_AUTH_REDIRECT='/'。/account(7 tab)整體留 stage g、f1 不建任何 /account route。stage g 建 AccountPage 時改此一常數即可(one-line)。
[FIX [D] 三處 redirect 逐處核(防 R1-⑥ 半修漏網)] 送審 plan 原有 3 處導 /account → v3 全改 POST_AUTH_REDIRECT='/':
  ① f1-a login server action 成功 → POST_AUTH_REDIRECT
  ② f1-b register 直登成功 → POST_AUTH_REDIRECT
  ③ f1-c /auth/callback exchangeCodeForSession 成功 → POST_AUTH_REDIRECT
  三處共用同一常數、不可只改①;最後 codex 確認逐處核三處皆 '/'、無殘留 /account。
驗收(肉眼驗)信號:server action 無 AuthError + session cookie 落地 + 導回首頁;f1-c Google 同。(實作層決策、非業務決策;Sean 復審已知,先取最小 scope 導首頁、不建 /account 佔位。)

═══════════════════════════════════════════
5. sub-slice 拆分(鐵則 4、各 15-45 分鐘可中斷 + 肉眼驗)
═══════════════════════════════════════════
f1-pre(infra、無 UI、= 鐵則 8 本體):
  - 裝依賴(FIX finding-3:不只 @supabase/ssr):apps/storefront 加 @supabase/ssr + workspace deps @pcm/use-cases + @pcm/schemas + @pcm/ports(server action 用 loginCustomer/registerCustomer〔use-cases〕、LoginInput/RegisterInput〔schemas〕、IAuthService 型別〔ports〕);pnpm-lock.yaml 納影響面。
  - 寫 lib/supabase/server.ts(async createServerClient)+ lib/supabase/browser.ts(createBrowserClient)+ lib/auth/composition.ts(async getAuthService、finding-4 inline-disable)
  - [明定 [G] OAuth 發起模型 = client-initiated] PRD §8.4 signInWithOAuth 走 client 端 + redirectTo ${origin}/auth/callback → 故 browser.ts(createBrowserClient)「必在 f1-pre scope」(非懸空);Google 鈕 client component 用此 factory 發起。server.ts factory 給 server action(register/login)+ /auth/callback(exchangeCodeForSession)。
  - 三綠 typecheck+lint+build。無 UI、肉眼驗 = build 綠(此段無頁面)。
f1-a(LoginPage UI + 登入接線):
  - components/LoginPage.tsx(client component、controlled inputs)直接搬 design L181-253(含 Google+LINE 鈕 markup 惰性 type="button"、FIDELITY-3/finding-8);<Header currentPage="login"/> + <HomeFooter />(finding-11)
  - styles/auth.css 直接搬 account.css L5-16(.ap-page/.ap-mono base)+ L326-476(auth-* 含 .auth-foot a)+ mobile L877-879(finding-1 + round2-1 + FIX final-1:此步驟明列三段、不可只搬 L326-476 漏 base class)
  - app/layout.tsx 加 import '../styles/auth.css'(FIX finding-2:否則頁面無樣式)
  - app/login/page.tsx route
  - login server action(收 object → LoginInput.parse → loginCustomer(await getAuthService(), creds))+ error copy 映射(finding-9)
  - 成功導 POST_AUTH_REDIRECT='/'(finding-6 / [D]①)
  - [D-f=A / [A][B]] 改 Header NAV_ROUTE_MAP.account '/account'→'/login';Header.test.tsx 新增「會員圖示 onClick→router.push('/login')」smoke 斷言(鐵則 11、原無 nav 路由斷言)
  - LoginPage.test.tsx smoke + login server action trust-boundary test
  - 三綠;肉眼驗(Sean):/login 對齊 design、Header 會員圖示點→/login、輸入正確帳密登入成功導首頁、錯誤顯示 auth-err
f1-b(RegisterPage UI + 註冊接線、依 D-c-(1) Confirm email OFF):
  - components/RegisterPage.tsx 搬 design L256-308(無社交鈕、D-e)、共用 auth.css;<Header currentPage="register"/> + <HomeFooter />
  - app/register/page.tsx route
  - [D-g=A / [E]] 手機必填:client presence 檢查加 phone(顯式必填 affordance、非靠 server 靜默報錯)、記為鐵則 1 design override(design L261 放行空手機、本案業務必填);server RegisterInput.parse 權威重驗(已 required regex、不改 schema)
  - register server action(收 object → RegisterInput.parse → registerCustomer)+ error copy 映射
  - 直登:Confirm email OFF 時 needsEmailConfirmation=false → 註冊後導 POST_AUTH_REDIRECT='/'([D]②)
  - RegisterPage.test.tsx smoke + trust-boundary test(試塞 tier 被 strip / agree≠true 被擋 / 空 phone 被擋)
  - 三綠;肉眼驗(Sean、卡 Confirm email OFF):/register 對齊 design、註冊後直登導首頁
f1-c(Google OAuth 完整閉環、依 D-c-(2)(3)、FIX finding-7:整個 Google flow 同一 slice、不留半成品):
  - f1-a 的 Google 鈕在本 slice 才補 onClick:createBrowserSupabaseClient().auth.signInWithOAuth({provider:'google', options:{redirectTo:`${window.location.origin}/auth/callback`}})
  - app/auth/callback/route.ts(GET、await createServerSupabaseClient + exchangeCodeForSession(code)、成功導 POST_AUTH_REDIRECT='/'〔[D]③〕、失敗導 /login?error=oauth)
  - [FIX final-3] LoginPage 讀 ?error query param(useSearchParams)→ 命中時 auth-err 顯示對應字面(如「Google 登入失敗，請重試」);LoginPage.test.tsx 補「?error 顯示 auth-err」斷言 → OAuth 失敗閉環可肉眼驗。
  - [FIX final-2:D-g=A 與 Google OAuth 新帳號矛盾 → 採 A] Google OAuth 首登繞過 RegisterInput、trigger 插 phone=''(DB customers.phone DEFAULT ''、可空;trigger COMMENT 明示 OAuth 走此路徑、M-1-14a 已設計 OAuth 會員可無手機)。⚠️ 此處「既有 DB 已符合」= f1 不產 migration / 不改 schema / 不改 trigger COMMENT(對齊 §7/§8 不動 schema);僅描述既有 DB 行為、非要動 DB。處置 A:「手機必填」只約束 email 註冊表單(D-g 原語境);Google OAuth 會員 phone 可空、callback 成功照常導 POST_AUTH_REDIRECT='/'(不導補填流程);OAuth 會員補 phone 留 stage g profile 完成流程 → 入 backlog(g-scope)。不選 B(callback 導手機補填新頁=擴 f1)/ C(禁 OAuth 新註冊=違 trigger COMMENT 設計意圖)。
  - callback route test
  - 三綠;肉眼驗(Sean、卡 §13.1 + Supabase redirect URLs 完成):點 Google 鈕走完 OAuth 回來已登入導首頁;OAuth 失敗回 /login 顯示錯誤
  (Google markup 在 f1-a 已搬但惰性;f1-c 才把 onClick + callback + test 一次接齊閉環,中間 slice 無「可點但壞掉」半成品。)

═══════════════════════════════════════════
6. Sean 端 dashboard 前置(D-c、code 碰不到、卡關才肉眼驗)
═══════════════════════════════════════════
(1) Confirm email OFF(Supabase Dashboard→Authentication→Providers/Email)→ f1-b 直登前置。[GAP-2]
(2) §13.1 Google OAuth(Google Cloud Console 建 OAuth client + redirect URI https://bmpnplmnldofgaohnaok.supabase.co/auth/v1/callback + Supabase Dashboard→Authentication→Providers→Google→Enable 貼 Client ID/Secret)→ f1-c 前置。[GAP-2]
(3) [FIX finding-10 + round2-4] Supabase Auth → URL Configuration → Redirect URLs(client 端 redirectTo=${origin}/auth/callback、未列 allow-list OAuth 會卡):
    - f1-c 本地肉眼驗「只需」:http://localhost:3000/auth/callback
    - 部署時(折入 backlog #4 部署、f1 本地階段不卡)再補:Vercel preview wildcard `https://<project>-*.vercel.app/auth/callback` + prod/custom domain `https://<domain>/auth/callback`
    → f1-c 本地前置只卡 localhost 那條;preview/prod 留部署 checklist。
未做的段:照「先做不卡的 infra/段、卡 Sean 完成才肉眼驗」序推進,不空轉。

═══════════════════════════════════════════
7. 預期影響面 + rollback(FIX finding-3 補全依賴)
═══════════════════════════════════════════
影響面:apps/storefront 新增 lib/supabase/{server,browser}.ts + lib/auth/composition.ts + components/{LoginPage,RegisterPage}.tsx(+test) + styles/auth.css + app/{login,register}/page.tsx + app/auth/callback/route.ts + 2 server action;改 app/layout.tsx(加 auth.css import);改 components/Header.tsx(NAV_ROUTE_MAP.account→'/login'、D-f=A)+ Header.test.tsx(新增 nav 斷言);apps/storefront/package.json 加 @supabase/ssr + @pcm/use-cases + @pcm/schemas + @pcm/ports;pnpm-lock.yaml 更新。不動 schema/migration/RLS/共用 port 內容/eslint config 檔(只 composition.ts inline-disable)。不碰 .env*。
rollback:純新增檔 + layout.tsx 一行 import + Header.tsx 一行 route + package.json 4 dep + lock;git revert 該(批)commit 即還原,無 schema/資料遷移、無破壞既有 products 流程。

透明告知 + 後續追蹤(非 f1 scope、防遺忘):
- [C] /cart 也是 dead route(Header NAV_ROUTE_MAP L27、無 /cart route)= 既有 tech debt、非本 f1 引入、非 f1 scope、留 M-3 cart;透明告知不順手改。
- [B 後續] D-f=A 是 stopgap(會員圖示不分登入態恆 →/login);stage g 須補「登入→/account、未登入→/login」會員態判斷 → 入 backlog(g-scope)。
- [H] ap-page/ap-mono 為共用 base(stage g 的 Cart/Account 也用);f1 先放 auth.css 正確,stage g 規劃 shared base 落點避免重複定義(鐵則 10) → 入 backlog(g-scope)、非 blocker。
- [final-2 後續] Google/LINE OAuth 會員 phone 可空(DB 設計允許);D-g「手機必填」只約束 email 表單。OAuth 會員補 phone(profile 完成流程 / 下單前強制補)→ 入 backlog(g-scope profile tab),否則無手機會員下單/配送缺資料。

═══════════════════════════════════════════
8. 禁止清單
═══════════════════════════════════════════
- 不動 deposit / vehicle(屬 stage g、D-d)
- 不建 /account route(整體留 stage g、finding-6)
- 不擴 IAuthService port(Google OAuth 繞 port)
- 不把 SupabaseAuthAdapter 搬去 @pcm/adapters public(D-b 否決 B)
- 不改 eslint.config.js files-override block(用 composition.ts inline-disable、finding-4)
- 不放 RegisterPage 社交鈕(D-e、鐵則 1)
- 不砍 LoginPage Google/LINE 鈕 markup(FIDELITY-3);兩鈕在各自 wiring slice 前惰性 type="button"(finding-8)
- 不用 alert/placeholder 充當未接線鈕行為(finding-8)
- 不做 middleware(留 stage g、finding-5b)
- 不動 .env*(permissions.deny 硬攔)
- 不動 schema/migration/RLS/pricing/order
- 不用 git add . / -A、不自動 push
- 金額禁浮點(本 f1 無金額路徑、deposit 在 g)
— 禁止清單結束 —
