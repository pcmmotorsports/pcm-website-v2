# `agent/line-mail` 交接 —— 2026-09-05 夜

> 🔴 **本檔最後一次更新時 `HEAD = cd605a4cd` · 未推 19 顆 · `origin/dev = fe20d792b`**(驗它還在:`git merge-base --is-ancestor cd605a4cd <你的 HEAD>` —— 
> 🔵 ⛔ ~~原本這裡寫 `228253eaa`~~:**標題那顆與括號裡驗的那顆不是同一顆**(codex R8 nit)
> ⇒ 📌 **一個【驗錯對象】的驗證式, 過了也不代表標題那句成立。**)。
> **本檔不是權威,版控才是** —— 每一段附了「自己重量一次」的指令。⚠️ **不要照抄本檔的數字。**
> 🛑 **為什麼要寫這一句**:今天這條線因為兩個「沒有重跑就寫下來的數字」各繞了一次
> (`FOR UPDATE` 12 vs 實際 20 · 未推 16 vs 實際 15)—— 兩次都是**別人重量才發現的**。

---

## 1. 今天這條線的 commit(舊 → 新)

🔴 **這一節的清單停在 `228253eaa`, 之後又多了幾顆(codex R6 那一輪與三顆 docs)** ——
**不要拿這個清單當「全部」**;當下的全集用 `git log --reverse --format='%h %s' origin/dev..HEAD`
(最後一次量:**19 顆**, 含 1 顆 merge)。

⚠️ ⛔ ~~我在訊息裡寫過「16 顆」而當時實際是 15~~ —— **那個 16 我沒有重跑就寫下來。**
📌 **而這一段自己就是那個病的實例**:上面那個數字每 commit 一次就過期一次。

```
1f7c57586  兜底掃描器 片1(它差點變成一封每天叫的假警報)
60a23921a  折 codex R3 六條(三條的共同形狀:那條路從來沒被走過)
be0f8470b  換版本號 140000 ⇒ 180000(撞到 -db 的 ACL 漂移那支)
905b97191  折 R4 五條(最重那條是 R2 的修法自己弄壞的)
370749796  R5 抓到我上一顆【宣稱修好而沒修】
dbfbbe786  片 C-2:讓「手動留白」的單離開掃描面
a5eadca43  折 codex 七條(其中兩條推翻我自己寫下的決定)
cf713e706  折 R2 七條(⑤「沒救」是推的, 量了才知道錯一半)
0ee066864  統計趟被取消時心跳不得寫成功
d2c436c5f  折 R3 六條(⑦ 我第一版改錯方向, 另一個窗開檔才抓到)
27c70becb  merge origin/dev —— 排程白名單 八 ⇒ 九
d11dcb8bc  070000 一句過期字面加刪除線
702958055  折 R4 三條 + nit(都是「量到 4 而沒問那四個是哪四個」)
5a7eb9445  會員中心那句加條件(它對後台手動建的單是假的)
228253eaa  折 R5 兩條 + nit(而我自己也數錯了一個數字)
```
**重量**:`git log --reverse --format='%h %s' origin/dev..HEAD`

---

## 2. 🔴 `19` 號貼板(`20260905210000`)貼了沒 —— 判別法

**這是這條線今天唯一答不出來的問題**,而它決定下一片的成本。
🛑 **唯讀查證那道授權不在施工窗手上** ⇒ 要主視窗或 Sean 跑。

✅ **它有一個【真的】判別式** —— 本支建了一個**新物件**(裸 `CREATE VIEW`,不是 `OR REPLACE`):
```sql
SELECT pg_catalog.to_regclass('public.pcm_manual_no_email_excluded') IS NOT NULL AS 貼過了,
       pg_catalog.to_regclass('public.pcm_order_created_email_pending') IS NOT NULL AS 正對照_應為_t,
       pg_catalog.to_regclass('public.zzz_never_a_view')              IS NOT NULL AS 負對照_應為_f;
```
· `t / t / f` ⇒ **貼過了** · `f / t / f` ⇒ **還沒貼** · 正對照不是 `t` ⇒ **這把尺沒接上, 別讀那個 f**
🔵 **為什麼這一支答得出來**:`CREATE OR REPLACE` 的物件「在不在」**零判別力**(貼之前就在),
而**新物件不一樣** —— 這是本支刻意用裸 `CREATE` 換來的副作用之一。
📌 影響:**沒貼** ⇒ 述詞單一來源那片可以直接改 `210000`(四支 view 只改一次);
**貼了** ⇒ 要開新 migration 再改一次 ⇒ **成本大約兩倍**。

---

## 3. 🔴 三件等 Sean 的東西, 順序寫死

```
① 貼板 23 = 20260905070000   (pcm_pending_refund_open_for 等新函式)
② 20260905180000             (兜底掃描器;⚠️ 今天還沒排上貼板 —— 那是本線的決定)
③ 白名單那一顆 commit 合進 dev
```

**為什麼是這個順序**(每一條都有落點,不是偏好):
- **① 必須在 ② 之前** —— `180000` 的掃描器每輪呼叫 `public.pcm_pending_refund_open_for`,
  而**那支函式只有 `070000` 建**(全 `supabase/migrations/` grep 只有一支)。
  ✅ 貼反了**不壞資料**:`20260905180000` 的前置閘會 `RAISE EXCEPTION` 並**指名** `20260905070000`。
- **② 必須在 ③ 之前** —— 理由逐字寫在 `20260905180000` 檔頭(搜「正確順序」那一段):
  白名單那一顆先進 dev 而排程還沒排 ⇒ **缺口期每天一封假警報, 而它沒有冷卻**。
- 🛑 **只貼 ① 不貼 ②** ⇒ `070000` 明文交給掃描器的那個競態(**兩筆付款同時打同一張已取消的單**)
  **沒有人接**。那不是我的推論,是 `070000` 自己那一行寫的。

**重量**(不要信上面的字):
```sh
grep -rln 'FUNCTION public.pcm_pending_refund_open_for' supabase/migrations/   # 應只有 070000
grep -n '正確順序' supabase/migrations/20260905180000_*.sql
```

---

## 4. 手上還開著的

| 事 | 狀態 | 誰 |
|---|---|---|
| codex **R6** 確認輪(`git diff 702958055...228253eaa`) | 跑中 | `-f8` |
| 貼板 23 **要再刷一次**(`228253eaa` 動了 070000 的註解) | 已通知 | `-8f` |
| `20260905180000` 要不要排上貼板 | **未決,本線的決定** | 本線 |
| 述詞抽單一來源 | **parked**(退場條件:伴生 view 有讀者 **或** 述詞第一次漂) | — |
| `⟦b4-PIECEBGATEGAPS⟧` ③ 的**剩餘部分** | **比板上寫的小** —— 見 §4b | 本線 |
| `paid-email-html.ts` 那顆「到會員中心查看訂單」按鈕 | **不動**(Sean 勾 A4) | Sean / `-f8` |
| `manual-order-no-email-wiring.test.ts` | **寫好了而刻意沒 commit**(片 E 那半還沒到,判別法見 §4a) | 本線 |

### 4b. 🔵 `⟦b4-PIECEBGATEGAPS⟧` ③ —— **板上寫的比實際大**(2026-09-05 夜當場量)

板列 `docs/launch-todo.md:338` 尾巴寫「只剩 ③ 在 `-mail`」,而 ③ 原文有**三半**:
```
格10 沒有匯款 sibling ⇒ 殺不掉「needs_settle 前誤取消」   ⇒ ✅ 已有 格10b(:213-229)
格16 只驗 reason 沒驗零副作用                             ⇒ ✅ 已有 格16b(:231-239)
duplicate / user_in_flight 沒有格子                        ⇒ 🔴 **真的還缺**
```
✅ **剩下的只有第三半**,而**它自己在探針裡留了落點**:
`docs/probes/sibling-lookup-bank-pending-probe.sh:242-246` 逐字
「**格20(`user_in_flight`)本輪【拿掉, 不留半成品】**…落點:⟦b4-PIECEBGATEGAPS⟧ ③」。
🔬 當場量:那支探針 **25 格 `check`**;`duplicate` / `user_in_flight` 全檔命中 **1 / 2 處,
而【逐處開檔看過, 全部在註解裡】** ⇒ **找不到【逐字用那兩個名字命名】的格子。**
🛑 **而那句話比「兩條路一格都沒有」窄**(codex R8 nit,⛔ ~~原本我寫的是後者~~)——
它證不到「**有沒有一格【沒有用那個名字】而實際上走到了那條路**」:一格可以在斷言別的東西時
順便撞到 `duplicate` 那條分支,而**我這把尺看不到它**。
✅ 要答那個問題得**跑覆蓋率**、或**在那條分支埋一發突變看有沒有格子紅** —— **兩件我都沒做。**
📌 ⇒ 這一格的結論是「**沒有人【明確地】在守那兩條路**」, 不是「**它們完全沒有被碰到**」。
🛑 **本線今晚沒有動它** —— 收工前 triage 挑到它,而 R7 PASS 之後主視窗要收批 ⇒ 不開新片。
⏰ **什麼時候做**:下一個接這條線的人,或有人要翻匯款 flag 之前。


### 4a. 🔴 片 E 到底到了沒 —— **不要用 `grep p_notification_email` 問這件事**

🛑 **我用過那把尺, 而它答錯了。** 當場量:
```
git grep -c 'p_notification_email' origin/dev -- '**/*.ts' '**/*.tsx'   ⇒ 7 支檔命中
```
⇒ 看起來像「片 E 到了」。**而它沒到。** 那 7 支是**別條路**的命中
(`packages/adapters/.../mappers/order.ts`、`database.types.ts` 這些顧客站/型別產生物)——
📌 **那個欄位名出現在很多地方, 而【手動建單那條路】只有一個落點。**

✅ **問對的那一格**(這條線的接線測試自己就是這樣問的):
```sh
grep -c 'p_notification_email' apps/admin/src/lib/orders/manual-order-repository.ts
git show origin/dev:apps/admin/src/lib/orders/manual-order-repository.ts | grep -c 'p_notification_email'
```
· 兩個都是 **0** ⇒ **片 E 那半還沒到**(2026-09-05 夜當場量,`origin/dev = fe20d792b`)
· 🔴🔴 ⛔ ~~變成 **≥1** ⇒ 才可以 commit 那支測試~~ —— **那個 gate 是錯的**(codex R8 ③):
  `grep -c` **對註解與死碼一樣印 ≥1** ⇒ 有人在那支檔寫一行「TODO: 要送 `p_notification_email`」
  ⇒ 這把尺說「到了」,而**值仍然沒有被送下去**。
  ✅ **唯一的 gate 是【那支接線測試自己全綠】**:
  ```sh
  npx vitest run packages/use-cases/src/manual-order-no-email-wiring.test.ts
  ```
  · rc=0 且 **4 passed** ⇒ 才 commit 它 · 任何一格紅 ⇒ **不 commit, 也不要放寬它**
  🔵 上面那兩行 `grep` **降級成【看一眼的線索】**, 不是判準 —— 它們答「這支檔提到它了嗎」,
  而測試答「**值真的被送下去了嗎**」。📌 **兩個問題差一整層。**
🔵 **而最可靠的判別式不是 grep, 是那支測試自己**:
`npx vitest run packages/use-cases/src/manual-order-no-email-wiring.test.ts`
—— 當場 `2 failed | 2 passed`,而**紅的那兩格逐字說出缺的是什麼**。
📌 ⇒ **它紅著就是它在做它該做的事**;有人為了讓它綠而放寬它,那就是把這條線唯一的跨檔守門關掉。

---

## 5. 🔴 這條線今天學到而值得帶走的三件

1. **「量到了 N」與「那 N 個是哪 N 個」是兩個宣稱。**
   一份抽到**別支 view** 的四套,數量照樣印 4/4/4。⇒ 數量之外要**逐名核**。
2. **一個對的結論會讓人不去查它旁邊那個錯的數字。**
   「剝掉註解後 = 0」是對的,而同一句裡的「12 次」是我用眼睛數的(實際 20)。
   判別句:**這個數字是我【跑出來的】還是我【看出來的】?**
3. **hash / 路徑 / 版本號這三種, 一律不准從記憶或推理產出。**
   我在給主視窗的訊息裡寫了一個 commit hash `2b0b70cb0` —— **它不存在, 是我編的**
   (`git commit` 我只看了 `tail -2`, 而那兩行裡沒有 hash, 我沒補跑 `git log -1`)。
   🛑 **它比前兩個數字錯得更重**:12 與 16 至少曾經對過,**這一個從來沒有對過。**
   📌 而 hash 是**最會被直接拿去跑**的值(`git show` / `cherry-pick` / 審查的 diff 範圍)
   ⇒ **它不會讓人懷疑, 它會讓人拿到 `unknown revision` 然後懷疑自己的樹。**
   ✅ 判別句收斂成一句:**我這一頁裡的每一個值, 是我【跑出來的】, 還是【看來的 / 推來的 / 記得的】?**

4. **矛盾常常不在事實裡, 在排版裡。**
   codex 說「三套互相矛盾的敘事」,我第一反應是挑一套當假的刪掉 —— 而**真相是兩個不同的競態**,
   我把它們寫成同一組條列。**刪掉的那一句原本是對的。**
