# docs/handoff/ 索引 — 現行 vs 歷史(2026-08-12 Sean 拍 Q-盤1=A 設立)

> 讀 handoff 前先看本檔。**歷史檔=僅供考古,內容可能與現況矛盾**——現況權威鏈照舊:可驗證事實 > STATUS.md > CURRENT.md > 其他。本索引由 `/pcm-housekeeping` 盤整輪維護。

## 現行(活的交接)
- `CURRENT.md` — 常駐跨 session 交接檔(唯一長期活檔)。
- 檔名日期 **2026-08-01 之後**的 handoff — 視同可能現行,引用前仍對 STATUS 驗一次。

## 歷史(2026-08-01 之前,約 56 檔)
- 全部視為**歷史紀錄**:當時為真、現在未必;檔內的路徑/行號/狀態一律過期預設。
- 不搬檔、不刪檔(Sean 拍板不搬;要考古直接按檔名日期找)。
- 已知易誤導的例子:早期 3DS/退款線 handoff 的結論多已被 08 月實測推翻,查證以 memory M-3 段與 `docs/reference/tappay-reference.md` 為準。

## 維護規則
- 新 handoff 照舊直接放本目錄,檔名帶日期即自動被本索引的日期線分類,**不用回來改本檔**。
- 每輪 `/pcm-housekeeping` 把日期線往前推(下輪評估把線推到當前 milestone 起點)。
