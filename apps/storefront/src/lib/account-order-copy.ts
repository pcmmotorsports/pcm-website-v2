/**
 * 會員中心「訂單件數算不完整」時給客人看的那一句(`#636`,2026-08-18)。
 *
 * ## 為什麼是一個常數,不是各自寫在兩個 tab 裡
 * 同一個狀態在 `OverviewTab`(最近訂單)與 `OrdersTab`(訂單記錄)各印一次。
 * 落地前是**兩份逐字相同的字面** ⇒ 改一邊忘另一邊時,**畫面不會紅、測試不會紅**,
 * 而客人會在同一個網站的兩頁看到兩種說法。共用一份讓那個失敗模式**結構上不存在**。
 *
 * ## 🔴 為什麼舊文案是錯的(這是 `#636` 的病,不是潤稿)
 * 舊句逐字:「這張訂單的品項太多,件數這次沒有完整載入。**請重新整理**,或聯絡我們。」
 * 而觸發它的是 `ORDER_LIST_ITEMS_EMBED_LIMIT = 500` —— **一個寫死的上限**
 * (`packages/adapters/src/supabase/mappers/order.ts`,搜 `ORDER_LIST_ITEMS_EMBED_LIMIT`)
 * ⇒ **重整一百次拿回同一個「?」**。
 * ⇒ 那句話叫客人去做一件**永遠不會成功**的事,而他會照做、會做很多次,
 *   然後以為是自己的網路或手機有問題。**「這次」兩個字也在暗示它是暫時的 —— 它不是。**
 * 🔴 全陣紀律逐字:**「請重新整理」只准出現在【真的重整就會好】的地方。**
 *
 * ## 四段骨架(每一段都有它要擋掉的一個誤解)
 * ① **發生什麼** —— 件數沒算完整(不說「載入失敗」,那會被讀成網路問題)
 * ② **固定還是暫時** —— 是系統的問題、不是客人的操作(⇒ 他不會去找自己的錯)
 * ③ **空白不等於沒有** —— 「?」不代表訂單是空的,商品都在(⇒ 他不會以為訂單掉了)
 * ④ **下一步 + 這個狀態下不要做什麼** —— 找我們;而且先不要拿這個數字對帳
 *
 * ## ✅ 天花板已拆(`#639 甲`,Sean 2026-08-18 拍板;本段 15:4x 更正)
 * ~~兩個呼叫端都把它掛在 `title=` 上,`title` 是 hover-only ⇒ 手機客人四段一段都到不了~~
 * ⇒ **兩個呼叫端現在都把整段【印在畫面上】**(`<p className="acc-order-note">`),
 *   `title` 那個載體**在整個 repo 已零命中**。
 * ```
 * 真顧客站實測（390 × 844、真登入、501 品項的單、localhost 非 127.0.0.1、hydrate 探針 15/14）
 *   四段出現在畫面文字   0 / 4  →  4 / 4
 *   那段話還掛在 title    2 處  →  0 處
 *   有說明那張卡的高度    總覽 235px（無說明 88px）／訂單記錄 239px（無說明 117px）
 * ```
 * 🔴 **代價是 Sean 拍板時就講明的**:那張卡會變 2.0–2.7 倍高。**不要為了「好看一點」把它折回收合式**
 *   —— 那是他沒選的乙(三案並排 <https://claude.ai/code/artifact/a15cced4-de75-4680-b3f8-6afd696d9832>)。
 * ⚠️ **本檔原本這一段寫「它到不了客人眼前」,而那已經不是事實了** ——
 *   一段講得很有說服力的天花板,修好之後**最容易被留在原地繼續嚇下一個人**。
 * 📌 **會員中心的訂單列表在手機上是主要使用情境** ⇒ 這不是邊角。
 *   要真的送達得換載體(可見的一行說明 / 可點開的說明),而那會動到版面 ⇒ 歸 design 與 Sean。
 *   立案 `#639`,本片不順手改(改了就是在沒有拍板的情況下動客人看得到的版面)。

 * ## 稱謂
 * 用「您」—— Sean 2026-08-18 拍板 `Q2`=甲:**顧客站用「您」、後台用「你」**。
 *
 * ## 🔴 段②【不能寫成「這是誰的錯」】—— 這是落地時修掉的一個真缺口
 * 初版寫「這是系統的問題,**不是您的操作造成的**」——**那句回答的是責任歸屬,不是持久性**。
 * 客人要的是「我還要不要再等/再試一次」,而那句話**答不了**。
 * ⇒ 改成「**固定限制、不會自己恢復**」,責任歸屬那半降級成附帶的後半句。
 * 🔴 **而這一段不能靠禁詞測**(「暫時」的正確寫法就是「**不是**暫時的」,禁詞根會把對的句子一起禁掉)
 * ⇒ 段②的守門是**要求它出現**(`toContain('固定限制')`),不是要求某個字不出現。
 * 📎 對齊來源(**指標指到 dev 上撈得到的那一份**,2026-08-18 10:4x 當場量):
 *    `apps/admin/src/components/print/picking-doc.tsx:177` 逐字
 *    「這是系統的固定限制,不是暫時的狀況。」`grep -c` 在 dev 上 ⇒ 1。
 *    後台【訂單列表】的同款膠囊(W7)也講同一件事:
 *      `grep -c '固定限制' apps/admin/src/components/orders/orders-table.tsx`
 *
 * 🔴🔴 **上面那個 grep 請【自己跑】,我不在這裡寫它的答案。**
 *    這一句我今天寫錯過**兩次**,而兩次落筆當下都是對的:
 *      10:26 我寫「兩面現在講同一件事」⇒ 審查實測 **0 命中**(那份還在 `pcm-bmw-m` 沒收割)
 *      10:4x 我改寫成「dev 上 0、收割後才成立」⇒ **20 分鐘後 `6dcaf0d1` 收了,又變假**
 *    ⇒ **「某一份改動進了主線沒有」是一句【世界的狀態】,不是【本檔的內容】** ——
 *      它變假的時候檔沒改、測試沒紅、`grep` 數不變 ⇒ **複掃型守門對它天生無效。**
 *    ⇒ 所以這裡**只留量法、不留數字**。要知道答案的人跑那一行,三秒。
 *    📎 而 dev 上一定撈得到的對照在這裡(那支沒有跨分支問題):
 *      `apps/admin/src/components/print/picking-doc.tsx` 搜 `固定限制`。
 *    ⚠️ **刻意不照抄的兩點**:①後台寫「請聯絡負責人」(員工的出口),客人面的出口是「與我們聯絡」
 *    ②後台字面寫了「500 筆上限」,**這裡不寫數字** —— 客人面吃的是**另一個常數**
 *      (`ORDER_LIST_ITEMS_EMBED_LIMIT`,與後台那個同值而不同源、各自可調)
 *      ⇒ 寫死數字會在其中一邊被調整的那天變成假話,而畫面不會紅。
 *
 * ⚠️ **禁用詞根**:`重新整理` / `重新載入` / `重整` / `刷新` / `重試` / `再試` / `稍後` / `稍候`。
 *    後台同一族守門實測過兩次白名單被穿透(「請重新整理再列印一次」「麻煩您重新整理一下看看」
 *    都曾全綠)⇒ 禁**詞根**比禁**句型**強。守門在 `account-order-copy.test.ts`。
 *
 * 🔴🔴 **而這份根清單【擋不完】,不要照著它以為這個形狀已經解決了。**
 *    我第一版在這裡寫「中文的祈使形舉不完,**只能**禁詞根」——**那句話比這份清單做得到的寬**,
 *    它會讓下一個人以為只要過了這幾格就安全。審查當場複算,**七種寫法全綠穿透**:
 *      `稍候`(U+5019,與清單裡的 `稍後` U+5F8C **是不同字**,當時漏掉、現已補上)
 *      `過一下再看` / `等一下再進來看` / `請重開頁面` / `請再載入一次` / `按 F5` / `請退出重進`
 *    ⇒ **這份清單擋的是【已知說法】,不是【所有說法】。**
 *      真正扛住段②的是上面那條**正向**斷言(必須出現「固定限制/不會自己恢復」)——
 *      **要求它把話講對,比列舉它不准講的話可靠。** 禁詞清單只是第二道。
 */
/**
 * 🔴 **「叫客人再做一次」那一族的禁詞【根】** —— 客人面文案共用的單一權威。
 *
 * ## 為什麼是【詞根】而不是句型白名單
 * 後台同一族守門實測被穿透兩次:
 *   ①只禁「重新整理後」⇒「請重新整理再列印一次」全綠
 *   ②補祈使形     ⇒「麻煩您重新整理一下看看」還是全綠
 * ⇒ **中文的祈使形舉不完**,白名單這個形狀本身就是錯的。
 * 🔴 `稍候`(U+5019)與 `稍後`(U+5F8C)**是不同字** —— 審查當場實測前者穿透,兩個都要在。
 *
 * ## 🔴 2026-09-03 為什麼把它從測試檔搬出來變成 export
 * `account-profile-copy.test.ts` 落地時**自己重打了一份**, 少了
 * `重新載入` / `重整` / `刷新` / `重試`, 而且把詞根 `稍候` 寫成了句子 `請稍候`
 * ⇒ 「請刷新頁面」「稍候片刻」**在那支檔全綠穿透**。
 * 🎯 **⇒ 搬結論而把清單搬窄了 —— 而那個窄化在 diff 上長得像「我也有寫那道守門」。**
 * ⇒ 📌 改成單一權威 + 兩邊 import ⇒ **那個失敗模式結構上不存在**, 不再靠下一個人記得。
 *
 * ⚠️ **`暫時` 刻意【不在】這份清單裡** —— 它的正確寫法就是「**不是**暫時的」,
 *    禁詞根會把對的句子一起判紅。⇒ 持久性只能**正面斷言**(見本檔的守門)。
 *    🔵 而**兩個世界都可能**的文案(例:`account-profile-copy`)正面斷言不了持久性
 *    ⇒ 那種檔可以在自己那支加禁 `暫時`, 而**代價要寫在它自己旁邊**。
 */
export const RETRY_PROMISE_WORD_ROOTS = [
  '重新整理',
  '重新載入',
  '重整',
  '刷新',
  '重試',
  '再試',
  '稍後',
  '稍候',
] as const;

export const ORDER_ITEM_COUNT_TRUNCATED_NOTE =
  '這張訂單的品項比較多,件數我們沒有算完整。這是系統的固定限制、不會自己恢復,' +
  '也不是您的操作造成的。「?」不代表這張訂單是空的 —— 您買的商品都在,' +
  '只是這個數字現在給不準。需要正確件數請與我們聯絡,也請先不要拿這個數字對帳。';

/**
 * 訂單**明細頁**的「品項清單可能不完整」那一句(`#240`,2026-08-23)。
 *
 * ## 🔴 為什麼不共用上面那一句 —— 兩個狀態的【事實】不同
 * ```
 * 上面那句(列表)  客人看到的是一個【算錯的數字】,而商品清單他根本看不到
 * 這一句(明細頁)  客人看到的是一份【可能少了幾列的清單】,而他會以為那就是全部
 * ```
 * ⇒ 列表那句的重點是「?不代表訂單是空的」;**明細頁的重點相反 ——「您看到的可能不是全部」**。
 * 共用會讓其中一頁講一件不成立的事,而**兩邊都不會紅**。
 *
 * ## 四段骨架照舊(每段擋掉一個誤解),而第 ③ 段是這裡特有的
 * ① 發生什麼 —— 品項太多,這一頁沒有列完整
 * ② 固定還是暫時 —— **系統的固定限制、不會自己恢復、不是客人的操作造成的**
 * ③ 🔴 **您看到的可能不是全部**(列表那句在這裡會變成反話 ⇒ 這段是重寫的,不是搬的)
 * ④ 下一步 + 這個狀態下不要做什麼 —— 找我們;先不要拿這一頁對帳
 *
 * ## 🔴 「請重新整理」不准出現(全陣紀律,理由與上面那句同一條)
 * 觸發它的是 `MEMBER_ORDER_DETAIL_ITEMS_EMBED_LIMIT = 200` —— **一個寫死的上限**
 * (`packages/adapters/src/supabase/mappers/order.ts`)⇒ **重整一百次拿回同一份清單。**
 * 叫客人去做一件永遠不會成功的事,他會照做很多次,然後以為是自己的網路有問題。
 */
export const ORDER_DETAIL_ITEMS_TRUNCATED_NOTE =
  '這張訂單的品項比較多,這一頁沒有把它們列完整。這是系統的固定限制、不會自己恢復,' +
  '也不是您的操作造成的。請注意:下面看到的商品可能不是全部,件數也因此給不準。' +
  '需要完整的品項清單請與我們聯絡,也請先不要拿這一頁對帳。';

/**
 * ⟦b9-SHIPUI⟧ 分批出貨的那句小字(Sean 2026-09-02 拍**丙**)。
 *
 * 他看到的選項字面(`拍板-20260902-上午.md:147`)**逐字**是:
 *   亮「已出貨 09-02」+ 小字「其餘商品出貨時會再通知您」
 *   ⚠️ 那是原檔的位元組 —— 不加粗體、不換成『』(codex 2026-09-02:我第二版仍然自己加了標記)。
 * ⛔ ~~本行原本寫「逐字:亮『已出貨 MM-DD』」~~ —— `MM-DD` 是**我們的轉寫**、不是他打的字
 *    (code-reviewer 2026-09-02 抓:同片 `OrderDetailView.test.tsx` 引的才對 ⇒ 一片之內兩種引法)。
 *    📌 **「逐字」這兩個字一旦寫下去,下一個人就不會再回去開那支檔了。**
 * ⛔ **那個 `MM-DD` 已被 Sean 自己推翻**(2026-09-02 Q31 拍甲「跟鄰居一致(`2026-09-02`)」,
 *    落點 `~/pcm-mailbox/拍板-20260902-上午.md:244`)⇒ 日期走 `YYYY-MM-DD`。
 *    🔵 **本常數不含日期**,所以那一格不影響這句文案 —— 留這行是給搜舊字面的人看的。
 *
 * 🔴 **它只在「已出貨而還沒全部出完」時出現** —— 全部出完時這句話對客人是**假的**,
 *    而一句對客人為假的安撫語, 比不寫更貴:他會為了等一批不存在的貨而不聯絡我們。
 * 🛑 **而「全部出完了沒」在品項被截斷時是【未知】** ⇒ mapper 保守判成「還沒出完」
 *    ⇒ 那時會印這一句。⛔ ~~那個方向的錯只是多印一句**無害**的話。~~
 *    ✅ **兩個方向都會傷人**(codex 2026-09-02;完整理由在 `MemberOrderDetail.allItemsShipped`
 *       的 docstring):白等一批不存在的貨, 客人**還會來問**;以為到齊了, 他**就不會來問**。
 *       ⇒ **我們選的是比較輕的那一邊, 而不是一個沒有代價的選擇。**
 * 🔵 **文案不寫「還有幾件」** —— 我們算得出撈到的那幾件, 而截斷時算不出全部;
 *    印一個可能是錯的數字, 比不印數字糟。
 */
export const ORDER_DETAIL_PARTIAL_SHIPMENT_NOTE = '其餘商品出貨時會再通知您。';
