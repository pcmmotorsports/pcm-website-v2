/**
 * 會員中心「**會員資料那一列讀不到**」時給客人看的那一句(Q28,2026-09-03)。
 *
 * ## 為什麼是一個常數
 * 同一個狀態在 `OverviewTab`(儲值金卡)與 `WalletTab`(餘額卡)各印一次。
 * 寫成兩份逐字相同的字面 ⇒ 改一邊忘另一邊時**畫面不會紅、測試不會紅**,
 * 而客人會在同一個網站的兩處看到兩種說法。
 * (骨架與理由對齊隔壁的 `account-order-copy.ts` —— 那支是同一個病的第一個現場。)
 *
 * ## 🔴 為什麼舊文案是錯的(這是缺陷,不是潤稿)
 * 舊字面逐字:~~「餘額**暫時**讀不到」~~ / ~~「**暫時**讀不到」~~
 *
 * 觸發它的 `customerError` 有**兩個世界**(`app/account/page.tsx` 那個 catch 自己列的):
 * ```
 * A  RLS / session 異常 · 網路一抖    ⇒ 那一列還在, **會自己好**
 * B  PGRST116 = 那一列【不存在】      ⇒ 🔴 **永久**
 *    建它的只有 trigger `on_auth_user_created`(AFTER INSERT ON auth.users),
 *    只在註冊那一瞬跑一次;顧客站讀取路徑**零 insert/upsert customers**
 *    ⇒ 沒有任何一條路會把那一列補回來。
 * ```
 * 🛑 ⇒ **「暫時」在世界 B 是一個假的承諾** —— 它叫一個永遠不會好的人去等。
 *
 * 🔴 全陣既有紀律(`account-order-copy.ts` 逐字):
 *    **「請重新整理」只准出現在【真的重整就會好】的地方。**
 *    ⇒ 「暫時」是同一條規矩的另一個字面 ⇒ 拿掉。
 *
 * ## 🔴🔴 而下游比餘額嚴重 —— 這才是這句話真正要講的事
 * ```
 * create_order 是 fail-closed:
 *   PERFORM 1 FROM public.customers WHERE user_id = v_uid;
 *   IF NOT FOUND THEN RAISE EXCEPTION 'create_order: 查無 customer profile(uid=%)'
 *   (supabase/migrations/20260901030000_m4b_zero_total_settle.sql,搜 `查無 customer profile`)
 * ```
 * 📌 **⇒ 世界 B 的客人不是「少看到一個數字」,他【下不了單】。**
 * ⇒ 而他在結帳頁只會拿到通用字面(`charge-actions.ts` 的 `MSG.generic`,
 *   Q2=A 拍板:error 不洩、catch 全吞成通用句)⇒ **那裡不會、也不該告訴他成因。**
 * ⇒ ⇒ 📌 **所以「這可能影響結帳」這件事只有這裡講得了。**
 *
 * ## 🔴 為什麼連「現在」都拿掉(R2 nit;而它是我自己那條規矩沒套到底)
 * 首版逐字:~~「餘額**現在**無法顯示」~~
 * 🛑 我禁了「暫時」, 而**「現在」達成同一個暗示** ——「現在不行」自然讀成「等一下就行」。
 * ⇒ 📌 **我把規矩寫進骨架①(不承諾會好), 然後在同一個字串裡用另一個詞違反它。**
 * 🎯 而抓到它的不是禁詞清單(清單裡沒有「現在」), 是審查者去讀了**骨架與字串對不對得上**
 *    ⇒ **一把禁詞尺永遠只擋得住它列過的詞;而「這句話有沒有守住自己的骨架」只有讀得出來。**
 *
 * ## 三段骨架(每一段擋掉一個誤解)
 * ① **不承諾會好** —— 沒有「暫時」「稍後再試」(世界 B 永遠不會好)
 * ② **講出結帳** —— 否則他以為只是少一個數字,直到結帳失敗才知道
 * ③ **一條不靠我們的出路** —— 聯絡客服;而客服真的能修(要人補那一列)
 *
 * ## 🔵 而價格【不會】錯,所以這句話不提價格
 * 顧客站每一個價都是 `price_general`,對每一種 tier 都一樣
 * (public view 排除 `price_store`;`lib/products.ts` 刻意釘 general,搜 `釘 general`)。
 * ⇒ 等級掉回一般會員**不會**讓他看到錯的金額 ⇒ 講價格會製造一個不存在的恐慌。
 *
 * ## 🔴 為什麼這句話【不提「等級」】(code-reviewer M4;首版寫了, 是錯的)
 * 首版逐字:~~「等級與餘額現在無法顯示」~~
 * 🛑 而**同一個畫面正在印一個等級** —— `OverviewTab.tsx` 在同一個 `balanceFailed` 世界
 *    照常 render `<TierBadge tier='general'>` 與「一般會員價(升級需聯絡客服)」。
 * ⇒ 📌 對世界 A 的經銷會員:**畫面【斷言】他是一般會員, 而旁邊那句話說沒顯示** ——
 *    兩句話互相矛盾, 而客人只會相信那顆徽章(它看起來像資料, 那句話看起來像免責)。
 * 🎯 **⇒ 那正是這一族要修的形狀(「我們不知道」被顯示成「你是」), 只是換到 tier 那一格。**
 * 🔴 **而 tier 卡本身【本片不修】** —— 它就是 Q28 拍板(甲:掉回去要出聲)的正題,
 *    屬第 3 步、等 Sean 批 plan。**本片只是不再多講一句它做不到的話。**
 *
 * ⚠️ **暫用字面,定稿是 Sean 的板**(主視窗 2026-09-03 批為暫用)。
 *    TODO(backlog `#964`):等 Sean 拍定稿 —— **不修的話會痛在**:
 *    這句話是世界 B 的客人唯一一次被告知「你可能結不了帳」,措辭沒定稿 = 那一次告知的品質沒有人負責。
 *    🔵 而 code-reviewer 提的兩全解「**重新整理後仍看不到, 請聯絡客服**」(它把 A/B 分流且免費)
 *       **被禁詞根 `重新整理` 擋掉** ⇒ 那是一個要 Sean 拍的取捨, 已列進待拍題, 不由我決定。
 */
export const PROFILE_UNREADABLE_NOTE =
  '會員資料讀不到,餘額無法顯示。這可能影響結帳,請聯絡客服。';
