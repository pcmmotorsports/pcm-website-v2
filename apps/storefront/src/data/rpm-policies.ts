// rpm-policies.ts — RPM 共用政策文案「單一真相」(結構化 runs、純資料無 React 依賴)
//
// ⚠️ 保固 / 退換 / 鑑賞期政策由 ProductTabs(OD-8 保固分頁、Sean 指定為 canonical「那份」)、
//    ProductFAQ(OD-10 N°04「保固與退換貨」)與 InfoShippingPage(A2 /info/shipping 退換貨 tab、
//    2026-07-03 第三消費端)**共用同一份**、不得各自抄一份分歧版本
//    (Sean 2026-06-03 釘:該鑑賞期免除是對客戶的法律主張、Sean 仍在確認準確性 →
//     改字面只動本檔這一處、三處同步渲染、永不漂移)。
// - 字面 byte-for-byte 取自 OD-8 ProductTabs 保固 pane(視覺真權威 OD 模板 §保固、鐵則 1)。
// - 含《消費者保護法》第 19 條第 1 項鑑賞期免除條款(L1 法律政策、hardcode 可接受、
//   未來 site_policies 後台接線;Sean 確認準確性後若需改字面 → 改本檔)。
// - 渲染:消費端(ProductTabs / ProductFAQ)各自把 PolicyRun[] map 成 JSX(string→文字、{b}→<strong>)。

import { OPENING_HOURS, openDaysLabel } from '@/lib/site-config';

/** 政策段落的最小單位:純文字 或 需加粗的文字({ b }) */
export type PolicyRun = string | { b: string };

/**
 * 營業時段字面(`RPM_WARRANTY_NOTES` 第 3 條用)。
 *
 * 🔴 **為什麼這一行要從 SSoT 衍生,而檔頭說「改字面只動本檔這一處」**:
 *    檔頭那句管的是**政策文案**(鑑賞期、瑕疵認定 —— 對客戶的法律主張、鐵則 1 byte-for-byte);
 *    營業時間**不是政策,是聯絡資訊** —— 它跟頁尾/手機選單/服務條款/schema.org 講的是同一件事,
 *    而 `legal-content.ts:38` 已經立了規矩「聯絡 / 登記資訊一律從 SSoT 衍生、不在本檔硬寫」。
 *
 * 🔴🔴 **這一支是 `#528` 的漏網之魚,而【它為什麼漏掉】比【它漏掉了】更值得記**:
 *    `site-config-wiring.test.ts` 的註解裡**寫著一條反向量法**,專門抓這種硬寫殘留:
 *        git grep -nE '週一[-–]週六|10:00[-–]19:00' -- apps/storefront/src
 *    **那條命令被寫下來了,而它【跑起來也撈不到本行】。**
 *
 *    決定性實驗(2026-08-19,兩行測資,`/usr/bin/grep -E`):
 *        測資 甲 `週一-週六`(半形 hyphen) / 乙 `週一–週六`(en-dash U+2013)
 *        `週一[-–]週六`  ⇒ **只中甲**
 *        `週一[-]週六`   ⇒ **只中甲(輸出逐字節相同)**
 *        `週一[–]週六`   ⇒ 空
 *    ⇒ 🔴 **`[-–]` 在 BSD grep 與 `git grep` 上就是 `[-]`** ——
 *      它**安靜地退化成只剩自己的 ASCII 成員**,而 en-dash 那一半被丟掉。
 *
 *    ⚠️ **引擎相依,不是「shell 上一律壞」**(2026-08-19 G6 打回我原本「恆零命中」的過寬說法):
 *      同三發改用 **ugrep**(Bash 工具裡手打的 `grep` 是它)⇒ `[-–]` **甲乙都中**。
 *      ⇒ **手打驗證會過、`#!/bin/bash` 腳本裡跑同一條會漏**,而兩邊都不報錯。判別 `type grep`。
 *      (機制推斷:BSD 的 `[…]` 是位元組類別。**這一句是推的;上面那三行是量到的。**)
 *
 *    🔴 **毒在哪:寫 `[-–]` 的人是想周全**(半形與 en-dash 都涵蓋),
 *    而那份周全**正是它退化的入口** —— 它安靜丟掉周全的那一半,只留原本就會抓到的那一半。
 *    ⇒ **混合型比全多位元組更毒**:後者回 0 還有機會被人問一句;
 *      混合型**有輸出、非零、看起來完全正常**,而它漏掉的正是你特地加進去的那個字元。
 *
 *    ⇒ 對應的機制不是「下次記得跑」,是 `site-config-wiring.test.ts` 的**分母守門**:
 *      掃全樹的時段字面、不在白名單上就紅,**而且它自帶一道自檢**
 *      (拿一個已知存在的字面餵它,撈不到 ⇒ 整格作廢),防的就是上面這種壞尺。
 *
 * 🔴 2026-09-12 Sean 貼稿改版:字面從 OD 原字面 `週一–週六 10:00–19:00`(en-dash)
 *    改成稿上的 `服務時間：週一至週六 10:00～19:00`。⛔ ~~「動前先開 OD 那幾支」~~ ——
 *    這次的權威是 Sean 的稿,不是 OD。
 */
const OPEN_HOURS_LINE = `，服務時間：${openDaysLabel('至')} ${OPENING_HOURS.opens}～${OPENING_HOURS.closes}`;

/**
 * 保固 / 退換 / 鑑賞期政策正文。
 * 🔴 2026-09-12 Sean 貼稿改版(`~/pcm-mailbox/2026-09-12-文案改版-四頁.md`)。
 *    稿上的 5 個小標壓成段首粗體(現有三個消費端只有「段落 + 一組條列」,主視窗同意不另做版面)。
 * 🔴 **鑑賞期那一段(第 3 段)是舊字一字不動** —— Sean 09-12 拍 Q3 乙:律師看過再改。
 */
export const RPM_WARRANTY_PARAGRAPHS: PolicyRun[][] = [
  ['本店多數品項為接單後向海外原廠專屬排產之', { b: '客製化／代訂商品' }, '。'],
  [
    '訂單一旦成立並向原廠送出排單，',
    { b: '恕無法取消訂單、更換款式或變更車型規格' },
    '。下單前請務必仔細確認適配車款、年份與安裝規格。',
  ],
  [
    '關於鑑賞期：本賣場屬於',
    { b: '客製化委任代購' },
    '，依《消費者保護法》第 19 條第 1 項，這類客製、代訂商品',
    { b: '不適用 7 天鑑賞期' },
    '。鑑賞期是讓您確認商品符不符合需求，不是商品的試用期——這點先跟您說明，下單前確認好就沒問題。',
  ],
  [
    { b: '驗收時效：' },
    '收到包裹後請於第一時間開箱清點。若發現商品瑕疵、缺件或品項寄送錯誤，請於',
    { b: '簽收翌日起 7 天內' },
    '拍照或錄影，並聯繫官方 LINE 客服，我們將儘速為您辦理免費換貨。',
  ],
  [
    { b: '退換條件：' },
    '申請退換之商品必須維持',
    { b: '全新未安裝、無使用痕跡' },
    '狀態，且',
    { b: '原始包裝完整' },
    '（包含原廠外盒、防震耗材、配件、說明書及購買憑證）。',
  ],
  [
    { b: '無法受理退換：' },
    '商品一經上車組裝、鎖附、通電，或已產生刮痕、油漬、磨損等任何人為使用痕跡，恕無法受理退換貨申請。',
  ],
];

/** 退換補充(ProductTabs 保固 pane + InfoShippingPage 退換貨 tab 渲染 ul;ProductFAQ 依 OD FAQ 模板省略、只放正文) */
export const RPM_WARRANTY_NOTES: PolicyRun[][] = [
  [
    '屬於瑕疵受理範圍：外觀表面嚴重破損、凹折或漆面瑕疵；碳纖維／金屬結構紋路明顯嚴重錯位；固定孔位明顯偏差、超出原廠合理公差範圍導致無法正常對位安裝。',
  ],
  [
    '不屬於瑕疵受理範圍：車輛行駛所致之人為碰撞、倒車、摔車損壞；未依專業規範施工、扭力不當造成之滑牙、斷裂或不當安裝損害；買方自行切割、鑽孔、修改或拆解等人為加工；手工製程容許之微小公差、不影響結構強度與組裝之細微外觀公差。',
  ],
  ['售後服務窗口：官方 LINE ', { b: '@pcmmoto' }, OPEN_HOURS_LINE],
];
