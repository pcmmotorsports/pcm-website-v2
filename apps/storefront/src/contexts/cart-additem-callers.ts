/**
 * `addItem` 回傳值的**豁免名單** —— 守門在 `cart-additem-callers.test.ts`。
 *
 * # 為什麼有這支檔
 * 2026-08-24 `N4`:`CartContext.addItem` 改成回傳「因為上限 `MAX_QTY` 而被夾掉幾件」,
 * 讓呼叫端不必自己去 `items` 裡找那一列。**但 TypeScript 不會強迫任何人去用那個回傳值。**
 * ⇒ 若沒有這道守門,病只是從「找不到那一列」換成「忽略回傳值」——
 *   **出口換了,而分母還是沒有人在看。**
 *
 * 在這之前的實錘:三個呼叫端裡只有桌機商品頁接了,另兩個(手機 sticky 買價列 / 商品卡快速加入)
 * 在車上已滿時**一件都沒進去卻說「已加入」** ⇒ Sean「不要靜默夾」的拍板只落到了桌機。
 *
 * # 守門怎麼運作
 * 測試**自己走一次檔案樹**數出所有 `addItem(` 呼叫點(分母是數出來的,不是誰列的),
 * 每一個都必須**接住回傳值**(`… = addItem(` 或 `return addItem(`)。
 * 接不住的,必須具名列在下面這份豁免名單裡並寫清楚理由 —— 否則測試紅、且會**指名是哪一支哪一行**。
 *
 * # 這份名單要怎麼加
 * 🔴 **加進來之前先想一次:真的不需要那個數字嗎?**
 * 「這個入口 qty 恆 1、最多只丟得掉 1 件」**不是**理由 ——
 * 上一版就是這樣寫的,而**丟掉幾件從來不是重點,重點是那一刻畫面說了什麼**。
 * 唯一站得住的理由是:**那次呼叫之後畫面沒有任何一句話在斷言「加進去了」。**
 *
 * 名單格式 = 相對 `apps/storefront/` 的路徑(與守門印出來的字面一致,方便直接貼)。
 */
export const ADDITEM_CALLERS_EXEMPT_FROM_DROP: readonly string[] = [
  // 目前是空的 —— 三個呼叫端全部都接了回傳值。
];
