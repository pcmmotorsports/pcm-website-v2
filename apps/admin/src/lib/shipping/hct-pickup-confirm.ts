// hct-pickup-confirm.ts — 新竹的箱子「手打單號就標出貨」之前要先勾一格(⟦走查 F8⟧)。
//
// 🔴 Sean 2026-09-11 拍 Q1 乙「不一定, 有時候會在新竹自己的網站, 或用手寫的託運單」、
//    Q2 乙「留著, 但員工要先勾『新竹已經收走了』才能按」
//    (plan `docs/plans/2026-09-11-hct-manual-ship-before-dispatch-plan.md` §4, 主視窗轉)。
// 🔴 **只管手打那條**:建箱彈窗「建箱並標出貨」與出貨卡「填單號並標記出貨」。
//    叫車那條(`shipment-dispatch-hct-action.ts`)直接呼 repository 的 `markShipmentShipped`,
//    **不經過這裡、也不經過那兩支 action** ⇒ 叫到車就標出貨, 不受影響。
// 🔴 畫面與 server 共用同一句話與同一個判斷 —— 兩邊各寫一份會各自漂。
//    (`shipment-actions.ts` 是 `'use server'`, 只能匯出 async 函式 ⇒ 常數住在這裡。)

/**
 * 🔴🔴 **兩顆鈕的名字 —— 而它們住在這裡, 是因為【今天為了它爛過一次】。**
 *
 * 🔬 2026-09-16 實況:那顆鈕稍早改名成「跟新竹要託運單號」(它走 `submitTransData`,
 *    **不是叫車**), 而**訊息層一句都沒跟著改** ⇒ 本檔下面那句
 *    `HCT_PICKUP_REQUIRED_MESSAGE` 逐字叫員工「再按【送新竹】」——
 *    📌 **那不是文案不一致, 是【指令錯誤】:他照著找, 找不到那顆鈕。**
 * ⇒ 🎯 **成因是「一顆鈕的名字住在的地方不只它自己那一支檔」** ——
 *    改名的人只看得到自己那一支, 而**沒有東西會叫**。
 * ⇒ ✅ 所以**凡是「叫使用者去按某顆鈕」的句子, 一律引這裡的常數, 不再手打字面。**
 *    下次改名 ⇒ 改這一行, 那幾句自己跟著對。
 *
 * 🛑 **射程講明**:它只綁住「**指名叫人去按**」那一類句子。
 *    純描述性的句子(例:「還沒跟新竹要過託運單號的箱 ⇒ 作廢重建」)**刻意不引常數** ——
 *    硬塞會讓句子變成一串引號, 而那一類句子改名了也不會把人指去一個不存在的地方。
 * ⚠️ **本檔名字比它的角色窄**(它叫 pickup-confirm, 而現在也放鈕名)——
 *    而檔頭那句「畫面與 server 共用同一句話, 兩邊各寫一份會各自漂」逐字就是這件事的理由,
 *    📌 **再開第二個家比名字不準更糟**。要搬的話兩處一起搬。
 */
export const HCT_REQUEST_NUMBER_BUTTON = '跟新竹要託運單號';
/** 🔵 真的叫車那一顆 —— 在**出貨清單頁右上角**(`shipment-pick.tsx`), 不在訂單頁。 */
export const HCT_DISPATCH_BUTTON = '新竹物流叫車';

/** 勾選框上的字。 */
export const HCT_PICKUP_CONFIRM_LABEL = '新竹已經把貨收走了(這個單號是新竹給的)';

/**
 * 沒勾就按(或繞過畫面直接送)時的那句話。
 *
 * 🔴🔴 **2026-09-16:這句話原本【指向一顆不存在的鈕】。**
 *    ⛔ ~~「先『只建箱』, 再按**「送新竹」**、到出貨清單『叫車』」~~
 *    那顆鈕 2026-09-16 稍早已經改名成 **「跟新竹要託運單號」**
 *    (`shipment-hct-submit-button.tsx`:它走 `submitTransData`, **不是叫車**)。
 *    ⇒ 📌 **這不是「文案不一致」, 是【指令錯誤】** —— 員工照著找, 找不到那顆鈕。
 *    ⇒ ⇒ 那次改名改了鈕、**沒掃到訊息層** —— 一顆鈕的名字住在的地方不只它自己那一支檔。
 * 🔵 順便把**流程講完整**(原本那句跳過了「叫車之後會發生什麼」的因果):
 *    只建箱 → 跟新竹要託運單號 → 到出貨清單叫車 → **叫到車系統會自己標出貨**。
 *    🛑 中間那一步不能省:沒有託運單號, 出貨清單那顆叫車按下去新竹不知道要收哪一箱。
 */
export const HCT_PICKUP_REQUIRED_MESSAGE =
  '新竹物流的箱子要先勾「新竹已經把貨收走了」才能標出貨。' +
  `還沒收走的話,先「只建箱」,再按「${HCT_REQUEST_NUMBER_BUTTON}」,` +
  `然後到左邊選單的「出貨清單」按「${HCT_DISPATCH_BUTTON}」—— 叫到車系統會自己標出貨。`;

/** 手打標出貨要不要先勾:只有新竹。順豐 / 其他照舊。 */
export function needsHctPickupConfirm(carrierCode: string): boolean {
  return carrierCode === 'hct';
}
