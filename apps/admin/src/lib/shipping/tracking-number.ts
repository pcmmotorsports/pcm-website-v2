// #551:貨運單號的格式檢查。**裝在【建箱 / 標記出貨】的表單上,不裝在列印端。**
//
// 🔴 **「裝哪一層」才是本檔的重點,不是「要不要驗」**(backlog `#551` 逐字):
//    列印端驗 = **最後一刻才發現,而那時貨已經要出門了**。
//    這裡驗 = 員工還在鍵盤前面的那一刻擋住。
//    📎 而 repo 自己的參考檔早就這樣寫了:`docs/reference/hct-logistics-api-reference.md`
//       錨點「後台表單應在前端擋掉」逐字:「不要讓員工按了送出才看到英文錯誤」。
//
// 🔴🔴 **分級依據 = 證據強度,而我第一版把證據數錯了(R1 code-reviewer 抓的,已重數):**
//    數法:`grep -n '10 碼\|Char(10)' 與 `grep -c '純數字'` 於
//    `docs/reference/hct-logistics-api-reference.md`,落筆當下:
//    ```
//    長度 10 碼   ⇒ 【四處】、跨兩章節（API 欄位規格 與 條碼規格）
//    純數字       ⇒ 【一處】：`grep -c '純數字'` ⇒ 1（就在「CODABAR」那一行）
//    檢查碼       ⇒ 【四處】：`grep -c '檢查碼'` ⇒ 4，跨 §5.3 與 §7 兩章
//    ```
//    🔴🔴 **而這個「四處」是 R2 用【同一把尺】重量出來的 —— 我第一版又數錯了一次(第三次)。**
//    我當時長度用寬 pattern(`10 碼|Char(10)`)、檢查碼用窄 pattern(`檢查碼 =`)
//    ⇒ **兩把寬窄不同的尺,量出「四處 vs 一處」這個我想要的對比。**
//    誠實版本:**公式**只有一處(`:176`);而**「檢查碼存在、而且 HCT 自己在驗」有四處** ——
//    其中 `:206` 是 HCT 官方錯誤清單逐字「**貨號檢查碼錯誤**」,`:179` 是本 repo 自己寫的
//    「檢查碼規則公開 ⇒ 我們可在送出前自行驗證」。**那兩句是本檔分級的反面證據,不引用等於藏起來。**
//    🔴🔴 **而「純數字」與「檢查碼」是同一張表【相鄰的兩行】、同一份 PDF、同受 §8-9 版本未確認**
//    ⇒ **我第一版宣稱「純數字有三處」是錯的**,而那個錯讓我把兩件證據一樣弱的事
//       分成了「擋」與「警告」兩級 —— **分級的理由與分級的結果對不上。**
//    ⇒ 現在改成:**只有長度擋(四處/跨章節),字元集與檢查碼都只警告(各一處/相鄰同源)。**
//    而同一份檔的 §8「未確認清單」第 9 條逐字寫著:兩份官方 PDF **是否為最新版未確認**。
//    ⚠️ **我方沒申請過 HCT 服務、從來沒看過一個真的貨號。**
//    ⇒ **公式若已改版,「擋」會讓員工出不了貨** —— 那比它要修的 bug 更糟。
//    ⇒ 等有人拿真貨號對過一次,再把警告升成擋(升級點寫在 `#551`)。
//
// 🔴🔴 **這道警告是【會自曝】的,那是它最重要的性質**(R2 F-B 算出來的):
//    `MOD(x,7)` 值域 0-6。**公式若是錯的**、真貨號尾碼均勻 0-9 ⇒
//    命中機率 = 7/(10×7) = **1/10** ⇒ **誤報率 90%**(不是我原本寫的 30%,那個算法只數了尾碼 7/8/9)。
//    ⚠️ 90% 聽起來像災難,而**它其實是這道守門唯一的體檢方式**:
//       公式對 ⇒ 只在真打錯時跳;公式錯 ⇒ **上工第一個小時就有人來罵**,不會靜靜爛掉。
//    🔴 但那也表示:**看到它頻繁跳的時候,要先懷疑公式,不是先懷疑員工。** 寫在 `#551` 升級點。
//
// ⚠️ **本檔是【資料衛生】不是安全層**(R2 F-J):真正寫 DB 的 `shipment-actions.ts`
//    完全不看格式 ⇒ 繞過畫面就繞過這裡。不搬上去是刻意的(打錯貨號不是攻擊面),
//    但**別讓「兩個入口都接了」被讀成「寫入層擋得住」**。
//
// 🔴 **`sf`(順豐)不驗,理由是【規則我方未查證】,不是【不需要驗】。**
//    這句話一定要留著:下一個人看到「只驗 hct」很容易讀成「順豐刻意豁免」,
//    然後在別處照抄這個「豁免」。**它不是豁免,它是一個沒補的洞。**
//    `other`(自取/自送)則是**本來就沒有貨號**,那才是真的不適用。

import { CARRIER_LABEL } from './carrier-label';

/** `block` = 擋住送出;`warn` = 印出來給人看,但**不擋**(見檔頭的證據強度那段)。 */
export type TrackingIssue = { level: 'block' | 'warn'; message: string };

/**
 * 🔴 兩個警告共用的結尾。**原本寫「新竹那邊會再驗一次」—— 那句是錯的**(R1 MF3):
 * `git grep -niE "hctapiweb|TransData|EDI_WebService" -- apps packages` ⇒ **零命中**,
 * 我們根本沒串 HCT API;而貨號是員工從 HCT 自己的託運單**抄下來**的,
 * 打錯的那一碼只存在我們 DB 裡、**包裹照樣寄達** ⇒ 新竹沒有任何東西可以驗。
 * ⚠️ 那句話會叫員工放心照送,而 `#551` 要修的病(客人查無此件)就走這條路。
 */
const TAIL = '請再核對一次 —— 送出之後沒有人會再幫你抓這個錯。';

/** 貨號 10 碼 = 前 9 碼 + `MOD(前 9 碼, 7)`(`hct-logistics-api-reference.md`,錨點「檢查碼 =」)。 */
function hctCheckDigit(first9: string): number {
  return Number(first9) % 7;
}

/**
 * 這個貨號有沒有問題;`null` = 沒問題(或**我們沒有能力判斷**)。
 *
 * ⚠️ **`null` 有兩種意思,而呼叫端不需要分辨**:「驗過了沒問題」與「這家我們驗不了」。
 *    兩者對**送出**這個動作的結論一樣(放行)⇒ 收成同一個回傳值。
 *    但**理由不一樣**,所以理由寫在這裡而不是靠回傳值表達(見檔頭 `sf` 那段)。
 *
 * 🔴 空字串回 `null`、不回 `block`:**「還沒填」與「填錯了」是兩件事**,
 *    而「非 other 必須有單號」那道已經在 `shipment-dialog.tsx`(錨點 `shipBlocker`)。
 *    在這裡也擋一次 ⇒ 員工會同時看到兩句話講同一件事。
 */
export function trackingNumberIssue(
  carrierCode: string,
  trackingNumber: string,
  /**
   * 🔴 **`false` = 員工還在打字 ⇒ 一律不回問題**(R2 F-A)。
   *
   * 沒有這個參數的話,`onChange` 逐鍵求值 ⇒ 打一個**完全正確**的 10 碼貨號,
   * 「這個是 1 碼…9 碼。請對著託運單再確認一次。」會**跳九次**。
   * ⇒ 在 100% 的正確輸入上叫九次的訊息,三十次之後就是背景雜訊 ——
   *   而那一格與「非 other 必須有單號」共用同一個位置,**會一起被無視**。
   * ⇒ 呼叫端用 `onBlur`(離開欄位)把它切成 `true`。
   */
  settled = true,
): TrackingIssue | null {
  const t = trackingNumber.trim();
  if (t === '') return null;
  if (!settled) return null;
  // 🔴 只有 hct 驗。`sf` = 規則未查證(不是豁免)、`other` = 本來就沒有貨號。
  if (carrierCode !== 'hct') return null;

  const name = CARRIER_LABEL.hct;
  // ⚠️ 訊息裡的貨運商名讀同一份表,不硬寫 —— 同一片十行前才立過這條規矩
  //    (`shipment-dialog.tsx` 錨點「驗證文案裡的貨運商名也讀同一份表」)。R1 MF6。

  // 【擋】長度 —— 四處、跨兩個章節印證,是本檔證據最硬的一條。
  if (t.length !== 10) {
    return { level: 'block', message: `${name}的貨號是 10 碼,這個是 ${t.length} 碼。請對著託運單再確認一次。` };
  }
  // 【警告】字元集 —— 只有一處寫過,與檢查碼同一張表的相鄰兩行 ⇒ 與檢查碼同級,不擋。
  if (!/^\d{10}$/.test(t)) {
    return { level: 'warn', message: `${name}的貨號照文件應該是純數字,這個不是。${TAIL}` };
  }
  const expected = hctCheckDigit(t.slice(0, 9));
  if (Number(t[9]) !== expected) {
    // R2 F-K:帶上貨運商名 —— 同一頁可能有別家的琥珀訊息並排。
    return {
      level: 'warn',
      message: `${name}貨號的檢查碼對不上(第 10 碼照規則應該是 ${expected})。${TAIL}`,
    };
  }
  return null;
}
