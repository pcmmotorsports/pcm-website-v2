/**
 * test-accounts — **會影響「今日對帳」那三個數字**的測試帳號清單(⟦b4-TESTACCT1⟧)。
 *
 * 🔴🔴 **它【不是】「所有測試帳號」的清單, 而那個差別是 code-reviewer 抓到的**:
 *    正式站 auth 裡至少還有一個 `g3-sandbox-test@pcmmotorsports.com`
 *    (板列 `⟦b4-TESTACCT1⟧`)—— 🔬 而 2026-09-05 唯讀實查:**它 0 張單 / 0 筆收款**
 *    ⇒ 📌 **把它加進來會讓畫面說「2 個帳號」, 而它對那三個數字一點影響都沒有**
 *      ⇒ 那是把一句本來準確的話變成誇大。**所以它刻意不在這裡。**
 * 🛑 **⇒ 要把這個常數用在別的地方之前(例如客戶數的標示), 先回頭看這一段** ——
 *    那時它會少算掉那個帳號, 而少算的正好是這整片在講的那一種東西。
 *
 * ## 為什麼有這支檔
 * 2026-09-05 唯讀實查正式庫:`bsas0830@gmail.com` 有 **1 張單 / 1 筆收款**,
 * 而**全站當時只有 2 張單** ⇒ 它佔一半;`order_paid_totals_v` 全表 **1 列就是它**。
 * ⇒ 🔴 **員工看到的每一個今日數字, 在那之前都含它, 而畫面上沒有任何一句話說。**
 *
 * ## Sean 2026-09-05 拍板(逐字)
 * > `Q-測試帳號: 乙 = 留著, 後台加一句「含測試資料」`
 *
 * 🛑 **那張單【不刪】** —— 它是 2026-08-18 最後一次真人端到端刷卡(而它失敗了)的唯一證據;
 *    刪掉它等於刪掉那次失敗的唯一紀錄。
 *
 * ## 🔴 這個常數怎麼用(而它的形狀就是它的退場機制)
 * 畫面那句話的顯示條件 = **`TEST_ACCOUNT_EMAILS.length > 0`**。
 * ⇒ 📌 **測試單清掉的那一天, 把這個陣列清空, 那句話就自己消失** ——
 *    不需要有人記得回去刪 UI, 也**不需要在畫面上多查一次資料庫**。
 * 🛑 **而它的代價要明寫**:這個陣列與正式庫**沒有任何對帳** ——
 *    它說「有 N 個測試帳號」, 而**它不知道那些帳號今天還有沒有單**。
 *    ⇒ 所以畫面上的字是「**可能含**」的語氣, 不是「這個數字裡有 X 元是假的」。
 *
 * ⚠️ **不硬寫進 UI 字串**(主視窗 `-f8` 2026-09-05 交辦逐字)——
 *    email 是 PII, 而告警/截圖會被轉發。畫面只印**個數**, 不印 email。
 */

/**
 * 我們自己測試用的帳號 email。
 *
 * 🔵 **清空這個陣列 = 讓畫面那句話消失。** 那是刻意的退場路徑, 不是漏寫。
 */
export const TEST_ACCOUNT_EMAILS: readonly string[] = [
  // 2026-08-18 tappay sandbox 端到端測試(座標 docs/probes/2026-08-18-tappay-sandbox-charge-log.md)
  'bsas0830@gmail.com',
];

/**
 * 客戶【數】那一面要用的清單 —— 它比上面那個**多一個**, 而那不是筆誤。
 *
 * 🔴🔴 **為什麼要第二個常數(理由就寫在本檔上半, 由它親自交辦下一個人)**:
 *    上面 `TEST_ACCOUNT_EMAILS` 的定義是「**會影響今日對帳那三個數字**的帳號」——
 *    `g3-sandbox-test@pcmmotorsports.com` **0 張單 / 0 筆收款** ⇒ 它對那三個數字沒有影響,
 *    所以**刻意**不在那個陣列裡(把它加進去會讓「N 個帳號」誇大)。
 * 🛑 **而「客戶數」是另一個分母** —— 它數的是 `customers` 的列, 不是訂單。
 *    🔬 2026-09-05 線【身分】`-auth` 唯讀實查:**兩個帳號在 `customers` 都在**
 *    (`8d402365…` / `c2707561…`)⇒ 📌 **客戶列表那個「共 N 位」裡, 兩個都算進去了。**
 *    ⚠️ **那個讀數不是我量的**(本窗無正式庫存取)—— 出處板列 `⟦b4-TESTACCT1⟧`。
 * 🎯 **⇒ 用上面那個常數去標客戶數, 會【少算一個】** —— 而少算的正好是這整片在講的東西。
 *
 * 🔵 **形狀刻意寫成【展開上面那個】而不是各自列一份** ——
 *    測試單清掉那天有人去改上面那個陣列, 這裡**自動跟著變**;
 *    兩份各自硬寫的世界裡, 改一邊而忘了另一邊**不會有任何訊號**。
 */
export const TEST_ACCOUNT_EMAILS_IN_CUSTOMER_COUNT: readonly string[] = [
  ...TEST_ACCOUNT_EMAILS,
  // 2026-08-18 tappay sandbox 測試帳號:0 張單 ⇒ 不影響對帳三卡, 而它【是一位客戶】
  'g3-sandbox-test@pcmmotorsports.com',
];
