// app/@panel/[...catchAll]/page.tsx — #350c:槽的萬用配對,一律回 `null`。
//
// 🔴 **它擋的是「跨區塊導航時面板黏著不放」**,不是可有可無的樣板檔:
//    平行路由槽在**軟導航**到一個「槽沒有配對內容」的 URL 時,會**保留槽的上一個狀態**
//    (`default.tsx` 只在硬導航 / 重新整理時生效)。沒有本檔的話,員工在 `/orders?panel=x`
//    開著面板、然後按側欄切到「客戶」⇒ 客戶頁右邊會**繼續掛著那張訂單**。
//    2026-08-10 真瀏覽器實測:有本檔 ⇒ 切到 `/customers` 面板確實清空(`D-403-Q` §① 表第 4 列)。
//
// ⚠️ **它擋不住什麼**(不要讀過頭):同一份實測也證明,在**攔截路由**架構下本檔對
//    「回到被攔截路由的父列表」**完全無效**。#350c 最後沒有用攔截路由,那條路徑改由
//    `app/@panel/orders/page.tsx` 自己讀 `panel` param 決定 —— 本檔只負責「其他所有 URL」。
export default function WorkspacePanelCatchAll() {
  return null;
}
