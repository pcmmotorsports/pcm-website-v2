/**
 * `@panel` 平行路由槽的 **default** —— #350b 工作區殼。
 *
 * 🔴🔴 **這支不是可有可無的樣板檔,它是這個機制的頭號坑**(主視窗 `D-391-A:5` Q1=A 段逐字:「`default.tsx` 漏寫會炸=寫進驗收+守門」):
 * Next 的平行路由在**軟導航**時會保留槽的上一個狀態,但在**硬導航 / 重新整理**時,
 * 找不到與當前 URL 相符的槽內容就會去找 `default.tsx` —— **沒有它就是 404**。
 * 症狀特別惡劣:開發時一路點都正常(全是軟導航),**使用者一按 F5 整頁就掛**。
 *
 * ⇒ 回 `null` = 「這個 URL 沒有面板要開」。殼那邊靠 CSS `:has()` 看出槽是空的,
 * 把面板欄與拖曳把手一起收起來(規則與理由在 `globals.css`)。
 *
 * ⚠️ **守門擋得住什麼**:`workspace-shell.test.ts` 有一格釘住**本檔存在**(刪掉 ⇒ 紅)。
 * **擋不住**:本檔還在、但內容被改成回一個會炸的東西,或改成回非 `null` 的殘留 UI ——
 * 那要真瀏覽器按 F5 才看得到,已掛進 350c 的真瀏覽器驗收清單(主視窗 `D-393-A` 核准)。
 * **不要把「有這個檔」讀成「重新整理已驗證」。**
 *
 * 🔴 **本檔仍然只服務硬導航**;軟導航離開時清空槽的是 `[...catchAll]/page.tsx`,兩者不可互相取代
 * (`default.tsx` 在軟導航時**不會**被重新套用 —— 2026-08-10 真瀏覽器實測,`D-403-Q` §①)。
 *
 * 🔴 **2026-08-10 更正**:350b 寫在這裡的「350c 用 intercepting route 接(`(.)orders/[id]` 之類)」
 * **已作廢** —— 攔截路由實測會讓面板黏著不放,主視窗改裁 searchParams 驅動。
 * 真正的面板內容在 `app/@panel/orders/page.tsx`(讀 `?panel=<uuid>`);**這個目錄下沒有攔截路由,別去找。**
 * 本檔仍然刻意不放任何訂單概念(面板是共用基礎設施,wave-plan `:23`)。
 */
export default function WorkspacePanelDefault() {
  return null;
}
