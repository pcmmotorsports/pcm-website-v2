{
  "name": "@pcm/admin",
  "version": "0.0.0",
  "private": true,
  "description": "PCM 後台 admin(Next.js 自寫、對齊前台 design)— 殼、待 M-4a-01 起逐 slice 裝框架",
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern"
  }
}
