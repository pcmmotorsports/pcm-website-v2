{
  "name": "@pcm/api",
  "version": "0.0.0",
  "private": true,
  "description": "PCM 後台 API(自寫、Supabase 直連)— 殼、待 M-1-03-main-a+ 裝框架(對齊 ADR-0005)",
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern"
  }
}
