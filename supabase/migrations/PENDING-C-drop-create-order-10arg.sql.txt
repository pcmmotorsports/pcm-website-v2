-- pcm:never-apply-until-B-deployed
-- ⚠️ **副檔名是 `.sql.txt` 不是 `.sql` —— 那是刻意的**:
--    放進 `supabase/migrations/*.sql` 會被靜態閘、`state-gates`、以及任何「掃 migrations」的東西
--    當成一支真的 migration。而它**還不能被貼**。
--    ✅ 要動它的人:改名成 `20260904XXXXXX_m4b_drop_create_order_10arg.sql` 再寫內容。
--
-- ============================================================
-- 段 1-C:DROP 掉 `create_order` 的 10 參數版
-- ============================================================
-- 🔴 **這是三步部署的最後一步, 而它【不能提前】。**
--
-- ── 前置(缺一不可, 而它們不是清單是條件)──────────────────────────
--   ① A 已貼(`20260904020000_m4b_create_order_payment_channel.sql`)
--   ② **B 已部署到顧客站, 而且【確認生效】** —— 不是「合併了」是「線上真的在送 11 個名字」
--   🔴 ③ **觀察期** —— 而理由是具體的:
--      仍在途的舊部署 / CDN 快取 / 使用者已開著的分頁, **都還在送 10 個名字**;
--      而這一支一 DROP, 它們**沒有任何匹配** ⇒ `PGRST202` ⇒ **那些人的結帳當場掛掉**。
--      ⇒ 🎯 **⇒ 判別訊號不是「過了幾天」, 是【正式庫最近一筆 10 名呼叫是什麼時候】**
--         ⇒ 而那個數字今天沒有人在量 ⇒ **要量它, 或者退而求其次:等到有把握的長度。**
--
-- ── 🛑 而【回退不是對稱的】, 這一格最容易被做錯 ─────────────────────
--   一旦 DROP:
--     · app 若要 rollback 回舊版(送 10 個名字)⇒ **沒有落點** ⇒ 結帳全掛
--   ⇒ ✅ **⇒ 所以正確順序永遠是:先恢復舊函式, 才能退 app。**
--   ⇒ 🔴 **⇒ 而那意味著這一支要附一份【重建舊函式的 SQL】** —— 不是只寫 DROP。
--      (舊那支的 prosrc 在 `20260604130000` 那一代之後被改過多次;
--       重建要用**正式庫當下的 `pg_get_functiondef`**, 不是 repo 檔。)
--
-- ── 內容(等 B 部署確認之後再寫)────────────────────────────────
--   1. 前置閘:斷言【新簽名存在】且【舊簽名存在】(否則這一支的前提不成立)
--   2. 🔴 前置閘:斷言舊那支的 `prosrc` 指紋 = 我們要 DROP 的那一份
--      ⇒ 否則它可能在 A 之後被別人 hotfix 過, 而我們會 DROP 掉一個沒看過的東西
--   3. `DROP FUNCTION public.create_order(jsonb,uuid,text,jsonb,uuid,text,text,text,text,text);`
--   4. 事後斷言:只剩一支、且是 11 參數那支、且 ACL 沒漂
--   🔴 **5. 而【C 一定要回頭改 allowlist】** ——
--      `apps/admin/src/lib/orders/subtotal-writers-allowlist.test.ts` 現在同時登記了
--      舊那支那一族與 `20260904020000`。C 把舊的 DROP 掉之後, 那些舊條目**是否還算寫入者**要重判。
--      🛑 **而那道閘【兩個方向都抓】** —— 它自己的註解逐字記過「同一天紅了兩次, 兩次都是【少一個】」
--      ⇒ 🎯 **⇒ C 不改那裡的話, 它會紅。而那是對的, 不是誤報。**
--
-- ── 📎 相關 ───────────────────────────────────────────────────
--   A  supabase/migrations/20260904020000_m4b_create_order_payment_channel.sql
--   而 A 的檔頭有三步的全貌、名字集合為什麼是分辨器、以及 PostgREST 實測數據。
--
-- 🔵 **這支檔存在的理由**:C 的內容現在在我腦子裡, 而三小時後不在。
--    ⇒ 📌 **一支放在那裡的檔, 比一個「有人要記得」可靠。**
