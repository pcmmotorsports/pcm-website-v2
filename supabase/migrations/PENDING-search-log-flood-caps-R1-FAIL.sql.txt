-- ⛔⛔ **這支【不是】可以貼的 migration —— 檔名是 `PENDING-…sql.txt` 就是為了讓它貼不出去。**
--
-- 🔴 **codex 關卡2(鐵則 12②③, 不降級)2026-09-05 判 `FAIL`, 至少 12 條 must-fix。**
--    完整輸出 = 本窗 `/tmp/codex-flood.out`(會隨機器清掉);要點抄在下面。
--
-- 🛑🛑 **最重的那一條 —— 它不是 bug, 是【設計方向錯了】(codex 第 4 條)**:
--    我用 `WHERE (SELECT count(*) …) < 5000` 當上限。而
--      · 從 0 灌到 5000, 累計要看約 **12,497,500** 個索引項
--      · **滿額之後, 每一發惡意 RPC 仍然掃約 5000 項, 卻一列都不寫**
--    ⇒ 🎯 **這道防線把「便宜的寫入」換成「不寫但要掃」** —— 攻擊者的成本沒變, 我們的變貴了。
--    ⇒ **防灌爆的東西自己變成放大器。** 要的是**原子計數器**, 不是每次 count。
--
-- 🔴 **第二重:上限根本不是硬上限**(第 1、2 條)—— Read Committed 之下 k 個並發都讀到 4999
--    ⇒ 實際會變成 `4999+k`。而「可接受超額多少」我**沒有量、也沒有訂** ⇒ 未確認。
--
-- 🛑 **而我在檔裡寫錯了一件事實, 留著當紀錄**(第 8 條):
--    ⛔ ~~「既有六支 job 都走 `pcm_cron.invoke_cron_route`, 本支直接跑 SQL ⇒ 偏離」~~
--    ⇒ **那句是假的**:`20260809170000_m4b_lifecycle_l3b_expire_unpaid_orders_schedule.sql:77`
--      就是一支**純 SQL** 的 `pcm-expire-unpaid-orders`。
--    ⇒ 📌 **我為一個【不存在的偏離】寫了一整段辯護** —— 而正確做法是**照抄那支先例**
--      (它會釘 `current_user=postgres`、函式 owner/ACL、job 的 username/database/schedule/command;
--       我只驗了同名 active)。
--
-- 🔴 **我自己那段算式的統計錯誤**(第 13 條):`2,000,000 / 17,520 = 114.155` 這個除法對,
--    而我拿「平均 114」去比「合法**尖峰** 1,000」—— **那是兩個不同的統計量**, 尖峰高不代表平均高。
--    ⇒ ✅ 能成立的只有一句:**「兩年到不了」尚未被證明**。⛔ ~~「差了近一個量級」~~ 是超射。
--    ⚠️ 另外 `interval '2 years'` 也不固定等於 730 天。
--
-- 其餘 must-fix(照抄, 下一輪逐條處理):
--   · purge 的 `DELETE … WHERE id IN (… OFFSET 2000000)` 在 200 萬列上是一個大交易
--     ⇒ 要有 batch 大小、最長執行時間、WAL 上界、積欠恢復策略(第 6、7 條)
--   · 新 job **沒有接進既有心跳** ⇒ **它失敗了沒有人會知道**(第 10 條;白名單只有六支)
--   · 事後閘有弱格與近恆真格:①只找 `prosrc` 裡的 `%< 5000%`(註解留著也綠)、
--     ②同交易內非條件 `CREATE INDEX` 之後必然成立、③a 被前面的 CREATE 間接保證(第 11 條)
--   · 反向對照⑤ 只證「anon 有有效 EXECUTE」—— 權限來自 PUBLIC 或角色繼承也會綠(第 12 條)
--   · rollback 照著貼**還原不回去**:舊版是 `CREATE FUNCTION`, 函式還在時會 already exists;
--     且 by-name schedule 可能覆寫既有同名 job 而 rollback 直接 unschedule(第 15、16 條)
--
-- ✅ **而下面這些是【已經驗過】的, 下一輪不用重跑**:
--    · `scripts/migration-new-file-static-checks.sh` 五道全過
--    · 本機拋棄式 PG 17.10(pg_cron 是樁):乾淨貼 rc=0;上限之下寫進 1 列;
--      灌到 5000 後再叫 ⇒ 一列都不加、不報錯;purge 保留期那半 deleted_expired=3;
--      總量那半改上限 10 ⇒ 刪 4990、剩 10 列且是 id 最大的十列(**留新刪舊方向正確**)
--    · 拋棄式庫當場抓到我一個錯:事後閘③ 原比 `ARRAY['search_path=']`, 實際存的是
--      `search_path=""` ⇒ 那道閘會對正確的函式**誤報**。已修成三格分開判。
--
-- ============================================================

-- ============================================================
-- 20260904280000 · 搜尋語料表的兩道上限(`⟦search-LOGFLOOD⟧`)
--
-- 🔴 病(線 `-front` 2026-09-05 量的):`anon` 可直接打 `/rest/v1/rpc/log_search_query`,
--    **換一個字就是一列, 無上限**;而表**零保留期、零總量上限**,
--    且 `service_role` 只有 `SELECT` ⇒ 🛑 **灌爆了連清都不能清。**
--
-- 本檔做三件事,而**第三件是刻意偏離既有慣例**:
--   ① `log_search_query` 內加【每小時全域上限】⇒ 撞到就 `DO NOTHING`
--   ② 新增 `purge_search_queries()`:刪過期 + 刪超過總量上限的最舊
--   ③ 掛 `pg_cron` —— 🔴 **既有六支 job 都走 `pcm_cron.invoke_cron_route('/api/cron/…')`,
--      而本支【直接跑 SQL】** ⇒ 偏離。理由:純 DB 清理不需要 HTTP 那一圈,
--      而多開一條路由 = 多一個要驗的授權面。**偏離寫在這裡, 不是藏起來。**
--
-- 🔴🔴 **`anon` 的 EXECUTE 一個字不動** —— 那是設計(不記身分、客人端直接呼叫),不是漏洞。
--
-- ============================================================
-- 🛑 N = 5,000 是【反推的上界, 不是量出來的門檻】
-- ============================================================
-- 唯讀實查 2026-09-05:`pg_class.reltuples = -1`(從沒 analyze)· `n_live_tup = 0`
--   · `last_autoanalyze` 空 · `pg_total_relation_size = 48 kB`(≈ 空表 + 兩個索引)
--   🟢 正對照:同一句對 `products` ⇒ **25,038**(尺是活的)
--   ⛔ 而**表本身唯讀角色讀不到**(`permission denied`, 只有 `service_role` 有 SELECT)
--      ⇒ **上面是目錄層估計, 不是逐列數。**
-- 🎯 ⇒ **「從今天的量推 N」做不到 —— 今天的量是零, 這張表 2026-09-04 夜才上線。**
-- ✅ 反推:一列 = 一個(相異字 × 路徑 × 小時)。站上訂單總數今天是 **1**;
--    就算哪天忙到 200 人各搜 5 個不同的字, 也才 **1,000 列/小時** ⇒ 取 **5,000**(5 倍餘裕)。
-- ⏰ **有真流量之後要重量一次** —— 那時 `n_live_tup` 才會是個數字。
--
-- ============================================================
-- 🔴 總量上限 200 萬:算式在這裡, 而**它不是「合法流量到不了」**
-- ============================================================
-- 2 年 = 730 天 × 24 = **17,520 小時**;2,000,000 ÷ 17,520 = **114 列/小時**。
-- 🛑 **⇒ 平均一超過 114 列/小時, 這道上限就會在【不到 2 年】的時候開始刪。**
--    而我上面反推的合法尖峰是 1,000 列/小時 ⇒ **兩者不是同一個量級。**
-- ⇒ 📌 **所以「保留 2 年」與「總量上限」在【有流量之後】會打架, 不會自己消失。**
--    ⚠️ **不要把 200 萬讀成「兩邊都滿足了」** —— 它只是**今天(零流量)看不到衝突**。
-- 🟢🟢 **Sean 2026-09-05 拍【甲 總量優先, 滿了刪最舊】**(原話逐字,
--    正本 `~/pcm-mailbox/Sean拍板-20260905.md` 第一題):
--      「甲 = 總量優先, 滿了刪最舊 (推薦: 上限設成正常流量兩年也到不了, 今天兩者不衝突)」
--    ⇒ 📌 **他拍的是【優先順序】, 而那個優先順序自此有授權。**
--
-- 🛑🛑 **而那句推薦裡的括號【我的算式不支持】—— 照實寫, 不要讓它被讀成已驗證**:
--    「上限設成正常流量兩年也到不了」**只在平均 < 114 列/小時的時候成立**(算式在上面),
--    而我反推的合法尖峰是 **1,000 列/小時** ⇒ **差了近一個量級。**
--    ⇒ 🎯 **今天(零流量)確實不衝突, 而「兩年也到不了」不是一個已經被證明的性質。**
--    ⇒ ⏰ **重讀時機**:`n_live_tup` 第一次變成真的數字那天 —— 那時把 114 那個門檻拿去對。
--    🔵 而**這不影響他那一板**:滿了刪最舊, 本來就是為了「真的滿了」那一天而拍的。
--    改上限只要改下面那個常數一處。
-- ============================================================

BEGIN;

-- ── 前置閘:確認我要改的那一版【就是正式庫在跑的那一版】────────────────────
-- 🔴 `CREATE OR REPLACE` 會把別人的熱修**靜靜蓋掉** ⇒ 先釘本體 md5。
--    (2026-09-05 唯讀實測:線上 = 檔內 = `fe38973f299f02b15f45f07243d68579`。)
DO $$
DECLARE v_md5 text;
BEGIN
  SELECT md5(p.prosrc) INTO v_md5
    FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
   WHERE n.nspname = 'public' AND p.proname = 'log_search_query';
  IF v_md5 IS DISTINCT FROM 'fe38973f299f02b15f45f07243d68579' THEN
    RAISE EXCEPTION '前置閘:log_search_query 本體不是我預期的那一版(實際 %)⇒ 有人改過, 停下來讀', v_md5;
  END IF;
END $$;

-- ── 1. 每小時上限要走得動 ⇒ 先給 created_at 一支索引 ───────────────────────
-- 🔴 既有那道 UNIQUE 是 `(query_raw, path, created_at)` —— **前導欄不是 `created_at`**
--    ⇒ 對「這一小時幾列」那種查詢**用不上** ⇒ 少了這支索引, 每一次 insert 都要掃全表。
CREATE INDEX search_queries_created_at_idx
  ON public.search_queries (created_at);

-- ── 2. 函式:加每小時全域上限 ──────────────────────────────────────────────
-- 🛑 **本體其餘部分逐字不動**(遮 email / 兩端截斷 / ON CONFLICT 那三段照抄)。
CREATE OR REPLACE FUNCTION public.log_search_query(
  p_query_raw    text,
  p_path         text,
  p_unmatched    text    DEFAULT NULL,
  p_result_count integer DEFAULT NULL
) RETURNS void
LANGUAGE sql
SECURITY DEFINER
SET search_path = ''
AS $fn$
  INSERT INTO public.search_queries (query_raw, path, unmatched, result_count)
  SELECT
    -- 🔵 兩端都截:CHECK 會擋掉過長的, 而**擋掉 = 那一筆語料消失**
    --    ⇒ 這裡先截成合法的, 讓它進得去。截斷長度與 CHECK 一致(100)。
    -- 🔴🔴 **遮掉 email(2026-09-04 codex must-fix ②)** —— 而它做在【DB 這一層】,
    --    不是做在 TS:呼叫端可能不只一個, 而遮罩漏在任何一個呼叫端上都沒有東西會叫。
    pg_catalog.left(pg_catalog.btrim(public.mask_obvious_pii(p_query_raw)), 100),
    p_path,
    pg_catalog.left(public.mask_obvious_pii(p_unmatched), 100),
    p_result_count
  -- 🔴🔴 **每小時全域上限**(`⟦search-LOGFLOOD⟧`, 2026-09-05)——
  --    撞到就【一列都不寫】, 而**它與下面那道 `ON CONFLICT` 是同一種安靜**:
  --    呼叫端是 fire-and-forget, 這裡 RAISE 只會讓客人的搜尋多一發被吞掉的錯。
  --    🛑 **代價明寫**:被擋掉的那一小時**語料是缺的**, 而**沒有任何東西會告訴我們它缺了**
  --       ⇒ 要知道有沒有撞到上限, 只能去看那一小時的列數是不是剛好等於上限。
  WHERE (
    SELECT pg_catalog.count(*)
      FROM public.search_queries s
     WHERE s.created_at = pg_catalog.date_trunc('hour', pg_catalog.now())
  ) < 5000
  -- 🔴 撞到那道 UNIQUE 就靜靜跳過 —— **不得讓它變成一個 error**:
  --    呼叫端是 fire-and-forget, 而「同一小時第二次搜同一個字」是**完全正常的客人行為**,
  --    讓它進 log 會把真的失敗淹掉。
  ON CONFLICT (query_raw, path, created_at) DO NOTHING;
$fn$;

-- 🔴 `CREATE OR REPLACE` **保留既有 ACL** ⇒ 上一支的 REVOKE/GRANT 仍然成立。
--    而下面的事後閘會**逐格重驗**, 不靠這句話。

-- ── 3. purge 函式 ─────────────────────────────────────────────────────────
CREATE FUNCTION public.purge_search_queries()
RETURNS TABLE (deleted_expired bigint, deleted_overflow bigint)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = ''
AS $fn$
DECLARE
  -- 🔴 保留期:Sean 2026-08-21 拍「搜尋紀錄保存 2 年」。
  c_retention CONSTANT interval := interval '2 years';
  -- 🔴 總量上限:**待 Sean 拍**(檔頭那一節有算式與衝突說明)。
  c_max_rows  CONSTANT bigint := 2000000;
  v_expired  bigint := 0;
  v_overflow bigint := 0;
BEGIN
  DELETE FROM public.search_queries
   WHERE created_at < pg_catalog.now() - c_retention;
  GET DIAGNOSTICS v_expired = ROW_COUNT;

  -- 🔴 只刪【超出上限的最舊那些】—— 用 id 排序而不是 created_at:
  --    `created_at` 被截到整點 ⇒ **同一小時的列它分不出先後**, 而 `id` 是 IDENTITY, 嚴格遞增。
  DELETE FROM public.search_queries
   WHERE id IN (
     SELECT s.id FROM public.search_queries s
      ORDER BY s.id DESC
      OFFSET c_max_rows
   );
  GET DIAGNOSTICS v_overflow = ROW_COUNT;

  RETURN QUERY SELECT v_expired, v_overflow;
END;
$fn$;

-- 🔴 函式**出生就對 PUBLIC 開 EXECUTE** ⇒ 先收再給。
-- 🛑 這一支**誰都不給** —— 它只由 `pg_cron`(postgres 身分)呼叫。
REVOKE ALL ON FUNCTION public.purge_search_queries() FROM PUBLIC;
REVOKE ALL ON FUNCTION public.purge_search_queries() FROM authenticated;
REVOKE ALL ON FUNCTION public.purge_search_queries() FROM anon;
REVOKE ALL ON FUNCTION public.purge_search_queries() FROM service_role;

-- ── 4. 排程 ───────────────────────────────────────────────────────────────
-- 🔴 by-name upsert 冪等 + 顯式 active(慣例抄 `20260723120000:128-133`)。
-- ⚠️ **偏離**:既有六支都呼 `pcm_cron.invoke_cron_route(...)`, 本支直接跑 SQL(理由見檔頭)。
DO $$
DECLARE v_id bigint;
BEGIN
  v_id := cron.schedule('pcm-search-log-purge', '17 3 * * *',
    $job$SELECT public.purge_search_queries()$job$);
  PERFORM cron.alter_job(job_id => v_id, active => true);
END $$;

-- ── 事後閘(fail-closed:任一格不成立 ⇒ 整檔 ROLLBACK)──────────────────────
DO $$
DECLARE v_cnt int; v_ok boolean;
BEGIN
  -- ① 每小時上限那句真的進去了(而不是我以為進去了)
  SELECT pg_catalog.count(*) INTO v_cnt
    FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
   WHERE n.nspname = 'public' AND p.proname = 'log_search_query'
     AND p.prosrc LIKE '%< 5000%';
  IF v_cnt <> 1 THEN RAISE EXCEPTION '事後閘①:log_search_query 裡找不到每小時上限'; END IF;

  -- ② 索引在(否則上面那個 count 每次掃全表)
  IF NOT EXISTS (SELECT 1 FROM pg_indexes
                  WHERE schemaname='public' AND indexname='search_queries_created_at_idx') THEN
    RAISE EXCEPTION '事後閘②:created_at 索引不在';
  END IF;

  -- ③ purge 是 SECURITY DEFINER 且 search_path 鎖空
  -- 🔴🔴 **這一格第一版寫錯了, 而【拋棄式 PG 當場抓到】** —— 留著當紀錄:
  --    ⛔ ~~`p.proconfig @> ARRAY['search_path=']`~~ ⇒ **存進去的字面是 `search_path=""`(帶兩個引號)**
  --    ⇒ 那個比對【永遠不成立】⇒ 這道閘會對一支**完全正確**的函式叫。
  --    📌 **而它壞的方向是【誤報】** —— 誤報會被關掉, 而關掉之後對真的該叫的那一種也失效。
  -- 🛑 **而第一版的訊息把【三個世界】寫成同一句**(函式不存在 / 不是 DEFINER / search_path 沒鎖)
  --    ⇒ 那正是我今晚一直在抓的病 ⇒ 拆成三句。
  IF NOT EXISTS (SELECT 1 FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
                  WHERE n.nspname='public' AND p.proname='purge_search_queries') THEN
    RAISE EXCEPTION '事後閘③a:purge_search_queries 根本沒建成';
  END IF;
  SELECT p.prosecdef INTO v_ok
    FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
   WHERE n.nspname='public' AND p.proname='purge_search_queries';
  IF v_ok IS NOT TRUE THEN RAISE EXCEPTION '事後閘③b:purge 不是 SECURITY DEFINER'; END IF;
  SELECT p.proconfig @> ARRAY['search_path=""'] INTO v_ok
    FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
   WHERE n.nspname='public' AND p.proname='purge_search_queries';
  IF v_ok IS NOT TRUE THEN RAISE EXCEPTION '事後閘③c:purge 的 search_path 沒鎖成空'; END IF;

  -- ④ purge 四個角色都沒有 EXECUTE(正向:確認它真的收乾淨了)
  IF pg_catalog.has_function_privilege('anon', 'public.purge_search_queries()', 'EXECUTE')
     OR pg_catalog.has_function_privilege('authenticated', 'public.purge_search_queries()', 'EXECUTE')
     OR pg_catalog.has_function_privilege('service_role', 'public.purge_search_queries()', 'EXECUTE') THEN
    RAISE EXCEPTION '事後閘④:purge 還有角色叫得動';
  END IF;

  -- ⑤ 🔵 **反向對照** —— 少了這一格, 「全部 REVOKE 掉」與「本來就沒開過」印同一個綠。
  --    `log_search_query` 對 `anon` **必須還在**(那是設計, 本檔不得動到它)。
  IF NOT pg_catalog.has_function_privilege(
           'anon', 'public.log_search_query(text, text, text, integer)', 'EXECUTE') THEN
    RAISE EXCEPTION '事後閘⑤:anon 對 log_search_query 的 EXECUTE 不見了 ⇒ 客人端會靜靜地全部寫不進去';
  END IF;

  -- ⑥ 排程掛上且 active
  SELECT pg_catalog.count(*) INTO v_cnt FROM cron.job
   WHERE jobname = 'pcm-search-log-purge' AND active;
  IF v_cnt <> 1 THEN RAISE EXCEPTION '事後閘⑥:pcm-search-log-purge 沒掛上或不是 active'; END IF;
END $$;

-- 新物件收權斷言(樣板逐字抄自 `20260904240000:135-228`, 只換清單 —— 🔴 不重寫, 重寫會漂)
DO $newobj_guard$
DECLARE
  -- 🔴 結尾的 ::text[] 不能拿掉 —— 清單清空時 ARRAY[] 無法推斷型別。
  v_relations text[] := ARRAY[]::text[];
  v_functions text[] := ARRAY[
    'public.purge_search_queries()'
  ]::text[];
  v_declares_nothing boolean := false;

  r          text;
  v_oid      oid;
  v_bad      int := 0;
  v_first    text;
  v_checked  int := 0;
  v_priv     text;
  v_col      text;
BEGIN
  IF cardinality(v_relations) = 0 AND cardinality(v_functions) = 0 THEN
    IF NOT v_declares_nothing THEN
      RAISE EXCEPTION
        '新物件收權斷言:兩份清單都是空的。本檔若真的沒新建物件，請把 v_declares_nothing 設成 true（明示），不要留空。';
    END IF;
    RAISE NOTICE '新物件收權斷言:本檔明示未新建任何物件，略過（已留痕）。';
    RETURN;
  END IF;

  FOREACH r IN ARRAY v_relations LOOP
    v_oid := pg_catalog.to_regclass(r);
    IF v_oid IS NULL THEN
      RAISE EXCEPTION '新物件收權斷言:找不到關聯 % —— 名字打錯或沒建成。拒繼續。', r;
    END IF;
    v_checked := v_checked + 1;
    -- 🔴🔴 **權限清單由伺服器推導,不手寫**(codex 2026-08-17 `must-fix`)。
    --    E-684 樣板手寫七種,而 **PG 17 有八種 —— 少了 `MAINTAIN`**。
    --    而本檔自己的病構造輸出就印著 `anon=MAINTAIN` ⇒ **樣板掃不到它自己舉的那個例子。**
    --    改成從 `acldefault('r', owner)` 推導:PG 之後再加第九種,這一臂自動入列。
    --    📎 同一個修法 B1-b 已經走過(`docs/specs/2026-08-16-m4b-e8b-b1b-migration-draft.sql:343-346`)。
    --    🔴 `DISTINCT` 在這裡是承重的:這是迴圈,同一權限型別出現兩次會讓 v_bad 多加一次。
    FOR v_priv IN
      SELECT DISTINCT d.privilege_type
        FROM aclexplode(acldefault('r', (SELECT relowner FROM pg_class WHERE oid = v_oid))) d
    LOOP
      IF has_table_privilege('anon', v_oid, v_priv)
         OR has_table_privilege('authenticated', v_oid, v_priv) THEN
        v_bad := v_bad + 1;
        IF v_first IS NULL THEN v_first := format('%s 上仍有 %s', r, v_priv); END IF;
      END IF;
    END LOOP;

    -- 🔴🔴 欄級授權必須另外問 —— `has_table_privilege` 對【只有欄級授權】的情況回 false。
    FOR v_col IN
      SELECT a.attname FROM pg_attribute a
       WHERE a.attrelid = v_oid AND a.attnum > 0 AND NOT a.attisdropped
    LOOP
      FOREACH v_priv IN ARRAY ARRAY['SELECT','INSERT','UPDATE','REFERENCES'] LOOP
        IF has_column_privilege('anon', v_oid, v_col, v_priv)
           OR has_column_privilege('authenticated', v_oid, v_col, v_priv) THEN
          v_bad := v_bad + 1;
          IF v_first IS NULL THEN
            v_first := format('%s.%s 上仍有【欄級】%s', r, v_col, v_priv);
          END IF;
        END IF;
      END LOOP;
    END LOOP;
  END LOOP;

  FOREACH r IN ARRAY v_functions LOOP
    v_oid := pg_catalog.to_regprocedure(r);
    IF v_oid IS NULL THEN
      RAISE EXCEPTION '新物件收權斷言:找不到函式 % —— 簽名打錯或沒建成。拒繼續。', r;
    END IF;
    v_checked := v_checked + 1;
    IF pg_catalog.has_function_privilege('anon', v_oid, 'EXECUTE')
       OR pg_catalog.has_function_privilege('authenticated', v_oid, 'EXECUTE') THEN
      v_bad := v_bad + 1;
      IF v_first IS NULL THEN v_first := format('%s 上仍有 EXECUTE', r); END IF;
    END IF;
  END LOOP;

  IF v_checked = 0 THEN
    RAISE EXCEPTION '新物件收權斷言:檢查數為 0 —— 這個斷言沒有分母，不算通過。';
  END IF;

  IF v_bad > 0 THEN
    RAISE EXCEPTION
      E'❌ 新物件收權斷言失敗:anon/authenticated 仍持有 % 項權限（第一個:%）。檢查了 % 個物件。\n'
      '   ⇒ 補上:REVOKE ALL ON <物件> FROM PUBLIC, anon, authenticated;\n'
      '   🔴 兩道都要下:FROM PUBLIC 收不到具名授權，FROM 具名 收不到 PUBLIC 授權。',
      v_bad, v_first, v_checked;
  END IF;

  RAISE NOTICE '✅ 新物件收權斷言通過:檢查 % 個物件，anon/authenticated 權限 0 項。', v_checked;
END
$newobj_guard$;

COMMIT;

-- ── ROLLBACK(要退的時候整段貼)───────────────────────────────────────────
--   SELECT cron.unschedule('pcm-search-log-purge');
--   DROP FUNCTION IF EXISTS public.purge_search_queries();
--   DROP INDEX IF EXISTS public.search_queries_created_at_idx;
--   -- 🔴 log_search_query 要退回 20260904200000 那一版(整段從那支檔複製), 不是 DROP。
