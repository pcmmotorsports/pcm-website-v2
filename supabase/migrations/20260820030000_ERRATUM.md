# 勘誤 · 20260820030000_m4b_e10_a8a3_cancel_gate_noncard.sql

> 🔴 **這份存在的理由:那支 migration 已 apply,而【已 apply 的 migration 連註解都不能動】。**
> 部署時序 gate(`scripts/deploy-order-gate.sh` 的 `pending_versions`)拿檔案 sha256
> 對 `supabase/APPLIED.tsv` 記的值比對,**對不上就把那支當成「還沒 apply」** ——
> 於是任何用到它建的函式的新程式碼都會被擋住推送。2026-08-21 真的擋了一次。
> ⇒ 更正只能寫在這裡,不能寫回那支 `.sql`。

> ⚠️ 而 2026-08-21 主視窗一度判斷「今天沒有守門在比對這個 sha256」——
> 那個判斷是**從一支腳本推出來的**(`scripts/migration-ledger-divergence.sh` 確實不比對、exit 0),
> 而**另一支比對**。🔴 **從一個量具的沉默,推不出「沒有任何量具在看」。**

## 勘誤內容(2026-08-21,窗 H 查正式庫函式 `prosrc` 得出,非讀 repo)

[2026-08-20 寫下時為真;2026-08-21 已由後續事件解除,見下]
[2026-08-21 起為真]
✅ 已 apply(Sean 本人 SQL Editor,2026-08-21 03:4x)。APPLIED.tsv 記載本檔緊接在
20260820020000(防護)之後套上,兩者同一時段、無真空期。詳見下方 :45 註記。
[2026-08-21 更正:上面這句的【理由】已過期,【結論】仍成立,理由已換]
片 A(本檔)已上線,`payment_not_unpaid` 那道閘不再擋——現在真的可以取消
已付款且非刷卡的單(查正式庫 `admin_cancel_order` 的 `prosrc` 確認)。
但「形狀造不出來」這句結論仍然是真的,原因換成:`20260820020000` 補上的
`cancelled_at IS NULL` 防護已在本檔之前套進正式庫、兩者無真空期(APPLIED.tsv 記載
時序),下游①②兩處查詢已正確排除已取消的單。真實資料「已取消且已付款」0 筆
(負對照 0)。⇒ **不要照原本那句「現行 payment_not_unpaid 閘還擋著」去判斷,
那道閘已經不擋了——擋住風險的是另一道防護。**
[2026-08-21 起為真]
✅ 那三處已逐處查證處理完畢(2026-08-21,查正式庫函式 `prosrc`,非讀 repo):
① find_active_sibling_own(20260624120001_*.sql)—— 已補 `cancelled_at IS NULL`
② begin_charge_attempt cart dedup 段 —— 已補同款檢查(同上,皆由 20260820020000 補上)
③ begin_charge_attempt per-user 閘段 —— 本來就不受影響(該段自帶 `payment_status <> 'paid'`
條件,已取消且已付款的單本來就進不了這個分支)
完整查證見 `~/pcm-mailbox/H-93-取消閘假成功風險查核-20260821.md`。

## 勘誤內容(2026-08-24,線C;窗 A 撞到、主視窗轉派)

### 🔴 檔頭 `:8` 那句「⛔ **本檔尚未 apply**」**已經不成立**

```
那支 .sql :8  逐字  「⛔ **本檔尚未 apply,而且不由施工窗 apply**」
量法(可重跑)  grep -c '^20260820030000' supabase/APPLIED.tsv   ⇒ 1
帳本那一列      20260820030000  dc8e7b3a…  2026-08-21
```
⇒ **它 2026-08-21 就 apply 了。** 檔頭與帳本互相矛盾,而**帳本是對的那一邊**。

⚠️ **後半句仍然成立**:「不由施工窗 apply」是**紀律**不是狀態,它沒有過期。
⇒ 過期的只有前半句「尚未 apply」。**不要把整句當成廢話。**

### 🔴 為什麼修的人不是作者、而且不是去改那支 `.sql`

- 修的人不是作者:窗 A 撞到這個矛盾、而它**不能改別人的檔**;主視窗把它轉派給線C。
- **不是去改那支 `.sql`** —— 那正是本勘誤檔存在的理由(見檔頭)。
  🔴 **2026-08-24 線C 當場複驗過那個理由,不是照抄**:
  ```
  現檔 sha256                                   dc8e7b3a0ffb47415e4d814ce7164d84fd40db47bb658d77cff6c789c492c85c
  supabase/APPLIED.tsv:275 記的                 dc8e7b3a0ffb47415e4d814ce7164d84fd40db47bb658d77cff6c789c492c85c   ⇒ 一字不差
  scripts/deploy-order-gate.sh:156              git show "$rev:$f" | shasum -a 256   ⇒ 它真的在比
  ```
  ⇒ **在那支 `.sql` 裡「劃掉舊句留痕」會改掉它的 sha256** ⇒ 部署閘把它當成沒 apply
    ⇒ 重演 2026-08-21 那次事故(當天真的擋了一次)。
  ⚠️ **那個處置本身是對的 repo 慣例 —— 只是【對這一族檔不適用】。**
    慣例的射程止於「檔案可以改」;已 apply 的 migration 不在射程內。

### 🔴 而它不只這一支 —— 同款有 7 支(2026-08-24 線C 量)

量法(可重跑):
```bash
for f in supabase/migrations/*.sql; do v=$(basename "$f"); v=${v%%_*}
  grep -q "^$v" supabase/APPLIED.tsv || continue
  grep -nE '^--.*(本檔尚未 apply|本支尚未 apply|正式庫仍未 apply 過|本檔尚未執行)' "$f" | head -2
done
```
命中:`20260717020000` / `20260801120000` / `20260820020000` / `20260820021000` /
`20260820030000`(本支)/ `20260820140000` / `20260821113000`。

⚠️ **本檔只處理 `20260820030000` 一支。** 其餘 6 支**尚未處理**,而它們**同樣改不得**
(都在 `APPLIED.tsv` 裡 ⇒ 都被 sha256 釘住)⇒ 各自需要自己的勘誤檔,或一份共用的。
📌 **這個分母寫在這裡的理由**:下一個人讀到本檔會以為「這類問題已經處理完了」。**沒有。**

## 那支 .sql 現在的內容

原樣未動,sha256 `dc8e7b3a0ffb47415e4d814ce7164d84fd40db47bb658d77cff6c789c492c85c`,與 `supabase/APPLIED.tsv` 記的一致。
`git show cbf51868 -- <該檔>` 可以看到那次被還原掉的編輯內容。
