-- 20260821113000_m4b_legal_terms_v4_search_logging.sql
-- 隱私政策追加兩項(站內搜尋)⇒ 新增條款版本列 '2026-08-21'。
--
-- ── 這一版改了什麼對外文字(只有這兩處,其餘一字未動)────────────────────────────
--   `apps/storefront/src/data/legal-content.ts`
--   [改動A] 「二、蒐集目的」那個【單句】裡,在「依法令保存交易紀錄、」之後插入一項:
--           站內搜尋功能之提供與改善、
--   [改動B] 「三、蒐集之個人資料類別」第一個【單句】裡,在「訂單與交易紀錄、」之後插入一項:
--           站內搜尋紀錄、
--   ⚠️ **兩節各自是一個頓號分隔的單句** ⇒ 這不是「加兩個句子」,是在既有單句裡各加一項。
--   ⚠️ 檔頭另加了四行註解(律師簽核限定延伸到新句)——
--     🔴 **註解不進 hash**(canonicalLegalPayload() 只序列化資料結構)⇒ 改註解不影響本檔的值。
--
-- ── 授權來源(逐字可查)────────────────────────────────────────────────────────
--   Sean 2026-08-21 看決策單答「**乙 = 直接發**」(memory `project_0821-sean-answers-seven-of-eight`)。
--   🔴 他授權的是【不看】,**不是**免審、也**不是**律師簽核:
--     · 鐵則 12⑤(對外不可回收)+ 12③(DB 寫入)雙中標 ⇒ codex 對抗審查照跑(主視窗)
--     · `legal-content.ts:6`「本次未經律師簽核」那句的限定**涵蓋本次追加**(已寫進該檔檔頭)
--   內容來源:搜尋日誌 `#183` 要蒐集站內搜尋詞(Sean 2026-08-21 `Q1=A`)。
--
-- ── content_hash 怎麼來的(可重跑)────────────────────────────────────────────
--   sha256(canonicalLegalPayload()),由 `legal-content-hash.test.ts` 的**失敗訊息**印出。
--   本值取得方式(2026-08-21,窗 C 實跑,repo 根):
--     npx vitest run src/data/legal-content-hash
--   螢幕逐字:`實際 hash = b250342b0ea4e7f9536bfbad9e50a93bfc9e4fda1a2289f6340d3459761e1376`
--   ✅ 尺是活的:同一發同時印出 Expected `12a1bb0c…`(舊版) ≠ Received `b250342b…`
--      ⇒ 兩個世界印不同的東西,不是恆回同一個值。
--
--   🔴 **這個值會過期,而 2026-08-19 那一支【真的被它咬過一次】**(該檔檔頭自述:
--     hash 在 apply 前因為有人改了字面而作廢)。
--
--   🔴🔴 **而【本檔的 SQL 自己抓不到這件事】**(codex 關卡2 MF-1/MF-2,2026-08-21):
--     下面 INSERT 的 hash 與 DO 區塊斷言的 hash 是**同一個硬編碼字面、同一個來源**
--     ⇒ 「repo 的文字漂移了、而 DB 寫進這個過期值」那個世界,**SQL 會成功、斷言會通過**。
--     ⇒ SQL 讀不到 repo,這是結構性的,不是註解寫得夠不夠用力的問題。
--
--   ✅ **⇒ 閘在 SQL 外面,而且是可跑的**(不是叮嚀):
--       bash scripts/legal-terms-preflight.sh 2026-08-21 \
--         b250342b0ea4e7f9536bfbad9e50a93bfc9e4fda1a2289f6340d3459761e1376 \
--         supabase/migrations/20260821113000_m4b_legal_terms_v4_search_logging.sql
--     判準 = **最後一行逐字等於 `PREFLIGHT_RESULT=PASS`**(不要只看退出碼:
--     呼叫端一接管線,`$?` 就變成右端那個指令的)。
--   🔴 **第三個參數(migration 路徑)不可省** —— 它綁定「你要遞的是【哪一支】」:
--     `legal-content-hash.test.ts:57-96` 只要求「**任一** migration 含該 version+hash」
--     ⇒ 目錄裡另一支也命中時,**你要遞的那一支就算被改壞,那道守門仍然綠**(codex R2 抓到)。
--     preflight 的閘 0 補上這一環:①指定的那支必須自己命中 ②不得有第二支重複 seed。
--     🔴 **跑之前先把本檔記載的兩處文字改動套進 `legal-content.ts`**(套用腳本見下方「⑤」那節)。
--     ✅ 那支腳本 2026-08-21 當場讓它表演過 **5 個世界**(不是宣稱,是量的):
--         A 文字沒套            ⇒ rc=1  末行 PREFLIGHT_RESULT=FAIL
--         B 文字套好            ⇒ rc=0  末行 PREFLIGHT_RESULT=PASS   ← 只有這一格 PASS
--         C 遞【錯的】migration ⇒ rc=1  FAIL
--         D 有第二支重複 seed   ⇒ rc=1  FAIL
--         E 參數沒給            ⇒ rc=2  FAIL
--       ⇒ 不是恆綠,也不是恆紅。而它跑完會把檔案還原
--         (**快照**還原,不是 `git checkout` —— 後者會殺掉你剛套的文字)。
--
--   🔴🔴 **這個閘【關不掉】的那一格,明講缺哪一道檢查**(codex R2 堆三,不是可折的 finding):
--     `preflight 回 PASS` 到 `人工把 SQL 貼進 Supabase SQL Editor` 之間,
--     別的窗改了 `legal-content.ts` ⇒ DB 寫進舊 hash,**而 preflight 已經回 PASS 了**。
--     ⇒ **缺的那一道檢查叫「apply 與 preflight 的原子綁定」,而它不存在** ——
--       apply 在人手上、檔在七窗共用的樹上,兩者無法綁在同一個交易裡。
--       任何往腳本裡加的防護都追不到它(那只是把競態窗口變小,不是關掉)。
--     ⇒ **所以這一格用【程序控制】+【事後偵測】,不是用程式控制**:
--       ① preflight 回 PASS 之後**立刻**遞出,中間不做別的事
--       ② 遞出的那一刻**再跑一次** preflight —— 那是我們控制得到的最後一個時點
--       ③ 🔴 **apply 之後做「後查複驗」** ⇐ **這才是這條真正的補救**,見下方對照查詢那一節末尾
--     📌 現況(2026-08-21 主視窗當場核):`legal-content.ts` 只有窗 C 一個在動
--       ⇒ **這個風險目前是理論的,不是進行中的**。而它會隨窗數變回真的,所以寫在這裡。
--   🔴 **而那個「跑」不是 Sean 做的** —— 他在 SQL Editor 貼 SQL,他不跑 vitest。
--     ⇒ 責任在**把這支 SQL 遞給他的那個窗**。這一行也要寫在 apply 佇列條目上,
--       因為讀這支檔的人與遞 SQL 的人不一定是同一個。
--
-- ── 🔴🔴 發布順序是硬性的,而顛倒的後果是【靜默的】──────────────────────────────
--   ① 本檔 apply ② 確認已套用 + 記 supabase/APPLIED.tsv ③ **才** bump
--      `apps/storefront/src/lib/legal/terms-version.ts` 兩個常數:
--        CURRENT_TERMS_VERSION      '2026-08-19' → '2026-08-21'
--        CURRENT_TERMS_CONTENT_HASH '12a1bb0c…77e4' → 'b250342b…1376'
--   ⚠️ **後果分兩種世界,而【本次是哪一種】要先講清楚**
--     (🔴 2026-08-21 更正:本窗原本裁「以檔頭為準」,codex 關卡2 MF-3 推翻,**該裁定收回**):
--       世界甲 —— **目標版本列在 DB 裡【還不存在】** ⇐ 🔴 **本次就是這一種**
--         先 bump 常數而 DB 沒那一列 ⇒ `order_legal_consents` 的 FK 收不到該版本
--         ⇒ **每一筆結帳 FK 違反、直接失敗**。出處 `terms-version.ts:19-29`。
--       世界乙 —— 目標版本列**已經存在**(例如已 apply、只是 hash 不同)
--         ⇒ 不會 FK 失敗,而是**成功建單、把同意掛到 hash 錯誤的版本列上 = 靜默舉證錯配**。
--         出處 `terms-version.ts:36-39`。
--     ⇒ ⇒ **兩處都對,它們在講不同的世界;而兩處都【沒有說自己在講哪個世界】** ——
--       這才是真正的缺陷。`legal-content-hash.test.ts:33` 的失敗訊息(「FK 違反、全站結帳斷線」)
--       對**本次**是正確的。**不要照本窗先前的說法把它「修正」掉。**
--       這個缺陷本檔不修(那是另外兩支檔的文案)⇒ **backlog `#804`**(2026-08-21 開)。
--       🔴 前一版這裡寫的是「已交主視窗開號」——**那是一句沒有編號的承接宣稱**,
--         下一個人無從查證它有沒有真的被接走。有編號才查得到。
--   ⚠️ 而 `legal-content.ts` 那兩處改動**已經在工作樹上了**(窗 C 2026-08-21 改)⇒
--     在常數 bump 之前,`legal-content-hash.test.ts` **會紅**,而鐵則 11 不准帶著紅 commit。
--     ⇒ 🔴 **那兩處字面改動與 bump 必須同一顆 commit,而那一顆要等 apply 之後。**
--       (與 2026-08-19 那支同形,不是新規矩。)
--
-- ── 開新鍵 '2026-08-21'、不覆寫任何既有列 ──────────────────────────────────────
--   #291 驗收條件:只能新增新版本列、不得修改既有列。
--   覆寫會把舊客人的同意掛到他沒看過的文字上 = 舉證錯配。
--   ✅ 本檔只 INSERT,沒有任何 UPDATE ⇒ 這個決定在「有沒有人簽過舊版」兩個世界都安全。
--
-- ── 對照查詢:**貼兩次**(apply 前一次、apply 後一次)────────────────────────────
--   🔴 **前例那一發是【恆真】的,不要照抄**(codex 關卡2 MF-4,2026-08-21):
--     前例只 select 版本與筆數、而且**只貼一次** ⇒
--       · 目標版若 apply 前就已存在且零簽署 ⇒ 前後都印 0
--       · 目標版若存在但 **hash 是錯的** ⇒ 一樣印 0
--     ⇒ 「本次 INSERT 真的生效」與「它本來就在那裡」對那把尺**長得一樣**。
--     ⇒ 修法兩件:①**把 `content_hash` 選出來**(那才是分得開的欄位)②**前後各貼一次**。
--
--     select v.version,
--            v.content_hash,
--            v.effective_at,
--            count(c.order_id) as 已簽筆數
--     from public.legal_terms_versions v
--     left join public.order_legal_consents c on c.terms_version = v.version
--     group by v.version, v.content_hash, v.effective_at
--     order by v.version;
--   🔴 用 LEFT JOIN 不用 `group by consents`:後者對**零筆的版本不印一列**(整列缺席,不是 0)。
--
--   **判準表 —— apply 前那一發印出什麼,決定你能不能往下走**:
--     ┌ 前:'2026-08-21' **整列缺席** ⇒ ✅ 正常路徑。apply 後該列出現且
--     │    `content_hash` = b250342b…1376、已簽 0
--     │    ⇒ 🟡 **與「本次 INSERT 生效」相容,而不等於它** —— 見下方第五世界:
--     │      「別人先插入同一組」在這把尺上長得一模一樣。**不要寫成「已證實生效」。**
--     ├ 前:該列已存在、hash **相同**、已簽 **0** ⇒ 🟡 之前套過。可安全重跑(`DO NOTHING`),
--     │    但要先弄清楚是誰在什麼時候套的,再決定要不要記 APPLIED.tsv
--     ├ 前:該列已存在、hash **相同**、已簽 **>0**(nit,第三世界)⇒ 🟡 同上可重跑,
--     │    **而已經有客人簽了這一版** ⇒ 記帳與後續改版都要知道這件事,不要當成「什麼都沒發生」
--     └ 前:該列已存在、hash **不同**(不論簽了幾筆)⇒ 🔴 **停。不要 apply、不要覆寫。**
--          下方 DO 區塊會 RAISE,而**你應該在跑到那裡之前就停下** ——
--          已有簽署時覆寫 = 把客人的同意掛到他沒看過的文字上。⇒ 改開一支新版本鍵。
--   ⚠️ **這一格前一版寫錯了,而它正好是本檔主題的活體示範**(codex R3):
--     舊字面說「負對照(**證明尺不是恆回同一串**):'2026-07-24' 那列前後必須逐字相同」。
--     🔴 **一個「必須維持相同」的觀察,證明的是【那一列沒被動到】(穩定性),
--        不可能同時證明【這把尺會對變化有反應】(靈敏度)** —— 我把穩定性量測寫成了靈敏度證明。
--   ⇒ 拆成兩件,各自說清楚證明的是什麼:
--     · **不擾動證據(穩定性)**:'2026-07-24' 那列的 `content_hash` 與已簽筆數
--       apply 前後**逐字相同** ⇒ 這支 SQL 沒有動到不該動的列。
--       (變了 ⇒ 出大事;沒變 ⇒ 只說明沒動它,**不說明尺是活的**。)
--     · **靈敏度證據**:'2026-08-21' 那一列**從缺席變成存在** ⇒ 同一把尺在兩個世界印出不同的東西。
--       🔴 **靈敏度只由這一半提供**,而它同時受第五世界的限制(見下)。
--
--   ⚠️ **第五世界:前查缺席 → 別人先插了同一組 → 後查出現**(codex R2)
--     ⇒ 後查看起來與「本次 INSERT 生效」**一模一樣**,DB 裡沒有欄位分得出是誰插的。
--     🔴 **部分判別**:本檔 `effective_at = now()` ⇒ 後查的 `effective_at`
--       必須落在【前查時刻】與【後查時刻】之間;落在前查之前 ⇒ 不是這一輪插的。
--       (它縮小窗口,**不能**分辨「同一輪裡另一個人搶先貼」。)
--     ⇒ 剩下那一格是**程序控制**:同一支 legal_terms migration **只由一個人負責遞**。
--       這件事我們控制得到,所以不為它加程式。**寫在這裡是為了讓下一個人知道它靠什麼成立。**
--
-- ── 🔴 apply 之後的【後查複驗】—— 競態那條的真正補救 ────────────────────────────
--   前面所有的閘都在 apply **之前**。而「貼進去的是不是對的」只有 apply **之後**答得出來。
--   Sean 貼完之後,由遞 SQL 的那個窗做兩件(兩件都做完才算收):
--     ① 後查那一發回來的 '2026-08-21' 列,`content_hash` 逐字等於
--        b250342b0ea4e7f9536bfbad9e50a93bfc9e4fda1a2289f6340d3459761e1376
--     ② **當場重跑一次 preflight**(同樣三個參數)⇒ 末行必須仍是 `PREFLIGHT_RESULT=PASS`
--   🔴 **兩件的意義不同,不可只做一件**:
--     ①答「DB 收到的是不是我們送的那個值」;②答「那個值到現在還對不對得上客人會讀到的字」。
--     ①過而②紅 = 有人在中間改了 `legal-content.ts` ⇒ **DB 那列已經是過期的**。
--   ⇒ 那個世界的處置:**不要改那一列,也不要 bump 常數** ——
--     改開一支新的 migration 用新的 hash。(既有列不可改,#291 驗收條件。)
--     ✅ 而它**還沒有客人簽過**(後查已簽筆數 0)⇒ 這個補救的代價只是多一列,不傷任何人。
--   ⚠️ 那張表 RLS 啟用 + 零 policy + REVOKE ALL(含 `anon`/`authenticated`/`service_role`)
--     ⇒ **那三個角色查不到**。
--     🔴 **不要把它讀成「只有 SQL Editor 查得到」**(前一版就是這樣寫的,codex R3 打掉)——
--       量到的是【那三個具名角色的權限】,而 DB owner、`postgres` 超級使用者、
--       `BYPASSRLS` 角色、以及直連字串等特權管道**都沒有被量過**。
--       ⇒ 準確的說法是:**一般應用層路徑查不到;特權路徑未量。**
--
-- ⏳ **本檔尚未 apply** —— 🔴 **而這句現在是【向正式庫問到的】,不是從台帳推的**(codex R3):
--   前一版的依據是 `grep -c '^20260821113000' supabase/APPLIED.tsv` ⇒ 0。
--   🔴 **那個 0 只證明【我們的台帳沒記載】,證明不了【正式庫裡沒有這一列】** ——
--     兩者不是同一件事(2026-08-21 當天已三度量到 MCP `apply_migration` 會自派版本號,
--     台帳與平台紀錄本來就會不同步)。
--   ✅ **實測(正式庫 bmpnplmnldofgaohnaok,唯讀,2026-08-21T04:07:26Z)**:
--       legal_terms_versions 目前有哪幾版 ⇒ '2026-06-30、2026-07-24、2026-08-19'
--       version = '2026-08-21' 的列數     ⇒ **0**      ← 本檔
--       正對照 version = '2026-08-19'     ⇒ **1**
--       負對照 version = '9999-12-31'     ⇒ **0**
--       supabase_migrations.schema_migrations 有沒有 '20260821113000' ⇒ 0(該台帳共 190 列)
--     ⇒ 三個數不一樣 ⇒ 這把尺分得開,那個 0 是真的。
--   📌 **這一發同時就是下方判準表要的「前查」**(時刻 2026-08-21T04:07:26Z)——
--     若 apply 發生在很久之後,**請重跑一次前查**,不要沿用這個時刻的快照。
-- ══════════════════════════════════════════════════════════════════════════════════

INSERT INTO public.legal_terms_versions (version, content_hash, effective_at)
VALUES (
  '2026-08-21',
  'b250342b0ea4e7f9536bfbad9e50a93bfc9e4fda1a2289f6340d3459761e1376',
  now()
)
ON CONFLICT (version) DO NOTHING;

-- 🔴 斷言:本檔 apply 之後,那一列必須真的在(而且 hash 是我們寫的那個)。
--    `DO NOTHING` 會在「版本鍵已存在但 hash 不同」時**靜默不動** ⇒ 沒有這一段就看不出來。
DO $$
DECLARE v_hash text;
BEGIN
  SELECT content_hash INTO v_hash
    FROM public.legal_terms_versions WHERE version = '2026-08-21';
  IF v_hash IS NULL THEN
    RAISE EXCEPTION '20260821113000: 版本列 2026-08-21 不存在 —— INSERT 沒生效';
  END IF;
  IF v_hash <> 'b250342b0ea4e7f9536bfbad9e50a93bfc9e4fda1a2289f6340d3459761e1376' THEN
    RAISE EXCEPTION
      '20260821113000: 版本列 2026-08-21 已存在而 content_hash 不同(現值 %)—— '
      '很可能是本檔的 hash 在 apply 前作廢了(有人改過 legal-content.ts)。'
      '停下,不要 bump 應用層常數,回去重跑 legal-content-hash 測試。', v_hash;
  END IF;
END $$;
