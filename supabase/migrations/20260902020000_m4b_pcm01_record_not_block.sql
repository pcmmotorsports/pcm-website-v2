-- ⟦b4-PCM01RECORD⟧ · 人工退款上限閘:從【擋住】改成【記得下來】——「標紅」那一半【不在本支】
--
-- ══ 🛑🛑 **本支【不得單獨上線】** ════════════════════════════════════════════
--   Sean 拍的甲逐字是「**記得下來, 但標紅**」—— **那句話有兩個動詞, 而「但」把它們綁在一起。**
--   而本支只做得到前半。⇒ 只上前半 = 交付他那句話的一半, 而**砍掉的正是保護那一半**。
--   🔴 **具體的傷害(量到的, 不是推的)**:
--     今天  員工把金額打成 15000(多一個 0)⇒ **被擋** ⇒ 他會發現
--     只上本支 ⇒ 15000 **安靜地進帳本**, 而畫面上【完全沒有東西會變】
--     ⇒ `apps/admin/src/components/orders/manual-refund-entry-section.tsx`(176 行)
--       提「上限 / cap / 餘裕」⇒ **0 處**(2026-09-02 `-5b` grep)
--   ⇒ 🛑 **那就是把一個【會被擋下的打字錯誤】換成一個【安靜進帳本的錯誤金額】。**
--   ⇒ 📌 而那道被拿掉的閘, 除了是業務規則, **它同時是一道打字檢查** —— 而那個角色沒有人接手。
--   ✅ **主視窗 `-0a` 2026-09-02 拍:兩半一起上。**
--   ✅ **codex 對抗審查獨立同意**(逐字):
--     「A | 有問題 | 會把原本可攔截的輸入錯誤變成無聲入帳;應等 UI 標紅／等價即時告警一起上線。」
--
-- ══ 一句話 ═══════════════════════════════════════════════════════════════════
--   `order_manual_refunds` 記的是【已經退給客人的錢】—— 錢是人先交回去了才來登記。
--   而今天的 `PCM01` 會擋:登記金額超過它算的上限就不讓你記。
--   ⇒ 🔴 **擋下它的結果不是少退一次錢, 是【少記一筆已經退掉的錢】** ⇒ 帳與事實分家。
--   Sean 2026-09-02 00:43 拍【甲】⇒ 本支把那個 `EXCEPTION` 換成 `WARNING`。
--
-- ══ 🔴 他看到的題目逐字(拍板檔 `~/pcm-mailbox/拍板-20260902-06題.md`)═══════════
--   「那個表單記的是【已經退給客人的錢】—— 錢是人先交回去了才來登記。
--     而現在系統會擋:登記金額超過它算的上限就不讓你記。」
--   甲(他選)= 記得下來但標紅 ⇒ **帳一定跟得上事實**, 而超出的那筆看得到
--   乙        = 維持擋住 ⇒ **錢已經退了卻記不進去, 帳會少一筆**
--
-- ══ 🔴 承重的那個事實(線 `-c7` 開檔驗過, 不是推的)═══════════════════════════
--   `apps/admin/src/components/orders/manual-refund-entry-section.tsx:88` 逐字
--   「記錄一筆**已經交還給客人**的現金或匯款退款,系統不會實際動錢——**錢是人交回去的。**」
--   ⇒ 📌 **這張表是【帳本】不是【授權】。錢在它看到那一列之前就離開了。**
--
-- ══ 🔴 而這道閘還有第二個獨立的洞, 它今天就在線上(同檔 `:70-79`, `-c7` 驗過)═══
--   失敗時表單**把員工填的金額原樣帶回**, 而 `PCM01` 的話是「只剩 N 元」
--   ⇒ **一個可編輯的表單 + 一句「只剩 N」= 在邀請他把金額改成 N 再送一次**
--   ⇒ 已退 M、只登記 N ⇒ **又一次短少, 而這次是員工照著畫面做的。**
--   ✅ **本支把那句話拿掉 ⇒ 這個洞的來源一起消失。**
--
-- ══ 🛑 本支【只動一件事】═══════════════════════════════════════════════════════
--   函式本體逐字沿用上一代 `20260831010000`
--   (`scripts/latest-definition-of.sh pcm_manual_refund_rail_cap_guard` ⇒ newest = live = 它),
--   **`PCM01` 與 `PCM02` 兩段都從 `RAISE EXCEPTION` 變成 `RAISE WARNING`。**
--   · `PCM03`(不能 DELETE)**不動** —— 它擋的是「把已退的錢從帳上抹掉」, 與這一題方向相反
--   · `v_headroom` 的算法、`OLD.order_id = NEW.order_id` 那道、`voided_at` 早退 —— 全部不動
--
-- ══ ✅ 而 `PCM02` 那一格:**我沒有順手改, 我去問了他 —— 而他答了甲** ═════════════
--   ⛔ ~~本支刻意不動 PCM02, 已開列 `⟦5b-PCM02SAMEHARM⟧` 端他, 而那個矛盾是已知且刻意的。~~
--   ✅ **2026-09-02 01:2x Sean 拍【甲】**:「一起改, 標一個不同的紅」⇒ 本支【兩段都改】。
--
--   🔵 **而這一格的過程值得留著, 因為它是這一整晚的縮影:**
--     · codex 判它是 must-fix, 用的字是「**正面矛盾**」—— 而它是對的:
--       本支剛說「擋下去的代價是帳跟不上事實」, 而它自己留著另一道會擋的。
--     · 而我當時【仍然沒有順手改】, 理由是:**他答的是【金額超過上限】那一題,**
--       **而【算不出上限】是另一個世界** —— 前者系統知道自己在擋什麼;
--       後者系統**不知道自己在看什麼**, 而在那種時候 fail-closed 有它自己的道理。
--     · ⇒ 📌 **我不會用一個他答過的題目, 去替他回答一個他沒被問到的。**
--     · ⇒ ✅ **所以我把它開成一列端他 —— 而三個小時後他答了, 而答案是「一起改」。**
--   🎯 **⇒ 結論與「順手改」相同, 而【路徑不同】:一個是我替他決定, 一個是他決定。**
--     **⇒ 而那個差別在碼上看不出來, 只在這一段看得出來。**
--
--   🔴 **而「不同的紅」是承重的**:兩種紅的下一步【相反】——
--     超額 ⇒ **確認金額**(數字可能打錯) · 算不出上限 ⇒ **找工程**(金額可能完全沒問題)
--     ⇒ 合成同一種紅 = 把「該找工程的人」送去改金額 ⇒ 那正是 `⟦b4-PCM05SPLIT⟧` 那一條。
--
-- ══ 🔵 「標紅」需要什麼 —— 而我原本這一節【只對了一半】═════════════════════════
--   ✅ **超額那一種確實不需要新欄位**:`public.pcm_manual_refund_rail_cap(order_id)`
--     **沒有 `GREATEST`**(`20260824010000:124-133`, 開檔看的)⇒ 超收之後它自然回負數
--     ⇒ 畫面層問「它 < 0 嗎」就知道 ⇒ **超出多少是算得出來的, 不是要存起來的。**
--   ⛔ ~~也不需要新狀態~~
--   🔴 **【codex must-fix】而【算不出上限】那一種不成立** ——
--     `cap IS NULL` 是**當下**的狀態。哪天那個算式恢復正常, `cap` 就有值了
--     ⇒ **而「這一筆在登記的當下算不出上限」這件事, 事後【重建不出來】。**
--     ⇒ 📌 **一個「當下為真」的狀態, 不能靠事後重算來還原** —— 它需要被【記下來】。
--     ⇒ 🛑 而那是新欄位或一張異常紀錄 ⇒ 動的是一張【錢】的表 ⇒ 不是本支順手做得了的。
--     ✅ 已開列 `⟦5b-CAPUNKNOWNSTATE⟧`。⇒ **在它做完之前, 那一種紅只在【當下】看得到。**
--   ⇒ 而畫面層那一半(要多紅、擺哪裡)= 視覺 = Sean 的 ⇒ **本支刻意不碰。**
--
-- ══ ⚠️ 新舊交錯上線 ═══════════════════════════════════════════════════════════
--   ⛔ ~~「舊 app + 新 DB ⇒ 仍然比今天好」/「兩個方向都不會壞」~~
--   🔴 **【codex must-fix】那兩句都是假的, 而它們是我寫的。留著加刪除線, 不要刪。**
--   **舊 app + 新 DB = 【無標紅、無攔截】** ⇒ 那正是上面那個「安靜進帳本」的窗口本身。
--     ⇒ 而我原句寫「仍然比今天好」—— 那是拿「記不進去」跟「記錯了而沒人知道」比,
--       而**後者不是比較好, 它只是比較安靜**。
--   **新 app + 舊 DB** ⇒ DB 仍擋 ⇒ 而若新 app 已經拿掉 `PCM01` 的處理, 它會**退化成泛用錯誤**
--     ⇒ 所以也不是「與今天逐字相同」。
--   ✅ **⇒ 兩個方向都有代價 ⇒ 這正是「兩半一起上」的理由, 而不是它的但書。**
--
-- ══ 📎 來源 ═══════════════════════════════════════════════════════════════════
--   拍板逐字 `~/pcm-mailbox/拍板-20260902-06題.md`
--   線 `-c7` 的 `⟦b4-CAPRACE1⟧` 交接(478 行, 三輪審查)—— 它的 R3 結論就是這一題的答案:
--   `~/pcm-mailbox/交件-b4-CAPRACE1-這片不要上-而那一列要改寫-20260902.md`

CREATE OR REPLACE FUNCTION public.pcm_manual_refund_rail_cap_guard()
RETURNS trigger
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $fn$
DECLARE
  v_cap       bigint;
  v_headroom  bigint;
BEGIN
  -- 🔴🔴 **`DELETE`(2026-08-24 R3 / Fable F2 must-fix)—— 選【甲】:在這裡擋,不 REVOKE。**
  --
  -- 病灶:本閘原本是 `BEFORE INSERT OR UPDATE`,而**沒有任何一片管這張表的 `DELETE` 權限**。
  --   刪掉一筆退款登記 ⇒ **額度憑空回來,而且零痕跡**(作廢至少留 `voided_at`;DELETE 什麼都不留)
  --   ⇒ 可以退第二次。
  -- 📌 **它與「作廢後復活」是同一族的第二個成員** —— 上一輪修掉復活,這是另一條門。
  -- 🔴 而最該記住的是它怎麼活下來的:
  --   **「我這片不管它」+「沒有別片管它」= 沒有人管它,而前一句單獨看完全合理。**
  --
  -- 為什麼選甲不選乙(REVOKE DELETE):
  --   · 甲**對所有角色生效,包含日後新增的** —— REVOKE 是逐角色的,而
  --     `ALTER DEFAULT PRIVILEGES` 可能早就發過整套(本 repo 今天已有兩次實錘)
  --   · 甲**與本片其他規則住在同一個檔** ⇒ 讀的人一次看得完,不必去翻 GRANT 的歷史
  --   · ⚠️ 甲的上限與本檔其他規則相同:`owner`/`superuser` 仍可 DISABLE TRIGGER。**照實寫。**
  -- ⚠️ **代價**:日後要清理歷史退款列時會被擋 ⇒ 那時要的是**一次性的受控停用**,不是刪掉這道。
  -- 🔴🔴 **而少了這道分支,結果【不是「刪得掉」】—— 是「靜靜地什麼都沒發生」**
  --    (2026-08-24 harness `M6` 實測,不是推論):
  --      DELETE 時 `NEW` 是 NULL ⇒ 下面每一個 `NEW.*` 都是 NULL
  --      ⇒ `NULL > v_headroom` 不成立 ⇒ 一路走到 `RETURN NEW`,而 **NEW 是 NULL**
  --      ⇒ **BEFORE ROW trigger 回 NULL = 取消這個動作,而且不報錯。**
  --    ⇒ 操作的人以為刪掉了,資料還在,沒有錯誤訊息也沒有 SQLSTATE。
  --    📌 **所以這道分支真正在做的事,是把【沉默】換成【一句說得出理由的拒絕】。**
  --
  -- 🔴🔴 **而最該讓下一個人知道的是這一句**:
  --    **這不是本片造成的行為,是本片【照出來】的 —— 而在本片之前,沒有人觀察過這張表的 DELETE。**
  --    那個「靜靜取消」從 `20260820010000` 建表之後就一直是這張表的行為,
  --    只是**沒有任何一片管過 DELETE**,所以也**沒有任何一發量測會經過它**。
  --    📌 一般化:**「接線 / 加一道分支」這個動作本身,常常是那個東西第一次被檢驗。**
  --      ⇒ 所以動一支老東西時,**先假設你會照出一些不是你造成的東西**,
  --        而它們會長得像你剛弄壞的 —— 判別法是問「這個行為在我動手【之前】存不存在」。
  IF TG_OP = 'DELETE' THEN
    RAISE EXCEPTION
      '人工退款登記「不能刪除」 —— 要取消請用「作廢」(它會把額度還回來,而且留得下紀錄)。'
      '🔴 直接刪掉會讓額度憑空回來且「查不到是誰做的」,那正是這道規則要防的。'
      USING ERRCODE = 'PCM03';
  END IF;

  IF NEW.voided_at IS NOT NULL THEN
    RETURN NEW;
  END IF;

  v_cap := public.pcm_manual_refund_rail_cap(NEW.order_id);

  IF v_cap IS NULL THEN
    -- 🔴 **自訂 SQLSTATE `PCM02`,而它有兩個用途,兩個都承重**:
    --   ① 畫面層要把這個錯翻成人話 ⇒ 它需要一個**機器認得的碼**;
    --      釘中文訊息字面 = 把文案變成 API,改一個字就壞。
    --   ② harness 的負測要分得出「**是我們這道閘擋的**」與「別的錯誤剛好也讓它失敗」。
    --      2026-08-24 codex 抓到:原本的負測只問「有沒有 error」
    --      ⇒ FK / CHECK / NOT NULL / 權限 / 斷線**全都會冒充成「上限命中」**。
    -- ⚠️ **這一段幾乎不會走到,而措辭要照實**(2026-08-24 codex nit):
    --    真的「讀不到帳本」通常直接拋權限 / relation 不存在的錯,**不會回 NULL 走到這裡**。
    --    會讓它回 NULL 的是**算式本身出了預期外的狀況** ⇒ 所以不寫「讀不到帳本」,
    --    寫「算不出來」。行為仍是 fail-closed,只是**不宣稱知道原因**。
    -- ⛔ ~~2026-09-02 `⟦b4-PCM01RECORD⟧`:這一格【刻意沒有跟著 PCM01 一起放行】~~
    -- ✅ **2026-09-02 01:2x Sean 拍【甲】:「一起改, 標一個不同的紅」** ——
    --    題目逐字是我自己開的那一列 `⟦5b-PCM02SAMEHARM⟧`(前一題沒有問到它)。
    --    ⇒ 而他選甲的理由與 `PCM01` 那一題同一條:**錢已經退了, 帳一定要跟得上事實。**
    -- 🔴🔴 **而「不同的紅」是承重的, 不是修飾** —— 兩種紅的【下一步相反】:
    --    超額(`PCM01` 那條)⇒ **確認金額**(數字可能打錯)
    --    算不出上限(這一條)⇒ **找工程**(系統不知道自己在看什麼, 而金額可能完全沒問題)
    --    ⇒ 📌 **合成同一種紅 = 把「該找工程的人」送去改金額** ——
    --      而那正是 `⟦b4-PCM05SPLIT⟧` 那一條(一碼兩義而下一步相反), 我剛做過它。
    -- ⚠️ **而 `v_cap` 是 NULL ⇒ `v_headroom` 也是 NULL ⇒ 下面那個超額比較【不會成立】**
    --    ⇒ 所以這一筆**只會有這一句 WARNING**, 不會同時吐超額那一句。**那是對的**:
    --      我們不知道上限, 就不該宣稱它超額。
    RAISE WARNING
      '人工退款登記:這張單【算不出可退上限】—— 已照實登記, 而系統無法判斷它有沒有超額。'
      '🔴 這與「金額太大」不同:那是金額問題, 這是系統算不出上限 ⇒ **請找工程確認**, 不要改金額重送。'
      '⇒ 畫面層應以 pcm_manual_refund_rail_cap(order_id) IS NULL 標一個【與超額不同】的紅。';
    RETURN NEW;
  END IF;

  v_headroom := v_cap;
  -- 🔴 `OLD.order_id = NEW.order_id` 這一項是 2026-08-24 codex 抓到的洞,**不是防禦性贅字**:
  --    少了它 ⇒ 把 A 單上一筆已退 1000 的列 `UPDATE … SET order_id = B` 時,
  --    cap 算的是 **B 單**(零實收 ⇒ 0),而餘裕卻加回 **A 單那筆**的 1000
  --    ⇒ B 拿到 1000 的假餘裕 ⇒ **通過**。
  --    ⚠️ 而這**不是併發** —— 是單一交易裡的一個合法 UPDATE,一個人就做得到。
  -- 🔵 **2026-09-02:這一段【留著】, 即使 PCM01 不再擋。**
  --    它算的 `v_headroom` 現在是【警告訊息裡那個數字】的來源 ——
  --    ⇒ 算錯它, 員工看到的「餘裕 N 元」就是錯的。**它從守門件變成量測件, 而不是變成廢的。**
  IF TG_OP = 'UPDATE' AND OLD.voided_at IS NULL AND OLD.order_id = NEW.order_id THEN
    v_headroom := v_cap + OLD.refund_amount;
  END IF;

  -- 🔴 **這一行的承重件不在本檔**(2026-08-24 R3 / Fable F4):
  --    `NEW.refund_amount` 若是 `NULL` ⇒ `NULL > v_headroom` 回 `NULL` ⇒ `IF` 不成立
  --    ⇒ **靜靜放行**。今天不會發生,靠的是**另一支 migration**:
  --      `20260820010000` 的 `refund_amount integer NOT NULL CHECK (refund_amount > 0)`
  --    ⇒ **那道 NOT NULL 是這道 trigger 的承重件,而它寫在別人的檔裡。**
  --    ⇒ 有人日後放寬它,這裡不會報錯,只會**靜靜放行**。
  -- ⛔ ~~✅ 所以前置閘現在**也驗那道 NOT NULL**(見 §0)—— 讓依賴變成會執行的東西,不是註解。~~
  -- 🔴🔴 **【R3 must-fix】那個 ✅ 是我從上一代逐字抄過來的, 而它在【本檔】指向一個不存在的東西**:
  --    **本檔沒有 §0, 也沒有任何前置閘。**上一代(`20260831010000`)有, 本檔沒有帶過來。
  --    ⇒ 📌 **一個指向不存在檢查的 ✅, 會關掉下一個人的查證** —— 而它比「沒寫」糟:
  --      沒寫的話他會自己去看;寫了他就不看了。
  --    🔴 **而成因是【逐字沿用】這個動作本身**:我為了不刪 Sean 的拍板紀錄而整段照抄,
  --      而照抄會把**只在原檔成立的句子**一起搬過來。
  --      ⇒ ⇒ **「不刪註解」與「註解仍然為真」是兩件事, 而前者做到了不保證後者。**
  --    ✅ **現況**:那道 NOT NULL(`20260820010000` 的 `refund_amount integer NOT NULL`)
  --      **今天仍然是這道 trigger 的承重件, 而【本檔沒有驗它】。**
  --      ⇒ 有人放寬它 ⇒ 這裡不會報錯, 只會**靜靜地不出聲**(見下一段)。
  -- 🔵 **2026-09-02:放行不再是「洞」, 而【少一句警告】仍然是。**
  --    `NULL > v_headroom` 回 NULL ⇒ 這個 `IF` 不成立 ⇒ **不會 WARNING**
  --    ⇒ 那一筆會靜靜進去而沒有人知道它超額 ⇒ **那道 NOT NULL 仍然承重, 只是承的東西換了。**
  IF NEW.refund_amount > v_headroom THEN
    -- ══ 🔴🔴 **Sean 2026-09-02 拍【甲】:從 `RAISE EXCEPTION` 換成 `RAISE WARNING`** ══
    --   而那一個字就是本支的全部:`WARNING` 不中止交易 ⇒ 那一列**進得去**。
    -- ⛔ ~~而它仍然進 DB log ⇒ **事後查得到**。~~
    -- 🔴 **【codex must-fix】那句話是【沒有保證的】, 而我寫得像保證。**
    --    `log_min_messages` 可以把 `WARNING` 濾掉;`client_min_messages` 也可以讓 client 完全收不到。
    --    ⇒ 📌 **所以正確的說法是:它【有機會】進 log, 而那取決於我們控制不到的設定。**
    --    ⇒ ⇒ **它不是一個可以依賴的訊號** —— 而那正是「標紅那一半必須一起上」的第二個理由:
    --      **這一支自己留下的痕跡, 我證不出它一定在。**
    -- ⚠️ **而 `WARNING` 員工看不到** —— 它不是「標紅」, 它是(可能的)留痕。
    --    **「標紅」是畫面層那一半, 而視覺是 Sean 的 ⇒ 本支刻意不碰。**
    -- 🔵 **而訊息裡不再出現「只剩 N 元可退」** —— 那正是上面「第二個獨立的洞」的來源:
    --    一個可編輯的表單 + 一句「只剩 N」= 在邀請員工把金額改成 N 再送一次。
    --    ⇒ 現在這段話是給【log】看的, 不是給員工照著做的。
    -- ⚠️ **負餘裕照實印, 不夾底**(舊版對員工顯示時用 `GREATEST(...,0)`, 理由是「-500 不可讀」)——
    --    而這裡的讀者是工程, 而**夾底會把「超收多少」這個資訊丟掉**。
    RAISE WARNING
      '人工退款登記超過這張單【現金 / 匯款】軌的可退上限:餘裕 % 元, 這一筆 % 元。'
      '⇒ 已【照實登記】(Sean 2026-09-02 拍甲:帳要跟得上事實), 而這張單的軌別餘裕會變成負數。'
      '⇒ 畫面層應以 pcm_manual_refund_rail_cap(order_id) < 0 標紅。',
      v_headroom, NEW.refund_amount;
  END IF;

  RETURN NEW;
END
$fn$;

-- ── 後置斷言:那道閘【真的不擋了】, 而它的鄰居【真的還在擋】 ──────────────────
-- 🔴 這一段問兩件事, 而第二件是【正對照】:
--    少了它, 一個「把整支 trigger 換成空的」的世界也會通過第一格。
--
-- 🛑🛑 **【codex must-fix】而它證不到的那件事, 要寫在它自己旁邊:**
--   它數的是**函式原始碼裡的字串 token** ⇒ 它證得了「那些字面在 / 不在」,
--   **證不到「那個 `RAISE WARNING` 真的在超額那一支分支裡」, 也證不到「它到得了」。**
--   ⇒ 把它搬進一段死碼、或塞進一個字串常值裡, **這一段照樣全綠。**
--   ⇒ 📌 **一個「數字面」的斷言, 對【那段碼會不會被執行】永遠是盲的。**
-- ✅ **⇒ 而【可達性】由另一個東西證:`scripts/pcm01-record-verify.sh`**
--   它在拋棄式 PG 裡**真的 INSERT 一筆超額的**, 然後看:那一列進不進得去 / 有沒有出聲 /
--   軌別餘裕是不是變負。🟢 並帶負對照(沒超額 ⇒ 不出聲)與正對照(`DELETE` 仍 `PCM03`)。
--   ⇒ ⇒ **那支才是這一片的行為證據;本段只是「檔案內容沒被改壞」的低階守門。**
--   ⚠️ **兩者都要** —— 而如果只留一個, 要留的是那支 harness, 不是這一段。
DO $post$
DECLARE
  v_src  text;
  v_p01  int;
  v_p02  int;
  v_p03  int;
  v_warn int;
BEGIN
  SELECT pg_catalog.pg_get_functiondef('public.pcm_manual_refund_rail_cap_guard()'::regprocedure)
    INTO v_src;
  -- 🔴 先剝註解再數 —— 本檔的註解裡就提到 `PCM01` 好幾次。
  --    (2026-09-02 同夜在 `20260902010000` 踩過一次一模一樣的:註解被算成碼, apply 當場紅。)
  --    ⚠️ 射程:這個剝法對【字串常值裡的 `--`】會誤剝;本函式的常值是中文句子, 沒有 `--`。
  v_src := regexp_replace(v_src, '/\*.*?\*/', '', 'gs');
  v_src := regexp_replace(v_src, '--[^\n]*', '', 'g');
  v_p01 := (length(v_src) - length(replace(v_src, 'ERRCODE = ''PCM01''', ''))) / length('ERRCODE = ''PCM01''');
  v_p02 := (length(v_src) - length(replace(v_src, 'ERRCODE = ''PCM02''', ''))) / length('ERRCODE = ''PCM02''');
  v_p03 := (length(v_src) - length(replace(v_src, 'ERRCODE = ''PCM03''', ''))) / length('ERRCODE = ''PCM03''');
  v_warn := (length(v_src) - length(replace(v_src, 'RAISE WARNING', ''))) / length('RAISE WARNING');
  IF v_p01 <> 0 THEN
    RAISE EXCEPTION 'PCM01 還在擋(出現 % 次, 期望 0)⇒ 本支沒有生效', v_p01;
  END IF;
  -- 🔴 而「不擋了」與「還會出聲」是兩個宣稱 —— 一個把整段拿掉的世界也會讓 v_p01 = 0

  -- 🟢 正對照:鄰居還在。少了這一格, 「整支 guard 被清空」與「只換掉 PCM01」印同一個綠。
  -- 🔴 **2026-09-02 Sean 拍 Q4 甲之後, `PCM02` 也不再擋** ⇒ 它的 `ERRCODE` 也應該是 0。
  --    而 `PCM03`(不能 DELETE)【仍然擋】⇒ 它是這一段唯一還該有 ERRCODE 的。
  IF v_p02 <> 0 THEN
    RAISE EXCEPTION 'PCM02 還在擋(出現 % 次, 期望 0)⇒ Sean 拍的 Q4 甲沒有生效', v_p02;
  END IF;
  IF v_p03 <> 1 THEN
    RAISE EXCEPTION 'PCM03 應 1 處, 實得 % ⇒ 本支動到了不該動的東西(DELETE 那道要留著)', v_p03;
  END IF;
  -- 🔴 而「兩種紅要不一樣」是這一片的承重件 ⇒ 兩句話都要在, 而且是【兩句】
  IF v_warn <> 2 THEN
    RAISE EXCEPTION
      'RAISE WARNING 出現 % 次, 期望 2 ⇒ 少了一種紅。'
      '⚠️ 而它【抓不到「兩種紅合流」】—— 兩句話改成逐字相同, 這裡仍然印 2。'
      '⇒ 那一格靠的是上面兩段各自的訊息內容, 而本斷言只數次數。', v_warn;
  END IF;
  RAISE NOTICE '✅ PCM01/PCM02 都不再擋(各 0 處)· 兩種 WARNING 各在(共 2 處)· PCM03 原封不動(1 處)';
END
$post$;
