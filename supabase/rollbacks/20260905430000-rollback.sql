-- ═══════════════════════════════════════════════════════════════════════════
-- Rollback:20260905430000_m4b_adp_revoke_anon_update_on_sequences
-- ═══════════════════════════════════════════════════════════════════════════
-- 🔵 **這一支的還原很單純, 而【單純不等於該做】** ——
--    正片只改一列預設權限 + 對既有序列跑一輪 REVOKE(今天實測 0 條有東西可收)。
-- 🛑 而「加回去」= **讓此後由 postgres 在 public 新建的序列, 重新自帶 `anon` 的 `UPDATE`**,
--    而序列上的 `UPDATE` 就是 **`setval()` 的鑰匙** ⇒ 🔴 **還原它 = 把那把鑰匙交回給任何一個訪客。**
--    ⇒ §0 攔截器在下面, 不是形式。
--
-- 🔴🔴 **而這一支【還原不回原狀】, 差別要先講清楚**:
--    正片的 2️⃣ 對**每一條**既有序列跑了 `REVOKE ALL ... FROM anon, authenticated`。
--    ⇒ 若某一條序列在貼之前**真的**有 anon 的權限(2026-09-06 實測是 0 條, 而**未來重貼時未必**),
--      本支**不會**把它加回去 —— 📌 **因為那一條的原始形狀沒有被記下來。**
--    ⇒ 🛑 **所以本支還原的是【預設權限那一列】, 不是【既有物件的 ACL】。**
--      要還原後者, 得先有貼之前的 `relacl` 快照(`scripts/acl-snapshot.sh`), 而本片沒有替每一條存。
--    ⇒ ✅ 那是刻意的:今天要還原的量是 0 條, 而**替一個 0 建快照機制**的成本大於它的價值。
--      **而這句話會過期** —— 重貼時 0 若不再是 0, 這一段就不成立了, 那時要先照快照走。
-- ═══════════════════════════════════════════════════════════════════════════

-- ── §0 攔截器(可執行)──────────────────────────────────────────────────
DO $$
BEGIN
  RAISE EXCEPTION '20260905430000 的 rollback 不可自動執行。還原它 = 讓新建的序列重新自帶 anon 的 UPDATE(= setval 的鑰匙)。先確認那個決定被推翻, 再手動把 §0 註解掉;並先讀上面「還原不回原狀」那一段。';
END $$;

-- 🔴🔴 **[codex 2026-09-06 must-fix] 還有一種世界, 這一支會【給出原本不存在的權限】**
--    正片有【態 B】:那一列裡 anon/authenticated 本來就不在(有人先收過, 或空庫重放)
--    ⇒ 正片走 NOTICE、**什麼都沒改**、照樣 COMMIT。
--    ⇒ 🛑 **而這一支不知道那件事** —— 它照樣 `GRANT UPDATE`,
--      📌 **於是「還原」動作【建立】了一個從來沒有存在過的授權。**
--    ⇒ ✅ **跑之前必答一句**:那一列在貼 20260905430000 之前, anon 到底有沒有 UPDATE?
--      答不出來 ⇒ **不要跑這一支**。查法(唯讀):
--        SELECT defaclacl::text FROM pg_default_acl d
--          JOIN pg_namespace n ON n.oid=d.defaclnamespace
--         WHERE n.nspname='public' AND d.defaclobjtype='S'
--           AND d.defaclrole = to_regrole('postgres');
--      ⇒ 而它只答得出【現在】。**貼之前的形狀在正片的前置閘① NOTICE 裡**(態 A 那行印了 anon=…)。
--
-- ── §R 還原(註解;取消註解前先讀上面那三段)────────────────────────────
-- ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public
--   GRANT UPDATE ON SEQUENCES TO anon, authenticated;
--
-- ⚠️ **射程**:`FOR ROLE postgres` 不可省 —— 不寫的話它改的是【執行者自己】的預設權限,
--    而執行者若不是 postgres, 這一行會建出一列**新的**而不是還原那一列
--    ⇒ 📌 那個世界裡它會【成功】而什麼都沒還原, 而下一個人會以為還原過了。
--
-- ⚠️ **權限名這裡【要列舉】, 而正片刻意【不列舉】—— 兩邊不對稱是對的**:
--    正片收的時候用 `REVOKE ALL`(不能漏掉任何一種);
--    而還原時只該把**原本量到的那一種**(`UPDATE`)加回去 ——
--    🔴 用 `GRANT ALL` 會把 `SELECT` / `USAGE` 一起給出去, 而**那兩種原本就不在那一列裡**
--    ⇒ 📌 **一個「還原」動作給出比原本更多的權限, 而它讀起來完全像還原。**
--
-- 🔵 **既有物件**:見檔頭那段 —— 本支不還原它們的 ACL, 那要靠貼前的快照。
