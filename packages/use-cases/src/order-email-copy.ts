/**
 * @module @pcm/use-cases/order-email-copy —— **訂單信對外文案的唯一一份定義。**
 *
 * ── 為什麼這支檔存在(Sean 2026-09-03 02:5x 拍甲)────────────────────────────────
 * 付款成功信**同時寄兩份**(純文字 + HTML,收信軟體自己挑一份顯示),而那兩份的文案
 * 原本是**兩個地方各打一次** ⇒ 它們**已經漂了**。2026-09-03 逐字元量到的差:
 * ```
 * 純文字 sweep-email-outbox.ts   我們會盡快為您安排出貨,出貨後會再寄一封通知給您。   逗號 U+002C 半形
 * HTML   paid-email-html.ts      …我們會盡快為您安排出貨，出貨後會再寄一封通知給您。 逗號 U+FF0C 全形
 * ```
 * 🎯 **同一個客人、同一封信,標點在兩份裡是不同的字元** —— 而**收信軟體挑哪一份是客人那端決定的**
 *    ⇒ 沒有任何人在維護那個差異,也沒有任何東西會叫。
 *
 * 🔴 **Sean 拍板時代價是寫在選項裡的**,逐字:
 *    「甲 要 —— 一份定義, 兩邊各自取用(repo 裡有現成的形狀可抄)/ 代價:要動到付款信那一對的結構」
 *    ⇒ **動付款信那一對的結構是被授權的**,不是範圍擴張。
 *
 * 🔴 **形狀抄 `packages/domain/src/order/order-hidden-rule.ts`** ——「唯一一份定義、兩個消費端各自取用」。
 *    ⚠️ 而那支檔自己記著一句要一起帶過來的:**副本在【測試】裡 = 絆線;副本在【production】裡 = 會漂的第二份真相。**
 *    ⇒ 所以測試裡那份 `EXPECTED_ORDER_CREATED_BODY` 手打副本**刻意留著**(它是絆線),
 *      而 production 兩個消費端**都從本檔取**。
 *
 * ── 🔴 字面的來源是【稿】,不是誰覺得比較順 ────────────────────────────────────
 * 真權威 = OD 專案 `pcm-524f` 的 `email-order-paid-A.html`(**Sean 2026-08-23 拍 A 版**)。
 * 2026-09-03 直接讀那份稿量到:
 * ```
 * 逗號 = U+FF0C(全形)· 句號 = U+3002 · 稿裡也有「這封信是這筆交易的明細。」這句開頭
 * 量法 grep -o "我們會盡快為您安排出貨[^<]*" <稿> ⇒ 我們會盡快為您安排出貨，出貨後會再寄一封通知給您。
 * ```
 * ⇒ 🛑 **所以統一的方向是【純文字向稿對齊】,不是稿向純文字對齊。**
 *   HTML 那一份本來就與稿逐字相同;會變的是純文字那半的逗號 `U+002C ⇒ U+FF0C`。
 *   📌 那不是「改文案」,是**把一個偏離稿的副本改回稿**(鐵則 1:design 直接搬、不翻譯)。
 *
 * ⚠️ **而這件事會動到那道鎖,明寫**:`sweep-email-outbox.test.ts` 的 `EXPECTED_ORDER_CREATED_BODY`
 *    是一個整串 `toBe` ⇒ 它的副作用就是「這串對外文案不得無聲改變」。
 *    本次**確實動了它**,而授權鏈是:Sean 2026-09-03 拍甲(統一)+ 稿(方向)。
 *    🔴 **下一個想改它的人:先找到你的那兩格,沒有就不要改。**
 */

/**
 * 付款成功信的「下一步」那一句 —— **兩份文案共用的主句**。
 *
 * 🛑 **不要在這裡加開頭的「這封信是這筆交易的明細。」** —— 那句**只有 HTML 有**(稿如此),
 *    純文字沒有。共用的是這一句,不是整段。
 */
export const ORDER_PAID_NEXT_STEP_SENTENCE =
  '我們會盡快為您安排出貨，出貨後會再寄一封通知給您。';

/**
 * HTML 那一份在主句**前面**多的那一句(稿有,純文字沒有)。
 * 放在本檔是為了讓「兩份差在哪」有一個看得到的落點,而不是散在兩支檔裡靠人比對。
 */
export const ORDER_PAID_HTML_LEAD_SENTENCE = '這封信是這筆交易的明細。';

// ─────────────────────────────────────────────────────────────────────────────
// 頁尾聯絡資訊(Sean 2026-09-03 勾 A2:純文字那份要補上)
//
// 🔴🔴 **為什麼放這裡而不是各寫一份** —— 這正是 A1/A2 那一族的病灶本身:
//    付款信寄出去是**兩份**(純文字 + 有排版),而**客人看到哪一份是他的收信軟體決定的**。
//    ⇒ 兩份各打一次字面 ⇒ 有人改了一份 ⇒ **另一份對【某些客人】就是舊的, 而沒有東西會紅。**
//    ⇒ 📌 **所以字面只能有一份, 兩邊都從這裡取。**
// ⚠️ **射程**:這裡只收「兩份都要出現」的字面。**版面**(HTML 的 table/style)不屬於這裡。
// ─────────────────────────────────────────────────────────────────────────────

/**
 * 「有任何問題…」那一句的**前半**(LINE ID 由呼叫端接在後面)。
 *
 * 🔴🔴 **逗號是全形 `U+FF0C` —— 而那不是我挑的, 是【照抄排版那份】的結果。**
 *    Sean 勾 A2 的授權字面逐字是「字面**可以照抄**排版那份」⇒ 照抄不會產生一個半形逗號。
 *    ⛔ ~~我第一版在純文字那邊打了半形 `U+002C`~~ ⇒ code-reviewer R1 抓到:
 *      **那等於替 Sean 答了 A7(標點統一往哪邊), 而 A7 是他【沒答】的格。**
 *    ⇒ 📌 **一個字元的選擇, 在兩份之間就是一個沒有人授權的決定。**
 * 🛑 **射程**:本常數只有「回覆這封信」那半句。**那個信箱有沒有人收 = A3, Sean 仍未答**
 *    ⇒ 若答「沒人收」, 要改的地方**從 1 處變成 2 處**(本片把它擴散到純文字那份了)。
 */
// 🔴🔴 **2026-09-03:那半句【暫時拿掉】—— 而它不是「不要了」, 是【現在還不是真的】。**
//    Sean 逐字:「直接回覆 resend 的信是收不到的對吧?那我們是不是應該去申請一個
//    `info@pcmmotorsports.com`… 有沒有可以不用花錢也可以讓客人寫信給 info@,
//    但是我 sean@ 的信箱收得到的方式?」
//    ⇒ 🎯 **他的意圖是【讓它變真】, 不是刪掉它。** 而在它變真之前, 我們不寄一句明知是假的話。
//    ⛔ ~~'有任何問題，回覆這封信或加入官方 LINE'~~(舊字面留著劃掉 —— 加回來的人要搜得到它)
// 🛑 **⇒ 而這是「暫時關掉保護而沒有人負責關回去」的鏡像** ——
//    暫時拿掉一句話, 而**必須有東西記得把它加回來** ⇒ 板列 `⟦b4-REPLYTO1⟧`(態 `parked`)。
//    🔴 而那一列的判別句**不是**「設定看起來對」, 是**真的寄一封信進 info@ 而收得到**。
export const ORDER_CONTACT_LEAD = '有任何問題，加入官方 LINE';

/**
 * 品項缺品名時印的字。**兩份共用** —— 而它原本是**兩支檔各手打一份**(今天逐字相同,
 * 而那靠的是人)。⇒ 收進來之後,改一次兩份一起改。
 */
export const ORDER_LINE_TITLE_MISSING = '(品名未記錄)';

/** 官方 LINE 的短網址(排版那份 `<a href>` 用的就是它)。 */
export const PCM_LINE_URL = 'https://lin.ee/egsf1Jy';

/** 官方 LINE ID(顯示字)。 */
export const PCM_LINE_ID = '@pcmmoto';

/** 公司抬頭與統編。⚠️ 中間是**全形空白**(U+3000),不是兩個半形 —— 照排版那份逐字。 */
export const PCM_COMPANY_LINE = '派達有限公司　統一編號 90003020';

/** 公司地址。 */
export const PCM_COMPANY_ADDRESS = '新北市新莊區化成路736巷18號1樓';

/**
 * 金額格式。**兩份共用** —— 排版那份的 `money()` 也委派到這裡。
 *
 * 🔴 `Math.trunc` 不是四捨五入:金額在本 repo 是整數(元),而**進位會讓兩份差一元**,
 *    那一元對客人是「你們算錯了」。⇒ 兩邊同一個函式 ⇒ 不可能差。
 */
/**
 * 🔴🔴 **印明細之前先問:這幾個數字加得起來嗎?**(codex 2026-09-03 對抗審查 must-fix)
 *
 * **病灶**:DB 的等式是
 * `total = subtotal + shipping_fee - discount_total + tax_total`
 * (`20260828100000_..._orders_tax_and_invoice_requested.sql:281` 逐字),
 * 而信裡的明細⛔ ~~**只列前三項**~~ ⇒ 🔴 **`tax_total > 0` 的那一天,客人會收到一張【加不起來】的帳。**
 *
 * ✅ **2026-09-04 訂正(`⟦b4-INVOICE5PCT⟧` 第 7 步;Sean 拍 Q1=乙)**:
 *    **稅額已經印出來了** —— 兩份信都在「折扣」之後、「訂單金額」之前多一列「稅額」(有稅才印)。
 *    而**本判準也已經加上第四項** ⇒ `subtotal + shippingFee - discountTotal + taxTotal === total`。
 *
 * 🔵 **而下面那段【當時的理由】留著, 因為它仍然成立**:
 * 🛑 ~~**而我【沒有】改成「把稅額也印出來」**~~,理由是這個判斷更耐久:
 *    我今天知道的漏項是稅;而**下一個被加進 `total` 的欄位,我今天不知道它叫什麼**。
 *    ⇒ 📌 **列舉漏項的修法, 只擋得住我已經想到的那一個。**
 *    ⇒ ✅ **問「加不加得起來」** —— 它對**任何**我沒想到的新欄位都成立。
 *    🎯 **⇒ 兩件事並存不矛盾:【印出來】是給客人看的, 【問加不加得起來】是給下一個加欄的人準備的。**
 *      而 2026-09-04 那次正是它預言的情形發生了 —— **只是這一次我們知道漏的是哪一項。**
 *
 * ✅ **加不起來時的動作 = 不印明細**(整段跳過),而不是印一個錯的或猜一個差額:
 *    信照寄、金額那段留白、客人仍看得到訂單編號與會員中心那句。
 *    🔴 **那是刻意的方向** —— **一張看不到明細的帳,比一張兜不攏的帳好。**
 *
 * ⚠️ **射程**:它只驗**這四個數字之間**的關係。它答不出「`total` 本身對不對」——
 *    那要對帳,不是算術。
 */
export function orderAmountsBalance(ctx: {
  subtotal: number;
  shippingFee: number;
  discountTotal: number;
  total: number;
  /**
   * 🔴🔴 **2026-09-04 加(`⟦b4-INVOICE5PCT⟧` 第 7 步;Sean 拍 Q1=乙)。**
   *
   * ⛔ ~~判準原本是 `subtotal + shippingFee - discountTotal === total`~~ ——
   *    那個式子**漏掉 DB 等式的第四項**, 而在稅恆為 0 的世界裡它**永遠是對的**
   *    ⇒ 📌 **一個漏項的判準, 在漏掉的那一項恆為 0 時, 與正確的判準【印同一個答案】。**
   *
   * 🔴 **它【必填】** —— 選填的話, 忘了帶的呼叫端會回到舊行為(把稅當 0)
   *    ⇒ **而那正是本片要修的那個 bug, 只是換了發生地點。**
   *    ⇒ ✅ 必填讓每一個呼叫端在 typecheck 那一刻被點名。
   */
  taxTotal: number;
}): boolean {
  return ctx.subtotal + ctx.shippingFee - ctx.discountTotal + ctx.taxTotal === ctx.total;
}

export function formatOrderAmount(n: number): string {
  return Math.trunc(n).toLocaleString('en-US');
}

/** 兩份共用的收尾指路句。 */
export const ORDER_MEMBER_CENTER_SENTENCE = '訂單明細與最新狀態請至 PCM 會員中心查看。';

// ─────────────────────────────────────────────────────────────────────────────
// 取消通知信(`order_unpaid_cancelled` —— **未付款的單被【員工】取消**)
//
// 🔴 **文案的來源:沒有稿,而我【不發明】。** 2026-09-03 掃過真權威,分母與正對照都留下:
//    OD 專案 12 支 · 檔名含 email 4 支 ⇒ 含 cancel **0**(🟢 正對照 含 paid **2**)
//    design-reference `.html` 2 支     ⇒ 提到「取消」**0**(🟢 正對照 提到「購物車」**1**)
//    ⇒ **那兩個 0 是「真的沒有」,不是「我沒找到」。**
//
// ✅ **⇒ 所以「為什麼取消」那一句【不在這裡定義】** —— 它由 `admin_cancel_order` 的七值映射表提供
//    (`20260830020000` 錨 `WHEN 'customer_request' THEN '依您要求取消'`),而那些字面**本來就是寫給客人看的**、
//    今天就在 `orders.cancelled_reason` 這一欄裡。⇒ **信件帶那個欄位,零新造文案。**
// 🛑 **而 `other` 那一格是【沒有審稿的對外字面】** —— 它的內容是員工自己打的,會原封進到信裡。
//    ⇒ 已端 Sean:建議 `other` 改用一句通用文字,員工打的字只留在後台。**在他回答之前,
//      呼叫端有責任決定要不要把 `other` 的原文放進來** —— 本檔只提供固定的那幾句。
//
// 🔵 **只做純文字、不做 HTML** —— 有先例:出貨信(`order_shipped`)今天就是純文字
//    (`buildOrderShippedText`),HTML 那條路只服務付款成功信。**與現況一致,不是新設計。**
// ─────────────────────────────────────────────────────────────────────────────

/**
 * 🔴 **這一句是本片唯一「新寫」的對外字面,而它承載的是一個【事實】不是一個說法**:
 * 這條線的射程是**未付款**的單(Sean 2026-09-03 拍乙)⇒ 客人**從頭到尾沒有付過錢**
 * ⇒ **信裡不可以提退款**(提了會讓他等一筆不存在的退款)。
 * ⚠️ 而它與 `order_cancelled`(刷卡且已全額退款)那條線**互斥** —— 那條線的信要講退款,
 *    而**那封信不歸本檔管**(它的文案沒有稿也沒有拍板,見規格 §10)。
 */
export const ORDER_UNPAID_CANCELLED_NO_CHARGE_SENTENCE =
  '這張訂單尚未付款，不會有任何款項產生。';

/**
 * 🔴🔴 **刷卡且【已全額退款】那條線的錢那一句**(Q10;Sean 2026-09-03 拍甲「補一封信」)。
 *
 * ⚠️ **與 `ORDER_UNPAID_CANCELLED_NO_CHARGE_SENTENCE` 互斥**:那句是「沒付過錢」,
 *    這句是「付過錢而已經退回去」。**兩句同時出現 = 有人接錯 event_type。**
 *
 * 🛑🛑 **這裡【刻意不寫】「X 個工作天到帳」, 而理由要寫下來因為它會被人「順手補上」**:
 *    到帳時間是**發卡行**決定的, 不是我們。寫了就是**一個我們控制不了的承諾**,
 *    而它與「回覆這封信」(那個信箱沒人收)、「請稍後再試」是**同一族** ——
 *    📌 **一句話承諾了一件不由我們決定的事, 而它在紙上讀起來像資訊。**
 *    ✅ ⇒ 只寫**已經發生的事實**:錢退回去了、退到原本那張卡。
 */
export const ORDER_CANCELLED_REFUNDED_SENTENCE =
  '您支付的款項已全額退回原付款方式。';


/**
 * 🔴🔴 **取消信【不可以假設客人知道自己付過款】—— 而那是量出來的,不是體貼。**
 *
 * `sweep-email-outbox.ts` 的付款成功信在寄出前會再查一次訂單,查到 `cancelled` 就**整封不寄**;
 * 而掃描每 5 分鐘一輪 ⇒
 * ```
 * 取消發生在那 5 分鐘【之內】⇒ 他【連付款成功信都沒收到】
 * 取消發生在那 5 分鐘【之後】⇒ 他收到了, 然後沒有下文
 * ```
 * 🎯 **⇒ 有一半的人沒收到過那封信** ⇒ 🛑 **所以開頭不能寫「您先前的訂單付款成功後…」**
 *    —— 對那一半的人, 那句話是**憑空冒出來的**。
 * ✅ ⇒ 下面這兩句就是那個結論的落點:**只講這封信在講哪一張單**, 不回溯他「應該」已經知道的事。
 *
 * ⛔ ~~我原本把這段掛在一個叫 `ORDER_CANCELLED_LEAD_SENTENCE` 的新常數上~~ ——
 *    🔴 **而那個常數【沒有任何人用】**(code-reviewer R1:全 repo 只有定義那一行)
 *    ⇒ 📌 **這份唯一的量測, 掛在一個死掉的常數上 ⇒ 下一個做清理的人拿掉它, 量測就消失了。**
 *    ⇒ ✅ 搬到**真的在用的**這兩個常數旁邊。
 */
/** 取消信的開頭句;`%s` 由呼叫端代入訂單編號(缺編號時走另一句,見 `buildOrderUnpaidCancelledText`)。 */
export const ORDER_CANCELLED_HEADLINE_WITH_ID = (displayId: string): string =>
  `您的訂單 ${displayId} 已取消。`;

/** 缺編號時的退化句 —— 🔴 **不印「undefined」也不印空白**(那會直接寄到客人眼前)。 */
export const ORDER_CANCELLED_HEADLINE_NO_ID = '您的訂單已取消。';

/**
 * 🔴🔴 **把「員工打的那段字」整理成【一行、有上限、沒有控制字元】的純文字。**
 *
 * **為什麼需要它**(codex 2026-09-03 對抗審查,三條 must-fix 同一個根):
 * `cancelled_reason` 的 `other` 那一格是**員工自己打的自由文字**,而它會**原封**進到寄給客人的信裡。
 * ```
 * 換行 / CR / tab            ⇒ 員工可以在信裡【重排段落】, 偽造成像是我們寫的客服指示或連結
 * 雙向與不可見控制字元        ⇒ 同上, 而且看不見
 * 無長度上限                  ⇒ 整包送去 provider, 沒有截斷也沒有可預測的失敗
 * 字面 "undefined" / "null"   ⇒ 它們是合法字串 ⇒ 會直接印在客人眼前(常見於上游 String(undefined))
 * ```
 * 🛑 **這不是 HTML 注入(純文字信沒有執行面),是【內容注入】** —— 而內容注入在一封
 *    「我們寄的」信裡,傷害是**它看起來像我們說的**。
 *
 * ⚠️ **它【不管語意】,明寫**:它擋不住員工打「退款將於三日內完成」這種**內容上錯誤**的句子。
 *    ⇒ 那一格**沒有機制擋得住**,只能靠 ①`other` 改成通用文字(已端 Sean)或 ②人審。
 *    ⇒ 📌 **不要把本函式讀成「信裡不會出現不該出現的話」** —— 它只保證**形狀**。
 */
export const CANCEL_REASON_MAX_LEN = 120;

export function sanitizeCustomerFacingReason(raw: string): string | null {
  // ① 控制字元與換行 ⇒ 空白(含 CR/LF/TAB、雙向控制、零寬、BOM)
  const flattened = raw
    .replace(/[\u0000-\u001F\u007F\u200B-\u200F\u2028\u2029\u202A-\u202E\u2066-\u2069\uFEFF]/g, ' ')
    // ② 連續空白收成一個(否則上面那步會留下一排空格,看起來像刻意排版)
    .replace(/\s+/g, ' ')
    .trim();
  if (flattened === '') return null;
  // ③ 🔴 字面 "undefined" / "null" 一律當沒有 —— 它們是合法字串, 而它們出現在客人眼前
  //    幾乎一定是上游的 bug(String(undefined)), 不是員工真的想說的話。
  if (flattened === 'undefined' || flattened === 'null') return null;
  // ④ 上限:超過就截斷並加省略號 —— **截斷要看得出來**, 靜默截斷會讓人以為那就是全部
  return flattened.length <= CANCEL_REASON_MAX_LEN
    ? flattened
    : `${flattened.slice(0, CANCEL_REASON_MAX_LEN)}…`;
}
