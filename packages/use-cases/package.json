{
  "name": "@pcm/use-cases",
  "version": "0.0.0",
  "private": true,
  "description": "跨 entity 業務流程(殼、待後續 slice 裝 use-cases)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
