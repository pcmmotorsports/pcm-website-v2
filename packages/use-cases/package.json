{
  "name": "@pcm/use-cases",
  "version": "0.0.0",
  "private": true,
  "description": "跨 entity 業務流程(M-1-14e-1b: 會員 register/login/logout/update-profile use-cases)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@pcm/domain": "workspace:*",
    "@pcm/ports": "workspace:*"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
