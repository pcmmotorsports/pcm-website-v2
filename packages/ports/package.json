{
  "name": "@pcm/ports",
  "version": "0.0.0",
  "private": true,
  "description": "抽象介面(殼、M-0-04 slice 裝 I*Repository / I*Adapter 簽名)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./contract": {
      "types": "./src/IProductRepository.contract.ts",
      "default": "./src/IProductRepository.contract.ts"
    }
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pcm/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
