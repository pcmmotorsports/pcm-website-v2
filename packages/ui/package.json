{
  "name": "@pcm/ui",
  "version": "0.0.0",
  "private": true,
  "description": "PCM 共用元件 + design tokens(editorial)— 殼、待後續 slice 裝 Storybook",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
