{
  "name": "@pcm/schemas",
  "version": "0.0.0",
  "private": true,
  "description": "PCM 共用型別 / Zod(前後台合約)— 殼、待後續 slice 加型別",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
