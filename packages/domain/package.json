{
  "name": "@pcm/domain",
  "version": "0.0.0",
  "private": true,
  "description": "9 個 bounded contexts 純邏輯 / framework-free(殼、待後續 slice 裝 entities)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
