# 2026-09-09 減法版(Sean 拍 Q2 甲)。只留守【產品】的閘:錢 / 權限 / schema / 部署。
# 拿掉的 12 支(守板子、帳本、常載大小、審查標記、腳本清單):git show 54bb99e18:.husky/pre-commit
# 每一道接 || exit $? 不接 || exit 1:閘用 0/1/2 三態,1 有問題、2 閘自己壞了。閘檔不見 ⇒ 擋下回 1(fail-closed)。

[ -f .husky/admin-dev-bypass-gate.sh ] || { printf '%s\n' '🔴 .husky/admin-dev-bypass-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/admin-dev-bypass-gate.sh' >&2; exit 1; }
bash .husky/admin-dev-bypass-gate.sh || exit $?

[ -f .husky/migration-ts-gate.sh ] || { printf '%s\n' '🔴 .husky/migration-ts-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/migration-ts-gate.sh' >&2; exit 1; }
sh .husky/migration-ts-gate.sh || exit $?

[ -f .husky/migration-post-commit-gate.sh ] || { printf '%s\n' '🔴 .husky/migration-post-commit-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/migration-post-commit-gate.sh' >&2; exit 1; }
bash .husky/migration-post-commit-gate.sh || exit $?

[ -f .husky/state-gates-freshness-gate.sh ] || { printf '%s\n' '🔴 .husky/state-gates-freshness-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/state-gates-freshness-gate.sh' >&2; exit 1; }
sh .husky/state-gates-freshness-gate.sh || exit $?

[ -f .husky/rls-service-role-policy-gate.sh ] || { printf '%s\n' '🔴 .husky/rls-service-role-policy-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/rls-service-role-policy-gate.sh' >&2; exit 1; }
sh .husky/rls-service-role-policy-gate.sh || exit $?

[ -f .husky/acl-drift-gate.sh ] || { printf '%s\n' '🔴 .husky/acl-drift-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/acl-drift-gate.sh' >&2; exit 1; }
sh .husky/acl-drift-gate.sh || exit $?

[ -f .husky/view-apply-gate.sh ] || { printf '%s\n' '🔴 .husky/view-apply-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/view-apply-gate.sh' >&2; exit 1; }
sh .husky/view-apply-gate.sh || exit $?

[ -f .husky/migration-reset-role-gate.sh ] || { printf '%s\n' '🔴 .husky/migration-reset-role-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/migration-reset-role-gate.sh' >&2; exit 1; }
sh .husky/migration-reset-role-gate.sh || exit $?

[ -f .husky/cron-allowlist-drift-gate.sh ] || { printf '%s\n' '🔴 .husky/cron-allowlist-drift-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/cron-allowlist-drift-gate.sh' >&2; exit 1; }
sh .husky/cron-allowlist-drift-gate.sh || exit $?

[ -f .husky/isolation-level-scan-gate.sh ] || { printf '%s\n' '🔴 .husky/isolation-level-scan-gate.sh 不見了 ⇒ 擋下(不放行);復原:git checkout -- .husky/isolation-level-scan-gate.sh' >&2; exit 1; }
sh .husky/isolation-level-scan-gate.sh || exit $?

pnpm exec lint-staged --no-stash || exit $?
