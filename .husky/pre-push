# 🔴 三段一律 `&&` 串接(關卡2 must-fix):第一版寫成兩行,typecheck 紅了照樣往下跑,
#    而 hook 的離場碼是**最後一行**的 ⇒ gate 綠就把 typecheck 的紅洗掉了 = 我自己拆掉一道既有守門。
# 🔴🔴 **2026-09-04 訂正:⛔ ~~「一律 `&&` 串接」就解決了~~ —— 那句話【不成立】。**
#    後來有人在鏈的下面加了 `if [ -f …always-loaded-size-gate.sh ]` 區塊
#    ⇒ **hook 的離場碼又變成【那個區塊的】** ⇒ 上面那個病原封裝回來, 而註解還寫著已修好。
#    🔬 實錘(拿這支真的檔跑, pnpm 換成必紅的樁):修前 **rc=0**(typecheck 紅而放行);
#       加 `|| exit 1` 後 **rc=1**。突變(把 `|| exit 1` 拿掉)⇒ 回 0 ⇒ 負對照有力。
#    🔴 而 `sh -e` 只救得了【最後一段】(R1 must-fix 訂正 —— 我第一版把射程講寬了):
#       husky9 的 runner `.husky/_/h` 末行是 `sh -e "$s"`, errexit 是開的, 而 POSIX 的規則是
#       **AND-OR 串列裡【非末段】的失敗被豁免, 末段的失敗照樣觸發**。當場量兩個世界:
#         `sh -e -c 'false && echo X; …'`  ⇒ rc=0  (false 在前段 ⇒ 豁免 ⇒ 後面照跑)
#         `sh -e -c 'true && false; echo X'` ⇒ rc=1、X 沒印 (false 在末段 ⇒ 觸發)
#    ⛔ ~~「AND-OR 串列結構上不觸發 errexit」~~ 作廢 —— 它只對**前段**成立。
#    🎯 ⇒ 這條鏈的 fail-open 涵蓋的是**第 1-4 支**(typecheck / lint / deploy-order-gate /
#       ledger-divergence);**第 5 支 `selftest-git-isolation-gate.sh` 紅時本來就擋得住。**
#       而修法照樣要 —— 前四支才是每次都在跑的那幾支。
#    🔴 用 `|| exit $?` 不是 `|| exit 1`:鏈裡 `migration-ledger-divergence.sh` 用 `exit 2/9`、
#       `selftest-git-isolation-gate.sh` 用 `exit 2`(本閘自己壞了)⇒ 壓成 1 會讓三態消失。
#    ⇒ 守著這件事的是 `scripts/husky-hook-wiring-check.sh` 的 `check_prepush`
#       (它掛在 lint-staged 的 `.husky/pre-*` 上, **每次改本檔都會跑**);
#       `deploy-order-gate-verify.sh` 格㊺ 是行為層的第二道, **而那支 harness 沒有接線、要手跑。**
# 🔴 **改本檔請用編輯器,不要 `mv` 或 `cp` 覆蓋**:2026-08-11 我用 `mv /tmp/… .husky/pre-push`
#    把 mode 從 **100755 洗成 100644**(`git diff` 的 `old mode/new mode` 才看得出來)。
#    husky 的 shim 目前仍會執行它,但那是 husky 的實作細節、不是 git 的保證 ——
#    hook 檔一旦不可執行,失效方式是**完全靜默**。改完請 `ls -l` 確認仍是 `-rwxr-xr-x`。
#    (harness `scripts/deploy-order-gate-verify.sh` 的格⑱ 會驗它,但那是**事後**才紅。)
# 🔴 部署時序 gate(Q26=A;plan docs/specs/2026-08-11-deploy-order-gate-plan.md v3):
#    git 把 `<local-ref> <local-sha> <remote-ref> <remote-sha>` 由 stdin 餵進來,
#    而 pnpm 那兩支不吃 stdin ⇒ 直接把 stdin 交給 gate(它自己逐行處理、也自己處理全 0 的 ref)。
# 🔴 `TURBO_FORCE=1` 兩支各加一次(#621;Sean 2026-08-17 拍甲「修,每次推多等 20-30 秒」):
#    少了它,turbo 命中既有快取時會 **replay 上一次的綠**。當場兩個世界實測 ——
#      pnpm typecheck               ⇒ `Time: 117ms >>> FULL TURBO`   (重播,什麼都沒跑)
#      TURBO_FORCE=1 pnpm typecheck ⇒ `Cached: 0 cached, 8 total`    (真跑)
#    ⇒ 沒有前綴時,這道閘的前兩項會在 117 毫秒內「通過」。正本 `docs/phase-1-backlog.md` `#524`/`#621`。
#    ⚠️ 第三段 `deploy-order-gate.sh` 不吃 turbo 快取、本來就真跑 ⇒ 不需要也不能加這個前綴。
# 🔴 第四段 migration 帳本分岔 gate(2026-08-21 W5;backlog #795/#800):
#    supabase db push 讀的是【平台帳本】supabase_migrations.schema_migrations,
#    而 SQL Editor 貼上去的那條路**完全不寫它** ⇒ 已套過的在它眼裡是沒套過 ⇒ 重跑。
#    2026-08-21 實測:13 支處於這個狀態,其中含不冪等的 20260820040000(重跑會 ERROR 42701)。
# 🔴 **兩支 gate 都吃 stdin,而 stdin 只能讀一次** ⇒ 先落成暫存檔再各餵一次。
#    直接串接會讓第二支拿到空的 stdin ⇒ 它會印「未檢查任何 ref」而**放行**,
#    也就是我會用新增一道閘的動作,把既有那道關掉。
# 🔴 **形狀不能改**(2026-08-21 codex R1 must-fix,實測抓到):既有 harness
#    `scripts/deploy-order-gate-verify.sh` 的格⑱ grep 的是「非註解行」裡的
#    `TURBO_FORCE=1 pnpm typecheck &&` / `TURBO_FORCE=1 pnpm lint &&` / `&& bash` 三串字面。
#    我第一版把它拆成 `lint || { …; exit 1; }` 換行再 `bash …` ⇒ **當場把那一格弄紅了**
#    (實跑:PASS=34 FAIL=2,其中一條就是⑱)⇒ 這裡改回單一 `&&` 鏈,語意相同、字面也相容。
# 🔴 `cat` 失敗要當場停:空的暫存檔會讓兩支 gate 都印「未檢查任何 ref」然後**放行** ——
#    也就是用「多加一道閘」的動作把既有那道一起關掉。
_R="$(git rev-parse --show-toplevel)"
_T="$(mktemp)" || exit 1
# 🔴 INT/TERM 的 handler 要自己 `exit`(2026-08-21 codex R2 must-fix):
#    只清暫存檔而不離場的話,收到訊號後**執行流會繼續往下跑**,後面全綠就 rc=0
#    ⇒ 被中斷的推變成「檢查過而通過」。EXIT 那條只負責清檔,不改離場碼。
trap 'rm -f "$_T"' EXIT
trap 'rm -f "$_T"; exit 1' INT TERM
cat > "$_T" || { echo '🔴 pre-push:讀不進 git 餵的 ref(stdin)⇒ 擋下,不放行' >&2; exit 1; }
# 🔴 第五段 selftest git 隔離 gate(2026-08-25;線 3 交件, 主視窗裁定掛這裡不掛 lint-staged):
#    今晚兩次事故都是「自帶拋棄式世界的 --selftest 在真 repo 上動手」——
#      board-state-consistency.py 清空整個 index(3,234 ⇒ 3)
#      view-apply-before-wire-gate.py 在真 repo 建 4 顆 commit 並把 dev 指過去
#    🔴 為什麼不掛 lint-staged:它慢到會讓人開始 --no-verify, 而那比沒有這道閘更糟。
#      而它守的是【repo 的一個狀態】不是每顆 commit 都會變的東西 ⇒ 用 push 的頻率問它才對。
#      ⚠️ **量級在這裡, 而每個數字都綁著它的世界**(§6-b:沒帶範圍的數字不進表):
#        · 2026-08-25 早先:`--selftest` **37 秒** / 全掃 **171 秒**(當時的證人格數 **未確認**)
#        · 同日線 3 加到 **12 格**後:`--selftest` **31-34 秒** / 全掃 **107-116 秒**
#      🔴 **這兩組不可直接相比** —— 機器負載不同(當晚八個窗共用一台), 格數也不同。
#      🔴 **不要把過期的數字直接刪掉** —— 「不掛 lint-staged」這個決定的**依據就是那個秒數**,
#         刪掉數字 ⇒ 有人會以為這決定沒依據而去推翻它。**過期的數字要加註時點, 不是拿掉。**
#      ⇒ 要現值當場跑(在 repo 根):
#         `time bash scripts/selftest-git-isolation-gate.sh --selftest`
#         `time bash scripts/selftest-git-isolation-gate.sh`
#      📌 同族要知道的:這道閘掛在一條 `&&` 鏈的**末端** ⇒ 前面任一段紅它根本不會跑
#         ⇒「pre-push 有這道閘」與「每次 push 都驗過隔離」是**兩個宣稱**。而前面紅時
#         畫面上是「push 被擋了」= 看起來就像守門在工作。**鏈式守門的漏跑長得像真紅。**
#    ⚠️ 它對【債表 KNOWN 上的】只印不擋, 對【表外新出現的】才 exit 1(線 3 拋棄式 repo 真 push 兩方向量過)。
#      **這裡刻意不寫支數** —— 債表會被清空也會被加回(2026-08-25 就清空過一次)。
#      要現值 —— **兩步, 不要只跑第二步**:
#        ① `grep -c '^KNOWN="' scripts/selftest-git-isolation-gate.sh`  ⇒ **必須是 1**
#        ② `sed -n '/^KNOWN="/,/^"/p' scripts/selftest-git-isolation-gate.sh`
#      🔴 **少了①, 「錨不見了(查法壞掉)」與「債表是空的」印的是同一個輸出。**
#      📌 一般化:一條「抽某個區段」的查法, 要先驗那個區段的**錨存在** ——
#         否則它在兩個世界印同一句話, 而其中一個世界是它自己壞了。
#    ⚠️ 它不吃 stdin, 而上面兩支已經把 $_T 用掉了 ⇒ 導 /dev/null 只是為了不讓它意外繼承。
# 🔴🔴 掃描型測試(`scripts/run-migration-scan-tests.sh`)**刻意【哪一道 hook 都不掛】**
#    —— 2026-09-04 主視窗裁定。它守的病是真的:那一族測試自己去讀 `supabase/migrations/`,
#    **不 import 被測的碼** ⇒ `vitest related` 的分母裡【結構上】沒有它們。
#    而**掛在哪裡**這件事被否決了兩次, 兩次的理由不同, 兩次都值得留著:
#    ① ⛔ ~~掛 lint-staged `supabase/migrations/*.sql`~~ —— **實測會遞迴**:
#       `scripts/migration-new-file-gate.test.ts` 會【造一個暫存 repo 跑真的 lint-staged】
#       ⇒ 觸發那支腳本 ⇒ 它跑那 71 支測試(含它自己)⇒ 再造暫存 repo ⇒ 沒有底。
#       當場量:全機 **148 支 vitest**、load 24→29。📌 **每一層都在做對的事, 只是層數沒有底。**
#       🎯 判別句:**那條 pipeline 的測試, 會不會【真的去跑那條 pipeline】?**
#    ② ⛔ ~~改掛本檔(pre-push)~~ —— 兩個理由:
#       · 只有主視窗 push, 而收割鏈在 push 前已跑全套 ×2 ⇒ 掃描型是它的**子集** ⇒ 純重工
#       · 真負載下**超過 100 秒**(隔離環境量到的 21 秒在 load 100+ 之下不成立)
#         ⇒ **閘死於誤報與死於太慢一樣常見, 而被關掉的閘等於沒有。**
#    ⇒ 現行做法 = **人的規矩**, 不是機制:動 migration 交件前自己跑一發。
#      正本在 `docs/patterns/slice-checkpoint.md` 鐵則 11 那節。
#    🛑 **而「規矩不如機制」這件事我們記過** ⇒ 這是一筆**明知較弱而選了它**的取捨, 不是疏漏。
# 🔴 裸 SQL 部署時序 gate(第 5 支;主視窗 -f8 2026-09-06 裁【乙】,不推翻 -48 的明令)
#    `deploy-order-gate.sh` 的偵測窗口錨在 `.rpc(` 那一行 + 後兩行
#    ⇒ 一支用 `client.query('SELECT public.X()')` 呼叫新 RPC 的碼,它**一個字都看不到**
#      (板列 `⟦b9-RPCBLIND1⟧`:那種形狀引到的函式 52 支,對那道閘隱形的 40 支,
#       出貨碼真的有呼叫點的 25 支 —— **幾乎全在 `packages/adapters/src/payment/`**)。
#    🛑 **-48 的明令沒有被繞過**:那句話擋的是【改 deploy-order-gate.sh】,理由是它的
#       爆炸半徑 = 所有人的 push。本支是**另一支檔、另一道閘**,那道閘一行都沒動。
#    🔵 **射程刻意與它對齊**(不是抄,是為了讓收到紅的人不用學第二種讀法):
#       只看 `refs/heads/dev` / `refs/heads/main`、只看**這次要推的範圍**改到的出貨檔、
#       判準讀**要推的那顆的樹**、git 失敗一律 fail-closed。
#    ⚠️ 它與 deploy-order-gate **不重疊也不取代** —— 那道看 `.rpc(`,本支看裸 SQL
#       ⇒ **兩支都跑過才等於兩種形狀都看過。**
TURBO_FORCE=1 pnpm typecheck && TURBO_FORCE=1 pnpm lint && bash "$_R/scripts/deploy-order-gate.sh" < "$_T" && python3 "$_R/scripts/rpc-raw-sql-callers.py" --gate < "$_T" && sh "$_R/scripts/migration-ledger-divergence.sh" --if-pushing-main < "$_T" && bash "$_R/scripts/selftest-git-isolation-gate.sh" < /dev/null || exit $?

# 🔴 常載檔字元數閘 —— 這裡是【第二道】, 而它擋的是 pre-commit 摸不到的路徑
#    (2026-09-02 codex R3 指出):`.husky/always-loaded-size-gate.sh` 只接 pre-commit,
#    而**乾淨 merge / 一般 rebase / cherry-pick / revert 不經過 pre-commit**
#    ⇒ 一個超標的 CLAUDE.md 可以從【一般 Git 路徑】進來, 不需要 --no-verify。
#    📌 ⇒ 「有一道閘」與「每一條進來的路都經過它」是兩個宣稱。
#    ⚠️ 而這一道量的是【要推的那顆 commit 上的 CLAUDE.md】, 不是工作樹也不是 index。
if [ -f "$_R/.husky/always-loaded-size-gate.sh" ]; then
  sh "$_R/.husky/always-loaded-size-gate.sh" --at-head || exit $?
else
  printf '%s\n' '🔴 .husky/always-loaded-size-gate.sh 不見了 ⇒ 擋下(不放行)' >&2
  exit 2
fi
