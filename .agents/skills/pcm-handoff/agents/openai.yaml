interface:
  display_name: "PCM Handoff"
  short_description: "讓 Codex 與 Claude 以共同真相安全交接"
  default_prompt: "Use $pcm-handoff to verify repo state and determine the task mode: report handoff content without writing in read-only mode; update handoff files only within the authorized scope in execution mode."
